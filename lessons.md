2026-04-18
- `run-wasm-smokes` 这种正式命令不要再做 `backend_driver <-> stage3` 双向转发。现场已经坐实：两边都以为“对方更适合执行”时，live 入口会直接递归自繁殖；最稳口径是 live `backend_driver` 本地实现，seed/stage0 只做 passthrough。
- `runtime_zero_c_surface_smoke` 这类会自己再拉 browser/wasm smoke 的验收，label 不能写死成一组常量。backend/stage0 并跑时必须按当前可执行目录或调用面派生唯一 label，不然会互踩同一组 `artifacts/v3_browser_host_wasm` / `artifacts/v3_wasm_smokes` 输出路径。
- browser/wasm 零脚本主线已经切干净后，历史 `artifacts/*.js` helper 残留也要物理删除。否则 `find *.js` 还会把旧时代跑出来的 helper 误判成当前主线仍依赖脚本。
- `Fmt"..."` 这种语法级模板不要再降成链式 `str + str + ...`。这轮已经现场坐实：最稳口径是编译期先拆成静态片段和占位表达式，多段时统一改写成单次 `Fmt([piece...])` 调用，让 native/wasm 都走一次总长预计算和一次分配；同时要补一条像 [strformat_fmt_lowering_smoke.cheng](/Users/lbcheng/cheng-lang/v3/src/tests/strformat_fmt_lowering_smoke.cheng) 这样的符号级 smoke，硬卡 `primary.o` 只能引用 `_cheng_v3_strformat_fmt_bridge`，不能回退到 `_driver_c_str_concat_bridge`。
- 手写字符串链 `"a" + x + "b" + y` 不能再和 `Fmt"..."` 走两套 lowering。最稳口径就是两边统一改写成 `Fmt([piece...])`，把 native/wasm 的分配形状收成一次总长计算、一次分配，并用 [str_concat_lowering_smoke.cheng](/Users/lbcheng/cheng-lang/v3/src/tests/str_concat_lowering_smoke.cheng) 这种符号级 smoke 硬卡不得再引用 `_driver_c_str_concat_bridge`。
- `std/strformat` 和 `std/strutils` 这层显式拼接接口也不能再各走各的。像这轮已经现场坐实：`sf.Fmt(parts)`、`sf.Lines(parts)`、`strutil.Join(parts, "")`、`strutil.Join(parts, "\n")` 都要在编译期先统一收成 bare `Fmt/Lines`，这样 native/wasm 才只剩同一条桥接和同一套分配形状。
- `cheng_node` 这类统一宿主只要要把轻节点 proof/query 真持久化，就不能只写链快照。像这轮 [cheng_node_state.cheng](/Users/lbcheng/cheng-lang/v3/src/project/cheng_node_state.cheng) 已经现场坐实：正式状态最少要同时写 `chain snapshot + object store + ledger event log`，加载时再按事件日志重放统一账本，不然 `manifest/proof/snapshot segment` 会全漂。
- ordinary 里别再把 `bytesFromHex(...)` 直接塞进 `let` 或 `return` 里的复合表达式。像这轮 `cheng_node_state` 已现场坐实：`bytesToString(bytesFromHex(raw))` 这种形状会直接卡在 `primary_object` lowering；最稳口径是本地手写最小 hex 解码，再拆成显式 `Bytes -> str` 中间变量。
- `cheng_node` 的 DID/operator 验收不要再借 `bio_did_chain_node` 高层 helper 证明自己没问题。最稳口径是像这轮 [cheng_node_chain_did_smoke.cheng](/Users/lbcheng/cheng-lang/v3/src/tests/cheng_node_chain_did_smoke.cheng) 这样直接按链事件原语造 chunk：`assetId/toAccount/amount=ordinal/fromAccount=seq/fromParentCid=chunk`，这样验证到的就是节点真正消费的链事实。
- 表达式还没做完整类型检查前，不要伪造一份“看起来已经全类型化”的 lowering 标签。像这轮 `if/try/result projection`，正式口径必须显式落成 `polymorphic/deferred`，先把未知保真写出来，后面再补真 typecheck。
- 只要表达式要跨 `parser -> compiler_csg -> lowering_plan` 传递，第一刀就先补稳定 `exprId + source span`，不要继续靠 `lineNumber/kind/detail` 临时猜同一节点。没有这层稳定主键，后面任何 typed metadata 都会继续漂。
- ordinary 控制面里，帮助文本或状态文本别再写成长串直接 `return "a" + "b" + "c"`。这轮 `cheng_node ctl` 已现场坐实：这种形状会把返回边界踩坏，`lr` 直接跳进 `__cstring` 常量区；最稳收法是像这轮这样用 `var out` 逐段 `out = out + ...`，尤其是 usage/help/report 这类高频字符串拼装函数。
- ordinary 新模块里，只要初始化阶段要造小型 `seq` backing store，第一刀不要直接赌 `reserve/setLen`。这轮 [cheng_node_map.cheng](/Users/lbcheng/cheng-lang/v3/src/project/cheng_node_map.cheng) 已经现场坐实：`V3ChengNodeStrIntMapInitInPlace(...)` 直接调这两个 helper 会卡在 `primary_object` lowering；最稳口径是先按槽位 `add` 出固定容量，等编译器把 helper 调用边界补稳后再回收。
- `program_runtime.combined` 这类历史 runtime link input 从主线退场后，别只看一次 [build_backend_driver_v3.log](/Users/lbcheng/cheng-lang/artifacts/v3_backend_driver/build_backend_driver_v3.log) 就算完成。最稳口径是补 host smoke，真实再编一条 ordinary fixture，并硬卡 report 不得出现 `program_runtime.combined` 或 `system_helpers_compat_bridge`；否则 seed/default smoke 列表一漂，旧 runtime input 很容易悄悄挂回来。
- `std/crypto/sha256` 这类需要严格 32 位环绕语义的实现，别在没补齐运算语义验证前直接从 `int64+显式 u32 截断` 改写成裸 `uint32` 版。像这轮 `sha256_runtime_smoke/fixed256_sha256_smoke` 已经现场坐实：源码看起来更“直”，但当前运行时的 `uint32` 算术/位运算链并没有等价守住，结果直接把标准向量算坏；最稳口径是先保留已通过 smoke 的 `int64 + u32(...)` 实现，再单独补一个逐轮中间态对拍后再谈收窄类型。
- formal spec 已经把跨模块可见性定成“首字母大写才导出”，`std/strformat` 不能再留 `strfmt.fmt / strfmt.lines` 这种小写兼容层；如果用户要语法级模板，就直接把 `Fmt / Lines` 下沉成裸 special form，公开文档和仓内示例统一写 `Fmt"..."`、`Fmt(parts)`、`Lines(parts)`。
- `chain_node` 这类长程账本 gate 一旦只在单个余额断言上炸，第一刀先逐轮重算账，不要先怀疑共识或 sync。这轮 `run-chain-node-resilience-smoke` 的第五轮里，`2002` 在 round4 余额 `60` 的基础上先收 `transfer +8` 再收 `mint +20`，真值是 `88`，不是手写的 `80`。
- `backend_driver` 新正式命令只补主实现不算完成。只要要让 live `backend_driver`、seed `stage0` passthrough、帮助文本和 gate 一起稳定，`compiler_runtime`、`compiler_main`、`stage1_bootstrap`、`gate_main` 和 `cheng_v3_seed.c` 的 `help/supported_commands/passthrough/dispatch` 就必须同一轮追平。
- `r2c-react-v3` 再遇到 `runtime_state_missing`，第一刀不要继续加等待时间，先把页面 failure context 落出来看 root/console/exception。这轮已经现场坐实：`message_thread/group_draft/node_thread` 真根不是 helper 慢，而是对照机自己在 [ChatPage.tsx](/Users/lbcheng/UniMaker/React.js/app/components/ChatPage.tsx) 先抛了 `Cannot access 'isInviteRoomOpen' before initialization`。
- React/TSX 里只要某个 `useEffect/useMemo` 在 render 时引用了后面才定义的 `const useCallback/useMemo`，那就是标准 TDZ 崩溃，不是什么偶发现象。最稳修法就是像这轮一样机械前移定义位置，不改语义、不加兜底。
- full truth 一旦已经全量重抓出来，就别再只补几条红 route。像这轮 43 条 live truth 已经把 `ecom_main/marketplace_main/home_bazi_overlay_open/trading_main/publish_*` 全部坐实；最稳口径是整批锁进共享 authoritative counts，再重跑 full matrix，而不是继续让 `exec-route-matrix` 穷举 candidate。
- 用户已经明确担心内存爆仓，所以全量 truth capture 默认走单 route 顺序抓取，不并发开多 Chrome。像这轮顺序抓 43 route 虽然慢一点，但把机器压力压住了，也没有再复现之前那种大内存冲击。
- `backend_driver` 专属正式命令别再先试一层 `gate_entry_main -> gate_main`。这轮 `run-chain-node-resilience-smoke` 已经现场坐实：整包拖 [gate_main.cheng](/Users/lbcheng/cheng-lang/v3/src/tooling/gate_main.cheng) 进 ordinary 主体，会把 `build-backend-driver` 直接拖回 `primary_object_unsupported_function_count`；最稳收法是直接落在 [backend_driver_main.cheng](/Users/lbcheng/cheng-lang/v3/src/tooling/backend_driver_main.cheng) 本地实现，再让 seed/stage0 走 passthrough。
- `host_ops` 这种 helper 只要第一次被 live 命令面真走到，就别假设“之前能编过所以这里也稳”。这轮已经现场坐实：短路 `FileExists`、内联 `MonoTimeNs(...)` 差值、以及把日志路径/输出拼在一个大表达式里，都会在 ordinary `build-backend-driver` 里重新炸红；正确收法是先拆显式局部变量，再分支写清楚。
- `r2c-react-v3` 这类 route truth 只要用户要“继续主线/继续 latest”，就别直接沿用上轮 matrix 里的旧计数。像这轮已经现场坐实：`trading_crosshair/game_mahjong/game_doudizhu` 的 live truth 分别漂到了 `118/148/207`；正式口径是先重抓能直达的 route，再更新共享 candidate。
- 共享 route catalog 里，只要某条 route 已经有 authoritative truth-locked candidate，`entry_surface` 和 `equivalent_entry_surface` 都不能再先命中。像这轮 `home_default/home_search_open/.../game_xiangqi`，如果还让 entry/equivalent 抢在前面，旧 `base_snapshot=94` 会直接把最新真值 `83` 覆盖掉。

2026-04-17
- 编译器根修一旦坐实，就把调用面的临时止血口撤掉，并把当时精确命中的 crash 夹具升成正式 smoke。像这轮 `chain_node` CLI，`V3ChainNodeCliExit(...)` 这种绕过返回边界的口子不该长期留着；最稳口径是删掉它，再让 `chain_node_snapshot_*_return_boundary_smoke` 和 `chain_node_cli_return_boundary_smoke` 直接守回归。
- ordinary 的隐式默认初始化别在整块清零之后还继续无条件递归下钻 record/tuple 字段。像这轮 `V3ChainNodeServeSnapshot` 返回边界炸点，真根不是“大局部”本身，而是 `v3_emit_implicit_default_into_address(...)` 对纯零默认的嵌套类型又做了一遍巨量 field walk；正式口径是先判定这个类型树里是否真的存在显式字段默认值或非零默认语义，没有就停在整块 zero-init。
- 用户提到“首字母大写导出”时，先核对当前实现是不是已经真按 formal spec 收严，再判断“没公开”到底是“模块里没有这个顶层符号”还是“可见性挡住了”。像这轮 `std/os.listDir`，真问题是之前 [os.cheng](/Users/lbcheng/cheng-lang/src/std/os.cheng) 里根本没有这个 API，不是大小写把它拦在模块外。
- `chain_node` / `libp2p` 这类已经验证通过的低层链路，不要再额外包一个 `syncOnce` 风格 convenience wrapper，把 `query/entries/snapshot` 这几类大 composite 局部堆进同一个 ordinary 函数里。像这轮 `v3ChainNodeLibp2pSyncOnce*`，函数体内步骤全跑完了，真正炸点却落在函数返回边界；最稳口径就是直接在调用面走 `SyncRequest -> DecodeLatestSnapshotFill -> v3ChainNodeSyncOnce` 这条已经证实稳定的原语链，或者继续拆成更小的单职责函数，别把它们重新捏回一个大 wrapper。
- `DiLoCo` 这类 `composite[]` 状态别再走 `add(state.deltas, out)` 直接整体追加。像这轮 `v3DiLoCoSubmitOuterDeltaFill(...)`，最稳口径就是先追加零值槽位，再按字段直写到 `state.deltas[pos]`，不然 `status/duplicate/QUIC twoproc` 都可能漂。
- `verify-diloco` 这类正式 host gate 不能只看 seed 临时编译器。像这轮就已经坐实：live `cheng.stage3` 还会额外吃到 [program_support_host_runtime_v3.cheng](/Users/lbcheng/cheng-lang/v3/src/runtime/program_support_host_runtime_v3.cheng) 里的最小 shell/pipe helper 路径；如果 host provider 少这层，`diloco_state_smoke.host` 会先在 `cheng_host_pipe_spawn_runtime` unresolved 上炸掉。
- `UE` 孤儿如果采样已经卡在 `dyld_start`，就不要再继续怀疑业务命令本身；先回到 macOS 的启动链，优先把 `fork/exec` 热路径改成 `posix_spawn` 或复用稳定的 `popen/system` 路径。
- seed C 和 selfhost Cheng 的执行面要一起收。只修 [cheng_v3_seed.c](/Users/lbcheng/cheng-lang/v3/bootstrap/cheng_v3_seed.c) 不够，像这轮 [program_support_backend_v3.cheng](/Users/lbcheng/cheng-lang/v3/src/runtime/program_support_backend_v3.cheng) 的 direct capture 如果还保留旧 `fork/execvp`，live 主线后面还会继续造同类问题。
- `quoteShell` 只包一层单引号不算完成，只要参数里有 `'` 就会坏。后面凡是再碰命令拼接，默认先检查 [compiler_main.cheng](/Users/lbcheng/cheng-lang/v3/src/tooling/compiler_main.cheng)、[os.cheng](/Users/lbcheng/cheng-lang/src/std/os.cheng) 和 runtime 内部 helper 是不是都用了真转义。
- 这类 orphan 问题验收时，不只看命令 `exit 0`；至少要在完整复验后再跑一次 `ps -Ao pid=,ppid=,state=,etime=,command= | awk '$2==1 && $3 ~ /^U/'`，确认没有新增 Cheng `UE`，再下“已止血”的结论。
- 用户已明确：模板插值如果目标是接近 Nim 的使用感，也不能破坏 formal spec 的导出规则；最终调用面直接收成裸 `Fmt"…{expr}…"` / `Fmt(parts)` / `Lines(parts)`，不要再保留 `strfmt.` 前缀依赖，更不要退回 Python 式 `format(template, args)`。
- 用户已明确：`std/strformat.fmt(parts)` 只是 `join`，不能拿来冒充 Nim/Python 风格格式化；如果目标是模板插值，就要补真正的模板接口语义，而不是只把 `out = out + ...` 改成 `fmt([...])`。
- 用户已明确：字段默认值只在“偏离隐式默认值”时才写；`bool=false`、整数=`0`、`str=""` 这些和隐式默认值一致的字段，一律不要再写成 `= false` / `= 0` / `= ""`。
- 用户已明确：`T[]/T[N]` 字段默认空值走隐式初始化，文档和业务类型定义不要再写 `tags: str[] = []` 这种重复默认值；统一写成 `tags: str[]`。
- `r2c-react-v3` 这种全量 truth capture 绝不能再抢固定 CDP 端口。像这轮 full matrix，旧 `9223` 只要被残留 headless Chrome 占住，第一条 route 就会假报 `Address already in use (48)`；最稳口径是 `--remote-debugging-port=0`，再从 `DevToolsActivePort` 回读真实端口，并在 helper 退出时做确定性的进程回收。
- `exec-route-matrix` 只要 truth `semantic_nodes_count` 根本不在候选集合里，就不准再把所有 candidate 白编一遍。像这轮 `game_minecraft/game_mahjong/group_draft`，继续枚举只会浪费很多分钟；最稳口径是直接 fail-fast 标 `truth_count_miss_*`，把问题明确留给 route candidate 生成层。
- `bootstrap-bridge` 里 `stage0` 不能再原地物料化最终 `cheng.stage0`。正式口径已经坐实：先落 `cheng.stage0.next`，`stage0 self-check` 和 `stage1 compile` 只跑候选；如果 live bootstrap compiler 就是最终 `cheng.stage0`，成功后直接丢弃候选，绝不把正式 `cheng.stage0` 安装成指向自己的软链。
- 只修 seed 或只修 Cheng 侧 bootstrap 都不够。`stage0.next` 候选执行和 `compiler==stage0` 的复用规则必须在 [cheng_v3_seed.c](/Users/lbcheng/cheng-lang/v3/bootstrap/cheng_v3_seed.c) 和 [compiler_main.cheng](/Users/lbcheng/cheng-lang/v3/src/tooling/compiler_main.cheng) 两边同时追平，不然 live `stage3/backend_driver` 很快又会和 seed 分叉。
- 大 report 文件不要在命令还没退时就下结论。像这轮 `/tmp/compiler_main_probe.report.txt`，半写状态会先露出 `primary_object_machine_words_missing` 这种 stub 残片；至少等 `system-link-exec` 真退出，再判断是不是 regression。
- 这台桌面环境里如果 `ps` 还留着 `PPID=1` 的 `UE` 条目，就不要把 orphan 问题写成“解决了”。这轮 `artifacts/v3_bootstrap/cheng.stage0 self-check` 前台虽然 `exit 0`，但 3 条 `UE` 仍然留在系统里且 `kill -9` 清不掉；主线 gate 成功和 orphan cleanup 必须分开记账。
- 当前 focused truth 模板已经可以直接拿去扫更多正式业务页，不用总从简单 tab 面试探。`node_thread/node_published_content/trading_main` 这轮都已经直接收绿；下一步更值的是系统扫剩余 route matrix，而不是继续手工挑最简单页面。
- `cheng.stage3 r2c-react-v3 compare-truth` 这种正式入口别只看 route compare 本身，先想清楚它会不会 freshness 重编 controller。只要 controller/provider 里还有 ordinary 不稳形状，正式入口就会在“业务 route 已经绿了”的情况下仍然炸掉。
- 当前 ordinary lowering 里，`&&` 复合条件、循环体内层层嵌套 `if`、以及 `uint8(int32(':'))` 这种字符转换比较都别再往 live controller/provider 里放。最稳口径是拆成局部变量、逐层 `if/while`，并直接用字节值 `58/61/47/92`。
- `node_detail`、`marketplace_main` 这种普通业务页现在也能直接按 focused truth 模板推进，不用每次都先从 tab 面试探。先抓 truth，再走 `tsx_ast -> codegen_surface -> route_runtime_main -> compare-truth`，如果红，再看是不是 route 语义层还是正式入口 freshness 层。
- `r2c-react-v3` 开跑前先验真值机是不是活的，不要默认 `127.0.0.1:4173` 就对。只要 `curl '?r2c_truth=1&r2c_route=...'` 不是返回主 `index.html`，先查是不是错跑成了 `vite preview` 且 `dist` 里根本没有主站入口；这种情况下所有 truth runtime 都只会假报 `runtime_state_missing`。
- 非首页 route 如果 compare 还是落到 `home_default`，第一刀先查 base snapshot seed，不要先修语义树。single-snapshot truth trace 的正式口径是“直接取该 route 自己做 entry/base snapshot”，只有多 snapshot trace 才回退优先 `home_default`。
- focused truth trace 不能要求 route catalog 全量 truth snapshot。只要当前输入是单 route truth trace，codegen-surface 就只该 truth-seed 命中 route，其他 route 保持原 catalog 推断，不准因为无关路由缺 truth snapshot 把主线挡死。
- `r2c-react-v3 compile` 这条 Cheng controller 主线里，`--truth-trace` 必须继续传给 codegen-surface；不然 out-dir 里的 `route_runtime_data.cheng` 会偷偷写回 `home_default`，后面 route compare 红的都是假问题。
- `cheng_v3_seed.c` 里递归 helper 不能只避开 `PATH_MAX` 级别数组，`tuple` 这种分支里的 `items[CHENG_V3_MAX_TYPE_FIELDS][4096]` 和整块 `V3TypeFieldDef` 也一样不能继续压栈。像这轮 `v3_compute_type_layout_impl(...) / v3_resolve_field_meta_impl(...)`，`CHENG_V3_MAX_TYPE_FIELDS=256` 时单层就接近 1MB，编 `compiler_main` 会先死在 `___chkstk_darwin`；这类 scratch 必须堆分配或做外置复用。
- `content_detail` 这条 `r2c-react-v3` 真值线如果已经缩到“只剩图片面板和根节点缺失”，别再回去拍 compare 规则。像这轮这样，先把静态值流补齐到可选链、`string.length`、一元负号、默认参数、`if/for...of/expr stmt` 和常见数组/字符串 helper，再补纯 helper 语义，才是最短真修法。
- `buildTruthContent` 这种 truth fixture 入口不能只把对象塞进 env 就算完。刚才这轮已经前台踩实：如果 `resolvePreviewMediaSource/collectImageDetailMediaRefs/resolveImageDetailSource` 这串纯 helper 还走 unknown，`ContentDetailPage` 里的 `images/displayImages` 还是会塌成 `[]`，最后图片区整块丢掉。
- source layout surface 的合成 mount root 规则不能写成“只给顶层 component_ref 补一层空 `div`”。这轮已经坐实：这样虽然能救 `content_detail`，却会把 `home_default` 打回少 1 个节点；正式口径是只要平铺结果里不存在 depth=0 的空 `div` 根，就统一补一个 synthetic root。
- 用户已经明确：`v3/codex` 只能保留一份源码，修 import 不能再靠 `v3/src/codex` 复制第二份实现，也不能靠 `v3/codex/src/std`、`v3/codex/v3` 这类 symlink 假共享。正式口径是把 `compiler_request/parser/cheng_v3_seed.c` 的包根判定和 shared `v3/std` 映射一次修正。
- `home_default` 这条线如果已经做到 `shared=83 / truth_only=0 / exec_only=16`，最后一刀别再回去硬补状态解释器。像这轮这样，把 `repeat` 里的 unknown branch 子树收成“按源码位点只保留一个 witness”，再把 `VirtualizedMasonry/HeightReporter` 的纯实现 wrapper 透传掉，就能直接把 compare 收到 `83/83/0/0`，而且不会引入新的 `truth_only`。
- `r2c-react-v3` 的静态值流别只认 `export { ... } from`，还得认 `export * from`。像这轮 `App.tsx -> ./domain/update`，barrel re-export 一旦不通，`getUpdateSnapshot/compareVersionVector` 整条都会掉成 unknown，`renderUpdateBanner()` 就会被过展开。
- `home_default` 这条 compare 一旦已经做到 `shared=83 / truth_only=0`，后面优先补的是“值流精化”，不是再砍 branch 大树。刚才这轮我已经前台踩实：直接把 unknown branch 子树整棵隐藏，会立刻从稳定的 `83 shared` 回退到 `61 shared / 22 truth_only`。
- `layout_surface_v1` 里的 bare `tree_text` 不能继续拿来和浏览器 semantic DOM 对拍。像这轮 `# ↻` 这种 JSX 裸文本，只会制造 exec-only；正确口径是让父 element 自己承载文本语义。
- `slice-gate` 接三验证人 BFT 正式验收时，基线必须统一从 `verify-bft-validator-three-node` / `v3_bft_expected_baseline(...)` 派生。旧的手工 `750/250` 只能算联调记录，当前 live 正式口径已经是 `height=1 alice=67 bob=33`。
- 三节点 BFT 的正式验收入口不能只停在 `deploy/status`。命令必须自己 `write report + stop cluster`，这样下一轮才不会被残留 `bft_state_machine/validator/state-serve` 进程污染。
- “同一台手机里的 PWA”和“安卓原生 DID”不能再按同一安全容器理解。UniMaker 这轮已经坐实：PWA 恢复走的是浏览器 `cheng-webauthn` DID，安卓原生走的是 `cheng-bio` 硬件 DID；即使在同一台设备上，恢复出来的 DID 也不会等值。没有本机 Passkey 的浏览器会直接掉到蓝牙/NFC/USB 的 hybrid transport，这不是在找“另一台手机”，而是浏览器拿不到本机平台认证器。
- PWA Passkey 创建不能假设浏览器一定实现 `AuthenticatorAttestationResponse.getPublicKey()`。UniMaker 这轮已经坐实：部分安卓浏览器只会在 `attestationObject.authData` 里给 COSE 公钥；如果前端只认 `getPublicKey()`，恢复链路会稳定报“浏览器没有返回Passkey公钥”。正式口径是优先吃 `getPublicKey()`，缺失时从 `attestationObject` 解析 COSE EC2(-7/P-256) 并转成 SPKI。
- `r2c-react-v3 compile/compare` 这类长流程当前会在 `/tmp/...` 产物已经落齐后，外层 controller 还继续挂着不退。后面再跑同类回归时，只要 `cheng_codegen_exec_snapshot_v1.json` 或 `compare_truth_report_v1.json` 已经落盘，就先按 `out-dir` scoped `pkill` 把旧 controller 收掉，再开下一轮，别让长尾继续吃内存。
- `cheng_v3_seed.c` 这类大 seed C 文件里，本地 `static` helper 不能再先用后定义。像这轮 `v3_bft_expected_baseline(...)`，只要漏了前置声明，fresh bootstrap 就会直接死在 `stage1_compile` 的 implicit declaration。
- `stage1_bootstrap.cheng` 的 `supported_commands` 不是装饰字段，而是 bootstrap 自检硬合同。只要 [compiler_main.cheng](/Users/lbcheng/cheng-lang/v3/src/tooling/compiler_main.cheng) 或 [gate_main.cheng](/Users/lbcheng/cheng-lang/v3/src/tooling/gate_main.cheng) 新增了 live 命令，`stage1_bootstrap.cheng` 不同步补 token，fresh bootstrap 就会卡死在 `stage1_self_check` 的 `supported_commands drift`。
- `home_default` 这条真值线一旦已经做到 `shared=83 / truth_only=0 / exec_only=26`，后面剩的就不是渲染原语缺口，而是状态精化。下一刀该继续盯 `renderUpdateBanner / tab badge / VirtualizedMasonry / bottom nav` 这 4 堆，不要再回去扩 `helper/.map()/lucide/node budget` 这种已经够用的层。
- `operatorId -> latest bundle` 这种恢复索引不能再做成“一个键只留一条最新记录”。UniMaker 这轮已经坐实：同一个 `operatorId` 下，安卓原生 `android_bio_v1` 和 PWA `browser_recovery_draft_v1` 会并存；如果服务端只按 `operatorId` 覆盖，PWA draft 一写入就会把安卓恢复材料顶掉，另一台安卓必然报“恢复密钥在链上没有找到 DID”。正式口径是同一 `operatorId` 下按 `bundleKind` 分层存，客户端查的时候显式带 preferred kinds。
- `dmit` 这种外部机器如果前面报过 `Connection timed out during banner exchange`，不要立刻当成“今天都没戏了”。这轮已经坐实它会间歇性恢复，所以远端主线一边要内建重试，一边要在关键时刻再短探一次，常常就能把第三台追平收完。
- 手工在远端重建 `x86_64-unknown-linux-gnu` 的 `bft_state_machine` 时，顺序必须钉死成 `bootstrap-bridge -> build-backend-driver --target:x86_64-unknown-linux-gnu -> build-bft-state-machine`。少掉中间那步显式 target，远端 `build-backend-driver` 会直接回落到错误的 `arm64` 主线。
- 三节点 deploy、remote x86 gate 这种真远端主线里，`ssh/rsync` 不能再按“一次失败就彻底失败”处理。像这轮 `dmit` 的 `Connection timed out during banner exchange`，不把 `compiler_main/gate_main/cheng_v3_seed.c` 一起收成固定重试并显式带 `BatchMode/ConnectTimeout/ServerAlive`，deploy 会被瞬时 SSH 抖动直接打断。
- `validator-proposer-daemon` 这种 timed accept server，一旦已经 reset 到 fresh session，就不能继续处理旧连接残留的 `Handshake/Short` 包。刚才这轮已经坐实：不先拦掉 stale non-initial，下一轮 accept 会直接炸成 `tls13 finished: handshake secrets ! ready`。
- `acceptTimed(waitMs)` 不能把“空闲等连接”和“已见 Initial 后等握手完成”混成一个总时钟。公网 RTT 稍高时，server 会在发完 flight 后先被 idle timeout 掐掉，客户端最后只剩 `pipe read timeout`；正式口径是空闲窗口和握手完成窗口分开算。
- 公网 QUIC 排障不要只看 client 报错；先抓 server 口的包。像这轮 `64` 的 `42061/udp`，`72B client Finished` 和 `77B app/select` 包都已经真到网卡，这一下就能确认问题在 server runtime accept 语义，不在公网没到。
- 三验证人 BFT 当前 quorum 是 `2/3`，所以当某一台机器的 SSH 本身失稳时，先用 `本机 + 64` 把 proposer/follower 真出块打通，再等第三台追平。不要把第一块主线验收和外部 SSH 故障绑死在一起。
- `recovery draft` 里的 `did` 不能再被当成浏览器最终设备 DID 去硬校验。UniMaker 这轮已经坐实：Android 侧 draft 发的是“便携恢复标识”，PWA 真正恢复出来的 DID 还会继续受当前 passkey 派生影响；如果导入路径硬拿 `draft.did` 去卡 `buildBundleFromRecoverySecret(...)`，恢复会稳定报 `browser_bio_did_recovery_did_mismatch`。正式口径是 draft 只负责“允许当前浏览器登记新 passkey 并恢复”，不承担最终设备 DID 等值证明。
- 安卓已有 DID 的恢复索引同步不能只挂在 `create/import` 新锚定上。UniMaker 这轮已经坐实：如果本机早就有 `publicBundleText + operatorId`，但单按钮入口在“已有 DID”分支直接短路返回，公网 `operatorId -> bundle` 索引会一直空，PWA 侧就永远只会报“恢复文本已识别但查不到 DID”。正式口径是：已有 DID 进入恢复材料检查时，也要强制把当前公开 bundle 再镜像到公网 recovery 索引。
- PWA 恢复错误不能再把“公网 recovery 索引 miss”和“查到的是安卓原生 bundle，浏览器压根不支持按同一硬件 DID 恢复”混成同一句。UniMaker 这轮已经坐实：用户会被误导成“是不是链没上”；正式口径是 operator miss 明确提示“先在安卓新版点一次生物验证DID完成同步”，查到 `android_bio_v1` 时则直接提示“当前 PWA 还不能按同一套硬件 DID 恢复”。
- `compare-truth` 这种合同面，别再只暴露一个 `semantic_nodes_count`。像这轮，如果不同时保留 `exec_snapshot_semantic_nodes_count=92` 和真正 compare basis 下的 `exec_semantic_nodes_count=54`，后面的人会继续拿异层数字修错地方。
- `r2c-react-v3` 真值回归里，只看到 `semantic_nodes_count` 不一致时，不要直接去拍脑袋修 surface。先把两边都补成节点清单再做 diff。这轮已经坐实：live truth DOM=`83`、exec snapshot=`92`、exec layout surface=`54`，单看 `92 vs 83` 根本看不出真正偏的是哪一层。
- `r2c-react-v3 compare-truth` 这条链里，和浏览器 DOM 对拍时不能把 `component/branch` 中间层节点继续算进最终语义树。`ContentPublishProgressOverlay`、`||`、`&&` 这些节点一旦出现在 `exec_only_nodes`，就说明当前 exec 侧口径还没降到 DOM 同层。
- `layout_surface_v1` 这条 source 投影线，先补“本地零参 helper、静态 `.map()`、lucide icon 子树”，再谈 prune。`home_default` 这轮已经坐实：不先把 `renderContent()/renderUpdateBanner()/sortedTabKeys.map()/lucide-react` 展开，compare 会稳定漏 header/tabs/nav，后面怎么调 compare 规则都修不正。
- `layout surface` 的 node budget 不能拍脑袋卡太小。`96` 在 `home_default` 上已经会直接截掉 bottom nav 和一批 icon/tree 节点；当前 `256 + 既有 module/component/rss/source chars budget` 这组约束才足够表达真实首屏，而且没有再把内存打爆。
- 当 direct compare 已经做到 `truth_only=0` 时，下一刀一定是 prune exec-only，不是继续补 missing truth。相反，如果这时用“未知条件默认隐藏”这种粗暴策略，现场会立刻从 `shared=83 / truth_only=0` 回退到 `shared=44 / truth_only=39`。
- ordinary controller 里不要把 record 字段直接塞进 `strings.len(...)`、三目或复合 fallback。像这轮 `strings.len(compare.truthState)` 就会触发 `primary_object_body_semantics_missing`；先落本地 `str` 再判断长度，形状才稳。
- `operatorId -> latest bundle` 这类远端恢复索引，服务端不能只校验 `operatorId/did/bundleCid` 外层字段，更不能把客户端塞来的 `bundleText` 原样吞下。UniMaker 这轮已经坐实：只要 ingress 不重算公开 bundle 的 canonical cid、也不显式拒绝 `rootSeed/deviceSeed`，私有 seed 迟早会被旧客户端重新上送。正式口径是“远端只接受公开 bundle；服务端重算 cid；出现私有字段直接 hard-fail”。
- QUIC 这类 range 合并模型别再写“回退插入点 + 原地平移 + 多段 merge”那种分支网。`10,12,11` 这种最普通序列这轮已经把 [ack_tracker_model.cheng](/Users/lbcheng/cheng-lang/v3/src/quic/congestion/ack_tracker_model.cheng) 打成 `idx=-1 len=64`；正式口径是顺序扫旧 ranges、临时 primitive arrays 归并、最后一次性回写。
- 新 smoke 只挂 [gate_main.cheng](/Users/lbcheng/cheng-lang/v3/src/tooling/gate_main.cheng) 不够。`run-host-smokes` / `run-stage23-libp2p-smokes` 的 live 默认列表同时在 [cheng_v3_seed.c](/Users/lbcheng/cheng-lang/v3/bootstrap/cheng_v3_seed.c)，不一起改并 fresh 重编 `stage0`，源码口径和 live 入口一定会漂。
- 当前 stage3 遇到“复合返回 + 局部绑定”还不稳的模块，先改 smoke 形状，不要硬顶。像这轮 `initMsQuicRuntime/initMsQuicStream/initMsQuicTls13HandshakeState`，直接调会卡 primary-object；改成 `validate settings + 零值 composite + *Into(...)` 后，host/stage23 就都通了。
- 想把 PWA 的 `operatorId -> latest bundle` 直接并到 `chain_node` 前，先统一 bundle 协议，别先接桥。UniMaker 这轮已经坐实：PWA 现在锚的是浏览器侧 JSON bundle，里面直接带 `rootSeed/deviceSeed/method=cheng-webauthn`；而 [bio_did_chain_node.cheng](/Users/lbcheng/cheng-lang/v3/src/project/bio_did_chain_node.cheng) 验的是正式 canonical DID-store bundle。两边文本合同没统一前，`ingress -> chain_node` 直切只会把恢复查询做成假统一。
- PWA 跨设备恢复如果提示“恢复文本已识别，但查不到 DID”，先别把锅甩给“链还没上”。UniMaker 这轮已经坐实：只要恢复入口还在查浏览器本地 `localStorage` 的 operator 索引，用户换一台设备就必然 miss。正式口径是把 `operatorId -> latest bundle` 做成共享 ingress/链投影查询，本地存储只保留镜像缓存，不能继续当权威源。
- seed 里所有“扫字符串边界”的地方都不要再各写一套 `前一个字符是不是 '\\'`。这轮已经坐实：只修 [cheng_v3_seed.c](/Users/lbcheng/cheng-lang/v3/bootstrap/cheng_v3_seed.c) 一个点不够，`split args / call parse / statement collect / assignment / member/index access / wasm trim` 会继续把 `\\\"` 误判成真引号。正式口径是统一走 `v3_quote_is_escaped(...)`。
- 遇到 ordinary 字符串链式表达式被误编时，先看 `print-asm`，别先猜 UIR 或运行时。像这轮 `bindText` 一眼就在汇编里露成了整段错误 `.asciz`，比在旁路上试探快得多。
- 为了定位编译器根因临时加的 probe 文件要当轮删掉，别把一次性排障残留留在测试面里。正式回归只保留能稳定表达合同面的 smoke。

2026-04-16
- 单按钮 DID 交互不能只覆盖“新建/恢复”首通，漏掉“已有 DID 重新查看恢复材料”。UniMaker 这轮已经坐实：如果 `生物验证DID` 在已有 DID 场景下只跑 ensure、不回显当前恢复文本，用户表面感知就是“点了没反应”，实际上是产品语义断了一截。正式口径是：已有 DID 且本机已存恢复材料时，单按钮默认直接弹恢复文本；缺恢复材料时再补生成并补锚。
- `operatorId` 这种跨端恢复索引键，派生公式必须在 Android / PWA / 链节点三边字节级一致，不能一边算 `sha256(tag bytes || secret bytes)`，另一边算 `sha256("tag|hex")`。UniMaker 这轮已经坐实：同一份 `recoverySecret` 会被导成两个不同 `operatorId`，用户第一眼只会看到“恢复文本不正确”，实际根因是协议键分叉。正式口径是统一成 `sha256(tag bytes || recoverySecretBytes)`，旧版 Android 公式只用于迁移识别，不能继续产新值。
- PWA 的恢复文本入口不能假设用户粘贴的是“纯 JSON 整段”。UniMaker 这轮已经坐实：真实输入会混进“粘贴/恢复文本”提示词、Markdown code fence、扫码识别残留前后缀；如果只做 `trim + JSON.parse`，明明是合法 `unimaker_bio_did_recovery_v1` 也会被打成“恢复文本不正确”。正式口径是先按字符串状态机精确抽出首个完整 JSON 对象，再走 payload 校验，别拿模糊正则乱截。
- PWA/原生共用恢复文本时，恢复解析不能只硬认单一 `kind`，也不能假设输入一定是裸 JSON 对象。UniMaker 这轮已经坐实：Android 导出的 `unimaker_bio_did_recovery_v1`、纯 `recoverySecret`，以及“JSON 字符串包着格式化 JSON”的形态都会真实出现；PWA 如果只认自己那版 payload，用户侧就只会看到“恢复文本不正确”。正式口径是恢复入口至少兼容 `Android kind / legacy PWA kind / raw recoverySecret / quoted JSON text` 四种输入，再把“格式不对”和“operator 查不到”分成不同错误。
- `validator-proposer-daemon` 这条 server 主线本身就会 `serve proposal + capture vote + serve commit`。deploy 里不能再额外起 `proposal-serve-daemon / commit-serve-daemon / vote-capture-daemon` 去抢同一组端口，不然 proposer 会直接在公网机上被打成 `msquic udp: bind failed`。
- `deploy-bft-validator-three-node` 这类多守护进程入口，不能只看 `*.ready` 文件判断 server 侧 QUIC 子服务都起来了。像这轮 `server.vote.ready` 在没起 `vote-capture-daemon` 时也会出现；最稳口径是把对应 `pid + ready + 端口/进程` 一起纳入启动等待，尤其是 vote 口不能漏。
- 多机验收里如果 `ssh` 已经能建 TCP 但卡在 `Connection timed out during banner exchange`，要立刻把它记成外部主机/SSH 面阻塞，不要继续把 deploy 卡住的尾部状态误算成 Cheng 语义面通过。
- Android `libchengv3libp2p.so` 这层 `v3_mobile_ffi.c` 不能只补 JNI 壳、不补协议面导出。UniMaker 这轮已经坐实：DID create/provision 全成功后，只要 `BlockchainService` 继续调用的 `libp2p_fabric_submit_event / libp2p_fabric_query_projection / libp2p_name_resolve / libp2p_credit_balance` 等符号没在 wrapper 里真实导出，真机就会在“DID 已生成，正在上链”这一步直接报 `unsupported_in_nim_mobile_ffi`。正式口径是把 Android 需要的 `fabric/social/name/credit` ABI 在 [v3_mobile_ffi.c](/Users/lbcheng/UniMaker/React.js/scripts/libp2p/cheng_shims/v3_mobile_ffi.c) 一次性补齐，并让 `bio_did_operator_lookup` 这种查询直接落在确定性索引上，不能再靠 UI 兜底或跳过上链。
- [NativeAppViewModel.kt](/Users/lbcheng/UniMaker/android/app/src/main/kotlin/com/unimaker/nativeapp/NativeAppViewModel.kt) 这类超大 Kotlin 类，如果编译突然报 `override fun onCleared` 变成 top-level、`companion object` 不允许出现在 file、最后还连着 `Syntax error: Expecting a top level declaration`，先别怀疑后半段业务代码，先查前面是不是多了一个孤立 `}` 把类提前关掉了。UniMaker 这轮已经坐实：媒体 RPC 附近多一个 `}`，会让后半个 `ViewModel` 全部被 Kotlin 当文件级声明；如果再跳过 `:app:compileDebugKotlin` 强行打包，装到手机上就会直接 `ClassNotFoundException: com.unimaker.nativeapp.UniMakerApplication` 白屏闪退。
- [ChengLibp2pNative.kt](/Users/lbcheng/UniMaker/android/core-bridge/src/main/kotlin/com/unimaker/app/libp2p/ChengLibp2pNative.kt) 这种 JNI 包装层也不能对 `hardwareAttestation/publicBundleText/bundleText` 这类 canonical 原文再补一层 `.trim()`。UniMaker 这轮真机已经坐实：就算 `feature-profile` 侧保住了结尾换行，只要 JNI 前再 `trim()` 一次，native 还是会稳定报 `non canonical hardware attestation want_len=1000 got_len=999 diff_at=999`。正式口径是“canonical 文本跨边界只判空，不修边，不标准化，不重写结尾换行”。
- [did.cheng](/Users/lbcheng/cheng-lang/v3/src/libp2p/identity/did.cheng) 里 DID 文本本身也不能再偷用 `prefix + suffix` 这种直拼。UniMaker 这轮真机已经坐实：`v3DidFromGenesisCid(...)` 和 `v3DidGenesisCidText(...)` 只要还走 `+`，native create 就会回 `ok=1` 但 `did=` 空串，连 `documentTextHex` 里解出来的 `did` 也一起丢，最终 Android 报 `bio_did_failed_missing_did`。正式口径是 DID 前缀、`g` 前缀这类协议文本也统一走 `v3DidTextAppend(...)`。
- [AndroidFingerprintDidHardware.kt](/Users/lbcheng/UniMaker/android/feature-profile/src/main/kotlin/com/unimaker/nativeapp/feature/profile/AndroidFingerprintDidHardware.kt) 和 [NodeDidIdentity.kt](/Users/lbcheng/UniMaker/android/feature-profile/src/main/kotlin/com/unimaker/nativeapp/feature/profile/NodeDidIdentity.kt) 里，`hardwareAttestation/publicBundleText/bundleText` 这类 canonical 文本绝不能再 `.trim()`。UniMaker 这轮真机已经坐实：只差最后一个 `0a` 换行时，native 会稳定报 `non canonical hardware attestation`；正式口径是“判空可 `isBlank()`，持久化和跨边界传递必须保留原文”。
- [bio_did_chain_node.cheng](/Users/lbcheng/cheng-lang/v3/src/project/bio_did_chain_node.cheng) 里 canonical attestation / canonical bundle / canonical document / DID 对比，别再用整串 `!=`。UniMaker 这轮真机已经坐实：签名校验都过了，最后还能在 `canonicalText != text` 这一步假报 `non canonical hardware attestation`。正式口径是统一复用逐字符的 `v3BioDidTextEqual(...)`。
- [bio_did_chain_node.cheng](/Users/lbcheng/cheng-lang/v3/src/project/bio_did_chain_node.cheng) 这类从 Android/JNI 侧传进来的 trusted attestor `rootKeyId`，不要直接把入参字符串塞进全局变量，也别再用整串 `!=` 做校验。UniMaker 这轮真机已经坐实：`android.keystore.biometric.v1` 这种常量都会在后续比对里漂成 `hardware attestation root key mismatch`。正式口径是写入时先用 [did.cheng](/Users/lbcheng/cheng-lang/v3/src/libp2p/identity/did.cheng) 的 `v3DidTextAppend("", text)` 做稳定拷贝，校验时按长度和逐字符比较，报错直接带 `expected_hex/got_hex`。
- [did.cheng](/Users/lbcheng/cheng-lang/v3/src/libp2p/identity/did.cheng) 这种 `key=value` 文本解析，不要再写 `let prefix = key + "="` 再配 `hasPrefix/sliceBytes`。UniMaker 这轮 Android 真机已经坐实：动态前缀拼接一旦漂掉，就会把整行 `v3_biometric_attestation_version=2` 当成 value，最后在 `parseInt32` 报错。正式口径是像这轮这样按 `keyLen` 逐字符比对，再显式检查 `line[keyLen] == '='`，最后用剩余长度切值。
- `mobile/hardware/biometric.cheng` 里 attestation/request wire 这种要落成 `key=value\n` 的文本，不能再用 `out = out + "key=" + strings.intToStr(...) + "\n"` 这种长连拼。UniMaker 这轮 Android 真机已经坐实：`purpose/platform/attestation_kind` 会被拼成脏值，随后在 `v3DidStoreParseInt32(...)` 报 `v3 did store: invalid int32`；正式口径是统一走 [did.cheng](/Users/lbcheng/cheng-lang/v3/src/libp2p/identity/did.cheng) 的 `v3DidStoreAppendLine(...)` 和 `v3DidTextAppend(...)`。
- [bio_did_mobile_biometric_smoke.cheng](/Users/lbcheng/cheng-lang/v3/src/tests/bio_did_mobile_biometric_smoke.cheng) 这类 mobile CAPI smoke，如果字符串来自 [mobile_capi.cheng](/Users/lbcheng/cheng-lang/v3/src/libp2p/mobile/mobile_capi.cheng) 的 `v3MobileCapiTextCString(...)`，释放时必须走同库 `v3MobileCapiStringFree(...)`，不能再拿 `cheng_free`。这轮 host smoke 的 `pointer being freed was not allocated` 就是旧释放器和 libc `malloc/free` 不匹配。
- Android 目标上别再用 `fn append(out: var str, ...)` 这类跨函数原地改字符串的写法。现场已经坐实：`v3DidStoreAppendLine(out: var str, ...)` 在 `mobile_capi/did/bio_did_chain_node` 这条 DID 主链上会把文本静默编成空串；最稳口径是改成纯返回式 `out = append(out, ...)`，把 `error/provision/request wire/document/bundle` 全部统一成返回新字符串。
- `verify-x86-linux-remote` 这类 ordinary 远端 gate，别再用 `"v3 " + smokeName + " ok"` 这种三段字符串直接做成功判定。现场已经坐实：远端 smoke 真通过也会被误判成 drift；最稳口径是单独 helper 顺序组装期望文本。
- `compiler_main.cheng` 里远端 gate 的 `summary/report/[host]` 文本不要再 inline 连拼三段以上。最稳口径是像这轮这样补 `append key=value line / append section label / pipe pair` 极小 helper，避免路径和节标签再次被编成字面量残片。
- 远端 `x86` gate 不能只 `rsync` 就直接跑旧 `stage0/stage3`。最稳顺序已经坐实：同步源码后先在远端 `cc` 当前 `cheng_v3_seed.c`，再显式带 `CHENG_V3_ROOT/CHENG_V3_TARGET` 跑 `bootstrap-bridge -> build-backend-driver -> print-build-plan -> smoke`。
- 零脚本收口不能只删叶子 wrapper。只要 [cheng_v3.sh](/Users/lbcheng/cheng-lang/v3/tooling/cheng_v3.sh)、[bootstrap_bridge_v3.sh](/Users/lbcheng/cheng-lang/v3/tooling/bootstrap_bridge_v3.sh)、[build_backend_driver_v3.sh](/Users/lbcheng/cheng-lang/v3/tooling/build_backend_driver_v3.sh) 这种用户面入口壳还在，`v3` 就还不是结构上的纯 Cheng；最稳口径是像这轮这样直接切成 `cheng.stage0 / cheng.stage3 / backend_driver` 三个真二进制，并让 `verify-orphan-guard` 直接禁止 `v3/tooling` 顶层再出现任何 `.sh`。
- `cheng.stage3` 这类自举产物如果命令名同时存在于 seed 内建和 pure Cheng `compiler_main`，不要默认一定先走 pure Cheng。像这轮 `print-build-plan/build-backend-driver` 就是先被 [cheng_v3_seed.c](/Users/lbcheng/cheng-lang/v3/bootstrap/cheng_v3_seed.c) 截走；命令口径要追平时，两层都要改。
- `var str` 这种复合出参在 seed 发射器里，右侧只要还会调函数，就必须像标量赋值一样先 spill 目标地址再算 RHS。`outValue = paramStr(...)` 这种形状不这么做，会静默写丢。
- `build-backend-driver` 的 plan 自检必须让候选和参考都显式带同一个 `--target`。只改 compile 阶段不够；后面的 `print-build-plan` 一旦裸跑，`x86_64-unknown-linux-gnu` 这种请求会偷偷退回默认 target。
- `cheng.stage3` 里和 live `backend_driver` 重名的命令，转发条件要按命令本身定，不能只看“有个可执行文件”。`build-backend-driver` 可以拿现有 live binary 自举重编自己，`print-build-plan` 则必须等 `v3_backend_driver_ready(...)` 为真后才直通，不然旧 backend 会把 stale 语义伪装成当前输出。
- `stage3 print-build-plan` 如果突然退回 seed 输出，先查 `bootstrap_artifacts_fresh`，不要先怀疑 helper 分流逻辑。这轮真根就是 [gate_main.cheng](/Users/lbcheng/cheng-lang/v3/src/tooling/gate_main.cheng) 的 mtime 比刚编完的 `stage2/stage3` 还新；正确顺序必须是 `bootstrap-bridge -> build-backend-driver -> print-build-plan`。
- UniMaker 这条 DID 主线已经定口径：跨设备连续性只认 `recoverySecret -> operatorId`，不把 Android `feature32` 当跨设备稳定身份。后续 create/import、链上索引、PWA/原生恢复都必须围着这条语义实现，不能再把本机生物特征误当“同一个人跨设备主键”。
- Android 双 `.so` 下，`libchengv3mobile.so` 的 create/import 结果字符串不能再靠 `driver_c_new_string_copy_n` 这种反向跨库桥物化。UniMaker 这轮真机 trace 已坐实：桥一断就会出现 `bio_did_create_empty_native_result` 且 `debug.handle=0`。正式口径是让 `mobile.so` 自己用 libc `malloc/free` 分配并导出同库 free，JNI 只消费这块本库内存。
- `mobile_capi` 里这种错误文本函数，别写成 `let out = "a" + value + "b"` 这种单表达式。UniMaker 这轮真机已经坐实，它会直接被编成字面量 `" + errorText + "`；最稳口径还是顺序 `var out` 逐段追加。
- `r2c-react-v3 codegen-surface` 这条已经被 lift 到 Cheng controller 的命令，focused smoke 也要直接走 `cheng.stage3 r2c-react-v3 codegen-surface`，不要继续在 `compiler_main.cheng` 里手写两条 Node 命令。
- controller 这层读 `summary.env` 的数字时，不要再赌 `parseInt`。最稳口径是复用 surface 那套最小整数解析器，不然 `module_count/route_count` 很容易假归零。
- controller 里重复 flag 如果是 `--runtime-root` 这类多值项，不要从 subcommand tail 复用旧 `collectFlagValues(args, ...)`。最稳口径是直接按原始 `cmdline.paramStr(i)` 扫。
- 当前仓如果 `bootstrap_bridge_v3.sh` 突然红到 `v3/bootstrap/cheng_v3_seed.c` 的 C 编译错误，先重跑 bootstrap 再下结论，不要第一时间把锅扣到刚改的 Cheng controller 文件上。
- `r2c-react-v3` 继续压 helper 面时，`codegen_surface` 这种中层一定先补 focused smoke，再去动 helper 收口。最稳合同就是 `summary.env + cheng_codegen_v1.json + cheng_codegen_route_catalog_v1.json + cheng_codegen_smoke_v1.json` 四件套。
- `compiler_main.cheng` 里新加的命令，不要默认 fresh `stage3` 已经真支持。像这轮 `r2c-react-v3-codegen-surface-smoke` 就是先只在 live backend driver 上线；fresh gate 里必须走 `artifacts/v3_backend_driver/cheng`，不能直接拿 `stage3Path` 假调。
- ordinary 里要拼长 shell 命令时，不要写成一整串 `a + b + c + ...`。最稳口径是 `var cmd = ...` 起手逐段追加；否则发射器可能把中间表达式字面漏进最终命令。
- `r2c-react-v3` 这类 helper 一旦已经不在 live 主链里，别只把状态合同改成空。最稳口径是合同、gate、入口一起改掉，再把死脚本物理删掉；否则后面很容易又被误当成活入口。
- `r2c-react-v3 native_gui_bundle` focused smoke 里，`ok/host_ready/renderer_ready` 卡 report，`route_state/runtime_mode/exec_snapshot_path/route_catalog_path` 卡 summary。不要把所有字段都硬绑到 report；summary 才是 bundle helper 自己的稳定合同面。
- `r2c-react-v3` 主线验收现在要分层：`native-gui-bundle-smoke` 先打 `codegen -> bundle`，`fresh-clean gate` 再打 host smokes + 三路由闭环。bundle 一红时先跑 focused smoke，不要默认整套大 gate 起手。
- `r2c-react-v3` 这类 feed 解析层里，别再拿 `&&/||` 当边界保护。当前 Cheng 这里不能指望它替你短路；像 `len > 0 && text[len-1] == ...` 这种写法，空串上会直接越界，正式写法必须先单独判长度，再进下标访问。
- `r2c-react-v3` ordinary surface 生成 JSON 和数字列时，别先赌 `\\\"` 字面逃逸或现成 `parseInt` 能稳过。当前最稳口径已经坐实：JSON quote 用 `char(34)` 组，数字列走 feed 专用最小解析器，并立刻补一个极小 smoke 把 `field/parse` 单独钉住。
- `r2c-react-v3` 三条核心路由 `home_default/content_detail/publish_selector` 的 bundle 复验要串行跑，不要并发压。大仓 + hostrun + bundle 同时跑很容易把内存和日志一起顶爆；先顺序跑完三条，再统一过 `fresh-clean gate` 才稳。
- Android 真机 trace 如果已经看到 `debug.raw.*=0` 且 `debug.wire.*=0`，别急着把锅甩给 `requestWire` 解码逻辑。UniMaker 这轮继续往下收后坐实：JNI 里哪怕已经改成“先拼 `requestWire` 再进 wire 入口”，只要还是 `chengStrFromStdString(requestWire)` 喂给 `mobile.so`，这条所谓 wire fallback 就仍然和主链共用同一个可疑的 `C++ -> ChengStr` ABI。正式收法必须是像这轮这样补 `v3MobileCapiBioDidCreateWireCStringRaw/HandleRaw`、`ImportWireCStringRaw/HandleRaw`，让 `requestWire` 本身也用 raw `char*` 过边界，再由 `mobile.so` 内部 `strFromCStringCopy(...)`。
- ordinary `mobile_capi` 这类 Android 共享库源码里，别临时加“只为真机排障”的新 Cheng 函数去吐 debug 文本。UniMaker 这轮现场又坐实：像 `v3MobileCapiBioDidCreateDebug(...)` 这种看起来正常的 `let/if/str` 逻辑，会直接把 `syncNativeLibp2p` 卡死在 `primary_object_body_semantics_missing`。真机排障优先在 JNI 里复用仓内已经能编过的入口做旁路探针，比如这轮用现成的 `v3MobileCapiBioDidCreateWire/CString/Handle` 当 raw 路径失败后的 fallback 和定位面。
- Android 双 `.so` 如果症状是“host smoke 全绿，但真机一到 JNI -> mobile.so 就只回空”，别只盯返回 ABI。UniMaker 这轮继续往前推后，真正更可疑的是入参 `ChengStr` ABI：`v3MobileCapiBioDidCreate(...)` 这类函数在 host 内部直调没问题，但 Android C++ 侧自己拼的 `ChengStr{data,len,store=0,flags=0}` 喂进 `mobile.so` 后，结果链会整体变空。正式收法是补 raw `char* -> str` 包装出口，比如 [mobile_capi.cheng](/Users/lbcheng/cheng-lang/v3/src/libp2p/mobile/mobile_capi.cheng) 里的 `v3MobileCapiBioDidCreateHandleRaw/ImportHandleRaw`，让 `mobile.so` 自己做 `strFromCStringCopy(...)`。
- Android mobile CAPI 如果调用面和底层本来就是定长/typed 参数，就别再让 JNI 先拼一层 `requestWire` 再让 [mobile_capi.cheng](/Users/lbcheng/cheng-lang/v3/src/libp2p/mobile/mobile_capi.cheng) 回解。UniMaker 这轮 `bioDidCreate/import` 在 host 的 wire smoke 是绿的，真机却持续 `debug.handle=0`；直接补 `v3MobileCapiBioDidCreateHandle/ImportHandle` 这种“typed 参数直达 handle”出口，再让 JNI 直调，才是正式主链。
- Android 双 `.so` 文本返回如果目标是“`mobile.so` 自己物化 C 字符串”，就不能在 [mobile_capi.cheng](/Users/lbcheng/cheng-lang/v3/src/libp2p/mobile/mobile_capi.cheng) 里继续写 `strToCStringTemp(text)`。UniMaker 这轮现场又坐实了一次：哪怕 JNI 已经改成 `char*`，只要 `v3MobileCapiTextCString` 里还走这步，`mobile.so` 依然会导入并调用跨库 `cheng_str_to_cstring_temp_bridge`，创建 DID 还是会空；正式修法就是像这轮这样直接按字节拷到 raw buffer，再交给 `driver_c_new_string_copy_n`。
- Android `.so` 动态符号表不能直接当“某条热路径还在调用”的证据。普通编译会把闭包里别的函数一起带进 `*UND* cheng_str_to_cstring_temp_bridge`；这轮真正该验的是 [libchengv3mobile.so](/Users/lbcheng/UniMaker/android/core-bridge/src/main/jniLibs/arm64-v8a/libchengv3mobile.so) 里 `v3MobileCapiTextCString` / `v3MobileCapiBioDidCreateWireCString` 的实际反汇编调用边，而不是只看 `objdump -T`。
- `peer_id` 这类协议层如果只是要 `base58btc`，不要继续直接依赖大而杂的 [multibase.cheng](/Users/lbcheng/cheng-lang/src/std/multiformats/multibase.cheng)。最稳口径是先拆出极小共享模块 [base58.cheng](/Users/lbcheng/cheng-lang/src/std/multiformats/base58.cheng)，再让 `peer_id` 和 `multibase` 都委托它。
- runtime ABI live 路径一旦收口，就把它钉进 [backend_runtime_abi_contract.env](/Users/lbcheng/cheng-lang/src/tooling/backend_runtime_abi_contract.env) 并让所有 gate/script 统一 source [backend_runtime_abi_contract.sh](/Users/lbcheng/cheng-lang/src/tooling/cheng_tooling_embedded_scripts/backend_runtime_abi_contract.sh)。不要再让脚本各自硬编码 `system_helpers_*` 路径，否则 `system_helpers_selflink_min_runtime.c` 这类退役入口一定会漂回来。
- `shared emit` 这类“请求面刚打通”的能力，下一步别只靠手工命令。最稳口径是马上补一条 mainless fixture smoke，既卡 report 字段，也卡磁盘产物真类型。
- [cheng_v3.sh](/Users/lbcheng/cheng-lang/v3/tooling/cheng_v3.sh) 往零脚本入口收时，先把 live `backend_driver/stage3` 放到优先级最前，shell 只留 cold-start fallback。这样用户面主路径已经是 Cheng，不会再被薄壳分叉带偏。
- 真 Linux `x86_64` 如果目标只是验 `exe/native link`，先直接上远端 fresh `cheng.stage3 system-link-exec --target:x86_64-unknown-linux-gnu`。远端 ordinary backend driver 自举还没完全绿时，不要把两条账混成一个 blocker。
- `peer_id` 这类协议层如果目标只是去掉 bridge，别先硬拖 [multibase.cheng](/Users/lbcheng/cheng-lang/src/std/multiformats/multibase.cheng) 进 ordinary 主线。先用最小 smoke 真编；如果 seed 还吃不下它的 composite/常量形状，就像这轮这样把实际只需要的 `base58btc` 算法本地化成 seed 可发射的纯 Cheng 版本。
- runtime ABI 权威不要再写成单文件。最稳口径已经坐实：专项 gate 先按 [system_helpers_backend.cheng](/Users/lbcheng/cheng-lang/src/std/system_helpers_backend.cheng) + 最小 native bridge 源组成的组合合同验真，再顺手核对兼容头 [system_helpers.h](/Users/lbcheng/cheng-lang/src/runtime/native/system_helpers.h)。
- [cheng_tooling.cheng](/Users/lbcheng/cheng-lang/src/tooling/cheng_tooling.cheng) 里的 runtime freshness/source list 只放 live 入口。像这轮这样保留 `system_helpers_selflink_shim.c + system_helpers_host_process_ffi_bridge.c`，并把 `system_helpers_io_time_bridge.c` 这种已经被踢出 live 合同的旧桥物理删掉；不要把 `system_helpers_selflink_min_runtime.c` 这种后备兼容入口继续混进 active proof。
- `native_gui_host_ready/native_gui_renderer_ready` 这类 readiness 不能在 [r2c-react-v3-native-gui-bundle.mjs](/Users/lbcheng/cheng-lang/v3/experimental/r2c-react-v3/r2c-react-v3-native-gui-bundle.mjs) 里继续硬写 `false`。只要前面已经拿到了 `first_batch host ABI + session preview + layout/style/layout plan + render plan + compiled runtime`，就必须按真产物现算，不然 report 会自己打自己脸。
- `r2c-react-v3` 继续收 Node helper 面时，别假删还在 live 的 helper。最稳口径是先在 [r2c_react_v3_controller_main.cheng](/Users/lbcheng/cheng-lang/v3/src/tooling/r2c_react_v3_controller_main.cheng) 把 `active_node_helpers/retired_node_helpers` 写成正式合同，再让 gate 对着这份合同硬卡。
- fresh-clean gate 如果要验 bundle runtime mode，别再去 grep controller report。像这轮这样，`runtime_mode` 在 `native_gui_bundle_v1.json`，不在 `native_gui_bundle_report_v1.json`；gate 必须直接验产物本体。
- `system_helpers_backend.cheng` host object 直编一旦已经绿了，下一层真 blocker 往往不是 lowering 本体，而是 ordinary compiler 请求面自己把 `--emit` 锁死了。这轮 `run-linux-object-smokes` 就是现场例子：provider 已经编通，真正拦路的是 [compiler_request.cheng](/Users/lbcheng/cheng-lang/v3/src/tooling/compiler_request.cheng) / [compiler_main.cheng](/Users/lbcheng/cheng-lang/v3/src/tooling/compiler_main.cheng) 只认 `exe`。
- `--emit` 这种正式协议不能 seed 说支持 `exe/shared/obj`，ordinary 却只认 `exe`。这类分叉会把 stage3、backend_driver 和 gate 打成假一致；修法必须落在公共请求层，而不是继续绕 gate 或改脚本。
- Linux object 验收别只信默认 smoke。仓里 [run-linux-object-smokes](/Users/lbcheng/cheng-lang/v3/bootstrap/cheng_v3_seed.c) 当前只覆盖 `aarch64-unknown-linux-gnu`；要确认 `x86_64-unknown-linux-gnu` 主线也真通，必须再单独跑 `build-chain-node-linux` 和 `build-rwad-bft-linux` 的 `x86_64` object 产物。
- Android JNI 这类纯文本 mobile CAPI 返回，优先直接走 `ChengStr -> cheng_str_to_cstring_temp_bridge` 主链。像 UniMaker 这轮 `bioDidCreate/import`，JNI 先绕 `buffer handle` 再转字符串，真机排障很脏；直接调 `v3MobileCapiBioDidCreateWire/ImportWire` 配 `bioDidResultOrError(...)`，链路更短，空结果时还能直接看到 `bridge_len/store/flags`。
- UniMaker 这轮 Android 真机 trace 已经坐实：`biometric -> capture -> native.call` 全绿时，`v3MobileCapiBioDidCreateWire` 这种跨 `libchengv3mobile.so -> libchengv3libp2p.so` 的 `ChengStr` 直返仍可能被吃空；双 `.so` 正式口径还是 `WireHandle + cheng_v3_buffer_handle_* bridge`，不要把 direct `ChengStr` 当生产返回 ABI。
- Android 这条 ordinary mobile CAPI 返回大文本时，别再拿 `strDataPtr(text)` 直接喂 `cheng_v3_buffer_handle_from_raw_bridge`。像这轮 `bioDidCreate/import` 的 provision 文本就是 store-backed `str`，`strDataPtr` 可能给空；最稳口径是先 `strToCStringTemp(text)` 真物化，再按 `len(text)` 拷进 buffer handle。
- Android 双 `.so` 下，`mobile.so` 自己构造出来的 `str` 也不要再交给 `libp2p.so` 导出的 `cheng_str_to_cstring_temp_bridge` 去物化。UniMaker 这轮 `debug.handle=0` 已现场坐实，跨 runtime 的 `str -> cstring` 解释会直接丢结果；最稳修法是在 `mobile_capi.cheng` 里本库内自己按字节拷到 raw buffer，再把 `raw ptr + len` 交给 `cheng_v3_buffer_handle_from_raw_bridge`。
- Android 双 `.so` 文本返回如果既不稳在 `ChengStr`，也不稳在跨库 `buffer handle`，正式口径直接收成“结果由 `mobile.so` 自己分配 C 字符串，JNI 立刻复制，再调 `mobile.so` 自己的 free”。UniMaker 这轮 `bioDidCreate/import` 这样收以后，返回边只剩单一 runtime，不再让两个 `.so` 共用字符串/handle 状态。
- 新增 host smoke 断言时，如果日志和源码对不上，先删对应的 `/Users/lbcheng/cheng-lang/artifacts/v3_hostrun/<name>*` 再跑。UniMaker 这轮 `bio_did_mobile_biometric_smoke` 就是先被旧产物打成假 crash，清掉后才回绿。
- `peer_id` 文本解析不能只拦空串。UniMaker 这轮 Android 启动白屏闪退，真根是 [peer_id.cheng](/Users/lbcheng/cheng-lang/v3/src/libp2p/core/peer_id.cheng) 把单字符 `"z"` 当 multibase 前缀继续切 `text[1..]`，现场直接打出 `bounds check failed: idx=1 len=1`，随后进程又被 `FORTIFY: pthread_mutex_lock called on a destroyed mutex` 连带杀掉。遇到这类前缀协议，至少把“只有前缀没有 payload”的最短坏输入单独判错，并补 smoke 卡住。
- [os_host_process.cheng](/Users/lbcheng/cheng-lang/src/std/os_host_process.cheng) 这类大函数如果既做 process 控制又直接返回复合结果，不要在函数体里新绑复合局部。像这轮 `execFileCapture` 只有它红，而 bridge/API smoke 都绿；最稳收法是拆成 `fill helper + 薄对象返回`。
- host smoke 一旦怀疑源码和运行结果对不上，先删 [artifacts/v3_hostrun](/Users/lbcheng/cheng-lang/artifacts/v3_hostrun) 里对应的 binary/log/object 再复验。旧 hostrun 产物会把上一次错误继续带出来，特别容易把“已经修掉的红点”误看成没修。
- [system_helpers_backend.cheng](/Users/lbcheng/cheng-lang/src/std/system_helpers_backend.cheng) 这类 provider host object 直编一旦红在 seed lowering，先别再误判成剩余 C 边界；最稳顺序是先把泛型 `load/store` 收成定型 helper，再本地补 `cheng_bytes_* / cheng_seq_*`，最后把命令拼接和字面量写入拍平成 ordinary 能发射的直线链。
- provider helper 里不要再写 `ptr("literal")`、转义字符字面量或 `if` 分支里一串 `pos += ...` 多语句。当前 seed 对这几种形状都不稳；像这轮这样改成 ASCII 数值直写和单赋值 helper 返回，host object 才能稳定编过。
- `system_helpers_backend` 这条线单独 `system-link-exec --emit:obj` 通过后，必须马上补跑 `bootstrap_bridge_v3.sh`、`build_backend_driver_v3.sh`、`verify_backend_ffi_handle_sandbox.sh`、`host-bridge-audit`，否则很容易只修了一个 isolated compile case，却把 active driver 或 `@ffi_handle` 主线悄悄打坏。
- Android 双 `.so` 这条链里，像 [mobile_capi.cheng](/Users/lbcheng/cheng-lang/v3/src/libp2p/mobile/mobile_capi.cheng) 这种 ordinary shared 如果 `importc("cheng_v3_buffer_handle_*")`，不要再在 Cheng 里临时造第二套结果 handle。当前最稳口径是让 wrapper `libchengv3libp2p.so` 直接导出同名 bridge，`libchengv3mobile.so` 保持 unresolved import，装载时在同一个 load group 里解析；JNI 也统一走这套 buffer-handle ABI。
- `host-bridge-audit` 不能再靠硬编码边界名报状态。像这轮 `pure_cheng_zero_c_ready=1` 能成立，是因为它改成了按 live provider 真导出的 `process/fs/socket/entropy` 能力现算；后面只要 audit 还写死字符串，结论就不可信。
- [os_host_process.cheng](/Users/lbcheng/cheng-lang/src/std/os_host_process.cheng) 这类普通 Cheng 宿主进程层，别再保留 `var out` 回写 ABI。最稳口径已经坐实：桥函数只返回 status，`pid/fd/eof/exitCode` 全走固定 slot getter，普通层只做显式读取。
- `importc/exportc` 这条边里，`pid:int64` getter 目前不够稳。像这轮现场已经坐实，process spawn 先落 `int32 pid getter` 再转 `int64` 才稳定；别再把跨边界小整数硬抬成 `int64` 以为更“通用”。
- stdout-only 的 shell 包装别再写成 `(...) 2>/dev/null`。这轮 `execFileCapture` 已经真打出过 `127`，直接拼 `command + " 2>/dev/null"` 才是当前稳定形状。
- 熵桥接不能只“导出过”。像这轮 [rand.cheng](/Users/lbcheng/cheng-lang/src/std/crypto/rand.cheng) 之前虽然声明了 entropy import，实际没走 provider；后面这种能力一旦说“已收回 pure Cheng”，必须补 smoke 真跑。
- ordinary `@ffi_handle` 不能只做 `register/resolve`，释放语义还必须跟正式 `@ffi_handle_consume(argN)` 对齐。像这轮 annotated stale trap 为空日志，不是 runtime 没接上，而是 release 后没有 invalidate handle 槽；最稳口径是 seed ordinary lowering 也按 consume 注解做显式失效。
- 直接调用 `cheng.stage3 system-link-exec` 时，别再赌 `BACKEND_RUNTIME_OBJ/BACKEND_NO_RUNTIME_C` 这类 shell 环境变量会自动进 native link。现场已经坐实，直调 binary 不吃这层 wrapper 语义；真要补额外对象，必须给 seed 一个正式入口，比如这轮补的 `--link-input:<obj>`。
- [system_helpers_backend.cheng](/Users/lbcheng/cheng-lang/src/std/system_helpers_backend.cheng) 当前编不出 host runtime object，已经不是 host bridge 边界问题。`host-bridge-audit` 这轮真实输出已是 `remaining_non_pure_cheng_boundary=none`；真正红点是 backend 还缺一组统一语义：`load/store` 标量化、`cheng_seq_get/set`、`cheng_bytes_*`、条件表达式复合实参和若干 `stmt_call/stmt_if/stmt_let` 形状。
- [cheng_safe_read_stream(...)](/Users/lbcheng/cheng-lang/v3/src/runtime/program_support_backend_v3.cheng) 不是“读完整文件”，只是“stream 为空时兜底 stdin”。以后看到名字里有 `safe_read_stream`，先查实现；真要把文件读成字符串，直接用 [cheng_read_text_file(...)](/Users/lbcheng/cheng-lang/v3/src/runtime/program_support_backend_v3.cheng) 或自己 `fread`。
- `v3` provider 这条线上，`importc/exportc` 带 `str[]` 形参本身可以成立；如果 direct argv 桥编得过却连不上，先查 live provider 有没有真公开对应符号，不要先把锅甩给 seq ABI。
- provider 返回 raw cstring 给普通 Cheng 调用方时，默认先走 `strFromCStringCopy(...) + explicit dispose`。`strFromCStringBorrow(...)` 只适合绝不释放、且生命周期完全受控的只读指针；一旦桥另一侧返回的是可释放内存，borrow 很容易演变成泄漏或误释放。
- ordinary 遇到 `scalar call resolve failed` 时，先别急着把业务函数降级。像这轮 [cheng_v3_seed.c](/Users/lbcheng/cheng-lang/v3/bootstrap/cheng_v3_seed.c) 已现场坐实，真根可能只是 `importc` resolver 只认单行签名；跨行 `@importc + fn` 一样要先 `rg` 源码，再补 resolver。
- [system_helpers_backend.cheng](/Users/lbcheng/cheng-lang/src/std/system_helpers_backend.cheng) 这类 provider stub 里，`store(outX, -1/1/0)` 不要再把字面量直接塞进去。Linux nolibc ordinary 很容易把它误判成坏参数；最稳写法是先落 `let value: int32/int64 = ...`，再 `store(outX, value)`。
- [os_host_process.cheng](/Users/lbcheng/cheng-lang/src/std/os_host_process.cheng) 这类 host FFI 边界里，`str -> cstring` 不要继续赌隐式桥接。像这轮 `cheng_pipe_spawn/cheng_pty_spawn/cheng_pty_write` 直接显式 `strToCStringTemp(...)`，ordinary 发射面会稳很多。
- `r2c-react-v3` controller 这条主线当前不能硬拗 `direct argv + ptr/seq FFI`。这轮 ordinary 已现场坐实，`cheng_exec_file_pipe_spawn(filePath, argvSeq, envSeq, ...)` 会直接卡在 `scalar call resolve failed`；最稳口径是先把执行边界收成独立 [r2c_process.cheng](/Users/lbcheng/cheng-lang/v3/src/tooling/r2c_process.cheng)，内部只用 ordinary 已稳定的 `std/os.execCmdEx`。
- 程序解析不要再赌 `command -v`。这轮 controller 已经现场打出过 `missing executable: node`，但 shell 里同机 `PATH` 明明能找到 `~/.nvm/.../node`；最稳口径是直接读 `PATH` 逐项拼真实候选路径。
- [os_host_process.cheng](/Users/lbcheng/cheng-lang/src/std/os_host_process.cheng) 这种基础层如果做过 pipe-capture 试探，收尾前必须拉回 ordinary 能编的稳定口径。当前最稳形状就是 `execFileCapture -> execCmdEx`、`execFileStatus -> execCmdStatus`，不要把实验版直接留在 live 树里。
- `system_helpers.c` 拆分线不能再拿“annotated fixture 编不过”当 runtime 依赖没清完。先分清两件事：`cheng_ffi_handle_*` runtime object 合同能不能真编真跑，和 `@ffi_handle` ordinary lowering 有没有接上；这轮已经坐实，前者已通，后者还卡在 `scalar call resolve failed ... rawHandleNewI32`。
- 这种 native pointer typedef 一定先展开再落 Cheng alias。像这轮 [system_helpers_backend.cheng](/Users/lbcheng/cheng-lang/src/std/system_helpers_backend.cheng) 里的 `DarwinDirentPtr = DarwinDirentPtr`，会直接把 stage3 编译拖进栈爆和超大内存；改成 `DarwinDirent *` 才回到可诊断的普通编译错误。
- 这类“已经收回 pure Cheng”的主线，不能只信前一轮文档和口头结论；继续干活前必须先 `rg` 真源码。像这轮 [peer_id.cheng](/Users/lbcheng/cheng-lang/v3/src/libp2p/core/peer_id.cheng) 和 [program_support_backend_v3.cheng](/Users/lbcheng/cheng-lang/v3/src/runtime/program_support_backend_v3.cheng) 就又漂回了旧 `cheng_v3_base58btc_*` 桥，不先扫文件就会被假状态误导。
- `host-bridge-audit` 只能按现场输出写结论。这轮真实状态已经是“旧 `exec_file_*` 桥清干净了，但还剩 `os_process_fs_socket_entropy_signal`”，所以只能说宿主边界继续缩小，不能把 `pure_cheng_zero_c_ready=0` 说成已经完成。
- `program_support_backend_v3` 这类 v3 provider 里，只要已经导出了 `file_exists/file_size/getcwd`，就别漏 `cheng_file_mtime`。这轮 `build-backend-driver` 现场已经坐实，普通 tooling 主链会直接因为 `_cheng_file_mtime` 未定义掉链接；最稳口径就是按 [system_helpers_backend.cheng](/Users/lbcheng/cheng-lang/src/std/system_helpers_backend.cheng) 的 Darwin `stat -> st_mtimespec.tv_sec` 现成实现补齐。
- `build-backend-driver` 不要让正在运行的 ordinary driver 直接自举自己。`r2c-react-v3 status/native-gui-runtime` 这轮已经坐实，fresh 保证一旦重进 `backend_driver/cheng build-backend-driver`，最容易踩 ordinary 还没吃稳的 helper/provider 语法面；最稳口径是让 [compiler_main.cheng](/Users/lbcheng/cheng-lang/v3/src/tooling/compiler_main.cheng) 优先回 `v3CompilerBridgeCompilerPath(...)` 这条稳定 Cheng bridge。
- `build-backend-driver` 的 build plan 对拍不要再额外造字符串裁剪 helper。像这轮 [v3CompilerBuildPlanComparableText](/Users/lbcheng/cheng-lang/v3/src/tooling/compiler_main.cheng) 这种文本过滤器，只会把普通编译面平白扩成新红点；如果计划文本原文就能稳定比对，直接比原文。
- `bootstrap-bridge` 真要从 shell 收回 pure Cheng，最稳切法是“shell 只管 seed freshness + cold-start 分发，live stage 候选安装全交给 `compiler_main bootstrap-bridge`”。seed 自己的重建入口还在 C 里时，不能假装 shell 已经能完全消失。
- `bootstrap-bridge` 和 `build-backend-driver` 一样，安装必须走 candidate artifact。`stage0.next -> stage1.next -> stage2.next -> stage3.next -> contract compare -> rename install` 是当前最稳口径；直接覆盖 live `cheng.stage*` 会把 fixed point 和失败回滚一起搞脏。
- live 路径里已经死掉的宿主桥要物理删，不要只在文档里说“以后别用”。像这轮 `exec_file_pipe_spawn`，最稳收法就是同时删 `os_host_process import`、`program_support_backend export` 和 `host-bridge-audit` 放行条件。
- shell 里的 freshness helper 不能写 `[ ! "$src" -nt "$bin" ]`。这轮已经现场坐实，`sh` 会把它吃成歪表达式，旧 `backend_driver/stage3/bootstrap_driver` 会被一直误判成 fresh；最稳写法就是显式 `if [ "$src" -nt "$bin" ]; then return 1; fi`。
- ordinary `compiler_main` 里这种长串宿主命令，不要赌多行 `\"...\" + ...` 表达式在 seed/ordinary 下都能被一致发射。像这轮 `planCompareCommand`，旧写法会把 `+ v3CompilerQuoteShell(...) +` 直接打进命令文本；最稳口径是逐段 `var cmd = ...; cmd = cmd + ...` 累加。
- 用户目标已经明确是“零新增 C 依赖、只用纯 Cheng”时，不能再给 `std/os execFile` 这类缺口补 [system_helpers.c](/Users/lbcheng/cheng-lang/src/runtime/native/system_helpers.c) 新桥。正确方向是继续收 pure Cheng provider，或者把调用点直接切回仓里已有的 Cheng 实现。
- `v3/libp2p/core/peer_id` 这种协议层模块不要继续挂 `base58btc` bridge。仓里已经有纯 Cheng [multibase.cheng](/Users/lbcheng/cheng-lang/src/std/multiformats/multibase.cheng)；直接切过去以后，`backend_driver/controller` 从干净产物重建会更稳，也更符合零 C 主线。
- `native-gui-bundle` 这条链的 controller 超时不能固定写死 `30000ms`。fresh `home_default/content_detail` 已经坐实，纯 Cheng 主链正常也可能超过 30 秒；最稳口径是给足硬预算，比如这轮的 `120000ms`。
- 遇到 out-dir 内的 launcher/provider 编译红灯时，先把同一条 `system-link-exec` 命令单独抽出来重放，再 fresh 一个全新 out-dir 复验。像这轮 `publish_selector`，第一次 `native_gui_launcher_compile_failed` 并不是稳定源码错，单独重放和 fresh 新目录都直接通过，真根更像旧中间态干扰。
- `backend_driver` 已经成了真 ordinary compiler 后，`cheng_v3.sh` 的用户面编译命令就不要再默认绕回 seed `stage3`。最稳口径是把 `status/print-build-plan/emit-csg/migrate-csg/verify-world/world-sync/prove-equivalence/prove-migration/publish-world/fresh-node-selfhost/selfhost-build/system-link-exec` 一口气切到 fresh `artifacts/v3_backend_driver/cheng`，只把 bootstrap/gate 留给 `stage3`。
- `build-backend-driver` 可以正式挂进 `backend_driver`，但冷启动和日常自举必须分层。最稳口径是：已有 fresh pure Cheng driver 时直接调它；没有时先用 `cheng.stage3 system-link-exec` 冷启动编出 `cheng.bootstrap`，再把后续 `build-backend-driver` 校验和安装全部交给 pure Cheng 实现，不能再走 seed 的旧特殊命令。
- `backend_driver` 自举自己时不能原地覆盖正在运行的 `cheng`。最稳口径是先产出 `cheng.next` 和 `cheng.next.v3.map`，跑完 `status/print-build-plan/zero-exit smoke` 后再按 `rename map -> rename binary` 安装。
- 如果 selfhost/reference 的 build plan 原文已经全等，就直接比原文，不要额外造行过滤扫描器。像这轮那段自定义 plan 裁剪逻辑，只带来了 `idx=len` 的 bounds crash，没有任何真实收益。
- `r2c-react-v3` runtime 一旦 ordinary 已能直接发完整 `native_gui_runtime_v1`，就不要再保留中间 helper 形态。最稳口径是 controller 直接执行 `native_gui_runtime_compiled_main`，`status` 也同步写成 `native_gui_runtime_helper=""`；退役 helper 文件直接删，不留“以后可能还用”的活口。
- `r2c-react-v3` 这种编译链主线完成后，验收不能只看一条路由。至少要同时复验一个启动页、一个长内容滚动页、一个交互命中页；这轮最小闭环就是 `home_default/content_detail/publish_selector` 三条。
- `bootstrap_bridge_v3.sh` 这种冷启动薄壳不能只要看到旧 `cheng.stage3` 可执行就直接复用。只要 [cheng_v3_seed.c](/Users/lbcheng/cheng-lang/v3/bootstrap/cheng_v3_seed.c) 或 [stage1_bootstrap.cheng](/Users/lbcheng/cheng-lang/v3/bootstrap/stage1_bootstrap.cheng) 比 live `stage3/stage0` 新，就必须直接回 seed runner 真重建；不然 seed 里的新命令实现根本进不了 live 主线。
- `backend_driver` 一旦真改成 ordinary `compiler_main` 物化物，`print-build-plan` 对拍就不能继续做整份文本全等。bootstrap `stage3` 会天然多出 `supported_targets/target_support*` 诊断行；最稳口径是只比 `target/linker/stage2_compiler/entry/output/source[*]` 这些 build plan 核心字段。
- `build_backend_driver_v3.report.txt` 不能再把 `materialized_source` 写成 `stage1_bootstrap.cheng`。这条报告现在必须显式钉住 `materialized_source=*compiler_main.cheng`，同时给出 `reference_plan_log` 和 `zero_exit_smoke_*`，否则用户面会继续被旧 bootstrap 壳误导。
- `compiler_request.v3CompilerReadFlagOrDefault(...)` 当前不能直接拿来做 ordinary selfhost compiler 的关键命令解析。像这轮 `system-link-exec` 已现场打出过 `unsupported --emit: --root:/...`；最稳口径是关键编译命令直接读 runtime `cmdline.readFlagOrDefault(...)`。
- 当前 ordinary runtime argv 会多带一个尾部空参数。凡是要把 argv 原样转发给 bridge compiler 的地方，都必须先过滤空尾参数，不然命令面会平白漂一格。
- `std/os` 的 `execFileStatus/execFileCapture/quoteShell` 当前不适合直接做 ordinary selfhost bridge。最稳收法是本地逐段拼 shell 命令，再自己做最小单引号包裹，不要继续赌链式字符串 helper。
- 当前 `v3` selfhost compiler 最稳的形状是“本地 pure Cheng 先落 deterministic plan/report，再把真执行交给 live Cheng compiler”；不要再把命令面停在 `native link not implemented`。
- `native-gui-runtime` 这类主链一旦切掉旧 helper，不要把退役入口和只服务它的 evaluator 死代码继续留仓。最稳口径是物理删入口文件，再把 shared 里只服务旧入口的 `reduce-kv -> runtime/render-plan` JS evaluator 一起删掉，不给后面误回滚留口子。
- `r2c-react-v3 native-gui-runtime` 这条链如果目标是把 Node 从 active runtime 面上摘掉，不要只改 controller，不要只改 bundle。最稳口径是像这轮这样同时把 controller、旧 shell wrapper、fresh bundle 共同指到同一个原生 helper：原生 helper 直接执行 `native_gui_runtime_compiled_main`，JSON 透传，`reduce-kv` 则按 runtime contract 原地重建 `native_gui_runtime_v1 + native_render_plan_v1`。
- `r2c-react-v3` 原生 GUI 一旦已经把 direct runtime 收回 `cheng.stage3 r2c-react-v3 native-gui-runtime`，就不要再让 `native-gui-bundle.mjs` 继续本地重建 `native_gui_runtime_v1`。最稳口径是 bundle 只产 `runtime_contract_v1 + native_gui_runtime_compiled_main`，然后直接调 controller 取回初始 runtime JSON，并归档独立 `native_gui_runtime_main.controller.log`。
- `r2c-react-v3` 原生 GUI 如果目标是“Cheng 主控，Node 退出实时主链”，宿主就不要继续执行 launcher/helper 路径。最稳口径是像这轮这样把 `native_gui_runtime_exe_path` 直接切到 `native_gui_runtime_compiled_main`，然后让 AppKit host 原生消费 `native_gui_runtime_reduce_kv_v1`，自己按 `native_layout_plan_v1.items` 重建 `state/render_plan`。
- `native_gui_runtime_reduce_kv_v1` 这条 compiled runtime 合同里，只要 `selected_index/focused_index/visible_layout_item_count` 已经是 compiled 真值，宿主就不要再重新猜命中树。最稳口径是直接用 layout item index 回填 `selected_source_module_path/source_line/interactive`，`typed_text/last_key` 只保留事件本地增量；这样 click/focus/text/key 和 source jump 都能稳定跟 compiled runtime 对齐。
- `v3` 只要已经有 live Cheng 编译器，bootstrap/build-backend-driver/status/print-build-plan 这条 active 主线就不要再把 `seed_source` 放进用户面 build plan 或报告里，也不要在日常刷新时先回 `cc -> cheng.stage0`。最稳口径是像这轮这样切到 `v3_selfhost`：直接拿当前 `cheng.stage3`/`backend_driver` 物化 `cheng.stage0`，再重编 `stage1 -> stage3`。但在 `stage3/backend_driver` 还没脱离 seed 代码体之前，`seed_source` 仍必须保留在 freshness 依赖里，不能假装已经彻底删掉。 |
- `r2c-react-v3` 原生 GUI 一旦遇到长内容路由，不要再把 64+ 个 `R2cNativeGuiItem` 直接堆在单个 `r2cNativeGuiItems()` 里。像这轮 `content_detail` 的 97 个 item，ordinary 会从 `item_64` 开始掉 `prepare binding infer type failed / primary_object_body_semantics_missing`；最稳口径是生成分块 append helper，把每个函数里的 composite 局部变量数压在稳定范围内。
- `open-source-on-click` 这条链不能在 click 后再被 focus/key/text 事件清空状态。像这轮 `publish_selector`，真实 click 已命中 `node_13 -> PublishTypeSelector.tsx:39`，但后续 focus 又把 `source_jump_*` 擦掉，Node 最后只会看到假失败；最稳口径是宿主只在新 click 时 `clearSourceJumpState()`，非点击 runtime 事件保留最近一次 click 的 jump 结果。
- `native_gui_host_macos.m` 这类带 scripted scenario 的宿主，不能一边靠真实 `mouseDown/scrollWheel/keyDown/windowDidResize` 驱动，一边又想要确定性自动验收。像这轮 fresh `run-native-gui` 的组合场景，真 runtime 已经通了，但宿主会漂成 `resize=1`、`scroll/click/focus/text=0`、`key=3`；最稳口径是 scripted resize 直接调 runtime，并在 scripted scenario 期间屏蔽真实输入事件。
- `r2c-react-v3` 已经把 runtime executable 收回 `cheng_controller_v1` 后，别忘了 `run-native-gui` 的“自动补 session”也是主链一部分。像这轮这样直接让它调用 `cheng.stage3/backend_driver r2c-react-v3 native-gui-bundle`，比继续回 shell wrapper 再设 `R2C_REACT_V3_NO_STAGE3_HANDOFF=1` 稳得多，不然主控面会暗中分叉。
- `r2c-react-v3` 原生 GUI 如果目标是“Cheng 主控，Node 最薄 helper”，别直接拿宿主去调 Node runtime executable。最稳口径是像这轮这样先落 `runtime_contract_v1 + shell launcher`，launcher 真执行 `cheng.stage3 r2c-react-v3 native-gui-runtime`，再由 controller 调最薄 Node helper；这样宿主入口和调度权先收回 Cheng，后面再继续削 helper 本体。
- `replay-hit-inspector` 这种二次消费 inspector 状态的入口，不能偷吃第一次 live 点击时临时拼出来的 `source_jump_*`。最稳口径是让 `native_gui_inspector_state_v1.json` 自己固化 `target_path/target_line`，而且直接从 `repo_root + source_module_path + source_line` 机械求值；这样 replay 和 live 点击走同一份真状态，不会因为第一次没开 `--open-source-on-click` 就断链。
- `r2c_react_v3_controller_main.cheng` 这类 live tooling controller 里，路径父目录别再调 `os.dirName(...)`。这轮已经坐实它会把 `emit composite binding` 直接打炸；最稳口径是统一走 `v3path.v3PathParentDir(...)`。
- `v2` 这条还会被 stage0/bootstrap C 直接吃到的 tooling helper，别在字符串拼接或 `add(...)` 参数位上写内联 `if ... else ...` 表达式。像这轮 `buildNetworkSmokeReportText(...)` 里的 `if stages.len > 0` 就会被 live `cheng_v2c` 直接打成 `unsupported if-expr`；最稳口径是先落局部变量，再拼最终字符串。
- 原生 GUI 真要做“点了就跳源码”，不要让 Node/helper 重新猜路径或补一条平行跳转链。最稳口径是宿主直接吃 `repo_root + source_module_path + source_line`，由 AppKit host 自己调 `xed --line`，再把 `source_jump_*` 状态原样回传给 helper/report/inspector。
- 收 C 依赖时，先看“合同里还点不点名它”，再看“真实 source list 和 live runtime 还会不会编它”。像 `system_helpers_ffi_raw_bridge.c` 这种只剩 ABI 文本残影、执行面已经完全不走的文件，最稳收法就是先从合同里摘掉，再物理删除文件；不要继续留着假 live 状态。
- 原生 GUI 真想继续做 inspector，不要只把命中结果散落在 `summary.env` 和 report 字段里。最稳口径是直接落独立 `native_gui_hit_inspector_v1.json`，把 `layout_item/source_node/style_node` 一起固化；这样后面做源码跳转、白盒调试或原生 inspector 面板时，不需要再二次回拼。
- 原生 GUI 真想做“从画面回源码”，`source_node_id` 不够。最稳口径是把 `module_path/component_name/line` 直接带进 `native_layout_plan_v1.items`，让 host 点击时原样回传；这样后面做源码跳转或 inspector，不需要再回 Node 猜一次。
- 旧 `cheng.stage0/stage3` 如果是按相对 `CHENG_V3_IMPL_SOURCE_PATH` 构建出来的，`bootstrap-bridge` 生成的 `stage1.generated.c` 会在 `artifacts/v3_bootstrap` 目录下找不到 `./v3/bootstrap/cheng_v3_seed.c`。最稳恢复法是先直接用 [cheng.bootstrap_bridge_runner](/Users/lbcheng/cheng-lang/artifacts/v3_bootstrap/cheng.bootstrap_bridge_runner) 跑一次 `bootstrap-bridge`，把 stage3 拉正后再回 `bootstrap_bridge_v3.sh` / `build_backend_driver_v3.sh` 官方入口。
- 用户当前主线是“移除脚本和 C 依赖”，排障时不要被 `compiler_main` 里历史残留的远程验证、跨机 smoke 之类侧线带跑。最稳顺序是先把这类残留从 live 入口和可编译集合里切掉，再回到 `stage3/backend_driver/system_helpers` 主线。
- 原生 GUI 一旦进入真实滚动，不要再让 session 只塞 `viewport_items`。最稳收法是 session/host 真吃 full `native_layout_plan.items`，宿主自己按 `scroll_offset_y` 做裁剪、可见项统计和命中测试；否则滚动和 source-node 回传永远会漂。
- `native_layout_plan` 的布局常量不能继续硬编码在 Node helper 里。最稳口径是像这轮这样由 Cheng preview 流直接下发 `layout_policy_source/layout_*`，Node 只做机械 plan 生成；这样后面把布局求解继续收回 Cheng 时，不会留下第二份真值。
- 原生 GUI 从 `style_layout_surface_v1` 再往前推时，别继续把坐标/截断/滚动这些临时逻辑塞在 `native_gui_session_v1` 里。最稳收法是先独立落 `native_layout_plan_v1`，把 `item_count/viewport_item_count/clipped_item_count/scroll_height` 一起写死，再让 session/host 只消费 plan。
- Cheng QUIC 的 client `dial`/`pipe read` 不能继续在非阻塞 UDP `queue empty` 上空转 5 秒。像这轮 [native_runtime.cheng](/Users/lbcheng/cheng-lang/v3/src/quic/native_runtime.cheng) 的 tight loop，单次 `probe-proposal` 失败握手就能冲到约 `1.3GB` 峰值；后续所有等待收包的热路径都必须带明确节流或真正的超时阻塞收包。
- `deploy-bft-validator-three-node` 这类会起 detached follower 的入口，不能在公网 proposal 口还没证实能完成 QUIC 握手前就直接放后台。最稳口径是先前台 probe，再起 follower；握手不过就 fail-fast，不允许后台重试链条继续吃机器资源。
- `msquic_chain_smoke` 这类会落本地块目录的 smoke，不能继续复用固定 artifact root 再赌 `std/os removeDir` 能清干净。最稳口径是先按 `base/_1/_2...` 选 fresh root，再把 reset 失败当硬错误；否则回放最容易被旧 journal 里的重复合法事件误导成“共识错了”。
- `compiler_core` 的 tooling/program 两条执行轨不能共用同一套 native support source 列表。普通程序如果还带 `v2/bootstrap/cheng_v2c_tooling.c`，会白白重编大 C；如果去掉后不同时把 [system_helpers_tooling_entry_bridge.c](/Users/lbcheng/cheng-lang/src/runtime/native/system_helpers_tooling_entry_bridge.c) 编成 `CHENG_TOOLING_ENTRY_NO_BOOTSTRAP` stub，又会直接 unresolved。最稳收法就是“tooling 轨保留 bootstrap tooling，program 轨 stub 化 tooling entry”。
- `quic_tls_transport_ecdsa_smoke` 不能在同一进程里对同一张叶子证书连续做两到三遍 ECDSA/X509 验签。当前实现下这会把内存和时延一起放大；最稳口径是保留“原始签名解码检查 + 一次正式 `x509VerifyChain`”，不要在 smoke 里重复验同一张 cert。
- `program-selfhost-check` 里 `quic` 和 `msquic` 不能共用一个拍脑袋的短超时。`quic_tls_transport_ecdsa_smoke` 很短，但 `msquic_chain_smoke` 正常跑完就明显更久；最稳收法是保留原生超时兜底，但把 network smoke 阈值提到能覆盖真实运行时间的量级，同时把关键阶段写进 stdout 合同。
2026-04-03
- 不要用长时间探测命令反复直接拉起 `v2/artifacts/bootstrap/cheng_v2_system_link_exec`。优先用 `cheng_v2c` 做只读检查；必须跑 `cheng_v2_system_link_exec` 时，只跑前台、一次性、可立即验收的命令，不留后台或悬挂会话。
2026-04-06
- 性能统计里的“第一梯队”目标，必须按同机 C 1:1 记，不准私自放宽成 `2x` 或拿特例输入伪装成常规基线。
2026-04-09
- `progress.md` 从这天起只记录开始时间；完成时间不在文档里写，由用户自行统计真实时间。历史条目不追改。
2026-04-10
- 用户明确要求文档目录用 `v3/docs`，不是 `v3/doc`；后续 `v3` 新文档一律落到 `v3/docs`。
2026-04-13
- 用户要的是“保留原叙事，再补当前实现”，不是把文档直接改写成纯事实稿。后续更新叙事型文档时，默认追加 `当前实现补充`，不要整篇覆盖。
2026-04-14
- 用户明确要求“纯 Cheng 是最终目标”时，不能把 `libc memcpy/memset/memcmp` 这类临时绕路当完成态。先用它们定位根因可以，但最后必须回到修发射器/修语义面，再把 runtime provider 退回纯 Cheng 实现。
- 用户已明确：`/Users/lbcheng/UniMaker/React.js` 后续允许按打通编译链的真实需要直接修改；但不是默认随手改，只有遇到硬阻塞才动。
- `primary object` 现阶段对大复合值最容易在“直接返回”“直接塞进 `Ok[...]`”“直接写 struct field”这三类形状上搬坏。热路径里优先改成 `into` + limb 拷贝，尤其是 `EcPoint / EcPointJacobian / BigInt` 这种大对象。
- `v3` seed 里的递归 helper 不能在栈上放 `V3ImportAlias[128]` 这种大数组。像 `v3_compute_type_layout_impl`、`v3_resolve_field_meta_impl` 这类递归函数，一旦沿着宿主大类型链展开，host compiler 自己会先栈爆；这类工作缓冲区必须搬到堆上或做外置缓存。
- `var T` 形参在 `v3` 里是“值类型为 `T`、传参 ABI 为 `ptr`、本地槽 `indirect_value=true`”这一整套语义，不能只记 `param_is_var` 却还按标量值槽落栈。否则入口会把地址截成 `bool/i32`，裸本地赋值也会改坏槽本身而不是回写到目标地址。
- 固定数组类型规范化不能只认字面量长度。像 `bool[oracle_types.v3OracleCommitteeMax]` 这种常量长度，必须先保留 `[]` 里的表达式文本，再走 top-level const 求值，最后写回成确定长度的 canonical type text。
- `consteval entry` 不能无条件吃掉 `exe` 主路径。只要 source closure 里还有顶层 `var ... = ...` 初始化，就必须保留完整 lowering 发射；不然模块 init 还会引用那些初始化 helper，最后在 native link 阶段掉未定义符号。
- failfast 并行 job 日志不能和 smoke 可执行文件共用同前缀。`run_v3_compile_exe.sh` 会做 `rm -f "$bin".*`，如果 job log 也叫 `$bin.log`，失败时外层只会看到“日志不存在”。
- ready-file 握手的等待窗必须和 watchdog 同量级；2 秒轮询在 gate 满载时会把正常 server 误判成启动失败。`chain_node/libp2p tcp` 这类双进程 smoke，ready 轮询至少要覆盖整个 20 秒 watchdog 窗口。
- 用户已明确：`cheng_v3.sh` 的主要命令能力已经收进 `cheng.stage3`。后续讨论“零脚本开发”时，不能再把 `cheng_v3.sh` 当成核心能力缺口；真正剩下的是 bootstrap 编排、并行 gate、宿主工具链和外部运行环境收口。
- `tailnet` 的 headscale probe 不能再把 `http://` 端点直接当空串；纯 Cheng runtime 需要自己做本地 HTTP GET、验状态行、切 body。否则 `tailnet_control_smoke` 这类真实流程会在 provider runtime 上静默掉回 `init`。
- `program_support_backend_v3` 里别把长串请求报文写成一条内联 `\"a\" + x + \"b\" + y`。这一形状在当前主线上会被误编成字面串，server 直接收到 `GET \" + path + \" HTTP/1.0`。请求报文要按步骤追加。
- `build_backend_driver_v3.sh` 也是并发热点。`panic/bounds/signal/debug runtime` 会同时抢 `artifacts/v3_backend_driver`；没有锁时会互相截断 `cheng.generated.c` 和输出二进制，表现成随机的 `stage2 compile-bootstrap failed`。这个入口必须像 `bootstrap_bridge_v3.sh` 一样做锁和 freshness 快路径。
- generic Linux `aarch64` 这种跨目标链路不要再靠 README 记忆判断。先真跑 `build_linux_nolibc_exe_v3.sh` / `build_chain_node_linux_v3.sh` / `build_rwad_bft_state_machine_linux_v3.sh` 看产物和 report，再把结论写回 gate 和文档，不然最容易把“已经打通”的路径继续写成旧阻塞。
- `slice-gate` 里不要把已经包含在 `run-host-smokes` 和 `run-stage23-libp2p-smokes` 里的 `chain_node process/three-node` 再手工跑一遍。重复 gate 不增加覆盖，只会把 ready/log/bin 目录复用带来的串扰放大成假失败。
- `PPID=1 && state=U` 这类 UE 孤儿进程不能只在主入口修。凡是 `v3/tooling` 里直接 `system-link-exec`、直接跑产物、或者自己起后台 server 的叶子脚本，都必须统一走 `guarded_exec_v3.py`；只修 `cheng_v3.sh`、`bootstrap_bridge_v3.sh` 这层不够，旁路脚本一样会漏。
- `bootstrap_bridge_v3.sh` 也是并发热点。多个入口一起补 bootstrap 时，如果没有锁和 freshness 快路径，会把同一个 `cheng.bootstrap_bridge_runner` 抢坏，表面症状是“missing temporary seed runner”。这个入口必须先串行化，再判断 `seed/stage1` 是否真的比 `stage3` 新。
- 用户要的是“纯 Cheng 的零脚本开发”时，`cheng_v3.sh` 和各类 `build/run/verify` 外壳只能保留参数透传，不能再承载编译、进程编排和日志校验主逻辑；这些能力必须收回 `cheng.stage3`。
- `guarded_exec_v3.py` 的 CLI 只认空格参数：`--log PATH --timeout N`，不认 `--log:PATH --timeout:N`。seed 里凡是用 `system(...)` 拼 guard 命令，都必须按这个语法写，不然 `stage3` 看起来像 bootstrap/c-ref 坏了，真根只是参数格式错。
- `bootstrap.env` 存在不代表 `cheng.stage3` 还是新的。只要 `v3/bootstrap/cheng_v3_seed.c` 或 `v3/bootstrap/stage1_bootstrap.cheng` 比当前 `cheng.stage3` 新，wrapper 就必须先回 seed 外根重建；不能看到 env 在就直接信旧 stage3。
- `v3/src/tooling/gate_main.cheng` 里的新子命令写进源码，不等于当前 `artifacts/v3_bootstrap/cheng.stage3` 和 `artifacts/v3_backend_driver/cheng` 立刻就有同名入口。只要 bootstrap 产物还没完全换代，叶子 wrapper 就必须优先复用已经公开的 `run-host-smokes --tail-only:*` / `run-stage23-libp2p-smokes --tail-only:*` 或直接编译器调用，不能先假设新命令已经 live。
- `verify-orphan-guard` 不能只扫 `.sh`。`v3/tooling` 顶层无后缀入口和 `.py` 一样会把 Python/guard 旁路重新带回主线，守卫必须按“顶层入口文件全扫”来收口。
- `bootstrap_bridge_v3.sh` 这类带锁 wrapper 不能在持锁后直接 `exec` 真命令。`exec` 会让 shell 自己消失，`trap cleanup` 根本没机会释放锁；正确形状是“写 owner pid + 等待时回收陈旧 pid + 普通子进程执行 + shell 走到 EXIT 清锁”。
- 只要 seed/stage3 的某个命令实现还是 shell out 到旧脚本，就不能把那个旧脚本同时压成“纯转发到 `cheng_v3.sh`”的薄壳。`run-host-smokes / run-stage23-libp2p-smokes` 这类总 smoke 一旦两边都变壳，就会形成 `shell -> stage3 -> shell` 递归。
- `run-host-smokes / run-stage23-libp2p-smokes` 这类总 smoke 的 live 入口当前在 `v3/bootstrap/cheng_v3_seed.c`，不是只在 `v3/src/tooling/gate_main.cheng`。要把总 smoke 真从脚本拉回编译器，必须先改 seed 并重建 bootstrap，再去压薄对应 shell。
- 总 smoke 和叶子 smoke 要分开收口。总 smoke 必须先保证只有一个真实执行体，叶子脚本才适合随后压成纯转发壳；反过来做最容易再次制造 `shell -> stage3 -> shell` 递归。
- `verify-orphan-guard` 这种门禁不要用“图省事”的宽规则。普通 `python3` 辅助脚本并不等于旧 guard 旁路；真正该禁的是 `guarded_exec_v3.py / orphan_guard_run.sh` 这类已退役守护入口和它们的直接残留引用。误把普通测试探针也算违规，只会让门禁先坏在无关处。
- Cheng 侧和 seed C 侧同时维护同名门禁时，必须防两类 bug：一类是 Cheng 侧把目录当文件读，另一类是 seed 侧脚本 passthrough 把命令再绕回 `cheng_v3.sh`。`verify-orphan-guard` 这轮已经证明，少修任意一侧都会挂死。
- 叶子脚本能不能压成纯壳，不看意愿，只看 live 命令面。当前 stage3/backend-driver 没公开的叶子入口，宁可保留真实执行体，也不能先压成假薄壳再让 gate/smoke 全面回归。
- `debug/runtime` 这条桥不能只写“已退出主链”。像 `v3_debug_runtime_shim.c` 这种死壳，只要文件还在，后面就会被重新塞回去；最稳收法是物理删除文件，再让 gate 直接验 `provider_source_paths` 和 provider object 导出符号白名单。
- `build_backend_driver_v3.sh` 这种冷启动薄壳，能直转发 fresh `stage3` 就不要再多绕一层 `cheng_v3.sh`。多一层壳就多一份 freshness/错误路径/日志行为定义。
- debug/runtime bridge 再缩时，`providerNativeSources`、`system_link_exec`、对象级 smoke 和 seed 里的 runtime source resolver 必须一起指向同一份 native 源文件；只改其中一处，桥接面会立刻分叉。
- `v3_cmd_system_link_exec` 这类 seed/backend-driver 入口绝不能把 `V3SystemLinkPlanStub / V3PrimaryObjectPlanStub / V3ObjectPlanStub / V3NativeLinkPlanStub` 这种大 plan 对象直接放栈上。Linux x86_64 默认 8MB 栈会在函数序言就撞死，表面看像 `system-link-exec` 随机段错误；这类大对象必须改成堆分配，再去看真正的 target/codegen 阻塞。
- `stage1_bootstrap.cheng`、`gate_main.cheng`、`compiler_runtime.cheng` 里的命令名补齐，不等于 live 的 `cheng.stage3` 已经有对应入口。真正的命令面以 `v3/bootstrap/cheng_v3_seed.c` 的 dispatch 为准；没改 seed、没重建 bootstrap 前，任何叶子壳都不能先压成纯转发。
- seed 里自己组 argv 跑 `execv` 时，数组最后一个槽必须显式写 `NULL`。像 `run-wasm-smokes`、`run-browser-host-wasm-smoke` 这种 Node harness，一旦漏掉结尾位，直接就是 `execv: Bad address`，不会给你温和报错。
- 长参数宿主命令不要再手写 argc 常量。`tailnet_control` 这轮已经证明，多一两个 flag 以后最容易把末尾关键参数静默截断；统一用 `sizeof(array)/sizeof(array[0])` 才是稳定写法。
- `v3/bootstrap/cheng_v3_seed.c` 自己也必须始终按严格宿主编译规则过。Darwin 下只用 `cc -std=c11 -pedantic` 编 seed 时，`strsep` 和 `st_mtimespec` 需要显式 `_GNU_SOURCE/_DARWIN_C_SOURCE`；这层不补齐，bootstrap 会先死在外根编译而不是死在 Cheng 逻辑。
- seed 里的 shell bridge helper 不能把命令构造和日志格式化混在一起。像 `v3_run_shell_command_logged(...)` 这种共享路径，只要 `snprintf(...)` 多塞一个实参，整条实际执行命令都会被污染，问题还会伪装成“下游脚本坏了”。 |
- `x509DecodeEcdsaSignature(...)` 这类固定宽度签名解码，不要再绕 `leftPad Bytes` 之类的中间拼装。当前编译路径下，ASN.1 整数值最稳的写法就是直接抄进最终固定 64 字节 `r||s` 输出，不然最容易在证书 `CertificateVerify` 上变成“材料看着都对，但签名就是 invalid”。
- ordinary `v3` smoke 里临时排障不能写动态 `echo(ErrorText(...))`。seed 只支持字面量 `echo`；要打印动态错误，就直接 `panic(ErrorText(...))` 或改成断言，不要再把编译链先打坏。
2026-04-15
- `v3` 上这类“纯 Cheng 密码学很慢”的问题，先别急着调窗口或怀疑协议。先确认 live 热路径到底有没有真走到 fixed/Jacobian/预计算主线；这轮 `P-256 verify/pubkey/generator mul` 的真根就是代码里快路径早有了，但调用还停在大整数慢路。
- 固定委员会的验证人公钥不能在验签时从私钥现算。委员会一旦固定，公钥就该当常量或配置读入；运行时重复派生既慢，也会把热路径和私钥材料绑得太紧。
- `FixedBytes32/64/65` 这类已经定长、上游已验形状的密码学热入口，不要再补一层重复 parse/shape check。多一轮就是多一轮白算，收益为零。
- 三节点 BFT bench 的结束时间要按“全网最后一次 commit 真落盘”算，最稳代理量就是三边最终 state 文件的最新 mtime；提交返回时间和外层 wait loop 结束时间都不是真实最终性时间。
- Darwin 目标的符号补名必须幂等。Mach-O 下既会遇到裸 `foo` 也会遇到已带 `_foo` 的符号；只要平台层无脑再补一次 `_`，汇编里就会出现 `__foo`，最后在 native link 阶段伪装成 provider 闭包缺失。
- 长跑 gate 里不要只会补默认 backend driver。`stage2/stage3` 这类 bootstrap 编译器路径在共享产物目录被刷新时也会暂时失效；compile helper 和 Cheng 侧 gate 都必须把“缺 bootstrap 编译器就先补 bootstrap bridge”写成正式逻辑，不然 `stage23` 会随机炸在中段。
- `cheng.stage3` 的 live 命令面最终还是以 `v3/bootstrap/cheng_v3_seed.c` 的 dispatch 为准。只改 `stage1_bootstrap / compiler_runtime / gate_main / cheng_v3.sh` 不够；新命令要想真 live，seed usage、自检 token、dispatch、实现必须一次性一起改。
- 只要 build 命令允许用户传 `out` 或 `report` 路径，就必须在 compile 前先 `v3_ensure_parent_dir(...)`。不然第一眼看上去像 codegen/linker 坏了，真根只是日志或产物父目录没建。
- 当 `host_ops` 已经接管并行和进程监管后，旧 shell helper 不能只“停用不删”。像 `failfast_parallel.sh` 这种死入口应该直接删除，并写进 `verify-orphan-guard` 禁止复活。
- debug runtime bridge 继续缩时，最稳形状不是把 live provider 再拆成多份 `providerNativeSources`，而是继续保留一份正式 provider 源文件，对内再拆 `trace/profile` include 单元。这样 `provider source path` 合同、seed resolver、system_link_exec 和对象级 gate 才不会分叉。
- `verify-debug-runtime` 不能只看 `provider_source_paths` 和导出符号。像 `runtime_debug_runtime_v3.o` 这类 bridge object，还必须再卡一层未定义符号白名单，不然额外宿主依赖会从 undefined surface 静默长回来。
- `bootstrap_bridge_v3.sh` 这类带锁 wrapper 再压薄时，允许直接调用 fresh `cheng.stage3 bootstrap-bridge`，但绝不能在持锁 shell 里 `exec`。必须让 shell 保持到 EXIT，靠 `trap cleanup` 真把锁清掉。
- bootstrap 冷启动分层要收得更准：只有 `v3/bootstrap/cheng_v3_seed.c` 比 `cheng.stage3` 新时，才需要重编临时 seed runner；如果只是 `stage1_bootstrap/compiler_main/compiler_runtime/compiler_request/gate_main` 变了，直接让 fresh `cheng.stage3 bootstrap-bridge` 处理才是最短路径。
- debug/runtime 宿主桥继续往下拆时，第一刀先拆 `host-only base`，不是先拆更多 provider 文件。把 env/path/stderr/getter resolve 这批纯宿主底座先单独收走，`trace/profile` 两块才不会继续互相夹带共享细节。
- Linux 对象门禁不能照搬 Darwin host debug bridge 的名字。当前 `aarch64-unknown-linux-gnu` 真在跑的是 `system_helpers_backend_nolibc_linux_aarch64.o`；要卡 Linux 依赖面，就必须直接检查这份 runtime object 的未定义 syscall 集合。
- `bootstrap_bridge_v3.sh` 继续收薄时别忘了现成的 `cheng.stage0`。只要 `stage0` 还是同代 seed 产物，就应该优先直接复用 `cheng.stage0 bootstrap-bridge`，不要每次都回临时 seed runner。
- 试过但没接进 live dispatch 的 helper 不能继续留在树上冒充能力。像这轮的 `profile_report.cheng/profile_report_main.cheng`，既然 `profile-run` 最终没走它们，就必须连同 contract、build plan、README 一起撤回真实口径。
- profiler 最稳的职责边界是：runtime 只产 `v3_profile_raw_v1` 原始样本，最终 `v3_profile_v1` 由编译器侧汇总；不要把热点聚合和报告文本格式重新塞回 live debug bridge。
- `x86_64-unknown-linux-gnu` 的字符串特判不能偷懒走通用临时寄存器。像 `str == str`、`str + str` 这种直接 call native bridge 的热路径，caller 必须按 SysV 真把参数地址放进 `rdi/rsi/rdx`，`sret` 也要占掉第一个整型参数位；只把地址塞进 `rax/rbx` 再 call，会在 provider 里伪装成“str bridge 自己崩了”。
- `v3_join_path(out, ..., out, suffix)` 这种原地拼接必须原生 alias-safe。只要 helper 直接 `snprintf(out, "%s/%s", out, suffix)`，产物路径就会被截成 `/<suffix>`，最后把 `.o/.exe` 直接写到文件系统根目录。
- 远端换 seed 不能只同步 `v3/bootstrap/cheng_v3_seed.c`。`v3/bootstrap/stage1_bootstrap.cheng` 不一起同步时，`cheng.stage0` 自检会直接报 `supported_commands drift`，看起来像 bootstrap 坏了，真根只是 seed/stage1 版本不一致。
- 远端验 backend driver 不能只看脚本打印 `ok`。这条链有 freshness 命中旧产物的可能；要确认真换代，最稳做法是删掉 `artifacts/v3_backend_driver` 再重建，并看产物时间戳或直接跑 x64 smoke。
- 多机 `chain_node` 部署时，先分清“进程在 `LISTEN`”和“对端真的能拨进来”不是一回事。像这次 64 上的 `ufw default deny incoming`，会让 dmit 的 `sync-once` 直接卡死在 TCP 收包；不先查端口放行规则，很容易把网络面误判成 Cheng/runtime 问题。
- `verify-debug-profile` 不能再一边说“runtime 只产 raw”，一边在 Cheng gate 里直接吃 `CHENG_V3_PROFILE_OUT`。只要 gate 还绕回 C 侧最终报告，profile 主线就没有真收口；最稳形状是 runtime 只写 raw，编译器和 gate 都统一走 live `profile-report`。
- `bootstrap_bridge_v3.sh` 这种冷启动壳不要再自己维护第二套 freshness 规则。壳只该负责选择现成 `stage3`、退到 `stage0`、最后才回临时 seed runner；真正的“哪些源码变了要不要重建”必须只留在 `stage0/stage3 bootstrap-bridge` 本体里。
- profile 这条线要真收口，不能只改 gate，还要把 live debug bridge 里的 `CHENG_V3_PROFILE_OUT` 和最终 `v3_profile_v1` 聚合一起删掉。runtime 只保留 raw，最终报告永远交给 live `profile-report`。
- `cheng_v3.sh` 这类薄壳不要再维护一长串逐条转发的 `case`。最稳形状是“特殊入口 + contract 命令组 + plain stage3 命令组”，这样命令面缩短后反而更不容易和 live stage3 dispatch 再漂。
- debug bridge 依赖面收缩后，`verify-debug-runtime` 的未定义符号白名单也要同步缩。像这轮删掉 final profile 聚合后，Darwin 白名单里的 `_qsort/_realloc/_strcmp` 如果不一起删，gate 会把正确收口误判成失败。
- `profile-report` 这类 tooling 一旦已经坐实在当前 seed/stage3 的 live 命令面里，就不能再额外留一条“编 helper -> 跑 helper”或“只在 compiler_main 留假 dispatch”的旁路。真正稳的形状是只保留一条当前命令面里的 live 实现，再把 freshness、锁和 README 全压回这条主链。
- `compiler_main.cheng` 里多一条命令 dispatch，不代表当前 `cheng.stage3/backend-driver` 真的已经有这个 live 命令。判断命令面是否已上线，必须先看 `v3/bootstrap/cheng_v3_seed.c` 的真实 dispatch，再看 fresh 产物能不能直接调用成功；做不到这两点时，`compiler_main` 里的同名入口就是假路径，必须删掉。
- `system_helpers_debug_trace_profile.c` 收掉旧依赖，不等于整条宿主链已经收口。只要共享宿主大 runtime `src/runtime/native/system_helpers.c` 还保留 `.v3.map` 文本加载或 `CHENG_V3_PROFILE_OUT` 最终报告，backend-driver 和旧 host runtime 仍会背着第二套依赖面；这类收口必须同时改 shared runtime 和专用 bridge，再用 gate 把旧词面直接卡死。
- `system_helpers.c` 和 `system_helpers_debug_trace_profile.c` 的收口不能只看“都删了旧词面”。只要 shared runtime 还手抄一份 trace/profile，本质上就是两套 live 实现，后面一定再漂；最稳做法是让 `system_helpers.c` 直接复用同一份 `system_helpers_debug_trace.inc / system_helpers_debug_profile.inc`，把旧本地实现物理删掉。
- `system_helpers.c` 和 `system_helpers_debug_trace_profile.c` 共享了 `trace/profile` 还不够。只要 `register_line_map` / `dump_backtrace` 入口桥还各留一份，后面改 `argv0/self-path/init` 时还是会再漂；最稳做法是再抽一层 `system_helpers_debug_entry.inc`，并让 gate 直接检查两边都包含它。
- `system_helpers.c` 和 `system_helpers_debug_trace_profile.c` 连 `entry bridge` 都共享以后，`host-only base` 也必须跟着共享。`env/path/write/getter resolve` 这层如果还各留一份，后面只要改一边，另一边就会继续漂；最稳做法是两边都直接包含同一份 `system_helpers_debug_host_base.inc`。
- 大改 seed/runtime 之后，验 `verify-debug-runtime / verify-debug-profile / host smoke` 不能只复用现成 `artifacts/v3_bootstrap` 和 `artifacts/v3_backend_driver`。这两份生成物命中旧版本时，症状会伪装成 ordinary fixture 编译红灯；最稳做法是直接删生成物强制重建再验。
- `host-only base` 共享以后，gate 也要跟着锁死 `system_helpers_debug_host_base.inc`。只查旧词面和 `entry.inc` 不够；如果不把 `host_base.inc` 也写进门禁，后面很容易又在 `system_helpers.c` 里手抄回第二份 env/path/write/getter resolve。
- 远端同步源码别再用 `rsync --relative` 搭绝对路径。它会把文件落到错误的嵌套目录里，表面上“同步成功”，实际远端还在编旧源码。多机修补时直接把文件推到准确目标路径。
- 文本 state 文件的 key 前缀长度必须按真实字符数切，不能手抄偏移。像 `last_voted_proposal_hash=` 实际是 25 个字符，写成 26 会直接把 64 位 hex 砍成 63 位，状态文件当场报废。
- `tcp_transport` 里把 `bytesToString(payload)` 这种大二进制字符串直接塞进 `importc` 调用不稳。proposal 这种小 payload 可能碰巧过，commit 这种大 payload 会把 bridge 调用搬坏；正确形状是先落本地 `str` 变量，再把变量传进 native bridge。
- `system_helpers.c` 和 `system_helpers_debug_trace_profile.c` 不能只共享 helper 实现。只要 crash trace、embedded line-map getter、raw profile 的常量、typedef 和 static global 还各写一份，状态面后面一定再漂；最稳做法是连这层也抽成同一份 `system_helpers_debug_state.inc`，再把它写进 `verify-debug-runtime` 硬门禁。
- `profile` 收口以后，`crash` 也不能继续让 C runtime 直接吐最终 `[cheng-v3] v3_crash_report_v1`。只要 runtime 还负责最终崩溃文本格式，seed/stage3 的 live 命令面和宿主桥就还是两条语义；最稳做法是 runtime 只写 `v3_crash_raw_v1`，最终报告统一交给 live `crash-report`。
- 删 `v1/v2` 旧代码不能按“`v3` 没 import”直接判断。先看全仓引用闭包；像 `src/std/system.cheng`、`src/std/cmdline.cheng` 这种 shared std 模块虽然不在 `v3/` 目录下，实际仍被 `v3` 主线大量依赖。能一口气删的，优先是全仓零引用的 tracked 生成物，比如 `v2/cheng-quic/*.tmp.linkobj` 和根目录那些 Mach-O probe。 |
- 当前仓库没有 tracked `v1/` 树；`v2/tests/contracts/*.expected`、`v2/tests/contracts/*probe.cheng` 和 `v2/vendor/cheng-quic` 这类文件看着像“旧东西”，但不少仍被 `v2` bootstrap/tooling 直接引用或作为 live symlink 存在。清理 `v2` 时先看整仓闭包，不要按文件名主观删。 |
- 这个仓库里没有 `v2/`、`v3/` 前缀或目录归属的 shared 代码，命名口径上就按 `v1` 看；但“算 `v1`”不等于“现在能删”，删除还是必须看全仓 live 闭包，尤其是 `src/std/*` 和 `src/runtime/native/*` 这批共享底座。 |
- 清 `src/` 下的 `v1` 旧代码时，最稳标准是两道门：一是不在 `v2/v3` import 图里，二是全仓没有显式路径引用。满足这两条的内部模块可以直接删；`src/std/*`、`src/runtime/native/*`、`src/web/*`、`src/tests/*` 这种共享或外部面不要和内部旧编译链一起一锅端。 |
- 清 `src/std/*`、`src/web/*` 这类共享面时，还要再补第三道门：模块名精确 import 也必须为零。只查全路径会漏掉 `import std/syncio`、`import std/net/stream/connection` 这种真实引用；删共享模块前先查 `^import <module>(\\s|$)`。 |
- `ssh host sh -lc '... sh -lc '\''echo $$ > pid && exec daemon'\'' &'` 这种远端后台形状不稳，外层 shell 很容易抱着后台 job 不退，最后本地 deploy 一直卡着。Linux 远端守护进程要用 `setsid -f sh -lc 'echo $$ > pid && exec ...'`；Darwin 本机没有 `setsid` 命令，本机后台再单独保留 `sh -lc ... &` 形状。 |
- `validator-follower-daemon` 里不能只把 `recv proposal/commit` 的网络超时当可重试；`send vote` 走的也是 `request-response`，一样会碰到 `v3 libp2p tcp: recv response: Operation timed out`。这类错误如果直接返回 fatal，本机 follower 会停在 `last_voted_height=1` 但永远不 apply commit；`sendVote` 也要走同一套 retryable 判定。 |
- `src/web/examples` 这类目录就算仓内零 import，只要 README 还把它当公开示例面暴露，也不能按死代码直接删。示例面和内部模块要分开裁决。 |
- `.meta` 这类 sidecar 不能只看 import/path 引用；像 wrapper 里按 `$0.meta` 读取的文件，本身就是 live 合同，删之前必须先查调用脚本的运行时约定。 |
- 删 `src/backend/tooling` 下的 sidecar/stage0 旧 C 文件时，至少同时过三道门：basename 零引用、全路径零引用、没有通配构建入口把它们扫进 native 源集。三道都过了才算真死代码。 |
- 像 `backend_driver_uir_sidecar_runtime_compat.c` 这种只剩 README 或 PURE-01 当“禁止回流 token”提到的旧 compat 文件，源码本体和禁词门禁要分开处理：源码该删就删，禁词扫描继续保留，别因为还要防回流就把死文件硬留在树上。 |
- 清共享模块时，basename 模糊命中不能当 live 证据。像 `sets.cheng` 很容易误撞 `hashsets.cheng`；要删 `src/std/*`、`src/web/*` 时，优先看精确 import、精确路径和 README 公开面。 |
- `src/web/cli/*` 和 `src/web/examples/*` 这类目录，就算仓内零 import，只要各自 README 还把它们当公开 CLI / 示例入口，也要按对外 surface 保留，不能和普通共享模块一锅端。 |
- 删 `src/std/*` 里的 shared 模块时，别只删源码本体。像 `std/c`、`std/hashes`、`std/system_c`、`std/syncio` 这种旧模块，常常还在 `stage1` 路径特判或 `verify_std_layout_sync` 清单里留着名字；源码和这些陈旧特判必须一起删。 |
- 清 `src/backend/tooling/*.cheng` 不能沿用 C sidecar 的“basename/path 为零就删”。像 `backend_driver_runtime_keepalive.cheng`、`backend_driver_uir_exports_keepalive.cheng` 这种文件，虽然没有路径字符串命中，但会被 `import backend/tooling/...` 直接吃进去；这一层必须改用“精确模块 import + 精确路径 + basename”三道门。 |
- 缩 `src/runtime/native` 时，最稳模式是“保留符号不变，把多个 live 小 bridge 收成少数正式 provider `.c`，旧实现退成 `.inc`，再一次性切换 `v2/bootstrap/tooling/current runtime` 三条 source list”。不要继续让几十个小 bridge `.c` 在不同 source list 里各自漂。 |
- `selflink_cmdline_bridge`、`selflink_str_eq_bridge` 这类只被 live source list 当 provider 引入、实现又很稳定的小桥，不该继续各占一个正式 `.c`。直接收成同一个 `selflink_support_bridge.c`，旧实现退 `.inc`，source list 最不容易再漂。 |
- `selflink_exe_entry_bridge`、`selflink_program_exe_entry_bridge` 这类“只有 track 不同、符号和语义固定”的入口桥，别用 runtime 分支去合并。最稳做法是一份 `system_helpers_selflink_entry_bridge.c` 加编译宏，在 program track 编译时显式传 `-DCHENG_SELFLINK_PROGRAM_ENTRY=1`，让 tooling/program 选择继续停在编译期。 |
- 如果某个 native sidecar 的符号已经完整并进 live runtime，本体却还只被验证脚本或嵌入脚本单独引用，那它就不是正式 provider 了。像 `system_helpers_float_bits.c` 这种文件，最稳做法是删源码，并把验证层直接改回 live `system_helpers.c`。 |
- 拆 `system_helpers_stdio_bridge.c` 时，第一刀先抽“只读 machine-target 常量桥”，不要一上来碰 `driver_c` 解释器主循环或状态面。这一簇只依赖 `ChengStrBridge + driver_c_str_eq_raw_bridge`，最适合先独立成正式 provider，再一次性追平 `runtime_provider_object_v2 / system_link_exec_v2 / cheng_v2c_tooling` 三条 live source list。 |
2026-04-15
- `deploy-bft-validator-three-node` 这类真机入口里，远端 `build-bft-state-machine` 不要再强绑 `BFT_STATE_RUN_SELF_TEST=1`。纯 Cheng `P-256 verify` 在远端主机上会把 build 自测拖成假卡死；deploy 应该拿后面的三节点实块收敛做真验收。
- 本地长期 daemon 不能再靠 `nohup sh -lc ... &`。这层壳在宿主下会把子进程一起带走；最稳做法是 seed 里原生 `fork/setsid/exec`，直接写 `pid/log`。
- 三节点 deploy 的收敛轮询必须把 remote/local query 失败当作“本轮未收敛”，不是立刻向 stderr 喷硬错误。偶发 SSH 抖动会让已经成功的部署看起来像失败。
- `deploy-bft-validator-three-node` 这条链不能在中途看到一次 `height=0` 就先判死。按当前 `proposal/vote/commit` 窗口配置，fresh `stop -> deploy` 常常要走完整轮窗口才会一起落到 `height=1`；验收要看 deploy 最终输出或随后单独 `status`。
- `64 / dmit / 本机` 这条 `v3` 三验证人链当前已经再次真对齐到 `height=1`、`alice=67`、`bob=33`、`app_hash=1db83b23eb2b038ccb6dafe2a307906cbcf7e8d683fcfebff7b92d0e873aae6d`。后面再收口时，重点放在 deploy 编排和性能，不要再怀疑账本或签名主语义是否能通。
- `v3` 当前 ABI 下，`importc/@exportc` 热路径不要再让 `str` 走 `var` 出参回写。`tcp_transport <-> runtime` 这轮已经验证：哪怕只剩一个 `var str`，也会在 bridge 回写阶段段错；最稳口径是“桥只返标量，字节结果改走 handle/slot 读回”。
- `program_support_backend_v3.cheng` 里 TCP server 侧的局部 helper 也别再用 `var str` 在函数间回写 `request/err`。就算不跨 C 边界，最稳形状也还是“当前函数本地槽直接消费”。
- Cheng 里带索引的边界判断不能赌 `&&/||` 会短路。像 `if !atEnd && text[i] == '\n'` 这种写法，在空串场景照样可能摸到 `text[0]`；涉及 `xs[i]` 时直接拆成显式 `if !atEnd:` 分支。
- proposer 先发 proposal、再开 vote capture 的双阶段循环里，follower 不能把 `sendVote` 失败当成“下轮再重新收 proposal”就完事。最稳做法是拿着同一个 proposal 短时重试发 vote，跨过 phase 切换；否则会稳定形成 proposer 空等票、follower 重拿 proposal 的活锁。
- `v3` seed 里凡是“已经是 shell 命令字符串”的入口，不能再走 `v3_run_command_logged_argv -> v3_run_shell_command_logged` 这条“双重 quoting”链。像 BFT 的 remote/local shell runner，这样会把 `ssh host sh -lc ...` 重新包坏；正确做法是直接把完整 shell 命令交给 `v3_run_shell_command_logged`。
- `pkill -f` 做 daemon 停机时，模式不能直接抄完整命令名，不然很容易把正在执行 stop 的 shell 自己一起命中。最稳形状是用子命令加 state/file，并把首字母写成 `[x]` 这种不会自匹配的正则。
- 三节点 BFT 压测要算“全网最终 commit 时间”，结束时间不能拿提交返回时间凑。当前最稳口径是取三边 state 文件或最终落盘文件里最晚的 mtime，再和 submit 起点做差。
- validator host 的 proposal `preview/apply` 不要先 decode 成 `V3BftTx[]` 再交回状态机。同一批 `proposal.txs` 走 `FinalizeBlockBytesSummary(...)` 一次做完才是热路径最短形状。
- 只要 seed 的 logged command helper 会去跑 `ssh`，child stdin 就必须先接 `/dev/null`。否则链已经收敛了，CLI 仍然可能卡在最后一条 `show-state/query-balance` 上不退。
- 远端非交互运维命令统一用 `ssh -n -o BatchMode=yes`，不要再赌默认 SSH 会自动像批处理一样收尾。
- BFT deploy/bench 的默认间隔和窗口不要散落硬编码。固定委员会链当前稳定值要作为 config 字段显式写死：`20/20/1000/1000/1000`，`default_max_txs=128`。
- `system_helpers_stdio_bridge.c` 继续往下拆时，第二刀优先抽 `path/file/plan` 这类纯工具桥，不要先碰 `run_stage_selfhost_host`、`compiler_core_local_payload` 这些挨着 selfhost 和解释器状态的大入口。判断标准很简单：依赖闭包如果还停在 `path/file/text` helper，就适合独立成 provider。 |
- `system_helpers_stdio_bridge.c` 第三刀最稳的是先抽 tooling entry：`print_usage/print_status/tooling-selfhost-check/program-selfhost-check/compiler_core_tooling_local_payload` 这簇只依赖 CLI argv、弱导入 tooling handle 和很薄的 payload label 解析；真正还绑着 `driver_c_prog_current_frame`、`abort_with_label_output`、`build_stage_artifacts` 的 selfhost 主入口要继续后放，不能混着拆。 |
- `system_helpers_stdio_bridge.c` 第四刀可以再抽 selfhost host 主入口，但 `driver_c_die_errno`、`driver_c_parent_dir_inplace` 这类程序运行时 builtin 仍要留在 `stdio bridge`。它们虽然贴着 selfhost 代码，真实依赖面却还在 `driver_c_prog` 主链里，不能按位置顺手搬走。 |
- `system_helpers_stdio_bridge.c` 每切一刀都先单编译剩余 `stdio bridge`。这轮最先暴露的问题不是 source list，而是 runtime helper 被抽空；先过单编译，再跑 backend driver 和 host smoke，定位会快很多。 |
- 抽 `system_helpers_stdio_bridge.c` 里的宿主弱桥时，定义可以搬走，但原型还要留在 `stdio bridge` 顶部。程序运行时 builtin 还会直接调用 `cheng_fopen/cheng_fclose/cheng_fread/cheng_fwrite/...`；不补前置声明，C99 会先在剩余文件里炸隐式声明。 |
- `cheng_fopen/get_stdin/get_stdout/get_stderr/cheng_fclose/cheng_fread/cheng_fwrite/cheng_fseek/cheng_ftell/cheng_system_entropy_fill/cheng_fflush/cheng_fgetc/cheng_file_exists/cheng_file_size/cheng_dir_exists/cheng_mkdir1/cheng_file_mtime/cheng_getcwd/cheng_rawbytes_get_at/cheng_rawbytes_set_at/driver_c_read_file_all` 这一簇宿主 `stdio/fs` 弱桥和 `driver_c_prog` 状态解耦，适合作为 `system_helpers_stdio_bridge.c` 的第五刀正式 provider。 |
- 当 `system_helpers_stdio_bridge.c` 剩余内容已经基本只剩 `driver_c_prog` 解释器和 program 入口时，不要再保留这个旧 provider 名。直接把 live provider 正式换成 `system_helpers_program_runtime_bridge.c`，再把旧 `system_helpers_stdio_bridge.c` 降成 fail-fast stub，最容易把陈旧 source list 当场炸出来。 |
- provider 正式换名前，先用 `rg` 把 live 代码面旧路径清零；换名后再单编译新 provider、单编译 `cheng_v2c_tooling.c`、重建 backend driver、跑 host smoke。这样能确认不是“只改了名字，主链还在吃旧路径”。 |
- 三验证人链如果只提供 latest commit 而没有 commit 校验过的 state sync，follower 只要漏掉一个高度，就会永久卡在 `proposal height mismatch`。这类恢复路径不能靠放宽窗口，必须补一条“latest quorum commit + machine snapshot 一致性校验”的状态追平主线。 |
- `query-summary` 这种单 state 单命令摘要口必须优先于 `show-state + 多次 query-balance` 拼装。多机 deploy/status/bench 轮询里，少一轮 SSH 就少一层抖动和假红灯。 |
- seed 里长 `snprintf` 命令串一旦新增 flag，第一件事不是相信肉眼，而是重新对 `%s/%d` 个数和参数个数。少一个实参时，最容易出现“编译通过、deploy 在起 daemon 前静默坏掉”的假挂死。 |
- 验 state-sync 恢复不要一上来整轮重跑远端 bench。最快、最真、最稳的验收是：先让三边收敛，手工杀掉一个 follower，让另外两边继续推进几个高度，再重启 follower，看它是否现场打出 `synced_height=` 并和另外两边重新一致。 |
- `state-sync` 这类文本载荷别再写内联 `"prefix" + x + "suffix"`。当前主线会把它误编成字面串；最稳口径是局部 `out` 分步追加。
- `machine_hex=` / `error=` 这类固定前缀 payload，slice 起点一定要按字节数写死后再数一遍。`machine_hex=` 是 `12`，写成 `11` 会把 `=` 一起带进 hex。
- 远端 `x86_64` 二进制用 `backend_driver` 重编前，先同步刚改过的 Cheng 源。否则远端编出来的还是旧逻辑，probe 看起来像“二进制没换”，真根其实是源码没过去。
- 三验证人 deploy 主线里的 Linux `x86_64` 产物，不要再让 `64` 自己走 remote `bootstrap/stage3 build`。最稳做法是只在 `dmit` 上用 Linux `backend_driver` 构建一次，再由本机 `scp -3 -B` 转拷到 `64`。
- seed/runtime 里只要编译器已经明确报“数组下标越界”，就按真实栈踩坏点处理，不要把它当噪音 warning。像这轮 `inputs[5]` 写进 `const char *inputs[5]`，线上现象就会直接变成 `*** stack smashing detected ***`。
- relay fresh build 要追当前源码时，别继续拿远端旧 `cheng.stage3` 当入口。先同步最新 `cheng_v3_seed.c`，在远端现编一个临时 seed runner，再用它执行 `build-backend-driver`，这样构建入口和当前补丁才是同一份源码。
- 三验证人链如果要求 fresh baseline 稳定落到 `height=1 / alice=67 / bob=33`，初始 `submit-mint + submit-transfer` 就必须在 proposer 启动前入队。先起 proposer 再写队列，会把第一块漂成空块，后面所有“高度=1”验收都会假失败。
- deploy 和 bench 不要各自维护一套 baseline 常量。`height=1`、`alice=mint-transfer`、`bob=transfer` 必须收成同一个 helper；只要一边硬编码、一边读 config，后面改 `mint_amount/transfer_amount` 时就会炸出假的 baseline 失败。
- 记录 bench 数字时，只认 fresh 前台重跑出来的最新结果。像这轮 `200 tx / 32 max_txs` 已更新成 `elapsed_ms=55973 / tps=3.573 / throughput_bytes_per_sec=114`，旧值不能继续沿用。
- `path/file/plan` 这类 helper 真想从 live C provider 里撤掉，不能只改 `.cheng` 源，还要先查 [v2/bootstrap/cheng_v2c_tooling.c](/Users/lbcheng/cheng-lang/v2/bootstrap/cheng_v2c_tooling.c) 的真实依赖面。这轮验证它只剩 source list 和 symbol whitelist，没有深层调用；最稳顺序就是“先把 Cheng helper 补齐，再删 live source list 和白名单，最后把旧 provider 降成 fail-fast stub”。
- `v3` 里继续把路径/文件 helper 收回 Cheng 时，先拿真 smoke 验，不要看到 `std/os` 现成 API 就直接全量替换。像这轮 [v3/src/tooling/path.cheng](/Users/lbcheng/cheng-lang/v3/src/tooling/path.cheng) 用 `os.fileExists(...)` 后，`compiler_runtime_smoke` 没炸，但 [run_v3_chain_node_cli_smoke.sh](/Users/lbcheng/cheng-lang/v3/tooling/run_v3_chain_node_cli_smoke.sh) 立刻暴露 seed ordinary lowering 还吃不稳这个形状；当前最稳写法是改成 `os.openRead + os.close` 这条已经在 `std/os.readFile` 主链里长期跑过的闭包。 |
- `std/cmdline` 适合作为 `readFirstFlagValue` 的统一入口，但别把 `parseInt32` 想当然带进所有 ordinary fixture 编译链。`tailnet_control_core` 这轮已经证明，seed ordinary fixture 仍可能在 `std/cmdline::cmdIsSpace` 的 `char` 条件上直接炸；当前最稳口径是“flag 读取统一走 `readFirstFlagValue`，整型解析在调用侧本地实现”。
- 原生 GUI 主线别等完整 CSS/layout 引擎都齐了才给可视化语义。先把 JSX 里静态可见的 `className/attr` 抬成 `layout_role/style_traits/detail_text`，马上就能把“纯标签树”推进成“样式感知 surface”，这是当前最短真路径。 |
- 原生 GUI 从“样式感知 surface”再往前走时，别把更多临时展示字段继续塞回 session。最稳收法是先落独立 `style_layout_surface_v1`，把 `visual_role/density/prominence/accent_tone/row_height/...` 收成正式 IR，再让 session/host 只消费这份 IR。 |
- 原生 GUI 新 IR 一旦落库，别忘了把 `run helper -> wrapper report -> cheng.stage3/backend_driver report` 三层一起追平。只补 bundle summary 不够，live 报告缺字段时，用户会直接把它当成“主线还没真接上”。 |
- `chain_node_cli_*_main` 这种 wrapper 一旦已经有 [chain_node_cli_core.cheng](/Users/lbcheng/cheng-lang/v3/src/project/chain_node_cli_core.cheng) 的正式 `Cmd*Main/Run`，就不要继续在每个入口里复制 `--state/--host/--port` 读参和报错逻辑。复制面一大，像 `--amount` 这种 `var out` 签名错位就会长期潜伏；最稳收法是 wrapper 直接回 core，只给确实需要不同输出格式的入口留特例。 |
- `selfhost_host` 这类 provider 里，如果某个 bridge 只是在替 `std/os` 做“exec 抓输出”或“文本文件 compare”，就不要只把 Cheng 侧调用改掉。最稳收法是一口气删掉 `@importc`、native provider 实现、`system_helpers.h` 声明，以及 `machine_pipeline/bootstrap` 里的 direct-exec whitelist；这样不会留下死符号回流口。 |
- `tooling-selfhost-host / stage-selfhost-host` 这类自举 orchestration 命令收回纯 Cheng 后，第一轮红灯先拿旧 stage0 C 入口复跑一遍再判断。像这轮 `release stdout mismatch` 的真根不是新实现，而是 `v2/tests/contracts/{tooling_release_artifact,network_selfhost,tooling_selfhost,full_selfhost}.expected` 已经跟当前确定性输出漂了；不做旧入口对照，很容易把合同漂移误判成逻辑回归。 |
- 还会被 stage0 直接编的老 `v2` 文件里，列表构造优先写显式 `seqs.add(...)`，不要再偷懒写裸 `add(...)`。当前 stage0 对这类重载解析还不稳，像 [compiler_core_runtime_v2.cheng](/Users/lbcheng/cheng-lang/v2/src/runtime/compiler_core_runtime_v2.cheng) 这轮就因为裸 `add(...)` 直接卡死在 usage/help helper。 |
- 只要 helper 会递归扫源码、展开组件或组大 JSON，就不能只靠“节点数截断”碰运气。最稳口径是进程 heap 上限和 `RSS / 模块数 / 展开数 / 源码尺寸` 一起硬卡，超预算直接失败，不允许把整机拖进 swap 或 watchdog reset。 |
- `P-256` 这类固定素数模数上的逆元，不要再走通用 `bigModInvOddBinary(...)`。这轮已经实锤：`ecdsaSignBytes(...) -> nModInv(k)` 一旦落到那条旧路径，就可能不收敛并持续分配 `BigInt`，把进程顶到 `178GB+`；最稳口径是固定模数直接走 Montgomery 指数逆元，同时给通用逆元保留硬步数上限，只负责 fail-fast，不负责主路径正确性。 |
2026-04-16
- `r2c-react-v3` 原生 GUI runtime 不要继续在 ordinary executable 里硬拗 `str/composite` JSON emitter。当前 seed/ordinary 会在 `void(str)` helper、返回字符串 builder、CLI 读参这几种形状上卡 `primary_object_body_semantics_missing`，或者把表达式直接写进字面文本；最稳口径是先把 runtime executable 收成受控 `node_helper_v1`，同时继续生成 Cheng runtime source 归档，等 ordinary string lowering 修完再切回。
- 原生 GUI 事件回环验收要拆成两条：一条钉 `resize/scroll/focus/text/key -> state/render_plan`，一条钉 `click -> hit/source/inspector`。不要把所有事件都压进一条坐标链里；一旦中间先 resize 再 scroll，后面的点击坐标很容易自然 miss，最后把假 miss 当成逻辑回归。
- `msquic` listener 绑定 `udp/0` 时，真正可拨号的地址只能以 datapath 返回的已分配端口为准。像这轮 [v2/cheng-quic/src/native_runtime.cheng](/Users/lbcheng/cheng-lang/v2/cheng-quic/src/native_runtime.cheng) 如果继续把请求地址当 live listener 地址，client 就会一直向 `0` 端口发包，表面只看到 `client_hello_ok` 后静默卡死。
- UDP datapath 的 canonical 地址必须全链路统一成 multiaddress。bind 结果、recv `fromAddr`、session 里的 `clientAddrText/listenerAddrText` 只要混入 `udp://...`，后面的 `parseMultiAddress(...)` 和 peer 元数据就会漂；最稳口径是统一回 `/ip4|ip6/.../udp/.../quic-v1`。
- `driver_c_prog` 下的网络收尾路径不要偷用 `std/os` 的未公开字段。像这轮 stop/unbind 末尾才炸出来的 `module=std/os field=cheng_close_fd`，本质上就是 live program runtime API 面没对齐；关闭 FD 一律走已公开的 `udpCloseFd(...)`。
- `v3 public QUIC` 这条线上，像 `acceptPending` 这种“由下层包处理函数回写全局，再让上层 accept loop 读回来”的状态侧信道不稳，必须改成局部返回值或同调用栈内的直接判定。像这轮公网 proposal server，`server_finished_ok=true`、`accept_pending=true` 和 `data_frame side=server len=45` 都已经出现，但 `accept()` 仍不返回，说明不能继续赌这类全局槽读回。
- `udpRecvFromNonblock(...)` 这条纯 Cheng UDP 主线不能再把 `MSG_DONTWAIT` 硬编码成单一常量。像这轮 `x86_64-unknown-linux-gnu` 上把它写成 `128`，`recvfrom(..., flags=128)` 实际不会非阻塞，结果 client/server 都静默卡进阻塞 `recvfrom`，表面像 QUIC `accept/app recv/pipe_read` 全部乱了；最稳口径是把 `MSG_DONTWAIT` 收成 runtime platform export，Linux 走 `0x40`，Darwin/BSD 走 `0x80`。
- 目标只是复验 `v3 public QUIC` 的 proposal 公网读回时，不要重跑 `deploy-bft-validator-three-node`。最短真链路是直接在 `64` 用真实 `sample_proposer/proposal.hex` 起 `proposal-serve-daemon`，再让本机和 `dmit` 各自跑 `probe-proposal`；这样能直接钉住 client 读链，不会被 `dmit` 上 `build-backend-driver` 的长链噪音拖住。
- `r2c-react-v3` 的原生 GUI runtime 如果继续走 ordinary `system-link-exec` executable，当前 ordinary lowering 还吃不下这类 `str/composite` builder：状态组装、字符串累加、CLI 读参和 render plan 文本生成都会在 `primary_object_body_semantics_missing` 卡死。最稳下一步不是继续在 helper 形状上硬拗，而是把这段 runtime 上移到 tooling command surface，或者先补 ordinary 对 `str/composite` 运行时 builder 的 lowering。 |
- `v3` 这类 provider bridge 收口时，只共享 `@importc` ABI 声明，不要急着再抽“共享 wrapper 模块”。当前编译器还吃不稳“导入模块里再调 provider-exported Cheng 符号”这条链，像 `tcp_bridge_provider_shared.cheng` 一接进 live 主线就会报 `scalar call resolve failed`；最稳做法是 ABI 一处定义，Darwin/Linux final wrapper 各自在本地 provider 收口，等编译器先补这类跨模块解析。 |
- `run_slice_gate.sh` 这类长链 gate，补完新回归点以后一定要真从头跑到尾并归档完整日志。像这轮先靠全量日志才炸出 `pin_runtime_quic_smoke` 缺 `_cheng_v3_udp_platform_msg_dontwait_bridge`，修完以后又把剩余问题收敛成单独的 stage2 `libp2p_webrtc_sync_smoke`；不跑全链，红灯会一直被后面的随机 smoke 掩住。 |
- 用户目标如果已经明确是“零 C 依赖、只用纯 Cheng”，就不要再花刀去补 `system_helpers.c/.h` 这类宿主 fallback 面。正确方向是把 live 主线继续往纯 Cheng provider 收、删掉 C 依赖，而不是把两套口径都补齐。 |
- 收纯 Cheng 主线时，不能只看真正的 link input。`freshness` 输入、seed 辅助构建函数、debug gate 的旁路文件检查，只要还点名旧 C provider，就都要一起清掉；不然主线已经纯了，辅助口径还会把人带回旧结论。 |
- 纯 Cheng provider object 的验收合同，不能继续照搬旧 C provider 的“只剩公开 ABI 导出”假设。当前 primary object/provider 会把顶层 helper 和全局槽一起导出；最稳口径是公开 ABI 单独白名单，额外导出只允许模块私有前缀，未定义符号白名单也必须按真实 object 输出追平。 |
- WebRTC 这类 provider-exported bridge 只要跨 `@importc` 回传字符串结果，就不要再走 `var str` 出参回写。当前最稳口径是“bridge 直接返 buffer handle，错误文本存 runtime 槽，再由调用侧按 handle 读回”；这样能同时避开 stage2 的出参回写不稳和 stage3 ordinary 的 composite 实参缺口。 |
- 只要当前编译器还会给“同函数里的多条表达式字符串”撞局部符号，错误字面量就不能直接散写在 wrapper 分支里。最稳做法是先落本地槽，再传给后续 helper；同理，`const str` 也不要直接做 composite 实参。 |
- ordinary `system-link-exec` 的 live provider 改动，不能只改 [system_link_exec.cheng](/Users/lbcheng/cheng-lang/v3/src/backend/system_link_exec.cheng)。只要活跃 backend driver 还是从 [cheng_v3_seed.c](/Users/lbcheng/cheng-lang/v3/bootstrap/cheng_v3_seed.c) 起，它就会继续吐旧 provider 路径；最稳顺序是“源码侧 provider 先改好，再把 seed 里的 `v3_provider_source_for_module(...)` 同步追平，最后立刻重建 backend driver 并用一条 ordinary smoke 验执行面”。 |
- 当前 seed 对 `program_support_backend_v3` 这类 provider helper，仍然不稳地吃不下“多条裸函数调用语句 + 立即跟字面量拼接”的形状。像这轮 `cheng_exec_cmd_ex`，最稳写法是收成 `cheng_raw_concat_export(...)` 的值传递链，不要在 helper 里连写一串 `cheng_bytes_copy(...)` 再赌发射器能稳过。 |
- `cheng_exec_cmd_ex` 这类 provider ABI 只要还靠 `var int64` 给 ordinary/controller 回写退出码，就可能在 provider 入口直接把 `exitCode` 指针写炸；最稳口径是改成“capture 返回输出 + 单独 `last_exit_code` getter”的无出参 ABI，彻底绕开这类回写。
- `r2c-react-v3` 的 direct `native-gui-runtime` 只需要 helper 退出码时，不要再走会抓输出的 `execCmdEx`。最稳收法是 controller 直接走 `execCmdStatus(...)`，真正的 runtime JSON 只从 `native_gui_runtime_direct_v1.json` 读回。
- seed 冷启动链里的本地 C helper，不要把前置声明放到第一次调用后面。像这轮 `v3_host_target_triple()`，平时复用现成 `stage3` 看不出来，一旦 fresh 走到 `bootstrap-bridge stage0_cc` 就会直接因为隐式声明炸死。 |
- migration gate 里“预期失败才算通过”的路径，不能走会主动向 stderr 打 `failed rc=...` 的 logged runner。像 `publish-blocked` 这种 stable 拒绝校验，最稳做法是静默执行、只读 log 文本断言，不要把正常拒绝再伪装成红灯。 |
- `compile-bootstrap` 生成 wrapper 时，`seed.c` include 必须直接写绝对路径。相对 `#include "v3/bootstrap/cheng_v3_seed.c"` 会按 generated 文件目录去找，到了 `artifacts/v3_bootstrap/*.generated.c` 就天然找不到。 |
- `program_support_backend_v3` 这类 provider helper 里，只要热循环/条件分支还在 seed 发射器覆盖面内，就不要再在 `for/if` 嵌套里塞 `cheng_str_copy_cstring(...)` 这种字符串 helper。最稳口径是直接写固定字节缓冲，把 JSON 转义这类逻辑收成纯字节拷贝。 |
- `r2c-react-v3` 这条 fresh-clean gate 不要拿大 JSON 文本硬做字符串断言。原生 GUI bundle 的大 JSON 适合归档，不适合做 gate；稳定闸门一律优先读 `*.summary.env` 这种小合同文件。 |
- `exec_surface` 如果已经不再决定主链语义，就要立刻从 `compile/native-gui-bundle` 主流程摘掉，只保留独立 helper 身份。主链里多留一层，就多一层回归和内存风险。 |
- backend driver 安装候选产物时，不要赌 `renameFile` 会覆盖旧目标。像 `.v3.map` 和二进制安装，最稳做法是先删旧文件，再 rename 候选，避免最后一步假失败。 |
- `bootstrap-bridge/build-backend-driver` 这类宿主 target 明确的命令，要做双保险：纯 Cheng 默认 target 自己能稳定判宿主，shell 入口也统一导出 `CHENG_V3_TARGET=host_target`。只修一层，旧 backend driver 或 bootstrap runner 就会把 target 悄悄写回 `arm64-apple-darwin`。 |
- `build-backend-driver` 的 build-plan drift 检查只适合同 target 自举，不适合跨 target 迁移。当前编译器 target 不同的时候，最稳口径是 `skip_target_mismatch`，再用 candidate 的 `status/plan/zero-exit smoke` 收 correctness；继续拿旧 target 的 current compiler 做金标准只会自锁。 |
- `dmit` 这种低内存 + Ubuntu glibc 2.39 的 Linux 机，如果 `cheng.stage3 system-link-exec` 直接撞 `malloc.c:2599 (sysmalloc)`，就别再把它当成“再加点 swap 就行”的纯资源问题。swap 只够区分 OOM 和真堆损坏；一旦加 swap 后还是 `Aborted`，就要立刻按 Linux/glibc 运行时内存 bug 单独排，而不是继续怀疑 target 或脚本入口。 |
- 旧 monolith tooling 能不能删，不看仓库里还有多少 grep 命中，只看 `v3` 主链真实执行面。像这轮 [src/tooling/cheng_tooling.cheng](/Users/lbcheng/cheng-lang/src/tooling/cheng_tooling.cheng)，虽然旧 `src/tooling` 还有大量残留引用，但 `v3` 真编译和 `v3` gate 已完全不依赖它；这种情况下可以直接物理删除，再把旧 wrapper 改成显式退役硬失败，别继续留“文件没了但入口还会半构建半报错”的中间态。 |
- `r2c-react-v3` controller 这类命令编排，不要再抽成“大量 `str` 入参 + 多个 `var out` 回写”的普通 helper。`truth-compare` 这轮已经实锤：这种形状会把传进 `r2cProcessRunProgramLogged(...)` 的 C 字符串指针污染掉，直接炸到 `cheng_cstrlen_raw`；最稳写法是当前子命令里顺序编排，或者单独再起子进程，不要复用大复合参数函数。 |
- `r2c-react-v3` surface/controller 只要函数签名里开始堆 `str[]/var str[]/var int32` 这类复合参数，就别再赌 ordinary ABI 会稳。像这轮 `RSG` builder，最短真修法就是单一 `record` 聚合传递，别继续在 helper 之间搬十几路数组。 |
- `r2c-react-v3` 的大 JSON 不能继续用 `out = out + ...` 收。模块、边和样式 token 一多，进程会直接被字符串复制拖进高内存；最稳口径是从一开始就流式 `os.write(...)`，并把验收尽量卡在小 `summary.env`。 |
- `os.write(file, \"...\" + expr + \"...\")` 这种“把字符串拼接表达式直接塞进写盘调用”的写法现在不稳，最坏会把表达式字面写进 JSON。动态片段先落局部变量，再分段写。 |
- `r2c` controller bin 的 freshness 不能绑 `backend_driver_out`。这轮已经坐实，正确前置只有 `stage3` 和 controller 自己的源码；否则 `build-backend-driver` 会被 controller 冷启动依赖反咬。 |
- `compile` focused smoke 里，`truth-trace` 只能卡 compare 层，不能提前塞给 `codegen-surface`。单路由 smoke 被全路由 snapshot 误伤时，看起来像 codegen 红，其实只是合同层级放错了。 |
- `run-native-gui` 这类“bundle/runtime/host 三段式”命令，主控权必须先回 Cheng controller。只要 Node helper 还在内部自己起 `native-gui-bundle`，它就不是薄 helper；正确收法是 controller 先准备 `bundle/session`，helper 只碰宿主。 |
- `run-native-gui` 的自定义 `session/bundle` 最稳口径是“同时显式给出且文件已存在才信”，否则就让 Cheng controller 重新生成默认产物。半存在或只给一路路径，最后只会把错误推迟到宿主 helper 才炸。 |
- `v3CompilerPrintBlock` 这种报告输出 helper，不要再赌 `split(text, '\n') + for` 在当前 ordinary 上永远安全。这轮已经坐实，删掉几条报告行以后 `host-bridge-audit` 会直接在打印阶段越界；最稳口径是手扫换行并逐段 `sliceBytes`，别把打印逻辑建在 `split` 上。
- Cheng 当前不该假设 `if a || b` / `if a && b` 一定会给你宿主语言级短路保护。像这轮 `atEnd || text[i] == '\n'`，`atEnd` 为真时依然可能先把 `text[i]` 读炸；正式写法必须先单独判边界，再进下标访问。
- 旧壳文件如果已经只剩 ABI 合同在点名，就不要再为了“兼容旧路径”把它们留在原目录里。像这轮 mobile stub，最稳做法是先把新旧合同同时切到 `v3` 新路径，再物理删掉旧文件；这样不会把 `v3` 再拖回 `src/runtime`。
- 旧能力只要还留在 `seed/stage1/gate_main` 的正式命令面里，就还是活依赖，不算“以后再删”。像这轮 `run-v2-selfhost-gate`，真正收口必须 usage、supported_commands、自检、dispatch、实现五处一起删，少一处都会继续把 `v2` 留在 `v3` 主链里。
- `r2c-react-v3` 这类 live ordinary controller，当前不要直接引 `runtime/json_ast::JsonNode` / `decentralized/json_parse` 进主链。像这轮 `replay-hit-inspector` 一旦把 JSON AST 当普通 helper 形状传来传去，controller 就会稳定炸在 `layout type def missing` 和 `primary_object_body_semantics_missing`；最稳口径是只在 controller 里做最小字符串扫描，或者把重 JSON 逻辑继续留在 helper/summary 合同层。
- current ordinary seed 对 `JSON helper = nested for/if + break + str helper return` 这种形状仍然很脆。像这轮 `JsonEscape/JsonFindBalancedEnd/JsonCountStringArrayItems`，最稳修法不是继续堆语法，而是把 helper 收成 `int32/bool` 这类标量返回，再在外层 `slice(...)` 组最终字符串。
- `r2c-react-v3 replay-hit-inspector` 当前读 `--inspector-state/--out` 最稳的还是全局 `cmdline.readFlagOrDefault(...)`。子命令 tail 自扫这轮已经实测会把 `--out` 读歪，直接把产物写回仓根假路径 `--inspector-state`；只要命令还是走 live controller 入口，就先别把这条 flag 读取切成局部 tail 版。 |
- `r2c-react-v3` 某个 Node helper 只要已经从 live controller 退役，就不要继续把同名脚本留在遗留 wrapper 旁边冒充能力。最稳口径是合同先清空、wrapper local fallback 改成显式硬失败、然后物理删脚本；否则后面 grep、状态报告和旧命令很容易把死 helper 又误拉回主线。 |
- `r2c-react-v3` 某个子命令只要已经被 Cheng controller 主控，legacy wrapper 就不要再保留第二套本地 shell 编排。`native-gui-bundle/run-native-gui` 这类四段式命令最稳口径就是“正常路径只做顶层 handoff，禁 handoff 时直接 fail-fast”，不要一边说 wrapper 已退役，一边还在壳里维护完整主流程。 |
- `compare-truth / exec-route-matrix / compare-truth-matrix` 这类真值回归命令也一样：只要 live controller 已经补齐 compat 子命令，legacy wrapper 就必须同步删掉本地 shell/Node 编排，只保留顶层 handoff 和 `NO_STAGE3_HANDOFF` 下的显式硬失败。 |
- ordinary/backend driver 会编整份 `compiler_main.cheng`，不会因为某个命令平时不调用就跳过死函数。像这轮 `verify-x86-linux-remote`，只要旧远端 gate 源码还留在 live 模块里，`build-backend-driver` 就会被这些死代码直接拖红；真修法必须物理删源码、dispatch、gate 和文档。
- 判断 `stage0` 是否真的追平源码，先看 `source mtime`、`binary mtime` 和 binary strings，不要先怀疑 `print-contract` 逻辑。这轮表面像“重编后仍然漂”，真根只是 `cheng.stage0` 比 `cheng_v3_seed.c` 旧。
- 删 `src/runtime/native` 旧文件时，不要直接从物理文件下手。最稳顺序是先摘掉旧合同和 `v2` source list 里的文件名，再用 `rg` 确认只剩历史记录命中，最后才物理删除；这样最不容易把 `v3` 主线误伤。
- `src/tooling/backend_runtime_abi_contract.env` 这种旧合同如果还要保留，就直接镜像 `v3` 的最小 ABI 面，不要继续点名已经退出 live 的 `src/runtime/native` 大桥。合同词面不收，后面的 grep 和 README 就会一直把历史残影误判成现役依赖。
- 旧 backend_driver 兼容面要真收零脚本，就不要留 shell 转译层。最稳做法是把 `<input.cheng> --output:<path>` 直接并进 `v3` 编译器二进制，再让 `cdrop` 直接找 `artifacts/v3_backend_driver/cheng` / `cheng.stage3`。
- 当前自举产物里，argv 兼容解析不要复用数组版 `readFlagOrDefault(..., 非空默认值)`，也不要把带 `out var` 的别名解析塞进 `a || b`。最稳写法是手扫 argv、逐字段赋值、前缀判断自己做字节比较。
- 旧 `v2` 入口真要退役，不能只删入口文件。先把 source list、bootstrap required paths 和旧合同里的文件名摘掉，再物理删除，才能避免仓里长期留下“文件没了但构建面还点名”的假活依赖。
- 纯空壳 C 薄层如果只剩合同文本还引用，就不要继续留着。像 `v3_mobile_runtime_stub.c/.h` 这种占位壳，合同已经收成最小 ABI 头时，直接删壳才是最稳收法。
- 归档链路如果还可能被误执行，就别留“缺文件后自然炸”的半死状态。像 `v2/bootstrap/cheng_v2c_tooling.c` 和 `Makefile` 这种旧入口，最稳做法是统一收成 `retired` fail-fast，让错误停在入口层，而不是深处缺文件。
- 已归档的大 C 入口如果还保留旧实现，grep 和误编译都会继续把它当活依赖。最稳做法不是只改 `Makefile`，而是把文件本体也缩成最小 retired stub。
- 归档 README 也要追平，不要继续把已删文件写成现役 source-of-truth 或 build recipe。历史说明要明确标成 archived note。
- `r2c-react-v3` 这种 wrapper 继续收口时，顶层 handoff 不能再对所有子命令无差别 `exec stage3`。必须先做一层“只转发 live controller 已支持命令”的白名单；否则像 `exec-surface` 这种已退役命令，会先被 stage3 吃成 `unknown subcommand`，把真正的 retired 语义盖掉。
- `src/runtime/native` 里的过渡 C provider 如果已经只剩归档 `v2` source list 在引用，就不要继续保留 rename 目标文件。先确认 `v3` 不再点名，再把 `v2` source list 摘掉、物理删文件，并把旧别名 stub 改成统一 `retired`；这比继续维护一个“moved to ...”的悬空路径更稳。
- `capture-truth` 产出的 `home_default.runtime_state.json` 这类浏览器状态文档，不等于 `truth_compare` / `compile --truth-trace` 要吃的 `truth_trace_v2`。比较链只认专门的 truth fixture；喂错格式时应当硬失败，不要为了跑通去偷偷兼容。
- `home_*` 这类截图 truth route 不能只让 `routeState` 变了，不改页面局部状态初值。像这轮 `home_search_open/home_sort_open/home_channel_manager_open/home_content_detail_open`，真根就是 [HomePage.tsx](/Users/lbcheng/UniMaker/React.js/app/components/HomePage.tsx) 没按 `initialTruthRoute` 初始化 `showSearch/showSortMenu/showTabSettings/selectedContent`，所以 truth 一直是假首页。
- `native_gui_host_macos.m` 新增渐变类命令时，必须先做 frame 裁剪。像这轮 `linear_gradient_rect`，不先 `NSRectClip(frame)`，对角渐变会直接越出 hero 区把 header 左上角吃掉。
- `home_content_detail_open` 的真页面是 [ContentDetailPage.tsx](/Users/lbcheng/UniMaker/React.js/app/components/ContentDetailPage.tsx) 全屏详情，不是首页壳里的占位卡。后面只要再遇到这种 route，先回 React 真值组件确认页面层级，再决定宿主要不要单独分流。
- `r2c-react-v3 status` 这类“只写状态 JSON”的命令，也会 fresh 编译整份 controller/surface ordinary 源。只要 live `cheng.stage3 r2c-react-v3 status` 还会掉到 [/Users/lbcheng/cheng-lang/artifacts/v3_r2c_react_v3_controller/build.compile.log](/Users/lbcheng/cheng-lang/artifacts/v3_r2c_react_v3_controller/build.compile.log) 的旧 unsupported function，上层新字段就不算真正落地；后面继续推 `pure_cheng_*` 合同时，先确认 fresh controller compile 还稳不稳。
- Cheng 当前对“字符串字面量恰好以转义引号 `\\\"` 收尾”的数组元素还吃不稳。像 needle 这类匹配串，最稳写法是换成不会以 `\\\"` 结束的稳定子串，比如文件名或中间字段，避免 backend driver 把后续语句一起吞进同一个表达式。
- 新增 QUIC gate 时，先看 `stage3 primary-object` 真能发射哪条子链，再决定 smoke 范围，不要把 `controller+cubic` 这种已知超出当前发射器覆盖面的形状直接塞进默认 gate。最稳收法是先把 `protocol/platform/sliding-window/BBR/pacing` 这条 live 能编能跑的路径接绿，再把 `cubic` 单独当成编译器覆盖面待补项。
- QUIC 拥塞这条线真要进 `v3` 默认 gate，正式口径应当优先落成确定性的整数固定点，不要把 live 算法继续绑在当前 seed 还没收全的浮点 lowering 上。像这轮 `cubic` 改成 `betaScaled/cParamScaled + 毫秒时间基 + 整数立方根` 以后，`controller + cubic + validation_suite` 就能一起稳定进 primary-object。
- `msquic` app read 这条链不能在 `pump` 后先判 `session.closed`，再取 app recv buffer。只要 payload 和 close 同轮到达，就会把已经进缓冲的应用数据误杀成 `msquic: pipe closed`；正式口径是先消费 app recv，再判关闭。
- QUIC 双进程 smoke 一旦出现 server `ok`、client `recv` 失败，先做最小手工双进程复现，不要直接怀疑 gate。最短真链是“单独编 server/client 二进制、ready 文件握手、抓 client stderr”，这样能最快判断是监听时序、关闭顺序，还是编译器发射问题。
- 两阶段服务只要第一阶段允许 `--port:0` 动态绑定，后续阶段就必须复用第一阶段真实绑定端口，不能再传回原始 `0`。最稳做法是第一阶段把真实端口写进 ready 文件，第二阶段显式读回这个端口再继续监听；否则 client 会一直拨旧端口，表面看起来像协议或 runtime 挂死。
- 真实 daemon smoke 如果只是要拿可执行产物，就不要依赖 `build-bft-state-machine` 这类桥接子命令。最稳做法是 smoke 自己调用 `system-link-exec` 直编目标主程序；这样不会被“当前 compiler binary 少一个子命令”这种非语义问题卡住。
- `run-host-smokes`、`build-backend-driver` 这类 fresh 重编入口一旦炸掉，先看 seed C 自己能不能重编 `stage1.generated.c`，不要先把锅甩给新 smoke。像这轮真正的硬阻塞是 `v3_bft_expected_baseline(...)` 缺前置声明；补上之后，`build-backend-driver` 和正式 smoke 入口都会恢复。
- seed C 大文件里，本地静态 helper 只要会在定义前先被调用，就必须补前置声明；否则平时用现成二进制跑不出问题，一到 `bootstrap-bridge` 生成 `stage1.generated.c` 重编时就会因为隐式声明直接炸掉。
- ordinary/backend_driver 这条线收命令面时，不能把整个 `gate_main` 直接 import 进 `compiler_main`。这轮已经现场坐实：`build-backend-driver` 会把 `gate_main` 里 seed 还吃不下的 build/runtime/helper 一起拖进 primary object，直接红成 `primary_object_body_semantics_missing`；更稳口径是继续扩顶层转发名单，只把 live 命令名并进来，不把大 gate 语义面整包编进 ordinary。
- 命令面回归不能只看 `compiler_runtime` 这种静态 usage 合同。像 `print-bootstrap / r2c-react-v3 help / build-zero-exit` 这类零脚本入口，真正会漂的是 live binary 行为；最稳口径是补一条 host smoke，直接执行 `artifacts/v3_backend_driver/cheng ...` 并校验产物，不要只靠帮助文本过关。
- 命令面 smoke 一旦已经坐实是 live 真链，就不要让它只埋在默认 smoke 列表里。像这轮 `backend_driver_command_surface_smoke`，真正稳的收法是继续把它升成正式 `verify-*` 命令，并且把 `help`、`supported_commands`、seed/stage3 dispatch 和 `slice-gate` 一次补齐；少一处，fresh bootstrap 或帮助面都会继续漂。
- `r2c-react-v3-codegen-surface-smoke`、`r2c-react-v3-native-gui-bundle-smoke`、`r2c-react-v3-fresh-clean-gate` 这类 backend-driver 专属 focused 命令，正式 gate 不要再直打 `stage3Path` 去赌 bootstrap dispatch 已追平。最稳收法是先补一条 host smoke，里面固定执行 live `artifacts/v3_backend_driver/cheng ...`，再让新的 `verify-*` 命令只复用这条 smoke。
- world/receipt 这类 report 文本解析不要再偷懒用 `startsWith(key + "=")` 或宽松字符串前缀。像这轮 `prev_head_cid` 和 `runtime_provider[...]` 都已经坐实会被错切到别的行；最稳口径是逐字符精确匹配 key，再机械检查后续分隔符。
- 从 ingress/store 里取出的文本如果后面还要跨步骤解析、重建或对拍，就先 own-copy，再往下传。像这轮 `compiler_world_libp2p` 的 `worldText/receiptText`，不先转成 owned text，fresh rebuild receipt 比对就会继续不稳。
- arm64 call lowering 里只要先调整了 `sp`，后续所有基于旧 SP 的 register spill reload、隐藏返回地址回读都必须同步重算偏移。不要只修显眼的一处；最稳做法是统一走同一个 `stack_arg_bytes` 校正口径，再补 smoke 把“大对象返回 + 嵌套字段直传”这类形状钉死。
- seed 递归 helper 只要还背着 `V3ImportAlias[CHENG_V3_MAX_IMPORT_ALIASES]` 这类大栈 scratch，就还不算真正收稳。像这轮 [cheng_v3_seed.c](/Users/lbcheng/cheng-lang/v3/bootstrap/cheng_v3_seed.c) 的 `v3_normalize_type_text_with_params(...)`、`v3_const_value_set_implicit_default(...)`、`v3_emit_implicit_default_into_address(...)`，把 alias scratch 也挪到堆上之后，`bootstrap-bridge -> build-backend-driver -> compiler_main` 这条链才彻底不再被递归栈帧放大。
- 改完 [cheng_v3_seed.c](/Users/lbcheng/cheng-lang/v3/bootstrap/cheng_v3_seed.c) 之后，不要直接拿旧 `cheng.stage3` 的报错判断新补丁成败。最稳顺序是先 `cc` 重编 `cheng.stage0`，再跑 `bootstrap-bridge` 把 `stage3` 追平源码，最后再复验 `system-link-exec/build-backend-driver/gate`；否则很容易被旧产物和旧日志带偏。
- 收纯 Cheng 主线时，不能只看真正的 link input。`runtime ABI contract`、seed 辅助构建路径、freshness 输入、debug gate 和 host audit 只要还点名旧 C 路径，就都要一起追平；否则执行面明明已经切掉，辅助链路还会继续把人带回“还有活依赖”的假结论。
- `program_support_host_runtime_v3` 这类宿主 provider 只保留 raw libc/POSIX wrapper，不要再把 `pipe_spawn` 这种带流程语义的 helper 也导出去。最稳口径是让 [program_support_backend_v3.cheng](/Users/lbcheng/cheng-lang/v3/src/runtime/program_support_backend_v3.cheng) 自己实现编排逻辑，宿主 provider 只负责最薄 ABI 面，不然很容易重新长出双活 bridge。
- `compiler_runtime_smoke` 这种 usage 合同 smoke 只要手写帮助文本，就必须和 [compiler_runtime.cheng](/Users/lbcheng/cheng-lang/v3/src/tooling/compiler_runtime.cheng) 同补丁更新。命令文案一改，最先红的就会是这种字符串断言；不想再被这类漂移绊住，就让 smoke 跟统一 helper 同步演进。
- `system-link-exec` 这类前台编译命令出现 `[cheng_v3_seed] composite call resolve failed ...` 时，先看最终退出码和产物，不要把中途诊断行直接当成硬失败。当前这批日志在某些 composite contract 构造上还是噪音，真正验收口径仍然是“命令是否 `exit 0`、产物是否可运行、报告是否完整”。
- 编译期噪音如果已经确定不该存在，就别再靠手工 probe 盯。最稳做法是把它直接塞进 `build-backend-driver` 这类 live 安装命令里做硬门禁；像这轮 `compiler_runtime_smoke.selftest` 就应该由安装流程自己生成 compile/run log，并对 `[cheng_v3_seed]` 做 fail-fast。
- 给 `build-backend-driver` 这类会重装 live 二进制的命令补新自检时，要记住第一次运行通常只是把新逻辑写进 `artifacts/v3_backend_driver/cheng`，第二次运行才是在用新 binary 执行新自检。验收别只看第一次结果，不然很容易误判“补丁没生效”。
- `backend_driver` 转发 `stage3` 时，只要 seed 侧也有“非 bootstrap 命令自动 handoff”逻辑，就必须带一条明确的禁止回手开关。这轮 `verify-debug-runtime` 已现场坐实：没有 `CHENG_V3_NO_BACKEND_DRIVER_HANDOFF=1` 时会直接递归互跳；最稳口径是 bridge 侧固定注入环境变量，seed passthrough 第一行就检查并短路。
- 当前 ordinary/stage3 对关键 freshness 判定别再写复合 `&&` 返回式。像这轮 `v3CompilerBootstrapBridgeFresh(...)`，源码签名完全正确也还是会被打成 `scalar call param mismatch / emit return failed`；最稳写法就是顺序 `if`，每个路径检查单独返回。
- bootstrap freshness 不能只手抄几条源文件名。像这轮 `run-chain-node-three-node-smoke`，真正变更点在 [host_ops.cheng](/Users/lbcheng/cheng-lang/v3/src/tooling/host_ops.cheng) 和 [os_host_process.cheng](/Users/lbcheng/cheng-lang/src/std/os_host_process.cheng) 这类 ordinary closure 传递依赖；最稳口径是把 bootstrap 输入集合直接从 ordinary closure 机械展开，再写进 snapshot 做 freshness 比较。
- Cheng 当前不能假设 `&&/||` 会可靠挡住后半段的索引或切片。只要条件里带 `raw[i]`、`text[i]`、`sliceBytes(...)` 这类访问，就先拆成 `itemEnd/lineEnd` 之类显式布尔，再在分支里读索引；否则像这轮 `verify-orphan-guard` 一样，会直接炸成 bounds check failed。
- orphan 问题不要只看 gate `ok`。最稳验收是“前台命令通过 + `ps -Ao pid=,ppid=,state=,etime=,command= | awk '$2==1 && $3 ~ /^U/'` 没有新增 Cheng `U/UE`”；只满足前一半还不算收口。
- `verify-debug-tools`、`verify-debug-runtime`、`verify-debug-profile` 这种正式 gate，不要各自手写“先 `bootstrap-bridge` 再 `build-backend-driver`”准备逻辑。最稳口径是抽成统一 `ensure_backend_driver_ready` helper，只在 freshness 真失效时才重建；这样不会在 live backend driver 已 fresh 时继续重复自举。
- 判断“重复自举是不是还在”时，不要只看 stdout 里有没有 `v3 backend driver ok`。最稳验收是直接看进程树，确认 `verify-*` 下面只剩真实 `system-link-exec` 子编译，没有新的 `build-backend-driver` 链。
- 循环条件里的 `while end < len(text) && text[end] != '\n'`、`while cursor < total && isSpace(line[cursor])` 也别再留。只要右半段会索引文本，就统一改成“先判边界，再在循环体里 `break`”；`gate_main` 这轮已经把这类写法现场坐实成同一个坑。
- `perf_gate` 这类 helper 真要验 live 行为，优先跑 `compare-bench` 这样的正式命令，不要先拿当前 ordinary 子集编不过的 smoke 判断补丁成败。否则很容易把“测试本身被旧 primary object 限制挡住”和“新补丁回归”混在一起。
- 会自动触发 `bootstrap-bridge` 的命令不要并发验。像这轮 `compare-bench` 和 `run-chain-node-three-node-smoke` 并跑时，直接撞出 `failed to install bootstrap stage0`；这类主线 gate 最稳口径是串行。
- `chengcache/runtime_selflink` 里的旧 `system_helpers.backend.combined.*.o`、`system_helpers_selflink_shim.*.o`、`system_helpers.ffi_*_bridge.*.o` 不清掉，live link 会继续把已经删掉的 `system_helpers` bridge 当成活依赖。碰到源码和 ABI 合同都已经追平、但 `build-backend-driver` 还在 unresolved 旧符号时，先删这批生成物，再判断是不是真回归。
- `tcp_transport`、`webrtc_transport` 这类 ordinary libp2p helper 当前不要走 alias 间接 bridge，也不要在 handle 拷贝热路径里用 `for/let` 形状。最稳口径是本地 `@importc` + 显式 `while`；否则像这轮 `compiler_world_fresh_node_selfhost_smoke` 一样，会先掉出 primary object 支持面。
- 共享字符串 helper 不要只测正例，负例必须有真 smoke。像这轮 `startsWith("sha256 ns_per_op=10.000", "sink=")` 被误判成 `true`，就是因为一直没人盯“应当返回 false”的主线路径。
- 当前 ordinary primary object 下，热路径前缀/过滤 helper 优先写成显式 `while`，不要继续赌 `for` 形状在所有负例比较上都稳定。至少 `startsWith(...)` 这轮已经现场坐实旧 `for` 写法会误判，直接反咬 `perf_gate` 数据面。
- `compiler_main` 里新接的 live verify/debug gate，优先走 `os.execCmdStatus(...) + 重定向日志 + 读回文本` 这条 ordinary 已坐实的命令执行面，不要直接复用 `hostops.v3RunProgramLogged(...)` / `hostops.v3RunFileLogged(...)` 或 `os.execCmdEx(...)` 复合返回。像这轮 `verify_orphan_guard_shell_scan` 已现场坐实：pipe-spawn capture 会直接落成 `sh: : command not found`，而 `execCmdStatus` 稳定。
- `verify-orphan-guard` 的扫描范围必须钉在 `v3/tooling` 顶层入口，不要递归扫 vendor 树。否则 `browser_webrtc/node_modules/playwright-core/bin/*.sh` 这种第三方脚本会把门禁打成假红。
- 收 live 命令面时，不要只信 `build-backend-driver` 的 `ok` 文案。至少再拿一份“当前源码直编 probe”对拍 live binary；如果 probe 已真跑通而 live 还没追上，就先把 verified probe 安装成 live backend driver，再跑正式入口。
- `r2c-react-v3 help` 这种只要求稳定帮助文本的命令，不要让它再触发 controller/build 链。最稳口径是在顶层 compiler 命令面直接短路打印帮助，把 `help` 和 `compile/codegen/native-gui` 真链拆开。
- `r2c` controller 当前 ordinary 主线里，`cmdline.paramCount()`、`os.get_stdout()`、`os.get_stderr()` 仍是高风险形状。只要命令编译链碰到这组三个入口，优先改成 `@importc` 直桥，不要继续赌 ordinary lowering 会自己吃下去。
- `execFilePipeSpawn(file, argv)` 这类文件路径 + argv API，不能退化成 shell 命令串。最稳口径是直接补 `execvp + pipe + fork` bridge；否则最小 `/bin/echo` 正例都会把共享宿主 smoke 和上层 gate 一起拖红。
- verify/focused smoke 里如果内部会调用 `v3CompilerEnsureBackendDriver(...)`，provider/runtime 一变更就可能先触发一次 live `backend_driver` 自刷新。碰到这种“命令长时间无输出”的现场，先看 `cheng.next`、`build_backend_driver_v3.report.txt` 和子进程树，分清是 freshness 重建还是真卡死。
- `build-backend-driver` 这种 live 自举命令，不能只看 `ok` 文案和最终 install。最稳验收要同时看 [build_backend_driver_v3.report.txt](/Users/lbcheng/cheng-lang/artifacts/v3_backend_driver/build_backend_driver_v3.report.txt) 里的 `current_compiler=` / `reference_compiler=` 是否都已经指回 live `artifacts/v3_backend_driver/cheng`，再看 [build_backend_driver_v3.install.txt](/Users/lbcheng/cheng-lang/artifacts/v3_backend_driver/build_backend_driver_v3.install.txt) 里的 `installed_status=ok`。 |
- `execFilePipeSpawn(...)` 改真 pipe 后，父进程如果不马上关写端，后面的 drain/wait 会永远等不到 EOF。像这轮 [host_ops.cheng](/Users/lbcheng/cheng-lang/v3/src/tooling/host_ops.cheng) 的 `v3SpawnFile(...)`，最稳口径就是返回前 `ptyClose(spawnRes.writeFd)`，并把 `writeFd=-1` 固定写回结果结构。 |
- `verify-r2c-react-v3-surface` 这类复用固定日志文件的总 gate，不能拿旧 `run.log` 当唯一真相。最稳判断顺序是先看子进程树是不是已经推进到 `fresh-clean-gate -> native-gui-bundle`，再看 `/tmp/r2c-native-gui-fresh-clean-gate-*/native_gui_bundle_report_v1.json` 是否逐个落出来，最后再看总 gate 退出码。 |
- `2026-04-17 20:00 +0800`：碰到 monorepo 自举链里成批“小写跨模块导入”报错时，先别沿着每个 helper 名字硬改一百处。先判断真实语义边界是不是 `compiler universe` 级别；这轮 `cheng/v3 + std + stage1` 已现场坐实，正确修法是把可见性规则收成 universe-private，再用独立临时包做负向 smoke 保住外部包隔离。 |
- `2026-04-17 20:00 +0800`：为了追运行期错误，不要往 ordinary smoke 主体里临时塞 `if !res.ok: echo(Error(res))` 这种复合诊断分支。当前 primary object 子集下这类形状会先把 smoke 自己编红；最稳口径是 transport/provider 补 last-error 合同，smoke 保持最小 assert。 |
- `2026-04-17 20:12 +0800`：`state` 文件、flag 文本这类短生命周期字符串只要要进入 `V3ChainNode` / `V3ChainNodeServeSnapshot` 这种长期对象，就不能继续保留 `SliceBytes(...)` 或 trim 返回的别名。像这轮 `chain_node init/show-state` 的 `139`，真根就是 `nodeId` 指向了局部文本缓冲；最稳口径是在对象边界统一 `newStringAlloc + RawmemCopy` 深拷贝。 |
- `2026-04-17 20:24 +0800`：业务运行时已经有最小复现时，不要被无关的 `backend_driver` 红点拖着一起大修。像这轮 `chain_node`，最短路径就是先用现成 `cheng.stage3 system-link-exec` 直编目标程序和 smoke 夹具，把 `CLI/three-node` 真链跑通，再把 `build-backend-driver` 阻塞单独记账。 |
- `2026-04-17 21:16 +0800`：live `backend_driver` 自己能回答的纯 tooling 查询命令，不要再机械转发给 `stage3`。像这轮 `print-build-plan`，一旦遇到 bootstrap 主线里也会回 handoff 的命令面，就会把官方 smoke 拉进 `backend_driver <-> stage3` 自旋；最稳口径是本地直接执行。 |
- `2026-04-17 21:16 +0800`：正式 smoke 卡住时，先查入口前置探针是不是在自旋，不要先怀疑业务程序本身。这轮 `run-chain-node-*` 的真红点就不在 `chain_node`，而在 build-plan 探针。 |
- `2026-04-17 21:37 +0800`：`build-backend-driver` 一旦失败，先确认它有没有把 `artifacts/v3_backend_driver/cheng` 一起打空；后续所有 leaf smoke 都依赖这条路径，别把“缺失 live compiler”误判成业务回归。 |
- `2026-04-17 21:37 +0800`：`system-link-exec` 的 shell 包裹要从 seed helper 根上切，不要在每个 smoke 外面补命令层绕行。像这轮把 [cheng_v3_seed.c](/Users/lbcheng/cheng-lang/v3/bootstrap/cheng_v3_seed.c) 的 `v3_compile_fixture_with_backend_driver_emit_target(...)` 改成直传 `argv` 后，`chain_node_cli` 和 `chain_node_three_node` 两条官方链一起收干净。 |
- `2026-04-17 22:05 +0800`：`build-backend-driver` 不能再用“正式路径已存在”当成功条件。最稳口径是固定编到候选 `cheng.next`，把 `status / verify-export-visibility / print-build-plan / ordinary_zero_exit_fixture` 全部对着候选产物跑完，再原子覆盖正式 `cheng`。 |
- `2026-04-17 22:05 +0800`：`run-host-smokes` 只要显式传了 `--compiler:` 或 `CHENG_V3_SMOKE_COMPILER`，就不要再抢先 `leaf_prepare_backend_driver`。否则像这轮 `build-backend-driver -> cheng.next verify-export-visibility` 一样，会直接自锁回默认 `backend_driver` 准备链。 |
- `2026-04-17 22:05 +0800`：改完 [cheng_v3_seed.c](/Users/lbcheng/cheng-lang/v3/bootstrap/cheng_v3_seed.c) 后，第一件事是确认 `artifacts/v3_bootstrap/cheng.stage0` 还是不是真 seed，不要继续拿指向 `cheng.stage3` 的旧链接做验收。先重编真实 `stage0`，再跑 `self-check / build-backend-driver / 官方 smoke`，这样验证的才是新补丁。 |
- `2026-04-18 00:05 +0800`：seed 想把日常命令 handoff 到 live `backend_driver`，不能只改 `prefers/requires passthrough` 列表；先看主 dispatch 顺序。这轮 `status/print-build-plan` 的真红点就是 `main(...)` 还在前面先本地吃掉命令。 |
- `2026-04-18 00:05 +0800`：`backend_driver` 任何日常命令，只要 `stage3` 不认识，就必须本地实现，不要继续赌转发链。像这轮 `host-bridge-audit`，只要还走 `backend_driver -> stage3`，结果就只会是 seed usage/unknown command。 |
- `2026-04-18 00:05 +0800`：审计 gate 必须跟着真实 ABI/contract 一起演进。像这轮 `host-bridge-audit` 还在硬卡 `core_runtime` 必须导入 `cheng_host_malloc/free`，而 live 实现已经切到 `cheng_malloc/cheng_free`；后面凡是再改 provider 合同，要先同步改 audit，否则一定会制造假红。 |
- `2026-04-18 03:15 +0800`：会生成固定文件名的 smoke 不要共用同一组产物路径。像这轮 `run-shared-emit-smokes`，并发跑 live `backend_driver` 和 `stage0` 时，只要两边都写 `artifacts/v3_shared_emit_smokes/shared_emit_fixture.*`，就会互删并误报缺产物；最稳口径是每次运行都落到独立 `artifacts/v3_shared_emit_smokes/<run_id>/`。 |
- `2026-04-18 04:05 +0800`：`r2c-react-v3` 的 Node helper 只要被 live `backend_driver` 当正式 smoke 依赖，就必须自己稳定落报告文件，不能只靠 stdout 一句 `ok`。这轮 `codegen-surface` 已现场坐实：不默认写 `codegen_surface.summary.env` 和 `codegen_surface_report_v1.json`，总 gate 会直接判红；最稳口径是 helper 固定产出 `summary/report/json`，smoke 只验这些权威文件。 |
- `2026-04-18 04:05 +0800`：`native-gui-bundle` 这条链只要 compiled runtime 已经能直接吐 `native_gui_runtime_v1`，就不要再绕回 ordinary 还不稳的 controller compile。最稳口径是 bundle 直接执行 `native_gui_runtime_compiled_main --runtime-contract ...`，再从 `compile_report_v1.json` 回填 host contract 缺的 `feature_hits/required_groups`，这样 `host_ready/renderer_ready` 会稳定对齐真产物。 |
- `2026-04-18 04:05 +0800`：`verify-r2c-react-v3-surface` 这种总 gate 是长跑串行链，不能只看 30 秒有没有新输出或旧 `run.log` 里最后一行。最稳判断顺序是：先看子进程是不是还在顺序跑 `codegen/native-gui/fresh-clean`，再看 `/tmp/r2c-native-gui-fresh-clean-gate-*/native_gui_bundle_report_v1.json` 是否已落齐，最后以总命令退出码和 `verify-r2c-react-v3-surface: ok` 为准。 |
- `2026-04-18 04:42 +0800`：只要 seed 已默认 handoff 到 live `backend_driver`，验证 seed C 真命令面时必须显式带 `CHENG_V3_NO_BACKEND_DRIVER_HANDOFF=1`。否则表面在跑 `cheng.stage0`，实际还是 live `backend_driver`，很容易把“转发成功”和“seed C 真补丁已生效”混为一谈。 |
- `2026-04-18 04:42 +0800`：三节点联机 gate 只要远端机器失联，就要先把机器故障和代码故障拆开。像这轮 `bench-bft-validator-three-node`，`stage3` 和 seed C 都一致卡在 `bft_stop_relay`，而 `ssh dmit` 已直接超时；这种场景最稳口径是先记成基础设施阻塞，不要拿假部署或单机替代去冒充“三节点已通过”。 |
- `2026-04-18 05:40 +0800`：`r2c-react-v3 render-compare` 这条截图对拍链，前置顺序不能想当然。当前最小正确闭环是 `tsx-frontend -> 从 tsx_ast_v1 生成最小 host/asset/tailwind manifest -> codegen-surface -> native-gui-bundle -> native-gui-run -> truth-runtime -> png compare`；少任一前置，链路都会在 `missing tsx_ast` 或 `missing host contract` 这种硬错误上停住。 |
- `2026-04-18 05:40 +0800`：browser truth 截图如果要和 native GUI 严格对齐，不能只靠 Chrome `--window-size`。这轮 `home_default` 已现场坐实：只设窗口大小会把 truth 图漂成 `500x757`；最稳口径是 CDP 先 `Emulation.setDeviceMetricsOverride(width,height,deviceScaleFactor=1)`，再 `Page.navigate(url)` 重载页面，然后再抓 `Page.captureScreenshot`。 |
- `2026-04-18 05:40 +0800`：原生 GUI 进入像素对拍阶段后，先确认 React 真值机真的在监听，再判断 `runtime_state_missing`。这轮 [/tmp/r2c-render-compare-home-default-v1/truth/home_default.runtime_failure_v1.json](/tmp/r2c-render-compare-home-default-v1/truth/home_default.runtime_failure_v1.json) 已现场坐实：`chrome-error://chromewebdata/ + ERR_CONNECTION_REFUSED` 的真根只是 `127.0.0.1:4173` 没起服务，不是页面语义或等待时序。 |
- `2026-04-18 06:10 +0800`：Mac host 里 `frameForRenderCommand(...)` 的 `y` 口径是 top-based。后面只要再做一次“顶到底”的手工转换，页面就会整页翻转；先看最终截图方向，再继续修细节。 |
- `2026-04-18 06:10 +0800`：要让 native GUI 真切到业务页面，启动阶段必须按 `runtime_state` 本地重建 render plan，不能继续信任 session 里预烘焙的 `native_render_plan_v1.json`。这轮 63% 的大 diff 真根就是 host 直接画了 debug preview。 |
- `2026-04-18 06:10 +0800`：native GUI 的视觉微调一旦可能影响 render-plan 分支，必须每次都复跑正式截图对拍。刚才一版首页 badge/chip tweak 会把 runtime render plan 打回 debug preview；这种细调不能凭肉眼留仓，必须以 `render_compare_report_v1.json` 为准。 |
- `2026-04-18 06:11 +0800`：三节点 BFT bench 前台长时间安静时，先查 `artifacts/v3_gate/bft_bench_wave_*.wait.*.summary.log` 和三台 `query-summary`，不要先下“共识挂死”的结论。这轮已经现场坐实：两轮都在继续推进，只是 bench 直到最终 report 才一次性打印结果。 |
- `2026-04-18 06:11 +0800`：`bft_state_machine query-summary` 现在必须显式带 `--asset --account-a --account-b`。后面所有运维探针和手工排障都不要再敲裸 `query-summary --state:...`。 |
- `2026-04-18 06:11 +0800`：`bench-bft-validator-three-node` 成功返回后不会自动 stop；前台那句 `v3 bft deploy: stopped` 只是 deploy 前清场。bench 跑完如果要回到空场，必须再显式执行 `stop-bft-validator-three-node`。 |
- `2026-04-18 07:12 +0800`：碰到 `v3 libp2p quic: accept: msquic native: accept handshake ! ready` 这种字样时，先确认它是不是瞬时 accept 状态，不要直接当成致命链路错误。当前三节点 bench 已现场坐实：把这类 `accept handshake ! ready / handshake ! ready / session ! ready / pipe read timeout` 收成窗口内可重试事件后，proposer 不再早退。 |
- `2026-04-18 07:12 +0800`：高负载三节点 bench 现场先看新鲜时间戳，不要被旧 wave 文件带偏。像这轮 `32x4` bench，真正的新鲜产物先出在 `bft_build_relay.log`、`bft_init_*`、`bft_seed_queue.log`，旧的 `wave_1/2` 文件还躺在目录里；最稳口径是先看 `ls -lt artifacts/v3_gate`，再看当前这轮文件。 |
- `2026-04-18 08:31 +0800`：ordinary lowering 仍然怕“复合返回值嵌套调用”。像这轮 `strutil.Strip(v3BackendDriverReadFlagOrDefault(...))` 会直接炸成 `composite call-into-address arg local missing`；最稳口径是先把复合返回值落成独立局部，再传给下一层函数。 |
- `2026-04-18 08:31 +0800`：`std/strings::[]` 这种基础 helper 只要形状太激进，就会把完全无关的 tooling build 一起拖红。当前更稳的 Cheng 口径是：顺序 `if` 早退，不连写 `||`；字符串字节读取优先复用 [std/system.cheng](/Users/lbcheng/cheng-lang/src/std/system.cheng) 已经跑通的 `system_ptr_add + load(...)` 形状。 |
- `2026-04-18 08:31 +0800`：`r2c-react-v3 status` 这种轻命令要真正稳定，不能只修 controller 源码。最稳口径是四层一起追平：`compiler_main`、`backend_driver_main`、`r2c_react_v3_controller_main`、`v3/experimental/r2c-react-v3/r2c-react-v3`，否则总会有一条入口回落到旧 controller compile。 |
- `2026-04-18 08:37 +0800`：`render-compare` 这种依赖外部 truth dev server 的链路，起手先验证 `baseUrl` 可达，不要先拉 Chrome。像这轮 `127.0.0.1:4173` 没起时，旧行为只会长时间空等；最稳口径是先硬失败 `truth_base_url_unreachable:<url>:...`。 |
- `2026-04-18 08:37 +0800`：详情页这类 close-to-truth 的原生壳里，`←/♡/⚑` 这种字符占位不能再留。只要 truth 侧是 SVG/icon component，native 侧就应直接上矢量 icon；否则像素 diff 会稳定先从这些小控件炸开。 |
- `2026-04-18 08:49 +0800`：像 `home_content_detail_open` 这种 truth 主图本身就是程序生成 SVG 的页面，宿主不要再手写“类似海报”。最稳口径是直接按 truth 资产的源几何去画；这轮把详情页主图收成 `960x1280` poster + `object-cover` 裁切之后，`total_abs_delta` 才真正明显掉下来。 |
- `2026-04-18 08:49 +0800`：截图对拍进入 close-to-truth 阶段后，不要只盯 `diff_pixel_count`。字体栅格和亚像素会让“像素完全相同数”很难线性下降；这时要同时看 `total_abs_delta` 和肉眼截图，别把已经明显更接近的画面误判成没进展。 |
- `2026-04-18 08:49 +0800`：远端 BFT 这类长命令不要再直接拼进 `ssh ... sh -lc '...'`。最稳口径是先写本地 `*.remote.sh`，再 `scp` 到远端执行；一旦出错，现场可以直接打开脚本文件定位到哪一行坏掉。 |
- `2026-04-18 08:49 +0800`：这类长 shell 文本只要还在 C 里自己手算 buffer，就必须按真实占用次数逐项核对。像这轮 `v3_bft_remote_build_ready_shell(...)` 少算一次 `run_log`，现场表现看起来像“引号坏了”，真根其实只是命令被截断。后面再写 `snprintf` 长命令，先数 `%s` 次数，再数 cap 乘数。 |
- `2026-04-18 08:58 +0800`：如果 truth 资产本身就是 SVG，macOS 原生宿主优先试 `NSImage(data:)` 直接吃同源 SVG，再做 `object-cover` 裁切。像这轮 `home_content_detail_open`，这条路比继续手绘渐变/圆/文字更接近浏览器 truth。 |
- `2026-04-18 08:58 +0800`：当第一处差异从某个控件边界转移到 header 文字栅格时，说明布局框已经基本对了，别再回头乱动几何。后面该优先收字体和文本渲染口径。 |
- `2026-04-18 09:16 +0800`：详情页这类 close-to-truth 对拍，先把 React 真值源读实再改宿主。像这轮只要先看 [App.tsx](/Users/lbcheng/UniMaker/React.js/app/App.tsx) 的 `buildTruthImageDataUrl(...)` 和 [ContentDetailPage.tsx](/Users/lbcheng/UniMaker/React.js/app/components/ContentDetailPage.tsx) 的图片区样式，就能确认 hero 的资源和裁切口径已经对上，后面不必再怀疑“主图拿错了”。 |
- `2026-04-18 09:16 +0800`：`home_content_detail_open` 这种页面进入细差阶段后，先按区块看 `total_abs_delta`，别只盯全图 `first_diff`。这轮现场已经坐实 hero 仍占了 `6302025` 的绝对色差，header 只有 `566366`；如果只盯 `(122,16)`，很容易把时间浪费在次要区域。 |
- `2026-04-18 09:16 +0800`：同源小 SVG icon 不是天然更接近浏览器截图。像这轮把 `arrow-left/user-plus/heart/flag` 改成 lucide SVG 后，正式对拍反而从稳态 `9911k` 回升到 `9956k`；后面只要是这类“看起来更正宗”的替换，都必须跑正式 `render-compare`，差了就当场撤回。 |
- `2026-04-18 09:16 +0800`：详情页这种整页图文页，先把主几何对齐，再谈细节字体。像这轮只把 header 从 `50px` 收到 React 的 `56px`、把 hero/detailTop 收到真实边界，`total_abs_delta` 就从 `9911460` 直接掉到 `5897049`；说明几何节奏一旦错了，后面所有文本和图片区都会被一起污染。 |
- `2026-04-18 09:16 +0800`：正文区的 `pt-4 / mt-2 / text-xs / h-2 / py-4` 这类 Tailwind 节奏不要靠肉眼猜。像这轮把正文、时间、统计、分隔条、评论标题按 React 真实布局节奏重排后，`home_content_detail_open` 又从 `5897049` 掉到了 `5860271`；后面再碰类似页，先从源码把垂直节奏抄实。 |
- `2026-04-18 09:16 +0800`：按钮这种小控件进入细差阶段时，不要来回抖一像素碰运气。像这轮关注按钮下调 `1px` 的正式结果 [/tmp/r2c-render-compare-home_content_detail_open-v20/render_compare_report_v1.json](/tmp/r2c-render-compare-home_content_detail_open-v20/render_compare_report_v1.json) 反而把 `total_abs_delta` 从 `5860271` 拉回 `5903859`；后面小控件只在有明确真值依据时再动。 |
- `2026-04-18 09:39 +0800`：用户明确说“先整体完成 GUI 框架再细化页面”时，主线就不要再困在单页像素差里。最稳口径是先把普通 route 的 fallback 从 `item_card` debug 墙切成统一 generic surface：统一 header、去 preview chrome、按 `native_layout_plan_v1.items` 生成 panel/control 骨架；等这层立住，再逐页追还原度。 |
- `2026-04-18 09:39 +0800`：`native_layout_plan_v1` 里的 `root/header/meta_component/meta_surface` 只是 preview 调试壳，不是业务页面本体。后面凡是用它来驱动原生 GUI，都先把这四项 trim 掉，再决定首屏 content top。 |
- `2026-04-18 09:39 +0800`：验证新宿主 fallback 时，先把“宿主画面变了”与“bundle 真链还在别处红”拆开。像这轮 `tab_messages`，generic surface 已用现成 session 探针坐实 `item_card=0`；live `render-compare` 失败则另记成 `program_support_backend_v3` provider compile 阻塞，不要把两件事混成“GUI 框架没完成”。 |
- `2026-04-18 10:08 +0800`：`program_support_backend_v3` 这类 provider 里，只要 ordinary 编译日志点名“composite arg local missing / emit if cond failed / prepare stmt call state failed”，第一刀就别猜 ABI，不要大改。最稳口径就是把复合返回值、`cheng_fixed32_zero()` 这类 composite 临时值、和三元表达式全拆成局部变量；这轮 `native_gui_bundle` 的 provider compile 就是这样直接收回来的。 |
- `2026-04-18 10:08 +0800`：generic GUI 框架是否真正接管 live route，必须看 `native_render_plan_runtime_v1.json` 的命令直方图，不要只看 summary。像这轮 `tab_messages-framework-v2` 虽然已经能落截图，但 render plan 还是 `item_card` 旧壳；直到刷新成 `tab_messages-framework-v3` 才变成统一骨架。 |
- `2026-04-18 10:08 +0800`：当用户要求“先整体完成 GUI 框架”时，验收口径就先定成“多条 live route 都已能正式跑到 native/truth 双截图”，不要先追求像素相似度。这轮 `tab_messages/tab_nodes/message_thread` 三条都已经正式落出 `render_compare_report_v1.json`，所以主线已经从“编译链不通”推进到了“统一骨架已立住，后面按页面提还原度”。 |
- `2026-04-18 17:45 +0800`：先看活引用再修编译错误。像这轮 `src/runtime/mobile/cheng_mobile_exports.c` 和 `v3/bench/c_ref/*`，真根不是“头路径坏了”，而是它们本来就是重复副本或旧基线入口；最稳收法是先把命令面和死文件删干净，再看剩下的真活链。 |
- `2026-04-18 18:05 +0800`：移动端共享层收口不要只停在头文件。像这轮 `cheng_mobile_exports/host_api/host_core`，如果实现文件还各自留在 Android/iOS 工程里，下一次 ABI 变动还是会双改漂移；最稳口径是把共享 `.c/.h` 一起挪到 `src/runtime/mobile`，平台目录只保留薄包装。 |
- `2026-04-18 10:53 +0800`：聊天页这类 route 只要已经拿到了真实静态文案和结构，就别再用 generic surface 硬顶。最稳口径是补共享绘制能力，比如 `word_wrap`、`vertical_align`、图片占位 SVG，再把 route 壳一次切成真实聊天页；这样收益比继续堆灰块骨架大得多。 |
- `2026-04-18 10:53 +0800`：带环境噪声的 truth 截图要先分清“业务壳”和“运行时告警”。像这轮 `tab_nodes` 的红黄 banner 在 current native contract 里根本没有对应文本，所以不能靠 GUI 宿主猜出来；后面要么把 banner 正式接进 runtime state，要么就把它明确排除在 GUI 壳验收之外。 |
- `2026-04-18 11:22 +0800`：`mobile-shell` 这条 ordinary 生成链里，Android 包路径不要再走 `strutil.Replace(appId, ".", "/")`。这轮现场已经坐实它会把 `org.cheng.smoke` 打成乱码目录；最稳口径是 `strutil.Split(appId, '.')` 再 `strutil.Join(parts, "/")`。 |
- `2026-04-18 11:22 +0800`：当前 `mobile_shell_codegen` 里不要用 `CharToStr(char(34))` 组引号。现场十六进制已经坐实它会把 quote 写成 `0x85`，连 manifest 和 Kotlin `System.loadLibrary(...)` 都会被污染；最稳口径是直接用源码字面量 `\"`。 |
- `2026-04-18 11:22 +0800`：命令面 smoke 的失败诊断要先保证 smoke 自己能编过。像这轮 `if rc != 0: if len(errorText) > 0: echo(errorText)` 会先把 `mobile_shell_codegen_smoke` 自己打成 `emit nested if failed`；最稳口径是单层 `if rc != 0`，错误文本落文件，终端只打固定提示。 |
- `2026-04-18 11:59 +0800`：ordinary 下的大字符串生成函数不要在拼接表达式里再套 composite helper。像这轮 `v3MobileShellIosProjectPbxproj(...)` 里把 `v3MobileShellQuote(opts.appId)` 直接塞进 `text = text + ...`，会先把整个函数打成 `prepare composite arg add-local failed`；最稳口径是直接写字面量，或者先把 helper 结果单独落局部。 |
- `2026-04-18 11:59 +0800`：手写 Xcode 工程时，跟 `.xcodeproj` 同目录的源码组优先用 `name` 不用 `path`。`path = Sources` 会把实际就在 `ios/` 根下的 `main.m/Info.plist` 错解到 `ios/Sources/*`，而 `name = Sources` 才是稳定骨架。 |
- `2026-04-18 11:59 +0800`：`mobile-shell` 这类平台工程生成面，iOS 最小验收不要停在文件存在。固定再跑一遍 `xcodebuild -list -project <generated.xcodeproj>`，确认 Xcode 真能读出 target/config/scheme，再算工程骨架成立。 |
- `2026-04-18 12:18 +0800`：三节点 BFT follower 不能只靠“等下一轮 commit”追高。像这轮 topup/wave wait，本机 follower 一旦错过某一轮 commit 窗口，bench 就会卡死；最稳口径是让 state sync 支持 `height<=0 => latest snapshot`，并在 follower idle 路径主动拉最新状态机。 |
- `2026-04-18 12:18 +0800`：`msquic: pipe closed` 在当前 QUIC/Follower 链里要当成传输级可重试事件，不要当业务致命错。像这轮本机 follower 就曾因为这条错误在 `wave_3` 后直接退出；把它和 `unexpected eof/pipe read timeout` 同级处理后，进程才不会平白自杀。 |
- `2026-04-18 12:18 +0800`：公网三节点 bench 的收敛判定不能裸依赖 SSH 薄控制面。最稳口径至少要做到两件事：一是远端 `ConnectTimeout` 不要太激进；二是在同一轮 wait 里对 retryable SSH summary 失败复用最后一个已知好值，别把已经抓到的 relay 正确状态因为下一次公网抖动重新判红。 |
- `2026-04-18 12:18 +0800`：bench 环境里如果远端机器还在自动拉起高 CPU 的旁路服务，比如 `/opt/unimaker/libp2p-ingress/src/index.js`，不要继续把 SSH 超时全归咎到 Cheng 主线。先把外部噪声源隔离掉，或者把 relay 观测面改成不依赖 SSH，再看 bench 真正的共识瓶颈。 |
- `2026-04-18 12:27 +0800`：当前 ordinary 生成链里，二进制 `uint8[]` 资源不要再拆成单独 helper 或长函数局部绑定。像这轮 Harmony app icon，只要走 `uint8[]` 返回函数或局部大字面量，就会直接掉 `emit composite binding failed`；最稳口径是改成文本资源，例如 `app_icon.svg`，统一走 `WriteFile`。
- `2026-04-18 12:27 +0800`：Harmony `module.json5` 的最小合同已经坐实，不能再省 `deliveryWithInstall`、`$string:app_name`、`startWindowIcon`、`startWindowBackground`。这几项一旦少任何一个，hvigor 会在 `PreBuild` 阶段直接 schema fail，根本进不到 native 编译。
- `2026-04-18 12:27 +0800`：Harmony XComponent 宿主链接库要按 SDK 真符号来，不要凭经验猜名字。当前现场已坐实：窗口库名是 `native_window`，`OH_NativeXComponent_RegisterCallback` 在 `libace_ndk.z.so`；所以生成的 CMake 至少要链 `ace_napi.z ace_ndk.z hilog_ndk.z native_window`。 |
- `2026-04-18 12:39 +0800`：`build-backend-driver` 只回一句 `v3 backend driver ok` 不代表 live 二进制真的吃到了刚改的 transitive tooling 源。像这轮 `mobile_shell_codegen` 新增 `build-probe` 后，live `artifacts/v3_backend_driver/cheng` 仍然只打印旧 `mobile-shell export` 帮助；最稳口径是直接用 `cheng.stage3 system-link-exec` 现编 `backend_driver_main` 候选，先用 `strings` 或真实子命令确认新能力已经进二进制，再回填正式路径。 |
- `2026-04-18 12:39 +0800`：`mobile-shell build-probe` 的 `app-id` 不能包含目标平台语言关键字。像这轮 `org.cheng.liveprobe.final` 在 Android 上会直接被 Gradle 判成非法 Java 包名；后面默认先避开 `final/class/package` 这类关键字段。 |
- `2026-04-18 12:39 +0800`：当前 `run-host-smokes` 的 ordinary fixture 链接合同还不适合承载 `mobile-shell build-probe` 这种 tooling-heavy 验收入口。即便 smoke 已收成“直接调正式 `backend_driver mobile-shell build-probe`”的最小形状，编译仍会在 `mobile_shell_build_probe_smoke.compile.log` 里炸进 `program_support_backend_v3` 那批 unresolved host/runtime symbol；所以现阶段先用 live `backend_driver` 真实命令和现有 `verify-backend-driver-command-surface` 守主线，不要继续在 host smoke 这条旧链接坑里打转。 |
- `2026-04-18 21:12 +0800`：用户明确说字符串拼接可以直接用 `strformat` 时，Cheng 示例和 fixture 优先用 `std/strformat` 的 `Fmt/Lines` 表达，不再手写长串 `+` 拼接；但要把 `strformat` 回归和别的 parser/codegen 回归拆开，别让同一个 smoke 同时背两个维度。 |
- `2026-04-18 13:32 +0800`：三节点远端控制面不要再把 `scp` 当成稳定前提。像这轮 bench 环境里，`ssh` 还能通时 `scp` 仍会单独炸成 `Connection closed`；最稳口径是直接把本地 `*.remote.sh` 通过 `ssh -T host sh` 的标准输入下发，远端只保留一条会话。 |
- `2026-04-18 13:32 +0800`：seed 这条 primary object/ordinary 主线不能假设 `export_symbol_name` 永远存在。像这轮 `build-bft-state-machine` 真红点就已经坐实：一旦导出名没挂上，provider 会只产出 mangled 符号，`_alloc/_copyMem/_cheng_host_bind` 这类原始导出符号整批消失；最稳口径是按 `source_path + signature_line_number` 回溯源注解，把 `@exportc/@compiler_top_level(export_name=...)` 直接重新解析出来。 |
- `2026-04-18 13:32 +0800`：follower 的 `request-response/recv once` 是 one-shot 流量，不要和长连接复用同一套大超时配置。现场已经坐实：如果仍沿用长超时，本机 follower 会长时间挂在 `msquicNativeDialPumpReady`；最稳口径是给 one-shot client 单独收成短握手、短空闲、短断开超时，再把送票循环限制成有界重试。 |
- `2026-04-18 13:32 +0800`：公网三节点 bench 先隔离远端自启动噪声，再判断 Cheng 主线。像这轮 `dmit/64` 上的 `unimaker-libp2p-ingress.service` 停掉后，远端负载立刻回落，随后 `stage0 deploy + 32x4 relay restart bench` 就能完整收口；以后再遇到“最后一段 wait 偶发拖长”，先查环境里是不是还在跑旁路服务。 |
- `2026-04-18 14:02 +0800`：live `backend_driver` 入口的 BFT bench 现在会 handoff 给 `cheng.stage3`，而且业务报告已经落稳后 wrapper 还可能挂住前台 shell。后面只要再从 live 入口跑正式 bench，默认同时盯 report 文件和 wrapper 进程；如果报告和清场都已经确认，就把 wrapper 当成回收 bug 单独处理，不要误判成共识还没完成。 |
- `2026-04-18 14:02 +0800`：追 fresh `backend_driver_main` rebuild 时，先看是不是被无关 tooling 文件里的旧 ordinary 形状拖住。像这轮真正的挡路点就是 `vpn_proxy_main` 里的旧自写 JSON parser，不是 BFT、不是 gate_main；最稳口径是先把那块 parser 的 `char` 比较链和老算术收平，再继续追 live fresh rebuild。 |
- `2026-04-18 13:45 +0800`：正式 verify 一旦已经能从 live `backend_driver` 和 `cheng.stage3` 两条入口稳定跑通，就不要继续拿旧的大链路 smoke 当唯一 gate。最稳口径是补一个最小 host smoke，只验证正式命令的退出码和关键输出。 |
- `2026-04-18 13:45 +0800`：当前 ordinary 对 `float64` 默认返回值仍有老坑。像这轮 [vpn_proxy_json.cheng](/Users/lbcheng/cheng-lang/v3/src/tooling/vpn_proxy_json.cheng) 的 `return 0.0` 会直接被误降成 `abi=i64`；后面再碰同类函数，默认先落 `float64` 局部再 return。 |
- `2026-04-18 15:05 +0800`：fresh `backend_driver_main` 再红时，先看真正的 transitive parser/file，不要继续沿用上一轮嘴上的大类结论。这轮现场已经坐实：fresh compile 真红点不是泛泛的 `vpn_proxy_main`，而是 [vpn_proxy_json.cheng](/Users/lbcheng/cheng-lang/v3/src/tooling/vpn_proxy_json.cheng) 和 [vpn_proxy_config.cheng](/Users/lbcheng/cheng-lang/v3/src/tooling/vpn_proxy_config.cheng) 那套旧自写 JSON/parser 形状；最稳收法是直接薄封装 `std/json`，再把业务限制单独补回。 |
- `2026-04-18 15:05 +0800`：替换 live 二进制后不要只看时间戳。最稳口径是像这轮一样直接 `shasum -a 256` 对 live 与候选，再同步 `.v3.map`；这样才能确认当前跑的真是 fresh 产物，不是“看起来像换了”。 |
- `2026-04-18 15:05 +0800`：live `bench-bft-validator-three-node` 前台长时间没输出时，先看 `artifacts/v3_gate/bft_bench_wave_*.summary.log` 和正式 report 的新鲜时间，不要立刻判成 wrapper 又挂死。这轮静默期间其实已经推进到 `wave_3/wave_4`，最后还把 `elapsed_ms` 收到 `78702`、`tps` 提到 `1.626`，并且 wrapper 自己正常退出。 |
- `2026-04-18 21:30 +0800`：只要业务 smoke 真要解析 JSON，就不要再把 `std/json.ParseJsonSafe(...)` 当成可用实现。这轮已经现场坐实：当前 bootstrap/runtime 口径下它稳定返回 `json parsing not implemented in bootstrap`；最稳收法是直接在业务模块里落一个严格子集解析器，再用 smoke 守住自己的真实输入面。 |
- `2026-04-18 21:30 +0800`：ordinary 敏感模块先做小而硬的语义闭包，不要急着复用大而全的通用层。像这轮 `vpn_proxy_json`，只支持对象、数组、字符串、整数、布尔、null，再把小数/指数统一硬失败，反而能让 `vpn_proxy_json/config/socks/protocol/routing` 五条 smoke 一次性收绿。 |
- `2026-04-18 15:37 +0800`：fresh `backend_driver_main` 只要被某个重量级命令子系统拖红，第一刀就别继续在主入口里修那个大闭包。最稳口径是先把命令面拆成独立 `tool_main` 或转给已经独立的入口，让 `backend_driver_main` 回到纯控制面；像这轮把 `vpn-proxy` 切到 [vpn_proxy_tool_main.cheng](/Users/lbcheng/cheng-lang/v3/src/tooling/vpn_proxy_tool_main.cheng)、把 BFT 命令组切到 [gate_main.cheng](/Users/lbcheng/cheng-lang/v3/src/tooling/gate_main.cheng) 后，fresh `backend_driver_main` 立刻恢复到 `primary_object_unsupported_function_count=0`。 |
- `2026-04-18 15:37 +0800`：按需现编子工具失败后，不要假设子进程一定会跟外层命令一起干净退出。这轮手测 `vpn-proxy` 时，`cheng.stage3 system-link-exec ... vpn_proxy_tool_main.cheng` 就残留成了孤儿；后面每次手测这类子工具失败，都要顺手查一次 `ps` 并清掉残留编译进程，避免再把机器拖进高压状态。 |
- `2026-04-18 16:20 +0800`：macOS 上凡是变成 `UE/UEs` 的 Cheng 孤儿，先用 `sample` 看是不是统一卡在 `_dyld_start`。这轮 `help`、`vpn-proxy help`、`build-bft-state-machine`、`stop-bft-validator-three-node` 全都只有 `dyld + 主二进制 + /dev/null + stdout/stderr pipe`，`PPID=1` 且 `kill -9` 无效，说明根因不在 Cheng `main` 之后的业务逻辑，而在进程启动链本身；最稳口径是避免并发直起多个 live `artifacts/v3_backend_driver/cheng`，并把这类现场单独当“启动层 wedged exec”处理。 |
- `2026-04-18 16:35 +0800`：live Cheng 可执行文件绝不能走“先删旧文件，再把候选 rename 上去”。这轮 `backend_driver` 和 `stage0` 的安装链都现场坐实了这个坑：一旦 live 路径中间出现空窗，macOS 上并发 `exec` 很容易直接卡进 `_dyld_start`，留下 `UE/UEs` 孤儿。最稳口径是候选和正式产物始终放在同目录，安装时只做单次原子 `rename(candidate, final)` 覆盖，禁止先 `unlink/remove` 最终路径。 |
- `2026-04-18 22:05 +0800`：既然目标是纯 Cheng 和零 C 依赖，就不要再为 process 启动面新增 C helper 或新 C 符号。最稳方向是：API 面先收成 `filePath + argv/env wire`，provider/runtime 自己在 Cheng 里解 wire、管进程，C 侧只做减法，先把 `system/popen` 之类热口摘掉。 |
- `2026-04-18 22:05 +0800`：排这类大链路时，先确认“这轮新红点有没有真的被切走”，再继续追剩余旧红。像这轮 `build-backend-driver` 和 `host_process_*`，只要日志里已经不再出现 `cheng_exec_file_status / cheng_exec_file_pipe_spawn / cheng_pipe_spawn / cheng_pty_spawn / cheng_pty_wait`，就说明进程面这刀已经命中；后面剩下的 `program_runtime.combined` unresolved 要单独记账，不要把它再混成同一个问题。 |
- `2026-04-18 18:22 +0800`：用户已经把 v3 的真风险钉死在“前端规范化太晚、seed/ordinary/wasm 不共享同一份规则”。这轮 shared/wasm mainless 导出也再次现场坐实：同一个 `@compiler_top_level(export_name=...)`，只要 compiler world 和 lowering 一边靠跨行状态、一边靠别的扫描规则，就会直接漂成 `export_surface_symbol_count=0` 和 mangled wasm symbol；后面凡是导出属性、语法糖、Result intrinsic，都必须先收成单一前缀扫描/规范化层，再让所有消费者共用。 |
- `2026-04-18 22:40 +0800`：`program_runtime.combined` 不是“历史包袱对象”，而是 ordinary/native-link 当前还在用的最小运行时聚合物。后面判断 `src/runtime/native` 能不能删时，先看三件事：active link inputs 里还有没有 `program_runtime.combined.*.o`，runtime split list 还有没有 native bridge 片段，provider `@exportc` 实化有没有真把 public symbol 接到 primary/native link。 |
- `2026-04-18 22:40 +0800`：ordinary program 入口 `main` 不能再被“entryFlag 自动导出”这条通用规则带出来。当前主线里 primary object 本身会发 `cheng_v3_program_argv_entry`，provider 再导出宿主 `main`；如果 compiler world / CSG 再把 entry `main` 自动导回 primary，链接就会稳定炸成 `duplicate symbol '_main'`。这条规则要在 seed 和 selfhost 两边一起追平。 |
- `2026-04-18 22:40 +0800`：看到 `infer binding type failed` 不要先归因到 ABI 或编译器大面。像这轮 `program_support_backend_v3.cheng` 的 WebRTC 桥，真根只是把 `fn ...(): str` 漏写成了函数名值；先查“是不是少了 `()` / 少了显式调用”往往比先查 lowering 更短。 |
- `2026-04-18 23:20 +0800`：只要 active runtime split list 在变，`program_runtime.combined` 的 cache 就不能只看“文件是否存在”。最稳口径是把 active split list 写成 manifest，再同时校验 manifest 一致性和 split object 新鲜度；否则旧 combined object 会假装自己还是当前主线。 |
- `2026-04-18 23:20 +0800`：拆 `src/runtime/native` 不能只做“减 split 数量”这半刀，还要顺手看 active `compat_bridge` 有没有把旧 C ABI 从侧门带回来。像这轮 `system_helpers_cmdline_ptr_pty_bridge.inc` 不撤，`___cheng_rt_paramCount` 和 `cheng_pty_*` 仍会回灌到 fresh ordinary 链接。 |
- `2026-04-18 23:20 +0800`：遇到 provider/runtime 边界函数时，不要为了追 exact export 先硬塞 `@exportc`。这轮 `cheng_pty_wait` 已现场坐实，错误 seam 会直接炸成 `EXC_BAD_ACCESS`；先保住 Cheng 本地语义闭环，再决定 exact C 名字是否真的需要暴露。 |
- `2026-04-18 23:55 +0800`：runtime split 收到只剩 1 片以后，不要继续保留“combined object”这个历史壳。最稳收法是把它变成 direct split，并把旧 combined 缓存当 stale 清掉；否则后面排活跃 link inputs 时会一直被这层壳干扰。 |
- `2026-04-18 23:55 +0800`：任何 cache object 只要开始依赖 `probe_split`，就必须同时补“缺 split 自举”和“源码新鲜度重编”。只做 direct split，不补 split 物化，现场一旦 cache 缺件就会立刻假红。 |
- `2026-04-18 23:55 +0800`：seed 和 stage3 的 object plan 规则必须同步，不要让 seed 私自多塞 runtime link input。像这轮 fresh `build-backend-driver` 的回归，就是 seed 还保留着老的 runtime 注入；看到 stage3 和 seed 报告不一致时，第一时间先查这类“旧兼容逻辑回流”。 |
- `2026-04-18 23:58 +0800`：live `backend_driver` 上的零 C 审计别再递归现编 [compiler_main.cheng](/Users/lbcheng/cheng-lang/v3/src/tooling/compiler_main.cheng)。这轮已经坐实，它会直接撞回 ordinary `primary_object_body_semantics_missing`；最稳口径是把当前真要守的零 C 面收成 `backend_driver_main` 自己的本地最小扫描。 |
- `2026-04-18 23:58 +0800`：删除旧命令面时，默认 smoke 列表必须同步换掉。像这轮 `runtime_program_obj` 入口已经删掉，如果还留着 `runtime_program_combined_*` 默认 smoke，就会继续替退场路径续命。 |
- `2026-04-18 23:58 +0800`：legacy native 路径只要还会在缺 `BACKEND_RUNTIME_OBJ` 时自动物化 combined runtime，零 C 主线就是假绿。最稳口径只能是显式 runtime obj 或直接硬失败，不能再偷偷补一个 `program_runtime.combined`。 |
- `2026-04-18 23:59 +0800`：wasm 里只要是“先拿目标地址，再去算复合 lvalue/member/index 源值”的路径，都不要把目标地址放进 `emit_lvalue_address(...)` 自己会复用的 scratch。像这轮 `expr_surface_abi`，`return values[0]` 和 `add(rows, droplets[idx])` 都是同一类串写：目标地址先落 `scratch_local/scratch2`，子表达式求值再把它覆盖。最稳口径是进入这类 lowering 前先把目标地址转存到独占 scratch，再做 copy/store。 |
- `2026-04-18 23:59 +0800`：删旧运行时目录时，顺序必须是“先迁仍存活的旧链路 source list，再清活动源码里的旧路径字面，最后物理删目录”。直接先删目录，只会把审计和 seed 死链一起炸出来，真假混在一团。 |
- `2026-04-18 23:59 +0800`：源码审计里不要把自己要查的旧路径完整字面写回源码。像这轮 `src/runtime/native/` 如果不拆成拼接常量，`legacy_runtime_native_source_path_present` 就会稳定自报自己命中自己。 |
- `2026-04-18 06:16 +0800`：删旧版本目录前，先查包键归一化和私有可见性是不是还把它当内部宇宙成员。像这轮 `v2` 真剩下的活口不在 build/runtime 主链，而在 seed 的 `v3_module_package_key(...)`/`v3_module_paths_share_package(...)` 和 stage1 的 `frontend_lib/semantics`；这几处不先摘，物理删目录也只是表面干净。 |
- `2026-04-18 06:16 +0800`：判断“旧版本是否还依赖着”时，优先看非文档活动源码和 fresh 验收，不要被 Markdown 历史记录带偏。像这轮 `v2` 目录删完后，文档里还会继续出现 `v2/...`，但真正该看的信号是 `rg` 在非文档源码里已经空、fresh `stage0/build-backend-driver/zero-c smokes` 继续全绿。 |
- `2026-04-18 20:30 +0800`：验证编译器源码改动时，先确认入口是不是会偷偷转发到旧 `stage3`。像这轮 live `backend_driver cheng.new run-host-smokes` 实际只会 forward 给现有 `artifacts/v3_bootstrap/cheng.stage3`，根本测不到新源码；真正要验 parser/csg/lowering 变化，必须直接跑 `cheng.stage3 run-host-smokes` 让它现编新的 smoke 二进制。 |
- `2026-04-18 20:30 +0800`：parser/语义 smoke 不要再在测试体里手拼超长源码字符串。像这轮 `parser_normalized_source_smoke` 最先炸的就是长串 `+` 拼接把运行时字符串桥压坏；最稳口径是把待解析源码落成 repo fixture 文件，再由 smoke 读文件去验 parser 逻辑。 |
- `2026-04-18 20:30 +0800`：当前 seed 还扛不住控制面源码里某些局部 `?:` 绑定推断，尤其是 `let name = cond ? a : ""` 这种字符串分支。只要目标是让 selfhost/stage3 现编稳定通过，编译器控制面代码优先写显式 `var + if`，不要继续给 seed 留推断空间。 |
- `2026-04-18 21:15 +0800`：`canonicalGraphCid` 只能承载规范化后的语义信息，不能混进 `exprSummary` 这类源码表面统计。否则像 `proc/else if` 迁到 `fn/elif` 这种语义等价、表面不同的迁移，会被误判成 canonical graph 漂移。 |
- `2026-04-18 21:15 +0800`：`Result[复合值]` 里只要装的是带 `seq/str[]` 的大结构，就不要直接跨后续调用长期拿着 `Value(...)` 用。最稳口径是先 clone 成自有对象，再继续传给 export surface、migration report、system link 这些后续链路。 |
- `2026-04-18 21:33 +0800`：browser host wasm 这条零脚本主线不能再把 helper 作为仓内 `.js` 源文件保留。最稳口径是把 helper payload 收进 Cheng 源码，在运行时临时物化；zero-c/zero-script 审计也要直接守这条活动源码面。 |
- `2026-04-18 21:33 +0800`：当前 ordinary 子集里，用独立 helper 模块走 `os.Open/os.WriteLine` 还不是稳形状。这类小写文件工具如果直接打红，就别继续耗在那条路上，先换成现有 ordinary 能稳定过的 `os.WriteFile(...)` 形状。 |
- `2026-04-18 21:33 +0800`：控制面不要在 Cheng 里逐行拼超长 helper 文本。像这轮 browser wasm runner，只要走大串拼接，`stage3 system-link-exec ... backend_driver_main.cheng` 就会撞 `RC:139`；最稳口径是分块编码，再在运行时解码落盘。 |
- `2026-04-18 21:33 +0800`：browser wasm smoke 的正确 build 入口是正式 `build-browser-host-wasm` 命令，不要为了方便把 [gate_main.cheng](/Users/lbcheng/cheng-lang/v3/src/tooling/gate_main.cheng) 整块编成 ordinary probe。先走权威产物链，再补 smoke 自己的最小运行面。 |
- `2026-04-18 21:33 +0800`：`build-backend-driver` 这类长链看起来长时间无输出时，先查子进程和日志，不要先判成挂死。verify-export 并行阶段本来就会安静很久，这轮最终就是这么跑完的。 |
- `2026-04-18 21:51 +0800`：`ExecFilePipeSpawn` 这条桥的 `writeFd` 必须真连到子进程 stdin，不能再偷懒复用 stdout 管道。像这轮 `node -` 直喂脚本时，旧实现会把整段源码原样写回自己的 run log；最稳口径就是双管道：父进程拿 `stdout read fd + stdin write fd`，子进程 `dup2(stdinReadFd, 0)`、`dup2(stdoutWriteFd, 1/2)`。 |
- `2026-04-18 21:51 +0800`：browser/wasm 运行面要清掉临时 helper 文件时，优先用 `node -` + stdin 直喂源码，不要再物化 `.js` 文件。只要 stdin pipe 是真的，这条路比“写一个临时 JS 文件再执行”更短也更稳。 |
- `2026-04-18 21:51 +0800`：给 `os_host_process` 增能力时，别只改内部 helper，公开包装也要一起补。像这轮 `PtyWrite` 没公开出来，第一条 stdin 回归 smoke 就直接编不过。 |
- `2026-04-18 21:51 +0800`：zero-c/zero-script 审计不能只看源码面。要真证明“运行时也不落 helper 文件”，就得像这轮一样在 smoke 里顺手跑真命令，再断言目标 `.js` 路径不存在。 |
- `2026-04-18 21:51 +0800`：现场若已经有旧的长跑 `run-host-smokes` 挂着，`build-backend-driver` wrapper 可能会被拖很久。这种时候不要硬等它当唯一安装通道；先把 fresh 候选验绿，再按同目录候选文件原子替换 live 二进制，并用 `shasum` 对平。 |
- `2026-04-18 21:50 +0800`：parser 里凡是会做 `line[i - 1]`、`line[rightPos]` 这类索引的边界判断，都不要再写成 `i <= 0 || ...`、`rightPos >= len || ...` 这种短路赌法。当前 ordinary/selfhost 这层不值得信任短路副作用，最稳口径是先落 `leftOk/rightOk` 局部，再显式 `if i > 0`、`if rightPos < len`。 |
- `2026-04-18 21:50 +0800`：表达式规范化改动不能只靠小 fixture 验。像这轮真正把 bug 钉出来的是 `compiler_pipeline_stub_smoke` 在真实 closure 里扫到 [os.cheng](/Users/lbcheng/cheng-lang/src/std/os.cheng) 的 `return defaultValue`；以后 parser/lowering 这种前端基础层改动，至少再带一条真实 source closure smoke 一起跑。 |
- `2026-04-18 22:53 +0800`：typed expr 这类“全闭包统计”验收不要直接对整条 source closure 做零值断言。closure 里只要混进 std/runtime/compiler 自身源码，全量 `typed_expr_abi_polymorphic_count=0 / typed_expr_lower_deferred_count=0` 就会被无关路径污染；最稳口径是先按目标 fixture 的 `sourcePath` 切片，再验那一片自己的 typed facts。 |
- `2026-04-18 22:53 +0800`：ordinary 编译器控制面里，`setLen(seq, 0)`、把复合字符串直接拼进调用参数、以及 smoke 里手写长串 `+` 都还是高风险形状。后面默认直接用“显式空序列赋值 + concat helper + 单字符串 literal”，不要再反复踩同一个坑。 |
- `2026-04-18 22:53 +0800`：为了先让 ordinary 通过，不要把前端错误信息永久降成模糊常量。像这轮 `func` alias 拒绝，走稳定 concat helper 后，既能保住精确文本 `unsupported top-level function alias 'func'`，又不会把控制面重新打红；以后遇到同类情况，优先恢复精确错误。 |
- `2026-04-18 22:59 +0800`：ordinary/stage0 热路径里的字符串 `+` 链不要再手写。只要是 wire key、bundle seed、错误文本、HTTP/relay/tailnet/webrtc 这类热路径，直接上稳定 helper，像 `concat_assign/prefix_key/lines_with_newline/build_bundle_seed` 这种小函数，比到处写 `a + b + c` 稳得多。 |
- `2026-04-18 22:59 +0800`：碰到 browser host native contract / wasm ABI 这类崩点，先切最小 probe，再用 `lldb` 把崩点钉到单个 helper 或单个 lowering 形状，别一上来就在整条 smoke 里盲修。 |
- `2026-04-18 22:59 +0800`：host smoke 行为一旦和最新源码对不上，先清 `artifacts/v3_hostrun/<label>` 再复跑；旧 label 目录会把很多已经修掉的问题伪装成还没修。 |
- `2026-04-18 23:08 +0800`：typed lowering 只要开始给后端当真 contract，就不要只停在 `type/abi/place/lowering` 这种浅标签。最稳口径是把 `call_kind + return_kind` 一起写进共享 fact，让 `compiler_csg/lowering_plan` 和后面所有消费者都吃同一份 key。 |
- `2026-04-18 23:08 +0800`：`str/rawbytes/seq/result` 这类复合值的地址/返回语义别再散在 native/wasm 各处手写判断。先收成 `type_prefers_address / function_return_prefers_address` 这种共享 helper，再把显式 `return`、隐式 `return`、静态返回槽、复合 call-return 这些总入口统一接过去，后面才不会继续 target 分叉。 |
- `2026-04-19 04:37 +0800`：只要新 helper 被插到 seed 前半段，就先把后面才定义的 C 符号一并补前置声明。像这轮 `v3_emit_address_call_arg_spill(...)` 一进来就会用到 `v3_emit_slot_address / v3_emit_local_value_address / v3_emit_call_arg_spill_store / v3_materialize_composite_expr_into_*`；不先补 prototype，`-std=c11 -pedantic` 会直接把它们全打成 implicit declaration。 |
- `2026-04-19 04:37 +0800`：wasm 参数分析和发射不要再分别写“是不是 composite”判断。最稳口径是像这轮一样先收一个 `call_arg_prefers_address`，再让 analyze path、statement path、builtin path、emit path 都吃这同一个判断；否则 native/wasm 刚收平，wasm 内部自己又会重新分叉。 |
