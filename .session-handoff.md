# 会话交接 (2026-05-13)

## 当前状态

- 冷编译器：28/28 回归 PASS
- combined kernel：3061 行，exit 42
- roadmap items 1-8：7/8 ✅

## add() target 判定扩展（本次核心修复）

**问题**：`add(var T[], T)` 调用中 target 为函数参数时，
`parse_seq_lvalue_from_span` 的 `locals_find` 找不到参数名
（因导入体编译期 locals 可能不完整）。

**修复**：
- `BodyIR` 新增 `param_name[COLD_MAX_I32_PARAMS]` 字段
- `parse_fn` 在参数注册时同步存储 arena-copy 的参数名
- `parse_seq_lvalue_from_span` 在 `locals_find` 失败后回退到
  `body->param_name` 查找，匹配序列类型参数（SLOT_SEQ_*）

**附带修复**：
- `cold_collect_import_module_signatures`：无 body 的导入函数
  （如 `@importc` 声明体）标记 `is_external`
- `parser_find_object` / `parser_resolve_object`：
  导入别名作用域内的裸名对象查找

## 下一卡点：opaque sequence 元素大小

`compiler_runtime_smoke` 下一错误：
`BackendBuildPlanSharedCompilerSourceUnits` 中 `BackendSourceUnit`
结构体 88 字节，但 opaque sequence 元素大小仅 32 字节。
需完整 struct 布局计算或变长序列存储支持。

## 已知 gap

1. **opaque sequence 元素大小不匹配**：需完整 struct 布局
2. **泛型 variant 构造**：`Ok[T](val)` 需要泛型实例化后重查找
3. **闭包环境捕获**：需要 env 结构 + trampoline
