# Cheng-lang Cold Compiler -- Session Handoff

## State
- **Cold compiler regression**: 28/28 PASS
- **Item 8**: `thread_atomic_orc_runtime_smoke` ✅ 编译运行（exit 11 = 单线程 skip）
- **Item 8**: `compiler_runtime_smoke` ❌ 需完整 `cheng/core/` 导入链
- **Combined kernel**: 2263 lines, CSG v2 往返验证

## 本次关键修复 (97adfa79)
1. `cold_compile_import_function_direct`：不可解析签名 `die` → `continue`
2. `cold_compile_reachable_import_bodies`：入口模块裸名函数无 body 时标记 `is_external`

## 已知 gap
- `WaitAtomicAtLeast`/`AtomicIncrement` 在隔离测试中正常编译，在完整文件中被标记 external
- 调试显示 `parse_fn` 正常返回 body（非 NULL，非 has_fallback），但可达性分析未将其纳入 codegen
- 推测：`main()` 中 `if thread.Parallelism() <= 1: return 11` 的提前返回导致可达性分析跳过后续调用

## 其他改进
- `function_cap` 32→512
- `--diag:dump_per_fn` 时输出 patch 详细信息

## Quick build + test
```bash
cd /Users/lbcheng/cheng-lang
cc -std=c11 -O2 -o /tmp/cheng_cold bootstrap/cheng_cold.c && \
bash tools/cold_regression_test.sh
```
