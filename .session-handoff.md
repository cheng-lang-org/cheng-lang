# Cheng-lang Cold Compiler -- Session Handoff

## State
- **Cold compiler regression**: 28/28 PASS
- **`thread_atomic_orc_runtime_smoke`**: 首次编译通过并运行（exit 11，4 个函数被 stub 化）
- **`atomic_i32_runtime_smoke`**: exit 0 ✅
- **Combined kernel**: 2263 lines，CSG v2 往返验证就位

## 本次关键修复

### Import body 编译容错 (97adfa79)
- `cold_compile_import_function_direct`：不可解析的函数签名 `die` → `continue`（与签名收集一致）
- `cold_compile_reachable_import_bodies`：入口模块裸名函数无 body 时标记 `is_external`

### 其他改进
- `function_cap` 32→512（降低 realloc 风险）
- `--diag:dump_per_fn` 时输出 patch 详细信息
- Combined kernel: CSG v2 stmt+expr token 往返验证

## 剩余 gap（Item 8）
`thread_atomic_orc_runtime_smoke` 中 `WaitAtomicAtLeast` 等函数被 stub 化。
这些函数在隔离测试中可正常编译，但在完整文件上下文中失败——推测与 `var` 全局块或错误恢复后的解析器状态有关。

## Quick build + test
```bash
cd /Users/lbcheng/cheng-lang
cc -std=c11 -O2 -o /tmp/cheng_cold bootstrap/cheng_cold.c && \
bash tools/cold_regression_test.sh
```
