import spawn_api_default_module as rt

var gCount: int32 = 0

@thread_boundary
fn producer() =
    gCount = gCount + 1

@thread_boundary
fn main(): int32 =
    rt.spawn(producer)
    return gCount - 1
