import std/rawbytes

fn main(): int32 =
    let bytes: rawbytes.Bytes = rawbytes.bytesFromString("ab")
    if rawbytes.bytesLen(bytes) != 2:
        return 1
    if rawbytes.bytesToString(bytes) != "ab":
        return 2
    return 0
