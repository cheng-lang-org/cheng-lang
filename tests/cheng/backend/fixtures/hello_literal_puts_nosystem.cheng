type
    cstring = char*
    str = char*

@importc("puts")
fn cPuts(s: cstring): int32

fn main(argc: int32, argv: str*): int32 =
    argc
    argv
    cPuts(cstring("hello literal puts nosystem"))
    return 0
