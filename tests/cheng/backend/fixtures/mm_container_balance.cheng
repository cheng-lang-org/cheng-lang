import std/seqs
import std/strings

fn main(): int32 =
    var values: str[]
    add(values, "a")
    add(values, "b")
    values[0] = "c"
    values[1] = "d"

    if values.len != 2:
        return 1
    if values[0] != "c":
        return 2
    if values[1] != "d":
        return 3

    return 0
