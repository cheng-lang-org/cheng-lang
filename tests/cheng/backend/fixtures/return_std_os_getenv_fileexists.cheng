import std/os

fn main(): int32 =
    let home: str = os.getEnvDefault("HOME", "")
    if len(home) == 0:
        return 11
    if !os.fileExists("/bin/sh"):
        return 12
    if !os.fileExists("/usr/bin/env"):
        return 13
    return 0
