import std/rawbytes

fn makeBytes(): rawbytes.Bytes =
    return rawbytes.bytesFromString("ab")

fn main(): int32 =
    let bytes: rawbytes.Bytes = makeBytes()
    if bytes.len != 2:
        return 1
    if bytes.data == nil:
        return 2
    return 0
