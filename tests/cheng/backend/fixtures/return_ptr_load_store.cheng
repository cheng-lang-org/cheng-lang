fn setValue(p: ptr, v: int32) =
    store(p, v)

fn loadValue(p: ptr): int32 =
    return load(p)

fn main(): int32 =
    var x: int32
    let p: ptr = addr(x)
    store(p, 42)
    let v: int32 = load(p)
    return v - 42
