# cheng-pkg-config.toml (template)
# 建议搜索顺序：
# 1) CLI 指定路径（如 --config:<file>）
# 2) 当前工作目录 ./cheng-pkg-config.toml
# 3) 项目根目录 configs/cheng-pkg-config.toml
# 4) $HOME/.cheng/cheng-pkg-config.toml

[remote]
mode = "auto"                # local|p2p|auto
peers = []
allow = []
deny = []
http_fallback = false
require_signature = true
require_registry_match = true
