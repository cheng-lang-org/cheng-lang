import cheng/stdlib/bootstrap/system
type
    Node = ref
        val: int32
        next: Node
fn main() =
    var n: Node = nil # n = new(Node) # new not supported yet? Or default[Node] for ref?
    # ref types usually handled by GC or manual alloc.
    # let's just declare type to see if type decl recursiveness crashes.
    echo "ok"
    main()
