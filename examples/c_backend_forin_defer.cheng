import cheng/stdlib/bootstrap/seqs

fn main(): int32 =
    var items: int32[] 
    seqAdd(items, 1)
    seqAdd(items, 2)
    var sum: int32 = 0
    defer:
        sum = sum + 1
    for x in items:
        sum = sum + x
    return sum
