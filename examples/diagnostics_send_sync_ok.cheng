module diagnostics_send_sync_ok

import std/sync

@thread_boundary
fn spawnJob(value: T) =
    0
fn main() =
    let a: Arc[int32] = arcNew 1
    spawnJob a
    let m: Mutex[int32] = mutexNew 2
    spawnJob m
    let l: RwLock[int32] = rwLockNew 3
    spawnJob l
    let atom: Atomic[int32] = atomicNew 4
    spawnJob atom
