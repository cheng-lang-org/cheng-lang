type
    MyObj =
        x: int32
type
    MyNode = ref MyObj
fn main() =
    var size: int = size
    of (MyObj)
    var p: void* = alloc(size)
    var n: MyNode = MyNode(p)
    n->x = 10
    if n->x == 10:
        # Use expression statement to satisfy strict statement requirement?
        echo "OK"
        main()
