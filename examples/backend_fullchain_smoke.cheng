@importc("puts")
fn cPuts(s: cstring): int32

fn sumTo(n: int32): int32 =
    var acc: int32 = 0
    for i in 0..<n:
        acc = acc + i
    return acc

fn main() =
    let got: int32 = sumTo(128)
    if got == 8128:
        cPuts("fullspec ok")
        return
    cPuts("fullspec fail")
