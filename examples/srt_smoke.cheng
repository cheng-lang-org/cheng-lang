import std/os
import std/strutils as strutil

fn main(): int32 =
    let cwd: str = os.getCurrentDir()
    if len cwd > 0:
        echo strutil.Join(["srt ok: ", cwd], "")
    else:
        echo "srt ok"
    return 0
