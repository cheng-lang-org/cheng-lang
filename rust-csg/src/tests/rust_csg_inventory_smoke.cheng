import cheng/rust-csg/core/inventory

fn main(): int32 =
    var stats = inventory.RustCsgCollectSourceStats()
    var reason = ""
    assert(inventory.RustCsgInventoryOk(stats, reason), reason)
    echo("rust_csg_inventory_smoke ok")
    return 0
