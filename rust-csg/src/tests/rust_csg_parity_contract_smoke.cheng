fn main(): int32 =
    let status = "explicitly_blocked"
    let reason = "rust-csg blocked: full Codex parity is not implemented"
    let blockedExit = 78
    assert(status == "explicitly_blocked", "rust-csg parity: status")
    assert(reason == "rust-csg blocked: full Codex parity is not implemented", "rust-csg parity: reason")
    assert(blockedExit != 0, "rust-csg parity: blocked exit non-zero")
    echo("rust_csg_parity_contract_smoke ok")
    return 0
