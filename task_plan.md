# 当前任务计划

- 迁移目标：仓库根包固定为 `pkg://cheng`，唯一源码树为 `src`，编译器内核在 `src/core`。
- 已完成：源码、测试、seed、r2c 工具路径和公开环境变量去掉旧版本标识。
- 已完成：`chain_node` 从核心编译门禁移除，保留为应用/领域手动命令。
- 已完成：自托管编译主线补资源硬门禁，避免 backend driver / follow-world 长编译无上限吃内存。
- 已完成：C seed `build-backend-driver` 长路径改成实时进度、默认 8GiB RSS 上限，并刷新出新的 `artifacts/backend_driver/cheng`。
- 已完成：旧根 shared builtin、旧根 tooling、驼峰应用目录收口到当前模块布局。
- 已完成：`run-host-smokes <smoke...>` 的普通 fixture 控制面从 C seed 迁到 backend driver 的纯 Cheng host smoke gate。
- 已完成：world/libp2p 自托管主线 smoke 通过，覆盖 managed dependency mirror、world bundle sync、fresh-node selfhost receipt、multi-hop migration proof。
- 已完成：`world-receipt` 控制面从 C seed 转入 `src/core/tooling/world_receipt_gate.cheng`，backend driver 不再把该命令转发给 `stage3 system-link-exec`；它是 world/proof 命令，不进入核心编译门禁。
- 已完成：backend driver 的 `print-line-map` 文本生成改由 `src/core/backend/line_map.cheng` 执行，不再为该命令转发到 C seed。
- 已完成：backend driver 的 `debug-report` / `print-symbols` 文本生成改由轻量 `src/core/tooling/debug_tools_gate.cheng` 执行，不再为这两个命令转发到 C seed。
- 已完成：world/full-closure manifest 的 root-stable 口径修正，`csg_root_cid` 全面使用 canonical CSG，source closure 按 module path 稳定排序，migration equivalence 不再绑定 source bundle/raw graph。
- 已完成：backend driver 的 `system-link-exec` 成功 exe 编译后由 `src/core/backend/line_map.cheng::WriteExecutableLineMap` 写 `.map` sidecar；C seed 在 driver 转发路径只负责链接，不再写该 sidecar。
- 已完成：纯 Cheng direct object writer 底座已新增 `object_buffer/object_symbols/object_relocs/macho_object_writer/direct_object_emit`，并接到 `system_link_exec_runtime` 的 standalone zero-exit、return-zero `emit:obj` 和 return-call BR26 relocation `emit:obj` 主线，已绕开 `.s` 汇编文本生成最小 Mach-O 对象。
- 已完成：backend driver 内置 `direct_object_writer_smoke` 和 `seed_minimization_boundary_smoke`，用真实 `system-link-exec` 报告断言 direct object、hidden seed surface、cache prime/restore，不再编译巨型测试 wrapper。
- 已完成：`system-link-exec` cache 和 provider object cache 的 codegen key 纳入纯 Cheng codegen/direct object writer 模块，避免 C seed 最小化后误命中旧缓存。
- 已完成：C seed 活命令面收窄为 `print-contract/self-check/bootstrap-bridge/build-backend-driver/compile-bootstrap`；非自举命令必须 handoff 到 backend driver，handoff 禁用时硬失败，不再回落到 C 旧实现。
- 已完成：`bootstrap/cheng_seed.c` 本体已按 clang 不可达清单删除旧 C 命令和辅助函数，C seed 语法检查无 `unused function` / `unneeded internal declaration` 警告。
- 已完成：`build-backend-driver` 性能先行优化，C seed `primary_object_emit` 复用 `primary_object_plan` 缓存的 scalar 函数文本，不再重复生成函数体；最新总耗时 `138992ms`。
- 已完成：backend driver 最小主线闭包裁剪，cross-target/debug-runtime/network/orphan/r2c/stage23-libp2p/wasm gate 不再直接 import 到 backend driver；最新总耗时 `109314ms`。
- 已推进：`primary_object_plan` 的 root/reachability 改成结构化规则：`--emit:exe` 只从入口函数沿内部 call 闭包发射，`--emit:obj` 以源内全部函数为 root；动态 pending 队列已改成 bounded `for ... in range` frontier。
- 已推进：primary object plan/report 新增 `primary_object_reachable_function_count`、`primary_object_first_missing_function`、`primary_object_first_missing_body_kind`，backend driver plan-not-ready 输出同步打印首个不可发射函数。
- 已推进：primary object plan/report 新增 `primary_object_first_missing_line_number`，只传结构化行号，不扩大 `PrimaryObjectIR` 函数记录；当前 blocker 精确到 `BackendDriverMain` 第 2878 行。
- 已完成：typed IR 新增最小 `let/var binding = Call(...)` statement 节点；诊断通过 `primary_object_first_missing_typed_ir_kind=stmt_let_call` 暴露，不再污染 `PrimaryObjectIR.bodyKind`。
- 已完成：最小 `let value: int32 = Helper(); return 0` 与 `return value` 语句切片已进入真实 codegen，body kind 为 `let_call_noarg_return_i32_const_N` / `let_call_noarg_return_result_i32`，Darwin arm64 direct writer 和文本汇编 fallback 均保存恢复 LR 后 `bl target`，再返回常量或调用结果。
- 已完成：最小 `let value: int32 = Helper(); if value <= 0: return 0; return value` 语句+控制流切片已进入真实 codegen，body kind 为 `let_call_noarg_i32_if_result_le_zero_return_zero_else_result_i32`，Darwin arm64 direct writer 和文本汇编 fallback 均在 `bl target` 后用 `cmp/b.gt` 分支并保存恢复 LR。
- 已完成：最小 `let value: str = EmptyText(); ConsumeText(value); return 0` 复合 local 跨语句传参切片已进入真实 codegen，body kind 为 `let_call_noarg_str_then_call_arg0_return_i32_const_0`，Darwin arm64 direct writer 和文本汇编 fallback 均在同一函数内发两次 `bl`、两条 BR26 relocation，第一次 call 使用隐藏 sret `x8`，第二次 call 通过 `x0` 传同一栈上 `str` local 地址。
- 已完成：同一 `str` sret/local/arg0 切片已支持无显式类型的 `let value = EmptyText()`；typed_expr 只识别 let-call 语句形状，lowering 必须从 typed call fact 的返回类型证明首个 call 是 `str` 后才产出 `let_call_noarg_str_then_call_arg0_return_i32_const_0`。
- 已推进：`strLenFast` 现在由 `typed_expr` 生成结构化 IR，`lowering_plan` 映射为 `str_len_fast_i32`，direct Mach-O writer 和文本汇编 fallback 均有真实 arm64 指令。
- 已推进：return-call 生成改为依赖 typed fact 的 `returnRoot`，普通 binding call 不再触发 `return_call_*`；新增 `non_return_call_not_primary_fixture` 做防回潮探针。
- 下一步：当前纯 self probe 已越过旧 `BackendDriverMain`/入口 statement blocker，新的硬缺口是 `_DirectObjectEmitWriteObject` 的 `instruction_word_zero`；继续把 `PrimaryObjectPlan.functionBodyIRs` 中的通用 BodyIR/statement sequence 正式物化为 Darwin arm64 instruction words 和 relocation，先覆盖 `DirectObjectEmitWriteObject` 这类多 statement、多 call、Result/composite local 路径。完成前纯 `system-link-exec` 自举仍会硬失败为 `primary_object_body_semantics_missing`，不能用假 stub、跳过 unsupported 函数或 C seed fallback 冒充完成。
- 已推进：纯 Cheng 推理内核与 smoke 已按新规范把计数型 `while` 改为 `for ... in range`，字符串组合改走 `Fmt`。
- 本次边界审计切片：
  - provider objects -> `src/core/backend/provider_object_materializer.cheng`; 验证用 `artifacts/backend_driver/cheng debug-report --root:. --in:src/tests/ordinary_call_chain_fixture.cheng --target:arm64-apple-darwin --emit:obj`
  - primary object emit -> `src/core/backend/primary_object_materializer.cheng`; 验证用 `artifacts/backend_driver/cheng print-asm --root:. --in:src/tests/ordinary_call_chain_fixture.cheng --target:arm64-apple-darwin --emit:obj`
  - native link -> `src/core/backend/native_link_materializer.cheng`; 验证用 `artifacts/backend_driver/cheng system-link-exec --root:. --in:src/tests/ordinary_zero_exit_fixture.cheng --target:arm64-apple-darwin --emit:exe --out:/tmp/cheng-native-link-smoke`
  - line-map text/print command -> 已落 `src/core/backend/line_map.cheng`; 验证用 `artifacts/backend_driver/cheng print-line-map --root:. --in:src/tests/ordinary_zero_exit_fixture.cheng --target:arm64-apple-darwin --emit:obj`
  - line-map sidecar write -> 已接 `src/core/backend/line_map.cheng::WriteExecutableLineMap` 到 backend driver `system-link-exec` 成功路径；验证用 `artifacts/backend_driver/cheng run-host-smokes seed_minimization_boundary_smoke`
  - debug tools -> 已接入 backend driver command surface 和 `verify-debug-tools`；验证用 `artifacts/backend_driver/cheng verify-debug-tools`
  - direct object writer -> 已接 standalone no-runtime exe、return-zero `emit:obj`、return-call BR26 relocation `emit:obj`、最小常量返回 call-chain direct exe、最小 `let call; return const/result` 非叶子 direct exe、最小 `let call result; if result <= 0 return 0 else return result` 控制流对象、最小 `str` sret/local/len 对象、最小 `str` sret local 再作为 arg0 传给后续 call 的多 statement/multi-reloc 对象、同一 `str` 传参切片的无显式类型 let-call 对象、最小二参 `float64 * float64 -> float64` 对象函数、最小二参 `int32 + int32 / int32 - int32 / int32 * int32 / int32 >> int32 -> int32` 对象函数、最小三参 `int32 * int32 + int32 -> int32` 与 `acc + lhs * rhs` 乘加对象函数和 `float64_mul_backend_smoke` 的真实 `fmul/fcmp` 退出码 exe；这些算术/乘加/let-call body kind 已改为 `typed_expr` 构建 `TypedExprIr`，`lowering_plan` 从 `TypedExprIr -> PrimaryObjectIR` 派生结构化 facts，`primary_object_plan` 只消费 `PrimaryObjectIR`，不再本地解析签名和 return 表达式。standalone 入口必须是 provider-free 严格 codegen 形状，其它已支持真实函数必须一起写入对象，dead-f64 exe 必须成功且 `otool` 可见 `fmul`，call-chain exe 必须 `otool -rv` 可见两条 `BR26`，let-call exe 必须保存恢复 `x29/x30` 并在真实 `bl` 位置生成 BR26，str local 后续传参切片必须 `otool -rv` 可见两条 `BR26` 且 `otool -tV` 可见第二次 call 前 `add x0, sp, #16`，无显式类型版本必须由 typed fact 返回类型证明为 `str`，i32 常量返回 exe 必须支持 `return 7` 和表达式体 `= 7`，退出 7 且报告 `system_link_exec_runtime_standalone_direct_entry=1`，i32 add/sub/mul/asr/madd 对象必须 `otool -tV` 可见 `add/sub/mul/asr/madd` 指令。entry bridge 机器码/relocation 已在 writer 内准备，但 backend driver 自身 `main` 仍未进入严格 codegen，非 standalone exe 全量自举继续 hard-fail 而不是降级。验证用 `artifacts/backend_driver/cheng run-host-smokes direct_object_writer_smoke seed_minimization_boundary_smoke float64_mul_direct_object_smoke float64_mul_backend_smoke`，以及 `artifacts/backend_driver/cheng system-link-exec --root:. --in:src/tests/ordinary_call_chain_fixture.cheng --target:arm64-apple-darwin --emit:obj --out:/tmp/cheng-call-chain-direct.o`
- 验收口径：源码、路径名、二进制字符串 grep 清零；`bootstrap-bridge`、路径 smoke、共享 builtin smoke、tooling 文档 smoke、应用迁移 smoke 通过。
