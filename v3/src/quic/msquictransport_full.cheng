# MsQuic transport full path (no-pointer consolidated).
#
# Full path now routes to native UDP datapath implementation.
# Stub remains test-only and is not in the production import chain.

import cheng/v3/quic/msquictransport_native
