# MsQuic platform time helpers(port scaffold).

import std/times
fn msquicPlatformNowMs(): int64 =
    return epochTimeNs() / 1000000

fn msquicPlatformNowUs(): int64 =
    return epochTimeNs() / 1000
