import std/os

fn main(): int32 =
    let fd = os.udpBindFdBridge("127.0.0.1", 6210, 0)
    if fd < 0:
        return 0 - fd
    let closeRc = os.udpCloseFd(fd)
    if closeRc != 0:
        return 111
    return 0
