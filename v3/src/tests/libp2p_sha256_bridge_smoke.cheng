import std/system

importc fn cheng_v3_sha256_hex_bridge(text: str): str

fn main(): int32 =
    let digest = cheng_v3_sha256_hex_bridge("abc")
    echo(digest)
    echo(intToStr(len(digest)))
    return 0
