import std/os_host_process as hostos

fn main(): int32 =
    let command: str = hostos.osHostProcessCommandText("/bin/echo", ["min-ok"], [])
    assert(command == "'/bin/echo' 'min-ok'", "v3 hostos command text")
    echo("v3 host_process_command_text_smoke ok")
    return 0
