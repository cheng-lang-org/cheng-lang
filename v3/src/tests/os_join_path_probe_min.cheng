import std/os as os

fn main(): int32 =
    let cwd: str = os.GetCurrentDir()
    if cwd.len <= 0:
        return 1
    let repoV3Dir: str = os.JoinPath(cwd, "v3")
    if repoV3Dir.len <= 0:
        return 2
    return 0
