#ifndef _GNU_SOURCE
#define _GNU_SOURCE 1
#endif

#ifndef _DEFAULT_SOURCE
#define _DEFAULT_SOURCE 1
#endif

#ifndef _POSIX_C_SOURCE
#define _POSIX_C_SOURCE 200809L
#endif

#ifndef _DARWIN_C_SOURCE
#define _DARWIN_C_SOURCE 1
#endif

#include <ctype.h>
#include <dirent.h>
#include <errno.h>
#include <fcntl.h>
#include <limits.h>
#include <stdbool.h>
#include <stdint.h>
#include <stdarg.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <signal.h>
#include <spawn.h>
#include <time.h>
#include <sys/stat.h>
#include <sys/wait.h>
#include <sys/types.h>
#include <unistd.h>
#if defined(__APPLE__)
#include <mach-o/dyld.h>
#endif

#ifndef PATH_MAX
#define PATH_MAX 4096
#endif

#if defined(__GNUC__) || defined(__clang__)
#define CHENG_V3_UNUSED __attribute__((unused))
#else
#define CHENG_V3_UNUSED
#endif

extern char **environ;

#ifndef CHENG_V3_IMPL_SOURCE_PATH
#define CHENG_V3_IMPL_SOURCE_PATH __FILE__
#endif

#ifndef CHENG_V3_EMBEDDED_SOURCE_PATH
#define CHENG_V3_EMBEDDED_SOURCE_PATH ""
#endif

#ifndef CHENG_V3_EMBEDDED_CONTRACT_TEXT
#define CHENG_V3_EMBEDDED_CONTRACT_TEXT ""
#endif

#define CHENG_V3_MAX_FIELDS 40
#define CHENG_V3_MAX_CALL_ARGS 16
#define CHENG_V3_CALL_ARG_REGS 8
#define CHENG_V3_CALL_ARG_SPILL_STRIDE ((CHENG_V3_MAX_CALL_ARGS + 4) * 8)
#define CHENG_V3_MAX_UNSUPPORTED_DETAILS 64
#define CHENG_V3_MAX_TYPE_FIELDS 256
#define CHENG_V3_MAX_TYPE_DEFS 512
#define CHENG_V3_MAX_TYPE_PARAMS 8
#define CHENG_V3_CID_HEX_LEN 64
#define CHENG_V3_CID_HEX_CAP 65
#define CHENG_V3_CONST_MAX_LOOP_ITERS 100000U

typedef struct {
    uint8_t bytes[32];
    char hex[CHENG_V3_CID_HEX_CAP];
} V3Cid32;

typedef struct {
    uint8_t *data;
    size_t len;
    size_t cap;
} V3ByteBuf;

typedef struct {
    uint8_t data[64];
    uint32_t datalen;
    uint64_t bitlen;
    uint32_t state[8];
} V3Sha256Ctx;

typedef struct {
    char *key;
    char *value;
} V3BootstrapField;

typedef struct {
    V3BootstrapField fields[CHENG_V3_MAX_FIELDS];
    size_t len;
    char source_path[PATH_MAX];
} V3BootstrapContract;

typedef struct {
    char **items;
    size_t len;
    size_t cap;
} V3OwnedStringVec;

typedef enum {
    V3_FORBIDDEN_SUBSTRING = 0,
    V3_FORBIDDEN_LABEL_COLON_STR = 1,
    V3_FORBIDDEN_WHOLE_WORD = 2
} V3ForbiddenPatternKind;

typedef struct {
    V3ForbiddenPatternKind kind;
    const char *needle;
    const char *label;
} V3ForbiddenPattern;

typedef struct {
    char *path;
    const char *label;
} V3ForbiddenHit;

typedef struct {
    V3ForbiddenHit *items;
    size_t len;
    size_t cap;
} V3ForbiddenHitVec;

typedef bool (*V3ReadyCheckFn)(const void *ctx);

static bool v3_quote_is_escaped(const char *text, size_t index);
static bool v3_status_is_exit_code(int status, int code);
static bool v3_run_binary_capture_output(char *const argv_local[],
                                         const char *log_path,
                                         int *status_out);
static bool v3_expand_strformat_fmt_expr(const char *expr_text,
                                         char *expanded_out,
                                         size_t expanded_cap,
                                         bool *changed_out,
                                         char *error_out,
                                         size_t error_cap);
static char *v3_debug_object_report(const char *path);
static bool v3_backend_driver_handoff_enabled(void);
static int v3_cmd_verify_export_visibility_impl(int argc, char **argv);
static int v3_cmd_verify_backend_driver_command_surface_impl(int argc, char **argv);
static int v3_cmd_verify_mobile_shell_build_probe_impl(int argc, char **argv);
static const char *v3_host_target_triple(void);
static bool v3_path_last_component_is(const char *path, const char *name);
static bool v3_path_exists_nonempty(const char *path);
static bool v3_package_manifest_exists(const char *dir);
static void v3_find_shared_v3_root(const char *package_root, char *out, size_t cap);
static void v3_shared_v3_package_root(const char *workspace_root, char *out, size_t cap);
static void v3_find_nearest_package_root(const char *base_root,
                                         const char *source_abs,
                                         char *out,
                                         size_t cap);
static bool v3_run_shell_command_status(const char *command, const char *label);
static bool v3_run_command_logged_argv(size_t arg_count,
                                       const char **args,
                                       const char *log_path,
                                       const char *label);

static const char *V3_REQUIRED_KEYS[] = {
    "syntax",
    "bootstrap_name",
    "bootstrap_entry",
    "compiler_class",
    "bootstrap_source_kind",
    "target",
    "compiler_parallel_model",
    "program_parallel_model",
    "ir_facts",
    "data_layout",
    "aot",
    "forbidden",
    "supported_commands",
    "compiler_entry_source",
    "compiler_runtime_source",
    "compiler_request_source",
    "tooling_gate_source",
    "lang_parser_source",
    "backend_system_link_plan_source",
    "backend_lowering_plan_source",
    "backend_primary_object_plan_source",
    "backend_object_plan_source",
    "backend_native_link_plan_source",
    "backend_system_link_exec_source",
    "runtime_core_runtime_source",
    "runtime_compiler_runtime_source",
    "runtime_debug_runtime_source",
    "tooling_bootstrap_contract_source",
    "backend_build_plan_source",
    "ordinary_command",
    "ordinary_pipeline_state",
    "user_program_auto_parallel",
    "bagua_bpi"
};

static void v3_usage(void) {
    puts("cheng_v3_seed");
    puts("usage:");
    puts("  note: when artifacts/v3_backend_driver/cheng is ready, non-bootstrap commands auto-forward there");
    puts("  cheng_v3_seed print-contract [--in:<path>]");
    puts("  cheng_v3_seed self-check [--in:<path>]");
    puts("  cheng_v3_seed status [--contract-in:<path>]");
    puts("  cheng_v3_seed print-build-plan [--contract-in:<path>]");
    puts("  cheng_v3_seed scan-hotpath");
    puts("  cheng_v3_seed print-bootstrap");
    puts("  cheng_v3_seed bootstrap-bridge");
    puts("  cheng_v3_seed build-backend-driver");
    puts("  cheng_v3_seed run-smokes");
    puts("  cheng_v3_seed slice-gate");
    puts("  cheng_v3_seed debug-report [--contract-in:<path>] [--in:<path>] [--root:<path>] [--emit:<exe|shared|obj>] [--target:<triple>] [--out:<path>] [--channel:<stable|edge>] [--world-head:<cid>] [--lock:<path>] [--baseline-surface:<path>] [--report-out:<path>]");
    puts("  cheng_v3_seed print-symbols [--contract-in:<path>] [--in:<path>] [--root:<path>] [--emit:<exe|shared|obj>] [--target:<triple>] [--out:<path>] [--channel:<stable|edge>] [--world-head:<cid>] [--lock:<path>] [--report-out:<path>]");
    puts("  cheng_v3_seed print-line-map [--contract-in:<path>] [--in:<path>] [--root:<path>] [--emit:<exe|shared|obj>] [--target:<triple>] [--out:<path>] [--channel:<stable|edge>] [--world-head:<cid>] [--lock:<path>] [--report-out:<path>]");
    puts("  cheng_v3_seed print-object --object:<path> [--report-out:<path>]");
    puts("  cheng_v3_seed print-elf --object:<path> [--report-out:<path>]");
    puts("  cheng_v3_seed print-asm [--contract-in:<path>] [--in:<path>] [--root:<path>] [--emit:<exe|shared|obj>] [--target:<triple>] [--out:<path>] [--channel:<stable|edge>] [--world-head:<cid>] [--lock:<path>] [--report-out:<path>]");
    puts("  cheng_v3_seed profile-run [--contract-in:<path>] [--in:<path>] [--root:<path>] [--target:<triple>] [--out:<path>] [--profile-hz:<int>] [--report-out:<path>]");
    puts("  cheng_v3_seed profile-report --in:<raw-report> [--out:<final-report>]");
    puts("  cheng_v3_seed crash-report --in:<raw-log> [--out:<final-report>]");
    puts("  cheng_v3_seed verify-orphan-guard");
    puts("  cheng_v3_seed verify-export-visibility");
    puts("  cheng_v3_seed verify-export-visibility-parallel");
    puts("  cheng_v3_seed verify-backend-driver-command-surface");
    puts("  cheng_v3_seed verify-mobile-shell-build-probe");
    puts("  cheng_v3_seed verify-chain-node-resilience [--compiler:<path>] [--label:<name>]");
    puts("  cheng_v3_seed verify-r2c-react-v3-surface");
    puts("  cheng_v3_seed verify-debug-tools");
    puts("  cheng_v3_seed verify-debug-runtime");
    puts("  cheng_v3_seed verify-debug-profile");
    puts("  cheng_v3_seed verify-diloco");
    puts("  cheng_v3_seed verify-windows-builtin");
    puts("  cheng_v3_seed verify-riscv64-builtin");
    puts("  cheng_v3_seed run-cross-target-smokes");
    puts("  cheng_v3_seed run-host-smokes [smoke_name ...]");
    puts("  cheng_v3_seed run-stage23-libp2p-smokes [smoke_name ...]");
    puts("  cheng_v3_seed build-zero-exit");
    puts("  cheng_v3_seed build-panic-trace");
    puts("  cheng_v3_seed build-bounds-trace");
    puts("  cheng_v3_seed build-signal-trace");
    puts("  cheng_v3_seed build-call-chain");
    puts("  cheng_v3_seed build-ffi-handle");
    puts("  cheng_v3_seed build-program-selfhost");
    puts("  cheng_v3_seed build-bft-state-machine");
    puts("  cheng_v3_seed deploy-bft-validator-three-node");
    puts("  cheng_v3_seed verify-bft-validator-three-node");
    puts("  cheng_v3_seed status-bft-validator-three-node");
    puts("  cheng_v3_seed stop-bft-validator-three-node");
    puts("  cheng_v3_seed bench-bft-validator-three-node [--tx-count:<n>] [--wave-count:<n>] [--transfer-amount:<n>] [--max-txs-per-block:<n>] [--restart-node:none|server|relay|local] [--restart-wave:<n>] [--restart-pause-ms:<n>]");
    puts("  cheng_v3_seed build-browser-host-wasm [out.wasm]");
    puts("  cheng_v3_seed build-chain-node");
    puts("  cheng_v3_seed build-rwad-bft-state-machine");
    puts("  cheng_v3_seed build-chain-node-linux");
    puts("  cheng_v3_seed build-linux-nolibc-exe");
    puts("  cheng_v3_seed build-rwad-bft-linux");
    puts("  cheng_v3_seed run-linux-object-smokes");
    puts("  cheng_v3_seed run-tcp-twoproc-smoke [--compiler:<path>] [--label:<name>]");
    puts("  cheng_v3_seed run-quic-twoproc-smoke [--compiler:<path>] [--label:<name>]");
    puts("  cheng_v3_seed run-udp-importc-smoke [--compiler:<path>] [--label:<name>]");
    puts("  cheng_v3_seed run-wasm-smokes [--compiler:<path>] [--label:<name>]");
    puts("  cheng_v3_seed run-browser-host-wasm-smoke [--compiler:<path>] [--label:<name>]");
    puts("  cheng_v3_seed run-chain-node-cli-smoke [--compiler:<path>] [--label:<name>]");
    puts("  cheng_v3_seed run-chain-node-process-smoke");
    puts("  cheng_v3_seed run-chain-node-three-node-smoke");
    puts("  cheng_v3_seed run-chain-node-resilience-smoke [--compiler:<path>] [--label:<name>]");
    puts("  cheng_v3_seed run-tailnet-control-smoke [--compiler:<path>] [--label:<name>]");
    puts("  cheng_v3_seed run-fresh-node-selfhost-gate [--compiler:<path>] [--label:<name>]");
    puts("  cheng_v3_seed run-migration-gate [--compiler:<path>] [--label:<name>]");
    puts("  cheng_v3_seed run-browser-webrtc-smokes [smoke_name ...]");
    puts("  cheng_v3_seed r2c-react-v3 <subcommand> [args]");
    puts("  cheng_v3_seed vpn-proxy <subcommand> [args]");
    puts("  cheng_v3_seed emit-csg [--contract-in:<path>] [--in:<path>] [--root:<path>] [--report-out:<path>]");
    puts("  cheng_v3_seed migrate-csg [--contract-in:<path>] [--legacy-in:<path>] [--in:<path>] [--root:<path>] [--out:<bundle-prefix>] [--report-out:<path>]");
    puts("  cheng_v3_seed verify-world [--contract-in:<path>] [--in:<path>] [--root:<path>] [--target:<triple>] [--channel:<stable|edge>] [--world-head:<cid>] [--lock:<path>] [--baseline-surface:<path>] [--report-out:<path>]");
    puts("  cheng_v3_seed world-sync [--contract-in:<path>] [--in:<path>] [--root:<path>] --target:<triple> --out:<bundle-prefix> [--channel:<stable|edge>] [--world-head:<cid>] [--lock:<path>] [--baseline-surface:<path>] [--report-out:<path>]");
    puts("  cheng_v3_seed prove-equivalence [--contract-in:<path>] [--in:<path>] [--root:<path>] [--target:<triple>] [--channel:<stable|edge>] --baseline-csg:<path> --baseline-surface:<path> --baseline-receipt:<path> [--world-head:<cid>] [--lock:<path>] [--report-out:<path>]");
    puts("  cheng_v3_seed prove-migration [--contract-in:<path>] [--legacy-in:<path>] [--in:<path>] [--root:<path>] [--target:<triple>] [--channel:<stable|edge>] [--out:<bundle-prefix>] [--world-head:<cid>] [--lock:<path>] [--report-out:<path>]");
    puts("  cheng_v3_seed publish-world [--contract-in:<path>] [--in:<path>] [--root:<path>] --target:<triple> --out:<bundle-prefix> [--channel:<stable|edge>] [--legacy-in:<path>] [--baseline-csg:<path>] [--baseline-surface:<path>] [--baseline-receipt:<path>] [--world-head:<cid>] [--lock:<path>] [--report-out:<path>]");
    puts("  cheng_v3_seed fresh-node-selfhost [--contract-in:<path>] [--in:<path>] [--root:<path>] --target:<triple> --out:<bundle-prefix> [--channel:<stable|edge>] [--legacy-in:<path>] [--baseline-csg:<path>] [--baseline-surface:<path>] [--baseline-receipt:<path>] [--world-head:<cid>] [--lock:<path>] [--report-out:<path>]");
    puts("  cheng_v3_seed selfhost-build [--contract-in:<path>] [--in:<path>] [--root:<path>] --target:<triple> --out:<path> [--channel:<stable|edge>] [--world-head:<cid>] [--lock:<path>] [--baseline-surface:<path>] [--report-out:<path>]");
    puts("  cheng_v3_seed system-link-exec [--contract-in:<path>] [--in:<path>] [--root:<path>] --emit:<exe|shared|obj> --target:<triple> --out:<path> [--link-input:<obj>] [--channel:<stable|edge>] [--report-out:<path>]");
    puts("  cheng_v3_seed compile-bootstrap --in:<path> --out:<path> [--report-out:<path>]");
}

static void *v3_xmalloc(size_t size) {
    void *ptr = malloc(size);
    if (!ptr) {
        fprintf(stderr, "[cheng_v3_seed] out of memory size=%zu\n", size);
        exit(1);
    }
    return ptr;
}

static char (*v3_alloc_call_arg_scratch(void))[4096] {
    return (char (*)[4096])v3_xmalloc(sizeof(char[CHENG_V3_MAX_CALL_ARGS][4096]));
}

static void *v3_xrealloc(void *ptr, size_t size) {
    void *next = realloc(ptr, size);
    if (!next) {
        fprintf(stderr, "[cheng_v3_seed] out of memory resize=%zu\n", size);
        exit(1);
    }
    return next;
}

static char *v3_strdup(const char *text) {
    size_t len = strlen(text);
    char *out = (char *)v3_xmalloc(len + 1);
    memcpy(out, text, len + 1);
    return out;
}

static char *v3_strdup_parts(const char *const *parts, size_t count) {
    size_t total = 0U;
    size_t i;
    char *out;
    char *cursor;
    for (i = 0U; i < count; ++i) {
        if (parts[i] != NULL) {
            total += strlen(parts[i]);
        }
    }
    out = (char *)v3_xmalloc(total + 1U);
    cursor = out;
    for (i = 0U; i < count; ++i) {
        size_t len = parts[i] != NULL ? strlen(parts[i]) : 0U;
        if (len > 0U) {
            memcpy(cursor, parts[i], len);
            cursor += len;
        }
    }
    *cursor = '\0';
    return out;
}

static char *v3_trim_inplace(char *text) {
    char *start = text;
    char *end;
    while (*start != '\0' && isspace((unsigned char)*start)) {
        start++;
    }
    end = start + strlen(start);
    while (end > start && isspace((unsigned char)end[-1])) {
        end--;
    }
    *end = '\0';
    return start;
}

static bool v3_streq(const char *a, const char *b) {
    return strcmp(a, b) == 0;
}

static void v3_owned_string_vec_init(V3OwnedStringVec *vec) {
    vec->items = NULL;
    vec->len = 0U;
    vec->cap = 0U;
}

static void v3_owned_string_vec_push_dup(V3OwnedStringVec *vec, const char *text) {
    if (vec->len + 1U > vec->cap) {
        size_t next_cap = vec->cap == 0U ? 16U : vec->cap * 2U;
        vec->items = (char **)v3_xrealloc(vec->items, next_cap * sizeof(char *));
        vec->cap = next_cap;
    }
    vec->items[vec->len++] = v3_strdup(text);
}

static char *v3_owned_string_vec_pop(V3OwnedStringVec *vec) {
    if (vec->len == 0U) {
        return NULL;
    }
    vec->len -= 1U;
    return vec->items[vec->len];
}

static void v3_owned_string_vec_free(V3OwnedStringVec *vec) {
    size_t i;
    for (i = 0U; i < vec->len; ++i) {
        free(vec->items[i]);
    }
    free(vec->items);
    vec->items = NULL;
    vec->len = 0U;
    vec->cap = 0U;
}

static void v3_forbidden_hit_vec_init(V3ForbiddenHitVec *vec) {
    vec->items = NULL;
    vec->len = 0U;
    vec->cap = 0U;
}

static void v3_forbidden_hit_vec_push(V3ForbiddenHitVec *vec,
                                      const char *path,
                                      const char *label) {
    if (vec->len + 1U > vec->cap) {
        size_t next_cap = vec->cap == 0U ? 16U : vec->cap * 2U;
        vec->items = (V3ForbiddenHit *)v3_xrealloc(vec->items,
                                                   next_cap * sizeof(V3ForbiddenHit));
        vec->cap = next_cap;
    }
    vec->items[vec->len].path = v3_strdup(path);
    vec->items[vec->len].label = label;
    vec->len += 1U;
}

static void v3_forbidden_hit_vec_free(V3ForbiddenHitVec *vec) {
    size_t i;
    for (i = 0U; i < vec->len; ++i) {
        free(vec->items[i].path);
    }
    free(vec->items);
    vec->items = NULL;
    vec->len = 0U;
    vec->cap = 0U;
}

static bool v3_has_csv_token(const char *csv, const char *needle) {
    size_t needle_len = strlen(needle);
    const char *cursor = csv;
    while (*cursor != '\0') {
        while (*cursor == ' ' || *cursor == '\t' || *cursor == ',') {
            cursor++;
        }
        if (*cursor == '\0') {
            return false;
        }
        const char *stop = cursor;
        while (*stop != '\0' && *stop != ',') {
            stop++;
        }
        while (stop > cursor && (stop[-1] == ' ' || stop[-1] == '\t')) {
            stop--;
        }
        if ((size_t)(stop - cursor) == needle_len && strncmp(cursor, needle, needle_len) == 0) {
            return true;
        }
        cursor = stop;
        if (*cursor == ',') {
            cursor++;
        }
    }
    return false;
}

static uint64_t v3_fnv1a64(const char *text) {
    uint64_t hash = 1469598103934665603ULL;
    const unsigned char *cursor = (const unsigned char *)text;
    while (*cursor != '\0') {
        hash ^= (uint64_t)(*cursor);
        hash *= 1099511628211ULL;
        cursor++;
    }
    return hash;
}

static const uint32_t v3_sha256_table[64] = {
    0x428a2f98U, 0x71374491U, 0xb5c0fbcfU, 0xe9b5dba5U, 0x3956c25bU, 0x59f111f1U, 0x923f82a4U, 0xab1c5ed5U,
    0xd807aa98U, 0x12835b01U, 0x243185beU, 0x550c7dc3U, 0x72be5d74U, 0x80deb1feU, 0x9bdc06a7U, 0xc19bf174U,
    0xe49b69c1U, 0xefbe4786U, 0x0fc19dc6U, 0x240ca1ccU, 0x2de92c6fU, 0x4a7484aaU, 0x5cb0a9dcU, 0x76f988daU,
    0x983e5152U, 0xa831c66dU, 0xb00327c8U, 0xbf597fc7U, 0xc6e00bf3U, 0xd5a79147U, 0x06ca6351U, 0x14292967U,
    0x27b70a85U, 0x2e1b2138U, 0x4d2c6dfcU, 0x53380d13U, 0x650a7354U, 0x766a0abbU, 0x81c2c92eU, 0x92722c85U,
    0xa2bfe8a1U, 0xa81a664bU, 0xc24b8b70U, 0xc76c51a3U, 0xd192e819U, 0xd6990624U, 0xf40e3585U, 0x106aa070U,
    0x19a4c116U, 0x1e376c08U, 0x2748774cU, 0x34b0bcb5U, 0x391c0cb3U, 0x4ed8aa4aU, 0x5b9cca4fU, 0x682e6ff3U,
    0x748f82eeU, 0x78a5636fU, 0x84c87814U, 0x8cc70208U, 0x90befffaU, 0xa4506cebU, 0xbef9a3f7U, 0xc67178f2U
};

static uint32_t v3_sha256_rotr(uint32_t value, uint32_t shift) {
    return (value >> shift) | (value << (32U - shift));
}

static void v3_sha256_init(V3Sha256Ctx *ctx) {
    ctx->datalen = 0U;
    ctx->bitlen = 0ULL;
    ctx->state[0] = 0x6a09e667U;
    ctx->state[1] = 0xbb67ae85U;
    ctx->state[2] = 0x3c6ef372U;
    ctx->state[3] = 0xa54ff53aU;
    ctx->state[4] = 0x510e527fU;
    ctx->state[5] = 0x9b05688cU;
    ctx->state[6] = 0x1f83d9abU;
    ctx->state[7] = 0x5be0cd19U;
}

static void v3_sha256_transform(V3Sha256Ctx *ctx, const uint8_t data[64]) {
    uint32_t m[64];
    uint32_t a, b, c, d, e, f, g, h;
    size_t i;
    for (i = 0U; i < 16U; ++i) {
        m[i] = ((uint32_t)data[i * 4U] << 24) |
               ((uint32_t)data[i * 4U + 1U] << 16) |
               ((uint32_t)data[i * 4U + 2U] << 8) |
               ((uint32_t)data[i * 4U + 3U]);
    }
    for (i = 16U; i < 64U; ++i) {
        uint32_t s0 = v3_sha256_rotr(m[i - 15U], 7U) ^ v3_sha256_rotr(m[i - 15U], 18U) ^ (m[i - 15U] >> 3U);
        uint32_t s1 = v3_sha256_rotr(m[i - 2U], 17U) ^ v3_sha256_rotr(m[i - 2U], 19U) ^ (m[i - 2U] >> 10U);
        m[i] = m[i - 16U] + s0 + m[i - 7U] + s1;
    }
    a = ctx->state[0];
    b = ctx->state[1];
    c = ctx->state[2];
    d = ctx->state[3];
    e = ctx->state[4];
    f = ctx->state[5];
    g = ctx->state[6];
    h = ctx->state[7];
    for (i = 0U; i < 64U; ++i) {
        uint32_t S1 = v3_sha256_rotr(e, 6U) ^ v3_sha256_rotr(e, 11U) ^ v3_sha256_rotr(e, 25U);
        uint32_t ch = (e & f) ^ ((~e) & g);
        uint32_t temp1 = h + S1 + ch + v3_sha256_table[i] + m[i];
        uint32_t S0 = v3_sha256_rotr(a, 2U) ^ v3_sha256_rotr(a, 13U) ^ v3_sha256_rotr(a, 22U);
        uint32_t maj = (a & b) ^ (a & c) ^ (b & c);
        uint32_t temp2 = S0 + maj;
        h = g;
        g = f;
        f = e;
        e = d + temp1;
        d = c;
        c = b;
        b = a;
        a = temp1 + temp2;
    }
    ctx->state[0] += a;
    ctx->state[1] += b;
    ctx->state[2] += c;
    ctx->state[3] += d;
    ctx->state[4] += e;
    ctx->state[5] += f;
    ctx->state[6] += g;
    ctx->state[7] += h;
}

static void v3_sha256_update(V3Sha256Ctx *ctx, const uint8_t *data, size_t len) {
    size_t i;
    for (i = 0U; i < len; ++i) {
        ctx->data[ctx->datalen] = data[i];
        ctx->datalen += 1U;
        if (ctx->datalen == 64U) {
            v3_sha256_transform(ctx, ctx->data);
            ctx->bitlen += 512ULL;
            ctx->datalen = 0U;
        }
    }
}

static void v3_sha256_final(V3Sha256Ctx *ctx, uint8_t hash[32]) {
    uint32_t i;
    i = ctx->datalen;
    if (ctx->datalen < 56U) {
        ctx->data[i++] = 0x80U;
        while (i < 56U) {
            ctx->data[i++] = 0x00U;
        }
    } else {
        ctx->data[i++] = 0x80U;
        while (i < 64U) {
            ctx->data[i++] = 0x00U;
        }
        v3_sha256_transform(ctx, ctx->data);
        memset(ctx->data, 0, 56U);
    }
    ctx->bitlen += (uint64_t)ctx->datalen * 8ULL;
    ctx->data[63] = (uint8_t)(ctx->bitlen);
    ctx->data[62] = (uint8_t)(ctx->bitlen >> 8);
    ctx->data[61] = (uint8_t)(ctx->bitlen >> 16);
    ctx->data[60] = (uint8_t)(ctx->bitlen >> 24);
    ctx->data[59] = (uint8_t)(ctx->bitlen >> 32);
    ctx->data[58] = (uint8_t)(ctx->bitlen >> 40);
    ctx->data[57] = (uint8_t)(ctx->bitlen >> 48);
    ctx->data[56] = (uint8_t)(ctx->bitlen >> 56);
    v3_sha256_transform(ctx, ctx->data);
    for (i = 0U; i < 4U; ++i) {
        hash[i] = (uint8_t)((ctx->state[0] >> (24U - i * 8U)) & 0xffU);
        hash[i + 4U] = (uint8_t)((ctx->state[1] >> (24U - i * 8U)) & 0xffU);
        hash[i + 8U] = (uint8_t)((ctx->state[2] >> (24U - i * 8U)) & 0xffU);
        hash[i + 12U] = (uint8_t)((ctx->state[3] >> (24U - i * 8U)) & 0xffU);
        hash[i + 16U] = (uint8_t)((ctx->state[4] >> (24U - i * 8U)) & 0xffU);
        hash[i + 20U] = (uint8_t)((ctx->state[5] >> (24U - i * 8U)) & 0xffU);
        hash[i + 24U] = (uint8_t)((ctx->state[6] >> (24U - i * 8U)) & 0xffU);
        hash[i + 28U] = (uint8_t)((ctx->state[7] >> (24U - i * 8U)) & 0xffU);
    }
}

static void v3_cid_fill_hex(V3Cid32 *cid) {
    static const char *HEX = "0123456789abcdef";
    size_t i;
    for (i = 0U; i < 32U; ++i) {
        cid->hex[i * 2U] = HEX[(cid->bytes[i] >> 4U) & 0x0fU];
        cid->hex[i * 2U + 1U] = HEX[cid->bytes[i] & 0x0fU];
    }
    cid->hex[CHENG_V3_CID_HEX_LEN] = '\0';
}

static int v3_hex_value(char ch) {
    if (ch >= '0' && ch <= '9') {
        return (int)(ch - '0');
    }
    if (ch >= 'a' && ch <= 'f') {
        return (int)(10 + (ch - 'a'));
    }
    if (ch >= 'A' && ch <= 'F') {
        return (int)(10 + (ch - 'A'));
    }
    return -1;
}

static V3Cid32 v3_cid_zero(void) {
    V3Cid32 out;
    memset(&out, 0, sizeof(out));
    v3_cid_fill_hex(&out);
    return out;
}

static V3Cid32 v3_cid_from_bytes(const uint8_t *data, size_t len) {
    V3Cid32 out;
    V3Sha256Ctx ctx;
    memset(&out, 0, sizeof(out));
    v3_sha256_init(&ctx);
    if (data && len > 0U) {
        v3_sha256_update(&ctx, data, len);
    }
    v3_sha256_final(&ctx, out.bytes);
    v3_cid_fill_hex(&out);
    return out;
}

static void v3_bytebuf_init(V3ByteBuf *buf, size_t cap) {
    buf->len = 0U;
    buf->cap = cap > 0U ? cap : 64U;
    buf->data = (uint8_t *)v3_xmalloc(buf->cap);
}

static void v3_bytebuf_reserve(V3ByteBuf *buf, size_t extra) {
    size_t need = buf->len + extra;
    if (need <= buf->cap) {
        return;
    }
    while (buf->cap < need) {
        buf->cap *= 2U;
    }
    buf->data = (uint8_t *)realloc(buf->data, buf->cap);
    if (!buf->data) {
        fprintf(stderr, "[cheng_v3_seed] out of memory while growing byte buffer\n");
        exit(1);
    }
}

static void v3_bytebuf_append_bytes(V3ByteBuf *buf, const uint8_t *data, size_t len) {
    if (len <= 0U) {
        return;
    }
    v3_bytebuf_reserve(buf, len);
    memcpy(buf->data + buf->len, data, len);
    buf->len += len;
}

static void v3_bytebuf_append_u32_be(V3ByteBuf *buf, uint32_t value) {
    uint8_t bytes[4];
    bytes[0] = (uint8_t)((value >> 24U) & 0xffU);
    bytes[1] = (uint8_t)((value >> 16U) & 0xffU);
    bytes[2] = (uint8_t)((value >> 8U) & 0xffU);
    bytes[3] = (uint8_t)(value & 0xffU);
    v3_bytebuf_append_bytes(buf, bytes, 4U);
}

static void v3_bytebuf_append_text(V3ByteBuf *buf, const char *text) {
    size_t len = text ? strlen(text) : 0U;
    v3_bytebuf_append_u32_be(buf, (uint32_t)len);
    if (len > 0U) {
        v3_bytebuf_append_bytes(buf, (const uint8_t *)text, len);
    }
}

static void v3_bytebuf_append_cid(V3ByteBuf *buf, V3Cid32 cid) {
    v3_bytebuf_append_bytes(buf, cid.bytes, 32U);
}

static V3Cid32 v3_cid_from_bytebuf(const V3ByteBuf *buf) {
    return v3_cid_from_bytes(buf->data, buf->len);
}

static bool v3_cid_equal(V3Cid32 left, V3Cid32 right) {
    return memcmp(left.bytes, right.bytes, sizeof(left.bytes)) == 0;
}

static bool v3_cid_from_hex_text(const char *text, V3Cid32 *out) {
    size_t i;
    if (!text || strlen(text) != CHENG_V3_CID_HEX_LEN) {
        return false;
    }
    memset(out, 0, sizeof(*out));
    for (i = 0U; i < 32U; ++i) {
        int high = v3_hex_value(text[i * 2U]);
        int low = v3_hex_value(text[i * 2U + 1U]);
        if (high < 0 || low < 0) {
            return false;
        }
        out->bytes[i] = (uint8_t)((high << 4) | low);
    }
    v3_cid_fill_hex(out);
    return true;
}

static void v3_bytebuf_free(V3ByteBuf *buf) {
    free(buf->data);
    buf->data = NULL;
    buf->len = 0U;
    buf->cap = 0U;
}

static char *v3_read_file(const char *path) {
    FILE *fp = fopen(path, "rb");
    long size;
    char *buffer;
    if (!fp) {
        fprintf(stderr, "[cheng_v3_seed] failed to open: %s\n", path);
        return NULL;
    }
    if (fseek(fp, 0, SEEK_END) != 0) {
        fclose(fp);
        fprintf(stderr, "[cheng_v3_seed] failed to seek: %s\n", path);
        return NULL;
    }
    size = ftell(fp);
    if (size < 0) {
        fclose(fp);
        fprintf(stderr, "[cheng_v3_seed] failed to size: %s\n", path);
        return NULL;
    }
    if (fseek(fp, 0, SEEK_SET) != 0) {
        fclose(fp);
        fprintf(stderr, "[cheng_v3_seed] failed to rewind: %s\n", path);
        return NULL;
    }
    buffer = (char *)v3_xmalloc((size_t)size + 1U);
    if (size > 0 && fread(buffer, 1U, (size_t)size, fp) != (size_t)size) {
        fclose(fp);
        free(buffer);
        fprintf(stderr, "[cheng_v3_seed] failed to read: %s\n", path);
        return NULL;
    }
    buffer[size] = '\0';
    fclose(fp);
    return buffer;
}

static void v3_contract_init(V3BootstrapContract *contract, const char *source_path) {
    size_t i;
    memset(contract, 0, sizeof(*contract));
    if (source_path && *source_path != '\0') {
        snprintf(contract->source_path, sizeof(contract->source_path), "%s", source_path);
    }
    for (i = 0; i < CHENG_V3_MAX_FIELDS; ++i) {
        contract->fields[i].key = NULL;
        contract->fields[i].value = NULL;
    }
}

static void v3_contract_free(V3BootstrapContract *contract) {
    size_t i;
    for (i = 0; i < contract->len; ++i) {
        free(contract->fields[i].key);
        free(contract->fields[i].value);
    }
    contract->len = 0;
}

static const char *v3_contract_get(const V3BootstrapContract *contract, const char *key) {
    size_t i;
    for (i = 0; i < contract->len; ++i) {
        if (v3_streq(contract->fields[i].key, key)) {
            return contract->fields[i].value;
        }
    }
    return NULL;
}

static bool v3_contract_add(V3BootstrapContract *contract, const char *key, const char *value) {
    size_t i;
    if (contract->len >= CHENG_V3_MAX_FIELDS) {
        fprintf(stderr, "[cheng_v3_seed] too many bootstrap fields\n");
        return false;
    }
    for (i = 0; i < contract->len; ++i) {
        if (v3_streq(contract->fields[i].key, key)) {
            fprintf(stderr, "[cheng_v3_seed] duplicate bootstrap field: %s\n", key);
            return false;
        }
    }
    contract->fields[contract->len].key = v3_strdup(key);
    contract->fields[contract->len].value = v3_strdup(value);
    contract->len += 1U;
    return true;
}

static bool v3_parse_contract_text(V3BootstrapContract *contract, const char *source_path, const char *text) {
    char *owned = v3_strdup(text);
    char *cursor = owned;
    char *line;
    v3_contract_init(contract, source_path);
    while ((line = strsep(&cursor, "\n")) != NULL) {
        char *comment = strchr(line, '#');
        char *eq;
        char *key;
        char *value;
        if (comment) {
            *comment = '\0';
        }
        line = v3_trim_inplace(line);
        if (*line == '\0') {
            continue;
        }
        eq = strchr(line, '=');
        if (!eq) {
            fprintf(stderr, "[cheng_v3_seed] invalid bootstrap line (missing '='): %s\n", line);
            free(owned);
            return false;
        }
        *eq = '\0';
        key = v3_trim_inplace(line);
        value = v3_trim_inplace(eq + 1);
        if (*key == '\0' || *value == '\0') {
            fprintf(stderr, "[cheng_v3_seed] invalid bootstrap line: %s = %s\n", key, value);
            free(owned);
            return false;
        }
        if (!v3_contract_add(contract, key, value)) {
            free(owned);
            return false;
        }
    }
    free(owned);
    return true;
}

static bool v3_parse_contract_file(V3BootstrapContract *contract, const char *path) {
    char *text = v3_read_file(path);
    bool ok;
    if (!text) {
        return false;
    }
    ok = v3_parse_contract_text(contract, path, text);
    free(text);
    return ok;
}

static bool v3_validate_contract(const V3BootstrapContract *contract) {
    static const char *REQUIRED_IR_FACTS[] = {
        "move", "borrow", "noalias", "escape", "layout", "abi"
    };
    static const char *REQUIRED_LAYOUT[] = {
        "dod", "soa", "fixed_layout"
    };
    static const char *REQUIRED_FORBIDDEN[] = {
        "local_payload",
        "exec_plan_payload",
        "payloadText",
        "BigInt",
        "legacy_proof_surface",
        "legacy_sidecar_mode"
    };
    size_t i;
    const char *ir_facts;
    const char *data_layout;
    const char *forbidden;
    const char *commands;
    for (i = 0; i < sizeof(V3_REQUIRED_KEYS) / sizeof(V3_REQUIRED_KEYS[0]); ++i) {
        if (!v3_contract_get(contract, V3_REQUIRED_KEYS[i])) {
            fprintf(stderr, "[cheng_v3_seed] missing bootstrap field: %s\n", V3_REQUIRED_KEYS[i]);
            return false;
        }
    }
    if (!v3_streq(v3_contract_get(contract, "syntax"), "v3-bootstrap-v1")) {
        fprintf(stderr, "[cheng_v3_seed] unsupported bootstrap syntax\n");
        return false;
    }
    if (!v3_streq(v3_contract_get(contract, "compiler_class"), "bootstrap_subset")) {
        fprintf(stderr, "[cheng_v3_seed] bootstrap compiler_class must be bootstrap_subset\n");
        return false;
    }
    if (!v3_streq(v3_contract_get(contract, "target"), "arm64-apple-darwin")) {
        fprintf(stderr, "[cheng_v3_seed] bootstrap target must be arm64-apple-darwin\n");
        return false;
    }
    if (!v3_streq(v3_contract_get(contract, "compiler_parallel_model"),
                  "two_pass,function_scheduler,thread_local_arena")) {
        fprintf(stderr, "[cheng_v3_seed] compiler parallel model drift\n");
        return false;
    }
    if (!v3_streq(v3_contract_get(contract, "program_parallel_model"),
                  "deterministic_single_thread")) {
        fprintf(stderr, "[cheng_v3_seed] program parallel model drift\n");
        return false;
    }
    if (!v3_streq(v3_contract_get(contract, "aot"), "required")) {
        fprintf(stderr, "[cheng_v3_seed] aot must be required\n");
        return false;
    }
    if (!v3_streq(v3_contract_get(contract, "user_program_auto_parallel"), "off")) {
        fprintf(stderr, "[cheng_v3_seed] user_program_auto_parallel must be off\n");
        return false;
    }
    if (!v3_streq(v3_contract_get(contract, "bagua_bpi"), "sidecar_only")) {
        fprintf(stderr, "[cheng_v3_seed] bagua_bpi must be sidecar_only\n");
        return false;
    }
    ir_facts = v3_contract_get(contract, "ir_facts");
    data_layout = v3_contract_get(contract, "data_layout");
    forbidden = v3_contract_get(contract, "forbidden");
    commands = v3_contract_get(contract, "supported_commands");
    for (i = 0; i < sizeof(REQUIRED_IR_FACTS) / sizeof(REQUIRED_IR_FACTS[0]); ++i) {
        if (!v3_has_csv_token(ir_facts, REQUIRED_IR_FACTS[i])) {
            fprintf(stderr, "[cheng_v3_seed] missing ir fact: %s\n", REQUIRED_IR_FACTS[i]);
            return false;
        }
    }
    for (i = 0; i < sizeof(REQUIRED_LAYOUT) / sizeof(REQUIRED_LAYOUT[0]); ++i) {
        if (!v3_has_csv_token(data_layout, REQUIRED_LAYOUT[i])) {
            fprintf(stderr, "[cheng_v3_seed] missing data layout default: %s\n", REQUIRED_LAYOUT[i]);
            return false;
        }
    }
    for (i = 0; i < sizeof(REQUIRED_FORBIDDEN) / sizeof(REQUIRED_FORBIDDEN[0]); ++i) {
        if (!v3_has_csv_token(forbidden, REQUIRED_FORBIDDEN[i])) {
            fprintf(stderr, "[cheng_v3_seed] missing forbidden token: %s\n", REQUIRED_FORBIDDEN[i]);
            return false;
        }
    }
    if (!v3_has_csv_token(commands, "print-contract") ||
        !v3_has_csv_token(commands, "self-check") ||
        !v3_has_csv_token(commands, "compile-bootstrap") ||
        !v3_has_csv_token(commands, "status") ||
        !v3_has_csv_token(commands, "print-build-plan") ||
        !v3_has_csv_token(commands, "scan-hotpath") ||
        !v3_has_csv_token(commands, "print-bootstrap") ||
        !v3_has_csv_token(commands, "bootstrap-bridge") ||
        !v3_has_csv_token(commands, "build-backend-driver") ||
        !v3_has_csv_token(commands, "run-smokes") ||
        !v3_has_csv_token(commands, "slice-gate") ||
        !v3_has_csv_token(commands, "debug-report") ||
        !v3_has_csv_token(commands, "print-symbols") ||
        !v3_has_csv_token(commands, "print-line-map") ||
        !v3_has_csv_token(commands, "print-object") ||
        !v3_has_csv_token(commands, "print-elf") ||
        !v3_has_csv_token(commands, "print-asm") ||
        !v3_has_csv_token(commands, "profile-run") ||
        !v3_has_csv_token(commands, "profile-report") ||
        !v3_has_csv_token(commands, "crash-report") ||
        !v3_has_csv_token(commands, "verify-orphan-guard") ||
        !v3_has_csv_token(commands, "verify-export-visibility") ||
        !v3_has_csv_token(commands, "verify-export-visibility-parallel") ||
        !v3_has_csv_token(commands, "verify-backend-driver-command-surface") ||
        !v3_has_csv_token(commands, "verify-mobile-shell-build-probe") ||
        !v3_has_csv_token(commands, "verify-chain-node-resilience") ||
        !v3_has_csv_token(commands, "verify-r2c-react-v3-surface") ||
        !v3_has_csv_token(commands, "verify-debug-tools") ||
        !v3_has_csv_token(commands, "verify-debug-runtime") ||
        !v3_has_csv_token(commands, "verify-debug-profile") ||
        !v3_has_csv_token(commands, "verify-diloco") ||
        !v3_has_csv_token(commands, "verify-windows-builtin") ||
        !v3_has_csv_token(commands, "verify-riscv64-builtin") ||
        !v3_has_csv_token(commands, "run-cross-target-smokes") ||
        !v3_has_csv_token(commands, "run-host-smokes") ||
        !v3_has_csv_token(commands, "run-stage23-libp2p-smokes") ||
        !v3_has_csv_token(commands, "build-zero-exit") ||
        !v3_has_csv_token(commands, "build-panic-trace") ||
        !v3_has_csv_token(commands, "build-bounds-trace") ||
        !v3_has_csv_token(commands, "build-signal-trace") ||
        !v3_has_csv_token(commands, "build-call-chain") ||
        !v3_has_csv_token(commands, "build-ffi-handle") ||
        !v3_has_csv_token(commands, "build-program-selfhost") ||
        !v3_has_csv_token(commands, "build-bft-state-machine") ||
        !v3_has_csv_token(commands, "deploy-bft-validator-three-node") ||
        !v3_has_csv_token(commands, "verify-bft-validator-three-node") ||
        !v3_has_csv_token(commands, "status-bft-validator-three-node") ||
        !v3_has_csv_token(commands, "stop-bft-validator-three-node") ||
        !v3_has_csv_token(commands, "bench-bft-validator-three-node") ||
        !v3_has_csv_token(commands, "build-browser-host-wasm") ||
        !v3_has_csv_token(commands, "build-chain-node") ||
        !v3_has_csv_token(commands, "build-rwad-bft-state-machine") ||
        !v3_has_csv_token(commands, "build-chain-node-linux") ||
        !v3_has_csv_token(commands, "build-linux-nolibc-exe") ||
        !v3_has_csv_token(commands, "build-rwad-bft-linux") ||
        !v3_has_csv_token(commands, "run-linux-object-smokes") ||
        !v3_has_csv_token(commands, "run-tcp-twoproc-smoke") ||
        !v3_has_csv_token(commands, "run-quic-twoproc-smoke") ||
        !v3_has_csv_token(commands, "run-udp-importc-smoke") ||
        !v3_has_csv_token(commands, "run-wasm-smokes") ||
        !v3_has_csv_token(commands, "run-browser-host-wasm-smoke") ||
        !v3_has_csv_token(commands, "run-chain-node-cli-smoke") ||
        !v3_has_csv_token(commands, "run-chain-node-process-smoke") ||
        !v3_has_csv_token(commands, "run-chain-node-three-node-smoke") ||
        !v3_has_csv_token(commands, "run-chain-node-resilience-smoke") ||
        !v3_has_csv_token(commands, "run-tailnet-control-smoke") ||
        !v3_has_csv_token(commands, "run-fresh-node-selfhost-gate") ||
        !v3_has_csv_token(commands, "run-migration-gate") ||
        !v3_has_csv_token(commands, "run-browser-webrtc-smokes") ||
        !v3_has_csv_token(commands, "r2c-react-v3") ||
        !v3_has_csv_token(commands, "vpn-proxy") ||
        !v3_has_csv_token(commands, "emit-csg") ||
        !v3_has_csv_token(commands, "migrate-csg") ||
        !v3_has_csv_token(commands, "verify-world") ||
        !v3_has_csv_token(commands, "world-sync") ||
        !v3_has_csv_token(commands, "prove-equivalence") ||
        !v3_has_csv_token(commands, "prove-migration") ||
        !v3_has_csv_token(commands, "publish-world") ||
        !v3_has_csv_token(commands, "fresh-node-selfhost") ||
        !v3_has_csv_token(commands, "selfhost-build") ||
        !v3_has_csv_token(commands, "system-link-exec")) {
        fprintf(stderr, "[cheng_v3_seed] supported_commands drift\n");
        return false;
    }
    if (!v3_streq(v3_contract_get(contract, "ordinary_command"), "system-link-exec")) {
        fprintf(stderr, "[cheng_v3_seed] ordinary_command drift\n");
        return false;
    }
    if (!v3_streq(v3_contract_get(contract, "ordinary_pipeline_state"),
                  "canonical_csg_verified_primary_object_codegen_missing")) {
        fprintf(stderr, "[cheng_v3_seed] ordinary_pipeline_state drift\n");
        return false;
    }
    return true;
}

static char *v3_contract_normalized_text(const V3BootstrapContract *contract) {
    static const char *ORDER[] = {
        "syntax",
        "bootstrap_name",
        "bootstrap_entry",
        "compiler_class",
        "bootstrap_source_kind",
        "target",
        "compiler_parallel_model",
        "program_parallel_model",
        "ir_facts",
        "data_layout",
        "aot",
        "forbidden",
        "supported_commands",
        "compiler_entry_source",
        "compiler_runtime_source",
        "compiler_request_source",
        "tooling_gate_source",
        "lang_parser_source",
        "backend_system_link_plan_source",
        "backend_lowering_plan_source",
        "backend_primary_object_plan_source",
        "backend_object_plan_source",
        "backend_native_link_plan_source",
        "backend_system_link_exec_source",
        "runtime_core_runtime_source",
        "runtime_compiler_runtime_source",
        "runtime_debug_runtime_source",
        "tooling_bootstrap_contract_source",
        "backend_build_plan_source",
        "ordinary_command",
        "ordinary_pipeline_state",
        "user_program_auto_parallel",
        "bagua_bpi"
    };
    size_t i;
    size_t cap = 8192U;
    size_t used = 0U;
    char *out = (char *)v3_xmalloc(cap);
    out[0] = '\0';
    for (i = 0; i < sizeof(ORDER) / sizeof(ORDER[0]); ++i) {
        const char *value = v3_contract_get(contract, ORDER[i]);
        int wrote;
        if (!value) {
            continue;
        }
        wrote = snprintf(out + used, cap - used, "%s=%s\n", ORDER[i], value);
        if (wrote < 0) {
            free(out);
            return NULL;
        }
        if (used + (size_t)wrote + 1U >= cap) {
            cap *= 2U;
            out = (char *)realloc(out, cap);
            if (!out) {
                fprintf(stderr, "[cheng_v3_seed] out of memory while normalizing contract\n");
                exit(1);
            }
            wrote = snprintf(out + used, cap - used, "%s=%s\n", ORDER[i], value);
            if (wrote < 0) {
                free(out);
                return NULL;
            }
        }
        used += (size_t)wrote;
    }
    return out;
}

static char *v3_c_escape(const char *text) {
    size_t i;
    size_t len = 0U;
    char *out;
    char *cursor;
    for (i = 0; text[i] != '\0'; ++i) {
        switch (text[i]) {
            case '\\':
            case '"':
                len += 2U;
                break;
            case '\n':
            case '\r':
            case '\t':
                len += 2U;
                break;
            default:
                len += 1U;
                break;
        }
    }
    out = (char *)v3_xmalloc(len + 1U);
    cursor = out;
    for (i = 0; text[i] != '\0'; ++i) {
        switch (text[i]) {
            case '\\':
                *cursor++ = '\\';
                *cursor++ = '\\';
                break;
            case '"':
                *cursor++ = '\\';
                *cursor++ = '"';
                break;
            case '\n':
                *cursor++ = '\\';
                *cursor++ = 'n';
                break;
            case '\r':
                *cursor++ = '\\';
                *cursor++ = 'r';
                break;
            case '\t':
                *cursor++ = '\\';
                *cursor++ = 't';
                break;
            default:
                *cursor++ = text[i];
                break;
        }
    }
    *cursor = '\0';
    return out;
}

static bool v3_write_text_file(const char *path, const char *text) {
    FILE *fp = fopen(path, "wb");
    size_t len = strlen(text);
    if (!fp) {
        fprintf(stderr, "[cheng_v3_seed] failed to write: %s\n", path);
        return false;
    }
    if (len > 0 && fwrite(text, 1U, len, fp) != len) {
        fclose(fp);
        fprintf(stderr, "[cheng_v3_seed] failed to flush: %s\n", path);
        return false;
    }
    fclose(fp);
    return true;
}

static bool v3_write_binary_file(const char *path, const unsigned char *data, size_t len) {
    FILE *fp = fopen(path, "wb");
    if (!fp) {
        fprintf(stderr, "[cheng_v3_seed] failed to write binary: %s\n", path);
        return false;
    }
    if (len > 0U && fwrite(data, 1U, len, fp) != len) {
        fclose(fp);
        fprintf(stderr, "[cheng_v3_seed] failed to flush binary: %s\n", path);
        return false;
    }
    fclose(fp);
    return true;
}

static bool v3_mkdir_p(const char *path) {
    char temp[PATH_MAX];
    size_t i;
    if (!path || *path == '\0') {
        return true;
    }
    if (strlen(path) >= sizeof(temp)) {
        fprintf(stderr, "[cheng_v3_seed] path too long: %s\n", path);
        return false;
    }
    snprintf(temp, sizeof(temp), "%s", path);
    for (i = 1U; temp[i] != '\0'; ++i) {
        if (temp[i] == '/') {
            temp[i] = '\0';
            if (temp[0] != '\0' && mkdir(temp, 0777) != 0 && errno != EEXIST) {
                fprintf(stderr, "[cheng_v3_seed] mkdir failed: %s\n", temp);
                return false;
            }
            temp[i] = '/';
        }
    }
    if (mkdir(temp, 0777) != 0 && errno != EEXIST) {
        fprintf(stderr, "[cheng_v3_seed] mkdir failed: %s\n", temp);
        return false;
    }
    return true;
}

static bool v3_ensure_parent_dir(const char *path) {
    char temp[PATH_MAX];
    char *slash;
    if (strlen(path) >= sizeof(temp)) {
        fprintf(stderr, "[cheng_v3_seed] path too long: %s\n", path);
        return false;
    }
    snprintf(temp, sizeof(temp), "%s", path);
    slash = strrchr(temp, '/');
    if (!slash) {
        return true;
    }
    if (slash == temp) {
        return true;
    }
    *slash = '\0';
    return v3_mkdir_p(temp);
}

static bool v3_copy_file_bytes(const char *source_path, const char *dest_path) {
    FILE *in = NULL;
    FILE *out = NULL;
    unsigned char buffer[16384];
    size_t read_len = 0U;
    if (!v3_ensure_parent_dir(dest_path)) {
        return false;
    }
    in = fopen(source_path, "rb");
    if (!in) {
        fprintf(stderr, "[cheng_v3_seed] failed to open copy source: %s\n", source_path);
        return false;
    }
    out = fopen(dest_path, "wb");
    if (!out) {
        fclose(in);
        fprintf(stderr, "[cheng_v3_seed] failed to open copy dest: %s\n", dest_path);
        return false;
    }
    while ((read_len = fread(buffer, 1U, sizeof(buffer), in)) > 0U) {
        if (fwrite(buffer, 1U, read_len, out) != read_len) {
            fclose(in);
            fclose(out);
            fprintf(stderr, "[cheng_v3_seed] failed to copy bytes: %s -> %s\n", source_path, dest_path);
            return false;
        }
    }
    fclose(in);
    fclose(out);
    return true;
}

static char *v3_shell_quote(const char *text) {
    size_t i;
    size_t len = 2U;
    char *out;
    char *cursor;
    for (i = 0; text[i] != '\0'; ++i) {
        if (text[i] == '\'') {
            len += 4U;
        } else {
            len += 1U;
        }
    }
    out = (char *)v3_xmalloc(len + 1U);
    cursor = out;
    *cursor++ = '\'';
    for (i = 0; text[i] != '\0'; ++i) {
        if (text[i] == '\'') {
            *cursor++ = '\'';
            *cursor++ = '\\';
            *cursor++ = '\'';
            *cursor++ = '\'';
        } else {
            *cursor++ = text[i];
        }
    }
    *cursor++ = '\'';
    *cursor = '\0';
    return out;
}

static bool v3_compile_wrapper(const char *wrapper_path, const char *out_path) {
    const char *cc = getenv("CC");
    char command[PATH_MAX * 3];
    char *quoted_cc;
    char *quoted_wrapper;
    char *quoted_out;
    if (!cc || *cc == '\0') {
        cc = "cc";
    }
    quoted_cc = v3_shell_quote(cc);
    quoted_wrapper = v3_shell_quote(wrapper_path);
    quoted_out = v3_shell_quote(out_path);
    snprintf(command, sizeof(command),
             "%s -std=c11 -O2 -Wall -Wextra -pedantic %s -o %s",
             quoted_cc, quoted_wrapper, quoted_out);
    free(quoted_cc);
    free(quoted_wrapper);
    free(quoted_out);
    if (!v3_run_shell_command_status(command, "compile-wrapper")) {
        fprintf(stderr, "[cheng_v3_seed] cc failed command=%s\n", command);
        return false;
    }
    return true;
}

static void v3_wrapper_impl_source_path(char *out, size_t cap) {
    char cwd[PATH_MAX];
    size_t len;
    if (out == NULL || cap == 0U) {
        return;
    }
    out[0] = '\0';
    if (CHENG_V3_IMPL_SOURCE_PATH[0] == '\0') {
        return;
    }
    len = strlen(CHENG_V3_IMPL_SOURCE_PATH);
    if (CHENG_V3_IMPL_SOURCE_PATH[0] == '/' ||
        CHENG_V3_IMPL_SOURCE_PATH[0] == '\\' ||
        (len > 1U && CHENG_V3_IMPL_SOURCE_PATH[1] == ':')) {
        snprintf(out, cap, "%s", CHENG_V3_IMPL_SOURCE_PATH);
        return;
    }
    if (getcwd(cwd, sizeof(cwd)) == NULL) {
        snprintf(out, cap, "%s", CHENG_V3_IMPL_SOURCE_PATH);
        return;
    }
    if (cwd[0] != '\0' && cwd[strlen(cwd) - 1U] == '/') {
        snprintf(out, cap, "%s%s", cwd, CHENG_V3_IMPL_SOURCE_PATH);
        return;
    }
    snprintf(out, cap, "%s/%s", cwd, CHENG_V3_IMPL_SOURCE_PATH);
}

static bool v3_write_wrapper_source(const char *wrapper_path,
                                    const char *embedded_source_path,
                                    const char *contract_text) {
    char impl_source_path[PATH_MAX];
    char *escaped_impl;
    char *escaped_source = v3_c_escape(embedded_source_path);
    char *escaped_contract = v3_c_escape(contract_text);
    size_t cap;
    char *text;
    v3_wrapper_impl_source_path(impl_source_path, sizeof(impl_source_path));
    escaped_impl = v3_c_escape(impl_source_path);
    cap = strlen(escaped_impl) + strlen(escaped_source) + strlen(escaped_contract) + 256U;
    text = (char *)v3_xmalloc(cap);
    snprintf(text, cap,
             "#define CHENG_V3_IMPL_SOURCE_PATH \"%s\"\n"
             "#define CHENG_V3_EMBEDDED_SOURCE_PATH \"%s\"\n"
             "#define CHENG_V3_EMBEDDED_CONTRACT_TEXT \"%s\"\n"
             "#include CHENG_V3_IMPL_SOURCE_PATH\n",
             escaped_impl, escaped_source, escaped_contract);
    free(escaped_impl);
    free(escaped_source);
    free(escaped_contract);
    if (!v3_ensure_parent_dir(wrapper_path)) {
        free(text);
        return false;
    }
    if (!v3_write_text_file(wrapper_path, text)) {
        free(text);
        return false;
    }
    free(text);
    return true;
}

static bool v3_write_compile_report(const char *report_path,
                                    const V3BootstrapContract *contract,
                                    const char *source_path,
                                    const char *out_path,
                                    const char *contract_text) {
    char buffer[8192];
    snprintf(buffer, sizeof(buffer),
             "compiler_class=%s\n"
             "target=%s\n"
             "source=%s\n"
             "output=%s\n"
             "contract_hash=%016llx\n"
             "compiler_parallel_model=%s\n"
             "program_parallel_model=%s\n"
             "data_layout=%s\n",
             v3_contract_get(contract, "compiler_class"),
             v3_contract_get(contract, "target"),
             source_path,
             out_path,
             (unsigned long long)v3_fnv1a64(contract_text),
             v3_contract_get(contract, "compiler_parallel_model"),
             v3_contract_get(contract, "program_parallel_model"),
             v3_contract_get(contract, "data_layout"));
    if (!v3_ensure_parent_dir(report_path)) {
        return false;
    }
    return v3_write_text_file(report_path, buffer);
}

typedef struct {
    char root[PATH_MAX];
    char target[128];
    char out_dir[PATH_MAX];
    char seed_source[PATH_MAX];
    char stage1_source[PATH_MAX];
    char compiler_entry_source[PATH_MAX];
    char backend_driver_entry_source[PATH_MAX];
    char compiler_runtime_source[PATH_MAX];
    char compiler_request_source[PATH_MAX];
    char compiler_world_source[PATH_MAX];
    char compiler_csg_source[PATH_MAX];
    char compiler_world_libp2p_source[PATH_MAX];
    char compiler_equivalence_source[PATH_MAX];
    char lang_parser_source[PATH_MAX];
    char backend_system_link_plan_source[PATH_MAX];
    char backend_lowering_plan_source[PATH_MAX];
    char backend_primary_object_plan_source[PATH_MAX];
    char backend_object_plan_source[PATH_MAX];
    char backend_native_link_plan_source[PATH_MAX];
    char backend_system_link_exec_source[PATH_MAX];
    char runtime_core_runtime_source[PATH_MAX];
    char runtime_compiler_runtime_source[PATH_MAX];
    char runtime_debug_runtime_source[PATH_MAX];
    char runtime_core_provider_source[PATH_MAX];
    char runtime_compiler_tooling_provider_source[PATH_MAX];
    char runtime_debug_provider_source[PATH_MAX];
    char runtime_program_support_provider_source[PATH_MAX];
    char tooling_gate_source[PATH_MAX];
    char tooling_bootstrap_contract_source[PATH_MAX];
    char tooling_host_ops_source[PATH_MAX];
    char tooling_path_source[PATH_MAX];
    char backend_build_plan_source[PATH_MAX];
    char stage0[PATH_MAX];
    char stage1[PATH_MAX];
    char stage2[PATH_MAX];
    char stage3[PATH_MAX];
    char env_path[PATH_MAX];
    char snapshot_path[PATH_MAX];
    char backend_driver_out[PATH_MAX];
    char stage0_build_log[PATH_MAX];
    char stage0_self_check_log[PATH_MAX];
    char stage1_build_log[PATH_MAX];
    char stage1_self_check_log[PATH_MAX];
    char stage2_build_log[PATH_MAX];
    char stage2_self_check_log[PATH_MAX];
    char stage3_build_log[PATH_MAX];
    char stage3_self_check_log[PATH_MAX];
    char stage2_contract_log[PATH_MAX];
    char stage3_contract_log[PATH_MAX];
    char stage1_report[PATH_MAX];
    char stage2_report[PATH_MAX];
    char stage3_report[PATH_MAX];
} V3BootstrapPaths;

static void v3_join_path(char *out, size_t cap, const char *a, const char *b);
static void v3_contract_workspace_root(const V3BootstrapContract *contract, char *out, size_t cap);
static bool v3_path_is_executable(const char *path);

static bool v3_bootstrap_workspace_root(char *out, size_t cap) {
    const char *env_root = getenv("CHENG_V3_ROOT");
    if (env_root != NULL && env_root[0] != '\0') {
        snprintf(out, cap, "%s", env_root);
        return true;
    }
    if (CHENG_V3_EMBEDDED_CONTRACT_TEXT[0] != '\0') {
        V3BootstrapContract contract;
        if (v3_parse_contract_text(&contract,
                                   CHENG_V3_EMBEDDED_SOURCE_PATH[0] != '\0' ? CHENG_V3_EMBEDDED_SOURCE_PATH : "<embedded>",
                                   CHENG_V3_EMBEDDED_CONTRACT_TEXT)) {
            v3_contract_workspace_root(&contract, out, cap);
            v3_contract_free(&contract);
            if (out[0] != '\0') {
                return true;
            }
        } else {
            v3_contract_free(&contract);
        }
    }
    if (getcwd(out, cap) == NULL) {
        perror("getcwd");
        out[0] = '\0';
        return false;
    }
    return true;
}

static void v3_bootstrap_target_default(char *out, size_t cap) {
    const char *env_target = getenv("CHENG_V3_TARGET");
    if (env_target != NULL && env_target[0] != '\0') {
        snprintf(out, cap, "%s", env_target);
        return;
    }
    env_target = getenv("V3_TARGET");
    if (env_target != NULL && env_target[0] != '\0') {
        snprintf(out, cap, "%s", env_target);
        return;
    }
    if (CHENG_V3_EMBEDDED_CONTRACT_TEXT[0] != '\0') {
        V3BootstrapContract contract;
        if (v3_parse_contract_text(&contract,
                                   CHENG_V3_EMBEDDED_SOURCE_PATH[0] != '\0' ? CHENG_V3_EMBEDDED_SOURCE_PATH : "<embedded>",
                                   CHENG_V3_EMBEDDED_CONTRACT_TEXT)) {
            const char *target = v3_contract_get(&contract, "target");
            snprintf(out, cap, "%s", target != NULL && target[0] != '\0' ? target : "arm64-apple-darwin");
            v3_contract_free(&contract);
            return;
        }
        v3_contract_free(&contract);
    }
    snprintf(out, cap, "%s", "arm64-apple-darwin");
}

static void v3_bootstrap_paths_init(V3BootstrapPaths *paths) {
    memset(paths, 0, sizeof(*paths));
    v3_bootstrap_workspace_root(paths->root, sizeof(paths->root));
    v3_bootstrap_target_default(paths->target, sizeof(paths->target));
    v3_join_path(paths->out_dir, sizeof(paths->out_dir), paths->root, "artifacts/v3_bootstrap");
    v3_join_path(paths->seed_source, sizeof(paths->seed_source), paths->root, "v3/bootstrap/cheng_v3_seed.c");
    v3_join_path(paths->stage1_source, sizeof(paths->stage1_source), paths->root, "v3/bootstrap/stage1_bootstrap.cheng");
    v3_join_path(paths->compiler_entry_source, sizeof(paths->compiler_entry_source), paths->root, "v3/src/tooling/compiler_main.cheng");
    v3_join_path(paths->backend_driver_entry_source, sizeof(paths->backend_driver_entry_source), paths->root, "v3/src/tooling/backend_driver_main.cheng");
    v3_join_path(paths->compiler_runtime_source, sizeof(paths->compiler_runtime_source), paths->root, "v3/src/tooling/compiler_runtime.cheng");
    v3_join_path(paths->compiler_request_source, sizeof(paths->compiler_request_source), paths->root, "v3/src/tooling/compiler_request.cheng");
    v3_join_path(paths->compiler_world_source, sizeof(paths->compiler_world_source), paths->root, "v3/src/tooling/compiler_world.cheng");
    v3_join_path(paths->compiler_csg_source, sizeof(paths->compiler_csg_source), paths->root, "v3/src/tooling/compiler_csg.cheng");
    v3_join_path(paths->compiler_world_libp2p_source, sizeof(paths->compiler_world_libp2p_source), paths->root, "v3/src/tooling/compiler_world_libp2p.cheng");
    v3_join_path(paths->compiler_equivalence_source, sizeof(paths->compiler_equivalence_source), paths->root, "v3/src/tooling/compiler_equivalence.cheng");
    v3_join_path(paths->lang_parser_source, sizeof(paths->lang_parser_source), paths->root, "v3/src/lang/parser.cheng");
    v3_join_path(paths->backend_system_link_plan_source, sizeof(paths->backend_system_link_plan_source), paths->root, "v3/src/backend/system_link_plan.cheng");
    v3_join_path(paths->backend_lowering_plan_source, sizeof(paths->backend_lowering_plan_source), paths->root, "v3/src/backend/lowering_plan.cheng");
    v3_join_path(paths->backend_primary_object_plan_source, sizeof(paths->backend_primary_object_plan_source), paths->root, "v3/src/backend/primary_object_plan.cheng");
    v3_join_path(paths->backend_object_plan_source, sizeof(paths->backend_object_plan_source), paths->root, "v3/src/backend/object_plan.cheng");
    v3_join_path(paths->backend_native_link_plan_source, sizeof(paths->backend_native_link_plan_source), paths->root, "v3/src/backend/native_link_plan.cheng");
    v3_join_path(paths->backend_system_link_exec_source, sizeof(paths->backend_system_link_exec_source), paths->root, "v3/src/backend/system_link_exec.cheng");
    v3_join_path(paths->runtime_core_runtime_source, sizeof(paths->runtime_core_runtime_source), paths->root, "v3/src/runtime/core_runtime_v3.cheng");
    v3_join_path(paths->runtime_compiler_runtime_source, sizeof(paths->runtime_compiler_runtime_source), paths->root, "v3/src/runtime/compiler_runtime_v3.cheng");
    v3_join_path(paths->runtime_debug_runtime_source, sizeof(paths->runtime_debug_runtime_source), paths->root, "v3/src/runtime/debug_runtime_v3.cheng");
    v3_join_path(paths->runtime_core_provider_source, sizeof(paths->runtime_core_provider_source), paths->root, "v3/src/runtime/core_runtime_provider_darwin_v3.cheng");
    v3_join_path(paths->runtime_compiler_tooling_provider_source, sizeof(paths->runtime_compiler_tooling_provider_source), paths->root, "v3/src/runtime/compiler_runtime_tooling_entry_provider_v3.cheng");
    v3_join_path(paths->runtime_debug_provider_source, sizeof(paths->runtime_debug_provider_source), paths->root, "v3/src/runtime/debug_runtime_stub_v3.cheng");
    v3_join_path(paths->runtime_program_support_provider_source, sizeof(paths->runtime_program_support_provider_source), paths->root, "v3/src/runtime/program_support_backend_v3.cheng");
    v3_join_path(paths->tooling_gate_source, sizeof(paths->tooling_gate_source), paths->root, "v3/src/tooling/gate_main.cheng");
    v3_join_path(paths->tooling_bootstrap_contract_source, sizeof(paths->tooling_bootstrap_contract_source), paths->root, "v3/src/tooling/bootstrap_contracts.cheng");
    v3_join_path(paths->tooling_host_ops_source, sizeof(paths->tooling_host_ops_source), paths->root, "v3/src/tooling/host_ops.cheng");
    v3_join_path(paths->tooling_path_source, sizeof(paths->tooling_path_source), paths->root, "v3/src/tooling/path.cheng");
    v3_join_path(paths->backend_build_plan_source, sizeof(paths->backend_build_plan_source), paths->root, "v3/src/backend/build_plan.cheng");
    v3_join_path(paths->stage0, sizeof(paths->stage0), paths->out_dir, "cheng.stage0");
    v3_join_path(paths->stage1, sizeof(paths->stage1), paths->out_dir, "cheng.stage1");
    v3_join_path(paths->stage2, sizeof(paths->stage2), paths->out_dir, "cheng.stage2");
    v3_join_path(paths->stage3, sizeof(paths->stage3), paths->out_dir, "cheng.stage3");
    v3_join_path(paths->env_path, sizeof(paths->env_path), paths->out_dir, "bootstrap.env");
    v3_join_path(paths->snapshot_path, sizeof(paths->snapshot_path), paths->out_dir, "bootstrap_snapshot.txt");
    v3_join_path(paths->backend_driver_out, sizeof(paths->backend_driver_out), paths->root, "artifacts/v3_backend_driver/cheng");
    v3_join_path(paths->stage0_build_log, sizeof(paths->stage0_build_log), paths->out_dir, "cheng.stage0.build.log");
    v3_join_path(paths->stage0_self_check_log, sizeof(paths->stage0_self_check_log), paths->out_dir, "cheng.stage0.self-check.log");
    v3_join_path(paths->stage1_build_log, sizeof(paths->stage1_build_log), paths->out_dir, "cheng.stage1.compile.log");
    v3_join_path(paths->stage1_self_check_log, sizeof(paths->stage1_self_check_log), paths->out_dir, "cheng.stage1.self-check.log");
    v3_join_path(paths->stage2_build_log, sizeof(paths->stage2_build_log), paths->out_dir, "cheng.stage2.compile.log");
    v3_join_path(paths->stage2_self_check_log, sizeof(paths->stage2_self_check_log), paths->out_dir, "cheng.stage2.self-check.log");
    v3_join_path(paths->stage3_build_log, sizeof(paths->stage3_build_log), paths->out_dir, "cheng.stage3.compile.log");
    v3_join_path(paths->stage3_self_check_log, sizeof(paths->stage3_self_check_log), paths->out_dir, "cheng.stage3.self-check.log");
    v3_join_path(paths->stage2_contract_log, sizeof(paths->stage2_contract_log), paths->out_dir, "cheng.stage2.contract.txt");
    v3_join_path(paths->stage3_contract_log, sizeof(paths->stage3_contract_log), paths->out_dir, "cheng.stage3.contract.txt");
    v3_join_path(paths->stage1_report, sizeof(paths->stage1_report), paths->out_dir, "cheng.stage1.report.txt");
    v3_join_path(paths->stage2_report, sizeof(paths->stage2_report), paths->out_dir, "cheng.stage2.report.txt");
    v3_join_path(paths->stage3_report, sizeof(paths->stage3_report), paths->out_dir, "cheng.stage3.report.txt");
}

static bool v3_bootstrap_artifacts_ready(const V3BootstrapPaths *paths) {
    struct stat st;
    return access(paths->stage2, X_OK) == 0 &&
           access(paths->stage3, X_OK) == 0 &&
           stat(paths->env_path, &st) == 0 &&
           S_ISREG(st.st_mode) &&
           st.st_size > 0 &&
           stat(paths->snapshot_path, &st) == 0 &&
           S_ISREG(st.st_mode) &&
           st.st_size > 0;
}

static bool v3_stat_mtime_ns(const char *path, uint64_t *out_ns) {
    struct stat st;
    uint64_t sec = 0;
    uint64_t nsec = 0;
    if (path == NULL || path[0] == '\0' || out_ns == NULL) {
        return false;
    }
    if (stat(path, &st) != 0) {
        return false;
    }
#if defined(__APPLE__)
    sec = (uint64_t)st.st_mtimespec.tv_sec;
    nsec = (uint64_t)st.st_mtimespec.tv_nsec;
#else
    sec = (uint64_t)st.st_mtime;
    nsec = 0;
#endif
    *out_ns = sec * 1000000000ULL + nsec;
    return true;
}

static bool v3_output_is_fresh_against_inputs(const char *output_path,
                                              const char *const *input_paths,
                                              size_t input_count) {
    uint64_t output_ns = 0;
    size_t i;
    if (!v3_stat_mtime_ns(output_path, &output_ns)) {
        return false;
    }
    for (i = 0; i < input_count; ++i) {
        uint64_t input_ns = 0;
        if (input_paths[i] == NULL || input_paths[i][0] == '\0') {
            continue;
        }
        if (!v3_stat_mtime_ns(input_paths[i], &input_ns)) {
            return false;
        }
        if (output_ns < input_ns) {
            return false;
        }
    }
    return true;
}

static bool v3_bootstrap_artifacts_fresh(const V3BootstrapPaths *paths) {
    const char *inputs[6];
    if (!v3_bootstrap_artifacts_ready(paths)) {
        return false;
    }
    inputs[0] = paths->seed_source;
    inputs[1] = paths->stage1_source;
    inputs[2] = paths->compiler_entry_source;
    inputs[3] = paths->compiler_runtime_source;
    inputs[4] = paths->compiler_request_source;
    inputs[5] = paths->tooling_gate_source;
    return v3_output_is_fresh_against_inputs(paths->stage2, inputs, 6U) &&
           v3_output_is_fresh_against_inputs(paths->stage3, inputs, 6U) &&
           v3_output_is_fresh_against_inputs(paths->env_path, inputs, 6U) &&
           v3_output_is_fresh_against_inputs(paths->snapshot_path, inputs, 6U);
}

static bool v3_backend_driver_ready(const V3BootstrapPaths *paths) {
    const char *inputs[28];
    if (paths == NULL || access(paths->backend_driver_out, X_OK) != 0) {
        return false;
    }
    if (!v3_bootstrap_artifacts_fresh(paths)) {
        return false;
    }
    inputs[0] = paths->env_path;
    inputs[1] = paths->stage2;
    inputs[2] = paths->stage3;
    inputs[3] = paths->backend_driver_entry_source;
    inputs[4] = paths->compiler_runtime_source;
    inputs[5] = paths->compiler_request_source;
    inputs[6] = paths->compiler_world_source;
    inputs[7] = paths->compiler_csg_source;
    inputs[8] = paths->compiler_world_libp2p_source;
    inputs[9] = paths->compiler_equivalence_source;
    inputs[10] = paths->lang_parser_source;
    inputs[11] = paths->backend_system_link_plan_source;
    inputs[12] = paths->backend_lowering_plan_source;
    inputs[13] = paths->backend_primary_object_plan_source;
    inputs[14] = paths->backend_object_plan_source;
    inputs[15] = paths->backend_native_link_plan_source;
    inputs[16] = paths->backend_system_link_exec_source;
    inputs[17] = paths->runtime_core_runtime_source;
    inputs[18] = paths->runtime_compiler_runtime_source;
    inputs[19] = paths->runtime_debug_runtime_source;
    inputs[20] = paths->tooling_bootstrap_contract_source;
    inputs[21] = paths->backend_build_plan_source;
    inputs[22] = paths->runtime_core_provider_source;
    inputs[23] = paths->runtime_compiler_tooling_provider_source;
    inputs[24] = paths->runtime_program_support_provider_source;
    inputs[25] = paths->runtime_debug_provider_source;
    inputs[26] = paths->tooling_host_ops_source;
    inputs[27] = paths->tooling_path_source;
    return v3_output_is_fresh_against_inputs(paths->backend_driver_out, inputs, 28U);
}

static bool v3_backend_driver_present(const V3BootstrapPaths *paths) {
    return paths != NULL && access(paths->backend_driver_out, X_OK) == 0;
}

static int v3_cmd_bootstrap_bridge(int argc, char **argv);
static int v3_cmd_build_backend_driver(int argc, char **argv);

static bool v3_ensure_backend_driver_ready(V3BootstrapPaths *paths,
                                           const char *context) {
    if (paths == NULL) {
        return false;
    }
    if (!v3_bootstrap_artifacts_fresh(paths) &&
        v3_cmd_bootstrap_bridge(0, NULL) != 0) {
        if (context != NULL && context[0] != '\0') {
            fprintf(stderr,
                    "[cheng_v3_seed] %s bootstrap-bridge failed\n",
                    context);
        }
        return false;
    }
    if (v3_backend_driver_present(paths)) {
        return true;
    }
    if (!v3_backend_driver_ready(paths) &&
        v3_cmd_build_backend_driver(0, NULL) != 0) {
        if (context != NULL && context[0] != '\0') {
            fprintf(stderr,
                    "[cheng_v3_seed] %s build-backend-driver failed\n",
                    context);
        }
        return false;
    }
    if (access(paths->backend_driver_out, X_OK) != 0) {
        if (context != NULL && context[0] != '\0') {
            fprintf(stderr,
                    "[cheng_v3_seed] %s missing backend driver: %s\n",
                    context,
                    paths->backend_driver_out);
        }
        return false;
    }
    return true;
}

static bool v3_lock_holder_alive(const char *lock_path, bool *alive_out) {
    char pid_text[64];
    ssize_t pid_len;
    char *end = NULL;
    long pid = 0;
    if (alive_out == NULL) {
        return false;
    }
    *alive_out = false;
    pid_len = readlink(lock_path, pid_text, sizeof(pid_text) - 1);
    if (pid_len < 0) {
        if (errno == ENOENT) {
            return true;
        }
        perror("readlink");
        return false;
    }
    pid_text[pid_len] = '\0';
    errno = 0;
    pid = strtol(pid_text, &end, 10);
    if (errno != 0 || end == pid_text || *end != '\0' || pid <= 0) {
        return true;
    }
    if (kill((pid_t)pid, 0) == 0 || errno == EPERM) {
        *alive_out = true;
        return true;
    }
    if (errno == ESRCH) {
        *alive_out = false;
        return true;
    }
    perror("kill");
    return false;
}

static bool v3_acquire_pid_lock(const char *lock_path,
                                bool *held_out,
                                V3ReadyCheckFn ready_fn,
                                const void *ready_ctx,
                                const char *label) {
    char pid_text[64];
    if (held_out != NULL) {
        *held_out = false;
    }
    snprintf(pid_text, sizeof(pid_text), "%ld", (long)getpid());
    for (;;) {
        if (symlink(pid_text, lock_path) == 0) {
            if (held_out != NULL) {
                *held_out = true;
            }
            return true;
        }
        if (errno != EEXIST) {
            fprintf(stderr, "[cheng_v3_seed] %s lock create failed: %s\n", label, lock_path);
            perror("symlink");
            return false;
        }
        if (ready_fn != NULL && ready_fn(ready_ctx)) {
            return true;
        }
        {
            bool alive = false;
            if (!v3_lock_holder_alive(lock_path, &alive)) {
                return false;
            }
            if (!alive) {
                if (unlink(lock_path) != 0 && errno != ENOENT) {
                    fprintf(stderr, "[cheng_v3_seed] %s lock cleanup failed: %s\n", label, lock_path);
                    perror("unlink");
                    return false;
                }
                continue;
            }
        }
        usleep(50000);
    }
}

static void v3_release_pid_lock(const char *lock_path, bool *held_io) {
    if (held_io == NULL || !*held_io) {
        return;
    }
    if (unlink(lock_path) != 0 && errno != ENOENT) {
        perror("unlink");
    }
    *held_io = false;
}

static bool v3_bootstrap_ready_check(const void *ctx) {
    return v3_bootstrap_artifacts_fresh((const V3BootstrapPaths *)ctx);
}

static bool v3_backend_driver_ready_check(const void *ctx) {
    return v3_backend_driver_ready((const V3BootstrapPaths *)ctx);
}

static bool v3_file_text_equal(const char *left_path, const char *right_path) {
    char *left = v3_read_file(left_path);
    char *right = v3_read_file(right_path);
    bool same = false;
    if (left != NULL && right != NULL) {
        same = strcmp(left, right) == 0;
    }
    free(left);
    free(right);
    return same;
}

static bool v3_build_plan_line_relevant(const char *line) {
    return strncmp(line, "target=", 7U) == 0 ||
           strncmp(line, "linker=", 7U) == 0 ||
           strncmp(line, "stage2_compiler=", 16U) == 0 ||
           strncmp(line, "entry=", 6U) == 0 ||
           strncmp(line, "output=", 7U) == 0 ||
           strncmp(line, "source[", 7U) == 0;
}

static char *v3_build_plan_comparable_text(const char *text) {
    size_t cap;
    char *out;
    size_t out_len = 0U;
    const char *cursor;
    if (text == NULL) {
        return NULL;
    }
    cap = strlen(text) + 1U;
    out = (char *)v3_xmalloc(cap);
    out[0] = '\0';
    cursor = text;
    while (*cursor != '\0') {
        const char *end = strchr(cursor, '\n');
        size_t len = end != NULL ? (size_t)(end - cursor) : strlen(cursor);
        char line[PATH_MAX * 4U];
        if (len >= sizeof(line)) {
            len = sizeof(line) - 1U;
        }
        memcpy(line, cursor, len);
        line[len] = '\0';
        if (v3_build_plan_line_relevant(line)) {
            if (out_len + len + 2U > cap) {
                size_t next_cap = cap * 2U + len + 2U;
                char *grown = (char *)realloc(out, next_cap);
                if (grown == NULL) {
                    free(out);
                    fprintf(stderr, "[cheng_v3_seed] out of memory\n");
                    exit(1);
                }
                out = grown;
                cap = next_cap;
            }
            memcpy(out + out_len, line, len);
            out_len += len;
            out[out_len++] = '\n';
            out[out_len] = '\0';
        }
        cursor = end != NULL ? end + 1 : cursor + len;
    }
    return out;
}

static bool v3_build_plan_files_equal(const char *left_path, const char *right_path) {
    char *left = v3_read_file(left_path);
    char *right = v3_read_file(right_path);
    char *left_cmp = NULL;
    char *right_cmp = NULL;
    bool same = false;
    if (left != NULL && right != NULL) {
        left_cmp = v3_build_plan_comparable_text(left);
        right_cmp = v3_build_plan_comparable_text(right);
        same = left_cmp != NULL && right_cmp != NULL && strcmp(left_cmp, right_cmp) == 0;
    }
    free(left);
    free(right);
    free(left_cmp);
    free(right_cmp);
    return same;
}

static bool v3_run_shell_command_logged(const char *command,
                                        const char *log_path,
                                        const char *label) {
    const char *argv_local[4];
    if (command == NULL || command[0] == '\0') {
        return false;
    }
    argv_local[0] = "/bin/sh";
    argv_local[1] = "-lc";
    argv_local[2] = command;
    argv_local[3] = NULL;
    return v3_run_command_logged_argv(3U, argv_local, log_path, label);
}

static bool v3_waitpid_nointr_silent(pid_t pid, int *status_out) {
    int status = 0;
    for (;;) {
        if (waitpid(pid, &status, 0) >= 0) {
            if (status_out != NULL) {
                *status_out = status;
            }
            return true;
        }
        if (errno == EINTR) {
            continue;
        }
        return false;
    }
}

static bool v3_waitpid_nointr(pid_t pid, int *status_out) {
    if (v3_waitpid_nointr_silent(pid, status_out)) {
        return true;
    }
    perror("waitpid");
    return false;
}

static bool v3_env_key_matches(const char *entry, const char *key) {
    size_t key_len;
    if (entry == NULL || key == NULL || key[0] == '\0') {
        return false;
    }
    key_len = strlen(key);
    return strncmp(entry, key, key_len) == 0 && entry[key_len] == '=';
}

static char *v3_env_override_entry(const char *key, const char *value) {
    size_t key_len;
    size_t value_len;
    char *out;
    if (key == NULL || value == NULL || key[0] == '\0') {
        return NULL;
    }
    key_len = strlen(key);
    value_len = strlen(value);
    out = (char *)v3_xmalloc(key_len + value_len + 2U);
    memcpy(out, key, key_len);
    out[key_len] = '=';
    memcpy(out + key_len + 1U, value, value_len);
    out[key_len + value_len + 1U] = '\0';
    return out;
}

static char **v3_copy_environ_with_overrides(const char *env_key_a,
                                             const char *env_value_a,
                                             const char *env_key_b,
                                             const char *env_value_b) {
    size_t count = 0U;
    size_t out_len = 0U;
    size_t i;
    char *override_a = v3_env_override_entry(env_key_a, env_value_a);
    char *override_b = v3_env_override_entry(env_key_b, env_value_b);
    char **out;
    bool same_key = false;
    if (override_a != NULL && override_b != NULL &&
        env_key_a != NULL && env_key_b != NULL &&
        strcmp(env_key_a, env_key_b) == 0) {
        same_key = true;
        free(override_a);
        override_a = NULL;
    }
    while (environ != NULL && environ[count] != NULL) {
        count += 1U;
    }
    out = (char **)v3_xmalloc(sizeof(char *) * (count + 3U));
    for (i = 0U; i < count; ++i) {
        const char *entry = environ[i];
        if (override_a != NULL && v3_env_key_matches(entry, env_key_a)) {
            continue;
        }
        if (override_b != NULL && v3_env_key_matches(entry, env_key_b)) {
            continue;
        }
        out[out_len++] = v3_strdup(entry);
    }
    if (override_a != NULL) {
        out[out_len++] = override_a;
    }
    if (override_b != NULL) {
        out[out_len++] = override_b;
    } else if (same_key) {
        out[out_len++] = v3_env_override_entry(env_key_b, env_value_b);
    }
    out[out_len] = NULL;
    return out;
}

static void v3_free_spawn_env(char **envp) {
    size_t i = 0U;
    if (envp == NULL) {
        return;
    }
    while (envp[i] != NULL) {
        free(envp[i]);
        i += 1U;
    }
    free(envp);
}

static void v3_write_spawn_error_log(const char *log_path,
                                     const char *program,
                                     int spawn_rc) {
    FILE *log_file;
    if (log_path == NULL || log_path[0] == '\0' || !v3_ensure_parent_dir(log_path)) {
        return;
    }
    log_file = fopen(log_path, "wb");
    if (log_file == NULL) {
        return;
    }
    fprintf(log_file,
            "spawn failed: %s rc=%d errno=%d (%s)\n",
            program != NULL ? program : "",
            spawn_rc,
            spawn_rc,
            strerror(spawn_rc));
    fclose(log_file);
}

static size_t v3_argv_count(char *const argv_local[]) {
    size_t count = 0U;
    if (argv_local == NULL) {
        return 0U;
    }
    while (argv_local[count] != NULL) {
        count += 1U;
    }
    return count;
}

static bool v3_spawn_argv_common(size_t arg_count,
                                 const char **args,
                                 const char *stdin_path,
                                 bool stdin_devnull,
                                 const char *stdout_path,
                                 bool merge_stderr,
                                 bool search_path,
                                 bool own_process_group,
                                 char *const envp[],
                                 pid_t *pid_out) {
    size_t i;
    char **exec_argv;
    posix_spawn_file_actions_t actions;
    posix_spawn_file_actions_t *actions_ptr = NULL;
    posix_spawnattr_t attr;
    posix_spawnattr_t *attr_ptr = NULL;
    pid_t pid = -1;
    int spawn_rc;
    bool actions_ready = false;
    bool attr_ready = false;
    short flags = 0;
    if (pid_out != NULL) {
        *pid_out = -1;
    }
    if (arg_count == 0U ||
        args == NULL ||
        args[0] == NULL ||
        args[0][0] == '\0' ||
        (stdin_path != NULL && stdin_path[0] == '\0')) {
        return false;
    }
    if (stdout_path != NULL && stdout_path[0] != '\0' && !v3_ensure_parent_dir(stdout_path)) {
        return false;
    }
    exec_argv = (char **)v3_xmalloc(sizeof(char *) * (arg_count + 1U));
    for (i = 0U; i < arg_count; ++i) {
        exec_argv[i] = (char *)(args[i] != NULL ? args[i] : "");
    }
    exec_argv[arg_count] = NULL;
    if (stdin_path != NULL || stdin_devnull || (stdout_path != NULL && stdout_path[0] != '\0') || merge_stderr) {
        if (posix_spawn_file_actions_init(&actions) != 0) {
            free(exec_argv);
            return false;
        }
        actions_ready = true;
        actions_ptr = &actions;
        if ((stdin_path != NULL &&
             posix_spawn_file_actions_addopen(&actions, STDIN_FILENO, stdin_path, O_RDONLY, 0) != 0) ||
            (stdin_path == NULL &&
             stdin_devnull &&
             posix_spawn_file_actions_addopen(&actions, STDIN_FILENO, "/dev/null", O_RDONLY, 0) != 0) ||
            ((stdout_path != NULL && stdout_path[0] != '\0') &&
             posix_spawn_file_actions_addopen(&actions,
                                              STDOUT_FILENO,
                                              stdout_path,
                                              O_WRONLY | O_CREAT | O_TRUNC,
                                              0644) != 0) ||
            (merge_stderr &&
             posix_spawn_file_actions_adddup2(&actions, STDOUT_FILENO, STDERR_FILENO) != 0)) {
            goto done;
        }
    }
    if (own_process_group) {
        if (posix_spawnattr_init(&attr) != 0) {
            goto done;
        }
        attr_ready = true;
        flags = (short)(flags | POSIX_SPAWN_SETPGROUP);
        if (posix_spawnattr_setflags(&attr, flags) != 0 ||
            posix_spawnattr_setpgroup(&attr, 0) != 0) {
            goto done;
        }
        attr_ptr = &attr;
    }
    spawn_rc = search_path
        ? posix_spawnp(&pid, exec_argv[0], actions_ptr, attr_ptr, exec_argv, envp != NULL ? envp : environ)
        : posix_spawn(&pid, exec_argv[0], actions_ptr, attr_ptr, exec_argv, envp != NULL ? envp : environ);
    if (spawn_rc != 0) {
        if (stdout_path != NULL && stdout_path[0] != '\0') {
            v3_write_spawn_error_log(stdout_path, exec_argv[0], spawn_rc);
        }
        errno = spawn_rc;
        perror(search_path ? "posix_spawnp" : "posix_spawn");
        goto done;
    }
    if (pid_out != NULL) {
        *pid_out = pid;
    }
    if (attr_ready) {
        posix_spawnattr_destroy(&attr);
    }
    posix_spawn_file_actions_destroy(&actions);
    free(exec_argv);
    return true;
done:
    if (attr_ready) {
        posix_spawnattr_destroy(&attr);
    }
    if (actions_ready) {
        posix_spawn_file_actions_destroy(&actions);
    }
    free(exec_argv);
    return false;
}

static bool v3_spawn_logged_argv(size_t arg_count,
                                 const char **args,
                                 const char *log_path,
                                 bool search_path,
                                 bool stdin_devnull,
                                 bool own_process_group,
                                 pid_t *pid_out) {
    return v3_spawn_argv_common(arg_count,
                                args,
                                NULL,
                                stdin_devnull,
                                log_path,
                                true,
                                search_path,
                                own_process_group,
                                NULL,
                                pid_out);
}

static bool v3_spawn_logged_argv_with_input_file(size_t arg_count,
                                                 const char **args,
                                                 const char *stdin_path,
                                                 const char *log_path,
                                                 bool search_path,
                                                 bool own_process_group,
                                                 pid_t *pid_out) {
    return v3_spawn_argv_common(arg_count,
                                args,
                                stdin_path,
                                false,
                                log_path,
                                true,
                                search_path,
                                own_process_group,
                                NULL,
                                pid_out);
}

static bool v3_spawn_argv_with_env(size_t arg_count,
                                   const char **args,
                                   bool search_path,
                                   bool own_process_group,
                                   char *const envp[],
                                   pid_t *pid_out) {
    return v3_spawn_argv_common(arg_count,
                                args,
                                NULL,
                                false,
                                NULL,
                                false,
                                search_path,
                                own_process_group,
                                envp,
                                pid_out);
}

static bool v3_run_command_status_argv(size_t arg_count,
                                       const char **args,
                                       bool search_path,
                                       const char *label) {
    pid_t pid = -1;
    int status = 0;
    if (!v3_spawn_argv_with_env(arg_count, args, search_path, false, NULL, &pid)) {
        return false;
    }
    if (!v3_waitpid_nointr(pid, &status)) {
        return false;
    }
    if (WIFEXITED(status) && WEXITSTATUS(status) == 0) {
        return true;
    }
    fprintf(stderr,
            "[cheng_v3_seed] %s failed status=%d program=%s\n",
            label != NULL ? label : "command",
            status,
            args != NULL && arg_count > 0U && args[0] != NULL ? args[0] : "");
    return false;
}

static bool v3_run_shell_command_status(const char *command, const char *label) {
    const char *argv_local[4];
    if (command == NULL || command[0] == '\0') {
        return false;
    }
    argv_local[0] = "/bin/sh";
    argv_local[1] = "-lc";
    argv_local[2] = command;
    argv_local[3] = NULL;
    return v3_run_command_status_argv(3U, argv_local, false, label);
}

static bool v3_run_command_logged_argv(size_t arg_count,
                                       const char **args,
                                       const char *log_path,
                                       const char *label) {
    pid_t pid;
    int status = 0;
    if (arg_count == 0U || args == NULL) {
        fprintf(stderr, "[cheng_v3_seed] %s missing argv\n", label);
        return false;
    }
    if (!v3_spawn_logged_argv(arg_count, args, log_path, true, true, false, &pid)) {
        return false;
    }
    if (!v3_waitpid_nointr(pid, &status)) {
        return false;
    }
    if (status != 0) {
        fprintf(stderr, "[cheng_v3_seed] %s failed rc=%d log=%s\n", label, status, log_path);
        return false;
    }
    return true;
}

static bool v3_run_command_logged_argv_quiet(size_t arg_count,
                                             const char **args,
                                             const char *log_path,
                                             const char *label) {
    pid_t pid;
    int status = 0;
    if (arg_count == 0U || args == NULL) {
        return false;
    }
    (void)label;
    if (!v3_spawn_logged_argv(arg_count, args, log_path, true, true, false, &pid)) {
        return false;
    }
    if (!v3_waitpid_nointr(pid, &status)) {
        return false;
    }
    return status == 0;
}

static bool v3_run_command_logged_argv_with_input_file(size_t arg_count,
                                                       const char **args,
                                                       const char *stdin_path,
                                                       const char *log_path,
                                                       const char *label,
                                                       bool quiet_failures) {
    pid_t pid;
    int status = 0;
    if (arg_count == 0U || args == NULL || stdin_path == NULL || stdin_path[0] == '\0') {
        return false;
    }
    if (!v3_spawn_logged_argv_with_input_file(arg_count,
                                              args,
                                              stdin_path,
                                              log_path,
                                              true,
                                              false,
                                              &pid)) {
        return false;
    }
    if (!v3_waitpid_nointr(pid, &status)) {
        return false;
    }
    if (status != 0 && !quiet_failures) {
        fprintf(stderr, "[cheng_v3_seed] %s failed rc=%d log=%s\n", label, status, log_path);
    }
    return status == 0;
}

static bool v3_bootstrap_write_env_and_snapshot(const V3BootstrapPaths *paths) {
    char *env_text;
    char *snapshot_text;
    size_t env_cap = 8192U;
    size_t snapshot_cap = 12288U;
    env_text = (char *)v3_xmalloc(env_cap);
    snapshot_text = (char *)v3_xmalloc(snapshot_cap);
    snprintf(env_text,
             env_cap,
             "V3_ROOT=%s\n"
             "V3_TARGET=%s\n"
             "V3_BOOTSTRAP_KIND=v3_selfhost\n"
             "V3_BOOTSTRAP_STAGE1_SOURCE=%s\n"
             "V3_COMPILER_ENTRY_SOURCE=%s\n"
             "V3_COMPILER_RUNTIME_SOURCE=%s\n"
             "V3_COMPILER_REQUEST_SOURCE=%s\n"
             "V3_BOOTSTRAP_STAGE0=%s\n"
             "V3_BOOTSTRAP_STAGE1=%s\n"
             "V3_BOOTSTRAP_STAGE2=%s\n"
             "V3_BOOTSTRAP_STAGE3=%s\n"
             "V3_BOOTSTRAP_ENV=%s\n"
             "V3_BOOTSTRAP_SNAPSHOT=%s\n"
             "V3_BOOTSTRAP_STAGE1_REPORT=%s\n"
             "V3_BOOTSTRAP_STAGE2_REPORT=%s\n"
             "V3_BOOTSTRAP_STAGE3_REPORT=%s\n",
             paths->root,
             paths->target,
             paths->stage1_source,
             paths->compiler_entry_source,
             paths->compiler_runtime_source,
             paths->compiler_request_source,
             paths->stage0,
             paths->stage1,
             paths->stage2,
             paths->stage3,
             paths->env_path,
             paths->snapshot_path,
             paths->stage1_report,
             paths->stage2_report,
             paths->stage3_report);
    snprintf(snapshot_text,
             snapshot_cap,
             "target=%s\n"
             "bootstrap_kind=v3_selfhost\n"
             "stage1_source=%s\n"
             "compiler_entry_source=%s\n"
             "compiler_runtime_source=%s\n"
             "compiler_request_source=%s\n"
             "tooling_gate_source=%s\n"
             "stage0=%s\n"
             "stage1=%s\n"
             "stage2=%s\n"
             "stage3=%s\n"
             "stage1_report=%s\n"
             "stage2_report=%s\n"
             "stage3_report=%s\n"
             "stage0_build_log=%s\n"
             "stage0_self_check_log=%s\n"
             "stage1_build_log=%s\n"
             "stage1_self_check_log=%s\n"
             "stage2_build_log=%s\n"
             "stage2_self_check_log=%s\n"
             "stage3_build_log=%s\n"
             "stage3_self_check_log=%s\n"
             "stage2_contract_log=%s\n"
             "stage3_contract_log=%s\n"
             "fixed_point=stage2_stage3_contract_match\n",
             paths->target,
             paths->stage1_source,
             paths->compiler_entry_source,
             paths->compiler_runtime_source,
             paths->compiler_request_source,
             paths->tooling_gate_source,
             paths->stage0,
             paths->stage1,
             paths->stage2,
             paths->stage3,
             paths->stage1_report,
             paths->stage2_report,
             paths->stage3_report,
             paths->stage0_build_log,
             paths->stage0_self_check_log,
             paths->stage1_build_log,
             paths->stage1_self_check_log,
             paths->stage2_build_log,
             paths->stage2_self_check_log,
             paths->stage3_build_log,
             paths->stage3_self_check_log,
             paths->stage2_contract_log,
             paths->stage3_contract_log);
    if (!v3_ensure_parent_dir(paths->env_path) ||
        !v3_write_text_file(paths->env_path, env_text) ||
        !v3_write_text_file(paths->snapshot_path, snapshot_text)) {
        free(env_text);
        free(snapshot_text);
        return false;
    }
    free(env_text);
    free(snapshot_text);
    return true;
}

static const char *v3_flag_value(int argc, char **argv, const char *flag) {
    size_t flag_len = strlen(flag);
    int i;
    for (i = 2; i < argc; ++i) {
        const char *arg = argv[i];
        if (strncmp(arg, flag, flag_len) == 0) {
            if (arg[flag_len] == ':') {
                return arg + flag_len + 1;
            }
            if (arg[flag_len] == '=') {
                return arg + flag_len + 1;
            }
            if (arg[flag_len] == '\0' && (i + 1) < argc) {
                return argv[i + 1];
            }
        }
    }
    return NULL;
}

#define CHENG_V3_MAX_PLAN_PATHS 256
#define CHENG_V3_MAX_PLAN_REASONS 16
#define CHENG_V3_MAX_RUNTIME_TARGETS 8
#define CHENG_V3_MAX_PROVIDER_MODULES 8
#define CHENG_V3_MAX_PLAN_FUNCTIONS 4096
#define CHENG_V3_MAX_SOURCE_LINES 16384
#define CHENG_V3_MAX_IMPORT_ALIASES 128
#define CHENG_V3_MAX_CALLEES 64
#define CHENG_V3_CONST_MAX_FIELDS 64
#define CHENG_V3_CONST_MAX_BINDINGS 64
#define CHENG_V3_CONST_MAX_ECHOS 8
#define CHENG_V3_MAX_TOP_LEVEL_CONSTS 2048
#define CHENG_V3_MAX_ASM_LOCALS 256
#define CHENG_V3_MAX_TYPE_TEXT 512
#define CHENG_V3_MAX_DEFAULT_EXPR 4096

typedef struct {
    char text[PATH_MAX];
} V3PlanPath;

static void v3_plan_path_add_unique(V3PlanPath *items,
                                    size_t *len,
                                    size_t cap,
                                    const char *text);

typedef struct {
    char owner_module_path[PATH_MAX];
    char target_module_path[PATH_MAX];
    char target_source_path[PATH_MAX];
    bool resolved;
} V3PlanImportEdge;

typedef struct {
    char alias_text[128];
    char module_path[PATH_MAX];
} V3ImportAlias;

typedef struct {
    char workspace_root[PATH_MAX];
    char package_root[PATH_MAX];
    char package_id[PATH_MAX];
    char entry_path[PATH_MAX];
    char output_path[PATH_MAX];
    char extra_link_input_path[PATH_MAX];
    char target_triple[128];
    char module_stem[PATH_MAX];
    char module_kind[64];
    char emit_kind[32];
    char entry_symbol[64];
    size_t runtime_target_count;
    V3PlanPath runtime_targets[CHENG_V3_MAX_RUNTIME_TARGETS];
    size_t provider_module_count;
    V3PlanPath provider_modules[CHENG_V3_MAX_PROVIDER_MODULES];
    size_t source_closure_count;
    V3PlanPath source_closure_paths[CHENG_V3_MAX_PLAN_PATHS];
    size_t owner_module_count;
    V3PlanPath owner_modules[8];
    size_t import_edge_count;
    V3PlanImportEdge import_edges[CHENG_V3_MAX_PLAN_PATHS];
    size_t unresolved_import_count;
    bool has_compiler_top_level;
    bool main_function_present;
    size_t missing_reason_count;
    char missing_reasons[CHENG_V3_MAX_PLAN_REASONS][128];
} V3SystemLinkPlanStub;

typedef struct {
    char source_path[PATH_MAX];
    char owner_module_path[PATH_MAX];
    char function_name[128];
    char symbol_text[PATH_MAX];
    char export_symbol_name[PATH_MAX];
    size_t signature_line_number;
    size_t body_first_line_number;
    size_t body_last_line_number;
    size_t param_count;
    char param_names[32][128];
    char param_types[32][CHENG_V3_MAX_TYPE_TEXT];
    bool param_is_var[32];
    char param_abi_classes[32][32];
    char return_type[CHENG_V3_MAX_TYPE_TEXT];
    char return_abi_class[32];
    char body_kind[64];
    char body_call_target[128];
    size_t body_call_arg_count;
    char body_call_arg0_name[128];
    char body_call_arg_names[CHENG_V3_MAX_CALL_ARGS][128];
    char first_body_line[512];
    size_t callee_count;
    V3PlanPath callee_symbols[CHENG_V3_MAX_CALLEES];
    bool is_entry;
    bool reachable;
    size_t generic_binding_count;
    char generic_binding_names[CHENG_V3_MAX_TYPE_PARAMS][128];
    char generic_binding_types[CHENG_V3_MAX_TYPE_PARAMS][256];
} V3LoweredFunctionStub;

typedef struct {
    char symbol_text[PATH_MAX];
    char owner_module_path[PATH_MAX];
    char const_name[128];
    char type_text[CHENG_V3_MAX_TYPE_TEXT];
    char abi_class[32];
    char str_value[8192];
    bool has_str_value;
    int64_t i64_value;
    bool bool_value;
} V3TopLevelConstStub;

typedef struct {
    char name[128];
    char type_text[CHENG_V3_MAX_TYPE_TEXT];
    char abi_class[32];
    int32_t stack_offset;
    int32_t stack_size;
    int32_t stack_align;
    bool indirect_value;
} V3AsmLocalSlot;

typedef enum {
    V3_TYPE_DEF_NONE = 0,
    V3_TYPE_DEF_ALIAS,
    V3_TYPE_DEF_RECORD
} V3TypeDefKind;

typedef enum {
    V3_FIELD_DEFAULT_EXPR_STABLE = 0,
    V3_FIELD_DEFAULT_EXPR_SIBLING_FIELD,
    V3_FIELD_DEFAULT_EXPR_CALL_UNSUPPORTED,
    V3_FIELD_DEFAULT_EXPR_UNSUPPORTED
} V3FieldDefaultExprKind;

typedef struct {
    char name[128];
    char type_text[CHENG_V3_MAX_TYPE_TEXT];
    bool has_explicit_name;
    bool has_default_expr;
    char default_expr[CHENG_V3_MAX_DEFAULT_EXPR];
} V3TypeFieldDef;

typedef struct {
    char source_path[PATH_MAX];
    char owner_module_path[PATH_MAX];
    char type_name[128];
    size_t generic_param_count;
    char generic_param_names[CHENG_V3_MAX_TYPE_PARAMS][128];
    V3TypeDefKind kind;
    bool ref_object;
    char alias_target[CHENG_V3_MAX_TYPE_TEXT];
    size_t field_count;
    V3TypeFieldDef fields[CHENG_V3_MAX_TYPE_FIELDS];
} V3TypeDefStub;

typedef struct {
    int32_t size;
    int32_t align;
} V3TypeLayoutStub;

typedef struct {
    size_t function_count;
    V3LoweredFunctionStub functions[CHENG_V3_MAX_PLAN_FUNCTIONS];
    size_t const_count;
    V3TopLevelConstStub consts[CHENG_V3_MAX_TOP_LEVEL_CONSTS];
    size_t type_def_count;
    V3TypeDefStub type_defs[CHENG_V3_MAX_TYPE_DEFS];
    size_t missing_reason_count;
    char missing_reasons[CHENG_V3_MAX_PLAN_REASONS][128];
} V3LoweringPlanStub;

typedef struct {
    char object_format[32];
    char output_path[PATH_MAX];
    char entry_path[PATH_MAX];
    char entry_symbol[128];
    bool consteval_entry_ready;
    size_t consteval_echo_count;
    V3PlanPath consteval_echo_texts[CHENG_V3_CONST_MAX_ECHOS];
    int32_t consteval_return_code;
    size_t item_count;
    int32_t item_ids[CHENG_V3_MAX_PLAN_FUNCTIONS];
    V3PlanPath symbol_names[CHENG_V3_MAX_PLAN_FUNCTIONS];
    size_t instruction_word_count;
    size_t data_label_count;
    size_t reloc_count;
    size_t unsupported_function_count;
    V3PlanPath unsupported_function_symbols[CHENG_V3_MAX_UNSUPPORTED_DETAILS];
    char unsupported_body_kinds[CHENG_V3_MAX_UNSUPPORTED_DETAILS][128];
    char unsupported_first_lines[CHENG_V3_MAX_UNSUPPORTED_DETAILS][128];
    char unsupported_abi_summaries[CHENG_V3_MAX_UNSUPPORTED_DETAILS][256];
    size_t missing_reason_count;
    char missing_reasons[CHENG_V3_MAX_PLAN_REASONS][128];
} V3PrimaryObjectPlanStub;

typedef struct {
    char primary_object_path[PATH_MAX];
    bool linux_nolibc_support_enabled;
    char linux_nolibc_runtime_source_path[PATH_MAX];
    char linux_nolibc_runtime_object_path[PATH_MAX];
    char linux_nolibc_startup_source_path[PATH_MAX];
    char linux_nolibc_startup_object_path[PATH_MAX];
    size_t provider_source_count;
    V3PlanPath provider_source_paths[CHENG_V3_MAX_PROVIDER_MODULES];
    size_t provider_object_count;
    V3PlanPath provider_object_paths[CHENG_V3_MAX_PROVIDER_MODULES];
    size_t link_input_count;
    V3PlanPath link_input_paths[CHENG_V3_MAX_PLAN_PATHS];
    size_t missing_reason_count;
    char missing_reasons[CHENG_V3_MAX_PLAN_REASONS][128];
} V3ObjectPlanStub;

typedef struct {
    char linker_program[PATH_MAX];
    char linker_flavor[64];
    char output_kind[64];
    char target_triple[128];
    char primary_object_path[PATH_MAX];
    char entry_symbol[128];
    bool consteval_builtin_ready;
    size_t consteval_echo_count;
    V3PlanPath consteval_echo_texts[CHENG_V3_CONST_MAX_ECHOS];
    int32_t consteval_return_code;
    size_t link_input_count;
    V3PlanPath link_input_paths[CHENG_V3_MAX_PLAN_PATHS];
    size_t missing_reason_count;
    char missing_reasons[CHENG_V3_MAX_PLAN_REASONS][128];
} V3NativeLinkPlanStub;

typedef struct {
    char name[128];
    uint32_t type;
    uint64_t offset;
    uint64_t size;
    uint64_t addralign;
    uint64_t entsize;
    uint32_t link;
    uint32_t info;
    int layout_kind;
} V3ElfSectionStub;

typedef struct {
    char name[PATH_MAX];
    uint8_t bind;
    uint8_t type;
    uint16_t shndx;
    uint64_t value;
    uint64_t size;
} V3ElfSymbolStub;

typedef struct {
    uint16_t target_shndx;
    uint64_t offset;
    uint32_t type;
    uint32_t sym_index;
    int64_t addend;
} V3ElfRelocStub;

typedef struct {
    int out_kind;
    uint64_t out_off;
} V3ElfSectionPlacementStub;

typedef struct {
    char path[PATH_MAX];
    uint16_t file_type;
    uint16_t machine;
    unsigned char *bytes;
    size_t len;
    size_t section_count;
    V3ElfSectionStub *sections;
    V3ElfSectionPlacementStub *placements;
    size_t symbol_count;
    V3ElfSymbolStub *symbols;
    size_t reloc_count;
    V3ElfRelocStub *relocs;
} V3ElfObjectStub;

typedef struct {
    char name[PATH_MAX];
    size_t object_index;
    uint32_t symbol_index;
} V3ElfGlobalDefStub;

typedef struct {
    char symbol_text[PATH_MAX];
    char export_symbol_name[PATH_MAX];
    int32_t layout_kind;
    int32_t effect_kind;
    int32_t capability_kind;
} V3CompilerExportEntry;

typedef struct {
    char channel[32];
    char package_id[PATH_MAX];
    V3Cid32 source_bundle_cid;
    V3Cid32 canonical_graph_cid;
    V3Cid32 graph_cid;
    size_t node_count;
    size_t edge_count;
    V3CompilerExportEntry *exports;
    size_t exported_count;
    V3Cid32 surface_cid;
    V3Cid32 compiler_pkg_cid;
    V3Cid32 std_pkg_cid;
    V3Cid32 runtime_pkg_cid;
    V3Cid32 manifest_root_cid;
    V3Cid32 world_head_cid;
    V3Cid32 canonical_output_digest_cid;
    V3Cid32 output_digest_cid;
    V3Cid32 receipt_cid;
    bool world_head_pinned;
    bool lock_check_enabled;
    bool lock_verified;
    bool baseline_surface_checked;
    bool baseline_surface_compatible;
    char lock_path[PATH_MAX];
    char baseline_surface_path[PATH_MAX];
    char *csg_report;
    char *surface_report;
    char *compiler_snapshot_report;
    char *std_snapshot_report;
    char *runtime_snapshot_report;
    char *world_report;
    char *receipt_report;
    char *lock_text;
} V3CompilerWorldArtifacts;

typedef struct {
    char source_path[PATH_MAX];
    char module_path[PATH_MAX];
    char *legacy_text;
    char *migrated_text;
} V3CompilerLegacySourceUnit;

typedef struct {
    char source_path[PATH_MAX];
    char module_path[PATH_MAX];
    char *text;
} V3CompilerSourceOverride;

typedef struct {
    char package_id[PATH_MAX];
    char entry_source_path[PATH_MAX];
    char entry_module_path[PATH_MAX];
    size_t unit_count;
    int32_t applied_rule_count;
    V3PlanPath applied_rules[CHENG_V3_MAX_PLAN_PATHS];
    V3Cid32 legacy_source_bundle_cid;
    V3Cid32 migrated_source_bundle_cid;
    V3Cid32 migration_cid;
} V3CompilerLegacyMigration;

typedef struct {
    char package_id[PATH_MAX];
    char channel[32];
    V3Cid32 baseline_graph_cid;
    V3Cid32 migrated_graph_cid;
    V3Cid32 baseline_surface_cid;
    V3Cid32 migrated_surface_cid;
    V3Cid32 baseline_receipt_cid;
    V3Cid32 migrated_receipt_cid;
    char target_triple[128];
    int32_t graph_equivalent;
    int32_t export_surface_compatible;
    int32_t compile_receipt_equivalent;
    char equivalence_kind[64];
    V3Cid32 proof_cid;
} V3CompilerEquivalenceProof;

typedef struct {
    char channel[32];
    int32_t stable_gate_required;
    int32_t equivalence_checked;
    int32_t equivalence_passed;
    int32_t publish_allowed;
    V3Cid32 world_head_cid;
    V3Cid32 proof_cid;
} V3CompilerPublishDecision;

static bool v3_target_is_darwin_arm64(const char *target_triple) {
    return target_triple != NULL && strcmp(target_triple, "arm64-apple-darwin") == 0;
}

static bool v3_target_is_android_aarch64(const char *target_triple) {
    return target_triple != NULL && strcmp(target_triple, "aarch64-linux-android") == 0;
}

static bool v3_target_is_linux_x86_64(const char *target_triple);
static bool v3_target_is_linux_generic_aarch64(const char *target_triple);
static bool v3_target_is_linux_riscv64(const char *target_triple);
static bool v3_target_is_windows_aarch64(const char *target_triple);
static bool v3_active_target_is_linux_x86_64(void);
static bool v3_abi_class_word_reg(const char *abi_class);
static const char *v3_x64_qreg_name(int reg_index);
static const char *v3_x64_dreg_name(int reg_index);
static const char *v3_x64_hreg_name(int reg_index);
static const char *v3_x64_breg_name(int reg_index);
static void v3_emit_x64_cmp_zero_set_bool(char *out,
                                          size_t cap,
                                          int src_reg,
                                          int dst_reg,
                                          bool wide,
                                          const char *cond);

static bool v3_target_is_ohos_aarch64(const char *target_triple) {
    return target_triple != NULL &&
           (strcmp(target_triple, "aarch64-linux-ohos") == 0 ||
            strcmp(target_triple, "aarch64-unknown-linux-ohos") == 0);
}

static bool v3_target_is_elf_aarch64(const char *target_triple) {
    return v3_target_is_android_aarch64(target_triple) ||
           v3_target_is_ohos_aarch64(target_triple) ||
           v3_target_is_linux_generic_aarch64(target_triple);
}

static bool v3_target_is_linux_x86_64(const char *target_triple) {
    return target_triple != NULL && strcmp(target_triple, "x86_64-unknown-linux-gnu") == 0;
}

static bool v3_target_is_linux_generic_aarch64(const char *target_triple) {
    return target_triple != NULL &&
           (strcmp(target_triple, "aarch64-unknown-linux-gnu") == 0 ||
            strcmp(target_triple, "arm64-unknown-linux-gnu") == 0);
}

static bool v3_target_is_linux_riscv64(const char *target_triple) {
    return target_triple != NULL &&
           strcmp(target_triple, "riscv64-unknown-linux-gnu") == 0;
}

static bool v3_target_is_windows_aarch64(const char *target_triple) {
    return target_triple != NULL &&
           (strcmp(target_triple, "aarch64-pc-windows-msvc") == 0 ||
            strcmp(target_triple, "arm64-pc-windows-msvc") == 0);
}

static bool v3_target_uses_consteval_builtin_executable(const char *target_triple) {
    return v3_target_is_windows_aarch64(target_triple) ||
           v3_target_is_linux_riscv64(target_triple);
}

static bool v3_target_matches_native_host(const char *target_triple) {
#if defined(__APPLE__) && defined(__aarch64__)
    return v3_target_is_darwin_arm64(target_triple);
#elif defined(__linux__) && defined(__x86_64__)
    return v3_target_is_linux_x86_64(target_triple);
#elif defined(__linux__) && defined(__aarch64__)
    return v3_target_is_linux_generic_aarch64(target_triple);
#elif defined(__linux__) && defined(__riscv)
    return v3_target_is_linux_riscv64(target_triple);
#else
    (void)target_triple;
    return false;
#endif
}

static bool v3_target_uses_linux_nolibc_runtime(const char *target_triple) {
    return v3_target_is_linux_generic_aarch64(target_triple);
}

static bool v3_target_is_wasm32_unknown_unknown(const char *target_triple) {
    return target_triple != NULL &&
           (strcmp(target_triple, "wasm32-unknown-unknown") == 0 ||
            strcmp(target_triple, "wasm32-unknown") == 0);
}

static bool v3_target_supports_seed_native_codegen(const char *target_triple) {
    return v3_target_is_darwin_arm64(target_triple) ||
           v3_target_is_android_aarch64(target_triple) ||
           v3_target_is_ohos_aarch64(target_triple) ||
           v3_target_is_wasm32_unknown_unknown(target_triple);
}

static const char *v3_supported_seed_target_csv(void) {
    return "arm64-apple-darwin,aarch64-linux-android,aarch64-linux-ohos,aarch64-unknown-linux-ohos,aarch64-unknown-linux-gnu,x86_64-unknown-linux-gnu,aarch64-pc-windows-msvc,riscv64-unknown-linux-gnu,wasm32-unknown-unknown";
}

static const char *v3_target_support_state(const char *target_triple) {
    if (v3_target_supports_seed_native_codegen(target_triple)) {
        return "supported";
    }
    if (v3_target_is_linux_x86_64(target_triple)) {
        return "supported";
    }
    if (v3_target_is_linux_generic_aarch64(target_triple)) {
        return "supported";
    }
    if (v3_target_uses_consteval_builtin_executable(target_triple)) {
        return "supported_consteval_builtin_only";
    }
    return "unsupported_target";
}

static const char *v3_target_support_detail(const char *target_triple) {
    if (v3_target_supports_seed_native_codegen(target_triple)) {
        return "v3_seed_native_codegen_available";
    }
    if (v3_target_is_linux_x86_64(target_triple) ||
        v3_target_is_linux_generic_aarch64(target_triple)) {
        return v3_target_is_linux_generic_aarch64(target_triple) ?
                   "v3_seed_generic_linux_aarch64_nolibc_pipeline_available" :
                   "v3_seed_generic_linux_x86_64_system_pipeline_available";
    }
    if (v3_target_is_windows_aarch64(target_triple)) {
        return "v3_seed_builtin_windows_aarch64_consteval_exe_available";
    }
    if (v3_target_is_linux_riscv64(target_triple)) {
        return "v3_seed_builtin_riscv64_consteval_exe_available";
    }
    return "v3_seed_target_triple_unrecognized";
}

static bool v3_target_supports_primary_object_codegen(const char *target_triple) {
    return v3_target_is_darwin_arm64(target_triple) ||
           v3_target_is_elf_aarch64(target_triple) ||
           v3_target_is_linux_x86_64(target_triple) ||
           v3_target_is_linux_riscv64(target_triple) ||
           v3_target_is_windows_aarch64(target_triple) ||
           v3_target_is_wasm32_unknown_unknown(target_triple);
}

static bool v3_target_supports_requested_emit(const char *target_triple,
                                              const char *emit_kind) {
    if (v3_target_supports_seed_native_codegen(target_triple)) {
        return true;
    }
    if (emit_kind != NULL &&
        (v3_streq(emit_kind, "obj") || v3_streq(emit_kind, "exe")) &&
        (v3_target_is_linux_generic_aarch64(target_triple) ||
         v3_target_is_linux_x86_64(target_triple))) {
        return true;
    }
    if (emit_kind != NULL &&
        v3_streq(emit_kind, "exe") &&
        v3_target_uses_consteval_builtin_executable(target_triple)) {
        return true;
    }
    return false;
}

static const char *v3_requested_emit_support_state(const char *target_triple,
                                                   const char *emit_kind) {
    if (v3_target_supports_requested_emit(target_triple, emit_kind)) {
        return "supported_for_requested_emit";
    }
    if (v3_target_is_linux_x86_64(target_triple)) {
        return "supported_for_requested_emit";
    }
    if (v3_target_is_linux_generic_aarch64(target_triple)) {
        return "unsupported_generic_linux_aarch64";
    }
    if (v3_target_uses_consteval_builtin_executable(target_triple)) {
        return "supported_for_requested_emit";
    }
    return "unsupported_target";
}

static const char *v3_requested_emit_support_detail(const char *target_triple,
                                                    const char *emit_kind) {
    if (v3_target_supports_seed_native_codegen(target_triple)) {
        return "v3_seed_native_codegen_available";
    }
    if (emit_kind != NULL && v3_streq(emit_kind, "obj")) {
        if (v3_target_is_linux_generic_aarch64(target_triple)) {
            return "v3_seed_generic_linux_aarch64_object_codegen_available";
        }
        if (v3_target_is_linux_x86_64(target_triple)) {
            return "v3_seed_generic_linux_x86_64_object_codegen_available";
        }
    }
    if (emit_kind != NULL && v3_streq(emit_kind, "exe")) {
        if (v3_target_is_linux_generic_aarch64(target_triple)) {
            return "v3_seed_generic_linux_aarch64_nolibc_exe_available";
        }
        if (v3_target_is_linux_x86_64(target_triple)) {
            return "v3_seed_generic_linux_x86_64_exe_available";
        }
        if (v3_target_is_windows_aarch64(target_triple)) {
            return "v3_seed_builtin_windows_aarch64_consteval_exe_available";
        }
        if (v3_target_is_linux_riscv64(target_triple)) {
            return "v3_seed_builtin_riscv64_consteval_exe_available";
        }
    }
    if (v3_target_is_linux_x86_64(target_triple) ||
        v3_target_is_linux_generic_aarch64(target_triple)) {
        return "v3_seed_generic_linux_requested_emit_not_supported";
    }
    return "v3_seed_target_triple_unrecognized";
}

static bool v3_target_needs_native_runtime_providers(const char *target_triple) {
    return !v3_target_is_wasm32_unknown_unknown(target_triple) &&
           !v3_target_uses_consteval_builtin_executable(target_triple);
}

static bool v3_target_requires_native_provider_modules(const char *target_triple) {
    return v3_target_needs_native_runtime_providers(target_triple) &&
           !v3_target_uses_linux_nolibc_runtime(target_triple);
}

static bool v3_target_supports_native_link_for_emit(const char *target_triple,
                                                    const char *emit_kind) {
    if (emit_kind == NULL || v3_streq(emit_kind, "obj")) {
        return false;
    }
    if (v3_target_supports_seed_native_codegen(target_triple)) {
        return true;
    }
    return v3_streq(emit_kind, "exe") &&
           (v3_target_is_linux_x86_64(target_triple) ||
            v3_target_uses_linux_nolibc_runtime(target_triple) ||
            v3_target_uses_consteval_builtin_executable(target_triple));
}

static bool v3_target_needs_leading_underscore(const char *target_triple) {
    return v3_target_is_darwin_arm64(target_triple);
}

static void v3_copy_platform_symbol_name(const char *target_triple,
                                         const char *symbol,
                                         char *out,
                                         size_t cap) {
    if (!out || cap == 0U) {
        return;
    }
    if (!symbol) {
        out[0] = '\0';
        return;
    }
    if (v3_target_needs_leading_underscore(target_triple) &&
        !(symbol[0] == 'L' && symbol[1] == '_') &&
        symbol[0] != '_') {
        snprintf(out, cap, "_%s", symbol);
        return;
    }
    snprintf(out, cap, "%s", symbol);
}

static void v3_copy_target_symbol_name(const V3SystemLinkPlanStub *plan,
                                       const char *symbol,
                                       char *out,
                                       size_t cap) {
    v3_copy_platform_symbol_name(plan != NULL ? plan->target_triple : "", symbol, out, cap);
}

typedef enum {
    V3_CONST_INVALID = 0,
    V3_CONST_BOOL,
    V3_CONST_I32,
    V3_CONST_F64,
    V3_CONST_STR,
    V3_CONST_SYMBOL,
    V3_CONST_RECORD
} V3ConstKind;

typedef struct V3ConstRecord V3ConstRecord;

typedef struct {
    V3ConstKind kind;
    bool bool_value;
    int32_t i32_value;
    double f64_value;
    char *text_value;
    V3ConstRecord *record_value;
} V3ConstValue;

struct V3ConstRecord {
    size_t field_count;
    char field_names[CHENG_V3_CONST_MAX_FIELDS][128];
    V3ConstValue field_values[CHENG_V3_CONST_MAX_FIELDS];
};

typedef struct {
    char name[128];
    V3ConstValue value;
} V3ConstBinding;

typedef struct {
    size_t binding_count;
    V3ConstBinding bindings[CHENG_V3_CONST_MAX_BINDINGS];
} V3ConstEnv;

typedef struct {
    bool ready;
    size_t echo_count;
    V3PlanPath echo_texts[CHENG_V3_CONST_MAX_ECHOS];
    int32_t return_code;
} V3ConstEntryProgram;

typedef struct {
    bool external;
    bool c_abi;
    const V3LoweredFunctionStub *lowered_function;
    char callee_name[PATH_MAX];
    char symbol_name[PATH_MAX];
    char return_type[CHENG_V3_MAX_TYPE_TEXT];
    char return_abi_class[32];
    size_t param_count;
    char param_types[CHENG_V3_MAX_CALL_ARGS][CHENG_V3_MAX_TYPE_TEXT];
    bool param_is_var[CHENG_V3_MAX_CALL_ARGS];
    char param_abi_classes[CHENG_V3_MAX_CALL_ARGS][32];
    bool ffi_handle_enabled;
    bool ffi_handle_return_is_handle;
    bool ffi_handle_param_is_handle[CHENG_V3_MAX_CALL_ARGS];
    int32_t ffi_handle_consume_index;
    char ffi_handle_register_symbol_name[PATH_MAX];
    char ffi_handle_resolve_symbol_name[PATH_MAX];
    char ffi_handle_invalidate_symbol_name[PATH_MAX];
} V3AsmCallTarget;

typedef enum {
    V3_RESULT_INTRINSIC_NONE = 0,
    V3_RESULT_INTRINSIC_IS_OK,
    V3_RESULT_INTRINSIC_IS_ERR,
    V3_RESULT_INTRINSIC_VALUE,
    V3_RESULT_INTRINSIC_ERROR,
    V3_RESULT_INTRINSIC_ERROR_TEXT,
    V3_RESULT_INTRINSIC_ERROR_INFO_OF,
    V3_RESULT_INTRINSIC_OK,
    V3_RESULT_INTRINSIC_ERR,
    V3_RESULT_INTRINSIC_ERR_CODE,
    V3_RESULT_INTRINSIC_ERR_INFO
} V3ResultIntrinsicKind;

typedef enum {
    V3_EXPR_SURFACE_ATOM = 0,
    V3_EXPR_SURFACE_IF,
    V3_EXPR_SURFACE_TERNARY,
    V3_EXPR_SURFACE_RANGE,
    V3_EXPR_SURFACE_CALL
} V3ExprSurfaceKind;

typedef struct {
    V3ExprSurfaceKind kind;
    bool rewritten;
    bool range_end_inclusive;
    V3ResultIntrinsicKind result_hint;
    char text[8192];
    char cond[4096];
    char when_true[4096];
    char when_false[4096];
    char callee[PATH_MAX];
    char args[CHENG_V3_MAX_CALL_ARGS][4096];
    size_t arg_count;
} V3ExprSurface;

typedef enum {
    V3_ABI_VALUE_VOID = 0,
    V3_ABI_VALUE_SCALAR,
    V3_ABI_VALUE_POINTER,
    V3_ABI_VALUE_COMPOSITE
} V3AbiValueKind;

typedef struct {
    char abi_class[16];
    V3AbiValueKind value_kind;
    bool prefers_address;
    bool wasm_i32_value;
    bool wasm_i32_param_slot;
    bool wasm_i32_return_slot;
} V3AbiDescriptor;

static bool v3_startswith(const char *text, const char *prefix) {
    size_t n = strlen(prefix);
    return strncmp(text, prefix, n) == 0;
}

static void v3_copy_text(char *dst, size_t cap, const char *src) {
    if (!src) {
        dst[0] = '\0';
        return;
    }
    snprintf(dst, cap, "%s", src);
}

static bool v3_symbol_has_cheng_internal_abi_prefix(const char *name) {
    return v3_startswith(name, "driver_c_") ||
           v3_startswith(name, "__cheng_") ||
           v3_startswith(name, "cheng_v3_") ||
           v3_startswith(name, "cheng_str_") ||
           v3_startswith(name, "cheng_seq_") ||
           v3_startswith(name, "cheng_rawbytes_") ||
           v3_startswith(name, "cheng_rawmem_") ||
           v3_startswith(name, "cheng_pty_") ||
           v3_startswith(name, "cheng_fd_") ||
           v3_startswith(name, "cheng_pipe_") ||
           v3_startswith(name, "cheng_exec_") ||
           v3_startswith(name, "cheng_epoch_") ||
           v3_startswith(name, "cheng_monotime_") ||
           v3_startswith(name, "cheng_system_") ||
           v3_startswith(name, "cheng_tailnet_") ||
           v3_startswith(name, "cheng_tcp_") ||
           v3_startswith(name, "rawmem") ||
           v3_streq(name, "alloc") ||
           v3_streq(name, "copyMem") ||
           v3_streq(name, "setMem") ||
           v3_streq(name, "ptr_add") ||
           v3_streq(name, "paramCount") ||
           v3_streq(name, "paramStr") ||
           v3_streq(name, "cmdCountFromRuntime") ||
           v3_streq(name, "cheng_cmd_ready_compat") ||
           v3_streq(name, "store");
}

static bool v3_symbol_uses_cheng_internal_abi(const char *symbol_name) {
    const char *name = symbol_name != NULL ? symbol_name : "";
    if (v3_symbol_has_cheng_internal_abi_prefix(name)) {
        return true;
    }
    return name[0] == '_' && v3_symbol_has_cheng_internal_abi_prefix(name + 1);
}

static bool v3_compiler_normalize_channel(const char *raw, char *out, size_t out_cap) {
    char copy[32];
    char *trimmed;
    if (!raw || *raw == '\0') {
        v3_copy_text(out, out_cap, "stable");
        return true;
    }
    v3_copy_text(copy, sizeof(copy), raw);
    trimmed = v3_trim_inplace(copy);
    if (*trimmed == '\0') {
        v3_copy_text(out, out_cap, "stable");
        return true;
    }
    if (!v3_streq(trimmed, "stable") && !v3_streq(trimmed, "edge")) {
        fprintf(stderr, "v3 compiler world: unsupported channel: %s\n", trimmed);
        return false;
    }
    v3_copy_text(out, out_cap, trimmed);
    return true;
}

static void v3_strip_trailing_comment_inplace(char *text) {
    bool in_string = false;
    size_t i;
    if (!text) {
        return;
    }
    for (i = 0; text[i] != '\0'; ++i) {
        if (text[i] == '"' && !v3_quote_is_escaped(text, i)) {
            in_string = !in_string;
            continue;
        }
        if (!in_string && text[i] == '#') {
            text[i] = '\0';
            break;
        }
    }
    v3_trim_inplace(text);
}

static void v3_const_value_init(V3ConstValue *value) {
    memset(value, 0, sizeof(*value));
    value->kind = V3_CONST_INVALID;
}

static void v3_const_value_free(V3ConstValue *value);

static void v3_const_record_free(V3ConstRecord *record) {
    size_t i;
    if (!record) {
        return;
    }
    for (i = 0; i < record->field_count; ++i) {
        v3_const_value_free(&record->field_values[i]);
    }
    free(record);
}

static void v3_const_value_free(V3ConstValue *value) {
    if (!value) {
        return;
    }
    if ((value->kind == V3_CONST_STR || value->kind == V3_CONST_SYMBOL) &&
        value->text_value != NULL) {
        free(value->text_value);
    }
    if (value->kind == V3_CONST_RECORD && value->record_value != NULL) {
        v3_const_record_free(value->record_value);
    }
    v3_const_value_init(value);
}

static bool v3_const_value_set_str_kind(V3ConstValue *value,
                                        V3ConstKind kind,
                                        const char *text) {
    v3_const_value_free(value);
    value->kind = kind;
    value->text_value = v3_strdup(text ? text : "");
    return true;
}

static bool v3_const_value_set_bool(V3ConstValue *value, bool bool_value) {
    v3_const_value_free(value);
    value->kind = V3_CONST_BOOL;
    value->bool_value = bool_value;
    return true;
}

static bool v3_const_value_set_i32(V3ConstValue *value, int32_t i32_value) {
    v3_const_value_free(value);
    value->kind = V3_CONST_I32;
    value->i32_value = i32_value;
    return true;
}

static bool v3_const_value_set_f64(V3ConstValue *value, double f64_value) {
    v3_const_value_free(value);
    value->kind = V3_CONST_F64;
    value->f64_value = f64_value;
    return true;
}

static bool v3_const_value_truthy(const V3ConstValue *value, bool *out) {
    if (value == NULL || out == NULL) {
        return false;
    }
    if (value->kind == V3_CONST_BOOL) {
        *out = value->bool_value;
        return true;
    }
    if (value->kind == V3_CONST_I32) {
        *out = value->i32_value != 0;
        return true;
    }
    if (value->kind == V3_CONST_F64) {
        *out = value->f64_value != 0.0;
        return true;
    }
    return false;
}

static bool v3_const_value_as_i32(const V3ConstValue *value, int32_t *out) {
    if (value == NULL || out == NULL) {
        return false;
    }
    if (value->kind == V3_CONST_I32) {
        *out = value->i32_value;
        return true;
    }
    if (value->kind == V3_CONST_BOOL) {
        *out = value->bool_value ? 1 : 0;
        return true;
    }
    if (value->kind == V3_CONST_F64) {
        if (value->f64_value < (double)INT32_MIN ||
            value->f64_value > (double)INT32_MAX) {
            return false;
        }
        *out = (int32_t)value->f64_value;
        return true;
    }
    return false;
}

static bool v3_const_value_as_f64(const V3ConstValue *value, double *out) {
    if (value == NULL || out == NULL) {
        return false;
    }
    if (value->kind == V3_CONST_F64) {
        *out = value->f64_value;
        return true;
    }
    if (value->kind == V3_CONST_I32) {
        *out = (double)value->i32_value;
        return true;
    }
    if (value->kind == V3_CONST_BOOL) {
        *out = value->bool_value ? 1.0 : 0.0;
        return true;
    }
    return false;
}

static bool v3_const_value_clone(const V3ConstValue *src, V3ConstValue *dst) {
    size_t i;
    v3_const_value_free(dst);
    dst->kind = src->kind;
    dst->bool_value = src->bool_value;
    dst->i32_value = src->i32_value;
    dst->f64_value = src->f64_value;
    if ((src->kind == V3_CONST_STR || src->kind == V3_CONST_SYMBOL) &&
        src->text_value != NULL) {
        dst->text_value = v3_strdup(src->text_value);
        return true;
    }
    if (src->kind == V3_CONST_RECORD && src->record_value != NULL) {
        V3ConstRecord *record = (V3ConstRecord *)v3_xmalloc(sizeof(V3ConstRecord));
        memset(record, 0, sizeof(*record));
        record->field_count = src->record_value->field_count;
        for (i = 0; i < src->record_value->field_count; ++i) {
            snprintf(record->field_names[i], sizeof(record->field_names[i]), "%s",
                     src->record_value->field_names[i]);
            v3_const_value_init(&record->field_values[i]);
            if (!v3_const_value_clone(&src->record_value->field_values[i],
                                      &record->field_values[i])) {
                v3_const_record_free(record);
                return false;
            }
        }
        dst->record_value = record;
    }
    return true;
}

static const V3ConstValue *v3_const_record_field(const V3ConstValue *record_value,
                                                 const char *field_name) {
    size_t i;
    if (!record_value || record_value->kind != V3_CONST_RECORD || record_value->record_value == NULL) {
        return NULL;
    }
    for (i = 0; i < record_value->record_value->field_count; ++i) {
        if (strcmp(record_value->record_value->field_names[i], field_name) == 0) {
            return &record_value->record_value->field_values[i];
        }
    }
    return NULL;
}

static bool v3_const_env_set(V3ConstEnv *env, const char *name, const V3ConstValue *value) {
    size_t i;
    for (i = 0; i < env->binding_count; ++i) {
        if (strcmp(env->bindings[i].name, name) == 0) {
            return v3_const_value_clone(value, &env->bindings[i].value);
        }
    }
    if (env->binding_count >= CHENG_V3_CONST_MAX_BINDINGS) {
        return false;
    }
    snprintf(env->bindings[env->binding_count].name,
             sizeof(env->bindings[env->binding_count].name),
             "%s",
             name);
    v3_const_value_init(&env->bindings[env->binding_count].value);
    if (!v3_const_value_clone(value, &env->bindings[env->binding_count].value)) {
        return false;
    }
    env->binding_count += 1U;
    return true;
}

static const V3ConstValue *v3_const_env_get(const V3ConstEnv *env, const char *name) {
    size_t i;
    for (i = 0; i < env->binding_count; ++i) {
        if (strcmp(env->bindings[i].name, name) == 0) {
            return &env->bindings[i].value;
        }
    }
    return NULL;
}

static void v3_const_env_free(V3ConstEnv *env) {
    size_t i;
    for (i = 0; i < env->binding_count; ++i) {
        v3_const_value_free(&env->bindings[i].value);
    }
    env->binding_count = 0U;
}

static void v3_trim_copy_text(const char *src, char *dst, size_t cap) {
    char temp[8192];
    if (strlen(src) >= sizeof(temp)) {
        v3_copy_text(dst, cap, src);
        return;
    }
    snprintf(temp, sizeof(temp), "%s", src);
    v3_copy_text(dst, cap, v3_trim_inplace(temp));
}

static int32_t v3_find_top_level_binary_op(const char *text, const char *op) {
    bool in_string = false;
    int32_t depth = 0;
    int32_t bracket_depth = 0;
    size_t op_len = strlen(op);
    size_t len = strlen(text);
    size_t i;
    for (i = 0; text[i] != '\0'; ++i) {
        if (text[i] == '"' && !v3_quote_is_escaped(text, i)) {
            in_string = !in_string;
            continue;
        }
        if (in_string) {
            continue;
        }
        if (text[i] == '(') {
            depth += 1;
            continue;
        }
        if (text[i] == ')') {
            depth -= 1;
            continue;
        }
        if (text[i] == '[') {
            bracket_depth += 1;
            continue;
        }
        if (text[i] == ']') {
            bracket_depth -= 1;
            continue;
        }
        if (depth == 0 &&
            bracket_depth == 0 &&
            strncmp(text + i, op, op_len) == 0) {
            if (op_len == 1U) {
                char prev = i > 0U ? text[i - 1U] : '\0';
                char next = i + 1U < len ? text[i + 1U] : '\0';
                if ((op[0] == '<' && (prev == '<' || next == '<' || next == '=')) ||
                    (op[0] == '>' && (prev == '>' || next == '>' || next == '=' || prev == '-')) ||
                    (op[0] == '&' && (prev == '&' || next == '&')) ||
                    (op[0] == '|' && (prev == '|' || next == '|')) ||
                    (op[0] == '-' && (next == '>' || prev == '-')) ||
                    (op[0] == '+' && (prev == '+' || next == '+'))) {
                    continue;
                }
            }
            return (int32_t)i;
        }
    }
    return -1;
}

static int32_t v3_find_top_level_char(const char *text, char target) {
    bool in_string = false;
    int32_t paren_depth = 0;
    int32_t bracket_depth = 0;
    int32_t brace_depth = 0;
    size_t i;
    if (!text) {
        return -1;
    }
    for (i = 0; text[i] != '\0'; ++i) {
        char ch = text[i];
        if (ch == '"' && !v3_quote_is_escaped(text, i)) {
            in_string = !in_string;
            continue;
        }
        if (in_string) {
            continue;
        }
        if (ch == '(') {
            paren_depth += 1;
            continue;
        }
        if (ch == ')') {
            paren_depth -= 1;
            continue;
        }
        if (ch == '[') {
            bracket_depth += 1;
            continue;
        }
        if (ch == ']') {
            bracket_depth -= 1;
            continue;
        }
        if (ch == '{') {
            brace_depth += 1;
            continue;
        }
        if (ch == '}') {
            brace_depth -= 1;
            continue;
        }
        if (paren_depth == 0 &&
            bracket_depth == 0 &&
            brace_depth == 0 &&
            ch == target) {
            return (int32_t)i;
        }
    }
    return -1;
}

static bool v3_split_top_level_args(const char *text,
                                    char items[][4096],
                                    size_t *item_count,
                                    size_t item_cap) {
    bool in_string = false;
    int32_t paren_depth = 0;
    int32_t bracket_depth = 0;
    int32_t brace_depth = 0;
    const char *start = text;
    size_t i;
    *item_count = 0U;
    for (i = 0; ; ++i) {
        const char ch = text[i];
        if (ch == '"' && !v3_quote_is_escaped(text, i)) {
            in_string = !in_string;
        } else if (!in_string && ch == '(') {
            paren_depth += 1;
        } else if (!in_string && ch == ')') {
            paren_depth -= 1;
        } else if (!in_string && ch == '[') {
            bracket_depth += 1;
        } else if (!in_string && ch == ']') {
            bracket_depth -= 1;
        } else if (!in_string && ch == '{') {
            brace_depth += 1;
        } else if (!in_string && ch == '}') {
            brace_depth -= 1;
        }
        if ((!in_string &&
             paren_depth == 0 &&
             bracket_depth == 0 &&
             brace_depth == 0 &&
             ch == ',') || ch == '\0') {
            if (*item_count >= item_cap) {
                return false;
            }
            snprintf(items[*item_count], sizeof(items[*item_count]), "%.*s", (int)(text + i - start), start);
            v3_trim_copy_text(items[*item_count], items[*item_count], sizeof(items[*item_count]));
            *item_count += 1U;
            start = text + i + 1;
        }
        if (ch == '\0') {
            break;
        }
    }
    if (*item_count == 1U && items[0][0] == '\0') {
        *item_count = 0U;
    }
    return true;
}

static bool v3_parse_call_text(const char *text,
                               char *callee,
                               size_t callee_cap,
                               char args[][4096],
                               size_t *arg_count,
                               size_t arg_cap) {
    char trimmed[8192];
    const char *open_paren = NULL;
    const char *close_paren = NULL;
    bool in_string = false;
    int32_t bracket_depth = 0;
    int32_t paren_depth = 0;
    char inner[8192];
    size_t i;
    v3_trim_copy_text(text, trimmed, sizeof(trimmed));
    for (i = 0; trimmed[i] != '\0'; ++i) {
        char ch = trimmed[i];
        if (ch == '"' && !v3_quote_is_escaped(trimmed, i)) {
            in_string = !in_string;
            continue;
        }
        if (in_string) {
            continue;
        }
        if (ch == '[') {
            bracket_depth += 1;
            continue;
        }
        if (ch == ']') {
            bracket_depth -= 1;
            continue;
        }
        if (ch == '(' && bracket_depth == 0) {
            if (!open_paren) {
                open_paren = trimmed + i;
            }
            paren_depth += 1;
            continue;
        }
        if (ch == ')' && bracket_depth == 0) {
            paren_depth -= 1;
            if (paren_depth == 0) {
                close_paren = trimmed + i;
            }
            continue;
        }
    }
    if (!open_paren || !close_paren || close_paren <= open_paren || paren_depth != 0 || bracket_depth != 0) {
        return false;
    }
    if (trimmed[strlen(trimmed) - 1U] != ')') {
        return false;
    }
    snprintf(callee, callee_cap, "%.*s", (int)(open_paren - trimmed), trimmed);
    v3_trim_copy_text(callee, callee, callee_cap);
    {
        int32_t callee_bracket_depth = 0;
        if (callee[0] == '\0') {
            return false;
        }
        for (i = 0; callee[i] != '\0'; ++i) {
            char ch = callee[i];
            if (ch == '[') {
                callee_bracket_depth += 1;
                continue;
            }
            if (ch == ']') {
                callee_bracket_depth -= 1;
                if (callee_bracket_depth < 0) {
                    return false;
                }
                continue;
            }
            if (callee_bracket_depth == 0 &&
                (isspace((unsigned char)ch) ||
                 ch == '&' || ch == '|' || ch == '+' || ch == '-' ||
                 ch == '*' || ch == '/' || ch == '%' || ch == '!' ||
                 ch == '<' || ch == '>' || ch == '=' || ch == '?')) {
                return false;
            }
        }
        if (callee_bracket_depth != 0) {
            return false;
        }
    }
    snprintf(inner, sizeof(inner), "%.*s", (int)(close_paren - open_paren - 1), open_paren + 1);
    return v3_split_top_level_args(inner, args, arg_count, arg_cap);
}

static bool v3_parse_list_literal_text(const char *text,
                                       char items[][4096],
                                       size_t *item_count,
                                       size_t item_cap) {
    char trimmed[8192];
    char inner[8192];
    size_t len;
    v3_trim_copy_text(text, trimmed, sizeof(trimmed));
    len = strlen(trimmed);
    if (len < 2U || trimmed[0] != '[' || trimmed[len - 1U] != ']') {
        return false;
    }
    snprintf(inner, sizeof(inner), "%.*s", (int)(len - 2U), trimmed + 1);
    return v3_split_top_level_args(inner, items, item_count, item_cap);
}

static bool v3_is_list_literal_text(const char *text, bool *nonempty_out) {
    char trimmed[8192];
    size_t len;
    v3_trim_copy_text(text, trimmed, sizeof(trimmed));
    len = strlen(trimmed);
    if (len < 2U || trimmed[0] != '[' || trimmed[len - 1U] != ']') {
        return false;
    }
    if (nonempty_out) {
        *nonempty_out = len > 2U;
    }
    return true;
}

static bool v3_parse_ternary_expr(const char *text,
                                  char *cond_out,
                                  size_t cond_cap,
                                  char *true_out,
                                  size_t true_cap,
                                  char *false_out,
                                  size_t false_cap) {
    char trimmed[8192];
    bool in_string = false;
    int32_t paren_depth = 0;
    int32_t bracket_depth = 0;
    int32_t brace_depth = 0;
    int32_t q_index = -1;
    int32_t colon_index = -1;
    size_t i;
    v3_trim_copy_text(text, trimmed, sizeof(trimmed));
    for (i = 0; trimmed[i] != '\0'; ++i) {
        char ch = trimmed[i];
        if (ch == '"' && !v3_quote_is_escaped(trimmed, i)) {
            in_string = !in_string;
            continue;
        }
        if (in_string) {
            continue;
        }
        if (ch == '(') {
            paren_depth += 1;
            continue;
        }
        if (ch == ')') {
            paren_depth -= 1;
            continue;
        }
        if (ch == '[') {
            bracket_depth += 1;
            continue;
        }
        if (ch == ']') {
            bracket_depth -= 1;
            continue;
        }
        if (ch == '{') {
            brace_depth += 1;
            continue;
        }
        if (ch == '}') {
            brace_depth -= 1;
            continue;
        }
        if (paren_depth == 0 && bracket_depth == 0 && brace_depth == 0) {
            if (ch == '?' && q_index < 0) {
                q_index = (int32_t)i;
                continue;
            }
            if (ch == ':' && q_index >= 0) {
                colon_index = (int32_t)i;
                break;
            }
        }
    }
    if (q_index <= 0 || colon_index <= q_index + 1) {
        return false;
    }
    snprintf(cond_out, cond_cap, "%.*s", q_index, trimmed);
    snprintf(true_out, true_cap, "%.*s", colon_index - q_index - 1, trimmed + q_index + 1);
    snprintf(false_out, false_cap, "%s", trimmed + colon_index + 1);
    v3_trim_copy_text(cond_out, cond_out, cond_cap);
    v3_trim_copy_text(true_out, true_out, true_cap);
    v3_trim_copy_text(false_out, false_out, false_cap);
    return cond_out[0] != '\0' && true_out[0] != '\0' && false_out[0] != '\0';
}

static int32_t v3_find_top_level_colon(const char *text);
static int32_t v3_find_top_level_else_expr_index(const char *text, size_t start_index) {
    bool in_string = false;
    int32_t paren_depth = 0;
    int32_t bracket_depth = 0;
    int32_t brace_depth = 0;
    int32_t if_expr_depth = 0;
    size_t i;
    if (text == NULL) {
        return -1;
    }
    for (i = start_index; text[i] != '\0'; ++i) {
        char ch = text[i];
        if (ch == '"' && !v3_quote_is_escaped(text, i)) {
            in_string = !in_string;
            continue;
        }
        if (in_string) {
            continue;
        }
        if (ch == '(') {
            paren_depth += 1;
            continue;
        }
        if (ch == ')') {
            paren_depth -= 1;
            continue;
        }
        if (ch == '[') {
            bracket_depth += 1;
            continue;
        }
        if (ch == ']') {
            bracket_depth -= 1;
            continue;
        }
        if (ch == '{') {
            brace_depth += 1;
            continue;
        }
        if (ch == '}') {
            brace_depth -= 1;
            continue;
        }
        if (paren_depth == 0 &&
            bracket_depth == 0 &&
            brace_depth == 0) {
            if (v3_startswith(text + i, "if ") &&
                (i == 0U ||
                 isspace((unsigned char)text[i - 1U]) ||
                 text[i - 1U] == ':')) {
                if_expr_depth += 1;
                continue;
            }
            if (v3_startswith(text + i, "else:") &&
                (i == 0U || isspace((unsigned char)text[i - 1U]))) {
                if (if_expr_depth > 0) {
                    if_expr_depth -= 1;
                    continue;
                }
                return (int32_t)i;
            }
        }
    }
    return -1;
}

static bool v3_parse_if_expr(const char *text,
                             char *cond_out,
                             size_t cond_cap,
                             char *true_out,
                             size_t true_cap,
                             char *false_out,
                             size_t false_cap) {
    char trimmed[8192];
    int32_t colon_index;
    int32_t else_index;
    v3_trim_copy_text(text, trimmed, sizeof(trimmed));
    if (!v3_startswith(trimmed, "if ")) {
        return false;
    }
    colon_index = v3_find_top_level_colon(trimmed);
    if (colon_index <= 3) {
        return false;
    }
    else_index = v3_find_top_level_else_expr_index(trimmed, (size_t)colon_index + 1U);
    if (else_index < 0 || else_index <= colon_index + 1) {
        return false;
    }
    snprintf(cond_out, cond_cap, "%.*s", colon_index - 3, trimmed + 3);
    snprintf(true_out,
             true_cap,
             "%.*s",
             else_index - colon_index - 1,
             trimmed + colon_index + 1);
    snprintf(false_out, false_cap, "%s", trimmed + else_index + 5);
    v3_trim_copy_text(cond_out, cond_out, cond_cap);
    v3_trim_copy_text(true_out, true_out, true_cap);
    v3_trim_copy_text(false_out, false_out, false_cap);
    return cond_out[0] != '\0' && true_out[0] != '\0' && false_out[0] != '\0';
}

static bool v3_trim_outer_parens(const char *expr_text, char *out, size_t cap);
static V3ResultIntrinsicKind v3_result_intrinsic_kind_from_name(const char *function_name);

static bool v3_parse_range_expr(const char *text,
                                char *start_expr_out,
                                size_t start_expr_cap,
                                char *end_expr_out,
                                size_t end_expr_cap,
                                bool *end_inclusive_out) {
    char trimmed[8192];
    int32_t range_sep = -1;
    size_t range_op_len = 0U;
    v3_trim_copy_text(text, trimmed, sizeof(trimmed));
    range_sep = v3_find_top_level_binary_op(trimmed, "..<");
    if (range_sep >= 0) {
        range_op_len = 3U;
        if (end_inclusive_out) {
            *end_inclusive_out = false;
        }
    } else {
        range_sep = v3_find_top_level_binary_op(trimmed, "..");
        if (range_sep >= 0) {
            range_op_len = 2U;
            if (end_inclusive_out) {
                *end_inclusive_out = true;
            }
        }
    }
    if (range_sep <= 0 || range_op_len == 0U) {
        return false;
    }
    snprintf(start_expr_out, start_expr_cap, "%.*s", range_sep, trimmed);
    snprintf(end_expr_out, end_expr_cap, "%s", trimmed + range_sep + (int32_t)range_op_len);
    v3_trim_copy_text(start_expr_out, start_expr_out, start_expr_cap);
    v3_trim_copy_text(end_expr_out, end_expr_out, end_expr_cap);
    return start_expr_out[0] != '\0' && end_expr_out[0] != '\0';
}

static void v3_expr_surface_init(V3ExprSurface *surface) {
    if (surface == NULL) {
        return;
    }
    memset(surface, 0, sizeof(*surface));
    surface->kind = V3_EXPR_SURFACE_ATOM;
    surface->result_hint = V3_RESULT_INTRINSIC_NONE;
}

static bool v3_classify_expr_surface(const char *expr_text,
                                     V3ExprSurface *surface,
                                     char *error_out,
                                     size_t error_cap) {
    char current[8192];
    char rewritten[32768];
    char outer[8192];
    char fmt_error[256];
    char start_expr[4096];
    char end_expr[4096];
    bool fmt_changed = false;
    size_t i;
    if (surface == NULL) {
        return false;
    }
    v3_expr_surface_init(surface);
    if (error_out && error_cap > 0U) {
        error_out[0] = '\0';
    }
    if (expr_text == NULL) {
        if (error_out && error_cap > 0U) {
            snprintf(error_out, error_cap, "%s", "expr_missing");
        }
        return false;
    }
    v3_trim_copy_text(expr_text, current, sizeof(current));
    if (current[0] == '\0') {
        if (error_out && error_cap > 0U) {
            snprintf(error_out, error_cap, "%s", "expr_empty");
        }
        return false;
    }
    for (i = 0U; i < 16U; ++i) {
        if (!v3_expand_strformat_fmt_expr(current,
                                          rewritten,
                                          sizeof(rewritten),
                                          &fmt_changed,
                                          fmt_error,
                                          sizeof(fmt_error))) {
            if (error_out && error_cap > 0U) {
                snprintf(error_out, error_cap, "%s", fmt_error[0] != '\0' ? fmt_error : "fmt_invalid");
            }
            return false;
        }
        if (fmt_changed) {
            surface->rewritten = true;
            v3_trim_copy_text(rewritten, current, sizeof(current));
            continue;
        }
        if (v3_trim_outer_parens(current, outer, sizeof(outer))) {
            surface->rewritten = true;
            v3_trim_copy_text(outer, current, sizeof(current));
            continue;
        }
        break;
    }
    snprintf(surface->text, sizeof(surface->text), "%s", current);
    if (v3_parse_if_expr(surface->text,
                         surface->cond,
                         sizeof(surface->cond),
                         surface->when_true,
                         sizeof(surface->when_true),
                         surface->when_false,
                         sizeof(surface->when_false))) {
        surface->kind = V3_EXPR_SURFACE_IF;
        return true;
    }
    if (v3_parse_ternary_expr(surface->text,
                              surface->cond,
                              sizeof(surface->cond),
                              surface->when_true,
                              sizeof(surface->when_true),
                              surface->when_false,
                              sizeof(surface->when_false))) {
        surface->kind = V3_EXPR_SURFACE_TERNARY;
        return true;
    }
    if (v3_parse_range_expr(surface->text,
                            start_expr,
                            sizeof(start_expr),
                            end_expr,
                            sizeof(end_expr),
                            &surface->range_end_inclusive)) {
        surface->kind = V3_EXPR_SURFACE_RANGE;
        snprintf(surface->when_true, sizeof(surface->when_true), "%s", start_expr);
        snprintf(surface->when_false, sizeof(surface->when_false), "%s", end_expr);
        return true;
    }
    if (v3_parse_call_text(surface->text,
                           surface->callee,
                           sizeof(surface->callee),
                           surface->args,
                           &surface->arg_count,
                           CHENG_V3_MAX_CALL_ARGS)) {
        const char *base = strrchr(surface->callee, '.');
        if (base != NULL) {
            base += 1;
        } else {
            base = surface->callee;
            {
                const char *scope = strrchr(base, ':');
                if (scope && scope != base && scope[-1] == ':') {
                    base = scope + 1;
                }
            }
        }
        surface->kind = V3_EXPR_SURFACE_CALL;
        surface->result_hint = v3_result_intrinsic_kind_from_name(base);
        return true;
    }
    return true;
}

static bool v3_statement_has_incomplete_if_expr(const char *statement) {
    char trimmed[8192];
    char expr[8192];
    char cond[4096];
    char if_true[4096];
    char if_false[4096];
    v3_trim_copy_text(statement, trimmed, sizeof(trimmed));
    if (v3_startswith(trimmed, "return ")) {
        v3_trim_copy_text(trimmed + 7, expr, sizeof(expr));
    } else {
        const char *rhs = strstr(trimmed, "= if ");
        if (!rhs) {
            return false;
        }
        v3_trim_copy_text(rhs + 2, expr, sizeof(expr));
    }
    if (!v3_startswith(expr, "if ")) {
        return false;
    }
    if (v3_parse_if_expr(expr,
                         cond,
                         sizeof(cond),
                         if_true,
                         sizeof(if_true),
                         if_false,
                         sizeof(if_false))) {
        return false;
    }
    return v3_find_top_level_colon(expr) >= 0;
}

static int32_t v3_next_expr_label_id(void) {
    static int32_t next_id = 0;
    next_id += 1;
    return next_id;
}

#define CHENG_V3_MAX_LOOP_LABEL_DEPTH 64

static char cheng_v3_loop_continue_labels[CHENG_V3_MAX_LOOP_LABEL_DEPTH][128];
static char cheng_v3_loop_break_labels[CHENG_V3_MAX_LOOP_LABEL_DEPTH][128];
static size_t cheng_v3_loop_label_depth = 0U;

static void v3_reset_loop_labels(void) {
    cheng_v3_loop_label_depth = 0U;
}

static bool v3_push_loop_labels(const char *continue_label, const char *break_label) {
    if (cheng_v3_loop_label_depth >= CHENG_V3_MAX_LOOP_LABEL_DEPTH) {
        return false;
    }
    snprintf(cheng_v3_loop_continue_labels[cheng_v3_loop_label_depth],
             sizeof(cheng_v3_loop_continue_labels[cheng_v3_loop_label_depth]),
             "%s",
             continue_label ? continue_label : "");
    snprintf(cheng_v3_loop_break_labels[cheng_v3_loop_label_depth],
             sizeof(cheng_v3_loop_break_labels[cheng_v3_loop_label_depth]),
             "%s",
             break_label ? break_label : "");
    cheng_v3_loop_label_depth += 1U;
    return true;
}

static void v3_pop_loop_labels(void) {
    if (cheng_v3_loop_label_depth > 0U) {
        cheng_v3_loop_label_depth -= 1U;
    }
}

static const char *v3_current_continue_label(void) {
    if (cheng_v3_loop_label_depth <= 0U) {
        return NULL;
    }
    return cheng_v3_loop_continue_labels[cheng_v3_loop_label_depth - 1U][0] != '\0'
               ? cheng_v3_loop_continue_labels[cheng_v3_loop_label_depth - 1U]
               : NULL;
}

static const char *v3_current_break_label(void) {
    if (cheng_v3_loop_label_depth <= 0U) {
        return NULL;
    }
    return cheng_v3_loop_break_labels[cheng_v3_loop_label_depth - 1U][0] != '\0'
               ? cheng_v3_loop_break_labels[cheng_v3_loop_label_depth - 1U]
               : NULL;
}

static int32_t v3_find_lowered_function_index_by_symbol(const V3LoweringPlanStub *lowering,
                                                        const char *symbol_text);
static bool v3_lowering_function_name_from_line(char *trimmed, char *out, size_t cap);
static bool v3_importc_function_name_from_line(char *trimmed, char *out, size_t cap);
static bool v3_importc_symbol_from_annotation(char *trimmed, char *out, size_t cap);
static bool v3_is_annotation_line(const char *trimmed);
static bool v3_annotation_named(const char *trimmed, const char *name);
static bool v3_parse_ffi_handle_consume_annotation(const char *trimmed, int32_t *out_idx);
static bool v3_exportc_symbol_from_annotation(char *trimmed, char *out, size_t cap);
static void v3_parse_import_aliases_from_lines(char **lines,
                                               size_t line_count,
                                               V3ImportAlias *aliases,
                                               size_t *alias_count,
                                               size_t alias_cap);
static const char *v3_alias_module_path(const V3ImportAlias *aliases,
                                        size_t alias_count,
                                        const char *alias_text);
static bool v3_identifier_exported_by_case(const char *name);
static void CHENG_V3_UNUSED v3_canonical_imported_member_name(const char *module_path,
                                                              char *member_name,
                                                              size_t member_name_cap);
static void v3_module_package_key(const char *module_path,
                                  char *out,
                                  size_t cap);
static bool v3_module_paths_share_package(const char *left_module_path,
                                          const char *right_module_path);
static const char *v3_alias_module_path_exported_member(const V3ImportAlias *aliases,
                                                        size_t alias_count,
                                                        const char *owner_module_path,
                                                        const char *alias_text,
                                                        char *member_name,
                                                        size_t member_name_cap);
static bool v3_resolve_bare_call_symbol(const V3LoweringPlanStub *lowering,
                                        const char *owner_module_path,
                                        const V3ImportAlias *aliases,
                                        size_t alias_count,
                                        const char *function_name,
                                        char *out,
                                        size_t cap);
static bool v3_parse_if_header(const char *statement,
                               char *cond_out,
                               size_t cond_cap,
                               char *inline_stmt_out,
                               size_t inline_stmt_cap);
static bool v3_parse_elif_header(const char *statement,
                                 char *cond_out,
                                 size_t cond_cap,
                                 char *inline_stmt_out,
                                 size_t inline_stmt_cap);
static bool v3_parse_else_header(const char *statement,
                                 char *inline_stmt_out,
                                 size_t inline_stmt_cap);
static bool v3_parse_while_header(const char *statement,
                                  char *cond_out,
                                  size_t cond_cap,
                                  char *inline_stmt_out,
                                  size_t inline_stmt_cap);
static bool v3_parse_for_header(const char *statement,
                                char *iter_name_out,
                                size_t iter_name_cap,
                                char *start_expr_out,
                                size_t start_expr_cap,
                                char *end_expr_out,
                                size_t end_expr_cap,
                                bool *end_inclusive_out,
                                char *inline_stmt_out,
                                size_t inline_stmt_cap);
static bool v3_parse_range_expr(const char *text,
                                char *start_expr_out,
                                size_t start_expr_cap,
                                char *end_expr_out,
                                size_t end_expr_cap,
                                bool *end_inclusive_out);
static bool v3_parse_assignment_meta(const char *statement,
                                     char *lhs_out,
                                     size_t lhs_cap,
                                     char *rhs_out,
                                     size_t rhs_cap);
static bool v3_parse_index_assignment_operands(const char *lhs_text,
                                               const char *rhs_text,
                                               char *base_out,
                                               size_t base_cap,
                                               char *index_out,
                                               size_t index_cap,
                                               char *value_out,
                                               size_t value_cap);
static void v3_mark_reachable_with_overload_group(V3LoweringPlanStub *lowering,
                                                  int32_t seed_index,
                                                  int32_t *queue,
                                                  size_t *queue_tail_io);
static bool v3_parse_index_access_expr(const char *expr_text,
                                       char *base_out,
                                       size_t base_cap,
                                       char *index_out,
                                       size_t index_cap);
static int32_t v3_call_expr_string_literal_count(const char *expr_text);
static void v3_strip_param_default_expr_text(const char *text,
                                             char *out,
                                             size_t cap);
static bool v3_function_has_unbound_generics(const V3LoweringPlanStub *lowering,
                                             const V3LoweredFunctionStub *function);
static bool v3_select_overloaded_function_symbol(const V3SystemLinkPlanStub *plan,
                                                 const V3LoweringPlanStub *lowering,
                                                 const V3LoweredFunctionStub *current_function,
                                                 const V3ImportAlias *aliases,
                                                 size_t alias_count,
                                                 const V3AsmLocalSlot *locals,
                                                 size_t local_count,
                                                 const char *module_path,
                                                 const char *function_name,
                                                 const char args[][4096],
                                                 size_t arg_count,
                                                 int32_t *selected_index_out,
                                                 char *out,
                                                 size_t cap);
static bool v3_select_visible_overloaded_function_symbol(const V3SystemLinkPlanStub *plan,
                                                         const V3LoweringPlanStub *lowering,
                                                         const V3LoweredFunctionStub *current_function,
                                                         const V3ImportAlias *aliases,
                                                         size_t alias_count,
                                                         const V3AsmLocalSlot *locals,
                                                         size_t local_count,
                                                         const char *function_name,
                                                         const char args[][4096],
                                                         size_t arg_count,
                                                         int32_t *selected_index_out,
                                                         char *out,
                                                         size_t cap);
static bool v3_is_intrinsic_call_name(const char *name);
static bool v3_is_intrinsic_module_call(const char *module_path, const char *function_name);
static const V3LoweredFunctionStub *v3_find_entry_lowered_function(const V3LoweringPlanStub *lowering);
static bool v3_function_has_unbound_generics(const V3LoweringPlanStub *lowering,
                                             const V3LoweredFunctionStub *function);
static void v3_source_path_to_module_path(const char *workspace_root,
                                          const char *package_root,
                                          const char *package_id,
                                          const char *source_path,
                                          char *out,
                                          size_t cap);
static bool v3_compiler_source_bundle_cid_with_overrides(const V3SystemLinkPlanStub *plan,
                                                         const V3CompilerSourceOverride *overrides,
                                                         size_t override_count,
                                                         V3Cid32 *out);
static int32_t v3_find_unique_lowered_function_index_by_module_and_name(const V3LoweringPlanStub *lowering,
                                                                        const char *module_path,
                                                                        const char *function_name);
static bool v3_build_compiler_world_artifacts_with_overrides(const V3SystemLinkPlanStub *plan,
                                                             const char *channel_raw,
                                                             const char *requested_world_head,
                                                             const char *lock_path,
                                                             const char *baseline_surface_path,
                                                             const V3CompilerSourceOverride *overrides,
                                                             size_t override_count,
                                                             V3CompilerWorldArtifacts *out);
static bool v3_write_world_bundle_text(const char *prefix,
                                       const char *suffix,
                                       const char *text);
static bool v3_materialize_cheng_object(const V3BootstrapContract *contract,
                                        const char *package_root,
                                        const char *source_path,
                                        const char *target_triple,
                                        const char *out_path);
static bool v3_compiler_equivalence_inputs_ready(int argc,
                                                 char **argv,
                                                 bool required,
                                                 bool *enabled_out);
static void v3_primary_symbol_name(const V3SystemLinkPlanStub *plan,
                                   const V3LoweredFunctionStub *function,
                                   char *out,
                                   size_t cap);
static bool v3_wasm_resolve_external_noarg_i32_from_source_path(const V3SystemLinkPlanStub *plan,
                                                                const char *source_path,
                                                                const char *callee,
                                                                char *symbol_name_out,
                                                                size_t symbol_name_cap);
static bool v3_wasm_resolve_external_arg0_i32_from_source_path(const V3SystemLinkPlanStub *plan,
                                                               const char *source_path,
                                                               const char *callee,
                                                               char *symbol_name_out,
                                                               size_t symbol_name_cap);
static bool v3_abi_class_scalar_or_ptr(const char *abi_class);
static void v3_strip_param_default_expr_text(const char *text,
                                             char *out,
                                             size_t cap);
static bool v3_find_type_def_by_normalized(const V3LoweringPlanStub *lowering,
                                           const char *normalized_type,
                                           int32_t *index_out);
static bool v3_function_has_unbound_generics(const V3LoweringPlanStub *lowering,
                                             const V3LoweredFunctionStub *function);
static bool v3_function_has_obvious_template_text(const V3LoweredFunctionStub *function);

static int32_t v3_line_indent(const char *line) {
    int32_t indent = 0;
    while (*line == ' ') {
        indent += 1;
        line++;
    }
    return indent;
}

static bool v3_collect_statement_from_lines(char **lines,
                                            size_t line_count,
                                            size_t *index_io,
                                            char *out,
                                            size_t out_cap) {
    size_t index = *index_io;
    size_t used = 0U;
    int32_t paren_depth = 0;
    int32_t bracket_depth = 0;
    int32_t brace_depth = 0;
    bool in_string = false;
    if (index >= line_count) {
        return false;
    }
    out[0] = '\0';
    while (index < line_count) {
        char piece[4096];
        char line_copy[4096];
        char *trimmed;
        size_t i;
        snprintf(line_copy, sizeof(line_copy), "%s", lines[index]);
        trimmed = v3_trim_inplace(line_copy);
        if (*trimmed == '\0' || *trimmed == '#') {
            index += 1U;
            if (used > 0U &&
                paren_depth <= 0 &&
                bracket_depth <= 0 &&
                brace_depth <= 0) {
                break;
            }
            continue;
        }
        if (used > 0U) {
            snprintf(out + used, out_cap - used, " ");
            used = strlen(out);
        }
        snprintf(piece, sizeof(piece), "%s", trimmed);
        v3_strip_trailing_comment_inplace(piece);
        if (piece[0] == '\0') {
            index += 1U;
            if (used > 0U &&
                paren_depth <= 0 &&
                bracket_depth <= 0 &&
                brace_depth <= 0) {
                break;
            }
            continue;
        }
        snprintf(out + used, out_cap - used, "%s", piece);
        used = strlen(out);
        for (i = 0; piece[i] != '\0'; ++i) {
            if (piece[i] == '"' && !v3_quote_is_escaped(piece, i)) {
                in_string = !in_string;
                continue;
            }
            if (in_string) {
                continue;
            }
            if (piece[i] == '(') {
                paren_depth += 1;
            } else if (piece[i] == ')') {
                paren_depth -= 1;
            } else if (piece[i] == '[') {
                bracket_depth += 1;
            } else if (piece[i] == ']') {
                bracket_depth -= 1;
            } else if (piece[i] == '{') {
                brace_depth += 1;
            } else if (piece[i] == '}') {
                brace_depth -= 1;
            }
        }
        index += 1U;
        if (paren_depth <= 0 &&
            bracket_depth <= 0 &&
            brace_depth <= 0 &&
            !(used >= 2U &&
              (strcmp(out + used - 2U, "||") == 0 ||
               strcmp(out + used - 2U, "&&") == 0 ||
               strcmp(out + used - 2U, "==") == 0 ||
               strcmp(out + used - 2U, "!=") == 0 ||
               strcmp(out + used - 2U, "<=") == 0 ||
               strcmp(out + used - 2U, ">=") == 0 ||
               strcmp(out + used - 2U, "<<") == 0 ||
               strcmp(out + used - 2U, ">>") == 0)) &&
            !(used >= 1U &&
              (out[used - 1U] == '|' ||
               out[used - 1U] == '^' ||
               out[used - 1U] == '&' ||
               out[used - 1U] == '+' ||
               out[used - 1U] == '-' ||
               out[used - 1U] == '*' ||
               out[used - 1U] == '/' ||
               out[used - 1U] == '%' ||
               out[used - 1U] == ','))) {
            if (v3_statement_has_incomplete_if_expr(out)) {
                continue;
            }
            break;
        }
    }
    *index_io = index;
    return used > 0U;
}

static bool v3_annotation_named(const char *trimmed, const char *name);

static bool v3_is_top_level_decl_start(const char *trimmed) {
    if (!trimmed || trimmed[0] == '\0') {
        return false;
    }
    if (v3_startswith(trimmed, "fn ")) {
        return true;
    }
    if (strcmp(trimmed, "type") == 0 || v3_startswith(trimmed, "type ")) {
        return true;
    }
    if (strcmp(trimmed, "const") == 0 || v3_startswith(trimmed, "const ")) {
        return true;
    }
    if (strcmp(trimmed, "var") == 0 || v3_startswith(trimmed, "var ")) {
        return true;
    }
    if (v3_startswith(trimmed, "import ") ||
        v3_startswith(trimmed, "importc ") ||
        trimmed[0] == '@') {
        return true;
    }
    return false;
}

static size_t v3_function_decl_prefix_len(const char *trimmed) {
    if (trimmed == NULL || trimmed[0] == '\0') {
        return 0U;
    }
    if (v3_startswith(trimmed, "fn ")) {
        return 3U;
    }
    return 0U;
}

static bool v3_compiler_top_level_export_symbol_from_annotation(char *trimmed,
                                                                char *out,
                                                                size_t cap) {
    char *export_pos;
    char *open_quote;
    char *close_quote;
    if (out != NULL && cap > 0U) {
        out[0] = '\0';
    }
    if (trimmed == NULL || out == NULL || cap == 0U) {
        return false;
    }
    if (!v3_annotation_named(trimmed, "compiler_top_level")) {
        return false;
    }
    export_pos = strstr(trimmed, "export_name");
    if (export_pos == NULL) {
        return false;
    }
    open_quote = strchr(export_pos, '"');
    if (open_quote == NULL) {
        return false;
    }
    close_quote = strchr(open_quote + 1, '"');
    if (close_quote == NULL || close_quote == open_quote + 1) {
        return false;
    }
    *close_quote = '\0';
    snprintf(out, cap, "%s", open_quote + 1);
    return true;
}

static bool v3_top_level_function_export_symbol_from_lines(char **lines,
                                                           size_t line_count,
                                                           size_t function_line_index,
                                                           char *out,
                                                           size_t cap) {
    size_t cursor;
    bool saw_annotation = false;
    if (out != NULL && cap > 0U) {
        out[0] = '\0';
    }
    if (lines == NULL || out == NULL || cap == 0U || function_line_index >= line_count) {
        return false;
    }
    cursor = function_line_index;
    while (cursor > 0U) {
        char line_copy[4096];
        char *trimmed;
        cursor -= 1U;
        snprintf(line_copy, sizeof(line_copy), "%s", lines[cursor]);
        trimmed = v3_trim_inplace(line_copy);
        if (trimmed[0] == '\0') {
            break;
        }
        if (trimmed[0] == '#') {
            continue;
        }
        if (v3_compiler_top_level_export_symbol_from_annotation(trimmed, out, cap) ||
            v3_exportc_symbol_from_annotation(trimmed, out, cap)) {
            return true;
        }
        if (v3_is_annotation_line(trimmed)) {
            saw_annotation = true;
            continue;
        }
        if (saw_annotation) {
            break;
        }
        return false;
    }
    return false;
}

static bool v3_source_text_has_explicit_compiler_exports(const char *text) {
    char *owned;
    char *cursor;
    char *line;
    bool out = false;
    if (text == NULL) {
        return false;
    }
    owned = v3_strdup(text);
    cursor = owned;
    while ((line = strsep(&cursor, "\n")) != NULL) {
        char trimmed[4096];
        char export_symbol[PATH_MAX];
        v3_trim_copy_text(line, trimmed, sizeof(trimmed));
        if (v3_compiler_top_level_export_symbol_from_annotation(trimmed,
                                                                export_symbol,
                                                                sizeof(export_symbol)) ||
            v3_exportc_symbol_from_annotation(trimmed,
                                                                export_symbol,
                                                                sizeof(export_symbol))) {
            out = true;
            break;
        }
    }
    free(owned);
    return out;
}

static size_t v3_count_function_body_statements(char **lines,
                                                size_t line_count,
                                                size_t signature_end_index) {
    size_t index = signature_end_index + 1U;
    size_t count = 0U;
    while (index < line_count) {
        char raw_copy[4096];
        char statement[8192];
        char *trimmed;
        snprintf(raw_copy, sizeof(raw_copy), "%s", lines[index]);
        trimmed = v3_trim_inplace(raw_copy);
        if (*trimmed == '\0') {
            index += 1U;
            continue;
        }
        if (v3_line_indent(lines[index]) <= 0 && v3_is_top_level_decl_start(trimmed)) {
            break;
        }
        if (!v3_collect_statement_from_lines(lines, line_count, &index, statement, sizeof(statement))) {
            break;
        }
        count += 1U;
    }
    return count;
}

static bool v3_is_last_logical_statement_in_function(char **lines,
                                                     size_t line_count,
                                                     size_t index_after_statement) {
    size_t index = index_after_statement;
    while (index < line_count) {
        char raw_copy[4096];
        char statement[8192];
        char *trimmed;
        snprintf(raw_copy, sizeof(raw_copy), "%s", lines[index]);
        trimmed = v3_trim_inplace(raw_copy);
        if (*trimmed == '\0') {
            index += 1U;
            continue;
        }
        if (v3_line_indent(lines[index]) <= 0 && v3_is_top_level_decl_start(trimmed)) {
            return true;
        }
        if (!v3_collect_statement_from_lines(lines, line_count, &index, statement, sizeof(statement))) {
            break;
        }
        return false;
    }
    return true;
}

static bool v3_const_is_whitespace_char(char ch) {
    return ch == ' ' || ch == '\t' || ch == '\n' || ch == '\r';
}

static bool v3_const_str_strip_ascii(const char *text, char *out, size_t cap) {
    const char *start = text;
    const char *end = text + strlen(text);
    while (*start != '\0' && v3_const_is_whitespace_char(*start)) {
        start++;
    }
    while (end > start && v3_const_is_whitespace_char(end[-1])) {
        end--;
    }
    snprintf(out, cap, "%.*s", (int)(end - start), start);
    return true;
}

static bool v3_const_os_is_absolute(const char *text) {
    size_t len = strlen(text);
    if (len <= 0U || text[0] == '\0') {
        return false;
    }
    if (text[0] == '/' || text[0] == '\\') {
        return true;
    }
    if (len > 1U && text[1] == ':') {
        return true;
    }
    return false;
}

static bool v3_const_os_join_path(const char *a, const char *b, char *out, size_t cap) {
    size_t a_len = strlen(a);
    size_t b_len = strlen(b);
    if (a_len <= 0U || a[0] == '\0') {
        snprintf(out, cap, "%s", b);
        return true;
    }
    if (b_len <= 0U || b[0] == '\0') {
        snprintf(out, cap, "%s", a);
        return true;
    }
    if (a[a_len - 1U] == '/' || a[a_len - 1U] == '\\') {
        snprintf(out, cap, "%s%s", a, b);
        return true;
    }
    snprintf(out, cap, "%s/%s", a, b);
    return true;
}

static bool v3_const_parse_function_signature(const char *line,
                                              const char *function_name,
                                              char param_names[][128],
                                              size_t *param_count,
                                              size_t param_cap) {
    const char *open_paren;
    const char *close_paren;
    char params[4096];
    char items[32][4096];
    size_t item_count = 0U;
    size_t i;
    *param_count = 0U;
    open_paren = strchr(line, '(');
    close_paren = strchr(line, ')');
    if (!open_paren || !close_paren || close_paren <= open_paren) {
        return false;
    }
    char header_copy[4096];
    snprintf(header_copy, sizeof(header_copy), "%s", line);
    if (v3_function_decl_prefix_len(v3_trim_inplace(header_copy)) == 0U) {
        return false;
    }
    if (strstr(line, function_name) == NULL) {
        return false;
    }
    snprintf(params, sizeof(params), "%.*s", (int)(close_paren - open_paren - 1), open_paren + 1);
    if (!v3_split_top_level_args(params, items, &item_count, 32U)) {
        return false;
    }
    for (i = 0; i < item_count; ++i) {
        char *colon = strchr(items[i], ':');
        if (*param_count >= param_cap) {
            return false;
        }
        if (colon) {
            *colon = '\0';
        }
        v3_trim_copy_text(items[i], param_names[*param_count], sizeof(param_names[*param_count]));
        if (param_names[*param_count][0] != '\0') {
            *param_count += 1U;
        }
    }
    return true;
}

static bool v3_type_text_strip_one_pointer_suffix(const char *type_text,
                                                  char *base_out,
                                                  size_t base_cap) {
    char trimmed[256];
    size_t len;
    if (!type_text || !base_out || base_cap == 0U) {
        return false;
    }
    v3_trim_copy_text(type_text, trimmed, sizeof(trimmed));
    len = strlen(trimmed);
    while (len > 0U && isspace((unsigned char)trimmed[len - 1U])) {
        trimmed[len - 1U] = '\0';
        len -= 1U;
    }
    if (len == 0U || trimmed[len - 1U] != '*') {
        return false;
    }
    trimmed[len - 1U] = '\0';
    v3_trim_copy_text(trimmed, base_out, base_cap);
    return base_out[0] != '\0';
}

static int32_t v3_type_text_pointer_depth(const char *type_text,
                                          char *base_out,
                                          size_t base_cap) {
    char current[256];
    char next[256];
    int32_t depth = 0;
    if (!type_text) {
        return 0;
    }
    v3_trim_copy_text(type_text, current, sizeof(current));
    while (v3_type_text_strip_one_pointer_suffix(current, next, sizeof(next))) {
        snprintf(current, sizeof(current), "%s", next);
        depth += 1;
    }
    if (base_out && base_cap > 0U) {
        snprintf(base_out, base_cap, "%s", current);
    }
    return depth;
}

static const char *v3_type_abi_class(const char *type_text) {
    char pointer_base[256];
    if (!type_text || *type_text == '\0' || strcmp(type_text, "void") == 0) {
        return "void";
    }
    if (v3_startswith(type_text, "ref@")) {
        return "ptr";
    }
    if (v3_startswith(type_text, "fn ") || v3_startswith(type_text, "fn(")) {
        return "ptr";
    }
    if (strcmp(type_text, "int8") == 0) {
        return "i8";
    }
    if (strcmp(type_text, "uint8") == 0) {
        return "u8";
    }
    if (strcmp(type_text, "int16") == 0) {
        return "i16";
    }
    if (strcmp(type_text, "uint16") == 0) {
        return "u16";
    }
    if (strcmp(type_text, "int32") == 0 ||
        strcmp(type_text, "int") == 0 ||
        strcmp(type_text, "uint32") == 0 ||
        strcmp(type_text, "char") == 0) {
        return "i32";
    }
    if (strcmp(type_text, "int64") == 0 ||
        strcmp(type_text, "uint64") == 0) {
        return "i64";
    }
    if (strcmp(type_text, "float") == 0 ||
        strcmp(type_text, "float32") == 0 ||
        strcmp(type_text, "float64") == 0) {
        return "i64";
    }
    if (strcmp(type_text, "bool") == 0) {
        return "bool";
    }
    if (strcmp(type_text, "ptr") == 0 ||
        strcmp(type_text, "cstring") == 0) {
        return "ptr";
    }
    if (v3_type_text_strip_one_pointer_suffix(type_text, pointer_base, sizeof(pointer_base))) {
        return "ptr";
    }
    return "composite";
}

static V3AbiDescriptor v3_abi_descriptor_from_class(const char *abi_class) {
    V3AbiDescriptor desc;
    memset(&desc, 0, sizeof(desc));
    snprintf(desc.abi_class, sizeof(desc.abi_class), "%s", abi_class ? abi_class : "void");
    if (abi_class == NULL || strcmp(abi_class, "void") == 0) {
        desc.value_kind = V3_ABI_VALUE_VOID;
        return desc;
    }
    if (strcmp(abi_class, "ptr") == 0) {
        desc.value_kind = V3_ABI_VALUE_POINTER;
        desc.wasm_i32_param_slot = true;
        desc.wasm_i32_return_slot = true;
        return desc;
    }
    if (strcmp(abi_class, "composite") == 0) {
        desc.value_kind = V3_ABI_VALUE_COMPOSITE;
        desc.prefers_address = true;
        desc.wasm_i32_param_slot = true;
        desc.wasm_i32_return_slot = true;
        return desc;
    }
    desc.value_kind = V3_ABI_VALUE_SCALAR;
    desc.wasm_i32_value =
        strcmp(abi_class, "i32") == 0 ||
        strcmp(abi_class, "u32") == 0 ||
        strcmp(abi_class, "bool") == 0 ||
        strcmp(abi_class, "i8") == 0 ||
        strcmp(abi_class, "u8") == 0 ||
        strcmp(abi_class, "i16") == 0 ||
        strcmp(abi_class, "u16") == 0;
    desc.wasm_i32_param_slot = desc.wasm_i32_value;
    desc.wasm_i32_return_slot = desc.wasm_i32_value || strcmp(abi_class, "i64") == 0;
    return desc;
}

static bool v3_is_internal_ref_type(const char *type_text) {
    return type_text != NULL && v3_startswith(type_text, "ref@");
}

static void v3_strip_internal_ref_type(const char *type_text, char *out, size_t cap) {
    if (cap == 0U) {
        return;
    }
    if (v3_is_internal_ref_type(type_text)) {
        snprintf(out, cap, "%s", type_text + 4);
        return;
    }
    snprintf(out, cap, "%s", type_text ? type_text : "");
}

static void v3_write_type_def_normalized(const V3TypeDefStub *type_def, char *out, size_t cap) {
    if (cap == 0U) {
        return;
    }
    if (type_def == NULL) {
        out[0] = '\0';
        return;
    }
    if (type_def->ref_object) {
        snprintf(out, cap, "ref@%s::%s", type_def->owner_module_path, type_def->type_name);
        return;
    }
    snprintf(out, cap, "%s::%s", type_def->owner_module_path, type_def->type_name);
}

static void v3_text_appendf(char *out, size_t cap, const char *fmt, ...);
static bool v3_abi_class_scalar_or_ptr(const char *abi_class);

static const char *v3_canonical_scalar_type_name(const char *type_text) {
    if (type_text == NULL) {
        return "";
    }
    if (strcmp(type_text, "int") == 0) {
        return "int32";
    }
    return type_text;
}

static bool v3_scalar_type_is_unsigned(const char *type_text) {
    const char *canonical = v3_canonical_scalar_type_name(type_text);
    return strcmp(canonical, "uint8") == 0 ||
           strcmp(canonical, "uint16") == 0 ||
           strcmp(canonical, "uint32") == 0 ||
           strcmp(canonical, "uint64") == 0;
}

static bool v3_text_case_equal(const char *lhs, const char *rhs) {
    size_t i = 0U;
    if (!lhs || !rhs) {
        return false;
    }
    while (lhs[i] != '\0' && rhs[i] != '\0') {
        if (tolower((unsigned char)lhs[i]) != tolower((unsigned char)rhs[i])) {
            return false;
        }
        i += 1U;
    }
    return lhs[i] == '\0' && rhs[i] == '\0';
}

static bool v3_emit_scalar_cast_reg(char *out,
                                    size_t cap,
                                    const char *src_type_text,
                                    const char *src_abi,
                                    const char *dst_type_text,
                                    const char *dst_abi,
                                    int reg_index) {
    const char *src_type = v3_canonical_scalar_type_name(src_type_text);
    const char *dst_type = v3_canonical_scalar_type_name(dst_type_text);
    if (!v3_abi_class_scalar_or_ptr(src_abi) || !v3_abi_class_scalar_or_ptr(dst_abi)) {
        return false;
    }
    if (strcmp(dst_abi, "bool") == 0) {
        if (v3_active_target_is_linux_x86_64()) {
            v3_emit_x64_cmp_zero_set_bool(out,
                                          cap,
                                          reg_index,
                                          reg_index,
                                          strcmp(src_abi, "i64") == 0 || strcmp(src_abi, "ptr") == 0,
                                          "ne");
        } else {
            v3_text_appendf(out, cap, "  cmp %c%d, #0\n  cset w%d, ne\n",
                            strcmp(src_abi, "i64") == 0 || strcmp(src_abi, "ptr") == 0 ? 'x' : 'w',
                            reg_index,
                            reg_index);
        }
        return true;
    }
    if (strcmp(src_abi, dst_abi) == 0 &&
        strcmp(src_type, dst_type) == 0) {
        return true;
    }
    if (v3_active_target_is_linux_x86_64()) {
        if (strcmp(dst_abi, "i64") == 0 || strcmp(dst_abi, "ptr") == 0) {
            if (strcmp(src_abi, "bool") == 0 || strcmp(src_abi, "u8") == 0) {
                v3_text_appendf(out, cap, "  movzbl %s, %s\n",
                                v3_x64_breg_name(reg_index),
                                v3_x64_dreg_name(reg_index));
                return true;
            }
            if (strcmp(src_abi, "i8") == 0) {
                v3_text_appendf(out, cap, "  movsbq %s, %s\n",
                                v3_x64_breg_name(reg_index),
                                v3_x64_qreg_name(reg_index));
                return true;
            }
            if (strcmp(src_abi, "u16") == 0) {
                v3_text_appendf(out, cap, "  movzwl %s, %s\n",
                                v3_x64_hreg_name(reg_index),
                                v3_x64_dreg_name(reg_index));
                return true;
            }
            if (strcmp(src_abi, "i16") == 0) {
                v3_text_appendf(out, cap, "  movswq %s, %s\n",
                                v3_x64_hreg_name(reg_index),
                                v3_x64_qreg_name(reg_index));
                return true;
            }
            if (strcmp(src_abi, "i32") == 0) {
                v3_text_appendf(out, cap, "  movslq %s, %s\n",
                                v3_x64_dreg_name(reg_index),
                                v3_x64_qreg_name(reg_index));
                return true;
            }
            if (strcmp(src_abi, "i64") == 0 || strcmp(src_abi, "ptr") == 0) {
                return true;
            }
            return false;
        }
        if (strcmp(dst_abi, "i32") == 0) {
            if (strcmp(src_abi, "i64") == 0 || strcmp(src_abi, "ptr") == 0) {
                v3_text_appendf(out, cap, "  movl %s, %s\n",
                                v3_x64_dreg_name(reg_index),
                                v3_x64_dreg_name(reg_index));
            }
            return true;
        }
        if (strcmp(dst_abi, "i16") == 0) {
            v3_text_appendf(out,
                            cap,
                            v3_scalar_type_is_unsigned(dst_type) ? "  movzwl %s, %s\n" : "  movswl %s, %s\n",
                            v3_x64_hreg_name(reg_index),
                            v3_x64_dreg_name(reg_index));
            return true;
        }
        if (strcmp(dst_abi, "u16") == 0) {
            v3_text_appendf(out, cap, "  movzwl %s, %s\n",
                            v3_x64_hreg_name(reg_index),
                            v3_x64_dreg_name(reg_index));
            return true;
        }
        if (strcmp(dst_abi, "i8") == 0) {
            v3_text_appendf(out,
                            cap,
                            v3_scalar_type_is_unsigned(dst_type) ? "  movzbl %s, %s\n" : "  movsbl %s, %s\n",
                            v3_x64_breg_name(reg_index),
                            v3_x64_dreg_name(reg_index));
            return true;
        }
        if (strcmp(dst_abi, "u8") == 0) {
            v3_text_appendf(out, cap, "  movzbl %s, %s\n",
                            v3_x64_breg_name(reg_index),
                            v3_x64_dreg_name(reg_index));
            return true;
        }
        return false;
    }
    if (strcmp(dst_abi, "i64") == 0 || strcmp(dst_abi, "ptr") == 0) {
        if (strcmp(src_abi, "bool") == 0 ||
            strcmp(src_abi, "i8") == 0 ||
            strcmp(src_abi, "u8") == 0 ||
            strcmp(src_abi, "i16") == 0 ||
            strcmp(src_abi, "u16") == 0 ||
            strcmp(src_abi, "i32") == 0) {
            if (v3_scalar_type_is_unsigned(src_type) ||
                strcmp(src_abi, "bool") == 0 ||
                strcmp(src_abi, "u8") == 0 ||
                strcmp(src_abi, "u16") == 0) {
                v3_text_appendf(out, cap, "  uxtw x%d, w%d\n", reg_index, reg_index);
            } else {
                v3_text_appendf(out, cap, "  sxtw x%d, w%d\n", reg_index, reg_index);
            }
            return true;
        }
        if (strcmp(src_abi, "i64") == 0 || strcmp(src_abi, "ptr") == 0) {
            return true;
        }
        return false;
    }
    if (strcmp(dst_abi, "i32") == 0) {
        if (strcmp(src_abi, "i64") == 0 || strcmp(src_abi, "ptr") == 0) {
            v3_text_appendf(out, cap, "  mov w%d, w%d\n", reg_index, reg_index);
            return true;
        }
        if (strcmp(src_abi, "bool") == 0 ||
            strcmp(src_abi, "i8") == 0 ||
            strcmp(src_abi, "u8") == 0 ||
            strcmp(src_abi, "i16") == 0 ||
            strcmp(src_abi, "u16") == 0 ||
            strcmp(src_abi, "i32") == 0) {
            return true;
        }
        return false;
    }
    if (strcmp(dst_abi, "i16") == 0) {
        v3_text_appendf(out,
                        cap,
                        v3_scalar_type_is_unsigned(dst_type) ? "  uxth w%d, w%d\n" : "  sxth w%d, w%d\n",
                        reg_index,
                        reg_index);
        return true;
    }
    if (strcmp(dst_abi, "u16") == 0) {
        v3_text_appendf(out, cap, "  uxth w%d, w%d\n", reg_index, reg_index);
        return true;
    }
    if (strcmp(dst_abi, "i8") == 0) {
        v3_text_appendf(out,
                        cap,
                        v3_scalar_type_is_unsigned(dst_type) ? "  uxtb w%d, w%d\n" : "  sxtb w%d, w%d\n",
                        reg_index,
                        reg_index);
        return true;
    }
    if (strcmp(dst_abi, "u8") == 0) {
        v3_text_appendf(out, cap, "  uxtb w%d, w%d\n", reg_index, reg_index);
        return true;
    }
    return false;
}

static bool v3_strip_var_type_text(const char *type_text,
                                   char *out,
                                   size_t cap) {
    char trimmed[256];
    if (!type_text || !out || cap == 0U) {
        return false;
    }
    v3_trim_copy_text(type_text, trimmed, sizeof(trimmed));
    if (!v3_startswith(trimmed, "var ")) {
        snprintf(out, cap, "%s", trimmed);
        return false;
    }
    v3_trim_copy_text(trimmed + 4, out, cap);
    return out[0] != '\0';
}

static bool v3_parse_top_level_const_binding(const char *statement,
                                             char *name_out,
                                             size_t name_cap,
                                             char *type_out,
                                             size_t type_cap,
                                             char *expr_out,
                                             size_t expr_cap) {
    const char *eq = strchr(statement, '=');
    char left[256];
    char right[4096];
    char *colon;
    if (!eq) {
        return false;
    }
    snprintf(left, sizeof(left), "%.*s", (int)(eq - statement), statement);
    snprintf(right, sizeof(right), "%s", eq + 1);
    v3_trim_copy_text(left, left, sizeof(left));
    v3_trim_copy_text(right, expr_out, expr_cap);
    if (left[0] == '\0' || expr_out[0] == '\0') {
        return false;
    }
    colon = strchr(left, ':');
    if (colon) {
        *colon = '\0';
        snprintf(type_out, type_cap, "%s", colon + 1);
        v3_trim_copy_text(type_out, type_out, type_cap);
    } else {
        type_out[0] = '\0';
    }
    v3_trim_copy_text(left, name_out, name_cap);
    return name_out[0] != '\0';
}

static int32_t v3_find_top_level_const_index_by_symbol(const V3LoweringPlanStub *lowering,
                                                       const char *symbol_text) {
    size_t i;
    for (i = 0; i < lowering->const_count; ++i) {
        if (strcmp(lowering->consts[i].symbol_text, symbol_text) == 0) {
            return (int32_t)i;
        }
    }
    return -1;
}

static bool v3_parse_string_literal_text(const char *expr_text,
                                         char *literal_out,
                                         size_t literal_cap);
static bool v3_parse_int_literal_text(const char *expr_text, int64_t *value_out);
static int32_t v3_find_top_level_binary_op_last(const char *text, const char *op);
static bool v3_parse_top_level_var_binding_text(const char *statement,
                                                char *name_out,
                                                size_t name_cap,
                                                char *type_out,
                                                size_t type_cap,
                                                bool *has_init_out,
                                                char *expr_out,
                                                size_t expr_cap);

static bool v3_try_resolve_i64_top_level_const_expr(const char *expr_text,
                                                    const V3LoweringPlanStub *lowering,
                                                    const char *owner_module_path,
                                                    const V3ImportAlias *aliases,
                                                    size_t alias_count,
                                                    int64_t *value_out) {
    char expr[4096];
    int64_t literal = 0;
    int32_t shl_index = -1;
    int32_t plus_index = -1;
    int32_t minus_index = -1;
    int64_t lhs = 0;
    int64_t rhs = 0;
    char left[4096];
    char right[4096];
    char symbol_text[PATH_MAX];
    int32_t index = -1;
    const V3TopLevelConstStub *existing = NULL;
    v3_trim_copy_text(expr_text, expr, sizeof(expr));
    if (expr[0] == '\0') {
        return false;
    }
    if (v3_parse_int_literal_text(expr, &literal)) {
        *value_out = literal;
        return true;
    }
    shl_index = v3_find_top_level_binary_op_last(expr, "<<");
    if (shl_index > 0) {
        snprintf(left, sizeof(left), "%.*s", shl_index, expr);
        snprintf(right, sizeof(right), "%s", expr + shl_index + 2);
        v3_trim_copy_text(left, left, sizeof(left));
        v3_trim_copy_text(right, right, sizeof(right));
        return v3_try_resolve_i64_top_level_const_expr(left,
                                                       lowering,
                                                       owner_module_path,
                                                       aliases,
                                                       alias_count,
                                                       &lhs) &&
               v3_try_resolve_i64_top_level_const_expr(right,
                                                       lowering,
                                                       owner_module_path,
                                                       aliases,
                                                       alias_count,
                                                       &rhs) &&
               rhs >= 0 &&
               rhs < 63 &&
               ((*value_out = (int64_t)((uint64_t)lhs << (uint32_t)rhs)), true);
    }
    plus_index = v3_find_top_level_binary_op_last(expr, "+");
    if (plus_index > 0) {
        snprintf(left, sizeof(left), "%.*s", plus_index, expr);
        snprintf(right, sizeof(right), "%s", expr + plus_index + 1);
        v3_trim_copy_text(left, left, sizeof(left));
        v3_trim_copy_text(right, right, sizeof(right));
        return v3_try_resolve_i64_top_level_const_expr(left,
                                                       lowering,
                                                       owner_module_path,
                                                       aliases,
                                                       alias_count,
                                                       &lhs) &&
               v3_try_resolve_i64_top_level_const_expr(right,
                                                       lowering,
                                                       owner_module_path,
                                                       aliases,
                                                       alias_count,
                                                       &rhs) &&
               ((*value_out = lhs + rhs), true);
    }
    minus_index = v3_find_top_level_binary_op_last(expr, "-");
    if (minus_index > 0) {
        snprintf(left, sizeof(left), "%.*s", minus_index, expr);
        snprintf(right, sizeof(right), "%s", expr + minus_index + 1);
        v3_trim_copy_text(left, left, sizeof(left));
        v3_trim_copy_text(right, right, sizeof(right));
        return v3_try_resolve_i64_top_level_const_expr(left,
                                                       lowering,
                                                       owner_module_path,
                                                       aliases,
                                                       alias_count,
                                                       &lhs) &&
               v3_try_resolve_i64_top_level_const_expr(right,
                                                       lowering,
                                                       owner_module_path,
                                                       aliases,
                                                       alias_count,
                                                       &rhs) &&
               ((*value_out = lhs - rhs), true);
    }
    if (strchr(expr, '.') != NULL) {
        char alias_text[128];
        char const_name[128];
        char expr_copy[4096];
        char *dot;
        const char *module_path;
        snprintf(expr_copy, sizeof(expr_copy), "%s", expr);
        dot = strchr(expr_copy, '.');
        *dot = '\0';
        snprintf(alias_text, sizeof(alias_text), "%s", expr_copy);
        snprintf(const_name, sizeof(const_name), "%s", dot + 1);
        module_path = v3_alias_module_path_exported_member(aliases,
                                                           alias_count,
                                                           owner_module_path,
                                                           alias_text,
                                                           const_name,
                                                           sizeof(const_name));
        if (!module_path) {
            return false;
        }
        snprintf(symbol_text, sizeof(symbol_text), "%s::%s", module_path, const_name);
    } else {
        snprintf(symbol_text, sizeof(symbol_text), "%s::%s", owner_module_path, expr);
    }
    index = v3_find_top_level_const_index_by_symbol(lowering, symbol_text);
    if (index < 0) {
        return false;
    }
    existing = &lowering->consts[index];
    if (strcmp(existing->abi_class, "i32") != 0 &&
        strcmp(existing->abi_class, "i64") != 0 &&
        strcmp(existing->abi_class, "bool") != 0) {
        return false;
    }
    *value_out = existing->i64_value;
    return true;
}

static bool v3_try_parse_scalar_const_literal(const char *expr_text,
                                              const char *declared_type,
                                              const V3LoweringPlanStub *lowering,
                                              const char *owner_module_path,
                                              const V3ImportAlias *aliases,
                                              size_t alias_count,
                                              V3TopLevelConstStub *out) {
    char expr[4096];
    char literal[8192];
    char *end = NULL;
    const char *declared_abi = v3_type_abi_class(declared_type);
    int64_t resolved_i64 = 0;
    v3_trim_copy_text(expr_text, expr, sizeof(expr));
    if (expr[0] == '\0') {
        return false;
    }
    memset(out, 0, sizeof(*out));
    if (strcmp(expr, "true") == 0 || strcmp(expr, "false") == 0) {
        snprintf(out->abi_class, sizeof(out->abi_class), "%s", "bool");
        out->bool_value = strcmp(expr, "true") == 0;
        out->i64_value = out->bool_value ? 1 : 0;
        return true;
    }
    if ((expr[0] >= '0' && expr[0] <= '9') ||
        (expr[0] == '-' && ((expr[1] >= '0' && expr[1] <= '9') || expr[1] == '0'))) {
        long long value = strtoll(expr, &end, 0);
        if (end && *v3_trim_inplace(end) == '\0') {
            snprintf(out->abi_class,
                     sizeof(out->abi_class),
                     "%s",
                     strcmp(declared_abi, "i64") == 0 ? "i64" : "i32");
            out->i64_value = (int64_t)value;
            return true;
        }
    }
    if ((declared_type[0] == '\0' ||
         strcmp(declared_abi, "i32") == 0 ||
         strcmp(declared_abi, "i64") == 0 ||
         strcmp(declared_abi, "bool") == 0) &&
        v3_try_resolve_i64_top_level_const_expr(expr,
                                                lowering,
                                                owner_module_path,
                                                aliases,
                                                alias_count,
                                                &resolved_i64)) {
        snprintf(out->abi_class,
                 sizeof(out->abi_class),
                 "%s",
                 strcmp(declared_abi, "i64") == 0 ? "i64" :
                 (strcmp(declared_abi, "bool") == 0 ? "bool" : "i32"));
        out->i64_value = resolved_i64;
        out->bool_value = resolved_i64 != 0;
        return true;
    }
    if (v3_parse_string_literal_text(expr, literal, sizeof(literal))) {
        if (declared_type[0] != '\0' &&
            strcmp(declared_type, "str") != 0 &&
            strcmp(declared_type, "cstring") != 0) {
            return false;
        }
        snprintf(out->abi_class, sizeof(out->abi_class), "%s", "composite");
        snprintf(out->str_value, sizeof(out->str_value), "%s", literal);
        out->has_str_value = true;
        return true;
    }
    if (strchr(expr, '(') == NULL && strchr(expr, '[') == NULL && strchr(expr, ' ') == NULL) {
        char symbol_text[PATH_MAX];
        const V3TopLevelConstStub *existing = NULL;
        if (strchr(expr, '.') != NULL) {
            char alias_text[128];
            char const_name[128];
            char expr_copy[4096];
            char *dot;
            const char *module_path;
            snprintf(expr_copy, sizeof(expr_copy), "%s", expr);
            dot = strchr(expr_copy, '.');
            *dot = '\0';
            snprintf(alias_text, sizeof(alias_text), "%s", expr_copy);
            snprintf(const_name, sizeof(const_name), "%s", dot + 1);
            module_path = v3_alias_module_path_exported_member(aliases,
                                                               alias_count,
                                                               owner_module_path,
                                                               alias_text,
                                                               const_name,
                                                               sizeof(const_name));
            if (!module_path) {
                return false;
            }
            snprintf(symbol_text, sizeof(symbol_text), "%s::%s", module_path, const_name);
        } else {
            snprintf(symbol_text, sizeof(symbol_text), "%s::%s", owner_module_path, expr);
        }
        {
            int32_t index = v3_find_top_level_const_index_by_symbol(lowering, symbol_text);
            if (index < 0) {
                return false;
            }
            existing = &lowering->consts[index];
        }
        snprintf(out->abi_class, sizeof(out->abi_class), "%s", existing->abi_class);
        snprintf(out->str_value, sizeof(out->str_value), "%s", existing->str_value);
        out->has_str_value = existing->has_str_value;
        out->i64_value = existing->i64_value;
        out->bool_value = existing->bool_value;
        if (strcmp(declared_abi, "i64") == 0 && strcmp(out->abi_class, "i32") == 0) {
            snprintf(out->abi_class, sizeof(out->abi_class), "%s", "i64");
        }
        if (declared_type[0] != '\0' &&
            strcmp(out->abi_class, "composite") == 0 &&
            strcmp(declared_type, "str") != 0 &&
            strcmp(declared_type, "cstring") != 0) {
            return false;
        }
        return out->abi_class[0] != '\0';
    }
    return false;
}

static bool v3_collect_top_level_consts_from_source(const V3SystemLinkPlanStub *plan,
                                                    const char *source_path,
                                                    V3LoweringPlanStub *lowering) {
    char owner_module_path[PATH_MAX];
    V3ImportAlias aliases[CHENG_V3_MAX_IMPORT_ALIASES];
    size_t alias_count = 0U;
    char *owned;
    char *cursor;
    char *line;
    char *lines[CHENG_V3_MAX_SOURCE_LINES];
    size_t line_count = 0U;
    size_t i;
    bool in_const_block = false;
    v3_source_path_to_module_path(plan->workspace_root,
                                  plan->package_root,
                                  plan->package_id,
                                  source_path,
                                  owner_module_path,
                                  sizeof(owner_module_path));
    owned = v3_read_file(source_path);
    if (!owned) {
        fprintf(stderr, "[cheng_v3_seed] type scan read failed: %s\n", source_path);
        return false;
    }
    cursor = owned;
    while ((line = strsep(&cursor, "\n")) != NULL) {
        if (line_count >= CHENG_V3_MAX_SOURCE_LINES) {
            fprintf(stderr, "[cheng_v3_seed] type scan line overflow: %s\n", source_path);
            free(owned);
            return false;
        }
        lines[line_count++] = line;
    }
    v3_parse_import_aliases_from_lines(lines,
                                       line_count,
                                       aliases,
                                       &alias_count,
                                       CHENG_V3_MAX_IMPORT_ALIASES);
    for (i = 0; i < line_count; ++i) {
        char line_copy[4096];
        char name[128];
        char type_text[CHENG_V3_MAX_TYPE_TEXT];
        char expr_text[4096];
        V3TopLevelConstStub value;
        int32_t indent;
        char *trimmed;
        snprintf(line_copy, sizeof(line_copy), "%s", lines[i]);
        indent = v3_line_indent(line_copy);
        trimmed = v3_trim_inplace(line_copy);
        if (*trimmed == '\0') {
            continue;
        }
        if (indent <= 0 &&
            v3_startswith(trimmed, "const ") &&
            v3_parse_top_level_const_binding(trimmed + 6,
                                             name,
                                             sizeof(name),
                                             type_text,
                                             sizeof(type_text),
                                             expr_text,
                                             sizeof(expr_text))) {
            if (!v3_try_parse_scalar_const_literal(expr_text,
                                                   type_text,
                                                   lowering,
                                                   owner_module_path,
                                                   aliases,
                                                   alias_count,
                                                   &value)) {
                continue;
            }
            if (lowering->const_count >= CHENG_V3_MAX_TOP_LEVEL_CONSTS) {
                free(owned);
                return false;
            }
            snprintf(lowering->consts[lowering->const_count].owner_module_path,
                     sizeof(lowering->consts[lowering->const_count].owner_module_path),
                     "%s",
                     owner_module_path);
            snprintf(lowering->consts[lowering->const_count].const_name,
                     sizeof(lowering->consts[lowering->const_count].const_name),
                     "%s",
                     name);
            snprintf(lowering->consts[lowering->const_count].symbol_text,
                     sizeof(lowering->consts[lowering->const_count].symbol_text),
                     "%s::%s",
                     owner_module_path,
                     name);
            snprintf(lowering->consts[lowering->const_count].type_text,
                     sizeof(lowering->consts[lowering->const_count].type_text),
                     "%s",
                     type_text[0] != '\0' ? type_text : "int32");
            snprintf(lowering->consts[lowering->const_count].abi_class,
                     sizeof(lowering->consts[lowering->const_count].abi_class),
                     "%s",
                     value.abi_class);
            snprintf(lowering->consts[lowering->const_count].str_value,
                     sizeof(lowering->consts[lowering->const_count].str_value),
                     "%s",
                     value.str_value);
            lowering->consts[lowering->const_count].has_str_value = value.has_str_value;
            lowering->consts[lowering->const_count].i64_value = value.i64_value;
            lowering->consts[lowering->const_count].bool_value = value.bool_value;
            lowering->const_count += 1U;
            continue;
        }
        if (indent <= 0 && strcmp(trimmed, "const") == 0) {
            in_const_block = true;
            continue;
        }
        if (!in_const_block) {
            continue;
        }
        if (indent <= 0) {
            in_const_block = false;
            continue;
        }
        if (!v3_parse_top_level_const_binding(trimmed,
                                              name,
                                              sizeof(name),
                                              type_text,
                                              sizeof(type_text),
                                              expr_text,
                                              sizeof(expr_text))) {
            continue;
        }
        if (!v3_try_parse_scalar_const_literal(expr_text,
                                               type_text,
                                               lowering,
                                               owner_module_path,
                                               aliases,
                                               alias_count,
                                               &value)) {
            continue;
        }
        if (lowering->const_count >= CHENG_V3_MAX_TOP_LEVEL_CONSTS) {
            free(owned);
            return false;
        }
        snprintf(lowering->consts[lowering->const_count].symbol_text,
                 sizeof(lowering->consts[lowering->const_count].symbol_text),
                 "%s::%s",
                 owner_module_path,
                 name);
        snprintf(lowering->consts[lowering->const_count].owner_module_path,
                 sizeof(lowering->consts[lowering->const_count].owner_module_path),
                 "%s",
                 owner_module_path);
        snprintf(lowering->consts[lowering->const_count].const_name,
                 sizeof(lowering->consts[lowering->const_count].const_name),
                 "%s",
                 name);
        snprintf(lowering->consts[lowering->const_count].type_text,
                 sizeof(lowering->consts[lowering->const_count].type_text),
                 "%s",
                 type_text[0] != '\0' ? type_text :
                 (strcmp(value.abi_class, "composite") == 0 && value.has_str_value ? "str" :
                 (strcmp(value.abi_class, "i64") == 0 ? "int64" :
                  (strcmp(value.abi_class, "bool") == 0 ? "bool" : "int32"))));
        snprintf(lowering->consts[lowering->const_count].abi_class,
                 sizeof(lowering->consts[lowering->const_count].abi_class),
                 "%s",
                 value.abi_class);
        snprintf(lowering->consts[lowering->const_count].str_value,
                 sizeof(lowering->consts[lowering->const_count].str_value),
                 "%s",
                 value.str_value);
        lowering->consts[lowering->const_count].has_str_value = value.has_str_value;
        lowering->consts[lowering->const_count].i64_value = value.i64_value;
        lowering->consts[lowering->const_count].bool_value = value.bool_value;
        lowering->const_count += 1U;
    }
    free(owned);
    return true;
}

static const V3TopLevelConstStub *v3_find_expr_top_level_const(const V3LoweringPlanStub *lowering,
                                                               const char *owner_module_path,
                                                               const V3ImportAlias *aliases,
                                                               size_t alias_count,
                                                               const char *expr_text) {
    char expr[4096];
    char symbol_text[PATH_MAX];
    int32_t index;
    v3_trim_copy_text(expr_text, expr, sizeof(expr));
    if (expr[0] == '\0' || strchr(expr, '(') != NULL || strchr(expr, '[') != NULL) {
        return NULL;
    }
    if (strchr(expr, '.') != NULL) {
        char alias_text[128];
        char const_name[128];
        char expr_copy[4096];
        char *dot;
        const char *module_path;
        snprintf(expr_copy, sizeof(expr_copy), "%s", expr);
        dot = strchr(expr_copy, '.');
        *dot = '\0';
        snprintf(alias_text, sizeof(alias_text), "%s", expr_copy);
        snprintf(const_name, sizeof(const_name), "%s", dot + 1);
        module_path = v3_alias_module_path_exported_member(aliases,
                                                           alias_count,
                                                           owner_module_path,
                                                           alias_text,
                                                           const_name,
                                                           sizeof(const_name));
        if (!module_path) {
            return NULL;
        }
        snprintf(symbol_text, sizeof(symbol_text), "%s::%s", module_path, const_name);
        index = v3_find_top_level_const_index_by_symbol(lowering, symbol_text);
        return index >= 0 ? &lowering->consts[index] : NULL;
    }
    snprintf(symbol_text, sizeof(symbol_text), "%s::%s", owner_module_path, expr);
    index = v3_find_top_level_const_index_by_symbol(lowering, symbol_text);
    if (index >= 0) {
        return &lowering->consts[index];
    }
    {
        const V3TopLevelConstStub *match = NULL;
        size_t i;
        for (i = 0; i < alias_count; ++i) {
            snprintf(symbol_text, sizeof(symbol_text), "%s::%s", aliases[i].module_path, expr);
            index = v3_find_top_level_const_index_by_symbol(lowering, symbol_text);
            if (index < 0) {
                continue;
            }
            if (match != NULL) {
                return NULL;
            }
            match = &lowering->consts[index];
        }
        if (match != NULL) {
            return match;
        }
    }
    {
        const V3TopLevelConstStub *match = NULL;
        size_t i;
        for (i = 0; i < lowering->const_count; ++i) {
            if (strcmp(lowering->consts[i].const_name, expr) != 0) {
                continue;
            }
            if (match != NULL) {
                return NULL;
            }
            match = &lowering->consts[i];
        }
        return match;
    }
}

static int32_t v3_align_up_i32(int32_t value, int32_t align) {
    if (align <= 1) {
        return value;
    }
    return (value + align - 1) / align * align;
}

static void v3_strip_var_prefix(const char *type_text, char *out, size_t cap) {
    char scratch[256];
    v3_trim_copy_text(type_text, scratch, sizeof(scratch));
    if (v3_startswith(scratch, "var ")) {
        v3_trim_copy_text(scratch + 4, out, cap);
        return;
    }
    v3_copy_text(out, cap, scratch);
}

static bool v3_eval_type_int32_expr(const V3LoweringPlanStub *lowering,
                                    const char *owner_module_path,
                                    const V3ImportAlias *aliases,
                                    size_t alias_count,
                                    const char *expr_text,
                                    int32_t *value_out) {
    char expr[256];
    char *end = NULL;
    long long parsed;
    const V3TopLevelConstStub *top_level_const;
    v3_trim_copy_text(expr_text, expr, sizeof(expr));
    if (expr[0] == '\0') {
        return false;
    }
    parsed = strtoll(expr, &end, 0);
    if (end && *v3_trim_inplace(end) == '\0') {
        *value_out = (int32_t)parsed;
        return true;
    }
    top_level_const = v3_find_expr_top_level_const(lowering,
                                                   owner_module_path,
                                                   aliases,
                                                   alias_count,
                                                   expr);
    if (!top_level_const) {
        return false;
    }
    *value_out = (int32_t)top_level_const->i64_value;
    return true;
}

static bool v3_load_import_aliases_from_source_path(const char *source_path,
                                                    V3ImportAlias *aliases,
                                                    size_t *alias_count,
                                                    size_t alias_cap);
static bool v3_is_bare_identifier_text(const char *text);
static int32_t v3_find_top_level_assignment_eq(const char *text);
static bool v3_normalize_type_text_with_params(const V3LoweringPlanStub *lowering,
                                               const char *owner_module_path,
                                               const V3ImportAlias *aliases,
                                               size_t alias_count,
                                               const char generic_param_names[][128],
                                               const char generic_param_types[][256],
                                               size_t generic_param_count,
                                               const char *type_text,
                                               char *out,
                                               size_t cap);

static bool v3_split_trailing_type_arg_items(const char *text,
                                             char *base_out,
                                             size_t base_cap,
                                             char items[][4096],
                                             size_t *item_count,
                                             size_t item_cap) {
    char trimmed[256];
    int32_t depth = 0;
    int32_t open_index = -1;
    size_t i;
    size_t len;
    char inner[256];
    v3_trim_copy_text(text, trimmed, sizeof(trimmed));
    if (base_out && base_cap > 0U) {
        base_out[0] = '\0';
    }
    if (item_count) {
        *item_count = 0U;
    }
    if (trimmed[0] == '\0') {
        return false;
    }
    len = strlen(trimmed);
    for (i = 0; i < len; ++i) {
        if (trimmed[i] == '[') {
            if (depth == 0) {
                open_index = (int32_t)i;
            }
            depth += 1;
            continue;
        }
        if (trimmed[i] == ']') {
            if (depth == 0) {
                return false;
            }
            depth -= 1;
            continue;
        }
    }
    if (depth != 0) {
        return false;
    }
    if (open_index <= 0 || trimmed[len - 1U] != ']') {
        v3_copy_text(base_out, base_cap, trimmed);
        return true;
    }
    snprintf(base_out, base_cap, "%.*s", open_index, trimmed);
    v3_trim_copy_text(base_out, base_out, base_cap);
    if (base_out[0] == '\0') {
        return false;
    }
    snprintf(inner, sizeof(inner), "%.*s", (int)(len - (size_t)open_index - 2U), trimmed + open_index + 1);
    v3_trim_copy_text(inner, inner, sizeof(inner));
    if (inner[0] == '\0') {
        return false;
    }
    return v3_split_top_level_args(inner, items, item_count, item_cap);
}

static bool v3_is_tuple_type_text(const char *type_text) {
    char trimmed[CHENG_V3_MAX_TYPE_TEXT];
    size_t len;
    v3_trim_copy_text(type_text, trimmed, sizeof(trimmed));
    len = strlen(trimmed);
    return len >= 7U &&
           v3_startswith(trimmed, "tuple[") &&
           trimmed[len - 1U] == ']';
}

static bool v3_is_seq_type_text(const char *type_text) {
    char trimmed[CHENG_V3_MAX_TYPE_TEXT];
    size_t len;
    v3_trim_copy_text(type_text, trimmed, sizeof(trimmed));
    len = strlen(trimmed);
    return len > 2U &&
           strcmp(trimmed + len - 2U, "[]") == 0;
}

static bool v3_parse_tuple_type_items(const char *type_text,
                                      char items[][4096],
                                      size_t *item_count,
                                      size_t item_cap) {
    char trimmed[CHENG_V3_MAX_TYPE_TEXT];
    char inner[CHENG_V3_MAX_TYPE_TEXT];
    size_t len;
    v3_trim_copy_text(type_text, trimmed, sizeof(trimmed));
    len = strlen(trimmed);
    if (len < 7U ||
        !v3_startswith(trimmed, "tuple[") ||
        trimmed[len - 1U] != ']') {
        return false;
    }
    snprintf(inner, sizeof(inner), "%.*s", (int)(len - 7U), trimmed + 6);
    return v3_split_top_level_args(inner, items, item_count, item_cap);
}

static void v3_tuple_field_synthetic_name(size_t index, char *out, size_t cap) {
    snprintf(out, cap, "__tuple_%zu", index);
}

static bool v3_parse_type_field_spec(const char *field_text,
                                     bool allow_implicit_name,
                                     V3TypeFieldDef *field_out) {
    char trimmed[CHENG_V3_MAX_DEFAULT_EXPR];
    char type_part[CHENG_V3_MAX_TYPE_TEXT];
    int32_t eq_index;
    int32_t colon_index;
    char *field_name = NULL;
    char *field_type = NULL;
    if (!field_text || !field_out) {
        return false;
    }
    memset(field_out, 0, sizeof(*field_out));
    v3_trim_copy_text(field_text, trimmed, sizeof(trimmed));
    v3_strip_trailing_comment_inplace(trimmed);
    v3_trim_copy_text(trimmed, trimmed, sizeof(trimmed));
    if (trimmed[0] == '\0') {
        return false;
    }
    eq_index = v3_find_top_level_assignment_eq(trimmed);
    if (eq_index >= 0) {
        snprintf(type_part, sizeof(type_part), "%.*s", eq_index, trimmed);
        v3_trim_copy_text(type_part, type_part, sizeof(type_part));
        snprintf(field_out->default_expr,
                 sizeof(field_out->default_expr),
                 "%s",
                 trimmed + eq_index + 1);
        v3_trim_copy_text(field_out->default_expr,
                          field_out->default_expr,
                          sizeof(field_out->default_expr));
        if (field_out->default_expr[0] == '\0') {
            return false;
        }
        field_out->has_default_expr = true;
    } else {
        snprintf(type_part, sizeof(type_part), "%s", trimmed);
        v3_trim_copy_text(type_part, type_part, sizeof(type_part));
    }
    colon_index = v3_find_top_level_char(type_part, ':');
    if (colon_index > 0) {
        type_part[colon_index] = '\0';
        field_name = v3_trim_inplace(type_part);
        field_type = v3_trim_inplace(type_part + colon_index + 1);
        if (field_name[0] != '\0' &&
            field_type[0] != '\0' &&
            v3_is_bare_identifier_text(field_name)) {
            snprintf(field_out->name, sizeof(field_out->name), "%s", field_name);
            snprintf(field_out->type_text, sizeof(field_out->type_text), "%s", field_type);
            field_out->has_explicit_name = true;
            return true;
        }
        if (!allow_implicit_name) {
            return false;
        }
    } else if (!allow_implicit_name) {
        return false;
    }
    if (!allow_implicit_name) {
        return false;
    }
    v3_trim_copy_text(field_text, trimmed, sizeof(trimmed));
    v3_strip_trailing_comment_inplace(trimmed);
    v3_trim_copy_text(trimmed, trimmed, sizeof(trimmed));
    eq_index = v3_find_top_level_assignment_eq(trimmed);
    if (eq_index >= 0) {
        snprintf(field_out->type_text, sizeof(field_out->type_text), "%.*s", eq_index, trimmed);
    } else {
        snprintf(field_out->type_text, sizeof(field_out->type_text), "%s", trimmed);
    }
    v3_trim_copy_text(field_out->type_text, field_out->type_text, sizeof(field_out->type_text));
    return field_out->type_text[0] != '\0';
}

static bool v3_validate_type_field_defaults(const char *source_path,
                                            const char *type_text,
                                            const V3TypeFieldDef *fields,
                                            size_t field_count);
static bool v3_parse_binding_meta(const char *statement,
                                  const char *prefix,
                                  char *name_out,
                                  size_t name_cap,
                                  char *type_out,
                                  size_t type_cap,
                                  char *expr_out,
                                  size_t expr_cap);
static bool v3_find_duplicate_named_arg_field(const char args[][4096],
                                              size_t arg_count,
                                              char *field_out,
                                              size_t field_cap,
                                              bool *valid_out);

static bool v3_parse_type_head_text(const char *text,
                                    char *base_name_out,
                                    size_t base_name_cap,
                                    char generic_param_names[][128],
                                    size_t *generic_param_count_out,
                                    size_t generic_param_cap) {
    char items[CHENG_V3_MAX_TYPE_PARAMS][4096];
    size_t item_count = 0U;
    size_t i;
    if (!v3_split_trailing_type_arg_items(text,
                                          base_name_out,
                                          base_name_cap,
                                          items,
                                          &item_count,
                                          CHENG_V3_MAX_TYPE_PARAMS)) {
        return false;
    }
    if (item_count > generic_param_cap) {
        return false;
    }
    if (generic_param_count_out) {
        *generic_param_count_out = item_count;
    }
    for (i = 0; i < item_count; ++i) {
        v3_trim_copy_text(items[i], generic_param_names[i], 128U);
        if (generic_param_names[i][0] == '\0') {
            return false;
        }
    }
    return base_name_out[0] != '\0';
}

static bool v3_parse_type_instance_text(const char *text,
                                        char *base_name_out,
                                        size_t base_name_cap,
                                        char type_args_out[][256],
                                        size_t *type_arg_count_out,
                                        size_t type_arg_cap) {
    char items[CHENG_V3_MAX_TYPE_PARAMS][4096];
    size_t item_count = 0U;
    size_t i;
    if (!v3_split_trailing_type_arg_items(text,
                                          base_name_out,
                                          base_name_cap,
                                          items,
                                          &item_count,
                                          CHENG_V3_MAX_TYPE_PARAMS)) {
        return false;
    }
    if (item_count > type_arg_cap) {
        return false;
    }
    if (type_arg_count_out) {
        *type_arg_count_out = item_count;
    }
    for (i = 0; i < item_count; ++i) {
        v3_trim_copy_text(items[i], type_args_out[i], 256U);
        if (type_args_out[i][0] == '\0') {
            return false;
        }
    }
    return base_name_out[0] != '\0';
}

static bool v3_split_qualified_type_text(const char *text,
                                         char *owner_out,
                                         size_t owner_cap,
                                         char *type_name_out,
                                         size_t type_name_cap) {
    char trimmed[PATH_MAX];
    int32_t depth = 0;
    int32_t sep_index = -1;
    size_t i;
    size_t len;
    v3_trim_copy_text(text, trimmed, sizeof(trimmed));
    len = strlen(trimmed);
    for (i = 0; i < len; ++i) {
        if (trimmed[i] == '[') {
            depth += 1;
            continue;
        }
        if (trimmed[i] == ']') {
            if (depth == 0) {
                return false;
            }
            depth -= 1;
            continue;
        }
        if (depth == 0 && i + 1U < len && trimmed[i] == ':' && trimmed[i + 1U] == ':') {
            sep_index = (int32_t)i;
            i += 1U;
        }
    }
    if (depth != 0 || sep_index <= 0) {
        return false;
    }
    snprintf(owner_out, owner_cap, "%.*s", sep_index, trimmed);
    snprintf(type_name_out, type_name_cap, "%s", trimmed + sep_index + 2);
    v3_trim_copy_text(owner_out, owner_out, owner_cap);
    v3_trim_copy_text(type_name_out, type_name_out, type_name_cap);
    return owner_out[0] != '\0' && type_name_out[0] != '\0';
}

static void v3_write_normalized_type_instance_text(const char *owner_module_path,
                                                   const char *type_name,
                                                   char type_args[][256],
                                                   size_t type_arg_count,
                                                   bool ref_object,
                                                   char *out,
                                                   size_t cap) {
    size_t i;
    snprintf(out, cap, "%s%s::%s", ref_object ? "ref@" : "", owner_module_path, type_name);
    for (i = 0; i < type_arg_count; ++i) {
        size_t used = strlen(out);
        if (used + 2U >= cap) {
            return;
        }
        snprintf(out + used, cap - used, "%s%s", i == 0U ? "[" : ", ", type_args[i]);
    }
    if (type_arg_count > 0U) {
        size_t used = strlen(out);
        if (used + 1U < cap) {
            snprintf(out + used, cap - used, "%s", "]");
        }
    }
}

static void v3_write_type_def_instance_normalized(const V3TypeDefStub *type_def,
                                                  char type_args[][256],
                                                  size_t type_arg_count,
                                                  char *out,
                                                  size_t cap) {
    if (cap == 0U) {
        return;
    }
    if (type_def == NULL) {
        out[0] = '\0';
        return;
    }
    v3_write_normalized_type_instance_text(type_def->owner_module_path,
                                           type_def->type_name,
                                           type_args,
                                           type_arg_count,
                                           type_def->ref_object,
                                           out,
                                           cap);
}

static const char *v3_find_generic_type_param(const char generic_param_names[][128],
                                              const char generic_param_types[][256],
                                              size_t generic_param_count,
                                              const char *type_name) {
    size_t i;
    for (i = 0; i < generic_param_count; ++i) {
        if (strcmp(generic_param_names[i], type_name) == 0) {
            return generic_param_types[i];
        }
    }
    return NULL;
}

static int32_t v3_find_type_def_index_by_symbol(const V3LoweringPlanStub *lowering,
                                                const char *owner_module_path,
                                                const char *type_name);
static bool v3_find_type_def_by_normalized(const V3LoweringPlanStub *lowering,
                                           const char *normalized_type,
                                           int32_t *index_out);
static bool v3_parse_fixed_array_type(const char *type_text,
                                      char *elem_type_out,
                                      size_t elem_cap,
                                      int32_t *len_out);
static bool v3_parse_fixed_array_type_meta(const char *type_text,
                                           char *elem_type_out,
                                           size_t elem_cap,
                                           char *len_expr_out,
                                           size_t len_expr_cap);
static bool v3_normalize_type_text(const V3LoweringPlanStub *lowering,
                                   const char *owner_module_path,
                                   const V3ImportAlias *aliases,
                                   size_t alias_count,
                                   const char *type_text,
                                   char *out,
                                   size_t cap) {
    char generic_param_names[CHENG_V3_MAX_TYPE_PARAMS][128];
    char generic_param_types[CHENG_V3_MAX_TYPE_PARAMS][256];
    memset(generic_param_names, 0, sizeof(generic_param_names));
    memset(generic_param_types, 0, sizeof(generic_param_types));
    return v3_normalize_type_text_with_params(lowering,
                                              owner_module_path,
                                              aliases,
                                              alias_count,
                                              generic_param_names,
                                              generic_param_types,
                                              0U,
                                              type_text,
                                              out,
                                              cap);
}

static bool v3_callee_is_constructor_like(const V3LoweringPlanStub *lowering,
                                          const char *owner_module_path,
                                          const V3ImportAlias *aliases,
                                          size_t alias_count,
                                          const char *callee) {
    char normalized[CHENG_V3_MAX_TYPE_TEXT];
    int32_t type_index = -1;
    if (callee == NULL || callee[0] == '\0') {
        return false;
    }
    if (v3_is_tuple_type_text(callee)) {
        return true;
    }
    if (!v3_normalize_type_text(lowering,
                                owner_module_path,
                                aliases,
                                alias_count,
                                callee,
                                normalized,
                                sizeof(normalized))) {
        return false;
    }
    if (strcmp(normalized, "Bytes") == 0) {
        return true;
    }
    return v3_find_type_def_by_normalized(lowering, normalized, &type_index);
}

static bool v3_normalize_type_text_with_params(const V3LoweringPlanStub *lowering,
                                               const char *owner_module_path,
                                               const V3ImportAlias *aliases,
                                               size_t alias_count,
                                               const char generic_param_names[][128],
                                               const char generic_param_types[][256],
                                               size_t generic_param_count,
                                               const char *type_text,
                                               char *out,
                                               size_t cap) {
    char trimmed[256];
    char inner[256];
    char len_expr[64];
    char normalized_inner[256];
    char base_name[128];
    char raw_type_args[CHENG_V3_MAX_TYPE_PARAMS][256];
    char normalized_type_args[CHENG_V3_MAX_TYPE_PARAMS][256];
    size_t raw_type_arg_count = 0U;
    int32_t fixed_len = 0;
    int32_t pointer_depth = 0;
    size_t i;
    int32_t type_index = -1;
    const char *generic_type_arg;
    v3_strip_var_prefix(type_text, trimmed, sizeof(trimmed));
    if (trimmed[0] == '\0') {
        return false;
    }
    generic_type_arg = v3_find_generic_type_param(generic_param_names,
                                                  generic_param_types,
                                                  generic_param_count,
                                                  trimmed);
    if (generic_type_arg) {
        v3_copy_text(out, cap, generic_type_arg);
        return true;
    }
    if (v3_is_internal_ref_type(trimmed)) {
        snprintf(out, cap, "%s", trimmed);
        return true;
    }
    if (v3_startswith(trimmed, "fn ") || v3_startswith(trimmed, "fn(")) {
        v3_copy_text(out, cap, trimmed);
        return true;
    }
    if (strcmp(trimmed, "void") == 0 ||
        strcmp(trimmed, "int8") == 0 ||
        strcmp(trimmed, "uint8") == 0 ||
        strcmp(trimmed, "int16") == 0 ||
        strcmp(trimmed, "uint16") == 0 ||
        strcmp(trimmed, "int32") == 0 ||
        strcmp(trimmed, "int") == 0 ||
        strcmp(trimmed, "uint32") == 0 ||
        strcmp(trimmed, "char") == 0 ||
        strcmp(trimmed, "int64") == 0 ||
        strcmp(trimmed, "uint64") == 0 ||
        strcmp(trimmed, "float") == 0 ||
        strcmp(trimmed, "float32") == 0 ||
        strcmp(trimmed, "float64") == 0 ||
        strcmp(trimmed, "bool") == 0 ||
        strcmp(trimmed, "ptr") == 0 ||
        strcmp(trimmed, "cstring") == 0 ||
        strcmp(trimmed, "str") == 0 ||
        strcmp(trimmed, "Bytes") == 0) {
        v3_copy_text(out, cap, trimmed);
        return true;
    }
    pointer_depth = v3_type_text_pointer_depth(trimmed, inner, sizeof(inner));
    if (pointer_depth > 0) {
        if (!v3_normalize_type_text_with_params(lowering,
                                                owner_module_path,
                                                aliases,
                                                alias_count,
                                                generic_param_names,
                                                generic_param_types,
                                                generic_param_count,
                                                inner,
                                                normalized_inner,
                                                sizeof(normalized_inner))) {
            return false;
        }
        snprintf(out, cap, "%s", normalized_inner);
        for (i = 0; i < (size_t)pointer_depth; ++i) {
            size_t used = strlen(out);
            if (used + 2U >= cap) {
                return false;
            }
            snprintf(out + used, cap - used, " *");
        }
        return true;
    }
    if (v3_parse_fixed_array_type_meta(trimmed,
                                       inner,
                                       sizeof(inner),
                                       len_expr,
                                       sizeof(len_expr)) &&
        v3_eval_type_int32_expr(lowering,
                                owner_module_path,
                                aliases,
                                alias_count,
                                len_expr,
                                &fixed_len)) {
        if (!v3_normalize_type_text_with_params(lowering,
                                                owner_module_path,
                                                aliases,
                                                alias_count,
                                                generic_param_names,
                                                generic_param_types,
                                                generic_param_count,
                                                inner,
                                                normalized_inner,
                                                sizeof(normalized_inner))) {
            return false;
        }
        snprintf(out, cap, "%s[%d]", normalized_inner, fixed_len);
        return true;
    }
    if (v3_startswith(trimmed, "Result[") && trimmed[strlen(trimmed) - 1U] == ']') {
        snprintf(inner, sizeof(inner), "%.*s", (int)(strlen(trimmed) - 8U), trimmed + 7);
        if (!v3_normalize_type_text_with_params(lowering,
                                                owner_module_path,
                                                aliases,
                                                alias_count,
                                                generic_param_names,
                                                generic_param_types,
                                                generic_param_count,
                                                inner,
                                                normalized_inner,
                                                sizeof(normalized_inner))) {
            return false;
        }
        snprintf(out, cap, "Result[%s]", normalized_inner);
        return true;
    }
    if (strlen(trimmed) > 2U && strcmp(trimmed + strlen(trimmed) - 2U, "[]") == 0) {
        snprintf(inner, sizeof(inner), "%.*s", (int)(strlen(trimmed) - 2U), trimmed);
        if (!v3_normalize_type_text_with_params(lowering,
                                                owner_module_path,
                                                aliases,
                                                alias_count,
                                                generic_param_names,
                                                generic_param_types,
                                                generic_param_count,
                                                inner,
                                                normalized_inner,
                                                sizeof(normalized_inner))) {
            return false;
        }
        snprintf(out, cap, "%s[]", normalized_inner);
        return true;
    }
    if (v3_is_tuple_type_text(trimmed)) {
        char (*items)[4096] = NULL;
        V3TypeFieldDef *field = NULL;
        size_t item_count = 0U;
        size_t item_index;
        size_t used;
        bool ok = false;
        items = (char (*)[4096])v3_xmalloc(sizeof(*items) * CHENG_V3_MAX_TYPE_FIELDS);
        field = (V3TypeFieldDef *)v3_xmalloc(sizeof(*field));
        if (!v3_parse_tuple_type_items(trimmed,
                                       items,
                                       &item_count,
                                       CHENG_V3_MAX_TYPE_FIELDS) ||
            item_count == 0U) {
            goto normalize_tuple_cleanup;
        }
        snprintf(out, cap, "%s", "tuple[");
        for (item_index = 0; item_index < item_count; ++item_index) {
            char normalized_field_type[CHENG_V3_MAX_TYPE_TEXT];
            if (!v3_parse_type_field_spec(items[item_index], true, field) ||
                !v3_normalize_type_text_with_params(lowering,
                                                    owner_module_path,
                                                    aliases,
                                                    alias_count,
                                                    generic_param_names,
                                                    generic_param_types,
                                                    generic_param_count,
                                                    field->type_text,
                                                    normalized_field_type,
                                                    sizeof(normalized_field_type))) {
                goto normalize_tuple_cleanup;
            }
            used = strlen(out);
            if (used + 4U >= cap) {
                goto normalize_tuple_cleanup;
            }
            if (item_index > 0U) {
                snprintf(out + used, cap - used, ", ");
                used = strlen(out);
            }
            if (field->has_explicit_name) {
                snprintf(out + used, cap - used, "%s: %s", field->name, normalized_field_type);
            } else {
                snprintf(out + used, cap - used, "%s", normalized_field_type);
            }
            used = strlen(out);
            if (field->has_default_expr) {
                if (used + 4U >= cap) {
                    goto normalize_tuple_cleanup;
                }
                snprintf(out + used, cap - used, " = %s", field->default_expr);
            }
        }
        used = strlen(out);
        if (used + 2U >= cap) {
            goto normalize_tuple_cleanup;
        }
        snprintf(out + used, cap - used, "]");
        ok = true;
normalize_tuple_cleanup:
        free(field);
        free(items);
        return ok;
    }
    if (!v3_parse_type_instance_text(trimmed,
                                     base_name,
                                     sizeof(base_name),
                                     raw_type_args,
                                     &raw_type_arg_count,
                                     CHENG_V3_MAX_TYPE_PARAMS)) {
        return false;
    }
    if (raw_type_arg_count > 0U) {
        char lookup_module[PATH_MAX];
        char lookup_name[128];
        const char *resolved_module_path = NULL;
        lookup_name[0] = '\0';
        for (i = 0; i < raw_type_arg_count; ++i) {
            if (!v3_normalize_type_text_with_params(lowering,
                                                    owner_module_path,
                                                    aliases,
                                                    alias_count,
                                                    generic_param_names,
                                                    generic_param_types,
                                                    generic_param_count,
                                                    raw_type_args[i],
                                                    normalized_type_args[i],
                                                    sizeof(normalized_type_args[i]))) {
                return false;
            }
        }
        if (v3_split_qualified_type_text(trimmed,
                                         lookup_module,
                                         sizeof(lookup_module),
                                         lookup_name,
                                         sizeof(lookup_name))) {
            type_index = v3_find_type_def_index_by_symbol(lowering, lookup_module, lookup_name);
            resolved_module_path = lookup_module;
            if (!v3_parse_type_instance_text(lookup_name,
                                             lookup_name,
                                             sizeof(lookup_name),
                                             raw_type_args,
                                             &raw_type_arg_count,
                                             CHENG_V3_MAX_TYPE_PARAMS)) {
                return false;
            }
        } else if (strchr(trimmed, '.') != NULL) {
            char alias_text[128];
            char *dot;
            char copy[256];
            snprintf(copy, sizeof(copy), "%s", trimmed);
            dot = strchr(copy, '.');
            *dot = '\0';
            snprintf(alias_text, sizeof(alias_text), "%s", copy);
            snprintf(lookup_name, sizeof(lookup_name), "%s", dot + 1);
            resolved_module_path =
                v3_alias_module_path_exported_member(aliases,
                                                     alias_count,
                                                     owner_module_path,
                                                     alias_text,
                                                     lookup_name,
                                                     sizeof(lookup_name));
            if (!resolved_module_path) {
                return false;
            }
            type_index = v3_find_type_def_index_by_symbol(lowering, resolved_module_path, lookup_name);
            if (!v3_parse_type_instance_text(lookup_name,
                                             lookup_name,
                                             sizeof(lookup_name),
                                             raw_type_args,
                                             &raw_type_arg_count,
                                             CHENG_V3_MAX_TYPE_PARAMS)) {
                return false;
            }
        } else {
            snprintf(lookup_name, sizeof(lookup_name), "%s", base_name);
            type_index = v3_find_type_def_index_by_symbol(lowering, owner_module_path, trimmed);
            resolved_module_path = owner_module_path;
            if (type_index < 0) {
                for (i = 0; i < alias_count; ++i) {
                    type_index = v3_find_type_def_index_by_symbol(lowering, aliases[i].module_path, trimmed);
                    if (type_index >= 0) {
                        resolved_module_path = aliases[i].module_path;
                        break;
                    }
                }
            }
        }
        if (type_index < 0 &&
            raw_type_arg_count == 1U &&
            v3_eval_type_int32_expr(lowering,
                                    owner_module_path,
                                    aliases,
                                    alias_count,
                                    raw_type_args[0],
                                    &fixed_len)) {
            if (!v3_normalize_type_text_with_params(lowering,
                                                    owner_module_path,
                                                    aliases,
                                                    alias_count,
                                                    generic_param_names,
                                                    generic_param_types,
                                                    generic_param_count,
                                                    base_name,
                                                    normalized_inner,
                                                    sizeof(normalized_inner))) {
                return false;
            }
            snprintf(out, cap, "%s[%d]", normalized_inner, fixed_len);
            return true;
        }
        if (type_index >= 0) {
            if (lowering->type_defs[type_index].kind == V3_TYPE_DEF_ALIAS) {
                if (lowering->type_defs[type_index].generic_param_count > 0U) {
                    V3ImportAlias *type_aliases = NULL;
                    size_t type_alias_count = 0U;
                    bool ok = false;
                    type_aliases = (V3ImportAlias *)v3_xmalloc(sizeof(*type_aliases) * CHENG_V3_MAX_IMPORT_ALIASES);
                    if (!v3_load_import_aliases_from_source_path(lowering->type_defs[type_index].source_path,
                                                                 type_aliases,
                                                                 &type_alias_count,
                                                                 CHENG_V3_MAX_IMPORT_ALIASES)) {
                        free(type_aliases);
                        return false;
                    }
                    ok = v3_normalize_type_text_with_params(lowering,
                                                            lowering->type_defs[type_index].owner_module_path,
                                                            type_aliases,
                                                            type_alias_count,
                                                            lowering->type_defs[type_index].generic_param_names,
                                                            normalized_type_args,
                                                            raw_type_arg_count,
                                                            lowering->type_defs[type_index].alias_target,
                                                            out,
                                                            cap);
                    free(type_aliases);
                    return ok;
                }
                return v3_normalize_type_text_with_params(lowering,
                                                          lowering->type_defs[type_index].owner_module_path,
                                                          aliases,
                                                          alias_count,
                                                          generic_param_names,
                                                          generic_param_types,
                                                          generic_param_count,
                                                          lowering->type_defs[type_index].alias_target,
                                                          out,
                                                          cap);
            }
            v3_write_type_def_instance_normalized(&lowering->type_defs[type_index],
                                                  normalized_type_args,
                                                  raw_type_arg_count,
                                                  out,
                                                  cap);
            return true;
        }
        if (!resolved_module_path) {
            resolved_module_path = owner_module_path;
        }
        v3_write_normalized_type_instance_text(resolved_module_path,
                                               lookup_name[0] != '\0' ? lookup_name : base_name,
                                               normalized_type_args,
                                               raw_type_arg_count,
                                               false,
                                               out,
                                               cap);
        return true;
    }
    if (v3_split_qualified_type_text(trimmed,
                                     inner,
                                     sizeof(inner),
                                     base_name,
                                     sizeof(base_name))) {
        type_index = v3_find_type_def_index_by_symbol(lowering, inner, base_name);
        if (type_index >= 0) {
            if (lowering->type_defs[type_index].kind == V3_TYPE_DEF_ALIAS) {
                return v3_normalize_type_text_with_params(lowering,
                                                          inner,
                                                          aliases,
                                                          alias_count,
                                                          generic_param_names,
                                                          generic_param_types,
                                                          generic_param_count,
                                                          lowering->type_defs[type_index].alias_target,
                                                          out,
                                                          cap);
            }
            v3_write_type_def_normalized(&lowering->type_defs[type_index], out, cap);
            return true;
        }
        v3_copy_text(out, cap, trimmed);
        return true;
    }
    if (strchr(trimmed, '.') != NULL) {
        char alias_text[128];
        char type_name[128];
        char copy[256];
        char *dot;
        const char *module_path;
        snprintf(copy, sizeof(copy), "%s", trimmed);
        dot = strchr(copy, '.');
        *dot = '\0';
        snprintf(alias_text, sizeof(alias_text), "%s", copy);
        snprintf(type_name, sizeof(type_name), "%s", dot + 1);
        module_path = v3_alias_module_path_exported_member(aliases,
                                                           alias_count,
                                                           owner_module_path,
                                                           alias_text,
                                                           type_name,
                                                           sizeof(type_name));
        if (!module_path) {
            return false;
        }
        type_index = v3_find_type_def_index_by_symbol(lowering, module_path, type_name);
        if (type_index >= 0) {
            if (lowering->type_defs[type_index].kind == V3_TYPE_DEF_ALIAS) {
                return v3_normalize_type_text_with_params(lowering,
                                                          module_path,
                                                          aliases,
                                                          alias_count,
                                                          generic_param_names,
                                                          generic_param_types,
                                                          generic_param_count,
                                                          lowering->type_defs[type_index].alias_target,
                                                          out,
                                                          cap);
            }
            v3_write_type_def_normalized(&lowering->type_defs[type_index], out, cap);
            return true;
        }
        snprintf(out, cap, "%s::%s", module_path, type_name);
        return true;
    }
    type_index = v3_find_type_def_index_by_symbol(lowering, owner_module_path, trimmed);
    if (type_index >= 0) {
        if (lowering->type_defs[type_index].kind == V3_TYPE_DEF_ALIAS) {
            return v3_normalize_type_text_with_params(lowering,
                                                      owner_module_path,
                                                      aliases,
                                                      alias_count,
                                                      generic_param_names,
                                                      generic_param_types,
                                                      generic_param_count,
                                                      lowering->type_defs[type_index].alias_target,
                                                      out,
                                                      cap);
        }
        v3_write_type_def_normalized(&lowering->type_defs[type_index], out, cap);
        return true;
    }
    for (i = 0; i < alias_count; ++i) {
        type_index = v3_find_type_def_index_by_symbol(lowering, aliases[i].module_path, trimmed);
        if (type_index >= 0) {
            if (lowering->type_defs[type_index].kind == V3_TYPE_DEF_ALIAS) {
                return v3_normalize_type_text_with_params(lowering,
                                                          aliases[i].module_path,
                                                          aliases,
                                                          alias_count,
                                                          generic_param_names,
                                                          generic_param_types,
                                                          generic_param_count,
                                                          lowering->type_defs[type_index].alias_target,
                                                          out,
                                                          cap);
            }
            v3_write_type_def_normalized(&lowering->type_defs[type_index], out, cap);
            return true;
        }
    }
    snprintf(out, cap, "%s::%s", owner_module_path, trimmed);
    return true;
}

static bool v3_resolve_pointer_pointee_type(const V3LoweringPlanStub *lowering,
                                            const char *owner_module_path,
                                            const V3ImportAlias *aliases,
                                            size_t alias_count,
                                            const char *pointer_type_text,
                                            char *pointee_type_out,
                                            size_t pointee_type_cap,
                                            char *pointee_abi_out,
                                            size_t pointee_abi_cap) {
    char normalized_pointer[256];
    char pointee_type[256];
    if (!v3_normalize_type_text(lowering,
                                owner_module_path,
                                aliases,
                                alias_count,
                                pointer_type_text,
                                normalized_pointer,
                                sizeof(normalized_pointer))) {
        return false;
    }
    if (strcmp(normalized_pointer, "ptr") == 0 ||
        strcmp(normalized_pointer, "cstring") == 0) {
        snprintf(pointee_type_out, pointee_type_cap, "%s", "char");
        snprintf(pointee_abi_out, pointee_abi_cap, "%s", "i32");
        return true;
    }
    if (!v3_type_text_strip_one_pointer_suffix(normalized_pointer, pointee_type, sizeof(pointee_type))) {
        return false;
    }
    snprintf(pointee_type_out, pointee_type_cap, "%s", pointee_type);
    snprintf(pointee_abi_out, pointee_abi_cap, "%s", v3_type_abi_class(pointee_type));
    return true;
}

static bool v3_resolve_scalar_or_pointer_cast_target(const V3LoweringPlanStub *lowering,
                                                     const char *owner_module_path,
                                                     const V3ImportAlias *aliases,
                                                     size_t alias_count,
                                                     const char *callee_text,
                                                     char *target_type_out,
                                                     size_t target_type_cap,
                                                     char *target_abi_out,
                                                     size_t target_abi_cap) {
    char normalized_type[256];
    const char *target_abi;
    if (!v3_normalize_type_text(lowering,
                                owner_module_path,
                                aliases,
                                alias_count,
                                callee_text,
                                normalized_type,
                                sizeof(normalized_type))) {
        return false;
    }
    target_abi = v3_type_abi_class(normalized_type);
    if (!v3_abi_class_scalar_or_ptr(target_abi)) {
        return false;
    }
    snprintf(target_type_out, target_type_cap, "%s", normalized_type);
    snprintf(target_abi_out, target_abi_cap, "%s", target_abi);
    return true;
}

static int32_t v3_find_type_def_index_by_symbol(const V3LoweringPlanStub *lowering,
                                                const char *owner_module_path,
                                                const char *type_name) {
    char lookup_base[128];
    char lookup_args[CHENG_V3_MAX_TYPE_PARAMS][256];
    size_t lookup_arg_count = 0U;
    size_t i;
    if (!v3_parse_type_instance_text(type_name,
                                     lookup_base,
                                     sizeof(lookup_base),
                                     lookup_args,
                                     &lookup_arg_count,
                                     CHENG_V3_MAX_TYPE_PARAMS)) {
        return -1;
    }
    for (i = 0; i < lowering->type_def_count; ++i) {
        if (strcmp(lowering->type_defs[i].owner_module_path, owner_module_path) == 0 &&
            lowering->type_defs[i].generic_param_count == lookup_arg_count &&
            strcmp(lowering->type_defs[i].type_name, lookup_base) == 0) {
            return (int32_t)i;
        }
    }
    for (i = 0; i < lowering->type_def_count; ++i) {
        if (strcmp(lowering->type_defs[i].owner_module_path, owner_module_path) == 0 &&
            lowering->type_defs[i].generic_param_count == lookup_arg_count &&
            v3_text_case_equal(lowering->type_defs[i].type_name, lookup_base)) {
            return (int32_t)i;
        }
    }
    return -1;
}

static bool v3_collect_type_defs_from_source(const V3SystemLinkPlanStub *plan,
                                             const char *source_path,
                                             V3LoweringPlanStub *lowering) {
    char owner_module_path[PATH_MAX];
    V3ImportAlias aliases[CHENG_V3_MAX_IMPORT_ALIASES];
    size_t alias_count = 0U;
    char *owned;
    char *cursor;
    char *line;
    char *lines[CHENG_V3_MAX_SOURCE_LINES];
    size_t line_count = 0U;
    size_t i = 0U;
    bool in_type_block = false;
    v3_source_path_to_module_path(plan->workspace_root,
                                  plan->package_root,
                                  plan->package_id,
                                  source_path,
                                  owner_module_path,
                                  sizeof(owner_module_path));
    owned = v3_read_file(source_path);
    if (!owned) {
        return false;
    }
    cursor = owned;
    while ((line = strsep(&cursor, "\n")) != NULL) {
        if (line_count >= CHENG_V3_MAX_SOURCE_LINES) {
            free(owned);
            return false;
        }
        lines[line_count++] = line;
    }
    v3_parse_import_aliases_from_lines(lines,
                                       line_count,
                                       aliases,
                                       &alias_count,
                                       CHENG_V3_MAX_IMPORT_ALIASES);
    while (i < line_count) {
        char line_copy[4096];
        char head_text[256];
        char *trimmed;
        int32_t indent;
        int32_t header_indent = 0;
        bool saw_type_head = false;
        snprintf(line_copy, sizeof(line_copy), "%s", lines[i]);
        indent = v3_line_indent(line_copy);
        trimmed = v3_trim_inplace(line_copy);
        if (*trimmed == '\0' || *trimmed == '#') {
            i += 1U;
            continue;
        }
        if (indent == 0 && strcmp(trimmed, "type") == 0) {
            in_type_block = true;
            i += 1U;
            continue;
        }
        if (indent == 0 && v3_startswith(trimmed, "type ")) {
            snprintf(head_text, sizeof(head_text), "%s", trimmed + 5);
            v3_trim_copy_text(head_text, head_text, sizeof(head_text));
            header_indent = 0;
            saw_type_head = strchr(head_text, '=') != NULL;
        } else if (in_type_block && indent > 0 && strchr(trimmed, '=') != NULL) {
            snprintf(head_text, sizeof(head_text), "%s", trimmed);
            header_indent = indent;
            saw_type_head = true;
        }
        if (!saw_type_head && !in_type_block) {
            i += 1U;
            continue;
        }
        if (!saw_type_head && indent == 0) {
            in_type_block = false;
            continue;
        }
        if (saw_type_head) {
            char name[128];
            char type_name[128];
            char generic_param_names[CHENG_V3_MAX_TYPE_PARAMS][128];
            size_t generic_param_count = 0U;
            char rhs[256];
            char normalized_rhs[256];
            char *eq = strchr(head_text, '=');
            V3TypeDefStub *type_def;
            *eq = '\0';
            snprintf(name, sizeof(name), "%s", head_text);
            snprintf(rhs, sizeof(rhs), "%s", eq + 1);
            v3_trim_copy_text(name, name, sizeof(name));
            v3_trim_copy_text(rhs, rhs, sizeof(rhs));
            if (!v3_parse_type_head_text(name,
                                         type_name,
                                         sizeof(type_name),
                                         generic_param_names,
                                         &generic_param_count,
                                         CHENG_V3_MAX_TYPE_PARAMS) ||
                type_name[0] == '\0' ||
                v3_find_type_def_index_by_symbol(lowering, owner_module_path, name) >= 0 ||
                lowering->type_def_count >= CHENG_V3_MAX_TYPE_DEFS) {
                fprintf(stderr, "[cheng_v3_seed] invalid or duplicate type head: %s line=%zu text=%s\n",
                        source_path,
                        i + 1U,
                        lines[i]);
                free(owned);
                return false;
            }
            type_def = &lowering->type_defs[lowering->type_def_count];
            memset(type_def, 0, sizeof(*type_def));
            snprintf(type_def->source_path, sizeof(type_def->source_path), "%s", source_path);
            snprintf(type_def->owner_module_path, sizeof(type_def->owner_module_path), "%s", owner_module_path);
            snprintf(type_def->type_name, sizeof(type_def->type_name), "%s", type_name);
            type_def->generic_param_count = generic_param_count;
            memcpy(type_def->generic_param_names,
                   generic_param_names,
                   sizeof(type_def->generic_param_names));
            if (rhs[0] != '\0') {
                if (strcmp(rhs, "enum") == 0) {
                    int32_t enum_ordinal = 0;
                    type_def->kind = V3_TYPE_DEF_ALIAS;
                    snprintf(type_def->alias_target, sizeof(type_def->alias_target), "%s", "int32");
                    lowering->type_def_count += 1U;
                    i += 1U;
                    while (i < line_count) {
                        char enum_line_copy[4096];
                        char *enum_trimmed;
                        int32_t enum_indent;
                        char enum_member_name[128];
                        snprintf(enum_line_copy, sizeof(enum_line_copy), "%s", lines[i]);
                        enum_indent = v3_line_indent(enum_line_copy);
                        enum_trimmed = v3_trim_inplace(enum_line_copy);
                        if (*enum_trimmed == '\0') {
                            i += 1U;
                            continue;
                        }
                        if (*enum_trimmed == '#') {
                            i += 1U;
                            continue;
                        }
                        if (enum_indent <= header_indent) {
                            break;
                        }
                        snprintf(enum_member_name, sizeof(enum_member_name), "%s", enum_trimmed);
                        v3_trim_copy_text(enum_member_name, enum_member_name, sizeof(enum_member_name));
                        if (enum_member_name[0] != '\0') {
                            size_t member_len = strcspn(enum_member_name, " \t#=");
                            enum_member_name[member_len] = '\0';
                        }
                        if (enum_member_name[0] != '\0') {
                            if (lowering->const_count >= CHENG_V3_MAX_TOP_LEVEL_CONSTS) {
                                free(owned);
                                return false;
                            }
                            snprintf(lowering->consts[lowering->const_count].symbol_text,
                                     sizeof(lowering->consts[lowering->const_count].symbol_text),
                                     "%s::%s",
                                     owner_module_path,
                                     enum_member_name);
                            snprintf(lowering->consts[lowering->const_count].owner_module_path,
                                     sizeof(lowering->consts[lowering->const_count].owner_module_path),
                                     "%s",
                                     owner_module_path);
                            snprintf(lowering->consts[lowering->const_count].const_name,
                                     sizeof(lowering->consts[lowering->const_count].const_name),
                                     "%s",
                                     enum_member_name);
                            snprintf(lowering->consts[lowering->const_count].type_text,
                                     sizeof(lowering->consts[lowering->const_count].type_text),
                                     "%s",
                                     name);
                            snprintf(lowering->consts[lowering->const_count].abi_class,
                                     sizeof(lowering->consts[lowering->const_count].abi_class),
                                     "%s",
                                     "i32");
                            lowering->consts[lowering->const_count].i64_value = (int64_t)enum_ordinal;
                            lowering->consts[lowering->const_count].bool_value = enum_ordinal != 0;
                            lowering->const_count += 1U;
                            enum_ordinal += 1;
                        }
                        i += 1U;
                    }
                    continue;
                }
                if (strcmp(rhs, "object") == 0) {
                    rhs[0] = '\0';
                } else if (strcmp(rhs, "ref") == 0 || strcmp(rhs, "ref object") == 0) {
                    type_def->ref_object = true;
                    rhs[0] = '\0';
                }
            }
            if (rhs[0] != '\0') {
                type_def->kind = V3_TYPE_DEF_ALIAS;
                if (type_def->generic_param_count > 0U) {
                    snprintf(type_def->alias_target, sizeof(type_def->alias_target), "%s", rhs);
                } else {
                    if (!v3_normalize_type_text(lowering,
                                                owner_module_path,
                                                aliases,
                                                alias_count,
                                                rhs,
                                                normalized_rhs,
                                                sizeof(normalized_rhs))) {
                        fprintf(stderr, "[cheng_v3_seed] type alias normalize failed: %s line=%zu text=%s\n",
                                source_path,
                                i + 1U,
                                lines[i]);
                        free(owned);
                        return false;
                    }
                    snprintf(type_def->alias_target, sizeof(type_def->alias_target), "%s", normalized_rhs);
                }
                lowering->type_def_count += 1U;
                i += 1U;
                continue;
            }
            type_def->kind = V3_TYPE_DEF_RECORD;
            i += 1U;
            while (i < line_count) {
                char field_line_copy[4096];
                char *field_trimmed;
                int32_t field_indent;
                V3TypeFieldDef parsed_field;
                snprintf(field_line_copy, sizeof(field_line_copy), "%s", lines[i]);
                field_indent = v3_line_indent(field_line_copy);
                field_trimmed = v3_trim_inplace(field_line_copy);
                if (*field_trimmed == '\0' || *field_trimmed == '#') {
                    i += 1U;
                    continue;
                }
                if (field_indent <= header_indent) {
                    break;
                }
                if (type_def->field_count >= CHENG_V3_MAX_TYPE_FIELDS ||
                    !v3_parse_type_field_spec(field_trimmed, false, &parsed_field)) {
                    fprintf(stderr, "[cheng_v3_seed] invalid type field: %s line=%zu text=%s\n",
                            source_path,
                            i + 1U,
                            lines[i]);
                    free(owned);
                    return false;
                }
                snprintf(type_def->fields[type_def->field_count].name,
                         sizeof(type_def->fields[type_def->field_count].name),
                         "%s",
                         parsed_field.name);
                if (type_def->fields[type_def->field_count].name[0] == '\0') {
                    fprintf(stderr, "[cheng_v3_seed] empty type field name: %s line=%zu\n",
                            source_path,
                            i + 1U);
                    free(owned);
                    return false;
                }
                type_def->fields[type_def->field_count].has_explicit_name = true;
                type_def->fields[type_def->field_count].has_default_expr = parsed_field.has_default_expr;
                if (parsed_field.has_default_expr) {
                    snprintf(type_def->fields[type_def->field_count].default_expr,
                             sizeof(type_def->fields[type_def->field_count].default_expr),
                             "%s",
                             parsed_field.default_expr);
                } else {
                    type_def->fields[type_def->field_count].default_expr[0] = '\0';
                }
                if (type_def->generic_param_count > 0U) {
                    v3_trim_copy_text(parsed_field.type_text,
                                      type_def->fields[type_def->field_count].type_text,
                                      sizeof(type_def->fields[type_def->field_count].type_text));
                } else if (!v3_normalize_type_text(lowering,
                                                   owner_module_path,
                                                   aliases,
                                                   alias_count,
                                                   parsed_field.type_text,
                                                   type_def->fields[type_def->field_count].type_text,
                                                   sizeof(type_def->fields[type_def->field_count].type_text))) {
                    fprintf(stderr, "[cheng_v3_seed] type field normalize failed: %s line=%zu text=%s\n",
                            source_path,
                            i + 1U,
                            lines[i]);
                    free(owned);
                    return false;
                }
                type_def->field_count += 1U;
                i += 1U;
            }
            if (!v3_validate_type_field_defaults(source_path,
                                                 type_def->type_name,
                                                 type_def->fields,
                                                 type_def->field_count)) {
                free(owned);
                return false;
            }
            lowering->type_def_count += 1U;
            continue;
        }
        fprintf(stderr, "[cheng_v3_seed] malformed type block: %s line=%zu text=%s\n",
                source_path,
                i + 1U,
                lines[i]);
        free(owned);
        return false;
    }
    free(owned);
    return true;
}

static bool v3_refresh_type_defs_from_source(const V3SystemLinkPlanStub *plan,
                                             const char *source_path,
                                             V3LoweringPlanStub *lowering) {
    char owner_module_path[PATH_MAX];
    V3ImportAlias aliases[CHENG_V3_MAX_IMPORT_ALIASES];
    size_t alias_count = 0U;
    char *owned;
    char *cursor;
    char *line;
    char *lines[CHENG_V3_MAX_SOURCE_LINES];
    size_t line_count = 0U;
    size_t i = 0U;
    bool in_type_block = false;
    v3_source_path_to_module_path(plan->workspace_root,
                                  plan->package_root,
                                  plan->package_id,
                                  source_path,
                                  owner_module_path,
                                  sizeof(owner_module_path));
    owned = v3_read_file(source_path);
    if (!owned) {
        return false;
    }
    cursor = owned;
    while ((line = strsep(&cursor, "\n")) != NULL) {
        if (line_count >= CHENG_V3_MAX_SOURCE_LINES) {
            free(owned);
            return false;
        }
        lines[line_count++] = line;
    }
    v3_parse_import_aliases_from_lines(lines,
                                       line_count,
                                       aliases,
                                       &alias_count,
                                       CHENG_V3_MAX_IMPORT_ALIASES);
    while (i < line_count) {
        char line_copy[4096];
        char head_text[256];
        char *trimmed;
        int32_t indent;
        int32_t header_indent = 0;
        bool saw_type_head = false;
        snprintf(line_copy, sizeof(line_copy), "%s", lines[i]);
        indent = v3_line_indent(line_copy);
        trimmed = v3_trim_inplace(line_copy);
        if (*trimmed == '\0' || *trimmed == '#') {
            i += 1U;
            continue;
        }
        if (indent == 0 && strcmp(trimmed, "type") == 0) {
            in_type_block = true;
            i += 1U;
            continue;
        }
        if (indent == 0 && v3_startswith(trimmed, "type ")) {
            snprintf(head_text, sizeof(head_text), "%s", trimmed + 5);
            v3_trim_copy_text(head_text, head_text, sizeof(head_text));
            header_indent = 0;
            saw_type_head = strchr(head_text, '=') != NULL;
        } else if (in_type_block && indent > 0 && strchr(trimmed, '=') != NULL) {
            snprintf(head_text, sizeof(head_text), "%s", trimmed);
            header_indent = indent;
            saw_type_head = true;
        }
        if (!saw_type_head && !in_type_block) {
            i += 1U;
            continue;
        }
        if (!saw_type_head && indent == 0) {
            in_type_block = false;
            continue;
        }
        if (saw_type_head) {
            char name[128];
            char type_name[128];
            char generic_param_names[CHENG_V3_MAX_TYPE_PARAMS][128];
            size_t generic_param_count = 0U;
            char rhs[256];
            char normalized_rhs[256];
            char *eq = strchr(head_text, '=');
            int32_t type_index;
            V3TypeDefStub *type_def;
            *eq = '\0';
            snprintf(name, sizeof(name), "%s", head_text);
            snprintf(rhs, sizeof(rhs), "%s", eq + 1);
            v3_trim_copy_text(name, name, sizeof(name));
            v3_trim_copy_text(rhs, rhs, sizeof(rhs));
            if (!v3_parse_type_head_text(name,
                                         type_name,
                                         sizeof(type_name),
                                         generic_param_names,
                                         &generic_param_count,
                                         CHENG_V3_MAX_TYPE_PARAMS)) {
                fprintf(stderr,
                        "[cheng_v3_seed] refresh type head parse failed source=%s type=%s line=%zu\n",
                        source_path,
                        name,
                        i + 1U);
                free(owned);
                return false;
            }
            type_index = v3_find_type_def_index_by_symbol(lowering, owner_module_path, name);
            if (type_index < 0) {
                fprintf(stderr,
                        "[cheng_v3_seed] refresh type def missing source=%s type=%s line=%zu\n",
                        source_path,
                        name,
                        i + 1U);
                free(owned);
                return false;
            }
            type_def = &lowering->type_defs[type_index];
            snprintf(type_def->source_path, sizeof(type_def->source_path), "%s", source_path);
            snprintf(type_def->type_name, sizeof(type_def->type_name), "%s", type_name);
            type_def->generic_param_count = generic_param_count;
            memcpy(type_def->generic_param_names,
                   generic_param_names,
                   sizeof(type_def->generic_param_names));
            type_def->ref_object = false;
            if (rhs[0] != '\0') {
                if (strcmp(rhs, "enum") == 0) {
                    i += 1U;
                    while (i < line_count) {
                        char enum_line_copy[4096];
                        char *enum_trimmed;
                        int32_t enum_indent;
                        snprintf(enum_line_copy, sizeof(enum_line_copy), "%s", lines[i]);
                        enum_indent = v3_line_indent(enum_line_copy);
                        enum_trimmed = v3_trim_inplace(enum_line_copy);
                        if (*enum_trimmed == '\0' || *enum_trimmed == '#') {
                            i += 1U;
                            continue;
                        }
                        if (enum_indent <= header_indent) {
                            break;
                        }
                        i += 1U;
                    }
                    continue;
                }
                if (strcmp(rhs, "object") == 0) {
                    rhs[0] = '\0';
                } else if (strcmp(rhs, "ref") == 0 || strcmp(rhs, "ref object") == 0) {
                    type_def->ref_object = true;
                    rhs[0] = '\0';
                }
            }
            if (rhs[0] != '\0') {
                type_def->kind = V3_TYPE_DEF_ALIAS;
                if (type_def->generic_param_count > 0U) {
                    snprintf(type_def->alias_target, sizeof(type_def->alias_target), "%s", rhs);
                } else {
                    if (!v3_normalize_type_text(lowering,
                                                owner_module_path,
                                                aliases,
                                                alias_count,
                                                rhs,
                                                normalized_rhs,
                                                sizeof(normalized_rhs))) {
                        fprintf(stderr,
                                "[cheng_v3_seed] refresh type alias normalize failed: %s line=%zu text=%s\n",
                                source_path,
                                i + 1U,
                                lines[i]);
                        free(owned);
                        return false;
                    }
                    snprintf(type_def->alias_target, sizeof(type_def->alias_target), "%s", normalized_rhs);
                }
                i += 1U;
                continue;
            }
            type_def->kind = V3_TYPE_DEF_RECORD;
            type_def->field_count = 0U;
            i += 1U;
            while (i < line_count) {
                char field_line_copy[4096];
                char *field_trimmed;
                int32_t field_indent;
                V3TypeFieldDef parsed_field;
                snprintf(field_line_copy, sizeof(field_line_copy), "%s", lines[i]);
                field_indent = v3_line_indent(field_line_copy);
                field_trimmed = v3_trim_inplace(field_line_copy);
                if (*field_trimmed == '\0' || *field_trimmed == '#') {
                    i += 1U;
                    continue;
                }
                if (field_indent <= header_indent) {
                    break;
                }
                if (type_def->field_count >= CHENG_V3_MAX_TYPE_FIELDS ||
                    !v3_parse_type_field_spec(field_trimmed, false, &parsed_field)) {
                    fprintf(stderr,
                            "[cheng_v3_seed] refresh invalid type field: %s line=%zu text=%s\n",
                            source_path,
                            i + 1U,
                            lines[i]);
                    free(owned);
                    return false;
                }
                snprintf(type_def->fields[type_def->field_count].name,
                         sizeof(type_def->fields[type_def->field_count].name),
                         "%s",
                         parsed_field.name);
                if (type_def->fields[type_def->field_count].name[0] == '\0') {
                    fprintf(stderr,
                            "[cheng_v3_seed] refresh empty type field name: %s line=%zu\n",
                            source_path,
                            i + 1U);
                    free(owned);
                    return false;
                }
                type_def->fields[type_def->field_count].has_explicit_name = true;
                type_def->fields[type_def->field_count].has_default_expr = parsed_field.has_default_expr;
                if (parsed_field.has_default_expr) {
                    snprintf(type_def->fields[type_def->field_count].default_expr,
                             sizeof(type_def->fields[type_def->field_count].default_expr),
                             "%s",
                             parsed_field.default_expr);
                } else {
                    type_def->fields[type_def->field_count].default_expr[0] = '\0';
                }
                if (type_def->generic_param_count > 0U) {
                    v3_trim_copy_text(parsed_field.type_text,
                                      type_def->fields[type_def->field_count].type_text,
                                      sizeof(type_def->fields[type_def->field_count].type_text));
                } else if (!v3_normalize_type_text(lowering,
                                                   owner_module_path,
                                                   aliases,
                                                   alias_count,
                                                   parsed_field.type_text,
                                                   type_def->fields[type_def->field_count].type_text,
                                                   sizeof(type_def->fields[type_def->field_count].type_text))) {
                    fprintf(stderr,
                            "[cheng_v3_seed] refresh type field normalize failed: %s line=%zu text=%s\n",
                            source_path,
                            i + 1U,
                            lines[i]);
                    free(owned);
                    return false;
                }
                type_def->field_count += 1U;
                i += 1U;
            }
            if (!v3_validate_type_field_defaults(source_path,
                                                 type_def->type_name,
                                                 type_def->fields,
                                                 type_def->field_count)) {
                free(owned);
                return false;
            }
            continue;
        }
        i += 1U;
    }
    free(owned);
    return true;
}

static bool v3_refresh_function_abis_from_source(const V3SystemLinkPlanStub *plan,
                                                 const char *source_path,
                                                 V3LoweringPlanStub *lowering) {
    char owner_module_path[PATH_MAX];
    V3ImportAlias aliases[CHENG_V3_MAX_IMPORT_ALIASES];
    size_t alias_count = 0U;
    char *owned;
    char *cursor;
    char *line;
    char *lines[CHENG_V3_MAX_SOURCE_LINES];
    size_t line_count = 0U;
    size_t i;
    v3_source_path_to_module_path(plan->workspace_root,
                                  plan->package_root,
                                  plan->package_id,
                                  source_path,
                                  owner_module_path,
                                  sizeof(owner_module_path));
    owned = v3_read_file(source_path);
    if (!owned) {
        return false;
    }
    cursor = owned;
    while ((line = strsep(&cursor, "\n")) != NULL) {
        if (line_count >= CHENG_V3_MAX_SOURCE_LINES) {
            free(owned);
            return false;
        }
        lines[line_count++] = line;
    }
    v3_parse_import_aliases_from_lines(lines,
                                       line_count,
                                       aliases,
                                       &alias_count,
                                       CHENG_V3_MAX_IMPORT_ALIASES);
    for (i = 0; i < lowering->function_count; ++i) {
        V3LoweredFunctionStub *function = &lowering->functions[i];
        size_t param_i;
        char normalized_type[256];
        if (strcmp(function->source_path, source_path) != 0) {
            continue;
        }
        for (param_i = 0; param_i < function->param_count; ++param_i) {
            char stripped_type[CHENG_V3_MAX_TYPE_TEXT];
            if (function->param_types[param_i][0] == '\0' ||
                strcmp(function->param_types[param_i], "unknown") == 0) {
                continue;
            }
            v3_strip_param_default_expr_text(function->param_types[param_i],
                                             stripped_type,
                                             sizeof(stripped_type));
            if (stripped_type[0] != '\0' &&
                strcmp(stripped_type, function->param_types[param_i]) != 0) {
                snprintf(function->param_types[param_i],
                         sizeof(function->param_types[param_i]),
                         "%s",
                         stripped_type);
            }
            if (!v3_normalize_type_text(lowering,
                                        function->owner_module_path[0] != '\0' ? function->owner_module_path
                                                                                : owner_module_path,
                                        aliases,
                                        alias_count,
                                        function->param_types[param_i],
                                        normalized_type,
                                        sizeof(normalized_type))) {
                snprintf(function->param_abi_classes[param_i],
                         sizeof(function->param_abi_classes[param_i]),
                         "%s",
                         v3_type_abi_class(function->param_types[param_i]));
                continue;
            }
            snprintf(function->param_types[param_i],
                     sizeof(function->param_types[param_i]),
                     "%s",
                     normalized_type);
            snprintf(function->param_abi_classes[param_i],
                     sizeof(function->param_abi_classes[param_i]),
                     "%s",
                     v3_type_abi_class(normalized_type));
        }
        if (function->return_type[0] != '\0' &&
            strcmp(function->return_type, "unknown") != 0 &&
            strcmp(function->return_type, "void") != 0) {
            if (!v3_normalize_type_text(lowering,
                                        function->owner_module_path[0] != '\0' ? function->owner_module_path
                                                                                : owner_module_path,
                                        aliases,
                                        alias_count,
                                        function->return_type,
                                        normalized_type,
                                        sizeof(normalized_type))) {
                snprintf(function->return_abi_class,
                         sizeof(function->return_abi_class),
                         "%s",
                         v3_type_abi_class(function->return_type));
                continue;
            }
            snprintf(function->return_type, sizeof(function->return_type), "%s", normalized_type);
        }
        snprintf(function->return_abi_class,
                 sizeof(function->return_abi_class),
                 "%s",
                 v3_type_abi_class(function->return_type));
    }
    free(owned);
    return true;
}

static bool v3_parse_fixed_array_type(const char *type_text,
                                      char *elem_type_out,
                                      size_t elem_cap,
                                      int32_t *len_out) {
    char copy[256];
    char len_text[64];
    char *open;
    char *close;
    char *end = NULL;
    long parsed_len;
    if (v3_startswith(type_text, "Result[") || strlen(type_text) <= 3U) {
        return false;
    }
    snprintf(copy, sizeof(copy), "%s", type_text);
    open = strrchr(copy, '[');
    close = strrchr(copy, ']');
    if (!open || !close || close <= open || close[1] != '\0') {
        return false;
    }
    *open = '\0';
    if (strchr(open + 1, '[') != NULL) {
        return false;
    }
    v3_trim_copy_text(copy, elem_type_out, elem_cap);
    snprintf(len_text, sizeof(len_text), "%.*s", (int)(close - open - 1), open + 1);
    v3_trim_copy_text(len_text, len_text, sizeof(len_text));
    if (elem_type_out[0] == '\0' || len_text[0] == '\0') {
        return false;
    }
    parsed_len = strtol(len_text, &end, 10);
    if (!end || *end != '\0' || parsed_len < 0L || parsed_len > INT32_MAX) {
        return false;
    }
    *len_out = (int32_t)parsed_len;
    return true;
}

static bool v3_parse_fixed_array_type_meta(const char *type_text,
                                           char *elem_type_out,
                                           size_t elem_cap,
                                           char *len_expr_out,
                                           size_t len_expr_cap) {
    char copy[256];
    char len_text[64];
    char *open;
    char *close;
    if (v3_startswith(type_text, "Result[") || strlen(type_text) <= 3U) {
        return false;
    }
    snprintf(copy, sizeof(copy), "%s", type_text);
    open = strrchr(copy, '[');
    close = strrchr(copy, ']');
    if (!open || !close || close <= open || close[1] != '\0') {
        return false;
    }
    *open = '\0';
    if (strchr(open + 1, '[') != NULL) {
        return false;
    }
    v3_trim_copy_text(copy, elem_type_out, elem_cap);
    snprintf(len_text, sizeof(len_text), "%.*s", (int)(close - open - 1), open + 1);
    v3_trim_copy_text(len_text, len_text, sizeof(len_text));
    if (elem_type_out[0] == '\0' || len_text[0] == '\0') {
        return false;
    }
    v3_trim_copy_text(len_text, len_expr_out, len_expr_cap);
    return true;
}

static const char *v3_active_codegen_target_triple = NULL;

static int32_t v3_active_target_pointer_size_i32(void) {
    if (v3_active_codegen_target_triple != NULL &&
        v3_target_is_wasm32_unknown_unknown(v3_active_codegen_target_triple)) {
        return 4;
    }
    return 8;
}

static int32_t v3_active_target_pointer_align_i32(void) {
    return v3_active_target_pointer_size_i32();
}

static int32_t v3_active_target_bytes_len_offset_i32(void) {
    return v3_active_target_pointer_size_i32();
}

static int32_t v3_active_target_bytes_size_i32(void) {
    return v3_align_up_i32(v3_active_target_bytes_len_offset_i32() + 4,
                           v3_active_target_pointer_align_i32());
}

static int32_t v3_active_target_str_len_offset_i32(void) {
    return v3_active_target_pointer_size_i32();
}

static int32_t v3_active_target_str_store_id_offset_i32(void) {
    return v3_active_target_str_len_offset_i32() + 4;
}

static int32_t v3_active_target_str_flags_offset_i32(void) {
    return v3_active_target_str_store_id_offset_i32() + 4;
}

static int32_t v3_active_target_str_size_i32(void) {
    return v3_align_up_i32(v3_active_target_str_flags_offset_i32() + 4,
                           v3_active_target_pointer_align_i32());
}

static int32_t v3_active_target_seq_buffer_offset_i32(void) {
    return 8;
}

static int32_t v3_active_target_seq_size_i32(void) {
    return v3_align_up_i32(v3_active_target_seq_buffer_offset_i32() + v3_active_target_pointer_size_i32(),
                           v3_active_target_pointer_align_i32());
}

static bool v3_compute_type_layout_impl(const V3LoweringPlanStub *lowering,
                                        const char *type_text,
                                        V3TypeLayoutStub *layout_out,
                                        size_t depth) {
    char stripped[256];
    char inner[256];
    char owner_module[PATH_MAX];
    char type_name[128];
    int32_t fixed_len = 0;
    int32_t found = -1;
    size_t i;
    if (depth > 64U) {
        return false;
    }
    v3_strip_var_prefix(type_text, stripped, sizeof(stripped));
    memset(layout_out, 0, sizeof(*layout_out));
    if (v3_startswith(stripped, "fn ") || v3_startswith(stripped, "fn(")) {
        layout_out->size = v3_active_target_pointer_size_i32();
        layout_out->align = v3_active_target_pointer_align_i32();
        return true;
    }
    if (v3_type_text_strip_one_pointer_suffix(stripped, inner, sizeof(inner))) {
        layout_out->size = v3_active_target_pointer_size_i32();
        layout_out->align = v3_active_target_pointer_align_i32();
        return true;
    }
    if (strcmp(stripped, "bool") == 0) {
        layout_out->size = 1;
        layout_out->align = 1;
        return true;
    }
    if (strcmp(stripped, "int8") == 0 ||
        strcmp(stripped, "uint8") == 0) {
        layout_out->size = 1;
        layout_out->align = 1;
        return true;
    }
    if (strcmp(stripped, "int16") == 0 ||
        strcmp(stripped, "uint16") == 0) {
        layout_out->size = 2;
        layout_out->align = 2;
        return true;
    }
    if (strcmp(stripped, "int32") == 0 ||
        strcmp(stripped, "int") == 0 ||
        strcmp(stripped, "uint32") == 0 ||
        strcmp(stripped, "char") == 0) {
        layout_out->size = 4;
        layout_out->align = 4;
        return true;
    }
    if (strcmp(stripped, "int64") == 0 ||
        strcmp(stripped, "uint64") == 0 ||
        strcmp(stripped, "float") == 0 ||
        strcmp(stripped, "float32") == 0 ||
        strcmp(stripped, "float64") == 0) {
        layout_out->size = 8;
        layout_out->align = 8;
        return true;
    }
    if (strcmp(stripped, "ptr") == 0 ||
        strcmp(stripped, "cstring") == 0) {
        layout_out->size = v3_active_target_pointer_size_i32();
        layout_out->align = v3_active_target_pointer_align_i32();
        return true;
    }
    if (strcmp(stripped, "str") == 0) {
        layout_out->size = v3_active_target_str_size_i32();
        layout_out->align = v3_active_target_pointer_align_i32();
        return true;
    }
    if (strcmp(stripped, "Bytes") == 0) {
        layout_out->size = v3_active_target_bytes_size_i32();
        layout_out->align = v3_active_target_pointer_align_i32();
        return true;
    }
    if (strlen(stripped) > 2U && strcmp(stripped + strlen(stripped) - 2U, "[]") == 0) {
        layout_out->size = v3_active_target_seq_size_i32();
        layout_out->align = v3_active_target_pointer_align_i32();
        return true;
    }
    if (v3_startswith(stripped, "Result[") && stripped[strlen(stripped) - 1U] == ']') {
        V3TypeLayoutStub value_layout;
        V3TypeLayoutStub err_layout;
        int32_t value_offset;
        int32_t err_offset;
        snprintf(inner, sizeof(inner), "%.*s", (int)(strlen(stripped) - 8U), stripped + 7);
        if (!v3_compute_type_layout_impl(lowering, inner, &value_layout, depth + 1U)) {
            fprintf(stderr,
                    "[cheng_v3_seed] result value layout failed type=%s inner=%s depth=%zu\n",
                    stripped,
                    inner,
                    depth);
            return false;
        }
        if (!v3_compute_type_layout_impl(lowering, "std/result::ErrorInfo", &err_layout, depth + 1U)) {
            fprintf(stderr,
                    "[cheng_v3_seed] result err layout failed type=%s depth=%zu\n",
                    stripped,
                    depth);
            return false;
        }
        value_offset = v3_align_up_i32(1, value_layout.align);
        err_offset = v3_align_up_i32(value_offset + value_layout.size, err_layout.align);
        layout_out->align = value_layout.align > err_layout.align ? value_layout.align : err_layout.align;
        if (layout_out->align < 1) {
            layout_out->align = 1;
        }
        layout_out->size = v3_align_up_i32(err_offset + err_layout.size, layout_out->align);
        return true;
    }
    if (v3_parse_fixed_array_type(stripped, inner, sizeof(inner), &fixed_len)) {
        V3TypeLayoutStub elem_layout;
        if (!v3_compute_type_layout_impl(lowering, inner, &elem_layout, depth + 1U)) {
            fprintf(stderr,
                    "[cheng_v3_seed] fixed array elem layout failed type=%s elem=%s len=%d depth=%zu\n",
                    stripped,
                    inner,
                    fixed_len,
                    depth);
            return false;
        }
        layout_out->align = elem_layout.align;
        layout_out->size = v3_align_up_i32(elem_layout.size * fixed_len, elem_layout.align);
        return true;
    }
    if (v3_is_tuple_type_text(stripped)) {
        char (*items)[4096] = NULL;
        V3TypeFieldDef *field = NULL;
        size_t item_count = 0U;
        bool ok = false;
        items = (char (*)[4096])v3_xmalloc(sizeof(*items) * CHENG_V3_MAX_TYPE_FIELDS);
        field = (V3TypeFieldDef *)v3_xmalloc(sizeof(*field));
        if (!v3_parse_tuple_type_items(stripped,
                                       items,
                                       &item_count,
                                       CHENG_V3_MAX_TYPE_FIELDS) ||
            item_count == 0U) {
            goto tuple_layout_cleanup;
        }
        layout_out->align = 1;
        layout_out->size = 0;
        for (i = 0; i < item_count; ++i) {
            V3TypeLayoutStub field_layout;
            int32_t field_align;
            if (!v3_parse_type_field_spec(items[i], true, field) ||
                !v3_compute_type_layout_impl(lowering, field->type_text, &field_layout, depth + 1U)) {
                goto tuple_layout_cleanup;
            }
            field_align = field_layout.align > 0 ? field_layout.align : 1;
            layout_out->size = v3_align_up_i32(layout_out->size, field_align);
            layout_out->size += field_layout.size;
            if (field_align > layout_out->align) {
                layout_out->align = field_align;
            }
        }
        layout_out->size = v3_align_up_i32(layout_out->size, layout_out->align);
        ok = true;
tuple_layout_cleanup:
        free(field);
        free(items);
        return ok;
    }
    if (v3_is_internal_ref_type(stripped)) {
        layout_out->size = v3_active_target_pointer_size_i32();
        layout_out->align = v3_active_target_pointer_align_i32();
        return true;
    }
    if (!v3_split_qualified_type_text(stripped,
                                      owner_module,
                                      sizeof(owner_module),
                                      type_name,
                                      sizeof(type_name))) {
        fprintf(stderr,
                "[cheng_v3_seed] layout split qualified type failed type=%s depth=%zu\n",
                stripped,
                depth);
        return false;
    }
    found = v3_find_type_def_index_by_symbol(lowering, owner_module, type_name);
    if (found < 0) {
        fprintf(stderr,
                "[cheng_v3_seed] layout type def missing type=%s owner=%s name=%s depth=%zu\n",
                stripped,
                owner_module,
                type_name,
                depth);
        return false;
    }
    {
        const V3TypeDefStub *type_def = &lowering->type_defs[found];
        char type_args[CHENG_V3_MAX_TYPE_PARAMS][256];
        size_t type_arg_count = 0U;
        V3ImportAlias *type_aliases = NULL;
        size_t type_alias_count = 0U;
        bool ok = false;
        if (!v3_parse_type_instance_text(type_name,
                                         type_name,
                                         sizeof(type_name),
                                         type_args,
                                         &type_arg_count,
                                         CHENG_V3_MAX_TYPE_PARAMS)) {
            goto layout_cleanup;
        }
        if (type_def->kind == V3_TYPE_DEF_ALIAS) {
            char alias_target[256];
            if (type_def->generic_param_count > 0U) {
                type_aliases = (V3ImportAlias *)v3_xmalloc(sizeof(*type_aliases) * CHENG_V3_MAX_IMPORT_ALIASES);
                if (!v3_load_import_aliases_from_source_path(type_def->source_path,
                                                             type_aliases,
                                                             &type_alias_count,
                                                             CHENG_V3_MAX_IMPORT_ALIASES) ||
                    type_def->generic_param_count != type_arg_count ||
                    !v3_normalize_type_text_with_params(lowering,
                                                        type_def->owner_module_path,
                                                        type_aliases,
                                                        type_alias_count,
                                                        type_def->generic_param_names,
                                                        type_args,
                                                        type_arg_count,
                                                        type_def->alias_target,
                                                        alias_target,
                                                        sizeof(alias_target))) {
                    goto layout_cleanup;
                }
            } else {
                snprintf(alias_target, sizeof(alias_target), "%s", type_def->alias_target);
            }
            ok = v3_compute_type_layout_impl(lowering, alias_target, layout_out, depth + 1U);
            goto layout_cleanup;
        }
        if (type_def->generic_param_count > 0U &&
            ((type_aliases = (V3ImportAlias *)v3_xmalloc(sizeof(*type_aliases) * CHENG_V3_MAX_IMPORT_ALIASES)) == NULL ||
             !v3_load_import_aliases_from_source_path(type_def->source_path,
                                                      type_aliases,
                                                      &type_alias_count,
                                                      CHENG_V3_MAX_IMPORT_ALIASES) ||
             type_def->generic_param_count != type_arg_count)) {
            goto layout_cleanup;
        }
        layout_out->align = 1;
        layout_out->size = 0;
        for (i = 0; i < type_def->field_count; ++i) {
            V3TypeLayoutStub field_layout;
            int32_t field_align;
            char resolved_field_type[256];
            if (type_def->generic_param_count > 0U) {
                if (!v3_normalize_type_text_with_params(lowering,
                                                        type_def->owner_module_path,
                                                        type_aliases,
                                                        type_alias_count,
                                                        type_def->generic_param_names,
                                                        type_args,
                                                        type_arg_count,
                                                        type_def->fields[i].type_text,
                                                        resolved_field_type,
                                                        sizeof(resolved_field_type))) {
                    goto layout_cleanup;
                }
            } else {
                snprintf(resolved_field_type, sizeof(resolved_field_type), "%s", type_def->fields[i].type_text);
            }
            if (!v3_compute_type_layout_impl(lowering,
                                             resolved_field_type,
                                             &field_layout,
                                             depth + 1U)) {
                fprintf(stderr,
                        "[cheng_v3_seed] layout field failed type=%s field=%s field_type=%s depth=%zu\n",
                        stripped,
                        type_def->fields[i].name,
                        resolved_field_type,
                        depth);
                goto layout_cleanup;
            }
            field_align = field_layout.align > 0 ? field_layout.align : 1;
            layout_out->size = v3_align_up_i32(layout_out->size, field_align);
            layout_out->size += field_layout.size;
            if (field_align > layout_out->align) {
                layout_out->align = field_align;
            }
        }
        layout_out->size = v3_align_up_i32(layout_out->size, layout_out->align);
        ok = true;
layout_cleanup:
        free(type_aliases);
        return ok;
    }
}

static bool v3_resolve_sizeof_expr_value(const V3LoweringPlanStub *lowering,
                                         const char *owner_module_path,
                                         const V3ImportAlias *aliases,
                                         size_t alias_count,
                                         const char *type_expr,
                                         int32_t *value_out) {
    char normalized_type[256];
    V3TypeLayoutStub layout;
    if (!lowering || !owner_module_path || !type_expr || !value_out) {
        return false;
    }
    if (!v3_normalize_type_text(lowering,
                                owner_module_path,
                                aliases,
                                alias_count,
                                type_expr,
                                normalized_type,
                                sizeof(normalized_type)) ||
        !v3_compute_type_layout_impl(lowering, normalized_type, &layout, 0U)) {
        return false;
    }
    *value_out = layout.size;
    return true;
}

static bool v3_signature_text_complete(const char *text);

static bool v3_parse_function_signature_meta(const char *line,
                                             const char *function_name,
                                             V3LoweredFunctionStub *function) {
    const char *open_paren;
    const char *close_paren = NULL;
    const char *body_sep;
    char header_copy[4096];
    char params[4096];
    char items[32][4096];
    char return_part[256];
    size_t item_count = 0U;
    size_t i;
    bool in_string = false;
    int32_t paren_depth = 0;
    open_paren = strchr(line, '(');
    if (!open_paren) {
        return false;
    }
    for (i = (size_t)(open_paren - line); line[i] != '\0'; ++i) {
        char ch = line[i];
        if (ch == '"' && !v3_quote_is_escaped(line, i)) {
            in_string = !in_string;
            continue;
        }
        if (in_string) {
            continue;
        }
        if (ch == '(') {
            paren_depth += 1;
            continue;
        }
        if (ch == ')') {
            paren_depth -= 1;
            if (paren_depth == 0) {
                close_paren = line + i;
                break;
            }
        }
    }
    if (!close_paren || close_paren <= open_paren) {
        return false;
    }
    body_sep = strrchr(line, '=');
    if (body_sep == NULL || body_sep <= close_paren) {
        body_sep = line + strlen(line);
    }
    snprintf(header_copy, sizeof(header_copy), "%s", line);
    if (v3_function_decl_prefix_len(v3_trim_inplace(header_copy)) == 0U) {
        return false;
    }
    if (strstr(line, function_name) == NULL) {
        return false;
    }
    function->param_count = 0U;
    function->return_type[0] = '\0';
    snprintf(function->return_abi_class, sizeof(function->return_abi_class), "%s", "void");
    snprintf(params, sizeof(params), "%.*s", (int)(close_paren - open_paren - 1), open_paren + 1);
    if (!v3_split_top_level_args(params, items, &item_count, 32U)) {
        return false;
    }
    {
        char inherited_type_text[CHENG_V3_MAX_TYPE_TEXT];
        bool inherited_is_var = false;
        inherited_type_text[0] = '\0';
        for (i = item_count; i-- > 0U;) {
            char item_copy[4096];
            char name_text[128];
            char type_text[4096];
            char type_without_default[4096];
            char value_type_text[4096];
            char *colon;
            bool param_is_var = false;
            snprintf(item_copy, sizeof(item_copy), "%s", items[i]);
            colon = strchr(item_copy, ':');
            if (item_count > 32U) {
                return false;
            }
            name_text[0] = '\0';
            type_text[0] = '\0';
            value_type_text[0] = '\0';
            if (colon) {
                *colon = '\0';
                snprintf(name_text, sizeof(name_text), "%s", item_copy);
                snprintf(type_text, sizeof(type_text), "%s", colon + 1);
                v3_strip_param_default_expr_text(type_text,
                                                 type_without_default,
                                                 sizeof(type_without_default));
                param_is_var = v3_strip_var_type_text(type_without_default,
                                                      value_type_text,
                                                      sizeof(value_type_text));
                if (param_is_var) {
                    v3_trim_copy_text(value_type_text,
                                      inherited_type_text,
                                      sizeof(inherited_type_text));
                } else {
                    v3_trim_copy_text(type_without_default,
                                      inherited_type_text,
                                      sizeof(inherited_type_text));
                }
                inherited_is_var = param_is_var;
            } else {
                snprintf(name_text, sizeof(name_text), "%s", item_copy);
                if (inherited_type_text[0] != '\0') {
                    snprintf(type_text, sizeof(type_text), "%s", inherited_type_text);
                    param_is_var = inherited_is_var;
                } else {
                    snprintf(type_text, sizeof(type_text), "%s", "unknown");
                }
            }
            v3_trim_copy_text(name_text,
                              function->param_names[i],
                              sizeof(function->param_names[i]));
            function->param_is_var[i] = param_is_var;
            v3_trim_copy_text(param_is_var ? value_type_text : type_text,
                              function->param_types[i],
                              sizeof(function->param_types[i]));
            snprintf(function->param_abi_classes[i],
                     sizeof(function->param_abi_classes[i]),
                     "%s",
                     v3_type_abi_class(function->param_types[i]));
        }
        function->param_count = item_count;
    }
    return_part[0] = '\0';
    snprintf(return_part, sizeof(return_part), "%.*s", (int)(body_sep - close_paren - 1), close_paren + 1);
    v3_trim_copy_text(return_part, return_part, sizeof(return_part));
    if (return_part[0] != '\0' && return_part[0] == ':') {
        memmove(return_part, return_part + 1, strlen(return_part));
        v3_trim_copy_text(return_part, function->return_type, sizeof(function->return_type));
    }
    if (function->return_type[0] == '\0') {
        snprintf(function->return_type, sizeof(function->return_type), "%s", "void");
    }
    snprintf(function->return_abi_class,
             sizeof(function->return_abi_class),
             "%s",
             v3_type_abi_class(function->return_type));
    return true;
}

static void v3_lowered_function_abi_summary(const V3LoweredFunctionStub *function,
                                            char *out,
                                            size_t cap) {
    size_t i;
    if (cap <= 1U) {
        return;
    }
    out[0] = '\0';
    snprintf(out + strlen(out),
             cap - strlen(out),
             "ret=%s/%s params=",
             function->return_type[0] != '\0' ? function->return_type : "void",
             function->return_abi_class[0] != '\0' ? function->return_abi_class : "void");
    if (function->param_count <= 0U) {
        strncat(out, "-", cap - strlen(out) - 1U);
        return;
    }
    for (i = 0; i < function->param_count; ++i) {
        char chunk[256];
        if (i > 0U) {
            strncat(out, ";", cap - strlen(out) - 1U);
        }
        snprintf(chunk,
                 sizeof(chunk),
                 "%s:%s%s/%s",
                 function->param_names[i][0] != '\0' ? function->param_names[i] : "_",
                 function->param_is_var[i] ? "var " : "",
                 function->param_types[i][0] != '\0' ? function->param_types[i] : "unknown",
                 function->param_abi_classes[i][0] != '\0' ? function->param_abi_classes[i] : "unknown");
        strncat(out, chunk, cap - strlen(out) - 1U);
    }
}

static bool v3_collect_function_signature(char **lines,
                                          size_t line_count,
                                          size_t fn_line_index,
                                          char *signature_out,
                                          size_t signature_cap,
                                          size_t *signature_end_index_out) {
    size_t i;
    signature_out[0] = '\0';
    for (i = fn_line_index; i < line_count; ++i) {
        char line_copy[4096];
        char *trimmed;
        snprintf(line_copy, sizeof(line_copy), "%s", lines[i]);
        trimmed = v3_trim_inplace(line_copy);
        if (*trimmed == '\0') {
            continue;
        }
        if (i > fn_line_index && v3_is_top_level_decl_start(trimmed)) {
            return false;
        }
        if (signature_out[0] != '\0') {
            snprintf(signature_out + strlen(signature_out),
                     signature_cap - strlen(signature_out),
                     " ");
        }
        snprintf(signature_out + strlen(signature_out),
                 signature_cap - strlen(signature_out),
                 "%s",
                 trimmed);
        if (strchr(trimmed, '=') != NULL) {
            *signature_end_index_out = i;
            return true;
        }
        if (v3_signature_text_complete(signature_out)) {
            size_t lookahead = i + 1U;
            bool continue_signature = false;
            bool has_block_body = false;
            while (lookahead < line_count) {
                char lookahead_copy[4096];
                char *lookahead_trimmed;
                snprintf(lookahead_copy, sizeof(lookahead_copy), "%s", lines[lookahead]);
                lookahead_trimmed = v3_trim_inplace(lookahead_copy);
                if (*lookahead_trimmed == '\0' || *lookahead_trimmed == '#') {
                    lookahead += 1U;
                    continue;
                }
                if (lookahead_trimmed[0] == '=' || lookahead_trimmed[0] == ':') {
                    continue_signature = true;
                } else if (v3_line_indent(lines[lookahead]) > 0) {
                    has_block_body = true;
                }
                break;
            }
            if (continue_signature) {
                continue;
            }
            if (has_block_body) {
                *signature_end_index_out = i;
                return true;
            }
            return false;
        }
    }
    return false;
}

static bool v3_load_function_source_lines(const V3LoweredFunctionStub *function,
                                          char ***lines_out,
                                          char **owned_out,
                                          size_t *line_count_out,
                                          size_t *fn_line_index_out,
                                          char param_names[][128],
                                          size_t *param_count_out,
                                          char owned_path[PATH_MAX]) {
    char *owned;
    char *cursor;
    char *line;
    char **lines;
    size_t line_count = 0U;
    size_t fn_line_index = 0U;
    bool found = false;
    bool prefer_signature_line = false;
    size_t i;
    size_t pass = 0U;
    char source_lookup_name[128];
    const char *specialized_suffix;
    snprintf(source_lookup_name, sizeof(source_lookup_name), "%s", function->function_name);
    specialized_suffix = strstr(source_lookup_name, "$g");
    if (function->generic_binding_count > 0U && specialized_suffix != NULL) {
        source_lookup_name[(size_t)(specialized_suffix - source_lookup_name)] = '\0';
    }
    v3_copy_text(owned_path, PATH_MAX, function->source_path);
    owned = v3_read_file(function->source_path);
    if (!owned) {
        return false;
    }
    lines = (char **)v3_xmalloc(sizeof(char *) * CHENG_V3_MAX_SOURCE_LINES);
    cursor = owned;
    while ((line = strsep(&cursor, "\n")) != NULL) {
        if (line_count >= CHENG_V3_MAX_SOURCE_LINES) {
            free(lines);
            free(owned);
            return false;
        }
        lines[line_count] = line;
        line_count += 1U;
    }
    prefer_signature_line =
        function->signature_line_number > 0U && function->signature_line_number <= line_count;
    for (pass = 0U; pass < (prefer_signature_line ? 2U : 1U) && !found; ++pass) {
        for (i = 0; i < line_count; ++i) {
            char header_copy[4096];
            char function_name[128];
            if (prefer_signature_line) {
                if (pass == 0U) {
                    if (i + 1U != function->signature_line_number) {
                        continue;
                    }
                } else if (i + 1U == function->signature_line_number) {
                    continue;
                }
            }
            snprintf(header_copy, sizeof(header_copy), "%s", lines[i]);
            if (v3_lowering_function_name_from_line(v3_trim_inplace(header_copy),
                                                    function_name,
                                                    sizeof(function_name)) &&
                strcmp(function_name, source_lookup_name) == 0) {
                char signature_text[8192];
                size_t signature_end_index = i;
                found = true;
                if (!v3_collect_function_signature(lines,
                                                   line_count,
                                                   i,
                                                   signature_text,
                                                   sizeof(signature_text),
                                                   &signature_end_index) ||
                    !v3_const_parse_function_signature(signature_text,
                                                      source_lookup_name,
                                                      param_names,
                                                      param_count_out,
                                                      CHENG_V3_CONST_MAX_BINDINGS)) {
                    found = false;
                    continue;
                }
                fn_line_index = signature_end_index;
                break;
            }
        }
    }
    if (!found) {
        free(lines);
        free(owned);
        return false;
    }
    *lines_out = lines;
    *owned_out = owned;
    *line_count_out = line_count;
    *fn_line_index_out = fn_line_index;
    return true;
}

static bool v3_const_build_source_function_stub(const V3LoweredFunctionStub *current_function,
                                                const char *symbol_text,
                                                V3LoweredFunctionStub *out) {
    char symbol_copy[PATH_MAX];
    char *sep;
    char **lines = NULL;
    char *owned_lines = NULL;
    size_t line_count = 0U;
    size_t fn_line_index = 0U;
    char param_names[CHENG_V3_CONST_MAX_BINDINGS][128];
    size_t param_count = 0U;
    char owned_path[PATH_MAX];
    if (current_function == NULL ||
        symbol_text == NULL ||
        symbol_text[0] == '\0' ||
        out == NULL ||
        current_function->source_path[0] == '\0' ||
        current_function->owner_module_path[0] == '\0') {
        return false;
    }
    snprintf(symbol_copy, sizeof(symbol_copy), "%s", symbol_text);
    sep = strstr(symbol_copy, "::");
    if (sep == NULL) {
        return false;
    }
    *sep = '\0';
    if (strcmp(symbol_copy, current_function->owner_module_path) != 0) {
        return false;
    }
    memset(out, 0, sizeof(*out));
    snprintf(out->source_path, sizeof(out->source_path), "%s", current_function->source_path);
    snprintf(out->owner_module_path, sizeof(out->owner_module_path), "%s", current_function->owner_module_path);
    snprintf(out->function_name, sizeof(out->function_name), "%s", sep + 2);
    snprintf(out->symbol_text, sizeof(out->symbol_text), "%s", symbol_text);
    if (!v3_load_function_source_lines(out,
                                       &lines,
                                       &owned_lines,
                                       &line_count,
                                       &fn_line_index,
                                       param_names,
                                       &param_count,
                                       owned_path)) {
        return false;
    }
    free(lines);
    free(owned_lines);
    return true;
}

static bool v3_const_eval_expr(const V3LoweringPlanStub *lowering,
                               const V3LoweredFunctionStub *current_function,
                               const V3ImportAlias *aliases,
                               size_t alias_count,
                               V3ConstEnv *env,
                               const char *expr_text,
                               V3ConstValue *out,
                               size_t depth);

static bool v3_const_eval_function(const V3LoweringPlanStub *lowering,
                                   const V3LoweredFunctionStub *function,
                                   const V3ConstValue *args,
                                   size_t arg_count,
                                   V3ConstValue *out,
                                   V3ConstEntryProgram *entry_program,
                                   size_t depth);

static bool v3_find_type_def_by_normalized(const V3LoweringPlanStub *lowering,
                                           const char *normalized_type,
                                           int32_t *index_out);
static void v3_init_default_eval_function(const V3LoweredFunctionStub *current_function,
                                          const char *owner_module_path,
                                          const char *source_path,
                                          V3LoweredFunctionStub *out);
static bool v3_expr_is_stable_field_default(const char *expr_text,
                                            const V3TypeFieldDef *fields,
                                            size_t field_count);
static bool v3_parse_default_expr_text(const char *expr_text,
                                       char *type_out,
                                       size_t type_cap);
static bool v3_parse_named_arg_meta(const char *arg_text,
                                    char *field_out,
                                    size_t field_cap,
                                    char *expr_out,
                                    size_t expr_cap);
static bool v3_tuple_args_cover_all_items(const char *tuple_type_text,
                                          const char args[][4096],
                                          size_t arg_count);
static bool v3_tuple_binding_expr_omits_items(const char *statement);

static bool v3_const_value_set_empty_seq(V3ConstValue *out) {
    V3ConstRecord *record = (V3ConstRecord *)v3_xmalloc(sizeof(V3ConstRecord));
    memset(record, 0, sizeof(*record));
    v3_const_value_init(&record->field_values[0]);
    v3_const_value_init(&record->field_values[1]);
    v3_const_value_init(&record->field_values[2]);
    snprintf(record->field_names[0], sizeof(record->field_names[0]), "%s", "len");
    snprintf(record->field_names[1], sizeof(record->field_names[1]), "%s", "cap");
    snprintf(record->field_names[2], sizeof(record->field_names[2]), "%s", "buffer");
    if (!v3_const_value_set_i32(&record->field_values[0], 0) ||
        !v3_const_value_set_i32(&record->field_values[1], 0) ||
        !v3_const_value_set_str_kind(&record->field_values[2], V3_CONST_SYMBOL, "nil")) {
        v3_const_record_free(record);
        return false;
    }
    record->field_count = 3U;
    v3_const_value_free(out);
    out->kind = V3_CONST_RECORD;
    out->record_value = record;
    return true;
}

static bool v3_const_record_set_field_value(V3ConstValue *record_value,
                                            const char *field_name,
                                            const V3ConstValue *field_value) {
    size_t i;
    if (record_value == NULL ||
        record_value->kind != V3_CONST_RECORD ||
        record_value->record_value == NULL ||
        field_name == NULL ||
        field_name[0] == '\0' ||
        field_value == NULL) {
        return false;
    }
    for (i = 0U; i < record_value->record_value->field_count; ++i) {
        if (strcmp(record_value->record_value->field_names[i], field_name) == 0) {
            return v3_const_value_clone(field_value,
                                        &record_value->record_value->field_values[i]);
        }
    }
    return false;
}

static bool v3_const_value_set_implicit_default(const V3LoweringPlanStub *lowering,
                                                const V3LoweredFunctionStub *current_function,
                                                const V3ImportAlias *aliases,
                                                size_t alias_count,
                                                const char *type_owner_module_path,
                                                const char *type_source_path,
                                                const char *type_text,
                                                V3ConstEnv *env,
                                                V3ConstValue *out,
                                                size_t depth) {
    char owner_module[PATH_MAX];
    char source_path[PATH_MAX];
    char normalized_type[CHENG_V3_MAX_TYPE_TEXT];
    int32_t type_index = -1;
    if (lowering == NULL ||
        type_text == NULL ||
        type_text[0] == '\0' ||
        out == NULL ||
        depth > 32U) {
        return false;
    }
    if (type_owner_module_path != NULL && type_owner_module_path[0] != '\0') {
        snprintf(owner_module, sizeof(owner_module), "%s", type_owner_module_path);
    } else if (current_function != NULL && current_function->owner_module_path[0] != '\0') {
        snprintf(owner_module, sizeof(owner_module), "%s", current_function->owner_module_path);
    } else {
        owner_module[0] = '\0';
    }
    if (type_source_path != NULL && type_source_path[0] != '\0') {
        snprintf(source_path, sizeof(source_path), "%s", type_source_path);
    } else if (current_function != NULL && current_function->source_path[0] != '\0') {
        snprintf(source_path, sizeof(source_path), "%s", current_function->source_path);
    } else {
        source_path[0] = '\0';
    }
    if (owner_module[0] == '\0' ||
        !v3_normalize_type_text(lowering,
                                owner_module,
                                aliases,
                                alias_count,
                                type_text,
                                normalized_type,
                                sizeof(normalized_type))) {
        return false;
    }
    if (strcmp(normalized_type, "bool") == 0) {
        return v3_const_value_set_bool(out, false);
    }
    if (strcmp(normalized_type, "int8") == 0 ||
        strcmp(normalized_type, "uint8") == 0 ||
        strcmp(normalized_type, "int16") == 0 ||
        strcmp(normalized_type, "uint16") == 0 ||
        strcmp(normalized_type, "int32") == 0 ||
        strcmp(normalized_type, "int") == 0 ||
        strcmp(normalized_type, "uint32") == 0 ||
        strcmp(normalized_type, "char") == 0 ||
        strcmp(normalized_type, "int64") == 0 ||
        strcmp(normalized_type, "uint64") == 0 ||
        strcmp(normalized_type, "enum") == 0) {
        return v3_const_value_set_i32(out, 0);
    }
    if (strcmp(normalized_type, "float") == 0 ||
        strcmp(normalized_type, "float32") == 0 ||
        strcmp(normalized_type, "float64") == 0) {
        return v3_const_value_set_f64(out, 0.0);
    }
    if (strcmp(normalized_type, "str") == 0 ||
        strcmp(normalized_type, "cstring") == 0) {
        return v3_const_value_set_str_kind(out, V3_CONST_STR, "");
    }
    if (strcmp(normalized_type, "ptr") == 0) {
        return v3_const_value_set_str_kind(out, V3_CONST_SYMBOL, "nil");
    }
    if (strlen(normalized_type) > 2U &&
        strcmp(normalized_type + strlen(normalized_type) - 2U, "[]") == 0) {
        return v3_const_value_set_empty_seq(out);
    }
    if (v3_is_tuple_type_text(normalized_type)) {
        char (*items)[4096] = NULL;
        V3TypeFieldDef *fields = NULL;
        V3ConstRecord *record = NULL;
        size_t item_count = 0U;
        size_t i;
        bool ok = false;
        items = (char (*)[4096])v3_xmalloc(sizeof(*items) * CHENG_V3_MAX_TYPE_FIELDS);
        fields = (V3TypeFieldDef *)v3_xmalloc(sizeof(*fields) * CHENG_V3_MAX_TYPE_FIELDS);
        if (!v3_parse_tuple_type_items(normalized_type,
                                       items,
                                       &item_count,
                                       CHENG_V3_MAX_TYPE_FIELDS)) {
            goto const_tuple_cleanup;
        }
        record = (V3ConstRecord *)v3_xmalloc(sizeof(V3ConstRecord));
        memset(record, 0, sizeof(*record));
        for (i = 0U; i < item_count; ++i) {
            V3ConstValue field_value;
            V3LoweredFunctionStub eval_function;
            if (!v3_parse_type_field_spec(items[i], true, &fields[i])) {
                goto const_tuple_cleanup;
            }
            if (!fields[i].has_explicit_name) {
                v3_tuple_field_synthetic_name(i, fields[i].name, sizeof(fields[i].name));
            }
            v3_const_value_init(&field_value);
            if (!v3_const_value_set_implicit_default(lowering,
                                                     current_function,
                                                     aliases,
                                                     alias_count,
                                                     owner_module,
                                                     source_path,
                                                     fields[i].type_text,
                                                     env,
                                                     &field_value,
                                                     depth + 1U)) {
                v3_const_value_free(&field_value);
                goto const_tuple_cleanup;
            }
            if (fields[i].has_default_expr) {
                if (!v3_expr_is_stable_field_default(fields[i].default_expr, fields, item_count)) {
                    v3_const_value_free(&field_value);
                    goto const_tuple_cleanup;
                }
                v3_init_default_eval_function(current_function,
                                              owner_module,
                                              source_path,
                                              &eval_function);
                if (!v3_const_eval_expr(lowering,
                                        &eval_function,
                                        aliases,
                                        alias_count,
                                        env,
                                        fields[i].default_expr,
                                        &field_value,
                                        depth + 1U)) {
                    v3_const_value_free(&field_value);
                    goto const_tuple_cleanup;
                }
            }
            snprintf(record->field_names[record->field_count],
                     sizeof(record->field_names[record->field_count]),
                     "%s",
                     fields[i].name);
            if (!v3_const_value_clone(&field_value,
                                      &record->field_values[record->field_count])) {
                v3_const_value_free(&field_value);
                goto const_tuple_cleanup;
            }
            v3_const_value_free(&field_value);
            record->field_count += 1U;
        }
        v3_const_value_free(out);
        out->kind = V3_CONST_RECORD;
        out->record_value = record;
        record = NULL;
        ok = true;
const_tuple_cleanup:
        v3_const_record_free(record);
        free(fields);
        free(items);
        return ok;
    }
    if (v3_find_type_def_by_normalized(lowering, normalized_type, &type_index)) {
        const V3TypeDefStub *type_def = &lowering->type_defs[type_index];
        char stripped[PATH_MAX];
        char type_owner[PATH_MAX];
        char type_instance_name[128];
        char type_name[128];
        char type_args[CHENG_V3_MAX_TYPE_PARAMS][256];
        size_t type_arg_count = 0U;
        V3ImportAlias *type_aliases = NULL;
        size_t type_alias_count = 0U;
        v3_strip_internal_ref_type(normalized_type, stripped, sizeof(stripped));
        if (!v3_split_qualified_type_text(stripped,
                                          type_owner,
                                          sizeof(type_owner),
                                          type_instance_name,
                                          sizeof(type_instance_name)) ||
            !v3_parse_type_instance_text(type_instance_name,
                                         type_name,
                                         sizeof(type_name),
                                         type_args,
                                         &type_arg_count,
                                         CHENG_V3_MAX_TYPE_PARAMS)) {
            return false;
        }
        type_aliases = (V3ImportAlias *)v3_xmalloc(sizeof(*type_aliases) * CHENG_V3_MAX_IMPORT_ALIASES);
        if (type_def->kind == V3_TYPE_DEF_ALIAS) {
            char alias_target[CHENG_V3_MAX_TYPE_TEXT];
            if (!v3_load_import_aliases_from_source_path(type_def->source_path,
                                                         type_aliases,
                                                         &type_alias_count,
                                                         CHENG_V3_MAX_IMPORT_ALIASES)) {
                free(type_aliases);
                return false;
            }
            if (type_def->generic_param_count > 0U) {
                if (type_def->generic_param_count != type_arg_count ||
                    !v3_normalize_type_text_with_params(lowering,
                                                        type_def->owner_module_path,
                                                        type_aliases,
                                                        type_alias_count,
                                                        type_def->generic_param_names,
                                                        type_args,
                                                        type_arg_count,
                                                        type_def->alias_target,
                                                        alias_target,
                                                        sizeof(alias_target))) {
                    free(type_aliases);
                    return false;
                }
            } else {
                snprintf(alias_target, sizeof(alias_target), "%s", type_def->alias_target);
            }
            {
                bool ok = v3_const_value_set_implicit_default(lowering,
                                                              current_function,
                                                              type_aliases,
                                                              type_alias_count,
                                                              type_def->owner_module_path,
                                                              type_def->source_path,
                                                              alias_target,
                                                              env,
                                                              out,
                                                              depth + 1U);
                free(type_aliases);
                return ok;
            }
        }
        if (type_def->kind == V3_TYPE_DEF_RECORD) {
            V3ConstRecord *record = (V3ConstRecord *)v3_xmalloc(sizeof(V3ConstRecord));
            memset(record, 0, sizeof(*record));
            size_t i;
            if (!v3_load_import_aliases_from_source_path(type_def->source_path,
                                                         type_aliases,
                                                         &type_alias_count,
                                                         CHENG_V3_MAX_IMPORT_ALIASES)) {
                free(type_aliases);
                v3_const_record_free(record);
                return false;
            }
            for (i = 0U; i < type_def->field_count; ++i) {
                char resolved_field_type[CHENG_V3_MAX_TYPE_TEXT];
                V3ConstValue field_value;
                V3LoweredFunctionStub eval_function;
                v3_const_value_init(&field_value);
                if (type_def->generic_param_count > 0U) {
                    if (type_def->generic_param_count != type_arg_count ||
                        !v3_normalize_type_text_with_params(lowering,
                                                            type_def->owner_module_path,
                                                            type_aliases,
                                                            type_alias_count,
                                                            type_def->generic_param_names,
                                                            type_args,
                                                            type_arg_count,
                                                            type_def->fields[i].type_text,
                                                            resolved_field_type,
                                                            sizeof(resolved_field_type))) {
                        free(type_aliases);
                        v3_const_value_free(&field_value);
                        v3_const_record_free(record);
                        return false;
                    }
                } else {
                    snprintf(resolved_field_type,
                             sizeof(resolved_field_type),
                             "%s",
                             type_def->fields[i].type_text);
                }
                if (!v3_const_value_set_implicit_default(lowering,
                                                         current_function,
                                                         type_aliases,
                                                         type_alias_count,
                                                         type_def->owner_module_path,
                                                         type_def->source_path,
                                                         resolved_field_type,
                                                         env,
                                                         &field_value,
                                                         depth + 1U)) {
                    free(type_aliases);
                    v3_const_value_free(&field_value);
                    v3_const_record_free(record);
                    return false;
                }
                if (type_def->fields[i].has_default_expr) {
                    if (!v3_expr_is_stable_field_default(type_def->fields[i].default_expr,
                                                         type_def->fields,
                                                         type_def->field_count)) {
                        free(type_aliases);
                        v3_const_value_free(&field_value);
                        v3_const_record_free(record);
                        return false;
                    }
                    v3_init_default_eval_function(current_function,
                                                  type_def->owner_module_path,
                                                  type_def->source_path,
                                                  &eval_function);
                    if (!v3_const_eval_expr(lowering,
                                            &eval_function,
                                            type_aliases,
                                            type_alias_count,
                                            env,
                                            type_def->fields[i].default_expr,
                                            &field_value,
                                            depth + 1U)) {
                        free(type_aliases);
                        v3_const_value_free(&field_value);
                        v3_const_record_free(record);
                        return false;
                    }
                }
                snprintf(record->field_names[record->field_count],
                         sizeof(record->field_names[record->field_count]),
                         "%s",
                         type_def->fields[i].name);
                if (!v3_const_value_clone(&field_value,
                                          &record->field_values[record->field_count])) {
                    free(type_aliases);
                    v3_const_value_free(&field_value);
                    v3_const_record_free(record);
                    return false;
                }
                v3_const_value_free(&field_value);
                record->field_count += 1U;
            }
            v3_const_value_free(out);
            out->kind = V3_CONST_RECORD;
            out->record_value = record;
            free(type_aliases);
            return true;
        }
        free(type_aliases);
    }
    if (v3_is_internal_ref_type(normalized_type)) {
        char inner[CHENG_V3_MAX_TYPE_TEXT];
        v3_strip_internal_ref_type(normalized_type, inner, sizeof(inner));
        return v3_const_value_set_implicit_default(lowering,
                                                   current_function,
                                                   aliases,
                                                   alias_count,
                                                   owner_module,
                                                   source_path,
                                                   inner,
                                                   env,
                                                   out,
                                                   depth + 1U);
    }
    return false;
}

static bool v3_const_value_eq(const V3ConstValue *lhs,
                              const V3ConstValue *rhs,
                              bool *out) {
    double lhs_f64 = 0.0;
    double rhs_f64 = 0.0;
    if (lhs->kind != rhs->kind) {
        if (v3_const_value_as_f64(lhs, &lhs_f64) &&
            v3_const_value_as_f64(rhs, &rhs_f64)) {
            *out = lhs_f64 == rhs_f64;
            return true;
        }
        *out = false;
        return true;
    }
    switch (lhs->kind) {
        case V3_CONST_BOOL:
            *out = lhs->bool_value == rhs->bool_value;
            return true;
        case V3_CONST_I32:
            *out = lhs->i32_value == rhs->i32_value;
            return true;
        case V3_CONST_F64:
            *out = lhs->f64_value == rhs->f64_value;
            return true;
        case V3_CONST_STR:
        case V3_CONST_SYMBOL:
            *out = strcmp(lhs->text_value ? lhs->text_value : "",
                          rhs->text_value ? rhs->text_value : "") == 0;
            return true;
        default:
            return false;
    }
}

static bool v3_const_record_new_from_named_args(const char *callee,
                                                const char args[][4096],
                                                size_t arg_count,
                                                const V3LoweringPlanStub *lowering,
                                                const V3LoweredFunctionStub *current_function,
                                                const V3ImportAlias *aliases,
                                                size_t alias_count,
                                                V3ConstEnv *env,
                                                V3ConstValue *out,
                                                size_t depth) {
    size_t i;
    char seen_fields[CHENG_V3_CONST_MAX_FIELDS][128];
    size_t seen_count = 0U;
    if (callee == NULL || callee[0] == '\0' || current_function == NULL) {
        return false;
    }
    if (v3_is_tuple_type_text(callee) &&
        !v3_tuple_args_cover_all_items(callee, args, arg_count)) {
        return false;
    }
    if (!v3_const_value_set_implicit_default(lowering,
                                             current_function,
                                             aliases,
                                             alias_count,
                                             current_function->owner_module_path,
                                             current_function->source_path,
                                             callee,
                                             env,
                                             out,
                                             depth + 1U) ||
        out->kind != V3_CONST_RECORD ||
        out->record_value == NULL) {
        return false;
    }
    for (i = 0; i < arg_count; ++i) {
        char field_name[128];
        char expr[4096];
        V3ConstValue field_value;
        size_t seen_index;
        if (!v3_parse_named_arg_meta(args[i],
                                     field_name,
                                     sizeof(field_name),
                                     expr,
                                     sizeof(expr))) {
            return false;
        }
        for (seen_index = 0U; seen_index < seen_count; ++seen_index) {
            if (strcmp(seen_fields[seen_index], field_name) == 0) {
                return false;
            }
        }
        v3_const_value_init(&field_value);
        if (!v3_const_eval_expr(lowering,
                                current_function,
                                aliases,
                                alias_count,
                                env,
                                expr,
                                &field_value,
                                depth + 1U) ||
            !v3_const_record_set_field_value(out, field_name, &field_value)) {
            v3_const_value_free(&field_value);
            return false;
        }
        v3_const_value_free(&field_value);
        if (seen_count >= CHENG_V3_CONST_MAX_FIELDS) {
            return false;
        }
        snprintf(seen_fields[seen_count], sizeof(seen_fields[seen_count]), "%s", field_name);
        seen_count += 1U;
    }
    return true;
}

static bool v3_const_eval_intrinsic_call(const char *callee,
                                         const char args[][4096],
                                         size_t arg_count,
                                         const V3LoweringPlanStub *lowering,
                                         const V3LoweredFunctionStub *current_function,
                                         const V3ImportAlias *aliases,
                                         size_t alias_count,
                                         V3ConstEnv *env,
                                         V3ConstValue *out,
                                         size_t depth) {
    V3ConstValue arg0;
    V3ConstValue arg1;
    char buffer[PATH_MAX];
    int32_t arg0_i32 = 0;
    bool arg0_truthy = false;
    v3_const_value_init(&arg0);
    v3_const_value_init(&arg1);
    if (strcmp(callee, "len") == 0 && arg_count == 1U) {
        if (!v3_const_eval_expr(lowering, current_function, aliases, alias_count, env, args[0], &arg0, depth + 1U)) {
            goto fail;
        }
        if (arg0.kind != V3_CONST_STR) {
            goto fail;
        }
        v3_const_value_set_i32(out, (int32_t)strlen(arg0.text_value ? arg0.text_value : ""));
        goto ok;
    }
    if (strcmp(callee, "int32") == 0 && arg_count == 1U) {
        if (!v3_const_eval_expr(lowering, current_function, aliases, alias_count, env, args[0], &arg0, depth + 1U)) {
            goto fail;
        }
        if (v3_const_value_as_i32(&arg0, &arg0_i32)) {
            v3_const_value_set_i32(out, arg0_i32);
            goto ok;
        }
        goto fail;
    }
    if ((strcmp(callee, "int64") == 0 ||
         strcmp(callee, "uint32") == 0 ||
         strcmp(callee, "uint64") == 0) && arg_count == 1U) {
        if (!v3_const_eval_expr(lowering, current_function, aliases, alias_count, env, args[0], &arg0, depth + 1U) ||
            !v3_const_value_as_i32(&arg0, &arg0_i32)) {
            goto fail;
        }
        v3_const_value_set_i32(out, arg0_i32);
        goto ok;
    }
    if ((strcmp(callee, "float32") == 0 ||
         strcmp(callee, "float64") == 0) && arg_count == 1U) {
        double arg0_f64 = 0.0;
        if (!v3_const_eval_expr(lowering, current_function, aliases, alias_count, env, args[0], &arg0, depth + 1U) ||
            !v3_const_value_as_f64(&arg0, &arg0_f64)) {
            goto fail;
        }
        if (strcmp(callee, "float32") == 0) {
            arg0_f64 = (double)((float)arg0_f64);
        }
        v3_const_value_set_f64(out, arg0_f64);
        goto ok;
    }
    if (strcmp(callee, "bool") == 0 && arg_count == 1U) {
        if (!v3_const_eval_expr(lowering, current_function, aliases, alias_count, env, args[0], &arg0, depth + 1U) ||
            !v3_const_value_truthy(&arg0, &arg0_truthy)) {
            goto fail;
        }
        v3_const_value_set_bool(out, arg0_truthy);
        goto ok;
    }
    if (strcmp(callee, "std/strutils::strip") == 0 && arg_count == 1U) {
        if (!v3_const_eval_expr(lowering, current_function, aliases, alias_count, env, args[0], &arg0, depth + 1U)) {
            goto fail;
        }
        if (arg0.kind != V3_CONST_STR) {
            goto fail;
        }
        v3_const_str_strip_ascii(arg0.text_value ? arg0.text_value : "", buffer, sizeof(buffer));
        v3_const_value_set_str_kind(out, V3_CONST_STR, buffer);
        goto ok;
    }
    if (strcmp(callee, "std/os::isAbsolute") == 0 && arg_count == 1U) {
        if (!v3_const_eval_expr(lowering, current_function, aliases, alias_count, env, args[0], &arg0, depth + 1U)) {
            goto fail;
        }
        if (arg0.kind != V3_CONST_STR) {
            goto fail;
        }
        v3_const_value_set_bool(out, v3_const_os_is_absolute(arg0.text_value ? arg0.text_value : ""));
        goto ok;
    }
    if (strcmp(callee, "std/os::joinPath") == 0 && arg_count == 2U) {
        if (!v3_const_eval_expr(lowering, current_function, aliases, alias_count, env, args[0], &arg0, depth + 1U) ||
            !v3_const_eval_expr(lowering, current_function, aliases, alias_count, env, args[1], &arg1, depth + 1U)) {
            goto fail;
        }
        if (arg0.kind != V3_CONST_STR || arg1.kind != V3_CONST_STR) {
            goto fail;
        }
        v3_const_os_join_path(arg0.text_value ? arg0.text_value : "",
                              arg1.text_value ? arg1.text_value : "",
                              buffer,
                              sizeof(buffer));
        v3_const_value_set_str_kind(out, V3_CONST_STR, buffer);
        goto ok;
    }
    if (strcmp(callee, "std/os::getEnvDefault") == 0 && arg_count == 2U) {
        const char *env_value;
        if (!v3_const_eval_expr(lowering, current_function, aliases, alias_count, env, args[0], &arg0, depth + 1U) ||
            !v3_const_eval_expr(lowering, current_function, aliases, alias_count, env, args[1], &arg1, depth + 1U)) {
            goto fail;
        }
        if (arg0.kind != V3_CONST_STR || arg1.kind != V3_CONST_STR) {
            goto fail;
        }
        env_value = getenv(arg0.text_value ? arg0.text_value : "");
        v3_const_value_set_str_kind(out,
                                    V3_CONST_STR,
                                    env_value != NULL ? env_value
                                                      : (arg1.text_value ? arg1.text_value : ""));
        goto ok;
    }
    if (strcmp(callee, "std/os::fileExists") == 0 && arg_count == 1U) {
        struct stat st;
        if (!v3_const_eval_expr(lowering, current_function, aliases, alias_count, env, args[0], &arg0, depth + 1U)) {
            goto fail;
        }
        if (arg0.kind != V3_CONST_STR) {
            goto fail;
        }
        v3_const_value_set_bool(out,
                                stat(arg0.text_value ? arg0.text_value : "", &st) == 0 &&
                                    !S_ISDIR(st.st_mode));
        goto ok;
    }
fail:
    v3_const_value_free(&arg0);
    v3_const_value_free(&arg1);
    return false;
ok:
    v3_const_value_free(&arg0);
    v3_const_value_free(&arg1);
    return true;
}

static bool v3_consteval_source_callee_is_importc_puts(const char *source_path,
                                                       const char *callee) {
    char *owned = NULL;
    char *cursor = NULL;
    char pending_symbol[128];
    if (source_path == NULL || source_path[0] == '\0' ||
        callee == NULL || callee[0] == '\0') {
        return false;
    }
    owned = v3_read_file(source_path);
    if (owned == NULL) {
        return false;
    }
    pending_symbol[0] = '\0';
    cursor = owned;
    while (cursor != NULL) {
        char *line = strsep(&cursor, "\n");
        char line_copy[4096];
        char function_name[128];
        char *trimmed;
        if (line == NULL) {
            break;
        }
        snprintf(line_copy, sizeof(line_copy), "%s", line);
        trimmed = v3_trim_inplace(line_copy);
        if (*trimmed == '\0') {
            continue;
        }
        if (v3_importc_symbol_from_annotation(trimmed, pending_symbol, sizeof(pending_symbol))) {
            continue;
        }
        if (pending_symbol[0] != '\0' && v3_is_annotation_line(trimmed)) {
            continue;
        }
        function_name[0] = '\0';
        if (v3_importc_function_name_from_line(trimmed, function_name, sizeof(function_name))) {
            if (strcmp(function_name, callee) == 0) {
                free(owned);
                return strcmp(function_name, "puts") == 0 ||
                       strcmp(function_name, "c_puts") == 0;
            }
            pending_symbol[0] = '\0';
            continue;
        }
        if (pending_symbol[0] != '\0' &&
            v3_lowering_function_name_from_line(trimmed, function_name, sizeof(function_name))) {
            if (strcmp(function_name, callee) == 0) {
                bool is_puts = strcmp(pending_symbol, "puts") == 0 ||
                               strcmp(pending_symbol, "c_puts") == 0;
                free(owned);
                return is_puts;
            }
            pending_symbol[0] = '\0';
            continue;
        }
        pending_symbol[0] = '\0';
    }
    free(owned);
    return false;
}

static bool v3_consteval_callee_is_puts_like(const V3LoweredFunctionStub *function,
                                             const char *callee) {
    if (callee == NULL || callee[0] == '\0') {
        return false;
    }
    if (strcmp(callee, "puts") == 0 || strcmp(callee, "c_puts") == 0) {
        return true;
    }
    if (function == NULL) {
        return false;
    }
    return v3_consteval_source_callee_is_importc_puts(function->source_path, callee);
}

static bool v3_const_resolve_call_symbol(const V3ImportAlias *aliases,
                                         size_t alias_count,
                                         const V3LoweredFunctionStub *current_function,
                                         const char *callee,
                                         char *symbol_text,
                                         size_t symbol_cap) {
    if (!symbol_text || symbol_cap == 0U) {
        return false;
    }
    symbol_text[0] = '\0';
    if (current_function == NULL || callee == NULL || callee[0] == '\0') {
        return false;
    }
    if (strstr(callee, "::") != NULL) {
        snprintf(symbol_text, symbol_cap, "%s", callee);
        return true;
    }
    if (strchr(callee, '.') != NULL) {
        char callee_copy[PATH_MAX];
        char *dot;
        const char *module_path;
        snprintf(callee_copy, sizeof(callee_copy), "%s", callee);
        dot = strchr(callee_copy, '.');
        *dot = '\0';
        module_path = v3_alias_module_path_exported_member(aliases,
                                                           alias_count,
                                                           current_function->owner_module_path,
                                                           callee_copy,
                                                           dot + 1,
                                                           sizeof(callee_copy) - (size_t)((dot + 1) - callee_copy));
        if (module_path == NULL) {
            return false;
        }
        snprintf(symbol_text, symbol_cap, "%s::%s", module_path, dot + 1);
        return true;
    }
    snprintf(symbol_text,
             symbol_cap,
             "%s::%s",
             current_function->owner_module_path,
             callee);
    return true;
}

static bool v3_const_split_binary_expr(const char *expr,
                                       const char *op,
                                       bool use_last,
                                       char *left_out,
                                       size_t left_cap,
                                       char *right_out,
                                       size_t right_cap) {
    int32_t op_index = use_last
                           ? v3_find_top_level_binary_op_last(expr, op)
                           : v3_find_top_level_binary_op(expr, op);
    size_t op_len = strlen(op);
    if (op_index <= 0) {
        return false;
    }
    snprintf(left_out, left_cap, "%.*s", op_index, expr);
    snprintf(right_out, right_cap, "%s", expr + op_index + op_len);
    v3_trim_copy_text(left_out, left_out, left_cap);
    v3_trim_copy_text(right_out, right_out, right_cap);
    return left_out[0] != '\0' && right_out[0] != '\0';
}

static bool v3_const_parse_i32_literal_text(const char *text, int32_t *out_value) {
    char *end = NULL;
    long parsed;
    if (text == NULL || text[0] == '\0') {
        return false;
    }
    if (!((text[0] >= '0' && text[0] <= '9') ||
          (text[0] == '-' && text[1] >= '0' && text[1] <= '9'))) {
        return false;
    }
    parsed = strtol(text, &end, 10);
    if (end == NULL || *end != '\0' || parsed < INT32_MIN || parsed > INT32_MAX) {
        return false;
    }
    if (out_value != NULL) {
        *out_value = (int32_t)parsed;
    }
    return true;
}

static bool v3_const_parse_f64_literal_text(const char *text, double *out_value) {
    char *end = NULL;
    double parsed;
    bool saw_float_marker = false;
    size_t i;
    if (text == NULL || text[0] == '\0') {
        return false;
    }
    for (i = 0; text[i] != '\0'; ++i) {
        if (text[i] == '.' || text[i] == 'e' || text[i] == 'E') {
            saw_float_marker = true;
            break;
        }
    }
    if (!saw_float_marker) {
        return false;
    }
    parsed = strtod(text, &end);
    if (end == NULL || *end != '\0') {
        return false;
    }
    if (out_value != NULL) {
        *out_value = parsed;
    }
    return true;
}

static bool v3_const_value_set_zero_for_type(const V3LoweringPlanStub *lowering,
                                             const V3LoweredFunctionStub *current_function,
                                             const V3ImportAlias *aliases,
                                             size_t alias_count,
                                             V3ConstEnv *env,
                                             const char *type_text,
                                             V3ConstValue *out,
                                             size_t depth) {
    if (current_function == NULL) {
        return false;
    }
    return v3_const_value_set_implicit_default(lowering,
                                               current_function,
                                               aliases,
                                               alias_count,
                                               current_function->owner_module_path,
                                               current_function->source_path,
                                               type_text,
                                               env,
                                               out,
                                               depth + 1U);
}

static bool v3_const_eval_expr(const V3LoweringPlanStub *lowering,
                               const V3LoweredFunctionStub *current_function,
                               const V3ImportAlias *aliases,
                               size_t alias_count,
                               V3ConstEnv *env,
                               const char *expr_text,
                               V3ConstValue *out,
                               size_t depth) {
    char expr[8192];
    char left[4096];
    char right[4096];
    char surface_error[256];
    char inner_type[CHENG_V3_MAX_TYPE_TEXT];
    char binary_op[4];
    int32_t literal_i32 = 0;
    double literal_f64 = 0.0;
    V3ExprSurface surface;
    if (depth > 64U) {
        return false;
    }
    v3_trim_copy_text(expr_text, expr, sizeof(expr));
    if (expr[0] == '\0') {
        return false;
    }
    if (!v3_classify_expr_surface(expr, &surface, surface_error, sizeof(surface_error))) {
        return false;
    }
    snprintf(expr, sizeof(expr), "%s", surface.text);
    if (surface.kind == V3_EXPR_SURFACE_IF || surface.kind == V3_EXPR_SURFACE_TERNARY) {
        V3ConstValue cond;
        bool cond_truthy = false;
        v3_const_value_init(&cond);
        if (!v3_const_eval_expr(lowering,
                                current_function,
                                aliases,
                                alias_count,
                                env,
                                surface.cond,
                                &cond,
                                depth + 1U) ||
            !v3_const_value_truthy(&cond, &cond_truthy)) {
            v3_const_value_free(&cond);
            return false;
        }
        v3_const_value_free(&cond);
        return v3_const_eval_expr(lowering,
                                  current_function,
                                  aliases,
                                  alias_count,
                                  env,
                                  cond_truthy ? surface.when_true : surface.when_false,
                                  out,
                                  depth + 1U);
    }
    if (expr[0] == '"' && expr[strlen(expr) - 1U] == '"') {
        char literal[8192];
        snprintf(literal, sizeof(literal), "%.*s", (int)(strlen(expr) - 2U), expr + 1);
        return v3_const_value_set_str_kind(out, V3_CONST_STR, literal);
    }
    if (strcmp(expr, "true") == 0) {
        return v3_const_value_set_bool(out, true);
    }
    if (strcmp(expr, "false") == 0) {
        return v3_const_value_set_bool(out, false);
    }
    if (strcmp(expr, "[]") == 0) {
        return v3_const_value_set_empty_seq(out);
    }
    if (v3_parse_default_expr_text(expr, inner_type, sizeof(inner_type))) {
        if (current_function == NULL) {
            return false;
        }
        return v3_const_value_set_implicit_default(lowering,
                                                   current_function,
                                                   aliases,
                                                   alias_count,
                                                   current_function->owner_module_path,
                                                   current_function->source_path,
                                                   inner_type,
                                                   env,
                                                   out,
                                                   depth + 1U);
    }
    if (v3_const_parse_i32_literal_text(expr, &literal_i32)) {
        return v3_const_value_set_i32(out, literal_i32);
    }
    if (v3_const_parse_f64_literal_text(expr, &literal_f64)) {
        return v3_const_value_set_f64(out, literal_f64);
    }
    if (v3_const_split_binary_expr(expr, "||", true, left, sizeof(left), right, sizeof(right))) {
        V3ConstValue lhs;
        bool lhs_truthy = false;
        v3_const_value_init(&lhs);
        if (!v3_const_eval_expr(lowering, current_function, aliases, alias_count, env, left, &lhs, depth + 1U) ||
            !v3_const_value_truthy(&lhs, &lhs_truthy)) {
            v3_const_value_free(&lhs);
            return false;
        }
        if (lhs_truthy) {
            v3_const_value_set_bool(out, true);
            v3_const_value_free(&lhs);
            return true;
        }
        v3_const_value_free(&lhs);
        if (!v3_const_eval_expr(lowering, current_function, aliases, alias_count, env, right, &lhs, depth + 1U) ||
            !v3_const_value_truthy(&lhs, &lhs_truthy)) {
            v3_const_value_free(&lhs);
            return false;
        }
        v3_const_value_set_bool(out, lhs_truthy);
        v3_const_value_free(&lhs);
        return true;
    }
    if (v3_const_split_binary_expr(expr, "&&", true, left, sizeof(left), right, sizeof(right))) {
        V3ConstValue lhs;
        bool lhs_truthy = false;
        v3_const_value_init(&lhs);
        if (!v3_const_eval_expr(lowering, current_function, aliases, alias_count, env, left, &lhs, depth + 1U) ||
            !v3_const_value_truthy(&lhs, &lhs_truthy)) {
            v3_const_value_free(&lhs);
            return false;
        }
        if (!lhs_truthy) {
            v3_const_value_set_bool(out, false);
            v3_const_value_free(&lhs);
            return true;
        }
        v3_const_value_free(&lhs);
        if (!v3_const_eval_expr(lowering, current_function, aliases, alias_count, env, right, &lhs, depth + 1U) ||
            !v3_const_value_truthy(&lhs, &lhs_truthy)) {
            v3_const_value_free(&lhs);
            return false;
        }
        v3_const_value_set_bool(out, lhs_truthy);
        v3_const_value_free(&lhs);
        return true;
    }
    if (v3_const_split_binary_expr(expr, "==", false, left, sizeof(left), right, sizeof(right))) {
        V3ConstValue lhs;
        V3ConstValue rhs;
        bool eq = false;
        v3_const_value_init(&lhs);
        v3_const_value_init(&rhs);
        if (!v3_const_eval_expr(lowering, current_function, aliases, alias_count, env, left, &lhs, depth + 1U) ||
            !v3_const_eval_expr(lowering, current_function, aliases, alias_count, env, right, &rhs, depth + 1U) ||
            !v3_const_value_eq(&lhs, &rhs, &eq)) {
            v3_const_value_free(&lhs);
            v3_const_value_free(&rhs);
            return false;
        }
        v3_const_value_free(&lhs);
        v3_const_value_free(&rhs);
        return v3_const_value_set_bool(out, eq);
    }
    if (v3_const_split_binary_expr(expr, "!=", false, left, sizeof(left), right, sizeof(right))) {
        V3ConstValue lhs;
        V3ConstValue rhs;
        bool eq = false;
        v3_const_value_init(&lhs);
        v3_const_value_init(&rhs);
        if (!v3_const_eval_expr(lowering, current_function, aliases, alias_count, env, left, &lhs, depth + 1U) ||
            !v3_const_eval_expr(lowering, current_function, aliases, alias_count, env, right, &rhs, depth + 1U) ||
            !v3_const_value_eq(&lhs, &rhs, &eq)) {
            v3_const_value_free(&lhs);
            v3_const_value_free(&rhs);
            return false;
        }
        v3_const_value_free(&lhs);
        v3_const_value_free(&rhs);
        return v3_const_value_set_bool(out, !eq);
    }
    binary_op[0] = '\0';
    if (v3_const_split_binary_expr(expr, "<=", false, left, sizeof(left), right, sizeof(right))) {
        snprintf(binary_op, sizeof(binary_op), "%s", "<=");
    } else if (v3_const_split_binary_expr(expr, ">=", false, left, sizeof(left), right, sizeof(right))) {
        snprintf(binary_op, sizeof(binary_op), "%s", ">=");
    } else if (v3_const_split_binary_expr(expr, "<", false, left, sizeof(left), right, sizeof(right))) {
        snprintf(binary_op, sizeof(binary_op), "%s", "<");
    } else if (v3_const_split_binary_expr(expr, ">", false, left, sizeof(left), right, sizeof(right))) {
        snprintf(binary_op, sizeof(binary_op), "%s", ">");
    }
    if (binary_op[0] != '\0') {
        V3ConstValue lhs;
        V3ConstValue rhs;
        bool result = false;
        int32_t lhs_i32 = 0;
        int32_t rhs_i32 = 0;
        double lhs_f64 = 0.0;
        double rhs_f64 = 0.0;
        bool use_float = false;
        v3_const_value_init(&lhs);
        v3_const_value_init(&rhs);
        if (!v3_const_eval_expr(lowering, current_function, aliases, alias_count, env, left, &lhs, depth + 1U) ||
            !v3_const_eval_expr(lowering, current_function, aliases, alias_count, env, right, &rhs, depth + 1U)) {
            v3_const_value_free(&lhs);
            v3_const_value_free(&rhs);
            return false;
        }
        use_float = lhs.kind == V3_CONST_F64 || rhs.kind == V3_CONST_F64;
        if (use_float) {
            if (!v3_const_value_as_f64(&lhs, &lhs_f64) ||
                !v3_const_value_as_f64(&rhs, &rhs_f64)) {
                v3_const_value_free(&lhs);
                v3_const_value_free(&rhs);
                return false;
            }
            if (strcmp(binary_op, "<=") == 0) {
                result = lhs_f64 <= rhs_f64;
            } else if (strcmp(binary_op, ">=") == 0) {
                result = lhs_f64 >= rhs_f64;
            } else if (strcmp(binary_op, "<") == 0) {
                result = lhs_f64 < rhs_f64;
            } else {
                result = lhs_f64 > rhs_f64;
            }
        } else if (!v3_const_value_as_i32(&lhs, &lhs_i32) ||
                   !v3_const_value_as_i32(&rhs, &rhs_i32)) {
            v3_const_value_free(&lhs);
            v3_const_value_free(&rhs);
            return false;
        } else if (strcmp(binary_op, "<=") == 0) {
            result = lhs_i32 <= rhs_i32;
        } else if (strcmp(binary_op, ">=") == 0) {
            result = lhs_i32 >= rhs_i32;
        } else if (strcmp(binary_op, "<") == 0) {
            result = lhs_i32 < rhs_i32;
        } else {
            result = lhs_i32 > rhs_i32;
        }
        v3_const_value_set_bool(out, result);
        v3_const_value_free(&lhs);
        v3_const_value_free(&rhs);
        return true;
    }
    if (v3_const_split_binary_expr(expr, "+", true, left, sizeof(left), right, sizeof(right))) {
        V3ConstValue lhs;
        V3ConstValue rhs;
        int32_t lhs_i32 = 0;
        int32_t rhs_i32 = 0;
        double lhs_f64 = 0.0;
        double rhs_f64 = 0.0;
        v3_const_value_init(&lhs);
        v3_const_value_init(&rhs);
        if (!v3_const_eval_expr(lowering, current_function, aliases, alias_count, env, left, &lhs, depth + 1U) ||
            !v3_const_eval_expr(lowering, current_function, aliases, alias_count, env, right, &rhs, depth + 1U)) {
            v3_const_value_free(&lhs);
            v3_const_value_free(&rhs);
            return false;
        }
        if (v3_const_value_as_i32(&lhs, &lhs_i32) &&
            v3_const_value_as_i32(&rhs, &rhs_i32)) {
            v3_const_value_set_i32(out, lhs_i32 + rhs_i32);
            v3_const_value_free(&lhs);
            v3_const_value_free(&rhs);
            return true;
        }
        if ((lhs.kind == V3_CONST_F64 || rhs.kind == V3_CONST_F64) &&
            v3_const_value_as_f64(&lhs, &lhs_f64) &&
            v3_const_value_as_f64(&rhs, &rhs_f64)) {
            v3_const_value_set_f64(out, lhs_f64 + rhs_f64);
            v3_const_value_free(&lhs);
            v3_const_value_free(&rhs);
            return true;
        }
        if ((lhs.kind == V3_CONST_STR || lhs.kind == V3_CONST_SYMBOL) &&
            (rhs.kind == V3_CONST_STR || rhs.kind == V3_CONST_SYMBOL)) {
            char joined[8192];
            snprintf(joined,
                     sizeof(joined),
                     "%s%s",
                     lhs.text_value ? lhs.text_value : "",
                     rhs.text_value ? rhs.text_value : "");
            v3_const_value_set_str_kind(out, V3_CONST_STR, joined);
            v3_const_value_free(&lhs);
            v3_const_value_free(&rhs);
            return true;
        }
        v3_const_value_free(&lhs);
        v3_const_value_free(&rhs);
        return false;
    }
    binary_op[0] = '\0';
    if (v3_const_split_binary_expr(expr, "-", true, left, sizeof(left), right, sizeof(right))) {
        snprintf(binary_op, sizeof(binary_op), "%s", "-");
    } else if (v3_const_split_binary_expr(expr, "*", true, left, sizeof(left), right, sizeof(right))) {
        snprintf(binary_op, sizeof(binary_op), "%s", "*");
    } else if (v3_const_split_binary_expr(expr, "/", true, left, sizeof(left), right, sizeof(right))) {
        snprintf(binary_op, sizeof(binary_op), "%s", "/");
    } else if (v3_const_split_binary_expr(expr, "%", true, left, sizeof(left), right, sizeof(right))) {
        snprintf(binary_op, sizeof(binary_op), "%s", "%");
    }
    if (binary_op[0] != '\0') {
        V3ConstValue lhs;
        V3ConstValue rhs;
        int32_t value = 0;
        int32_t lhs_i32 = 0;
        int32_t rhs_i32 = 0;
        double lhs_f64 = 0.0;
        double rhs_f64 = 0.0;
        bool use_float = false;
        v3_const_value_init(&lhs);
        v3_const_value_init(&rhs);
        if (!v3_const_eval_expr(lowering, current_function, aliases, alias_count, env, left, &lhs, depth + 1U) ||
            !v3_const_eval_expr(lowering, current_function, aliases, alias_count, env, right, &rhs, depth + 1U)) {
            v3_const_value_free(&lhs);
            v3_const_value_free(&rhs);
            return false;
        }
        use_float = lhs.kind == V3_CONST_F64 || rhs.kind == V3_CONST_F64;
        if (use_float) {
            if (!v3_const_value_as_f64(&lhs, &lhs_f64) ||
                !v3_const_value_as_f64(&rhs, &rhs_f64)) {
                v3_const_value_free(&lhs);
                v3_const_value_free(&rhs);
                return false;
            }
            if (strcmp(binary_op, "%") == 0 || rhs_f64 == 0.0) {
                v3_const_value_free(&lhs);
                v3_const_value_free(&rhs);
                return false;
            }
            if (strcmp(binary_op, "-") == 0) {
                v3_const_value_set_f64(out, lhs_f64 - rhs_f64);
            } else if (strcmp(binary_op, "*") == 0) {
                v3_const_value_set_f64(out, lhs_f64 * rhs_f64);
            } else {
                v3_const_value_set_f64(out, lhs_f64 / rhs_f64);
            }
            v3_const_value_free(&lhs);
            v3_const_value_free(&rhs);
            return true;
        }
        if (!v3_const_value_as_i32(&lhs, &lhs_i32) ||
            !v3_const_value_as_i32(&rhs, &rhs_i32)) {
            v3_const_value_free(&lhs);
            v3_const_value_free(&rhs);
            return false;
        }
        if (strcmp(binary_op, "-") == 0) {
            value = lhs_i32 - rhs_i32;
        } else if (strcmp(binary_op, "*") == 0) {
            value = lhs_i32 * rhs_i32;
        } else if (strcmp(binary_op, "/") == 0) {
            if (rhs_i32 == 0) {
                v3_const_value_free(&lhs);
                v3_const_value_free(&rhs);
                return false;
            }
            value = lhs_i32 / rhs_i32;
        } else {
            if (rhs_i32 == 0) {
                v3_const_value_free(&lhs);
                v3_const_value_free(&rhs);
                return false;
            }
            value = lhs_i32 % rhs_i32;
        }
        v3_const_value_set_i32(out, value);
        v3_const_value_free(&lhs);
        v3_const_value_free(&rhs);
        return true;
    }
    binary_op[0] = '\0';
    if (v3_const_split_binary_expr(expr, "<<", true, left, sizeof(left), right, sizeof(right))) {
        snprintf(binary_op, sizeof(binary_op), "%s", "<<");
    } else if (v3_const_split_binary_expr(expr, ">>", true, left, sizeof(left), right, sizeof(right))) {
        snprintf(binary_op, sizeof(binary_op), "%s", ">>");
    } else if (v3_const_split_binary_expr(expr, "|", true, left, sizeof(left), right, sizeof(right))) {
        snprintf(binary_op, sizeof(binary_op), "%s", "|");
    } else if (v3_const_split_binary_expr(expr, "^", true, left, sizeof(left), right, sizeof(right))) {
        snprintf(binary_op, sizeof(binary_op), "%s", "^");
    } else if (v3_const_split_binary_expr(expr, "&", true, left, sizeof(left), right, sizeof(right))) {
        snprintf(binary_op, sizeof(binary_op), "%s", "&");
    }
    if (binary_op[0] != '\0') {
        V3ConstValue lhs;
        V3ConstValue rhs;
        int32_t lhs_i32 = 0;
        int32_t rhs_i32 = 0;
        int32_t value = 0;
        v3_const_value_init(&lhs);
        v3_const_value_init(&rhs);
        if (!v3_const_eval_expr(lowering, current_function, aliases, alias_count, env, left, &lhs, depth + 1U) ||
            !v3_const_eval_expr(lowering, current_function, aliases, alias_count, env, right, &rhs, depth + 1U) ||
            !v3_const_value_as_i32(&lhs, &lhs_i32) ||
            !v3_const_value_as_i32(&rhs, &rhs_i32)) {
            v3_const_value_free(&lhs);
            v3_const_value_free(&rhs);
            return false;
        }
        if (strcmp(binary_op, "<<") == 0) {
            value = lhs_i32 << (rhs_i32 & 31);
        } else if (strcmp(binary_op, ">>") == 0) {
            value = lhs_i32 >> (rhs_i32 & 31);
        } else if (strcmp(binary_op, "|") == 0) {
            value = lhs_i32 | rhs_i32;
        } else if (strcmp(binary_op, "^") == 0) {
            value = lhs_i32 ^ rhs_i32;
        } else {
            value = lhs_i32 & rhs_i32;
        }
        v3_const_value_set_i32(out, value);
        v3_const_value_free(&lhs);
        v3_const_value_free(&rhs);
        return true;
    }
    if (expr[0] == '!') {
        V3ConstValue inner;
        bool truthy = false;
        v3_const_value_init(&inner);
        if (!v3_const_eval_expr(lowering, current_function, aliases, alias_count, env, expr + 1, &inner, depth + 1U) ||
            !v3_const_value_truthy(&inner, &truthy)) {
            v3_const_value_free(&inner);
            return false;
        }
        v3_const_value_set_bool(out, !truthy);
        v3_const_value_free(&inner);
        return true;
    }
    if (expr[0] == '~') {
        V3ConstValue inner;
        int32_t inner_i32 = 0;
        v3_const_value_init(&inner);
        if (!v3_const_eval_expr(lowering, current_function, aliases, alias_count, env, expr + 1, &inner, depth + 1U) ||
            !v3_const_value_as_i32(&inner, &inner_i32)) {
            v3_const_value_free(&inner);
            return false;
        }
        v3_const_value_set_i32(out, ~inner_i32);
        v3_const_value_free(&inner);
        return true;
    }
    if (expr[0] == '-') {
        V3ConstValue inner;
        v3_const_value_init(&inner);
        if (!v3_const_eval_expr(lowering, current_function, aliases, alias_count, env, expr + 1, &inner, depth + 1U)) {
            v3_const_value_free(&inner);
            return false;
        }
        if (inner.kind == V3_CONST_F64) {
            v3_const_value_set_f64(out, -inner.f64_value);
        } else if (inner.kind == V3_CONST_I32) {
            v3_const_value_set_i32(out, -inner.i32_value);
        } else {
            v3_const_value_free(&inner);
            return false;
        }
        v3_const_value_free(&inner);
        return true;
    }
    if (expr[strlen(expr) - 1U] == ')') {
        char callee[PATH_MAX];
        char (*args)[4096] = NULL;
        size_t arg_count = 0U;
        bool has_named_fields = false;
        size_t i;
        args = (char (*)[4096])v3_xmalloc(sizeof(*args) * 32U);
        if (v3_parse_call_text(expr, callee, sizeof(callee), args, &arg_count, 32U)) {
            if (strcmp(callee, "new") == 0 && arg_count == 1U && current_function != NULL) {
                bool ok = v3_const_value_set_implicit_default(lowering,
                                                              current_function,
                                                              aliases,
                                                              alias_count,
                                                              current_function->owner_module_path,
                                                              current_function->source_path,
                                                              args[0],
                                                              env,
                                                              out,
                                                              depth + 1U);
                if (ok) {
                    free(args);
                    return true;
                }
            }
            for (i = 0; i < arg_count; ++i) {
                if (v3_find_top_level_binary_op(args[i], ":") > 0) {
                    has_named_fields = true;
                    break;
                }
            }
            if ((has_named_fields || (callee[0] >= 'A' && callee[0] <= 'Z')) &&
                current_function != NULL) {
                bool ok = v3_const_record_new_from_named_args(callee,
                                                              args,
                                                              arg_count,
                                                              lowering,
                                                              current_function,
                                                              aliases,
                                                              alias_count,
                                                              env,
                                                              out,
                                                              depth + 1U);
                free(args);
                return ok;
            }
            if (v3_is_intrinsic_call_name(callee)) {
                bool ok = v3_const_eval_intrinsic_call(callee,
                                                       args,
                                                       arg_count,
                                                       lowering,
                                                       current_function,
                                                       aliases,
                                                       alias_count,
                                                       env,
                                                       out,
                                                       depth + 1U);
                free(args);
                return ok;
            }
            {
                char symbol_text[PATH_MAX];
                int32_t function_index;
                V3ConstValue arg_values[32];
                size_t arg_index;
                char callee_symbol[PATH_MAX];
                V3LoweredFunctionStub source_function;
                const V3LoweredFunctionStub *target_function = NULL;
                bool intrinsic_ok = false;
                for (arg_index = 0; arg_index < arg_count; ++arg_index) {
                    v3_const_value_init(&arg_values[arg_index]);
                }
                if (strchr(callee, '.') != NULL) {
                    char alias_text[128];
                    char function_name[128];
                    char callee_copy[PATH_MAX];
                    char *dot;
                    snprintf(callee_copy, sizeof(callee_copy), "%s", callee);
                    dot = strchr(callee_copy, '.');
                    *dot = '\0';
                    snprintf(alias_text, sizeof(alias_text), "%s", callee_copy);
                    snprintf(function_name, sizeof(function_name), "%s", dot + 1);
                    {
                        const char *module_path =
                            v3_alias_module_path_exported_member(aliases,
                                                                 alias_count,
                                                                 current_function->owner_module_path,
                                                                 alias_text,
                                                                 function_name,
                                                                 sizeof(function_name));
                        if (module_path) {
                            snprintf(callee_symbol, sizeof(callee_symbol), "%s::%s", module_path, function_name);
                            if (v3_is_intrinsic_module_call(module_path, function_name)) {
                                intrinsic_ok = v3_const_eval_intrinsic_call(callee_symbol,
                                                                            args,
                                                                            arg_count,
                                                                            lowering,
                                                                            current_function,
                                                                            aliases,
                                                                            alias_count,
                                                                            env,
                                                                            out,
                                                                            depth + 1U);
                            }
                        }
                    }
                }
                if (intrinsic_ok) {
                    for (arg_index = 0; arg_index < arg_count; ++arg_index) {
                        v3_const_value_free(&arg_values[arg_index]);
                    }
                    free(args);
                    return true;
                }
                if (!v3_const_resolve_call_symbol(aliases,
                                                  alias_count,
                                                  current_function,
                                                  callee,
                                                  symbol_text,
                                                  sizeof(symbol_text))) {
                    for (arg_index = 0; arg_index < arg_count; ++arg_index) {
                        v3_const_value_free(&arg_values[arg_index]);
                    }
                    free(args);
                    return false;
                }
                function_index = v3_find_lowered_function_index_by_symbol(lowering, symbol_text);
                if (function_index >= 0) {
                    target_function = &lowering->functions[function_index];
                } else if (v3_const_build_source_function_stub(current_function,
                                                               symbol_text,
                                                               &source_function)) {
                    target_function = &source_function;
                } else {
                    for (arg_index = 0; arg_index < arg_count; ++arg_index) {
                        v3_const_value_free(&arg_values[arg_index]);
                    }
                    free(args);
                    return false;
                }
                for (arg_index = 0; arg_index < arg_count; ++arg_index) {
                    if (!v3_const_eval_expr(lowering,
                                            current_function,
                                            aliases,
                                            alias_count,
                                            env,
                                            args[arg_index],
                                            &arg_values[arg_index],
                                            depth + 1U)) {
                        size_t j;
                        for (j = 0; j < arg_count; ++j) {
                            v3_const_value_free(&arg_values[j]);
                        }
                        free(args);
                        return false;
                    }
                }
                if (!v3_const_eval_function(lowering,
                                            target_function,
                                            arg_values,
                                            arg_count,
                                            out,
                                            NULL,
                                            depth + 1U)) {
                    size_t j;
                    for (j = 0; j < arg_count; ++j) {
                        v3_const_value_free(&arg_values[j]);
                    }
                    free(args);
                    return false;
                }
                for (arg_index = 0; arg_index < arg_count; ++arg_index) {
                    v3_const_value_free(&arg_values[arg_index]);
                }
                free(args);
                return true;
            }
        }
        free(args);
    }
    if (strchr(expr, '.') != NULL) {
        char chain[16][128];
        size_t chain_len = 0U;
        const char *start = expr;
        const char *cursor = expr;
        V3ConstValue current;
        v3_const_value_init(&current);
        while (1) {
            if (*cursor == '.' || *cursor == '\0') {
                if (chain_len >= 16U) {
                    return false;
                }
                snprintf(chain[chain_len], sizeof(chain[chain_len]), "%.*s", (int)(cursor - start), start);
                v3_trim_copy_text(chain[chain_len], chain[chain_len], sizeof(chain[chain_len]));
                chain_len += 1U;
                if (*cursor == '\0') {
                    break;
                }
                start = cursor + 1;
            }
            cursor++;
        }
        {
            const V3ConstValue *binding = v3_const_env_get(env, chain[0]);
            size_t i;
            if (!binding) {
                v3_const_value_free(&current);
                return false;
            }
            if (!v3_const_value_clone(binding, &current)) {
                return false;
            }
            for (i = 1; i < chain_len; ++i) {
                const V3ConstValue *field = v3_const_record_field(&current, chain[i]);
                V3ConstValue next;
                if (!field) {
                    v3_const_value_free(&current);
                    return false;
                }
                v3_const_value_init(&next);
                if (!v3_const_value_clone(field, &next)) {
                    v3_const_value_free(&current);
                    return false;
                }
                v3_const_value_free(&current);
                current = next;
            }
            if (!v3_const_value_clone(&current, out)) {
                v3_const_value_free(&current);
                return false;
            }
            v3_const_value_free(&current);
            return true;
        }
    }
    {
        const V3ConstValue *binding = v3_const_env_get(env, expr);
        if (binding) {
            return v3_const_value_clone(binding, out);
        }
    }
    return v3_const_value_set_str_kind(out,
                                       (expr[0] >= 'A' && expr[0] <= 'Z') ? V3_CONST_SYMBOL : V3_CONST_STR,
                                       expr);
}

static bool v3_const_parse_binding_name(const char *statement,
                                        const char *prefix,
                                        char *name_out,
                                        size_t name_cap,
                                        char *expr_out,
                                        size_t expr_cap) {
    const char *cursor = statement + strlen(prefix);
    const char *eq = strchr(cursor, '=');
    char name_part[256];
    if (!eq) {
        return false;
    }
    snprintf(name_part, sizeof(name_part), "%.*s", (int)(eq - cursor), cursor);
    v3_trim_copy_text(name_part, name_part, sizeof(name_part));
    if (strchr(name_part, ':') != NULL) {
        *strchr(name_part, ':') = '\0';
    }
    v3_trim_copy_text(name_part, name_out, name_cap);
    snprintf(expr_out, expr_cap, "%s", eq + 1);
    v3_trim_copy_text(expr_out, expr_out, expr_cap);
    return name_out[0] != '\0';
}

static bool v3_const_identifier_char(char ch) {
    unsigned char uch = (unsigned char)ch;
    return isalnum(uch) || ch == '_';
}

static bool v3_const_text_has_identifier_token(const char *text,
                                               const char *name) {
    size_t text_len;
    size_t name_len;
    bool in_string = false;
    size_t i;
    if (text == NULL || name == NULL || name[0] == '\0') {
        return false;
    }
    text_len = strlen(text);
    name_len = strlen(name);
    if (name_len == 0U || name_len > text_len) {
        return false;
    }
    for (i = 0U; i < text_len; ++i) {
        char ch = text[i];
        if (ch == '"' && !v3_quote_is_escaped(text, i)) {
            in_string = !in_string;
            continue;
        }
        if (!in_string && ch == '#') {
            break;
        }
        if (in_string) {
            continue;
        }
        if (i + name_len > text_len) {
            break;
        }
        if (strncmp(text + i, name, name_len) != 0) {
            continue;
        }
        if (i > 0U && v3_const_identifier_char(text[i - 1U])) {
            continue;
        }
        if (i + name_len < text_len &&
            v3_const_identifier_char(text[i + name_len])) {
            continue;
        }
        return true;
    }
    return false;
}

static bool v3_const_function_uses_top_level_name(char **lines,
                                                  size_t line_count,
                                                  size_t fn_line_index,
                                                  const char *name) {
    size_t i;
    if (lines == NULL || name == NULL || name[0] == '\0' || fn_line_index >= line_count) {
        return false;
    }
    for (i = fn_line_index; i < line_count; ++i) {
        char line_copy[4096];
        char *trimmed;
        int32_t indent;
        snprintf(line_copy, sizeof(line_copy), "%s", lines[i]);
        trimmed = v3_trim_inplace(line_copy);
        if (*trimmed == '\0') {
            continue;
        }
        indent = v3_line_indent(lines[i]);
        if (i > fn_line_index &&
            indent <= 0 &&
            v3_is_top_level_decl_start(trimmed)) {
            break;
        }
        if (i == fn_line_index) {
            char *eq = strrchr(trimmed, '=');
            if (eq != NULL &&
                v3_const_text_has_identifier_token(eq + 1, name)) {
                return true;
            }
            continue;
        }
        if (v3_const_text_has_identifier_token(trimmed, name)) {
            return true;
        }
    }
    return false;
}

static bool v3_const_env_load_top_level_vars(const V3LoweringPlanStub *lowering,
                                             const V3LoweredFunctionStub *function,
                                             const V3ImportAlias *aliases,
                                             size_t alias_count,
                                             char **lines,
                                             size_t line_count,
                                             size_t fn_line_index,
                                             V3ConstEnv *env,
                                             size_t depth) {
    bool in_var_block = false;
    size_t i;
    for (i = 0; i < line_count; ++i) {
        char line_copy[4096];
        char name[128];
        char raw_type[256];
        char expr[4096];
        bool has_init = false;
        int32_t indent;
        char *trimmed;
        const char *decl = NULL;
        V3ConstValue bound;
        bool have_value = false;
        v3_const_value_init(&bound);
        snprintf(line_copy, sizeof(line_copy), "%s", lines[i]);
        indent = v3_line_indent(lines[i]);
        trimmed = v3_trim_inplace(line_copy);
        if (*trimmed == '\0' || *trimmed == '#') {
            v3_const_value_free(&bound);
            continue;
        }
        if (indent <= 0 && strcmp(trimmed, "var") == 0) {
            in_var_block = true;
            v3_const_value_free(&bound);
            continue;
        }
        if (indent <= 0 && !v3_startswith(trimmed, "var ")) {
            in_var_block = false;
        }
        if (indent <= 0 && v3_startswith(trimmed, "var ")) {
            decl = trimmed + 4;
        } else if (indent > 0 && in_var_block) {
            decl = trimmed;
        }
        if (decl == NULL ||
            !v3_parse_top_level_var_binding_text(decl,
                                                 name,
                                                 sizeof(name),
                                                 raw_type,
                                                 sizeof(raw_type),
                                                 &has_init,
                                                 expr,
                                                 sizeof(expr))) {
            v3_const_value_free(&bound);
            continue;
        }
        if (function != NULL &&
            !v3_const_function_uses_top_level_name(lines,
                                                  line_count,
                                                  fn_line_index,
                                                  name)) {
            v3_const_value_free(&bound);
            continue;
        }
        if (has_init) {
            have_value = v3_const_eval_expr(lowering,
                                            function,
                                            aliases,
                                            alias_count,
                                            env,
                                            expr,
                                            &bound,
                                            depth + 1U);
        } else if (raw_type[0] != '\0') {
            have_value = v3_const_value_set_zero_for_type(lowering,
                                                         function,
                                                         aliases,
                                                         alias_count,
                                                         env,
                                                         raw_type,
                                                         &bound,
                                                         depth + 1U);
        }
        if (have_value) {
            if (!v3_const_env_set(env, name, &bound)) {
                v3_const_value_free(&bound);
                return false;
            }
        }
        v3_const_value_free(&bound);
    }
    return true;
}

static bool v3_const_skip_nested_block(char **lines,
                                       size_t line_count,
                                       size_t *index_io,
                                       int32_t parent_indent) {
    while (*index_io < line_count) {
        char raw_copy[4096];
        char *trimmed;
        snprintf(raw_copy, sizeof(raw_copy), "%s", lines[*index_io]);
        trimmed = v3_trim_inplace(raw_copy);
        if (*trimmed == '\0') {
            *index_io += 1U;
            continue;
        }
        if (v3_line_indent(lines[*index_io]) <= parent_indent) {
            return true;
        }
        *index_io += 1U;
    }
    return true;
}

static bool v3_const_record_entry_echo(V3ConstEntryProgram *entry_program,
                                       const V3ConstValue *value) {
    if (entry_program == NULL ||
        value == NULL ||
        value->kind != V3_CONST_STR ||
        entry_program->echo_count >= CHENG_V3_CONST_MAX_ECHOS) {
        return false;
    }
    snprintf(entry_program->echo_texts[entry_program->echo_count].text,
             sizeof(entry_program->echo_texts[entry_program->echo_count].text),
             "%s",
             value->text_value ? value->text_value : "");
    entry_program->echo_count += 1U;
    return true;
}

static bool v3_const_skip_following_if_branches(char **lines,
                                                size_t line_count,
                                                size_t *index_io,
                                                int32_t parent_indent) {
    while (*index_io < line_count) {
        char raw_copy[4096];
        char *trimmed;
        int32_t indent;
        size_t branch_header_index = *index_io;
        snprintf(raw_copy, sizeof(raw_copy), "%s", lines[*index_io]);
        trimmed = v3_trim_inplace(raw_copy);
        if (*trimmed == '\0') {
            *index_io += 1U;
            continue;
        }
        indent = v3_line_indent(lines[*index_io]);
        if (indent != parent_indent ||
            !(strcmp(trimmed, "else:") == 0 ||
              (v3_startswith(trimmed, "elif ") && trimmed[strlen(trimmed) - 1U] == ':'))) {
            return true;
        }
        *index_io = branch_header_index + 1U;
        if (!v3_const_skip_nested_block(lines, line_count, index_io, parent_indent)) {
            return false;
        }
    }
    return true;
}

static bool v3_const_eval_call_statement(const V3LoweringPlanStub *lowering,
                                         const V3LoweredFunctionStub *function,
                                         const V3ImportAlias *aliases,
                                         size_t alias_count,
                                         V3ConstEnv *env,
                                         const char *statement,
                                         V3ConstEntryProgram *entry_program,
                                         size_t depth) {
    char callee[PATH_MAX];
    char args_text[32][4096];
    size_t arg_count = 0U;
    V3ConstValue value;
    v3_const_value_init(&value);
    if (!v3_parse_call_text(statement, callee, sizeof(callee), args_text, &arg_count, 32U)) {
        v3_const_value_free(&value);
        return false;
    }
    if (strcmp(callee, "echo") == 0) {
        bool ok = arg_count == 1U &&
                  v3_const_eval_expr(lowering,
                                     function,
                                     aliases,
                                     alias_count,
                                     env,
                                     args_text[0],
                                     &value,
                                     depth + 1U) &&
                  v3_const_record_entry_echo(entry_program, &value);
        v3_const_value_free(&value);
        return ok;
    }
    if (v3_consteval_callee_is_puts_like(function, callee)) {
        bool ok = arg_count == 1U &&
                  v3_const_eval_expr(lowering,
                                     function,
                                     aliases,
                                     alias_count,
                                     env,
                                     args_text[0],
                                     &value,
                                     depth + 1U) &&
                  v3_const_record_entry_echo(entry_program, &value);
        v3_const_value_free(&value);
        return ok;
    }
    if (!v3_const_eval_expr(lowering,
                            function,
                            aliases,
                            alias_count,
                            env,
                            statement,
                            &value,
                            depth + 1U)) {
        v3_const_value_free(&value);
        return false;
    }
    v3_const_value_free(&value);
    return true;
}

static bool v3_const_eval_block(const V3LoweringPlanStub *lowering,
                                const V3LoweredFunctionStub *function,
                                const V3ImportAlias *aliases,
                                size_t alias_count,
                                V3ConstEnv *env,
                                char **lines,
                                size_t line_count,
                                size_t *index_io,
                                int32_t parent_indent,
                                V3ConstValue *out,
                                bool *returned,
                                V3ConstEntryProgram *entry_program,
                                bool *loop_break,
                                bool *loop_continue,
                                size_t depth);

static bool v3_const_eval_inline_statement(const V3LoweringPlanStub *lowering,
                                           const V3LoweredFunctionStub *function,
                                           const V3ImportAlias *aliases,
                                           size_t alias_count,
                                           V3ConstEnv *env,
                                           const char *statement,
                                           V3ConstValue *out,
                                           bool *returned,
                                           V3ConstEntryProgram *entry_program,
                                           bool *loop_break,
                                           bool *loop_continue,
                                           size_t depth) {
    char line_storage[8192];
    char *inline_lines[1];
    size_t inline_index = 0U;
    if (statement == NULL || statement[0] == '\0') {
        return true;
    }
    snprintf(line_storage, sizeof(line_storage), "%s", statement);
    inline_lines[0] = line_storage;
    return v3_const_eval_block(lowering,
                               function,
                               aliases,
                               alias_count,
                               env,
                               inline_lines,
                               1U,
                               &inline_index,
                               -1,
                               out,
                               returned,
                               entry_program,
                               loop_break,
                               loop_continue,
                               depth + 1U);
}

static bool v3_const_eval_block(const V3LoweringPlanStub *lowering,
                                const V3LoweredFunctionStub *function,
                                const V3ImportAlias *aliases,
                                size_t alias_count,
                                V3ConstEnv *env,
                                char **lines,
                                size_t line_count,
                                size_t *index_io,
                                int32_t parent_indent,
                                V3ConstValue *out,
                                bool *returned,
                                V3ConstEntryProgram *entry_program,
                                bool *loop_break,
                                bool *loop_continue,
                                size_t depth) {
    while (*index_io < line_count) {
        char statement[8192];
        char raw_copy[4096];
        char *trimmed;
        int32_t indent;
        snprintf(raw_copy, sizeof(raw_copy), "%s", lines[*index_io]);
        trimmed = v3_trim_inplace(raw_copy);
        if (*trimmed == '\0') {
            *index_io += 1U;
            continue;
        }
        indent = v3_line_indent(lines[*index_io]);
        if (indent <= parent_indent ||
            (indent <= 0 && v3_is_top_level_decl_start(trimmed))) {
            return true;
        }
        if (!v3_collect_statement_from_lines(lines, line_count, index_io, statement, sizeof(statement))) {
            return false;
        }
        if (v3_startswith(statement, "let ") || v3_startswith(statement, "var ")) {
            char name[128];
            char expr[4096];
            V3ConstValue bound;
            v3_const_value_init(&bound);
            if (!v3_const_parse_binding_name(statement,
                                             v3_startswith(statement, "let ") ? "let " : "var ",
                                             name,
                                             sizeof(name),
                                             expr,
                                             sizeof(expr)) ||
                !v3_const_eval_expr(lowering,
                                    function,
                                    aliases,
                                    alias_count,
                                    env,
                                    expr,
                                    &bound,
                                    depth + 1U) ||
                !v3_const_env_set(env, name, &bound)) {
                v3_const_value_free(&bound);
                return false;
            }
            v3_const_value_free(&bound);
            continue;
        }
        if (v3_startswith(statement, "if ") || v3_startswith(statement, "elif ")) {
            size_t body_start = *index_io;
            char inline_stmt[8192];
            char cond_text[4096];
            V3ConstValue cond;
            bool cond_truthy = false;
            v3_const_value_init(&cond);
            inline_stmt[0] = '\0';
            if (!(v3_startswith(statement, "if ") ?
                      v3_parse_if_header(statement,
                                         cond_text,
                                         sizeof(cond_text),
                                         inline_stmt,
                                         sizeof(inline_stmt)) :
                      v3_parse_elif_header(statement,
                                           cond_text,
                                           sizeof(cond_text),
                                           inline_stmt,
                                           sizeof(inline_stmt)))) {
                v3_const_value_free(&cond);
                return false;
            }
            if (!v3_const_eval_expr(lowering,
                                    function,
                                    aliases,
                                    alias_count,
                                    env,
                                    cond_text,
                                    &cond,
                                    depth + 1U) ||
                !v3_const_value_truthy(&cond, &cond_truthy)) {
                v3_const_value_free(&cond);
                return false;
            }
            if (cond_truthy) {
                size_t branch_index = body_start;
                if (!((inline_stmt[0] != '\0' &&
                       v3_const_eval_inline_statement(lowering,
                                                      function,
                                                      aliases,
                                                      alias_count,
                                                      env,
                                                      inline_stmt,
                                                      out,
                                                      returned,
                                                      entry_program,
                                                      loop_break,
                                                      loop_continue,
                                                      depth + 1U)) ||
                      (inline_stmt[0] == '\0' &&
                       v3_const_eval_block(lowering,
                                           function,
                                           aliases,
                                           alias_count,
                                           env,
                                           lines,
                                           line_count,
                                           &branch_index,
                                           indent,
                                           out,
                                           returned,
                                           entry_program,
                                           loop_break,
                                           loop_continue,
                                           depth + 1U))) ||
                    !v3_const_skip_following_if_branches(lines,
                                                         line_count,
                                                         &branch_index,
                                                         indent)) {
                    v3_const_value_free(&cond);
                    return false;
                }
                *index_io = branch_index;
                v3_const_value_free(&cond);
                if (*returned || (loop_break != NULL && *loop_break) ||
                    (loop_continue != NULL && *loop_continue)) {
                    return true;
                }
                continue;
            }
            if (inline_stmt[0] == '\0' &&
                !v3_const_skip_nested_block(lines, line_count, index_io, indent)) {
                v3_const_value_free(&cond);
                return false;
            }
            v3_const_value_free(&cond);
            continue;
        }
        if (v3_startswith(statement, "else")) {
            size_t branch_index = *index_io;
            char inline_stmt[8192];
            inline_stmt[0] = '\0';
            if (!v3_parse_else_header(statement, inline_stmt, sizeof(inline_stmt))) {
                return false;
            }
            if (!((inline_stmt[0] != '\0' &&
                   v3_const_eval_inline_statement(lowering,
                                                  function,
                                                  aliases,
                                                  alias_count,
                                                  env,
                                                  inline_stmt,
                                                  out,
                                                  returned,
                                                  entry_program,
                                                  loop_break,
                                                  loop_continue,
                                                  depth + 1U)) ||
                  (inline_stmt[0] == '\0' &&
                   v3_const_eval_block(lowering,
                                       function,
                                       aliases,
                                       alias_count,
                                       env,
                                       lines,
                                       line_count,
                                       &branch_index,
                                       indent,
                                       out,
                                       returned,
                                       entry_program,
                                       loop_break,
                                       loop_continue,
                                       depth + 1U)))) {
                return false;
            }
            *index_io = branch_index;
            if (*returned || (loop_break != NULL && *loop_break) ||
                (loop_continue != NULL && *loop_continue)) {
                return true;
            }
            continue;
        }
        if (v3_startswith(statement, "while ")) {
            char cond_text[4096];
            char inline_stmt[8192];
            size_t body_start = *index_io;
            size_t body_end = body_start;
            size_t iter_count = 0U;
            inline_stmt[0] = '\0';
            if (!v3_parse_while_header(statement,
                                       cond_text,
                                       sizeof(cond_text),
                                       inline_stmt,
                                       sizeof(inline_stmt))) {
                return false;
            }
            if (inline_stmt[0] == '\0' &&
                !v3_const_skip_nested_block(lines, line_count, &body_end, indent)) {
                return false;
            }
            while (true) {
                V3ConstValue cond;
                bool cond_truthy = false;
                bool broke = false;
                bool continued = false;
                v3_const_value_init(&cond);
                if (!v3_const_eval_expr(lowering,
                                        function,
                                        aliases,
                                        alias_count,
                                        env,
                                        cond_text,
                                        &cond,
                                        depth + 1U) ||
                    !v3_const_value_truthy(&cond, &cond_truthy)) {
                    v3_const_value_free(&cond);
                    return false;
                }
                if (!cond_truthy) {
                    v3_const_value_free(&cond);
                    break;
                }
                v3_const_value_free(&cond);
                iter_count += 1U;
                if (iter_count > CHENG_V3_CONST_MAX_LOOP_ITERS) {
                    return false;
                }
                if (inline_stmt[0] != '\0') {
                    if (!v3_const_eval_inline_statement(lowering,
                                                        function,
                                                        aliases,
                                                        alias_count,
                                                        env,
                                                        inline_stmt,
                                                        out,
                                                        returned,
                                                        entry_program,
                                                        &broke,
                                                        &continued,
                                                        depth + 1U)) {
                        return false;
                    }
                } else {
                    size_t loop_body_index = body_start;
                    if (!v3_const_eval_block(lowering,
                                             function,
                                             aliases,
                                             alias_count,
                                             env,
                                             lines,
                                             line_count,
                                             &loop_body_index,
                                             indent,
                                             out,
                                             returned,
                                             entry_program,
                                             &broke,
                                             &continued,
                                             depth + 1U)) {
                        return false;
                    }
                }
                if (*returned) {
                    *index_io = body_end;
                    return true;
                }
                if (broke) {
                    break;
                }
                if (continued) {
                    continue;
                }
            }
            *index_io = body_end;
            continue;
        }
        if (v3_startswith(statement, "for ")) {
            char iter_name[128];
            char start_expr[4096];
            char end_expr[4096];
            char inline_stmt[8192];
            bool end_inclusive = false;
            size_t body_start = *index_io;
            size_t body_end = body_start;
            int32_t start_i32 = 0;
            int32_t end_i32 = 0;
            int32_t iter_value;
            V3ConstValue start_value;
            V3ConstValue end_value;
            v3_const_value_init(&start_value);
            v3_const_value_init(&end_value);
            if (!v3_parse_for_header(statement,
                                     iter_name,
                                     sizeof(iter_name),
                                     start_expr,
                                     sizeof(start_expr),
                                     end_expr,
                                     sizeof(end_expr),
                                     &end_inclusive,
                                     inline_stmt,
                                     sizeof(inline_stmt)) ||
                !v3_const_eval_expr(lowering,
                                    function,
                                    aliases,
                                    alias_count,
                                    env,
                                    start_expr,
                                    &start_value,
                                    depth + 1U) ||
                !v3_const_eval_expr(lowering,
                                    function,
                                    aliases,
                                    alias_count,
                                    env,
                                    end_expr,
                                    &end_value,
                                    depth + 1U) ||
                !v3_const_value_as_i32(&start_value, &start_i32) ||
                !v3_const_value_as_i32(&end_value, &end_i32)) {
                v3_const_value_free(&start_value);
                v3_const_value_free(&end_value);
                return false;
            }
            v3_const_value_free(&start_value);
            v3_const_value_free(&end_value);
            if (inline_stmt[0] == '\0' &&
                !v3_const_skip_nested_block(lines, line_count, &body_end, indent)) {
                return false;
            }
            for (iter_value = start_i32;
                 end_inclusive ? (iter_value <= end_i32) : (iter_value < end_i32);
                 iter_value += 1) {
                V3ConstValue iter_const;
                bool broke = false;
                bool continued = false;
                v3_const_value_init(&iter_const);
                v3_const_value_set_i32(&iter_const, iter_value);
                if (!v3_const_env_set(env, iter_name, &iter_const)) {
                    v3_const_value_free(&iter_const);
                    return false;
                }
                v3_const_value_free(&iter_const);
                if (inline_stmt[0] != '\0') {
                    if (!v3_const_eval_inline_statement(lowering,
                                                        function,
                                                        aliases,
                                                        alias_count,
                                                        env,
                                                        inline_stmt,
                                                        out,
                                                        returned,
                                                        entry_program,
                                                        &broke,
                                                        &continued,
                                                        depth + 1U)) {
                        return false;
                    }
                } else {
                    size_t loop_body_index = body_start;
                    if (!v3_const_eval_block(lowering,
                                             function,
                                             aliases,
                                             alias_count,
                                             env,
                                             lines,
                                             line_count,
                                             &loop_body_index,
                                             indent,
                                             out,
                                             returned,
                                             entry_program,
                                             &broke,
                                             &continued,
                                             depth + 1U)) {
                        return false;
                    }
                }
                if (*returned) {
                    *index_io = body_end;
                    return true;
                }
                if (broke) {
                    break;
                }
                if (continued) {
                    continue;
                }
            }
            *index_io = body_end;
            continue;
        }
        if (v3_startswith(statement, "assert(")) {
            char callee[PATH_MAX];
            char args_text[32][4096];
            size_t statement_arg_count = 0U;
            V3ConstValue cond;
            bool cond_truthy = false;
            v3_const_value_init(&cond);
            if (!v3_parse_call_text(statement, callee, sizeof(callee), args_text, &statement_arg_count, 32U) ||
                strcmp(callee, "assert") != 0 ||
                statement_arg_count < 1U ||
                !v3_const_eval_expr(lowering,
                                    function,
                                    aliases,
                                    alias_count,
                                    env,
                                    args_text[0],
                                    &cond,
                                    depth + 1U) ||
                !v3_const_value_truthy(&cond, &cond_truthy) ||
                !cond_truthy) {
                v3_const_value_free(&cond);
                return false;
            }
            v3_const_value_free(&cond);
            continue;
        }
        if (strcmp(statement, "break") == 0) {
            if (loop_break == NULL) {
                return false;
            }
            *loop_break = true;
            return true;
        }
        if (strcmp(statement, "continue") == 0) {
            if (loop_continue == NULL) {
                return false;
            }
            *loop_continue = true;
            return true;
        }
        if (v3_startswith(statement, "return ")) {
            if (!v3_const_eval_expr(lowering,
                                    function,
                                    aliases,
                                    alias_count,
                                    env,
                                    statement + 7,
                                    out,
                                    depth + 1U)) {
                return false;
            }
            *returned = true;
            return true;
        }
        {
            char lhs[4096];
            char rhs[4096];
            if (v3_parse_assignment_meta(statement, lhs, sizeof(lhs), rhs, sizeof(rhs))) {
                V3ConstValue bound;
                v3_const_value_init(&bound);
                if (v3_const_env_get(env, lhs) == NULL ||
                    !v3_const_eval_expr(lowering,
                                        function,
                                        aliases,
                                        alias_count,
                                        env,
                                        rhs,
                                        &bound,
                                        depth + 1U) ||
                    !v3_const_env_set(env, lhs, &bound)) {
                    v3_const_value_free(&bound);
                    return false;
                }
                v3_const_value_free(&bound);
                continue;
            }
        }
        {
            char callee[PATH_MAX];
            char args_text[32][4096];
            size_t arg_count = 0U;
            if (!v3_parse_call_text(statement, callee, sizeof(callee), args_text, &arg_count, 32U)) {
                return false;
            }
            if (!v3_const_eval_call_statement(lowering,
                                              function,
                                              aliases,
                                              alias_count,
                                              env,
                                              statement,
                                              entry_program,
                                              depth + 1U)) {
                return false;
            }
            continue;
        }
    }
    return true;
}

static bool v3_const_eval_function(const V3LoweringPlanStub *lowering,
                                   const V3LoweredFunctionStub *function,
                                   const V3ConstValue *args,
                                   size_t arg_count,
                                   V3ConstValue *out,
                                   V3ConstEntryProgram *entry_program,
                                   size_t depth) {
    char **lines = NULL;
    char *owned_lines = NULL;
    size_t line_count = 0U;
    size_t fn_line_index = 0U;
    char (*param_names)[128] = NULL;
    size_t param_count = 0U;
    char source_path[PATH_MAX];
    V3ImportAlias *aliases = NULL;
    size_t alias_count = 0U;
    V3ConstEnv env;
    bool ok = false;
    bool returned = false;
    size_t block_index;
    size_t i;
    memset(&env, 0, sizeof(env));
    if (depth > 64U) {
        return false;
    }
    param_names = (char (*)[128])v3_xmalloc(sizeof(*param_names) * CHENG_V3_CONST_MAX_BINDINGS);
    aliases = (V3ImportAlias *)v3_xmalloc(sizeof(*aliases) * CHENG_V3_MAX_IMPORT_ALIASES);
    if (!v3_load_function_source_lines(function,
                                       &lines,
                                       &owned_lines,
                                       &line_count,
                                       &fn_line_index,
                                       param_names,
                                       &param_count,
                                       source_path)) {
        free(param_names);
        free(aliases);
        return false;
    }
    v3_parse_import_aliases_from_lines(lines,
                                       line_count,
                                       aliases,
                                       &alias_count,
                                       CHENG_V3_MAX_IMPORT_ALIASES);
    if (!v3_const_env_load_top_level_vars(lowering,
                                          function,
                                          aliases,
                                          alias_count,
                                          lines,
                                          line_count,
                                          fn_line_index,
                                          &env,
                                          depth + 1U)) {
        goto cleanup;
    }
    if (param_count != arg_count) {
        goto cleanup;
    }
    for (i = 0; i < arg_count; ++i) {
        if (!v3_const_env_set(&env, param_names[i], &args[i])) {
            goto cleanup;
        }
    }
    block_index = fn_line_index + 1U;
    ok = v3_const_eval_block(lowering,
                             function,
                             aliases,
                             alias_count,
                             &env,
                             lines,
                             line_count,
                             &block_index,
                             0,
                             out,
                             &returned,
                             entry_program,
                             NULL,
                             NULL,
                             depth + 1U) &&
         returned;
cleanup:
    v3_const_env_free(&env);
    free(lines);
    free(owned_lines);
    free(param_names);
    free(aliases);
    return ok;
}

static bool v3_try_consteval_entry_program(const V3SystemLinkPlanStub *plan,
                                           const V3LoweringPlanStub *lowering,
                                           V3ConstEntryProgram *program) {
    const V3LoweredFunctionStub *entry_function = v3_find_entry_lowered_function(lowering);
    V3ConstValue result;
    (void)plan;
    if (!entry_function) {
        return false;
    }
    memset(program, 0, sizeof(*program));
    v3_const_value_init(&result);
    if (!v3_const_eval_function(lowering,
                                entry_function,
                                NULL,
                                0U,
                                &result,
                                program,
                                0U) ||
        result.kind != V3_CONST_I32) {
        v3_const_value_free(&result);
        memset(program, 0, sizeof(*program));
        return false;
    }
    program->ready = true;
    program->return_code = result.i32_value;
    v3_const_value_free(&result);
    return true;
}

static bool v3_parent_dir(const char *path, char *out, size_t cap) {
    const char *slash;
    size_t len;
    if (!path || *path == '\0') {
        out[0] = '\0';
        return false;
    }
    slash = strrchr(path, '/');
    if (!slash) {
        out[0] = '\0';
        return false;
    }
    len = (size_t)(slash - path);
    if (len <= 0U || len >= cap) {
        out[0] = '\0';
        return false;
    }
    memcpy(out, path, len);
    out[len] = '\0';
    return true;
}

static void v3_join_path(char *out, size_t cap, const char *a, const char *b) {
    char *a_copy = NULL;
    char *b_copy = NULL;
    const char *left = a;
    const char *right = b;
    if (out != NULL && a != NULL && out == a) {
        size_t n = strlen(a) + 1U;
        a_copy = (char *)malloc(n);
        if (a_copy != NULL) {
            memcpy(a_copy, a, n);
            left = a_copy;
        }
    }
    if (out != NULL && b != NULL && out == b) {
        size_t n = strlen(b) + 1U;
        b_copy = (char *)malloc(n);
        if (b_copy != NULL) {
            memcpy(b_copy, b, n);
            right = b_copy;
        }
    }
    if (!left || *left == '\0') {
        v3_copy_text(out, cap, right ? right : "");
        free(a_copy);
        free(b_copy);
        return;
    }
    if (!right || *right == '\0') {
        v3_copy_text(out, cap, left);
        free(a_copy);
        free(b_copy);
        return;
    }
    if (right[0] == '/') {
        v3_copy_text(out, cap, right);
        free(a_copy);
        free(b_copy);
        return;
    }
    if (left[strlen(left) - 1U] == '/') {
        snprintf(out, cap, "%s%s", left, right);
    } else {
        snprintf(out, cap, "%s/%s", left, right);
    }
    free(a_copy);
    free(b_copy);
}

static void v3_strip_ext(char *text) {
    char *dot = strrchr(text, '.');
    if (dot) {
        *dot = '\0';
    }
}

static void v3_contract_workspace_root(const V3BootstrapContract *contract, char *out, size_t cap) {
    char bootstrap_dir[PATH_MAX];
    char package_root[PATH_MAX];
    if (!v3_parent_dir(contract->source_path, bootstrap_dir, sizeof(bootstrap_dir)) ||
        !v3_parent_dir(bootstrap_dir, package_root, sizeof(package_root)) ||
        !v3_parent_dir(package_root, out, cap)) {
        out[0] = '\0';
    }
}

static void v3_contract_package_root(const V3BootstrapContract *contract, char *out, size_t cap) {
    char bootstrap_dir[PATH_MAX];
    if (!v3_parent_dir(contract->source_path, bootstrap_dir, sizeof(bootstrap_dir)) ||
        !v3_parent_dir(bootstrap_dir, out, cap)) {
        out[0] = '\0';
    }
}

static void v3_resolve_contract_path(const V3BootstrapContract *contract,
                                     const char *key,
                                     char *out,
                                     size_t cap) {
    char workspace_root[PATH_MAX];
    const char *value = v3_contract_get(contract, key);
    v3_contract_workspace_root(contract, workspace_root, sizeof(workspace_root));
    v3_join_path(out, cap, workspace_root, value ? value : "");
}

static void v3_default_package_root(const V3BootstrapContract *contract, char *out, size_t cap) {
    v3_contract_package_root(contract, out, cap);
}

static bool v3_package_manifest_exists(const char *dir) {
    char manifest_path[PATH_MAX];
    if (!dir || dir[0] == '\0') {
        return false;
    }
    v3_join_path(manifest_path, sizeof(manifest_path), dir, "cheng-package.toml");
    return access(manifest_path, R_OK) == 0;
}

static void v3_find_shared_v3_root(const char *package_root, char *out, size_t cap) {
    char cursor[PATH_MAX];
    char parent[PATH_MAX];
    out[0] = '\0';
    if (!package_root || package_root[0] == '\0') {
        return;
    }
    v3_copy_text(cursor, sizeof(cursor), package_root);
    while (cursor[0] != '\0') {
        if (v3_path_last_component_is(cursor, "v3") &&
            v3_package_manifest_exists(cursor)) {
            v3_copy_text(out, cap, cursor);
            return;
        }
        if (!v3_parent_dir(cursor, parent, sizeof(parent)) ||
            parent[0] == '\0' ||
            strcmp(parent, cursor) == 0) {
            break;
        }
        v3_copy_text(cursor, sizeof(cursor), parent);
    }
}

static void v3_shared_v3_package_root(const char *workspace_root, char *out, size_t cap) {
    char shared_v3_root[PATH_MAX];
    out[0] = '\0';
    if (!workspace_root || workspace_root[0] == '\0') {
        return;
    }
    v3_join_path(shared_v3_root, sizeof(shared_v3_root), workspace_root, "v3");
    if (v3_package_manifest_exists(shared_v3_root)) {
        v3_copy_text(out, cap, shared_v3_root);
    }
}

static void v3_workspace_root_from_package_root(const char *package_root, char *out, size_t cap) {
    char shared_v3_root[PATH_MAX];
    v3_find_shared_v3_root(package_root, shared_v3_root, sizeof(shared_v3_root));
    if (shared_v3_root[0] != '\0') {
        if (v3_parent_dir(shared_v3_root, out, cap)) {
            return;
        }
        v3_copy_text(out, cap, shared_v3_root);
        return;
    }
    v3_copy_text(out, cap, package_root);
}

static void v3_package_id_from_root(const char *package_root, char *out, size_t cap) {
    const char *slash = strrchr(package_root, '/');
    const char *base = slash ? slash + 1 : package_root;
    if (strcmp(base, "v3") == 0) {
        v3_copy_text(out, cap, "cheng/v3");
        return;
    }
    snprintf(out, cap, "cheng/%s", base);
}

static bool v3_path_last_component_is(const char *path, const char *name) {
    const char *slash;
    const char *base;
    if (!path || !name) {
        return false;
    }
    slash = strrchr(path, '/');
    base = slash ? slash + 1 : path;
    return strcmp(base, name) == 0;
}

static bool v3_path_is_within_dir(const char *path, const char *dir) {
    char dir_copy[PATH_MAX];
    size_t dir_len;
    if (!path || path[0] == '\0' || !dir || dir[0] == '\0') {
        return false;
    }
    v3_copy_text(dir_copy, sizeof(dir_copy), dir);
    dir_len = strlen(dir_copy);
    while (dir_len > 1U && dir_copy[dir_len - 1U] == '/') {
        dir_copy[--dir_len] = '\0';
    }
    if (strcmp(path, dir_copy) == 0) {
        return true;
    }
    return strncmp(path, dir_copy, dir_len) == 0 && path[dir_len] == '/';
}

static void v3_resolve_package_source_path(const char *package_root,
                                           const char *source_path,
                                           char *out,
                                           size_t cap) {
    char workspace_root[PATH_MAX];
    if (!source_path || source_path[0] == '\0') {
        out[0] = '\0';
        return;
    }
    if (v3_const_os_is_absolute(source_path)) {
        v3_copy_text(out, cap, source_path);
        return;
    }
    v3_workspace_root_from_package_root(package_root, workspace_root, sizeof(workspace_root));
    v3_join_path(out, cap, workspace_root, source_path);
}

static void v3_find_nearest_package_root(const char *base_root,
                                         const char *source_abs,
                                         char *out,
                                         size_t cap) {
    char cursor[PATH_MAX];
    char parent[PATH_MAX];
    const char *slash;
    out[0] = '\0';
    if (!base_root || base_root[0] == '\0' || !source_abs || source_abs[0] == '\0') {
        return;
    }
    slash = strrchr(source_abs, '/');
    if (!slash || slash == source_abs) {
        if (v3_package_manifest_exists(base_root)) {
            v3_copy_text(out, cap, base_root);
        }
        return;
    }
    snprintf(cursor, sizeof(cursor), "%.*s", (int)(slash - source_abs), source_abs);
    while (cursor[0] != '\0') {
        if (strcmp(cursor, base_root) != 0 &&
            !v3_path_is_within_dir(cursor, base_root)) {
            break;
        }
        if (v3_package_manifest_exists(cursor)) {
            v3_copy_text(out, cap, cursor);
            return;
        }
        if (!v3_parent_dir(cursor, parent, sizeof(parent)) ||
            parent[0] == '\0' ||
            strcmp(parent, cursor) == 0) {
            break;
        }
        v3_copy_text(cursor, sizeof(cursor), parent);
    }
    if (v3_package_manifest_exists(base_root)) {
        v3_copy_text(out, cap, base_root);
    }
}

static void v3_resolve_effective_package_root(const V3BootstrapContract *contract,
                                              const char *package_root_flag,
                                              const char *source_path,
                                              char *out,
                                              size_t cap) {
    char default_package_root[PATH_MAX];
    char package_root[PATH_MAX];
    char base_root[PATH_MAX];
    char source_abs[PATH_MAX];
    char v3_package_root[PATH_MAX];
    char nearest_package_root[PATH_MAX];
    v3_default_package_root(contract, default_package_root, sizeof(default_package_root));
    v3_copy_text(package_root,
                 sizeof(package_root),
                 package_root_flag && *package_root_flag ? package_root_flag : default_package_root);
    v3_copy_text(base_root, sizeof(base_root), package_root);
    v3_resolve_package_source_path(package_root, source_path, source_abs, sizeof(source_abs));
    if (!v3_path_last_component_is(base_root, "v3") && source_abs[0] != '\0') {
        v3_join_path(v3_package_root, sizeof(v3_package_root), base_root, "v3");
        if (v3_path_is_within_dir(source_abs, v3_package_root) &&
            v3_package_manifest_exists(v3_package_root)) {
            v3_copy_text(base_root, sizeof(base_root), v3_package_root);
        }
    }
    if (source_abs[0] != '\0' && v3_path_is_within_dir(source_abs, base_root)) {
        v3_find_nearest_package_root(base_root, source_abs, nearest_package_root, sizeof(nearest_package_root));
        if (nearest_package_root[0] != '\0') {
            v3_copy_text(out, cap, nearest_package_root);
            return;
        }
    }
    v3_copy_text(out, cap, base_root);
}

static bool v3_path_exists_nonempty(const char *path) {
    struct stat st;
    if (!path || *path == '\0') {
        return false;
    }
    if (stat(path, &st) != 0) {
        return false;
    }
    return S_ISREG(st.st_mode) && st.st_size > 0;
}

static bool v3_plan_paths_contains(const V3PlanPath *items, size_t len, const char *path) {
    size_t i;
    for (i = 0; i < len; ++i) {
        if (v3_streq(items[i].text, path)) {
            return true;
        }
    }
    return false;
}

static void v3_plan_add_reason(V3SystemLinkPlanStub *plan, const char *reason) {
    if (plan->missing_reason_count >= CHENG_V3_MAX_PLAN_REASONS) {
        return;
    }
    snprintf(plan->missing_reasons[plan->missing_reason_count],
             sizeof(plan->missing_reasons[plan->missing_reason_count]),
             "%s",
             reason);
    plan->missing_reason_count += 1U;
}

static void v3_lowering_add_reason(V3LoweringPlanStub *plan, const char *reason) {
    if (plan->missing_reason_count >= CHENG_V3_MAX_PLAN_REASONS) {
        return;
    }
    snprintf(plan->missing_reasons[plan->missing_reason_count],
             sizeof(plan->missing_reasons[plan->missing_reason_count]),
             "%s",
             reason);
    plan->missing_reason_count += 1U;
}

static void v3_object_add_reason(V3ObjectPlanStub *plan, const char *reason) {
    if (plan->missing_reason_count >= CHENG_V3_MAX_PLAN_REASONS) {
        return;
    }
    snprintf(plan->missing_reasons[plan->missing_reason_count],
             sizeof(plan->missing_reasons[plan->missing_reason_count]),
             "%s",
             reason);
    plan->missing_reason_count += 1U;
}

static void v3_primary_add_reason(V3PrimaryObjectPlanStub *plan, const char *reason) {
    if (plan->missing_reason_count >= CHENG_V3_MAX_PLAN_REASONS) {
        return;
    }
    snprintf(plan->missing_reasons[plan->missing_reason_count],
             sizeof(plan->missing_reasons[plan->missing_reason_count]),
             "%s",
             reason);
    plan->missing_reason_count += 1U;
}

static void v3_primary_add_unsupported(V3PrimaryObjectPlanStub *plan,
                                       const V3LoweredFunctionStub *function) {
    size_t i;
    char abi_summary[256];
    if (plan->unsupported_function_count >= CHENG_V3_MAX_UNSUPPORTED_DETAILS) {
        return;
    }
    for (i = 0; i < plan->unsupported_function_count; ++i) {
        if (strcmp(plan->unsupported_function_symbols[i].text, function->symbol_text) == 0) {
            return;
        }
    }
    snprintf(plan->unsupported_function_symbols[plan->unsupported_function_count].text,
             sizeof(plan->unsupported_function_symbols[plan->unsupported_function_count].text),
             "%s",
             function->symbol_text);
    snprintf(plan->unsupported_body_kinds[plan->unsupported_function_count],
             sizeof(plan->unsupported_body_kinds[plan->unsupported_function_count]),
             "%s",
             function->body_kind);
    snprintf(plan->unsupported_first_lines[plan->unsupported_function_count],
             sizeof(plan->unsupported_first_lines[plan->unsupported_function_count]),
             "%s",
             function->first_body_line);
    v3_lowered_function_abi_summary(function, abi_summary, sizeof(abi_summary));
    snprintf(plan->unsupported_abi_summaries[plan->unsupported_function_count],
             sizeof(plan->unsupported_abi_summaries[plan->unsupported_function_count]),
             "%s",
             abi_summary);
    plan->unsupported_function_count += 1U;
}

static void v3_native_link_add_reason(V3NativeLinkPlanStub *plan, const char *reason) {
    if (plan->missing_reason_count >= CHENG_V3_MAX_PLAN_REASONS) {
        return;
    }
    snprintf(plan->missing_reasons[plan->missing_reason_count],
             sizeof(plan->missing_reasons[plan->missing_reason_count]),
             "%s",
             reason);
    plan->missing_reason_count += 1U;
}

static bool v3_plan_reasons_contains(char reasons[CHENG_V3_MAX_PLAN_REASONS][128],
                                     size_t reason_count,
                                     const char *reason) {
    size_t i;
    for (i = 0; i < reason_count; ++i) {
        if (strcmp(reasons[i], reason) == 0) {
            return true;
        }
    }
    return false;
}

static void v3_plan_add_path(V3PlanPath *items, size_t *len, size_t cap, const char *path) {
    if (*len >= cap) {
        return;
    }
    snprintf(items[*len].text, sizeof(items[*len].text), "%s", path);
    *len += 1U;
}

static bool v3_plan_import_edge_exists(const V3SystemLinkPlanStub *plan,
                                       const V3PlanImportEdge *edge) {
    size_t i;
    for (i = 0U; i < plan->import_edge_count; ++i) {
        if (v3_streq(plan->import_edges[i].owner_module_path, edge->owner_module_path) &&
            v3_streq(plan->import_edges[i].target_module_path, edge->target_module_path) &&
            v3_streq(plan->import_edges[i].target_source_path, edge->target_source_path) &&
            plan->import_edges[i].resolved == edge->resolved) {
            return true;
        }
    }
    return false;
}

static void v3_populate_runtime_requirements(V3SystemLinkPlanStub *plan) {
    if (!v3_streq(plan->emit_kind, "exe")) {
        return;
    }
    if (!v3_target_needs_native_runtime_providers(plan->target_triple)) {
        v3_plan_add_path(plan->runtime_targets,
                         &plan->runtime_target_count,
                         CHENG_V3_MAX_RUNTIME_TARGETS,
                         "runtime/compiler.program_wasm_entry");
        return;
    }
    if (v3_streq(plan->module_kind, "compiler_control_plane")) {
        v3_plan_add_path(plan->runtime_targets,
                         &plan->runtime_target_count,
                         CHENG_V3_MAX_RUNTIME_TARGETS,
                         "runtime/compiler.tooling_argv_entry");
    } else {
        v3_plan_add_path(plan->runtime_targets,
                         &plan->runtime_target_count,
                         CHENG_V3_MAX_RUNTIME_TARGETS,
                         "runtime/compiler.program_argv_entry");
    }
    if (!v3_target_requires_native_provider_modules(plan->target_triple)) {
        return;
    }
    v3_plan_add_path(plan->provider_modules,
                     &plan->provider_module_count,
                     CHENG_V3_MAX_PROVIDER_MODULES,
                     "runtime/compiler_runtime_v3");
    v3_plan_add_path(plan->provider_modules,
                     &plan->provider_module_count,
                     CHENG_V3_MAX_PROVIDER_MODULES,
                     "runtime/core_runtime_v3");
    v3_plan_add_path(plan->provider_modules,
                     &plan->provider_module_count,
                     CHENG_V3_MAX_PROVIDER_MODULES,
                     "runtime/debug_runtime_v3");
    v3_plan_add_path(plan->provider_modules,
                     &plan->provider_module_count,
                     CHENG_V3_MAX_PROVIDER_MODULES,
                     "runtime/program_support_v3");
    v3_plan_add_path(plan->provider_modules,
                     &plan->provider_module_count,
                     CHENG_V3_MAX_PROVIDER_MODULES,
                     "runtime/program_support_host_v3");
}

static void v3_source_path_to_module_path(const char *workspace_root,
                                          const char *package_root,
                                          const char *package_id,
                                          const char *source_path,
                                          char *out,
                                          size_t cap) {
    char prefix[PATH_MAX];
    char rel[PATH_MAX];
    char stem[PATH_MAX];
    char shared_v3_root[PATH_MAX];
    const char *last_slash;
    snprintf(prefix, sizeof(prefix), "%s/src/std/", workspace_root);
    if (v3_startswith(source_path, prefix)) {
        snprintf(rel, sizeof(rel), "%s", source_path + strlen(prefix));
        v3_strip_ext(rel);
        snprintf(out, cap, "std/%s", rel);
        return;
    }
    snprintf(prefix, sizeof(prefix), "%s/src/core/", workspace_root);
    if (v3_startswith(source_path, prefix)) {
        snprintf(rel, sizeof(rel), "%s", source_path + strlen(prefix));
        v3_strip_ext(rel);
        snprintf(out, cap, "core/%s", rel);
        return;
    }
    snprintf(prefix, sizeof(prefix), "%s/src/", package_root);
    if (v3_startswith(source_path, prefix)) {
        snprintf(rel, sizeof(rel), "%s", source_path + strlen(prefix));
        v3_strip_ext(rel);
        snprintf(out, cap, "%s/%s", package_id, rel);
        return;
    }
    v3_shared_v3_package_root(workspace_root, shared_v3_root, sizeof(shared_v3_root));
    if (shared_v3_root[0] != '\0') {
        snprintf(prefix, sizeof(prefix), "%s/src/", shared_v3_root);
        if (v3_startswith(source_path, prefix)) {
            snprintf(rel, sizeof(rel), "%s", source_path + strlen(prefix));
            v3_strip_ext(rel);
            snprintf(out, cap, "cheng/v3/%s", rel);
            return;
        }
    }
    last_slash = strrchr(source_path, '/');
    snprintf(stem, sizeof(stem), "%s", last_slash ? last_slash + 1 : source_path);
    v3_strip_ext(stem);
    v3_copy_text(out, cap, stem);
}

static void v3_module_path_to_source_path(const char *workspace_root,
                                          const char *package_root,
                                          const char *package_id,
                                          const char *module_path,
                                          char *out,
                                          size_t cap) {
    char shared_v3_root[PATH_MAX];
    out[0] = '\0';
    if (v3_startswith(module_path, "std/")) {
        char rel[PATH_MAX];
        snprintf(rel, sizeof(rel), "src/%s.cheng", module_path);
        v3_join_path(out, cap, workspace_root, rel);
        return;
    }
    if (v3_startswith(module_path, "core/")) {
        char rel[PATH_MAX];
        snprintf(rel, sizeof(rel), "src/%s.cheng", module_path);
        v3_join_path(out, cap, workspace_root, rel);
        return;
    }
    v3_shared_v3_package_root(workspace_root, shared_v3_root, sizeof(shared_v3_root));
    if (shared_v3_root[0] != '\0' &&
        (v3_streq(module_path, "cheng/v3") ||
         (v3_startswith(module_path, "cheng/v3/") && module_path[strlen("cheng/v3")] == '/'))) {
        const char *tail = module_path + strlen("cheng/v3");
        char rel[PATH_MAX];
        if (*tail == '/') {
            tail++;
        }
        if (*tail != '\0') {
            snprintf(rel, sizeof(rel), "src/%s.cheng", tail);
            v3_join_path(out, cap, shared_v3_root, rel);
        }
        return;
    }
    if (v3_streq(module_path, package_id) ||
        (v3_startswith(module_path, package_id) && module_path[strlen(package_id)] == '/')) {
        const char *tail = module_path + strlen(package_id);
        char rel[PATH_MAX];
        if (*tail == '/') {
            tail++;
        }
        snprintf(rel, sizeof(rel), "src/%s.cheng", tail);
        v3_join_path(out, cap, package_root, rel);
        return;
    }
}

static bool v3_source_has_top_level(const char *text) {
    char *owned = v3_strdup(text);
    char *cursor = owned;
    char *line;
    bool out = false;
    while ((line = strsep(&cursor, "\n")) != NULL) {
        char *trimmed = v3_trim_inplace(line);
        if (v3_startswith(trimmed, "import ") ||
            v3_startswith(trimmed, "type") ||
            v3_startswith(trimmed, "fn ") ||
            v3_startswith(trimmed, "iterator ") ||
            v3_startswith(trimmed, "const ") ||
            v3_startswith(trimmed, "let ") ||
            v3_startswith(trimmed, "var ")) {
            out = true;
            break;
        }
    }
    free(owned);
    return out;
}

static bool v3_source_has_main(const char *text) {
    char *owned = v3_strdup(text);
    char *cursor = owned;
    char *line;
    bool out = false;
    while ((line = strsep(&cursor, "\n")) != NULL) {
        char *trimmed = v3_trim_inplace(line);
        if (v3_startswith(trimmed, "fn main(")) {
            out = true;
            break;
        }
    }
    free(owned);
    return out;
}

static bool v3_source_has_legacy_func_alias(const char *text) {
    char *owned = v3_strdup(text);
    char *cursor = owned;
    char *line;
    bool out = false;
    while ((line = strsep(&cursor, "\n")) != NULL) {
        char *trimmed = v3_trim_inplace(line);
        if (v3_startswith(trimmed, "func ")) {
            out = true;
            break;
        }
    }
    free(owned);
    return out;
}

static bool v3_parse_import_edges(const char *workspace_root,
                                  const char *package_root,
                                  const char *package_id,
                                  const char *owner_module_path,
                                  const char *text,
                                  V3PlanImportEdge *edges,
                                  size_t *edge_len,
                                  size_t edge_cap) {
    char *owned = v3_strdup(text);
    char *cursor = owned;
    char *line;
    while ((line = strsep(&cursor, "\n")) != NULL) {
        char *trimmed = v3_trim_inplace(line);
        char *token;
        if (!v3_startswith(trimmed, "import ")) {
            continue;
        }
        token = trimmed + strlen("import ");
        while (*token != '\0' && isspace((unsigned char)*token)) {
            token++;
        }
        if (*token == '\0') {
            free(owned);
            return false;
        }
        {
            char module_path[PATH_MAX];
            char *stop = token;
            while (*stop != '\0' && !isspace((unsigned char)*stop)) {
                stop++;
            }
            if (*stop != '\0') {
                *stop = '\0';
            }
            snprintf(module_path, sizeof(module_path), "%s", token);
            if (*edge_len >= edge_cap) {
                free(owned);
                return false;
            }
            snprintf(edges[*edge_len].owner_module_path, sizeof(edges[*edge_len].owner_module_path), "%s", owner_module_path);
            snprintf(edges[*edge_len].target_module_path, sizeof(edges[*edge_len].target_module_path), "%s", module_path);
            v3_module_path_to_source_path(workspace_root,
                                          package_root,
                                          package_id,
                                          module_path,
                                          edges[*edge_len].target_source_path,
                                          sizeof(edges[*edge_len].target_source_path));
            edges[*edge_len].resolved = v3_path_exists_nonempty(edges[*edge_len].target_source_path);
            *edge_len += 1U;
        }
    }
    free(owned);
    return true;
}

static bool v3_ident_start_char(char ch) {
    return (ch >= 'a' && ch <= 'z') ||
           (ch >= 'A' && ch <= 'Z') ||
           ch == '_';
}

static bool v3_ident_char(char ch) {
    return v3_ident_start_char(ch) ||
           (ch >= '0' && ch <= '9');
}

static const char *v3_module_default_alias(const char *module_path) {
    const char *slash = strrchr(module_path, '/');
    return slash ? slash + 1 : module_path;
}

static void v3_parse_import_aliases_from_lines(char **lines,
                                               size_t line_count,
                                               V3ImportAlias *aliases,
                                               size_t *alias_count,
                                               size_t alias_cap) {
    size_t i;
    *alias_count = 0U;
    for (i = 0; i < line_count; ++i) {
        char *trimmed = v3_trim_inplace(lines[i]);
        char *cursor;
        char module_path[PATH_MAX];
        char alias_text[128];
        char *space;
        if (!v3_startswith(trimmed, "import ")) {
            continue;
        }
        cursor = trimmed + strlen("import ");
        while (*cursor != '\0' && isspace((unsigned char)*cursor)) {
            cursor++;
        }
        if (*cursor == '\0') {
            continue;
        }
        if (strchr(cursor, '[') != NULL) {
            continue;
        }
        space = cursor;
        while (*space != '\0' && !isspace((unsigned char)*space)) {
            space++;
        }
        if (space == cursor) {
            continue;
        }
        snprintf(module_path, sizeof(module_path), "%.*s", (int)(space - cursor), cursor);
        snprintf(alias_text, sizeof(alias_text), "%s", v3_module_default_alias(module_path));
        while (*space != '\0' && isspace((unsigned char)*space)) {
            space++;
        }
        if (v3_startswith(space, "as ")) {
            char *alias_cursor = space + 3;
            char *alias_stop = alias_cursor;
            while (*alias_cursor != '\0' && isspace((unsigned char)*alias_cursor)) {
                alias_cursor++;
            }
            alias_stop = alias_cursor;
            while (*alias_stop != '\0' && !isspace((unsigned char)*alias_stop)) {
                alias_stop++;
            }
            if (alias_stop > alias_cursor) {
                snprintf(alias_text, sizeof(alias_text), "%.*s", (int)(alias_stop - alias_cursor), alias_cursor);
            }
        }
        if (*alias_count >= alias_cap) {
            return;
        }
        snprintf(aliases[*alias_count].alias_text, sizeof(aliases[*alias_count].alias_text), "%s", alias_text);
        snprintf(aliases[*alias_count].module_path, sizeof(aliases[*alias_count].module_path), "%s", module_path);
        *alias_count += 1U;
    }
}

static bool v3_load_import_aliases_from_source_path(const char *source_path,
                                                    V3ImportAlias *aliases,
                                                    size_t *alias_count,
                                                    size_t alias_cap) {
    char *owned;
    char *cursor;
    char *line;
    char *lines[CHENG_V3_MAX_SOURCE_LINES];
    size_t line_count = 0U;
    owned = v3_read_file(source_path);
    if (!owned) {
        return false;
    }
    cursor = owned;
    while ((line = strsep(&cursor, "\n")) != NULL) {
        if (line_count >= CHENG_V3_MAX_SOURCE_LINES) {
            free(owned);
            return false;
        }
        lines[line_count++] = line;
    }
    v3_parse_import_aliases_from_lines(lines, line_count, aliases, alias_count, alias_cap);
    free(owned);
    return true;
}

static const char *v3_alias_module_path(const V3ImportAlias *aliases,
                                        size_t alias_count,
                                        const char *alias_text) {
    size_t i;
    for (i = 0; i < alias_count; ++i) {
        if (strcmp(aliases[i].alias_text, alias_text) == 0) {
            return aliases[i].module_path;
        }
    }
    return NULL;
}

static bool v3_identifier_exported_by_case(const char *name) {
    return name != NULL &&
           name[0] >= 'A' &&
           name[0] <= 'Z';
}

static bool v3_is_strformat_fmt_name(const char *name) {
    return name != NULL &&
           strcmp(name, "Fmt") == 0;
}

static bool v3_is_strformat_join_name(const char *name) {
    return name != NULL &&
           (strcmp(name, "Fmt") == 0 ||
            strcmp(name, "Lines") == 0);
}

static bool v3_resolve_strformat_join_call_target(const V3SystemLinkPlanStub *plan,
                                                  const char *function_name,
                                                  V3AsmCallTarget *target) {
    if (!plan || !function_name || !target || !v3_is_strformat_join_name(function_name)) {
        return false;
    }
    memset(target, 0, sizeof(*target));
    target->ffi_handle_consume_index = -1;
    target->external = true;
    snprintf(target->callee_name, sizeof(target->callee_name), "%s", function_name);
    v3_copy_target_symbol_name(plan,
                               strcmp(function_name, "Fmt") == 0
                                   ? "cheng_v3_strformat_fmt_bridge"
                                   : "cheng_v3_strformat_lines_bridge",
                               target->symbol_name,
                               sizeof(target->symbol_name));
    snprintf(target->return_type, sizeof(target->return_type), "%s", "str");
    snprintf(target->return_abi_class, sizeof(target->return_abi_class), "%s", "composite");
    target->param_count = 1U;
    snprintf(target->param_types[0], sizeof(target->param_types[0]), "%s", "str[]");
    snprintf(target->param_abi_classes[0], sizeof(target->param_abi_classes[0]), "%s", "composite");
    return true;
}

static void v3_module_package_key(const char *module_path,
                                  char *out,
                                  size_t cap) {
    const char *normalized_path = module_path;
    const char *first_slash;
    const char *second_slash;
    size_t len;
    if (!out || cap == 0U) {
        return;
    }
    out[0] = '\0';
    if (!normalized_path || normalized_path[0] == '\0') {
        return;
    }
    if (strncmp(normalized_path, "pkg://", 6U) == 0) {
        normalized_path += 6U;
    }
    first_slash = strchr(normalized_path, '/');
    if (first_slash == NULL) {
        if (strcmp(normalized_path, "v3") == 0) {
            v3_copy_text(out, cap, "cheng/v3");
            return;
        }
        v3_copy_text(out, cap, normalized_path);
        return;
    }
    if (strncmp(normalized_path, "v3/", 3U) == 0) {
        v3_copy_text(out, cap, "cheng/v3");
        return;
    }
    if (strncmp(normalized_path, "cheng/", 6U) == 0) {
        second_slash = strchr(first_slash + 1, '/');
        if (second_slash != NULL) {
            len = (size_t)(second_slash - normalized_path);
            snprintf(out, cap, "%.*s", (int)len, normalized_path);
            return;
        }
    }
    len = (size_t)(first_slash - normalized_path);
    snprintf(out, cap, "%.*s", (int)len, normalized_path);
}

static bool v3_module_paths_share_package(const char *left_module_path,
                                          const char *right_module_path) {
    char left_key[PATH_MAX];
    char right_key[PATH_MAX];
    v3_module_package_key(left_module_path, left_key, sizeof(left_key));
    v3_module_package_key(right_module_path, right_key, sizeof(right_key));
    if (left_key[0] == '\0' || right_key[0] == '\0') {
        return false;
    }
    if ((strcmp(left_key, "cheng/v3") == 0 ||
         strcmp(left_key, "std") == 0 ||
         strcmp(left_key, "stage1") == 0) &&
        (strcmp(right_key, "cheng/v3") == 0 ||
         strcmp(right_key, "std") == 0 ||
         strcmp(right_key, "stage1") == 0)) {
        return true;
    }
    return strcmp(left_key, right_key) == 0;
}

static const char *v3_alias_module_path_exported_member(const V3ImportAlias *aliases,
                                                        size_t alias_count,
                                                        const char *owner_module_path,
                                                        const char *alias_text,
                                                        char *member_name,
                                                        size_t member_name_cap) {
    const char *module_path = v3_alias_module_path(aliases, alias_count, alias_text);
    (void)member_name_cap;
    if (module_path == NULL) {
        return NULL;
    }
    if (!v3_identifier_exported_by_case(member_name) &&
        !v3_module_paths_share_package(owner_module_path, module_path)) {
        fprintf(stderr,
                "v3 compiler: imported private symbol '%s' is not exported; use uppercase initial\n",
                member_name != NULL ? member_name : "");
        return NULL;
    }
    return module_path;
}

static bool v3_function_name_is_identifier_text(const char *name) {
    size_t i;
    if (name == NULL || name[0] == '\0' || !v3_ident_start_char(name[0])) {
        return false;
    }
    for (i = 1U; name[i] != '\0'; ++i) {
        if (!v3_ident_char(name[i])) {
            return false;
        }
    }
    return true;
}

static bool v3_function_name_visible_from_module(const char *owner_module_path,
                                                 const V3ImportAlias *aliases,
                                                 size_t alias_count,
                                                 const char *candidate_module_path,
                                                 const char *function_name) {
    size_t i;
    if (owner_module_path == NULL || owner_module_path[0] == '\0' ||
        candidate_module_path == NULL || candidate_module_path[0] == '\0' ||
        function_name == NULL || function_name[0] == '\0') {
        return false;
    }
    if (strcmp(owner_module_path, candidate_module_path) == 0) {
        return true;
    }
    for (i = 0U; i < alias_count; ++i) {
        if (strcmp(aliases[i].module_path, candidate_module_path) != 0) {
            continue;
        }
        if (!v3_function_name_is_identifier_text(function_name)) {
            return true;
        }
        return v3_identifier_exported_by_case(function_name) ||
               v3_module_paths_share_package(owner_module_path, candidate_module_path);
    }
    return false;
}

static bool v3_is_intrinsic_call_name(const char *name) {
    static const char *NAMES[] = {
        "assert",
        "echo",
        "len",
        "IsOk",
        "IsErr",
        "Value",
        "Error",
        "ErrorText",
        "ErrorInfoOf",
        "int",
        "int8",
        "int16",
        "int32",
        "int64",
        "uint8",
        "uint16",
        "uint32",
        "uint64",
        "bool",
        "char",
        "float",
        "float32",
        "float64",
        "str",
        "cstring",
        "ptr",
        "new"
    };
    size_t i;
    for (i = 0; i < sizeof(NAMES) / sizeof(NAMES[0]); ++i) {
        if (strcmp(NAMES[i], name) == 0) {
            return true;
        }
    }
    return false;
}

static bool v3_is_intrinsic_module_call(const char *module_path, const char *function_name) {
    if (strcmp(module_path, "std/os") == 0 &&
        (strcmp(function_name, "isAbsolute") == 0 ||
         strcmp(function_name, "joinPath") == 0 ||
         strcmp(function_name, "getEnvDefault") == 0 ||
         strcmp(function_name, "fileExists") == 0)) {
        return true;
    }
    if (strcmp(module_path, "std/strutils") == 0 &&
        strcmp(function_name, "strip") == 0) {
        return true;
    }
    return false;
}

static bool v3_resolve_intrinsic_module_call_target(const char *module_path,
                                                    const V3SystemLinkPlanStub *plan,
                                                    const char *function_name,
                                                    V3AsmCallTarget *target) {
    memset(target, 0, sizeof(*target));
    target->ffi_handle_consume_index = -1;
    target->external = true;
    if (strcmp(module_path, "std/strutils") == 0 &&
        strcmp(function_name, "strip") == 0) {
        snprintf(target->callee_name, sizeof(target->callee_name), "%s", "strip");
        v3_copy_target_symbol_name(plan,
                                   "cheng_v3_str_strip_bridge",
                                   target->symbol_name,
                                   sizeof(target->symbol_name));
        snprintf(target->return_type, sizeof(target->return_type), "%s", "str");
        snprintf(target->return_abi_class, sizeof(target->return_abi_class), "%s", "composite");
        target->param_count = 1U;
        snprintf(target->param_types[0], sizeof(target->param_types[0]), "%s", "str");
        snprintf(target->param_abi_classes[0], sizeof(target->param_abi_classes[0]), "%s", "composite");
        return true;
    }
    if (strcmp(module_path, "std/os") == 0 &&
        strcmp(function_name, "isAbsolute") == 0) {
        snprintf(target->callee_name, sizeof(target->callee_name), "%s", "isAbsolute");
        v3_copy_target_symbol_name(plan,
                                   "cheng_v3_os_is_absolute_bridge",
                                   target->symbol_name,
                                   sizeof(target->symbol_name));
        snprintf(target->return_type, sizeof(target->return_type), "%s", "bool");
        snprintf(target->return_abi_class, sizeof(target->return_abi_class), "%s", "bool");
        target->param_count = 1U;
        snprintf(target->param_types[0], sizeof(target->param_types[0]), "%s", "str");
        snprintf(target->param_abi_classes[0], sizeof(target->param_abi_classes[0]), "%s", "composite");
        return true;
    }
    if (strcmp(module_path, "std/os") == 0 &&
        strcmp(function_name, "joinPath") == 0) {
        snprintf(target->callee_name, sizeof(target->callee_name), "%s", "joinPath");
        v3_copy_target_symbol_name(plan,
                                   "cheng_v3_os_join_path_bridge",
                                   target->symbol_name,
                                   sizeof(target->symbol_name));
        snprintf(target->return_type, sizeof(target->return_type), "%s", "str");
        snprintf(target->return_abi_class, sizeof(target->return_abi_class), "%s", "composite");
        target->param_count = 2U;
        snprintf(target->param_types[0], sizeof(target->param_types[0]), "%s", "str");
        snprintf(target->param_types[1], sizeof(target->param_types[1]), "%s", "str");
        snprintf(target->param_abi_classes[0], sizeof(target->param_abi_classes[0]), "%s", "composite");
        snprintf(target->param_abi_classes[1], sizeof(target->param_abi_classes[1]), "%s", "composite");
        return true;
    }
    if (strcmp(module_path, "std/os") == 0 &&
        strcmp(function_name, "fileExists") == 0) {
        snprintf(target->callee_name, sizeof(target->callee_name), "%s", "fileExists");
        v3_copy_target_symbol_name(plan,
                                   "cheng_v3_os_file_exists_bridge",
                                   target->symbol_name,
                                   sizeof(target->symbol_name));
        snprintf(target->return_type, sizeof(target->return_type), "%s", "bool");
        snprintf(target->return_abi_class, sizeof(target->return_abi_class), "%s", "bool");
        target->param_count = 1U;
        snprintf(target->param_types[0], sizeof(target->param_types[0]), "%s", "str");
        snprintf(target->param_abi_classes[0], sizeof(target->param_abi_classes[0]), "%s", "composite");
        return true;
    }
    return false;
}

static bool v3_is_builtin_statement_call_name(const char *callee) {
    if (!callee || *callee == '\0') {
        return false;
    }
    return strcmp(callee, "add") == 0 ||
           strcmp(callee, "assert") == 0 ||
           strcmp(callee, "echo") == 0 ||
           strcmp(callee, "panic") == 0 ||
           strcmp(callee, "system.panic") == 0 ||
           strcmp(callee, "store") == 0;
}

static void v3_lowered_function_add_callee(V3LoweredFunctionStub *function,
                                           const char *symbol_text) {
    size_t i;
    if (!symbol_text || *symbol_text == '\0') {
        return;
    }
    for (i = 0; i < function->callee_count; ++i) {
        if (strcmp(function->callee_symbols[i].text, symbol_text) == 0) {
            return;
        }
    }
    if (function->callee_count >= CHENG_V3_MAX_CALLEES) {
        return;
    }
    snprintf(function->callee_symbols[function->callee_count].text,
             sizeof(function->callee_symbols[function->callee_count].text),
             "%s",
             symbol_text);
    function->callee_count += 1U;
}

static void v3_lowered_function_add_all_callees_by_name(const V3LoweringPlanStub *lowering,
                                                        V3LoweredFunctionStub *function,
                                                        const char *function_name) {
    size_t i;
    if (!function_name || *function_name == '\0') {
        return;
    }
    for (i = 0; i < lowering->function_count; ++i) {
        if (strcmp(lowering->functions[i].function_name, function_name) == 0) {
            v3_lowered_function_add_callee(function, lowering->functions[i].symbol_text);
        }
    }
}

static void v3_lowered_function_add_all_callees_by_module_and_name(const V3LoweringPlanStub *lowering,
                                                                   V3LoweredFunctionStub *function,
                                                                   const char *module_path,
                                                                   const char *function_name) {
    size_t i;
    if (!module_path || module_path[0] == '\0' ||
        !function_name || function_name[0] == '\0') {
        return;
    }
    for (i = 0; i < lowering->function_count; ++i) {
        if (strcmp(lowering->functions[i].owner_module_path, module_path) == 0 &&
            strcmp(lowering->functions[i].function_name, function_name) == 0) {
            v3_lowered_function_add_callee(function, lowering->functions[i].symbol_text);
        }
    }
}

static CHENG_V3_UNUSED void v3_lowered_function_add_visible_callees_by_name(const V3LoweringPlanStub *lowering,
                                                                            V3LoweredFunctionStub *function,
                                                                            const char *owner_module_path,
                                                                            const V3ImportAlias *aliases,
                                                                            size_t alias_count,
                                                                            const char *function_name) {
    size_t i;
    if (!lowering || !function || !owner_module_path || owner_module_path[0] == '\0' ||
        !function_name || function_name[0] == '\0') {
        return;
    }
    for (i = 0U; i < lowering->function_count; ++i) {
        if (strcmp(lowering->functions[i].function_name, function_name) != 0) {
            continue;
        }
        if (strcmp(function_name, "[]") == 0 &&
            (strcmp(lowering->functions[i].owner_module_path, "std/system") == 0 ||
             strcmp(lowering->functions[i].owner_module_path, "std/strings") == 0)) {
            continue;
        }
        if (!v3_function_name_visible_from_module(owner_module_path,
                                                  aliases,
                                                  alias_count,
                                                  lowering->functions[i].owner_module_path,
                                                  function_name)) {
            continue;
        }
        v3_lowered_function_add_callee(function, lowering->functions[i].symbol_text);
    }
}

static void v3_lowered_function_overload_symbol_text(const V3LoweredFunctionStub *function,
                                                     char *out,
                                                     size_t cap) {
    char key[4096];
    uint64_t hash;
    size_t i;
    if (!function || !out || cap == 0U) {
        return;
    }
    snprintf(key,
             sizeof(key),
             "%s::%s->%s",
             function->owner_module_path,
             function->function_name,
             function->return_type);
    for (i = 0U; i < function->param_count; ++i) {
        size_t used = strlen(key);
        if (used + 8U >= sizeof(key)) {
            break;
        }
        snprintf(key + used,
                 sizeof(key) - used,
                 "|%s%s",
                 function->param_is_var[i] ? "var " : "",
                 function->param_types[i]);
    }
    hash = v3_fnv1a64(key);
    snprintf(out,
             cap,
             "%s::%s$o%016llx",
             function->owner_module_path,
             function->function_name,
             (unsigned long long)hash);
}

static void v3_assign_unique_lowered_symbol_texts(V3LoweringPlanStub *lowering,
                                                  size_t current_index) {
    const char *module_path;
    const char *function_name;
    size_t match_count = 0U;
    size_t i;
    if (!lowering || current_index >= lowering->function_count) {
        return;
    }
    module_path = lowering->functions[current_index].owner_module_path;
    function_name = lowering->functions[current_index].function_name;
    for (i = 0U; i <= current_index; ++i) {
        if (strcmp(lowering->functions[i].owner_module_path, module_path) == 0 &&
            strcmp(lowering->functions[i].function_name, function_name) == 0) {
            match_count += 1U;
        }
    }
    if (match_count <= 1U) {
        snprintf(lowering->functions[current_index].symbol_text,
                 sizeof(lowering->functions[current_index].symbol_text),
                 "%s::%s",
                 module_path,
                 function_name);
        return;
    }
    for (i = 0U; i <= current_index; ++i) {
        if (strcmp(lowering->functions[i].owner_module_path, module_path) == 0 &&
            strcmp(lowering->functions[i].function_name, function_name) == 0) {
            v3_lowered_function_overload_symbol_text(&lowering->functions[i],
                                                     lowering->functions[i].symbol_text,
                                                     sizeof(lowering->functions[i].symbol_text));
        }
    }
}

static int32_t v3_find_lowered_function_index_by_symbol_scan(const V3LoweringPlanStub *lowering,
                                                             const char *symbol_text) {
    size_t i;
    for (i = 0; i < lowering->function_count; ++i) {
        if (strcmp(lowering->functions[i].symbol_text, symbol_text) == 0) {
            return (int32_t)i;
        }
    }
    return -1;
}

static size_t v3_count_lowered_functions_by_module_and_name(const V3LoweringPlanStub *lowering,
                                                            const char *module_path,
                                                            const char *function_name) {
    size_t i;
    size_t count = 0U;
    for (i = 0; i < lowering->function_count; ++i) {
        if (strcmp(lowering->functions[i].owner_module_path, module_path) == 0 &&
            strcmp(lowering->functions[i].function_name, function_name) == 0) {
            count += 1U;
        }
    }
    return count;
}

static int32_t v3_find_unique_lowered_function_index_by_module_and_name(const V3LoweringPlanStub *lowering,
                                                                        const char *module_path,
                                                                        const char *function_name) {
    size_t i;
    int32_t found = -1;
    if (!lowering || !module_path || module_path[0] == '\0' ||
        !function_name || function_name[0] == '\0') {
        return -1;
    }
    for (i = 0U; i < lowering->function_count; ++i) {
        if (strcmp(lowering->functions[i].owner_module_path, module_path) == 0 &&
            strcmp(lowering->functions[i].function_name, function_name) == 0) {
            if (found >= 0) {
                return -2;
            }
            found = (int32_t)i;
        }
    }
    return found;
}

static size_t v3_count_lowered_functions_by_name(const V3LoweringPlanStub *lowering,
                                                 const char *function_name) {
    size_t i;
    size_t count = 0U;
    for (i = 0; i < lowering->function_count; ++i) {
        if (strcmp(lowering->functions[i].function_name, function_name) == 0) {
            count += 1U;
        }
    }
    return count;
}

static int32_t v3_find_unique_lowered_function_index_by_name(const V3LoweringPlanStub *lowering,
                                                             const char *function_name) {
    size_t i;
    int32_t found = -1;
    for (i = 0; i < lowering->function_count; ++i) {
        if (strcmp(lowering->functions[i].function_name, function_name) == 0) {
            if (found >= 0) {
                return -2;
            }
            found = (int32_t)i;
        }
    }
    return found;
}

static CHENG_V3_UNUSED void v3_strip_param_default_expr_text(const char *text,
                                                             char *out,
                                                             size_t cap) {
    char trimmed[4096];
    int32_t eq_index;
    if (!out || cap == 0U) {
        return;
    }
    out[0] = '\0';
    if (!text) {
        return;
    }
    v3_trim_copy_text(text, trimmed, sizeof(trimmed));
    eq_index = v3_find_top_level_assignment_eq(trimmed);
    if (eq_index >= 0) {
        snprintf(out, cap, "%.*s", eq_index, trimmed);
    } else {
        snprintf(out, cap, "%s", trimmed);
    }
    v3_trim_copy_text(out, out, cap);
}

static bool v3_resolve_bare_call_symbol(const V3LoweringPlanStub *lowering,
                                        const char *owner_module_path,
                                        const V3ImportAlias *aliases,
                                        size_t alias_count,
                                        const char *function_name,
                                        char *out,
                                        size_t cap) {
    size_t match_count = 0U;
    char candidate[PATH_MAX];
    if (!lowering || !owner_module_path || !function_name || !out || cap == 0U) {
        return false;
    }
    if (owner_module_path[0] == '\0' || function_name[0] == '\0') {
        out[0] = '\0';
        return false;
    }
    {
        int32_t local_index =
            v3_find_unique_lowered_function_index_by_module_and_name(lowering, owner_module_path, function_name);
        if (local_index >= 0) {
            snprintf(out, cap, "%s", lowering->functions[local_index].symbol_text);
            return true;
        }
    }
    if (v3_count_lowered_functions_by_module_and_name(lowering, owner_module_path, function_name) == 1U) {
        snprintf(candidate, sizeof(candidate), "%s::%s", owner_module_path, function_name);
        snprintf(out, cap, "%s", candidate);
        return true;
    }
    out[0] = '\0';
    (void)aliases;
    (void)alias_count;
    if (match_count == 1U) {
        return true;
    }
    if (v3_count_lowered_functions_by_name(lowering, function_name) == 1U) {
        int32_t global_index = v3_find_unique_lowered_function_index_by_name(lowering, function_name);
        if (global_index >= 0 && lowering->functions[global_index].symbol_text[0] != '\0') {
            snprintf(out, cap, "%s", lowering->functions[global_index].symbol_text);
            return true;
        }
    }
    return false;
}

static void v3_strip_trailing_type_args_text(const char *text, char *out, size_t cap) {
    char trimmed[PATH_MAX];
    int32_t depth = 0;
    int32_t open_index = -1;
    size_t i;
    size_t len;
    v3_trim_copy_text(text, trimmed, sizeof(trimmed));
    len = strlen(trimmed);
    for (i = 0; i < len; ++i) {
        if (trimmed[i] == '[') {
            if (depth == 0) {
                open_index = (int32_t)i;
            }
            depth += 1;
            continue;
        }
        if (trimmed[i] == ']') {
            if (depth > 0) {
                depth -= 1;
            }
            continue;
        }
    }
    if (open_index > 0 && len > 0U && trimmed[len - 1U] == ']' && depth == 0) {
        snprintf(out, cap, "%.*s", open_index, trimmed);
        v3_trim_copy_text(out, out, cap);
        return;
    }
    v3_copy_text(out, cap, trimmed);
}

static bool v3_extract_trailing_type_arg_text(const char *text, char *out, size_t cap) {
    char trimmed[PATH_MAX];
    int32_t depth = 0;
    int32_t open_index = -1;
    size_t i;
    size_t len;
    v3_trim_copy_text(text, trimmed, sizeof(trimmed));
    len = strlen(trimmed);
    for (i = 0; i < len; ++i) {
        if (trimmed[i] == '[') {
            if (depth == 0) {
                open_index = (int32_t)i;
            }
            depth += 1;
            continue;
        }
        if (trimmed[i] == ']') {
            if (depth == 0) {
                return false;
            }
            depth -= 1;
            continue;
        }
    }
    if (open_index <= 0 || len <= (size_t)open_index + 1U || trimmed[len - 1U] != ']' || depth != 0) {
        return false;
    }
    snprintf(out, cap, "%.*s", (int)(len - (size_t)open_index - 2U), trimmed + open_index + 1);
    v3_trim_copy_text(out, out, cap);
    return out[0] != '\0';
}

static bool v3_parse_default_expr_text(const char *expr_text,
                                       char *type_out,
                                       size_t type_cap) {
    char base[128];
    if (!expr_text || !type_out || type_cap == 0U) {
        return false;
    }
    v3_strip_trailing_type_args_text(expr_text, base, sizeof(base));
    if (strcmp(base, "default") != 0) {
        return false;
    }
    if (!v3_extract_trailing_type_arg_text(expr_text, type_out, type_cap)) {
        return false;
    }
    v3_trim_copy_text(type_out, type_out, type_cap);
    return type_out[0] != '\0';
}

static V3ResultIntrinsicKind v3_result_intrinsic_kind_from_name(const char *function_name) {
    if (strcmp(function_name, "IsOk") == 0) {
        return V3_RESULT_INTRINSIC_IS_OK;
    }
    if (strcmp(function_name, "IsErr") == 0) {
        return V3_RESULT_INTRINSIC_IS_ERR;
    }
    if (strcmp(function_name, "Value") == 0) {
        return V3_RESULT_INTRINSIC_VALUE;
    }
    if (strcmp(function_name, "Error") == 0) {
        return V3_RESULT_INTRINSIC_ERROR;
    }
    if (strcmp(function_name, "ErrorText") == 0) {
        return V3_RESULT_INTRINSIC_ERROR_TEXT;
    }
    if (strcmp(function_name, "ErrorInfoOf") == 0) {
        return V3_RESULT_INTRINSIC_ERROR_INFO_OF;
    }
    if (strcmp(function_name, "Ok") == 0) {
        return V3_RESULT_INTRINSIC_OK;
    }
    if (strcmp(function_name, "Err") == 0) {
        return V3_RESULT_INTRINSIC_ERR;
    }
    if (strcmp(function_name, "ErrCode") == 0) {
        return V3_RESULT_INTRINSIC_ERR_CODE;
    }
    if (strcmp(function_name, "ErrInfo") == 0) {
        return V3_RESULT_INTRINSIC_ERR_INFO;
    }
    return V3_RESULT_INTRINSIC_NONE;
}

static V3ResultIntrinsicKind v3_resolve_result_intrinsic_kind(const V3LoweringPlanStub *lowering,
                                                              const char *owner_module_path,
                                                              const V3ImportAlias *aliases,
                                                              size_t alias_count,
                                                              const char *callee) {
    char stripped[PATH_MAX];
    char resolved_symbol[PATH_MAX];
    char module_or_alias[PATH_MAX];
    char function_name[128];
    char direct[PATH_MAX];
    char *dot;
    char *scope;
    V3ResultIntrinsicKind kind;
    v3_strip_trailing_type_args_text(callee, stripped, sizeof(stripped));
    if (stripped[0] == '\0') {
        return V3_RESULT_INTRINSIC_NONE;
    }
    snprintf(direct, sizeof(direct), "%s", stripped);
    scope = strrchr(direct, ':');
    if (scope && scope != direct && scope[-1] == ':') {
        scope[-1] = '\0';
        kind = v3_result_intrinsic_kind_from_name(scope + 1);
        if (kind != V3_RESULT_INTRINSIC_NONE && strcmp(direct, "std/result") == 0) {
            return kind;
        }
        return V3_RESULT_INTRINSIC_NONE;
    }
    snprintf(module_or_alias, sizeof(module_or_alias), "%s", stripped);
    dot = strchr(module_or_alias, '.');
    if (dot) {
        const char *module_path;
        *dot = '\0';
        snprintf(function_name, sizeof(function_name), "%s", dot + 1);
        kind = v3_result_intrinsic_kind_from_name(function_name);
        if (kind == V3_RESULT_INTRINSIC_NONE) {
            return V3_RESULT_INTRINSIC_NONE;
        }
        module_path = v3_alias_module_path(aliases, alias_count, module_or_alias);
        if (module_path && strcmp(module_path, "std/result") == 0) {
            return kind;
        }
        return V3_RESULT_INTRINSIC_NONE;
    }
    kind = v3_result_intrinsic_kind_from_name(stripped);
    if (kind == V3_RESULT_INTRINSIC_NONE) {
        return V3_RESULT_INTRINSIC_NONE;
    }
    (void)lowering;
    (void)owner_module_path;
    (void)aliases;
    (void)alias_count;
    resolved_symbol[0] = '\0';
    return kind;
}

static void v3_collect_calls_from_text_resolved(const char *expr_text,
                                                const char *owner_module_path,
                                                const V3ImportAlias *aliases,
                                                size_t alias_count,
                                                const V3LoweringPlanStub *lowering,
                                                V3LoweredFunctionStub *function);

static bool v3_collect_calls_from_fmt_template_token(const char *token,
                                                     const char *arg_start,
                                                     const char *owner_module_path,
                                                     const V3ImportAlias *aliases,
                                                     size_t alias_count,
                                                     const V3LoweringPlanStub *lowering,
                                                     V3LoweredFunctionStub *function) {
    char fmt_expr[8192];
    char expanded_expr[32768];
    char fmt_error[256];
    const char *base = NULL;
    size_t literal_len = 0U;
    size_t i;
    bool changed = false;
    if (!token || !arg_start || arg_start[0] != '"' || !owner_module_path || !lowering || !function) {
        return false;
    }
    base = strrchr(token, '.');
    base = base ? base + 1 : token;
    if (!v3_is_strformat_fmt_name(base)) {
        return false;
    }
    for (i = 0U; arg_start[i] != '\0'; ++i) {
        if (arg_start[i] == '"' && !v3_quote_is_escaped(arg_start, i)) {
            if (i == 0U) {
                continue;
            }
            literal_len = i + 1U;
            break;
        }
    }
    if (literal_len == 0U) {
        return false;
    }
    snprintf(fmt_expr, sizeof(fmt_expr), "%s%.*s", token, (int)literal_len, arg_start);
    if (!v3_expand_strformat_fmt_expr(fmt_expr,
                                      expanded_expr,
                                      sizeof(expanded_expr),
                                      &changed,
                                      fmt_error,
                                      sizeof(fmt_error)) ||
        !changed) {
        return false;
    }
    v3_collect_calls_from_text_resolved(expanded_expr,
                                        owner_module_path,
                                        aliases,
                                        alias_count,
                                        lowering,
                                        function);
    return true;
}

static size_t v3_build_param_locals_for_call_collection(const V3LoweredFunctionStub *function,
                                                        V3AsmLocalSlot *locals,
                                                        size_t local_cap) {
    size_t i;
    size_t count = 0U;
    if (!function || !locals || local_cap == 0U) {
        return 0U;
    }
    memset(locals, 0, sizeof(V3AsmLocalSlot) * local_cap);
    for (i = 0U; i < function->param_count && count < local_cap; ++i) {
        snprintf(locals[count].name, sizeof(locals[count].name), "%s", function->param_names[i]);
        snprintf(locals[count].type_text, sizeof(locals[count].type_text), "%s", function->param_types[i]);
        snprintf(locals[count].abi_class,
                 sizeof(locals[count].abi_class),
                 "%s",
                 function->param_abi_classes[i][0] != '\0'
                     ? function->param_abi_classes[i]
                     : v3_type_abi_class(function->param_types[i]));
        locals[count].indirect_value = v3_abi_descriptor_from_class(locals[count].abi_class).prefers_address;
        count += 1U;
    }
    return count;
}

static bool v3_extract_call_expr_from_cursor(const char *token_start,
                                             const char *paren_start,
                                             char *out,
                                             size_t cap) {
    const char *cursor;
    int32_t paren_depth = 0;
    int32_t bracket_depth = 0;
    int32_t brace_depth = 0;
    bool in_string = false;
    if (!token_start || !paren_start || *paren_start != '(' || !out || cap == 0U) {
        return false;
    }
    cursor = token_start;
    while (*cursor != '\0') {
        size_t index = (size_t)(cursor - token_start);
        if (*cursor == '"' && !v3_quote_is_escaped(token_start, index)) {
            in_string = !in_string;
            cursor += 1;
            continue;
        }
        if (!in_string) {
            if (*cursor == '(') {
                paren_depth += 1;
            } else if (*cursor == ')') {
                paren_depth -= 1;
                if (paren_depth == 0 && bracket_depth == 0 && brace_depth == 0) {
                    cursor += 1;
                    snprintf(out, cap, "%.*s", (int)(cursor - token_start), token_start);
                    v3_trim_copy_text(out, out, cap);
                    return true;
                }
            } else if (*cursor == '[') {
                bracket_depth += 1;
            } else if (*cursor == ']') {
                if (bracket_depth > 0) {
                    bracket_depth -= 1;
                }
            } else if (*cursor == '{') {
                brace_depth += 1;
            } else if (*cursor == '}') {
                if (brace_depth > 0) {
                    brace_depth -= 1;
                }
            }
        }
        cursor += 1;
    }
    return false;
}

static void v3_lowered_function_add_preferred_visible_operator_callees(const V3LoweringPlanStub *lowering,
                                                                       V3LoweredFunctionStub *function,
                                                                       const char *owner_module_path,
                                                                       const V3ImportAlias *aliases,
                                                                       size_t alias_count,
                                                                       const char *function_name) {
    size_t i;
    bool has_custom = false;
    if (!lowering || !function || !owner_module_path || owner_module_path[0] == '\0' ||
        !function_name || function_name[0] == '\0') {
        return;
    }
    for (i = 0U; i < lowering->function_count; ++i) {
        bool builtin_candidate;
        if (strcmp(lowering->functions[i].function_name, function_name) != 0) {
            continue;
        }
        if (!v3_function_name_visible_from_module(owner_module_path,
                                                  aliases,
                                                  alias_count,
                                                  lowering->functions[i].owner_module_path,
                                                  function_name)) {
            continue;
        }
        builtin_candidate = strcmp(lowering->functions[i].owner_module_path, "std/system") == 0 ||
                            strcmp(lowering->functions[i].owner_module_path, "std/strings") == 0;
        if (!builtin_candidate) {
            has_custom = true;
            break;
        }
    }
    for (i = 0U; i < lowering->function_count; ++i) {
        bool builtin_candidate;
        if (strcmp(lowering->functions[i].function_name, function_name) != 0) {
            continue;
        }
        if (!v3_function_name_visible_from_module(owner_module_path,
                                                  aliases,
                                                  alias_count,
                                                  lowering->functions[i].owner_module_path,
                                                  function_name)) {
            continue;
        }
        builtin_candidate = strcmp(lowering->functions[i].owner_module_path, "std/system") == 0 ||
                            strcmp(lowering->functions[i].owner_module_path, "std/strings") == 0;
        if (!has_custom &&
            builtin_candidate &&
            (strcmp(function_name, "[]") == 0 || strcmp(function_name, "[]=") == 0)) {
            continue;
        }
        if (has_custom && builtin_candidate) {
            continue;
        }
        v3_lowered_function_add_callee(function, lowering->functions[i].symbol_text);
    }
}

static void v3_collect_calls_from_text_resolved(const char *expr_text,
                                                const char *owner_module_path,
                                                const V3ImportAlias *aliases,
                                                size_t alias_count,
                                                const V3LoweringPlanStub *lowering,
                                                V3LoweredFunctionStub *function) {
    char assign_lhs[4096];
    char assign_rhs[4096];
    char base_expr[4096];
    char index_expr[4096];
    char value_expr[4096];
    V3AsmLocalSlot param_locals[CHENG_V3_MAX_CALL_ARGS];
    size_t param_local_count =
        v3_build_param_locals_for_call_collection(function, param_locals, CHENG_V3_MAX_CALL_ARGS);
    if (v3_parse_assignment_meta(expr_text,
                                 assign_lhs,
                                 sizeof(assign_lhs),
                                 assign_rhs,
                                 sizeof(assign_rhs)) &&
        v3_parse_index_assignment_operands(assign_lhs,
                                           assign_rhs,
                                           base_expr,
                                           sizeof(base_expr),
                                           index_expr,
                                           sizeof(index_expr),
                                           value_expr,
                                           sizeof(value_expr))) {
        v3_collect_calls_from_text_resolved(base_expr,
                                            owner_module_path,
                                            aliases,
                                            alias_count,
                                            lowering,
                                            function);
        v3_collect_calls_from_text_resolved(index_expr,
                                            owner_module_path,
                                            aliases,
                                            alias_count,
                                            lowering,
                                            function);
        v3_collect_calls_from_text_resolved(value_expr,
                                            owner_module_path,
                                            aliases,
                                            alias_count,
                                            lowering,
                                            function);
        {
            char operator_args[CHENG_V3_MAX_CALL_ARGS][4096];
            char resolved_symbol[PATH_MAX];
            memset(operator_args, 0, sizeof(operator_args));
            snprintf(operator_args[0], sizeof(operator_args[0]), "%s", base_expr);
            snprintf(operator_args[1], sizeof(operator_args[1]), "%s", index_expr);
            snprintf(operator_args[2], sizeof(operator_args[2]), "%s", value_expr);
            if (v3_select_visible_overloaded_function_symbol(NULL,
                                                             lowering,
                                                             function,
                                                             aliases,
                                                             alias_count,
                                                             param_locals,
                                                             param_local_count,
                                                             "[]=",
                                                             operator_args,
                                                             3U,
                                                             NULL,
                                                             resolved_symbol,
                                                             sizeof(resolved_symbol))) {
                v3_lowered_function_add_callee(function, resolved_symbol);
            } else {
                v3_lowered_function_add_preferred_visible_operator_callees(lowering,
                                                                           function,
                                                                           owner_module_path,
                                                                           aliases,
                                                                           alias_count,
                                                                           "[]=");
            }
        }
        return;
    }
    const char *cursor = expr_text;
    bool in_string = false;
    while (*cursor != '\0') {
        if (*cursor == '"' && !v3_quote_is_escaped(expr_text, (size_t)(cursor - expr_text))) {
            in_string = !in_string;
            cursor++;
            continue;
        }
        if (in_string) {
            cursor++;
            continue;
        }
        if (v3_ident_start_char(*cursor)) {
            const char *start = cursor;
            const char *stop = cursor + 1;
            char token[PATH_MAX];
            char resolved_symbol[PATH_MAX];
            const char *lookahead;
            while (*stop != '\0') {
                if (*stop == '.') {
                    if (!v3_ident_start_char(stop[1])) {
                        break;
                    }
                    stop++;
                    while (v3_ident_char(*stop)) {
                        stop++;
                    }
                    continue;
                }
                if (!v3_ident_char(*stop)) {
                    break;
                }
                stop++;
            }
            snprintf(token, sizeof(token), "%.*s", (int)(stop - start), start);
            lookahead = stop;
            while (*lookahead != '\0' && isspace((unsigned char)*lookahead)) {
                lookahead++;
            }
            if (*lookahead == '(') {
                if (v3_is_builtin_statement_call_name(token)) {
                    cursor = stop;
                    continue;
                }
                {
                    char call_expr[8192];
                    char parsed_callee[PATH_MAX];
                    char parsed_args[CHENG_V3_MAX_CALL_ARGS][4096];
                    size_t parsed_arg_count = 0U;
                    bool parsed_call = false;
                    if (v3_extract_call_expr_from_cursor(start,
                                                         lookahead,
                                                         call_expr,
                                                         sizeof(call_expr)) &&
                        v3_parse_call_text(call_expr,
                                           parsed_callee,
                                           sizeof(parsed_callee),
                                           parsed_args,
                                           &parsed_arg_count,
                                           CHENG_V3_MAX_CALL_ARGS)) {
                        parsed_call = true;
                    }
                    char *dot = strchr(token, '.');
                    V3ResultIntrinsicKind result_kind =
                        v3_resolve_result_intrinsic_kind(lowering,
                                                         owner_module_path,
                                                         aliases,
                                                         alias_count,
                                                         token);
                    if (dot) {
                        char alias_text[128];
                        char function_name[128];
                        const char *module_path;
                        snprintf(alias_text, sizeof(alias_text), "%.*s", (int)(dot - token), token);
                        snprintf(function_name, sizeof(function_name), "%s", dot + 1);
                        module_path =
                            v3_alias_module_path_exported_member(aliases,
                                                                 alias_count,
                                                                 owner_module_path,
                                                                 alias_text,
                                                                 function_name,
                                                                 sizeof(function_name));
                        if (module_path &&
                            !v3_is_intrinsic_module_call(module_path, function_name) &&
                            v3_result_intrinsic_kind_from_name(function_name) == V3_RESULT_INTRINSIC_NONE) {
                            if (parsed_call &&
                                v3_select_overloaded_function_symbol(NULL,
                                                                     lowering,
                                                                     function,
                                                                     aliases,
                                                                     alias_count,
                                                                     param_locals,
                                                                     param_local_count,
                                                                     module_path,
                                                                     function_name,
                                                                     parsed_args,
                                                                     parsed_arg_count,
                                                                     NULL,
                                                                     resolved_symbol,
                                                                     sizeof(resolved_symbol))) {
                                v3_lowered_function_add_callee(function, resolved_symbol);
                            } else {
                                int32_t unique_index =
                                    v3_find_unique_lowered_function_index_by_module_and_name(lowering,
                                                                                            module_path,
                                                                                            function_name);
                                if (unique_index >= 0) {
                                    snprintf(resolved_symbol,
                                             sizeof(resolved_symbol),
                                             "%s",
                                             lowering->functions[unique_index].symbol_text);
                                    if (v3_find_lowered_function_index_by_symbol_scan(lowering, resolved_symbol) >= 0) {
                                        v3_lowered_function_add_callee(function, resolved_symbol);
                                    }
                                } else {
                                    v3_lowered_function_add_all_callees_by_module_and_name(lowering,
                                                                                           function,
                                                                                           module_path,
                                                                                           function_name);
                                }
                            }
                        }
                    } else if (!v3_is_intrinsic_call_name(token) &&
                               result_kind == V3_RESULT_INTRINSIC_NONE &&
                               parsed_call &&
                               v3_select_visible_overloaded_function_symbol(NULL,
                                                                            lowering,
                                                                            function,
                                                                            aliases,
                                                                            alias_count,
                                                                            param_locals,
                                                                            param_local_count,
                                                                            token,
                                                                            parsed_args,
                                                                            parsed_arg_count,
                                                                            NULL,
                                                                            resolved_symbol,
                                                                            sizeof(resolved_symbol))) {
                        v3_lowered_function_add_callee(function, resolved_symbol);
                    } else if (!v3_is_intrinsic_call_name(token) &&
                               result_kind == V3_RESULT_INTRINSIC_NONE &&
                               v3_resolve_bare_call_symbol(lowering,
                                                           owner_module_path,
                                                           aliases,
                                                           alias_count,
                                                           token,
                                                           resolved_symbol,
                                                           sizeof(resolved_symbol))) {
                        v3_lowered_function_add_callee(function, resolved_symbol);
                    } else if (!v3_is_intrinsic_call_name(token) &&
                               result_kind == V3_RESULT_INTRINSIC_NONE) {
                        v3_lowered_function_add_all_callees_by_name(lowering, function, token);
                    }
                }
            } else if (*lookahead != '\0' &&
                       (v3_ident_start_char(*lookahead) ||
                        isdigit((unsigned char)*lookahead) ||
                        *lookahead == '"' ||
                        *lookahead == '-' ||
                        *lookahead == '!' ||
                        *lookahead == '[')) {
                if (*lookahead == '"' &&
                    v3_collect_calls_from_fmt_template_token(token,
                                                             lookahead,
                                                             owner_module_path,
                                                             aliases,
                                                             alias_count,
                                                             lowering,
                                                             function)) {
                    cursor = stop;
                    continue;
                }
                if (*lookahead == '[') {
                    v3_lowered_function_add_preferred_visible_operator_callees(lowering,
                                                                               function,
                                                                               owner_module_path,
                                                                               aliases,
                                                                               alias_count,
                                                                               "[]");
                    cursor = stop;
                    continue;
                }
                if (v3_is_builtin_statement_call_name(token)) {
                    cursor = stop;
                    continue;
                }
                char *dot = strchr(token, '.');
                V3ResultIntrinsicKind result_kind =
                    v3_resolve_result_intrinsic_kind(lowering,
                                                     owner_module_path,
                                                     aliases,
                                                     alias_count,
                                                     token);
                if (dot) {
                    char alias_text[128];
                    char function_name[128];
                    const char *module_path;
                    snprintf(alias_text, sizeof(alias_text), "%.*s", (int)(dot - token), token);
                    snprintf(function_name, sizeof(function_name), "%s", dot + 1);
                    module_path =
                        v3_alias_module_path_exported_member(aliases,
                                                             alias_count,
                                                             owner_module_path,
                                                             alias_text,
                                                             function_name,
                                                             sizeof(function_name));
                    if (module_path &&
                        !v3_is_intrinsic_module_call(module_path, function_name) &&
                        v3_result_intrinsic_kind_from_name(function_name) == V3_RESULT_INTRINSIC_NONE) {
                        int32_t unique_index =
                            v3_find_unique_lowered_function_index_by_module_and_name(lowering,
                                                                                    module_path,
                                                                                    function_name);
                        if (unique_index >= 0) {
                            snprintf(resolved_symbol,
                                     sizeof(resolved_symbol),
                                     "%s",
                                     lowering->functions[unique_index].symbol_text);
                            if (v3_find_lowered_function_index_by_symbol_scan(lowering, resolved_symbol) >= 0) {
                                v3_lowered_function_add_callee(function, resolved_symbol);
                            }
                        } else {
                            v3_lowered_function_add_all_callees_by_module_and_name(lowering,
                                                                                   function,
                                                                                   module_path,
                                                                                   function_name);
                        }
                    }
                } else if (!v3_is_intrinsic_call_name(token) &&
                           result_kind == V3_RESULT_INTRINSIC_NONE &&
                           v3_resolve_bare_call_symbol(lowering,
                                                       owner_module_path,
                                                       aliases,
                                                       alias_count,
                                                       token,
                                                       resolved_symbol,
                                                       sizeof(resolved_symbol))) {
                    v3_lowered_function_add_callee(function, resolved_symbol);
                } else if (!v3_is_intrinsic_call_name(token) &&
                           result_kind == V3_RESULT_INTRINSIC_NONE) {
                    v3_lowered_function_add_all_callees_by_name(lowering, function, token);
                }
            }
            cursor = stop;
            continue;
        }
        cursor++;
    }
}

static bool v3_lowering_function_name_from_line(char *trimmed, char *out, size_t cap) {
    char *token;
    char *stop;
    size_t prefix_len;
    out[0] = '\0';
    prefix_len = v3_function_decl_prefix_len(trimmed);
    if (prefix_len == 0U) {
        return false;
    }
    token = trimmed + prefix_len;
    while (*token != '\0' && isspace((unsigned char)*token)) {
        token++;
    }
    if (*token == '\0') {
        return false;
    }
    if (*token == '`') {
        stop = strchr(token + 1, '`');
        if (stop == NULL || stop == token + 1) {
            return false;
        }
        *stop = '\0';
        snprintf(out, cap, "%s", token + 1);
        return true;
    }
    stop = token;
    while (*stop != '\0' &&
           *stop != '(' &&
           *stop != ' ' &&
           *stop != '\t') {
        stop++;
    }
    if (stop == token) {
        return false;
    }
    *stop = '\0';
    snprintf(out, cap, "%s", token);
    return true;
}

static bool v3_importc_function_name_from_line(char *trimmed, char *out, size_t cap) {
    char *token;
    char *stop;
    out[0] = '\0';
    if (!v3_startswith(trimmed, "importc fn ")) {
        return false;
    }
    token = trimmed + 10;
    while (*token != '\0' && isspace((unsigned char)*token)) {
        token++;
    }
    if (*token == '\0') {
        return false;
    }
    if (*token == '`') {
        stop = strchr(token + 1, '`');
        if (stop == NULL || stop == token + 1) {
            return false;
        }
        *stop = '\0';
        snprintf(out, cap, "%s", token + 1);
        return true;
    }
    stop = token;
    while (*stop != '\0' &&
           *stop != '(' &&
           *stop != ' ' &&
           *stop != '\t') {
        stop++;
    }
    if (stop == token) {
        return false;
    }
    *stop = '\0';
    snprintf(out, cap, "%s", token);
    return true;
}

static bool v3_importc_symbol_from_annotation(char *trimmed, char *out, size_t cap) {
    char *open_quote;
    char *close_quote;
    if (!v3_startswith(trimmed, "@importc(")) {
        return false;
    }
    out[0] = '\0';
    open_quote = strchr(trimmed, '"');
    if (open_quote == NULL) {
        return false;
    }
    close_quote = strchr(open_quote + 1, '"');
    if (close_quote == NULL || close_quote == open_quote + 1) {
        return false;
    }
    *close_quote = '\0';
    snprintf(out, cap, "%s", open_quote + 1);
    return true;
}

static bool v3_is_annotation_line(const char *trimmed) {
    return trimmed != NULL && trimmed[0] == '@';
}

static bool v3_annotation_named(const char *trimmed, const char *name) {
    size_t name_len;
    const char *cursor;
    if (!v3_is_annotation_line(trimmed) || name == NULL || name[0] == '\0') {
        return false;
    }
    cursor = trimmed + 1;
    while (*cursor != '\0' && isspace((unsigned char)*cursor)) {
        cursor++;
    }
    name_len = strlen(name);
    if (!v3_startswith(cursor, name)) {
        return false;
    }
    cursor += name_len;
    return *cursor == '\0' ||
           *cursor == '(' ||
           isspace((unsigned char)*cursor);
}

static bool v3_exportc_symbol_from_annotation(char *trimmed, char *out, size_t cap) {
    char *open_quote;
    char *close_quote;
    if (!v3_startswith(trimmed, "@exportc(") &&
        !v3_startswith(trimmed, "@ exportc(")) {
        return false;
    }
    out[0] = '\0';
    open_quote = strchr(trimmed, '"');
    if (open_quote == NULL) {
        return false;
    }
    close_quote = strchr(open_quote + 1, '"');
    if (close_quote == NULL || close_quote == open_quote + 1) {
        return false;
    }
    *close_quote = '\0';
    snprintf(out, cap, "%s", open_quote + 1);
    return true;
}

static bool v3_type_is_handle_scalar_text(const char *type_text) {
    char trimmed[128];
    if (type_text == NULL || type_text[0] == '\0') {
        return false;
    }
    v3_trim_copy_text(type_text, trimmed, sizeof(trimmed));
    return strcmp(trimmed, "uint64") == 0 ||
           strcmp(trimmed, "uint32") == 0 ||
           strcmp(trimmed, "u64") == 0 ||
           strcmp(trimmed, "u32") == 0;
}

static bool v3_parse_ffi_handle_consume_annotation(const char *trimmed, int32_t *out_idx) {
    const char *open_paren;
    const char *close_paren;
    char token[128];
    const char *cursor;
    int32_t value = 0;
    if (out_idx != NULL) {
        *out_idx = -1;
    }
    if (!v3_annotation_named(trimmed, "ffi_handle_consume")) {
        return false;
    }
    open_paren = strchr(trimmed, '(');
    close_paren = open_paren != NULL ? strrchr(open_paren + 1, ')') : NULL;
    if (open_paren == NULL || close_paren == NULL || close_paren <= open_paren + 1) {
        return false;
    }
    snprintf(token, sizeof(token), "%.*s", (int)(close_paren - open_paren - 1), open_paren + 1);
    v3_trim_copy_text(token, token, sizeof(token));
    cursor = strrchr(token, '=');
    if (cursor != NULL) {
        cursor += 1;
    } else {
        cursor = token;
    }
    while (*cursor != '\0' && isspace((unsigned char)*cursor)) {
        cursor++;
    }
    if (!v3_startswith(cursor, "arg")) {
        return false;
    }
    cursor += 3;
    if (!isdigit((unsigned char)*cursor)) {
        return false;
    }
    while (*cursor != '\0') {
        if (!isdigit((unsigned char)*cursor)) {
            return false;
        }
        value = value * 10 + (int32_t)(*cursor - '0');
        cursor++;
    }
    if (out_idx != NULL) {
        *out_idx = value;
    }
    return true;
}

static bool v3_signature_text_complete(const char *text) {
    const char *open_paren = strchr(text, '(');
    bool in_string = false;
    int32_t paren_depth = 0;
    bool saw_close = false;
    size_t i;
    if (text == NULL || text[0] == '\0') {
        return false;
    }
    if (open_paren == NULL) {
        return true;
    }
    for (i = (size_t)(open_paren - text); text[i] != '\0'; ++i) {
        char ch = text[i];
        if (ch == '"' && !v3_quote_is_escaped(text, i)) {
            in_string = !in_string;
            continue;
        }
        if (in_string) {
            continue;
        }
        if (ch == '(') {
            paren_depth += 1;
            continue;
        }
        if (ch == ')') {
            if (paren_depth > 0) {
                paren_depth -= 1;
            }
            if (paren_depth == 0) {
                saw_close = true;
            }
        }
    }
    return saw_close && paren_depth == 0;
}

static bool v3_append_signature_line(char *out, size_t cap, const char *line) {
    char trimmed[4096];
    size_t out_len;
    if (out == NULL || cap <= 1U || line == NULL) {
        return false;
    }
    v3_trim_copy_text(line, trimmed, sizeof(trimmed));
    if (trimmed[0] == '\0') {
        return true;
    }
    out_len = strlen(out);
    if (out_len > 0U) {
        if (out_len + 1U >= cap) {
            return false;
        }
        out[out_len++] = ' ';
        out[out_len] = '\0';
    }
    if (out_len + strlen(trimmed) + 1U > cap) {
        return false;
    }
    snprintf(out + out_len, cap - out_len, "%s", trimmed);
    return true;
}

static bool v3_collect_signature_text(char **cursor_io,
                                      const char *first_line,
                                      char *out,
                                      size_t cap) {
    out[0] = '\0';
    if (!v3_append_signature_line(out, cap, first_line)) {
        return false;
    }
    while (!v3_signature_text_complete(out)) {
        char *next_line;
        if (cursor_io == NULL || *cursor_io == NULL) {
            return false;
        }
        next_line = strsep(cursor_io, "\n");
        if (next_line == NULL) {
            return false;
        }
        if (!v3_append_signature_line(out, cap, next_line)) {
            return false;
        }
    }
    return true;
}

static bool v3_parse_importc_signature_meta(const char *line,
                                            const char *function_name,
                                            V3LoweredFunctionStub *function) {
    char line_copy[4096];
    char fake_signature[4096];
    char *trimmed;
    snprintf(line_copy, sizeof(line_copy), "%s", line);
    trimmed = v3_trim_inplace(line_copy);
    if (v3_startswith(trimmed, "importc fn ")) {
        snprintf(fake_signature, sizeof(fake_signature), "%s = 0", trimmed + 8);
        return v3_parse_function_signature_meta(fake_signature, function_name, function);
    }
    if (v3_function_decl_prefix_len(trimmed) == 0U) {
        return false;
    }
    snprintf(fake_signature, sizeof(fake_signature), "%s = 0", trimmed);
    return v3_parse_function_signature_meta(fake_signature, function_name, function);
}

static bool v3_resolve_importc_call_target_from_source_path(const V3SystemLinkPlanStub *plan,
                                                            const V3LoweringPlanStub *lowering,
                                                            const char *source_path,
                                                            const char *callee,
                                                            V3AsmCallTarget *target) {
    char *owned = NULL;
    char owner_module_path[PATH_MAX];
    V3ImportAlias aliases[CHENG_V3_MAX_IMPORT_ALIASES];
    size_t alias_count = 0U;
    V3LoweredFunctionStub probe;
    char pending_symbol[PATH_MAX];
    bool pending_ffi_handle = false;
    int32_t pending_ffi_handle_consume_index = -1;
    if (source_path == NULL || source_path[0] == '\0' ||
        callee == NULL || callee[0] == '\0' ||
        target == NULL) {
        return false;
    }
    owned = v3_read_file(source_path);
    if (owned == NULL) {
        return false;
    }
    pending_symbol[0] = '\0';
    v3_source_path_to_module_path(plan->workspace_root,
                                  plan->package_root,
                                  plan->package_id,
                                  source_path,
                                  owner_module_path,
                                  sizeof(owner_module_path));
    if (!v3_load_import_aliases_from_source_path(source_path,
                                                 aliases,
                                                 &alias_count,
                                                 CHENG_V3_MAX_IMPORT_ALIASES)) {
        free(owned);
        return false;
    }
    {
        char *cursor = owned;
        char *line = NULL;
        while ((line = strsep(&cursor, "\n")) != NULL) {
        char line_copy[4096];
        char signature_copy[4096];
        char function_name[128];
        size_t i;
        char *trimmed;
        char *signature_trimmed;
        bool matched = false;
        snprintf(line_copy, sizeof(line_copy), "%s", line);
        trimmed = v3_trim_inplace(line_copy);
        if (*trimmed == '\0') {
            continue;
        }
        if (v3_importc_symbol_from_annotation(trimmed, pending_symbol, sizeof(pending_symbol))) {
            pending_ffi_handle = false;
            pending_ffi_handle_consume_index = -1;
            continue;
        }
        if (pending_symbol[0] != '\0' && v3_is_annotation_line(trimmed)) {
            if (v3_annotation_named(trimmed, "ffi_handle")) {
                pending_ffi_handle = true;
            } else {
                int32_t consume_index = -1;
                if (v3_parse_ffi_handle_consume_annotation(trimmed, &consume_index)) {
                    pending_ffi_handle_consume_index = consume_index;
                }
            }
            continue;
        }
        function_name[0] = '\0';
        if (v3_importc_function_name_from_line(trimmed, function_name, sizeof(function_name))) {
            matched = true;
        } else if (pending_symbol[0] != '\0' &&
                   v3_lowering_function_name_from_line(trimmed, function_name, sizeof(function_name))) {
            matched = true;
        } else {
            pending_symbol[0] = '\0';
            pending_ffi_handle = false;
            pending_ffi_handle_consume_index = -1;
            continue;
        }
        if (!matched || strcmp(function_name, callee) != 0) {
            if (pending_symbol[0] == '\0') {
                continue;
            }
            pending_symbol[0] = '\0';
            pending_ffi_handle = false;
            pending_ffi_handle_consume_index = -1;
            continue;
        }
        {
            const char *symbol_name = function_name;
            char collected_signature[4096];
            memset(&probe, 0, sizeof(probe));
            if (!v3_collect_signature_text(&cursor,
                                           line,
                                           collected_signature,
                                           sizeof(collected_signature))) {
                free(owned);
                return false;
            }
            snprintf(signature_copy, sizeof(signature_copy), "%s", collected_signature);
            signature_trimmed = v3_trim_inplace(signature_copy);
            if (!v3_parse_importc_signature_meta(signature_trimmed, function_name, &probe)) {
                free(owned);
                return false;
            }
            if (pending_symbol[0] != '\0' && v3_function_decl_prefix_len(trimmed) != 0U) {
                symbol_name = pending_symbol;
            }
            memset(target, 0, sizeof(*target));
            target->ffi_handle_consume_index = -1;
            target->external = true;
            snprintf(target->callee_name, sizeof(target->callee_name), "%s", function_name);
            v3_copy_target_symbol_name(plan, symbol_name, target->symbol_name, sizeof(target->symbol_name));
            target->c_abi = !v3_symbol_uses_cheng_internal_abi(symbol_name);
            {
                char normalized_type[256];
                if (probe.return_type[0] != '\0' &&
                    strcmp(probe.return_type, "void") != 0 &&
                    v3_normalize_type_text(lowering,
                                           owner_module_path,
                                           aliases,
                                           alias_count,
                                           probe.return_type,
                                           normalized_type,
                                           sizeof(normalized_type))) {
                    snprintf(target->return_type, sizeof(target->return_type), "%s", normalized_type);
                    snprintf(target->return_abi_class,
                             sizeof(target->return_abi_class),
                             "%s",
                             v3_type_abi_class(target->return_type));
                } else {
                    snprintf(target->return_type, sizeof(target->return_type), "%s", probe.return_type);
                    snprintf(target->return_abi_class, sizeof(target->return_abi_class), "%s", probe.return_abi_class);
                }
            }
            target->param_count = probe.param_count;
            for (i = 0; i < probe.param_count && i < CHENG_V3_MAX_CALL_ARGS; ++i) {
                char normalized_type[256];
                if (probe.param_types[i][0] != '\0' &&
                    v3_normalize_type_text(lowering,
                                           owner_module_path,
                                           aliases,
                                           alias_count,
                                           probe.param_types[i],
                                           normalized_type,
                                           sizeof(normalized_type))) {
                    snprintf(target->param_types[i], sizeof(target->param_types[i]), "%s", normalized_type);
                    snprintf(target->param_abi_classes[i],
                             sizeof(target->param_abi_classes[i]),
                             "%s",
                             v3_type_abi_class(target->param_types[i]));
                } else {
                    snprintf(target->param_types[i], sizeof(target->param_types[i]), "%s", probe.param_types[i]);
                    snprintf(target->param_abi_classes[i], sizeof(target->param_abi_classes[i]), "%s", probe.param_abi_classes[i]);
                }
                target->param_is_var[i] = probe.param_is_var[i];
                target->ffi_handle_param_is_handle[i] =
                    pending_ffi_handle && v3_type_is_handle_scalar_text(probe.param_types[i]);
            }
            if (pending_ffi_handle) {
                target->ffi_handle_enabled = true;
                target->ffi_handle_return_is_handle = v3_type_is_handle_scalar_text(probe.return_type);
                if (pending_ffi_handle_consume_index >= 0 &&
                    pending_ffi_handle_consume_index < (int32_t)target->param_count &&
                    target->ffi_handle_param_is_handle[pending_ffi_handle_consume_index]) {
                    target->ffi_handle_consume_index = pending_ffi_handle_consume_index;
                }
                snprintf(target->ffi_handle_register_symbol_name,
                         sizeof(target->ffi_handle_register_symbol_name),
                         "%s",
                         "cheng_ffi_handle_register_ptr");
                snprintf(target->ffi_handle_resolve_symbol_name,
                         sizeof(target->ffi_handle_resolve_symbol_name),
                         "%s",
                         "cheng_ffi_handle_resolve_ptr");
                snprintf(target->ffi_handle_invalidate_symbol_name,
                         sizeof(target->ffi_handle_invalidate_symbol_name),
                         "%s",
                         "cheng_ffi_handle_invalidate");
            }
            free(owned);
            return true;
        }
        }
    }
    free(owned);
    return false;
}

static bool v3_resolve_importc_call_target_from_module_path(const V3SystemLinkPlanStub *plan,
                                                            const V3LoweringPlanStub *lowering,
                                                            const char *module_path,
                                                            const char *callee,
                                                            V3AsmCallTarget *target) {
    char source_path[PATH_MAX];
    if (!plan || !module_path || module_path[0] == '\0' || !callee || callee[0] == '\0' || !target) {
        return false;
    }
    v3_module_path_to_source_path(plan->workspace_root,
                                  plan->package_root,
                                  plan->package_id,
                                  module_path,
                                  source_path,
                                  sizeof(source_path));
    if (source_path[0] == '\0') {
        return false;
    }
    return v3_resolve_importc_call_target_from_source_path(plan,
                                                           lowering,
                                                           source_path,
                                                           callee,
                                                           target);
}

static bool v3_resolve_importc_call_target_from_aliases(const V3SystemLinkPlanStub *plan,
                                                        const V3LoweringPlanStub *lowering,
                                                        const V3LoweredFunctionStub *current_function,
                                                        const V3ImportAlias *aliases,
                                                        size_t alias_count,
                                                        const char *callee,
                                                        V3AsmCallTarget *target) {
    size_t i;
    if (!plan || !lowering || !current_function || !callee || callee[0] == '\0' || !target) {
        return false;
    }
    for (i = 0U; i < alias_count; ++i) {
        if (!v3_module_paths_share_package(current_function->owner_module_path,
                                           aliases[i].module_path)) {
            continue;
        }
        if (v3_resolve_importc_call_target_from_module_path(plan,
                                                            lowering,
                                                            aliases[i].module_path,
                                                            callee,
                                                            target)) {
            return true;
        }
    }
    return false;
}

static bool v3_lowering_parse_return_call_noarg_i32(const char *trimmed,
                                                    char *target_name,
                                                    size_t cap) {
    const char *prefix = "return ";
    const char *call;
    const char *open_paren;
    size_t name_len;
    size_t i;
    if (!v3_startswith(trimmed, prefix)) {
        return false;
    }
    call = trimmed + strlen(prefix);
    open_paren = strchr(call, '(');
    if (!open_paren || strcmp(open_paren, "()") != 0) {
        return false;
    }
    name_len = (size_t)(open_paren - call);
    if (name_len <= 0U || name_len + 1U > cap) {
        return false;
    }
    memcpy(target_name, call, name_len);
    target_name[name_len] = '\0';
    v3_trim_copy_text(target_name, target_name, cap);
    if (target_name[0] == '\0') {
        return false;
    }
    for (i = 0; target_name[i] != '\0'; ++i) {
        unsigned char ch = (unsigned char)target_name[i];
        if (!(isalnum(ch) || ch == '_' || ch == '.' || ch == ':')) {
            return false;
        }
    }
    return true;
}

static bool v3_lowering_parse_return_call_arg0_i32(const char *trimmed,
                                                   char *target_name,
                                                   size_t target_cap,
                                                   char *arg_name,
                                                   size_t arg_cap) {
    const char *prefix = "return ";
    const char *call;
    const char *open_paren;
    const char *close_paren;
    size_t name_len;
    size_t arg_len;
    size_t i;
    if (!v3_startswith(trimmed, prefix)) {
        return false;
    }
    call = trimmed + strlen(prefix);
    open_paren = strchr(call, '(');
    close_paren = strrchr(call, ')');
    if (open_paren == NULL || close_paren == NULL || close_paren <= open_paren || close_paren[1] != '\0') {
        return false;
    }
    if (strcmp(open_paren, "()") == 0) {
        return false;
    }
    name_len = (size_t)(open_paren - call);
    arg_len = (size_t)(close_paren - open_paren - 1);
    if (name_len == 0U || name_len + 1U > target_cap || arg_len == 0U || arg_len + 1U > arg_cap) {
        return false;
    }
    memcpy(target_name, call, name_len);
    target_name[name_len] = '\0';
    memcpy(arg_name, open_paren + 1, arg_len);
    arg_name[arg_len] = '\0';
    v3_trim_copy_text(target_name, target_name, target_cap);
    v3_trim_copy_text(arg_name, arg_name, arg_cap);
    if (target_name[0] == '\0' || arg_name[0] == '\0' || strchr(arg_name, ',') != NULL) {
        return false;
    }
    for (i = 0; target_name[i] != '\0'; ++i) {
        unsigned char ch = (unsigned char)target_name[i];
        if (!(isalnum(ch) || ch == '_' || ch == '.' || ch == ':')) {
            return false;
        }
    }
    for (i = 0; arg_name[i] != '\0'; ++i) {
        unsigned char ch = (unsigned char)arg_name[i];
        if (!(isalnum(ch) || ch == '_')) {
            return false;
        }
    }
    return true;
}

static bool v3_lowering_parse_return_call_params_i32(const char *trimmed,
                                                     char *target_name,
                                                     size_t target_cap,
                                                     char arg_names[CHENG_V3_MAX_CALL_ARGS][128],
                                                     size_t *arg_count_out) {
    const char *prefix = "return ";
    const char *call;
    const char *open_paren;
    const char *close_paren;
    char args_copy[512];
    char *cursor = NULL;
    char *piece = NULL;
    size_t name_len;
    size_t arg_count = 0U;
    size_t i;
    if (arg_count_out != NULL) {
        *arg_count_out = 0U;
    }
    if (!v3_startswith(trimmed, prefix)) {
        return false;
    }
    call = trimmed + strlen(prefix);
    open_paren = strchr(call, '(');
    close_paren = strrchr(call, ')');
    if (open_paren == NULL || close_paren == NULL || close_paren <= open_paren || close_paren[1] != '\0') {
        return false;
    }
    if (strcmp(open_paren, "()") == 0) {
        return false;
    }
    name_len = (size_t)(open_paren - call);
    if (name_len == 0U || name_len + 1U > target_cap) {
        return false;
    }
    memcpy(target_name, call, name_len);
    target_name[name_len] = '\0';
    v3_trim_copy_text(target_name, target_name, target_cap);
    if (target_name[0] == '\0') {
        return false;
    }
    for (i = 0; target_name[i] != '\0'; ++i) {
        unsigned char ch = (unsigned char)target_name[i];
        if (!(isalnum(ch) || ch == '_' || ch == '.' || ch == ':')) {
            return false;
        }
    }
    snprintf(args_copy, sizeof(args_copy), "%.*s", (int)(close_paren - open_paren - 1), open_paren + 1);
    cursor = args_copy;
    while ((piece = strsep(&cursor, ",")) != NULL) {
        char trimmed_arg[128];
        size_t j;
        v3_trim_copy_text(piece, trimmed_arg, sizeof(trimmed_arg));
        if (trimmed_arg[0] == '\0') {
            return false;
        }
        if (arg_count >= CHENG_V3_MAX_CALL_ARGS) {
            return false;
        }
        for (j = 0; trimmed_arg[j] != '\0'; ++j) {
            unsigned char ch = (unsigned char)trimmed_arg[j];
            if (!(isalnum(ch) || ch == '_')) {
                return false;
            }
        }
        snprintf(arg_names[arg_count], 128, "%s", trimmed_arg);
        arg_count += 1U;
    }
    if (arg_count < 2U) {
        return false;
    }
    if (arg_count_out != NULL) {
        *arg_count_out = arg_count;
    }
    return true;
}

static size_t v3_function_body_last_line_number(char **lines,
                                                size_t line_count,
                                                size_t signature_end_index) {
    size_t i;
    size_t out = 0U;
    for (i = signature_end_index + 1U; i < line_count; ++i) {
        char line_copy[4096];
        char *trimmed;
        snprintf(line_copy, sizeof(line_copy), "%s", lines[i]);
        trimmed = v3_trim_inplace(line_copy);
        if (*trimmed == '\0' || *trimmed == '#') {
            continue;
        }
        if (v3_line_indent(lines[i]) <= 0 && v3_is_top_level_decl_start(trimmed)) {
            break;
        }
        out = i + 1U;
    }
    return out;
}

static void v3_lowering_classify_single_statement(const char *trimmed,
                                                  char *body_kind,
                                                  size_t body_kind_cap,
                                                  char *call_target,
                                                  size_t call_target_cap,
                                                  size_t *call_arg_count_out,
                                                  char *call_arg0_name,
                                                  size_t call_arg0_name_cap,
                                                  char call_arg_names[CHENG_V3_MAX_CALL_ARGS][128]) {
    char call_name[128];
    char call_arg_name[128];
    char call_arg_names_local[CHENG_V3_MAX_CALL_ARGS][128];
    size_t call_arg_count = 0U;
    call_target[0] = '\0';
    if (call_arg_count_out != NULL) {
        *call_arg_count_out = 0U;
    }
    call_arg0_name[0] = '\0';
    memset(call_arg_names, 0, sizeof(char) * CHENG_V3_MAX_CALL_ARGS * 128U);
    if (strcmp(trimmed, "return 0") == 0) {
        snprintf(body_kind, body_kind_cap, "%s", "return_zero_i32");
        return;
    }
    if (v3_lowering_parse_return_call_noarg_i32(trimmed, call_name, sizeof(call_name))) {
        snprintf(body_kind, body_kind_cap, "%s", "return_call_noarg_i32");
        snprintf(call_target, call_target_cap, "%s", call_name);
        return;
    }
    if (v3_lowering_parse_return_call_arg0_i32(trimmed,
                                               call_name,
                                               sizeof(call_name),
                                               call_arg_name,
                                               sizeof(call_arg_name))) {
        snprintf(body_kind, body_kind_cap, "%s", "return_call_arg0_i32");
        snprintf(call_target, call_target_cap, "%s", call_name);
        snprintf(call_arg0_name, call_arg0_name_cap, "%s", call_arg_name);
        if (call_arg_count_out != NULL) {
            *call_arg_count_out = 1U;
        }
        snprintf(call_arg_names[0], 128, "%s", call_arg_name);
        return;
    }
    if (v3_lowering_parse_return_call_params_i32(trimmed,
                                                 call_name,
                                                 sizeof(call_name),
                                                 call_arg_names_local,
                                                 &call_arg_count)) {
        size_t arg_index;
        snprintf(body_kind, body_kind_cap, "%s", "return_call_params_i32");
        snprintf(call_target, call_target_cap, "%s", call_name);
        if (call_arg_count_out != NULL) {
            *call_arg_count_out = call_arg_count;
        }
        snprintf(call_arg0_name, call_arg0_name_cap, "%s", call_arg_names_local[0]);
        for (arg_index = 0U; arg_index < call_arg_count && arg_index < CHENG_V3_MAX_CALL_ARGS; ++arg_index) {
            snprintf(call_arg_names[arg_index], 128, "%s", call_arg_names_local[arg_index]);
        }
        return;
    }
    if (v3_startswith(trimmed, "return ")) {
        snprintf(body_kind, body_kind_cap, "%s", "return_expr");
        return;
    }
    if (v3_startswith(trimmed, "var ")) {
        snprintf(body_kind, body_kind_cap, "%s", "stmt_var");
        return;
    }
    if (v3_startswith(trimmed, "let ")) {
        snprintf(body_kind, body_kind_cap, "%s", "stmt_let");
        return;
    }
    if (v3_startswith(trimmed, "if ")) {
        snprintf(body_kind, body_kind_cap, "%s", "stmt_if");
        return;
    }
    if (v3_startswith(trimmed, "for ")) {
        snprintf(body_kind, body_kind_cap, "%s", "stmt_for");
        return;
    }
    if (v3_startswith(trimmed, "while ")) {
        snprintf(body_kind, body_kind_cap, "%s", "stmt_while");
        return;
    }
    if (v3_startswith(trimmed, "case ")) {
        snprintf(body_kind, body_kind_cap, "%s", "stmt_case");
        return;
    }
    if (strchr(trimmed, '(') != NULL && trimmed[strlen(trimmed) - 1U] == ')') {
        snprintf(body_kind, body_kind_cap, "%s", "stmt_call");
        return;
    }
    snprintf(body_kind, body_kind_cap, "%s", "unsupported");
}

static void v3_lowering_function_body_kind(char **lines,
                                           size_t line_count,
                                           size_t signature_end_index,
                                           char *body_kind,
                                           size_t body_kind_cap,
                                           char *call_target,
                                           size_t call_target_cap,
                                           size_t *call_arg_count_out,
                                           char *call_arg0_name,
                                           size_t call_arg0_name_cap,
                                           char call_arg_names[CHENG_V3_MAX_CALL_ARGS][128],
                                           char *first_body_line,
                                           size_t first_body_line_cap,
                                           size_t *body_first_line_number_out,
                                           size_t *body_last_line_number_out) {
    size_t i;
    bool saw_body_line = false;
    char signature_copy[8192];
    char *signature_trimmed;
    char *eq;
    snprintf(body_kind, body_kind_cap, "%s", "unsupported");
    call_target[0] = '\0';
    if (call_arg_count_out != NULL) {
        *call_arg_count_out = 0U;
    }
    call_arg0_name[0] = '\0';
    memset(call_arg_names, 0, sizeof(char) * CHENG_V3_MAX_CALL_ARGS * 128U);
    first_body_line[0] = '\0';
    if (body_first_line_number_out != NULL) {
        *body_first_line_number_out = 0U;
    }
    if (body_last_line_number_out != NULL) {
        *body_last_line_number_out = 0U;
    }
    snprintf(signature_copy, sizeof(signature_copy), "%s", lines[signature_end_index]);
    signature_trimmed = v3_trim_inplace(signature_copy);
    eq = strrchr(signature_trimmed, '=');
    if (eq != NULL) {
        char inline_body[8192];
        v3_trim_copy_text(eq + 1, inline_body, sizeof(inline_body));
        if (inline_body[0] != '\0') {
            if (body_first_line_number_out != NULL) {
                *body_first_line_number_out = signature_end_index + 1U;
            }
            if (body_last_line_number_out != NULL) {
                *body_last_line_number_out = signature_end_index + 1U;
            }
            snprintf(first_body_line, first_body_line_cap, "%s", inline_body);
            v3_lowering_classify_single_statement(inline_body,
                                                  body_kind,
                                                  body_kind_cap,
                                                  call_target,
                                                  call_target_cap,
                                                  call_arg_count_out,
                                                  call_arg0_name,
                                                  call_arg0_name_cap,
                                                  call_arg_names);
            if (strcmp(body_kind, "stmt_call") == 0 || strcmp(body_kind, "unsupported") == 0) {
                snprintf(body_kind, body_kind_cap, "%s", "return_expr");
            }
            return;
        }
    }
    for (i = signature_end_index + 1U; i < line_count; ++i) {
        char line_copy[4096];
        char *trimmed;
        snprintf(line_copy, sizeof(line_copy), "%s", lines[i]);
        trimmed = v3_trim_inplace(line_copy);
        if (*trimmed == '\0' || *trimmed == '#') {
            continue;
        }
        if (v3_line_indent(lines[i]) <= 0 && v3_is_top_level_decl_start(trimmed)) {
            break;
        }
        if (body_first_line_number_out != NULL && *body_first_line_number_out == 0U) {
            *body_first_line_number_out = i + 1U;
        }
        if (body_last_line_number_out != NULL) {
            *body_last_line_number_out = i + 1U;
        }
        if (saw_body_line) {
            snprintf(body_kind, body_kind_cap, "%s", "unsupported");
            call_target[0] = '\0';
            return;
        }
        saw_body_line = true;
        snprintf(first_body_line, first_body_line_cap, "%s", trimmed);
        v3_lowering_classify_single_statement(trimmed,
                                              body_kind,
                                              body_kind_cap,
                                              call_target,
                                              call_target_cap,
                                              call_arg_count_out,
                                              call_arg0_name,
                                              call_arg0_name_cap,
                                              call_arg_names);
        return;
    }
}

static bool v3_function_body_has_tuple_binding_omission(char **lines,
                                                        size_t line_count,
                                                        size_t signature_end_index,
                                                        char *statement_out,
                                                        size_t statement_cap) {
    size_t index = signature_end_index + 1U;
    while (index < line_count) {
        char raw_copy[4096];
        char statement[8192];
        char *trimmed;
        snprintf(raw_copy, sizeof(raw_copy), "%s", lines[index]);
        trimmed = v3_trim_inplace(raw_copy);
        if (*trimmed == '\0') {
            index += 1U;
            continue;
        }
        if (v3_line_indent(lines[index]) <= 0 && v3_is_top_level_decl_start(trimmed)) {
            break;
        }
        if (!v3_collect_statement_from_lines(lines, line_count, &index, statement, sizeof(statement))) {
            break;
        }
        if ((v3_startswith(statement, "let ") || v3_startswith(statement, "var ")) &&
            v3_tuple_binding_expr_omits_items(statement)) {
            snprintf(statement_out, statement_cap, "%s", statement);
            return true;
        }
    }
    return false;
}

static bool v3_statement_has_duplicate_constructor_field(const V3LoweringPlanStub *lowering,
                                                         const char *owner_module_path,
                                                         const V3ImportAlias *aliases,
                                                         size_t alias_count,
                                                         const char *statement,
                                                         char *callee_out,
                                                         size_t callee_cap,
                                                         char *field_out,
                                                         size_t field_cap) {
    char expr[4096];
    char name[128];
    char type_text[CHENG_V3_MAX_TYPE_TEXT];
    char callee[PATH_MAX];
    char (*args)[4096] = v3_alloc_call_arg_scratch();
    size_t arg_count = 0U;
    bool valid = true;
    bool duplicate = false;
    if (callee_out && callee_cap > 0U) {
        callee_out[0] = '\0';
    }
    if (field_out && field_cap > 0U) {
        field_out[0] = '\0';
    }
    if (statement == NULL) {
        free(args);
        return false;
    }
    if ((v3_startswith(statement, "let ") || v3_startswith(statement, "var ")) &&
        v3_parse_binding_meta(statement,
                              v3_startswith(statement, "let ") ? "let " : "var ",
                              name,
                              sizeof(name),
                              type_text,
                              sizeof(type_text),
                              expr,
                              sizeof(expr))) {
    } else if (v3_startswith(statement, "return ")) {
        snprintf(expr, sizeof(expr), "%s", statement + 7);
        v3_trim_copy_text(expr, expr, sizeof(expr));
    } else {
        snprintf(expr, sizeof(expr), "%s", statement);
        v3_trim_copy_text(expr, expr, sizeof(expr));
    }
    if (!v3_parse_call_text(expr, callee, sizeof(callee), args, &arg_count, CHENG_V3_MAX_CALL_ARGS)) {
        free(args);
        return false;
    }
    if (!v3_callee_is_constructor_like(lowering,
                                       owner_module_path,
                                       aliases,
                                       alias_count,
                                       callee)) {
        free(args);
        return false;
    }
    duplicate = v3_find_duplicate_named_arg_field(args, arg_count, field_out, field_cap, &valid);
    if (duplicate && callee_out && callee_cap > 0U) {
        snprintf(callee_out, callee_cap, "%s", callee);
    }
    free(args);
    return duplicate && valid;
}

static bool v3_function_body_has_duplicate_constructor_field(const V3LoweringPlanStub *lowering,
                                                             const char *owner_module_path,
                                                             const V3ImportAlias *aliases,
                                                             size_t alias_count,
                                                             char **lines,
                                                             size_t line_count,
                                                             size_t signature_end_index,
                                                             char *statement_out,
                                                             size_t statement_cap,
                                                             char *callee_out,
                                                             size_t callee_cap,
                                                             char *field_out,
                                                             size_t field_cap) {
    size_t index = signature_end_index + 1U;
    while (index < line_count) {
        char raw_copy[4096];
        char statement[8192];
        char *trimmed;
        snprintf(raw_copy, sizeof(raw_copy), "%s", lines[index]);
        trimmed = v3_trim_inplace(raw_copy);
        if (*trimmed == '\0') {
            index += 1U;
            continue;
        }
        if (v3_line_indent(lines[index]) <= 0 && v3_is_top_level_decl_start(trimmed)) {
            break;
        }
        if (!v3_collect_statement_from_lines(lines, line_count, &index, statement, sizeof(statement))) {
            break;
        }
        if (v3_statement_has_duplicate_constructor_field(lowering,
                                                        owner_module_path,
                                                        aliases,
                                                        alias_count,
                                                        statement,
                                                        callee_out,
                                                        callee_cap,
                                                        field_out,
                                                        field_cap)) {
            snprintf(statement_out, statement_cap, "%s", statement);
            return true;
        }
    }
    return false;
}

static bool v3_collect_lowering_functions_from_source(const V3SystemLinkPlanStub *plan,
                                                      const char *source_path,
                                                      V3LoweringPlanStub *lowering) {
    char owner_module_path[PATH_MAX];
    V3ImportAlias aliases[CHENG_V3_MAX_IMPORT_ALIASES];
    size_t alias_count = 0U;
    char *owned;
    char *cursor;
    char *line;
    char *lines[CHENG_V3_MAX_SOURCE_LINES];
    size_t line_count = 0U;
    size_t i;
    v3_source_path_to_module_path(plan->workspace_root,
                                  plan->package_root,
                                  plan->package_id,
                                  source_path,
                                  owner_module_path,
                                  sizeof(owner_module_path));
    owned = v3_read_file(source_path);
    if (!owned) {
        return false;
    }
    cursor = owned;
    while ((line = strsep(&cursor, "\n")) != NULL) {
        if (line_count >= CHENG_V3_MAX_SOURCE_LINES) {
            free(owned);
            fprintf(stderr, "[cheng_v3_seed] lowering source line inventory too large\n");
            return false;
        }
        lines[line_count++] = line;
    }
    v3_parse_import_aliases_from_lines(lines,
                                       line_count,
                                       aliases,
                                       &alias_count,
                                       CHENG_V3_MAX_IMPORT_ALIASES);
    for (i = 0; i < line_count; ++i) {
        char function_name[128];
        char signature_text[8192];
        char line_copy[4096];
        char captured_export_symbol[PATH_MAX];
        size_t signature_end_index = 0U;
        size_t body_statement_count = 0U;
        char *trimmed;
        captured_export_symbol[0] = '\0';
        snprintf(line_copy, sizeof(line_copy), "%s", lines[i]);
        trimmed = v3_trim_inplace(line_copy);
        if (!v3_lowering_function_name_from_line(trimmed, function_name, sizeof(function_name))) {
            continue;
        }
        if (!v3_collect_function_signature(lines,
                                           line_count,
                                           i,
                                           signature_text,
                                           sizeof(signature_text),
                                           &signature_end_index)) {
            continue;
        }
        if (lowering->function_count >= CHENG_V3_MAX_PLAN_FUNCTIONS) {
            free(owned);
            fprintf(stderr, "[cheng_v3_seed] lowering function inventory too large\n");
            return false;
        }
        snprintf(lowering->functions[lowering->function_count].source_path,
                 sizeof(lowering->functions[lowering->function_count].source_path),
                 "%s",
                 source_path);
        snprintf(lowering->functions[lowering->function_count].owner_module_path,
                 sizeof(lowering->functions[lowering->function_count].owner_module_path),
                 "%s",
                 owner_module_path);
        snprintf(lowering->functions[lowering->function_count].function_name,
                 sizeof(lowering->functions[lowering->function_count].function_name),
                 "%s",
                 function_name);
        snprintf(lowering->functions[lowering->function_count].symbol_text,
                 sizeof(lowering->functions[lowering->function_count].symbol_text),
                 "%s::%s",
                 owner_module_path,
                 function_name);
        v3_top_level_function_export_symbol_from_lines(lines,
                                                       line_count,
                                                       i,
                                                       captured_export_symbol,
                                                       sizeof(captured_export_symbol));
        lowering->functions[lowering->function_count].signature_line_number = i + 1U;
        if (!v3_parse_function_signature_meta(signature_text,
                                              function_name,
                                              &lowering->functions[lowering->function_count])) {
            free(owned);
            fprintf(stderr, "[cheng_v3_seed] malformed function signature metadata\n");
            return false;
        }
        for (size_t param_i = 0; param_i < lowering->functions[lowering->function_count].param_count; ++param_i) {
            char normalized_type[128];
            if (v3_normalize_type_text(lowering,
                                       owner_module_path,
                                       aliases,
                                       alias_count,
                                       lowering->functions[lowering->function_count].param_types[param_i],
                                       normalized_type,
                                       sizeof(normalized_type))) {
                snprintf(lowering->functions[lowering->function_count].param_types[param_i],
                         sizeof(lowering->functions[lowering->function_count].param_types[param_i]),
                         "%s",
                         normalized_type);
            }
            snprintf(lowering->functions[lowering->function_count].param_abi_classes[param_i],
                     sizeof(lowering->functions[lowering->function_count].param_abi_classes[param_i]),
                     "%s",
                     v3_type_abi_class(lowering->functions[lowering->function_count].param_types[param_i]));
        }
        {
            char normalized_return_type[CHENG_V3_MAX_TYPE_TEXT];
            if (v3_normalize_type_text(lowering,
                                       owner_module_path,
                                       aliases,
                                       alias_count,
                                       lowering->functions[lowering->function_count].return_type,
                                       normalized_return_type,
                                       sizeof(normalized_return_type))) {
                snprintf(lowering->functions[lowering->function_count].return_type,
                         sizeof(lowering->functions[lowering->function_count].return_type),
                         "%s",
                         normalized_return_type);
            }
            snprintf(lowering->functions[lowering->function_count].return_abi_class,
                     sizeof(lowering->functions[lowering->function_count].return_abi_class),
                     "%s",
                     v3_type_abi_class(lowering->functions[lowering->function_count].return_type));
        }
        v3_assign_unique_lowered_symbol_texts(lowering, lowering->function_count);
        v3_lowering_function_body_kind(lines,
                                       line_count,
                                       signature_end_index,
                                       lowering->functions[lowering->function_count].body_kind,
                                       sizeof(lowering->functions[lowering->function_count].body_kind),
                                       lowering->functions[lowering->function_count].body_call_target,
                                       sizeof(lowering->functions[lowering->function_count].body_call_target),
                                       &lowering->functions[lowering->function_count].body_call_arg_count,
                                       lowering->functions[lowering->function_count].body_call_arg0_name,
                                       sizeof(lowering->functions[lowering->function_count].body_call_arg0_name),
                                       lowering->functions[lowering->function_count].body_call_arg_names,
                                       lowering->functions[lowering->function_count].first_body_line,
                                       sizeof(lowering->functions[lowering->function_count].first_body_line),
                                       &lowering->functions[lowering->function_count].body_first_line_number,
                                       &lowering->functions[lowering->function_count].body_last_line_number);
        if (strcmp(lowering->functions[lowering->function_count].body_kind, "return_call_noarg_i32") == 0 &&
            (lowering->functions[lowering->function_count].param_count != 0U ||
             strcmp(lowering->functions[lowering->function_count].return_abi_class, "i32") != 0)) {
            snprintf(lowering->functions[lowering->function_count].body_kind,
                     sizeof(lowering->functions[lowering->function_count].body_kind),
                     "%s",
                     "return_expr");
        }
        if (strcmp(lowering->functions[lowering->function_count].body_kind, "return_call_arg0_i32") == 0 &&
            (lowering->functions[lowering->function_count].param_count != 1U ||
             strcmp(lowering->functions[lowering->function_count].param_abi_classes[0], "i32") != 0 ||
             strcmp(lowering->functions[lowering->function_count].return_abi_class, "i32") != 0)) {
            snprintf(lowering->functions[lowering->function_count].body_kind,
                     sizeof(lowering->functions[lowering->function_count].body_kind),
                     "%s",
                     "return_expr");
        }
        if (strcmp(lowering->functions[lowering->function_count].body_kind, "return_call_params_i32") == 0) {
            bool params_ok = lowering->functions[lowering->function_count].body_call_arg_count >= 2U &&
                             lowering->functions[lowering->function_count].body_call_arg_count <= 4U &&
                             lowering->functions[lowering->function_count].param_count ==
                               lowering->functions[lowering->function_count].body_call_arg_count &&
                             strcmp(lowering->functions[lowering->function_count].return_abi_class, "i32") == 0;
            size_t param_index;
            for (param_index = 0U; params_ok &&
                                  param_index < lowering->functions[lowering->function_count].body_call_arg_count;
                 ++param_index) {
                if (strcmp(lowering->functions[lowering->function_count].param_abi_classes[param_index], "i32") != 0 ||
                    strcmp(lowering->functions[lowering->function_count].body_call_arg_names[param_index],
                           lowering->functions[lowering->function_count].param_names[param_index]) != 0) {
                    params_ok = false;
                }
            }
            if (!params_ok) {
                snprintf(lowering->functions[lowering->function_count].body_kind,
                         sizeof(lowering->functions[lowering->function_count].body_kind),
                         "%s",
                         "return_expr");
            }
        }
        lowering->functions[lowering->function_count].body_last_line_number =
            v3_function_body_last_line_number(lines, line_count, signature_end_index);
        body_statement_count =
            v3_count_function_body_statements(lines, line_count, signature_end_index);
        {
            char bad_statement[8192];
            if (v3_function_body_has_tuple_binding_omission(lines,
                                                            line_count,
                                                            signature_end_index,
                                                            bad_statement,
                                                            sizeof(bad_statement))) {
                fprintf(stderr,
                        "[cheng_v3_seed] tuple literal omission unsupported function=%s stmt=%s\n",
                        lowering->functions[lowering->function_count].symbol_text,
                        bad_statement);
                free(owned);
                return false;
            }
        }
        {
            char bad_statement[8192];
            char bad_callee[PATH_MAX];
            char bad_field[128];
            if (v3_function_body_has_duplicate_constructor_field(lowering,
                                                                 owner_module_path,
                                                                 aliases,
                                                                 alias_count,
                                                                 lines,
                                                                 line_count,
                                                                 signature_end_index,
                                                                 bad_statement,
                                                                 sizeof(bad_statement),
                                                                 bad_callee,
                                                                 sizeof(bad_callee),
                                                                 bad_field,
                                                                 sizeof(bad_field))) {
                fprintf(stderr,
                        "[cheng_v3_seed] duplicate constructor field function=%s callee=%s field=%s stmt=%s\n",
                        lowering->functions[lowering->function_count].symbol_text,
                        bad_callee,
                        bad_field,
                        bad_statement);
                free(owned);
                return false;
            }
        }
        if (strcmp(lowering->functions[lowering->function_count].return_abi_class, "void") != 0) {
            if (body_statement_count == 1U &&
                strcmp(lowering->functions[lowering->function_count].body_kind, "stmt_call") == 0) {
                snprintf(lowering->functions[lowering->function_count].body_kind,
                         sizeof(lowering->functions[lowering->function_count].body_kind),
                         "%s",
                         "return_expr");
            } else if (body_statement_count == 1U &&
                       strcmp(lowering->functions[lowering->function_count].body_kind, "unsupported") == 0 &&
                       lowering->functions[lowering->function_count].first_body_line[0] != '\0' &&
                       !v3_startswith(lowering->functions[lowering->function_count].first_body_line, "if ") &&
                       !v3_startswith(lowering->functions[lowering->function_count].first_body_line, "for ") &&
                       !v3_startswith(lowering->functions[lowering->function_count].first_body_line, "while ") &&
                       !v3_startswith(lowering->functions[lowering->function_count].first_body_line, "case ") &&
                       !v3_startswith(lowering->functions[lowering->function_count].first_body_line, "of ")) {
                snprintf(lowering->functions[lowering->function_count].body_kind,
                         sizeof(lowering->functions[lowering->function_count].body_kind),
                         "%s",
                         "return_expr");
            }
        }
        lowering->functions[lowering->function_count].is_entry =
            v3_streq(source_path, plan->entry_path) &&
            v3_streq(function_name, plan->entry_symbol);
        lowering->functions[lowering->function_count].reachable = false;
        lowering->functions[lowering->function_count].callee_count = 0U;
        lowering->functions[lowering->function_count].generic_binding_count = 0U;
        memset(lowering->functions[lowering->function_count].generic_binding_names,
               0,
               sizeof(lowering->functions[lowering->function_count].generic_binding_names));
        memset(lowering->functions[lowering->function_count].generic_binding_types,
               0,
               sizeof(lowering->functions[lowering->function_count].generic_binding_types));
        if (captured_export_symbol[0] != '\0') {
            snprintf(lowering->functions[lowering->function_count].export_symbol_name,
                     sizeof(lowering->functions[lowering->function_count].export_symbol_name),
                     "%s",
                     captured_export_symbol);
        }
        lowering->function_count += 1U;
    }
    free(owned);
    return true;
}

static int32_t v3_find_lowered_function_index_by_symbol(const V3LoweringPlanStub *lowering,
                                                        const char *symbol_text) {
    return v3_find_lowered_function_index_by_symbol_scan(lowering, symbol_text);
}

static bool v3_refresh_function_callees(const V3LoweringPlanStub *lowering,
                                        V3LoweredFunctionStub *function) {
    char **lines = NULL;
    char *owned_lines = NULL;
    size_t line_count = 0U;
    size_t fn_line_index = 0U;
    char param_names[CHENG_V3_CONST_MAX_BINDINGS][128];
    size_t param_count = 0U;
    char owned_path[PATH_MAX];
    V3ImportAlias aliases[CHENG_V3_MAX_IMPORT_ALIASES];
    size_t alias_count = 0U;
    size_t body_line_index;
    if (!v3_load_function_source_lines(function,
                                       &lines,
                                       &owned_lines,
                                       &line_count,
                                       &fn_line_index,
                                       param_names,
                                       &param_count,
                                       owned_path)) {
        return false;
    }
    (void)param_names;
    (void)param_count;
    v3_parse_import_aliases_from_lines(lines,
                                       line_count,
                                       aliases,
                                       &alias_count,
                                       CHENG_V3_MAX_IMPORT_ALIASES);
    function->callee_count = 0U;
    {
        char signature_copy[8192];
        char *eq;
        snprintf(signature_copy, sizeof(signature_copy), "%s", lines[fn_line_index]);
        eq = strrchr(signature_copy, '=');
        if (eq != NULL) {
            char inline_body[8192];
            v3_trim_copy_text(eq + 1, inline_body, sizeof(inline_body));
            if (inline_body[0] != '\0') {
                v3_collect_calls_from_text_resolved(inline_body,
                                                    function->owner_module_path,
                                                    aliases,
                                                    alias_count,
                                                    lowering,
                                                    function);
            }
        }
    }
    for (body_line_index = fn_line_index + 1U; body_line_index < line_count; ++body_line_index) {
        char line_copy[4096];
        char *body_trimmed;
        snprintf(line_copy, sizeof(line_copy), "%s", lines[body_line_index]);
        body_trimmed = v3_trim_inplace(line_copy);
        if (*body_trimmed == '\0') {
            continue;
        }
        if (v3_line_indent(lines[body_line_index]) <= 0 &&
            v3_is_top_level_decl_start(body_trimmed)) {
            break;
        }
        v3_collect_calls_from_text_resolved(body_trimmed,
                                            function->owner_module_path,
                                            aliases,
                                            alias_count,
                                            lowering,
                                            function);
    }
    free(lines);
    free(owned_lines);
    return true;
}

static bool v3_refresh_lowering_callees(V3LoweringPlanStub *lowering) {
    size_t i;
    for (i = 0; i < lowering->function_count; ++i) {
        if (!v3_refresh_function_callees(lowering, &lowering->functions[i])) {
            return false;
        }
    }
    return true;
}

static bool v3_load_source_lines(const char *source_path,
                                 char ***lines_out,
                                 char **owned_out,
                                 size_t *line_count_out);
static bool v3_parse_top_level_var_binding_text(const char *statement,
                                                char *name_out,
                                                size_t name_cap,
                                                char *type_out,
                                                size_t type_cap,
                                                bool *has_init_out,
                                                char *expr_out,
                                                size_t expr_cap);

static bool v3_seed_reachable_from_module_inits(const V3SystemLinkPlanStub *plan,
                                                V3LoweringPlanStub *lowering,
                                                int32_t *queue,
                                                size_t *queue_tail_io) {
    size_t source_index;
    for (source_index = 0; source_index < plan->source_closure_count; ++source_index) {
        const char *source_path = plan->source_closure_paths[source_index].text;
        char owner_module_path[PATH_MAX];
        char **lines = NULL;
        char *owned = NULL;
        size_t line_count = 0U;
        V3ImportAlias aliases[CHENG_V3_MAX_IMPORT_ALIASES];
        size_t alias_count = 0U;
        bool in_var_block = false;
        size_t line_index;
        V3LoweredFunctionStub synthetic_function;
        if (!v3_load_source_lines(source_path, &lines, &owned, &line_count)) {
            return false;
        }
        v3_source_path_to_module_path(plan->workspace_root,
                                      plan->package_root,
                                      plan->package_id,
                                      source_path,
                                      owner_module_path,
                                      sizeof(owner_module_path));
        v3_parse_import_aliases_from_lines(lines,
                                           line_count,
                                           aliases,
                                           &alias_count,
                                           CHENG_V3_MAX_IMPORT_ALIASES);
        memset(&synthetic_function, 0, sizeof(synthetic_function));
        snprintf(synthetic_function.source_path, sizeof(synthetic_function.source_path), "%s", source_path);
        snprintf(synthetic_function.owner_module_path,
                 sizeof(synthetic_function.owner_module_path),
                 "%s",
                 owner_module_path);
        snprintf(synthetic_function.function_name,
                 sizeof(synthetic_function.function_name),
                 "%s",
                 "__v3_module_init");
        snprintf(synthetic_function.symbol_text,
                 sizeof(synthetic_function.symbol_text),
                 "%s::__v3_module_init",
                 owner_module_path);
        for (line_index = 0; line_index < line_count; ++line_index) {
            char line_copy[4096];
            char name[128];
            char raw_type[256];
            char expr[4096];
            bool has_init = false;
            int32_t indent;
            char *trimmed;
            size_t callee_index;
            snprintf(line_copy, sizeof(line_copy), "%s", lines[line_index]);
            indent = v3_line_indent(line_copy);
            trimmed = v3_trim_inplace(line_copy);
            if (*trimmed == '\0' || *trimmed == '#') {
                continue;
            }
            if (indent <= 0 && strcmp(trimmed, "var") == 0) {
                in_var_block = true;
                continue;
            }
            if (indent <= 0 && !v3_startswith(trimmed, "var ")) {
                in_var_block = false;
            }
            if (indent <= 0 && v3_startswith(trimmed, "var ")) {
                trimmed += 4;
            } else if (!in_var_block || indent <= 0) {
                continue;
            }
            if (!v3_parse_top_level_var_binding_text(trimmed,
                                                     name,
                                                     sizeof(name),
                                                     raw_type,
                                                     sizeof(raw_type),
                                                     &has_init,
                                                     expr,
                                                     sizeof(expr)) ||
                !has_init) {
                continue;
            }
            synthetic_function.callee_count = 0U;
            v3_collect_calls_from_text_resolved(expr,
                                                owner_module_path,
                                                aliases,
                                                alias_count,
                                                lowering,
                                                &synthetic_function);
            for (callee_index = 0; callee_index < synthetic_function.callee_count; ++callee_index) {
                int32_t found =
                    v3_find_lowered_function_index_by_symbol(lowering,
                                                             synthetic_function.callee_symbols[callee_index].text);
                if (found >= 0 && !lowering->functions[found].reachable) {
                    v3_mark_reachable_with_overload_group(lowering, found, queue, queue_tail_io);
                }
            }
        }
        free(lines);
        free(owned);
    }
    return true;
}

static bool v3_source_has_top_level_var_init(const char *source_path,
                                             bool *has_init_out) {
    char **lines = NULL;
    char *owned = NULL;
    size_t line_count = 0U;
    bool in_var_block = false;
    size_t i;
    bool has_init = false;
    if (has_init_out != NULL) {
        *has_init_out = false;
    }
    if (source_path == NULL || source_path[0] == '\0' || has_init_out == NULL) {
        return false;
    }
    if (!v3_load_source_lines(source_path, &lines, &owned, &line_count)) {
        return false;
    }
    for (i = 0; i < line_count; ++i) {
        char line_copy[4096];
        char name[128];
        char raw_type[256];
        char expr[4096];
        bool binding_has_init = false;
        int32_t indent;
        char *trimmed;
        snprintf(line_copy, sizeof(line_copy), "%s", lines[i]);
        indent = v3_line_indent(line_copy);
        trimmed = v3_trim_inplace(line_copy);
        if (*trimmed == '\0' || *trimmed == '#') {
            continue;
        }
        if (indent <= 0 && strcmp(trimmed, "var") == 0) {
            in_var_block = true;
            continue;
        }
        if (indent <= 0 && !v3_startswith(trimmed, "var ")) {
            in_var_block = false;
        }
        if (indent <= 0 && v3_startswith(trimmed, "var ")) {
            trimmed += 4;
        } else if (!in_var_block || indent <= 0) {
            continue;
        }
        if (!v3_parse_top_level_var_binding_text(trimmed,
                                                 name,
                                                 sizeof(name),
                                                 raw_type,
                                                 sizeof(raw_type),
                                                 &binding_has_init,
                                                 expr,
                                                 sizeof(expr))) {
            continue;
        }
        if (binding_has_init) {
            has_init = true;
            break;
        }
    }
    free(lines);
    free(owned);
    *has_init_out = has_init;
    return true;
}

static bool v3_plan_has_module_global_inits(const V3SystemLinkPlanStub *plan) {
    size_t i;
    if (plan == NULL) {
        return true;
    }
    for (i = 0U; i < plan->source_closure_count; ++i) {
        bool has_init = false;
        if (!v3_source_has_top_level_var_init(plan->source_closure_paths[i].text, &has_init)) {
            return true;
        }
        if (has_init) {
            return true;
        }
    }
    return false;
}

static void v3_mark_reachable_with_overload_group(V3LoweringPlanStub *lowering,
                                                  int32_t seed_index,
                                                  int32_t *queue,
                                                  size_t *queue_tail_io) {
    const char *owner_module_path;
    const char *function_name;
    size_t i;
    if (lowering == NULL ||
        queue == NULL ||
        queue_tail_io == NULL ||
        seed_index < 0 ||
        (size_t)seed_index >= lowering->function_count) {
        return;
    }
    owner_module_path = lowering->functions[seed_index].owner_module_path;
    function_name = lowering->functions[seed_index].function_name;
    if (!lowering->functions[seed_index].reachable) {
        lowering->functions[seed_index].reachable = true;
        queue[(*queue_tail_io)++] = seed_index;
    }
    for (i = 0U; i < lowering->function_count; ++i) {
        if ((int32_t)i == seed_index) {
            continue;
        }
        if (strcmp(lowering->functions[i].owner_module_path, owner_module_path) != 0 ||
            strcmp(lowering->functions[i].function_name, function_name) != 0) {
            continue;
        }
        if (v3_function_has_unbound_generics(lowering, &lowering->functions[i]) ||
            v3_function_has_obvious_template_text(&lowering->functions[i])) {
            continue;
        }
        if (lowering->functions[i].reachable) {
            continue;
        }
        lowering->functions[i].reachable = true;
        queue[(*queue_tail_io)++] = (int32_t)i;
    }
}

static CHENG_V3_UNUSED void v3_dedup_lowering_functions(V3LoweringPlanStub *lowering) {
    size_t write_index;
    size_t i;
    if (!lowering) {
        return;
    }
    write_index = 0U;
    for (i = 0U; i < lowering->function_count; ++i) {
        size_t j;
        bool duplicate = false;
        for (j = 0U; j < write_index; ++j) {
            if (strcmp(lowering->functions[j].source_path,
                       lowering->functions[i].source_path) != 0 ||
                strcmp(lowering->functions[j].owner_module_path,
                       lowering->functions[i].owner_module_path) != 0 ||
                strcmp(lowering->functions[j].function_name,
                       lowering->functions[i].function_name) != 0 ||
                lowering->functions[j].signature_line_number !=
                    lowering->functions[i].signature_line_number ||
                lowering->functions[j].generic_binding_count !=
                    lowering->functions[i].generic_binding_count) {
                continue;
            }
            duplicate = true;
            break;
        }
        if (duplicate) {
            continue;
        }
        if (write_index != i) {
            lowering->functions[write_index] = lowering->functions[i];
        }
        write_index += 1U;
    }
    lowering->function_count = write_index;
}

static bool v3_lowering_entry_has_explicit_exports(const V3SystemLinkPlanStub *plan,
                                                   const V3LoweringPlanStub *lowering) {
    size_t i;
    if (plan == NULL || lowering == NULL || plan->entry_path[0] == '\0') {
        return false;
    }
    for (i = 0U; i < lowering->function_count; ++i) {
        if (!v3_streq(lowering->functions[i].source_path, plan->entry_path)) {
            continue;
        }
        if (lowering->functions[i].export_symbol_name[0] != '\0') {
            return true;
        }
    }
    return false;
}

static bool v3_lowered_function_is_entry_export_root(const V3SystemLinkPlanStub *plan,
                                                     const V3LoweringPlanStub *lowering,
                                                     const V3LoweredFunctionStub *function,
                                                     bool entry_has_explicit_exports) {
    if (plan == NULL || lowering == NULL || function == NULL || plan->entry_path[0] == '\0') {
        return false;
    }
    if (!v3_streq(function->source_path, plan->entry_path)) {
        return false;
    }
    if (!entry_has_explicit_exports) {
        return true;
    }
    return function->export_symbol_name[0] != '\0';
}

static void v3_prune_lowering_to_reachable(const V3SystemLinkPlanStub *plan,
                                           V3LoweringPlanStub *lowering) {
    int32_t queue[CHENG_V3_MAX_PLAN_FUNCTIONS];
    size_t queue_head = 0U;
    size_t queue_tail = 0U;
    size_t i;
    size_t write_index = 0U;
    bool saw_entry = false;
    bool seeded_shared_roots = false;
    bool seeded_object_roots = false;
    bool entry_has_explicit_exports = v3_lowering_entry_has_explicit_exports(plan, lowering);
    for (i = 0; i < lowering->function_count; ++i) {
        lowering->functions[i].reachable = false;
        if (lowering->functions[i].is_entry) {
            v3_mark_reachable_with_overload_group(lowering, (int32_t)i, queue, &queue_tail);
            saw_entry = true;
        }
    }
    if (v3_streq(plan->emit_kind, "shared") && plan->entry_path[0] != '\0') {
        for (i = 0; i < lowering->function_count; ++i) {
            if (!v3_lowered_function_is_entry_export_root(plan,
                                                          lowering,
                                                          &lowering->functions[i],
                                                          entry_has_explicit_exports)) {
                continue;
            }
            if (lowering->functions[i].reachable) {
                seeded_shared_roots = true;
                continue;
            }
            v3_mark_reachable_with_overload_group(lowering, (int32_t)i, queue, &queue_tail);
            seeded_shared_roots = true;
        }
    }
    if (!seeded_shared_roots &&
        v3_streq(plan->emit_kind, "obj") && plan->entry_path[0] != '\0') {
        for (i = 0; i < lowering->function_count; ++i) {
            if (!v3_lowered_function_is_entry_export_root(plan,
                                                          lowering,
                                                          &lowering->functions[i],
                                                          entry_has_explicit_exports)) {
                continue;
            }
            if (lowering->functions[i].reachable) {
                seeded_object_roots = true;
                continue;
            }
            v3_mark_reachable_with_overload_group(lowering, (int32_t)i, queue, &queue_tail);
            seeded_object_roots = true;
        }
    }
    if (!saw_entry && !seeded_shared_roots && !seeded_object_roots) {
        return;
    }
    if (!v3_seed_reachable_from_module_inits(plan, lowering, queue, &queue_tail)) {
        return;
    }
    while (queue_head < queue_tail) {
        const V3LoweredFunctionStub *function = &lowering->functions[queue[queue_head++]];
        size_t callee_index;
        for (callee_index = 0; callee_index < function->callee_count; ++callee_index) {
            int32_t found = v3_find_lowered_function_index_by_symbol(lowering,
                                                                     function->callee_symbols[callee_index].text);
            if (found >= 0 && !lowering->functions[found].reachable) {
                v3_mark_reachable_with_overload_group(lowering, found, queue, &queue_tail);
            }
        }
    }
    for (i = 0; i < lowering->function_count; ++i) {
        if (!lowering->functions[i].reachable) {
            continue;
        }
        if (write_index != i) {
            lowering->functions[write_index] = lowering->functions[i];
        }
        write_index += 1U;
    }
    lowering->function_count = write_index;
}

static bool v3_build_lowering_plan_stub(const V3SystemLinkPlanStub *plan,
                                        V3LoweringPlanStub *lowering) {
    size_t i;
    bool saw_entry = false;
    memset(lowering, 0, sizeof(*lowering));
    for (i = 0; i < plan->source_closure_count; ++i) {
        if (!v3_collect_top_level_consts_from_source(plan,
                                                     plan->source_closure_paths[i].text,
                                                     lowering)) {
            return false;
        }
    }
    for (i = 0; i < plan->source_closure_count; ++i) {
        if (!v3_collect_type_defs_from_source(plan,
                                              plan->source_closure_paths[i].text,
                                              lowering)) {
            return false;
        }
    }
    for (i = 0; i < plan->source_closure_count; ++i) {
        if (!v3_refresh_type_defs_from_source(plan,
                                              plan->source_closure_paths[i].text,
                                              lowering)) {
            return false;
        }
    }
    for (i = 0; i < plan->source_closure_count; ++i) {
        if (!v3_collect_lowering_functions_from_source(plan,
                                                       plan->source_closure_paths[i].text,
                                                       lowering)) {
            return false;
        }
    }
    for (i = 0; i < plan->source_closure_count; ++i) {
        if (!v3_refresh_function_abis_from_source(plan,
                                                  plan->source_closure_paths[i].text,
                                                  lowering)) {
            return false;
        }
    }
    v3_dedup_lowering_functions(lowering);
    if (lowering->function_count <= 0U) {
        v3_lowering_add_reason(lowering, "lowering_functions_missing");
        return true;
    }
    if (plan->entry_symbol[0] != '\0') {
        for (i = 0; i < lowering->function_count; ++i) {
            if (lowering->functions[i].is_entry) {
                saw_entry = true;
                break;
            }
        }
        if (!saw_entry) {
            v3_lowering_add_reason(lowering, "lowering_entry_function_missing");
        }
    }
    if (!v3_refresh_lowering_callees(lowering)) {
        v3_lowering_add_reason(lowering, "lowering_callee_refresh_failed");
        return true;
    }
    v3_prune_lowering_to_reachable(plan, lowering);
    return true;
}

static void v3_sync_lowering_exports_from_world(const V3CompilerWorldArtifacts *world,
                                                V3LoweringPlanStub *lowering) {
    size_t i;
    if (world == NULL || lowering == NULL) {
        return;
    }
    for (i = 0U; i < world->exported_count; ++i) {
        int32_t function_index =
            v3_find_lowered_function_index_by_symbol(lowering, world->exports[i].symbol_text);
        if (function_index < 0) {
            continue;
        }
        v3_copy_text(lowering->functions[function_index].export_symbol_name,
                     sizeof(lowering->functions[function_index].export_symbol_name),
                     world->exports[i].export_symbol_name);
    }
}

static void v3_sanitize_path_part(const char *text, char *out, size_t cap) {
    size_t i;
    size_t used = 0U;
    if (!text || cap <= 0U) {
        return;
    }
    out[0] = '\0';
    for (i = 0; text[i] != '\0' && used + 1U < cap; ++i) {
        const char ch = text[i];
        const bool digit = ch >= '0' && ch <= '9';
        const bool upper = ch >= 'A' && ch <= 'Z';
        const bool lower = ch >= 'a' && ch <= 'z';
        out[used++] = (digit || upper || lower) ? ch : '_';
    }
    out[used] = '\0';
}

static void v3_primary_symbol_name(const V3SystemLinkPlanStub *plan,
                                   const V3LoweredFunctionStub *function,
                                   char *out,
                                   size_t cap) {
    char raw[PATH_MAX];
    char sanitized[PATH_MAX];
    char unique_name[PATH_MAX];
    if (function->export_symbol_name[0] != '\0') {
        v3_copy_target_symbol_name(plan, function->export_symbol_name, out, cap);
        return;
    }
    v3_lowered_function_overload_symbol_text(function, raw, sizeof(raw));
    if (cap == 0U) {
        return;
    }
    v3_sanitize_path_part(raw, sanitized, sizeof(sanitized));
    if (function->signature_line_number > 0U) {
        snprintf(unique_name,
                 sizeof(unique_name),
                 "%s__L%zu",
                 sanitized,
                 function->signature_line_number);
        v3_copy_target_symbol_name(plan, unique_name, out, cap);
        return;
    }
    v3_copy_target_symbol_name(plan, sanitized, out, cap);
}

static void v3_runtime_entry_bridge_symbol(const V3SystemLinkPlanStub *plan,
                                           char *out,
                                           size_t cap) {
    v3_copy_target_symbol_name(plan,
                               v3_streq(plan->module_kind, "compiler_control_plane") ?
                                   "cheng_v3_tooling_argv_entry" :
                                   "cheng_v3_program_argv_entry",
                               out,
                               cap);
}

static void v3_text_append(char *out, size_t cap, const char *text);
static void v3_text_appendf(char *out, size_t cap, const char *fmt, ...);

static const char *v3_x64_qreg_name(int reg_index) {
    switch (reg_index) {
        case 0: return "%rax";
        case 1: return "%rbx";
        case 2: return "%rdx";
        case 3: return "%rcx";
        case 4: return "%r8";
        case 5: return "%r9";
        case 6: return "%r10";
        case 7: return "%r11";
        case 8: return "%r12";
        case 9: return "%r13";
        case 10: return "%r14";
        case 11: return "%r15";
        case 14: return "%rdi";
        case 15: return "%rsi";
        case 16: return "%r10";
        case 17: return "%r11";
        case 29: return "%rbp";
        default: return "%rax";
    }
}

static const char *v3_x64_dreg_name(int reg_index) {
    switch (reg_index) {
        case 0: return "%eax";
        case 1: return "%ebx";
        case 2: return "%edx";
        case 3: return "%ecx";
        case 4: return "%r8d";
        case 5: return "%r9d";
        case 6: return "%r10d";
        case 7: return "%r11d";
        case 8: return "%r12d";
        case 9: return "%r13d";
        case 10: return "%r14d";
        case 11: return "%r15d";
        case 14: return "%edi";
        case 15: return "%esi";
        case 16: return "%r10d";
        case 17: return "%r11d";
        case 29: return "%ebp";
        default: return "%eax";
    }
}

static const char *v3_x64_hreg_name(int reg_index) {
    switch (reg_index) {
        case 0: return "%ax";
        case 1: return "%bx";
        case 2: return "%dx";
        case 3: return "%cx";
        case 4: return "%r8w";
        case 5: return "%r9w";
        case 6: return "%r10w";
        case 7: return "%r11w";
        case 8: return "%r12w";
        case 9: return "%r13w";
        case 10: return "%r14w";
        case 11: return "%r15w";
        case 14: return "%di";
        case 15: return "%si";
        case 16: return "%r10w";
        case 17: return "%r11w";
        case 29: return "%bp";
        default: return "%ax";
    }
}

static const char *v3_x64_breg_name(int reg_index) {
    switch (reg_index) {
        case 0: return "%al";
        case 1: return "%bl";
        case 2: return "%dl";
        case 3: return "%cl";
        case 4: return "%r8b";
        case 5: return "%r9b";
        case 6: return "%r10b";
        case 7: return "%r11b";
        case 8: return "%r12b";
        case 9: return "%r13b";
        case 10: return "%r14b";
        case 11: return "%r15b";
        case 14: return "%dil";
        case 15: return "%sil";
        case 16: return "%r10b";
        case 17: return "%r11b";
        default: return "%al";
    }
}

static int v3_call_arg_reg_count_for_target(const V3SystemLinkPlanStub *plan,
                                            bool composite_return) {
    if (plan != NULL && v3_target_is_linux_x86_64(plan->target_triple)) {
        return composite_return ? 5 : 6;
    }
    return CHENG_V3_CALL_ARG_REGS;
}

static const char *CHENG_V3_UNUSED v3_x64_call_arg_qreg(size_t arg_index, bool composite_return) {
    static const char *NORMAL[] = {"%rdi", "%rsi", "%rdx", "%rcx", "%r8", "%r9"};
    static const char *SRET[] = {"%rsi", "%rdx", "%rcx", "%r8", "%r9"};
    if (composite_return) {
        return arg_index < sizeof(SRET) / sizeof(SRET[0]) ? SRET[arg_index] : NULL;
    }
    return arg_index < sizeof(NORMAL) / sizeof(NORMAL[0]) ? NORMAL[arg_index] : NULL;
}

static const char *CHENG_V3_UNUSED v3_x64_call_arg_dreg(size_t arg_index, bool composite_return) {
    static const char *NORMAL[] = {"%edi", "%esi", "%edx", "%ecx", "%r8d", "%r9d"};
    static const char *SRET[] = {"%esi", "%edx", "%ecx", "%r8d", "%r9d"};
    if (composite_return) {
        return arg_index < sizeof(SRET) / sizeof(SRET[0]) ? SRET[arg_index] : NULL;
    }
    return arg_index < sizeof(NORMAL) / sizeof(NORMAL[0]) ? NORMAL[arg_index] : NULL;
}

static const char *CHENG_V3_UNUSED v3_x64_call_arg_breg(size_t arg_index, bool composite_return) {
    static const char *NORMAL[] = {"%dil", "%sil", "%dl", "%cl", "%r8b", "%r9b"};
    static const char *SRET[] = {"%sil", "%dl", "%cl", "%r8b", "%r9b"};
    if (composite_return) {
        return arg_index < sizeof(SRET) / sizeof(SRET[0]) ? SRET[arg_index] : NULL;
    }
    return arg_index < sizeof(NORMAL) / sizeof(NORMAL[0]) ? NORMAL[arg_index] : NULL;
}

static int v3_x64_call_arg_virtual_reg(size_t arg_index, bool composite_return) {
    static const int NORMAL[] = {14, 15, 2, 3, 4, 5};
    static const int SRET[] = {15, 2, 3, 4, 5};
    if (composite_return) {
        return arg_index < sizeof(SRET) / sizeof(SRET[0]) ? SRET[arg_index] : -1;
    }
    return arg_index < sizeof(NORMAL) / sizeof(NORMAL[0]) ? NORMAL[arg_index] : -1;
}

static int v3_first_call_arg_virtual_reg(bool composite_return) {
    if (v3_active_target_is_linux_x86_64()) {
        return v3_x64_call_arg_virtual_reg(0U, composite_return);
    }
    return 0;
}

static int v3_x64_pick_scratch_reg(int reg_a, int reg_b) {
    if (reg_a != 16 && reg_b != 16) {
        return 16;
    }
    if (reg_a != 17 && reg_b != 17) {
        return 17;
    }
    if (reg_a != 0 && reg_b != 0) {
        return 0;
    }
    if (reg_a != 2 && reg_b != 2) {
        return 2;
    }
    return 3;
}

static bool v3_x64_reg_is_callee_saved(int reg_index) {
    return reg_index == 1 || reg_index == 8 || reg_index == 9 ||
           reg_index == 10 || reg_index == 11 || reg_index == 29;
}

static int v3_x64_pick_preserved_reg(int reg_a, int reg_b) {
    static const int CANDIDATES[] = {8, 9, 10, 11, 1};
    size_t i;
    for (i = 0; i < sizeof(CANDIDATES) / sizeof(CANDIDATES[0]); ++i) {
        if (CANDIDATES[i] != reg_a && CANDIDATES[i] != reg_b) {
            return CANDIDATES[i];
        }
    }
    return -1;
}

static int v3_x64_pick_preserved_reg3(int reg_a, int reg_b, int reg_c) {
    static const int CANDIDATES[] = {8, 9, 10, 11, 1};
    size_t i;
    for (i = 0; i < sizeof(CANDIDATES) / sizeof(CANDIDATES[0]); ++i) {
        if (CANDIDATES[i] != reg_a &&
            CANDIDATES[i] != reg_b &&
            CANDIDATES[i] != reg_c) {
            return CANDIDATES[i];
        }
    }
    return -1;
}

static void v3_emit_x64_prepare_rdi_rsi_q_q(char *out,
                                            size_t cap,
                                            int rdi_src_reg,
                                            int rsi_src_reg) {
    int safe_rsi_src_reg = rsi_src_reg;
    if (rdi_src_reg != 14 && rsi_src_reg == 14) {
        int scratch_reg = v3_x64_pick_scratch_reg(rdi_src_reg, rsi_src_reg);
        v3_text_appendf(out,
                        cap,
                        "  movq %s, %s\n",
                        v3_x64_qreg_name(14),
                        v3_x64_qreg_name(scratch_reg));
        safe_rsi_src_reg = scratch_reg;
    }
    if (rdi_src_reg != 14) {
        v3_text_appendf(out,
                        cap,
                        "  movq %s, %%rdi\n",
                        v3_x64_qreg_name(rdi_src_reg));
    }
    if (safe_rsi_src_reg != 15) {
        v3_text_appendf(out,
                        cap,
                        "  movq %s, %%rsi\n",
                        v3_x64_qreg_name(safe_rsi_src_reg));
    }
}

static void v3_emit_x64_prepare_rdi_esi_q_d(char *out,
                                            size_t cap,
                                            int rdi_src_reg,
                                            int esi_src_reg) {
    int safe_esi_src_reg = esi_src_reg;
    if (rdi_src_reg != 14 && esi_src_reg == 14) {
        int scratch_reg = v3_x64_pick_scratch_reg(rdi_src_reg, esi_src_reg);
        v3_text_appendf(out,
                        cap,
                        "  movq %s, %s\n",
                        v3_x64_qreg_name(14),
                        v3_x64_qreg_name(scratch_reg));
        safe_esi_src_reg = scratch_reg;
    }
    if (rdi_src_reg != 14) {
        v3_text_appendf(out,
                        cap,
                        "  movq %s, %%rdi\n",
                        v3_x64_qreg_name(rdi_src_reg));
    }
    if (safe_esi_src_reg != 15) {
        v3_text_appendf(out,
                        cap,
                        "  movl %s, %%esi\n",
                        v3_x64_dreg_name(safe_esi_src_reg));
    }
}

static void v3_emit_x64_prepare_edi_esi_d_d(char *out,
                                            size_t cap,
                                            int edi_src_reg,
                                            int esi_src_reg) {
    int safe_esi_src_reg = esi_src_reg;
    if (edi_src_reg != 14 && esi_src_reg == 14) {
        int scratch_reg = v3_x64_pick_scratch_reg(edi_src_reg, esi_src_reg);
        v3_text_appendf(out,
                        cap,
                        "  movq %s, %s\n",
                        v3_x64_qreg_name(14),
                        v3_x64_qreg_name(scratch_reg));
        safe_esi_src_reg = scratch_reg;
    }
    if (edi_src_reg != 14) {
        v3_text_appendf(out,
                        cap,
                        "  movl %s, %%edi\n",
                        v3_x64_dreg_name(edi_src_reg));
    }
    if (safe_esi_src_reg != 15) {
        v3_text_appendf(out,
                        cap,
                        "  movl %s, %%esi\n",
                        v3_x64_dreg_name(safe_esi_src_reg));
    }
}

static int32_t v3_incoming_stack_arg_offset_for_target(const V3SystemLinkPlanStub *plan,
                                                       int32_t frame_size,
                                                       int32_t stack_arg_index) {
    if (plan != NULL && v3_target_is_linux_x86_64(plan->target_triple)) {
        return frame_size + 56 + stack_arg_index * 8;
    }
    return frame_size + stack_arg_index * 8;
}

static const char *v3_x64_cond_suffix(const char *cond) {
    if (cond == NULL) {
        return "e";
    }
    if (strcmp(cond, "eq") == 0) return "e";
    if (strcmp(cond, "ne") == 0) return "ne";
    if (strcmp(cond, "lt") == 0) return "l";
    if (strcmp(cond, "le") == 0) return "le";
    if (strcmp(cond, "gt") == 0) return "g";
    if (strcmp(cond, "ge") == 0) return "ge";
    return "e";
}

static void v3_emit_x64_move_reg(char *out,
                                 size_t cap,
                                 int dst_reg,
                                 int src_reg,
                                 const char *abi_class) {
    if (v3_abi_class_word_reg(abi_class)) {
        if (dst_reg != src_reg) {
            v3_text_appendf(out, cap,
                            "  movl %s, %s\n",
                            v3_x64_dreg_name(src_reg),
                            v3_x64_dreg_name(dst_reg));
        }
        return;
    }
    if (dst_reg != src_reg) {
        v3_text_appendf(out, cap,
                        "  movq %s, %s\n",
                        v3_x64_qreg_name(src_reg),
                        v3_x64_qreg_name(dst_reg));
    }
}

static void v3_emit_move_reg_abi(char *out,
                                 size_t cap,
                                 int dst_reg,
                                 int src_reg,
                                 const char *abi_class) {
    if (v3_active_target_is_linux_x86_64()) {
        v3_emit_x64_move_reg(out, cap, dst_reg, src_reg, abi_class);
        return;
    }
    if (dst_reg == src_reg) {
        return;
    }
    if (v3_abi_class_word_reg(abi_class)) {
        v3_text_appendf(out, cap, "  mov w%d, w%d\n", dst_reg, src_reg);
    } else {
        v3_text_appendf(out, cap, "  mov x%d, x%d\n", dst_reg, src_reg);
    }
}

static void v3_emit_x64_cmp_zero_set_bool(char *out,
                                          size_t cap,
                                          int src_reg,
                                          int dst_reg,
                                          bool wide,
                                          const char *cond) {
    v3_text_appendf(out, cap,
                    "  cmp%s $0, %s\n"
                    "  movl $0, %s\n"
                    "  set%s %s\n",
                    wide ? "q" : "l",
                    wide ? v3_x64_qreg_name(src_reg) : v3_x64_dreg_name(src_reg),
                    v3_x64_dreg_name(dst_reg),
                    v3_x64_cond_suffix(cond),
                    v3_x64_breg_name(dst_reg));
}

static void v3_emit_x64_cmp_regs_set_bool(char *out,
                                          size_t cap,
                                          int left_reg,
                                          int right_reg,
                                          int dst_reg,
                                          bool wide,
                                          const char *cond) {
    v3_text_appendf(out, cap,
                    "  cmp%s %s, %s\n"
                    "  movl $0, %s\n"
                    "  set%s %s\n",
                    wide ? "q" : "l",
                    wide ? v3_x64_qreg_name(right_reg) : v3_x64_dreg_name(right_reg),
                    wide ? v3_x64_qreg_name(left_reg) : v3_x64_dreg_name(left_reg),
                    v3_x64_dreg_name(dst_reg),
                    v3_x64_cond_suffix(cond),
                    v3_x64_breg_name(dst_reg));
}

static void v3_emit_x64_branch_zero(char *out,
                                    size_t cap,
                                    int reg_index,
                                    bool wide,
                                    bool branch_if_zero,
                                    const char *label) {
    v3_text_appendf(out, cap,
                    "  cmp%s $0, %s\n"
                    "  j%s %s\n",
                    wide ? "q" : "l",
                    wide ? v3_x64_qreg_name(reg_index) : v3_x64_dreg_name(reg_index),
                    branch_if_zero ? "e" : "ne",
                    label);
}

static void v3_emit_branch_zero(char *out,
                                size_t cap,
                                int reg_index,
                                bool wide,
                                const char *label) {
    if (v3_active_target_is_linux_x86_64()) {
        v3_emit_x64_branch_zero(out, cap, reg_index, wide, true, label);
        return;
    }
    v3_text_appendf(out, cap,
                    "  cmp %c%d, #0\n"
                    "  b.eq %s\n",
                    wide ? 'x' : 'w',
                    reg_index,
                    label);
}

static void v3_emit_branch_nonzero(char *out,
                                   size_t cap,
                                   int reg_index,
                                   bool wide,
                                   const char *label) {
    if (v3_active_target_is_linux_x86_64()) {
        v3_emit_x64_branch_zero(out, cap, reg_index, wide, false, label);
        return;
    }
    v3_text_appendf(out, cap,
                    "  cmp %c%d, #0\n"
                    "  b.ne %s\n",
                    wide ? 'x' : 'w',
                    reg_index,
                    label);
}

static void v3_emit_jump_label(char *out,
                               size_t cap,
                               const char *label) {
    v3_text_appendf(out,
                    cap,
                    v3_active_target_is_linux_x86_64() ? "  jmp %s\n" : "  b %s\n",
                    label);
}

static bool v3_active_target_is_linux_x86_64(void) {
    return v3_target_is_linux_x86_64(v3_active_codegen_target_triple);
}

static void v3_append_cstring_section_for_target(const V3SystemLinkPlanStub *plan,
                                                 char *out,
                                                 size_t cap) {
    if (v3_target_is_elf_aarch64(plan->target_triple) ||
        v3_target_is_linux_x86_64(plan->target_triple)) {
        v3_text_append(out, cap, ".section .rodata\n");
    } else {
        v3_text_append(out, cap, ".section __TEXT,__cstring,cstring_literals\n");
    }
}

static void v3_append_const_section_for_target(const V3SystemLinkPlanStub *plan,
                                               char *out,
                                               size_t cap) {
    if (v3_target_is_elf_aarch64(plan->target_triple) ||
        v3_target_is_linux_x86_64(plan->target_triple)) {
        v3_text_append(out, cap, ".section .data.rel.ro\n.p2align 3\n");
    } else {
        v3_text_append(out, cap, ".section __DATA_CONST,__const\n.p2align 3\n");
    }
}

static void v3_append_symbol_trailer_for_target(const V3SystemLinkPlanStub *plan,
                                                char *out,
                                                size_t cap) {
    if (v3_target_is_darwin_arm64(plan->target_triple)) {
        v3_text_append(out, cap, ".subsections_via_symbols\n");
    }
}

static bool v3_emit_symbol_address_for_target(const V3SystemLinkPlanStub *plan,
                                              char *out,
                                              size_t cap,
                                              int reg_index,
                                              const char *symbol_name) {
    bool local_label = false;
    if (plan == NULL || symbol_name == NULL || symbol_name[0] == '\0') {
        return false;
    }
    local_label = v3_startswith(symbol_name, "L_");
    if (v3_target_is_elf_aarch64(plan->target_triple)) {
        if ((v3_streq(plan->emit_kind, "shared") || v3_streq(plan->emit_kind, "obj")) && !local_label) {
            v3_text_appendf(out, cap,
                            "  adrp x%d, :got:%s\n"
                            "  ldr x%d, [x%d, #:got_lo12:%s]\n",
                            reg_index,
                            symbol_name,
                            reg_index,
                            reg_index,
                            symbol_name);
            return true;
        }
        v3_text_appendf(out, cap,
                        "  adrp x%d, %s\n"
                        "  add x%d, x%d, :lo12:%s\n",
                        reg_index,
                        symbol_name,
                        reg_index,
                        reg_index,
                        symbol_name);
        return true;
    }
    if (v3_target_is_linux_x86_64(plan->target_triple)) {
        if ((v3_streq(plan->emit_kind, "shared") || v3_streq(plan->emit_kind, "obj")) && !local_label) {
            v3_text_appendf(out, cap,
                            "  movq %s@GOTPCREL(%%rip), %s\n",
                            symbol_name,
                            v3_x64_qreg_name(reg_index));
            return true;
        }
        v3_text_appendf(out, cap,
                        "  leaq %s(%%rip), %s\n",
                        symbol_name,
                        v3_x64_qreg_name(reg_index));
        return true;
    }
    v3_text_appendf(out, cap,
                    "  adrp x%d, %s@PAGE\n"
                    "  add x%d, x%d, %s@PAGEOFF\n",
                    reg_index,
                    symbol_name,
                    reg_index,
                    reg_index,
                    symbol_name);
    return true;
}

static void v3_emit_call_named_symbol(const V3SystemLinkPlanStub *plan,
                                      char *out,
                                      size_t cap,
                                      const char *symbol) {
    char target_symbol[PATH_MAX];
    v3_copy_target_symbol_name(plan, symbol, target_symbol, sizeof(target_symbol));
    if (plan != NULL && v3_target_is_linux_x86_64(plan->target_triple)) {
        v3_text_appendf(out, cap, "  call %s\n", target_symbol);
        return;
    }
    v3_text_appendf(out, cap, "  bl %s\n", target_symbol);
}

static void v3_emit_call_target_symbol_text(const V3SystemLinkPlanStub *plan,
                                            char *out,
                                            size_t cap,
                                            const char *target_symbol) {
    if (plan != NULL && v3_target_is_linux_x86_64(plan->target_triple)) {
        v3_text_appendf(out, cap, "  call %s\n", target_symbol);
        return;
    }
    v3_text_appendf(out, cap, "  bl %s\n", target_symbol);
}

static void v3_embedded_line_map_end_label(size_t ordinal,
                                           char *out,
                                           size_t cap) {
    snprintf(out, cap, "L_v3_embedded_line_map_end_%zu", ordinal);
}

static void v3_embedded_line_map_function_name_label(size_t ordinal,
                                                     char *out,
                                                     size_t cap) {
    snprintf(out, cap, "L_v3_embedded_line_map_fn_%zu", ordinal);
}

static void v3_embedded_line_map_source_path_label(size_t ordinal,
                                                   char *out,
                                                   size_t cap) {
    snprintf(out, cap, "L_v3_embedded_line_map_src_%zu", ordinal);
}

static bool v3_append_embedded_line_map(char *text,
                                        size_t cap,
                                        const V3SystemLinkPlanStub *plan,
                                        const V3LoweringPlanStub *lowering,
                                        const V3LoweredFunctionStub *single_function) {
    const V3LoweredFunctionStub *entry_function = NULL;
    size_t function_entry_count = 0U;
    size_t bridge_ordinal = single_function != NULL ? 1U : lowering->function_count;
    size_t i;
    if (!v3_streq(plan->emit_kind, "exe")) {
        return true;
    }
    entry_function = single_function != NULL ? single_function : v3_find_entry_lowered_function(lowering);
    if (single_function != NULL) {
        function_entry_count = 1U;
    } else {
        for (i = 0; i < lowering->function_count; ++i) {
            if (v3_function_has_unbound_generics(lowering, &lowering->functions[i]) ||
                v3_function_has_obvious_template_text(&lowering->functions[i])) {
                continue;
            }
            function_entry_count += 1U;
        }
    }
    {
        size_t entry_count = function_entry_count + (entry_function != NULL ? 1U : 0U);
    if (entry_count == 0U) {
        return true;
    }
    v3_append_cstring_section_for_target(plan, text, cap);
    for (i = 0; single_function != NULL ? i < 1U : i < lowering->function_count; ++i) {
        const V3LoweredFunctionStub *function =
            single_function != NULL ? single_function : &lowering->functions[i];
        char fn_label[64];
        char src_label[64];
        char *escaped_function;
        char *escaped_source;
        if (single_function == NULL &&
            (v3_function_has_unbound_generics(lowering, function) ||
             v3_function_has_obvious_template_text(function))) {
            continue;
        }
        v3_embedded_line_map_function_name_label(i, fn_label, sizeof(fn_label));
        v3_embedded_line_map_source_path_label(i, src_label, sizeof(src_label));
        escaped_function = v3_c_escape(function->function_name);
        escaped_source = v3_c_escape(function->source_path);
        v3_text_appendf(text, cap,
                        "%s:\n"
                        "  .asciz \"%s\"\n"
                        "%s:\n"
                        "  .asciz \"%s\"\n",
                        fn_label,
                        escaped_function,
                        src_label,
                        escaped_source);
        free(escaped_function);
        free(escaped_source);
    }
    if (entry_function != NULL) {
        char fn_label[64];
        char src_label[64];
        char *escaped_function;
        char *escaped_source;
        v3_embedded_line_map_function_name_label(bridge_ordinal, fn_label, sizeof(fn_label));
        v3_embedded_line_map_source_path_label(bridge_ordinal, src_label, sizeof(src_label));
        escaped_function = v3_c_escape(entry_function->function_name);
        escaped_source = v3_c_escape(entry_function->source_path);
        v3_text_appendf(text, cap,
                        "%s:\n"
                        "  .asciz \"%s\"\n"
                        "%s:\n"
                        "  .asciz \"%s\"\n",
                        fn_label,
                        escaped_function,
                        src_label,
                        escaped_source);
        free(escaped_function);
        free(escaped_source);
    }
    v3_append_const_section_for_target(plan, text, cap);
    v3_text_append(text, cap, "L_v3_embedded_line_map_entries:\n");
    for (i = 0; single_function != NULL ? i < 1U : i < lowering->function_count; ++i) {
        const V3LoweredFunctionStub *function =
            single_function != NULL ? single_function : &lowering->functions[i];
        char symbol_name[PATH_MAX];
        char end_label[64];
        char fn_label[64];
        char src_label[64];
        if (single_function == NULL &&
            (v3_function_has_unbound_generics(lowering, function) ||
             v3_function_has_obvious_template_text(function))) {
            continue;
        }
        v3_primary_symbol_name(plan, function, symbol_name, sizeof(symbol_name));
        v3_embedded_line_map_end_label(i, end_label, sizeof(end_label));
        v3_embedded_line_map_function_name_label(i, fn_label, sizeof(fn_label));
        v3_embedded_line_map_source_path_label(i, src_label, sizeof(src_label));
        v3_text_appendf(text, cap,
                        "  .quad %s\n"
                        "  .quad %s\n"
                        "  .quad %s\n"
                        "  .quad %s\n"
                        "  .long %zu\n"
                        "  .long %zu\n"
                        "  .long %zu\n"
                        "  .long 0\n",
                        symbol_name,
                        end_label,
                        fn_label,
                        src_label,
                        function->signature_line_number,
                        function->body_first_line_number,
                        function->body_last_line_number);
    }
    if (entry_function != NULL) {
        char bridge_symbol[PATH_MAX];
        char end_label[64];
        char fn_label[64];
        char src_label[64];
        v3_runtime_entry_bridge_symbol(plan, bridge_symbol, sizeof(bridge_symbol));
        v3_embedded_line_map_end_label(bridge_ordinal, end_label, sizeof(end_label));
        v3_embedded_line_map_function_name_label(bridge_ordinal, fn_label, sizeof(fn_label));
        v3_embedded_line_map_source_path_label(bridge_ordinal, src_label, sizeof(src_label));
        v3_text_appendf(text, cap,
                        "  .quad %s\n"
                        "  .quad %s\n"
                        "  .quad %s\n"
                        "  .quad %s\n"
                        "  .long %zu\n"
                        "  .long %zu\n"
                        "  .long %zu\n"
                        "  .long 0\n",
                        bridge_symbol,
                        end_label,
                        fn_label,
                        src_label,
                        entry_function->signature_line_number,
                        entry_function->body_first_line_number,
                        entry_function->body_last_line_number);
    }
    {
        char entries_getter_symbol[PATH_MAX];
        char count_getter_symbol[PATH_MAX];
        v3_copy_target_symbol_name(plan,
                                   "cheng_v3_embedded_line_map_entries_get",
                                   entries_getter_symbol,
                                   sizeof(entries_getter_symbol));
        v3_copy_target_symbol_name(plan,
                                   "cheng_v3_embedded_line_map_count_get",
                                   count_getter_symbol,
                                   sizeof(count_getter_symbol));
        v3_text_appendf(text, cap,
                        "L_v3_embedded_line_map_count_value:\n"
                        "  .quad %zu\n"
                        ".text\n"
                        ".p2align 2\n"
                        ".globl %s\n"
                        "%s:\n",
                        entry_count,
                        entries_getter_symbol,
                        entries_getter_symbol);
        if (!v3_emit_symbol_address_for_target(plan, text, cap, 0, "L_v3_embedded_line_map_entries")) {
            return false;
        }
        v3_text_appendf(text, cap,
                        "  ret\n"
                        ".globl %s\n"
                        "%s:\n",
                        count_getter_symbol,
                        count_getter_symbol);
        if (!v3_emit_symbol_address_for_target(plan, text, cap, 0, "L_v3_embedded_line_map_count_value")) {
            return false;
        }
        if (v3_target_is_linux_x86_64(plan->target_triple)) {
            v3_text_append(text, cap,
                           "  movq 0(%rax), %rax\n"
                           "  retq\n");
        } else {
            v3_text_append(text, cap,
                           "  ldr x0, [x0]\n"
                           "  ret\n");
        }
    }
    }
    return true;
}

static void v3_line_map_path_for_output(const char *output_path,
                                        char *out,
                                        size_t cap) {
    snprintf(out, cap, "%s.v3.map", output_path);
}

static char *v3_build_line_map_text(const V3SystemLinkPlanStub *plan,
                                    const V3LoweringPlanStub *lowering) {
    const V3LoweredFunctionStub *entry_function;
    size_t cap;
    char *text;
    size_t i;
    size_t entry_count = 0U;
    entry_function = v3_find_entry_lowered_function(lowering);
    cap = 256U + (lowering->function_count + 1U) * (PATH_MAX * 2U + 192U);
    text = (char *)v3_xmalloc(cap);
    text[0] = '\0';
    for (i = 0; i < lowering->function_count; ++i) {
        if (v3_function_has_unbound_generics(lowering, &lowering->functions[i]) ||
            v3_function_has_obvious_template_text(&lowering->functions[i])) {
            continue;
        }
        entry_count += 1U;
    }
    strncat(text, "v3_line_map_v1\n", cap - strlen(text) - 1U);
    snprintf(text + strlen(text),
             cap - strlen(text),
             "entry_count=%zu\n",
             entry_count + (entry_function != NULL ? 1U : 0U));
    for (i = 0; i < lowering->function_count; ++i) {
        char primary_symbol[PATH_MAX];
        const V3LoweredFunctionStub *function = &lowering->functions[i];
        if (v3_function_has_unbound_generics(lowering, function)) {
            continue;
        }
        v3_primary_symbol_name(plan, function, primary_symbol, sizeof(primary_symbol));
        snprintf(text + strlen(text),
                 cap - strlen(text),
                 "entry\t%s\t%s\t%s\t%zu\t%zu\t%zu\n",
                 primary_symbol,
                 function->function_name,
                 function->source_path,
                 function->signature_line_number,
                 function->body_first_line_number,
                 function->body_last_line_number);
    }
    if (entry_function != NULL) {
        char bridge_symbol[PATH_MAX];
        v3_runtime_entry_bridge_symbol(plan, bridge_symbol, sizeof(bridge_symbol));
        snprintf(text + strlen(text),
                 cap - strlen(text),
                 "entry\t%s\t%s\t%s\t%zu\t%zu\t%zu\n",
                 bridge_symbol,
                 entry_function->function_name,
                 entry_function->source_path,
                 entry_function->signature_line_number,
                 entry_function->body_first_line_number,
                 entry_function->body_last_line_number);
    }
    return text;
}

static bool v3_write_executable_line_map(const V3SystemLinkPlanStub *plan,
                                         const V3LoweringPlanStub *lowering) {
    char map_path[PATH_MAX];
    char *text;
    if (!v3_streq(plan->emit_kind, "exe") || plan->output_path[0] == '\0') {
        return true;
    }
    v3_line_map_path_for_output(plan->output_path, map_path, sizeof(map_path));
    text = v3_build_line_map_text(plan, lowering);
    if (!v3_ensure_parent_dir(map_path) || !v3_write_text_file(map_path, text)) {
        free(text);
        return false;
    }
    free(text);
    return true;
}

static const V3LoweredFunctionStub *v3_find_entry_lowered_function(const V3LoweringPlanStub *lowering) {
    size_t i;
    for (i = 0; i < lowering->function_count; ++i) {
        if (lowering->functions[i].is_entry) {
            return &lowering->functions[i];
        }
    }
    return NULL;
}

static bool v3_abi_class_scalar_or_ptr(const char *abi_class) {
    V3AbiDescriptor desc = v3_abi_descriptor_from_class(abi_class);
    return desc.value_kind != V3_ABI_VALUE_COMPOSITE;
}

static bool v3_abi_class_word_reg(const char *abi_class) {
    return strcmp(abi_class, "i32") == 0 ||
           strcmp(abi_class, "i8") == 0 ||
           strcmp(abi_class, "u8") == 0 ||
           strcmp(abi_class, "i16") == 0 ||
           strcmp(abi_class, "u16") == 0 ||
           strcmp(abi_class, "bool") == 0;
}

static void v3_type_text_from_abi_class(const char *abi_class,
                                        char *type_out,
                                        size_t type_cap) {
    if (strcmp(abi_class, "i8") == 0) {
        snprintf(type_out, type_cap, "%s", "int8");
        return;
    }
    if (strcmp(abi_class, "u8") == 0) {
        snprintf(type_out, type_cap, "%s", "uint8");
        return;
    }
    if (strcmp(abi_class, "i16") == 0) {
        snprintf(type_out, type_cap, "%s", "int16");
        return;
    }
    if (strcmp(abi_class, "u16") == 0) {
        snprintf(type_out, type_cap, "%s", "uint16");
        return;
    }
    if (strcmp(abi_class, "i32") == 0) {
        snprintf(type_out, type_cap, "%s", "int32");
        return;
    }
    if (strcmp(abi_class, "i64") == 0) {
        snprintf(type_out, type_cap, "%s", "int64");
        return;
    }
    if (strcmp(abi_class, "bool") == 0) {
        snprintf(type_out, type_cap, "%s", "bool");
        return;
    }
    if (strcmp(abi_class, "ptr") == 0) {
        snprintf(type_out, type_cap, "%s", "ptr");
        return;
    }
    if (strcmp(abi_class, "void") == 0) {
        snprintf(type_out, type_cap, "%s", "void");
        return;
    }
    type_out[0] = '\0';
}

static void v3_text_append(char *out, size_t cap, const char *text) {
    strncat(out, text, cap - strlen(out) - 1U);
}

static void v3_text_appendf(char *out, size_t cap, const char *fmt, ...) {
    va_list ap;
    size_t used = strlen(out);
    if (used + 1U >= cap) {
        return;
    }
    va_start(ap, fmt);
    vsnprintf(out + used, cap - used, fmt, ap);
    va_end(ap);
}

static bool v3_trim_outer_parens(const char *expr_text, char *out, size_t cap) {
    char expr[8192];
    size_t len;
    int32_t depth = 0;
    bool in_string = false;
    size_t i;
    v3_trim_copy_text(expr_text, expr, sizeof(expr));
    len = strlen(expr);
    if (len < 2U || expr[0] != '(' || expr[len - 1U] != ')') {
        return false;
    }
    for (i = 0; i < len - 1U; ++i) {
        char ch = expr[i];
        if (ch == '"' && !v3_quote_is_escaped(expr, i)) {
            in_string = !in_string;
            continue;
        }
        if (in_string) {
            continue;
        }
        if (ch == '(') {
            depth += 1;
        } else if (ch == ')') {
            depth -= 1;
            if (depth == 0 && i != len - 1U) {
                return false;
            }
        }
    }
    snprintf(out, cap, "%.*s", (int)(len - 2U), expr + 1);
    v3_trim_copy_text(out, out, cap);
    return true;
}

static bool v3_quote_is_escaped(const char *text, size_t index) {
    size_t backslash_count = 0U;
    if (!text) {
        return false;
    }
    while (index > 0U && text[index - 1U] == '\\') {
        backslash_count += 1U;
        index -= 1U;
    }
    return (backslash_count & 1U) != 0U;
}

static int32_t v3_find_top_level_binary_op_last(const char *text, const char *op) {
    bool in_string = false;
    bool in_char = false;
    int32_t depth = 0;
    int32_t bracket_depth = 0;
    size_t len = strlen(text);
    size_t op_len = strlen(op);
    size_t i;
    if (len < op_len) {
        return -1;
    }
    for (i = len; i-- > 0U; ) {
        char ch = text[i];
        if (!in_char && ch == '"' && !v3_quote_is_escaped(text, i)) {
            in_string = !in_string;
            continue;
        }
        if (!in_string && ch == '\'' && !v3_quote_is_escaped(text, i)) {
            in_char = !in_char;
            continue;
        }
        if (in_string || in_char) {
            continue;
        }
        if (ch == ')') {
            depth += 1;
            continue;
        }
        if (ch == '(') {
            depth -= 1;
            continue;
        }
        if (ch == ']') {
            bracket_depth += 1;
            continue;
        }
        if (ch == '[') {
            bracket_depth -= 1;
            continue;
        }
        if (depth == 0 &&
            bracket_depth == 0 &&
            i + op_len <= len &&
            strncmp(text + i, op, op_len) == 0) {
            if (op_len == 1U) {
                char prev = i > 0U ? text[i - 1U] : '\0';
                char next = i + 1U < len ? text[i + 1U] : '\0';
                if ((op[0] == '<' && (prev == '<' || next == '<' || next == '=')) ||
                    (op[0] == '>' && (prev == '>' || next == '>' || next == '=' || prev == '-')) ||
                    (op[0] == '&' && (prev == '&' || next == '&')) ||
                    (op[0] == '|' && (prev == '|' || next == '|')) ||
                    (op[0] == '-' && (next == '>' || prev == '-')) ||
                    (op[0] == '+' && (prev == '+' || next == '+'))) {
                    continue;
                }
            }
            if (op_len == 1U && (op[0] == '+' || op[0] == '-' || op[0] == '*' || op[0] == '&')) {
                size_t prev = i;
                while (prev > 0U && isspace((unsigned char)text[prev - 1U])) {
                    prev -= 1U;
                }
                if (prev == 0U ||
                    text[prev - 1U] == '(' ||
                    text[prev - 1U] == '[' ||
                    text[prev - 1U] == '{' ||
                    text[prev - 1U] == ',' ||
                    text[prev - 1U] == ':' ||
                    text[prev - 1U] == '?' ||
                    text[prev - 1U] == '=' ||
                    text[prev - 1U] == '!' ||
                    text[prev - 1U] == '|' ||
                    text[prev - 1U] == '^' ||
                    text[prev - 1U] == '&' ||
                    text[prev - 1U] == '+' ||
                    text[prev - 1U] == '-' ||
                    text[prev - 1U] == '*' ||
                    text[prev - 1U] == '/' ||
                    text[prev - 1U] == '%' ||
                    text[prev - 1U] == '<' ||
                    text[prev - 1U] == '>') {
                    continue;
                }
            }
            return (int32_t)i;
        }
    }
    return -1;
}

static bool v3_parse_prefix_single_arg_call(const char *expr_text,
                                            char *callee,
                                            size_t callee_cap,
                                            char *arg_text,
                                            size_t arg_cap) {
    char expr[8192];
    bool in_string = false;
    int32_t depth = 0;
    size_t i;
    v3_trim_copy_text(expr_text, expr, sizeof(expr));
    for (i = 0; expr[i] != '\0'; ++i) {
        char ch = expr[i];
        if (ch == '"' && !v3_quote_is_escaped(expr, i)) {
            in_string = !in_string;
            continue;
        }
        if (in_string) {
            continue;
        }
        if (ch == '(') {
            depth += 1;
            continue;
        }
        if (ch == ')') {
            depth -= 1;
            continue;
        }
        if (depth == 0 && isspace((unsigned char)ch)) {
            snprintf(callee, callee_cap, "%.*s", (int)i, expr);
            snprintf(arg_text, arg_cap, "%s", expr + i + 1U);
            v3_trim_copy_text(callee, callee, callee_cap);
            v3_trim_copy_text(arg_text, arg_text, arg_cap);
            if (strchr(callee, '(') != NULL ||
                strchr(callee, ')') != NULL ||
                strchr(callee, '[') != NULL ||
                strchr(callee, ']') != NULL) {
                return false;
            }
            if (arg_text[0] == '<' ||
                arg_text[0] == '>' ||
                arg_text[0] == '+' ||
                arg_text[0] == '-' ||
                arg_text[0] == '*' ||
                arg_text[0] == '/' ||
                arg_text[0] == '%' ||
                arg_text[0] == '&' ||
                arg_text[0] == '|' ||
                arg_text[0] == '^' ||
                arg_text[0] == '=' ||
                arg_text[0] == '!' ||
                arg_text[0] == '?' ||
                arg_text[0] == ':') {
                return false;
            }
            return callee[0] != '\0' && arg_text[0] != '\0';
        }
    }
    {
        int32_t paren_depth = 0;
        int32_t bracket_depth = 0;
        int32_t brace_depth = 0;
        for (i = 0; expr[i] != '\0'; ++i) {
            char ch = expr[i];
            if (ch == '"' && !v3_quote_is_escaped(expr, i)) {
                if (!in_string &&
                    paren_depth == 0 &&
                    bracket_depth == 0 &&
                    brace_depth == 0) {
                    size_t j;
                    snprintf(callee, callee_cap, "%.*s", (int)i, expr);
                    snprintf(arg_text, arg_cap, "%s", expr + i);
                    v3_trim_copy_text(callee, callee, callee_cap);
                    v3_trim_copy_text(arg_text, arg_text, arg_cap);
                    if (callee[0] == '\0' ||
                        arg_text[0] != '"' ||
                        arg_text[strlen(arg_text) - 1U] != '"') {
                        return false;
                    }
                    for (j = 0; callee[j] != '\0'; ++j) {
                        unsigned char cj = (unsigned char)callee[j];
                        if (!(isalnum(cj) || cj == '_' || cj == '.')) {
                            return false;
                        }
                    }
                    return true;
                }
                in_string = !in_string;
                continue;
            }
            if (in_string) {
                continue;
            }
            if (ch == '(') {
                paren_depth += 1;
            } else if (ch == ')') {
                paren_depth -= 1;
            } else if (ch == '[') {
                bracket_depth += 1;
            } else if (ch == ']') {
                bracket_depth -= 1;
            } else if (ch == '{') {
                brace_depth += 1;
            } else if (ch == '}') {
                brace_depth -= 1;
            }
        }
    }
    return false;
}

static bool v3_parse_address_of_expr(const char *expr_text,
                                     char *inner_out,
                                     size_t inner_cap) {
    static const char *OPS[] = {
        "||", "&&", "==", "!=", "<=", ">=", "<<", ">>", "<", ">", "|", "^", "&", "+", "-", "*", "/", "%"
    };
    char expr[4096];
    size_t i;
    v3_trim_copy_text(expr_text, expr, sizeof(expr));
    if (expr[0] != '&' || expr[1] == '\0') {
        return false;
    }
    for (i = 0; i < sizeof(OPS) / sizeof(OPS[0]); ++i) {
        int32_t op_index = v3_find_top_level_binary_op_last(expr, OPS[i]);
        if (op_index > 0) {
            return false;
        }
    }
    v3_trim_copy_text(expr + 1, inner_out, inner_cap);
    return inner_out[0] != '\0';
}

static bool v3_parse_deref_expr(const char *expr_text,
                                char *inner_out,
                                size_t inner_cap) {
    static const char *OPS[] = {
        "||", "&&", "==", "!=", "<=", ">=", "<<", ">>", "<", ">", "|", "^", "&", "+", "-", "*", "/", "%"
    };
    char expr[4096];
    size_t i;
    v3_trim_copy_text(expr_text, expr, sizeof(expr));
    if (expr[0] != '*' || expr[1] == '\0') {
        return false;
    }
    for (i = 0; i < sizeof(OPS) / sizeof(OPS[0]); ++i) {
        int32_t op_index = v3_find_top_level_binary_op_last(expr, OPS[i]);
        if (op_index > 0) {
            return false;
        }
    }
    v3_trim_copy_text(expr + 1, inner_out, inner_cap);
    return inner_out[0] != '\0';
}

static int32_t v3_find_top_level_assignment_eq(const char *text);

static bool v3_parse_binding_meta(const char *statement,
                                  const char *prefix,
                                  char *name_out,
                                  size_t name_cap,
                                  char *type_out,
                                  size_t type_cap,
                                  char *expr_out,
                                  size_t expr_cap) {
    const char *cursor = statement + strlen(prefix);
    int32_t eq_index = v3_find_top_level_assignment_eq(cursor);
    int32_t colon_index;
    char left[CHENG_V3_MAX_TYPE_TEXT];
    if (eq_index < 0) {
        return false;
    }
    snprintf(left, sizeof(left), "%.*s", eq_index, cursor);
    v3_trim_copy_text(left, left, sizeof(left));
    colon_index = v3_find_top_level_char(left, ':');
    if (colon_index > 0) {
        snprintf(type_out, type_cap, "%s", left + colon_index + 1);
        v3_trim_copy_text(type_out, type_out, type_cap);
        left[colon_index] = '\0';
    } else {
        type_out[0] = '\0';
    }
    snprintf(name_out, name_cap, "%s", left);
    v3_trim_copy_text(name_out, name_out, name_cap);
    snprintf(expr_out, expr_cap, "%s", cursor + eq_index + 1);
    v3_trim_copy_text(expr_out, expr_out, expr_cap);
    return name_out[0] != '\0' && expr_out[0] != '\0';
}

static bool v3_parse_binding_decl_meta(const char *statement,
                                       const char *prefix,
                                       char *name_out,
                                       size_t name_cap,
                                       char *type_out,
                                       size_t type_cap) {
    const char *cursor = statement + strlen(prefix);
    char left[CHENG_V3_MAX_TYPE_TEXT];
    int32_t colon_index;
    if (v3_find_top_level_assignment_eq(cursor) >= 0) {
        return false;
    }
    snprintf(left, sizeof(left), "%s", cursor);
    v3_trim_copy_text(left, left, sizeof(left));
    colon_index = v3_find_top_level_char(left, ':');
    if (colon_index <= 0) {
        return false;
    }
    left[colon_index] = '\0';
    snprintf(name_out, name_cap, "%s", left);
    v3_trim_copy_text(name_out, name_out, name_cap);
    snprintf(type_out, type_cap, "%s", left + colon_index + 1);
    v3_trim_copy_text(type_out, type_out, type_cap);
    return name_out[0] != '\0' && type_out[0] != '\0';
}

static int32_t v3_find_top_level_assignment_eq(const char *text) {
    bool in_string = false;
    int32_t paren_depth = 0;
    int32_t bracket_depth = 0;
    int32_t brace_depth = 0;
    size_t i;
    for (i = 0; text && text[i] != '\0'; ++i) {
        char ch = text[i];
        if (ch == '"' && !v3_quote_is_escaped(text, i)) {
            in_string = !in_string;
            continue;
        }
        if (in_string) {
            continue;
        }
        if (ch == '(') {
            paren_depth += 1;
            continue;
        }
        if (ch == ')') {
            paren_depth -= 1;
            continue;
        }
        if (ch == '[') {
            bracket_depth += 1;
            continue;
        }
        if (ch == ']') {
            bracket_depth -= 1;
            continue;
        }
        if (ch == '{') {
            brace_depth += 1;
            continue;
        }
        if (ch == '}') {
            brace_depth -= 1;
            continue;
        }
        if (paren_depth == 0 && bracket_depth == 0 && brace_depth == 0 && ch == '=') {
            char prev = i > 0 ? text[i - 1] : '\0';
            char next = text[i + 1];
            if (prev == '=' || prev == '!' || prev == '<' || prev == '>' || next == '=') {
                continue;
            }
            return (int32_t)i;
        }
    }
    return -1;
}

static bool v3_parse_assignment_meta(const char *statement,
                                     char *lhs_out,
                                     size_t lhs_cap,
                                     char *rhs_out,
                                     size_t rhs_cap) {
    int32_t eq = v3_find_top_level_assignment_eq(statement);
    if (eq < 0) {
        return false;
    }
    snprintf(lhs_out, lhs_cap, "%.*s", eq, statement);
    v3_trim_copy_text(lhs_out, lhs_out, lhs_cap);
    snprintf(rhs_out, rhs_cap, "%s", statement + eq + 1);
    v3_trim_copy_text(rhs_out, rhs_out, rhs_cap);
    return lhs_out[0] != '\0' && rhs_out[0] != '\0';
}

static bool v3_parse_index_assignment_operands(const char *lhs_text,
                                               const char *rhs_text,
                                               char *base_out,
                                               size_t base_cap,
                                               char *index_out,
                                               size_t index_cap,
                                               char *value_out,
                                               size_t value_cap) {
    if (base_out && base_cap > 0U) {
        base_out[0] = '\0';
    }
    if (index_out && index_cap > 0U) {
        index_out[0] = '\0';
    }
    if (value_out && value_cap > 0U) {
        value_out[0] = '\0';
    }
    if (lhs_text == NULL ||
        rhs_text == NULL ||
        base_out == NULL ||
        index_out == NULL ||
        value_out == NULL ||
        !v3_parse_index_access_expr(lhs_text, base_out, base_cap, index_out, index_cap)) {
        return false;
    }
    v3_trim_copy_text(rhs_text, value_out, value_cap);
    return base_out[0] != '\0' &&
           index_out[0] != '\0' &&
           value_out[0] != '\0';
}

static int32_t v3_assignment_string_literal_count(const char *lhs_text,
                                                  const char *rhs_text) {
    char base_expr[4096];
    char index_expr[4096];
    char value_expr[4096];
    if (v3_parse_index_assignment_operands(lhs_text,
                                           rhs_text,
                                           base_expr,
                                           sizeof(base_expr),
                                           index_expr,
                                           sizeof(index_expr),
                                           value_expr,
                                           sizeof(value_expr))) {
        return v3_call_expr_string_literal_count(base_expr) +
               v3_call_expr_string_literal_count(index_expr) +
               v3_call_expr_string_literal_count(value_expr);
    }
    return v3_call_expr_string_literal_count(rhs_text);
}

static bool v3_load_source_lines(const char *source_path,
                                 char ***lines_out,
                                 char **owned_out,
                                 size_t *line_count_out) {
    char *owned;
    char *cursor;
    char *line;
    char **lines;
    size_t line_count = 0U;
    owned = v3_read_file(source_path);
    if (!owned) {
        return false;
    }
    lines = (char **)v3_xmalloc(sizeof(char *) * CHENG_V3_MAX_SOURCE_LINES);
    cursor = owned;
    while ((line = strsep(&cursor, "\n")) != NULL) {
        if (line_count >= CHENG_V3_MAX_SOURCE_LINES) {
            free(lines);
            free(owned);
            return false;
        }
        lines[line_count++] = line;
    }
    *lines_out = lines;
    *owned_out = owned;
    *line_count_out = line_count;
    return true;
}

static bool v3_parse_top_level_var_binding_text(const char *statement,
                                                char *name_out,
                                                size_t name_cap,
                                                char *type_out,
                                                size_t type_cap,
                                                bool *has_init_out,
                                                char *expr_out,
                                                size_t expr_cap) {
    char synthetic[8192];
    snprintf(synthetic, sizeof(synthetic), "var %s", statement);
    if (v3_parse_binding_meta(synthetic,
                              "var ",
                              name_out,
                              name_cap,
                              type_out,
                              type_cap,
                              expr_out,
                              expr_cap)) {
        *has_init_out = true;
        return true;
    }
    expr_out[0] = '\0';
    if (v3_parse_binding_decl_meta(synthetic,
                                   "var ",
                                   name_out,
                                   name_cap,
                                   type_out,
                                   type_cap)) {
        *has_init_out = false;
        return true;
    }
    return false;
}

static void v3_global_var_symbol_name(const V3SystemLinkPlanStub *plan,
                                      const char *module_path,
                                      const char *var_name,
                                      char *out,
                                      size_t cap) {
    char raw[PATH_MAX];
    char sanitized[PATH_MAX];
    if (cap == 0U) {
        return;
    }
    snprintf(raw, sizeof(raw), "%s__%s", module_path, var_name);
    v3_sanitize_path_part(raw, sanitized, sizeof(sanitized));
    v3_copy_target_symbol_name(plan, sanitized, out, cap);
}

static void v3_module_init_symbol_name(const V3SystemLinkPlanStub *plan,
                                       const char *module_path,
                                       char *out,
                                       size_t cap) {
    char raw[PATH_MAX];
    char sanitized[PATH_MAX];
    if (cap == 0U) {
        return;
    }
    snprintf(raw, sizeof(raw), "%s::__v3_module_init", module_path);
    v3_sanitize_path_part(raw, sanitized, sizeof(sanitized));
    v3_copy_target_symbol_name(plan, sanitized, out, cap);
}

static bool v3_is_bare_identifier_text(const char *text) {
    size_t i;
    if (!text || text[0] == '\0') {
        return false;
    }
    if (!(isalpha((unsigned char)text[0]) || text[0] == '_')) {
        return false;
    }
    for (i = 1U; text[i] != '\0'; ++i) {
        unsigned char ch = (unsigned char)text[i];
        if (!(isalnum(ch) || ch == '_')) {
            return false;
        }
    }
    return true;
}

static bool v3_find_top_level_var_decl(const V3SystemLinkPlanStub *plan,
                                       const V3LoweringPlanStub *lowering,
                                       const char *owner_module_path,
                                       const char *var_name,
                                       char *type_out,
                                       size_t type_cap,
                                       char *abi_out,
                                       size_t abi_cap) {
    char source_path[PATH_MAX];
    char **lines = NULL;
    char *owned = NULL;
    size_t line_count = 0U;
    V3ImportAlias aliases[CHENG_V3_MAX_IMPORT_ALIASES];
    size_t alias_count = 0U;
    size_t i;
    bool in_var_block = false;
    if (!plan || !lowering || !owner_module_path || !type_out || !abi_out ||
        !v3_is_bare_identifier_text(var_name)) {
        return false;
    }
    v3_module_path_to_source_path(plan->workspace_root,
                                  plan->package_root,
                                  plan->package_id,
                                  owner_module_path,
                                  source_path,
                                  sizeof(source_path));
    if (source_path[0] == '\0' ||
        !v3_path_exists_nonempty(source_path) ||
        !v3_load_source_lines(source_path, &lines, &owned, &line_count)) {
        return false;
    }
    v3_parse_import_aliases_from_lines(lines,
                                       line_count,
                                       aliases,
                                       &alias_count,
                                       CHENG_V3_MAX_IMPORT_ALIASES);
    for (i = 0; i < line_count; ++i) {
        char line_copy[4096];
        char name[128];
        char raw_type[256];
        char expr[4096];
        char normalized_type[256];
        bool has_init = false;
        int32_t indent;
        char *trimmed;
        snprintf(line_copy, sizeof(line_copy), "%s", lines[i]);
        indent = v3_line_indent(line_copy);
        trimmed = v3_trim_inplace(line_copy);
        if (*trimmed == '\0' || *trimmed == '#') {
            continue;
        }
        if (indent <= 0 &&
            v3_startswith(trimmed, "var ") &&
            v3_parse_top_level_var_binding_text(trimmed + 4,
                                                name,
                                                sizeof(name),
                                                raw_type,
                                                sizeof(raw_type),
                                                &has_init,
                                                expr,
                                                sizeof(expr))) {
            if (strcmp(name, var_name) == 0 &&
                raw_type[0] != '\0' &&
                v3_normalize_type_text(lowering,
                                       owner_module_path,
                                       aliases,
                                       alias_count,
                                       raw_type,
                                       normalized_type,
                                       sizeof(normalized_type))) {
                snprintf(type_out, type_cap, "%s", normalized_type);
                snprintf(abi_out, abi_cap, "%s", v3_type_abi_class(normalized_type));
                free(lines);
                free(owned);
                return true;
            }
            continue;
        }
        if (indent <= 0 && strcmp(trimmed, "var") == 0) {
            in_var_block = true;
            continue;
        }
        if (!in_var_block) {
            continue;
        }
        if (indent <= 0) {
            in_var_block = false;
            continue;
        }
        if (!v3_parse_top_level_var_binding_text(trimmed,
                                                 name,
                                                 sizeof(name),
                                                 raw_type,
                                                 sizeof(raw_type),
                                                 &has_init,
                                                 expr,
                                                 sizeof(expr))) {
            continue;
        }
        if (strcmp(name, var_name) == 0 &&
            raw_type[0] != '\0' &&
            v3_normalize_type_text(lowering,
                                   owner_module_path,
                                   aliases,
                                   alias_count,
                                   raw_type,
                                   normalized_type,
                                   sizeof(normalized_type))) {
            snprintf(type_out, type_cap, "%s", normalized_type);
            snprintf(abi_out, abi_cap, "%s", v3_type_abi_class(normalized_type));
            free(lines);
            free(owned);
            return true;
        }
    }
    free(lines);
    free(owned);
    return false;
}

static bool v3_emit_owner_global_var_address(const V3SystemLinkPlanStub *plan,
                                             const V3LoweringPlanStub *lowering,
                                             const V3LoweredFunctionStub *current_function,
                                             const char *expr_text,
                                             char *type_out,
                                             size_t type_cap,
                                             char *abi_out,
                                             size_t abi_cap,
                                             int addr_reg,
                                             char *out,
                                             size_t cap) {
    char symbol_name[PATH_MAX];
    if (!v3_find_top_level_var_decl(plan,
                                    lowering,
                                    current_function->owner_module_path,
                                    expr_text,
                                    type_out,
                                    type_cap,
                                    abi_out,
                                    abi_cap)) {
        return false;
    }
    v3_global_var_symbol_name(plan,
                              current_function->owner_module_path,
                              expr_text,
                              symbol_name,
                              sizeof(symbol_name));
    return v3_emit_symbol_address_for_target(plan, out, cap, addr_reg, symbol_name);
}

static bool v3_append_module_global_storage(const V3SystemLinkPlanStub *plan,
                                            const V3LoweringPlanStub *lowering,
                                            const char *source_path,
                                            char *out,
                                            size_t cap) {
    char owner_module_path[PATH_MAX];
    char **lines = NULL;
    char *owned = NULL;
    size_t line_count = 0U;
    V3ImportAlias aliases[CHENG_V3_MAX_IMPORT_ALIASES];
    size_t alias_count = 0U;
    size_t i;
    bool in_var_block = false;
    bool emitted_any = false;
    v3_source_path_to_module_path(plan->workspace_root,
                                  plan->package_root,
                                  plan->package_id,
                                  source_path,
                                  owner_module_path,
                                  sizeof(owner_module_path));
    if (!v3_path_exists_nonempty(source_path) ||
        !v3_load_source_lines(source_path, &lines, &owned, &line_count)) {
        return false;
    }
    v3_parse_import_aliases_from_lines(lines,
                                       line_count,
                                       aliases,
                                       &alias_count,
                                       CHENG_V3_MAX_IMPORT_ALIASES);
    for (i = 0; i < line_count; ++i) {
        char line_copy[4096];
        char name[128];
        char raw_type[256];
        char expr[4096];
        char normalized_type[256];
        char symbol_name[PATH_MAX];
        V3TypeLayoutStub layout;
        bool has_init = false;
        int32_t indent;
        int32_t align_log2 = 0;
        char *trimmed;
        snprintf(line_copy, sizeof(line_copy), "%s", lines[i]);
        indent = v3_line_indent(line_copy);
        trimmed = v3_trim_inplace(line_copy);
        if (*trimmed == '\0' || *trimmed == '#') {
            continue;
        }
        if (indent <= 0 && strcmp(trimmed, "var") == 0) {
            in_var_block = true;
            continue;
        }
        if (indent <= 0 && !v3_startswith(trimmed, "var ")) {
            in_var_block = false;
        }
        if (indent <= 0 && v3_startswith(trimmed, "var ")) {
            trimmed += 4;
        } else if (!in_var_block || indent <= 0) {
            continue;
        }
        if (!v3_parse_top_level_var_binding_text(trimmed,
                                                 name,
                                                 sizeof(name),
                                                 raw_type,
                                                 sizeof(raw_type),
                                                 &has_init,
                                                 expr,
                                                 sizeof(expr)) ||
            raw_type[0] == '\0' ||
            !v3_normalize_type_text(lowering,
                                    owner_module_path,
                                    aliases,
                                    alias_count,
                                    raw_type,
                                    normalized_type,
                                    sizeof(normalized_type)) ||
            !v3_compute_type_layout_impl(lowering, normalized_type, &layout, 0U)) {
            continue;
        }
        while ((1 << align_log2) < layout.align && align_log2 < 15) {
            align_log2 += 1;
        }
        v3_global_var_symbol_name(plan, owner_module_path, name, symbol_name, sizeof(symbol_name));
        if (!emitted_any) {
            strncat(out, ".data\n", cap - strlen(out) - 1U);
            emitted_any = true;
        }
        v3_text_appendf(out, cap,
                        ".globl %s\n"
                        ".p2align %d\n"
                        "%s:\n"
                        "  .space %d\n",
                        symbol_name,
                        align_log2,
                        symbol_name,
                        layout.size > 0 ? layout.size : 1);
    }
    free(lines);
    free(owned);
    return true;
}

static bool v3_parse_string_literal_text(const char *expr_text,
                                         char *text_out,
                                         size_t text_cap) {
    char expr[4096];
    size_t len;
    size_t write_len = 0;
    size_t i = 0;
    v3_trim_copy_text(expr_text, expr, sizeof(expr));
    len = strlen(expr);
    if (len < 2U || expr[0] != '"' || expr[len - 1U] != '"') {
        return false;
    }
    if (text_cap == 0U) {
        return false;
    }
    for (i = 1U; i + 1U < len; ++i) {
        unsigned char ch = (unsigned char)expr[i];
        bool escaped = false;
        if (ch == '\\') {
            unsigned char esc = 0;
            if (i + 2U >= len) {
                return false;
            }
            esc = (unsigned char)expr[++i];
            escaped = true;
            if (esc == 'n') {
                ch = '\n';
            } else if (esc == 'r') {
                ch = '\r';
            } else if (esc == 't') {
                ch = '\t';
            } else if (esc == '\\') {
                ch = '\\';
            } else if (esc == '"') {
                ch = '"';
            } else if (esc == '\'') {
                ch = '\'';
            } else if (esc == '0') {
                return false;
            } else {
                ch = esc;
            }
        }
        if (!escaped && ch == '"') {
            return false;
        }
        if (write_len + 1U >= text_cap) {
            return false;
        }
        text_out[write_len++] = (char)ch;
    }
    text_out[write_len] = '\0';
    return true;
}

static bool v3_str_append_text(char *out, size_t cap, const char *text) {
    size_t used;
    size_t need;
    if (!out || cap == 0U || !text) {
        return false;
    }
    used = strlen(out);
    need = strlen(text);
    if (used + need + 1U > cap) {
        return false;
    }
    memcpy(out + used, text, need + 1U);
    return true;
}

static bool v3_str_append_quoted_literal(char *out, size_t cap, const char *text) {
    size_t i;
    if (!v3_str_append_text(out, cap, "\"")) {
        return false;
    }
    for (i = 0; text && text[i] != '\0'; ++i) {
        unsigned char ch = (unsigned char)text[i];
        if (ch == '\n') {
            if (!v3_str_append_text(out, cap, "\\n")) {
                return false;
            }
            continue;
        }
        if (ch == '\r') {
            if (!v3_str_append_text(out, cap, "\\r")) {
                return false;
            }
            continue;
        }
        if (ch == '\t') {
            if (!v3_str_append_text(out, cap, "\\t")) {
                return false;
            }
            continue;
        }
        if (ch == '\\') {
            if (!v3_str_append_text(out, cap, "\\\\")) {
                return false;
            }
            continue;
        }
        if (ch == '"') {
            if (!v3_str_append_text(out, cap, "\\\"")) {
                return false;
            }
            continue;
        }
        {
            size_t used = strlen(out);
            if (used + 2U > cap) {
                return false;
            }
            out[used] = (char)ch;
            out[used + 1U] = '\0';
        }
    }
    return v3_str_append_text(out, cap, "\"");
}

static bool v3_strformat_fmt_append_piece(char *out,
                                          size_t cap,
                                          const char *piece_text,
                                          bool quoted,
                                          bool *has_piece_io) {
    if (!piece_text || piece_text[0] == '\0') {
        return true;
    }
    if (*has_piece_io && !v3_str_append_text(out, cap, " + ")) {
        return false;
    }
    if (quoted) {
        if (!v3_str_append_quoted_literal(out, cap, piece_text)) {
            return false;
        }
    } else {
        if (!v3_str_append_text(out, cap, "(") ||
            !v3_str_append_text(out, cap, piece_text) ||
            !v3_str_append_text(out, cap, ")")) {
            return false;
        }
    }
    *has_piece_io = true;
    return true;
}

static bool v3_expand_strformat_fmt_expr(const char *expr_text,
                                         char *expanded_out,
                                         size_t expanded_cap,
                                         bool *changed_out,
                                         char *error_out,
                                         size_t error_cap) {
    char callee[PATH_MAX];
    char arg_text[4096];
    char literal[4096];
    char expr_buf[8192];
    char expr_piece[4096];
    char literal_piece[4096];
    char (*args)[4096] = NULL;
    size_t arg_count = 0U;
    size_t i = 0U;
    size_t literal_len = 0U;
    bool changed = false;
    bool has_piece = false;
    const char *format_arg = NULL;
    const char *base = NULL;
    if (expanded_out && expanded_cap > 0U) {
        expanded_out[0] = '\0';
    }
    if (changed_out) {
        *changed_out = false;
    }
    if (error_out && error_cap > 0U) {
        error_out[0] = '\0';
    }
    if (!expr_text || !expanded_out || expanded_cap == 0U) {
        return false;
    }
    args = v3_alloc_call_arg_scratch();
    callee[0] = '\0';
    arg_text[0] = '\0';
    if (v3_parse_call_text(expr_text, callee, sizeof(callee), args, &arg_count, CHENG_V3_MAX_CALL_ARGS)) {
        if (arg_count != 1U) {
            free(args);
            return true;
        }
        format_arg = args[0];
    } else if (v3_parse_prefix_single_arg_call(expr_text, callee, sizeof(callee), arg_text, sizeof(arg_text))) {
        format_arg = arg_text;
    } else {
        free(args);
        return true;
    }
    base = strrchr(callee, '.');
    base = base ? base + 1 : callee;
    if (!v3_is_strformat_fmt_name(base)) {
        free(args);
        return true;
    }
    if (!format_arg || !v3_parse_string_literal_text(format_arg, literal, sizeof(literal))) {
        free(args);
        return true;
    }
    for (i = 0U; literal[i] != '\0';) {
        char ch = literal[i];
        if (ch == '{') {
            if (literal[i + 1U] == '{') {
                changed = true;
                if (literal_len + 2U >= sizeof(literal_piece)) {
                    if (error_out && error_cap > 0U) {
                        snprintf(error_out, error_cap, "fmt literal too large");
                    }
                    free(args);
                    return false;
                }
                literal_piece[literal_len++] = '{';
                literal_piece[literal_len] = '\0';
                i += 2U;
                continue;
            }
            literal_piece[literal_len] = '\0';
            if (!v3_strformat_fmt_append_piece(expanded_out,
                                               expanded_cap,
                                               literal_piece,
                                               true,
                                               &has_piece)) {
                if (error_out && error_cap > 0U) {
                    snprintf(error_out, error_cap, "fmt expansion too large");
                }
                free(args);
                return false;
            }
            literal_len = 0U;
            changed = true;
            {
                size_t start = i + 1U;
                int32_t paren_depth = 0;
                int32_t bracket_depth = 0;
                int32_t brace_depth = 0;
                bool in_string = false;
                bool in_char = false;
                i = start;
                while (literal[i] != '\0') {
                    char inner = literal[i];
                    if (inner == '"' && !in_char && !v3_quote_is_escaped(literal, i)) {
                        in_string = !in_string;
                        i += 1U;
                        continue;
                    }
                    if (inner == '\'' && !in_string && !v3_quote_is_escaped(literal, i)) {
                        in_char = !in_char;
                        i += 1U;
                        continue;
                    }
                    if (in_string || in_char) {
                        i += 1U;
                        continue;
                    }
                    if (inner == '(') {
                        paren_depth += 1;
                        i += 1U;
                        continue;
                    }
                    if (inner == ')') {
                        if (paren_depth > 0) {
                            paren_depth -= 1;
                        }
                        i += 1U;
                        continue;
                    }
                    if (inner == '[') {
                        bracket_depth += 1;
                        i += 1U;
                        continue;
                    }
                    if (inner == ']') {
                        if (bracket_depth > 0) {
                            bracket_depth -= 1;
                        }
                        i += 1U;
                        continue;
                    }
                    if (inner == '{') {
                        brace_depth += 1;
                        i += 1U;
                        continue;
                    }
                    if (inner == '}') {
                        if (paren_depth == 0 && bracket_depth == 0 && brace_depth == 0) {
                            break;
                        }
                        if (brace_depth > 0) {
                            brace_depth -= 1;
                        }
                        i += 1U;
                        continue;
                    }
                    i += 1U;
                }
                if (literal[i] != '}') {
                    if (error_out && error_cap > 0U) {
                        snprintf(error_out, error_cap, "fmt placeholder missing '}'");
                    }
                    free(args);
                    return false;
                }
                snprintf(expr_buf, sizeof(expr_buf), "%.*s", (int)(i - start), literal + start);
                v3_trim_copy_text(expr_buf, expr_piece, sizeof(expr_piece));
                if (expr_piece[0] == '\0') {
                    if (error_out && error_cap > 0U) {
                        snprintf(error_out, error_cap, "fmt placeholder empty");
                    }
                    free(args);
                    return false;
                }
                if (!v3_strformat_fmt_append_piece(expanded_out,
                                                   expanded_cap,
                                                   expr_piece,
                                                   false,
                                                   &has_piece)) {
                    if (error_out && error_cap > 0U) {
                        snprintf(error_out, error_cap, "fmt expansion too large");
                    }
                    free(args);
                    return false;
                }
                i += 1U;
                continue;
            }
        }
        if (ch == '}') {
            if (literal[i + 1U] == '}') {
                changed = true;
                if (literal_len + 2U >= sizeof(literal_piece)) {
                    if (error_out && error_cap > 0U) {
                        snprintf(error_out, error_cap, "fmt literal too large");
                    }
                    free(args);
                    return false;
                }
                literal_piece[literal_len++] = '}';
                literal_piece[literal_len] = '\0';
                i += 2U;
                continue;
            }
            if (error_out && error_cap > 0U) {
                snprintf(error_out, error_cap, "fmt literal has unmatched '}'");
            }
            free(args);
            return false;
        }
        if (literal_len + 2U >= sizeof(literal_piece)) {
            if (error_out && error_cap > 0U) {
                snprintf(error_out, error_cap, "fmt literal too large");
            }
            free(args);
            return false;
        }
        literal_piece[literal_len++] = ch;
        literal_piece[literal_len] = '\0';
        i += 1U;
    }
    literal_piece[literal_len] = '\0';
    if (!v3_strformat_fmt_append_piece(expanded_out,
                                       expanded_cap,
                                       literal_piece,
                                       true,
                                       &has_piece)) {
        if (error_out && error_cap > 0U) {
            snprintf(error_out, error_cap, "fmt expansion too large");
        }
        free(args);
        return false;
    }
    if (!changed) {
        free(args);
        return true;
    }
    if (!has_piece && !v3_str_append_text(expanded_out, expanded_cap, "\"\"")) {
        if (error_out && error_cap > 0U) {
            snprintf(error_out, error_cap, "fmt expansion too large");
        }
        free(args);
        return false;
    }
    if (changed_out) {
        *changed_out = true;
    }
    free(args);
    return true;
}

static bool v3_parse_char_literal_text(const char *expr_text, int32_t *value_out) {
    char expr[64];
    size_t len;
    v3_trim_copy_text(expr_text, expr, sizeof(expr));
    len = strlen(expr);
    if (len < 3U || expr[0] != '\'' || expr[len - 1U] != '\'') {
        return false;
    }
    if (len == 3U) {
        if (value_out) {
            *value_out = (unsigned char)expr[1];
        }
        return true;
    }
    if (len == 4U && expr[1] == '\\') {
        int32_t value = 0;
        switch (expr[2]) {
            case 'n': value = '\n'; break;
            case 'r': value = '\r'; break;
            case 't': value = '\t'; break;
            case '0': value = '\0'; break;
            case '\\': value = '\\'; break;
            case '\'': value = '\''; break;
            case '"': value = '"'; break;
            default: return false;
        }
        if (value_out) {
            *value_out = value;
        }
        return true;
    }
    return false;
}

static bool v3_parse_int_literal_text(const char *expr_text, int64_t *value_out) {
    char expr[128];
    char *end = NULL;
    long long parsed;
    v3_trim_copy_text(expr_text, expr, sizeof(expr));
    if (expr[0] == '\0') {
        return false;
    }
    parsed = strtoll(expr, &end, 0);
    if (!end || *v3_trim_inplace(end) != '\0') {
        return false;
    }
    if (value_out) {
        *value_out = (int64_t)parsed;
    }
    return true;
}

static bool v3_parse_member_access_expr(const char *expr_text,
                                        char *base_out,
                                        size_t base_cap,
                                        char *field_out,
                                        size_t field_cap);
static bool v3_parse_index_access_expr(const char *expr_text,
                                       char *base_out,
                                       size_t base_cap,
                                       char *index_out,
                                       size_t index_cap);
static bool v3_parse_named_arg_meta(const char *arg_text,
                                    char *field_out,
                                    size_t field_cap,
                                    char *expr_out,
                                    size_t expr_cap);

static int32_t v3_expr_string_literal_lexical_count(const char *expr_text) {
    const char *cursor = expr_text;
    bool in_string = false;
    bool escape = false;
    int32_t count = 0;
    if (!expr_text) {
        return 0;
    }
    while (*cursor) {
        char ch = *cursor;
        if (!in_string) {
            if (ch == '"') {
                in_string = true;
                escape = false;
                count += 1;
            }
            cursor += 1;
            continue;
        }
        if (escape) {
            escape = false;
            cursor += 1;
            continue;
        }
        if (ch == '\\') {
            escape = true;
            cursor += 1;
            continue;
        }
        if (ch == '"') {
            in_string = false;
        }
        cursor += 1;
    }
    return count;
}

static int32_t v3_call_expr_string_literal_count_slow(const char *expr_text, int32_t depth);

static int32_t v3_call_expr_string_literal_count_inner(const char *expr_text, int32_t depth) {
    if (!expr_text) {
        return 0;
    }
    if (depth >= 24) {
        return v3_expr_string_literal_lexical_count(expr_text);
    }
    if (strlen(expr_text) >= 4096U) {
        return v3_expr_string_literal_lexical_count(expr_text);
    }
    return v3_call_expr_string_literal_count_slow(expr_text, depth);
}

static int32_t v3_call_expr_string_literal_count_slow(const char *expr_text, int32_t depth) {
    static const char *OPS[] = {
        "||", "&&", "==", "!=", "<=", ">=", "<<", ">>", "<", ">", "|", "^", "&", "+", "-", "*", "/", "%"
    };
    char expr[4096];
    char named_field[256];
    char named_expr[4096];
    char base_name[4096];
    char field_name[256];
    char index_expr[4096];
    char callee[PATH_MAX];
    char arg_text[4096] = {0};
    char surface_error[256];
    size_t i;
    size_t expr_len = 0U;
    int32_t best = 0;
    V3ExprSurface surface;
    v3_trim_copy_text(expr_text, expr, sizeof(expr));
    if (expr[0] == '\0') {
        return 0;
    }
    if (!v3_classify_expr_surface(expr, &surface, surface_error, sizeof(surface_error))) {
        return 0;
    }
    snprintf(expr, sizeof(expr), "%s", surface.text);
    expr_len = strlen(expr);
    if (v3_parse_named_arg_meta(expr,
                                named_field,
                                sizeof(named_field),
                                named_expr,
                                sizeof(named_expr))) {
        if (strcmp(named_expr, expr) == 0 || strlen(named_expr) >= expr_len) {
            return 0;
        }
        return v3_call_expr_string_literal_count_inner(named_expr, depth + 1);
    }
    if (expr[0] == '!') {
        return v3_call_expr_string_literal_count_inner(expr + 1, depth + 1);
    }
    if (expr[0] == '-' && expr[1] != '\0' &&
        !v3_parse_int_literal_text(expr, NULL)) {
        return v3_call_expr_string_literal_count_inner(expr + 1, depth + 1);
    }
    if (surface.kind == V3_EXPR_SURFACE_IF || surface.kind == V3_EXPR_SURFACE_TERNARY) {
        int32_t cond_count = v3_call_expr_string_literal_count_inner(surface.cond, depth + 1);
        int32_t true_count = v3_call_expr_string_literal_count_inner(surface.when_true, depth + 1);
        int32_t false_count = v3_call_expr_string_literal_count_inner(surface.when_false, depth + 1);
        best = cond_count > true_count ? cond_count : true_count;
        return best > false_count ? best : false_count;
    }
    if (surface.kind == V3_EXPR_SURFACE_RANGE) {
        int32_t start_count = v3_call_expr_string_literal_count_inner(surface.when_true, depth + 1);
        int32_t end_count = v3_call_expr_string_literal_count_inner(surface.when_false, depth + 1);
        return start_count > end_count ? start_count : end_count;
    }
    for (i = 0; i < sizeof(OPS) / sizeof(OPS[0]); ++i) {
        int32_t op_index = v3_find_top_level_binary_op_last(expr, OPS[i]);
        if (op_index >= 0) {
            char left[4096];
            char right[4096];
            int32_t left_count;
            int32_t right_count;
            snprintf(left, sizeof(left), "%.*s", op_index, expr);
            snprintf(right, sizeof(right), "%s", expr + op_index + strlen(OPS[i]));
            v3_trim_copy_text(left, left, sizeof(left));
            v3_trim_copy_text(right, right, sizeof(right));
            left_count = v3_call_expr_string_literal_count_inner(left, depth + 1);
            right_count = v3_call_expr_string_literal_count_inner(right, depth + 1);
            return left_count > right_count ? left_count : right_count;
        }
    }
    if (v3_parse_member_access_expr(expr, base_name, sizeof(base_name), field_name, sizeof(field_name))) {
        if (strcmp(base_name, expr) == 0 || strlen(base_name) >= expr_len) {
            return 0;
        }
        return v3_call_expr_string_literal_count_inner(base_name, depth + 1);
    }
    if (v3_parse_index_access_expr(expr, base_name, sizeof(base_name), index_expr, sizeof(index_expr))) {
        int32_t base_count = 0;
        int32_t index_count = 0;
        if (strcmp(base_name, expr) != 0 && strlen(base_name) < expr_len) {
            base_count = v3_call_expr_string_literal_count_inner(base_name, depth + 1);
        }
        if (strcmp(index_expr, expr) != 0 && strlen(index_expr) < expr_len) {
            index_count = v3_call_expr_string_literal_count_inner(index_expr, depth + 1);
        }
        return base_count > index_count ? base_count : index_count;
    }
    {
        char (*args)[4096] = v3_alloc_call_arg_scratch();
        size_t arg_count = 0U;
        if (v3_parse_list_literal_text(expr, args, &arg_count, CHENG_V3_MAX_CALL_ARGS)) {
            for (i = 0; i < arg_count; ++i) {
                int32_t arg_count_value = 0;
                if (strcmp(args[i], expr) != 0 && strlen(args[i]) < expr_len) {
                    arg_count_value = v3_call_expr_string_literal_count_inner(args[i], depth + 1);
                }
                if (arg_count_value > best) {
                    best = arg_count_value;
                }
            }
            free(args);
            return best;
        }
        free(args);
    }
    {
        char (*args)[4096] = v3_alloc_call_arg_scratch();
        size_t arg_count = 0U;
        if (v3_parse_call_text(expr, callee, sizeof(callee), args, &arg_count, CHENG_V3_MAX_CALL_ARGS) ||
            v3_parse_prefix_single_arg_call(expr, callee, sizeof(callee), arg_text, sizeof(arg_text))) {
            int32_t top_level_count = 0;
            if (arg_count == 0U && arg_text[0] != '\0') {
                snprintf(args[0], sizeof(args[0]), "%s", arg_text);
                arg_count = 1U;
            }
            for (i = 0; i < arg_count; ++i) {
                char literal[4096];
                int32_t nested_count = 0;
                if (v3_parse_string_literal_text(args[i], literal, sizeof(literal))) {
                    top_level_count += 1;
                }
                if (strcmp(args[i], expr) != 0 && strlen(args[i]) < expr_len) {
                    nested_count = v3_call_expr_string_literal_count_inner(args[i], depth + 1);
                }
                if (nested_count > best) {
                    best = nested_count;
                }
            }
            free(args);
            return top_level_count > best ? top_level_count : best;
        }
        free(args);
    }
    return 0;
}

static int32_t v3_call_expr_string_literal_count(const char *expr_text) {
    return v3_call_expr_string_literal_count_inner(expr_text, 0);
}

static bool v3_is_trivial_prepare_literal_text(const char *expr_text) {
    char trimmed[4096];
    char literal_text[4096];
    int32_t char_value = 0;
    size_t expr_len;
    if (!expr_text) {
        return false;
    }
    v3_trim_copy_text(expr_text, trimmed, sizeof(trimmed));
    expr_len = strlen(trimmed);
    return (expr_len >= 2U && trimmed[0] == '"' && trimmed[expr_len - 1U] == '"') ||
           (expr_len >= 3U && trimmed[0] == '\'' && trimmed[expr_len - 1U] == '\'') ||
           v3_parse_string_literal_text(trimmed, literal_text, sizeof(literal_text)) ||
           v3_parse_char_literal_text(trimmed, &char_value) ||
           v3_parse_int_literal_text(trimmed, NULL) ||
           strcmp(trimmed, "true") == 0 ||
           strcmp(trimmed, "false") == 0 ||
           strcmp(trimmed, "nil") == 0;
}

static void v3_call_arg_temp_name(const V3LoweredFunctionStub *function,
                                  const char *callee,
                                  size_t arg_index,
                                  const char *expr_text,
                                  char *out,
                                  size_t cap) {
    char trimmed_expr[4096];
    char key[4608];
    uint64_t hash;
    v3_trim_copy_text(expr_text, trimmed_expr, sizeof(trimmed_expr));
    snprintf(key,
             sizeof(key),
             "%s|%s|%zu|%s",
             function->symbol_text,
             callee,
             arg_index,
             trimmed_expr);
    hash = v3_fnv1a64(key);
    snprintf(out, cap, "__v3_calltmp_%016llx", (unsigned long long)hash);
}

static void v3_expr_temp_name(const V3LoweredFunctionStub *function,
                              const char *expr_text,
                              char *out,
                              size_t cap) {
    char trimmed_expr[4096];
    char key[4608];
    uint64_t hash;
    v3_trim_copy_text(expr_text, trimmed_expr, sizeof(trimmed_expr));
    snprintf(key,
             sizeof(key),
             "%s|expr|%s",
             function->symbol_text,
             trimmed_expr);
    hash = v3_fnv1a64(key);
    snprintf(out, cap, "__v3_exprtmp_%016llx", (unsigned long long)hash);
}

static int32_t v3_find_local_slot(const V3AsmLocalSlot *locals,
                                  size_t local_count,
                                  const char *name);
static bool v3_add_local_slot_for_type(const V3LoweringPlanStub *lowering,
                                       const char *owner_module_path,
                                       const V3ImportAlias *aliases,
                                       size_t alias_count,
                                       V3AsmLocalSlot *locals,
                                       size_t *local_count,
                                       size_t local_cap,
                                       const char *name,
                                       const char *type_text,
                                       const char *abi_class,
                                       bool as_indirect_param,
                                       int32_t *next_offset_io);
static bool v3_infer_expr_type(const V3SystemLinkPlanStub *plan,
                               const V3LoweringPlanStub *lowering,
                               const V3LoweredFunctionStub *current_function,
                               const V3ImportAlias *aliases,
                               size_t alias_count,
                               const V3AsmLocalSlot *locals,
                               size_t local_count,
                               const char *expr_text,
                               char *type_out,
                               size_t type_cap,
                               char *abi_out,
                               size_t abi_cap);
static bool v3_emit_lvalue_address(const V3SystemLinkPlanStub *plan,
                                   const V3LoweringPlanStub *lowering,
                                   const V3LoweredFunctionStub *current_function,
                                   const V3ImportAlias *aliases,
                                   size_t alias_count,
                                   const V3AsmLocalSlot *locals,
                                   size_t local_count,
                                   const char *expr_text,
                                   char *field_type_out,
                                   size_t field_type_cap,
                                   char *field_abi_out,
                                   size_t field_abi_cap,
                                   int addr_reg,
                                   int32_t call_arg_base,
                                   int32_t string_temp_base,
                                   int32_t string_temp_stride,
                                   int call_depth,
                                   char *out,
                                   size_t cap);

static bool v3_prepare_composite_call_arg_temp(const V3SystemLinkPlanStub *plan,
                                               const V3LoweringPlanStub *lowering,
                                               const V3LoweredFunctionStub *current_function,
                                               const V3ImportAlias *aliases,
                                               size_t alias_count,
                                               V3AsmLocalSlot *locals,
                                               size_t *local_count,
                                               size_t local_cap,
                                               int32_t *next_offset_io,
                                               const char *callee,
                                               size_t arg_index,
                                               const char *arg_expr,
                                               const char *preferred_type,
                                               const char *preferred_abi) {
    char named_field[128];
    char named_expr[4096];
    char temp_name[128];
    char inferred_type[256];
    char inferred_abi[32];
    bool list_nonempty = false;
    char fixed_elem_type[256];
    char list_items[CHENG_V3_MAX_CALL_ARGS][4096];
    size_t list_item_count = 0U;
    int32_t fixed_len = 0;
    const char *value_expr = arg_expr;
    if (v3_parse_named_arg_meta(arg_expr,
                                named_field,
                                sizeof(named_field),
                                named_expr,
                                sizeof(named_expr))) {
        value_expr = named_expr;
    }
    if (v3_find_local_slot(locals, *local_count, value_expr) >= 0) {
        return true;
    }
    v3_call_arg_temp_name(current_function,
                          callee,
                          arg_index,
                          value_expr,
                          temp_name,
                          sizeof(temp_name));
    if (v3_find_local_slot(locals, *local_count, temp_name) >= 0) {
        return true;
    }
    if (preferred_type &&
        preferred_abi &&
        preferred_type[0] != '\0' &&
        preferred_abi[0] != '\0' &&
        !v3_abi_class_scalar_or_ptr(preferred_abi)) {
        if (strlen(preferred_type) > 2U &&
            strcmp(preferred_type + strlen(preferred_type) - 2U, "[]") == 0 &&
            v3_is_list_literal_text(value_expr, &list_nonempty)) {
            snprintf(inferred_type, sizeof(inferred_type), "%s", preferred_type);
            snprintf(inferred_abi, sizeof(inferred_abi), "%s", preferred_abi);
        } else if (v3_parse_fixed_array_type(preferred_type,
                                             fixed_elem_type,
                                             sizeof(fixed_elem_type),
                                             &fixed_len) &&
                   v3_is_list_literal_text(value_expr, &list_nonempty)) {
            if (!v3_parse_list_literal_text(value_expr,
                                            list_items,
                                            &list_item_count,
                                            CHENG_V3_MAX_CALL_ARGS) ||
                (int32_t)list_item_count != fixed_len) {
                fprintf(stderr,
                        "[cheng_v3_seed] prepare composite arg fixed-list mismatch caller=%s callee=%s arg_index=%zu expr=%s preferred_type=%s list_items=%zu fixed_len=%d\n",
                        current_function->symbol_text,
                        callee,
                        arg_index,
                        arg_expr,
                        preferred_type,
                        list_item_count,
                        fixed_len);
                return false;
            }
            snprintf(inferred_type, sizeof(inferred_type), "%s", preferred_type);
            snprintf(inferred_abi, sizeof(inferred_abi), "%s", preferred_abi);
        } else {
            snprintf(inferred_type, sizeof(inferred_type), "%s", preferred_type);
            snprintf(inferred_abi, sizeof(inferred_abi), "%s", preferred_abi);
        }
    } else if (!v3_infer_expr_type(plan,
                                   lowering,
                                   current_function,
                                   aliases,
                                   alias_count,
                                   locals,
                                   *local_count,
                                   value_expr,
                                   inferred_type,
                                   sizeof(inferred_type),
                                   inferred_abi,
                                   sizeof(inferred_abi))) {
        fprintf(stderr,
                "[cheng_v3_seed] prepare composite arg infer failed caller=%s callee=%s arg_index=%zu expr=%s preferred_type=%s preferred_abi=%s\n",
                current_function->symbol_text,
                callee,
                arg_index,
                arg_expr,
                preferred_type ? preferred_type : "",
                preferred_abi ? preferred_abi : "");
        return false;
    }
    if (v3_abi_class_scalar_or_ptr(inferred_abi)) {
        return true;
    }
    if (!v3_add_local_slot_for_type(lowering,
                                    current_function->owner_module_path,
                                    aliases,
                                    alias_count,
                                    locals,
                                    local_count,
                                    local_cap,
                                    temp_name,
                                    inferred_type,
                                    inferred_abi,
                                    false,
                                    next_offset_io)) {
        fprintf(stderr,
                "[cheng_v3_seed] prepare composite arg add-local failed caller=%s callee=%s arg_index=%zu expr=%s inferred_type=%s inferred_abi=%s\n",
                current_function->symbol_text,
                callee,
                arg_index,
                arg_expr,
                inferred_type,
                inferred_abi);
        return false;
    }
    return true;
}

static bool v3_prepare_composite_expr_temp(const V3SystemLinkPlanStub *plan,
                                           const V3LoweringPlanStub *lowering,
                                           const V3LoweredFunctionStub *current_function,
                                           const V3ImportAlias *aliases,
                                           size_t alias_count,
                                           V3AsmLocalSlot *locals,
                                           size_t *local_count,
                                           size_t local_cap,
                                           int32_t *next_offset_io,
                                           const char *expr_text) {
    char temp_name[128];
    char inferred_type[256];
    char inferred_abi[32];
    if (v3_find_local_slot(locals, *local_count, expr_text) >= 0) {
        return true;
    }
    v3_expr_temp_name(current_function, expr_text, temp_name, sizeof(temp_name));
    if (v3_find_local_slot(locals, *local_count, temp_name) >= 0) {
        return true;
    }
    if (!v3_infer_expr_type(plan,
                            lowering,
                            current_function,
                            aliases,
                            alias_count,
                            locals,
                            *local_count,
                            expr_text,
                            inferred_type,
                            sizeof(inferred_type),
                            inferred_abi,
                            sizeof(inferred_abi))) {
        return false;
    }
    if (v3_abi_class_scalar_or_ptr(inferred_abi)) {
        return true;
    }
    return v3_add_local_slot_for_type(lowering,
                                      current_function->owner_module_path,
                                      aliases,
                                      alias_count,
                                      locals,
                                      local_count,
                                      local_cap,
                                      temp_name,
                                      inferred_type,
                                      inferred_abi,
                                      false,
                                      next_offset_io);
}

static bool v3_emit_direct_composite_call_arg_address(const V3SystemLinkPlanStub *plan,
                                                      const V3LoweringPlanStub *lowering,
                                                      const V3LoweredFunctionStub *current_function,
                                                      const V3ImportAlias *aliases,
                                                      size_t alias_count,
                                                      const V3AsmLocalSlot *locals,
                                                      size_t local_count,
                                                      const char *expr_text,
                                                      const char *expected_type,
                                                      const char *expected_abi,
                                                      int addr_reg,
                                                      int32_t call_arg_base,
                                                      int32_t string_temp_base,
                                                      int32_t string_temp_stride,
                                                      int call_depth,
                                                      char *out,
                                                      size_t cap) {
    char actual_type[256];
    char actual_abi[32];
    char normalized_expected[256];
    char normalized_actual[256];
    if (!plan || !lowering || !current_function || !expr_text ||
        !expected_type || !expected_abi ||
        expected_type[0] == '\0' ||
        strcmp(expected_abi, "composite") != 0) {
        return false;
    }
    if (!v3_emit_lvalue_address(plan,
                                lowering,
                                current_function,
                                aliases,
                                alias_count,
                                locals,
                                local_count,
                                expr_text,
                                actual_type,
                                sizeof(actual_type),
                                actual_abi,
                                sizeof(actual_abi),
                                addr_reg,
                                call_arg_base,
                                string_temp_base,
                                string_temp_stride,
                                call_depth,
                                out,
                                cap) ||
        strcmp(actual_abi, "composite") != 0 ||
        !v3_normalize_type_text(lowering,
                                current_function->owner_module_path,
                                aliases,
                                alias_count,
                                expected_type,
                                normalized_expected,
                                sizeof(normalized_expected)) ||
        !v3_normalize_type_text(lowering,
                                current_function->owner_module_path,
                                aliases,
                                alias_count,
                                actual_type,
                                normalized_actual,
                                sizeof(normalized_actual))) {
        return false;
    }
    return strcmp(normalized_expected, normalized_actual) == 0;
}

static void v3_update_max_i32(int32_t *value_io, int32_t candidate) {
    if (candidate > *value_io) {
        *value_io = candidate;
    }
}

static const char *v3_resolved_type_abi_class(const V3LoweringPlanStub *lowering,
                                              const char *owner_module_path,
                                              const V3ImportAlias *aliases,
                                              size_t alias_count,
                                              const char *type_text,
                                              char *normalized_out,
                                              size_t normalized_cap) {
    if (normalized_out && normalized_cap > 0U) {
        normalized_out[0] = '\0';
    }
    if (!type_text || type_text[0] == '\0') {
        return "void";
    }
    if (lowering &&
        owner_module_path &&
        v3_normalize_type_text(lowering,
                               owner_module_path,
                               aliases,
                               alias_count,
                               type_text,
                               normalized_out,
                               normalized_cap)) {
        return v3_type_abi_class(normalized_out);
    }
    return v3_type_abi_class(type_text);
}

static int32_t v3_find_local_slot(const V3AsmLocalSlot *locals,
                                  size_t local_count,
                                  const char *name) {
    size_t i;
    for (i = 0; i < local_count; ++i) {
        if (strcmp(locals[i].name, name) == 0) {
            return (int32_t)i;
        }
    }
    return -1;
}

static bool v3_is_noop_local_reference_statement(const V3AsmLocalSlot *locals,
                                                 size_t local_count,
                                                 const char *statement) {
    char trimmed[256];
    if (!statement || statement[0] == '\0') {
        return false;
    }
    snprintf(trimmed, sizeof(trimmed), "%s", statement);
    v3_trim_inplace(trimmed);
    if (!v3_is_bare_identifier_text(trimmed)) {
        return false;
    }
    return v3_find_local_slot(locals, local_count, trimmed) >= 0;
}

static bool v3_add_local_slot_sized(V3AsmLocalSlot *locals,
                                    size_t *local_count,
                                    size_t local_cap,
                                    const char *name,
                                    const char *type_text,
                                    const char *abi_class,
                                    bool indirect_value,
                                    int32_t slot_size,
                                    int32_t slot_align,
                                    int32_t *next_offset_io) {
    int32_t align = slot_align > 0 ? slot_align : 1;
    int32_t size = slot_size > 0 ? slot_size : 8;
    if (*local_count >= local_cap) {
        return false;
    }
    snprintf(locals[*local_count].name, sizeof(locals[*local_count].name), "%s", name);
    snprintf(locals[*local_count].type_text, sizeof(locals[*local_count].type_text), "%s", type_text);
    snprintf(locals[*local_count].abi_class, sizeof(locals[*local_count].abi_class), "%s", abi_class);
    locals[*local_count].indirect_value = indirect_value;
    locals[*local_count].stack_align = align;
    locals[*local_count].stack_size = size;
    *next_offset_io = v3_align_up_i32(*next_offset_io, align);
    locals[*local_count].stack_offset = *next_offset_io;
    *next_offset_io += v3_align_up_i32(size, 8);
    *local_count += 1U;
    return true;
}

static bool v3_add_local_slot_for_type(const V3LoweringPlanStub *lowering,
                                       const char *owner_module_path,
                                       const V3ImportAlias *aliases,
                                       size_t alias_count,
                                       V3AsmLocalSlot *locals,
                                       size_t *local_count,
                                       size_t local_cap,
                                       const char *name,
                                       const char *type_text,
                                       const char *abi_class,
                                       bool as_indirect_param,
                                       int32_t *next_offset_io) {
    V3TypeLayoutStub layout;
    char normalized_type[256];
    const char *resolved_abi_class;
    (void)abi_class;
    if (!v3_normalize_type_text(lowering,
                                owner_module_path,
                                aliases,
                                alias_count,
                                type_text,
                                normalized_type,
                                sizeof(normalized_type))) {
        fprintf(stderr,
                "[cheng_v3_seed] normalize type failed owner=%s type=%s\n",
                owner_module_path,
                type_text);
        return false;
    }
    resolved_abi_class = v3_type_abi_class(normalized_type);
    if (strcmp(resolved_abi_class, "void") == 0) {
        return false;
    }
    if (v3_abi_class_scalar_or_ptr(resolved_abi_class)) {
        return v3_add_local_slot_sized(locals,
                                       local_count,
                                       local_cap,
                                       name,
                                       normalized_type,
                                       resolved_abi_class,
                                       as_indirect_param,
                                       8,
                                       8,
                                       next_offset_io);
    }
    if (!v3_compute_type_layout_impl(lowering, normalized_type, &layout, 0U)) {
        fprintf(stderr,
                "[cheng_v3_seed] layout failed owner=%s type=%s normalized=%s\n",
                owner_module_path,
                type_text,
                normalized_type);
        return false;
    }
    return v3_add_local_slot_sized(locals,
                                   local_count,
                                   local_cap,
                                   name,
                                   normalized_type,
                                   resolved_abi_class,
                                   as_indirect_param,
                                   as_indirect_param ? 8 : layout.size,
                                   as_indirect_param ? 8 : layout.align,
                                   next_offset_io);
}

static bool v3_emit_sp_offset_address(char *out, size_t cap, int dst_reg, int32_t offset);
static bool v3_emit_slot_address(char *out,
                                 size_t cap,
                                 const V3AsmLocalSlot *slot,
                                 int reg_index);
static bool v3_emit_sp_store_reg(char *out,
                                 size_t cap,
                                 int32_t offset,
                                 const char *abi_class,
                                 int src_reg);
static bool v3_emit_sp_load_reg(char *out,
                                size_t cap,
                                int32_t offset,
                                const char *abi_class,
                                int dst_reg);
static void v3_emit_load_scalar_from_address(char *out,
                                             size_t cap,
                                             int addr_reg,
                                             int32_t offset,
                                             const char *abi_class,
                                             int dst_reg);
static void v3_emit_store_scalar_to_address(char *out,
                                            size_t cap,
                                            int addr_reg,
                                            int32_t offset,
                                            const char *abi_class,
                                            int src_reg);
static bool v3_x64_external_memory_arg_stack_bytes(const V3LoweringPlanStub *lowering,
                                                   const V3LoweredFunctionStub *current_function,
                                                   const V3ImportAlias *aliases,
                                                   size_t alias_count,
                                                   const V3AsmCallTarget *target,
                                                   size_t arg_index,
                                                   int32_t *stack_bytes_out);
static bool v3_emit_call_from_spills(const V3SystemLinkPlanStub *plan,
                                     const V3LoweringPlanStub *lowering,
                                     const V3LoweredFunctionStub *current_function,
                                     const V3ImportAlias *aliases,
                                     size_t alias_count,
                                     const V3AsmCallTarget *target,
                                     bool composite_return,
                                     int32_t call_arg_base,
                                     int call_depth,
                                     char *out,
                                     size_t cap);
static const char *v3_param_pass_abi_class(const V3AsmLocalSlot *slot,
                                           const char *value_abi_class);
static bool v3_emit_local_value_address(char *out,
                                        size_t cap,
                                        const V3AsmLocalSlot *slot,
                                        int reg_index);

static void v3_emit_load_slot(char *out,
                              size_t cap,
                              const V3AsmLocalSlot *slot,
                              int reg_index) {
    if (v3_active_target_is_linux_x86_64()) {
        if (!slot || slot->stack_offset < 0) {
            abort();
        }
        if (slot->indirect_value) {
            v3_text_appendf(out, cap,
                            "  movq %d(%%rsp), %s\n",
                            slot->stack_offset,
                            v3_x64_qreg_name(16));
            v3_emit_load_scalar_from_address(out, cap, 16, 0, slot->abi_class, reg_index);
        } else if (strcmp(slot->abi_class, "ptr") == 0) {
            v3_text_appendf(out, cap,
                            "  movq %d(%%rsp), %s\n",
                            slot->stack_offset,
                            v3_x64_qreg_name(reg_index));
        } else if (strcmp(slot->abi_class, "i8") == 0) {
            v3_text_appendf(out, cap,
                            "  movsbq %d(%%rsp), %s\n",
                            slot->stack_offset,
                            v3_x64_qreg_name(reg_index));
        } else if (strcmp(slot->abi_class, "u8") == 0 ||
                   strcmp(slot->abi_class, "bool") == 0) {
            v3_text_appendf(out, cap,
                            "  movzbl %d(%%rsp), %s\n",
                            slot->stack_offset,
                            v3_x64_dreg_name(reg_index));
        } else if (strcmp(slot->abi_class, "i16") == 0) {
            v3_text_appendf(out, cap,
                            "  movswq %d(%%rsp), %s\n",
                            slot->stack_offset,
                            v3_x64_qreg_name(reg_index));
        } else if (strcmp(slot->abi_class, "u16") == 0) {
            v3_text_appendf(out, cap,
                            "  movzwl %d(%%rsp), %s\n",
                            slot->stack_offset,
                            v3_x64_dreg_name(reg_index));
        } else if (strcmp(slot->abi_class, "i32") == 0) {
            v3_text_appendf(out, cap,
                            "  movslq %d(%%rsp), %s\n",
                            slot->stack_offset,
                            v3_x64_qreg_name(reg_index));
        } else {
            v3_text_appendf(out, cap,
                            "  movq %d(%%rsp), %s\n",
                            slot->stack_offset,
                            v3_x64_qreg_name(reg_index));
        }
        return;
    }
    if (!slot || slot->stack_offset < 0 || !v3_emit_sp_offset_address(out, cap, 16, slot->stack_offset)) {
        abort();
    }
    if (slot->indirect_value) {
        v3_text_appendf(out, cap, "  ldr x16, [x16]\n");
        v3_emit_load_scalar_from_address(out, cap, 16, 0, slot->abi_class, reg_index);
    } else if (strcmp(slot->abi_class, "ptr") == 0) {
        v3_text_appendf(out, cap, "  ldr x%d, [x16]\n", reg_index);
    } else if (strcmp(slot->abi_class, "i8") == 0) {
        v3_text_appendf(out, cap, "  ldrsb x%d, [x16]\n", reg_index);
    } else if (strcmp(slot->abi_class, "u8") == 0) {
        v3_text_appendf(out, cap, "  ldrb w%d, [x16]\n", reg_index);
    } else if (strcmp(slot->abi_class, "i16") == 0) {
        v3_text_appendf(out, cap, "  ldrsh x%d, [x16]\n", reg_index);
    } else if (strcmp(slot->abi_class, "u16") == 0) {
        v3_text_appendf(out, cap, "  ldrh w%d, [x16]\n", reg_index);
    } else if (strcmp(slot->abi_class, "i32") == 0) {
        v3_text_appendf(out, cap, "  ldrsw x%d, [x16]\n", reg_index);
    } else if (strcmp(slot->abi_class, "bool") == 0) {
        v3_text_appendf(out, cap, "  ldrb w%d, [x16]\n", reg_index);
    } else {
        v3_text_appendf(out, cap, "  ldr x%d, [x16]\n", reg_index);
    }
}

static void v3_emit_store_slot(char *out,
                               size_t cap,
                               const V3AsmLocalSlot *slot,
                               int reg_index) {
    if (v3_active_target_is_linux_x86_64()) {
        if (!slot || slot->stack_offset < 0) {
            abort();
        }
        if (slot->indirect_value) {
            v3_text_appendf(out, cap,
                            "  movq %d(%%rsp), %s\n",
                            slot->stack_offset,
                            v3_x64_qreg_name(16));
            v3_emit_store_scalar_to_address(out, cap, 16, 0, slot->abi_class, reg_index);
        } else if (strcmp(slot->abi_class, "ptr") == 0) {
            v3_text_appendf(out, cap,
                            "  movq %s, %d(%%rsp)\n",
                            v3_x64_qreg_name(reg_index),
                            slot->stack_offset);
        } else if (strcmp(slot->abi_class, "i8") == 0 ||
                   strcmp(slot->abi_class, "u8") == 0 ||
                   strcmp(slot->abi_class, "bool") == 0) {
            v3_text_appendf(out, cap,
                            "  movb %s, %d(%%rsp)\n",
                            v3_x64_breg_name(reg_index),
                            slot->stack_offset);
        } else if (strcmp(slot->abi_class, "i16") == 0 ||
                   strcmp(slot->abi_class, "u16") == 0) {
            v3_text_appendf(out, cap,
                            "  movw %s, %d(%%rsp)\n",
                            v3_x64_hreg_name(reg_index),
                            slot->stack_offset);
        } else if (strcmp(slot->abi_class, "i32") == 0) {
            v3_text_appendf(out, cap,
                            "  movl %s, %d(%%rsp)\n",
                            v3_x64_dreg_name(reg_index),
                            slot->stack_offset);
        } else {
            v3_text_appendf(out, cap,
                            "  movq %s, %d(%%rsp)\n",
                            v3_x64_qreg_name(reg_index),
                            slot->stack_offset);
        }
        return;
    }
    if (!slot || slot->stack_offset < 0 || !v3_emit_sp_offset_address(out, cap, 16, slot->stack_offset)) {
        abort();
    }
    if (slot->indirect_value) {
        v3_text_appendf(out, cap, "  ldr x16, [x16]\n");
        v3_emit_store_scalar_to_address(out, cap, 16, 0, slot->abi_class, reg_index);
    } else if (strcmp(slot->abi_class, "ptr") == 0) {
        v3_text_appendf(out, cap, "  str x%d, [x16]\n", reg_index);
    } else if (strcmp(slot->abi_class, "i8") == 0 ||
               strcmp(slot->abi_class, "u8") == 0 ||
               strcmp(slot->abi_class, "bool") == 0) {
        v3_text_appendf(out, cap, "  strb w%d, [x16]\n", reg_index);
    } else if (strcmp(slot->abi_class, "i16") == 0 ||
               strcmp(slot->abi_class, "u16") == 0) {
        v3_text_appendf(out, cap, "  strh w%d, [x16]\n", reg_index);
    } else if (strcmp(slot->abi_class, "i32") == 0) {
        v3_text_appendf(out, cap, "  str w%d, [x16]\n", reg_index);
    } else {
        v3_text_appendf(out, cap, "  str x%d, [x16]\n", reg_index);
    }
}

static bool v3_emit_local_value_address(char *out,
                                        size_t cap,
                                        const V3AsmLocalSlot *slot,
                                        int reg_index) {
    if (slot == NULL) {
        return false;
    }
    if (slot->indirect_value) {
        if (v3_active_target_is_linux_x86_64()) {
            v3_text_appendf(out, cap,
                            "  movq %d(%%rsp), %s\n",
                            slot->stack_offset,
                            v3_x64_qreg_name(reg_index));
            return true;
        }
        if (!v3_emit_sp_offset_address(out, cap, reg_index, slot->stack_offset)) {
            return false;
        }
        v3_text_appendf(out, cap, "  ldr x%d, [x%d]\n", reg_index, reg_index);
        return true;
    }
    return v3_emit_slot_address(out, cap, slot, reg_index);
}

static const char *v3_param_pass_abi_class(const V3AsmLocalSlot *slot,
                                           const char *value_abi_class) {
    if (slot != NULL && slot->indirect_value) {
        return "ptr";
    }
    return value_abi_class;
}

static bool v3_external_call_signature(const V3SystemLinkPlanStub *plan,
                                       const char *name,
                                       V3AsmCallTarget *target) {
    static const struct {
        const char *name;
        const char *symbol;
        const char *ret;
        size_t argc;
        const char *args[8];
    } TABLE[] = {
        {"rawmemAsVoidPtrBridge", "rawmemAsVoid", "ptr", 1U, {"ptr"}},
        {"rawmemAsVoidCStringBridge", "rawmemAsVoid", "ptr", 1U, {"ptr"}},
        {"rawmemCopyRaw", "copyMem", "void", 3U, {"ptr", "ptr", "i32"}},
        {"rawmemSet", "setMem", "void", 3U, {"ptr", "i32", "i32"}},
        {"rawmemPtrAdd", "ptr_add", "ptr", 2U, {"ptr", "i32"}},
        {"system_ptr_add", "ptr_add", "ptr", 2U, {"ptr", "i32"}},
        {"puts", "puts", "i32", 1U, {"ptr"}},
        {"c_puts", "c_puts", "i32", 1U, {"ptr"}},
        {"exit", "exit", "void", 1U, {"i32"}},
        {"c_strlen", "cheng_cstrlen", "i32", 1U, {"ptr"}},
        {"cheng_str_param_to_cstring_compat", "cheng_str_param_to_cstring_compat", "ptr", 1U, {"ptr"}},
        {"alloc", "alloc", "ptr", 1U, {"i32"}},
        {"cheng_seq_set_grow", "cheng_seq_set_grow", "ptr", 3U, {"ptr", "i32", "i32"}},
        {"cheng_seq_get", "cheng_seq_get", "ptr", 4U, {"ptr", "i32", "i32", "i32"}},
        {"cheng_seq_set", "cheng_seq_set", "ptr", 4U, {"ptr", "i32", "i32", "i32"}},
        {"cheng_seq_string_elem_bytes_compat_system", "cheng_seq_string_elem_bytes_compat", "i32", 0U, {0}},
        {"cheng_seq_header_len_get", "cheng_seq_header_len_get", "i32", 1U, {"ptr"}},
        {"cheng_seq_header_len_set", "cheng_seq_header_len_set", "void", 2U, {"ptr", "i32"}},
        {"cheng_seq_header_buffer_get", "cheng_seq_header_buffer_get", "ptr", 1U, {"ptr"}},
        {"cheng_seq_header_buffer_set", "cheng_seq_header_buffer_set", "void", 2U, {"ptr", "ptr"}},
        {"cheng_ptr_seq_get_compat", "cheng_ptr_seq_get_compat", "ptr", 2U, {"composite", "i32"}},
        {"cheng_ptr_seq_set_compat", "cheng_ptr_seq_set_compat", "void", 3U, {"ptr", "i32", "ptr"}},
        {"ptrLoadCompat", "load_ptr", "ptr", 1U, {"ptr"}},
        {"ptrStoreCompat", "store_ptr", "void", 2U, {"ptr", "ptr"}},
        {"int32LoadCompat", "load_int32", "i32", 1U, {"ptr"}},
        {"int32StoreCompat", "store_int32", "void", 2U, {"ptr", "i32"}},
        {"chengStrBridgeLoadCompatInto", "cheng_str_bridge_load_into", "void", 2U, {"ptr", "ptr"}},
        {"chengStrBridgeStoreCompatRaw", "store", "void", 2U, {"ptr", "composite"}},
        {"rawmemWriteI8Compat", "cheng_rawmem_write_i8", "void", 3U, {"ptr", "i32", "i8"}},
        {"memCopyCompat", "cheng_memcpy", "void", 3U, {"ptr", "ptr", "i32"}},
        {"memSetCompat", "cheng_memset", "void", 3U, {"ptr", "i32", "i32"}},
        {"memZeroCompat", "cheng_zero_mem_compat", "void", 2U, {"ptr", "i32"}},
        {"chengRawbytesGetAt", "cheng_rawbytes_get_at", "i32", 2U, {"ptr", "i32"}},
        {"chengRawbytesSetAt", "cheng_rawbytes_set_at", "void", 3U, {"ptr", "i32", "i32"}},
        {"driver_c_i32_to_str", "driver_c_i32_to_str", "ptr", 1U, {"i32"}},
        {"driver_c_i64_to_str", "driver_c_i64_to_str", "ptr", 1U, {"i64"}},
        {"driver_c_u64_to_str", "driver_c_u64_to_str", "ptr", 1U, {"i64"}},
        {"driver_c_i32_to_cstring", "driver_c_i32_to_str", "ptr", 1U, {"i32"}},
        {"driver_c_i64_to_cstring", "driver_c_i64_to_str", "ptr", 1U, {"i64"}},
        {"driver_c_u64_to_cstring", "driver_c_u64_to_str", "ptr", 1U, {"i64"}},
        {"intToStrRaw", "driver_c_i32_to_str", "ptr", 1U, {"i32"}},
        {"int64ToStrRaw", "driver_c_i64_to_str", "ptr", 1U, {"i64"}},
        {"uint64ToStrRaw", "driver_c_u64_to_str", "ptr", 1U, {"i64"}},
        {"cheng_epoch_time_seconds", "cheng_epoch_time_seconds", "i64", 0U, {0}},
        {"chengSystemEntropyFill", "cheng_system_entropy_fill", "i32", 2U, {"ptr", "i32"}}
    };
    const char *lookup = name;
    const char *dot = strrchr(name, '.');
    const char *scope = strstr(name, "::");
    size_t i;
    if (scope && (!dot || scope > dot)) {
        lookup = scope + 2;
    } else if (dot) {
        lookup = dot + 1;
    }
    for (i = 0; i < sizeof(TABLE) / sizeof(TABLE[0]); ++i) {
        size_t j;
        if (strcmp(TABLE[i].name, lookup) != 0) {
            continue;
        }
        memset(target, 0, sizeof(*target));
        target->ffi_handle_consume_index = -1;
        target->external = true;
        snprintf(target->callee_name, sizeof(target->callee_name), "%s", lookup);
        v3_copy_target_symbol_name(plan, TABLE[i].symbol, target->symbol_name, sizeof(target->symbol_name));
        target->c_abi = !v3_symbol_uses_cheng_internal_abi(TABLE[i].symbol);
        v3_type_text_from_abi_class(TABLE[i].ret, target->return_type, sizeof(target->return_type));
        snprintf(target->return_abi_class, sizeof(target->return_abi_class), "%s", TABLE[i].ret);
        target->param_count = TABLE[i].argc;
        for (j = 0; j < TABLE[i].argc; ++j) {
            v3_type_text_from_abi_class(TABLE[i].args[j],
                                        target->param_types[j],
                                        sizeof(target->param_types[j]));
            snprintf(target->param_abi_classes[j],
                     sizeof(target->param_abi_classes[j]),
                     "%s",
                     TABLE[i].args[j]);
        }
        return true;
    }
    return false;
}

static bool v3_parse_result_inner_type(const char *result_type,
                                       char *inner_type_out,
                                       size_t inner_type_cap);
static bool v3_resolve_call_target(const V3SystemLinkPlanStub *plan,
                                   const V3LoweringPlanStub *lowering,
                                   const V3LoweredFunctionStub *current_function,
                                   const V3ImportAlias *aliases,
                                   size_t alias_count,
                                   const char *callee,
                                   V3AsmCallTarget *target);

static bool v3_extract_function_generic_placeholder(const V3LoweringPlanStub *lowering,
                                                    const char *owner_module_path,
                                                    const char *type_text,
                                                    char *name_out,
                                                    size_t name_cap) {
    const char *tail;
    size_t i;
    if (!lowering ||
        !owner_module_path || owner_module_path[0] == '\0' ||
        !type_text || type_text[0] == '\0' ||
        !name_out || name_cap == 0U ||
        !v3_startswith(type_text, owner_module_path)) {
        return false;
    }
    tail = type_text + strlen(owner_module_path);
    if (tail[0] != ':' || tail[1] != ':') {
        return false;
    }
    tail += 2;
    if (!v3_ident_start_char(*tail)) {
        return false;
    }
    for (i = 0U; tail[i] != '\0'; ++i) {
        if (!v3_ident_char(tail[i])) {
            return false;
        }
    }
    if (v3_find_type_def_index_by_symbol(lowering, owner_module_path, tail) >= 0) {
        return false;
    }
    snprintf(name_out, name_cap, "%s", tail);
    return true;
}

static int32_t v3_find_generic_binding_index(const char generic_names[][128],
                                             size_t generic_count,
                                             const char *generic_name) {
    size_t i;
    for (i = 0U; i < generic_count; ++i) {
        if (strcmp(generic_names[i], generic_name) == 0) {
            return (int32_t)i;
        }
    }
    return -1;
}

static bool v3_normalize_type_text_for_function(const V3LoweringPlanStub *lowering,
                                                const V3LoweredFunctionStub *function,
                                                const V3ImportAlias *aliases,
                                                size_t alias_count,
                                                const char *type_text,
                                                char *out,
                                                size_t cap) {
    char bound_type[CHENG_V3_MAX_TYPE_TEXT];
    char generic_name[128];
    char inner[CHENG_V3_MAX_TYPE_TEXT];
    int32_t fixed_len = 0;
    int32_t pointer_depth = 0;
    size_t i;
    if (!lowering || !function) {
        return false;
    }
    if (function->generic_binding_count > 0U) {
        v3_trim_copy_text(type_text, bound_type, sizeof(bound_type));
        if (v3_extract_function_generic_placeholder(lowering,
                                                    function->owner_module_path,
                                                    bound_type,
                                                    generic_name,
                                                    sizeof(generic_name))) {
            int32_t binding_index =
                v3_find_generic_binding_index(function->generic_binding_names,
                                              function->generic_binding_count,
                                              generic_name);
            if (binding_index >= 0) {
                snprintf(bound_type, sizeof(bound_type), "%s", function->generic_binding_types[binding_index]);
            }
        } else if (v3_parse_result_inner_type(bound_type, inner, sizeof(inner))) {
            if (!v3_normalize_type_text_for_function(lowering,
                                                     function,
                                                     aliases,
                                                     alias_count,
                                                     inner,
                                                     inner,
                                                     sizeof(inner))) {
                return false;
            }
            snprintf(bound_type, sizeof(bound_type), "Result[%s]", inner);
        } else if (strlen(bound_type) > 2U &&
                   strcmp(bound_type + strlen(bound_type) - 2U, "[]") == 0) {
            snprintf(inner, sizeof(inner), "%.*s", (int)(strlen(bound_type) - 2U), bound_type);
            if (!v3_normalize_type_text_for_function(lowering,
                                                     function,
                                                     aliases,
                                                     alias_count,
                                                     inner,
                                                     inner,
                                                     sizeof(inner))) {
                return false;
            }
            snprintf(bound_type, sizeof(bound_type), "%s[]", inner);
        } else if (v3_parse_fixed_array_type(bound_type, inner, sizeof(inner), &fixed_len)) {
            if (!v3_normalize_type_text_for_function(lowering,
                                                     function,
                                                     aliases,
                                                     alias_count,
                                                     inner,
                                                     inner,
                                                     sizeof(inner))) {
                return false;
            }
            snprintf(bound_type, sizeof(bound_type), "%s[%d]", inner, fixed_len);
        } else {
            pointer_depth = v3_type_text_pointer_depth(bound_type, inner, sizeof(inner));
            if (pointer_depth > 0) {
                if (!v3_normalize_type_text_for_function(lowering,
                                                         function,
                                                         aliases,
                                                         alias_count,
                                                         inner,
                                                         inner,
                                                         sizeof(inner))) {
                    return false;
                }
                snprintf(bound_type, sizeof(bound_type), "%s", inner);
                for (i = 0U; i < (size_t)pointer_depth; ++i) {
                    size_t used = strlen(bound_type);
                    if (used + 2U >= sizeof(bound_type)) {
                        return false;
                    }
                    snprintf(bound_type + used, sizeof(bound_type) - used, " *");
                }
            }
        }
        return v3_normalize_type_text_with_params(lowering,
                                                  function->owner_module_path,
                                                  aliases,
                                                  alias_count,
                                                  function->generic_binding_names,
                                                  function->generic_binding_types,
                                                  function->generic_binding_count,
                                                  bound_type,
                                                  out,
                                                  cap);
    }
    return v3_normalize_type_text(lowering,
                                  function->owner_module_path,
                                  aliases,
                                  alias_count,
                                  type_text,
                                  out,
                                  cap);
}

static bool v3_type_text_contains_function_placeholder(const V3LoweringPlanStub *lowering,
                                                       const char *owner_module_path,
                                                       const char *type_text,
                                                       unsigned depth) {
    char stripped[256];
    const char *cursor;
    if (!lowering || !owner_module_path || !type_text || depth > 16U) {
        return false;
    }
    v3_strip_var_prefix(type_text, stripped, sizeof(stripped));
    if (stripped[0] == '\0') {
        return false;
    }
    cursor = stripped;
    while ((cursor = strstr(cursor, owner_module_path)) != NULL) {
        const char *tail = cursor + strlen(owner_module_path);
        char ident[128];
        size_t ident_len = 0U;
        if (tail[0] != ':' || tail[1] != ':') {
            cursor += 1;
            continue;
        }
        tail += 2;
        if (!v3_ident_start_char(*tail)) {
            cursor += 1;
            continue;
        }
        while (tail[ident_len] != '\0' &&
               v3_ident_char(tail[ident_len]) &&
               ident_len + 1U < sizeof(ident)) {
            ident[ident_len] = tail[ident_len];
            ident_len += 1U;
        }
        ident[ident_len] = '\0';
        if (ident_len > 0U &&
            v3_find_type_def_index_by_symbol(lowering, owner_module_path, ident) < 0) {
            return true;
        }
        cursor = tail + ident_len;
    }
    return false;
}

static bool v3_function_has_unbound_generics(const V3LoweringPlanStub *lowering,
                                             const V3LoweredFunctionStub *function) {
    size_t i;
    if (!lowering || !function || function->generic_binding_count > 0U) {
        return false;
    }
    if (v3_type_text_contains_function_placeholder(lowering,
                                                   function->owner_module_path,
                                                   function->return_type,
                                                   0U)) {
        return true;
    }
    for (i = 0U; i < function->param_count; ++i) {
        if (v3_type_text_contains_function_placeholder(lowering,
                                                       function->owner_module_path,
                                                       function->param_types[i],
                                                       0U)) {
            return true;
        }
    }
    return false;
}

static bool v3_type_text_contains_obvious_template_ident(const char *type_text) {
    const char *cursor;
    if (!type_text || type_text[0] == '\0') {
        return false;
    }
    cursor = type_text;
    while ((cursor = strstr(cursor, "::")) != NULL) {
        const char *ident = cursor + 2;
        if ((ident[0] == 'T' || ident[0] == 'K' || ident[0] == 'V') &&
            !v3_ident_char(ident[1])) {
            return true;
        }
        cursor = ident;
        if (*cursor != '\0') {
            cursor += 1;
        }
    }
    return false;
}

static bool v3_function_has_obvious_template_text(const V3LoweredFunctionStub *function) {
    size_t i;
    if (!function) {
        return false;
    }
    if (v3_type_text_contains_obvious_template_ident(function->return_type)) {
        return true;
    }
    for (i = 0U; i < function->param_count; ++i) {
        if (v3_type_text_contains_obvious_template_ident(function->param_types[i])) {
            return true;
        }
    }
    return false;
}

static bool v3_resolve_bound_elem_size_value(const V3LoweringPlanStub *lowering,
                                             const V3LoweredFunctionStub *current_function,
                                             const V3ImportAlias *aliases,
                                             size_t alias_count,
                                             const char *callee_text,
                                             int32_t *size_out) {
    char base_name[PATH_MAX];
    char type_arg[CHENG_V3_MAX_TYPE_TEXT];
    char normalized_type[CHENG_V3_MAX_TYPE_TEXT];
    V3TypeLayoutStub layout;
    const char *base;
    if (!lowering || !current_function || !callee_text || !size_out) {
        return false;
    }
    v3_strip_trailing_type_args_text(callee_text, base_name, sizeof(base_name));
    base = strrchr(base_name, '.');
    base = base ? base + 1 : base_name;
    if (strcmp(base, "elemSize") != 0 ||
        !v3_extract_trailing_type_arg_text(callee_text, type_arg, sizeof(type_arg))) {
        return false;
    }
    if (!v3_normalize_type_text_for_function(lowering,
                                             current_function,
                                             aliases,
                                             alias_count,
                                             type_arg,
                                             normalized_type,
                                             sizeof(normalized_type)) ||
        !v3_compute_type_layout_impl(lowering, normalized_type, &layout, 0U)) {
        return false;
    }
    *size_out = layout.size;
    return true;
}

static void v3_specialized_function_name(const V3LoweredFunctionStub *function,
                                         const char generic_names[][128],
                                         const char generic_types[][256],
                                         size_t generic_count,
                                         char *out,
                                         size_t cap) {
    char key[4096];
    uint64_t hash;
    size_t i;
    snprintf(key, sizeof(key), "%s", function->symbol_text);
    for (i = 0U; i < generic_count; ++i) {
        size_t used = strlen(key);
        if (used + 4U >= sizeof(key)) {
            break;
        }
        snprintf(key + used,
                 sizeof(key) - used,
                 "|%s=%s",
                 generic_names[i],
                 generic_types[i]);
    }
    hash = v3_fnv1a64(key);
    snprintf(out, cap, "%s$g%016llx", function->function_name, (unsigned long long)hash);
}

static bool v3_ensure_specialized_function(const V3LoweringPlanStub *lowering,
                                           const V3LoweredFunctionStub *template_function,
                                           const char generic_names[][128],
                                           const char generic_types[][256],
                                           size_t generic_count,
                                           char *out_symbol,
                                           size_t out_symbol_cap) {
    V3LoweringPlanStub *mutable_lowering = (V3LoweringPlanStub *)lowering;
    char specialized_name[128];
    char specialized_symbol[PATH_MAX];
    size_t i;
    if (!mutable_lowering || !template_function || !out_symbol || out_symbol_cap == 0U) {
        return false;
    }
    if (generic_count == 0U) {
        snprintf(out_symbol, out_symbol_cap, "%s", template_function->symbol_text);
        return true;
    }
    v3_specialized_function_name(template_function,
                                 generic_names,
                                 generic_types,
                                 generic_count,
                                 specialized_name,
                                 sizeof(specialized_name));
    snprintf(specialized_symbol,
             sizeof(specialized_symbol),
             "%s::%s",
             template_function->owner_module_path,
             specialized_name);
    for (i = 0U; i < mutable_lowering->function_count; ++i) {
        if (strcmp(mutable_lowering->functions[i].symbol_text, specialized_symbol) == 0) {
            snprintf(out_symbol, out_symbol_cap, "%s", specialized_symbol);
            return true;
        }
    }
    if (mutable_lowering->function_count >= CHENG_V3_MAX_PLAN_FUNCTIONS) {
        return false;
    }
    mutable_lowering->functions[mutable_lowering->function_count] = *template_function;
    snprintf(mutable_lowering->functions[mutable_lowering->function_count].function_name,
             sizeof(mutable_lowering->functions[mutable_lowering->function_count].function_name),
             "%s",
             specialized_name);
    snprintf(mutable_lowering->functions[mutable_lowering->function_count].symbol_text,
             sizeof(mutable_lowering->functions[mutable_lowering->function_count].symbol_text),
             "%s",
             specialized_symbol);
    mutable_lowering->functions[mutable_lowering->function_count].generic_binding_count = generic_count;
    memset(mutable_lowering->functions[mutable_lowering->function_count].generic_binding_names,
           0,
           sizeof(mutable_lowering->functions[mutable_lowering->function_count].generic_binding_names));
    memset(mutable_lowering->functions[mutable_lowering->function_count].generic_binding_types,
           0,
           sizeof(mutable_lowering->functions[mutable_lowering->function_count].generic_binding_types));
    for (i = 0U; i < generic_count && i < CHENG_V3_MAX_TYPE_PARAMS; ++i) {
        snprintf(mutable_lowering->functions[mutable_lowering->function_count].generic_binding_names[i],
                 sizeof(mutable_lowering->functions[mutable_lowering->function_count].generic_binding_names[i]),
                 "%s",
                 generic_names[i]);
        snprintf(mutable_lowering->functions[mutable_lowering->function_count].generic_binding_types[i],
                 sizeof(mutable_lowering->functions[mutable_lowering->function_count].generic_binding_types[i]),
                 "%s",
                 generic_types[i]);
    }
    mutable_lowering->functions[mutable_lowering->function_count].callee_count = 0U;
    mutable_lowering->functions[mutable_lowering->function_count].reachable = true;
    mutable_lowering->function_count += 1U;
    snprintf(out_symbol, out_symbol_cap, "%s", specialized_symbol);
    return true;
}

static bool v3_overload_match_type(const V3LoweringPlanStub *lowering,
                                   const char *candidate_owner_module_path,
                                   const char *param_type,
                                   const char *arg_type,
                                   char generic_names[][128],
                                   char generic_types[][256],
                                   size_t *generic_count_io,
                                   int32_t *score_io,
                                   unsigned depth) {
    char trimmed_param[256];
    char trimmed_arg[256];
    char generic_name[128];
    char param_inner[256];
    char arg_inner[256];
    char normalized_param[256];
    char normalized_arg[256];
    int32_t fixed_param_len = 0;
    int32_t fixed_arg_len = 0;
    int32_t pointer_depth_param = 0;
    int32_t pointer_depth_arg = 0;
    if (!lowering || !candidate_owner_module_path || !param_type || !arg_type ||
        !generic_count_io || !score_io || depth > 16U) {
        return false;
    }
    v3_trim_copy_text(param_type, trimmed_param, sizeof(trimmed_param));
    v3_trim_copy_text(arg_type, trimmed_arg, sizeof(trimmed_arg));
    if (trimmed_param[0] == '\0' || trimmed_arg[0] == '\0') {
        return false;
    }
    if (!v3_normalize_type_text(lowering,
                                candidate_owner_module_path,
                                NULL,
                                0U,
                                trimmed_param,
                                normalized_param,
                                sizeof(normalized_param))) {
        snprintf(normalized_param, sizeof(normalized_param), "%s", trimmed_param);
    }
    if (!v3_normalize_type_text(lowering,
                                candidate_owner_module_path,
                                NULL,
                                0U,
                                trimmed_arg,
                                normalized_arg,
                                sizeof(normalized_arg))) {
        snprintf(normalized_arg, sizeof(normalized_arg), "%s", trimmed_arg);
    }
    if (strcmp(normalized_param, "int") == 0) {
        snprintf(normalized_param, sizeof(normalized_param), "%s", "int32");
    } else if (strcmp(normalized_param, "uint") == 0) {
        snprintf(normalized_param, sizeof(normalized_param), "%s", "uint32");
    }
    if (strcmp(normalized_arg, "int") == 0) {
        snprintf(normalized_arg, sizeof(normalized_arg), "%s", "int32");
    } else if (strcmp(normalized_arg, "uint") == 0) {
        snprintf(normalized_arg, sizeof(normalized_arg), "%s", "uint32");
    }
    if (strcmp(trimmed_arg, "nil") == 0) {
        if (strcmp(normalized_param, "ptr") == 0 ||
            strcmp(normalized_param, "cstring") == 0 ||
            v3_type_text_pointer_depth(normalized_param, param_inner, sizeof(param_inner)) > 0 ||
            v3_is_internal_ref_type(normalized_param)) {
            *score_io += 1;
            return true;
        }
        return false;
    }
    if (strcmp(normalized_param, normalized_arg) == 0 ||
        strcmp(trimmed_param, trimmed_arg) == 0) {
        *score_io += 4;
        return true;
    }
    if (v3_extract_function_generic_placeholder(lowering,
                                                candidate_owner_module_path,
                                                trimmed_param,
                                                generic_name,
                                                sizeof(generic_name))) {
        int32_t binding_index = v3_find_generic_binding_index(generic_names,
                                                              *generic_count_io,
                                                              generic_name);
        if (binding_index >= 0) {
            return v3_overload_match_type(lowering,
                                          candidate_owner_module_path,
                                          generic_types[binding_index],
                                          trimmed_arg,
                                          generic_names,
                                          generic_types,
                                          generic_count_io,
                                          score_io,
                                          depth + 1U);
        }
        if (*generic_count_io >= CHENG_V3_MAX_TYPE_PARAMS) {
            return false;
        }
        snprintf(generic_names[*generic_count_io], 128, "%s", generic_name);
        snprintf(generic_types[*generic_count_io], 256, "%s", trimmed_arg);
        *generic_count_io += 1U;
        *score_io += 1;
        return true;
    }
    if (v3_parse_result_inner_type(normalized_param, param_inner, sizeof(param_inner)) &&
        v3_parse_result_inner_type(normalized_arg, arg_inner, sizeof(arg_inner))) {
        *score_io += 1;
        return v3_overload_match_type(lowering,
                                      candidate_owner_module_path,
                                      param_inner,
                                      arg_inner,
                                      generic_names,
                                      generic_types,
                                      generic_count_io,
                                      score_io,
                                      depth + 1U);
    }
    if (strlen(normalized_param) > 2U &&
        strlen(normalized_arg) > 2U &&
        strcmp(normalized_param + strlen(normalized_param) - 2U, "[]") == 0 &&
        strcmp(normalized_arg + strlen(normalized_arg) - 2U, "[]") == 0) {
        snprintf(param_inner, sizeof(param_inner), "%.*s", (int)(strlen(normalized_param) - 2U), normalized_param);
        snprintf(arg_inner, sizeof(arg_inner), "%.*s", (int)(strlen(normalized_arg) - 2U), normalized_arg);
        *score_io += 1;
        return v3_overload_match_type(lowering,
                                      candidate_owner_module_path,
                                      param_inner,
                                      arg_inner,
                                      generic_names,
                                      generic_types,
                                      generic_count_io,
                                      score_io,
                                      depth + 1U);
    }
    if (v3_parse_fixed_array_type(normalized_param,
                                  param_inner,
                                  sizeof(param_inner),
                                  &fixed_param_len) &&
        v3_parse_fixed_array_type(normalized_arg,
                                  arg_inner,
                                  sizeof(arg_inner),
                                  &fixed_arg_len) &&
        fixed_param_len == fixed_arg_len) {
        *score_io += 1;
        return v3_overload_match_type(lowering,
                                      candidate_owner_module_path,
                                      param_inner,
                                      arg_inner,
                                      generic_names,
                                      generic_types,
                                      generic_count_io,
                                      score_io,
                                      depth + 1U);
    }
    pointer_depth_param = v3_type_text_pointer_depth(normalized_param, param_inner, sizeof(param_inner));
    pointer_depth_arg = v3_type_text_pointer_depth(normalized_arg, arg_inner, sizeof(arg_inner));
    if (pointer_depth_param > 0 && pointer_depth_param == pointer_depth_arg) {
        *score_io += 1;
        return v3_overload_match_type(lowering,
                                      candidate_owner_module_path,
                                      param_inner,
                                      arg_inner,
                                      generic_names,
                                      generic_types,
                                      generic_count_io,
                                      score_io,
                                      depth + 1U);
    }
    return false;
}

static bool v3_expr_can_bind_var_arg(const V3AsmLocalSlot *locals,
                                     size_t local_count,
                                     const char *expr_text) {
    char trimmed[4096];
    char base_expr[4096];
    char index_expr[4096];
    char field_name[256];
    if (!expr_text) {
        return false;
    }
    v3_trim_copy_text(expr_text, trimmed, sizeof(trimmed));
    if (trimmed[0] == '\0') {
        return false;
    }
    if (v3_find_local_slot(locals, local_count, trimmed) >= 0) {
        return true;
    }
    if (v3_parse_member_access_expr(trimmed,
                                    base_expr,
                                    sizeof(base_expr),
                                    field_name,
                                    sizeof(field_name)) ||
        v3_parse_index_access_expr(trimmed,
                                   base_expr,
                                   sizeof(base_expr),
                                   index_expr,
                                   sizeof(index_expr))) {
        return true;
    }
    return false;
}

static bool v3_select_overloaded_function_symbol(const V3SystemLinkPlanStub *plan,
                                                 const V3LoweringPlanStub *lowering,
                                                 const V3LoweredFunctionStub *current_function,
                                                 const V3ImportAlias *aliases,
                                                 size_t alias_count,
                                                 const V3AsmLocalSlot *locals,
                                                 size_t local_count,
                                                 const char *module_path,
                                                 const char *function_name,
                                                 const char args[][4096],
                                                 size_t arg_count,
                                                 int32_t *selected_index_out,
                                                 char *out,
                                                 size_t cap) {
    int32_t best_index = -1;
    int32_t best_score = -1;
    int32_t best_exact_score = -1;
    bool ambiguous = false;
    const char *trace_overload = getenv("CHENG_V3_TRACE_OVERLOAD");
    char best_generic_names[CHENG_V3_MAX_TYPE_PARAMS][128];
    char best_generic_types[CHENG_V3_MAX_TYPE_PARAMS][256];
    size_t best_generic_count = SIZE_MAX;
    size_t i;
    (void)plan;
    if (selected_index_out != NULL) {
        *selected_index_out = -1;
    }
    if (!lowering || !current_function || !module_path || module_path[0] == '\0' ||
        !function_name || function_name[0] == '\0' || !out || cap == 0U) {
        return false;
    }
    out[0] = '\0';
    for (i = 0U; i < lowering->function_count; ++i) {
        const V3LoweredFunctionStub *candidate = &lowering->functions[i];
        char generic_names[CHENG_V3_MAX_TYPE_PARAMS][128];
        char generic_types[CHENG_V3_MAX_TYPE_PARAMS][256];
        size_t generic_count = 0U;
        int32_t score = 0;
        int32_t exact_score = 0;
        size_t arg_index;
        if (strcmp(candidate->owner_module_path, module_path) != 0 ||
            strcmp(candidate->function_name, function_name) != 0 ||
            candidate->param_count != arg_count) {
            continue;
        }
        memset(generic_names, 0, sizeof(generic_names));
        memset(generic_types, 0, sizeof(generic_types));
        for (arg_index = 0U; arg_index < arg_count; ++arg_index) {
            char arg_type[256];
            char arg_abi[32];
            char normalized_arg_type[256];
            if (candidate->param_is_var[arg_index] &&
                !v3_expr_can_bind_var_arg(locals, local_count, args[arg_index])) {
                score = -1;
                break;
            }
            if (strcmp(args[arg_index], "nil") == 0) {
                snprintf(normalized_arg_type, sizeof(normalized_arg_type), "%s", "nil");
            } else if (strcmp(args[arg_index], "[]") == 0) {
                snprintf(normalized_arg_type, sizeof(normalized_arg_type), "%s", "[]");
            } else if (!v3_infer_expr_type(plan,
                                           lowering,
                                           current_function,
                                           aliases,
                                           alias_count,
                                           locals,
                                           local_count,
                                           args[arg_index],
                                           arg_type,
                                           sizeof(arg_type),
                                           arg_abi,
                                           sizeof(arg_abi)) ||
                       !v3_normalize_type_text(lowering,
                                               current_function->owner_module_path,
                                               aliases,
                                               alias_count,
                                               arg_type,
                                               normalized_arg_type,
                                               sizeof(normalized_arg_type))) {
                score = -1;
                break;
            }
            if (strcmp(normalized_arg_type, "[]") == 0) {
                if (!(strlen(candidate->param_types[arg_index]) > 2U &&
                      strcmp(candidate->param_types[arg_index] + strlen(candidate->param_types[arg_index]) - 2U, "[]") == 0)) {
                    score = -1;
                    break;
                }
                score += 1;
                exact_score += 1;
                continue;
            }
            if (!v3_overload_match_type(lowering,
                                        candidate->owner_module_path,
                                        candidate->param_types[arg_index],
                                        normalized_arg_type,
                                        generic_names,
                                        generic_types,
                                        &generic_count,
                                        &score,
                                        0U)) {
                score = -1;
                break;
            }
            if (strcmp(candidate->param_types[arg_index], normalized_arg_type) == 0) {
                exact_score += 2;
            }
        }
            if (score < 0) {
                if (trace_overload &&
                    trace_overload[0] != '\0' &&
                    (strcmp(trace_overload, "1") == 0 ||
                     strstr(current_function->symbol_text, trace_overload) != NULL ||
                     strstr(function_name, trace_overload) != NULL)) {
                    size_t debug_i;
                    fprintf(stderr,
                        "[cheng_v3_seed] overload reject caller=%s callee=%s candidate=%s params=%zu\n",
                        current_function->symbol_text,
                        function_name,
                        candidate->symbol_text,
                        candidate->param_count);
                    for (debug_i = 0U; debug_i < candidate->param_count; ++debug_i) {
                        fprintf(stderr,
                                "[cheng_v3_seed] overload reject candidate_param[%zu]=%s%s\n",
                                debug_i,
                                candidate->param_is_var[debug_i] ? "var " : "",
                                candidate->param_types[debug_i]);
                    }
                    for (debug_i = 0U; debug_i < arg_count; ++debug_i) {
                        char debug_arg_type[256];
                        char debug_arg_abi[32];
                        char debug_normalized_arg[256];
                        debug_arg_type[0] = '\0';
                        debug_arg_abi[0] = '\0';
                        debug_normalized_arg[0] = '\0';
                        if (v3_infer_expr_type(plan,
                                               lowering,
                                               current_function,
                                               aliases,
                                               alias_count,
                                               locals,
                                               local_count,
                                               args[debug_i],
                                               debug_arg_type,
                                               sizeof(debug_arg_type),
                                               debug_arg_abi,
                                               sizeof(debug_arg_abi)) &&
                            v3_normalize_type_text(lowering,
                                                   current_function->owner_module_path,
                                                   aliases,
                                                   alias_count,
                                                   debug_arg_type,
                                                   debug_normalized_arg,
                                                   sizeof(debug_normalized_arg))) {
                            fprintf(stderr,
                                    "[cheng_v3_seed] overload reject arg[%zu]=%s type=%s normalized=%s abi=%s\n",
                                    debug_i,
                                    args[debug_i],
                                    debug_arg_type,
                                    debug_normalized_arg,
                                    debug_arg_abi);
                        } else {
                            fprintf(stderr,
                                    "[cheng_v3_seed] overload reject arg[%zu]=%s type=<infer-failed>\n",
                                    debug_i,
                                    args[debug_i]);
                        }
                    }
                }
                continue;
            }
        if (trace_overload &&
            trace_overload[0] != '\0' &&
            (strcmp(trace_overload, "1") == 0 ||
             strstr(current_function->symbol_text, trace_overload) != NULL ||
             strstr(function_name, trace_overload) != NULL)) {
            char signature[1024];
            size_t sig_used = 0U;
            size_t sig_i;
            signature[0] = '\0';
            sig_used = (size_t)snprintf(signature,
                                        sizeof(signature),
                                        "%s(",
                                        candidate->function_name);
            for (sig_i = 0U; sig_i < candidate->param_count && sig_used + 8U < sizeof(signature); ++sig_i) {
                sig_used += (size_t)snprintf(signature + sig_used,
                                             sizeof(signature) - sig_used,
                                             "%s%s%s",
                                             sig_i == 0U ? "" : ", ",
                                             candidate->param_is_var[sig_i] ? "var " : "",
                                             candidate->param_types[sig_i]);
            }
            if (sig_used + 8U < sizeof(signature)) {
                snprintf(signature + sig_used,
                         sizeof(signature) - sig_used,
                         "): %s",
                         candidate->return_type);
            }
            fprintf(stderr,
                    "[cheng_v3_seed] overload match caller=%s callee=%s candidate=%s signature=%s source=%s:%zu score=%d exact=%d generic_count=%zu\n",
                    current_function->symbol_text,
                    function_name,
                    candidate->symbol_text,
                    signature,
                    candidate->source_path,
                    candidate->signature_line_number,
                    score,
                    exact_score,
                    generic_count);
        }
        if (score > best_score) {
            best_index = (int32_t)i;
            best_score = score;
            best_exact_score = exact_score;
            ambiguous = false;
            best_generic_count = generic_count;
            memcpy(best_generic_names, generic_names, sizeof(best_generic_names));
            memcpy(best_generic_types, generic_types, sizeof(best_generic_types));
        } else if (score == best_score && exact_score > best_exact_score) {
            best_index = (int32_t)i;
            best_exact_score = exact_score;
            ambiguous = false;
            best_generic_count = generic_count;
            memcpy(best_generic_names, generic_names, sizeof(best_generic_names));
            memcpy(best_generic_types, generic_types, sizeof(best_generic_types));
        } else if (score == best_score &&
                   exact_score == best_exact_score &&
                   generic_count < best_generic_count) {
            best_index = (int32_t)i;
            ambiguous = false;
            best_generic_count = generic_count;
            memcpy(best_generic_names, generic_names, sizeof(best_generic_names));
            memcpy(best_generic_types, generic_types, sizeof(best_generic_types));
        } else if (score == best_score && exact_score == best_exact_score) {
            ambiguous = true;
        }
    }
    if (best_index < 0 || ambiguous) {
        if (trace_overload &&
            trace_overload[0] != '\0' &&
            (strcmp(trace_overload, "1") == 0 ||
             strstr(current_function->symbol_text, trace_overload) != NULL ||
             strstr(function_name, trace_overload) != NULL)) {
            fprintf(stderr,
                    "[cheng_v3_seed] overload unresolved caller=%s callee=%s best_index=%d ambiguous=%d\n",
                    current_function->symbol_text,
                    function_name,
                    best_index,
                    ambiguous ? 1 : 0);
        }
        return false;
    }
    if (trace_overload &&
        trace_overload[0] != '\0' &&
        (strcmp(trace_overload, "1") == 0 ||
         strstr(current_function->symbol_text, trace_overload) != NULL ||
         strstr(function_name, trace_overload) != NULL)) {
        fprintf(stderr,
                "[cheng_v3_seed] overload pick caller=%s callee=%s winner=%s\n",
                current_function->symbol_text,
                function_name,
                lowering->functions[best_index].symbol_text);
    }
    if (trace_overload &&
        trace_overload[0] != '\0' &&
        (strcmp(trace_overload, "1") == 0 ||
         strstr(current_function->symbol_text, trace_overload) != NULL ||
         strstr(function_name, trace_overload) != NULL)) {
        fprintf(stderr,
                "[cheng_v3_seed] overload pick caller=%s callee=%s winner=%s score=%d exact=%d\n",
                current_function->symbol_text,
                function_name,
                lowering->functions[best_index].symbol_text,
                best_score,
                best_exact_score);
    }
    if (selected_index_out != NULL) {
        *selected_index_out = best_index;
    }
    return v3_ensure_specialized_function(lowering,
                                          &lowering->functions[best_index],
                                          best_generic_names,
                                          best_generic_types,
                                          best_generic_count,
                                          out,
                                          cap);
}

static bool v3_select_visible_overloaded_function_symbol(const V3SystemLinkPlanStub *plan,
                                                         const V3LoweringPlanStub *lowering,
                                                         const V3LoweredFunctionStub *current_function,
                                                         const V3ImportAlias *aliases,
                                                         size_t alias_count,
                                                         const V3AsmLocalSlot *locals,
                                                         size_t local_count,
                                                         const char *function_name,
                                                         const char args[][4096],
                                                         size_t arg_count,
                                                         int32_t *selected_index_out,
                                                         char *out,
                                                         size_t cap) {
    int32_t best_index = -1;
    int32_t best_score = -1;
    int32_t best_exact_score = -1;
    bool ambiguous = false;
    const char *trace_overload = getenv("CHENG_V3_TRACE_OVERLOAD");
    char best_generic_names[CHENG_V3_MAX_TYPE_PARAMS][128];
    char best_generic_types[CHENG_V3_MAX_TYPE_PARAMS][256];
    size_t best_generic_count = SIZE_MAX;
    size_t i;
    (void)plan;
    if (selected_index_out != NULL) {
        *selected_index_out = -1;
    }
    if (!lowering || !current_function ||
        !function_name || function_name[0] == '\0' || !out || cap == 0U) {
        return false;
    }
    out[0] = '\0';
    for (i = 0U; i < lowering->function_count; ++i) {
        const V3LoweredFunctionStub *candidate = &lowering->functions[i];
        char generic_names[CHENG_V3_MAX_TYPE_PARAMS][128];
        char generic_types[CHENG_V3_MAX_TYPE_PARAMS][256];
        size_t generic_count = 0U;
        int32_t score = 0;
        int32_t exact_score = 0;
        size_t arg_index;
        if (strcmp(candidate->function_name, function_name) != 0 ||
            candidate->param_count != arg_count ||
            !v3_function_name_visible_from_module(current_function->owner_module_path,
                                                  aliases,
                                                  alias_count,
                                                  candidate->owner_module_path,
                                                  function_name)) {
            continue;
        }
        memset(generic_names, 0, sizeof(generic_names));
        memset(generic_types, 0, sizeof(generic_types));
        for (arg_index = 0U; arg_index < arg_count; ++arg_index) {
            char arg_type[256];
            char arg_abi[32];
            char normalized_arg_type[256];
            if (candidate->param_is_var[arg_index] &&
                !v3_expr_can_bind_var_arg(locals, local_count, args[arg_index])) {
                score = -1;
                break;
            }
            if (strcmp(args[arg_index], "nil") == 0) {
                snprintf(normalized_arg_type, sizeof(normalized_arg_type), "%s", "nil");
            } else if (strcmp(args[arg_index], "[]") == 0) {
                snprintf(normalized_arg_type, sizeof(normalized_arg_type), "%s", "[]");
            } else if (!v3_infer_expr_type(plan,
                                           lowering,
                                           current_function,
                                           aliases,
                                           alias_count,
                                           locals,
                                           local_count,
                                           args[arg_index],
                                           arg_type,
                                           sizeof(arg_type),
                                           arg_abi,
                                           sizeof(arg_abi)) ||
                       !v3_normalize_type_text(lowering,
                                               current_function->owner_module_path,
                                               aliases,
                                               alias_count,
                                               arg_type,
                                               normalized_arg_type,
                                               sizeof(normalized_arg_type))) {
                score = -1;
                break;
            }
            if (strcmp(normalized_arg_type, "[]") == 0) {
                if (!(strlen(candidate->param_types[arg_index]) > 2U &&
                      strcmp(candidate->param_types[arg_index] +
                                 strlen(candidate->param_types[arg_index]) - 2U,
                             "[]") == 0)) {
                    score = -1;
                    break;
                }
                score += 1;
                exact_score += 1;
                continue;
            }
            if (!v3_overload_match_type(lowering,
                                        candidate->owner_module_path,
                                        candidate->param_types[arg_index],
                                        normalized_arg_type,
                                        generic_names,
                                        generic_types,
                                        &generic_count,
                                        &score,
                                        0U)) {
                score = -1;
                break;
            }
            if (strcmp(candidate->param_types[arg_index], normalized_arg_type) == 0) {
                exact_score += 2;
            }
        }
        if (score < 0) {
            continue;
        }
        if (trace_overload &&
            trace_overload[0] != '\0' &&
            (strcmp(trace_overload, "1") == 0 ||
             strstr(current_function->symbol_text, trace_overload) != NULL ||
             strstr(function_name, trace_overload) != NULL)) {
            char signature[1024];
            size_t sig_used = 0U;
            size_t sig_i;
            signature[0] = '\0';
            sig_used = (size_t)snprintf(signature,
                                        sizeof(signature),
                                        "%s(",
                                        candidate->function_name);
            for (sig_i = 0U; sig_i < candidate->param_count && sig_used + 8U < sizeof(signature); ++sig_i) {
                sig_used += (size_t)snprintf(signature + sig_used,
                                             sizeof(signature) - sig_used,
                                             "%s%s%s",
                                             sig_i == 0U ? "" : ", ",
                                             candidate->param_is_var[sig_i] ? "var " : "",
                                             candidate->param_types[sig_i]);
            }
            if (sig_used + 8U < sizeof(signature)) {
                snprintf(signature + sig_used,
                         sizeof(signature) - sig_used,
                         "): %s",
                         candidate->return_type);
            }
            fprintf(stderr,
                    "[cheng_v3_seed] overload match caller=%s callee=%s candidate=%s signature=%s source=%s:%zu score=%d exact=%d generic_count=%zu\n",
                    current_function->symbol_text,
                    function_name,
                    candidate->symbol_text,
                    signature,
                    candidate->source_path,
                    candidate->signature_line_number,
                    score,
                    exact_score,
                    generic_count);
        }
        if (score > best_score) {
            best_index = (int32_t)i;
            best_score = score;
            best_exact_score = exact_score;
            ambiguous = false;
            best_generic_count = generic_count;
            memcpy(best_generic_names, generic_names, sizeof(best_generic_names));
            memcpy(best_generic_types, generic_types, sizeof(best_generic_types));
        } else if (score == best_score && exact_score > best_exact_score) {
            best_index = (int32_t)i;
            best_exact_score = exact_score;
            ambiguous = false;
            best_generic_count = generic_count;
            memcpy(best_generic_names, generic_names, sizeof(best_generic_names));
            memcpy(best_generic_types, generic_types, sizeof(best_generic_types));
        } else if (score == best_score &&
                   exact_score == best_exact_score &&
                   generic_count < best_generic_count) {
            best_index = (int32_t)i;
            ambiguous = false;
            best_generic_count = generic_count;
            memcpy(best_generic_names, generic_names, sizeof(best_generic_names));
            memcpy(best_generic_types, generic_types, sizeof(best_generic_types));
        } else if (score == best_score && exact_score == best_exact_score) {
            ambiguous = true;
        }
    }
    if (best_index < 0 || ambiguous) {
        if (trace_overload &&
            trace_overload[0] != '\0' &&
            (strcmp(trace_overload, "1") == 0 ||
             strstr(current_function->symbol_text, trace_overload) != NULL ||
             strstr(function_name, trace_overload) != NULL)) {
            fprintf(stderr,
                    "[cheng_v3_seed] overload unresolved caller=%s callee=%s best_index=%d ambiguous=%d\n",
                    current_function->symbol_text,
                    function_name,
                    best_index,
                    ambiguous ? 1 : 0);
        }
        return false;
    }
    if (trace_overload &&
        trace_overload[0] != '\0' &&
        (strcmp(trace_overload, "1") == 0 ||
         strstr(current_function->symbol_text, trace_overload) != NULL ||
         strstr(function_name, trace_overload) != NULL)) {
        fprintf(stderr,
                "[cheng_v3_seed] overload pick caller=%s callee=%s winner=%s score=%d exact=%d\n",
                current_function->symbol_text,
                function_name,
                lowering->functions[best_index].symbol_text,
                best_score,
                best_exact_score);
    }
    if (selected_index_out != NULL) {
        *selected_index_out = best_index;
    }
    return v3_ensure_specialized_function(lowering,
                                          &lowering->functions[best_index],
                                          best_generic_names,
                                          best_generic_types,
                                          best_generic_count,
                                          out,
                                          cap);
}

static bool v3_resolve_call_target_with_args(const V3SystemLinkPlanStub *plan,
                                             const V3LoweringPlanStub *lowering,
                                             const V3LoweredFunctionStub *current_function,
                                             const V3ImportAlias *aliases,
                                             size_t alias_count,
                                             const V3AsmLocalSlot *locals,
                                             size_t local_count,
                                             const char *callee,
                                             const char args[][4096],
                                             size_t arg_count,
                                             V3AsmCallTarget *target) {
    char trimmed_callee[PATH_MAX];
    char symbol_text[PATH_MAX];
    int32_t function_index;
    int32_t selected_function_index = -1;
    int32_t char_value = 0;
    bool resolved = false;
    if (!plan || !lowering || !current_function || !callee || !target) {
        return false;
    }
    v3_trim_copy_text(callee, trimmed_callee, sizeof(trimmed_callee));
    if (trimmed_callee[0] == '\0') {
        return false;
    }
    if (v3_parse_int_literal_text(trimmed_callee, NULL) ||
        v3_parse_char_literal_text(trimmed_callee, &char_value) ||
        strcmp(trimmed_callee, "true") == 0 ||
        strcmp(trimmed_callee, "false") == 0 ||
        strcmp(trimmed_callee, "nil") == 0 ||
        (trimmed_callee[0] == '"' &&
         trimmed_callee[strlen(trimmed_callee) - 1U] == '"')) {
        return false;
    }
    callee = trimmed_callee;
    symbol_text[0] = '\0';
    if (strchr(callee, '.') == NULL &&
        v3_is_strformat_join_name(callee) &&
        v3_resolve_strformat_join_call_target(plan, callee, target)) {
        if (args != NULL && arg_count != target->param_count) {
            return false;
        }
        return true;
    }
    if (strchr(callee, '.') != NULL) {
        char callee_copy[PATH_MAX];
        char *dot;
        const char *module_path;
        const char *function_name;
        snprintf(callee_copy, sizeof(callee_copy), "%s", callee);
        dot = strchr(callee_copy, '.');
        *dot = '\0';
        module_path = v3_alias_module_path_exported_member(aliases,
                                                           alias_count,
                                                           current_function->owner_module_path,
                                                           callee_copy,
                                                           dot + 1,
                                                           sizeof(callee_copy) - (size_t)((dot + 1) - callee_copy));
        if (module_path) {
            function_name = dot + 1;
            if (v3_resolve_intrinsic_module_call_target(module_path, plan, function_name, target)) {
                return true;
            }
            if (args != NULL &&
                v3_select_overloaded_function_symbol(plan,
                                                     lowering,
                                                     current_function,
                                                     aliases,
                                                     alias_count,
                                                     locals,
                                                     local_count,
                                                     module_path,
                                                     function_name,
                                                     args,
                                                     arg_count,
                                                     &selected_function_index,
                                                     symbol_text,
                                                     sizeof(symbol_text))) {
                resolved = true;
            } else {
                int32_t unique_index =
                    v3_find_unique_lowered_function_index_by_module_and_name(lowering,
                                                                            module_path,
                                                                            function_name);
                if (unique_index >= 0) {
                    snprintf(symbol_text,
                             sizeof(symbol_text),
                             "%s",
                             lowering->functions[unique_index].symbol_text);
                    resolved = true;
                } else if (v3_resolve_importc_call_target_from_module_path(plan,
                                                                           lowering,
                                                                           module_path,
                                                                           function_name,
                                                                           target)) {
                    return true;
                }
            }
        }
    } else if (!v3_is_intrinsic_call_name(callee) &&
               ((args != NULL &&
                 v3_select_visible_overloaded_function_symbol(plan,
                                                              lowering,
                                                              current_function,
                                                              aliases,
                                                              alias_count,
                                                              locals,
                                                              local_count,
                                                              callee,
                                                              args,
                                                              arg_count,
                                                              &selected_function_index,
                                                              symbol_text,
                                                              sizeof(symbol_text))) ||
                v3_resolve_bare_call_symbol(lowering,
                                            current_function->owner_module_path,
                                            aliases,
                                            alias_count,
                                            callee,
                                            symbol_text,
                                            sizeof(symbol_text)))) {
        resolved = true;
    } else if (!v3_is_intrinsic_call_name(callee) &&
               v3_resolve_importc_call_target_from_aliases(plan,
                                                           lowering,
                                                           current_function,
                                                           aliases,
                                                           alias_count,
                                                           callee,
                                                           target)) {
        return true;
    } else if (v3_const_resolve_call_symbol(aliases,
                                            alias_count,
                                            current_function,
                                            callee,
                                            symbol_text,
                                            sizeof(symbol_text))) {
        resolved = true;
    }
    if (resolved) {
        function_index = -1;
        if (symbol_text[0] != '\0') {
            function_index = v3_find_lowered_function_index_by_symbol(lowering, symbol_text);
        }
        if (function_index < 0 && selected_function_index >= 0) {
            function_index = selected_function_index;
        }
        if (function_index >= 0) {
            const V3LoweredFunctionStub *callee_function = &lowering->functions[function_index];
            V3ImportAlias callee_aliases[CHENG_V3_MAX_IMPORT_ALIASES];
            size_t callee_alias_count = 0U;
            size_t i;
            if (!v3_load_import_aliases_from_source_path(callee_function->source_path,
                                                         callee_aliases,
                                                         &callee_alias_count,
                                                         CHENG_V3_MAX_IMPORT_ALIASES)) {
                fprintf(stderr,
                        "[cheng_v3_seed] callee alias load failed: %s\n",
                        callee_function->source_path);
                return false;
            }
            memset(target, 0, sizeof(*target));
            target->ffi_handle_consume_index = -1;
            target->external = false;
            target->lowered_function = callee_function;
            snprintf(target->callee_name, sizeof(target->callee_name), "%s", callee_function->function_name);
            v3_primary_symbol_name(plan, callee_function, target->symbol_name, sizeof(target->symbol_name));
            if (!v3_normalize_type_text_for_function(lowering,
                                                     callee_function,
                                                     callee_aliases,
                                                     callee_alias_count,
                                                     callee_function->return_type,
                                                     target->return_type,
                                                     sizeof(target->return_type))) {
                fprintf(stderr,
                        "[cheng_v3_seed] callee return type normalize failed function=%s type=%s\n",
                        callee_function->symbol_text,
                        callee_function->return_type);
                return false;
            }
            snprintf(target->return_abi_class,
                     sizeof(target->return_abi_class),
                     "%s",
                     v3_type_abi_class(target->return_type));
            target->param_count = callee_function->param_count;
            for (i = 0; i < callee_function->param_count && i < CHENG_V3_MAX_CALL_ARGS; ++i) {
                target->param_is_var[i] = callee_function->param_is_var[i];
                if (!v3_normalize_type_text_for_function(lowering,
                                                         callee_function,
                                                         callee_aliases,
                                                         callee_alias_count,
                                                         callee_function->param_types[i],
                                                         target->param_types[i],
                                                         sizeof(target->param_types[i]))) {
                    fprintf(stderr,
                            "[cheng_v3_seed] callee param type normalize failed function=%s param=%s type=%s\n",
                            callee_function->symbol_text,
                            callee_function->param_names[i],
                            callee_function->param_types[i]);
                    return false;
                }
                snprintf(target->param_abi_classes[i],
                         sizeof(target->param_abi_classes[i]),
                         "%s",
                         v3_type_abi_class(target->param_types[i]));
            }
            return true;
        }
        {
            char symbol_copy[PATH_MAX];
            char *scope;
            char module_path[PATH_MAX];
            char source_path[PATH_MAX];
            snprintf(symbol_copy, sizeof(symbol_copy), "%s", symbol_text);
            scope = strrchr(symbol_copy, ':');
            if (scope && scope != symbol_copy && scope[-1] == ':') {
                scope[-1] = '\0';
                snprintf(module_path, sizeof(module_path), "%s", symbol_copy);
                v3_module_path_to_source_path(plan->workspace_root,
                                              plan->package_root,
                                              plan->package_id,
                                              module_path,
                                              source_path,
                                              sizeof(source_path));
                if (source_path[0] != '\0' &&
                    v3_resolve_importc_call_target_from_source_path(plan,
                                                                    lowering,
                                                                    source_path,
                                                                    scope + 1,
                                                                    target)) {
                    return true;
                }
            }
        }
    }
    if (v3_resolve_importc_call_target_from_source_path(plan,
                                                        lowering,
                                                        current_function->source_path,
                                                        callee,
                                                        target)) {
        return true;
    }
    return v3_external_call_signature(plan, callee, target);
}

static bool v3_resolve_return_call_noarg_i32_target(const V3SystemLinkPlanStub *plan,
                                                    const V3LoweringPlanStub *lowering,
                                                    const V3LoweredFunctionStub *current_function,
                                                    V3AsmCallTarget *target) {
    V3ImportAlias aliases[CHENG_V3_MAX_IMPORT_ALIASES];
    size_t alias_count = 0U;
    if (current_function == NULL || target == NULL ||
        current_function->body_call_target[0] == '\0') {
        return false;
    }
    if (!v3_load_import_aliases_from_source_path(current_function->source_path,
                                                 aliases,
                                                 &alias_count,
                                                 CHENG_V3_MAX_IMPORT_ALIASES)) {
        fprintf(stderr,
                "[cheng_v3_seed] owner alias load failed: %s\n",
                current_function->source_path);
        return false;
    }
    if (!v3_resolve_call_target(plan,
                                lowering,
                                current_function,
                                aliases,
                                alias_count,
                                current_function->body_call_target,
                                target)) {
        return false;
    }
    return target->param_count == 0U;
}

static bool v3_resolve_return_call_arg0_i32_target(const V3SystemLinkPlanStub *plan,
                                                   const V3LoweringPlanStub *lowering,
                                                   const V3LoweredFunctionStub *current_function,
                                                   V3AsmCallTarget *target) {
    V3ImportAlias aliases[CHENG_V3_MAX_IMPORT_ALIASES];
    size_t alias_count = 0U;
    if (current_function == NULL || target == NULL ||
        current_function->body_call_target[0] == '\0' ||
        current_function->param_count != 1U ||
        strcmp(current_function->param_abi_classes[0], "i32") != 0 ||
        strcmp(current_function->body_call_arg0_name, current_function->param_names[0]) != 0) {
        return false;
    }
    if (!v3_load_import_aliases_from_source_path(current_function->source_path,
                                                 aliases,
                                                 &alias_count,
                                                 CHENG_V3_MAX_IMPORT_ALIASES)) {
        fprintf(stderr,
                "[cheng_v3_seed] owner alias load failed: %s\n",
                current_function->source_path);
        return false;
    }
    if (!v3_resolve_call_target(plan,
                                lowering,
                                current_function,
                                aliases,
                                alias_count,
                                current_function->body_call_target,
                                target)) {
        char wasm_symbol[PATH_MAX];
        wasm_symbol[0] = '\0';
        if (!v3_wasm_resolve_external_arg0_i32_from_source_path(plan,
                                                                current_function->source_path,
                                                                current_function->body_call_target,
                                                                wasm_symbol,
                                                                sizeof(wasm_symbol))) {
            return false;
        }
        memset(target, 0, sizeof(*target));
        target->ffi_handle_consume_index = -1;
        target->external = true;
        snprintf(target->symbol_name, sizeof(target->symbol_name), "%s", wasm_symbol);
        snprintf(target->return_type, sizeof(target->return_type), "%s", "int32");
        snprintf(target->return_abi_class, sizeof(target->return_abi_class), "%s", "i32");
        target->param_count = 1U;
        snprintf(target->param_types[0], sizeof(target->param_types[0]), "%s", "int32");
        snprintf(target->param_abi_classes[0], sizeof(target->param_abi_classes[0]), "%s", "i32");
    }
    return target->param_count == 1U &&
           strcmp(target->return_abi_class, "i32") == 0 &&
           strcmp(target->param_abi_classes[0], "i32") == 0;
}

static bool v3_wasm_resolve_external_i32_params_from_source_path(const V3SystemLinkPlanStub *plan,
                                                                 const char *source_path,
                                                                 const char *callee,
                                                                 size_t expected_argc,
                                                                 char *symbol_name_out,
                                                                 size_t symbol_name_cap);

static bool v3_resolve_return_call_params_i32_target(const V3SystemLinkPlanStub *plan,
                                                     const V3LoweringPlanStub *lowering,
                                                     const V3LoweredFunctionStub *current_function,
                                                     V3AsmCallTarget *target) {
    V3ImportAlias aliases[CHENG_V3_MAX_IMPORT_ALIASES];
    size_t alias_count = 0U;
    size_t arg_index;
    if (current_function == NULL || target == NULL ||
        current_function->body_call_target[0] == '\0' ||
        current_function->body_call_arg_count < 2U ||
        current_function->body_call_arg_count > CHENG_V3_MAX_CALL_ARGS ||
        current_function->param_count != current_function->body_call_arg_count ||
        strcmp(current_function->return_abi_class, "i32") != 0) {
        return false;
    }
    for (arg_index = 0U; arg_index < current_function->body_call_arg_count; ++arg_index) {
        if (strcmp(current_function->param_abi_classes[arg_index], "i32") != 0 ||
            strcmp(current_function->body_call_arg_names[arg_index], current_function->param_names[arg_index]) != 0) {
            return false;
        }
    }
    if (!v3_load_import_aliases_from_source_path(current_function->source_path,
                                                 aliases,
                                                 &alias_count,
                                                 CHENG_V3_MAX_IMPORT_ALIASES)) {
        fprintf(stderr,
                "[cheng_v3_seed] owner alias load failed: %s\n",
                current_function->source_path);
        return false;
    }
    if (!v3_resolve_call_target(plan,
                                lowering,
                                current_function,
                                aliases,
                                alias_count,
                                current_function->body_call_target,
                                target)) {
        char wasm_symbol[PATH_MAX];
        wasm_symbol[0] = '\0';
        if (!v3_wasm_resolve_external_i32_params_from_source_path(plan,
                                                                  current_function->source_path,
                                                                  current_function->body_call_target,
                                                                  current_function->body_call_arg_count,
                                                                  wasm_symbol,
                                                                  sizeof(wasm_symbol))) {
            return false;
        }
        memset(target, 0, sizeof(*target));
        target->ffi_handle_consume_index = -1;
        target->external = true;
        snprintf(target->symbol_name, sizeof(target->symbol_name), "%s", wasm_symbol);
        target->c_abi = !v3_symbol_uses_cheng_internal_abi(wasm_symbol);
        snprintf(target->return_type, sizeof(target->return_type), "%s", "int32");
        snprintf(target->return_abi_class, sizeof(target->return_abi_class), "%s", "i32");
        target->param_count = current_function->body_call_arg_count;
        for (arg_index = 0U; arg_index < target->param_count; ++arg_index) {
            snprintf(target->param_types[arg_index], sizeof(target->param_types[arg_index]), "%s", "int32");
            snprintf(target->param_abi_classes[arg_index], sizeof(target->param_abi_classes[arg_index]), "%s", "i32");
        }
    }
    if (strcmp(target->return_abi_class, "i32") != 0 ||
        target->param_count != current_function->body_call_arg_count) {
        return false;
    }
    for (arg_index = 0U; arg_index < target->param_count; ++arg_index) {
        if (strcmp(target->param_abi_classes[arg_index], "i32") != 0) {
            return false;
        }
    }
    return true;
}

static void v3_emit_call_arg_spill_store(char *out,
                                         size_t cap,
                                         int32_t call_arg_base,
                                         int call_depth,
                                         size_t arg_index,
                                         const char *abi_class,
                                         int src_reg_index) {
    int32_t offset = call_arg_base + call_depth * CHENG_V3_CALL_ARG_SPILL_STRIDE + (int32_t)arg_index * 8;
    if (!v3_emit_sp_store_reg(out, cap, offset, abi_class, src_reg_index)) {
        abort();
    }
}

static void v3_emit_call_arg_spill_load(char *out,
                                        size_t cap,
                                        int32_t call_arg_base,
                                        int call_depth,
                                        size_t arg_index,
                                        int dest_reg_index,
                                        const char *abi_class) {
    int32_t offset = call_arg_base + call_depth * CHENG_V3_CALL_ARG_SPILL_STRIDE + (int32_t)arg_index * 8;
    if (!v3_emit_sp_load_reg(out, cap, offset, abi_class, dest_reg_index)) {
        abort();
    }
}

static void v3_emit_call_arg_spill_load_adjusted(char *out,
                                                 size_t cap,
                                                 int32_t call_arg_base,
                                                 int call_depth,
                                                 size_t arg_index,
                                                 int dest_reg_index,
                                                 const char *abi_class,
                                                 int32_t extra_sp_offset) {
    int32_t offset =
        call_arg_base + call_depth * CHENG_V3_CALL_ARG_SPILL_STRIDE + (int32_t)arg_index * 8 + extra_sp_offset;
    if (!v3_emit_sp_load_reg(out, cap, offset, abi_class, dest_reg_index)) {
        abort();
    }
}

static const char *v3_call_arg_load_abi(const V3AsmCallTarget *target, size_t arg_index) {
    if (target == NULL || arg_index >= CHENG_V3_MAX_CALL_ARGS) {
        return "ptr";
    }
    if (target->ffi_handle_enabled && target->ffi_handle_param_is_handle[arg_index]) {
        return "ptr";
    }
    if (target->param_is_var[arg_index]) {
        return "ptr";
    }
    return v3_abi_class_scalar_or_ptr(target->param_abi_classes[arg_index]) ?
           target->param_abi_classes[arg_index] : "ptr";
}

static void v3_emit_unary_helper_call(const V3SystemLinkPlanStub *plan,
                                      char *out,
                                      size_t cap,
                                      const char *symbol_name,
                                      const char *arg_abi_class,
                                      int src_reg,
                                      const char *ret_abi_class,
                                      int dst_reg) {
    int arg_reg = v3_active_target_is_linux_x86_64() ? 14 : 0;
    v3_emit_move_reg_abi(out, cap, arg_reg, src_reg, arg_abi_class);
    v3_emit_call_named_symbol(plan, out, cap, symbol_name);
    v3_emit_move_reg_abi(out, cap, dst_reg, 0, ret_abi_class);
}

static void v3_emit_call_dest_spill_store(char *out,
                                          size_t cap,
                                          int32_t call_arg_base,
                                          int call_depth,
                                          int src_reg_index) {
    int32_t offset =
        call_arg_base + call_depth * CHENG_V3_CALL_ARG_SPILL_STRIDE + (int32_t)CHENG_V3_MAX_CALL_ARGS * 8;
    if (!v3_emit_sp_store_reg(out, cap, offset, "ptr", src_reg_index)) {
        abort();
    }
}

static void v3_emit_call_dest_spill_store_abi(char *out,
                                              size_t cap,
                                              int32_t call_arg_base,
                                              int call_depth,
                                              const char *abi_class,
                                              int src_reg_index) {
    int32_t offset =
        call_arg_base + call_depth * CHENG_V3_CALL_ARG_SPILL_STRIDE + (int32_t)CHENG_V3_MAX_CALL_ARGS * 8;
    if (!v3_emit_sp_store_reg(out, cap, offset, abi_class, src_reg_index)) {
        abort();
    }
}

static void v3_emit_call_dest_spill_load(char *out,
                                         size_t cap,
                                         int32_t call_arg_base,
                                         int call_depth,
                                         int dest_reg_index) {
    int32_t offset =
        call_arg_base + call_depth * CHENG_V3_CALL_ARG_SPILL_STRIDE + (int32_t)CHENG_V3_MAX_CALL_ARGS * 8;
    if (!v3_emit_sp_load_reg(out, cap, offset, "ptr", dest_reg_index)) {
        abort();
    }
}

static void v3_emit_call_dest_spill_load_adjusted(char *out,
                                                  size_t cap,
                                                  int32_t call_arg_base,
                                                  int call_depth,
                                                  int dest_reg_index,
                                                  int32_t extra_sp_offset) {
    int32_t offset =
        call_arg_base + call_depth * CHENG_V3_CALL_ARG_SPILL_STRIDE +
        (int32_t)CHENG_V3_MAX_CALL_ARGS * 8 + extra_sp_offset;
    if (!v3_emit_sp_load_reg(out, cap, offset, "ptr", dest_reg_index)) {
        abort();
    }
}

static void v3_emit_call_dest_spill_load_abi(char *out,
                                             size_t cap,
                                             int32_t call_arg_base,
                                             int call_depth,
                                             int dest_reg_index,
                                             const char *abi_class) {
    int32_t offset =
        call_arg_base + call_depth * CHENG_V3_CALL_ARG_SPILL_STRIDE + (int32_t)CHENG_V3_MAX_CALL_ARGS * 8;
    if (!v3_emit_sp_load_reg(out, cap, offset, abi_class, dest_reg_index)) {
        abort();
    }
}

static void v3_emit_binary_spill_store(char *out,
                                       size_t cap,
                                       int32_t call_arg_base,
                                       int call_depth,
                                       const char *abi_class,
                                       int src_reg_index) {
    int32_t offset =
        call_arg_base + call_depth * CHENG_V3_CALL_ARG_SPILL_STRIDE + (int32_t)(CHENG_V3_MAX_CALL_ARGS + 1) * 8;
    if (!v3_emit_sp_store_reg(out, cap, offset, abi_class, src_reg_index)) {
        abort();
    }
}

static void v3_emit_binary_spill_load(char *out,
                                      size_t cap,
                                      int32_t call_arg_base,
                                      int call_depth,
                                      int dest_reg_index,
                                      const char *abi_class) {
    int32_t offset =
        call_arg_base + call_depth * CHENG_V3_CALL_ARG_SPILL_STRIDE + (int32_t)(CHENG_V3_MAX_CALL_ARGS + 1) * 8;
    if (!v3_emit_sp_load_reg(out, cap, offset, abi_class, dest_reg_index)) {
        abort();
    }
}

static void v3_emit_address_spill_store(char *out,
                                        size_t cap,
                                        int32_t call_arg_base,
                                        int call_depth,
                                        int src_reg_index) {
    int32_t offset =
        call_arg_base + call_depth * CHENG_V3_CALL_ARG_SPILL_STRIDE + (int32_t)(CHENG_V3_MAX_CALL_ARGS + 2) * 8;
    if (!v3_emit_sp_store_reg(out, cap, offset, "ptr", src_reg_index)) {
        abort();
    }
}

static void v3_emit_address_spill_load(char *out,
                                       size_t cap,
                                       int32_t call_arg_base,
                                       int call_depth,
                                       int dest_reg_index) {
    int32_t offset =
        call_arg_base + call_depth * CHENG_V3_CALL_ARG_SPILL_STRIDE + (int32_t)(CHENG_V3_MAX_CALL_ARGS + 2) * 8;
    if (!v3_emit_sp_load_reg(out, cap, offset, "ptr", dest_reg_index)) {
        abort();
    }
}

static void v3_emit_index_spill_store(char *out,
                                      size_t cap,
                                      int32_t call_arg_base,
                                      int call_depth,
                                      int src_reg_index) {
    int32_t offset =
        call_arg_base + call_depth * CHENG_V3_CALL_ARG_SPILL_STRIDE + (int32_t)(CHENG_V3_MAX_CALL_ARGS + 3) * 8;
    if (!v3_emit_sp_store_reg(out, cap, offset, "i64", src_reg_index)) {
        abort();
    }
}

static void v3_emit_index_spill_load(char *out,
                                     size_t cap,
                                     int32_t call_arg_base,
                                     int call_depth,
                                     int dest_reg_index) {
    int32_t offset =
        call_arg_base + call_depth * CHENG_V3_CALL_ARG_SPILL_STRIDE + (int32_t)(CHENG_V3_MAX_CALL_ARGS + 3) * 8;
    if (!v3_emit_sp_load_reg(out, cap, offset, "i64", dest_reg_index)) {
        abort();
    }
}

static bool v3_emit_cstring_literal_label(const V3SystemLinkPlanStub *plan,
                                          const V3LoweredFunctionStub *current_function,
                                          const char *literal_text,
                                          int32_t *string_label_index_io,
                                          char *label_out,
                                          size_t label_cap,
                                          char *out,
                                          size_t cap) {
    char safe_name[128];
    char *escaped;
    int32_t label_id = 0;
    const char *label_prefix = "L_v3_strlit";
    v3_sanitize_path_part(current_function->symbol_text, safe_name, sizeof(safe_name));
    if (string_label_index_io) {
        label_id = *string_label_index_io;
        *string_label_index_io += 1;
    } else {
        label_id = v3_next_expr_label_id();
        label_prefix = "L_v3_expr_strlit";
    }
    snprintf(label_out,
             label_cap,
             "%s_%s_%d",
             label_prefix,
             safe_name,
             label_id);
    escaped = v3_c_escape(literal_text);
    if (!escaped) {
        return false;
    }
    v3_append_cstring_section_for_target(plan, out, cap);
    v3_text_appendf(out, cap,
                    "%s:\n"
                    "  .asciz \"%s\"\n"
                    ".text\n"
                    ".p2align 2\n",
                    label_out,
                    escaped);
    free(escaped);
    return true;
}

static bool v3_emit_zero_address_range(char *out,
                                       size_t cap,
                                       int addr_reg,
                                       int32_t size);
static bool v3_emit_mov_imm(char *out, size_t cap, int reg_index, int64_t value);
static void v3_emit_store_scalar_to_address(char *out,
                                            size_t cap,
                                            int addr_reg,
                                            int32_t offset,
                                            const char *abi_class,
                                            int src_reg);

static bool v3_emit_str_literal_into_slot(const V3SystemLinkPlanStub *plan,
                                          const V3LoweredFunctionStub *current_function,
                                          const V3AsmLocalSlot *dest_slot,
                                          const char *literal_text,
                                          int32_t *string_label_index_io,
                                          char *out,
                                          size_t cap) {
    char label[128];
    int32_t str_size = v3_active_target_str_size_i32();
    int32_t str_len_offset = v3_active_target_str_len_offset_i32();
    if (!dest_slot || strcmp(dest_slot->type_text, "str") != 0 || dest_slot->stack_offset < 0) {
        return false;
    }
    if (!v3_emit_cstring_literal_label(plan,
                                       current_function,
                                       literal_text,
                                       string_label_index_io,
                                       label,
                                       sizeof(label),
                                       out,
                                       cap)) {
        return false;
    }
    if (!v3_emit_slot_address(out, cap, dest_slot, 15)) {
        return false;
    }
    if (!v3_emit_symbol_address_for_target(plan, out, cap, 9, label)) {
        return false;
    }
    if (!v3_emit_zero_address_range(out, cap, 15, str_size) ||
        !v3_emit_mov_imm(out, cap, 10, (int64_t)strlen(literal_text))) {
        return false;
    }
    v3_emit_store_scalar_to_address(out, cap, 15, 0, "ptr", 9);
    v3_emit_store_scalar_to_address(out, cap, 15, str_len_offset, "i32", 10);
    return true;
}

static bool v3_emit_str_literal_into_address(const V3SystemLinkPlanStub *plan,
                                             const V3LoweredFunctionStub *current_function,
                                             const char *literal_text,
                                             int dest_addr_reg,
                                             int32_t *string_label_index_io,
                                             char *out,
                                             size_t cap) {
    char label[128];
    int32_t str_size = v3_active_target_str_size_i32();
    int32_t str_len_offset = v3_active_target_str_len_offset_i32();
    if (!v3_emit_cstring_literal_label(plan,
                                       current_function,
                                       literal_text,
                                       string_label_index_io,
                                       label,
                                       sizeof(label),
                                       out,
                                       cap)) {
        return false;
    }
    if (!v3_emit_symbol_address_for_target(plan, out, cap, 9, label)) {
        return false;
    }
    if (!v3_emit_zero_address_range(out, cap, dest_addr_reg, str_size) ||
        !v3_emit_mov_imm(out, cap, 10, (int64_t)strlen(literal_text))) {
        return false;
    }
    v3_emit_store_scalar_to_address(out, cap, dest_addr_reg, 0, "ptr", 9);
    v3_emit_store_scalar_to_address(out, cap, dest_addr_reg, str_len_offset, "i32", 10);
    return true;
}

static bool v3_emit_slot_address(char *out,
                                 size_t cap,
                                 const V3AsmLocalSlot *slot,
                                 int reg_index);
static bool v3_emit_add_address_offset(char *out,
                                       size_t cap,
                                       int dst_reg,
                                       int src_reg,
                                       int32_t offset);
static bool v3_emit_mov_imm(char *out, size_t cap, int reg_index, int64_t value);
static bool v3_emit_sp_store_reg(char *out,
                                 size_t cap,
                                 int32_t offset,
                                 const char *abi_class,
                                 int src_reg);
static bool v3_emit_sp_load_reg(char *out,
                                size_t cap,
                                int32_t offset,
                                const char *abi_class,
                                int dst_reg);

static bool v3_emit_zero_address_range(char *out,
                                       size_t cap,
                                       int addr_reg,
                                       int32_t size) {
    if (v3_active_target_is_linux_x86_64()) {
        int32_t offset = 0;
        while (offset + 8 <= size) {
            int32_t chunk_base = offset;
            int32_t chunk_limit = size;
            if (chunk_limit - chunk_base > 32760) {
                chunk_limit = chunk_base + 32760;
            }
            if (!v3_emit_add_address_offset(out, cap, 14, addr_reg, chunk_base)) {
                return false;
            }
            while (offset + 8 <= chunk_limit) {
                v3_text_appendf(out, cap,
                                "  movq $0, %d(%s)\n",
                                offset - chunk_base,
                                v3_x64_qreg_name(14));
                offset += 8;
            }
        }
        if (offset + 4 <= size) {
            if (!v3_emit_add_address_offset(out, cap, 14, addr_reg, offset)) {
                return false;
            }
            v3_text_appendf(out, cap, "  movl $0, 0(%s)\n", v3_x64_qreg_name(14));
            offset += 4;
        }
        while (offset < size) {
            if (!v3_emit_add_address_offset(out, cap, 14, addr_reg, offset)) {
                return false;
            }
            v3_text_appendf(out, cap, "  movb $0, 0(%s)\n", v3_x64_qreg_name(14));
            offset += 1;
        }
        return true;
    }
    int32_t offset = 0;
    while (offset + 8 <= size) {
        int32_t chunk_base = offset;
        int32_t chunk_limit = size;
        if (chunk_limit - chunk_base > 32760) {
            chunk_limit = chunk_base + 32760;
        }
        if (!v3_emit_add_address_offset(out, cap, 14, addr_reg, chunk_base)) {
            return false;
        }
        while (offset + 8 <= chunk_limit) {
            v3_text_appendf(out, cap, "  str xzr, [x14, #%d]\n", offset - chunk_base);
            offset += 8;
        }
    }
    if (offset + 4 <= size) {
        if (!v3_emit_add_address_offset(out, cap, 14, addr_reg, offset)) {
            return false;
        }
        v3_text_appendf(out, cap, "  str wzr, [x14, #0]\n");
        offset += 4;
    }
    while (offset < size) {
        if (!v3_emit_add_address_offset(out, cap, 14, addr_reg, offset)) {
            return false;
        }
        v3_text_appendf(out, cap, "  strb wzr, [x14, #0]\n");
        offset += 1;
    }
    return true;
}

static bool CHENG_V3_UNUSED v3_emit_zero_slot(char *out,
                                              size_t cap,
                                              const V3AsmLocalSlot *slot) {
    if (!slot) {
        return false;
    }
    if (!v3_emit_slot_address(out, cap, slot, 15)) {
        return false;
    }
    return v3_emit_zero_address_range(out, cap, 15, slot->stack_size);
}

static void v3_emit_load_scalar_from_address(char *out,
                                             size_t cap,
                                             int addr_reg,
                                             int32_t offset,
                                             const char *abi_class,
                                             int dst_reg) {
    if (v3_active_target_is_linux_x86_64()) {
        if (strcmp(abi_class, "i8") == 0) {
            v3_text_appendf(out, cap, "  movsbq %d(%s), %s\n", offset, v3_x64_qreg_name(addr_reg), v3_x64_qreg_name(dst_reg));
        } else if (strcmp(abi_class, "u8") == 0 || strcmp(abi_class, "bool") == 0) {
            v3_text_appendf(out, cap, "  movzbl %d(%s), %s\n", offset, v3_x64_qreg_name(addr_reg), v3_x64_dreg_name(dst_reg));
        } else if (strcmp(abi_class, "i16") == 0) {
            v3_text_appendf(out, cap, "  movswq %d(%s), %s\n", offset, v3_x64_qreg_name(addr_reg), v3_x64_qreg_name(dst_reg));
        } else if (strcmp(abi_class, "u16") == 0) {
            v3_text_appendf(out, cap, "  movzwl %d(%s), %s\n", offset, v3_x64_qreg_name(addr_reg), v3_x64_dreg_name(dst_reg));
        } else if (strcmp(abi_class, "i32") == 0) {
            v3_text_appendf(out, cap, "  movslq %d(%s), %s\n", offset, v3_x64_qreg_name(addr_reg), v3_x64_qreg_name(dst_reg));
        } else {
            v3_text_appendf(out, cap, "  movq %d(%s), %s\n", offset, v3_x64_qreg_name(addr_reg), v3_x64_qreg_name(dst_reg));
        }
        return;
    }
    if (offset > 4095) {
        if (!v3_emit_add_address_offset(out, cap, 16, addr_reg, offset)) {
            abort();
        }
        addr_reg = 16;
        offset = 0;
    }
    if (strcmp(abi_class, "i8") == 0) {
        v3_text_appendf(out, cap, "  ldrsb x%d, [x%d, #%d]\n", dst_reg, addr_reg, offset);
    } else if (strcmp(abi_class, "u8") == 0 || strcmp(abi_class, "bool") == 0) {
        v3_text_appendf(out, cap, "  ldrb w%d, [x%d, #%d]\n", dst_reg, addr_reg, offset);
    } else if (strcmp(abi_class, "i16") == 0) {
        v3_text_appendf(out, cap, "  ldrsh x%d, [x%d, #%d]\n", dst_reg, addr_reg, offset);
    } else if (strcmp(abi_class, "u16") == 0) {
        v3_text_appendf(out, cap, "  ldrh w%d, [x%d, #%d]\n", dst_reg, addr_reg, offset);
    } else if (strcmp(abi_class, "i32") == 0) {
        v3_text_appendf(out, cap, "  ldrsw x%d, [x%d, #%d]\n", dst_reg, addr_reg, offset);
    } else {
        v3_text_appendf(out, cap, "  ldr x%d, [x%d, #%d]\n", dst_reg, addr_reg, offset);
    }
}

static void v3_emit_store_scalar_to_address(char *out,
                                            size_t cap,
                                            int addr_reg,
                                            int32_t offset,
                                            const char *abi_class,
                                            int src_reg) {
    if (v3_active_target_is_linux_x86_64()) {
        if (strcmp(abi_class, "i8") == 0 ||
            strcmp(abi_class, "u8") == 0 ||
            strcmp(abi_class, "bool") == 0) {
            v3_text_appendf(out, cap, "  movb %s, %d(%s)\n", v3_x64_breg_name(src_reg), offset, v3_x64_qreg_name(addr_reg));
        } else if (strcmp(abi_class, "i16") == 0 ||
                   strcmp(abi_class, "u16") == 0) {
            v3_text_appendf(out, cap, "  movw %s, %d(%s)\n", v3_x64_hreg_name(src_reg), offset, v3_x64_qreg_name(addr_reg));
        } else if (strcmp(abi_class, "i32") == 0) {
            v3_text_appendf(out, cap, "  movl %s, %d(%s)\n", v3_x64_dreg_name(src_reg), offset, v3_x64_qreg_name(addr_reg));
        } else {
            v3_text_appendf(out, cap, "  movq %s, %d(%s)\n", v3_x64_qreg_name(src_reg), offset, v3_x64_qreg_name(addr_reg));
        }
        return;
    }
    if (offset > 4095) {
        if (!v3_emit_add_address_offset(out, cap, 16, addr_reg, offset)) {
            abort();
        }
        addr_reg = 16;
        offset = 0;
    }
    if (strcmp(abi_class, "i8") == 0 ||
        strcmp(abi_class, "u8") == 0 ||
        strcmp(abi_class, "bool") == 0) {
        v3_text_appendf(out, cap, "  strb w%d, [x%d, #%d]\n", src_reg, addr_reg, offset);
    } else if (strcmp(abi_class, "i16") == 0 ||
               strcmp(abi_class, "u16") == 0) {
        v3_text_appendf(out, cap, "  strh w%d, [x%d, #%d]\n", src_reg, addr_reg, offset);
    } else if (strcmp(abi_class, "i32") == 0) {
        v3_text_appendf(out, cap, "  str w%d, [x%d, #%d]\n", src_reg, addr_reg, offset);
    } else {
        v3_text_appendf(out, cap, "  str x%d, [x%d, #%d]\n", src_reg, addr_reg, offset);
    }
}

static bool v3_emit_copy_address_to_address(const V3SystemLinkPlanStub *plan,
                                            char *out,
                                            size_t cap,
                                            int dst_reg,
                                            int src_reg,
                                            int32_t size) {
    if (size < 0) {
        return false;
    }
    if (v3_active_target_is_linux_x86_64()) {
        int preserved_dst_reg = -1;
        int preserved_src_reg = -1;
        if (!v3_x64_reg_is_callee_saved(dst_reg)) {
            preserved_dst_reg = v3_x64_pick_preserved_reg(dst_reg, src_reg);
            if (preserved_dst_reg < 0) {
                return false;
            }
            v3_text_appendf(out, cap,
                            "  movq %s, %s\n",
                            v3_x64_qreg_name(dst_reg),
                            v3_x64_qreg_name(preserved_dst_reg));
        }
        if (src_reg != dst_reg &&
            !v3_x64_reg_is_callee_saved(src_reg)) {
            preserved_src_reg = v3_x64_pick_preserved_reg3(dst_reg,
                                                           src_reg,
                                                           preserved_dst_reg);
            if (preserved_src_reg < 0) {
                return false;
            }
            v3_text_appendf(out, cap,
                            "  movq %s, %s\n",
                            v3_x64_qreg_name(src_reg),
                            v3_x64_qreg_name(preserved_src_reg));
        }
        v3_emit_x64_prepare_rdi_rsi_q_q(out, cap, dst_reg, src_reg);
        v3_text_appendf(out, cap, "  movl $%d, %%edx\n", size);
        v3_emit_call_named_symbol(plan, out, cap, "copyMem");
        if (preserved_src_reg >= 0) {
            v3_text_appendf(out, cap,
                            "  movq %s, %s\n",
                            v3_x64_qreg_name(preserved_src_reg),
                            v3_x64_qreg_name(src_reg));
        }
        if (preserved_dst_reg >= 0) {
            v3_text_appendf(out, cap,
                            "  movq %s, %s\n",
                            v3_x64_qreg_name(preserved_dst_reg),
                            v3_x64_qreg_name(dst_reg));
        }
        return true;
    }
    v3_text_appendf(out, cap,
                    "  mov x0, x%d\n"
                    "  mov x1, x%d\n",
                    dst_reg,
                    src_reg);
    if (!v3_emit_mov_imm(out, cap, 2, size)) {
        return false;
    }
    v3_emit_call_named_symbol(plan, out, cap, "copyMem");
    return true;
}

static bool v3_x64_external_memory_arg_stack_bytes(const V3LoweringPlanStub *lowering,
                                                   const V3LoweredFunctionStub *current_function,
                                                   const V3ImportAlias *aliases,
                                                   size_t alias_count,
                                                   const V3AsmCallTarget *target,
                                                   size_t arg_index,
                                                   int32_t *stack_bytes_out) {
    char normalized_type[256];
    V3TypeLayoutStub layout;
    if (!stack_bytes_out) {
        return false;
    }
    *stack_bytes_out = 0;
    if (!v3_active_target_is_linux_x86_64() ||
        target == NULL ||
        !target->external ||
        !target->c_abi ||
        arg_index >= target->param_count ||
        strcmp(target->param_abi_classes[arg_index], "composite") != 0) {
        return true;
    }
    if (!lowering ||
        !current_function ||
        !v3_normalize_type_text(lowering,
                                current_function->owner_module_path,
                                aliases,
                                alias_count,
                                target->param_types[arg_index],
                                normalized_type,
                                sizeof(normalized_type)) ||
        !v3_compute_type_layout_impl(lowering, normalized_type, &layout, 0U)) {
        fprintf(stderr,
                "[cheng_v3_seed] x64 external composite arg layout failed caller=%s callee=%s arg_index=%zu type=%s\n",
                current_function ? current_function->symbol_text : "<nil>",
                target ? target->callee_name : "<nil>",
                arg_index,
                target ? target->param_types[arg_index] : "<nil>");
        return false;
    }
    if (layout.size <= 0 || layout.align <= 0 || layout.align > 8) {
        fprintf(stderr,
                "[cheng_v3_seed] x64 external composite arg invalid layout caller=%s callee=%s arg_index=%zu type=%s size=%d align=%d\n",
                current_function->symbol_text,
                target->callee_name,
                arg_index,
                normalized_type,
                layout.size,
                layout.align);
        return false;
    }
    if (layout.size <= 16) {
        fprintf(stderr,
                "[cheng_v3_seed] x64 external small composite arg unsupported caller=%s callee=%s arg_index=%zu type=%s size=%d\n",
                current_function->symbol_text,
                target->callee_name,
                arg_index,
                normalized_type,
                layout.size);
        return false;
    }
    *stack_bytes_out = v3_align_up_i32(layout.size, 8);
    return true;
}

static bool v3_emit_call_from_spills(const V3SystemLinkPlanStub *plan,
                                     const V3LoweringPlanStub *lowering,
                                     const V3LoweredFunctionStub *current_function,
                                     const V3ImportAlias *aliases,
                                     size_t alias_count,
                                     const V3AsmCallTarget *target,
                                     bool composite_return,
                                     int32_t call_arg_base,
                                     int call_depth,
                                     char *out,
                                     size_t cap) {
    size_t i;
    if (!target) {
        return false;
    }
    if (v3_active_target_is_linux_x86_64() && target->external && target->c_abi) {
        int call_reg_count = v3_call_arg_reg_count_for_target(plan, composite_return);
        int gp_arg_index = 0;
        int32_t raw_stack_bytes = 0;
        int32_t stack_arg_bytes = 0;
        int32_t stack_offset = 0;
        for (i = 0; i < target->param_count; ++i) {
            int32_t composite_stack_bytes = 0;
            if (!v3_x64_external_memory_arg_stack_bytes(lowering,
                                                        current_function,
                                                        aliases,
                                                        alias_count,
                                                        target,
                                                        i,
                                                        &composite_stack_bytes)) {
                return false;
            }
            if (composite_stack_bytes > 0) {
                raw_stack_bytes += composite_stack_bytes;
                continue;
            }
            if (gp_arg_index < call_reg_count) {
                gp_arg_index += 1;
                continue;
            }
            raw_stack_bytes += 8;
            gp_arg_index += 1;
        }
        stack_arg_bytes = v3_align_up_i32(raw_stack_bytes, 16);
        if (stack_arg_bytes > 0) {
            v3_text_appendf(out, cap, "  subq $%d, %%rsp\n", stack_arg_bytes);
        }
        gp_arg_index = 0;
        stack_offset = 0;
        for (i = 0; i < target->param_count; ++i) {
            const char *arg_abi = v3_call_arg_load_abi(target, i);
            int32_t composite_stack_bytes = 0;
            if (!v3_x64_external_memory_arg_stack_bytes(lowering,
                                                        current_function,
                                                        aliases,
                                                        alias_count,
                                                        target,
                                                        i,
                                                        &composite_stack_bytes)) {
                return false;
            }
            if (composite_stack_bytes > 0) {
                v3_emit_call_arg_spill_load_adjusted(out,
                                                     cap,
                                                     call_arg_base,
                                                     call_depth,
                                                     i,
                                                     9,
                                                     "ptr",
                                                     stack_arg_bytes);
                if (!v3_emit_sp_offset_address(out, cap, 14, stack_offset) ||
                    !v3_emit_copy_address_to_address(plan,
                                                     out,
                                                     cap,
                                                     14,
                                                     9,
                                                     composite_stack_bytes)) {
                    return false;
                }
                stack_offset += composite_stack_bytes;
                continue;
            }
            if (gp_arg_index < call_reg_count) {
                gp_arg_index += 1;
                continue;
            }
            v3_emit_call_arg_spill_load_adjusted(out,
                                                 cap,
                                                 call_arg_base,
                                                 call_depth,
                                                 i,
                                                 9,
                                                 arg_abi,
                                                 stack_arg_bytes);
            if (strcmp(arg_abi, "i32") == 0 || strcmp(arg_abi, "bool") == 0) {
                v3_text_appendf(out, cap, "  movl %s, %d(%%rsp)\n",
                                v3_x64_dreg_name(9),
                                stack_offset);
            } else {
                v3_text_appendf(out, cap, "  movq %s, %d(%%rsp)\n",
                                v3_x64_qreg_name(9),
                                stack_offset);
            }
            stack_offset += 8;
            gp_arg_index += 1;
        }
        gp_arg_index = 0;
        for (i = 0; i < target->param_count; ++i) {
            int32_t composite_stack_bytes = 0;
            if (!v3_x64_external_memory_arg_stack_bytes(lowering,
                                                        current_function,
                                                        aliases,
                                                        alias_count,
                                                        target,
                                                        i,
                                                        &composite_stack_bytes)) {
                return false;
            }
            if (composite_stack_bytes > 0) {
                continue;
            }
            if (gp_arg_index >= call_reg_count) {
                gp_arg_index += 1;
                continue;
            }
            v3_emit_call_arg_spill_load_adjusted(out,
                                                 cap,
                                                 call_arg_base,
                                                 call_depth,
                                                 i,
                                                 v3_x64_call_arg_virtual_reg((size_t)gp_arg_index, composite_return),
                                                 v3_call_arg_load_abi(target, i),
                                                 stack_arg_bytes);
            gp_arg_index += 1;
        }
        if (composite_return) {
            v3_emit_call_dest_spill_load_adjusted(out,
                                                  cap,
                                                  call_arg_base,
                                                  call_depth,
                                                  14,
                                                  stack_arg_bytes);
        }
        v3_emit_call_target_symbol_text(plan, out, cap, target->symbol_name);
        if (stack_arg_bytes > 0) {
            v3_text_appendf(out, cap, "  addq $%d, %%rsp\n", stack_arg_bytes);
        }
        return true;
    }
    {
        int call_reg_count = v3_call_arg_reg_count_for_target(plan, composite_return);
        int32_t stack_arg_bytes = 0;
        if ((int)target->param_count > call_reg_count) {
            stack_arg_bytes =
                v3_align_up_i32((int32_t)(target->param_count - (size_t)call_reg_count) * 8, 16);
            if (v3_active_target_is_linux_x86_64()) {
                v3_text_appendf(out, cap, "  subq $%d, %%rsp\n", stack_arg_bytes);
            } else {
                v3_text_appendf(out, cap, "  sub sp, sp, #%d\n", stack_arg_bytes);
            }
            for (i = (size_t)call_reg_count; i < target->param_count; ++i) {
                const char *stack_abi = v3_call_arg_load_abi(target, i);
                int32_t offset = (int32_t)(i - (size_t)call_reg_count) * 8;
                v3_emit_call_arg_spill_load_adjusted(out,
                                                     cap,
                                                     call_arg_base,
                                                     call_depth,
                                                     i,
                                                     9,
                                                     stack_abi,
                                                     stack_arg_bytes);
                if (v3_active_target_is_linux_x86_64()) {
                    if (strcmp(stack_abi, "i32") == 0 || strcmp(stack_abi, "bool") == 0) {
                        v3_text_appendf(out, cap, "  movl %s, %d(%%rsp)\n",
                                        v3_x64_dreg_name(9),
                                        offset);
                    } else {
                        v3_text_appendf(out, cap, "  movq %s, %d(%%rsp)\n",
                                        v3_x64_qreg_name(9),
                                        offset);
                    }
                } else if (strcmp(stack_abi, "i32") == 0 || strcmp(stack_abi, "bool") == 0) {
                    v3_text_appendf(out, cap, "  str w9, [sp, #%d]\n", offset);
                } else {
                    v3_text_appendf(out, cap, "  str x9, [sp, #%d]\n", offset);
                }
            }
        }
        for (i = 0; i < target->param_count && (int)i < call_reg_count; ++i) {
            int dest_reg = v3_active_target_is_linux_x86_64() ?
                           v3_x64_call_arg_virtual_reg(i, composite_return) :
                           (int)i;
            v3_emit_call_arg_spill_load_adjusted(out,
                                                 cap,
                                                 call_arg_base,
                                                 call_depth,
                                                 i,
                                                 dest_reg,
                                                 v3_call_arg_load_abi(target, i),
                                                 stack_arg_bytes);
        }
        if (composite_return) {
            v3_emit_call_dest_spill_load_adjusted(out,
                                                  cap,
                                                  call_arg_base,
                                                  call_depth,
                                                  v3_active_target_is_linux_x86_64() ? 14 : 8,
                                                  stack_arg_bytes);
        }
        v3_emit_call_target_symbol_text(plan, out, cap, target->symbol_name);
        if ((int)target->param_count > call_reg_count) {
            if (v3_active_target_is_linux_x86_64()) {
                v3_text_appendf(out, cap, "  addq $%d, %%rsp\n", stack_arg_bytes);
            } else {
                v3_text_appendf(out, cap, "  add sp, sp, #%d\n", stack_arg_bytes);
            }
        }
    }
    return true;
}

static bool v3_emit_copy_slot_to_address(const V3SystemLinkPlanStub *plan,
                                         const V3LoweringPlanStub *lowering,
                                         char *out,
                                         size_t cap,
                                         const V3AsmLocalSlot *src_slot,
                                         int dst_reg) {
    V3TypeLayoutStub layout;
    char value_type[256];
    int32_t copy_size;
    int src_reg;
    if (!src_slot || src_slot->stack_size < 0) {
        return false;
    }
    if (src_slot->indirect_value) {
        if (!lowering) {
            return false;
        }
        v3_strip_var_type_text(src_slot->type_text, value_type, sizeof(value_type));
        if (!v3_compute_type_layout_impl(lowering, value_type, &layout, 0U)) {
            return false;
        }
        copy_size = layout.size;
        src_reg = dst_reg == 14 ? 15 : 14;
        if (!v3_emit_local_value_address(out, cap, src_slot, src_reg)) {
            return false;
        }
        return v3_emit_copy_address_to_address(plan, out, cap, dst_reg, src_reg, copy_size);
    }
    copy_size = src_slot->stack_size;
    src_reg = dst_reg == 14 ? 15 : 14;
    if (!v3_emit_local_value_address(out, cap, src_slot, src_reg)) {
        return false;
    }
    return v3_emit_copy_address_to_address(plan, out, cap, dst_reg, src_reg, copy_size);
}

static bool v3_emit_mov_imm(char *out, size_t cap, int reg_index, int64_t value) {
    if (v3_active_target_is_linux_x86_64()) {
        if (value >= INT32_MIN && value <= INT32_MAX) {
            v3_text_appendf(out, cap,
                            "  movq $%lld, %s\n",
                            (long long)value,
                            v3_x64_qreg_name(reg_index));
            return true;
        }
        v3_text_appendf(out, cap,
                        "  movabsq $%lld, %s\n",
                        (long long)value,
                        v3_x64_qreg_name(reg_index));
        return true;
    }
    uint64_t bits = (uint64_t)value;
    bool emitted = false;
    int shift;
    if (value >= -65535 && value <= 65535) {
        v3_text_appendf(out, cap, "  mov x%d, #%lld\n", reg_index, (long long)value);
        return true;
    }
    for (shift = 0; shift <= 48; shift += 16) {
        uint16_t part = (uint16_t)((bits >> shift) & 0xffffU);
        if (!emitted) {
            if (part == 0U && shift < 48) {
                continue;
            }
            v3_text_appendf(out,
                            cap,
                            "  movz x%d, #%u, lsl #%d\n",
                            reg_index,
                            (unsigned)part,
                            shift);
            emitted = true;
            continue;
        }
        if (part != 0U) {
            v3_text_appendf(out,
                            cap,
                            "  movk x%d, #%u, lsl #%d\n",
                            reg_index,
                            (unsigned)part,
                            shift);
        }
    }
    if (!emitted) {
        v3_text_appendf(out, cap, "  mov x%d, #0\n", reg_index);
    }
    return true;
}

static bool v3_parse_member_access_expr(const char *expr_text,
                                        char *base_out,
                                        size_t base_cap,
                                        char *field_out,
                                        size_t field_cap) {
    char expr[4096];
    bool in_string = false;
    int32_t paren_depth = 0;
    int32_t bracket_depth = 0;
    int32_t last_sep = -1;
    int32_t sep_len = 0;
    size_t i;
    size_t len;
    v3_trim_copy_text(expr_text, expr, sizeof(expr));
    len = strlen(expr);
    if (len <= 0U) {
        return false;
    }
    for (i = 0; i < len; ++i) {
        char ch = expr[i];
        if (ch == '"' && !v3_quote_is_escaped(expr, i)) {
            in_string = !in_string;
            continue;
        }
        if (in_string) {
            continue;
        }
        if (ch == '(') {
            paren_depth += 1;
            continue;
        }
        if (ch == ')') {
            paren_depth -= 1;
            if (paren_depth < 0) {
                return false;
            }
            continue;
        }
        if (ch == '[') {
            bracket_depth += 1;
            continue;
        }
        if (ch == ']') {
            bracket_depth -= 1;
            if (bracket_depth < 0) {
                return false;
            }
            continue;
        }
        if (ch == '.' && paren_depth == 0 && bracket_depth == 0) {
            last_sep = (int32_t)i;
            sep_len = 1;
            continue;
        }
        if (ch == '-' &&
            paren_depth == 0 &&
            bracket_depth == 0 &&
            i + 1U < len &&
            expr[i + 1U] == '>') {
            last_sep = (int32_t)i;
            sep_len = 2;
        }
    }
    if (in_string || paren_depth != 0 || bracket_depth != 0 || last_sep <= 0 || (size_t)last_sep + (size_t)sep_len >= len) {
        return false;
    }
    snprintf(base_out, base_cap, "%.*s", last_sep, expr);
    v3_trim_copy_text(base_out, base_out, base_cap);
    snprintf(field_out, field_cap, "%s", expr + last_sep + sep_len);
    v3_trim_copy_text(field_out, field_out, field_cap);
    if (base_out[0] == '\0' ||
        field_out[0] == '\0' ||
        strchr(field_out, '.') != NULL ||
        strstr(field_out, "->") != NULL ||
        strchr(field_out, '[') != NULL ||
        strchr(field_out, ']') != NULL ||
        strchr(field_out, '(') != NULL ||
        strchr(field_out, ')') != NULL ||
        strchr(field_out, ' ') != NULL) {
        return false;
    }
    return true;
}

static bool v3_parse_index_access_expr(const char *expr_text,
                                       char *base_out,
                                       size_t base_cap,
                                       char *index_out,
                                       size_t index_cap) {
    char expr[4096];
    bool in_string = false;
    int32_t paren_depth = 0;
    int32_t bracket_depth = 0;
    int32_t top_open = -1;
    size_t i;
    size_t len;
    v3_trim_copy_text(expr_text, expr, sizeof(expr));
    len = strlen(expr);
    if (len < 3U || expr[len - 1U] != ']') {
        return false;
    }
    for (i = 0; i < len; ++i) {
        char ch = expr[i];
        if (ch == '"' && !v3_quote_is_escaped(expr, i)) {
            in_string = !in_string;
            continue;
        }
        if (in_string) {
            continue;
        }
        if (ch == '(') {
            paren_depth += 1;
            continue;
        }
        if (ch == ')') {
            paren_depth -= 1;
            if (paren_depth < 0) {
                return false;
            }
            continue;
        }
        if (ch == '[') {
            if (paren_depth == 0 && bracket_depth == 0) {
                top_open = (int32_t)i;
            }
            bracket_depth += 1;
            continue;
        }
        if (ch == ']') {
            bracket_depth -= 1;
            if (bracket_depth < 0) {
                return false;
            }
            continue;
        }
    }
    if (in_string || paren_depth != 0 || bracket_depth != 0 || top_open <= 0) {
        return false;
    }
    snprintf(base_out, base_cap, "%.*s", top_open, expr);
    v3_trim_copy_text(base_out, base_out, base_cap);
    snprintf(index_out, index_cap, "%.*s", (int)(len - (size_t)top_open - 2U), expr + top_open + 1);
    v3_trim_copy_text(index_out, index_out, index_cap);
    return base_out[0] != '\0' && index_out[0] != '\0';
}

static bool v3_find_type_def_by_normalized(const V3LoweringPlanStub *lowering,
                                           const char *normalized_type,
                                           int32_t *index_out) {
    char stripped[PATH_MAX];
    char inner[256];
    char owner_module[PATH_MAX];
    char type_instance_name[128];
    char type_args[CHENG_V3_MAX_TYPE_PARAMS][256];
    int32_t fixed_len = 0;
    size_t type_arg_count = 0U;
    if (index_out) {
        *index_out = -1;
    }
    v3_strip_internal_ref_type(normalized_type, stripped, sizeof(stripped));
    if (stripped[0] == '\0' ||
        v3_is_tuple_type_text(stripped) ||
        v3_type_text_strip_one_pointer_suffix(stripped, inner, sizeof(inner)) ||
        (strlen(stripped) > 2U && strcmp(stripped + strlen(stripped) - 2U, "[]") == 0) ||
        v3_startswith(stripped, "Result[") ||
        v3_parse_fixed_array_type(stripped, inner, sizeof(inner), &fixed_len)) {
        return false;
    }
    if (!v3_split_qualified_type_text(stripped,
                                      owner_module,
                                      sizeof(owner_module),
                                      type_instance_name,
                                      sizeof(type_instance_name)) ||
        !v3_parse_type_instance_text(type_instance_name,
                                     type_instance_name,
                                     sizeof(type_instance_name),
                                     type_args,
                                     &type_arg_count,
                                     CHENG_V3_MAX_TYPE_PARAMS)) {
        return false;
    }
    if (index_out) {
        *index_out = v3_find_type_def_index_by_symbol(lowering, owner_module, type_instance_name);
    }
    return index_out && *index_out >= 0;
}

static bool v3_resolve_field_meta_impl(const V3LoweringPlanStub *lowering,
                                       const char *normalized_type,
                                       const char *field_name,
                                       char *field_type_out,
                                       size_t field_type_cap,
                                       char *field_abi_out,
                                       size_t field_abi_cap,
                                       int32_t *field_offset_out,
                                       unsigned depth) {
    char pointer_base[256];
    int32_t type_index = -1;
    size_t i;
    if (depth > 8U) {
        return false;
    }
    if (v3_type_text_strip_one_pointer_suffix(normalized_type, pointer_base, sizeof(pointer_base))) {
        return v3_resolve_field_meta_impl(lowering,
                                          pointer_base,
                                          field_name,
                                          field_type_out,
                                          field_type_cap,
                                          field_abi_out,
                                          field_abi_cap,
                                          field_offset_out,
                                          depth + 1U);
    }
    if (strcmp(normalized_type, "Bytes") == 0) {
        if (strcmp(field_name, "data") == 0) {
            snprintf(field_type_out, field_type_cap, "%s", "ptr");
            snprintf(field_abi_out, field_abi_cap, "%s", "ptr");
            *field_offset_out = 0;
            return true;
        }
        if (strcmp(field_name, "len") == 0) {
            snprintf(field_type_out, field_type_cap, "%s", "int32");
            snprintf(field_abi_out, field_abi_cap, "%s", "i32");
            *field_offset_out = v3_active_target_bytes_len_offset_i32();
            return true;
        }
        return false;
    }
    if (strcmp(normalized_type, "str") == 0) {
        if (strcmp(field_name, "data") == 0) {
            snprintf(field_type_out, field_type_cap, "%s", "ptr");
            snprintf(field_abi_out, field_abi_cap, "%s", "ptr");
            *field_offset_out = 0;
            return true;
        }
        if (strcmp(field_name, "len") == 0) {
            snprintf(field_type_out, field_type_cap, "%s", "int32");
            snprintf(field_abi_out, field_abi_cap, "%s", "i32");
            *field_offset_out = v3_active_target_str_len_offset_i32();
            return true;
        }
        if (strcmp(field_name, "storeId") == 0 || strcmp(field_name, "store_id") == 0) {
            snprintf(field_type_out, field_type_cap, "%s", "int32");
            snprintf(field_abi_out, field_abi_cap, "%s", "i32");
            *field_offset_out = v3_active_target_str_store_id_offset_i32();
            return true;
        }
        if (strcmp(field_name, "flags") == 0) {
            snprintf(field_type_out, field_type_cap, "%s", "int32");
            snprintf(field_abi_out, field_abi_cap, "%s", "i32");
            *field_offset_out = v3_active_target_str_flags_offset_i32();
            return true;
        }
        return false;
    }
    if (strlen(normalized_type) > 2U &&
        strcmp(normalized_type + strlen(normalized_type) - 2U, "[]") == 0) {
        if (strcmp(field_name, "len") == 0) {
            snprintf(field_type_out, field_type_cap, "%s", "int32");
            snprintf(field_abi_out, field_abi_cap, "%s", "i32");
            *field_offset_out = 0;
            return true;
        }
        if (strcmp(field_name, "cap") == 0) {
            snprintf(field_type_out, field_type_cap, "%s", "int32");
            snprintf(field_abi_out, field_abi_cap, "%s", "i32");
            *field_offset_out = 4;
            return true;
        }
        if (strcmp(field_name, "buffer") == 0) {
            snprintf(field_type_out, field_type_cap, "%s", "ptr");
            snprintf(field_abi_out, field_abi_cap, "%s", "ptr");
            *field_offset_out = v3_active_target_seq_buffer_offset_i32();
            return true;
        }
        return false;
    }
    if (v3_startswith(normalized_type, "Result[") && normalized_type[strlen(normalized_type) - 1U] == ']') {
        V3TypeLayoutStub value_layout;
        V3TypeLayoutStub err_layout;
        char value_type[256];
        int32_t value_offset;
        snprintf(value_type,
                 sizeof(value_type),
                 "%.*s",
                 (int)(strlen(normalized_type) - 8U),
                 normalized_type + 7);
        if (!v3_compute_type_layout_impl(lowering, value_type, &value_layout, depth + 1U) ||
            !v3_compute_type_layout_impl(lowering, "std/result::ErrorInfo", &err_layout, depth + 1U)) {
            return false;
        }
        value_offset = v3_align_up_i32(1, value_layout.align);
        if (strcmp(field_name, "ok") == 0) {
            snprintf(field_type_out, field_type_cap, "%s", "bool");
            snprintf(field_abi_out, field_abi_cap, "%s", "bool");
            *field_offset_out = 0;
            return true;
        }
        if (strcmp(field_name, "value") == 0) {
            snprintf(field_type_out, field_type_cap, "%s", value_type);
            snprintf(field_abi_out, field_abi_cap, "%s", v3_type_abi_class(value_type));
            *field_offset_out = value_offset;
            return true;
        }
        if (strcmp(field_name, "err") == 0) {
            snprintf(field_type_out, field_type_cap, "%s", "std/result::ErrorInfo");
            snprintf(field_abi_out, field_abi_cap, "%s", "composite");
            *field_offset_out = v3_align_up_i32(value_offset + value_layout.size, err_layout.align);
            return true;
        }
        return false;
    }
    if (v3_is_tuple_type_text(normalized_type)) {
        char (*items)[4096] = NULL;
        V3TypeFieldDef *field = NULL;
        size_t item_count = 0U;
        int32_t offset = 0;
        bool ok = false;
        items = (char (*)[4096])v3_xmalloc(sizeof(*items) * CHENG_V3_MAX_TYPE_FIELDS);
        field = (V3TypeFieldDef *)v3_xmalloc(sizeof(*field));
        if (!v3_parse_tuple_type_items(normalized_type,
                                       items,
                                       &item_count,
                                       CHENG_V3_MAX_TYPE_FIELDS)) {
            goto tuple_field_meta_cleanup;
        }
        for (i = 0; i < item_count; ++i) {
            V3TypeLayoutStub field_layout;
            int32_t field_align;
            if (!v3_parse_type_field_spec(items[i], true, field) ||
                !v3_compute_type_layout_impl(lowering,
                                             field->type_text,
                                             &field_layout,
                                             depth + 1U)) {
                goto tuple_field_meta_cleanup;
            }
            field_align = field_layout.align > 0 ? field_layout.align : 1;
            offset = v3_align_up_i32(offset, field_align);
            if (field->has_explicit_name &&
                strcmp(field->name, field_name) == 0) {
                snprintf(field_type_out, field_type_cap, "%s", field->type_text);
                snprintf(field_abi_out, field_abi_cap, "%s", v3_type_abi_class(field->type_text));
                *field_offset_out = offset;
                ok = true;
                goto tuple_field_meta_cleanup;
            }
            offset += field_layout.size;
        }
tuple_field_meta_cleanup:
        free(field);
        free(items);
        return ok;
    }
    {
        char stripped[PATH_MAX];
        char owner_module[PATH_MAX];
        char type_instance_name[128];
        char type_name[128];
        char type_args[CHENG_V3_MAX_TYPE_PARAMS][256];
        size_t type_arg_count = 0U;
        V3ImportAlias *type_aliases = NULL;
        size_t type_alias_count = 0U;
        const V3TypeDefStub *type_def;
        bool ok = false;
        v3_strip_internal_ref_type(normalized_type, stripped, sizeof(stripped));
        if (!v3_split_qualified_type_text(stripped,
                                          owner_module,
                                          sizeof(owner_module),
                                          type_instance_name,
                                          sizeof(type_name)) ||
            !v3_parse_type_instance_text(type_instance_name,
                                         type_name,
                                         sizeof(type_name),
                                         type_args,
                                         &type_arg_count,
                                         CHENG_V3_MAX_TYPE_PARAMS)) {
            goto field_meta_cleanup;
        }
        type_index = v3_find_type_def_index_by_symbol(lowering, owner_module, type_instance_name);
        if (type_index < 0) {
            goto field_meta_cleanup;
        }
        type_def = &lowering->type_defs[type_index];
        if (type_def->kind == V3_TYPE_DEF_ALIAS) {
            char alias_target[256];
            if (type_def->generic_param_count > 0U) {
                type_aliases = (V3ImportAlias *)v3_xmalloc(sizeof(*type_aliases) * CHENG_V3_MAX_IMPORT_ALIASES);
                if (!v3_load_import_aliases_from_source_path(type_def->source_path,
                                                             type_aliases,
                                                             &type_alias_count,
                                                             CHENG_V3_MAX_IMPORT_ALIASES) ||
                    !v3_normalize_type_text_with_params(lowering,
                                                        type_def->owner_module_path,
                                                        type_aliases,
                                                        type_alias_count,
                                                        type_def->generic_param_names,
                                                        type_args,
                                                        type_arg_count,
                                                        type_def->alias_target,
                                                        alias_target,
                                                        sizeof(alias_target))) {
                    goto field_meta_cleanup;
                }
            } else {
                snprintf(alias_target, sizeof(alias_target), "%s", type_def->alias_target);
            }
            ok = v3_resolve_field_meta_impl(lowering,
                                            alias_target,
                                            field_name,
                                            field_type_out,
                                            field_type_cap,
                                            field_abi_out,
                                            field_abi_cap,
                                            field_offset_out,
                                            depth + 1U);
            goto field_meta_cleanup;
        }
        if (type_def->kind != V3_TYPE_DEF_RECORD) {
            goto field_meta_cleanup;
        }
        if (type_def->generic_param_count > 0U &&
            ((type_aliases = (V3ImportAlias *)v3_xmalloc(sizeof(*type_aliases) * CHENG_V3_MAX_IMPORT_ALIASES)) == NULL ||
             !v3_load_import_aliases_from_source_path(type_def->source_path,
                                                      type_aliases,
                                                      &type_alias_count,
                                                      CHENG_V3_MAX_IMPORT_ALIASES) ||
             type_def->generic_param_count != type_arg_count)) {
            goto field_meta_cleanup;
        }
        {
            int32_t offset = 0;
            int32_t i;
            for (i = 0; i < (int32_t)type_def->field_count; ++i) {
                V3TypeLayoutStub field_layout;
                int32_t field_align;
                char resolved_field_type[256];
                if (type_def->generic_param_count > 0U) {
                    if (!v3_normalize_type_text_with_params(lowering,
                                                            type_def->owner_module_path,
                                                            type_aliases,
                                                            type_alias_count,
                                                            type_def->generic_param_names,
                                                            type_args,
                                                            type_arg_count,
                                                            type_def->fields[i].type_text,
                                                            resolved_field_type,
                                                            sizeof(resolved_field_type))) {
                        goto field_meta_cleanup;
                    }
                } else {
                    snprintf(resolved_field_type, sizeof(resolved_field_type), "%s", type_def->fields[i].type_text);
                }
                if (!v3_compute_type_layout_impl(lowering,
                                                 resolved_field_type,
                                                 &field_layout,
                                                 depth + 1U)) {
                    goto field_meta_cleanup;
                }
                field_align = field_layout.align > 0 ? field_layout.align : 1;
                offset = v3_align_up_i32(offset, field_align);
                if (strcmp(type_def->fields[i].name, field_name) == 0) {
                    snprintf(field_type_out, field_type_cap, "%s", resolved_field_type);
                    snprintf(field_abi_out, field_abi_cap, "%s", v3_type_abi_class(resolved_field_type));
                    *field_offset_out = offset;
                    ok = true;
                    goto field_meta_cleanup;
                }
                offset += field_layout.size;
            }
        }
field_meta_cleanup:
        free(type_aliases);
        return ok;
    }
    return false;
}

static bool v3_resolve_index_access_call_target(const V3SystemLinkPlanStub *plan,
                                                const V3LoweringPlanStub *lowering,
                                                const V3LoweredFunctionStub *current_function,
                                                const V3ImportAlias *aliases,
                                                size_t alias_count,
                                                const V3AsmLocalSlot *locals,
                                                size_t local_count,
                                                const char *base_expr,
                                                const char *index_expr,
                                                V3AsmCallTarget *target) {
    char (*args)[4096] = v3_alloc_call_arg_scratch();
    bool ok;
    if (args == NULL || base_expr == NULL || index_expr == NULL || target == NULL) {
        free(args);
        return false;
    }
    snprintf(args[0], sizeof(args[0]), "%s", base_expr);
    snprintf(args[1], sizeof(args[1]), "%s", index_expr);
    ok = v3_resolve_call_target_with_args(plan,
                                          lowering,
                                          current_function,
                                          aliases,
                                          alias_count,
                                          locals,
                                          local_count,
                                          "[]",
                                          (const char (*)[4096])args,
                                          2U,
                                          target);
    free(args);
    return ok;
}

static bool v3_resolve_call_target(const V3SystemLinkPlanStub *plan,
                                   const V3LoweringPlanStub *lowering,
                                   const V3LoweredFunctionStub *current_function,
                                   const V3ImportAlias *aliases,
                                   size_t alias_count,
                                   const char *callee,
                                   V3AsmCallTarget *target) {
    return v3_resolve_call_target_with_args(plan,
                                            lowering,
                                            current_function,
                                            aliases,
                                            alias_count,
                                            NULL,
                                            0U,
                                            callee,
                                            NULL,
                                            0U,
                                            target);
}

static bool v3_resolve_field_meta(const V3LoweringPlanStub *lowering,
                                  const char *base_type_text,
                                  const char *field_name,
                                  char *field_type_out,
                                  size_t field_type_cap,
                                  char *field_abi_out,
                                  size_t field_abi_cap,
                                  int32_t *field_offset_out) {
    char normalized_type[256];
    v3_strip_var_prefix(base_type_text, normalized_type, sizeof(normalized_type));
    return v3_resolve_field_meta_impl(lowering,
                                      normalized_type,
                                      field_name,
                                      field_type_out,
                                      field_type_cap,
                                      field_abi_out,
                                      field_abi_cap,
                                      field_offset_out,
                                      0U);
}

static bool v3_resolve_field_path_expr(const V3LoweringPlanStub *lowering,
                                       const V3AsmLocalSlot *locals,
                                       size_t local_count,
                                       const char *expr_text,
                                       int32_t *base_local_index_out,
                                       char *field_type_out,
                                       size_t field_type_cap,
                                       char *field_abi_out,
                                       size_t field_abi_cap,
                                       int32_t *field_offset_out) {
    char expr[4096];
    char *segments[32];
    size_t segment_count = 0U;
    char *cursor;
    char current_type[256];
    int32_t local_index;
    int32_t total_offset = 0;
    size_t i;
    v3_trim_copy_text(expr_text, expr, sizeof(expr));
    if (expr[0] == '\0' ||
        strchr(expr, '(') != NULL ||
        strchr(expr, '[') != NULL ||
        strchr(expr, ' ') != NULL) {
        return false;
    }
    cursor = expr;
    segments[segment_count++] = cursor;
    while (*cursor != '\0') {
        if (*cursor == '.') {
            *cursor = '\0';
            if (segment_count >= 32U || cursor[1] == '\0') {
                return false;
            }
            segments[segment_count++] = cursor + 1;
        }
        cursor += 1;
    }
    if (segment_count < 2U) {
        return false;
    }
    local_index = v3_find_local_slot(locals, local_count, segments[0]);
    if (local_index < 0) {
        return false;
    }
    snprintf(current_type, sizeof(current_type), "%s", locals[local_index].type_text);
    for (i = 1; i < segment_count; ++i) {
        int32_t current_offset = 0;
        char next_type[256];
        char next_abi[32];
        if (!v3_resolve_field_meta(lowering,
                                   current_type,
                                   segments[i],
                                   next_type,
                                   sizeof(next_type),
                                   next_abi,
                                   sizeof(next_abi),
                                   &current_offset)) {
            return false;
        }
        total_offset += current_offset;
        snprintf(current_type, sizeof(current_type), "%s", next_type);
        snprintf(field_abi_out, field_abi_cap, "%s", next_abi);
    }
    if (base_local_index_out) {
        *base_local_index_out = local_index;
    }
    if (field_type_out) {
        snprintf(field_type_out, field_type_cap, "%s", current_type);
    }
    if (field_offset_out) {
        *field_offset_out = total_offset;
    }
    return true;
}

static bool v3_emit_index_access_address(const V3SystemLinkPlanStub *plan,
                                         const V3LoweringPlanStub *lowering,
                                         const V3LoweredFunctionStub *current_function,
                                         const V3ImportAlias *aliases,
                                         size_t alias_count,
                                         const V3AsmLocalSlot *locals,
                                         size_t local_count,
                                         const char *expr_text,
                                         char *field_type_out,
                                         size_t field_type_cap,
                                         char *field_abi_out,
                                         size_t field_abi_cap,
                                         int addr_reg,
                                         int32_t call_arg_base,
                                         int32_t string_temp_base,
                                         int32_t string_temp_stride,
                                         int call_depth,
                                         char *out,
                                         size_t cap);
static bool v3_infer_expr_type(const V3SystemLinkPlanStub *plan,
                               const V3LoweringPlanStub *lowering,
                               const V3LoweredFunctionStub *current_function,
                               const V3ImportAlias *aliases,
                               size_t alias_count,
                               const V3AsmLocalSlot *locals,
                               size_t local_count,
                               const char *expr_text,
                               char *type_out,
                               size_t type_cap,
                               char *abi_out,
                               size_t abi_cap);

static bool v3_emit_field_path_address(char *out,
                                       size_t cap,
                                       const V3AsmLocalSlot *locals,
                                       size_t local_count,
                                       const V3LoweringPlanStub *lowering,
                                       const char *expr_text,
                                       char *field_type_out,
                                       size_t field_type_cap,
                                       char *field_abi_out,
                                       size_t field_abi_cap,
                                       int addr_reg) {
    char expr[4096];
    char *segments[32];
    size_t segment_count = 0U;
    char *cursor;
    char current_type[256];
    int32_t local_index;
    size_t i;
    v3_trim_copy_text(expr_text, expr, sizeof(expr));
    if (expr[0] == '\0' ||
        strchr(expr, '(') != NULL ||
        strchr(expr, '[') != NULL ||
        strchr(expr, ' ') != NULL) {
        return false;
    }
    cursor = expr;
    segments[segment_count++] = cursor;
    while (*cursor != '\0') {
        if (*cursor == '.') {
            *cursor = '\0';
            if (segment_count >= 32U || cursor[1] == '\0') {
                return false;
            }
            segments[segment_count++] = cursor + 1;
        }
        cursor += 1;
    }
    if (segment_count < 2U) {
        return false;
    }
    local_index = v3_find_local_slot(locals, local_count, segments[0]);
    if (local_index < 0 ||
        !v3_emit_local_value_address(out, cap, &locals[local_index], addr_reg)) {
        return false;
    }
    snprintf(current_type, sizeof(current_type), "%s", locals[local_index].type_text);
    for (i = 1; i < segment_count; ++i) {
        int32_t field_offset = 0;
        char next_type[256];
        char next_abi[32];
        if (v3_is_internal_ref_type(current_type)) {
            v3_emit_load_scalar_from_address(out, cap, addr_reg, 0, "ptr", addr_reg);
        }
        if (!v3_resolve_field_meta(lowering,
                                   current_type,
                                   segments[i],
                                   next_type,
                                   sizeof(next_type),
                                   next_abi,
                                   sizeof(next_abi),
                                   &field_offset)) {
            return false;
        }
        if (field_offset != 0 &&
            !v3_emit_add_address_offset(out, cap, addr_reg, addr_reg, field_offset)) {
            return false;
        }
        snprintf(current_type, sizeof(current_type), "%s", next_type);
        snprintf(field_abi_out, field_abi_cap, "%s", next_abi);
    }
    snprintf(field_type_out, field_type_cap, "%s", current_type);
    return true;
}

static bool v3_emit_result_field_address(const V3SystemLinkPlanStub *plan,
                                         const V3LoweringPlanStub *lowering,
                                         const V3LoweredFunctionStub *current_function,
                                         const V3ImportAlias *aliases,
                                         size_t alias_count,
                                         const V3AsmLocalSlot *locals,
                                         size_t local_count,
                                         const char *arg_expr,
                                         const char *field_name,
                                         int addr_reg,
                                         int32_t call_arg_base,
                                         int32_t string_temp_base,
                                         int32_t string_temp_stride,
                                         int32_t *string_label_index_io,
                                         int call_depth,
                                         char *out,
                                         size_t cap,
                                         char *field_type_out,
                                         size_t field_type_cap,
                                         char *field_abi_out,
                                         size_t field_abi_cap);

static bool v3_emit_member_access_address(const V3SystemLinkPlanStub *plan,
                                          const V3LoweringPlanStub *lowering,
                                          const V3LoweredFunctionStub *current_function,
                                          const V3ImportAlias *aliases,
                                          size_t alias_count,
                                          const V3AsmLocalSlot *locals,
                                          size_t local_count,
                                          const char *expr_text,
                                          char *field_type_out,
                                          size_t field_type_cap,
                                          char *field_abi_out,
                                          size_t field_abi_cap,
                                          int addr_reg,
                                          int32_t call_arg_base,
                                          int32_t string_temp_base,
                                          int32_t string_temp_stride,
                                          int call_depth,
                                          char *out,
                                          size_t cap) {
    char base_expr[4096];
    char field_name[128];
    char base_type[256];
    char base_abi[32];
    char normalized_base_type[256];
    char nested_type[256];
    char nested_abi[32];
    char result_callee[PATH_MAX];
    char result_arg_text[4096] = {0};
    char result_value_expr[4096] = {0};
    size_t result_arg_count = 0U;
    int32_t field_offset = 0;
    int32_t local_index = -1;
    if (!v3_parse_member_access_expr(expr_text,
                                     base_expr,
                                     sizeof(base_expr),
                                     field_name,
                                     sizeof(field_name)) ||
        !v3_infer_expr_type(plan,
                            lowering,
                            current_function,
                            aliases,
                            alias_count,
                            locals,
                            local_count,
                            base_expr,
                            base_type,
                            sizeof(base_type),
                            base_abi,
                            sizeof(base_abi)) ||
        !v3_normalize_type_text(lowering,
                                current_function->owner_module_path,
                                aliases,
                                alias_count,
                                base_type,
                                normalized_base_type,
                                sizeof(normalized_base_type)) ||
        !v3_resolve_field_meta(lowering,
                               base_type,
                               field_name,
                               field_type_out,
                               field_type_cap,
                               field_abi_out,
                               field_abi_cap,
                               &field_offset)) {
        return false;
    }
    local_index = v3_find_local_slot(locals, local_count, base_expr);
    if (local_index >= 0) {
        if (!v3_emit_local_value_address(out, cap, &locals[local_index], addr_reg)) {
            return false;
        }
    } else if (v3_emit_owner_global_var_address(plan,
                                                lowering,
                                                current_function,
                                                base_expr,
                                                nested_type,
                                                sizeof(nested_type),
                                                nested_abi,
                                                sizeof(nested_abi),
                                                addr_reg,
                                                out,
                                                cap)) {
    } else {
        bool result_value_base = false;
        char (*result_args)[4096] = v3_alloc_call_arg_scratch();
        if ((v3_parse_call_text(base_expr,
                                result_callee,
                                sizeof(result_callee),
                                result_args,
                                &result_arg_count,
                                CHENG_V3_MAX_CALL_ARGS) ||
             v3_parse_prefix_single_arg_call(base_expr,
                                             result_callee,
                                             sizeof(result_callee),
                                             result_arg_text,
                                             sizeof(result_arg_text))) &&
            ((result_arg_count == 1U) || (result_arg_count == 0U && result_arg_text[0] != '\0')) &&
            v3_resolve_result_intrinsic_kind(lowering,
                                            current_function->owner_module_path,
                                            aliases,
                                            alias_count,
                                            result_callee) == V3_RESULT_INTRINSIC_VALUE) {
            if (result_arg_count == 0U) {
                snprintf(result_value_expr, sizeof(result_value_expr), "%s", result_arg_text);
            } else {
                snprintf(result_value_expr, sizeof(result_value_expr), "%s", result_args[0]);
            }
            result_value_base = true;
        }
        free(result_args);
        if (result_value_base) {
            if (!v3_emit_result_field_address(plan,
                                              lowering,
                                              current_function,
                                              aliases,
                                              alias_count,
                                              locals,
                                              local_count,
                                              result_value_expr,
                                              "value",
                                              addr_reg,
                                              call_arg_base,
                                              string_temp_base,
                                              string_temp_stride,
                                              NULL,
                                              call_depth,
                                              out,
                                              cap,
                                              nested_type,
                                              sizeof(nested_type),
                                              nested_abi,
                                              sizeof(nested_abi))) {
                return false;
            }
        } else if (!v3_emit_index_access_address(plan,
                                                 lowering,
                                                 current_function,
                                                 aliases,
                                                 alias_count,
                                                 locals,
                                                 local_count,
                                                 base_expr,
                                                 nested_type,
                                                 sizeof(nested_type),
                                                 nested_abi,
                                                 sizeof(nested_abi),
                                                 addr_reg,
                                                 call_arg_base,
                                                 string_temp_base,
                                                 string_temp_stride,
                                                 call_depth + 1,
                                                 out,
                                                 cap) &&
                   !v3_emit_member_access_address(plan,
                                                  lowering,
                                                  current_function,
                                                  aliases,
                                                  alias_count,
                                                  locals,
                                                  local_count,
                                                  base_expr,
                                                  nested_type,
                                                  sizeof(nested_type),
                                                  nested_abi,
                                                  sizeof(nested_abi),
                                                  addr_reg,
                                                  call_arg_base,
                                                  string_temp_base,
                                                  string_temp_stride,
                                                  call_depth + 1,
                                                  out,
                                                  cap) &&
                   !v3_emit_field_path_address(out,
                                               cap,
                                               locals,
                                               local_count,
                                               lowering,
                                               base_expr,
                                               nested_type,
                                               sizeof(nested_type),
                                               nested_abi,
                                               sizeof(nested_abi),
                                               addr_reg)) {
            return false;
        }
    }
    if (v3_is_internal_ref_type(base_type) ||
        (strcmp(base_abi, "ptr") == 0 &&
         (strcmp(normalized_base_type, "ptr") == 0 ||
          strcmp(normalized_base_type, "cstring") == 0 ||
          v3_type_text_strip_one_pointer_suffix(normalized_base_type, nested_type, sizeof(nested_type))))) {
        v3_emit_load_scalar_from_address(out, cap, addr_reg, 0, "ptr", addr_reg);
    }
    if (!v3_emit_add_address_offset(out, cap, addr_reg, addr_reg, field_offset)) {
        return false;
    }
    return true;
}

static bool v3_infer_expr_type(const V3SystemLinkPlanStub *plan,
                               const V3LoweringPlanStub *lowering,
                               const V3LoweredFunctionStub *current_function,
                               const V3ImportAlias *aliases,
                               size_t alias_count,
                               const V3AsmLocalSlot *locals,
                               size_t local_count,
                               const char *expr_text,
                               char *type_out,
                               size_t type_cap,
                               char *abi_out,
                               size_t abi_cap);
static bool v3_codegen_expr_scalar(const V3SystemLinkPlanStub *plan,
                                   const V3LoweringPlanStub *lowering,
                                   const V3LoweredFunctionStub *current_function,
                                   const V3ImportAlias *aliases,
                                   size_t alias_count,
                                   const V3AsmLocalSlot *locals,
                                   size_t local_count,
                                   const char *expr_text,
                                   const char *expected_abi,
                                   int target_reg,
                                   int32_t call_arg_base,
                                   int32_t string_temp_base,
                                   int32_t string_temp_stride,
                                   int call_depth,
                                   char *out,
                                   size_t cap);
static bool v3_parse_named_arg_meta(const char *arg_text,
                                    char *field_out,
                                    size_t field_cap,
                                    char *expr_out,
                                    size_t expr_cap);
static bool v3_find_duplicate_named_arg_field(const char args[][4096],
                                              size_t arg_count,
                                              char *field_out,
                                              size_t field_cap,
                                              bool *valid_out);
static bool v3_named_arg_fields_unique(const char args[][4096], size_t arg_count);
static bool v3_tuple_args_cover_all_items(const char *tuple_type_text,
                                          const char args[][4096],
                                          size_t arg_count);
static V3FieldDefaultExprKind v3_classify_field_default_expr(const char *expr_text,
                                                             const V3TypeFieldDef *fields,
                                                             size_t field_count,
                                                             char *detail_out,
                                                             size_t detail_cap);
static void v3_report_invalid_field_default(const char *source_path,
                                            const char *type_text,
                                            const char *field_name,
                                            const char *expr_text,
                                            V3FieldDefaultExprKind kind,
                                            const char *detail);
static bool v3_validate_type_field_defaults(const char *source_path,
                                            const char *type_text,
                                            const V3TypeFieldDef *fields,
                                            size_t field_count);

static bool v3_emit_index_access_address(const V3SystemLinkPlanStub *plan,
                                         const V3LoweringPlanStub *lowering,
                                         const V3LoweredFunctionStub *current_function,
                                         const V3ImportAlias *aliases,
                                         size_t alias_count,
                                         const V3AsmLocalSlot *locals,
                                         size_t local_count,
                                         const char *expr_text,
                                         char *field_type_out,
                                         size_t field_type_cap,
                                         char *field_abi_out,
                                         size_t field_abi_cap,
                                         int addr_reg,
                                         int32_t call_arg_base,
                                         int32_t string_temp_base,
                                         int32_t string_temp_stride,
                                         int call_depth,
                                         char *out,
                                         size_t cap) {
    char expr[4096];
    char base_expr[4096];
    char index_expr[4096];
    char base_type[256];
    char base_abi[32];
    char normalized_base_type[256];
    char elem_type[256];
    char elem_abi[32];
    V3TypeLayoutStub elem_layout;
    int32_t fixed_len = 0;
    int32_t local_index;
    int32_t elem_size = 0;
    int data_offset = -1;
    bool base_is_fixed_array = false;
    const bool target_is_linux_x86_64 =
        plan != NULL && v3_target_is_linux_x86_64(plan->target_triple);
    int base_reg = addr_reg == 13 ? 14 : 13;
    int index_reg = 11;
    int size_reg = 12;
    if (target_is_linux_x86_64) {
        const int temp_regs[] = {9, 10, 11, 8, 16, 17};
        size_t temp_i;
        base_reg = -1;
        index_reg = -1;
        size_reg = -1;
        for (temp_i = 0U; temp_i < sizeof(temp_regs) / sizeof(temp_regs[0]); ++temp_i) {
            if (temp_regs[temp_i] != addr_reg) {
                base_reg = temp_regs[temp_i];
                break;
            }
        }
        for (temp_i = 0U; temp_i < sizeof(temp_regs) / sizeof(temp_regs[0]); ++temp_i) {
            if (temp_regs[temp_i] != addr_reg && temp_regs[temp_i] != base_reg) {
                index_reg = temp_regs[temp_i];
                break;
            }
        }
        for (temp_i = 0U; temp_i < sizeof(temp_regs) / sizeof(temp_regs[0]); ++temp_i) {
            if (temp_regs[temp_i] != addr_reg &&
                temp_regs[temp_i] != base_reg &&
                temp_regs[temp_i] != index_reg) {
                size_reg = temp_regs[temp_i];
                break;
            }
        }
        if (base_reg < 0 || index_reg < 0 || size_reg < 0) {
            return false;
        }
    }
    v3_trim_copy_text(expr_text, expr, sizeof(expr));
    if (!v3_parse_index_access_expr(expr, base_expr, sizeof(base_expr), index_expr, sizeof(index_expr)) ||
        !v3_infer_expr_type(plan,
                            lowering,
                            current_function,
                            aliases,
                            alias_count,
                            locals,
                            local_count,
                            base_expr,
                            base_type,
                            sizeof(base_type),
                            base_abi,
                            sizeof(base_abi)) ||
        !v3_normalize_type_text(lowering,
                                current_function->owner_module_path,
                                aliases,
                                alias_count,
                                base_type,
                                normalized_base_type,
                                sizeof(normalized_base_type))) {
        return false;
    }
    if (strcmp(normalized_base_type, "Bytes") == 0) {
        snprintf(elem_type, sizeof(elem_type), "%s", "int32");
        snprintf(elem_abi, sizeof(elem_abi), "%s", "i32");
        elem_size = 1;
        data_offset = 0;
    } else if (strcmp(normalized_base_type, "str") == 0) {
        snprintf(elem_type, sizeof(elem_type), "%s", "char");
        snprintf(elem_abi, sizeof(elem_abi), "%s", "i32");
        elem_size = 1;
        data_offset = 0;
    } else if (strlen(normalized_base_type) > 2U &&
               strcmp(normalized_base_type + strlen(normalized_base_type) - 2U, "[]") == 0) {
        snprintf(elem_type,
                 sizeof(elem_type),
                 "%.*s",
                 (int)(strlen(normalized_base_type) - 2U),
                 normalized_base_type);
        snprintf(elem_abi, sizeof(elem_abi), "%s", v3_type_abi_class(elem_type));
        if (!v3_compute_type_layout_impl(lowering, elem_type, &elem_layout, 0U)) {
            return false;
        }
        elem_size = elem_layout.size;
        data_offset = 8;
    } else if (v3_parse_fixed_array_type(normalized_base_type, elem_type, sizeof(elem_type), &fixed_len)) {
        base_is_fixed_array = true;
        snprintf(elem_abi, sizeof(elem_abi), "%s", v3_type_abi_class(elem_type));
        if (!v3_compute_type_layout_impl(lowering, elem_type, &elem_layout, 0U)) {
            return false;
        }
        elem_size = elem_layout.size;
    } else {
        return false;
    }
    local_index = v3_find_local_slot(locals, local_count, base_expr);
    if (local_index >= 0) {
        if (v3_abi_class_scalar_or_ptr(locals[local_index].abi_class) &&
            strcmp(normalized_base_type, "Bytes") != 0 &&
            strcmp(normalized_base_type, "str") != 0) {
            return false;
        }
        if (!v3_emit_local_value_address(out, cap, &locals[local_index], base_reg)) {
            return false;
        }
    } else {
        char nested_type[256];
        char nested_abi[32];
        if (v3_emit_owner_global_var_address(plan,
                                             lowering,
                                             current_function,
                                             base_expr,
                                             nested_type,
                                             sizeof(nested_type),
                                             nested_abi,
                                             sizeof(nested_abi),
                                             base_reg,
                                             out,
                                             cap)) {
            /* already loaded */
        } else if (!v3_emit_member_access_address(plan,
                                                  lowering,
                                                  current_function,
                                                  aliases,
                                                  alias_count,
                                                  locals,
                                                  local_count,
                                                  base_expr,
                                                  nested_type,
                                                  sizeof(nested_type),
                                                  nested_abi,
                                                  sizeof(nested_abi),
                                                  base_reg,
                                                  call_arg_base,
                                                  string_temp_base,
                                                  string_temp_stride,
                                                  call_depth + 1,
                                                  out,
                                                  cap) &&
                   !v3_emit_index_access_address(plan,
                                                 lowering,
                                                 current_function,
                                                 aliases,
                                                 alias_count,
                                                 locals,
                                                 local_count,
                                                 base_expr,
                                                 nested_type,
                                                 sizeof(nested_type),
                                                 nested_abi,
                                                 sizeof(nested_abi),
                                                 base_reg,
                                                 call_arg_base,
                                                 string_temp_base,
                                                 string_temp_stride,
                                                 call_depth + 1,
                                                 out,
                                                 cap) &&
                   !v3_emit_field_path_address(out,
                                               cap,
                                               locals,
                                               local_count,
                                               lowering,
                                               base_expr,
                                               nested_type,
                                               sizeof(nested_type),
                                               nested_abi,
                                               sizeof(nested_abi),
                                               base_reg)) {
            return false;
        }
    }
    v3_emit_address_spill_store(out, cap, call_arg_base, call_depth, base_reg);
    if (!v3_codegen_expr_scalar(plan,
                                lowering,
                                current_function,
                                aliases,
                                alias_count,
                                locals,
                                local_count,
                                index_expr,
                                "i64",
                                index_reg,
                                call_arg_base,
                                string_temp_base,
                                string_temp_stride,
                                call_depth + 1,
                                out,
                                cap)) {
        return false;
    }
    v3_emit_address_spill_load(out, cap, call_arg_base, call_depth, base_reg);
    if (elem_size <= 0) {
        return false;
    }
    if (base_is_fixed_array || strcmp(normalized_base_type, "Bytes") == 0 || strcmp(normalized_base_type, "str") == 0 ||
        (strlen(normalized_base_type) > 2U &&
         strcmp(normalized_base_type + strlen(normalized_base_type) - 2U, "[]") == 0)) {
        if (base_is_fixed_array) {
            if (!v3_emit_mov_imm(out, cap, size_reg, fixed_len)) {
                return false;
            }
        } else if (strcmp(normalized_base_type, "Bytes") == 0 || strcmp(normalized_base_type, "str") == 0) {
            v3_emit_load_scalar_from_address(out, cap, base_reg, 8, "i32", size_reg);
        } else {
            v3_emit_load_scalar_from_address(out, cap, base_reg, 0, "i32", size_reg);
        }
        v3_emit_address_spill_store(out, cap, call_arg_base, call_depth, base_reg);
        v3_emit_index_spill_store(out, cap, call_arg_base, call_depth, index_reg);
        if (target_is_linux_x86_64) {
            v3_emit_x64_prepare_edi_esi_d_d(out, cap, size_reg, index_reg);
        } else {
            v3_text_appendf(out, cap, "  mov w0, w%d\n", size_reg);
            v3_text_appendf(out, cap, "  mov w1, w%d\n", index_reg);
        }
        v3_emit_call_named_symbol(plan, out, cap, "cheng_bounds_check");
        v3_emit_address_spill_load(out, cap, call_arg_base, call_depth, base_reg);
        v3_emit_index_spill_load(out, cap, call_arg_base, call_depth, index_reg);
    }
    if (data_offset >= 0) {
        v3_emit_load_scalar_from_address(out, cap, base_reg, data_offset, "ptr", base_reg);
    }
    if (elem_size != 1) {
        if (!v3_emit_mov_imm(out, cap, size_reg, elem_size)) {
            return false;
        }
        if (target_is_linux_x86_64) {
            v3_text_appendf(out, cap, "  imulq %s, %s\n",
                            v3_x64_qreg_name(size_reg),
                            v3_x64_qreg_name(index_reg));
        } else {
            v3_text_appendf(out, cap, "  mul x%d, x%d, x%d\n", index_reg, index_reg, size_reg);
        }
    }
    if (target_is_linux_x86_64) {
        if (addr_reg != base_reg) {
            v3_text_appendf(out, cap, "  movq %s, %s\n",
                            v3_x64_qreg_name(base_reg),
                            v3_x64_qreg_name(addr_reg));
        }
        v3_text_appendf(out, cap, "  addq %s, %s\n",
                        v3_x64_qreg_name(index_reg),
                        v3_x64_qreg_name(addr_reg));
    } else {
        v3_text_appendf(out, cap, "  add x%d, x%d, x%d\n", addr_reg, base_reg, index_reg);
    }
    if (field_type_out) {
        snprintf(field_type_out, field_type_cap, "%s", elem_type);
    }
    if (field_abi_out) {
        snprintf(field_abi_out, field_abi_cap, "%s", elem_abi);
    }
    return true;
}

static bool v3_infer_lvalue_expr_type(const V3SystemLinkPlanStub *plan,
                                      const V3LoweringPlanStub *lowering,
                                      const V3LoweredFunctionStub *current_function,
                                      const V3ImportAlias *aliases,
                                      size_t alias_count,
                                      const V3AsmLocalSlot *locals,
                                      size_t local_count,
                                      const char *expr_text,
                                      char *field_type_out,
                                      size_t field_type_cap,
                                      char *field_abi_out,
                                      size_t field_abi_cap) {
    char base_expr[4096];
    char field_name[128];
    char index_expr[4096];
    char base_type[256];
    char base_abi[32];
    char normalized_base_type[256];
    int32_t field_offset = 0;
    int32_t base_local_index = -1;
    int32_t fixed_len = 0;
    if (v3_parse_deref_expr(expr_text, base_expr, sizeof(base_expr)) &&
        v3_infer_expr_type(plan,
                           lowering,
                           current_function,
                           aliases,
                           alias_count,
                           locals,
                           local_count,
                           base_expr,
                           base_type,
                           sizeof(base_type),
                           base_abi,
                           sizeof(base_abi)) &&
        strcmp(base_abi, "ptr") == 0 &&
        v3_resolve_pointer_pointee_type(lowering,
                                        current_function->owner_module_path,
                                        aliases,
                                        alias_count,
                                        base_type,
                                        field_type_out,
                                        field_type_cap,
                                        field_abi_out,
                                        field_abi_cap)) {
        return true;
    }
    if (v3_resolve_field_path_expr(lowering,
                                   locals,
                                   local_count,
                                   expr_text,
                                   &base_local_index,
                                   field_type_out,
                                   field_type_cap,
                                   field_abi_out,
                                   field_abi_cap,
                                   &field_offset)) {
        return true;
    }
    base_local_index = v3_find_local_slot(locals, local_count, expr_text);
    if (base_local_index >= 0) {
        snprintf(field_type_out, field_type_cap, "%s", locals[base_local_index].type_text);
        snprintf(field_abi_out, field_abi_cap, "%s", locals[base_local_index].abi_class);
        return true;
    }
    if (v3_find_top_level_var_decl(plan,
                                   lowering,
                                   current_function->owner_module_path,
                                   expr_text,
                                   field_type_out,
                                   field_type_cap,
                                   field_abi_out,
                                   field_abi_cap)) {
        return true;
    }
    if (v3_parse_member_access_expr(expr_text,
                                    base_expr,
                                    sizeof(base_expr),
                                    field_name,
                                    sizeof(field_name)) &&
        v3_infer_expr_type(plan,
                           lowering,
                           current_function,
                           aliases,
                           alias_count,
                           locals,
                           local_count,
                           base_expr,
                           base_type,
                           sizeof(base_type),
                           base_abi,
                           sizeof(base_abi)) &&
        v3_resolve_field_meta(lowering,
                              base_type,
                              field_name,
                              field_type_out,
                              field_type_cap,
                              field_abi_out,
                              field_abi_cap,
                              &field_offset)) {
        return true;
    }
    if (!v3_parse_index_access_expr(expr_text,
                                    base_expr,
                                    sizeof(base_expr),
                                    index_expr,
                                    sizeof(index_expr)) ||
        !v3_infer_expr_type(plan,
                            lowering,
                            current_function,
                            aliases,
                            alias_count,
                            locals,
                            local_count,
                            base_expr,
                            base_type,
                            sizeof(base_type),
                            base_abi,
                            sizeof(base_abi)) ||
        !v3_normalize_type_text(lowering,
                                current_function->owner_module_path,
                                aliases,
                                alias_count,
                                base_type,
                                normalized_base_type,
                                sizeof(normalized_base_type))) {
        return false;
    }
    if (strcmp(normalized_base_type, "Bytes") == 0) {
        snprintf(field_type_out, field_type_cap, "%s", "int32");
        snprintf(field_abi_out, field_abi_cap, "%s", "i32");
        return true;
    }
    if (strcmp(normalized_base_type, "str") == 0) {
        snprintf(field_type_out, field_type_cap, "%s", "char");
        snprintf(field_abi_out, field_abi_cap, "%s", "i32");
        return true;
    }
    if (strlen(normalized_base_type) > 2U &&
        strcmp(normalized_base_type + strlen(normalized_base_type) - 2U, "[]") == 0) {
        snprintf(field_type_out,
                 field_type_cap,
                 "%.*s",
                 (int)(strlen(normalized_base_type) - 2U),
                 normalized_base_type);
        snprintf(field_abi_out, field_abi_cap, "%s", v3_type_abi_class(field_type_out));
        return true;
    }
    if (v3_parse_fixed_array_type(normalized_base_type,
                                  field_type_out,
                                  field_type_cap,
                                  &fixed_len)) {
        snprintf(field_abi_out, field_abi_cap, "%s", v3_type_abi_class(field_type_out));
        return true;
    }
    return false;
}

static bool v3_emit_lvalue_address(const V3SystemLinkPlanStub *plan,
                                   const V3LoweringPlanStub *lowering,
                                   const V3LoweredFunctionStub *current_function,
                                   const V3ImportAlias *aliases,
                                   size_t alias_count,
                                   const V3AsmLocalSlot *locals,
                                   size_t local_count,
                                   const char *expr_text,
                                   char *field_type_out,
                                   size_t field_type_cap,
                                   char *field_abi_out,
                                   size_t field_abi_cap,
                                   int addr_reg,
                                   int32_t call_arg_base,
                                   int32_t string_temp_base,
                                   int32_t string_temp_stride,
                                   int call_depth,
                                   char *out,
                                   size_t cap) {
    char deref_expr[4096];
    char pointer_type[256];
    char pointer_abi[32];
    if (v3_parse_deref_expr(expr_text, deref_expr, sizeof(deref_expr))) {
        int pointer_reg = addr_reg == 12 ? 11 : 12;
        if (!v3_infer_expr_type(plan,
                                lowering,
                                current_function,
                                aliases,
                                alias_count,
                                locals,
                                local_count,
                                deref_expr,
                                pointer_type,
                                sizeof(pointer_type),
                                pointer_abi,
                                sizeof(pointer_abi)) ||
            strcmp(pointer_abi, "ptr") != 0 ||
            !v3_resolve_pointer_pointee_type(lowering,
                                             current_function->owner_module_path,
                                             aliases,
                                             alias_count,
                                             pointer_type,
                                             field_type_out,
                                             field_type_cap,
                                             field_abi_out,
                                             field_abi_cap) ||
            !v3_codegen_expr_scalar(plan,
                                    lowering,
                                    current_function,
                                    aliases,
                                    alias_count,
                                    locals,
                                    local_count,
                                    deref_expr,
                                    "ptr",
                                    pointer_reg,
                                    call_arg_base,
                                    string_temp_base,
                                    string_temp_stride,
                                    call_depth + 1,
                                    out,
                                    cap)) {
            return false;
        }
        if (pointer_reg != addr_reg) {
            if (v3_active_target_is_linux_x86_64()) {
                v3_emit_x64_move_reg(out, cap, addr_reg, pointer_reg, "ptr");
            } else {
                v3_text_appendf(out, cap, "  mov x%d, x%d\n", addr_reg, pointer_reg);
            }
        }
        return true;
    }
    if (v3_emit_owner_global_var_address(plan,
                                         lowering,
                                         current_function,
                                         expr_text,
                                         field_type_out,
                                         field_type_cap,
                                         field_abi_out,
                                         field_abi_cap,
                                         addr_reg,
                                         out,
                                         cap) ||
        v3_emit_member_access_address(plan,
                                      lowering,
                                      current_function,
                                      aliases,
                                      alias_count,
                                      locals,
                                      local_count,
                                      expr_text,
                                      field_type_out,
                                      field_type_cap,
                                      field_abi_out,
                                      field_abi_cap,
                                      addr_reg,
                                      call_arg_base,
                                      string_temp_base,
                                      string_temp_stride,
                                      call_depth,
                                      out,
                                      cap) ||
        v3_emit_field_path_address(out,
                                   cap,
                                   locals,
                                   local_count,
                                   lowering,
                                   expr_text,
                                   field_type_out,
                                   field_type_cap,
                                   field_abi_out,
                                   field_abi_cap,
                                   addr_reg) ||
        v3_emit_index_access_address(plan,
                                     lowering,
                                     current_function,
                                     aliases,
                                     alias_count,
                                     locals,
                                     local_count,
                                     expr_text,
                                     field_type_out,
                                     field_type_cap,
                                     field_abi_out,
                                     field_abi_cap,
                                     addr_reg,
                                     call_arg_base,
                                     string_temp_base,
                                     string_temp_stride,
                                     call_depth,
                                     out,
                                     cap)) {
        return true;
    }
    return false;
}

typedef struct {
    char expr[8192];
    char outer[8192];
    char base_name[256];
    char field_name[128];
    char index_expr[4096];
    char if_cond[4096];
    char if_true[4096];
    char if_false[4096];
    char ternary_cond[4096];
    char ternary_true[4096];
    char ternary_false[4096];
    char named_field[128];
    char named_expr[4096];
    char callee[PATH_MAX];
    char arg_text[4096];
    char args[CHENG_V3_MAX_CALL_ARGS][4096];
} V3ExprPrepScratch;

static bool v3_prepare_expr_call_state(const V3SystemLinkPlanStub *plan,
                                       const V3LoweringPlanStub *lowering,
                                       const V3LoweredFunctionStub *current_function,
                                       const V3ImportAlias *aliases,
                                       size_t alias_count,
                                       V3AsmLocalSlot *locals,
                                       size_t *local_count,
                                       size_t local_cap,
                                       int32_t *next_offset_io,
                                       const char *expr_text,
                                       int call_depth,
                                       int32_t *max_call_depth_io);

static bool v3_prepare_expr_call_state_impl(const V3SystemLinkPlanStub *plan,
                                            const V3LoweringPlanStub *lowering,
                                            const V3LoweredFunctionStub *current_function,
                                            const V3ImportAlias *aliases,
                                            size_t alias_count,
                                            V3AsmLocalSlot *locals,
                                            size_t *local_count,
                                            size_t local_cap,
                                            int32_t *next_offset_io,
                                            const char *expr_text,
                                            int call_depth,
                                            int32_t *max_call_depth_io,
                                            V3ExprPrepScratch *scratch) {
    static const char *OPS[] = {
        "||", "&&", "==", "!=", "<=", ">=", "<<", ">>", "<", ">", "|", "^", "&", "+", "-", "*", "/", "%"
    };
#define expr (scratch->expr)
#define outer (scratch->outer)
#define base_name (scratch->base_name)
#define field_name (scratch->field_name)
#define index_expr (scratch->index_expr)
#define if_cond (scratch->if_cond)
#define if_true (scratch->if_true)
#define if_false (scratch->if_false)
#define ternary_cond (scratch->ternary_cond)
#define ternary_true (scratch->ternary_true)
#define ternary_false (scratch->ternary_false)
#define named_field (scratch->named_field)
#define named_expr (scratch->named_expr)
#define callee (scratch->callee)
#define arg_text (scratch->arg_text)
#define args (scratch->args)
    char surface_error[256];
    V3ExprSurface surface;
    size_t arg_count = 0U;
    size_t i;
    bool parsed_call_expr = false;
    v3_trim_copy_text(expr_text, expr, sizeof(expr));
    if (expr[0] == '\0') {
        return true;
    }
    if (v3_is_trivial_prepare_literal_text(expr)) {
        return true;
    }
    if (v3_parse_named_arg_meta(expr,
                                named_field,
                                sizeof(named_field),
                                named_expr,
                                sizeof(named_expr))) {
        return v3_prepare_expr_call_state(plan,
                                          lowering,
                                          current_function,
                                          aliases,
                                          alias_count,
                                          locals,
                                          local_count,
                                          local_cap,
                                          next_offset_io,
                                          named_expr,
                                          call_depth,
                                          max_call_depth_io);
    }
    if (!v3_classify_expr_surface(expr, &surface, surface_error, sizeof(surface_error))) {
        return false;
    }
    snprintf(expr, sizeof(expr), "%s", surface.text);
    if (surface.kind == V3_EXPR_SURFACE_IF || surface.kind == V3_EXPR_SURFACE_TERNARY) {
        return v3_prepare_expr_call_state(plan,
                                          lowering,
                                          current_function,
                                          aliases,
                                          alias_count,
                                          locals,
                                          local_count,
                                          local_cap,
                                          next_offset_io,
                                          surface.cond,
                                          call_depth,
                                          max_call_depth_io) &&
               v3_prepare_expr_call_state(plan,
                                          lowering,
                                          current_function,
                                          aliases,
                                          alias_count,
                                          locals,
                                          local_count,
                                          local_cap,
                                          next_offset_io,
                                          surface.when_true,
                                          call_depth,
                                          max_call_depth_io) &&
               v3_prepare_expr_call_state(plan,
                                          lowering,
                                          current_function,
                                          aliases,
                                          alias_count,
                                          locals,
                                          local_count,
                                          local_cap,
                                          next_offset_io,
                                          surface.when_false,
                                          call_depth,
                                          max_call_depth_io);
    }
    if (surface.kind == V3_EXPR_SURFACE_RANGE) {
        return v3_prepare_expr_call_state(plan,
                                          lowering,
                                          current_function,
                                          aliases,
                                          alias_count,
                                          locals,
                                          local_count,
                                          local_cap,
                                          next_offset_io,
                                          surface.when_true,
                                          call_depth,
                                          max_call_depth_io) &&
               v3_prepare_expr_call_state(plan,
                                          lowering,
                                          current_function,
                                          aliases,
                                          alias_count,
                                          locals,
                                          local_count,
                                          local_cap,
                                          next_offset_io,
                                          surface.when_false,
                                          call_depth,
                                          max_call_depth_io);
    }
    if (expr[0] == '!') {
        return v3_prepare_expr_call_state(plan,
                                          lowering,
                                          current_function,
                                          aliases,
                                          alias_count,
                                          locals,
                                          local_count,
                                          local_cap,
                                          next_offset_io,
                                          expr + 1,
                                          call_depth,
                                          max_call_depth_io);
    }
    if (expr[0] == '-' && expr[1] != '\0' &&
        !v3_parse_int_literal_text(expr, NULL)) {
        return v3_prepare_expr_call_state(plan,
                                          lowering,
                                          current_function,
                                          aliases,
                                          alias_count,
                                          locals,
                                          local_count,
                                          local_cap,
                                          next_offset_io,
                                          expr + 1,
                                          call_depth,
                                          max_call_depth_io);
    }
    if (v3_parse_address_of_expr(expr, base_name, sizeof(base_name)) ||
        v3_parse_deref_expr(expr, base_name, sizeof(base_name))) {
        return v3_prepare_expr_call_state(plan,
                                          lowering,
                                          current_function,
                                          aliases,
                                          alias_count,
                                          locals,
                                          local_count,
                                          local_cap,
                                          next_offset_io,
                                          base_name,
                                          call_depth,
                                          max_call_depth_io);
    }
    for (i = 0; i < sizeof(OPS) / sizeof(OPS[0]); ++i) {
        int32_t op_index = v3_find_top_level_binary_op_last(expr, OPS[i]);
        if (op_index >= 0) {
            char left[4096];
            char right[4096];
            int next_call_depth = call_depth + 1;
            snprintf(left, sizeof(left), "%.*s", op_index, expr);
            snprintf(right, sizeof(right), "%s", expr + op_index + strlen(OPS[i]));
            v3_trim_copy_text(left, left, sizeof(left));
            v3_trim_copy_text(right, right, sizeof(right));
            v3_update_max_i32(max_call_depth_io, next_call_depth);
            return v3_prepare_expr_call_state(plan,
                                              lowering,
                                              current_function,
                                              aliases,
                                              alias_count,
                                              locals,
                                              local_count,
                                              local_cap,
                                              next_offset_io,
                                              left,
                                              next_call_depth,
                                              max_call_depth_io) &&
                   v3_prepare_expr_call_state(plan,
                                              lowering,
                                              current_function,
                                              aliases,
                                              alias_count,
                                              locals,
                                              local_count,
                                              local_cap,
                                              next_offset_io,
                                              right,
                                              next_call_depth,
                                              max_call_depth_io);
        }
    }
    if (v3_find_expr_top_level_const(lowering,
                                     current_function->owner_module_path,
                                     aliases,
                                     alias_count,
                                     expr) != NULL) {
        return true;
    }
    if (v3_parse_call_text(expr, callee, sizeof(callee), args, &arg_count, CHENG_V3_MAX_CALL_ARGS) ||
        v3_parse_prefix_single_arg_call(expr, callee, sizeof(callee), arg_text, sizeof(arg_text))) {
        parsed_call_expr = true;
    }
    if (!parsed_call_expr &&
        v3_parse_member_access_expr(expr, base_name, sizeof(base_name), field_name, sizeof(field_name))) {
        return v3_prepare_expr_call_state(plan,
                                          lowering,
                                          current_function,
                                          aliases,
                                          alias_count,
                                          locals,
                                          local_count,
                                          local_cap,
                                          next_offset_io,
                                          base_name,
                                          call_depth,
                                          max_call_depth_io);
    }
    if (!parsed_call_expr &&
        v3_parse_index_access_expr(expr, base_name, sizeof(base_name), index_expr, sizeof(index_expr))) {
        return v3_prepare_expr_call_state(plan,
                                          lowering,
                                          current_function,
                                          aliases,
                                          alias_count,
                                          locals,
                                          local_count,
                                          local_cap,
                                          next_offset_io,
                                          base_name,
                                          call_depth,
                                          max_call_depth_io) &&
               v3_prepare_expr_call_state(plan,
                                          lowering,
                                          current_function,
                                          aliases,
                                          alias_count,
                                          locals,
                                          local_count,
                                          local_cap,
                                          next_offset_io,
                                              index_expr,
                                              call_depth,
                                              max_call_depth_io);
    }
    if (!parsed_call_expr &&
        v3_parse_list_literal_text(expr, args, &arg_count, CHENG_V3_MAX_CALL_ARGS)) {
        for (i = 0; i < arg_count; ++i) {
            if (!v3_prepare_expr_call_state(plan,
                                            lowering,
                                            current_function,
                                            aliases,
                                            alias_count,
                                            locals,
                                            local_count,
                                            local_cap,
                                            next_offset_io,
                                            args[i],
                                            call_depth,
                                            max_call_depth_io)) {
                return false;
            }
        }
        return true;
    }
    {
        char default_type[CHENG_V3_MAX_TYPE_TEXT];
        if (v3_parse_default_expr_text(expr, default_type, sizeof(default_type))) {
            return true;
        }
    }
    if (parsed_call_expr ||
        v3_parse_call_text(expr, callee, sizeof(callee), args, &arg_count, CHENG_V3_MAX_CALL_ARGS) ||
        v3_parse_prefix_single_arg_call(expr, callee, sizeof(callee), arg_text, sizeof(arg_text))) {
        V3AsmCallTarget target;
        bool has_target = false;
        bool is_constructor_call = false;
        V3ResultIntrinsicKind result_kind;
        int next_call_depth = call_depth + 1;
        is_constructor_call = v3_callee_is_constructor_like(lowering,
                                                            current_function->owner_module_path,
                                                            aliases,
                                                            alias_count,
                                                            callee);
        if (arg_count == 0U && arg_text[0] != '\0') {
            snprintf(args[0], sizeof(args[0]), "%s", arg_text);
            arg_count = 1U;
        }
        if (is_constructor_call && has_target == false) {
            bool has_named_fields = false;
            for (i = 0; i < arg_count; ++i) {
                if (v3_find_top_level_binary_op(args[i], ":") > 0) {
                    has_named_fields = true;
                    break;
                }
            }
            if (has_named_fields) {
                char duplicate_field[128];
                bool named_fields_valid = true;
                if (v3_find_duplicate_named_arg_field(args,
                                                      arg_count,
                                                      duplicate_field,
                                                      sizeof(duplicate_field),
                                                      &named_fields_valid)) {
                    fprintf(stderr,
                            "[cheng_v3_seed] duplicate constructor field function=%s callee=%s field=%s expr=%s\n",
                            current_function->symbol_text,
                            callee,
                            duplicate_field,
                            expr);
                    return false;
                }
                if (!named_fields_valid || !v3_named_arg_fields_unique(args, arg_count)) {
                    return false;
                }
            }
        }
        if (strcmp(callee, "new") == 0 && arg_count == 1U) {
            return true;
        }
        if (strcmp(callee, "sizeof") == 0 && arg_count == 1U) {
            return true;
        }
        {
            char cast_target_type[256];
            char cast_target_abi[32];
            if (arg_count == 1U &&
                v3_resolve_scalar_or_pointer_cast_target(lowering,
                                                         current_function->owner_module_path,
                                                         aliases,
                                                         alias_count,
                                                         callee,
                                                         cast_target_type,
                                                         sizeof(cast_target_type),
                                                         cast_target_abi,
                                                         sizeof(cast_target_abi))) {
                return v3_prepare_expr_call_state(plan,
                                                  lowering,
                                                  current_function,
                                                  aliases,
                                                  alias_count,
                                                  locals,
                                                  local_count,
                                                  local_cap,
                                                  next_offset_io,
                                                  args[0],
                                                  call_depth,
                                                  max_call_depth_io);
            }
        }
        if ((strcmp(callee, "int") == 0 ||
             strcmp(callee, "int64") == 0 ||
             strcmp(callee, "int32") == 0 ||
             strcmp(callee, "uint32") == 0 ||
             strcmp(callee, "uint64") == 0 ||
             strcmp(callee, "int16") == 0 ||
             strcmp(callee, "uint16") == 0 ||
             strcmp(callee, "int8") == 0 ||
             strcmp(callee, "uint8") == 0 ||
             strcmp(callee, "ptr") == 0 ||
             strcmp(callee, "cstring") == 0 ||
             strcmp(callee, "bool") == 0) && arg_count == 1U) {
            return v3_prepare_expr_call_state(plan,
                                              lowering,
                                              current_function,
                                              aliases,
                                              alias_count,
                                              locals,
                                              local_count,
                                              local_cap,
                                              next_offset_io,
                                              args[0],
                                              call_depth,
                                              max_call_depth_io);
        }
        v3_update_max_i32(max_call_depth_io, next_call_depth);
        result_kind = v3_resolve_result_intrinsic_kind(lowering,
                                                       current_function->owner_module_path,
                                                       aliases,
                                                       alias_count,
                                                       callee);
        has_target = v3_resolve_call_target_with_args(plan,
                                                      lowering,
                                                      current_function,
                                                      aliases,
                                                      alias_count,
                                                      locals,
                                                      *local_count,
                                                      callee,
                                                      (const char (*)[4096])args,
                                                      arg_count,
                                                      &target);
        for (i = 0; i < arg_count; ++i) {
            if (!v3_is_trivial_prepare_literal_text(args[i]) &&
                !v3_prepare_expr_call_state(plan,
                                            lowering,
                                            current_function,
                                            aliases,
                                            alias_count,
                                            locals,
                                            local_count,
                                            local_cap,
                                            next_offset_io,
                                            args[i],
                                            next_call_depth,
                                            max_call_depth_io)) {
                fprintf(stderr,
                        "[cheng_v3_seed] prepare call arg state failed caller=%s callee=%s arg_index=%zu expr=%s\n",
                        current_function->symbol_text,
                        callee,
                        i,
                        args[i]);
                return false;
            }
            if (is_constructor_call) {
                continue;
            }
            if ((result_kind == V3_RESULT_INTRINSIC_OK && arg_count == 1U) ||
                (result_kind == V3_RESULT_INTRINSIC_ERR && arg_count == 1U) ||
                (result_kind == V3_RESULT_INTRINSIC_ERR_INFO && arg_count == 1U) ||
                (result_kind == V3_RESULT_INTRINSIC_ERR_CODE &&
                 arg_count == 2U &&
                 i < 1U)) {
                continue;
            }
            if (has_target &&
                i < target.param_count &&
                target.param_is_var[i]) {
                continue;
            }
            if (has_target &&
                i < target.param_count &&
                v3_abi_class_scalar_or_ptr(target.param_abi_classes[i])) {
                continue;
            }
            if (!v3_prepare_composite_call_arg_temp(plan,
                                                    lowering,
                                                    current_function,
                                                    aliases,
                                                    alias_count,
                                                    locals,
                                                    local_count,
                                                    local_cap,
                                                    next_offset_io,
                                                    callee,
                                                    i,
                                                    args[i],
                                                    has_target && i < target.param_count
                                                        ? target.param_types[i]
                                                        : "",
                                                    has_target && i < target.param_count
                                                        ? target.param_abi_classes[i]
                                                        : "")) {
                fprintf(stderr,
                        "[cheng_v3_seed] prepare composite call arg temp failed caller=%s callee=%s arg_index=%zu expr=%s has_target=%d\n",
                        current_function->symbol_text,
                        callee,
                        i,
                        args[i],
                        has_target ? 1 : 0);
                return false;
            }
        }
        if ((result_kind == V3_RESULT_INTRINSIC_IS_ERR ||
             result_kind == V3_RESULT_INTRINSIC_IS_OK ||
             result_kind == V3_RESULT_INTRINSIC_VALUE ||
             result_kind == V3_RESULT_INTRINSIC_ERROR ||
             result_kind == V3_RESULT_INTRINSIC_ERROR_TEXT ||
             result_kind == V3_RESULT_INTRINSIC_ERROR_INFO_OF) &&
            arg_count == 1U &&
            !v3_prepare_composite_expr_temp(plan,
                                            lowering,
                                            current_function,
                                            aliases,
                                            alias_count,
                                            locals,
                                            local_count,
                                            local_cap,
                                            next_offset_io,
                                            args[0])) {
            return false;
        }
    }
#undef expr
#undef outer
#undef base_name
#undef field_name
#undef index_expr
#undef if_cond
#undef if_true
#undef if_false
#undef ternary_cond
#undef ternary_true
#undef ternary_false
#undef named_field
#undef named_expr
#undef callee
#undef arg_text
#undef args
    return true;
}

static bool v3_prepare_expr_call_state(const V3SystemLinkPlanStub *plan,
                                       const V3LoweringPlanStub *lowering,
                                       const V3LoweredFunctionStub *current_function,
                                       const V3ImportAlias *aliases,
                                       size_t alias_count,
                                       V3AsmLocalSlot *locals,
                                       size_t *local_count,
                                       size_t local_cap,
                                       int32_t *next_offset_io,
                                       const char *expr_text,
                                       int call_depth,
                                       int32_t *max_call_depth_io) {
    char surface_error[256];
    char trimmed_expr[8192];
    char expr_type[256];
    char expr_abi[32];
    char lvalue_type[256];
    char lvalue_abi[32];
    V3ExprSurface surface;
    if (!v3_classify_expr_surface(expr_text, &surface, surface_error, sizeof(surface_error))) {
        fprintf(stderr,
                "[cheng_v3_seed] expr surface classify failed function=%s expr=%s reason=%s\n",
                current_function ? current_function->symbol_text : "-",
                expr_text ? expr_text : "-",
                surface_error[0] != '\0' ? surface_error : "invalid expr");
        return false;
    }
    V3ExprPrepScratch *scratch = (V3ExprPrepScratch *)v3_xmalloc(sizeof(V3ExprPrepScratch));
    memset(scratch, 0, sizeof(V3ExprPrepScratch));
    bool ok = v3_prepare_expr_call_state_impl(plan,
                                              lowering,
                                              current_function,
                                              aliases,
                                              alias_count,
                                              locals,
                                              local_count,
                                              local_cap,
                                              next_offset_io,
                                              surface.text,
                                              call_depth,
                                              max_call_depth_io,
                                              scratch);
    free(scratch);
    if (!ok) {
        return false;
    }
    if (surface.text[0] == '\0') {
        return true;
    }
    v3_trim_copy_text(surface.text, trimmed_expr, sizeof(trimmed_expr));
    if (trimmed_expr[0] == '\0' ||
        v3_find_local_slot(locals, *local_count, trimmed_expr) >= 0 ||
        v3_infer_lvalue_expr_type(plan,
                                  lowering,
                                  current_function,
                                  aliases,
                                  alias_count,
                                  locals,
                                  *local_count,
                                  trimmed_expr,
                                  lvalue_type,
                                  sizeof(lvalue_type),
                                  lvalue_abi,
                                  sizeof(lvalue_abi))) {
        return true;
    }
    if (!v3_infer_expr_type(plan,
                            lowering,
                            current_function,
                            aliases,
                            alias_count,
                            locals,
                            *local_count,
                            trimmed_expr,
                            expr_type,
                            sizeof(expr_type),
                            expr_abi,
                            sizeof(expr_abi)) ||
        v3_abi_class_scalar_or_ptr(expr_abi)) {
        return true;
    }
    if (!v3_prepare_composite_expr_temp(plan,
                                        lowering,
                                        current_function,
                                        aliases,
                                        alias_count,
                                        locals,
                                        local_count,
                                        local_cap,
                                        next_offset_io,
                                        trimmed_expr)) {
        return true;
    }
    return true;
}

static void v3_emit_reg_to_return(char *out,
                                  size_t cap,
                                  int src_reg_index,
                                  const char *abi_class) {
    if (v3_active_target_is_linux_x86_64()) {
        if (v3_abi_class_word_reg(abi_class)) {
            if (src_reg_index != 0) {
                v3_text_appendf(out, cap, "  movl %s, %%eax\n", v3_x64_dreg_name(src_reg_index));
            }
        } else if (strcmp(abi_class, "i64") == 0 || strcmp(abi_class, "ptr") == 0) {
            if (src_reg_index != 0) {
                v3_text_appendf(out, cap, "  movq %s, %%rax\n", v3_x64_qreg_name(src_reg_index));
            }
        }
        return;
    }
    if (v3_abi_class_word_reg(abi_class)) {
        v3_text_appendf(out, cap, "  mov w0, w%d\n", src_reg_index);
    } else if (strcmp(abi_class, "i64") == 0 || strcmp(abi_class, "ptr") == 0) {
        if (src_reg_index != 0) {
            v3_text_appendf(out, cap, "  mov x0, x%d\n", src_reg_index);
        }
    }
}

static bool v3_emit_sp_adjust(char *out, size_t cap, bool subtract, int32_t amount) {
    if (amount < 0) {
        return false;
    }
    if (amount == 0) {
        return true;
    }
    if (v3_active_target_is_linux_x86_64()) {
        v3_text_appendf(out, cap,
                        "  %sq $%d, %%rsp\n",
                        subtract ? "sub" : "add",
                        amount);
        return true;
    }
    if (amount <= 4095) {
        v3_text_appendf(out, cap, "  %s sp, sp, #%d\n", subtract ? "sub" : "add", amount);
        return true;
    }
    if (!v3_emit_mov_imm(out, cap, 17, amount)) {
        return false;
    }
    v3_text_appendf(out, cap, "  %s sp, sp, x17\n", subtract ? "sub" : "add");
    return true;
}

static bool v3_emit_sp_offset_address(char *out, size_t cap, int dst_reg, int32_t offset) {
    if (offset < 0) {
        return false;
    }
    if (v3_active_target_is_linux_x86_64()) {
        if (offset == 0) {
            v3_text_appendf(out, cap, "  movq %%rsp, %s\n", v3_x64_qreg_name(dst_reg));
        } else {
            v3_text_appendf(out, cap, "  leaq %d(%%rsp), %s\n", offset, v3_x64_qreg_name(dst_reg));
        }
        return true;
    }
    if (offset == 0) {
        v3_text_appendf(out, cap, "  mov x%d, sp\n", dst_reg);
        return true;
    }
    if (offset <= 4095) {
        v3_text_appendf(out, cap, "  add x%d, sp, #%d\n", dst_reg, offset);
        return true;
    }
    if (!v3_emit_mov_imm(out, cap, 17, offset)) {
        return false;
    }
    v3_text_appendf(out, cap, "  add x%d, sp, x17\n", dst_reg);
    return true;
}

static bool v3_emit_add_address_offset(char *out,
                                       size_t cap,
                                       int dst_reg,
                                       int src_reg,
                                       int32_t offset) {
    if (offset < 0) {
        return false;
    }
    if (v3_active_target_is_linux_x86_64()) {
        if (offset == 0) {
            v3_emit_x64_move_reg(out, cap, dst_reg, src_reg, "ptr");
        } else {
            v3_text_appendf(out, cap,
                            "  leaq %d(%s), %s\n",
                            offset,
                            v3_x64_qreg_name(src_reg),
                            v3_x64_qreg_name(dst_reg));
        }
        return true;
    }
    if (offset == 0) {
        if (dst_reg != src_reg) {
            v3_text_appendf(out, cap, "  mov x%d, x%d\n", dst_reg, src_reg);
        }
        return true;
    }
    if (offset <= 4095) {
        v3_text_appendf(out, cap, "  add x%d, x%d, #%d\n", dst_reg, src_reg, offset);
        return true;
    }
    if (!v3_emit_mov_imm(out, cap, 17, offset)) {
        return false;
    }
    v3_text_appendf(out, cap, "  add x%d, x%d, x17\n", dst_reg, src_reg);
    return true;
}

static bool v3_emit_sp_store_reg(char *out,
                                 size_t cap,
                                 int32_t offset,
                                 const char *abi_class,
                                 int src_reg) {
    if (v3_active_target_is_linux_x86_64()) {
        if (strcmp(abi_class, "i8") == 0 ||
            strcmp(abi_class, "u8") == 0 ||
            strcmp(abi_class, "bool") == 0) {
            v3_text_appendf(out, cap, "  movb %s, %d(%%rsp)\n", v3_x64_breg_name(src_reg), offset);
        } else if (strcmp(abi_class, "i16") == 0 ||
                   strcmp(abi_class, "u16") == 0) {
            v3_text_appendf(out, cap, "  movw %s, %d(%%rsp)\n", v3_x64_dreg_name(src_reg), offset);
        } else if (strcmp(abi_class, "i32") == 0) {
            v3_text_appendf(out, cap, "  movl %s, %d(%%rsp)\n", v3_x64_dreg_name(src_reg), offset);
        } else {
            v3_text_appendf(out, cap, "  movq %s, %d(%%rsp)\n", v3_x64_qreg_name(src_reg), offset);
        }
        return true;
    }
    if (!v3_emit_sp_offset_address(out, cap, 16, offset)) {
        return false;
    }
    v3_emit_store_scalar_to_address(out, cap, 16, 0, abi_class, src_reg);
    return true;
}

static bool v3_emit_sp_load_reg(char *out,
                                size_t cap,
                                int32_t offset,
                                const char *abi_class,
                                int dst_reg) {
    if (v3_active_target_is_linux_x86_64()) {
        if (strcmp(abi_class, "i8") == 0) {
            v3_text_appendf(out, cap, "  movsbq %d(%%rsp), %s\n", offset, v3_x64_qreg_name(dst_reg));
        } else if (strcmp(abi_class, "u8") == 0 ||
                   strcmp(abi_class, "bool") == 0) {
            v3_text_appendf(out, cap, "  movzbl %d(%%rsp), %s\n", offset, v3_x64_dreg_name(dst_reg));
        } else if (strcmp(abi_class, "i16") == 0) {
            v3_text_appendf(out, cap, "  movswq %d(%%rsp), %s\n", offset, v3_x64_qreg_name(dst_reg));
        } else if (strcmp(abi_class, "u16") == 0) {
            v3_text_appendf(out, cap, "  movzwl %d(%%rsp), %s\n", offset, v3_x64_dreg_name(dst_reg));
        } else if (strcmp(abi_class, "i32") == 0) {
            v3_text_appendf(out, cap, "  movslq %d(%%rsp), %s\n", offset, v3_x64_qreg_name(dst_reg));
        } else {
            v3_text_appendf(out, cap, "  movq %d(%%rsp), %s\n", offset, v3_x64_qreg_name(dst_reg));
        }
        return true;
    }
    if (!v3_emit_sp_offset_address(out, cap, 16, offset)) {
        return false;
    }
    v3_emit_load_scalar_from_address(out, cap, 16, 0, abi_class, dst_reg);
    return true;
}

static bool v3_emit_function_epilogue(char *out, size_t cap, int32_t frame_size) {
    if (v3_active_target_is_linux_x86_64()) {
        if (!v3_emit_sp_adjust(out, cap, false, frame_size)) {
            return false;
        }
        v3_text_append(out, cap,
                       "  popq %r15\n"
                       "  popq %r14\n"
                       "  popq %r13\n"
                       "  popq %r12\n"
                       "  popq %rbx\n"
                       "  popq %rbp\n"
                       "  retq\n");
        return true;
    }
    int32_t save_offset = frame_size - 16;
    if (save_offset < 0) {
        return false;
    }
    if (save_offset <= 504 && (save_offset % 8) == 0) {
        v3_text_appendf(out, cap, "  ldp x29, x30, [sp, #%d]\n", save_offset);
    } else if (!v3_emit_sp_offset_address(out, cap, 16, save_offset)) {
        return false;
    } else {
        v3_text_appendf(out, cap, "  ldp x29, x30, [x16]\n");
    }
    if (!v3_emit_sp_adjust(out, cap, false, frame_size)) {
        return false;
    }
    v3_text_appendf(out, cap, "  ret\n");
    return true;
}

static bool v3_validate_scalar_function_frame_layout(const V3LoweredFunctionStub *function,
                                                     int32_t max_call_depth,
                                                     int32_t call_arg_base,
                                                     int32_t string_temp_base,
                                                     int32_t string_temp_stride,
                                                     int32_t frame_size) {
    int32_t spill_layers = max_call_depth + 2;
    int32_t save_offset = frame_size - 16;
    int32_t call_arg_end;
    int32_t string_temp_end;
    if (spill_layers < 2) {
        spill_layers = 2;
    }
    call_arg_end = call_arg_base + spill_layers * CHENG_V3_CALL_ARG_SPILL_STRIDE;
    string_temp_end = string_temp_base;
    if (string_temp_stride > 0) {
        string_temp_end += string_temp_stride * spill_layers;
    }
    if (call_arg_end > string_temp_base) {
        fprintf(stderr,
                "[cheng_v3_seed] invalid frame layout function=%s call_args_overlap string_temp_base=%d call_arg_end=%d max_call_depth=%d frame_size=%d\n",
                function->symbol_text,
                string_temp_base,
                call_arg_end,
                max_call_depth,
                frame_size);
        return false;
    }
    if (string_temp_end + 16 > save_offset) {
        fprintf(stderr,
                "[cheng_v3_seed] invalid frame layout function=%s string_temps_overlap_save call_arg_base=%d string_temp_base=%d string_temp_stride=%d max_call_depth=%d string_temp_end=%d save_offset=%d frame_size=%d\n",
                function->symbol_text,
                call_arg_base,
                string_temp_base,
                string_temp_stride,
                max_call_depth,
                string_temp_end,
                save_offset,
                frame_size);
        return false;
    }
    return true;
}

static void v3_maybe_debug_scalar_function_frame_layout(const V3LoweredFunctionStub *function,
                                                        int32_t next_offset,
                                                        int32_t max_call_depth,
                                                        int32_t max_string_literal_temps,
                                                        int32_t call_arg_base,
                                                        int32_t string_temp_base,
                                                        int32_t string_temp_stride,
                                                        int32_t frame_size) {
    const char *needle = getenv("CHENG_V3_DEBUG_FRAME_LAYOUT");
    int32_t spill_layers = max_call_depth + 2;
    int32_t string_temp_end = string_temp_base;
    if (!needle || needle[0] == '\0') {
        return;
    }
    if (strcmp(needle, "1") != 0 &&
        strstr(function->symbol_text, needle) == NULL) {
        return;
    }
    if (spill_layers < 2) {
        spill_layers = 2;
    }
    if (string_temp_stride > 0) {
        string_temp_end += string_temp_stride * spill_layers;
    }
    fprintf(stderr,
            "[cheng_v3_seed] frame-debug function=%s next_offset=%d max_call_depth=%d max_string_literals=%d call_arg_base=%d string_temp_base=%d string_temp_stride=%d string_temp_end=%d frame_size=%d save_offset=%d\n",
            function->symbol_text,
            next_offset,
            max_call_depth,
            max_string_literal_temps,
            call_arg_base,
            string_temp_base,
            string_temp_stride,
            string_temp_end,
            frame_size,
            frame_size - 16);
}

static void v3_maybe_debug_scalar_function_locals(const V3LoweredFunctionStub *function,
                                                  const V3AsmLocalSlot *locals,
                                                  size_t local_count,
                                                  int32_t frame_size) {
    const char *needle = getenv("CHENG_V3_DEBUG_FRAME_LAYOUT");
    size_t i;
    if (!needle || needle[0] == '\0') {
        return;
    }
    if (strcmp(needle, "1") != 0 &&
        strstr(function->symbol_text, needle) == NULL) {
        return;
    }
    fprintf(stderr,
            "[cheng_v3_seed] locals-debug function=%s frame_size=%d save_offset=%d local_count=%zu\n",
            function->symbol_text,
            frame_size,
            frame_size - 16,
            local_count);
    for (i = 0; i < local_count; ++i) {
        fprintf(stderr,
                "[cheng_v3_seed] local[%zu] name=%s type=%s abi=%s indirect=%d offset=%d size=%d align=%d end=%d\n",
                i,
                locals[i].name,
                locals[i].type_text,
                locals[i].abi_class,
                locals[i].indirect_value ? 1 : 0,
                locals[i].stack_offset,
                locals[i].stack_size,
                locals[i].stack_align,
                locals[i].stack_offset + locals[i].stack_size);
    }
}

static bool v3_codegen_expr_scalar(const V3SystemLinkPlanStub *plan,
                                   const V3LoweringPlanStub *lowering,
                                   const V3LoweredFunctionStub *current_function,
                                   const V3ImportAlias *aliases,
                                   size_t alias_count,
                                   const V3AsmLocalSlot *locals,
                                   size_t local_count,
                                   const char *expr_text,
                                   const char *expected_abi,
                                   int target_reg,
                                   int32_t call_arg_base,
                                   int32_t string_temp_base,
                                   int32_t string_temp_stride,
                                   int call_depth,
                                   char *out,
                                   size_t cap);
static bool v3_parse_result_inner_type(const char *result_type,
                                       char *inner_type_out,
                                       size_t inner_type_cap);
static bool v3_emit_implicit_default_into_address(const V3SystemLinkPlanStub *plan,
                                                  const V3LoweringPlanStub *lowering,
                                                  const V3LoweredFunctionStub *current_function,
                                                  const V3ImportAlias *aliases,
                                                  size_t alias_count,
                                                  const char *type_owner_module_path,
                                                  const char *type_source_path,
                                                  const char *type_text,
                                                  int dest_addr_reg,
                                                  int32_t call_arg_base,
                                                  int32_t string_temp_base,
                                                  int32_t string_temp_stride,
                                                  int32_t *string_label_index_io,
                                                  int call_depth,
                                                  char *out,
                                                  size_t cap,
                                                  unsigned depth);
static bool v3_emit_result_field_address(const V3SystemLinkPlanStub *plan,
                                         const V3LoweringPlanStub *lowering,
                                         const V3LoweredFunctionStub *current_function,
                                         const V3ImportAlias *aliases,
                                         size_t alias_count,
                                         const V3AsmLocalSlot *locals,
                                         size_t local_count,
                                         const char *arg_expr,
                                         const char *field_name,
                                         int addr_reg,
                                         int32_t call_arg_base,
                                         int32_t string_temp_base,
                                         int32_t string_temp_stride,
                                         int32_t *string_label_index_io,
                                         int call_depth,
                                         char *out,
                                         size_t cap,
                                         char *field_type_out,
                                         size_t field_type_cap,
                                         char *field_abi_out,
                                         size_t field_abi_cap);
static bool v3_codegen_call_into_address(const V3SystemLinkPlanStub *plan,
                                         const V3LoweringPlanStub *lowering,
                                         const V3LoweredFunctionStub *current_function,
                                         const V3ImportAlias *aliases,
                                         size_t alias_count,
                                         const V3AsmLocalSlot *locals,
                                         size_t local_count,
                                         const char *callee,
                                         char args[][4096],
                                         size_t arg_count,
                                         int dest_addr_reg,
                                         int32_t call_arg_base,
                                         int32_t string_temp_base,
                                         int32_t string_temp_stride,
                                         int32_t *string_label_index_io,
                                         int call_depth,
                                         char *out,
                                         size_t cap);
static bool v3_materialize_composite_expr_into_slot(const V3SystemLinkPlanStub *plan,
                                                    const V3LoweringPlanStub *lowering,
                                                    const V3LoweredFunctionStub *current_function,
                                                    const V3ImportAlias *aliases,
                                                    size_t alias_count,
                                                    const V3AsmLocalSlot *locals,
                                                    size_t local_count,
                                                    const char *callee,
                                                    size_t arg_index,
                                                    const char *expr_text,
                                                    const V3AsmLocalSlot *dest_slot,
                                                    int32_t call_arg_base,
                                                    int32_t string_temp_base,
                                                    int32_t string_temp_stride,
                                                    int32_t *string_label_index_io,
                                                    int call_depth,
                                                    char *out,
                                                    size_t cap);
static bool v3_materialize_composite_expr_into_address(const V3SystemLinkPlanStub *plan,
                                                       const V3LoweringPlanStub *lowering,
                                                       const V3LoweredFunctionStub *current_function,
                                                       const V3ImportAlias *aliases,
                                                       size_t alias_count,
                                                       const V3AsmLocalSlot *locals,
                                                       size_t local_count,
                                                       const char *expr_text,
                                                       const char *dest_type_text,
                                                       int dest_addr_reg,
                                                       int32_t call_arg_base,
                                                       int32_t string_temp_base,
                                                       int32_t string_temp_stride,
                                                       int32_t *string_label_index_io,
                                                       int call_depth,
                                                       char *out,
                                                       size_t cap);
static bool v3_emit_slot_address(char *out,
                                 size_t cap,
                                 const V3AsmLocalSlot *slot,
                                 int reg_index);

static bool v3_infer_expr_type(const V3SystemLinkPlanStub *plan,
                               const V3LoweringPlanStub *lowering,
                               const V3LoweredFunctionStub *current_function,
                               const V3ImportAlias *aliases,
                               size_t alias_count,
                               const V3AsmLocalSlot *locals,
                               size_t local_count,
                               const char *expr_text,
                               char *type_out,
                               size_t type_cap,
                               char *abi_out,
                               size_t abi_cap) {
    static const char *OPS[] = {
        "||", "&&", "==", "!=", "<=", ">=", "<<", ">>", "<", ">", "|", "^", "&", "+", "-", "*", "/", "%"
    };
    char expr[8192];
    char callee[PATH_MAX];
    char arg_text[4096] = {0};
    char base_expr[4096];
    char index_expr[4096];
    char field_name[128];
    char surface_error[256];
    char left[4096];
    char right[4096];
    size_t i;
    int32_t local_index;
    int32_t char_value = 0;
    int64_t literal_value;
    V3ExprSurface surface;
    v3_trim_copy_text(expr_text, expr, sizeof(expr));
    if (expr[0] == '\0') {
        return false;
    }
    {
        char fmt_callee[PATH_MAX];
        char fmt_arg_text[4096];
        char fmt_literal[4096];
        const char *fmt_base = NULL;
        char (*fmt_args)[4096] = v3_alloc_call_arg_scratch();
        size_t fmt_arg_count = 0U;
        bool fmt_invocation = false;
        if (v3_parse_call_text(expr,
                               fmt_callee,
                               sizeof(fmt_callee),
                               fmt_args,
                               &fmt_arg_count,
                               CHENG_V3_MAX_CALL_ARGS) &&
            fmt_arg_count == 1U &&
            v3_parse_string_literal_text(fmt_args[0], fmt_literal, sizeof(fmt_literal))) {
            fmt_invocation = true;
        } else if (v3_parse_prefix_single_arg_call(expr,
                                                   fmt_callee,
                                                   sizeof(fmt_callee),
                                                   fmt_arg_text,
                                                   sizeof(fmt_arg_text)) &&
                   v3_parse_string_literal_text(fmt_arg_text, fmt_literal, sizeof(fmt_literal))) {
            fmt_invocation = true;
        }
        fmt_base = strrchr(fmt_callee, '.');
        fmt_base = fmt_base ? fmt_base + 1 : fmt_callee;
        if (fmt_invocation && v3_is_strformat_fmt_name(fmt_base)) {
            free(fmt_args);
            snprintf(type_out, type_cap, "%s", "str");
            snprintf(abi_out, abi_cap, "%s", "composite");
            return true;
        }
        free(fmt_args);
    }
    if (!v3_classify_expr_surface(expr, &surface, surface_error, sizeof(surface_error))) {
        fprintf(stderr,
                "[cheng_v3_seed] expr surface classify failed function=%s expr=%s reason=%s\n",
                current_function ? current_function->symbol_text : "-",
                expr,
                surface_error[0] != '\0' ? surface_error : "invalid expr");
        return false;
    }
    snprintf(expr, sizeof(expr), "%s", surface.text);
    if (expr[0] == '"' && expr[strlen(expr) - 1U] == '"') {
        snprintf(type_out, type_cap, "%s", "str");
        snprintf(abi_out, abi_cap, "%s", "composite");
        return true;
    }
    if (strcmp(expr, "true") == 0 || strcmp(expr, "false") == 0) {
        snprintf(type_out, type_cap, "%s", "bool");
        snprintf(abi_out, abi_cap, "%s", "bool");
        return true;
    }
    if (strcmp(expr, "nil") == 0) {
        snprintf(type_out, type_cap, "%s", "ptr");
        snprintf(abi_out, abi_cap, "%s", "ptr");
        return true;
    }
    if (v3_parse_char_literal_text(expr, &char_value)) {
        snprintf(type_out, type_cap, "%s", "char");
        snprintf(abi_out, abi_cap, "%s", "i32");
        return true;
    }
    if (v3_parse_int_literal_text(expr, &literal_value)) {
        if (literal_value >= INT32_MIN && literal_value <= INT32_MAX) {
            snprintf(type_out, type_cap, "%s", "int32");
            snprintf(abi_out, abi_cap, "%s", "i32");
        } else {
            snprintf(type_out, type_cap, "%s", "int64");
            snprintf(abi_out, abi_cap, "%s", "i64");
        }
        return true;
    }
    if (expr[0] == '!') {
        char inner_type[256];
        char inner_abi[32];
        if (!v3_infer_expr_type(plan,
                                lowering,
                                current_function,
                                aliases,
                                alias_count,
                                locals,
                                local_count,
                                expr + 1,
                                inner_type,
                                sizeof(inner_type),
                                inner_abi,
                                sizeof(inner_abi)) ||
            strcmp(inner_abi, "bool") != 0) {
            return false;
        }
        snprintf(type_out, type_cap, "%s", "bool");
        snprintf(abi_out, abi_cap, "%s", "bool");
        return true;
    }
    if (expr[0] == '-' && expr[1] != '\0' &&
        !v3_parse_int_literal_text(expr, NULL)) {
        if (!v3_infer_expr_type(plan,
                                lowering,
                                current_function,
                                aliases,
                                alias_count,
                                locals,
                                local_count,
                                expr + 1,
                                type_out,
                                type_cap,
                                abi_out,
                                abi_cap) ||
            !v3_abi_class_scalar_or_ptr(abi_out) ||
            strcmp(abi_out, "bool") == 0 ||
            strcmp(abi_out, "ptr") == 0) {
            return false;
        }
        return true;
    }
    if (v3_parse_address_of_expr(expr, base_expr, sizeof(base_expr))) {
        char pointee_type[256];
        char pointee_abi[32];
        char normalized_pointee_type[256];
        if (!v3_infer_lvalue_expr_type(plan,
                                       lowering,
                                       current_function,
                                       aliases,
                                       alias_count,
                                       locals,
                                       local_count,
                                       base_expr,
                                       pointee_type,
                                       sizeof(pointee_type),
                                       pointee_abi,
                                       sizeof(pointee_abi)) ||
            !v3_normalize_type_text(lowering,
                                    current_function->owner_module_path,
                                    aliases,
                                    alias_count,
                                    pointee_type,
                                    normalized_pointee_type,
                                    sizeof(normalized_pointee_type))) {
            return false;
        }
        snprintf(type_out, type_cap, "%s *", normalized_pointee_type);
        snprintf(abi_out, abi_cap, "%s", "ptr");
        return true;
    }
    if (v3_parse_deref_expr(expr, base_expr, sizeof(base_expr))) {
        char pointer_type[256];
        char pointer_abi[32];
        if (!v3_infer_expr_type(plan,
                                lowering,
                                current_function,
                                aliases,
                                alias_count,
                                locals,
                                local_count,
                                base_expr,
                                pointer_type,
                                sizeof(pointer_type),
                                pointer_abi,
                                sizeof(pointer_abi)) ||
            strcmp(pointer_abi, "ptr") != 0 ||
            !v3_resolve_pointer_pointee_type(lowering,
                                             current_function->owner_module_path,
                                             aliases,
                                             alias_count,
                                             pointer_type,
                                             type_out,
                                             type_cap,
                                             abi_out,
                                             abi_cap)) {
            return false;
        }
        return true;
    }
    if (surface.kind == V3_EXPR_SURFACE_IF) {
        char left_type[256];
        char left_abi[32];
        char right_type[256];
        char right_abi[32];
        char normalized_left_type[256];
        char normalized_right_type[256];
        if (!v3_infer_expr_type(plan,
                                lowering,
                                current_function,
                                aliases,
                                alias_count,
                                locals,
                                local_count,
                                surface.when_true,
                                left_type,
                                sizeof(left_type),
                                left_abi,
                                sizeof(left_abi)) ||
            !v3_infer_expr_type(plan,
                                lowering,
                                current_function,
                                aliases,
                                alias_count,
                                locals,
                                local_count,
                                surface.when_false,
                                right_type,
                                sizeof(right_type),
                                right_abi,
                                sizeof(right_abi)) ||
            !v3_normalize_type_text(lowering,
                                    current_function->owner_module_path,
                                    aliases,
                                    alias_count,
                                    left_type,
                                    normalized_left_type,
                                    sizeof(normalized_left_type)) ||
            !v3_normalize_type_text(lowering,
                                    current_function->owner_module_path,
                                    aliases,
                                    alias_count,
                                    right_type,
                                    normalized_right_type,
                                    sizeof(normalized_right_type)) ||
            strcmp(left_abi, right_abi) != 0 ||
            strcmp(normalized_left_type, normalized_right_type) != 0) {
            return false;
        }
        snprintf(type_out, type_cap, "%s", normalized_left_type);
        snprintf(abi_out, abi_cap, "%s", left_abi);
        return true;
    }
    if (surface.kind == V3_EXPR_SURFACE_TERNARY) {
        char left_type[256];
        char left_abi[32];
        char right_type[256];
        char right_abi[32];
        char normalized_left_type[256];
        char normalized_right_type[256];
        if (!v3_infer_expr_type(plan,
                                lowering,
                                current_function,
                                aliases,
                                alias_count,
                                locals,
                                local_count,
                                surface.when_true,
                                left_type,
                                sizeof(left_type),
                                left_abi,
                                sizeof(left_abi)) ||
            !v3_infer_expr_type(plan,
                                lowering,
                                current_function,
                                aliases,
                                alias_count,
                                locals,
                                local_count,
                                surface.when_false,
                                right_type,
                                sizeof(right_type),
                                right_abi,
                                sizeof(right_abi)) ||
            !v3_normalize_type_text(lowering,
                                    current_function->owner_module_path,
                                    aliases,
                                    alias_count,
                                    left_type,
                                    normalized_left_type,
                                    sizeof(normalized_left_type)) ||
            !v3_normalize_type_text(lowering,
                                    current_function->owner_module_path,
                                    aliases,
                                    alias_count,
                                    right_type,
                                    normalized_right_type,
                                    sizeof(normalized_right_type)) ||
            strcmp(normalized_left_type, normalized_right_type) != 0) {
            return false;
        }
        snprintf(type_out, type_cap, "%s", normalized_left_type);
        snprintf(abi_out, abi_cap, "%s", v3_type_abi_class(normalized_left_type));
        return true;
    }
    for (i = 0; i < sizeof(OPS) / sizeof(OPS[0]); ++i) {
        char left_type[256];
        char left_abi[32];
        char right_type[256];
        char right_abi[32];
        char normalized_left_type[256];
        char normalized_right_type[256];
        int32_t op_index = v3_find_top_level_binary_op_last(expr, OPS[i]);
        if (op_index < 0) {
            continue;
        }
        snprintf(left, sizeof(left), "%.*s", op_index, expr);
        snprintf(right, sizeof(right), "%s", expr + op_index + strlen(OPS[i]));
        v3_trim_copy_text(left, left, sizeof(left));
        v3_trim_copy_text(right, right, sizeof(right));
        if (!v3_infer_expr_type(plan,
                                lowering,
                                current_function,
                                aliases,
                                alias_count,
                                locals,
                                local_count,
                                left,
                                left_type,
                                sizeof(left_type),
                                left_abi,
                                sizeof(left_abi)) ||
            !v3_infer_expr_type(plan,
                                lowering,
                                current_function,
                                aliases,
                                alias_count,
                                locals,
                                local_count,
                                right,
                                right_type,
                                sizeof(right_type),
                                right_abi,
                                sizeof(right_abi))) {
            return false;
        }
        if (strcmp(OPS[i], "||") == 0 || strcmp(OPS[i], "&&") == 0 ||
            strcmp(OPS[i], "==") == 0 || strcmp(OPS[i], "!=") == 0 ||
            strcmp(OPS[i], "<") == 0 || strcmp(OPS[i], "<=") == 0 ||
            strcmp(OPS[i], ">") == 0 || strcmp(OPS[i], ">=") == 0) {
            snprintf(type_out, type_cap, "%s", "bool");
            snprintf(abi_out, abi_cap, "%s", "bool");
            return true;
        }
        if (!v3_normalize_type_text(lowering,
                                    current_function->owner_module_path,
                                    aliases,
                                    alias_count,
                                    left_type,
                                    normalized_left_type,
                                    sizeof(normalized_left_type)) ||
            !v3_normalize_type_text(lowering,
                                    current_function->owner_module_path,
                                    aliases,
                                    alias_count,
                                    right_type,
                                    normalized_right_type,
                                    sizeof(normalized_right_type))) {
            return false;
        }
        if (strcmp(OPS[i], "+") == 0 &&
            (strcmp(normalized_left_type, "str") == 0 ||
             strcmp(normalized_right_type, "str") == 0)) {
            snprintf(type_out, type_cap, "%s", "str");
            snprintf(abi_out, abi_cap, "%s", "composite");
            return true;
        }
        if (!v3_abi_class_scalar_or_ptr(left_abi) || !v3_abi_class_scalar_or_ptr(right_abi)) {
            return false;
        }
        if (strcmp(left_abi, "i64") == 0 || strcmp(right_abi, "i64") == 0 ||
            strcmp(normalized_left_type, "int64") == 0 || strcmp(normalized_right_type, "int64") == 0) {
            snprintf(type_out, type_cap, "%s", "int64");
            snprintf(abi_out, abi_cap, "%s", "i64");
        } else {
            snprintf(type_out, type_cap, "%s", "int32");
            snprintf(abi_out, abi_cap, "%s", "i32");
        }
        return true;
    }
    {
        const V3TopLevelConstStub *top_level_const =
            v3_find_expr_top_level_const(lowering,
                                         current_function->owner_module_path,
                                         aliases,
                                         alias_count,
                                         expr);
        if (top_level_const) {
            snprintf(type_out, type_cap, "%s", top_level_const->type_text);
            snprintf(abi_out, abi_cap, "%s", top_level_const->abi_class);
            return true;
        }
    }
    {
        size_t arg_count = 0U;
        char (*args)[4096] = v3_alloc_call_arg_scratch();
        if (v3_parse_list_literal_text(expr, args, &arg_count, CHENG_V3_MAX_CALL_ARGS)) {
            char elem_type[256];
            char elem_abi[32];
            char normalized_elem_type[256];
            size_t item_index;
            if (arg_count == 0U) {
                free(args);
                return false;
            }
            if (!v3_infer_expr_type(plan,
                                    lowering,
                                    current_function,
                                    aliases,
                                    alias_count,
                                    locals,
                                    local_count,
                                    args[0],
                                    elem_type,
                                    sizeof(elem_type),
                                    elem_abi,
                                    sizeof(elem_abi)) ||
                !v3_normalize_type_text(lowering,
                                        current_function->owner_module_path,
                                        aliases,
                                        alias_count,
                                        elem_type,
                                        normalized_elem_type,
                                        sizeof(normalized_elem_type))) {
                free(args);
                return false;
            }
            for (item_index = 1; item_index < arg_count; ++item_index) {
                char item_type[256];
                char item_abi[32];
                char normalized_item_type[256];
                if (!v3_infer_expr_type(plan,
                                        lowering,
                                        current_function,
                                        aliases,
                                        alias_count,
                                        locals,
                                        local_count,
                                        args[item_index],
                                        item_type,
                                        sizeof(item_type),
                                        item_abi,
                                        sizeof(item_abi)) ||
                    !v3_normalize_type_text(lowering,
                                            current_function->owner_module_path,
                                            aliases,
                                            alias_count,
                                            item_type,
                                            normalized_item_type,
                                            sizeof(normalized_item_type)) ||
                    strcmp(normalized_elem_type, normalized_item_type) != 0) {
                    free(args);
                    return false;
                }
            }
            free(args);
            snprintf(type_out, type_cap, "%s[]", normalized_elem_type);
            snprintf(abi_out, abi_cap, "%s", "composite");
            return true;
        }
        free(args);
    }
    if (v3_parse_index_access_expr(expr, base_expr, sizeof(base_expr), index_expr, sizeof(index_expr))) {
        char base_type[256];
        char base_abi[32];
        char normalized_base_type[256];
        V3AsmCallTarget target;
        int32_t fixed_len = 0;
        if (!v3_infer_expr_type(plan,
                                lowering,
                                current_function,
                                aliases,
                                alias_count,
                                locals,
                                local_count,
                                base_expr,
                                base_type,
                                sizeof(base_type),
                                base_abi,
                                sizeof(base_abi)) ||
            !v3_normalize_type_text(lowering,
                                    current_function->owner_module_path,
                                    aliases,
                                    alias_count,
                                    base_type,
                                    normalized_base_type,
                                    sizeof(normalized_base_type))) {
            return false;
        }
        if (strcmp(normalized_base_type, "Bytes") == 0) {
            snprintf(type_out, type_cap, "%s", "int32");
            snprintf(abi_out, abi_cap, "%s", "i32");
            return true;
        }
        if (strcmp(normalized_base_type, "str") == 0) {
            snprintf(type_out, type_cap, "%s", "char");
            snprintf(abi_out, abi_cap, "%s", "i32");
            return true;
        }
        if (strlen(normalized_base_type) > 2U &&
            strcmp(normalized_base_type + strlen(normalized_base_type) - 2U, "[]") == 0) {
            snprintf(type_out,
                     type_cap,
                     "%.*s",
                     (int)(strlen(normalized_base_type) - 2U),
                     normalized_base_type);
            snprintf(abi_out, abi_cap, "%s", v3_type_abi_class(type_out));
            return true;
        }
        if (v3_parse_fixed_array_type(normalized_base_type, type_out, type_cap, &fixed_len)) {
            snprintf(abi_out, abi_cap, "%s", v3_type_abi_class(type_out));
            return true;
        }
        if (!v3_resolve_index_access_call_target(plan,
                                                 lowering,
                                                 current_function,
                                                 aliases,
                                                 alias_count,
                                                 locals,
                                                 local_count,
                                                 base_expr,
                                                 index_expr,
                                                 &target)) {
            return false;
        }
        snprintf(type_out, type_cap, "%s", target.return_type);
        snprintf(abi_out, abi_cap, "%s", target.return_abi_class);
        return true;
    }
    if (v3_parse_member_access_expr(expr, base_expr, sizeof(base_expr), field_name, sizeof(field_name))) {
        char base_type[256];
        char base_abi[32];
        int32_t field_offset = 0;
        if (!v3_infer_expr_type(plan,
                                lowering,
                                current_function,
                                aliases,
                                alias_count,
                                locals,
                                local_count,
                                base_expr,
                                base_type,
                                sizeof(base_type),
                                base_abi,
                                sizeof(base_abi)) ||
            !v3_resolve_field_meta(lowering,
                                   base_type,
                                   field_name,
                                   type_out,
                                   type_cap,
                                   abi_out,
                                   abi_cap,
                                   &field_offset)) {
            return false;
        }
        return true;
    }
    if (v3_resolve_field_path_expr(lowering,
                                   locals,
                                   local_count,
                                   expr,
                                   &local_index,
                                   type_out,
                                   type_cap,
                                   abi_out,
                                   abi_cap,
                                   NULL)) {
        return true;
    }
    if (v3_parse_default_expr_text(expr, type_out, type_cap)) {
        char normalized_default_type[CHENG_V3_MAX_TYPE_TEXT];
        if (!v3_normalize_type_text(lowering,
                                    current_function->owner_module_path,
                                    aliases,
                                    alias_count,
                                    type_out,
                                    normalized_default_type,
                                    sizeof(normalized_default_type))) {
            return false;
        }
        snprintf(type_out, type_cap, "%s", normalized_default_type);
        snprintf(abi_out, abi_cap, "%s", v3_type_abi_class(normalized_default_type));
        return true;
    }
    local_index = v3_find_local_slot(locals, local_count, expr);
    if (local_index >= 0) {
        snprintf(type_out, type_cap, "%s", locals[local_index].type_text);
        snprintf(abi_out, abi_cap, "%s", locals[local_index].abi_class);
        return true;
    }
    {
        const V3TopLevelConstStub *top_level_const =
            v3_find_expr_top_level_const(lowering,
                                         current_function->owner_module_path,
                                         aliases,
                                         alias_count,
                                         expr);
        if (top_level_const) {
            snprintf(type_out, type_cap, "%s", top_level_const->type_text);
            snprintf(abi_out, abi_cap, "%s", top_level_const->abi_class);
            return true;
        }
    }
    if (v3_find_top_level_var_decl(plan,
                                   lowering,
                                   current_function->owner_module_path,
                                   expr,
                                   type_out,
                                   type_cap,
                                   abi_out,
                                   abi_cap)) {
        return true;
    }
    {
        size_t arg_count = 0U;
        char (*args)[4096] = v3_alloc_call_arg_scratch();
        if (v3_parse_call_text(expr, callee, sizeof(callee), args, &arg_count, CHENG_V3_MAX_CALL_ARGS)) {
            V3AsmCallTarget target;
            V3ResultIntrinsicKind result_kind =
                v3_resolve_result_intrinsic_kind(lowering,
                                                 current_function->owner_module_path,
                                                 aliases,
                                                 alias_count,
                                                 callee);
            bool has_named_fields = false;
            int32_t elem_size_value = 0;
            if (arg_count == 0U &&
                v3_resolve_bound_elem_size_value(lowering,
                                                 current_function,
                                                 aliases,
                                                 alias_count,
                                                 callee,
                                                 &elem_size_value)) {
                free(args);
                snprintf(type_out, type_cap, "%s", "int32");
                snprintf(abi_out, abi_cap, "%s", "i32");
                return true;
            }
            for (i = 0; i < arg_count; ++i) {
                if (v3_find_top_level_binary_op(args[i], ":") > 0) {
                    has_named_fields = true;
                    break;
                }
            }
            if (has_named_fields) {
                char duplicate_field[128];
                bool named_fields_valid = true;
                if (v3_find_duplicate_named_arg_field(args,
                                                      arg_count,
                                                      duplicate_field,
                                                      sizeof(duplicate_field),
                                                      &named_fields_valid)) {
                    fprintf(stderr,
                            "[cheng_v3_seed] duplicate constructor field function=%s callee=%s field=%s expr=%s\n",
                            current_function->symbol_text,
                            callee,
                            duplicate_field,
                            expr);
                    free(args);
                    return false;
                }
                if (!named_fields_valid || !v3_named_arg_fields_unique(args, arg_count)) {
                    free(args);
                    return false;
                }
            }
            if (v3_is_tuple_type_text(callee) &&
                !v3_tuple_args_cover_all_items(callee, args, arg_count)) {
                fprintf(stderr,
                        "[cheng_v3_seed] tuple literal omission unsupported function=%s type=%s expr=%s\n",
                        current_function->symbol_text,
                        callee,
                        expr);
                free(args);
                return false;
            }
            if ((result_kind == V3_RESULT_INTRINSIC_IS_ERR ||
                 result_kind == V3_RESULT_INTRINSIC_IS_OK) && arg_count == 1U) {
            char arg_type[256];
            char arg_abi[32];
            if (!v3_infer_expr_type(plan,
                                    lowering,
                                    current_function,
                                    aliases,
                                    alias_count,
                                    locals,
                                    local_count,
                                    args[0],
                                    arg_type,
                                    sizeof(arg_type),
                                    arg_abi,
                                    sizeof(arg_abi)) ||
                strcmp(arg_abi, "composite") != 0 ||
                !v3_startswith(arg_type, "Result[")) {
                free(args);
                return false;
            }
            free(args);
            snprintf(type_out, type_cap, "%s", "bool");
            snprintf(abi_out, abi_cap, "%s", "bool");
            return true;
            }
            if ((result_kind == V3_RESULT_INTRINSIC_VALUE ||
                 result_kind == V3_RESULT_INTRINSIC_ERROR ||
                 result_kind == V3_RESULT_INTRINSIC_ERROR_TEXT ||
                 result_kind == V3_RESULT_INTRINSIC_ERROR_INFO_OF) && arg_count == 1U) {
            char arg_type[256];
            char arg_abi[32];
            if (!v3_infer_expr_type(plan,
                                    lowering,
                                    current_function,
                                    aliases,
                                    alias_count,
                                    locals,
                                    local_count,
                                    args[0],
                                    arg_type,
                                    sizeof(arg_type),
                                    arg_abi,
                                    sizeof(arg_abi)) ||
                strcmp(arg_abi, "composite") != 0 ||
                !v3_startswith(arg_type, "Result[")) {
                free(args);
                return false;
            }
            if (result_kind == V3_RESULT_INTRINSIC_VALUE) {
                char inner_type[256];
                if (!v3_parse_result_inner_type(arg_type, inner_type, sizeof(inner_type))) {
                    free(args);
                    return false;
                }
                free(args);
                snprintf(type_out, type_cap, "%s", inner_type);
                snprintf(abi_out, abi_cap, "%s", v3_type_abi_class(inner_type));
                return true;
            }
            if (result_kind == V3_RESULT_INTRINSIC_ERROR_INFO_OF) {
                free(args);
                snprintf(type_out, type_cap, "%s", "std/result::ErrorInfo");
                snprintf(abi_out, abi_cap, "%s", "composite");
                return true;
            }
            free(args);
            snprintf(type_out, type_cap, "%s", "str");
            snprintf(abi_out, abi_cap, "%s", "composite");
            return true;
            }
            if (result_kind == V3_RESULT_INTRINSIC_OK && arg_count == 1U) {
            char value_type[256];
            char value_abi[32];
            char normalized_value_type[256];
            if (!v3_infer_expr_type(plan,
                                    lowering,
                                    current_function,
                                    aliases,
                                    alias_count,
                                    locals,
                                    local_count,
                                    args[0],
                                    value_type,
                                    sizeof(value_type),
                                    value_abi,
                                    sizeof(value_abi)) ||
                !v3_normalize_type_text(lowering,
                                        current_function->owner_module_path,
                                        aliases,
                                        alias_count,
                                        value_type,
                                        normalized_value_type,
                                        sizeof(normalized_value_type))) {
                free(args);
                return false;
            }
            free(args);
            snprintf(type_out, type_cap, "Result[%s]", normalized_value_type);
            snprintf(abi_out, abi_cap, "%s", "composite");
            return true;
            }
            if ((result_kind == V3_RESULT_INTRINSIC_ERR && arg_count == 1U) ||
                (result_kind == V3_RESULT_INTRINSIC_ERR_CODE && arg_count == 2U) ||
                (result_kind == V3_RESULT_INTRINSIC_ERR_INFO && arg_count == 1U)) {
            char explicit_inner_type[256];
            char normalized_inner_type[256];
            if (!v3_extract_trailing_type_arg_text(callee,
                                                   explicit_inner_type,
                                                   sizeof(explicit_inner_type)) ||
                !v3_normalize_type_text(lowering,
                                        current_function->owner_module_path,
                                        aliases,
                                        alias_count,
                                        explicit_inner_type,
                                        normalized_inner_type,
                                        sizeof(normalized_inner_type))) {
                free(args);
                return false;
            }
            free(args);
            snprintf(type_out, type_cap, "Result[%s]", normalized_inner_type);
            snprintf(abi_out, abi_cap, "%s", "composite");
            return true;
            }
            if (strcmp(callee, "len") == 0 && arg_count == 1U) {
            char arg_type[256];
            char arg_abi[32];
            if (!v3_infer_expr_type(plan,
                                    lowering,
                                    current_function,
                                    aliases,
                                    alias_count,
                                    locals,
                                    local_count,
                                    args[0],
                                    arg_type,
                                    sizeof(arg_type),
                                    arg_abi,
                                    sizeof(arg_abi))) {
                free(args);
                return false;
            }
            if (strcmp(arg_type, "str") != 0 &&
                strcmp(arg_type, "Bytes") != 0 &&
                !(strlen(arg_type) > 2U && strcmp(arg_type + strlen(arg_type) - 2U, "[]") == 0)) {
                free(args);
                return false;
            }
            free(args);
            snprintf(type_out, type_cap, "%s", "int32");
            snprintf(abi_out, abi_cap, "%s", "i32");
            return true;
            }
            if (strcmp(callee, "sizeof") == 0 && arg_count == 1U) {
            int32_t sizeof_value = 0;
            if (!v3_resolve_sizeof_expr_value(lowering,
                                              current_function->owner_module_path,
                                              aliases,
                                              alias_count,
                                              args[0],
                                              &sizeof_value)) {
                free(args);
                return false;
            }
            free(args);
            snprintf(type_out, type_cap, "%s", "int32");
            snprintf(abi_out, abi_cap, "%s", "i32");
            return true;
            }
            if (strcmp(callee, "new") == 0 && arg_count == 1U) {
            char alloc_type[256];
            if (!v3_normalize_type_text(lowering,
                                        current_function->owner_module_path,
                                        aliases,
                                        alias_count,
                                        args[0],
                                        alloc_type,
                                        sizeof(alloc_type)) ||
                !v3_is_internal_ref_type(alloc_type)) {
                free(args);
                return false;
            }
            free(args);
            snprintf(type_out, type_cap, "%s", alloc_type);
            snprintf(abi_out, abi_cap, "%s", "ptr");
            return true;
            }
            if (v3_is_strformat_join_name(callee) && arg_count == 1U) {
            char arg_type[256];
            char arg_abi[32];
            char normalized_arg_type[256];
            if (!v3_infer_expr_type(plan,
                                    lowering,
                                    current_function,
                                    aliases,
                                    alias_count,
                                    locals,
                                    local_count,
                                    args[0],
                                    arg_type,
                                    sizeof(arg_type),
                                    arg_abi,
                                    sizeof(arg_abi)) ||
                !v3_normalize_type_text(lowering,
                                        current_function->owner_module_path,
                                        aliases,
                                        alias_count,
                                        arg_type,
                                        normalized_arg_type,
                                        sizeof(normalized_arg_type)) ||
                strcmp(normalized_arg_type, "str[]") != 0) {
                free(args);
                return false;
            }
            free(args);
            snprintf(type_out, type_cap, "%s", "str");
            snprintf(abi_out, abi_cap, "%s", "composite");
            return true;
            }
            {
            char cast_target_type[256];
            char cast_target_abi[32];
            if (arg_count == 1U &&
                v3_resolve_scalar_or_pointer_cast_target(lowering,
                                                         current_function->owner_module_path,
                                                         aliases,
                                                         alias_count,
                                                         callee,
                                                         cast_target_type,
                                                         sizeof(cast_target_type),
                                                         cast_target_abi,
                                                         sizeof(cast_target_abi))) {
                free(args);
                snprintf(type_out, type_cap, "%s", cast_target_type);
                snprintf(abi_out, abi_cap, "%s", cast_target_abi);
                return true;
            }
            }
            if ((strcmp(callee, "int") == 0 ||
                 strcmp(callee, "int64") == 0 ||
                 strcmp(callee, "int32") == 0 ||
                 strcmp(callee, "uint32") == 0 ||
                 strcmp(callee, "uint64") == 0 ||
                 strcmp(callee, "ptr") == 0 ||
                 strcmp(callee, "cstring") == 0 ||
                 strcmp(callee, "bool") == 0) && arg_count == 1U) {
            if (strcmp(callee, "int") == 0 || strcmp(callee, "int32") == 0) {
                free(args);
                snprintf(type_out, type_cap, "%s", "int32");
                snprintf(abi_out, abi_cap, "%s", "i32");
            } else if (strcmp(callee, "uint32") == 0) {
                free(args);
                snprintf(type_out, type_cap, "%s", "uint32");
                snprintf(abi_out, abi_cap, "%s", "i32");
            } else if (strcmp(callee, "int64") == 0) {
                free(args);
                snprintf(type_out, type_cap, "%s", "int64");
                snprintf(abi_out, abi_cap, "%s", "i64");
            } else if (strcmp(callee, "uint64") == 0) {
                free(args);
                snprintf(type_out, type_cap, "%s", "uint64");
                snprintf(abi_out, abi_cap, "%s", "i64");
            } else if (strcmp(callee, "bool") == 0) {
                free(args);
                snprintf(type_out, type_cap, "%s", "bool");
                snprintf(abi_out, abi_cap, "%s", "bool");
            } else {
                free(args);
                snprintf(type_out, type_cap, "%s", strcmp(callee, "cstring") == 0 ? "cstring" : "ptr");
                snprintf(abi_out, abi_cap, "%s", "ptr");
            }
            return true;
            }
            if (v3_resolve_call_target_with_args(plan,
                                                 lowering,
                                                 current_function,
                                                 aliases,
                                                 alias_count,
                                                 locals,
                                                 local_count,
                                                 callee,
                                                 args,
                                                 arg_count,
                                                 &target)) {
            if (target.return_abi_class[0] == '\0' || strcmp(target.return_abi_class, "void") == 0) {
                free(args);
                return false;
            }
            if (target.return_type[0] != '\0') {
                snprintf(type_out, type_cap, "%s", target.return_type);
            } else {
                v3_type_text_from_abi_class(target.return_abi_class, type_out, type_cap);
            }
            free(args);
            snprintf(abi_out, abi_cap, "%s", target.return_abi_class);
            return type_out[0] != '\0';
            }
            if (v3_callee_is_constructor_like(lowering,
                                              current_function->owner_module_path,
                                              aliases,
                                              alias_count,
                                              callee)) {
                char normalized_ctor[256];
                if (!v3_normalize_type_text(lowering,
                                            current_function->owner_module_path,
                                            aliases,
                                            alias_count,
                                            callee,
                                            normalized_ctor,
                                            sizeof(normalized_ctor))) {
                    free(args);
                    return false;
                }
                free(args);
                snprintf(type_out, type_cap, "%s", normalized_ctor);
                snprintf(abi_out, abi_cap, "%s", v3_type_abi_class(normalized_ctor));
                return true;
            }
        }
        free(args);
    }
    if (v3_parse_prefix_single_arg_call(expr, callee, sizeof(callee), arg_text, sizeof(arg_text))) {
        V3AsmCallTarget target;
        V3ResultIntrinsicKind result_kind =
            v3_resolve_result_intrinsic_kind(lowering,
                                             current_function->owner_module_path,
                                             aliases,
                                             alias_count,
                                             callee);
        if (result_kind == V3_RESULT_INTRINSIC_IS_ERR ||
            result_kind == V3_RESULT_INTRINSIC_IS_OK) {
            char arg_type[256];
            char arg_abi[32];
            if (!v3_infer_expr_type(plan,
                                    lowering,
                                    current_function,
                                    aliases,
                                    alias_count,
                                    locals,
                                    local_count,
                                    arg_text,
                                    arg_type,
                                    sizeof(arg_type),
                                    arg_abi,
                                    sizeof(arg_abi)) ||
                strcmp(arg_abi, "composite") != 0 ||
                !v3_startswith(arg_type, "Result[")) {
                return false;
            }
            snprintf(type_out, type_cap, "%s", "bool");
            snprintf(abi_out, abi_cap, "%s", "bool");
            return true;
        }
        if (result_kind == V3_RESULT_INTRINSIC_VALUE ||
            result_kind == V3_RESULT_INTRINSIC_ERROR ||
            result_kind == V3_RESULT_INTRINSIC_ERROR_TEXT ||
            result_kind == V3_RESULT_INTRINSIC_ERROR_INFO_OF) {
            char arg_type[256];
            char arg_abi[32];
            if (!v3_infer_expr_type(plan,
                                    lowering,
                                    current_function,
                                    aliases,
                                    alias_count,
                                    locals,
                                    local_count,
                                    arg_text,
                                    arg_type,
                                    sizeof(arg_type),
                                    arg_abi,
                                    sizeof(arg_abi)) ||
                strcmp(arg_abi, "composite") != 0 ||
                !v3_startswith(arg_type, "Result[")) {
                return false;
            }
            if (result_kind == V3_RESULT_INTRINSIC_VALUE) {
                char inner_type[256];
                if (!v3_parse_result_inner_type(arg_type, inner_type, sizeof(inner_type))) {
                    return false;
                }
                snprintf(type_out, type_cap, "%s", inner_type);
                snprintf(abi_out, abi_cap, "%s", v3_type_abi_class(inner_type));
                return true;
            }
            if (result_kind == V3_RESULT_INTRINSIC_ERROR_INFO_OF) {
                snprintf(type_out, type_cap, "%s", "std/result::ErrorInfo");
                snprintf(abi_out, abi_cap, "%s", "composite");
                return true;
            }
            snprintf(type_out, type_cap, "%s", "str");
            snprintf(abi_out, abi_cap, "%s", "composite");
            return true;
        }
        if (result_kind == V3_RESULT_INTRINSIC_OK) {
            char value_type[256];
            char value_abi[32];
            char normalized_value_type[256];
            if (!v3_infer_expr_type(plan,
                                    lowering,
                                    current_function,
                                    aliases,
                                    alias_count,
                                    locals,
                                    local_count,
                                    arg_text,
                                    value_type,
                                    sizeof(value_type),
                                    value_abi,
                                    sizeof(value_abi)) ||
                !v3_normalize_type_text(lowering,
                                        current_function->owner_module_path,
                                        aliases,
                                        alias_count,
                                        value_type,
                                        normalized_value_type,
                                        sizeof(normalized_value_type))) {
                return false;
            }
            snprintf(type_out, type_cap, "Result[%s]", normalized_value_type);
            snprintf(abi_out, abi_cap, "%s", "composite");
            return true;
        }
        if ((result_kind == V3_RESULT_INTRINSIC_ERR ||
             result_kind == V3_RESULT_INTRINSIC_ERR_INFO) &&
            v3_extract_trailing_type_arg_text(callee, type_out, type_cap) &&
            v3_normalize_type_text(lowering,
                                   current_function->owner_module_path,
                                   aliases,
                                   alias_count,
                                   type_out,
                                   type_out,
                                   type_cap)) {
            snprintf(abi_out, abi_cap, "%s", "composite");
            {
                char normalized_inner_type[256];
                snprintf(normalized_inner_type, sizeof(normalized_inner_type), "%s", type_out);
                snprintf(type_out, type_cap, "Result[%s]", normalized_inner_type);
            }
            return true;
        }
        if (strcmp(callee, "len") == 0) {
            char arg_type[256];
            char arg_abi[32];
            if (!v3_infer_expr_type(plan,
                                    lowering,
                                    current_function,
                                    aliases,
                                    alias_count,
                                    locals,
                                    local_count,
                                    arg_text,
                                    arg_type,
                                    sizeof(arg_type),
                                    arg_abi,
                                    sizeof(arg_abi))) {
                return false;
            }
            if (strcmp(arg_type, "str") != 0 &&
                strcmp(arg_type, "Bytes") != 0 &&
                !(strlen(arg_type) > 2U && strcmp(arg_type + strlen(arg_type) - 2U, "[]") == 0)) {
                return false;
            }
            snprintf(type_out, type_cap, "%s", "int32");
            snprintf(abi_out, abi_cap, "%s", "i32");
            return true;
        }
        if (strcmp(callee, "sizeof") == 0) {
            int32_t sizeof_value = 0;
            if (!v3_resolve_sizeof_expr_value(lowering,
                                              current_function->owner_module_path,
                                              aliases,
                                              alias_count,
                                              arg_text,
                                              &sizeof_value)) {
                return false;
            }
            snprintf(type_out, type_cap, "%s", "int32");
            snprintf(abi_out, abi_cap, "%s", "i32");
            return true;
        }
        if (v3_is_strformat_join_name(callee)) {
            char arg_type[256];
            char arg_abi[32];
            char normalized_arg_type[256];
            if (!v3_infer_expr_type(plan,
                                    lowering,
                                    current_function,
                                    aliases,
                                    alias_count,
                                    locals,
                                    local_count,
                                    arg_text,
                                    arg_type,
                                    sizeof(arg_type),
                                    arg_abi,
                                    sizeof(arg_abi)) ||
                !v3_normalize_type_text(lowering,
                                        current_function->owner_module_path,
                                        aliases,
                                        alias_count,
                                        arg_type,
                                        normalized_arg_type,
                                        sizeof(normalized_arg_type)) ||
                strcmp(normalized_arg_type, "str[]") != 0) {
                return false;
            }
            snprintf(type_out, type_cap, "%s", "str");
            snprintf(abi_out, abi_cap, "%s", "composite");
            return true;
        }
        {
            char cast_target_type[256];
            char cast_target_abi[32];
            if (v3_resolve_scalar_or_pointer_cast_target(lowering,
                                                         current_function->owner_module_path,
                                                         aliases,
                                                         alias_count,
                                                         callee,
                                                         cast_target_type,
                                                         sizeof(cast_target_type),
                                                         cast_target_abi,
                                                         sizeof(cast_target_abi))) {
                snprintf(type_out, type_cap, "%s", cast_target_type);
                snprintf(abi_out, abi_cap, "%s", cast_target_abi);
                return true;
            }
        }
        if ((strcmp(callee, "int") == 0 ||
             strcmp(callee, "int32") == 0 ||
             strcmp(callee, "uint32") == 0 ||
             strcmp(callee, "int64") == 0 ||
             strcmp(callee, "uint64") == 0 ||
             strcmp(callee, "ptr") == 0 ||
             strcmp(callee, "cstring") == 0 ||
             strcmp(callee, "bool") == 0)) {
            if (strcmp(callee, "int") == 0 || strcmp(callee, "int32") == 0) {
                snprintf(type_out, type_cap, "%s", "int32");
                snprintf(abi_out, abi_cap, "%s", "i32");
            } else if (strcmp(callee, "uint32") == 0) {
                snprintf(type_out, type_cap, "%s", "uint32");
                snprintf(abi_out, abi_cap, "%s", "i32");
            } else if (strcmp(callee, "int64") == 0) {
                snprintf(type_out, type_cap, "%s", "int64");
                snprintf(abi_out, abi_cap, "%s", "i64");
            } else if (strcmp(callee, "uint64") == 0) {
                snprintf(type_out, type_cap, "%s", "uint64");
                snprintf(abi_out, abi_cap, "%s", "i64");
            } else if (strcmp(callee, "bool") == 0) {
                snprintf(type_out, type_cap, "%s", "bool");
                snprintf(abi_out, abi_cap, "%s", "bool");
            } else {
                snprintf(type_out, type_cap, "%s", strcmp(callee, "cstring") == 0 ? "cstring" : "ptr");
                snprintf(abi_out, abi_cap, "%s", "ptr");
            }
            return true;
        }
        {
            char single_arg[1][4096];
            snprintf(single_arg[0], sizeof(single_arg[0]), "%s", arg_text);
            if (v3_resolve_call_target_with_args(plan,
                                                 lowering,
                                                 current_function,
                                                 aliases,
                                                 alias_count,
                                                 locals,
                                                 local_count,
                                                 callee,
                                                 single_arg,
                                                 1U,
                                                 &target)) {
            if (target.return_abi_class[0] == '\0' || strcmp(target.return_abi_class, "void") == 0) {
                return false;
            }
            if (target.return_type[0] != '\0') {
                snprintf(type_out, type_cap, "%s", target.return_type);
            } else {
                v3_type_text_from_abi_class(target.return_abi_class, type_out, type_cap);
            }
            snprintf(abi_out, abi_cap, "%s", target.return_abi_class);
            return type_out[0] != '\0';
            }
        }
    }
    {
        V3AsmCallTarget function_target;
        if (strchr(expr, '(') == NULL &&
            strchr(expr, '[') == NULL &&
            strchr(expr, ' ') == NULL &&
            v3_resolve_call_target(plan,
                                   lowering,
                                   current_function,
                                   aliases,
                                   alias_count,
                                   expr,
                                   &function_target)) {
            snprintf(type_out, type_cap, "%s", "ptr");
            snprintf(abi_out, abi_cap, "%s", "ptr");
            return true;
        }
    }
    return false;
}

static bool v3_try_prepare_index_assignment_call_state(const V3SystemLinkPlanStub *plan,
                                                       const V3LoweringPlanStub *lowering,
                                                       const V3LoweredFunctionStub *current_function,
                                                       const V3ImportAlias *aliases,
                                                       size_t alias_count,
                                                       V3AsmLocalSlot *locals,
                                                       size_t *local_count,
                                                       size_t local_cap,
                                                       int32_t *next_offset_io,
                                                       const char *lhs_text,
                                                       const char *rhs_text,
                                                       int call_depth,
                                                       int32_t *max_call_depth_io,
                                                       bool *handled_out) {
    char args[3][4096];
    char base_type[256];
    char base_abi[32];
    char normalized_base_type[256];
    char fixed_elem_type[256];
    V3AsmCallTarget target;
    size_t i;
    int next_call_depth = call_depth + 1;
    bool has_target = false;
    if (handled_out != NULL) {
        *handled_out = false;
    }
    if (!v3_parse_index_assignment_operands(lhs_text,
                                            rhs_text,
                                            args[0],
                                            sizeof(args[0]),
                                            args[1],
                                            sizeof(args[1]),
                                            args[2],
                                            sizeof(args[2]))) {
        return true;
    }
    if (v3_infer_expr_type(plan,
                           lowering,
                           current_function,
                           aliases,
                           alias_count,
                           locals,
                           *local_count,
                           args[0],
                           base_type,
                           sizeof(base_type),
                           base_abi,
                           sizeof(base_abi)) &&
        v3_normalize_type_text(lowering,
                               current_function->owner_module_path,
                               aliases,
                               alias_count,
                               base_type,
                               normalized_base_type,
                               sizeof(normalized_base_type)) &&
        (strcmp(normalized_base_type, "Bytes") == 0 ||
         strcmp(normalized_base_type, "str") == 0 ||
         (strlen(normalized_base_type) > 2U &&
          strcmp(normalized_base_type + strlen(normalized_base_type) - 2U, "[]") == 0) ||
         v3_parse_fixed_array_type(normalized_base_type,
                                   fixed_elem_type,
                                   sizeof(fixed_elem_type),
                                   &(int32_t){0}))) {
        return true;
    }
    v3_update_max_i32(max_call_depth_io, next_call_depth);
    for (i = 0U; i < 3U; ++i) {
        if (!v3_prepare_expr_call_state(plan,
                                        lowering,
                                        current_function,
                                        aliases,
                                        alias_count,
                                        locals,
                                        local_count,
                                        local_cap,
                                        next_offset_io,
                                        args[i],
                                        next_call_depth,
                                        max_call_depth_io)) {
            return false;
        }
    }
    has_target =
        v3_resolve_call_target_with_args(plan,
                                         lowering,
                                         current_function,
                                         aliases,
                                         alias_count,
                                         locals,
                                         *local_count,
                                         "[]=",
                                         (const char (*)[4096])args,
                                         3U,
                                         &target);
    if (!has_target) {
        return true;
    }
    for (i = 0U; i < 3U; ++i) {
        if (i < target.param_count &&
            (target.param_is_var[i] ||
             v3_abi_class_scalar_or_ptr(target.param_abi_classes[i]))) {
            continue;
        }
        if (!v3_prepare_composite_call_arg_temp(plan,
                                                lowering,
                                                current_function,
                                                aliases,
                                                alias_count,
                                                locals,
                                                local_count,
                                                local_cap,
                                                next_offset_io,
                                                "[]=",
                                                i,
                                                args[i],
                                                i < target.param_count ? target.param_types[i] : "",
                                                i < target.param_count ? target.param_abi_classes[i] : "")) {
            return false;
        }
    }
    if (handled_out != NULL) {
        *handled_out = true;
    }
    return true;
}

static bool v3_codegen_call_scalar_resolved(const V3SystemLinkPlanStub *plan,
                                            const V3LoweringPlanStub *lowering,
                                            const V3LoweredFunctionStub *current_function,
                                            const V3ImportAlias *aliases,
                                            size_t alias_count,
                                            const V3AsmLocalSlot *locals,
                                            size_t local_count,
                                            const char *callee,
                                            char args[][4096],
                                            size_t arg_count,
                                            const V3AsmCallTarget *resolved_target,
                                            const char *expected_abi,
                                            int target_reg,
                                            int32_t call_arg_base,
                                            int32_t string_temp_base,
                                            int32_t string_temp_stride,
                                            int32_t *string_label_index_io,
                                            int call_depth,
                                            char *out,
                                            size_t cap) {
    V3AsmCallTarget target;
    size_t i;
    int32_t ffi_handle_consume_index = -1;
    if (resolved_target != NULL) {
        memcpy(&target, resolved_target, sizeof(target));
    } else if (!v3_resolve_call_target_with_args(plan,
                                                 lowering,
                                                 current_function,
                                                 aliases,
                                                 alias_count,
                                                 locals,
                                                 local_count,
                                                 callee,
                                                 (const char (*)[4096])args,
                                                 arg_count,
                                                 &target)) {
        fprintf(stderr,
                "[cheng_v3_seed] scalar call resolve failed caller=%s callee=%s\n",
                current_function->symbol_text,
                callee);
        return false;
    }
    if (!v3_abi_class_scalar_or_ptr(target.return_abi_class)) {
        fprintf(stderr,
                "[cheng_v3_seed] scalar call non-scalar return caller=%s callee=%s ret_abi=%s\n",
                current_function->symbol_text,
                callee,
                target.return_abi_class);
        return false;
    }
    if (target.param_count != arg_count || target.param_count > CHENG_V3_MAX_CALL_ARGS) {
        fprintf(stderr,
                "[cheng_v3_seed] scalar call param mismatch caller=%s callee=%s expected=%zu got=%zu\n",
                current_function->symbol_text,
                callee,
                target.param_count,
                arg_count);
        return false;
    }
    ffi_handle_consume_index = target.ffi_handle_consume_index;
    for (i = 0; i < arg_count; ++i) {
        if (target.param_is_var[i]) {
            char lvalue_type[256];
            char lvalue_abi[32];
            int32_t local_index = v3_find_local_slot(locals, local_count, args[i]);
            if (local_index >= 0) {
                if (!v3_emit_local_value_address(out, cap, &locals[local_index], 15)) {
                    fprintf(stderr,
                            "[cheng_v3_seed] scalar call var-arg address failed caller=%s callee=%s arg_index=%zu local=%s\n",
                            current_function->symbol_text,
                            callee,
                            i,
                            locals[local_index].name);
                    return false;
                }
            } else if (!v3_emit_lvalue_address(plan,
                                               lowering,
                                               current_function,
                                               aliases,
                                               alias_count,
                                               locals,
                                               local_count,
                                               args[i],
                                               lvalue_type,
                                               sizeof(lvalue_type),
                                               lvalue_abi,
                                               sizeof(lvalue_abi),
                                               15,
                                               call_arg_base,
                                               string_temp_base,
                                               string_temp_stride,
                                               call_depth + 1,
                                               out,
                                               cap)) {
                fprintf(stderr,
                        "[cheng_v3_seed] scalar call var-arg lvalue failed caller=%s callee=%s arg_index=%zu expr=%s\n",
                        current_function->symbol_text,
                        callee,
                        i,
                        args[i]);
                return false;
            }
            v3_emit_call_arg_spill_store(out,
                                         cap,
                                         call_arg_base,
                                         call_depth,
                                         i,
                                         "ptr",
                                         15);
            continue;
        }
        if (target.ffi_handle_enabled && (int32_t)i == ffi_handle_consume_index) {
            if (!v3_codegen_expr_scalar(plan,
                                        lowering,
                                        current_function,
                                        aliases,
                                        alias_count,
                                        locals,
                                        local_count,
                                        args[i],
                                        target.param_abi_classes[i],
                                        9,
                                        call_arg_base,
                                        string_temp_base,
                                        string_temp_stride,
                                        call_depth + 1,
                                        out,
                                        cap)) {
                fprintf(stderr,
                        "[cheng_v3_seed] scalar call ffi_handle consume arg emit failed caller=%s callee=%s arg_index=%zu expr=%s abi=%s\n",
                        current_function->symbol_text,
                        callee,
                        i,
                        args[i],
                        target.param_abi_classes[i]);
                return false;
            }
            v3_emit_call_dest_spill_store_abi(out,
                                              cap,
                                              call_arg_base,
                                              call_depth,
                                              target.param_abi_classes[i],
                                              9);
            v3_emit_unary_helper_call(plan,
                                      out,
                                      cap,
                                      target.ffi_handle_resolve_symbol_name,
                                      target.param_abi_classes[i],
                                      9,
                                      "ptr",
                                      0);
            v3_emit_call_arg_spill_store(out,
                                         cap,
                                         call_arg_base,
                                         call_depth,
                                         i,
                                         "ptr",
                                         0);
            continue;
        }
        if (target.ffi_handle_enabled && target.ffi_handle_param_is_handle[i]) {
            if (!v3_codegen_expr_scalar(plan,
                                        lowering,
                                        current_function,
                                        aliases,
                                        alias_count,
                                        locals,
                                        local_count,
                                        args[i],
                                        target.param_abi_classes[i],
                                        9,
                                        call_arg_base,
                                        string_temp_base,
                                        string_temp_stride,
                                        call_depth + 1,
                                        out,
                                        cap)) {
                fprintf(stderr,
                        "[cheng_v3_seed] scalar call ffi_handle arg emit failed caller=%s callee=%s arg_index=%zu expr=%s abi=%s\n",
                        current_function->symbol_text,
                        callee,
                        i,
                        args[i],
                        target.param_abi_classes[i]);
                return false;
            }
            v3_emit_unary_helper_call(plan,
                                      out,
                                      cap,
                                      target.ffi_handle_resolve_symbol_name,
                                      target.param_abi_classes[i],
                                      9,
                                      "ptr",
                                      0);
            v3_emit_call_arg_spill_store(out,
                                         cap,
                                         call_arg_base,
                                         call_depth,
                                         i,
                                         "ptr",
                                         0);
            continue;
        }
        if (v3_abi_class_scalar_or_ptr(target.param_abi_classes[i])) {
            if (!v3_codegen_expr_scalar(plan,
                                        lowering,
                                        current_function,
                                        aliases,
                                        alias_count,
                                        locals,
                                        local_count,
                                        args[i],
                                        target.param_abi_classes[i],
                                        9,
                                        call_arg_base,
                                        string_temp_base,
                                        string_temp_stride,
                                        call_depth + 1,
                                        out,
                                        cap)) {
                fprintf(stderr,
                        "[cheng_v3_seed] scalar call scalar-arg emit failed caller=%s callee=%s arg_index=%zu expr=%s abi=%s\n",
                        current_function->symbol_text,
                        callee,
                        i,
                        args[i],
                        target.param_abi_classes[i]);
                return false;
            }
            v3_emit_call_arg_spill_store(out,
                                         cap,
                                         call_arg_base,
                                         call_depth,
                                         i,
                                         target.param_abi_classes[i],
                                         9);
            continue;
        }
        {
            int32_t local_index = v3_find_local_slot(locals, local_count, args[i]);
            char temp_name[128];
            bool need_materialize = false;
            if (getenv("CHENG_V3_TRACE_ARG_KIND") != NULL) {
                fprintf(stderr,
                        "[cheng_v3_seed] scalar arg kind caller=%s callee=%s arg_index=%zu param_type=%s param_abi=%s expr=%s\n",
                        current_function->symbol_text,
                        callee,
                        i,
                        target.param_types[i],
                        target.param_abi_classes[i],
                        args[i]);
            }
            {
                char literal_text[4096];
                if (strcmp(target.param_types[i], "str") == 0 &&
                    v3_parse_string_literal_text(args[i], literal_text, sizeof(literal_text))) {
                    V3AsmLocalSlot scratch_slot;
                    int32_t literal_slot = 0;
                    size_t literal_i;
                    for (literal_i = 0; literal_i <= i; ++literal_i) {
                        char prior_literal[4096];
                        if (strcmp(target.param_types[literal_i], "str") == 0 &&
                            v3_parse_string_literal_text(args[literal_i], prior_literal, sizeof(prior_literal))) {
                            literal_slot += 1;
                        }
                    }
                    literal_slot -= 1;
                    if (literal_slot < 0 ||
                        string_temp_stride < ((literal_slot + 2) * v3_active_target_str_size_i32())) {
                        fprintf(stderr,
                                "[cheng_v3_seed] scalar call str-arg scratch overflow caller=%s callee=%s arg_index=%zu expr=%s\n",
                                current_function->symbol_text,
                                callee,
                                i,
                                args[i]);
                        return false;
                    }
                    memset(&scratch_slot, 0, sizeof(scratch_slot));
                    snprintf(scratch_slot.name, sizeof(scratch_slot.name), "%s", "__v3_call_str_arg");
                    snprintf(scratch_slot.type_text, sizeof(scratch_slot.type_text), "%s", "str");
                    snprintf(scratch_slot.abi_class, sizeof(scratch_slot.abi_class), "%s", "composite");
                    scratch_slot.stack_offset =
                        string_temp_base + call_depth * string_temp_stride +
                        v3_active_target_str_size_i32() +
                        literal_slot * v3_active_target_str_size_i32();
                    scratch_slot.stack_size = v3_active_target_str_size_i32();
                    scratch_slot.stack_align = v3_active_target_pointer_align_i32();
                    if (!v3_emit_slot_address(out, cap, &scratch_slot, 15) ||
                        !v3_materialize_composite_expr_into_address(plan,
                                                                    lowering,
                                                                    current_function,
                                                                    aliases,
                                                                    alias_count,
                                                                    locals,
                                                                    local_count,
                                                                    args[i],
                                                                    "str",
                                                                    15,
                                                                    call_arg_base,
                                                                    string_temp_base,
                                                                    string_temp_stride,
                                                                    string_label_index_io,
                                                                    call_depth + 1,
                                                                    out,
                                                                    cap)) {
                        fprintf(stderr,
                                "[cheng_v3_seed] scalar call str-arg materialize failed caller=%s callee=%s arg_index=%zu expr=%s\n",
                                current_function->symbol_text,
                                callee,
                                i,
                                args[i]);
                        return false;
                    }
                    v3_emit_call_arg_spill_store(out,
                                                 cap,
                                                 call_arg_base,
                                                 call_depth,
                                                 i,
                                                 "ptr",
                                                 15);
                    continue;
                }
            }
            if (local_index < 0) {
                v3_call_arg_temp_name(current_function,
                                      callee,
                                      i,
                                      args[i],
                                      temp_name,
                                      sizeof(temp_name));
                local_index = v3_find_local_slot(locals, local_count, temp_name);
                need_materialize = true;
                if (local_index < 0) {
                    v3_expr_temp_name(current_function,
                                      args[i],
                                      temp_name,
                                      sizeof(temp_name));
                    local_index = v3_find_local_slot(locals, local_count, temp_name);
                }
                if (local_index < 0) {
                    if (v3_emit_direct_composite_call_arg_address(plan,
                                                                  lowering,
                                                                  current_function,
                                                                  aliases,
                                                                  alias_count,
                                                                  locals,
                                                                  local_count,
                                                                  args[i],
                                                                  target.param_types[i],
                                                                  target.param_abi_classes[i],
                                                                  15,
                                                                  call_arg_base,
                                                                  string_temp_base,
                                                                  string_temp_stride,
                                                                  call_depth + 1,
                                                                  out,
                                                                  cap)) {
                        v3_emit_call_arg_spill_store(out,
                                                     cap,
                                                     call_arg_base,
                                                     call_depth,
                                                     i,
                                                     "ptr",
                                                     15);
                        continue;
                    }
                    fprintf(stderr,
                            "[cheng_v3_seed] scalar call composite-arg local missing caller=%s callee=%s arg_index=%zu expr=%s\n",
                            current_function->symbol_text,
                            callee,
                            i,
                            args[i]);
                    return false;
                }
            }
            if (need_materialize &&
                !v3_materialize_composite_expr_into_slot(plan,
                                                         lowering,
                                                         current_function,
                                                         aliases,
                                                         alias_count,
                                                         locals,
                                                         local_count,
                                                         callee,
                                                         i,
                                                         args[i],
                                                         &locals[local_index],
                                                         call_arg_base,
                                                         string_temp_base,
                                                         string_temp_stride,
                                                         string_label_index_io,
                                                         call_depth + 1,
                                                         out,
                                                         cap)) {
                fprintf(stderr,
                        "[cheng_v3_seed] scalar call composite-arg materialize failed caller=%s callee=%s arg_index=%zu expr=%s\n",
                        current_function->symbol_text,
                        callee,
                        i,
                        args[i]);
                return false;
            }
            if (!v3_emit_local_value_address(out, cap, &locals[local_index], 15)) {
                fprintf(stderr,
                        "[cheng_v3_seed] scalar call composite-arg address failed caller=%s callee=%s arg_index=%zu local=%s\n",
                        current_function->symbol_text,
                        callee,
                        i,
                        locals[local_index].name);
                return false;
            }
            v3_emit_call_arg_spill_store(out,
                                         cap,
                                         call_arg_base,
                                         call_depth,
                                         i,
                                         "ptr",
                                         15);
        }
    }
    {
        if (!v3_emit_call_from_spills(plan,
                                      lowering,
                                      current_function,
                                      aliases,
                                      alias_count,
                                      &target,
                                      false,
                                      call_arg_base,
                                      call_depth,
                                      out,
                                      cap)) {
            return false;
        }
        if (ffi_handle_consume_index >= 0) {
            if (strcmp(target.return_abi_class, "void") != 0) {
                v3_emit_call_arg_spill_store(out,
                                             cap,
                                             call_arg_base,
                                             call_depth,
                                             (size_t)ffi_handle_consume_index,
                                             target.return_abi_class,
                                             0);
            }
            v3_emit_call_dest_spill_load_abi(out,
                                             cap,
                                             call_arg_base,
                                             call_depth,
                                             9,
                                             target.param_abi_classes[ffi_handle_consume_index]);
            v3_emit_unary_helper_call(plan,
                                      out,
                                      cap,
                                      target.ffi_handle_invalidate_symbol_name,
                                      target.param_abi_classes[ffi_handle_consume_index],
                                      9,
                                      "i32",
                                      0);
            if (strcmp(target.return_abi_class, "void") != 0) {
                v3_emit_call_arg_spill_load(out,
                                            cap,
                                            call_arg_base,
                                            call_depth,
                                            (size_t)ffi_handle_consume_index,
                                            0,
                                            target.return_abi_class);
            }
        }
        if (target.ffi_handle_enabled && target.ffi_handle_return_is_handle) {
            v3_emit_unary_helper_call(plan,
                                      out,
                                      cap,
                                      target.ffi_handle_register_symbol_name,
                                      "ptr",
                                      0,
                                      target.return_abi_class,
                                      0);
        }
    }
    if (strcmp(expected_abi, "void") != 0 &&
        strcmp(target.return_abi_class, expected_abi) != 0) {
        if (!(strcmp(expected_abi, "i64") == 0 && strcmp(target.return_abi_class, "i32") == 0) &&
            !(strcmp(target.return_abi_class, "i32") == 0 &&
              (strcmp(expected_abi, "i8") == 0 ||
               strcmp(expected_abi, "u8") == 0 ||
               strcmp(expected_abi, "i16") == 0 ||
               strcmp(expected_abi, "u16") == 0)) &&
            !(strcmp(expected_abi, "ptr") == 0 && strcmp(target.return_abi_class, "ptr") == 0)) {
            fprintf(stderr,
                    "[cheng_v3_seed] scalar call return abi mismatch caller=%s callee=%s expected=%s got=%s\n",
                    current_function->symbol_text,
                    callee,
                    expected_abi,
                    target.return_abi_class);
            return false;
        }
    }
    if (strcmp(expected_abi, "void") != 0 && target_reg != 0) {
        if (strcmp(expected_abi, "i64") == 0 &&
            strcmp(target.return_abi_class, "i32") == 0) {
            if (v3_active_target_is_linux_x86_64()) {
                v3_text_appendf(out, cap, "  movslq %%eax, %s\n", v3_x64_qreg_name(target_reg));
            } else {
                v3_text_appendf(out, cap, "  sxtw x%d, w0\n", target_reg);
            }
        } else if (v3_abi_class_word_reg(expected_abi)) {
            if (v3_active_target_is_linux_x86_64()) {
                v3_text_appendf(out, cap, "  movl %%eax, %s\n", v3_x64_dreg_name(target_reg));
            } else {
                v3_text_appendf(out, cap, "  mov w%d, w0\n", target_reg);
            }
        } else {
            if (v3_active_target_is_linux_x86_64()) {
                v3_text_appendf(out, cap, "  movq %%rax, %s\n", v3_x64_qreg_name(target_reg));
            } else {
                v3_text_appendf(out, cap, "  mov x%d, x0\n", target_reg);
            }
        }
    }
    return true;
}

static bool v3_codegen_call_scalar(const V3SystemLinkPlanStub *plan,
                                   const V3LoweringPlanStub *lowering,
                                   const V3LoweredFunctionStub *current_function,
                                   const V3ImportAlias *aliases,
                                   size_t alias_count,
                                   const V3AsmLocalSlot *locals,
                                   size_t local_count,
                                   const char *callee,
                                   char args[][4096],
                                   size_t arg_count,
                                   const char *expected_abi,
                                   int target_reg,
                                   int32_t call_arg_base,
                                   int32_t string_temp_base,
                                   int32_t string_temp_stride,
                                   int32_t *string_label_index_io,
                                   int call_depth,
                                   char *out,
                                   size_t cap) {
    return v3_codegen_call_scalar_resolved(plan,
                                           lowering,
                                           current_function,
                                           aliases,
                                           alias_count,
                                           locals,
                                           local_count,
                                           callee,
                                           args,
                                           arg_count,
                                           NULL,
                                           expected_abi,
                                           target_reg,
                                           call_arg_base,
                                           string_temp_base,
                                           string_temp_stride,
                                           string_label_index_io,
                                           call_depth,
                                           out,
                                           cap);
}

static bool v3_emit_result_constructor_into_address(const V3SystemLinkPlanStub *plan,
                                                    const V3LoweringPlanStub *lowering,
                                                    const V3LoweredFunctionStub *current_function,
                                                    const V3ImportAlias *aliases,
                                                    size_t alias_count,
                                                    const V3AsmLocalSlot *locals,
                                                    size_t local_count,
                                                    V3ResultIntrinsicKind kind,
                                                    const char *callee,
                                                    char args[][4096],
                                                    size_t arg_count,
                                                    const char *dest_type_text,
                                                    int dest_addr_reg,
                                                    int32_t call_arg_base,
                                                    int32_t string_temp_base,
                                                    int32_t string_temp_stride,
                                                    int32_t *string_label_index_io,
                                                    int call_depth,
                                                    char *out,
                                                    size_t cap) {
    char normalized_dest_type[256];
    char explicit_inner_type[256];
    char normalized_explicit_inner_type[256];
    char result_inner_type[256];
    char ok_type[256];
    char ok_abi[32];
    char value_type[256];
    char value_abi[32];
    char err_type[256];
    char err_abi[32];
    char err_code_type[256];
    char err_code_abi[32];
    char err_msg_type[256];
    char err_msg_abi[32];
    V3TypeLayoutStub result_layout;
    int32_t ok_offset = 0;
    int32_t value_offset = 0;
    int32_t err_offset = 0;
    int32_t err_code_offset = 0;
    int32_t err_msg_offset = 0;
    int value_addr_reg = dest_addr_reg == 14 ? 15 : 14;
    int err_addr_reg = dest_addr_reg == 15 ? 13 : 15;
    int err_msg_addr_reg = err_addr_reg == 14 ? 13 : 14;
    if (!v3_normalize_type_text(lowering,
                                current_function->owner_module_path,
                                aliases,
                                alias_count,
                                dest_type_text,
                                normalized_dest_type,
                                sizeof(normalized_dest_type)) ||
        !v3_startswith(normalized_dest_type, "Result[") ||
        !v3_parse_result_inner_type(normalized_dest_type,
                                    result_inner_type,
                                    sizeof(result_inner_type)) ||
        !v3_compute_type_layout_impl(lowering, normalized_dest_type, &result_layout, 0U) ||
        !v3_resolve_field_meta(lowering,
                               normalized_dest_type,
                               "ok",
                               ok_type,
                               sizeof(ok_type),
                               ok_abi,
                               sizeof(ok_abi),
                               &ok_offset) ||
        !v3_resolve_field_meta(lowering,
                               normalized_dest_type,
                               "value",
                               value_type,
                               sizeof(value_type),
                               value_abi,
                               sizeof(value_abi),
                               &value_offset) ||
        !v3_resolve_field_meta(lowering,
                               normalized_dest_type,
                               "err",
                               err_type,
                               sizeof(err_type),
                               err_abi,
                               sizeof(err_abi),
                               &err_offset)) {
        return false;
    }
    if (v3_extract_trailing_type_arg_text(callee,
                                          explicit_inner_type,
                                          sizeof(explicit_inner_type))) {
        if (!v3_normalize_type_text(lowering,
                                    current_function->owner_module_path,
                                    aliases,
                                    alias_count,
                                    explicit_inner_type,
                                    normalized_explicit_inner_type,
                                    sizeof(normalized_explicit_inner_type)) ||
            strcmp(normalized_explicit_inner_type, result_inner_type) != 0) {
            return false;
        }
    }
    if (!v3_emit_zero_address_range(out, cap, dest_addr_reg, result_layout.size)) {
        return false;
    }
    v3_emit_call_dest_spill_store(out,
                                  cap,
                                  call_arg_base,
                                  call_depth,
                                  dest_addr_reg);
    if (kind == V3_RESULT_INTRINSIC_OK) {
        if (arg_count != 1U || !v3_emit_mov_imm(out, cap, 9, 1)) {
            return false;
        }
        v3_emit_call_dest_spill_load(out,
                                     cap,
                                     call_arg_base,
                                     call_depth,
                                     dest_addr_reg);
        v3_emit_store_scalar_to_address(out, cap, dest_addr_reg, ok_offset, "bool", 9);
        if (v3_abi_class_scalar_or_ptr(value_abi)) {
            v3_emit_address_spill_store(out, cap, call_arg_base, 0, 14);
            if (!v3_codegen_expr_scalar(plan,
                                        lowering,
                                        current_function,
                                        aliases,
                                        alias_count,
                                        locals,
                                        local_count,
                                        args[0],
                                        value_abi,
                                        9,
                                        call_arg_base,
                                        string_temp_base,
                                        string_temp_stride,
                                        call_depth + 1,
                                        out,
                                        cap)) {
                return false;
            }
            v3_emit_call_dest_spill_load(out,
                                         cap,
                                         call_arg_base,
                                         call_depth,
                                         value_addr_reg);
            if (value_offset != 0 &&
                !v3_emit_add_address_offset(out, cap, value_addr_reg, value_addr_reg, value_offset)) {
                return false;
            }
            v3_emit_store_scalar_to_address(out, cap, value_addr_reg, 0, value_abi, 9);
            return true;
        }
        v3_emit_call_dest_spill_load(out,
                                     cap,
                                     call_arg_base,
                                     call_depth,
                                     value_addr_reg);
        if (value_offset != 0 &&
            !v3_emit_add_address_offset(out, cap, value_addr_reg, value_addr_reg, value_offset)) {
            return false;
        }
        return v3_materialize_composite_expr_into_address(plan,
                                                          lowering,
                                                          current_function,
                                                          aliases,
                                                          alias_count,
                                                          locals,
                                                          local_count,
                                                          args[0],
                                                          value_type,
                                                          value_addr_reg,
                                                          call_arg_base,
                                                          string_temp_base,
                                                          string_temp_stride,
                                                          string_label_index_io,
                                                          call_depth + 1,
                                                          out,
                                                          cap);
    }
    v3_emit_call_dest_spill_load(out,
                                 cap,
                                 call_arg_base,
                                 call_depth,
                                 err_addr_reg);
    if (err_offset != 0 &&
        !v3_emit_add_address_offset(out, cap, err_addr_reg, err_addr_reg, err_offset)) {
        return false;
    }
    if (kind == V3_RESULT_INTRINSIC_ERR_INFO) {
        return arg_count == 1U &&
               strcmp(err_abi, "composite") == 0 &&
               v3_materialize_composite_expr_into_address(plan,
                                                          lowering,
                                                          current_function,
                                                          aliases,
                                                          alias_count,
                                                          locals,
                                                          local_count,
                                                          args[0],
                                                          err_type,
                                                          err_addr_reg,
                                                          call_arg_base,
                                                          string_temp_base,
                                                          string_temp_stride,
                                                          string_label_index_io,
                                                          call_depth + 1,
                                                          out,
                                                          cap);
    }
    if (!v3_resolve_field_meta(lowering,
                               err_type,
                               "code",
                               err_code_type,
                               sizeof(err_code_type),
                               err_code_abi,
                               sizeof(err_code_abi),
                               &err_code_offset) ||
        !v3_resolve_field_meta(lowering,
                               err_type,
                               "msg",
                               err_msg_type,
                               sizeof(err_msg_type),
                               err_msg_abi,
                               sizeof(err_msg_abi),
                               &err_msg_offset)) {
        return false;
    }
    if (kind == V3_RESULT_INTRINSIC_ERR) {
        if (arg_count != 1U || !v3_emit_mov_imm(out, cap, 9, 1)) {
            return false;
        }
        v3_emit_store_scalar_to_address(out, cap, err_addr_reg, err_code_offset, "i32", 9);
        v3_emit_call_dest_spill_load(out,
                                     cap,
                                     call_arg_base,
                                     call_depth,
                                     err_msg_addr_reg);
        if (err_offset != 0 &&
            !v3_emit_add_address_offset(out, cap, err_msg_addr_reg, err_msg_addr_reg, err_offset)) {
            return false;
        }
        if (err_msg_offset != 0 &&
            !v3_emit_add_address_offset(out, cap, err_msg_addr_reg, err_msg_addr_reg, err_msg_offset)) {
            return false;
        }
        return strcmp(err_msg_abi, "composite") == 0 &&
               v3_materialize_composite_expr_into_address(plan,
                                                          lowering,
                                                          current_function,
                                                          aliases,
                                                          alias_count,
                                                          locals,
                                                          local_count,
                                                          args[0],
                                                          err_msg_type,
                                                          err_msg_addr_reg,
                                                          call_arg_base,
                                                          string_temp_base,
                                                          string_temp_stride,
                                                          string_label_index_io,
                                                          call_depth + 1,
                                                          out,
                                                          cap);
    }
    if (kind == V3_RESULT_INTRINSIC_ERR_CODE) {
        if (arg_count != 2U || strcmp(err_msg_abi, "composite") != 0) {
            return false;
        }
        if (!v3_codegen_expr_scalar(plan,
                                    lowering,
                                    current_function,
                                    aliases,
                                    alias_count,
                                    locals,
                                    local_count,
                                    args[0],
                                    "i32",
                                    9,
                                    call_arg_base,
                                    string_temp_base,
                                    string_temp_stride,
                                    call_depth + 1,
                                        out,
                                        cap)) {
            return false;
        }
        v3_emit_store_scalar_to_address(out, cap, err_addr_reg, err_code_offset, "i32", 9);
        v3_emit_call_dest_spill_load(out,
                                     cap,
                                     call_arg_base,
                                     call_depth,
                                     err_msg_addr_reg);
        if (err_offset != 0 &&
            !v3_emit_add_address_offset(out, cap, err_msg_addr_reg, err_msg_addr_reg, err_offset)) {
            return false;
        }
        if (err_msg_offset != 0 &&
            !v3_emit_add_address_offset(out, cap, err_msg_addr_reg, err_msg_addr_reg, err_msg_offset)) {
            return false;
        }
        return v3_materialize_composite_expr_into_address(plan,
                                                          lowering,
                                                          current_function,
                                                          aliases,
                                                          alias_count,
                                                          locals,
                                                          local_count,
                                                          args[1],
                                                          err_msg_type,
                                                          err_msg_addr_reg,
                                                          call_arg_base,
                                                          string_temp_base,
                                                          string_temp_stride,
                                                          string_label_index_io,
                                                          call_depth + 1,
                                                          out,
                                                          cap);
    }
    return false;
}

static bool v3_codegen_binary_expr_scalar(const V3SystemLinkPlanStub *plan,
                                          const V3LoweringPlanStub *lowering,
                                          const V3LoweredFunctionStub *current_function,
                                          const V3ImportAlias *aliases,
                                          size_t alias_count,
                                          const V3AsmLocalSlot *locals,
                                          size_t local_count,
                                          const char *expr_text,
                                          const char *op,
                                          const char *expected_abi,
                                          int target_reg,
                                          int32_t call_arg_base,
                                          int32_t string_temp_base,
                                          int32_t string_temp_stride,
                                          int call_depth,
                                          char *out,
                                          size_t cap) {
    int32_t op_index = v3_find_top_level_binary_op_last(expr_text, op);
    char left[4096];
    char right[4096];
    char left_type[256];
    char left_abi[32];
    bool left_is_unsigned = false;
    if (op_index < 0) {
        return false;
    }
    snprintf(left, sizeof(left), "%.*s", op_index, expr_text);
    snprintf(right, sizeof(right), "%s", expr_text + op_index + strlen(op));
    v3_trim_copy_text(left, left, sizeof(left));
    v3_trim_copy_text(right, right, sizeof(right));
    if (!v3_infer_expr_type(plan,
                            lowering,
                            current_function,
                            aliases,
                            alias_count,
                            locals,
                            local_count,
                            left,
                            left_type,
                            sizeof(left_type),
                            left_abi,
                            sizeof(left_abi))) {
        return false;
    }
    left_is_unsigned = v3_scalar_type_is_unsigned(left_type);
    if (!v3_codegen_expr_scalar(plan,
                                lowering,
                                current_function,
                                aliases,
                                alias_count,
                                locals,
                                local_count,
                                left,
                                expected_abi,
                                target_reg,
                                call_arg_base,
                                string_temp_base,
                                string_temp_stride,
                                call_depth + 1,
                                out,
                                cap)) {
        return false;
    }
    v3_emit_binary_spill_store(out,
                               cap,
                               call_arg_base,
                               call_depth,
                               expected_abi,
                               target_reg);
    if (!v3_codegen_expr_scalar(plan,
                                lowering,
                                current_function,
                                aliases,
                                alias_count,
                                locals,
                                local_count,
                                right,
                                expected_abi,
                                target_reg + 1,
                                call_arg_base,
                                string_temp_base,
                                string_temp_stride,
                                call_depth + 1,
                                out,
                                cap)) {
        return false;
    }
    v3_emit_binary_spill_load(out,
                              cap,
                              call_arg_base,
                              call_depth,
                              target_reg,
                              expected_abi);
    if (v3_active_target_is_linux_x86_64()) {
        const bool wide = strcmp(expected_abi, "i64") == 0 || strcmp(expected_abi, "ptr") == 0;
        int work_reg = target_reg;
        if (strcmp(op, "|") == 0) {
            v3_text_appendf(out, cap,
                            wide ? "  orq %s, %s\n" : "  orl %s, %s\n",
                            wide ? v3_x64_qreg_name(target_reg + 1) : v3_x64_dreg_name(target_reg + 1),
                            wide ? v3_x64_qreg_name(target_reg) : v3_x64_dreg_name(target_reg));
        } else if (strcmp(op, "||") == 0) {
            v3_text_appendf(out, cap, "  orl %s, %s\n",
                            v3_x64_dreg_name(target_reg + 1),
                            v3_x64_dreg_name(target_reg));
        } else if (strcmp(op, "^") == 0) {
            v3_text_appendf(out, cap,
                            wide ? "  xorq %s, %s\n" : "  xorl %s, %s\n",
                            wide ? v3_x64_qreg_name(target_reg + 1) : v3_x64_dreg_name(target_reg + 1),
                            wide ? v3_x64_qreg_name(target_reg) : v3_x64_dreg_name(target_reg));
        } else if (strcmp(op, "&") == 0) {
            v3_text_appendf(out, cap,
                            wide ? "  andq %s, %s\n" : "  andl %s, %s\n",
                            wide ? v3_x64_qreg_name(target_reg + 1) : v3_x64_dreg_name(target_reg + 1),
                            wide ? v3_x64_qreg_name(target_reg) : v3_x64_dreg_name(target_reg));
        } else if (strcmp(op, "&&") == 0) {
            v3_text_appendf(out, cap, "  andl %s, %s\n",
                            v3_x64_dreg_name(target_reg + 1),
                            v3_x64_dreg_name(target_reg));
        } else if (strcmp(op, "+") == 0) {
            v3_text_appendf(out, cap,
                            wide ? "  addq %s, %s\n" : "  addl %s, %s\n",
                            wide ? v3_x64_qreg_name(target_reg + 1) : v3_x64_dreg_name(target_reg + 1),
                            wide ? v3_x64_qreg_name(target_reg) : v3_x64_dreg_name(target_reg));
        } else if (strcmp(op, "-") == 0) {
            v3_text_appendf(out, cap,
                            wide ? "  subq %s, %s\n" : "  subl %s, %s\n",
                            wide ? v3_x64_qreg_name(target_reg + 1) : v3_x64_dreg_name(target_reg + 1),
                            wide ? v3_x64_qreg_name(target_reg) : v3_x64_dreg_name(target_reg));
        } else if (strcmp(op, "*") == 0) {
            v3_text_appendf(out, cap,
                            wide ? "  imulq %s, %s\n" : "  imull %s, %s\n",
                            wide ? v3_x64_qreg_name(target_reg + 1) : v3_x64_dreg_name(target_reg + 1),
                            wide ? v3_x64_qreg_name(target_reg) : v3_x64_dreg_name(target_reg));
        } else if (strcmp(op, "/") == 0 || strcmp(op, "%") == 0) {
            if (wide) {
                v3_text_appendf(out, cap, "  movq %s, %s\n",
                                v3_x64_qreg_name(target_reg + 1),
                                v3_x64_qreg_name(17));
                if (target_reg != 0) {
                    v3_text_appendf(out, cap, "  movq %s, %%rax\n", v3_x64_qreg_name(target_reg));
                }
                if (left_is_unsigned) {
                    v3_text_append(out, cap, "  xorl %edx, %edx\n");
                    v3_text_appendf(out, cap, "  divq %s\n", v3_x64_qreg_name(17));
                } else {
                    v3_text_append(out, cap, "  cqto\n");
                    v3_text_appendf(out, cap, "  idivq %s\n", v3_x64_qreg_name(17));
                }
                if (strcmp(op, "/") == 0) {
                    if (target_reg != 0) {
                        v3_text_appendf(out, cap, "  movq %%rax, %s\n", v3_x64_qreg_name(target_reg));
                    }
                } else if (target_reg != 2) {
                    v3_text_appendf(out, cap, "  movq %%rdx, %s\n", v3_x64_qreg_name(target_reg));
                }
            } else {
                v3_text_appendf(out, cap, "  movl %s, %s\n",
                                v3_x64_dreg_name(target_reg + 1),
                                v3_x64_dreg_name(17));
                if (target_reg != 0) {
                    v3_text_appendf(out, cap, "  movl %s, %%eax\n", v3_x64_dreg_name(target_reg));
                }
                if (left_is_unsigned) {
                    v3_text_append(out, cap, "  xorl %edx, %edx\n");
                    v3_text_appendf(out, cap, "  divl %s\n", v3_x64_dreg_name(17));
                } else {
                    v3_text_append(out, cap, "  cdq\n");
                    v3_text_appendf(out, cap, "  idivl %s\n", v3_x64_dreg_name(17));
                }
                if (strcmp(op, "/") == 0) {
                    if (target_reg != 0) {
                        v3_text_appendf(out, cap, "  movl %%eax, %s\n", v3_x64_dreg_name(target_reg));
                    }
                } else if (target_reg != 2) {
                    v3_text_appendf(out, cap, "  movl %%edx, %s\n", v3_x64_dreg_name(target_reg));
                }
            }
        } else if (strcmp(op, "<<") == 0 || strcmp(op, ">>") == 0) {
            if (target_reg == 3) {
                v3_emit_x64_move_reg(out, cap, 16, target_reg, wide ? "i64" : "i32");
                work_reg = 16;
            }
            v3_text_appendf(out, cap, "  movl %s, %%ecx\n", v3_x64_dreg_name(target_reg + 1));
            if (strcmp(op, "<<") == 0) {
                v3_text_appendf(out, cap,
                                wide ? "  shlq %%cl, %s\n" : "  shll %%cl, %s\n",
                                wide ? v3_x64_qreg_name(work_reg) : v3_x64_dreg_name(work_reg));
            } else if (strcmp(left_abi, "i32") == 0) {
                v3_text_appendf(out, cap, "  sarl %%cl, %s\n", v3_x64_dreg_name(work_reg));
                v3_text_appendf(out, cap, "  movslq %s, %s\n",
                                v3_x64_dreg_name(work_reg),
                                v3_x64_qreg_name(work_reg));
            } else if (strcmp(left_abi, "u32") == 0 ||
                       strcmp(left_abi, "i16") == 0 ||
                       strcmp(left_abi, "u16") == 0 ||
                       strcmp(left_abi, "i8") == 0 ||
                       strcmp(left_abi, "u8") == 0 ||
                       strcmp(left_abi, "bool") == 0) {
                v3_text_appendf(out, cap, "  shrl %%cl, %s\n", v3_x64_dreg_name(work_reg));
            } else {
                v3_text_appendf(out, cap,
                                left_is_unsigned ? "  shrq %%cl, %s\n" : "  sarq %%cl, %s\n",
                                v3_x64_qreg_name(work_reg));
            }
            if (work_reg != target_reg) {
                v3_emit_x64_move_reg(out, cap, target_reg, work_reg, wide ? "i64" : "i32");
            }
        } else {
            return false;
        }
    } else {
        if (strcmp(op, "|") == 0) {
            v3_text_appendf(out, cap, "  orr x%d, x%d, x%d\n", target_reg, target_reg, target_reg + 1);
        } else if (strcmp(op, "||") == 0) {
            v3_text_appendf(out, cap, "  orr w%d, w%d, w%d\n", target_reg, target_reg, target_reg + 1);
        } else if (strcmp(op, "^") == 0) {
            v3_text_appendf(out, cap, "  eor x%d, x%d, x%d\n", target_reg, target_reg, target_reg + 1);
        } else if (strcmp(op, "&") == 0) {
            v3_text_appendf(out, cap, "  and x%d, x%d, x%d\n", target_reg, target_reg, target_reg + 1);
        } else if (strcmp(op, "&&") == 0) {
            v3_text_appendf(out, cap, "  and w%d, w%d, w%d\n", target_reg, target_reg, target_reg + 1);
        } else if (strcmp(op, "+") == 0) {
            v3_text_appendf(out, cap, "  add x%d, x%d, x%d\n", target_reg, target_reg, target_reg + 1);
        } else if (strcmp(op, "-") == 0) {
            v3_text_appendf(out, cap, "  sub x%d, x%d, x%d\n", target_reg, target_reg, target_reg + 1);
        } else if (strcmp(op, "*") == 0) {
            v3_text_appendf(out, cap, "  mul x%d, x%d, x%d\n", target_reg, target_reg, target_reg + 1);
        } else if (strcmp(op, "/") == 0) {
            v3_text_appendf(out, cap,
                            left_is_unsigned ?
                            "  udiv x%d, x%d, x%d\n" :
                            "  sdiv x%d, x%d, x%d\n",
                            target_reg,
                            target_reg,
                            target_reg + 1);
        } else if (strcmp(op, "%") == 0) {
            v3_text_appendf(out, cap,
                            left_is_unsigned ?
                            "  udiv x14, x%d, x%d\n"
                            "  msub x%d, x14, x%d, x%d\n" :
                            "  sdiv x14, x%d, x%d\n"
                            "  msub x%d, x14, x%d, x%d\n",
                            target_reg,
                            target_reg + 1,
                            target_reg,
                            target_reg + 1,
                            target_reg);
        } else if (strcmp(op, "<<") == 0) {
            v3_text_appendf(out, cap, "  lslv x%d, x%d, x%d\n", target_reg, target_reg, target_reg + 1);
        } else if (strcmp(op, ">>") == 0) {
            if (strcmp(left_abi, "i32") == 0) {
                v3_text_appendf(out, cap, "  asrv w%d, w%d, w%d\n", target_reg, target_reg, target_reg + 1);
                v3_text_appendf(out, cap, "  sxtw x%d, w%d\n", target_reg, target_reg);
            } else if (strcmp(left_abi, "u32") == 0 ||
                       strcmp(left_abi, "i16") == 0 ||
                       strcmp(left_abi, "u16") == 0 ||
                       strcmp(left_abi, "i8") == 0 ||
                       strcmp(left_abi, "u8") == 0 ||
                       strcmp(left_abi, "bool") == 0) {
                v3_text_appendf(out, cap, "  lsrv w%d, w%d, w%d\n", target_reg, target_reg, target_reg + 1);
            } else {
                v3_text_appendf(out,
                                cap,
                                left_is_unsigned ?
                                "  lsrv x%d, x%d, x%d\n" :
                                "  asrv x%d, x%d, x%d\n",
                                target_reg,
                                target_reg,
                                target_reg + 1);
            }
        } else {
            return false;
        }
    }
    return true;
}

static bool v3_codegen_compare_expr_scalar(const V3SystemLinkPlanStub *plan,
                                           const V3LoweringPlanStub *lowering,
                                           const V3LoweredFunctionStub *current_function,
                                           const V3ImportAlias *aliases,
                                           size_t alias_count,
                                           const V3AsmLocalSlot *locals,
                                           size_t local_count,
                                           const char *expr_text,
                                           const char *op,
                                           int target_reg,
                                           int32_t call_arg_base,
                                           int32_t string_temp_base,
                                           int32_t string_temp_stride,
                                           int call_depth,
                                           char *out,
                                           size_t cap) {
    int32_t op_index = v3_find_top_level_binary_op_last(expr_text, op);
    char left[4096];
    char right[4096];
    char left_type[256];
    char left_abi[32];
    char right_type[256];
    char right_abi[32];
    char normalized_left_type[256];
    char normalized_right_type[256];
    const char *compare_abi = "i64";
    bool compare_wide = true;
    const char *cond = NULL;
    if (op_index < 0) {
        return false;
    }
    snprintf(left, sizeof(left), "%.*s", op_index, expr_text);
    snprintf(right, sizeof(right), "%s", expr_text + op_index + strlen(op));
    v3_trim_copy_text(left, left, sizeof(left));
    v3_trim_copy_text(right, right, sizeof(right));
    if (!v3_infer_expr_type(plan,
                            lowering,
                            current_function,
                            aliases,
                            alias_count,
                            locals,
                            local_count,
                            left,
                            left_type,
                            sizeof(left_type),
                            left_abi,
                            sizeof(left_abi))) {
        fprintf(stderr,
                "[cheng_v3_seed] compare infer lhs failed function=%s expr=%s lhs=%s op=%s rhs=%s\n",
                current_function->symbol_text,
                expr_text,
                left,
                op,
                right);
        return false;
    }
    if (!v3_infer_expr_type(plan,
                            lowering,
                            current_function,
                            aliases,
                            alias_count,
                            locals,
                            local_count,
                            right,
                            right_type,
                            sizeof(right_type),
                            right_abi,
                            sizeof(right_abi))) {
        fprintf(stderr,
                "[cheng_v3_seed] compare infer rhs failed function=%s expr=%s lhs=%s op=%s rhs=%s lhs_type=%s lhs_abi=%s\n",
                current_function->symbol_text,
                expr_text,
                left,
                op,
                right,
                left_type,
                left_abi);
        return false;
    }
    if (!v3_normalize_type_text(lowering,
                                current_function->owner_module_path,
                                aliases,
                                alias_count,
                                left_type,
                                normalized_left_type,
                                sizeof(normalized_left_type)) ||
        !v3_normalize_type_text(lowering,
                                current_function->owner_module_path,
                                aliases,
                                alias_count,
                                right_type,
                                normalized_right_type,
                                sizeof(normalized_right_type))) {
        fprintf(stderr,
                "[cheng_v3_seed] compare normalize failed function=%s expr=%s lhs_type=%s lhs_abi=%s rhs_type=%s rhs_abi=%s\n",
                current_function->symbol_text,
                expr_text,
                left_type,
                left_abi,
                right_type,
                right_abi);
        return false;
    }
    if (strcmp(normalized_left_type, "str") == 0 &&
        strcmp(normalized_right_type, "str") == 0 &&
        strcmp(left_abi, "composite") == 0 &&
        strcmp(right_abi, "composite") == 0) {
        V3AsmLocalSlot left_scratch_slot;
        V3AsmLocalSlot right_scratch_slot;
        memset(&left_scratch_slot, 0, sizeof(left_scratch_slot));
        memset(&right_scratch_slot, 0, sizeof(right_scratch_slot));
        snprintf(left_scratch_slot.name, sizeof(left_scratch_slot.name), "%s", "__v3_cmpstr_lhs");
        snprintf(left_scratch_slot.type_text, sizeof(left_scratch_slot.type_text), "%s", "str");
        snprintf(left_scratch_slot.abi_class, sizeof(left_scratch_slot.abi_class), "%s", "composite");
        left_scratch_slot.stack_offset = string_temp_base + call_depth * string_temp_stride;
        left_scratch_slot.stack_size = v3_active_target_str_size_i32();
        left_scratch_slot.stack_align = v3_active_target_pointer_align_i32();
        snprintf(right_scratch_slot.name, sizeof(right_scratch_slot.name), "%s", "__v3_cmpstr_rhs");
        snprintf(right_scratch_slot.type_text, sizeof(right_scratch_slot.type_text), "%s", "str");
        snprintf(right_scratch_slot.abi_class, sizeof(right_scratch_slot.abi_class), "%s", "composite");
        right_scratch_slot.stack_offset =
            left_scratch_slot.stack_offset + v3_active_target_str_size_i32();
        right_scratch_slot.stack_size = v3_active_target_str_size_i32();
        right_scratch_slot.stack_align = v3_active_target_pointer_align_i32();
        if (!v3_emit_slot_address(out, cap, &left_scratch_slot, 15) ||
            !v3_materialize_composite_expr_into_address(plan,
                                                        lowering,
                                                        current_function,
                                                        aliases,
                                                        alias_count,
                                                        locals,
                                                        local_count,
                                                        left,
                                                        "str",
                                                        15,
                                                        call_arg_base,
                                                        string_temp_base,
                                                        string_temp_stride,
                                                        NULL,
                                                        call_depth + 1,
                                                        out,
                                                        cap)) {
            return false;
        }
        if (!v3_emit_slot_address(out, cap, &right_scratch_slot, 15) ||
            !v3_materialize_composite_expr_into_address(plan,
                                                        lowering,
                                                        current_function,
                                                        aliases,
                                                        alias_count,
                                                        locals,
                                                        local_count,
                                                        right,
                                                        "str",
                                                        15,
                                                        call_arg_base,
                                                        string_temp_base,
                                                        string_temp_stride,
                                                        NULL,
                                                        call_depth + 1,
                                                        out,
                                                        cap)) {
            return false;
        }
        if (strcmp(op, "==") == 0 || strcmp(op, "!=") == 0) {
            V3AsmCallTarget str_eq_target;
            memset(&str_eq_target, 0, sizeof(str_eq_target));
            str_eq_target.external = true;
            str_eq_target.param_count = 2U;
            snprintf(str_eq_target.callee_name, sizeof(str_eq_target.callee_name), "%s", "driver_c_str_eq_bridge");
            v3_copy_target_symbol_name(plan,
                                       "driver_c_str_eq_bridge",
                                       str_eq_target.symbol_name,
                                       sizeof(str_eq_target.symbol_name));
            snprintf(str_eq_target.return_type, sizeof(str_eq_target.return_type), "%s", "int32");
            snprintf(str_eq_target.return_abi_class, sizeof(str_eq_target.return_abi_class), "%s", "i32");
            snprintf(str_eq_target.param_types[0], sizeof(str_eq_target.param_types[0]), "%s", "str");
            snprintf(str_eq_target.param_types[1], sizeof(str_eq_target.param_types[1]), "%s", "str");
            snprintf(str_eq_target.param_abi_classes[0], sizeof(str_eq_target.param_abi_classes[0]), "%s", "composite");
            snprintf(str_eq_target.param_abi_classes[1], sizeof(str_eq_target.param_abi_classes[1]), "%s", "composite");
            if (!v3_emit_local_value_address(out, cap, &left_scratch_slot, 15) ||
                !v3_emit_local_value_address(out, cap, &right_scratch_slot, 9)) {
                return false;
            }
            v3_emit_call_arg_spill_store(out,
                                         cap,
                                         call_arg_base,
                                         call_depth,
                                         0U,
                                         "ptr",
                                         15);
            v3_emit_call_arg_spill_store(out,
                                         cap,
                                         call_arg_base,
                                         call_depth,
                                         1U,
                                         "ptr",
                                         9);
            if (!v3_emit_call_from_spills(plan,
                                          lowering,
                                          current_function,
                                          aliases,
                                          alias_count,
                                          &str_eq_target,
                                          false,
                                          call_arg_base,
                                          call_depth,
                                          out,
                                          cap)) {
                return false;
            }
            if (v3_active_target_is_linux_x86_64()) {
                v3_emit_x64_cmp_zero_set_bool(out,
                                              cap,
                                              0,
                                              target_reg,
                                              false,
                                              strcmp(op, "==") == 0 ? "ne" : "eq");
            } else {
                v3_text_appendf(out, cap,
                                "  cmp w0, #0\n"
                                "  cset w%d, %s\n",
                                target_reg,
                                strcmp(op, "==") == 0 ? "ne" : "eq");
            }
            return true;
        } else if (strcmp(op, "<") == 0) {
            cond = "lt";
        } else if (strcmp(op, "<=") == 0) {
            cond = "le";
        } else if (strcmp(op, ">") == 0) {
            cond = "gt";
        } else if (strcmp(op, ">=") == 0) {
            cond = "ge";
        } else {
            return false;
        }
        if (v3_active_target_is_linux_x86_64()) {
            if (!v3_emit_slot_address(out, cap, &left_scratch_slot, 14) ||
                !v3_emit_slot_address(out, cap, &right_scratch_slot, 15)) {
                return false;
            }
        } else if (!v3_emit_slot_address(out, cap, &left_scratch_slot, 0) ||
                   !v3_emit_slot_address(out, cap, &right_scratch_slot, 1)) {
            return false;
        }
        if (v3_active_target_is_linux_x86_64()) {
            v3_text_append(out, cap,
                           "  movq 0(%rax), %rdi\n"
                           "  movq 0(%rbx), %rsi\n");
        } else {
            v3_text_appendf(out, cap,
                            "  ldr x0, [x0, #0]\n"
                            "  ldr x1, [x1, #0]\n");
        }
        v3_emit_call_named_symbol(plan, out, cap, "cheng_strcmp");
        if (v3_active_target_is_linux_x86_64()) {
            v3_emit_x64_cmp_zero_set_bool(out, cap, 0, target_reg, false, cond);
        } else {
            v3_text_appendf(out, cap,
                            "  cmp w0, #0\n"
                            "  cset w%d, %s\n",
                            target_reg,
                            cond);
        }
        return true;
    }
    if (strcmp(left_abi, "ptr") == 0 && strcmp(right_abi, "ptr") == 0) {
        compare_abi = "ptr";
        compare_wide = true;
    } else if (strcmp(left_abi, "i64") == 0 || strcmp(right_abi, "i64") == 0) {
        compare_abi = "i64";
        compare_wide = true;
    } else {
        compare_abi = "i32";
        compare_wide = false;
    }
    if (!v3_codegen_expr_scalar(plan,
                                lowering,
                                current_function,
                                aliases,
                                alias_count,
                                locals,
                                local_count,
                                left,
                                compare_abi,
                                target_reg,
                                call_arg_base,
                                string_temp_base,
                                string_temp_stride,
                                call_depth + 1,
                                out,
                                cap)) {
        fprintf(stderr,
                "[cheng_v3_seed] compare codegen lhs failed function=%s expr=%s lhs=%s rhs=%s compare_abi=%s\n",
                current_function->symbol_text,
                expr_text,
                left,
                right,
                compare_abi);
        return false;
    }
    v3_emit_binary_spill_store(out,
                               cap,
                               call_arg_base,
                               call_depth,
                               compare_abi,
                               target_reg);
    if (!v3_codegen_expr_scalar(plan,
                                lowering,
                                current_function,
                                aliases,
                                alias_count,
                                locals,
                                local_count,
                                right,
                                compare_abi,
                                target_reg + 1,
                                call_arg_base,
                                string_temp_base,
                                string_temp_stride,
                                call_depth + 1,
                                out,
                                cap)) {
        fprintf(stderr,
                "[cheng_v3_seed] compare codegen rhs failed function=%s expr=%s lhs=%s rhs=%s compare_abi=%s\n",
                current_function->symbol_text,
                expr_text,
                left,
                right,
                compare_abi);
        return false;
    }
    v3_emit_binary_spill_load(out,
                              cap,
                              call_arg_base,
                              call_depth,
                              target_reg,
                              compare_abi);
    if (strcmp(op, "==") == 0) {
        cond = "eq";
    } else if (strcmp(op, "!=") == 0) {
        cond = "ne";
    } else if (strcmp(op, "<") == 0) {
        cond = "lt";
    } else if (strcmp(op, "<=") == 0) {
        cond = "le";
    } else if (strcmp(op, ">") == 0) {
        cond = "gt";
    } else if (strcmp(op, ">=") == 0) {
        cond = "ge";
    } else {
        return false;
    }
    if (compare_wide) {
        if (v3_active_target_is_linux_x86_64()) {
            v3_emit_x64_cmp_regs_set_bool(out, cap, target_reg, target_reg + 1, target_reg, true, cond);
        } else {
            v3_text_appendf(out, cap,
                            "  cmp x%d, x%d\n"
                            "  cset w%d, %s\n",
                            target_reg,
                            target_reg + 1,
                            target_reg,
                            cond);
        }
    } else {
        if (v3_active_target_is_linux_x86_64()) {
            v3_emit_x64_cmp_regs_set_bool(out, cap, target_reg, target_reg + 1, target_reg, false, cond);
        } else {
            v3_text_appendf(out, cap,
                            "  cmp w%d, w%d\n"
                            "  cset w%d, %s\n",
                            target_reg,
                            target_reg + 1,
                            target_reg,
                            cond);
        }
    }
    return true;
}

static bool v3_parse_result_inner_type(const char *result_type,
                                       char *inner_type_out,
                                       size_t inner_type_cap) {
    size_t len;
    if (!v3_startswith(result_type, "Result[")) {
        return false;
    }
    len = strlen(result_type);
    if (len < 9U || result_type[len - 1U] != ']') {
        return false;
    }
    snprintf(inner_type_out, inner_type_cap, "%.*s", (int)(len - 8U), result_type + 7);
    return inner_type_out[0] != '\0';
}

static bool v3_emit_result_field_address(const V3SystemLinkPlanStub *plan,
                                         const V3LoweringPlanStub *lowering,
                                         const V3LoweredFunctionStub *current_function,
                                         const V3ImportAlias *aliases,
                                         size_t alias_count,
                                         const V3AsmLocalSlot *locals,
                                         size_t local_count,
                                         const char *arg_expr,
                                         const char *field_name,
                                         int addr_reg,
                                         int32_t call_arg_base,
                                         int32_t string_temp_base,
                                         int32_t string_temp_stride,
                                         int32_t *string_label_index_io,
                                         int call_depth,
                                         char *out,
                                         size_t cap,
                                         char *field_type_out,
                                         size_t field_type_cap,
                                         char *field_abi_out,
                                         size_t field_abi_cap) {
    char arg_type[256];
    char arg_abi[32];
    char trimmed[4096];
    char temp_name[128];
    int32_t field_offset = 0;
    int32_t local_index;
    v3_trim_copy_text(arg_expr, trimmed, sizeof(trimmed));
    if (!v3_infer_expr_type(plan,
                            lowering,
                            current_function,
                            aliases,
                            alias_count,
                            locals,
                            local_count,
                            trimmed,
                            arg_type,
                            sizeof(arg_type),
                            arg_abi,
                            sizeof(arg_abi)) ||
        strcmp(arg_abi, "composite") != 0 ||
        !v3_startswith(arg_type, "Result[")) {
        return false;
    }
    local_index = v3_find_local_slot(locals, local_count, trimmed);
    if (local_index >= 0) {
        if (!v3_emit_local_value_address(out, cap, &locals[local_index], addr_reg)) {
            return false;
        }
    } else {
        v3_expr_temp_name(current_function, trimmed, temp_name, sizeof(temp_name));
        local_index = v3_find_local_slot(locals, local_count, temp_name);
        if (local_index >= 0) {
            if (!v3_materialize_composite_expr_into_slot(plan,
                                                         lowering,
                                                         current_function,
                                                         aliases,
                                                         alias_count,
                                                         locals,
                                                         local_count,
                                                         "__v3_result_expr",
                                                         0U,
                                                         trimmed,
                                                         &locals[local_index],
                                                         call_arg_base,
                                                         string_temp_base,
                                                         string_temp_stride,
                                                         string_label_index_io,
                                                         call_depth + 1,
                                                         out,
                                                         cap) ||
                !v3_emit_local_value_address(out, cap, &locals[local_index], addr_reg)) {
                return false;
            }
        } else if (!v3_emit_member_access_address(plan,
                                                  lowering,
                                                  current_function,
                                                  aliases,
                                                  alias_count,
                                                  locals,
                                                  local_count,
                                                  trimmed,
                                                  arg_type,
                                                  sizeof(arg_type),
                                                  arg_abi,
                                                  sizeof(arg_abi),
                                                  addr_reg,
                                                  0,
                                                  0,
                                                  0,
                                                  0,
                                                  out,
                                                  cap) &&
                   !v3_emit_field_path_address(out,
                                               cap,
                                               locals,
                                               local_count,
                                               lowering,
                                               trimmed,
                                               arg_type,
                                               sizeof(arg_type),
                                               arg_abi,
                                               sizeof(arg_abi),
                                               addr_reg)) {
            return false;
        }
    }
    if (!v3_resolve_field_meta(lowering,
                               arg_type,
                               field_name,
                               field_type_out,
                               field_type_cap,
                               field_abi_out,
                               field_abi_cap,
                               &field_offset)) {
        return false;
    }
    if (field_offset != 0 &&
        !v3_emit_add_address_offset(out, cap, addr_reg, addr_reg, field_offset)) {
        return false;
    }
    return true;
}

static bool v3_codegen_intrinsic_len_expr(const V3SystemLinkPlanStub *plan,
                                          const V3LoweringPlanStub *lowering,
                                          const V3LoweredFunctionStub *current_function,
                                          const V3ImportAlias *aliases,
                                          size_t alias_count,
                                          const V3AsmLocalSlot *locals,
                                          size_t local_count,
                                          const char *arg_expr,
                                          int target_reg,
                                          char *out,
                                          size_t cap) {
    char trimmed[4096];
    char field_type[256];
    char field_abi[32];
    char arg_type[256];
    char arg_abi[32];
    int32_t local_index;
    v3_trim_copy_text(arg_expr, trimmed, sizeof(trimmed));
    if (v3_parse_string_literal_text(trimmed, field_type, sizeof(field_type))) {
        return v3_emit_mov_imm(out, cap, target_reg, (int64_t)strlen(field_type));
    }
    if (strcmp(trimmed, "[]") == 0) {
        return v3_emit_mov_imm(out, cap, target_reg, 0);
    }
    if (v3_emit_member_access_address(plan,
                                      lowering,
                                      current_function,
                                      aliases,
                                      alias_count,
                                      locals,
                                      local_count,
                                      trimmed,
                                      field_type,
                                      sizeof(field_type),
                                      field_abi,
                                      sizeof(field_abi),
                                      15,
                                      0,
                                      0,
                                      0,
                                      0,
                                      out,
                                      cap) ||
        v3_emit_field_path_address(out,
                                   cap,
                                   locals,
                                   local_count,
                                   lowering,
                                   trimmed,
                                   field_type,
                                   sizeof(field_type),
                                   field_abi,
                                   sizeof(field_abi),
                                   15)) {
        int32_t offset = 0;
        if (strcmp(field_type, "str") == 0 || strcmp(field_type, "Bytes") == 0) {
            offset = 8;
        } else if (strlen(field_type) > 2U && strcmp(field_type + strlen(field_type) - 2U, "[]") == 0) {
            offset = 0;
        } else {
            return false;
        }
        v3_emit_load_scalar_from_address(out, cap, 15, offset, "i32", target_reg);
        return true;
    }
    local_index = v3_find_local_slot(locals, local_count, trimmed);
    if (local_index >= 0) {
        int32_t offset = 0;
        if (strcmp(locals[local_index].type_text, "str") == 0 || strcmp(locals[local_index].type_text, "Bytes") == 0) {
            offset = 8;
        } else if (strlen(locals[local_index].type_text) > 2U &&
                   strcmp(locals[local_index].type_text + strlen(locals[local_index].type_text) - 2U, "[]") == 0) {
            offset = 0;
        } else {
            return false;
        }
        if (!v3_emit_local_value_address(out, cap, &locals[local_index], 15)) {
            return false;
        }
        v3_emit_load_scalar_from_address(out, cap, 15, offset, "i32", target_reg);
        return true;
    }
    if (!v3_infer_expr_type(plan,
                            lowering,
                            current_function,
                            aliases,
                            alias_count,
                            locals,
                            local_count,
                            trimmed,
                            arg_type,
                            sizeof(arg_type),
                            arg_abi,
                            sizeof(arg_abi))) {
        return false;
    }
    if (strcmp(arg_type, "str") != 0 &&
        strcmp(arg_type, "Bytes") != 0 &&
        !(strlen(arg_type) > 2U && strcmp(arg_type + strlen(arg_type) - 2U, "[]") == 0)) {
        return false;
    }
    return false;
}

static bool v3_codegen_expr_scalar(const V3SystemLinkPlanStub *plan,
                                   const V3LoweringPlanStub *lowering,
                                   const V3LoweredFunctionStub *current_function,
                                   const V3ImportAlias *aliases,
                                   size_t alias_count,
                                   const V3AsmLocalSlot *locals,
                                   size_t local_count,
                                   const char *expr_text,
                                   const char *expected_abi,
                                   int target_reg,
                                   int32_t call_arg_base,
                                   int32_t string_temp_base,
                                   int32_t string_temp_stride,
                                   int call_depth,
                                   char *out,
                                   size_t cap) {
    char expr[8192];
    char callee[PATH_MAX];
    char arg_text[4096] = {0};
    char base_expr[4096];
    char index_expr[4096];
    char surface_error[256];
    int32_t local_index;
    int32_t char_value = 0;
    int64_t literal_value;
    V3ExprSurface surface;
    if (target_reg >= 15) {
        return false;
    }
    if (!v3_abi_class_scalar_or_ptr(expected_abi)) {
        return false;
    }
    v3_trim_copy_text(expr_text, expr, sizeof(expr));
    if (expr[0] == '\0') {
        return false;
    }
    if (!v3_classify_expr_surface(expr, &surface, surface_error, sizeof(surface_error))) {
        fprintf(stderr,
                "[cheng_v3_seed] scalar expr surface classify failed function=%s expr=%s reason=%s\n",
                current_function ? current_function->symbol_text : "-",
                expr,
                surface_error[0] != '\0' ? surface_error : "invalid expr");
        return false;
    }
    snprintf(expr, sizeof(expr), "%s", surface.text);
    if (expr[0] == '"' && expr[strlen(expr) - 1U] == '"' &&
        strcmp(expected_abi, "ptr") == 0) {
        char literal[4096];
        char label[128];
        if (!v3_parse_string_literal_text(expr, literal, sizeof(literal)) ||
            !v3_emit_cstring_literal_label(plan,
                                           current_function,
                                           literal,
                                           NULL,
                                           label,
                                           sizeof(label),
                                           out,
                                           cap)) {
            return false;
        }
        return v3_emit_symbol_address_for_target(plan, out, cap, target_reg, label);
    }
    if (expr[0] == '-' && expr[1] != '\0' &&
        !v3_parse_int_literal_text(expr, &literal_value)) {
        char inner_type[256];
        char inner_abi[32];
        if (!v3_infer_expr_type(plan,
                                lowering,
                                current_function,
                                aliases,
                                alias_count,
                                locals,
                                local_count,
                                expr + 1,
                                inner_type,
                                sizeof(inner_type),
                                inner_abi,
                                sizeof(inner_abi)) ||
            !v3_abi_class_scalar_or_ptr(inner_abi) ||
            strcmp(inner_abi, "bool") == 0 ||
            strcmp(inner_abi, "ptr") == 0 ||
            !v3_codegen_expr_scalar(plan,
                                    lowering,
                                    current_function,
                                    aliases,
                                    alias_count,
                                    locals,
                                    local_count,
                                    expr + 1,
                                    inner_abi,
                                    target_reg,
                                    call_arg_base,
                                    string_temp_base,
                                    string_temp_stride,
                                    call_depth + 1,
                                    out,
                                    cap)) {
            return false;
        }
        if (strcmp(inner_abi, "i64") == 0) {
            if (v3_active_target_is_linux_x86_64()) {
                v3_text_appendf(out, cap, "  negq %s\n", v3_x64_qreg_name(target_reg));
            } else {
                v3_text_appendf(out, cap, "  neg x%d, x%d\n", target_reg, target_reg);
            }
        } else {
            if (v3_active_target_is_linux_x86_64()) {
                v3_text_appendf(out, cap, "  negl %s\n", v3_x64_dreg_name(target_reg));
            } else {
                v3_text_appendf(out, cap, "  neg w%d, w%d\n", target_reg, target_reg);
            }
        }
        return v3_emit_scalar_cast_reg(out,
                                       cap,
                                       inner_type,
                                       inner_abi,
                                       strcmp(expected_abi, "i64") == 0 ? "int64" :
                                       strcmp(expected_abi, "i32") == 0 ? "int32" :
                                       strcmp(expected_abi, "i16") == 0 ? "int16" :
                                       strcmp(expected_abi, "u16") == 0 ? "uint16" :
                                       strcmp(expected_abi, "i8") == 0 ? "int8" :
                                       strcmp(expected_abi, "u8") == 0 ? "uint8" : inner_type,
                                       expected_abi,
                                       target_reg);
    }
    if (v3_parse_address_of_expr(expr, base_expr, sizeof(base_expr))) {
        char pointee_type[256];
        char pointee_abi[32];
        int32_t address_local_index = v3_find_local_slot(locals, local_count, base_expr);
        if (address_local_index >= 0) {
            return v3_emit_local_value_address(out, cap, &locals[address_local_index], target_reg);
        }
        if (!v3_emit_lvalue_address(plan,
                                    lowering,
                                    current_function,
                                    aliases,
                                    alias_count,
                                    locals,
                                    local_count,
                                    base_expr,
                                    pointee_type,
                                    sizeof(pointee_type),
                                    pointee_abi,
                                    sizeof(pointee_abi),
                                    target_reg,
                                    call_arg_base,
                                    string_temp_base,
                                    string_temp_stride,
                                    call_depth + 1,
                                    out,
                                    cap)) {
            return false;
        }
        return true;
    }
    if (v3_parse_deref_expr(expr, base_expr, sizeof(base_expr))) {
        char pointer_type[256];
        char pointer_abi[32];
        char pointee_type[256];
        char pointee_abi[32];
        if (!v3_infer_expr_type(plan,
                                lowering,
                                current_function,
                                aliases,
                                alias_count,
                                locals,
                                local_count,
                                base_expr,
                                pointer_type,
                                sizeof(pointer_type),
                                pointer_abi,
                                sizeof(pointer_abi)) ||
            strcmp(pointer_abi, "ptr") != 0 ||
            !v3_resolve_pointer_pointee_type(lowering,
                                             current_function->owner_module_path,
                                             aliases,
                                             alias_count,
                                             pointer_type,
                                             pointee_type,
                                             sizeof(pointee_type),
                                             pointee_abi,
                                             sizeof(pointee_abi)) ||
            !v3_abi_class_scalar_or_ptr(pointee_abi) ||
            !v3_codegen_expr_scalar(plan,
                                    lowering,
                                    current_function,
                                    aliases,
                                    alias_count,
                                    locals,
                                    local_count,
                                    base_expr,
                                    "ptr",
                                    target_reg,
                                    call_arg_base,
                                    string_temp_base,
                                    string_temp_stride,
                                    call_depth + 1,
                                    out,
                                    cap)) {
            return false;
        }
        if (strcmp(pointee_type, "char") == 0) {
            v3_emit_load_scalar_from_address(out, cap, target_reg, 0, "u8", target_reg);
            return true;
        }
        v3_emit_load_scalar_from_address(out, cap, target_reg, 0, pointee_abi, target_reg);
        return true;
    }
    if (surface.kind == V3_EXPR_SURFACE_IF) {
        int32_t label_id = v3_next_expr_label_id();
        char false_label[128];
        char end_label[128];
        if (!v3_codegen_expr_scalar(plan,
                                    lowering,
                                    current_function,
                                    aliases,
                                    alias_count,
                                    locals,
                                    local_count,
                                    surface.cond,
                                    "bool",
                                    target_reg,
                                    call_arg_base,
                                    string_temp_base,
                                    string_temp_stride,
                                    call_depth + 1,
                                    out,
                                    cap)) {
            return false;
        }
        snprintf(false_label, sizeof(false_label), "L_v3_ifexpr_false_%d", label_id);
        snprintf(end_label, sizeof(end_label), "L_v3_ifexpr_end_%d", label_id);
        if (v3_active_target_is_linux_x86_64()) {
            v3_emit_x64_branch_zero(out, cap, target_reg, false, true, false_label);
        } else {
            v3_text_appendf(out, cap,
                            "  cmp w%d, #0\n"
                            "  b.eq %s\n",
                            target_reg,
                            false_label);
        }
        if (!v3_codegen_expr_scalar(plan,
                                    lowering,
                                    current_function,
                                    aliases,
                                    alias_count,
                                    locals,
                                    local_count,
                                    surface.when_true,
                                    expected_abi,
                                    target_reg,
                                    call_arg_base,
                                    string_temp_base,
                                    string_temp_stride,
                                    call_depth + 1,
                                    out,
                                    cap)) {
            return false;
        }
        if (v3_active_target_is_linux_x86_64()) {
            v3_text_appendf(out, cap,
                            "  jmp %s\n"
                            "%s:\n",
                            end_label,
                            false_label);
        } else {
            v3_text_appendf(out, cap,
                            "  b %s\n"
                            "%s:\n",
                            end_label,
                            false_label);
        }
        if (!v3_codegen_expr_scalar(plan,
                                    lowering,
                                    current_function,
                                    aliases,
                                    alias_count,
                                    locals,
                                    local_count,
                                    surface.when_false,
                                    expected_abi,
                                    target_reg,
                                    call_arg_base,
                                    string_temp_base,
                                    string_temp_stride,
                                    call_depth + 1,
                                    out,
                                    cap)) {
            return false;
        }
        v3_text_appendf(out, cap, "%s:\n", end_label);
        return true;
    }
    if (surface.kind == V3_EXPR_SURFACE_TERNARY) {
        int32_t label_id = v3_next_expr_label_id();
        char false_label[128];
        char end_label[128];
        if (!v3_codegen_expr_scalar(plan,
                                    lowering,
                                    current_function,
                                    aliases,
                                    alias_count,
                                    locals,
                                    local_count,
                                    surface.cond,
                                    "bool",
                                    target_reg,
                                    call_arg_base,
                                    string_temp_base,
                                    string_temp_stride,
                                    call_depth + 1,
                                    out,
                                    cap)) {
            return false;
        }
        snprintf(false_label, sizeof(false_label), "L_v3_expr_false_%d", label_id);
        snprintf(end_label, sizeof(end_label), "L_v3_expr_end_%d", label_id);
        if (v3_active_target_is_linux_x86_64()) {
            v3_emit_x64_branch_zero(out, cap, target_reg, false, true, false_label);
        } else {
            v3_text_appendf(out, cap,
                            "  cmp w%d, #0\n"
                            "  b.eq %s\n",
                            target_reg,
                            false_label);
        }
        if (!v3_codegen_expr_scalar(plan,
                                    lowering,
                                    current_function,
                                    aliases,
                                    alias_count,
                                    locals,
                                    local_count,
                                    surface.when_true,
                                    expected_abi,
                                    target_reg,
                                    call_arg_base,
                                    string_temp_base,
                                    string_temp_stride,
                                    call_depth + 1,
                                    out,
                                    cap)) {
            return false;
        }
        if (v3_active_target_is_linux_x86_64()) {
            v3_text_appendf(out, cap,
                            "  jmp %s\n"
                            "%s:\n",
                            end_label,
                            false_label);
        } else {
            v3_text_appendf(out, cap,
                            "  b %s\n"
                            "%s:\n",
                            end_label,
                            false_label);
        }
        if (!v3_codegen_expr_scalar(plan,
                                    lowering,
                                    current_function,
                                    aliases,
                                    alias_count,
                                    locals,
                                    local_count,
                                    surface.when_false,
                                    expected_abi,
                                    target_reg,
                                    call_arg_base,
                                    string_temp_base,
                                    string_temp_stride,
                                    call_depth + 1,
                                    out,
                                    cap)) {
            return false;
        }
        v3_text_appendf(out, cap, "%s:\n", end_label);
        return true;
    }
    if (v3_codegen_binary_expr_scalar(plan, lowering, current_function, aliases, alias_count,
                                      locals, local_count, expr, "||", "bool", target_reg,
                                      call_arg_base, string_temp_base, string_temp_stride, call_depth,
                                      out, cap)) {
        if (v3_active_target_is_linux_x86_64()) {
            v3_emit_x64_cmp_zero_set_bool(out, cap, target_reg, target_reg, false, "ne");
        } else {
            v3_text_appendf(out, cap, "  cmp w%d, #0\n  cset w%d, ne\n", target_reg, target_reg);
        }
        return true;
    }
    if (v3_codegen_binary_expr_scalar(plan, lowering, current_function, aliases, alias_count,
                                      locals, local_count, expr, "&&", "bool", target_reg,
                                      call_arg_base, string_temp_base, string_temp_stride, call_depth,
                                      out, cap)) {
        if (v3_active_target_is_linux_x86_64()) {
            v3_emit_x64_cmp_zero_set_bool(out, cap, target_reg, target_reg, false, "ne");
        } else {
            v3_text_appendf(out, cap, "  cmp w%d, #0\n  cset w%d, ne\n", target_reg, target_reg);
        }
        return true;
    }
    if (v3_codegen_compare_expr_scalar(plan, lowering, current_function, aliases, alias_count,
                                       locals, local_count, expr, "==", target_reg,
                                       call_arg_base, string_temp_base, string_temp_stride, call_depth,
                                       out, cap) ||
        v3_codegen_compare_expr_scalar(plan, lowering, current_function, aliases, alias_count,
                                       locals, local_count, expr, "!=", target_reg,
                                       call_arg_base, string_temp_base, string_temp_stride, call_depth,
                                       out, cap) ||
        v3_codegen_compare_expr_scalar(plan, lowering, current_function, aliases, alias_count,
                                       locals, local_count, expr, "<=", target_reg,
                                       call_arg_base, string_temp_base, string_temp_stride, call_depth,
                                       out, cap) ||
        v3_codegen_compare_expr_scalar(plan, lowering, current_function, aliases, alias_count,
                                       locals, local_count, expr, ">=", target_reg,
                                       call_arg_base, string_temp_base, string_temp_stride, call_depth,
                                       out, cap) ||
        v3_codegen_compare_expr_scalar(plan, lowering, current_function, aliases, alias_count,
                                       locals, local_count, expr, "<", target_reg,
                                       call_arg_base, string_temp_base, string_temp_stride, call_depth,
                                       out, cap) ||
        v3_codegen_compare_expr_scalar(plan, lowering, current_function, aliases, alias_count,
                                       locals, local_count, expr, ">", target_reg,
                                       call_arg_base, string_temp_base, string_temp_stride, call_depth,
                                       out, cap)) {
        return true;
    }
    if (v3_codegen_binary_expr_scalar(plan, lowering, current_function, aliases, alias_count,
                                      locals, local_count, expr, "|", expected_abi, target_reg,
                                      call_arg_base, string_temp_base, string_temp_stride, call_depth,
                                      out, cap) ||
        v3_codegen_binary_expr_scalar(plan, lowering, current_function, aliases, alias_count,
                                      locals, local_count, expr, "^", expected_abi, target_reg,
                                      call_arg_base, string_temp_base, string_temp_stride, call_depth,
                                      out, cap) ||
        v3_codegen_binary_expr_scalar(plan, lowering, current_function, aliases, alias_count,
                                      locals, local_count, expr, "&", expected_abi, target_reg,
                                      call_arg_base, string_temp_base, string_temp_stride, call_depth,
                                      out, cap) ||
        v3_codegen_binary_expr_scalar(plan, lowering, current_function, aliases, alias_count,
                                      locals, local_count, expr, "+", expected_abi, target_reg,
                                      call_arg_base, string_temp_base, string_temp_stride, call_depth,
                                      out, cap) ||
        v3_codegen_binary_expr_scalar(plan, lowering, current_function, aliases, alias_count,
                                      locals, local_count, expr, "-", expected_abi, target_reg,
                                      call_arg_base, string_temp_base, string_temp_stride, call_depth,
                                      out, cap) ||
        v3_codegen_binary_expr_scalar(plan, lowering, current_function, aliases, alias_count,
                                      locals, local_count, expr, "*", expected_abi, target_reg,
                                      call_arg_base, string_temp_base, string_temp_stride, call_depth,
                                      out, cap) ||
        v3_codegen_binary_expr_scalar(plan, lowering, current_function, aliases, alias_count,
                                      locals, local_count, expr, "/", expected_abi, target_reg,
                                      call_arg_base, string_temp_base, string_temp_stride, call_depth,
                                      out, cap) ||
        v3_codegen_binary_expr_scalar(plan, lowering, current_function, aliases, alias_count,
                                      locals, local_count, expr, "%", expected_abi, target_reg,
                                      call_arg_base, string_temp_base, string_temp_stride, call_depth,
                                      out, cap) ||
        v3_codegen_binary_expr_scalar(plan, lowering, current_function, aliases, alias_count,
                                      locals, local_count, expr, "<<", expected_abi, target_reg,
                                      call_arg_base, string_temp_base, string_temp_stride, call_depth,
                                      out, cap) ||
        v3_codegen_binary_expr_scalar(plan, lowering, current_function, aliases, alias_count,
                                      locals, local_count, expr, ">>", expected_abi, target_reg,
                                      call_arg_base, string_temp_base, string_temp_stride, call_depth,
                                      out, cap)) {
        return true;
    }
    {
        const V3TopLevelConstStub *top_level_const =
            v3_find_expr_top_level_const(lowering,
                                         current_function->owner_module_path,
                                         aliases,
                                         alias_count,
                                         expr);
        if (top_level_const) {
            if (strcmp(top_level_const->abi_class, "bool") == 0) {
                return v3_emit_mov_imm(out, cap, target_reg, top_level_const->bool_value ? 1 : 0);
            }
            if (strcmp(top_level_const->abi_class, "i32") == 0 ||
                strcmp(top_level_const->abi_class, "i64") == 0) {
                return v3_emit_mov_imm(out, cap, target_reg, top_level_const->i64_value);
            }
            return false;
        }
    }
    if (expr[0] == '!') {
        if (!v3_codegen_expr_scalar(plan,
                                    lowering,
                                    current_function,
                                    aliases,
                                    alias_count,
                                    locals,
                                    local_count,
                                    expr + 1,
                                    "bool",
                                    target_reg,
                                    call_arg_base,
                                    string_temp_base,
                                    string_temp_stride,
                                    call_depth,
                                    out,
                                    cap)) {
            return false;
        }
        if (v3_active_target_is_linux_x86_64()) {
            v3_emit_x64_cmp_zero_set_bool(out, cap, target_reg, target_reg, false, "eq");
        } else {
            v3_text_appendf(out, cap, "  cmp w%d, #0\n  cset w%d, eq\n", target_reg, target_reg);
        }
        return true;
    }
    if (v3_parse_index_access_expr(expr, base_expr, sizeof(base_expr), index_expr, sizeof(index_expr))) {
        char field_type[256];
        char field_abi[32];
        char base_type[256];
        char base_abi[32];
        if (v3_emit_index_access_address(plan,
                                         lowering,
                                         current_function,
                                         aliases,
                                         alias_count,
                                         locals,
                                         local_count,
                                         expr,
                                         field_type,
                                         sizeof(field_type),
                                         field_abi,
                                         sizeof(field_abi),
                                         15,
                                         call_arg_base,
                                         string_temp_base,
                                         string_temp_stride,
                                         call_depth,
                                         out,
                                         cap)) {
            if (!v3_infer_expr_type(plan,
                                    lowering,
                                    current_function,
                                    aliases,
                                    alias_count,
                                    locals,
                                    local_count,
                                    base_expr,
                                    base_type,
                                    sizeof(base_type),
                                    base_abi,
                                    sizeof(base_abi)) ||
                !v3_abi_class_scalar_or_ptr(field_abi)) {
                return false;
            }
            if (strcmp(base_type, "Bytes") == 0 ||
                strcmp(base_type, "str") == 0 ||
                strcmp(field_type, "char") == 0) {
                v3_emit_load_scalar_from_address(out, cap, 15, 0, "u8", target_reg);
            } else {
                v3_emit_load_scalar_from_address(out, cap, 15, 0, field_abi, target_reg);
            }
            return true;
        }
        {
            char (*args)[4096] = v3_alloc_call_arg_scratch();
            bool ok;
            snprintf(args[0], sizeof(args[0]), "%s", base_expr);
            snprintf(args[1], sizeof(args[1]), "%s", index_expr);
            ok = v3_codegen_call_scalar(plan,
                                        lowering,
                                        current_function,
                                        aliases,
                                        alias_count,
                                        locals,
                                        local_count,
                                        "[]",
                                        args,
                                        2U,
                                        expected_abi,
                                        target_reg,
                                        call_arg_base,
                                        string_temp_base,
                                        string_temp_stride,
                                        NULL,
                                        call_depth,
                                        out,
                                        cap);
            free(args);
            return ok;
        }
    }
    {
        char field_type[256];
        char field_abi[32];
        if (v3_emit_member_access_address(plan,
                                          lowering,
                                          current_function,
                                          aliases,
                                          alias_count,
                                          locals,
                                          local_count,
                                          expr,
                                          field_type,
                                          sizeof(field_type),
                                          field_abi,
                                          sizeof(field_abi),
                                          15,
                                          call_arg_base,
                                          string_temp_base,
                                          string_temp_stride,
                                          call_depth,
                                          out,
                                          cap) ||
            v3_emit_field_path_address(out,
                                       cap,
                                       locals,
                                       local_count,
                                       lowering,
                                       expr,
                                       field_type,
                                       sizeof(field_type),
                                       field_abi,
                                       sizeof(field_abi),
                                       15)) {
            if (!v3_abi_class_scalar_or_ptr(field_abi)) {
                return false;
            }
            v3_emit_load_scalar_from_address(out, cap, 15, 0, field_abi, target_reg);
            return true;
        }
    }
    {
        char default_type[CHENG_V3_MAX_TYPE_TEXT];
        char normalized_default_type[CHENG_V3_MAX_TYPE_TEXT];
        const char *default_abi;
        if (v3_parse_default_expr_text(expr, default_type, sizeof(default_type))) {
            if (!v3_normalize_type_text(lowering,
                                        current_function->owner_module_path,
                                        aliases,
                                        alias_count,
                                        default_type,
                                        normalized_default_type,
                                        sizeof(normalized_default_type))) {
                return false;
            }
            default_abi = v3_type_abi_class(normalized_default_type);
            if (!v3_abi_class_scalar_or_ptr(default_abi)) {
                return false;
            }
            return v3_emit_mov_imm(out, cap, target_reg, 0);
        }
    }
    {
        size_t arg_count = 0U;
        char (*args)[4096] = v3_alloc_call_arg_scratch();
        if (v3_parse_call_text(expr, callee, sizeof(callee), args, &arg_count, CHENG_V3_MAX_CALL_ARGS)) {
        V3ResultIntrinsicKind result_kind =
            v3_resolve_result_intrinsic_kind(lowering,
                                             current_function->owner_module_path,
                                             aliases,
                                             alias_count,
                                             callee);
        int32_t elem_size_value = 0;
        if (arg_count == 0U &&
            v3_resolve_bound_elem_size_value(lowering,
                                             current_function,
                                             aliases,
                                             alias_count,
                                             callee,
                                             &elem_size_value)) {
            free(args);
            return v3_emit_mov_imm(out, cap, target_reg, elem_size_value);
        }
        if ((result_kind == V3_RESULT_INTRINSIC_IS_ERR ||
             result_kind == V3_RESULT_INTRINSIC_IS_OK) && arg_count == 1U) {
            char field_type[256];
            char field_abi[32];
            if (!v3_emit_result_field_address(plan,
                                              lowering,
                                              current_function,
                                              aliases,
                                              alias_count,
                                              locals,
                                              local_count,
                                              args[0],
                                              "ok",
                                              15,
                                              call_arg_base,
                                              string_temp_base,
                                              string_temp_stride,
                                              NULL,
                                              call_depth,
                                              out,
                                              cap,
                                              field_type,
                                              sizeof(field_type),
                                              field_abi,
                                              sizeof(field_abi))) {
                free(args);
                return false;
            }
            free(args);
            v3_emit_load_scalar_from_address(out, cap, 15, 0, "i32", target_reg);
            if (result_kind == V3_RESULT_INTRINSIC_IS_ERR) {
                if (v3_active_target_is_linux_x86_64()) {
                    v3_emit_x64_cmp_zero_set_bool(out, cap, target_reg, target_reg, false, "eq");
                } else {
                    v3_text_appendf(out, cap, "  cmp w%d, #0\n  cset w%d, eq\n", target_reg, target_reg);
                }
            }
            return true;
        }
        if (result_kind == V3_RESULT_INTRINSIC_VALUE && arg_count == 1U) {
            char field_type[256];
            char field_abi[32];
            if (!v3_emit_result_field_address(plan,
                                              lowering,
                                              current_function,
                                              aliases,
                                              alias_count,
                                              locals,
                                              local_count,
                                              args[0],
                                              "value",
                                              15,
                                              call_arg_base,
                                              string_temp_base,
                                              string_temp_stride,
                                              NULL,
                                              call_depth,
                                              out,
                                              cap,
                                              field_type,
                                              sizeof(field_type),
                                              field_abi,
                                              sizeof(field_abi)) ||
                !v3_abi_class_scalar_or_ptr(field_abi)) {
                free(args);
                return false;
            }
            free(args);
            v3_emit_load_scalar_from_address(out, cap, 15, 0, field_abi, target_reg);
            return true;
        }
        if (strcmp(callee, "len") == 0 && arg_count == 1U) {
            char len_arg[4096];
            snprintf(len_arg, sizeof(len_arg), "%s", args[0]);
            free(args);
            return v3_codegen_intrinsic_len_expr(plan,
                                                 lowering,
                                                 current_function,
                                                 aliases,
                                                 alias_count,
                                                 locals,
                                                 local_count,
                                                 len_arg,
                                                 target_reg,
                                                 out,
                                                 cap);
        }
        if (strcmp(callee, "sizeof") == 0 && arg_count == 1U) {
            int32_t sizeof_value = 0;
            if (!v3_resolve_sizeof_expr_value(lowering,
                                              current_function->owner_module_path,
                                              aliases,
                                              alias_count,
                                              args[0],
                                              &sizeof_value)) {
                free(args);
                return false;
            }
            free(args);
            return v3_emit_mov_imm(out, cap, target_reg, sizeof_value);
        }
        if (strcmp(callee, "new") == 0 && arg_count == 1U) {
            char alloc_type[256];
            char pointee_type[256];
            V3TypeLayoutStub layout;
            if (!v3_normalize_type_text(lowering,
                                        current_function->owner_module_path,
                                        aliases,
                                        alias_count,
                                        args[0],
                                        alloc_type,
                                        sizeof(alloc_type)) ||
                !v3_is_internal_ref_type(alloc_type)) {
                free(args);
                return false;
            }
            v3_strip_internal_ref_type(alloc_type, pointee_type, sizeof(pointee_type));
            if (!v3_compute_type_layout_impl(lowering, pointee_type, &layout, 0U) ||
                !v3_emit_mov_imm(out, cap, v3_first_call_arg_virtual_reg(false), layout.size)) {
                free(args);
                return false;
            }
            v3_emit_call_named_symbol(plan, out, cap, "alloc");
            if (target_reg != 0) {
                v3_emit_move_reg_abi(out, cap, target_reg, 0, "ptr");
            }
            if (target_reg != 15) {
                v3_emit_address_spill_store(out, cap, call_arg_base, 0, target_reg);
                v3_emit_address_spill_load(out, cap, call_arg_base, 0, 15);
                if (!v3_emit_implicit_default_into_address(plan,
                                                           lowering,
                                                           current_function,
                                                           aliases,
                                                           alias_count,
                                                           current_function->owner_module_path,
                                                           current_function->source_path,
                                                           pointee_type,
                                                           15,
                                                           call_arg_base,
                                                           string_temp_base,
                                                           string_temp_stride,
                                                           NULL,
                                                           call_depth,
                                                           out,
                                                           cap,
                                                           0U)) {
                    free(args);
                    return false;
                }
                v3_emit_address_spill_load(out, cap, call_arg_base, 0, target_reg);
                free(args);
                return true;
            }
            {
                bool ok = v3_emit_implicit_default_into_address(plan,
                                                                lowering,
                                                                current_function,
                                                                aliases,
                                                                alias_count,
                                                                current_function->owner_module_path,
                                                                current_function->source_path,
                                                                pointee_type,
                                                                target_reg,
                                                                call_arg_base,
                                                                string_temp_base,
                                                                string_temp_stride,
                                                                NULL,
                                                                call_depth,
                                                                out,
                                                                cap,
                                                                0U);
                free(args);
                return ok;
            }
        }
        {
            char target_type[256];
            char target_abi[32];
            if (arg_count == 1U &&
                v3_resolve_scalar_or_pointer_cast_target(lowering,
                                                         current_function->owner_module_path,
                                                         aliases,
                                                         alias_count,
                                                         callee,
                                                         target_type,
                                                         sizeof(target_type),
                                                         target_abi,
                                                         sizeof(target_abi))) {
                char arg_type[256];
                char arg_abi[32];
                if (!v3_infer_expr_type(plan,
                                        lowering,
                                        current_function,
                                        aliases,
                                        alias_count,
                                        locals,
                                        local_count,
                                        args[0],
                                        arg_type,
                                        sizeof(arg_type),
                                        arg_abi,
                                        sizeof(arg_abi)) ||
                    !v3_codegen_expr_scalar(plan,
                                            lowering,
                                            current_function,
                                            aliases,
                                            alias_count,
                                            locals,
                                            local_count,
                                            args[0],
                                            arg_abi,
                                            target_reg,
                                            call_arg_base,
                                            string_temp_base,
                                            string_temp_stride,
                                            call_depth,
                                            out,
                                            cap) ||
                    !v3_emit_scalar_cast_reg(out,
                                             cap,
                                             arg_type,
                                             arg_abi,
                                             target_type,
                                             target_abi,
                                             target_reg)) {
                    free(args);
                    return false;
                }
                free(args);
                return true;
            }
        }
        if ((strcmp(callee, "int") == 0 ||
             strcmp(callee, "int64") == 0 ||
             strcmp(callee, "int32") == 0 ||
             strcmp(callee, "uint32") == 0 ||
             strcmp(callee, "uint64") == 0 ||
             strcmp(callee, "int16") == 0 ||
             strcmp(callee, "uint16") == 0 ||
             strcmp(callee, "int8") == 0 ||
             strcmp(callee, "uint8") == 0 ||
             strcmp(callee, "ptr") == 0 ||
             strcmp(callee, "cstring") == 0 ||
             strcmp(callee, "bool") == 0) && arg_count == 1U) {
            char arg_type[256];
            char arg_abi[32];
            const char *target_type =
                strcmp(callee, "int") == 0 ? "int32" :
                strcmp(callee, "int32") == 0 ? "int32" :
                strcmp(callee, "uint32") == 0 ? "uint32" :
                strcmp(callee, "int64") == 0 ? "int64" :
                strcmp(callee, "uint64") == 0 ? "uint64" :
                strcmp(callee, "int16") == 0 ? "int16" :
                strcmp(callee, "uint16") == 0 ? "uint16" :
                strcmp(callee, "int8") == 0 ? "int8" :
                strcmp(callee, "uint8") == 0 ? "uint8" :
                strcmp(callee, "bool") == 0 ? "bool" :
                strcmp(callee, "cstring") == 0 ? "cstring" : "ptr";
            const char *target_abi =
                strcmp(target_type, "int64") == 0 || strcmp(target_type, "uint64") == 0 ? "i64" :
                strcmp(target_type, "int16") == 0 ? "i16" :
                strcmp(target_type, "uint16") == 0 ? "u16" :
                strcmp(target_type, "int8") == 0 ? "i8" :
                strcmp(target_type, "uint8") == 0 ? "u8" :
                strcmp(target_type, "bool") == 0 ? "bool" :
                (strcmp(target_type, "ptr") == 0 || strcmp(target_type, "cstring") == 0) ? "ptr" : "i32";
            if (!v3_infer_expr_type(plan,
                                    lowering,
                                    current_function,
                                    aliases,
                                    alias_count,
                                    locals,
                                    local_count,
                                    args[0],
                                    arg_type,
                                    sizeof(arg_type),
                                    arg_abi,
                                    sizeof(arg_abi)) ||
                !v3_codegen_expr_scalar(plan,
                                        lowering,
                                        current_function,
                                        aliases,
                                        alias_count,
                                        locals,
                                        local_count,
                                        args[0],
                                        arg_abi,
                                        target_reg,
                                        call_arg_base,
                                        string_temp_base,
                                        string_temp_stride,
                                        call_depth,
                                        out,
                                        cap) ||
                !v3_emit_scalar_cast_reg(out,
                                         cap,
                                         arg_type,
                                         arg_abi,
                                         target_type,
                                         target_abi,
                                         target_reg)) {
                free(args);
                return false;
            }
            free(args);
            return true;
        }
            {
                bool ok = v3_codegen_call_scalar(plan,
                                                 lowering,
                                                 current_function,
                                                 aliases,
                                                 alias_count,
                                                 locals,
                                                 local_count,
                                                 callee,
                                                 args,
                                                 arg_count,
                                                 expected_abi,
                                                 target_reg,
                                                 call_arg_base,
                                                 string_temp_base,
                                                 string_temp_stride,
                                                 NULL,
                                                 call_depth,
                                                 out,
                                                 cap);
                free(args);
                return ok;
            }
        }
        free(args);
    }
    if (v3_parse_prefix_single_arg_call(expr, callee, sizeof(callee), arg_text, sizeof(arg_text))) {
        V3ResultIntrinsicKind result_kind =
            v3_resolve_result_intrinsic_kind(lowering,
                                             current_function->owner_module_path,
                                             aliases,
                                             alias_count,
                                             callee);
        if (result_kind == V3_RESULT_INTRINSIC_IS_ERR ||
            result_kind == V3_RESULT_INTRINSIC_IS_OK) {
            char field_type[256];
            char field_abi[32];
            if (!v3_emit_result_field_address(plan,
                                              lowering,
                                              current_function,
                                              aliases,
                                              alias_count,
                                              locals,
                                              local_count,
                                              arg_text,
                                              "ok",
                                              15,
                                              call_arg_base,
                                              string_temp_base,
                                              string_temp_stride,
                                              NULL,
                                              call_depth,
                                              out,
                                              cap,
                                              field_type,
                                              sizeof(field_type),
                                              field_abi,
                                              sizeof(field_abi))) {
                return false;
            }
            v3_emit_load_scalar_from_address(out, cap, 15, 0, "i32", target_reg);
            if (result_kind == V3_RESULT_INTRINSIC_IS_ERR) {
                if (v3_active_target_is_linux_x86_64()) {
                    v3_emit_x64_cmp_zero_set_bool(out, cap, target_reg, target_reg, false, "eq");
                } else {
                    v3_text_appendf(out, cap, "  cmp w%d, #0\n  cset w%d, eq\n", target_reg, target_reg);
                }
            }
            return true;
        }
        if (result_kind == V3_RESULT_INTRINSIC_VALUE) {
            char field_type[256];
            char field_abi[32];
            if (!v3_emit_result_field_address(plan,
                                              lowering,
                                              current_function,
                                              aliases,
                                              alias_count,
                                              locals,
                                              local_count,
                                              arg_text,
                                              "value",
                                              15,
                                              call_arg_base,
                                              string_temp_base,
                                              string_temp_stride,
                                              NULL,
                                              call_depth,
                                              out,
                                              cap,
                                              field_type,
                                              sizeof(field_type),
                                              field_abi,
                                              sizeof(field_abi)) ||
                !v3_abi_class_scalar_or_ptr(field_abi)) {
                return false;
            }
            v3_emit_load_scalar_from_address(out, cap, 15, 0, field_abi, target_reg);
            return true;
        }
        if (strcmp(callee, "len") == 0) {
            return v3_codegen_intrinsic_len_expr(plan,
                                                 lowering,
                                                 current_function,
                                                 aliases,
                                                 alias_count,
                                                 locals,
                                                 local_count,
                                                 arg_text,
                                                 target_reg,
                                                 out,
                                                 cap);
        }
        if (strcmp(callee, "sizeof") == 0) {
            int32_t sizeof_value = 0;
            if (!v3_resolve_sizeof_expr_value(lowering,
                                              current_function->owner_module_path,
                                              aliases,
                                              alias_count,
                                              arg_text,
                                              &sizeof_value)) {
                return false;
            }
            return v3_emit_mov_imm(out, cap, target_reg, sizeof_value);
        }
        {
            char target_type[256];
            char target_abi[32];
            if (v3_resolve_scalar_or_pointer_cast_target(lowering,
                                                         current_function->owner_module_path,
                                                         aliases,
                                                         alias_count,
                                                         callee,
                                                         target_type,
                                                         sizeof(target_type),
                                                         target_abi,
                                                         sizeof(target_abi))) {
                char arg_type[256];
                char arg_abi[32];
                if (!v3_infer_expr_type(plan,
                                        lowering,
                                        current_function,
                                        aliases,
                                        alias_count,
                                        locals,
                                        local_count,
                                        arg_text,
                                        arg_type,
                                        sizeof(arg_type),
                                        arg_abi,
                                        sizeof(arg_abi)) ||
                    !v3_codegen_expr_scalar(plan,
                                            lowering,
                                            current_function,
                                            aliases,
                                            alias_count,
                                            locals,
                                            local_count,
                                            arg_text,
                                            arg_abi,
                                            target_reg,
                                            call_arg_base,
                                            string_temp_base,
                                            string_temp_stride,
                                            call_depth,
                                            out,
                                            cap) ||
                    !v3_emit_scalar_cast_reg(out,
                                             cap,
                                             arg_type,
                                             arg_abi,
                                             target_type,
                                             target_abi,
                                             target_reg)) {
                    return false;
                }
                return true;
            }
        }
        if ((strcmp(callee, "int") == 0 ||
             strcmp(callee, "int32") == 0 ||
             strcmp(callee, "uint32") == 0 ||
             strcmp(callee, "int64") == 0 ||
             strcmp(callee, "uint64") == 0 ||
             strcmp(callee, "ptr") == 0 ||
             strcmp(callee, "cstring") == 0 ||
             strcmp(callee, "bool") == 0) &&
            arg_text[0] != '\0') {
            char arg_type[256];
            char arg_abi[32];
            const char *target_type =
                strcmp(callee, "int") == 0 ? "int32" :
                strcmp(callee, "int32") == 0 ? "int32" :
                strcmp(callee, "uint32") == 0 ? "uint32" :
                strcmp(callee, "int64") == 0 ? "int64" :
                strcmp(callee, "uint64") == 0 ? "uint64" :
                strcmp(callee, "bool") == 0 ? "bool" :
                strcmp(callee, "cstring") == 0 ? "cstring" : "ptr";
            const char *target_abi =
                strcmp(target_type, "int64") == 0 || strcmp(target_type, "uint64") == 0 ? "i64" :
                strcmp(target_type, "bool") == 0 ? "bool" :
                (strcmp(target_type, "ptr") == 0 || strcmp(target_type, "cstring") == 0) ? "ptr" : "i32";
            if (!v3_infer_expr_type(plan,
                                    lowering,
                                    current_function,
                                    aliases,
                                    alias_count,
                                    locals,
                                    local_count,
                                    arg_text,
                                    arg_type,
                                    sizeof(arg_type),
                                    arg_abi,
                                    sizeof(arg_abi)) ||
                !v3_codegen_expr_scalar(plan,
                                        lowering,
                                        current_function,
                                        aliases,
                                        alias_count,
                                        locals,
                                        local_count,
                                        arg_text,
                                        arg_abi,
                                        target_reg,
                                        call_arg_base,
                                        string_temp_base,
                                        string_temp_stride,
                                        call_depth,
                                        out,
                                        cap) ||
                !v3_emit_scalar_cast_reg(out,
                                         cap,
                                         arg_type,
                                         arg_abi,
                                         target_type,
                                         target_abi,
                                         target_reg)) {
                return false;
            }
            return true;
        }
        {
            char (*args)[4096] = v3_alloc_call_arg_scratch();
            bool ok;
            snprintf(args[0], sizeof(args[0]), "%s", arg_text);
            ok = v3_codegen_call_scalar(plan,
                                        lowering,
                                        current_function,
                                        aliases,
                                        alias_count,
                                        locals,
                                        local_count,
                                        callee,
                                        args,
                                        1U,
                                        expected_abi,
                                        target_reg,
                                        call_arg_base,
                                        string_temp_base,
                                        string_temp_stride,
                                        NULL,
                                        call_depth,
                                        out,
                                        cap);
            free(args);
            return ok;
        }
    }
    local_index = v3_find_local_slot(locals, local_count, expr);
    if (local_index >= 0) {
        v3_emit_load_slot(out, cap, &locals[local_index], target_reg);
        return true;
    }
    {
        const V3TopLevelConstStub *top_level_const =
            v3_find_expr_top_level_const(lowering,
                                         current_function->owner_module_path,
                                         aliases,
                                         alias_count,
                                         expr);
        if (top_level_const) {
            if (strcmp(top_level_const->abi_class, "bool") == 0) {
                return v3_emit_mov_imm(out, cap, target_reg, top_level_const->bool_value ? 1 : 0);
            }
            if (strcmp(top_level_const->abi_class, "i32") == 0 ||
                strcmp(top_level_const->abi_class, "i64") == 0) {
                return v3_emit_mov_imm(out, cap, target_reg, top_level_const->i64_value);
            }
            return false;
        }
    }
    {
        char global_type[256];
        char global_abi[32];
        if (v3_find_top_level_var_decl(plan,
                                       lowering,
                                       current_function->owner_module_path,
                                       expr,
                                       global_type,
                                       sizeof(global_type),
                                       global_abi,
                                       sizeof(global_abi))) {
            if (!v3_abi_class_scalar_or_ptr(global_abi) ||
                !v3_emit_owner_global_var_address(plan,
                                                  lowering,
                                                  current_function,
                                                  expr,
                                                  global_type,
                                                  sizeof(global_type),
                                                  global_abi,
                                                  sizeof(global_abi),
                                                  15,
                                                  out,
                                                  cap)) {
                return false;
            }
            v3_emit_load_scalar_from_address(out, cap, 15, 0, global_abi, target_reg);
            return true;
        }
    }
    if (strcmp(expected_abi, "ptr") == 0 &&
        strchr(expr, '(') == NULL &&
        strchr(expr, '[') == NULL &&
        strchr(expr, ' ') == NULL) {
        V3AsmCallTarget function_target;
        if (v3_resolve_call_target(plan,
                                   lowering,
                                   current_function,
                                   aliases,
                                   alias_count,
                                   expr,
                                   &function_target)) {
            return v3_emit_symbol_address_for_target(plan,
                                                     out,
                                                     cap,
                                                     target_reg,
                                                     function_target.symbol_name);
        }
    }
    if (v3_parse_char_literal_text(expr, &char_value)) {
        return v3_emit_mov_imm(out, cap, target_reg, char_value);
    }
    if (v3_parse_int_literal_text(expr, &literal_value)) {
        return v3_emit_mov_imm(out, cap, target_reg, literal_value);
    }
    if (strcmp(expr, "true") == 0) {
        return v3_emit_mov_imm(out, cap, target_reg, 1);
    }
    if (strcmp(expr, "false") == 0 || strcmp(expr, "nil") == 0) {
        return v3_emit_mov_imm(out, cap, target_reg, 0);
    }
    return false;
}

static bool v3_emit_slot_address(char *out,
                                 size_t cap,
                                 const V3AsmLocalSlot *slot,
                                 int reg_index) {
    if (slot->stack_offset < 0) {
        return false;
    }
    if (slot->indirect_value) {
        if (!v3_emit_sp_load_reg(out, cap, slot->stack_offset, "ptr", reg_index)) {
            return false;
        }
        return true;
    }
    return v3_emit_sp_offset_address(out, cap, reg_index, slot->stack_offset);
}

static bool v3_parse_named_arg_meta(const char *arg_text,
                                    char *field_out,
                                    size_t field_cap,
                                    char *expr_out,
                                    size_t expr_cap) {
    bool in_string = false;
    int32_t paren_depth = 0;
    int32_t bracket_depth = 0;
    size_t i;
    char trimmed[4096];
    v3_trim_copy_text(arg_text, trimmed, sizeof(trimmed));
    for (i = 0; trimmed[i] != '\0'; ++i) {
        char ch = trimmed[i];
        if (ch == '"' && !v3_quote_is_escaped(trimmed, i)) {
            in_string = !in_string;
            continue;
        }
        if (in_string) {
            continue;
        }
        if (ch == '(') {
            paren_depth += 1;
            continue;
        }
        if (ch == ')') {
            paren_depth -= 1;
            continue;
        }
        if (ch == '[') {
            bracket_depth += 1;
            continue;
        }
        if (ch == ']') {
            bracket_depth -= 1;
            continue;
        }
        if (paren_depth == 0 && bracket_depth == 0 && ch == ':') {
            snprintf(field_out, field_cap, "%.*s", (int)i, trimmed);
            v3_trim_copy_text(field_out, field_out, field_cap);
            snprintf(expr_out, expr_cap, "%s", trimmed + i + 1);
            v3_trim_copy_text(expr_out, expr_out, expr_cap);
            return field_out[0] != '\0' && expr_out[0] != '\0';
        }
    }
    return false;
}

static bool v3_find_duplicate_named_arg_field(const char args[][4096],
                                              size_t arg_count,
                                              char *field_out,
                                              size_t field_cap,
                                              bool *valid_out) {
    char seen_fields[CHENG_V3_MAX_CALL_ARGS][128];
    size_t seen_count = 0U;
    size_t i;
    if (valid_out) {
        *valid_out = true;
    }
    if (field_out && field_cap > 0U) {
        field_out[0] = '\0';
    }
    for (i = 0; i < arg_count; ++i) {
        char field_name[128];
        char field_expr[4096];
        size_t j;
        if (!v3_parse_named_arg_meta(args[i],
                                     field_name,
                                     sizeof(field_name),
                                     field_expr,
                                     sizeof(field_expr))) {
            if (valid_out) {
                *valid_out = false;
            }
            return false;
        }
        for (j = 0; j < seen_count; ++j) {
            if (strcmp(seen_fields[j], field_name) == 0) {
                if (field_out && field_cap > 0U) {
                    snprintf(field_out, field_cap, "%s", field_name);
                }
                return true;
            }
        }
        if (seen_count >= CHENG_V3_MAX_CALL_ARGS) {
            if (valid_out) {
                *valid_out = false;
            }
            return false;
        }
        snprintf(seen_fields[seen_count], sizeof(seen_fields[seen_count]), "%s", field_name);
        seen_count += 1U;
    }
    return false;
}

static bool v3_named_arg_fields_unique(const char args[][4096], size_t arg_count) {
    bool valid = true;
    if (v3_find_duplicate_named_arg_field(args, arg_count, NULL, 0U, &valid)) {
        return false;
    }
    if (!valid) {
        return false;
    }
    return true;
}

static bool v3_tuple_args_cover_all_items(const char *tuple_type_text,
                                          const char args[][4096],
                                          size_t arg_count) {
    char (*items)[4096] = (char (*)[4096])v3_xmalloc(sizeof(char[CHENG_V3_MAX_TYPE_FIELDS][4096]));
    V3TypeFieldDef *fields = (V3TypeFieldDef *)v3_xmalloc(sizeof(V3TypeFieldDef) * CHENG_V3_MAX_TYPE_FIELDS);
    size_t item_count = 0U;
    size_t i;
    bool has_named = false;
    bool ok = false;
    if (!v3_parse_tuple_type_items(tuple_type_text,
                                   items,
                                   &item_count,
                                   CHENG_V3_MAX_TYPE_FIELDS)) {
        goto cleanup;
    }
    if (arg_count != item_count) {
        goto cleanup;
    }
    for (i = 0U; i < arg_count; ++i) {
        char field_name[128];
        char field_expr[4096];
        if (v3_parse_named_arg_meta(args[i],
                                    field_name,
                                    sizeof(field_name),
                                    field_expr,
                                    sizeof(field_expr))) {
            has_named = true;
            break;
        }
    }
    if (!has_named) {
        ok = true;
        goto cleanup;
    }
    if (!v3_named_arg_fields_unique(args, arg_count)) {
        goto cleanup;
    }
    for (i = 0U; i < arg_count; ++i) {
        char field_name[128];
        char field_expr[4096];
        if (!v3_parse_named_arg_meta(args[i],
                                     field_name,
                                     sizeof(field_name),
                                     field_expr,
                                     sizeof(field_expr))) {
            goto cleanup;
        }
    }
    for (i = 0U; i < item_count; ++i) {
        size_t j;
        bool seen = false;
        if (!v3_parse_type_field_spec(items[i], true, &fields[i]) ||
            !fields[i].has_explicit_name) {
            goto cleanup;
        }
        for (j = 0U; j < arg_count; ++j) {
            char field_name[128];
            char field_expr[4096];
            if (v3_parse_named_arg_meta(args[j],
                                        field_name,
                                        sizeof(field_name),
                                        field_expr,
                                        sizeof(field_expr)) &&
                strcmp(field_name, fields[i].name) == 0) {
                seen = true;
                break;
            }
        }
        if (!seen) {
            goto cleanup;
        }
    }
    ok = true;
cleanup:
    free(fields);
    free(items);
    return ok;
}

static bool v3_tuple_expr_covers_all_items(const char *tuple_type_text,
                                           const char *expr_text) {
    char inner[4096];
    char callee[PATH_MAX];
    char field_name[128];
    char field_expr[4096];
    if (!v3_is_tuple_type_text(tuple_type_text) ||
        expr_text == NULL ||
        expr_text[0] == '\0') {
        return true;
    }
    {
        char (*args)[4096] = v3_alloc_call_arg_scratch();
        size_t arg_count = 0U;
        bool parsed = v3_parse_call_text(expr_text,
                                         callee,
                                         sizeof(callee),
                                         args,
                                         &arg_count,
                                         CHENG_V3_MAX_CALL_ARGS);
        bool ok = parsed &&
                  v3_is_tuple_type_text(callee) &&
                  strcmp(callee, tuple_type_text) == 0 &&
                  v3_tuple_args_cover_all_items(tuple_type_text, args, arg_count);
        free(args);
        if (parsed && v3_is_tuple_type_text(callee)) {
            return ok;
        }
    }
    if (v3_trim_outer_parens(expr_text, inner, sizeof(inner))) {
        char (*args)[4096] = v3_alloc_call_arg_scratch();
        size_t arg_count = 0U;
        char inner_field_name[128];
        char inner_field_expr[4096];
        bool split = v3_split_top_level_args(inner, args, &arg_count, CHENG_V3_MAX_CALL_ARGS);
        bool ok = split &&
                  v3_tuple_args_cover_all_items(tuple_type_text, args, arg_count);
        if (!split &&
            v3_parse_named_arg_meta(inner,
                                    inner_field_name,
                                    sizeof(inner_field_name),
                                    inner_field_expr,
                                    sizeof(inner_field_expr))) {
            snprintf(args[0], sizeof(args[0]), "%s", inner);
            ok = v3_tuple_args_cover_all_items(tuple_type_text, args, 1U);
            free(args);
            return ok;
        }
        free(args);
        if (split) {
            return ok;
        }
    }
    if (v3_parse_named_arg_meta(expr_text,
                                field_name,
                                sizeof(field_name),
                                field_expr,
                                sizeof(field_expr))) {
        char (*args)[4096] = v3_alloc_call_arg_scratch();
        bool ok;
        snprintf(args[0], sizeof(args[0]), "%s", expr_text);
        ok = v3_tuple_args_cover_all_items(tuple_type_text, args, 1U);
        free(args);
        return ok;
    }
    return true;
}

static bool v3_tuple_binding_expr_complete(const char *type_text,
                                           const char *expr_text) {
    if (!v3_is_tuple_type_text(type_text) ||
        expr_text == NULL ||
        expr_text[0] == '\0') {
        return true;
    }
    return v3_tuple_expr_covers_all_items(type_text, expr_text);
}

static bool v3_tuple_binding_expr_omits_items(const char *statement) {
    const char *prefix = NULL;
    const char *cursor;
    char left[CHENG_V3_MAX_TYPE_TEXT];
    char type_text[CHENG_V3_MAX_TYPE_TEXT];
    char expr[4096];
    int32_t eq_index;
    int32_t colon_index;
    if (statement == NULL) {
        return false;
    }
    if (v3_startswith(statement, "let ")) {
        prefix = "let ";
    } else if (v3_startswith(statement, "var ")) {
        prefix = "var ";
    } else {
        return false;
    }
    cursor = statement + strlen(prefix);
    eq_index = v3_find_top_level_assignment_eq(cursor);
    if (eq_index < 0) {
        return false;
    }
    snprintf(left, sizeof(left), "%.*s", eq_index, cursor);
    v3_trim_copy_text(left, left, sizeof(left));
    colon_index = v3_find_top_level_char(left, ':');
    if (colon_index <= 0) {
        return false;
    }
    snprintf(type_text, sizeof(type_text), "%s", left + colon_index + 1);
    v3_trim_copy_text(type_text, type_text, sizeof(type_text));
    if (!v3_is_tuple_type_text(type_text)) {
        return false;
    }
    snprintf(expr, sizeof(expr), "%s", cursor + eq_index + 1);
    v3_trim_copy_text(expr, expr, sizeof(expr));
    return expr[0] != '\0' &&
           !v3_tuple_expr_covers_all_items(type_text, expr);
}

static int32_t v3_type_storage_size_i32(const V3LoweringPlanStub *lowering,
                                        const char *type_text) {
    const char *abi = v3_type_abi_class(type_text);
    V3TypeLayoutStub layout;
    if (strcmp(abi, "bool") == 0 ||
        strcmp(abi, "i8") == 0 ||
        strcmp(abi, "u8") == 0) {
        return 1;
    }
    if (strcmp(abi, "i16") == 0 ||
        strcmp(abi, "u16") == 0) {
        return 2;
    }
    if (strcmp(abi, "i32") == 0) {
        return 4;
    }
    if (strcmp(abi, "i64") == 0 ||
        strcmp(abi, "ptr") == 0) {
        return 8;
    }
    if (!v3_compute_type_layout_impl(lowering, type_text, &layout, 0U)) {
        return -1;
    }
    return layout.size;
}

static void v3_init_default_eval_function(const V3LoweredFunctionStub *current_function,
                                          const char *owner_module_path,
                                          const char *source_path,
                                          V3LoweredFunctionStub *out) {
    memset(out, 0, sizeof(*out));
    if (current_function) {
        memcpy(out, current_function, sizeof(*out));
    }
    if (owner_module_path) {
        snprintf(out->owner_module_path, sizeof(out->owner_module_path), "%s", owner_module_path);
    }
    if (source_path) {
        snprintf(out->source_path, sizeof(out->source_path), "%s", source_path);
    }
    if (out->symbol_text[0] == '\0') {
        snprintf(out->symbol_text, sizeof(out->symbol_text), "%s", "__v3_default_init");
    }
}

static const char *v3_callee_base_name(const char *callee) {
    const char *base = strrchr(callee, '.');
    const char *scope = strstr(callee, "::");
    if (base) {
        return base + 1;
    }
    while (scope) {
        callee = scope + 2;
        scope = strstr(callee, "::");
    }
    return callee;
}

static bool v3_find_default_field_name_in_expr(const char *expr_text,
                                               const V3TypeFieldDef *fields,
                                               size_t field_count,
                                               char *field_out,
                                               size_t field_cap) {
    char expr[CHENG_V3_MAX_DEFAULT_EXPR];
    bool in_string = false;
    size_t i;
    size_t token_start = 0U;
    bool in_ident = false;
    if (field_out && field_cap > 0U) {
        field_out[0] = '\0';
    }
    v3_trim_copy_text(expr_text, expr, sizeof(expr));
    for (i = 0; ; ++i) {
        unsigned char ch = (unsigned char)expr[i];
        if (expr[i] == '"' && !v3_quote_is_escaped(expr, i)) {
            in_string = !in_string;
        }
        if (!in_string && (isalpha(ch) || ch == '_')) {
            if (!in_ident) {
                token_start = i;
                in_ident = true;
            }
        } else if (in_ident) {
            char token[128];
            size_t j;
            snprintf(token, sizeof(token), "%.*s", (int)(i - token_start), expr + token_start);
            for (j = 0; j < field_count; ++j) {
                if (fields[j].has_explicit_name &&
                    fields[j].name[0] != '\0' &&
                    strcmp(fields[j].name, token) == 0) {
                    if (field_out && field_cap > 0U) {
                        snprintf(field_out, field_cap, "%s", token);
                    }
                    return true;
                }
            }
            in_ident = false;
        }
        if (expr[i] == '\0') {
            break;
        }
    }
    return false;
}

static V3FieldDefaultExprKind v3_classify_field_default_expr(const char *expr_text,
                                                             const V3TypeFieldDef *fields,
                                                             size_t field_count,
                                                             char *detail_out,
                                                             size_t detail_cap) {
    static const char *OPS[] = {
        "||", "&&", "==", "!=", "<=", ">=", "<<", ">>", "<", ">", "|", "^", "&", "+", "-", "*", "/", "%"
    };
    char expr[CHENG_V3_MAX_DEFAULT_EXPR];
    char left[CHENG_V3_MAX_DEFAULT_EXPR];
    char right[CHENG_V3_MAX_DEFAULT_EXPR];
    char callee[PATH_MAX];
    char arg_text[4096];
    size_t i;
    char inner[CHENG_V3_MAX_DEFAULT_EXPR];
    if (detail_out && detail_cap > 0U) {
        detail_out[0] = '\0';
    }
    if (!expr_text) {
        return V3_FIELD_DEFAULT_EXPR_UNSUPPORTED;
    }
    v3_trim_copy_text(expr_text, expr, sizeof(expr));
    if (expr[0] == '\0') {
        return V3_FIELD_DEFAULT_EXPR_UNSUPPORTED;
    }
    while (v3_trim_outer_parens(expr, inner, sizeof(inner))) {
        snprintf(expr, sizeof(expr), "%s", inner);
    }
    if (expr[0] == '"' && expr[strlen(expr) - 1U] == '"') {
        return V3_FIELD_DEFAULT_EXPR_STABLE;
    }
    if (strcmp(expr, "[]") == 0 ||
        strcmp(expr, "true") == 0 ||
        strcmp(expr, "false") == 0) {
        return V3_FIELD_DEFAULT_EXPR_STABLE;
    }
    if (v3_const_parse_i32_literal_text(expr, NULL) ||
        v3_const_parse_f64_literal_text(expr, NULL)) {
        return V3_FIELD_DEFAULT_EXPR_STABLE;
    }
    if (v3_parse_default_expr_text(expr, inner, sizeof(inner))) {
        return V3_FIELD_DEFAULT_EXPR_STABLE;
    }
    if (v3_parse_if_expr(expr,
                         left,
                         sizeof(left),
                         right,
                         sizeof(right),
                         inner,
                         sizeof(inner))) {
        V3FieldDefaultExprKind branch_kind =
            v3_classify_field_default_expr(left, fields, field_count, detail_out, detail_cap);
        if (branch_kind != V3_FIELD_DEFAULT_EXPR_STABLE) {
            return branch_kind;
        }
        branch_kind =
            v3_classify_field_default_expr(right, fields, field_count, detail_out, detail_cap);
        if (branch_kind != V3_FIELD_DEFAULT_EXPR_STABLE) {
            return branch_kind;
        }
        return v3_classify_field_default_expr(inner, fields, field_count, detail_out, detail_cap);
    }
    if (v3_parse_ternary_expr(expr,
                              left,
                              sizeof(left),
                              right,
                              sizeof(right),
                              inner,
                              sizeof(inner))) {
        V3FieldDefaultExprKind branch_kind =
            v3_classify_field_default_expr(left, fields, field_count, detail_out, detail_cap);
        if (branch_kind != V3_FIELD_DEFAULT_EXPR_STABLE) {
            return branch_kind;
        }
        branch_kind =
            v3_classify_field_default_expr(right, fields, field_count, detail_out, detail_cap);
        if (branch_kind != V3_FIELD_DEFAULT_EXPR_STABLE) {
            return branch_kind;
        }
        return v3_classify_field_default_expr(inner, fields, field_count, detail_out, detail_cap);
    }
    if (expr[0] == '!' || expr[0] == '-' || expr[0] == '~') {
        return v3_classify_field_default_expr(expr + 1, fields, field_count, detail_out, detail_cap);
    }
    for (i = 0; i < sizeof(OPS) / sizeof(OPS[0]); ++i) {
        int32_t op_index = v3_find_top_level_binary_op_last(expr, OPS[i]);
        if (op_index > 0) {
            snprintf(left, sizeof(left), "%.*s", op_index, expr);
            snprintf(right, sizeof(right), "%s", expr + op_index + strlen(OPS[i]));
            v3_trim_copy_text(left, left, sizeof(left));
            v3_trim_copy_text(right, right, sizeof(right));
            {
                V3FieldDefaultExprKind side_kind =
                    v3_classify_field_default_expr(left, fields, field_count, detail_out, detail_cap);
                if (side_kind != V3_FIELD_DEFAULT_EXPR_STABLE) {
                    return side_kind;
                }
                return v3_classify_field_default_expr(right, fields, field_count, detail_out, detail_cap);
            }
        }
    }
    if (v3_parse_member_access_expr(expr, left, sizeof(left), right, sizeof(right)) ||
        v3_parse_index_access_expr(expr, left, sizeof(left), right, sizeof(right))) {
        if (v3_find_default_field_name_in_expr(expr, fields, field_count, left, sizeof(left))) {
            if (detail_out && detail_cap > 0U) {
                snprintf(detail_out, detail_cap, "%s", left);
            }
            return V3_FIELD_DEFAULT_EXPR_SIBLING_FIELD;
        }
        return V3_FIELD_DEFAULT_EXPR_UNSUPPORTED;
    }
    {
        char (*args)[4096] = v3_alloc_call_arg_scratch();
        size_t arg_count = 0U;
        if (v3_parse_call_text(expr, callee, sizeof(callee), args, &arg_count, CHENG_V3_MAX_CALL_ARGS) ||
            v3_parse_prefix_single_arg_call(expr, callee, sizeof(callee), arg_text, sizeof(arg_text))) {
            const char *base = v3_callee_base_name(callee);
            bool allowed =
                strcmp(callee, "new") == 0 ||
                strcmp(base, "int") == 0 ||
                strcmp(base, "int8") == 0 ||
                strcmp(base, "int16") == 0 ||
                strcmp(base, "int32") == 0 ||
                strcmp(base, "int64") == 0 ||
                strcmp(base, "uint8") == 0 ||
                strcmp(base, "uint16") == 0 ||
                strcmp(base, "uint32") == 0 ||
                strcmp(base, "uint64") == 0 ||
                strcmp(base, "bool") == 0 ||
                strcmp(base, "char") == 0 ||
                strcmp(base, "float") == 0 ||
                strcmp(base, "float32") == 0 ||
                strcmp(base, "float64") == 0 ||
                strcmp(base, "str") == 0 ||
                strcmp(base, "cstring") == 0 ||
                strcmp(base, "ptr") == 0 ||
                (base[0] >= 'A' && base[0] <= 'Z');
            if (!allowed) {
                if (detail_out && detail_cap > 0U) {
                    snprintf(detail_out, detail_cap, "%s", callee);
                }
                free(args);
                return V3_FIELD_DEFAULT_EXPR_CALL_UNSUPPORTED;
            }
            if (arg_count == 0U && arg_text[0] != '\0') {
                snprintf(args[0], sizeof(args[0]), "%s", arg_text);
                arg_count = 1U;
            }
            for (i = 0; i < arg_count; ++i) {
                char named_field[128];
                char named_expr[4096];
                const char *arg_expr = args[i];
                V3FieldDefaultExprKind arg_kind;
                if (v3_parse_named_arg_meta(args[i],
                                            named_field,
                                            sizeof(named_field),
                                            named_expr,
                                            sizeof(named_expr))) {
                    arg_expr = named_expr;
                }
                arg_kind = v3_classify_field_default_expr(arg_expr, fields, field_count, detail_out, detail_cap);
                if (arg_kind != V3_FIELD_DEFAULT_EXPR_STABLE) {
                    free(args);
                    return arg_kind;
                }
            }
            free(args);
            return V3_FIELD_DEFAULT_EXPR_STABLE;
        }
        free(args);
    }
    if (v3_is_bare_identifier_text(expr)) {
        if (v3_find_default_field_name_in_expr(expr, fields, field_count, left, sizeof(left))) {
            if (detail_out && detail_cap > 0U) {
                snprintf(detail_out, detail_cap, "%s", left);
            }
            return V3_FIELD_DEFAULT_EXPR_SIBLING_FIELD;
        }
        return V3_FIELD_DEFAULT_EXPR_STABLE;
    }
    return V3_FIELD_DEFAULT_EXPR_UNSUPPORTED;
}

static void v3_report_invalid_field_default(const char *source_path,
                                            const char *type_text,
                                            const char *field_name,
                                            const char *expr_text,
                                            V3FieldDefaultExprKind kind,
                                            const char *detail) {
    const char *safe_source = source_path != NULL && source_path[0] != '\0' ? source_path : "<unknown>";
    const char *safe_type = type_text != NULL && type_text[0] != '\0' ? type_text : "<anonymous>";
    const char *safe_field = field_name != NULL && field_name[0] != '\0' ? field_name : "<unnamed>";
    const char *safe_expr = expr_text != NULL ? expr_text : "";
    switch (kind) {
        case V3_FIELD_DEFAULT_EXPR_SIBLING_FIELD:
            fprintf(stderr,
                    "[cheng_v3_seed] field default references sibling field source=%s type=%s field=%s sibling=%s expr=%s\n",
                    safe_source,
                    safe_type,
                    safe_field,
                    (detail != NULL && detail[0] != '\0') ? detail : "<unknown>",
                    safe_expr);
            return;
        case V3_FIELD_DEFAULT_EXPR_CALL_UNSUPPORTED:
            fprintf(stderr,
                    "[cheng_v3_seed] field default call unsupported source=%s type=%s field=%s callee=%s expr=%s\n",
                    safe_source,
                    safe_type,
                    safe_field,
                    (detail != NULL && detail[0] != '\0') ? detail : "<unknown>",
                    safe_expr);
            return;
        case V3_FIELD_DEFAULT_EXPR_UNSUPPORTED:
            fprintf(stderr,
                    "[cheng_v3_seed] field default expression unsupported source=%s type=%s field=%s expr=%s\n",
                    safe_source,
                    safe_type,
                    safe_field,
                    safe_expr);
            return;
        case V3_FIELD_DEFAULT_EXPR_STABLE:
        default:
            return;
    }
}

static bool v3_validate_type_field_defaults(const char *source_path,
                                            const char *type_text,
                                            const V3TypeFieldDef *fields,
                                            size_t field_count) {
    size_t i;
    for (i = 0U; i < field_count; ++i) {
        char detail[256];
        char field_name[128];
        V3FieldDefaultExprKind kind;
        if (!fields[i].has_default_expr) {
            continue;
        }
        if (fields[i].has_explicit_name && fields[i].name[0] != '\0') {
            snprintf(field_name, sizeof(field_name), "%s", fields[i].name);
        } else {
            v3_tuple_field_synthetic_name(i, field_name, sizeof(field_name));
        }
        kind = v3_classify_field_default_expr(fields[i].default_expr,
                                              fields,
                                              field_count,
                                              detail,
                                              sizeof(detail));
        if (kind == V3_FIELD_DEFAULT_EXPR_STABLE) {
            continue;
        }
        v3_report_invalid_field_default(source_path,
                                        type_text,
                                        field_name,
                                        fields[i].default_expr,
                                        kind,
                                        detail);
        return false;
    }
    return true;
}

static bool v3_expr_is_stable_field_default(const char *expr_text,
                                            const V3TypeFieldDef *fields,
                                            size_t field_count) {
    return v3_classify_field_default_expr(expr_text,
                                          fields,
                                          field_count,
                                          NULL,
                                          0U) == V3_FIELD_DEFAULT_EXPR_STABLE;
}

static bool v3_type_needs_recursive_default_init(const V3LoweringPlanStub *lowering,
                                                 const V3ImportAlias *aliases,
                                                 size_t alias_count,
                                                 const char *type_owner_module_path,
                                                 const char *type_text,
                                                 unsigned depth) {
    char normalized_type[CHENG_V3_MAX_TYPE_TEXT];
    int32_t type_index = -1;
    if (depth > 32U ||
        !v3_normalize_type_text(lowering,
                                type_owner_module_path,
                                aliases,
                                alias_count,
                                type_text,
                                normalized_type,
                                sizeof(normalized_type))) {
        return true;
    }
    if (v3_is_tuple_type_text(normalized_type)) {
        char (*items)[4096] = NULL;
        V3TypeFieldDef *fields = NULL;
        size_t item_count = 0U;
        size_t i;
        bool need_recursive = false;
        items = (char (*)[4096])v3_xmalloc(sizeof(*items) * CHENG_V3_MAX_TYPE_FIELDS);
        fields = (V3TypeFieldDef *)v3_xmalloc(sizeof(*fields) * CHENG_V3_MAX_TYPE_FIELDS);
        if (!v3_parse_tuple_type_items(normalized_type,
                                       items,
                                       &item_count,
                                       CHENG_V3_MAX_TYPE_FIELDS) ||
            item_count == 0U) {
            need_recursive = true;
            goto tuple_cleanup;
        }
        for (i = 0U; i < item_count; ++i) {
            if (!v3_parse_type_field_spec(items[i], true, &fields[i])) {
                need_recursive = true;
                goto tuple_cleanup;
            }
            if (fields[i].has_default_expr ||
                v3_type_needs_recursive_default_init(lowering,
                                                     aliases,
                                                     alias_count,
                                                     type_owner_module_path,
                                                     fields[i].type_text,
                                                     depth + 1U)) {
                need_recursive = true;
                goto tuple_cleanup;
            }
        }
tuple_cleanup:
        free(fields);
        free(items);
        return need_recursive;
    }
    if (v3_find_type_def_by_normalized(lowering, normalized_type, &type_index)) {
        const V3TypeDefStub *type_def = &lowering->type_defs[type_index];
        V3ImportAlias *type_aliases = NULL;
        size_t type_alias_count = 0U;
        char stripped[PATH_MAX];
        char owner_module[PATH_MAX];
        char type_instance_name[128];
        char type_name[128];
        char type_args[CHENG_V3_MAX_TYPE_PARAMS][256];
        size_t type_arg_count = 0U;
        type_aliases = (V3ImportAlias *)v3_xmalloc(sizeof(*type_aliases) * CHENG_V3_MAX_IMPORT_ALIASES);
        v3_strip_internal_ref_type(normalized_type, stripped, sizeof(stripped));
        if (type_aliases == NULL ||
            !v3_split_qualified_type_text(stripped,
                                          owner_module,
                                          sizeof(owner_module),
                                          type_instance_name,
                                          sizeof(type_instance_name)) ||
            !v3_parse_type_instance_text(type_instance_name,
                                         type_name,
                                         sizeof(type_name),
                                         type_args,
                                         &type_arg_count,
                                         CHENG_V3_MAX_TYPE_PARAMS) ||
            !v3_load_import_aliases_from_source_path(type_def->source_path,
                                                     type_aliases,
                                                     &type_alias_count,
                                                     CHENG_V3_MAX_IMPORT_ALIASES)) {
            free(type_aliases);
            return true;
        }
        if (type_def->kind == V3_TYPE_DEF_ALIAS) {
            char alias_target[CHENG_V3_MAX_TYPE_TEXT];
            bool need_recursive = true;
            if (type_def->generic_param_count > 0U) {
                if (type_def->generic_param_count == type_arg_count &&
                    v3_normalize_type_text_with_params(lowering,
                                                       type_def->owner_module_path,
                                                       type_aliases,
                                                       type_alias_count,
                                                       type_def->generic_param_names,
                                                       type_args,
                                                       type_arg_count,
                                                       type_def->alias_target,
                                                       alias_target,
                                                       sizeof(alias_target))) {
                    need_recursive = v3_type_needs_recursive_default_init(lowering,
                                                                          type_aliases,
                                                                          type_alias_count,
                                                                          type_def->owner_module_path,
                                                                          alias_target,
                                                                          depth + 1U);
                }
            } else {
                snprintf(alias_target, sizeof(alias_target), "%s", type_def->alias_target);
                need_recursive = v3_type_needs_recursive_default_init(lowering,
                                                                      type_aliases,
                                                                      type_alias_count,
                                                                      type_def->owner_module_path,
                                                                      alias_target,
                                                                      depth + 1U);
            }
            free(type_aliases);
            return need_recursive;
        }
        if (type_def->kind == V3_TYPE_DEF_RECORD) {
            size_t i;
            for (i = 0U; i < type_def->field_count; ++i) {
                char resolved_field_type[CHENG_V3_MAX_TYPE_TEXT];
                if (type_def->fields[i].has_default_expr) {
                    free(type_aliases);
                    return true;
                }
                if (type_def->generic_param_count > 0U) {
                    if (type_def->generic_param_count != type_arg_count ||
                        !v3_normalize_type_text_with_params(lowering,
                                                            type_def->owner_module_path,
                                                            type_aliases,
                                                            type_alias_count,
                                                            type_def->generic_param_names,
                                                            type_args,
                                                            type_arg_count,
                                                            type_def->fields[i].type_text,
                                                            resolved_field_type,
                                                            sizeof(resolved_field_type))) {
                        free(type_aliases);
                        return true;
                    }
                } else {
                    snprintf(resolved_field_type,
                             sizeof(resolved_field_type),
                             "%s",
                             type_def->fields[i].type_text);
                }
                if (v3_type_needs_recursive_default_init(lowering,
                                                         type_aliases,
                                                         type_alias_count,
                                                         type_def->owner_module_path,
                                                         resolved_field_type,
                                                         depth + 1U)) {
                    free(type_aliases);
                    return true;
                }
            }
            free(type_aliases);
            return false;
        }
        free(type_aliases);
    }
    return false;
}

static bool v3_emit_field_default_expr_into_address(const V3SystemLinkPlanStub *plan,
                                                    const V3LoweringPlanStub *lowering,
                                                    const V3LoweredFunctionStub *current_function,
                                                    const char *type_owner_module_path,
                                                    const char *type_source_path,
                                                    const char *container_type_text,
                                                    const V3ImportAlias *type_aliases,
                                                    size_t type_alias_count,
                                                    const V3TypeFieldDef *fields,
                                                    size_t field_count,
                                                    const char *field_name,
                                                    const char *field_expr,
                                                    const char *field_type,
                                                    int dest_addr_reg,
                                                    int32_t call_arg_base,
                                                    int32_t string_temp_base,
                                                    int32_t string_temp_stride,
                                                    int32_t *string_label_index_io,
                                                    int call_depth,
                                                    char *out,
                                                    size_t cap) {
    V3LoweredFunctionStub eval_function;
    const char *field_abi = v3_type_abi_class(field_type);
    if (!v3_expr_is_stable_field_default(field_expr, fields, field_count)) {
        char detail[256];
        V3FieldDefaultExprKind kind =
            v3_classify_field_default_expr(field_expr, fields, field_count, detail, sizeof(detail));
        v3_report_invalid_field_default(type_source_path,
                                        container_type_text,
                                        field_name,
                                        field_expr,
                                        kind,
                                        detail);
        return false;
    }
    v3_init_default_eval_function(current_function,
                                  type_owner_module_path,
                                  type_source_path,
                                  &eval_function);
    if (v3_abi_class_scalar_or_ptr(field_abi)) {
        if (!v3_codegen_expr_scalar(plan,
                                    lowering,
                                    &eval_function,
                                    type_aliases,
                                    type_alias_count,
                                    NULL,
                                    0U,
                                    field_expr,
                                    field_abi,
                                    9,
                                    call_arg_base,
                                    string_temp_base,
                                    string_temp_stride,
                                    call_depth + 1,
                                    out,
                                    cap)) {
            return false;
        }
        v3_emit_store_scalar_to_address(out, cap, dest_addr_reg, 0, field_abi, 9);
        return true;
    }
    return v3_materialize_composite_expr_into_address(plan,
                                                      lowering,
                                                      &eval_function,
                                                      type_aliases,
                                                      type_alias_count,
                                                      NULL,
                                                      0U,
                                                      field_expr,
                                                      field_type,
                                                      dest_addr_reg,
                                                      call_arg_base,
                                                      string_temp_base,
                                                      string_temp_stride,
                                                      string_label_index_io,
                                                      call_depth + 1,
                                                      out,
                                                      cap);
}

static bool v3_emit_implicit_default_into_address(const V3SystemLinkPlanStub *plan,
                                                  const V3LoweringPlanStub *lowering,
                                                  const V3LoweredFunctionStub *current_function,
                                                  const V3ImportAlias *aliases,
                                                  size_t alias_count,
                                                  const char *type_owner_module_path,
                                                  const char *type_source_path,
                                                  const char *type_text,
                                                  int dest_addr_reg,
                                                  int32_t call_arg_base,
                                                  int32_t string_temp_base,
                                                  int32_t string_temp_stride,
                                                  int32_t *string_label_index_io,
                                                  int call_depth,
                                                  char *out,
                                                  size_t cap,
                                                  unsigned depth) {
    char normalized_type[CHENG_V3_MAX_TYPE_TEXT];
    int32_t type_index = -1;
    int32_t storage_size;
    if (depth > 32U ||
        !v3_normalize_type_text(lowering,
                                type_owner_module_path,
                                aliases,
                                alias_count,
                                type_text,
                                normalized_type,
                                sizeof(normalized_type))) {
        return false;
    }
    if (v3_find_type_def_by_normalized(lowering, normalized_type, &type_index)) {
        const V3TypeDefStub *type_def = &lowering->type_defs[type_index];
        char stripped[PATH_MAX];
        char owner_module[PATH_MAX];
        char type_instance_name[128];
        char type_name[128];
        char type_args[CHENG_V3_MAX_TYPE_PARAMS][256];
        size_t type_arg_count = 0U;
        V3ImportAlias *type_aliases = NULL;
        size_t type_alias_count = 0U;
        v3_strip_internal_ref_type(normalized_type, stripped, sizeof(stripped));
        if (!v3_split_qualified_type_text(stripped,
                                          owner_module,
                                          sizeof(owner_module),
                                          type_instance_name,
                                          sizeof(type_instance_name)) ||
            !v3_parse_type_instance_text(type_instance_name,
                                         type_name,
                                         sizeof(type_name),
                                         type_args,
                                         &type_arg_count,
                                         CHENG_V3_MAX_TYPE_PARAMS)) {
            return false;
        }
        type_aliases = (V3ImportAlias *)v3_xmalloc(sizeof(*type_aliases) * CHENG_V3_MAX_IMPORT_ALIASES);
        if (type_def->kind == V3_TYPE_DEF_ALIAS) {
            char alias_target[CHENG_V3_MAX_TYPE_TEXT];
            if (!v3_load_import_aliases_from_source_path(type_def->source_path,
                                                         type_aliases,
                                                         &type_alias_count,
                                                         CHENG_V3_MAX_IMPORT_ALIASES)) {
                free(type_aliases);
                return false;
            }
            if (type_def->generic_param_count > 0U) {
                if (type_def->generic_param_count != type_arg_count ||
                    !v3_normalize_type_text_with_params(lowering,
                                                        type_def->owner_module_path,
                                                        type_aliases,
                                                        type_alias_count,
                                                        type_def->generic_param_names,
                                                        type_args,
                                                        type_arg_count,
                                                        type_def->alias_target,
                                                        alias_target,
                                                        sizeof(alias_target))) {
                    free(type_aliases);
                    return false;
                }
            } else {
                snprintf(alias_target, sizeof(alias_target), "%s", type_def->alias_target);
            }
            {
                bool ok = v3_emit_implicit_default_into_address(plan,
                                                                lowering,
                                                                current_function,
                                                                type_aliases,
                                                                type_alias_count,
                                                                type_def->owner_module_path,
                                                                type_def->source_path,
                                                                alias_target,
                                                                dest_addr_reg,
                                                                call_arg_base,
                                                                string_temp_base,
                                                                string_temp_stride,
                                                                string_label_index_io,
                                                                call_depth,
                                                                out,
                                                                cap,
                                                                depth + 1U);
                free(type_aliases);
                return ok;
            }
        }
        free(type_aliases);
    }
    storage_size = v3_type_storage_size_i32(lowering, normalized_type);
    if (storage_size <= 0 ||
        !v3_emit_zero_address_range(out, cap, dest_addr_reg, storage_size)) {
        return false;
    }
    if (!v3_type_needs_recursive_default_init(lowering,
                                              aliases,
                                              alias_count,
                                              type_owner_module_path,
                                              normalized_type,
                                              depth)) {
        return true;
    }
    if (v3_is_tuple_type_text(normalized_type)) {
        char (*items)[4096] = NULL;
        V3TypeFieldDef *fields = NULL;
        size_t item_count = 0U;
        size_t i;
        int32_t offset = 0;
        bool ok = false;
        items = (char (*)[4096])v3_xmalloc(sizeof(*items) * CHENG_V3_MAX_TYPE_FIELDS);
        fields = (V3TypeFieldDef *)v3_xmalloc(sizeof(*fields) * CHENG_V3_MAX_TYPE_FIELDS);
        if (!v3_parse_tuple_type_items(normalized_type,
                                       items,
                                       &item_count,
                                       CHENG_V3_MAX_TYPE_FIELDS) ||
            item_count == 0U) {
            goto emit_tuple_cleanup;
        }
        for (i = 0; i < item_count; ++i) {
            if (!v3_parse_type_field_spec(items[i], true, &fields[i])) {
                goto emit_tuple_cleanup;
            }
            if (!fields[i].has_explicit_name) {
                v3_tuple_field_synthetic_name(i, fields[i].name, sizeof(fields[i].name));
            }
        }
        if (!v3_validate_type_field_defaults(type_source_path,
                                             normalized_type,
                                             fields,
                                             item_count)) {
            goto emit_tuple_cleanup;
        }
        for (i = 0; i < item_count; ++i) {
            V3TypeLayoutStub field_layout;
            int32_t field_align;
            int field_addr_reg = dest_addr_reg == 14 ? 15 : 14;
            if (!v3_compute_type_layout_impl(lowering,
                                             fields[i].type_text,
                                             &field_layout,
                                             depth + 1U)) {
                fprintf(stderr,
                        "[cheng_v3_seed] tuple default field layout failed function=%s type=%s field=%s field_type=%s depth=%u\n",
                        current_function ? current_function->symbol_text : "<nil>",
                        normalized_type,
                        fields[i].name,
                        fields[i].type_text,
                        (unsigned)(depth + 1U));
                goto emit_tuple_cleanup;
            }
            field_align = field_layout.align > 0 ? field_layout.align : 1;
            offset = v3_align_up_i32(offset, field_align);
            v3_emit_call_dest_spill_store(out, cap, call_arg_base, call_depth, dest_addr_reg);
            v3_emit_call_dest_spill_load(out, cap, call_arg_base, call_depth, field_addr_reg);
            if (offset != 0 &&
                !v3_emit_add_address_offset(out, cap, field_addr_reg, field_addr_reg, offset)) {
                fprintf(stderr,
                        "[cheng_v3_seed] tuple default field offset failed function=%s type=%s field=%s offset=%d\n",
                        current_function ? current_function->symbol_text : "<nil>",
                        normalized_type,
                        fields[i].name,
                        offset);
                goto emit_tuple_cleanup;
            }
            if (!v3_emit_implicit_default_into_address(plan,
                                                       lowering,
                                                       current_function,
                                                       aliases,
                                                       alias_count,
                                                       type_owner_module_path,
                                                       type_source_path,
                                                       fields[i].type_text,
                                                       field_addr_reg,
                                                       call_arg_base,
                                                       string_temp_base,
                                                       string_temp_stride,
                                                       string_label_index_io,
                                                       call_depth + 1,
                                                       out,
                                                       cap,
                                                       depth + 1U)) {
                fprintf(stderr,
                        "[cheng_v3_seed] tuple default field init failed function=%s type=%s field=%s field_type=%s depth=%u\n",
                        current_function ? current_function->symbol_text : "<nil>",
                        normalized_type,
                        fields[i].name,
                        fields[i].type_text,
                        (unsigned)(depth + 1U));
                goto emit_tuple_cleanup;
            }
            if (fields[i].has_default_expr) {
                v3_emit_call_dest_spill_store(out, cap, call_arg_base, call_depth, dest_addr_reg);
                v3_emit_call_dest_spill_load(out, cap, call_arg_base, call_depth, field_addr_reg);
                if (offset != 0 &&
                    !v3_emit_add_address_offset(out, cap, field_addr_reg, field_addr_reg, offset)) {
                    fprintf(stderr,
                            "[cheng_v3_seed] tuple default expr offset failed function=%s type=%s field=%s offset=%d\n",
                            current_function ? current_function->symbol_text : "<nil>",
                            normalized_type,
                            fields[i].name,
                            offset);
                    goto emit_tuple_cleanup;
                }
                if (!v3_emit_field_default_expr_into_address(plan,
                                                             lowering,
                                                             current_function,
                                                             type_owner_module_path,
                                                             type_source_path,
                                                             normalized_type,
                                                             aliases,
                                                             alias_count,
                                                             fields,
                                                             item_count,
                                                             fields[i].name,
                                                             fields[i].default_expr,
                                                             fields[i].type_text,
                                                             field_addr_reg,
                                                             call_arg_base,
                                                             string_temp_base,
                                                             string_temp_stride,
                                                             string_label_index_io,
                                                             call_depth,
                                                             out,
                                                             cap)) {
                    fprintf(stderr,
                            "[cheng_v3_seed] tuple default expr init failed function=%s type=%s field=%s expr=%s\n",
                            current_function ? current_function->symbol_text : "<nil>",
                            normalized_type,
                            fields[i].name,
                            fields[i].default_expr);
                    goto emit_tuple_cleanup;
                }
            }
            v3_emit_call_dest_spill_load(out, cap, call_arg_base, call_depth, dest_addr_reg);
            offset += field_layout.size;
        }
        if (item_count > 0U) {
            v3_emit_call_dest_spill_load(out, cap, call_arg_base, call_depth, dest_addr_reg);
        }
        ok = true;
emit_tuple_cleanup:
        free(fields);
        free(items);
        return ok;
    }
    if (type_index >= 0) {
        const V3TypeDefStub *type_def = &lowering->type_defs[type_index];
        char stripped[PATH_MAX];
        char owner_module[PATH_MAX];
        char type_instance_name[128];
        char type_name[128];
        char type_args[CHENG_V3_MAX_TYPE_PARAMS][256];
        size_t type_arg_count = 0U;
        V3ImportAlias *type_aliases = NULL;
        size_t type_alias_count = 0U;
        size_t i;
        if (type_def->kind != V3_TYPE_DEF_RECORD) {
            return true;
        }
        v3_strip_internal_ref_type(normalized_type, stripped, sizeof(stripped));
        if (!v3_split_qualified_type_text(stripped,
                                          owner_module,
                                          sizeof(owner_module),
                                          type_instance_name,
                                          sizeof(type_instance_name)) ||
            !v3_parse_type_instance_text(type_instance_name,
                                         type_name,
                                         sizeof(type_name),
                                         type_args,
                                         &type_arg_count,
                                         CHENG_V3_MAX_TYPE_PARAMS) ||
            ((type_aliases = (V3ImportAlias *)v3_xmalloc(sizeof(*type_aliases) * CHENG_V3_MAX_IMPORT_ALIASES)) == NULL) ||
            !v3_load_import_aliases_from_source_path(type_def->source_path,
                                                     type_aliases,
                                                     &type_alias_count,
                                                     CHENG_V3_MAX_IMPORT_ALIASES)) {
            free(type_aliases);
            return false;
        }
        for (i = 0; i < type_def->field_count; ++i) {
            char resolved_field_type[CHENG_V3_MAX_TYPE_TEXT];
            char field_abi[32];
            int32_t field_offset = 0;
            int field_addr_reg = dest_addr_reg == 14 ? 15 : 14;
            if (type_def->generic_param_count > 0U) {
                if (type_def->generic_param_count != type_arg_count ||
                    !v3_normalize_type_text_with_params(lowering,
                                                        type_def->owner_module_path,
                                                        type_aliases,
                                                        type_alias_count,
                                                        type_def->generic_param_names,
                                                        type_args,
                                                        type_arg_count,
                                                        type_def->fields[i].type_text,
                                                        resolved_field_type,
                                                        sizeof(resolved_field_type))) {
                    free(type_aliases);
                    return false;
                }
            } else {
                snprintf(resolved_field_type, sizeof(resolved_field_type), "%s", type_def->fields[i].type_text);
            }
            snprintf(field_abi, sizeof(field_abi), "%s", v3_type_abi_class(resolved_field_type));
            if (!v3_resolve_field_meta(lowering,
                                       normalized_type,
                                       type_def->fields[i].name,
                                       resolved_field_type,
                                       sizeof(resolved_field_type),
                                       field_abi,
                                       sizeof(field_abi),
                                       &field_offset)) {
                fprintf(stderr,
                        "[cheng_v3_seed] record default field meta failed function=%s type=%s field=%s source_type=%s\n",
                        current_function ? current_function->symbol_text : "<nil>",
                        normalized_type,
                        type_def->fields[i].name,
                        type_def->fields[i].type_text);
                free(type_aliases);
                return false;
            }
            v3_emit_call_dest_spill_store(out, cap, call_arg_base, call_depth, dest_addr_reg);
            v3_emit_call_dest_spill_load(out, cap, call_arg_base, call_depth, field_addr_reg);
            if (field_offset != 0 &&
                !v3_emit_add_address_offset(out, cap, field_addr_reg, field_addr_reg, field_offset)) {
                fprintf(stderr,
                        "[cheng_v3_seed] record default field offset failed function=%s type=%s field=%s offset=%d\n",
                        current_function ? current_function->symbol_text : "<nil>",
                        normalized_type,
                        type_def->fields[i].name,
                        field_offset);
                free(type_aliases);
                return false;
            }
            if (!v3_emit_implicit_default_into_address(plan,
                                                       lowering,
                                                       current_function,
                                                       type_aliases,
                                                       type_alias_count,
                                                       type_def->owner_module_path,
                                                       type_def->source_path,
                                                       resolved_field_type,
                                                       field_addr_reg,
                                                       call_arg_base,
                                                       string_temp_base,
                                                       string_temp_stride,
                                                       string_label_index_io,
                                                       call_depth + 1,
                                                       out,
                                                       cap,
                                                       depth + 1U)) {
                fprintf(stderr,
                        "[cheng_v3_seed] record default field init failed function=%s type=%s field=%s field_type=%s depth=%u\n",
                        current_function ? current_function->symbol_text : "<nil>",
                        normalized_type,
                        type_def->fields[i].name,
                        resolved_field_type,
                        (unsigned)(depth + 1U));
                free(type_aliases);
                return false;
            }
            if (type_def->fields[i].has_default_expr) {
                if (!v3_emit_field_default_expr_into_address(plan,
                                                             lowering,
                                                             current_function,
                                                             type_def->owner_module_path,
                                                             type_def->source_path,
                                                             normalized_type,
                                                             type_aliases,
                                                             type_alias_count,
                                                             type_def->fields,
                                                             type_def->field_count,
                                                             type_def->fields[i].name,
                                                             type_def->fields[i].default_expr,
                                                             resolved_field_type,
                                                             field_addr_reg,
                                                             call_arg_base,
                                                             string_temp_base,
                                                             string_temp_stride,
                                                             string_label_index_io,
                                                             call_depth,
                                                             out,
                                                             cap)) {
                    fprintf(stderr,
                            "[cheng_v3_seed] record default expr init failed function=%s type=%s field=%s expr=%s\n",
                            current_function ? current_function->symbol_text : "<nil>",
                            normalized_type,
                            type_def->fields[i].name,
                            type_def->fields[i].default_expr);
                    free(type_aliases);
                    return false;
                }
            }
            v3_emit_call_dest_spill_load(out, cap, call_arg_base, call_depth, dest_addr_reg);
        }
        if (type_def->field_count > 0U) {
            v3_emit_call_dest_spill_load(out, cap, call_arg_base, call_depth, dest_addr_reg);
        }
        free(type_aliases);
    }
    return true;
}

static bool v3_emit_constructor_into_address(const V3SystemLinkPlanStub *plan,
                                             const V3LoweringPlanStub *lowering,
                                             const V3LoweredFunctionStub *current_function,
                                             const V3ImportAlias *aliases,
                                             size_t alias_count,
                                             const V3AsmLocalSlot *locals,
                                             size_t local_count,
                                             const char *callee,
                                             char args[][4096],
                                             size_t arg_count,
                                             const char *dest_type_text,
                                             int dest_addr_reg,
                                             int32_t call_arg_base,
                                             int32_t string_temp_base,
                                             int32_t string_temp_stride,
                                             int32_t *string_label_index_io,
                                             int call_depth,
                                             char *out,
                                             size_t cap);

static bool v3_codegen_call_into_address(const V3SystemLinkPlanStub *plan,
                                         const V3LoweringPlanStub *lowering,
                                         const V3LoweredFunctionStub *current_function,
                                         const V3ImportAlias *aliases,
                                         size_t alias_count,
                                         const V3AsmLocalSlot *locals,
                                         size_t local_count,
                                         const char *callee,
                                         char args[][4096],
                                         size_t arg_count,
                                         int dest_addr_reg,
                                         int32_t call_arg_base,
                                         int32_t string_temp_base,
                                         int32_t string_temp_stride,
                                         int32_t *string_label_index_io,
                                         int call_depth,
                                         char *out,
                                         size_t cap) {
    V3AsmCallTarget target;
    size_t i;
    int32_t string_temp_index = 0;
    if (!v3_resolve_call_target_with_args(plan,
                                          lowering,
                                          current_function,
                                          aliases,
                                          alias_count,
                                          locals,
                                          local_count,
                                          callee,
                                          (const char (*)[4096])args,
                                          arg_count,
                                          &target)) {
        return false;
    }
    if (strcmp(target.return_abi_class, "composite") != 0 ||
        target.param_count != arg_count ||
        target.param_count > CHENG_V3_MAX_CALL_ARGS) {
        return false;
    }
    v3_emit_call_dest_spill_store(out,
                                  cap,
                                  call_arg_base,
                                  call_depth,
                                  dest_addr_reg);
    for (i = 0; i < arg_count; ++i) {
        if (target.param_is_var[i]) {
            char lvalue_type[256];
            char lvalue_abi[32];
            int32_t local_index = v3_find_local_slot(locals, local_count, args[i]);
            if (local_index >= 0) {
                if (!v3_emit_local_value_address(out, cap, &locals[local_index], 15)) {
                    return false;
                }
            } else if (!v3_emit_lvalue_address(plan,
                                               lowering,
                                               current_function,
                                               aliases,
                                               alias_count,
                                               locals,
                                               local_count,
                                               args[i],
                                               lvalue_type,
                                               sizeof(lvalue_type),
                                               lvalue_abi,
                                               sizeof(lvalue_abi),
                                               15,
                                               call_arg_base,
                                               string_temp_base,
                                               string_temp_stride,
                                               call_depth + 1,
                                               out,
                                               cap)) {
                return false;
            }
            v3_emit_call_arg_spill_store(out,
                                         cap,
                                         call_arg_base,
                                         call_depth,
                                         i,
                                         "ptr",
                                         15);
            continue;
        }
        if (strcmp(target.param_types[i], "str") == 0) {
            char literal[4096];
            int32_t scratch_offset;
            if (string_label_index_io &&
                v3_parse_string_literal_text(args[i], literal, sizeof(literal))) {
                scratch_offset =
                    string_temp_base + call_depth * string_temp_stride +
                    v3_active_target_str_size_i32() +
                    string_temp_index * v3_active_target_str_size_i32();
                if (string_temp_stride <
                    ((string_temp_index + 2) * v3_active_target_str_size_i32())) {
                    fprintf(stderr,
                            "[cheng_v3_seed] composite call direct str-arg scratch overflow caller=%s callee=%s arg_index=%zu expr=%s\n",
                            current_function->symbol_text,
                            callee,
                            i,
                            args[i]);
                    return false;
                }
                string_temp_index += 1;
                {
                    char scratch_name[64];
                    V3AsmLocalSlot scratch_slot;
                    snprintf(scratch_name, sizeof(scratch_name), "__v3_strtmp_%d_%zu", call_depth, i);
                    memset(&scratch_slot, 0, sizeof(scratch_slot));
                    snprintf(scratch_slot.name, sizeof(scratch_slot.name), "%s", scratch_name);
                    snprintf(scratch_slot.type_text, sizeof(scratch_slot.type_text), "%s", "str");
                    snprintf(scratch_slot.abi_class, sizeof(scratch_slot.abi_class), "%s", "composite");
                    scratch_slot.stack_offset = scratch_offset;
                    scratch_slot.stack_size = v3_active_target_str_size_i32();
                    scratch_slot.stack_align = v3_active_target_pointer_align_i32();
                    if (!v3_emit_str_literal_into_slot(plan,
                                                       current_function,
                                                       &scratch_slot,
                                                       literal,
                                                       string_label_index_io,
                                                       out,
                                                       cap) ||
                        !v3_emit_slot_address(out, cap, &scratch_slot, 15)) {
                        return false;
                    }
                    v3_emit_call_arg_spill_store(out,
                                                 cap,
                                                 call_arg_base,
                                                 call_depth,
                                                 i,
                                                 "ptr",
                                                 15);
                }
                continue;
            }
        }
        if (v3_abi_class_scalar_or_ptr(target.param_abi_classes[i])) {
            if (!v3_codegen_expr_scalar(plan,
                                        lowering,
                                        current_function,
                                        aliases,
                                        alias_count,
                                        locals,
                                        local_count,
                                        args[i],
                                        target.param_abi_classes[i],
                                        9,
                                        call_arg_base,
                                        string_temp_base,
                                        string_temp_stride,
                                        call_depth + 1,
                                        out,
                                        cap)) {
                return false;
            }
            v3_emit_call_arg_spill_store(out,
                                         cap,
                                         call_arg_base,
                                         call_depth,
                                         i,
                                         target.param_abi_classes[i],
                                         9);
            continue;
        }
        {
            int32_t local_index = v3_find_local_slot(locals, local_count, args[i]);
            char temp_name[128];
            bool need_materialize = false;
            {
                char literal_text[4096];
                if (strcmp(target.param_types[i], "str") == 0 &&
                    v3_parse_string_literal_text(args[i], literal_text, sizeof(literal_text))) {
                    V3AsmLocalSlot scratch_slot;
                    int32_t literal_slot = 0;
                    size_t literal_i;
                    for (literal_i = 0; literal_i <= i; ++literal_i) {
                        char prior_literal[4096];
                        if (strcmp(target.param_types[literal_i], "str") == 0 &&
                            v3_parse_string_literal_text(args[literal_i], prior_literal, sizeof(prior_literal))) {
                            literal_slot += 1;
                        }
                    }
                    literal_slot -= 1;
                    if (literal_slot < 0 ||
                        string_temp_stride < ((literal_slot + 2) * v3_active_target_str_size_i32())) {
                        fprintf(stderr,
                                "[cheng_v3_seed] composite call str-arg scratch overflow caller=%s callee=%s arg_index=%zu expr=%s\n",
                                current_function->symbol_text,
                                callee,
                                i,
                                args[i]);
                        return false;
                    }
                    memset(&scratch_slot, 0, sizeof(scratch_slot));
                    snprintf(scratch_slot.name, sizeof(scratch_slot.name), "%s", "__v3_call_str_arg");
                    snprintf(scratch_slot.type_text, sizeof(scratch_slot.type_text), "%s", "str");
                    snprintf(scratch_slot.abi_class, sizeof(scratch_slot.abi_class), "%s", "composite");
                    scratch_slot.stack_offset =
                        string_temp_base + call_depth * string_temp_stride +
                        v3_active_target_str_size_i32() +
                        literal_slot * v3_active_target_str_size_i32();
                    scratch_slot.stack_size = v3_active_target_str_size_i32();
                    scratch_slot.stack_align = v3_active_target_pointer_align_i32();
                    if (!v3_emit_slot_address(out, cap, &scratch_slot, 15) ||
                        !v3_materialize_composite_expr_into_address(plan,
                                                                    lowering,
                                                                    current_function,
                                                                    aliases,
                                                                    alias_count,
                                                                    locals,
                                                                    local_count,
                                                                    args[i],
                                                                    "str",
                                                                    15,
                                                                    call_arg_base,
                                                                    string_temp_base,
                                                                    string_temp_stride,
                                                                    string_label_index_io,
                                                                    call_depth + 1,
                                                                    out,
                                                                    cap)) {
                        fprintf(stderr,
                                "[cheng_v3_seed] composite call str-arg materialize failed caller=%s callee=%s arg_index=%zu expr=%s\n",
                                current_function->symbol_text,
                                callee,
                                i,
                                args[i]);
                        return false;
                    }
                    v3_emit_call_arg_spill_store(out,
                                                 cap,
                                                 call_arg_base,
                                                 call_depth,
                                                 i,
                                                 "ptr",
                                                 15);
                    continue;
                }
            }
            if (local_index < 0) {
                v3_call_arg_temp_name(current_function,
                                      callee,
                                      i,
                                      args[i],
                                      temp_name,
                                      sizeof(temp_name));
                local_index = v3_find_local_slot(locals, local_count, temp_name);
                need_materialize = true;
                if (local_index < 0) {
                    v3_expr_temp_name(current_function,
                                      args[i],
                                      temp_name,
                                      sizeof(temp_name));
                    local_index = v3_find_local_slot(locals, local_count, temp_name);
                }
            }
            if (local_index < 0) {
                if (v3_emit_direct_composite_call_arg_address(plan,
                                                              lowering,
                                                              current_function,
                                                              aliases,
                                                              alias_count,
                                                              locals,
                                                              local_count,
                                                              args[i],
                                                              target.param_types[i],
                                                              target.param_abi_classes[i],
                                                              15,
                                                              call_arg_base,
                                                              string_temp_base,
                                                              string_temp_stride,
                                                              call_depth + 1,
                                                              out,
                                                              cap)) {
                    v3_emit_call_arg_spill_store(out,
                                                 cap,
                                                 call_arg_base,
                                                 call_depth,
                                                 i,
                                                 "ptr",
                                                 15);
                    continue;
                }
                fprintf(stderr,
                        "[cheng_v3_seed] composite call-into-address arg local missing caller=%s callee=%s arg_index=%zu expr=%s\n",
                        current_function->symbol_text,
                        callee,
                        i,
                        args[i]);
                return false;
            }
            if (need_materialize &&
                !v3_materialize_composite_expr_into_slot(plan,
                                                         lowering,
                                                         current_function,
                                                         aliases,
                                                         alias_count,
                                                         locals,
                                                         local_count,
                                                         callee,
                                                         i,
                                                         args[i],
                                                         &locals[local_index],
                                                         call_arg_base,
                                                         string_temp_base,
                                                         string_temp_stride,
                                                         string_label_index_io,
                                                         call_depth + 1,
                                                         out,
                                                         cap)) {
                fprintf(stderr,
                        "[cheng_v3_seed] composite call-into-address arg materialize failed caller=%s callee=%s arg_index=%zu expr=%s\n",
                        current_function->symbol_text,
                        callee,
                        i,
                        args[i]);
                return false;
            }
            if (!v3_emit_local_value_address(out, cap, &locals[local_index], 15)) {
                fprintf(stderr,
                        "[cheng_v3_seed] composite call-into-address arg address failed caller=%s callee=%s arg_index=%zu local=%s\n",
                        current_function->symbol_text,
                        callee,
                        i,
                        locals[local_index].name);
                return false;
            }
            v3_emit_call_arg_spill_store(out,
                                         cap,
                                         call_arg_base,
                                         call_depth,
                                         i,
                                         "ptr",
                                         15);
        }
    }
    return v3_emit_call_from_spills(plan,
                                    lowering,
                                    current_function,
                                    aliases,
                                    alias_count,
                                    &target,
                                    true,
                                    call_arg_base,
                                    call_depth,
                                    out,
                                    cap);
}

static bool v3_try_codegen_index_assignment_call(const V3SystemLinkPlanStub *plan,
                                                 const V3LoweringPlanStub *lowering,
                                                 const V3LoweredFunctionStub *current_function,
                                                 const V3ImportAlias *aliases,
                                                 size_t alias_count,
                                                 const V3AsmLocalSlot *locals,
                                                 size_t local_count,
                                                 const char *lhs_text,
                                                 const char *rhs_text,
                                                 int32_t call_arg_base,
                                                 int32_t string_temp_base,
                                                 int32_t string_temp_stride,
                                                 int32_t *string_label_index_io,
                                                 int call_depth,
                                                 char *out,
                                                 size_t cap,
                                                 bool *handled_out) {
    char args[3][4096];
    char base_type[256];
    char base_abi[32];
    char normalized_base_type[256];
    char fixed_elem_type[256];
    V3AsmCallTarget target;
    if (handled_out != NULL) {
        *handled_out = false;
    }
    if (!v3_parse_index_assignment_operands(lhs_text,
                                            rhs_text,
                                            args[0],
                                            sizeof(args[0]),
                                            args[1],
                                            sizeof(args[1]),
                                            args[2],
                                            sizeof(args[2])) ||
        (v3_infer_expr_type(plan,
                            lowering,
                            current_function,
                            aliases,
                            alias_count,
                            locals,
                            local_count,
                            args[0],
                            base_type,
                            sizeof(base_type),
                            base_abi,
                            sizeof(base_abi)) &&
         v3_normalize_type_text(lowering,
                                current_function->owner_module_path,
                                aliases,
                                alias_count,
                                base_type,
                                normalized_base_type,
                                sizeof(normalized_base_type)) &&
         (strcmp(normalized_base_type, "Bytes") == 0 ||
          strcmp(normalized_base_type, "str") == 0 ||
          (strlen(normalized_base_type) > 2U &&
           strcmp(normalized_base_type + strlen(normalized_base_type) - 2U, "[]") == 0) ||
          v3_parse_fixed_array_type(normalized_base_type,
                                    fixed_elem_type,
                                    sizeof(fixed_elem_type),
                                    &(int32_t){0}))) ||
        !v3_resolve_call_target_with_args(plan,
                                          lowering,
                                          current_function,
                                          aliases,
                                          alias_count,
                                          locals,
                                          local_count,
                                          "[]=",
                                          (const char (*)[4096])args,
                                          3U,
                                          &target)) {
        return true;
    }
    if (!v3_codegen_call_scalar_resolved(plan,
                                         lowering,
                                         current_function,
                                         aliases,
                                         alias_count,
                                         locals,
                                         local_count,
                                         "[]=",
                                         args,
                                         3U,
                                         &target,
                                         "void",
                                         9,
                                         call_arg_base,
                                         string_temp_base,
                                         string_temp_stride,
                                         string_label_index_io,
                                         call_depth,
                                         out,
                                         cap)) {
        return false;
    }
    if (handled_out != NULL) {
        *handled_out = true;
    }
    return true;
}

static bool v3_emit_constructor_into_address(const V3SystemLinkPlanStub *plan,
                                             const V3LoweringPlanStub *lowering,
                                             const V3LoweredFunctionStub *current_function,
                                             const V3ImportAlias *aliases,
                                             size_t alias_count,
                                             const V3AsmLocalSlot *locals,
                                             size_t local_count,
                                             const char *callee,
                                             char args[][4096],
                                             size_t arg_count,
                                             const char *dest_type_text,
                                             int dest_addr_reg,
                                             int32_t call_arg_base,
                                             int32_t string_temp_base,
                                             int32_t string_temp_stride,
                                             int32_t *string_label_index_io,
                                             int call_depth,
                                             char *out,
                                             size_t cap) {
    size_t i;
    char seen_fields[CHENG_V3_MAX_CALL_ARGS][128];
    size_t seen_count = 0U;
    (void)callee;
    if (v3_is_tuple_type_text(dest_type_text) &&
        !v3_tuple_args_cover_all_items(dest_type_text,
                                       (const char (*)[4096])args,
                                       arg_count)) {
        return false;
    }
    if (!v3_emit_implicit_default_into_address(plan,
                                               lowering,
                                               current_function,
                                               aliases,
                                               alias_count,
                                               current_function->owner_module_path,
                                               current_function->source_path,
                                               dest_type_text,
                                               dest_addr_reg,
                                               call_arg_base,
                                               string_temp_base,
                                               string_temp_stride,
                                               string_label_index_io,
                                               call_depth,
                                               out,
                                               cap,
                                               0U)) {
        return false;
    }
    v3_emit_call_dest_spill_store(out,
                                  cap,
                                  call_arg_base,
                                  call_depth,
                                  dest_addr_reg);
    for (i = 0; i < arg_count; ++i) {
        char field_name[128];
        char field_expr[4096];
        char field_type[256];
        char field_abi[32];
        int32_t field_offset = 0;
        int field_addr_reg = dest_addr_reg == 14 ? 15 : 14;
        size_t seen_index;
        if (!v3_parse_named_arg_meta(args[i],
                                     field_name,
                                     sizeof(field_name),
                                     field_expr,
                                     sizeof(field_expr)) ||
            !v3_resolve_field_meta(lowering,
                                   dest_type_text,
                                   field_name,
                                   field_type,
                                   sizeof(field_type),
                                   field_abi,
                                   sizeof(field_abi),
                                   &field_offset)) {
            return false;
        }
        for (seen_index = 0; seen_index < seen_count; ++seen_index) {
            if (strcmp(seen_fields[seen_index], field_name) == 0) {
                return false;
            }
        }
        if (seen_count >= CHENG_V3_MAX_CALL_ARGS) {
            return false;
        }
        snprintf(seen_fields[seen_count], sizeof(seen_fields[seen_count]), "%s", field_name);
        seen_count += 1U;
        if (v3_abi_class_scalar_or_ptr(field_abi)) {
            if (!v3_codegen_expr_scalar(plan,
                                        lowering,
                                        current_function,
                                        aliases,
                                        alias_count,
                                        locals,
                                        local_count,
                                        field_expr,
                                        field_abi,
                                        9,
                                        call_arg_base,
                                        string_temp_base,
                                        string_temp_stride,
                                        call_depth + 1,
                                        out,
                                        cap)) {
                return false;
            }
            v3_emit_call_dest_spill_load(out,
                                         cap,
                                         call_arg_base,
                                         call_depth,
                                         dest_addr_reg);
            v3_emit_store_scalar_to_address(out, cap, dest_addr_reg, field_offset, field_abi, 9);
            continue;
        }
        v3_emit_call_dest_spill_load(out,
                                     cap,
                                     call_arg_base,
                                     call_depth,
                                     field_addr_reg);
        if (field_offset != 0 &&
            !v3_emit_add_address_offset(out, cap, field_addr_reg, field_addr_reg, field_offset)) {
            return false;
        }
        if (!v3_materialize_composite_expr_into_address(plan,
                                                        lowering,
                                                        current_function,
                                                        aliases,
                                                        alias_count,
                                                        locals,
                                                        local_count,
                                                        field_expr,
                                                        field_type,
                                                        field_addr_reg,
                                                        call_arg_base,
                                                        string_temp_base,
                                                        string_temp_stride,
                                                        string_label_index_io,
                                                        call_depth + 1,
                                                        out,
                                                        cap)) {
            return false;
        }
    }
    return true;
}

static bool v3_materialize_composite_expr_into_address(const V3SystemLinkPlanStub *plan,
                                                       const V3LoweringPlanStub *lowering,
                                                       const V3LoweredFunctionStub *current_function,
                                                       const V3ImportAlias *aliases,
                                                       size_t alias_count,
                                                       const V3AsmLocalSlot *locals,
                                                       size_t local_count,
                                                       const char *expr_text,
                                                       const char *dest_type_text,
                                                       int dest_addr_reg,
                                                       int32_t call_arg_base,
                                                       int32_t string_temp_base,
                                                       int32_t string_temp_stride,
                                                       int32_t *string_label_index_io,
                                                       int call_depth,
                                                       char *out,
                                                       size_t cap) {
    char expr[4096];
    char normalized_dest_type[256];
    char literal[4096];
    char left_expr[4096];
    char right_expr[4096];
    char inner_callee[PATH_MAX];
    char arg_text[4096] = {0};
    char surface_error[256];
    char base_expr[4096];
    char index_expr[4096];
    int32_t local_index;
    V3TypeLayoutStub layout;
    V3ExprSurface surface;
    v3_trim_copy_text(expr_text, expr, sizeof(expr));
    if (expr[0] == '\0') {
        return false;
    }
    if (!v3_classify_expr_surface(expr, &surface, surface_error, sizeof(surface_error))) {
        fprintf(stderr,
                "[cheng_v3_seed] composite expr surface classify failed function=%s expr=%s reason=%s\n",
                current_function ? current_function->symbol_text : "-",
                expr,
                surface_error[0] != '\0' ? surface_error : "invalid expr");
        return false;
    }
    snprintf(expr, sizeof(expr), "%s", surface.text);
    if (!v3_normalize_type_text(lowering,
                                current_function->owner_module_path,
                                aliases,
                                alias_count,
                                dest_type_text,
                                normalized_dest_type,
                                sizeof(normalized_dest_type))) {
        return false;
    }
    if (v3_is_tuple_type_text(normalized_dest_type) &&
        !v3_tuple_expr_covers_all_items(normalized_dest_type, expr)) {
        fprintf(stderr,
                "[cheng_v3_seed] tuple literal omission unsupported function=%s type=%s expr=%s\n",
                current_function->symbol_text,
                normalized_dest_type,
                expr);
        return false;
    }
    {
        char default_type[CHENG_V3_MAX_TYPE_TEXT];
        char normalized_default_type[CHENG_V3_MAX_TYPE_TEXT];
        if (v3_parse_default_expr_text(expr, default_type, sizeof(default_type))) {
            if (!v3_normalize_type_text(lowering,
                                        current_function->owner_module_path,
                                        aliases,
                                        alias_count,
                                        default_type,
                                        normalized_default_type,
                                        sizeof(normalized_default_type)) ||
                strcmp(normalized_default_type, normalized_dest_type) != 0) {
                return false;
            }
            return v3_emit_implicit_default_into_address(plan,
                                                         lowering,
                                                         current_function,
                                                         aliases,
                                                         alias_count,
                                                         current_function->owner_module_path,
                                                         current_function->source_path,
                                                         normalized_dest_type,
                                                         dest_addr_reg,
                                                         call_arg_base,
                                                         string_temp_base,
                                                         string_temp_stride,
                                                         string_label_index_io,
                                                         call_depth,
                                                         out,
                                                         cap,
                                                         0U);
        }
    }
    if (surface.kind == V3_EXPR_SURFACE_IF) {
        int32_t label_id = v3_next_expr_label_id();
        char false_label[128];
        char end_label[128];
        v3_emit_call_dest_spill_store(out,
                                      cap,
                                      call_arg_base,
                                      call_depth,
                                      dest_addr_reg);
        if (!v3_codegen_expr_scalar(plan,
                                    lowering,
                                    current_function,
                                    aliases,
                                    alias_count,
                                    locals,
                                    local_count,
                                    surface.cond,
                                    "bool",
                                    9,
                                    call_arg_base,
                                    string_temp_base,
                                    string_temp_stride,
                                    call_depth + 1,
                                    out,
                                    cap)) {
            return false;
        }
        snprintf(false_label, sizeof(false_label), "L_v3_ifcexpr_false_%d", label_id);
        snprintf(end_label, sizeof(end_label), "L_v3_ifcexpr_end_%d", label_id);
        v3_emit_branch_zero(out, cap, 9, false, false_label);
        v3_emit_call_dest_spill_load(out,
                                     cap,
                                     call_arg_base,
                                     call_depth,
                                     dest_addr_reg);
        if (!v3_materialize_composite_expr_into_address(plan,
                                                        lowering,
                                                        current_function,
                                                        aliases,
                                                        alias_count,
                                                        locals,
                                                        local_count,
                                                        surface.when_true,
                                                        normalized_dest_type,
                                                        dest_addr_reg,
                                                        call_arg_base,
                                                        string_temp_base,
                                                        string_temp_stride,
                                                        string_label_index_io,
                                                        call_depth + 1,
                                                        out,
                                                        cap)) {
            return false;
        }
        v3_emit_jump_label(out, cap, end_label);
        v3_text_appendf(out, cap, "%s:\n", false_label);
        v3_emit_call_dest_spill_load(out,
                                     cap,
                                     call_arg_base,
                                     call_depth,
                                     dest_addr_reg);
        if (!v3_materialize_composite_expr_into_address(plan,
                                                        lowering,
                                                        current_function,
                                                        aliases,
                                                        alias_count,
                                                        locals,
                                                        local_count,
                                                        surface.when_false,
                                                        normalized_dest_type,
                                                        dest_addr_reg,
                                                        call_arg_base,
                                                        string_temp_base,
                                                        string_temp_stride,
                                                        string_label_index_io,
                                                        call_depth + 1,
                                                        out,
                                                        cap)) {
            return false;
        }
        v3_text_appendf(out, cap, "%s:\n", end_label);
        return true;
    }
    if (surface.kind == V3_EXPR_SURFACE_TERNARY) {
        int32_t label_id = v3_next_expr_label_id();
        char false_label[128];
        char end_label[128];
        v3_emit_call_dest_spill_store(out,
                                      cap,
                                      call_arg_base,
                                      call_depth,
                                      dest_addr_reg);
        if (!v3_codegen_expr_scalar(plan,
                                    lowering,
                                    current_function,
                                    aliases,
                                    alias_count,
                                    locals,
                                    local_count,
                                    surface.cond,
                                    "bool",
                                    9,
                                    call_arg_base,
                                    string_temp_base,
                                    string_temp_stride,
                                    call_depth + 1,
                                    out,
                                    cap)) {
            return false;
        }
        snprintf(false_label, sizeof(false_label), "L_v3_cexpr_false_%d", label_id);
        snprintf(end_label, sizeof(end_label), "L_v3_cexpr_end_%d", label_id);
        v3_emit_branch_zero(out, cap, 9, false, false_label);
        v3_emit_call_dest_spill_load(out,
                                     cap,
                                     call_arg_base,
                                     call_depth,
                                     dest_addr_reg);
        if (!v3_materialize_composite_expr_into_address(plan,
                                                        lowering,
                                                        current_function,
                                                        aliases,
                                                        alias_count,
                                                        locals,
                                                        local_count,
                                                        surface.when_true,
                                                        normalized_dest_type,
                                                        dest_addr_reg,
                                                        call_arg_base,
                                                        string_temp_base,
                                                        string_temp_stride,
                                                        string_label_index_io,
                                                        call_depth + 1,
                                                        out,
                                                        cap)) {
            return false;
        }
        v3_emit_jump_label(out, cap, end_label);
        v3_text_appendf(out, cap, "%s:\n", false_label);
        v3_emit_call_dest_spill_load(out,
                                     cap,
                                     call_arg_base,
                                     call_depth,
                                     dest_addr_reg);
        if (!v3_materialize_composite_expr_into_address(plan,
                                                        lowering,
                                                        current_function,
                                                        aliases,
                                                        alias_count,
                                                        locals,
                                                        local_count,
                                                        surface.when_false,
                                                        normalized_dest_type,
                                                        dest_addr_reg,
                                                        call_arg_base,
                                                        string_temp_base,
                                                        string_temp_stride,
                                                        string_label_index_io,
                                                        call_depth + 1,
                                                        out,
                                                        cap)) {
            return false;
        }
        v3_text_appendf(out, cap, "%s:\n", end_label);
        return true;
    }
    if (v3_parse_string_literal_text(expr, literal, sizeof(literal))) {
        return strcmp(normalized_dest_type, "str") == 0 &&
               v3_emit_str_literal_into_address(plan,
                                               current_function,
                                               literal,
                                               dest_addr_reg,
                                               string_label_index_io,
                                               out,
                                               cap);
    }
    if (strcmp(normalized_dest_type, "str") == 0) {
        int32_t plus_index = v3_find_top_level_binary_op_last(expr, "+");
        if (plus_index >= 0) {
            V3AsmLocalSlot left_slot;
            V3AsmLocalSlot right_slot;
            V3AsmCallTarget concat_target;
            snprintf(left_expr, sizeof(left_expr), "%.*s", plus_index, expr);
            snprintf(right_expr, sizeof(right_expr), "%s", expr + plus_index + 1);
            v3_trim_copy_text(left_expr, left_expr, sizeof(left_expr));
            v3_trim_copy_text(right_expr, right_expr, sizeof(right_expr));
            if (string_temp_stride < 48) {
                return false;
            }
            memset(&left_slot, 0, sizeof(left_slot));
            memset(&right_slot, 0, sizeof(right_slot));
            snprintf(left_slot.name, sizeof(left_slot.name), "%s", "__v3_str_concat_lhs");
            snprintf(left_slot.type_text, sizeof(left_slot.type_text), "%s", "str");
            snprintf(left_slot.abi_class, sizeof(left_slot.abi_class), "%s", "composite");
            left_slot.stack_offset = string_temp_base + call_depth * string_temp_stride;
            left_slot.stack_size = v3_active_target_str_size_i32();
            left_slot.stack_align = v3_active_target_pointer_align_i32();
            snprintf(right_slot.name, sizeof(right_slot.name), "%s", "__v3_str_concat_rhs");
            snprintf(right_slot.type_text, sizeof(right_slot.type_text), "%s", "str");
            snprintf(right_slot.abi_class, sizeof(right_slot.abi_class), "%s", "composite");
            right_slot.stack_offset = left_slot.stack_offset + v3_active_target_str_size_i32();
            right_slot.stack_size = v3_active_target_str_size_i32();
            right_slot.stack_align = v3_active_target_pointer_align_i32();
            v3_emit_call_dest_spill_store(out,
                                          cap,
                                          call_arg_base,
                                          call_depth,
                                          dest_addr_reg);
            if (!v3_emit_slot_address(out, cap, &left_slot, 15) ||
                !v3_materialize_composite_expr_into_address(plan,
                                                            lowering,
                                                            current_function,
                                                            aliases,
                                                            alias_count,
                                                            locals,
                                                            local_count,
                                                            left_expr,
                                                            "str",
                                                            15,
                                                            call_arg_base,
                                                            string_temp_base,
                                                            string_temp_stride,
                                                            string_label_index_io,
                                                            call_depth + 1,
                                                            out,
                                                            cap) ||
                !v3_emit_slot_address(out, cap, &right_slot, 15) ||
                !v3_materialize_composite_expr_into_address(plan,
                                                            lowering,
                                                            current_function,
                                                            aliases,
                                                            alias_count,
                                                            locals,
                                                            local_count,
                                                            right_expr,
                                                            "str",
                                                            15,
                                                            call_arg_base,
                                                            string_temp_base,
                                                            string_temp_stride,
                                                            string_label_index_io,
                                                            call_depth + 1,
                                                            out,
                                                            cap)) {
                return false;
            }
            memset(&concat_target, 0, sizeof(concat_target));
            concat_target.external = true;
            concat_target.param_count = 2U;
            snprintf(concat_target.callee_name, sizeof(concat_target.callee_name), "%s", "driver_c_str_concat_bridge");
            v3_copy_target_symbol_name(plan,
                                       "driver_c_str_concat_bridge",
                                       concat_target.symbol_name,
                                       sizeof(concat_target.symbol_name));
            snprintf(concat_target.return_type, sizeof(concat_target.return_type), "%s", "str");
            snprintf(concat_target.return_abi_class, sizeof(concat_target.return_abi_class), "%s", "composite");
            snprintf(concat_target.param_types[0], sizeof(concat_target.param_types[0]), "%s", "str");
            snprintf(concat_target.param_types[1], sizeof(concat_target.param_types[1]), "%s", "str");
            snprintf(concat_target.param_abi_classes[0], sizeof(concat_target.param_abi_classes[0]), "%s", "composite");
            snprintf(concat_target.param_abi_classes[1], sizeof(concat_target.param_abi_classes[1]), "%s", "composite");
            if (!v3_emit_local_value_address(out, cap, &left_slot, 15) ||
                !v3_emit_local_value_address(out, cap, &right_slot, 9)) {
                return false;
            }
            v3_emit_call_arg_spill_store(out,
                                         cap,
                                         call_arg_base,
                                         call_depth,
                                         0U,
                                         "ptr",
                                         15);
            v3_emit_call_arg_spill_store(out,
                                         cap,
                                         call_arg_base,
                                         call_depth,
                                         1U,
                                         "ptr",
                                         9);
            return v3_emit_call_from_spills(plan,
                                            lowering,
                                            current_function,
                                            aliases,
                                            alias_count,
                                            &concat_target,
                                            true,
                                            call_arg_base,
                                            call_depth,
                                            out,
                                            cap);
        }
    }
    if (strcmp(expr, "[]") == 0) {
        return strlen(normalized_dest_type) > 2U &&
               strcmp(normalized_dest_type + strlen(normalized_dest_type) - 2U, "[]") == 0 &&
               v3_compute_type_layout_impl(lowering, normalized_dest_type, &layout, 0U) &&
               v3_emit_zero_address_range(out, cap, dest_addr_reg, layout.size);
    }
    {
        size_t arg_count = 0U;
        char (*args)[4096] = v3_alloc_call_arg_scratch();
        if (v3_parse_list_literal_text(expr, args, &arg_count, CHENG_V3_MAX_CALL_ARGS)) {
            char elem_type[256];
            char elem_abi[32];
            int32_t elem_size = 0;
            int32_t fixed_len = 0;
            int scalar_value_reg = 9;
            int buffer_reg = dest_addr_reg == 14 ? 13 : 14;
            int elem_addr_reg = dest_addr_reg == 15 ? 14 : 15;
            size_t i;
            if (v3_active_target_is_linux_x86_64()) {
                if (dest_addr_reg == scalar_value_reg) {
                    scalar_value_reg = 10;
                }
                buffer_reg = 10;
                if (buffer_reg == dest_addr_reg || buffer_reg == scalar_value_reg) {
                    buffer_reg = 11;
                }
                if (buffer_reg == dest_addr_reg || buffer_reg == scalar_value_reg) {
                    buffer_reg = 8;
                }
            }
            if (v3_parse_fixed_array_type(normalized_dest_type,
                                          elem_type,
                                          sizeof(elem_type),
                                          &fixed_len)) {
                if ((int32_t)arg_count != fixed_len) {
                    free(args);
                    return false;
                }
            } else if (arg_count > 0U &&
                       strlen(normalized_dest_type) > 2U &&
                       strcmp(normalized_dest_type + strlen(normalized_dest_type) - 2U, "[]") == 0) {
                snprintf(elem_type,
                         sizeof(elem_type),
                         "%.*s",
                         (int)(strlen(normalized_dest_type) - 2U),
                         normalized_dest_type);
            } else {
                free(args);
                return false;
            }
            snprintf(elem_abi, sizeof(elem_abi), "%s", v3_type_abi_class(elem_type));
            if (strcmp(elem_abi, "i8") == 0 ||
                strcmp(elem_abi, "u8") == 0 ||
                strcmp(elem_abi, "bool") == 0) {
                elem_size = 1;
            } else if (strcmp(elem_abi, "i16") == 0 ||
                       strcmp(elem_abi, "u16") == 0) {
                elem_size = 2;
            } else if (strcmp(elem_abi, "i32") == 0) {
                elem_size = 4;
            } else if (strcmp(elem_abi, "i64") == 0 || strcmp(elem_abi, "ptr") == 0) {
                elem_size = 8;
            } else if (!v3_compute_type_layout_impl(lowering, elem_type, &layout, 0U)) {
                free(args);
                return false;
            } else {
                elem_size = layout.size;
            }
            if (elem_size <= 0) {
                free(args);
                return false;
            }
            if (v3_parse_fixed_array_type(normalized_dest_type,
                                          elem_type,
                                          sizeof(elem_type),
                                          &fixed_len)) {
                for (i = 0; i < arg_count; ++i) {
                    int32_t elem_offset = elem_size * (int32_t)i;
                    if (strcmp(elem_abi, "i8") == 0 ||
                        strcmp(elem_abi, "u8") == 0 ||
                        strcmp(elem_abi, "i16") == 0 ||
                        strcmp(elem_abi, "u16") == 0 ||
                        strcmp(elem_abi, "i32") == 0 ||
                        strcmp(elem_abi, "bool") == 0 ||
                        strcmp(elem_abi, "i64") == 0 ||
                        strcmp(elem_abi, "ptr") == 0) {
                        if (!v3_codegen_expr_scalar(plan,
                                                    lowering,
                                                    current_function,
                                                    aliases,
                                                    alias_count,
                                                    locals,
                                                    local_count,
                                                    args[i],
                                                    elem_abi,
                                                    scalar_value_reg,
                                                    call_arg_base,
                                                    string_temp_base,
                                                    string_temp_stride,
                                                    call_depth + 1,
                                                    out,
                                                    cap)) {
                            free(args);
                            return false;
                        }
                        v3_emit_store_scalar_to_address(out,
                                                        cap,
                                                        dest_addr_reg,
                                                        elem_offset,
                                                        elem_abi,
                                                        scalar_value_reg);
                        continue;
                    }
                    if (elem_offset == 0) {
                        if (elem_addr_reg != dest_addr_reg) {
                            if (v3_active_target_is_linux_x86_64()) {
                                v3_emit_x64_move_reg(out, cap, elem_addr_reg, dest_addr_reg, "ptr");
                            } else {
                                v3_text_appendf(out, cap, "  mov x%d, x%d\n", elem_addr_reg, dest_addr_reg);
                            }
                        }
                    } else if (!v3_emit_add_address_offset(out,
                                                           cap,
                                                           elem_addr_reg,
                                                           dest_addr_reg,
                                                           elem_offset)) {
                        free(args);
                        return false;
                    }
                    if (!v3_materialize_composite_expr_into_address(plan,
                                                                    lowering,
                                                                    current_function,
                                                                    aliases,
                                                                    alias_count,
                                                                    locals,
                                                                    local_count,
                                                                    args[i],
                                                                    elem_type,
                                                                    elem_addr_reg,
                                                                    call_arg_base,
                                                                    string_temp_base,
                                                                    string_temp_stride,
                                                                    string_label_index_io,
                                                                    call_depth + 1,
                                                                    out,
                                                                    cap)) {
                        free(args);
                        return false;
                    }
                }
                free(args);
                return true;
            }
            if (arg_count == 0U ||
                !v3_emit_mov_imm(out,
                                 cap,
                                 v3_first_call_arg_virtual_reg(false),
                                 (int64_t)elem_size * (int64_t)arg_count)) {
                free(args);
                return false;
            }
            v3_emit_call_dest_spill_store(out,
                                          cap,
                                          call_arg_base,
                                          call_depth,
                                          dest_addr_reg);
            v3_emit_call_named_symbol(plan, out, cap, "alloc");
            v3_emit_call_dest_spill_load(out,
                                         cap,
                                         call_arg_base,
                                         call_depth,
                                         dest_addr_reg);
            if (!v3_emit_mov_imm(out, cap, 9, (int64_t)arg_count)) {
                free(args);
                return false;
            }
            v3_emit_store_scalar_to_address(out, cap, dest_addr_reg, 0, "i32", 9);
            v3_emit_store_scalar_to_address(out, cap, dest_addr_reg, 4, "i32", 9);
            v3_emit_store_scalar_to_address(out, cap, dest_addr_reg, 8, "ptr", 0);
            v3_emit_call_dest_spill_store(out,
                                          cap,
                                          call_arg_base,
                                          call_depth,
                                          dest_addr_reg);
            for (i = 0; i < arg_count; ++i) {
                int32_t elem_offset = elem_size * (int32_t)i;
                if (strcmp(elem_abi, "i8") == 0 ||
                    strcmp(elem_abi, "u8") == 0 ||
                    strcmp(elem_abi, "i16") == 0 ||
                    strcmp(elem_abi, "u16") == 0 ||
                    strcmp(elem_abi, "i32") == 0 || strcmp(elem_abi, "bool") == 0 ||
                    strcmp(elem_abi, "i64") == 0 || strcmp(elem_abi, "ptr") == 0) {
                    v3_emit_call_dest_spill_store(out,
                                                  cap,
                                                  call_arg_base,
                                                  call_depth,
                                                  dest_addr_reg);
                    if (!v3_codegen_expr_scalar(plan,
                                                lowering,
                                                current_function,
                                                aliases,
                                                alias_count,
                                                locals,
                                                local_count,
                                                args[i],
                                                elem_abi,
                                                scalar_value_reg,
                                                call_arg_base,
                                                string_temp_base,
                                                string_temp_stride,
                                                call_depth + 1,
                                                out,
                                                cap)) {
                        free(args);
                        return false;
                    }
                    v3_emit_call_dest_spill_load(out,
                                                 cap,
                                                 call_arg_base,
                                                 call_depth,
                                                 dest_addr_reg);
                    v3_emit_load_scalar_from_address(out, cap, dest_addr_reg, 8, "ptr", buffer_reg);
                    v3_emit_store_scalar_to_address(out,
                                                    cap,
                                                    buffer_reg,
                                                    elem_offset,
                                                    elem_abi,
                                                    scalar_value_reg);
                    continue;
                }
                v3_emit_call_dest_spill_load(out,
                                             cap,
                                             call_arg_base,
                                             call_depth,
                                             dest_addr_reg);
                v3_emit_load_scalar_from_address(out, cap, dest_addr_reg, 8, "ptr", buffer_reg);
                if (elem_offset == 0) {
                    if (v3_active_target_is_linux_x86_64()) {
                        v3_text_appendf(out, cap,
                                        "  movq %s, %s\n",
                                        v3_x64_qreg_name(buffer_reg),
                                        v3_x64_qreg_name(15));
                    } else {
                        v3_text_appendf(out, cap, "  mov x15, x%d\n", buffer_reg);
                    }
                } else if (!v3_emit_add_address_offset(out, cap, 15, buffer_reg, elem_offset)) {
                    free(args);
                    return false;
                }
                if (!v3_materialize_composite_expr_into_address(plan,
                                                                lowering,
                                                                current_function,
                                                                aliases,
                                                                alias_count,
                                                                locals,
                                                                local_count,
                                                                args[i],
                                                                elem_type,
                                                                15,
                                                                call_arg_base,
                                                                string_temp_base,
                                                                string_temp_stride,
                                                                string_label_index_io,
                                                                call_depth + 1,
                                                                out,
                                                                cap)) {
                    free(args);
                    return false;
                }
            }
            free(args);
            return true;
        }
        free(args);
    }
    {
        size_t arg_count = 0U;
        char (*args)[4096] = v3_alloc_call_arg_scratch();
        if (v3_parse_call_text(expr, inner_callee, sizeof(inner_callee), args, &arg_count, CHENG_V3_MAX_CALL_ARGS) &&
            arg_count == 1U) {
            V3ResultIntrinsicKind result_kind =
                v3_resolve_result_intrinsic_kind(lowering,
                                                 current_function->owner_module_path,
                                                 aliases,
                                                 alias_count,
                                                 inner_callee);
            char field_type[256];
            char field_abi[32];
            int src_addr_reg = dest_addr_reg == 14 ? 15 : 14;
            if (result_kind == V3_RESULT_INTRINSIC_OK ||
                result_kind == V3_RESULT_INTRINSIC_ERR ||
                result_kind == V3_RESULT_INTRINSIC_ERR_INFO) {
                bool ok = v3_emit_result_constructor_into_address(plan,
                                                                  lowering,
                                                                  current_function,
                                                                  aliases,
                                                                  alias_count,
                                                                  locals,
                                                                  local_count,
                                                                  result_kind,
                                                                  inner_callee,
                                                                  args,
                                                                  arg_count,
                                                                  normalized_dest_type,
                                                                  dest_addr_reg,
                                                                  call_arg_base,
                                                                  string_temp_base,
                                                                  string_temp_stride,
                                                                  string_label_index_io,
                                                                  call_depth,
                                                                  out,
                                                                  cap);
                free(args);
                return ok;
            }
            if (result_kind == V3_RESULT_INTRINSIC_VALUE) {
                bool ok;
                if (!v3_emit_result_field_address(plan,
                                                  lowering,
                                                  current_function,
                                                  aliases,
                                                  alias_count,
                                                  locals,
                                                  local_count,
                                                  args[0],
                                                  "value",
                                                  src_addr_reg,
                                                  call_arg_base,
                                                  string_temp_base,
                                                  string_temp_stride,
                                                  string_label_index_io,
                                                  call_depth,
                                                  out,
                                                  cap,
                                                  field_type,
                                                  sizeof(field_type),
                                                  field_abi,
                                                  sizeof(field_abi)) ||
                    strcmp(field_abi, "composite") != 0 ||
                    !v3_compute_type_layout_impl(lowering, field_type, &layout, 0U)) {
                    free(args);
                    return false;
                }
                ok = v3_emit_copy_address_to_address(plan, out, cap, dest_addr_reg, src_addr_reg, layout.size);
                free(args);
                return ok;
            }
            if (result_kind == V3_RESULT_INTRINSIC_ERROR_INFO_OF) {
                bool ok;
                if (!v3_emit_result_field_address(plan,
                                                  lowering,
                                                  current_function,
                                                  aliases,
                                                  alias_count,
                                                  locals,
                                                  local_count,
                                                  args[0],
                                                  "err",
                                                  src_addr_reg,
                                                  call_arg_base,
                                                  string_temp_base,
                                                  string_temp_stride,
                                                  string_label_index_io,
                                                  call_depth,
                                                  out,
                                                  cap,
                                                  field_type,
                                                  sizeof(field_type),
                                                  field_abi,
                                                  sizeof(field_abi)) ||
                    strcmp(field_abi, "composite") != 0 ||
                    !v3_compute_type_layout_impl(lowering, field_type, &layout, 0U)) {
                    free(args);
                    return false;
                }
                ok = v3_emit_copy_address_to_address(plan, out, cap, dest_addr_reg, src_addr_reg, layout.size);
                free(args);
                return ok;
            }
            if (result_kind == V3_RESULT_INTRINSIC_ERROR ||
                result_kind == V3_RESULT_INTRINSIC_ERROR_TEXT) {
                bool ok;
                if (!v3_emit_result_field_address(plan,
                                                  lowering,
                                                  current_function,
                                                  aliases,
                                                  alias_count,
                                                  locals,
                                                  local_count,
                                                  args[0],
                                                  "err",
                                                  src_addr_reg,
                                                  call_arg_base,
                                                  string_temp_base,
                                                  string_temp_stride,
                                                  string_label_index_io,
                                                  call_depth,
                                                  out,
                                                  cap,
                                                  field_type,
                                                  sizeof(field_type),
                                                  field_abi,
                                                  sizeof(field_abi)) ||
                    !v3_resolve_field_meta(lowering,
                                           field_type,
                                           "msg",
                                           field_type,
                                           sizeof(field_type),
                                           field_abi,
                                           sizeof(field_abi),
                                           &local_index) ||
                    strcmp(field_abi, "composite") != 0 ||
                    !v3_compute_type_layout_impl(lowering, field_type, &layout, 0U)) {
                    free(args);
                    return false;
                }
                if (local_index != 0 &&
                    !v3_emit_add_address_offset(out, cap, src_addr_reg, src_addr_reg, local_index)) {
                    free(args);
                    return false;
                }
                ok = v3_emit_copy_address_to_address(plan, out, cap, dest_addr_reg, src_addr_reg, layout.size);
                free(args);
                return ok;
            }
        }
        free(args);
    }
    local_index = v3_find_local_slot(locals, local_count, expr);
    if (local_index >= 0 && !v3_abi_class_scalar_or_ptr(locals[local_index].abi_class)) {
        return v3_emit_copy_slot_to_address(plan, lowering, out, cap, &locals[local_index], dest_addr_reg);
    }
    {
        const V3TopLevelConstStub *top_level_const =
            v3_find_expr_top_level_const(lowering,
                                         current_function->owner_module_path,
                                         aliases,
                                         alias_count,
                                         expr);
        if (top_level_const) {
            if (strcmp(top_level_const->type_text, "str") == 0 &&
                strcmp(top_level_const->abi_class, "composite") == 0 &&
                top_level_const->has_str_value) {
                return strcmp(normalized_dest_type, "str") == 0 &&
                       v3_emit_str_literal_into_address(plan,
                                                       current_function,
                                                       top_level_const->str_value,
                                                       dest_addr_reg,
                                                       string_label_index_io,
                                                       out,
                                                       cap);
            }
            return false;
        }
    }
    {
        char global_type[256];
        char global_abi[32];
        int src_addr_reg = dest_addr_reg == 14 ? 15 : 14;
        if (v3_emit_owner_global_var_address(plan,
                                             lowering,
                                             current_function,
                                             expr,
                                             global_type,
                                             sizeof(global_type),
                                             global_abi,
                                             sizeof(global_abi),
                                             src_addr_reg,
                                             out,
                                             cap)) {
            if (strcmp(global_abi, "composite") != 0 ||
                !v3_compute_type_layout_impl(lowering, global_type, &layout, 0U)) {
                return false;
            }
            return v3_emit_copy_address_to_address(plan, out, cap, dest_addr_reg, src_addr_reg, layout.size);
        }
    }
    {
        char field_type[256];
        char field_abi[32];
        int src_addr_reg = dest_addr_reg == 14 ? 15 : 14;
        v3_emit_call_dest_spill_store(out,
                                      cap,
                                      call_arg_base,
                                      call_depth,
                                      dest_addr_reg);
        if (v3_emit_index_access_address(plan,
                                         lowering,
                                         current_function,
                                         aliases,
                                         alias_count,
                                         locals,
                                         local_count,
                                         expr,
                                         field_type,
                                         sizeof(field_type),
                                         field_abi,
                                         sizeof(field_abi),
                                         src_addr_reg,
                                         call_arg_base,
                                         string_temp_base,
                                         string_temp_stride,
                                         call_depth,
                                         out,
                                         cap)) {
            if (v3_abi_class_scalar_or_ptr(field_abi) ||
                !v3_compute_type_layout_impl(lowering, field_type, &layout, 0U)) {
                return false;
            }
            v3_emit_call_dest_spill_load(out,
                                         cap,
                                         call_arg_base,
                                         call_depth,
                                         dest_addr_reg);
            return v3_emit_copy_address_to_address(plan, out, cap, dest_addr_reg, src_addr_reg, layout.size);
        }
        if (v3_parse_index_access_expr(expr,
                                       base_expr,
                                       sizeof(base_expr),
                                       index_expr,
                                       sizeof(index_expr))) {
            char (*args)[4096] = v3_alloc_call_arg_scratch();
            bool ok;
            snprintf(args[0], sizeof(args[0]), "%s", base_expr);
            snprintf(args[1], sizeof(args[1]), "%s", index_expr);
            ok = v3_codegen_call_into_address(plan,
                                              lowering,
                                              current_function,
                                              aliases,
                                              alias_count,
                                              locals,
                                              local_count,
                                              "[]",
                                              args,
                                              2U,
                                              dest_addr_reg,
                                              call_arg_base,
                                              string_temp_base,
                                              string_temp_stride,
                                              string_label_index_io,
                                              call_depth,
                                              out,
                                              cap);
            free(args);
            return ok;
        }
    }
    {
        char field_type[256];
        char field_abi[32];
        int src_addr_reg = dest_addr_reg == 14 ? 15 : 14;
        v3_emit_call_dest_spill_store(out,
                                      cap,
                                      call_arg_base,
                                      call_depth,
                                      dest_addr_reg);
        if (v3_emit_member_access_address(plan,
                                          lowering,
                                          current_function,
                                          aliases,
                                          alias_count,
                                          locals,
                                          local_count,
                                          expr,
                                          field_type,
                                          sizeof(field_type),
                                          field_abi,
                                          sizeof(field_abi),
                                          src_addr_reg,
                                          call_arg_base,
                                          string_temp_base,
                                          string_temp_stride,
                                          call_depth,
                                          out,
                                          cap) ||
            v3_emit_field_path_address(out,
                                       cap,
                                       locals,
                                       local_count,
                                       lowering,
                                       expr,
                                       field_type,
                                       sizeof(field_type),
                                       field_abi,
                                       sizeof(field_abi),
                                       src_addr_reg)) {
            if (v3_abi_class_scalar_or_ptr(field_abi) ||
                !v3_compute_type_layout_impl(lowering, field_type, &layout, 0U)) {
                return false;
            }
            v3_emit_call_dest_spill_load(out,
                                         cap,
                                         call_arg_base,
                                         call_depth,
                                         dest_addr_reg);
            return v3_emit_copy_address_to_address(plan, out, cap, dest_addr_reg, src_addr_reg, layout.size);
        }
    }
    {
        size_t arg_count = 0U;
        char (*args)[4096] = v3_alloc_call_arg_scratch();
        if (v3_parse_call_text(expr, inner_callee, sizeof(inner_callee), args, &arg_count, CHENG_V3_MAX_CALL_ARGS)) {
            V3AsmCallTarget target;
            V3ResultIntrinsicKind result_kind =
                v3_resolve_result_intrinsic_kind(lowering,
                                                 current_function->owner_module_path,
                                                 aliases,
                                                 alias_count,
                                                 inner_callee);
            if (result_kind == V3_RESULT_INTRINSIC_ERR_CODE) {
                bool ok = v3_emit_result_constructor_into_address(plan,
                                                                  lowering,
                                                                  current_function,
                                                                  aliases,
                                                                  alias_count,
                                                                  locals,
                                                                  local_count,
                                                                  result_kind,
                                                                  inner_callee,
                                                                  args,
                                                                  arg_count,
                                                                  normalized_dest_type,
                                                                  dest_addr_reg,
                                                                  call_arg_base,
                                                                  string_temp_base,
                                                                  string_temp_stride,
                                                                  string_label_index_io,
                                                                  call_depth,
                                                                  out,
                                                                  cap);
                free(args);
                return ok;
            }
            if (v3_resolve_call_target_with_args(plan,
                                                 lowering,
                                                 current_function,
                                                 aliases,
                                                 alias_count,
                                                 locals,
                                                 local_count,
                                                 inner_callee,
                                                 args,
                                                 arg_count,
                                                 &target)) {
                if (strcmp(target.return_abi_class, "composite") != 0) {
                    free(args);
                    return false;
                }
                if (!v3_codegen_call_into_address(plan,
                                                  lowering,
                                                  current_function,
                                                  aliases,
                                                  alias_count,
                                                  locals,
                                                  local_count,
                                                  inner_callee,
                                                  args,
                                                  arg_count,
                                                  dest_addr_reg,
                                                  call_arg_base,
                                                  string_temp_base,
                                                  string_temp_stride,
                                                  string_label_index_io,
                                                  call_depth,
                                                  out,
                                                  cap)) {
                    free(args);
                    return false;
                }
                free(args);
                return true;
            }
            if (v3_callee_is_constructor_like(lowering,
                                              current_function->owner_module_path,
                                              aliases,
                                              alias_count,
                                              inner_callee)) {
                bool ok = v3_emit_constructor_into_address(plan,
                                                           lowering,
                                                           current_function,
                                                           aliases,
                                                           alias_count,
                                                           locals,
                                                           local_count,
                                                           inner_callee,
                                                           args,
                                                           arg_count,
                                                           normalized_dest_type,
                                                           dest_addr_reg,
                                                           call_arg_base,
                                                           string_temp_base,
                                                           string_temp_stride,
                                                           string_label_index_io,
                                                           call_depth,
                                                           out,
                                                           cap);
                free(args);
                return ok;
            }
            free(args);
            return false;
        }
        free(args);
    }
    if (v3_parse_prefix_single_arg_call(expr, inner_callee, sizeof(inner_callee), arg_text, sizeof(arg_text))) {
        V3AsmCallTarget target;
        V3ResultIntrinsicKind result_kind =
            v3_resolve_result_intrinsic_kind(lowering,
                                             current_function->owner_module_path,
                                             aliases,
                                             alias_count,
                                             inner_callee);
        char (*args)[4096] = v3_alloc_call_arg_scratch();
        snprintf(args[0], sizeof(args[0]), "%s", arg_text);
        if (result_kind == V3_RESULT_INTRINSIC_OK ||
            result_kind == V3_RESULT_INTRINSIC_ERR ||
            result_kind == V3_RESULT_INTRINSIC_ERR_INFO) {
            bool ok = v3_emit_result_constructor_into_address(plan,
                                                              lowering,
                                                              current_function,
                                                              aliases,
                                                              alias_count,
                                                              locals,
                                                              local_count,
                                                              result_kind,
                                                              inner_callee,
                                                              args,
                                                              1U,
                                                              normalized_dest_type,
                                                              dest_addr_reg,
                                                              call_arg_base,
                                                              string_temp_base,
                                                              string_temp_stride,
                                                              string_label_index_io,
                                                              call_depth,
                                                              out,
                                                              cap);
            free(args);
            return ok;
        }
        if (!v3_resolve_call_target_with_args(plan,
                                              lowering,
                                              current_function,
                                              aliases,
                                              alias_count,
                                              locals,
                                              local_count,
                                              inner_callee,
                                              (const char (*)[4096])args,
                                              1U,
                                              &target) ||
            strcmp(target.return_abi_class, "composite") != 0) {
            free(args);
            return false;
        }
        {
            bool ok = v3_codegen_call_into_address(plan,
                                                   lowering,
                                                   current_function,
                                                   aliases,
                                                   alias_count,
                                                   locals,
                                                   local_count,
                                                   inner_callee,
                                                   args,
                                                   1U,
                                                   dest_addr_reg,
                                                   call_arg_base,
                                                   string_temp_base,
                                                   string_temp_stride,
                                                   string_label_index_io,
                                                   call_depth,
                                                   out,
                                                   cap);
            free(args);
            return ok;
        }
    }
    return false;
}

static bool v3_materialize_composite_expr_into_slot(const V3SystemLinkPlanStub *plan,
                                                    const V3LoweringPlanStub *lowering,
                                                    const V3LoweredFunctionStub *current_function,
                                                    const V3ImportAlias *aliases,
                                                    size_t alias_count,
                                                    const V3AsmLocalSlot *locals,
                                                    size_t local_count,
                                                    const char *callee,
                                                    size_t arg_index,
                                                    const char *expr_text,
                                                    const V3AsmLocalSlot *dest_slot,
                                                    int32_t call_arg_base,
                                                    int32_t string_temp_base,
                                                    int32_t string_temp_stride,
                                                    int32_t *string_label_index_io,
                                                    int call_depth,
                                                    char *out,
                                                    size_t cap) {
    (void)callee;
    (void)arg_index;
    if (!dest_slot || !v3_emit_slot_address(out, cap, dest_slot, 15)) {
        return false;
    }
    return v3_materialize_composite_expr_into_address(plan,
                                                      lowering,
                                                      current_function,
                                                      aliases,
                                                      alias_count,
                                                      locals,
                                                      local_count,
                                                      expr_text,
                                                      dest_slot->type_text,
                                                      15,
                                                      call_arg_base,
                                                      string_temp_base,
                                                      string_temp_stride,
                                                      string_label_index_io,
                                                      call_depth,
                                                      out,
                                                      cap);
}

static bool v3_emit_builtin_str_expr_address(const V3SystemLinkPlanStub *plan,
                                             const V3LoweringPlanStub *lowering,
                                             const V3LoweredFunctionStub *current_function,
                                             const V3ImportAlias *aliases,
                                             size_t alias_count,
                                             const V3AsmLocalSlot *locals,
                                             size_t local_count,
                                             const char *expr_text,
                                             const char *temp_slot_name,
                                             int addr_reg,
                                             int32_t call_arg_base,
                                             int32_t string_temp_base,
                                             int32_t string_temp_stride,
                                             int32_t *string_label_index_io,
                                             int call_depth,
                                             char *out,
                                             size_t cap) {
    char field_type[256];
    char field_abi[32];
    int32_t local_index = -1;
    local_index = v3_find_local_slot(locals, local_count, expr_text);
    if (local_index >= 0) {
        return v3_emit_local_value_address(out, cap, &locals[local_index], addr_reg);
    }
    if (v3_emit_member_access_address(plan,
                                      lowering,
                                      current_function,
                                      aliases,
                                      alias_count,
                                      locals,
                                      local_count,
                                      expr_text,
                                      field_type,
                                      sizeof(field_type),
                                      field_abi,
                                      sizeof(field_abi),
                                      addr_reg,
                                      call_arg_base,
                                      string_temp_base,
                                      string_temp_stride,
                                      call_depth,
                                      out,
                                      cap) ||
        v3_emit_field_path_address(out,
                                   cap,
                                   locals,
                                   local_count,
                                   lowering,
                                   expr_text,
                                   field_type,
                                   sizeof(field_type),
                                   field_abi,
                                   sizeof(field_abi),
                                   addr_reg)) {
        return strcmp(field_type, "str") == 0 &&
               strcmp(field_abi, "composite") == 0;
    }
    {
        V3AsmLocalSlot temp_slot;
        if (string_temp_stride < v3_active_target_str_size_i32()) {
            return false;
        }
        memset(&temp_slot, 0, sizeof(temp_slot));
        snprintf(temp_slot.name, sizeof(temp_slot.name), "%s", temp_slot_name);
        snprintf(temp_slot.type_text, sizeof(temp_slot.type_text), "%s", "str");
        snprintf(temp_slot.abi_class, sizeof(temp_slot.abi_class), "%s", "composite");
        temp_slot.stack_offset = string_temp_base;
        temp_slot.stack_size = v3_active_target_str_size_i32();
        temp_slot.stack_align = v3_active_target_pointer_align_i32();
        if (!v3_emit_slot_address(out, cap, &temp_slot, addr_reg) ||
            !v3_materialize_composite_expr_into_address(plan,
                                                        lowering,
                                                        current_function,
                                                        aliases,
                                                        alias_count,
                                                        locals,
                                                        local_count,
                                                        expr_text,
                                                        "str",
                                                        addr_reg,
                                                        call_arg_base,
                                                        string_temp_base,
                                                        string_temp_stride,
                                                        string_label_index_io,
                                                        call_depth + 1,
                                                        out,
                                                        cap)) {
            return false;
        }
        return v3_emit_slot_address(out, cap, &temp_slot, addr_reg);
    }
}

static bool v3_emit_builtin_str_to_cstring_bridge(const V3SystemLinkPlanStub *plan,
                                                  const V3LoweringPlanStub *lowering,
                                                  const V3LoweredFunctionStub *current_function,
                                                  const V3ImportAlias *aliases,
                                                  size_t alias_count,
                                                  int addr_reg,
                                                  int32_t call_arg_base,
                                                  int call_depth,
                                                  char *out,
                                                  size_t cap) {
    V3AsmCallTarget target;
    memset(&target, 0, sizeof(target));
    target.external = true;
    target.param_count = 1U;
    snprintf(target.callee_name, sizeof(target.callee_name), "%s", "cheng_str_to_cstring_temp_bridge");
    v3_copy_target_symbol_name(plan,
                               "cheng_str_to_cstring_temp_bridge",
                               target.symbol_name,
                               sizeof(target.symbol_name));
    snprintf(target.return_type, sizeof(target.return_type), "%s", "ptr");
    snprintf(target.return_abi_class, sizeof(target.return_abi_class), "%s", "ptr");
    snprintf(target.param_types[0], sizeof(target.param_types[0]), "%s", "str");
    snprintf(target.param_abi_classes[0], sizeof(target.param_abi_classes[0]), "%s", "composite");
    v3_emit_call_arg_spill_store(out,
                                 cap,
                                 call_arg_base,
                                 call_depth,
                                 0U,
                                 "ptr",
                                 addr_reg);
    return v3_emit_call_from_spills(plan,
                                    lowering,
                                    current_function,
                                    aliases,
                                    alias_count,
                                    &target,
                                    false,
                                    call_arg_base,
                                    call_depth,
                                    out,
                                    cap);
}

static bool v3_try_emit_builtin_statement_impl(const V3SystemLinkPlanStub *plan,
                                               const V3LoweringPlanStub *lowering,
                                               const V3LoweredFunctionStub *current_function,
                                               const V3ImportAlias *aliases,
                                               size_t alias_count,
                                               const V3AsmLocalSlot *locals,
                                               size_t local_count,
                                               const char *callee,
                                               char args[][4096],
                                               size_t arg_count,
                                               int32_t call_arg_base,
                                               int32_t string_temp_base,
                                               int32_t string_temp_stride,
                                               int32_t *string_label_index_io,
                                               int32_t *control_label_index_io,
                                               char *out,
                                               size_t cap,
                                               bool *handled_out) {
    (void)control_label_index_io;
    *handled_out = false;
    if (strcmp(callee, "store") == 0 && arg_count == 2U) {
        char value_type[256];
        char value_abi[32];
        *handled_out = true;
        if (!v3_codegen_expr_scalar(plan,
                                    lowering,
                                    current_function,
                                    aliases,
                                    alias_count,
                                    locals,
                                    local_count,
                                    args[0],
                                    "ptr",
                                    14,
                                    call_arg_base,
                                    string_temp_base,
                                    string_temp_stride,
                                    0,
                                    out,
                                    cap) ||
            !v3_infer_expr_type(plan,
                                lowering,
                                current_function,
                                aliases,
                                alias_count,
                                locals,
                                local_count,
                                args[1],
                                value_type,
                                sizeof(value_type),
                                value_abi,
                                sizeof(value_abi))) {
            return false;
        }
        if (v3_abi_class_scalar_or_ptr(value_abi)) {
            if (!v3_codegen_expr_scalar(plan,
                                        lowering,
                                        current_function,
                                        aliases,
                                        alias_count,
                                        locals,
                                        local_count,
                                        args[1],
                                        value_abi,
                                        9,
                                        call_arg_base,
                                        string_temp_base,
                                        string_temp_stride,
                                        0,
                                        out,
                                        cap)) {
                return false;
            }
            v3_emit_address_spill_load(out, cap, call_arg_base, 0, 14);
            v3_emit_store_scalar_to_address(out, cap, 14, 0, value_abi, 9);
            return true;
        }
        return v3_materialize_composite_expr_into_address(plan,
                                                          lowering,
                                                          current_function,
                                                          aliases,
                                                          alias_count,
                                                          locals,
                                                          local_count,
                                                          args[1],
                                                          value_type,
                                                          14,
                                                          call_arg_base,
                                                          string_temp_base,
                                                          string_temp_stride,
                                                          string_label_index_io,
                                                          0,
                                                          out,
                                                          cap);
    }
    if (strcmp(callee, "add") == 0 && arg_count == 2U) {
        char seq_type[256];
        char seq_abi[32];
        char elem_type[256];
        char elem_abi[32];
        char field_type[256];
        char field_abi[32];
        int32_t seq_local_index = -1;
        int32_t elem_size = 0;
        V3TypeLayoutStub elem_layout;
        *handled_out = true;
        if (!v3_infer_expr_type(plan,
                                lowering,
                                current_function,
                                aliases,
                                alias_count,
                                locals,
                                local_count,
                                args[0],
                                seq_type,
                                sizeof(seq_type),
                                seq_abi,
                                sizeof(seq_abi)) ||
            strcmp(seq_abi, "composite") != 0 ||
            strlen(seq_type) <= 2U ||
            strcmp(seq_type + strlen(seq_type) - 2U, "[]") != 0) {
            return false;
        }
        snprintf(elem_type, sizeof(elem_type), "%.*s", (int)(strlen(seq_type) - 2U), seq_type);
        snprintf(elem_abi, sizeof(elem_abi), "%s", v3_type_abi_class(elem_type));
        if (strcmp(elem_abi, "i8") == 0 ||
            strcmp(elem_abi, "u8") == 0 ||
            strcmp(elem_abi, "bool") == 0) {
            elem_size = 1;
        } else if (strcmp(elem_abi, "i16") == 0 ||
                   strcmp(elem_abi, "u16") == 0) {
            elem_size = 2;
        } else if (strcmp(elem_abi, "i32") == 0) {
            elem_size = 4;
        } else if (strcmp(elem_abi, "i64") == 0 || strcmp(elem_abi, "ptr") == 0) {
            elem_size = 8;
        } else if (!v3_compute_type_layout_impl(lowering, elem_type, &elem_layout, 0U)) {
            return false;
        } else {
            elem_size = elem_layout.size;
        }
        if (elem_size <= 0) {
            return false;
        }
        seq_local_index = v3_find_local_slot(locals, local_count, args[0]);
        if (seq_local_index >= 0) {
            if (!v3_emit_slot_address(out, cap, &locals[seq_local_index], 15)) {
                return false;
            }
        } else if ((!v3_emit_member_access_address(plan,
                                                   lowering,
                                                   current_function,
                                                   aliases,
                                                   alias_count,
                                                   locals,
                                                   local_count,
                                                   args[0],
                                                   field_type,
                                                   sizeof(field_type),
                                                   field_abi,
                                                   sizeof(field_abi),
                                                   15,
                                                   call_arg_base,
                                                   string_temp_base,
                                                   string_temp_stride,
                                                   0,
                                                   out,
                                                   cap) &&
                    !v3_emit_field_path_address(out,
                                                cap,
                                                locals,
                                                local_count,
                                                lowering,
                                                args[0],
                                                field_type,
                                                sizeof(field_type),
                                                field_abi,
                                                sizeof(field_abi),
                                                15)) ||
                   strcmp(field_abi, "composite") != 0) {
            return false;
        }
        if (v3_active_target_is_linux_x86_64()) {
            v3_emit_load_scalar_from_address(out, cap, 15, 0, "i32", 10);
            v3_emit_x64_prepare_rdi_esi_q_d(out, cap, 15, 10);
        } else {
            v3_text_appendf(out, cap,
                            "  ldrsw x10, [x15, #0]\n"
                            "  mov x0, x15\n"
                            "  mov x1, x10\n");
        }
        if (!v3_emit_mov_imm(out, cap, 2, elem_size)) {
            return false;
        }
        v3_emit_call_named_symbol(plan, out, cap, "cheng_seq_set_grow");
        v3_emit_address_spill_store(out, cap, call_arg_base, 0, 0);
        if (v3_abi_class_scalar_or_ptr(elem_abi)) {
            if (!v3_codegen_expr_scalar(plan,
                                        lowering,
                                        current_function,
                                        aliases,
                                        alias_count,
                                        locals,
                                        local_count,
                                        args[1],
                                        elem_abi,
                                        9,
                                        call_arg_base,
                                        string_temp_base,
                                        string_temp_stride,
                                        1,
                                        out,
                                        cap)) {
                return false;
            }
            v3_emit_address_spill_load(out, cap, call_arg_base, 0, 0);
            v3_emit_store_scalar_to_address(out, cap, 0, 0, elem_abi, 9);
            return true;
        }
        v3_emit_address_spill_load(out, cap, call_arg_base, 0, 15);
        return v3_materialize_composite_expr_into_address(plan,
                                                          lowering,
                                                          current_function,
                                                          aliases,
                                                          alias_count,
                                                          locals,
                                                          local_count,
                                                          args[1],
                                                          elem_type,
                                                          15,
                                                          call_arg_base,
                                                          string_temp_base,
                                                          string_temp_stride,
                                                          string_label_index_io,
                                                          1,
                                                          out,
                                                          cap);
    }
    if ((strcmp(callee, "panic") == 0 || strcmp(callee, "system.panic") == 0) && arg_count == 1U) {
        char literal[4096];
        char label[128];
        char arg_type[256];
        char arg_abi[32];
        *handled_out = true;
        if (v3_parse_string_literal_text(args[0], literal, sizeof(literal))) {
            if (!v3_emit_cstring_literal_label(plan,
                                               current_function,
                                               literal,
                                               string_label_index_io,
                                               label,
                                               sizeof(label),
                                               out,
                                               cap)) {
                return false;
            }
            if (!v3_emit_symbol_address_for_target(plan, out, cap, 0, label)) {
                return false;
            }
            if (v3_active_target_is_linux_x86_64()) {
                v3_text_append(out, cap, "  movq %rax, %rdi\n");
            }
            v3_emit_call_named_symbol(plan, out, cap, "cheng_v3_panic_cstring_and_exit");
            return true;
        }
        if (!v3_infer_expr_type(plan,
                                lowering,
                                current_function,
                                aliases,
                                alias_count,
                                locals,
                                local_count,
                                args[0],
                                arg_type,
                                sizeof(arg_type),
                                arg_abi,
                                sizeof(arg_abi))) {
            return false;
        }
        if (strcmp(arg_abi, "ptr") == 0) {
            if (!v3_codegen_expr_scalar(plan,
                                        lowering,
                                        current_function,
                                        aliases,
                                        alias_count,
                                        locals,
                                        local_count,
                                        args[0],
                                        "ptr",
                                        0,
                                        call_arg_base,
                                        string_temp_base,
                                        string_temp_stride,
                                        0,
                                        out,
                                        cap)) {
                return false;
            }
            if (v3_active_target_is_linux_x86_64()) {
                v3_text_append(out, cap, "  movq %rax, %rdi\n");
            }
            v3_emit_call_named_symbol(plan, out, cap, "cheng_v3_panic_cstring_and_exit");
            return true;
        }
        if (strcmp(arg_type, "str") != 0 || strcmp(arg_abi, "composite") != 0) {
            return false;
        }
        if (!v3_emit_builtin_str_expr_address(plan,
                                              lowering,
                                              current_function,
                                              aliases,
                                              alias_count,
                                              locals,
                                              local_count,
                                              args[0],
                                              "__v3_panic_str_tmp",
                                              15,
                                              call_arg_base,
                                              string_temp_base,
                                              string_temp_stride,
                                              string_label_index_io,
                                              0,
                                              out,
                                              cap) ||
            !v3_emit_builtin_str_to_cstring_bridge(plan,
                                                   lowering,
                                                   current_function,
                                                   aliases,
                                                   alias_count,
                                                   15,
                                                   call_arg_base,
                                                   0,
                                                   out,
                                                   cap)) {
            return false;
        }
        if (v3_active_target_is_linux_x86_64()) {
            v3_text_append(out, cap, "  movq %rax, %rdi\n");
        }
        v3_emit_call_named_symbol(plan, out, cap, "cheng_v3_panic_cstring_and_exit");
        return true;
    }
    if (strcmp(callee, "echo") == 0) {
        char literal[4096];
        char label[128];
        char arg_type[256];
        char arg_abi[32];
        *handled_out = true;
        if (arg_count != 1U) {
            return false;
        }
        if (v3_parse_string_literal_text(args[0], literal, sizeof(literal))) {
            if (!v3_emit_cstring_literal_label(plan,
                                               current_function,
                                               literal,
                                               string_label_index_io,
                                               label,
                                               sizeof(label),
                                               out,
                                               cap)) {
                return false;
            }
            if (!v3_emit_symbol_address_for_target(plan, out, cap, 0, label)) {
                return false;
            }
            if (v3_active_target_is_linux_x86_64()) {
                v3_text_append(out, cap, "  movq %rax, %rdi\n");
            }
            v3_emit_call_named_symbol(plan, out, cap, "puts");
            return true;
        }
        if (!v3_infer_expr_type(plan,
                                lowering,
                                current_function,
                                aliases,
                                alias_count,
                                locals,
                                local_count,
                                args[0],
                                arg_type,
                                sizeof(arg_type),
                                arg_abi,
                                sizeof(arg_abi)) ||
            strcmp(arg_type, "str") != 0 ||
            strcmp(arg_abi, "composite") != 0) {
            return false;
        }
        if (!v3_emit_builtin_str_expr_address(plan,
                                              lowering,
                                              current_function,
                                              aliases,
                                              alias_count,
                                              locals,
                                              local_count,
                                              args[0],
                                              "__v3_echo_str_tmp",
                                              15,
                                              call_arg_base,
                                              string_temp_base,
                                              string_temp_stride,
                                              string_label_index_io,
                                              0,
                                              out,
                                              cap) ||
            !v3_emit_builtin_str_to_cstring_bridge(plan,
                                                   lowering,
                                                   current_function,
                                                   aliases,
                                                   alias_count,
                                                   15,
                                                   call_arg_base,
                                                   0,
                                                   out,
                                                   cap)) {
            return false;
        }
        if (v3_active_target_is_linux_x86_64()) {
            v3_text_append(out, cap, "  movq %rax, %rdi\n");
        }
        v3_emit_call_named_symbol(plan, out, cap, "puts");
        return true;
    }
    if (strcmp(callee, "assert") == 0) {
        char ok_label[128];
        int32_t label_id = v3_next_expr_label_id();
        char literal[4096];
        char label[128];
        *handled_out = true;
        if ((arg_count != 1U && arg_count != 2U) ||
            !v3_codegen_expr_scalar(plan,
                                    lowering,
                                    current_function,
                                    aliases,
                                    alias_count,
                                    locals,
                                    local_count,
                                    args[0],
                                    "bool",
                                    9,
                                    call_arg_base,
                                    string_temp_base,
                                    string_temp_stride,
                                    0,
                                    out,
                                    cap)) {
            return false;
        }
        snprintf(ok_label, sizeof(ok_label), "L_v3_assert_ok_%d", label_id);
        v3_emit_branch_nonzero(out, cap, 9, false, ok_label);
        if (arg_count == 2U) {
            if (!v3_parse_string_literal_text(args[1], literal, sizeof(literal))) {
                return false;
            }
        } else {
            snprintf(literal, sizeof(literal), "%s", "assert failed");
        }
        if (!v3_emit_cstring_literal_label(plan,
                                           current_function,
                                           literal,
                                           string_label_index_io,
                                           label,
                                           sizeof(label),
                                           out,
                                           cap)) {
            return false;
        }
        if (!v3_emit_symbol_address_for_target(plan, out, cap, 0, label)) {
            return false;
        }
        if (v3_active_target_is_linux_x86_64()) {
            v3_text_append(out, cap, "  movq %rax, %rdi\n");
        }
        v3_emit_call_named_symbol(plan, out, cap, "cheng_v3_panic_cstring_and_exit");
        v3_text_appendf(out, cap, "%s:\n", ok_label);
        return true;
    }
    return true;
}

static bool v3_try_emit_builtin_statement(const V3SystemLinkPlanStub *plan,
                                          const V3LoweringPlanStub *lowering,
                                          const V3LoweredFunctionStub *current_function,
                                          const V3ImportAlias *aliases,
                                          size_t alias_count,
                                          const V3AsmLocalSlot *locals,
                                          size_t local_count,
                                          const char *statement,
                                          int32_t call_arg_base,
                                          int32_t string_temp_base,
                                          int32_t string_temp_stride,
                                          int32_t *string_label_index_io,
                                          int32_t *control_label_index_io,
                                          char *out,
                                          size_t cap,
                                          bool *handled_out) {
    char callee[PATH_MAX];
    size_t arg_count = 0U;
    char (*args)[4096] = v3_alloc_call_arg_scratch();
    bool ok = true;
    callee[0] = '\0';
    *handled_out = false;
    if (v3_parse_call_text(statement, callee, sizeof(callee), args, &arg_count, CHENG_V3_MAX_CALL_ARGS)) {
        ok = v3_try_emit_builtin_statement_impl(plan,
                                                lowering,
                                                current_function,
                                                aliases,
                                                alias_count,
                                                locals,
                                                local_count,
                                                callee,
                                                args,
                                                arg_count,
                                                call_arg_base,
                                                string_temp_base,
                                                string_temp_stride,
                                                string_label_index_io,
                                                control_label_index_io,
                                                out,
                                                cap,
                                                handled_out);
    }
    free(args);
    return ok;
}

static int32_t v3_find_top_level_colon(const char *text) {
    bool in_string = false;
    bool in_char = false;
    int32_t paren_depth = 0;
    int32_t bracket_depth = 0;
    int32_t brace_depth = 0;
    size_t i;
    for (i = 0; text && text[i] != '\0'; ++i) {
        char ch = text[i];
        bool escaped = false;
        size_t slash = i;
        while (slash > 0U && text[slash - 1U] == '\\') {
            escaped = !escaped;
            slash -= 1U;
        }
        if (!in_char && ch == '"' && !escaped) {
            in_string = !in_string;
            continue;
        }
        if (!in_string && ch == '\'' && !escaped) {
            in_char = !in_char;
            continue;
        }
        if (in_char) {
            continue;
        }
        if (in_string) {
            continue;
        }
        if (ch == '(') {
            paren_depth += 1;
            continue;
        }
        if (ch == ')') {
            paren_depth -= 1;
            continue;
        }
        if (ch == '[') {
            bracket_depth += 1;
            continue;
        }
        if (ch == ']') {
            bracket_depth -= 1;
            continue;
        }
        if (ch == '{') {
            brace_depth += 1;
            continue;
        }
        if (ch == '}') {
            brace_depth -= 1;
            continue;
        }
        if (paren_depth == 0 && bracket_depth == 0 && brace_depth == 0 && ch == ':') {
            return (int32_t)i;
        }
    }
    return -1;
}

static bool v3_parse_if_like_header(const char *statement,
                                    const char *keyword,
                                    char *cond_out,
                                    size_t cond_cap,
                                    char *inline_stmt_out,
                                    size_t inline_stmt_cap) {
    char trimmed[8192];
    int32_t colon;
    size_t keyword_len = strlen(keyword);
    if (!v3_startswith(statement, keyword)) {
        return false;
    }
    v3_trim_copy_text(statement, trimmed, sizeof(trimmed));
    colon = v3_find_top_level_colon(trimmed);
    if (colon < 0) {
        return false;
    }
    snprintf(cond_out, cond_cap, "%.*s", colon - (int32_t)keyword_len, trimmed + keyword_len);
    v3_trim_copy_text(cond_out, cond_out, cond_cap);
    snprintf(inline_stmt_out, inline_stmt_cap, "%s", trimmed + colon + 1);
    v3_trim_copy_text(inline_stmt_out, inline_stmt_out, inline_stmt_cap);
    return cond_out[0] != '\0';
}

static bool v3_parse_if_header(const char *statement,
                               char *cond_out,
                               size_t cond_cap,
                               char *inline_stmt_out,
                               size_t inline_stmt_cap) {
    return v3_parse_if_like_header(statement,
                                   "if ",
                                   cond_out,
                                   cond_cap,
                                   inline_stmt_out,
                                   inline_stmt_cap);
}

static bool v3_parse_elif_header(const char *statement,
                                 char *cond_out,
                                 size_t cond_cap,
                                 char *inline_stmt_out,
                                 size_t inline_stmt_cap) {
    return v3_parse_if_like_header(statement,
                                   "elif ",
                                   cond_out,
                                   cond_cap,
                                   inline_stmt_out,
                                   inline_stmt_cap);
}

static bool v3_parse_else_header(const char *statement,
                                 char *inline_stmt_out,
                                 size_t inline_stmt_cap) {
    char trimmed[8192];
    int32_t colon;
    if (!v3_startswith(statement, "else")) {
        return false;
    }
    v3_trim_copy_text(statement, trimmed, sizeof(trimmed));
    colon = v3_find_top_level_colon(trimmed);
    if (colon < 0) {
        return false;
    }
    snprintf(inline_stmt_out, inline_stmt_cap, "%s", trimmed + colon + 1);
    v3_trim_copy_text(inline_stmt_out, inline_stmt_out, inline_stmt_cap);
    return true;
}

static bool v3_parse_case_header(const char *statement,
                                 char *expr_out,
                                 size_t expr_cap) {
    if (!v3_startswith(statement, "case ")) {
        return false;
    }
    v3_trim_copy_text(statement + 5, expr_out, expr_cap);
    return expr_out[0] != '\0';
}

static bool v3_parse_case_arm_header(const char *statement,
                                     bool *is_else_out,
                                     int64_t *match_value_out,
                                     char *inline_stmt_out,
                                     size_t inline_stmt_cap) {
    char trimmed[8192];
    int32_t colon;
    char value_text[128];
    *is_else_out = false;
    *match_value_out = 0;
    inline_stmt_out[0] = '\0';
    v3_trim_copy_text(statement, trimmed, sizeof(trimmed));
    colon = v3_find_top_level_colon(trimmed);
    if (colon < 0) {
        return false;
    }
    if (v3_startswith(trimmed, "of ")) {
        snprintf(value_text, sizeof(value_text), "%.*s", colon - 3, trimmed + 3);
        v3_trim_copy_text(value_text, value_text, sizeof(value_text));
        if (value_text[0] == '\0') {
            return false;
        }
        *match_value_out = strtoll(value_text, NULL, 0);
    } else if (v3_startswith(trimmed, "else")) {
        *is_else_out = true;
    } else {
        return false;
    }
    snprintf(inline_stmt_out, inline_stmt_cap, "%s", trimmed + colon + 1);
    v3_trim_copy_text(inline_stmt_out, inline_stmt_out, inline_stmt_cap);
    return inline_stmt_out[0] != '\0';
}

static bool v3_parse_while_header(const char *statement,
                                  char *cond_out,
                                  size_t cond_cap,
                                  char *inline_stmt_out,
                                  size_t inline_stmt_cap) {
    char trimmed[8192];
    int32_t colon;
    if (!v3_startswith(statement, "while ")) {
        return false;
    }
    v3_trim_copy_text(statement, trimmed, sizeof(trimmed));
    colon = v3_find_top_level_colon(trimmed);
    if (colon < 0) {
        return false;
    }
    snprintf(cond_out, cond_cap, "%.*s", colon - 6, trimmed + 6);
    v3_trim_copy_text(cond_out, cond_out, cond_cap);
    snprintf(inline_stmt_out, inline_stmt_cap, "%s", trimmed + colon + 1);
    v3_trim_copy_text(inline_stmt_out, inline_stmt_out, inline_stmt_cap);
    return cond_out[0] != '\0';
}

static bool v3_parse_for_header(const char *statement,
                                char *iter_name_out,
                                size_t iter_name_cap,
                                char *start_expr_out,
                                size_t start_expr_cap,
                                char *end_expr_out,
                                size_t end_expr_cap,
                                bool *end_inclusive_out,
                                char *inline_stmt_out,
                                size_t inline_stmt_cap) {
    char trimmed[8192];
    int32_t colon;
    char header[8192];
    char *in_sep;
    char *range_text;
    if (!v3_startswith(statement, "for ")) {
        fprintf(stderr, "[cheng_v3_seed] parse for header reject prefix stmt=%s\n", statement);
        return false;
    }
    v3_trim_copy_text(statement, trimmed, sizeof(trimmed));
    colon = v3_find_top_level_colon(trimmed);
    if (colon < 0) {
        fprintf(stderr, "[cheng_v3_seed] parse for header missing colon stmt=%s trimmed=%s\n",
                statement,
                trimmed);
        return false;
    }
    snprintf(header, sizeof(header), "%.*s", colon - 4, trimmed + 4);
    v3_trim_copy_text(header, header, sizeof(header));
    in_sep = strstr(header, " in ");
    if (!in_sep) {
        fprintf(stderr, "[cheng_v3_seed] parse for header missing in-sep stmt=%s header=%s\n",
                statement,
                header);
        return false;
    }
    *in_sep = '\0';
    range_text = in_sep + 4;
    v3_trim_copy_text(header, iter_name_out, iter_name_cap);
    if (!v3_parse_range_expr(range_text,
                             start_expr_out,
                             start_expr_cap,
                             end_expr_out,
                             end_expr_cap,
                             end_inclusive_out)) {
        fprintf(stderr, "[cheng_v3_seed] parse for header missing range-sep stmt=%s range=%s\n",
                statement,
                range_text);
        return false;
    }
    snprintf(inline_stmt_out, inline_stmt_cap, "%s", trimmed + colon + 1);
    v3_trim_copy_text(inline_stmt_out, inline_stmt_out, inline_stmt_cap);
    if (iter_name_out[0] == '\0' || start_expr_out[0] == '\0' || end_expr_out[0] == '\0') {
        fprintf(stderr,
                "[cheng_v3_seed] parse for header empty part stmt=%s iter=%s start=%s end=%s\n",
                statement,
                iter_name_out,
                start_expr_out,
                end_expr_out);
        return false;
    }
    return true;
}

static bool v3_prepare_if_statement_state(const V3SystemLinkPlanStub *plan,
                                          const V3LoweringPlanStub *lowering,
                                          const V3LoweredFunctionStub *function,
                                          const V3ImportAlias *aliases,
                                          size_t alias_count,
                                          V3AsmLocalSlot *locals,
                                          size_t *local_count,
                                          size_t local_cap,
                                          int32_t *next_offset_io,
                                          char **lines,
                                          size_t line_count,
                                          size_t *index_io,
                                          int32_t parent_indent,
                                          const char *statement,
                                          int32_t *max_call_depth_io,
                                          int32_t *max_string_literal_temps_io);
static bool v3_prepare_while_statement_state(const V3SystemLinkPlanStub *plan,
                                             const V3LoweringPlanStub *lowering,
                                             const V3LoweredFunctionStub *function,
                                             const V3ImportAlias *aliases,
                                             size_t alias_count,
                                             V3AsmLocalSlot *locals,
                                             size_t *local_count,
                                             size_t local_cap,
                                             int32_t *next_offset_io,
                                             char **lines,
                                             size_t line_count,
                                             size_t *index_io,
                                             int32_t parent_indent,
                                             const char *statement,
                                             int32_t *max_call_depth_io,
                                             int32_t *max_string_literal_temps_io);
static bool v3_prepare_for_statement_state(const V3SystemLinkPlanStub *plan,
                                           const V3LoweringPlanStub *lowering,
                                           const V3LoweredFunctionStub *function,
                                           const V3ImportAlias *aliases,
                                           size_t alias_count,
                                           V3AsmLocalSlot *locals,
                                           size_t *local_count,
                                           size_t local_cap,
                                           int32_t *next_offset_io,
                                           char **lines,
                                           size_t line_count,
                                           size_t *index_io,
                                           int32_t parent_indent,
                                           const char *statement,
                                           int32_t *max_call_depth_io,
                                           int32_t *max_string_literal_temps_io);
static bool v3_prepare_case_const_table_statement(const V3SystemLinkPlanStub *plan,
                                                  const V3LoweringPlanStub *lowering,
                                                  const V3LoweredFunctionStub *function,
                                                  const V3ImportAlias *aliases,
                                                  size_t alias_count,
                                                  V3AsmLocalSlot *locals,
                                                  size_t *local_count,
                                                  size_t local_cap,
                                                  int32_t *next_offset_io,
                                                  char **lines,
                                                  size_t line_count,
                                                  size_t *index_io,
                                                  int32_t parent_indent,
                                                  const char *statement,
                                                  int32_t *max_call_depth_io,
                                                  int32_t *max_string_literal_temps_io);

static bool v3_prepare_non_if_statement_state(const V3SystemLinkPlanStub *plan,
                                              const V3LoweringPlanStub *lowering,
                                              const V3LoweredFunctionStub *function,
                                              const V3ImportAlias *aliases,
                                              size_t alias_count,
                                              V3AsmLocalSlot *locals,
                                              size_t *local_count,
                                              size_t local_cap,
                                              int32_t *next_offset_io,
                                              const char *statement,
                                              int32_t *max_call_depth_io,
                                              int32_t *max_string_literal_temps_io) {
    char name[128];
    char type_text[CHENG_V3_MAX_TYPE_TEXT];
    char expr[4096];
    if (v3_tuple_binding_expr_omits_items(statement)) {
        fprintf(stderr,
                "[cheng_v3_seed] tuple literal omission unsupported function=%s stmt=%s\n",
                function->symbol_text,
                statement);
        return false;
    }
    if ((v3_startswith(statement, "let ") || v3_startswith(statement, "var ")) &&
        v3_parse_binding_decl_meta(statement,
                                   v3_startswith(statement, "let ") ? "let " : "var ",
                                   name,
                                   sizeof(name),
                                   type_text,
                                   sizeof(type_text))) {
        char normalized_type[256];
        const char *abi_class = v3_resolved_type_abi_class(lowering,
                                                           function->owner_module_path,
                                                           aliases,
                                                           alias_count,
                                                           type_text,
                                                           normalized_type,
                                                           sizeof(normalized_type));
        if (v3_find_local_slot(locals, *local_count, name) < 0) {
            return v3_add_local_slot_for_type(lowering,
                                              function->owner_module_path,
                                              aliases,
                                              alias_count,
                                              locals,
                                              local_count,
                                              local_cap,
                                              name,
                                              type_text,
                                              abi_class,
                                              false,
                                              next_offset_io);
        }
        return true;
    }
    if ((v3_startswith(statement, "let ") || v3_startswith(statement, "var ")) &&
        v3_parse_binding_meta(statement,
                              v3_startswith(statement, "let ") ? "let " : "var ",
                              name,
                              sizeof(name),
                              type_text,
                              sizeof(type_text),
                              expr,
                              sizeof(expr))) {
        const char *abi_class;
        char inferred_type[CHENG_V3_MAX_TYPE_TEXT];
        char inferred_abi[32];
        if (type_text[0] == '\0') {
            if (!v3_infer_expr_type(plan,
                                    lowering,
                                    function,
                                    aliases,
                                    alias_count,
                                    locals,
                                    *local_count,
                                    expr,
                                    inferred_type,
                                    sizeof(inferred_type),
                                    inferred_abi,
                                    sizeof(inferred_abi))) {
                fprintf(stderr,
                        "[cheng_v3_seed] infer binding type failed function=%s name=%s expr=%s\n",
                        function->symbol_text,
                        name,
                        expr);
                return false;
            }
            snprintf(type_text, sizeof(type_text), "%s", inferred_type);
        }
        abi_class = v3_type_abi_class(type_text);
        if (type_text[0] == '\0' || strcmp(abi_class, "void") == 0) {
            return false;
        }
        if (v3_find_local_slot(locals, *local_count, name) < 0 &&
            !v3_add_local_slot_for_type(lowering,
                                        function->owner_module_path,
                                        aliases,
                                        alias_count,
                                        locals,
                                        local_count,
                                        local_cap,
                                        name,
                                        type_text,
                                        abi_class,
                                        false,
                                        next_offset_io)) {
            fprintf(stderr,
                    "[cheng_v3_seed] add binding slot failed function=%s name=%s type=%s abi=%s expr=%s\n",
                    function->symbol_text,
                    name,
                    type_text,
                    abi_class,
                    expr);
            return false;
        }
        if (!v3_tuple_binding_expr_complete(type_text, expr)) {
            fprintf(stderr,
                    "[cheng_v3_seed] tuple literal omission unsupported function=%s name=%s type=%s expr=%s\n",
                    function->symbol_text,
                    name,
                    type_text,
                    expr);
            return false;
        }
        if (v3_call_expr_string_literal_count(expr) > *max_string_literal_temps_io) {
            *max_string_literal_temps_io = v3_call_expr_string_literal_count(expr);
        }
        if (!v3_prepare_expr_call_state(plan,
                                        lowering,
                                        function,
                                        aliases,
                                        alias_count,
                                        locals,
                                        local_count,
                                        local_cap,
                                        next_offset_io,
                                        expr,
                                        1,
                                        max_call_depth_io)) {
            fprintf(stderr,
                    "[cheng_v3_seed] prepare binding expr failed function=%s name=%s type=%s abi=%s expr=%s\n",
                    function->symbol_text,
                    name,
                    type_text,
                    abi_class,
                    expr);
            return false;
        }
        return true;
    }
    if (strcmp(statement, "continue") == 0 || strcmp(statement, "break") == 0) {
        return true;
    }
    if (strcmp(statement, "return") == 0) {
        return true;
    }
    if (v3_startswith(statement, "panic ")) {
        if (v3_call_expr_string_literal_count(statement + 6) > *max_string_literal_temps_io) {
            *max_string_literal_temps_io = v3_call_expr_string_literal_count(statement + 6);
        }
        if (!v3_prepare_expr_call_state(plan,
                                        lowering,
                                        function,
                                        aliases,
                                        alias_count,
                                        locals,
                                        local_count,
                                        local_cap,
                                        next_offset_io,
                                        statement + 6,
                                        0,
                                        max_call_depth_io)) {
            fprintf(stderr,
                    "[cheng_v3_seed] prepare panic expr failed function=%s expr=%s\n",
                    function->symbol_text,
                    statement + 6);
            return false;
        }
        return true;
    }
    if (v3_startswith(statement, "return ")) {
        if (v3_call_expr_string_literal_count(statement + 7) > *max_string_literal_temps_io) {
            *max_string_literal_temps_io = v3_call_expr_string_literal_count(statement + 7);
        }
        if (!v3_prepare_expr_call_state(plan,
                                        lowering,
                                        function,
                                        aliases,
                                        alias_count,
                                        locals,
                                        local_count,
                                        local_cap,
                                        next_offset_io,
                                        statement + 7,
                                        0,
                                        max_call_depth_io)) {
            fprintf(stderr,
                    "[cheng_v3_seed] prepare return expr failed function=%s expr=%s\n",
                    function->symbol_text,
                    statement + 7);
            return false;
        }
        return true;
    }
    if (v3_parse_assignment_meta(statement,
                                 name,
                                 sizeof(name),
                                 expr,
                                 sizeof(expr))) {
        int32_t string_literal_count = v3_assignment_string_literal_count(name, expr);
        bool handled = false;
        if (string_literal_count > *max_string_literal_temps_io) {
            *max_string_literal_temps_io = string_literal_count;
        }
        if (!v3_try_prepare_index_assignment_call_state(plan,
                                                        lowering,
                                                        function,
                                                        aliases,
                                                        alias_count,
                                                        locals,
                                                        local_count,
                                                        local_cap,
                                                        next_offset_io,
                                                        name,
                                                        expr,
                                                        0,
                                                        max_call_depth_io,
                                                        &handled)) {
            fprintf(stderr,
                    "[cheng_v3_seed] prepare index assignment call state failed function=%s target=%s expr=%s stmt=%s\n",
                    function->symbol_text,
                    name,
                    expr,
                    statement);
            return false;
        }
        if (handled) {
            return true;
        }
        if (!v3_prepare_expr_call_state(plan,
                                        lowering,
                                        function,
                                        aliases,
                                        alias_count,
                                        locals,
                                        local_count,
                                        local_cap,
                                        next_offset_io,
                                        expr,
                                        0,
                                        max_call_depth_io)) {
            fprintf(stderr,
                    "[cheng_v3_seed] prepare assignment expr failed function=%s lhs=%s expr=%s\n",
                    function->symbol_text,
                    name,
                    expr);
            return false;
        }
        return true;
    }
    if (strchr(statement, '(') != NULL) {
        if (v3_call_expr_string_literal_count(statement) > *max_string_literal_temps_io) {
            *max_string_literal_temps_io = v3_call_expr_string_literal_count(statement);
        }
        if (!v3_prepare_expr_call_state(plan,
                                        lowering,
                                        function,
                                        aliases,
                                        alias_count,
                                        locals,
                                        local_count,
                                        local_cap,
                                        next_offset_io,
                                        statement,
                                        0,
                                        max_call_depth_io)) {
            fprintf(stderr,
                    "[cheng_v3_seed] prepare stmt call state failed function=%s stmt=%s\n",
                    function->symbol_text,
                    statement);
            return false;
        }
        return true;
    }
    {
        char expr_type[256];
        char expr_abi[32];
        if (v3_infer_expr_type(plan,
                               lowering,
                               function,
                               aliases,
                               alias_count,
                               locals,
                               *local_count,
                               statement,
                               expr_type,
                               sizeof(expr_type),
                               expr_abi,
                               sizeof(expr_abi)) &&
            v3_abi_class_scalar_or_ptr(expr_abi)) {
            if (v3_call_expr_string_literal_count(statement) > *max_string_literal_temps_io) {
                *max_string_literal_temps_io = v3_call_expr_string_literal_count(statement);
            }
            return v3_prepare_expr_call_state(plan,
                                              lowering,
                                              function,
                                              aliases,
                                              alias_count,
                                              locals,
                                              local_count,
                                              local_cap,
                                              next_offset_io,
                                              statement,
                                              0,
                                              max_call_depth_io);
        }
    }
    return false;
}

static bool v3_prepare_if_statement_state(const V3SystemLinkPlanStub *plan,
                                          const V3LoweringPlanStub *lowering,
                                          const V3LoweredFunctionStub *function,
                                          const V3ImportAlias *aliases,
                                          size_t alias_count,
                                          V3AsmLocalSlot *locals,
                                          size_t *local_count,
                                          size_t local_cap,
                                          int32_t *next_offset_io,
                                          char **lines,
                                          size_t line_count,
                                          size_t *index_io,
                                          int32_t parent_indent,
                                          const char *statement,
                                          int32_t *max_call_depth_io,
                                          int32_t *max_string_literal_temps_io) {
    char current_stmt[8192];
    snprintf(current_stmt, sizeof(current_stmt), "%s", statement);
    for (;;) {
        char cond[4096];
        char inline_stmt[8192];
        char next_clause[8192];
        bool is_else = false;
        bool has_next_clause = false;
        if (v3_startswith(current_stmt, "if ")) {
            if (!v3_parse_if_header(current_stmt, cond, sizeof(cond), inline_stmt, sizeof(inline_stmt))) {
                fprintf(stderr,
                        "[cheng_v3_seed] parse if header failed function=%s stmt=%s\n",
                        function->symbol_text,
                        current_stmt);
                return false;
            }
        } else if (v3_startswith(current_stmt, "elif ")) {
            if (!v3_parse_elif_header(current_stmt, cond, sizeof(cond), inline_stmt, sizeof(inline_stmt))) {
                fprintf(stderr,
                        "[cheng_v3_seed] parse elif header failed function=%s stmt=%s\n",
                        function->symbol_text,
                        current_stmt);
                return false;
            }
        } else {
            is_else = true;
            if (!v3_parse_else_header(current_stmt, inline_stmt, sizeof(inline_stmt))) {
                fprintf(stderr,
                        "[cheng_v3_seed] parse else header failed function=%s stmt=%s\n",
                        function->symbol_text,
                        current_stmt);
                return false;
            }
            cond[0] = '\0';
        }
        if (!is_else) {
            if (v3_call_expr_string_literal_count(cond) > *max_string_literal_temps_io) {
                *max_string_literal_temps_io = v3_call_expr_string_literal_count(cond);
            }
            if (!v3_prepare_expr_call_state(plan,
                                            lowering,
                                            function,
                                            aliases,
                                            alias_count,
                                            locals,
                                            local_count,
                                            local_cap,
                                            next_offset_io,
                                            cond,
                                            0,
                                            max_call_depth_io)) {
                fprintf(stderr,
                        "[cheng_v3_seed] prepare if cond failed function=%s cond=%s\n",
                        function->symbol_text,
                        cond);
                return false;
            }
        }
        if (inline_stmt[0] != '\0') {
            if (v3_startswith(inline_stmt, "if ") ||
                v3_startswith(inline_stmt, "while ") ||
                v3_startswith(inline_stmt, "for ") ||
                v3_startswith(inline_stmt, "case ")) {
                return false;
            }
            if (!v3_prepare_non_if_statement_state(plan,
                                                   lowering,
                                                   function,
                                                   aliases,
                                                   alias_count,
                                                   locals,
                                                   local_count,
                                                   local_cap,
                                                   next_offset_io,
                                                   inline_stmt,
                                                   max_call_depth_io,
                                                   max_string_literal_temps_io)) {
                return false;
            }
        } else {
            while (*index_io < line_count) {
                char raw_copy[4096];
                char *trimmed;
                int32_t indent;
                char nested_stmt[8192];
                snprintf(raw_copy, sizeof(raw_copy), "%s", lines[*index_io]);
                trimmed = v3_trim_inplace(raw_copy);
                if (*trimmed == '\0') {
                    *index_io += 1U;
                    continue;
                }
                indent = v3_line_indent(lines[*index_io]);
                if (indent < parent_indent) {
                    break;
                }
                if (indent == parent_indent &&
                    (v3_startswith(trimmed, "elif ") || v3_startswith(trimmed, "else"))) {
                    if (!v3_collect_statement_from_lines(lines,
                                                         line_count,
                                                         index_io,
                                                         next_clause,
                                                         sizeof(next_clause))) {
                        return false;
                    }
                    has_next_clause = true;
                    break;
                }
                if (indent <= parent_indent) {
                    break;
                }
                if (!v3_collect_statement_from_lines(lines, line_count, index_io, nested_stmt, sizeof(nested_stmt))) {
                    return false;
                }
                if (v3_startswith(nested_stmt, "if ")) {
                if (!v3_prepare_if_statement_state(plan,
                                                   lowering,
                                                   function,
                                                   aliases,
                                                       alias_count,
                                                       locals,
                                                       local_count,
                                                       local_cap,
                                                       next_offset_io,
                                                       lines,
                                                       line_count,
                                                       index_io,
                                                       indent,
                                                   nested_stmt,
                                                   max_call_depth_io,
                                                   max_string_literal_temps_io)) {
                    fprintf(stderr,
                            "[cheng_v3_seed] prepare if child if failed function=%s parent=%s nested=%s\n",
                            function->symbol_text,
                            current_stmt,
                            nested_stmt);
                    return false;
                }
                continue;
            }
            if (v3_startswith(nested_stmt, "while ")) {
                    if (!v3_prepare_while_statement_state(plan,
                                                          lowering,
                                                          function,
                                                          aliases,
                                                          alias_count,
                                                          locals,
                                                          local_count,
                                                          local_cap,
                                                          next_offset_io,
                                                          lines,
                                                          line_count,
                                                          index_io,
                                                          indent,
                                                      nested_stmt,
                                                      max_call_depth_io,
                                                      max_string_literal_temps_io)) {
                    fprintf(stderr,
                            "[cheng_v3_seed] prepare if child while failed function=%s parent=%s nested=%s\n",
                            function->symbol_text,
                            current_stmt,
                            nested_stmt);
                    return false;
                }
                continue;
            }
            if (v3_startswith(nested_stmt, "for ")) {
                    if (!v3_prepare_for_statement_state(plan,
                                                        lowering,
                                                        function,
                                                        aliases,
                                                        alias_count,
                                                        locals,
                                                        local_count,
                                                        local_cap,
                                                        next_offset_io,
                                                        lines,
                                                        line_count,
                                                        index_io,
                                                        indent,
                                                    nested_stmt,
                                                    max_call_depth_io,
                                                    max_string_literal_temps_io)) {
                    fprintf(stderr,
                            "[cheng_v3_seed] prepare if child for failed function=%s parent=%s nested=%s\n",
                            function->symbol_text,
                            current_stmt,
                            nested_stmt);
                    return false;
                }
                continue;
            }
                if (!v3_prepare_non_if_statement_state(plan,
                                                       lowering,
                                                       function,
                                                       aliases,
                                                       alias_count,
                                                       locals,
                                                       local_count,
                                                       local_cap,
                                                       next_offset_io,
                                                       nested_stmt,
                                                       max_call_depth_io,
                                                       max_string_literal_temps_io)) {
                    fprintf(stderr,
                            "[cheng_v3_seed] prepare if nested stmt failed function=%s parent=%s nested=%s\n",
                            function->symbol_text,
                            current_stmt,
                            nested_stmt);
                    return false;
                }
            }
        }
        if (!has_next_clause) {
            break;
        }
        snprintf(current_stmt, sizeof(current_stmt), "%s", next_clause);
    }
    return true;
}

static bool v3_prepare_case_const_table_statement(const V3SystemLinkPlanStub *plan,
                                                  const V3LoweringPlanStub *lowering,
                                                  const V3LoweredFunctionStub *function,
                                                  const V3ImportAlias *aliases,
                                                  size_t alias_count,
                                                  V3AsmLocalSlot *locals,
                                                  size_t *local_count,
                                                  size_t local_cap,
                                                  int32_t *next_offset_io,
                                                  char **lines,
                                                  size_t line_count,
                                                  size_t *index_io,
                                                  int32_t parent_indent,
                                                  const char *statement,
                                                  int32_t *max_call_depth_io,
                                                  int32_t *max_string_literal_temps_io) {
    char case_expr[4096];
    if (!v3_parse_case_header(statement, case_expr, sizeof(case_expr)) ||
        !v3_prepare_expr_call_state(plan,
                                    lowering,
                                    function,
                                    aliases,
                                    alias_count,
                                    locals,
                                    local_count,
                                    local_cap,
                                    next_offset_io,
                                    case_expr,
                                    0,
                                    max_call_depth_io)) {
        return false;
    }
    while (*index_io < line_count) {
        char raw_copy[4096];
        char *trimmed;
        int32_t indent;
        char arm_stmt[8192];
        bool is_else = false;
        int64_t match_value = 0;
        char inline_stmt[8192];
        snprintf(raw_copy, sizeof(raw_copy), "%s", lines[*index_io]);
        trimmed = v3_trim_inplace(raw_copy);
        if (*trimmed == '\0') {
            *index_io += 1U;
            continue;
        }
        indent = v3_line_indent(lines[*index_io]);
        if (indent < parent_indent) {
            break;
        }
        if (indent == parent_indent &&
            !v3_startswith(trimmed, "of ") &&
            !v3_startswith(trimmed, "else")) {
            break;
        }
        if (!v3_collect_statement_from_lines(lines, line_count, index_io, arm_stmt, sizeof(arm_stmt)) ||
            !v3_parse_case_arm_header(arm_stmt,
                                      &is_else,
                                      &match_value,
                                      inline_stmt,
                                      sizeof(inline_stmt))) {
            return false;
        }
        if (v3_startswith(inline_stmt, "if ") ||
            v3_startswith(inline_stmt, "while ") ||
            v3_startswith(inline_stmt, "for ") ||
            v3_startswith(inline_stmt, "case ")) {
            return false;
        }
        if (v3_call_expr_string_literal_count(inline_stmt) > *max_string_literal_temps_io) {
            *max_string_literal_temps_io = v3_call_expr_string_literal_count(inline_stmt);
        }
        if (!v3_prepare_non_if_statement_state(plan,
                                               lowering,
                                               function,
                                               aliases,
                                               alias_count,
                                               locals,
                                               local_count,
                                               local_cap,
                                               next_offset_io,
                                               inline_stmt,
                                               max_call_depth_io,
                                               max_string_literal_temps_io)) {
            return false;
        }
    }
    return true;
}

static bool v3_prepare_while_statement_state(const V3SystemLinkPlanStub *plan,
                                             const V3LoweringPlanStub *lowering,
                                             const V3LoweredFunctionStub *function,
                                             const V3ImportAlias *aliases,
                                             size_t alias_count,
                                             V3AsmLocalSlot *locals,
                                             size_t *local_count,
                                             size_t local_cap,
                                             int32_t *next_offset_io,
                                             char **lines,
                                             size_t line_count,
                                             size_t *index_io,
                                             int32_t parent_indent,
                                             const char *statement,
                                             int32_t *max_call_depth_io,
                                             int32_t *max_string_literal_temps_io) {
    char cond[4096];
    char inline_stmt[8192];
    if (!v3_parse_while_header(statement, cond, sizeof(cond), inline_stmt, sizeof(inline_stmt))) {
        fprintf(stderr,
                "[cheng_v3_seed] parse while header failed function=%s stmt=%s\n",
                function->symbol_text,
                statement);
        return false;
    }
    if (v3_call_expr_string_literal_count(cond) > *max_string_literal_temps_io) {
        *max_string_literal_temps_io = v3_call_expr_string_literal_count(cond);
    }
    if (!v3_prepare_expr_call_state(plan,
                                    lowering,
                                    function,
                                    aliases,
                                    alias_count,
                                    locals,
                                    local_count,
                                    local_cap,
                                    next_offset_io,
                                    cond,
                                    0,
                                    max_call_depth_io)) {
        fprintf(stderr,
                "[cheng_v3_seed] prepare while cond failed function=%s cond=%s\n",
                function->symbol_text,
                cond);
        return false;
    }
    if (inline_stmt[0] != '\0') {
        if (v3_startswith(inline_stmt, "if ") ||
            v3_startswith(inline_stmt, "while ") ||
            v3_startswith(inline_stmt, "for ")) {
            return false;
        }
        if (!v3_prepare_non_if_statement_state(plan,
                                               lowering,
                                               function,
                                               aliases,
                                               alias_count,
                                               locals,
                                               local_count,
                                               local_cap,
                                               next_offset_io,
                                               inline_stmt,
                                               max_call_depth_io,
                                               max_string_literal_temps_io)) {
            fprintf(stderr,
                    "[cheng_v3_seed] prepare while inline stmt failed function=%s stmt=%s inline=%s\n",
                    function->symbol_text,
                    statement,
                    inline_stmt);
            return false;
        }
        return true;
    }
    while (*index_io < line_count) {
        char raw_copy[4096];
        char *trimmed;
        int32_t indent;
        char nested_stmt[8192];
        snprintf(raw_copy, sizeof(raw_copy), "%s", lines[*index_io]);
        trimmed = v3_trim_inplace(raw_copy);
        if (*trimmed == '\0') {
            *index_io += 1U;
            continue;
        }
        indent = v3_line_indent(lines[*index_io]);
        if (indent <= parent_indent) {
            break;
        }
        if (!v3_collect_statement_from_lines(lines, line_count, index_io, nested_stmt, sizeof(nested_stmt))) {
            return false;
        }
        if (v3_startswith(nested_stmt, "if ")) {
            if (!v3_prepare_if_statement_state(plan,
                                               lowering,
                                               function,
                                               aliases,
                                               alias_count,
                                               locals,
                                               local_count,
                                               local_cap,
                                               next_offset_io,
                                               lines,
                                               line_count,
                                               index_io,
                                               indent,
                                               nested_stmt,
                                               max_call_depth_io,
                                               max_string_literal_temps_io)) {
                fprintf(stderr,
                        "[cheng_v3_seed] prepare while child if failed function=%s parent=%s nested=%s\n",
                        function->symbol_text,
                        statement,
                        nested_stmt);
                return false;
            }
            continue;
        }
        if (v3_startswith(nested_stmt, "while ")) {
            if (!v3_prepare_while_statement_state(plan,
                                                  lowering,
                                                  function,
                                                  aliases,
                                                  alias_count,
                                                  locals,
                                                  local_count,
                                                  local_cap,
                                                  next_offset_io,
                                                  lines,
                                                  line_count,
                                                  index_io,
                                                  indent,
                                                  nested_stmt,
                                                  max_call_depth_io,
                                                  max_string_literal_temps_io)) {
                fprintf(stderr,
                        "[cheng_v3_seed] prepare while child while failed function=%s parent=%s nested=%s\n",
                        function->symbol_text,
                        statement,
                        nested_stmt);
                return false;
            }
            continue;
        }
        if (v3_startswith(nested_stmt, "for ")) {
            if (!v3_prepare_for_statement_state(plan,
                                                lowering,
                                                function,
                                                aliases,
                                                alias_count,
                                                locals,
                                                local_count,
                                                local_cap,
                                                next_offset_io,
                                                lines,
                                                line_count,
                                                index_io,
                                                indent,
                                                nested_stmt,
                                                max_call_depth_io,
                                                max_string_literal_temps_io)) {
                fprintf(stderr,
                        "[cheng_v3_seed] prepare while child for failed function=%s parent=%s nested=%s\n",
                        function->symbol_text,
                        statement,
                        nested_stmt);
                return false;
            }
            continue;
        }
        if (!v3_prepare_non_if_statement_state(plan,
                                               lowering,
                                               function,
                                               aliases,
                                               alias_count,
                                               locals,
                                               local_count,
                                               local_cap,
                                               next_offset_io,
                                               nested_stmt,
                                               max_call_depth_io,
                                               max_string_literal_temps_io)) {
            fprintf(stderr,
                    "[cheng_v3_seed] prepare while nested stmt failed function=%s parent=%s nested=%s\n",
                    function->symbol_text,
                    statement,
                    nested_stmt);
            return false;
        }
    }
    return true;
}

static bool v3_prepare_for_statement_state(const V3SystemLinkPlanStub *plan,
                                           const V3LoweringPlanStub *lowering,
                                           const V3LoweredFunctionStub *function,
                                           const V3ImportAlias *aliases,
                                           size_t alias_count,
                                           V3AsmLocalSlot *locals,
                                           size_t *local_count,
                                           size_t local_cap,
                                           int32_t *next_offset_io,
                                           char **lines,
                                           size_t line_count,
                                           size_t *index_io,
                                           int32_t parent_indent,
                                           const char *statement,
                                           int32_t *max_call_depth_io,
                                           int32_t *max_string_literal_temps_io) {
    char iter_name[128];
    char start_expr[4096];
    char end_expr[4096];
    bool end_inclusive = false;
    char inline_stmt[8192];
    if (!v3_parse_for_header(statement,
                             iter_name,
                             sizeof(iter_name),
                             start_expr,
                             sizeof(start_expr),
                             end_expr,
                             sizeof(end_expr),
                             &end_inclusive,
                             inline_stmt,
                             sizeof(inline_stmt))) {
        fprintf(stderr,
                "[cheng_v3_seed] parse for header failed function=%s stmt=%s\n",
                function->symbol_text,
                statement);
        return false;
    }
    if (v3_find_local_slot(locals, *local_count, iter_name) < 0 &&
        !v3_add_local_slot_for_type(lowering,
                                    function->owner_module_path,
                                    aliases,
                                    alias_count,
                                    locals,
                                    local_count,
                                    local_cap,
                                    iter_name,
                                    "int32",
                                    "i32",
                                    false,
                                    next_offset_io)) {
        return false;
    }
    if (v3_call_expr_string_literal_count(start_expr) > *max_string_literal_temps_io) {
        *max_string_literal_temps_io = v3_call_expr_string_literal_count(start_expr);
    }
    if (v3_call_expr_string_literal_count(end_expr) > *max_string_literal_temps_io) {
        *max_string_literal_temps_io = v3_call_expr_string_literal_count(end_expr);
    }
    if (!v3_prepare_expr_call_state(plan,
                                    lowering,
                                    function,
                                    aliases,
                                    alias_count,
                                    locals,
                                    local_count,
                                    local_cap,
                                    next_offset_io,
                                    start_expr,
                                    0,
                                    max_call_depth_io) ||
        !v3_prepare_expr_call_state(plan,
                                    lowering,
                                    function,
                                    aliases,
                                    alias_count,
                                    locals,
                                    local_count,
                                    local_cap,
                                    next_offset_io,
                                    end_expr,
                                    0,
                                    max_call_depth_io)) {
        fprintf(stderr,
                "[cheng_v3_seed] prepare for range failed function=%s start=%s end=%s\n",
                function->symbol_text,
                start_expr,
                end_expr);
        return false;
    }
    if (inline_stmt[0] != '\0') {
        if (v3_startswith(inline_stmt, "if ") ||
            v3_startswith(inline_stmt, "while ") ||
            v3_startswith(inline_stmt, "for ")) {
            return false;
        }
        if (!v3_prepare_non_if_statement_state(plan,
                                               lowering,
                                               function,
                                               aliases,
                                               alias_count,
                                               locals,
                                               local_count,
                                               local_cap,
                                               next_offset_io,
                                               inline_stmt,
                                               max_call_depth_io,
                                               max_string_literal_temps_io)) {
            fprintf(stderr,
                    "[cheng_v3_seed] prepare for inline stmt failed function=%s stmt=%s inline=%s\n",
                    function->symbol_text,
                    statement,
                    inline_stmt);
            return false;
        }
        return true;
    }
    while (*index_io < line_count) {
        char raw_copy[4096];
        char *trimmed;
        int32_t indent;
        char nested_stmt[8192];
        snprintf(raw_copy, sizeof(raw_copy), "%s", lines[*index_io]);
        trimmed = v3_trim_inplace(raw_copy);
        if (*trimmed == '\0') {
            *index_io += 1U;
            continue;
        }
        indent = v3_line_indent(lines[*index_io]);
        if (indent <= parent_indent) {
            break;
        }
        if (!v3_collect_statement_from_lines(lines, line_count, index_io, nested_stmt, sizeof(nested_stmt))) {
            return false;
        }
        if (v3_startswith(nested_stmt, "if ")) {
            if (!v3_prepare_if_statement_state(plan,
                                               lowering,
                                               function,
                                               aliases,
                                               alias_count,
                                               locals,
                                               local_count,
                                               local_cap,
                                               next_offset_io,
                                               lines,
                                               line_count,
                                               index_io,
                                               indent,
                                               nested_stmt,
                                               max_call_depth_io,
                                               max_string_literal_temps_io)) {
                fprintf(stderr,
                        "[cheng_v3_seed] prepare for child if failed function=%s parent=%s nested=%s\n",
                        function->symbol_text,
                        statement,
                        nested_stmt);
                return false;
            }
            continue;
        }
        if (v3_startswith(nested_stmt, "while ")) {
            if (!v3_prepare_while_statement_state(plan,
                                                  lowering,
                                                  function,
                                                  aliases,
                                                  alias_count,
                                                  locals,
                                                  local_count,
                                                  local_cap,
                                                  next_offset_io,
                                                  lines,
                                                  line_count,
                                                  index_io,
                                                  indent,
                                                  nested_stmt,
                                                  max_call_depth_io,
                                                  max_string_literal_temps_io)) {
                fprintf(stderr,
                        "[cheng_v3_seed] prepare for child while failed function=%s parent=%s nested=%s\n",
                        function->symbol_text,
                        statement,
                        nested_stmt);
                return false;
            }
            continue;
        }
        if (v3_startswith(nested_stmt, "for ")) {
            if (!v3_prepare_for_statement_state(plan,
                                                lowering,
                                                function,
                                                aliases,
                                                alias_count,
                                                locals,
                                                local_count,
                                                local_cap,
                                                next_offset_io,
                                                lines,
                                                line_count,
                                                index_io,
                                                indent,
                                                nested_stmt,
                                                max_call_depth_io,
                                                max_string_literal_temps_io)) {
                fprintf(stderr,
                        "[cheng_v3_seed] prepare for child for failed function=%s parent=%s nested=%s\n",
                        function->symbol_text,
                        statement,
                        nested_stmt);
                return false;
            }
            continue;
        }
        if (!v3_prepare_non_if_statement_state(plan,
                                               lowering,
                                               function,
                                               aliases,
                                               alias_count,
                                               locals,
                                               local_count,
                                               local_cap,
                                               next_offset_io,
                                               nested_stmt,
                                               max_call_depth_io,
                                               max_string_literal_temps_io)) {
            fprintf(stderr,
                    "[cheng_v3_seed] prepare for nested stmt failed function=%s parent=%s nested=%s\n",
                    function->symbol_text,
                    statement,
                    nested_stmt);
            return false;
        }
    }
    return true;
}

static bool v3_emit_if_statement(const V3SystemLinkPlanStub *plan,
                                 const V3LoweringPlanStub *lowering,
                                 const V3LoweredFunctionStub *function,
                                 const V3ImportAlias *aliases,
                                 size_t alias_count,
                                 const V3AsmLocalSlot *locals,
                                 size_t local_count,
                                 char **lines,
                                 size_t line_count,
                                 size_t *index_io,
                                 int32_t parent_indent,
                                 const char *statement,
                                 int32_t call_arg_base,
                                 int32_t string_temp_base,
                                 int32_t string_temp_stride,
                                 int32_t *string_label_index_io,
                                 int32_t *control_label_index_io,
                                 int32_t frame_size,
                                 int32_t sret_local_index,
                                 char *out,
                                 size_t cap);
static bool v3_emit_while_statement(const V3SystemLinkPlanStub *plan,
                                    const V3LoweringPlanStub *lowering,
                                    const V3LoweredFunctionStub *function,
                                    const V3ImportAlias *aliases,
                                    size_t alias_count,
                                    const V3AsmLocalSlot *locals,
                                    size_t local_count,
                                    char **lines,
                                    size_t line_count,
                                    size_t *index_io,
                                    int32_t parent_indent,
                                    const char *statement,
                                    int32_t call_arg_base,
                                    int32_t string_temp_base,
                                    int32_t string_temp_stride,
                                    int32_t *string_label_index_io,
                                    int32_t *control_label_index_io,
                                    int32_t frame_size,
                                    int32_t sret_local_index,
                                    char *out,
                                    size_t cap);
static bool v3_emit_for_statement(const V3SystemLinkPlanStub *plan,
                                  const V3LoweringPlanStub *lowering,
                                  const V3LoweredFunctionStub *function,
                                  const V3ImportAlias *aliases,
                                  size_t alias_count,
                                  const V3AsmLocalSlot *locals,
                                  size_t local_count,
                                  char **lines,
                                  size_t line_count,
                                  size_t *index_io,
                                  int32_t parent_indent,
                                  const char *statement,
                                  int32_t call_arg_base,
                                  int32_t string_temp_base,
                                  int32_t string_temp_stride,
                                  int32_t *string_label_index_io,
                                  int32_t *control_label_index_io,
                                  int32_t frame_size,
                                  int32_t sret_local_index,
                                  char *out,
                                  size_t cap);
static bool v3_emit_case_const_table_statement(const V3SystemLinkPlanStub *plan,
                                               const V3LoweringPlanStub *lowering,
                                               const V3LoweredFunctionStub *function,
                                               const V3ImportAlias *aliases,
                                               size_t alias_count,
                                               const V3AsmLocalSlot *locals,
                                               size_t local_count,
                                               char **lines,
                                               size_t line_count,
                                               size_t *index_io,
                                               int32_t parent_indent,
                                               const char *statement,
                                               int32_t call_arg_base,
                                               int32_t string_temp_base,
                                               int32_t string_temp_stride,
                                               int32_t *string_label_index_io,
                                               int32_t *control_label_index_io,
                                               int32_t frame_size,
                                               int32_t sret_local_index,
                                               bool *terminated_out,
                                               char *out,
                                               size_t cap);

static bool v3_is_implicit_return_statement(const V3LoweredFunctionStub *function,
                                            char **lines,
                                            size_t line_count,
                                            size_t next_index,
                                            const char *statement) {
    if (strcmp(function->body_kind, "return_expr") != 0) {
        return false;
    }
    if (strcmp(statement, "return") == 0 ||
        v3_startswith(statement, "return ")) {
        return false;
    }
    return v3_is_last_logical_statement_in_function(lines, line_count, next_index);
}

static bool v3_emit_non_if_statement(const V3SystemLinkPlanStub *plan,
                                     const V3LoweringPlanStub *lowering,
                                     const V3LoweredFunctionStub *function,
                                     const V3ImportAlias *aliases,
                                     size_t alias_count,
                                     const V3AsmLocalSlot *locals,
                                     size_t local_count,
                                     const char *statement,
                                     int32_t call_arg_base,
                                     int32_t string_temp_base,
                                     int32_t string_temp_stride,
                                     int32_t *string_label_index_io,
                                     int32_t *control_label_index_io,
                                     int32_t frame_size,
                                     int32_t sret_local_index,
                                     char *out,
                                     size_t cap,
                                     bool *terminated_out) {
    char name[128];
    char type_text[CHENG_V3_MAX_TYPE_TEXT];
    char expr[4096];
    *terminated_out = false;
    if (strcmp(statement, "continue") == 0) {
        const char *label = v3_current_continue_label();
        if (!label) {
            return false;
        }
        v3_emit_jump_label(out, cap, label);
        *terminated_out = true;
        return true;
    }
    if (strcmp(statement, "break") == 0) {
        const char *label = v3_current_break_label();
        if (!label) {
            return false;
        }
        v3_emit_jump_label(out, cap, label);
        *terminated_out = true;
        return true;
    }
    if (v3_tuple_binding_expr_omits_items(statement)) {
        fprintf(stderr,
                "[cheng_v3_seed] tuple literal omission unsupported function=%s stmt=%s\n",
                function->symbol_text,
                statement);
        return false;
    }
    if ((v3_startswith(statement, "let ") || v3_startswith(statement, "var ")) &&
        v3_parse_binding_decl_meta(statement,
                                   v3_startswith(statement, "let ") ? "let " : "var ",
                                   name,
                                   sizeof(name),
                                   type_text,
                                   sizeof(type_text))) {
        int32_t local_index = v3_find_local_slot(locals, local_count, name);
        return local_index >= 0 &&
               v3_emit_slot_address(out, cap, &locals[local_index], 15) &&
               v3_emit_implicit_default_into_address(plan,
                                                    lowering,
                                                    function,
                                                    aliases,
                                                    alias_count,
                                                    function->owner_module_path,
                                                    function->source_path,
                                                    locals[local_index].type_text,
                                                    15,
                                                    call_arg_base,
                                                    string_temp_base,
                                                    string_temp_stride,
                                                    string_label_index_io,
                                                    0,
                                                    out,
                                                    cap,
                                                    0U);
    }
    if ((v3_startswith(statement, "let ") || v3_startswith(statement, "var ")) &&
        v3_parse_binding_meta(statement,
                              v3_startswith(statement, "let ") ? "let " : "var ",
                              name,
                              sizeof(name),
                              type_text,
                              sizeof(type_text),
                              expr,
                              sizeof(expr))) {
        int32_t local_index = v3_find_local_slot(locals, local_count, name);
        const char *abi_class;
        char inferred_type[CHENG_V3_MAX_TYPE_TEXT];
        char inferred_abi[32];
        char normalized_type[256];
        if (type_text[0] == '\0') {
            if (!v3_infer_expr_type(plan,
                                    lowering,
                                    function,
                                    aliases,
                                    alias_count,
                                    locals,
                                    local_count,
                                    expr,
                                    inferred_type,
                                    sizeof(inferred_type),
                                    inferred_abi,
                                    sizeof(inferred_abi))) {
                return false;
            }
            snprintf(type_text, sizeof(type_text), "%s", inferred_type);
        }
        if (!v3_tuple_binding_expr_complete(type_text, expr)) {
            return false;
        }
        abi_class = v3_resolved_type_abi_class(lowering,
                                               function->owner_module_path,
                                               aliases,
                                               alias_count,
                                               type_text,
                                               normalized_type,
                                               sizeof(normalized_type));
        if (local_index < 0 || locals[local_index].stack_offset < 0) {
            return false;
        }
        if (v3_abi_class_scalar_or_ptr(abi_class)) {
            if (!v3_codegen_expr_scalar(plan,
                                        lowering,
                                        function,
                                        aliases,
                                        alias_count,
                                        locals,
                                        local_count,
                                        expr,
                                        abi_class,
                                        9,
                                        call_arg_base,
                                        string_temp_base,
                                        string_temp_stride,
                                        0,
                                        out,
                                        cap)) {
                return false;
            }
            v3_emit_store_slot(out, cap, &locals[local_index], 9);
            return true;
        }
        return v3_materialize_composite_expr_into_slot(plan,
                                                       lowering,
                                                       function,
                                                       aliases,
                                                       alias_count,
                                                       locals,
                                                       local_count,
                                                       name,
                                                       0U,
                                                       expr,
                                                       &locals[local_index],
                                                       call_arg_base,
                                                       string_temp_base,
                                                       string_temp_stride,
                                                       string_label_index_io,
                                                       0,
                                                       out,
                                                       cap);
    }
    if (v3_parse_assignment_meta(statement,
                                 name,
                                 sizeof(name),
                                 expr,
                                 sizeof(expr))) {
        int32_t local_index = v3_find_local_slot(locals, local_count, name);
        char field_type[256];
        char field_abi[32];
        bool handled = false;
        if (!v3_try_codegen_index_assignment_call(plan,
                                                  lowering,
                                                  function,
                                                  aliases,
                                                  alias_count,
                                                  locals,
                                                  local_count,
                                                  name,
                                                  expr,
                                                  call_arg_base,
                                                  string_temp_base,
                                                  string_temp_stride,
                                                  string_label_index_io,
                                                  0,
                                                  out,
                                                  cap,
                                                  &handled)) {
            return false;
        }
        if (handled) {
            return true;
        }
        if (local_index >= 0) {
            const char *abi_class = locals[local_index].abi_class;
            if (v3_abi_class_scalar_or_ptr(abi_class)) {
                if (!v3_codegen_expr_scalar(plan,
                                            lowering,
                                            function,
                                            aliases,
                                            alias_count,
                                            locals,
                                            local_count,
                                            expr,
                                            abi_class,
                                            9,
                                            call_arg_base,
                                            string_temp_base,
                                            string_temp_stride,
                                            0,
                                            out,
                                            cap)) {
                    return false;
                }
                v3_emit_store_slot(out, cap, &locals[local_index], 9);
                return true;
            }
            return v3_materialize_composite_expr_into_slot(plan,
                                                           lowering,
                                                           function,
                                                           aliases,
                                                           alias_count,
                                                           locals,
                                                           local_count,
                                                           name,
                                                           0U,
                                                           expr,
                                                           &locals[local_index],
                                                           call_arg_base,
                                                           string_temp_base,
                                                           string_temp_stride,
                                                           string_label_index_io,
                                                           0,
                                                           out,
                                                           cap);
        }
        if (v3_infer_lvalue_expr_type(plan,
                                      lowering,
                                      function,
                                      aliases,
                                      alias_count,
                                      locals,
                                      local_count,
                                      name,
                                      field_type,
                                      sizeof(field_type),
                                      field_abi,
                                      sizeof(field_abi))) {
            if (v3_abi_class_scalar_or_ptr(field_abi)) {
                if (!v3_emit_lvalue_address(plan,
                                            lowering,
                                            function,
                                            aliases,
                                            alias_count,
                                            locals,
                                            local_count,
                                            name,
                                            field_type,
                                            sizeof(field_type),
                                            field_abi,
                                            sizeof(field_abi),
                                            15,
                                            call_arg_base,
                                            string_temp_base,
                                            string_temp_stride,
                                            0,
                                            out,
                                            cap)) {
                    return false;
                }
                v3_emit_address_spill_store(out, cap, call_arg_base, 0, 15);
                if (!v3_codegen_expr_scalar(plan,
                                            lowering,
                                            function,
                                            aliases,
                                            alias_count,
                                            locals,
                                            local_count,
                                            expr,
                                            field_abi,
                                            9,
                                            call_arg_base,
                                            string_temp_base,
                                            string_temp_stride,
                                            1,
                                            out,
                                            cap)) {
                    return false;
                }
                v3_emit_address_spill_load(out, cap, call_arg_base, 0, 15);
                v3_emit_store_scalar_to_address(out, cap, 15, 0, field_abi, 9);
                return true;
            }
            if (!v3_emit_lvalue_address(plan,
                                        lowering,
                                        function,
                                        aliases,
                                        alias_count,
                                        locals,
                                        local_count,
                                        name,
                                        field_type,
                                        sizeof(field_type),
                                        field_abi,
                                        sizeof(field_abi),
                                        15,
                                        call_arg_base,
                                        string_temp_base,
                                        string_temp_stride,
                                        0,
                                        out,
                                        cap)) {
                return false;
            }
            return v3_materialize_composite_expr_into_address(plan,
                                                              lowering,
                                                              function,
                                                              aliases,
                                                              alias_count,
                                                              locals,
                                                              local_count,
                                                              expr,
                                                              field_type,
                                                              15,
                                                              call_arg_base,
                                                              string_temp_base,
                                                              string_temp_stride,
                                                              string_label_index_io,
                                                              0,
                                                              out,
                                                              cap);
        }
        return false;
    }
    if (strcmp(statement, "return") == 0) {
        if (strcmp(function->return_abi_class, "void") != 0) {
            return false;
        }
        if (!v3_emit_function_epilogue(out, cap, frame_size)) {
            return false;
        }
        *terminated_out = true;
        return true;
    }
    if (v3_startswith(statement, "panic ")) {
        bool handled = false;
        char call_text[8192];
        snprintf(call_text, sizeof(call_text), "panic(%s)", statement + 6);
        if (!v3_try_emit_builtin_statement(plan,
                                           lowering,
                                           function,
                                           aliases,
                                           alias_count,
                                           locals,
                                           local_count,
                                           call_text,
                                           call_arg_base,
                                           string_temp_base,
                                           string_temp_stride,
                                           string_label_index_io,
                                           control_label_index_io,
                                           out,
                                           cap,
                                           &handled) ||
            !handled) {
            return false;
        }
        *terminated_out = true;
        return true;
    }
    if (v3_startswith(statement, "return ")) {
        if (strcmp(function->return_abi_class, "composite") == 0) {
            if (sret_local_index < 0 ||
                !v3_emit_slot_address(out, cap, &locals[sret_local_index], 15) ||
                !v3_materialize_composite_expr_into_address(plan,
                                                            lowering,
                                                            function,
                                                            aliases,
                                                            alias_count,
                                                            locals,
                                                            local_count,
                                                            statement + 7,
                                                            function->return_type,
                                                            15,
                                                            call_arg_base,
                                                            string_temp_base,
                                                            string_temp_stride,
                                                            string_label_index_io,
                                                            0,
                                                            out,
                                                            cap)) {
                return false;
            }
        } else if (!v3_codegen_expr_scalar(plan,
                                           lowering,
                                           function,
                                           aliases,
                                           alias_count,
                                           locals,
                                           local_count,
                                           statement + 7,
                                           function->return_abi_class,
                                           9,
                                           call_arg_base,
                                           string_temp_base,
                                           string_temp_stride,
                                           0,
                                           out,
                                           cap)) {
            return false;
        } else {
            v3_emit_reg_to_return(out, cap, 9, function->return_abi_class);
        }
        if (!v3_emit_function_epilogue(out, cap, frame_size)) {
            return false;
        }
        *terminated_out = true;
        return true;
    }
    if (strchr(statement, '(') != NULL) {
        bool handled = false;
        char callee[PATH_MAX];
        char (*args)[4096] = v3_alloc_call_arg_scratch();
        size_t arg_count = 0U;
        callee[0] = '\0';
        v3_parse_call_text(statement, callee, sizeof(callee), args, &arg_count, CHENG_V3_MAX_CALL_ARGS);
        if (!v3_try_emit_builtin_statement(plan,
                                           lowering,
                                           function,
                                           aliases,
                                           alias_count,
                                           locals,
                                           local_count,
                                           statement,
                                           call_arg_base,
                                           string_temp_base,
                                           string_temp_stride,
                                           string_label_index_io,
                                           control_label_index_io,
                                           out,
                                           cap,
                                           &handled)) {
            free(args);
            return false;
        }
        if (handled) {
            if (strcmp(callee, "panic") == 0 || strcmp(callee, "system.panic") == 0) {
                *terminated_out = true;
            }
            free(args);
            return true;
        }
        if (!v3_parse_call_text(statement, callee, sizeof(callee), args, &arg_count, CHENG_V3_MAX_CALL_ARGS)) {
            free(args);
            return false;
        }
        {
            bool ok = v3_codegen_call_scalar(plan,
                                             lowering,
                                             function,
                                             aliases,
                                             alias_count,
                                             locals,
                                             local_count,
                                             callee,
                                             args,
                                             arg_count,
                                             "void",
                                             9,
                                             call_arg_base,
                                             string_temp_base,
                                             string_temp_stride,
                                             string_label_index_io,
                                             0,
                                             out,
                                             cap);
            free(args);
            return ok;
        }
    }
    {
        char expr_type[256];
        char expr_abi[32];
        if (v3_infer_expr_type(plan,
                               lowering,
                               function,
                               aliases,
                               alias_count,
                               locals,
                               local_count,
                               statement,
                               expr_type,
                               sizeof(expr_type),
                               expr_abi,
                               sizeof(expr_abi)) &&
            v3_abi_class_scalar_or_ptr(expr_abi)) {
            return v3_codegen_expr_scalar(plan,
                                          lowering,
                                          function,
                                          aliases,
                                          alias_count,
                                          locals,
                                          local_count,
                                          statement,
                                          expr_abi,
                                          9,
                                          call_arg_base,
                                          string_temp_base,
                                          string_temp_stride,
                                          0,
                                          out,
                                          cap);
        }
    }
    return false;
}

static bool v3_emit_if_statement(const V3SystemLinkPlanStub *plan,
                                 const V3LoweringPlanStub *lowering,
                                 const V3LoweredFunctionStub *function,
                                 const V3ImportAlias *aliases,
                                 size_t alias_count,
                                 const V3AsmLocalSlot *locals,
                                 size_t local_count,
                                 char **lines,
                                 size_t line_count,
                                 size_t *index_io,
                                 int32_t parent_indent,
                                 const char *statement,
                                 int32_t call_arg_base,
                                 int32_t string_temp_base,
                                 int32_t string_temp_stride,
                                 int32_t *string_label_index_io,
                                 int32_t *control_label_index_io,
                                 int32_t frame_size,
                                 int32_t sret_local_index,
                                 char *out,
                                 size_t cap) {
    char current_stmt[8192];
    char end_label[128];
    int32_t chain_id = v3_next_expr_label_id();
    snprintf(end_label, sizeof(end_label), "L_v3_if_end_%d", chain_id);
    snprintf(current_stmt, sizeof(current_stmt), "%s", statement);
    for (;;) {
        char cond[4096];
        char inline_stmt[8192];
        char next_clause[8192];
        char next_label[128];
        bool is_else = false;
        bool has_next_clause = false;
        bool branch_terminated = false;
        bool arm_has_cond = false;
        if (v3_startswith(current_stmt, "if ")) {
            arm_has_cond = true;
            if (!v3_parse_if_header(current_stmt, cond, sizeof(cond), inline_stmt, sizeof(inline_stmt))) {
                fprintf(stderr,
                        "[cheng_v3_seed] emit if parse header failed function=%s stmt=%s\n",
                        function->symbol_text,
                        current_stmt);
                return false;
            }
        } else if (v3_startswith(current_stmt, "elif ")) {
            arm_has_cond = true;
            if (!v3_parse_elif_header(current_stmt, cond, sizeof(cond), inline_stmt, sizeof(inline_stmt))) {
                fprintf(stderr,
                        "[cheng_v3_seed] emit elif parse header failed function=%s stmt=%s\n",
                        function->symbol_text,
                        current_stmt);
                return false;
            }
        } else {
            is_else = true;
            if (!v3_parse_else_header(current_stmt, inline_stmt, sizeof(inline_stmt))) {
                fprintf(stderr,
                        "[cheng_v3_seed] emit else parse header failed function=%s stmt=%s\n",
                        function->symbol_text,
                        current_stmt);
                return false;
            }
            cond[0] = '\0';
        }
        if (arm_has_cond) {
            int32_t next_id = v3_next_expr_label_id();
            snprintf(next_label, sizeof(next_label), "L_v3_if_next_%d", next_id);
            if (!v3_codegen_expr_scalar(plan,
                                        lowering,
                                        function,
                                        aliases,
                                        alias_count,
                                        locals,
                                        local_count,
                                        cond,
                                        "bool",
                                        9,
                                        call_arg_base,
                                        string_temp_base,
                                        string_temp_stride,
                                        0,
                                        out,
                                        cap)) {
                fprintf(stderr,
                        "[cheng_v3_seed] emit if cond failed function=%s cond=%s\n",
                        function->symbol_text,
                        cond);
                return false;
            }
            v3_emit_branch_zero(out, cap, 9, false, next_label);
        } else {
            next_label[0] = '\0';
        }
        if (inline_stmt[0] != '\0') {
            if (v3_startswith(inline_stmt, "if ") ||
                v3_startswith(inline_stmt, "while ") ||
                v3_startswith(inline_stmt, "for ") ||
                v3_startswith(inline_stmt, "case ")) {
                fprintf(stderr,
                        "[cheng_v3_seed] emit if inline compound reject function=%s inline=%s\n",
                        function->symbol_text,
                        inline_stmt);
                return false;
            }
            if (!v3_emit_non_if_statement(plan,
                                          lowering,
                                          function,
                                          aliases,
                                          alias_count,
                                          locals,
                                          local_count,
                                          inline_stmt,
                                          call_arg_base,
                                          string_temp_base,
                                          string_temp_stride,
                                          string_label_index_io,
                                          control_label_index_io,
                                          frame_size,
                                          sret_local_index,
                                          out,
                                          cap,
                                          &branch_terminated)) {
                fprintf(stderr,
                        "[cheng_v3_seed] emit if inline stmt failed function=%s inline=%s\n",
                        function->symbol_text,
                        inline_stmt);
                return false;
            }
        } else {
            while (*index_io < line_count) {
                char raw_copy[4096];
                char *trimmed;
                int32_t indent;
                char nested_stmt[8192];
                snprintf(raw_copy, sizeof(raw_copy), "%s", lines[*index_io]);
                trimmed = v3_trim_inplace(raw_copy);
                if (*trimmed == '\0') {
                    *index_io += 1U;
                    continue;
                }
                indent = v3_line_indent(lines[*index_io]);
                if (indent < parent_indent) {
                    break;
                }
                if (indent == parent_indent &&
                    (v3_startswith(trimmed, "elif ") || v3_startswith(trimmed, "else"))) {
                    if (!v3_collect_statement_from_lines(lines,
                                                         line_count,
                                                         index_io,
                                                         next_clause,
                                                         sizeof(next_clause))) {
                        fprintf(stderr,
                                "[cheng_v3_seed] emit if collect next clause failed function=%s stmt=%s\n",
                                function->symbol_text,
                                current_stmt);
                        return false;
                    }
                    has_next_clause = true;
                    break;
                }
                if (indent <= parent_indent) {
                    break;
                }
                if (branch_terminated) {
                    *index_io += 1U;
                    continue;
                }
                if (!v3_collect_statement_from_lines(lines, line_count, index_io, nested_stmt, sizeof(nested_stmt))) {
                    fprintf(stderr,
                            "[cheng_v3_seed] emit if collect nested stmt failed function=%s parent=%s\n",
                            function->symbol_text,
                            current_stmt);
                    return false;
                }
                if (v3_startswith(nested_stmt, "if ")) {
                    if (!v3_emit_if_statement(plan,
                                              lowering,
                                              function,
                                              aliases,
                                              alias_count,
                                              locals,
                                              local_count,
                                              lines,
                                              line_count,
                                              index_io,
                                              indent,
                                              nested_stmt,
                                              call_arg_base,
                                              string_temp_base,
                                              string_temp_stride,
                                              string_label_index_io,
                                              control_label_index_io,
                                              frame_size,
                                              sret_local_index,
                                              out,
                                              cap)) {
                        fprintf(stderr,
                                "[cheng_v3_seed] emit nested if failed function=%s parent=%s nested=%s\n",
                                function->symbol_text,
                                current_stmt,
                                nested_stmt);
                        return false;
                    }
                    continue;
                }
                if (v3_startswith(nested_stmt, "while ")) {
                    if (!v3_emit_while_statement(plan,
                                                 lowering,
                                                 function,
                                                 aliases,
                                                 alias_count,
                                                 locals,
                                                 local_count,
                                                 lines,
                                                 line_count,
                                                 index_io,
                                                 indent,
                                                 nested_stmt,
                                                 call_arg_base,
                                                 string_temp_base,
                                                 string_temp_stride,
                                                 string_label_index_io,
                                                 control_label_index_io,
                                                 frame_size,
                                                 sret_local_index,
                                                 out,
                                                 cap)) {
                        fprintf(stderr,
                                "[cheng_v3_seed] emit nested while failed function=%s parent=%s nested=%s\n",
                                function->symbol_text,
                                current_stmt,
                                nested_stmt);
                        return false;
                    }
                    continue;
                }
                if (v3_startswith(nested_stmt, "for ")) {
                    if (!v3_emit_for_statement(plan,
                                               lowering,
                                               function,
                                               aliases,
                                               alias_count,
                                               locals,
                                               local_count,
                                               lines,
                                               line_count,
                                               index_io,
                                               indent,
                                               nested_stmt,
                                               call_arg_base,
                                               string_temp_base,
                                               string_temp_stride,
                                               string_label_index_io,
                                               control_label_index_io,
                                               frame_size,
                                               sret_local_index,
                                               out,
                                               cap)) {
                        fprintf(stderr,
                                "[cheng_v3_seed] emit nested for failed function=%s parent=%s nested=%s\n",
                                function->symbol_text,
                                current_stmt,
                                nested_stmt);
                        return false;
                    }
                    continue;
                }
                if (!v3_emit_non_if_statement(plan,
                                              lowering,
                                              function,
                                              aliases,
                                              alias_count,
                                              locals,
                                              local_count,
                                              nested_stmt,
                                              call_arg_base,
                                              string_temp_base,
                                              string_temp_stride,
                                              string_label_index_io,
                                              control_label_index_io,
                                              frame_size,
                                              sret_local_index,
                                              out,
                                              cap,
                                              &branch_terminated)) {
                    fprintf(stderr,
                            "[cheng_v3_seed] emit if nested stmt failed function=%s parent=%s nested=%s\n",
                            function->symbol_text,
                            current_stmt,
                            nested_stmt);
                    return false;
                }
            }
        }
        if (!is_else && !branch_terminated) {
            v3_emit_jump_label(out, cap, end_label);
        }
        if (!is_else) {
            v3_text_appendf(out, cap, "%s:\n", next_label);
        }
        if (!has_next_clause) {
            break;
        }
        snprintf(current_stmt, sizeof(current_stmt), "%s", next_clause);
    }
    v3_text_appendf(out, cap, "%s:\n", end_label);
    return true;
}

static bool v3_emit_while_statement(const V3SystemLinkPlanStub *plan,
                                    const V3LoweringPlanStub *lowering,
                                    const V3LoweredFunctionStub *function,
                                    const V3ImportAlias *aliases,
                                    size_t alias_count,
                                    const V3AsmLocalSlot *locals,
                                    size_t local_count,
                                    char **lines,
                                    size_t line_count,
                                    size_t *index_io,
                                    int32_t parent_indent,
                                    const char *statement,
                                    int32_t call_arg_base,
                                    int32_t string_temp_base,
                                    int32_t string_temp_stride,
                                    int32_t *string_label_index_io,
                                    int32_t *control_label_index_io,
                                    int32_t frame_size,
                                    int32_t sret_local_index,
                                    char *out,
                                    size_t cap) {
    char cond[4096];
    char inline_stmt[8192];
    char start_label[128];
    char end_label[128];
    bool branch_terminated = false;
    int32_t label_id = v3_next_expr_label_id();
    if (!v3_parse_while_header(statement, cond, sizeof(cond), inline_stmt, sizeof(inline_stmt))) {
        return false;
    }
    snprintf(start_label, sizeof(start_label), "L_v3_while_start_%d", label_id);
    snprintf(end_label, sizeof(end_label), "L_v3_while_end_%d", label_id);
    if (!v3_push_loop_labels(start_label, end_label)) {
        return false;
    }
    v3_text_appendf(out, cap, "%s:\n", start_label);
    if (!v3_codegen_expr_scalar(plan,
                                lowering,
                                function,
                                aliases,
                                alias_count,
                                locals,
                                local_count,
                                cond,
                                "bool",
                                9,
                                call_arg_base,
                                string_temp_base,
                                string_temp_stride,
                                0,
                                out,
                                cap)) {
        return false;
    }
    v3_emit_branch_zero(out, cap, 9, false, end_label);
    if (inline_stmt[0] != '\0') {
        if (v3_startswith(inline_stmt, "if ") ||
            v3_startswith(inline_stmt, "while ") ||
            v3_startswith(inline_stmt, "for ")) {
            return false;
        }
        if (!v3_emit_non_if_statement(plan,
                                      lowering,
                                      function,
                                      aliases,
                                      alias_count,
                                      locals,
                                      local_count,
                                      inline_stmt,
                                      call_arg_base,
                                      string_temp_base,
                                      string_temp_stride,
                                      string_label_index_io,
                                      control_label_index_io,
                                      frame_size,
                                      sret_local_index,
                                      out,
                                      cap,
                                      &branch_terminated)) {
            v3_pop_loop_labels();
            return false;
        }
        v3_emit_jump_label(out, cap, start_label);
        v3_text_appendf(out, cap, "%s:\n", end_label);
        v3_pop_loop_labels();
        return true;
    }
    while (*index_io < line_count) {
        char raw_copy[4096];
        char *trimmed;
        int32_t indent;
        char nested_stmt[8192];
        snprintf(raw_copy, sizeof(raw_copy), "%s", lines[*index_io]);
        trimmed = v3_trim_inplace(raw_copy);
        if (*trimmed == '\0') {
            *index_io += 1U;
            continue;
        }
        indent = v3_line_indent(lines[*index_io]);
        if (indent <= parent_indent) {
            break;
        }
        if (branch_terminated) {
            *index_io += 1U;
            continue;
        }
        if (!v3_collect_statement_from_lines(lines, line_count, index_io, nested_stmt, sizeof(nested_stmt))) {
            v3_pop_loop_labels();
            return false;
        }
        if (v3_startswith(nested_stmt, "if ")) {
            if (!v3_emit_if_statement(plan,
                                      lowering,
                                      function,
                                      aliases,
                                      alias_count,
                                      locals,
                                      local_count,
                                      lines,
                                      line_count,
                                      index_io,
                                      indent,
                                      nested_stmt,
                                      call_arg_base,
                                      string_temp_base,
                                      string_temp_stride,
                                      string_label_index_io,
                                      control_label_index_io,
                                      frame_size,
                                      sret_local_index,
                                      out,
                                      cap)) {
                v3_pop_loop_labels();
                return false;
            }
            continue;
        }
        if (v3_startswith(nested_stmt, "while ")) {
            if (!v3_emit_while_statement(plan,
                                         lowering,
                                         function,
                                         aliases,
                                         alias_count,
                                         locals,
                                         local_count,
                                         lines,
                                         line_count,
                                         index_io,
                                         indent,
                                         nested_stmt,
                                         call_arg_base,
                                         string_temp_base,
                                         string_temp_stride,
                                         string_label_index_io,
                                         control_label_index_io,
                                         frame_size,
                                         sret_local_index,
                                         out,
                                         cap)) {
                v3_pop_loop_labels();
                return false;
            }
            continue;
        }
        if (v3_startswith(nested_stmt, "for ")) {
            if (!v3_emit_for_statement(plan,
                                       lowering,
                                       function,
                                       aliases,
                                       alias_count,
                                       locals,
                                       local_count,
                                       lines,
                                       line_count,
                                       index_io,
                                       indent,
                                       nested_stmt,
                                       call_arg_base,
                                       string_temp_base,
                                       string_temp_stride,
                                       string_label_index_io,
                                       control_label_index_io,
                                       frame_size,
                                       sret_local_index,
                                       out,
                                       cap)) {
                v3_pop_loop_labels();
                return false;
            }
            continue;
        }
        if (!v3_emit_non_if_statement(plan,
                                      lowering,
                                      function,
                                      aliases,
                                      alias_count,
                                      locals,
                                      local_count,
                                      nested_stmt,
                                      call_arg_base,
                                      string_temp_base,
                                      string_temp_stride,
                                      string_label_index_io,
                                      control_label_index_io,
                                      frame_size,
                                      sret_local_index,
                                      out,
                                      cap,
                                      &branch_terminated)) {
            v3_pop_loop_labels();
            return false;
        }
    }
    v3_emit_jump_label(out, cap, start_label);
    v3_text_appendf(out, cap, "%s:\n", end_label);
    v3_pop_loop_labels();
    return true;
}

static bool v3_emit_case_const_table_statement(const V3SystemLinkPlanStub *plan,
                                               const V3LoweringPlanStub *lowering,
                                               const V3LoweredFunctionStub *function,
                                               const V3ImportAlias *aliases,
                                               size_t alias_count,
                                               const V3AsmLocalSlot *locals,
                                               size_t local_count,
                                               char **lines,
                                               size_t line_count,
                                               size_t *index_io,
                                               int32_t parent_indent,
                                               const char *statement,
                                               int32_t call_arg_base,
                                               int32_t string_temp_base,
                                               int32_t string_temp_stride,
                                               int32_t *string_label_index_io,
                                               int32_t *control_label_index_io,
                                               int32_t frame_size,
                                               int32_t sret_local_index,
                                               bool *terminated_out,
                                               char *out,
                                               size_t cap) {
    char case_expr[4096];
    char end_label[128];
    bool saw_else = false;
    bool all_arms_terminated = true;
    int32_t end_id = v3_next_expr_label_id();
    if (!v3_parse_case_header(statement, case_expr, sizeof(case_expr)) ||
        !v3_codegen_expr_scalar(plan,
                                lowering,
                                function,
                                aliases,
                                alias_count,
                                locals,
                                local_count,
                                case_expr,
                                "i64",
                                9,
                                call_arg_base,
                                string_temp_base,
                                string_temp_stride,
                                0,
                                out,
                                cap)) {
        fprintf(stderr,
                "[cheng_v3_seed] emit case selector failed function=%s stmt=%s expr=%s\n",
                function->symbol_text,
                statement,
                case_expr);
        return false;
    }
    snprintf(end_label, sizeof(end_label), "L_v3_case_end_%d", end_id);
    while (*index_io < line_count) {
        char raw_copy[4096];
        char *trimmed;
        int32_t indent;
        char arm_stmt[8192];
        char inline_stmt[8192];
        char next_label[128];
        bool is_else = false;
        bool branch_terminated = false;
        int64_t match_value = 0;
        int32_t next_id = v3_next_expr_label_id();
        snprintf(raw_copy, sizeof(raw_copy), "%s", lines[*index_io]);
        trimmed = v3_trim_inplace(raw_copy);
        if (*trimmed == '\0') {
            *index_io += 1U;
            continue;
        }
        indent = v3_line_indent(lines[*index_io]);
        if (indent < parent_indent) {
            break;
        }
        if (indent == parent_indent &&
            !v3_startswith(trimmed, "of ") &&
            !v3_startswith(trimmed, "else")) {
            break;
        }
        if (!v3_collect_statement_from_lines(lines, line_count, index_io, arm_stmt, sizeof(arm_stmt)) ||
            !v3_parse_case_arm_header(arm_stmt,
                                      &is_else,
                                      &match_value,
                                      inline_stmt,
                                      sizeof(inline_stmt))) {
            fprintf(stderr,
                    "[cheng_v3_seed] emit case arm parse failed function=%s stmt=%s\n",
                    function->symbol_text,
                    statement);
            return false;
        }
        if (!is_else) {
            snprintf(next_label, sizeof(next_label), "L_v3_case_next_%d", next_id);
            if (!v3_emit_mov_imm(out, cap, 10, match_value)) {
                return false;
            }
            if (v3_active_target_is_linux_x86_64()) {
                v3_text_appendf(out, cap,
                                "  cmpq %s, %s\n"
                                "  jne %s\n",
                                v3_x64_qreg_name(10),
                                v3_x64_qreg_name(9),
                                next_label);
            } else {
                v3_text_appendf(out, cap,
                                "  cmp x9, x10\n"
                                "  b.ne %s\n",
                                next_label);
            }
        } else {
            saw_else = true;
            next_label[0] = '\0';
        }
        if (!v3_emit_non_if_statement(plan,
                                      lowering,
                                      function,
                                      aliases,
                                      alias_count,
                                      locals,
                                      local_count,
                                      inline_stmt,
                                      call_arg_base,
                                      string_temp_base,
                                      string_temp_stride,
                                      string_label_index_io,
                                      control_label_index_io,
                                      frame_size,
                                      sret_local_index,
                                      out,
                                      cap,
                                      &branch_terminated)) {
            fprintf(stderr,
                    "[cheng_v3_seed] emit case arm body failed function=%s arm=%s inline=%s\n",
                    function->symbol_text,
                    arm_stmt,
                    inline_stmt);
            return false;
        }
        if (!branch_terminated) {
            all_arms_terminated = false;
        }
        if (!branch_terminated) {
            v3_text_appendf(out, cap,
                            v3_active_target_is_linux_x86_64() ? "  jmp %s\n" : "  b %s\n",
                            end_label);
        }
        if (!is_else) {
            v3_text_appendf(out, cap, "%s:\n", next_label);
        }
    }
    if (!saw_else) {
        fprintf(stderr,
                "[cheng_v3_seed] emit case missing else function=%s stmt=%s\n",
                function->symbol_text,
                statement);
        return false;
    }
    v3_text_appendf(out, cap, "%s:\n", end_label);
    if (terminated_out) {
        *terminated_out = saw_else && all_arms_terminated;
    }
    return true;
}

static bool v3_emit_for_statement(const V3SystemLinkPlanStub *plan,
                                  const V3LoweringPlanStub *lowering,
                                  const V3LoweredFunctionStub *function,
                                  const V3ImportAlias *aliases,
                                  size_t alias_count,
                                  const V3AsmLocalSlot *locals,
                                  size_t local_count,
                                  char **lines,
                                  size_t line_count,
                                  size_t *index_io,
                                  int32_t parent_indent,
                                  const char *statement,
                                  int32_t call_arg_base,
                                  int32_t string_temp_base,
                                  int32_t string_temp_stride,
                                  int32_t *string_label_index_io,
                                  int32_t *control_label_index_io,
                                  int32_t frame_size,
                                  int32_t sret_local_index,
                                  char *out,
                                  size_t cap) {
    char iter_name[128];
    char start_expr[4096];
    char end_expr[4096];
    bool end_inclusive = false;
    char inline_stmt[8192];
    char start_label[128];
    char continue_label[128];
    char end_label[128];
    int32_t iter_local_index;
    bool branch_terminated = false;
    int32_t label_id = v3_next_expr_label_id();
    if (!v3_parse_for_header(statement,
                             iter_name,
                             sizeof(iter_name),
                             start_expr,
                             sizeof(start_expr),
                             end_expr,
                             sizeof(end_expr),
                             &end_inclusive,
                             inline_stmt,
                             sizeof(inline_stmt))) {
        fprintf(stderr,
                "[cheng_v3_seed] emit for parse header failed function=%s stmt=%s\n",
                function->symbol_text,
                statement);
        return false;
    }
    iter_local_index = v3_find_local_slot(locals, local_count, iter_name);
    if (iter_local_index < 0) {
        return false;
    }
    if (!v3_codegen_expr_scalar(plan,
                                lowering,
                                function,
                                aliases,
                                alias_count,
                                locals,
                                local_count,
                                start_expr,
                                "i32",
                                9,
                                call_arg_base,
                                string_temp_base,
                                string_temp_stride,
                                0,
                                out,
                                cap)) {
        fprintf(stderr,
                "[cheng_v3_seed] emit for start expr failed function=%s stmt=%s start=%s\n",
                function->symbol_text,
                statement,
                start_expr);
        return false;
    }
    v3_emit_store_slot(out, cap, &locals[iter_local_index], 9);
    snprintf(start_label, sizeof(start_label), "L_v3_for_start_%d", label_id);
    snprintf(continue_label, sizeof(continue_label), "L_v3_for_continue_%d", label_id);
    snprintf(end_label, sizeof(end_label), "L_v3_for_end_%d", label_id);
    if (!v3_push_loop_labels(continue_label, end_label)) {
        return false;
    }
    v3_text_appendf(out, cap, "%s:\n", start_label);
    v3_emit_load_slot(out, cap, &locals[iter_local_index], 9);
    if (!v3_codegen_expr_scalar(plan,
                                lowering,
                                function,
                                aliases,
                                alias_count,
                                locals,
                                local_count,
                                end_expr,
                                "i32",
                                10,
                                call_arg_base,
                                string_temp_base,
                                string_temp_stride,
                                0,
                                out,
                                cap)) {
        fprintf(stderr,
                "[cheng_v3_seed] emit for end expr failed function=%s stmt=%s end=%s\n",
                function->symbol_text,
                statement,
                end_expr);
        return false;
    }
    if (end_inclusive) {
        if (v3_active_target_is_linux_x86_64()) {
            v3_text_appendf(out, cap,
                            "  cmpq %s, %s\n"
                            "  jg %s\n",
                            v3_x64_qreg_name(10),
                            v3_x64_qreg_name(9),
                            end_label);
        } else {
            v3_text_appendf(out, cap,
                            "  cmp x9, x10\n"
                            "  b.gt %s\n",
                            end_label);
        }
    } else {
        if (v3_active_target_is_linux_x86_64()) {
            v3_text_appendf(out, cap,
                            "  cmpq %s, %s\n"
                            "  jge %s\n",
                            v3_x64_qreg_name(10),
                            v3_x64_qreg_name(9),
                            end_label);
        } else {
            v3_text_appendf(out, cap,
                            "  cmp x9, x10\n"
                            "  b.ge %s\n",
                            end_label);
        }
    }
    if (inline_stmt[0] != '\0') {
        if (v3_startswith(inline_stmt, "if ") ||
            v3_startswith(inline_stmt, "while ") ||
            v3_startswith(inline_stmt, "for ")) {
            fprintf(stderr,
                    "[cheng_v3_seed] emit for inline compound reject function=%s inline=%s\n",
                    function->symbol_text,
                    inline_stmt);
            return false;
        }
        if (!v3_emit_non_if_statement(plan,
                                      lowering,
                                      function,
                                      aliases,
                                      alias_count,
                                      locals,
                                      local_count,
                                      inline_stmt,
                                      call_arg_base,
                                      string_temp_base,
                                      string_temp_stride,
                                      string_label_index_io,
                                      control_label_index_io,
                                      frame_size,
                                      sret_local_index,
                                      out,
                                      cap,
                                      &branch_terminated)) {
            fprintf(stderr,
                    "[cheng_v3_seed] emit for inline stmt failed function=%s inline=%s\n",
                    function->symbol_text,
                    inline_stmt);
            v3_pop_loop_labels();
            return false;
        }
        v3_text_appendf(out, cap, "%s:\n", continue_label);
        v3_emit_load_slot(out, cap, &locals[iter_local_index], 9);
        if (v3_active_target_is_linux_x86_64()) {
            v3_text_appendf(out, cap, "  addq $1, %s\n", v3_x64_qreg_name(9));
        } else {
            v3_text_appendf(out, cap, "  add x9, x9, #1\n");
        }
        v3_emit_store_slot(out, cap, &locals[iter_local_index], 9);
        v3_emit_jump_label(out, cap, start_label);
        v3_text_appendf(out, cap, "%s:\n", end_label);
        v3_pop_loop_labels();
        return true;
    }
    while (*index_io < line_count) {
        char raw_copy[4096];
        char *trimmed;
        int32_t indent;
        char nested_stmt[8192];
        snprintf(raw_copy, sizeof(raw_copy), "%s", lines[*index_io]);
        trimmed = v3_trim_inplace(raw_copy);
        if (*trimmed == '\0') {
            *index_io += 1U;
            continue;
        }
        indent = v3_line_indent(lines[*index_io]);
        if (indent <= parent_indent) {
            break;
        }
        if (branch_terminated) {
            *index_io += 1U;
            continue;
        }
        if (!v3_collect_statement_from_lines(lines, line_count, index_io, nested_stmt, sizeof(nested_stmt))) {
            fprintf(stderr,
                    "[cheng_v3_seed] emit for collect nested stmt failed function=%s parent=%s\n",
                    function->symbol_text,
                    statement);
            v3_pop_loop_labels();
            return false;
        }
        if (v3_startswith(nested_stmt, "if ")) {
            if (!v3_emit_if_statement(plan,
                                      lowering,
                                      function,
                                      aliases,
                                      alias_count,
                                      locals,
                                      local_count,
                                      lines,
                                      line_count,
                                      index_io,
                                      indent,
                                      nested_stmt,
                                      call_arg_base,
                                      string_temp_base,
                                      string_temp_stride,
                                      string_label_index_io,
                                      control_label_index_io,
                                      frame_size,
                                      sret_local_index,
                                      out,
                                      cap)) {
                fprintf(stderr,
                        "[cheng_v3_seed] emit for nested if failed function=%s parent=%s nested=%s\n",
                        function->symbol_text,
                        statement,
                        nested_stmt);
                v3_pop_loop_labels();
                return false;
            }
            continue;
        }
        if (v3_startswith(nested_stmt, "while ")) {
            if (!v3_emit_while_statement(plan,
                                         lowering,
                                         function,
                                         aliases,
                                         alias_count,
                                         locals,
                                         local_count,
                                         lines,
                                         line_count,
                                         index_io,
                                         indent,
                                         nested_stmt,
                                         call_arg_base,
                                         string_temp_base,
                                         string_temp_stride,
                                         string_label_index_io,
                                         control_label_index_io,
                                         frame_size,
                                         sret_local_index,
                                         out,
                                         cap)) {
                fprintf(stderr,
                        "[cheng_v3_seed] emit for nested while failed function=%s parent=%s nested=%s\n",
                        function->symbol_text,
                        statement,
                        nested_stmt);
                v3_pop_loop_labels();
                return false;
            }
            continue;
        }
        if (v3_startswith(nested_stmt, "for ")) {
            if (!v3_emit_for_statement(plan,
                                       lowering,
                                       function,
                                       aliases,
                                       alias_count,
                                       locals,
                                       local_count,
                                       lines,
                                       line_count,
                                       index_io,
                                       indent,
                                       nested_stmt,
                                       call_arg_base,
                                       string_temp_base,
                                       string_temp_stride,
                                       string_label_index_io,
                                       control_label_index_io,
                                       frame_size,
                                       sret_local_index,
                                       out,
                                       cap)) {
                fprintf(stderr,
                        "[cheng_v3_seed] emit for nested for failed function=%s parent=%s nested=%s\n",
                        function->symbol_text,
                        statement,
                        nested_stmt);
                v3_pop_loop_labels();
                return false;
            }
            continue;
        }
        if (!v3_emit_non_if_statement(plan,
                                      lowering,
                                      function,
                                      aliases,
                                      alias_count,
                                      locals,
                                      local_count,
                                      nested_stmt,
                                      call_arg_base,
                                      string_temp_base,
                                      string_temp_stride,
                                      string_label_index_io,
                                      control_label_index_io,
                                      frame_size,
                                      sret_local_index,
                                      out,
                                      cap,
                                      &branch_terminated)) {
            fprintf(stderr,
                    "[cheng_v3_seed] emit for nested stmt failed function=%s parent=%s nested=%s\n",
                    function->symbol_text,
                    statement,
                    nested_stmt);
            v3_pop_loop_labels();
            return false;
        }
    }
    v3_text_appendf(out, cap, "%s:\n", continue_label);
    v3_emit_load_slot(out, cap, &locals[iter_local_index], 9);
    if (v3_active_target_is_linux_x86_64()) {
        v3_text_appendf(out, cap, "  addq $1, %s\n", v3_x64_qreg_name(9));
    } else {
        v3_text_appendf(out, cap, "  add x9, x9, #1\n");
    }
    v3_emit_store_slot(out, cap, &locals[iter_local_index], 9);
    v3_emit_jump_label(out, cap, start_label);
    v3_text_appendf(out, cap, "%s:\n", end_label);
    v3_pop_loop_labels();
    return true;
}

static bool v3_try_emit_scalar_function(const V3SystemLinkPlanStub *plan,
                                        const V3LoweringPlanStub *lowering,
                                        const V3LoweredFunctionStub *function,
                                        char *out,
                                        size_t cap,
                                        size_t *instruction_words_out) {
    const char *trace_emit_scalar = getenv("CHENG_V3_TRACE_EMIT_SCALAR");
    char **lines = NULL;
    char *owned_lines = NULL;
    size_t line_count = 0U;
    size_t fn_line_index = 0U;
    char param_names[CHENG_V3_CONST_MAX_BINDINGS][128];
    size_t param_count = 0U;
    char owned_path[PATH_MAX];
    V3ImportAlias aliases[CHENG_V3_MAX_IMPORT_ALIASES];
    size_t alias_count = 0U;
    V3AsmLocalSlot locals[CHENG_V3_MAX_ASM_LOCALS];
    size_t local_count = 0U;
    int32_t next_offset = 0;
    int32_t max_call_depth = 0;
    int32_t call_arg_base = 0;
    int32_t max_string_literal_temps = 0;
    int32_t string_temp_base = 0;
    int32_t string_temp_stride = 0;
    int32_t string_label_index = 0;
    int32_t control_label_index = 0;
    int32_t sret_local_index = -1;
    int32_t frame_size;
    char effective_return_type[CHENG_V3_MAX_TYPE_TEXT];
    char effective_return_abi[32];
    char effective_param_types[CHENG_V3_MAX_CALL_ARGS][CHENG_V3_MAX_TYPE_TEXT];
    char effective_param_abis[CHENG_V3_MAX_CALL_ARGS][32];
    size_t i;
    bool saw_return = false;
    bool composite_return = false;
    bool void_return = false;
    bool has_inline_signature_body = false;
    char inline_signature_body[8192];
    inline_signature_body[0] = '\0';
    if (trace_emit_scalar &&
        trace_emit_scalar[0] != '\0' &&
        strcmp(trace_emit_scalar, "0") != 0) {
        fprintf(stderr,
                "[cheng_v3_seed] emit scalar function=%s source=%s\n",
                function->symbol_text,
                function->source_path);
    }
    if (!v3_load_function_source_lines(function,
                                       &lines,
                                       &owned_lines,
                                       &line_count,
                                       &fn_line_index,
                                       param_names,
                                       &param_count,
                                       owned_path)) {
        return false;
    }
    v3_reset_loop_labels();
    v3_parse_import_aliases_from_lines(lines, line_count, aliases, &alias_count, CHENG_V3_MAX_IMPORT_ALIASES);
    if (!v3_normalize_type_text_for_function(lowering,
                                             function,
                                             aliases,
                                             alias_count,
                                             function->return_type,
                                             effective_return_type,
                                             sizeof(effective_return_type))) {
        free(lines);
        free(owned_lines);
        return false;
    }
    snprintf(effective_return_abi, sizeof(effective_return_abi), "%s", v3_type_abi_class(effective_return_type));
    composite_return = strcmp(effective_return_abi, "composite") == 0;
    void_return = strcmp(effective_return_abi, "void") == 0;
    {
        char signature_copy[8192];
        char *eq;
        snprintf(signature_copy, sizeof(signature_copy), "%s", lines[fn_line_index]);
        eq = strrchr(signature_copy, '=');
        if (eq) {
            v3_trim_copy_text(eq + 1, inline_signature_body, sizeof(inline_signature_body));
            has_inline_signature_body = inline_signature_body[0] != '\0';
        }
    }
    memset(locals, 0, sizeof(locals));
    memset(effective_param_types, 0, sizeof(effective_param_types));
    memset(effective_param_abis, 0, sizeof(effective_param_abis));
    for (i = 0; i < function->param_count; ++i) {
        char effective_param_type[CHENG_V3_MAX_TYPE_TEXT];
        char effective_param_abi[32];
        if (!v3_normalize_type_text_for_function(lowering,
                                                 function,
                                                 aliases,
                                                 alias_count,
                                                 function->param_types[i],
                                                 effective_param_type,
                                                 sizeof(effective_param_type))) {
            free(lines);
            free(owned_lines);
            return false;
        }
        snprintf(effective_param_abi, sizeof(effective_param_abi), "%s", v3_type_abi_class(effective_param_type));
        snprintf(effective_param_types[i], sizeof(effective_param_types[i]), "%s", effective_param_type);
        snprintf(effective_param_abis[i], sizeof(effective_param_abis[i]), "%s", effective_param_abi);
        if (!v3_add_local_slot_for_type(lowering,
                                        function->owner_module_path,
                                        aliases,
                                        alias_count,
                                        locals,
                                        &local_count,
                                        CHENG_V3_MAX_ASM_LOCALS,
                                        function->param_names[i],
                                        effective_param_type,
                                        effective_param_abi,
                                        function->param_is_var[i] ||
                                        !v3_abi_class_scalar_or_ptr(effective_param_abi),
                                        &next_offset)) {
            fprintf(stderr,
                    "[cheng_v3_seed] emit param slot failed function=%s param=%s type=%s abi=%s\n",
                    function->symbol_text,
                    function->param_names[i],
                    effective_param_type,
                    effective_param_abi);
            free(lines);
            free(owned_lines);
            return false;
        }
    }
    if (composite_return) {
        sret_local_index = (int32_t)local_count;
        if (!v3_add_local_slot_sized(locals,
                                     &local_count,
                                     CHENG_V3_MAX_ASM_LOCALS,
                                     "__v3_sret",
                                     "ptr",
                                     "ptr",
                                     true,
                                     8,
                                     8,
                                     &next_offset)) {
            free(lines);
            free(owned_lines);
            return false;
        }
    }
    if (has_inline_signature_body &&
        strcmp(function->body_kind, "return_expr") == 0) {
        if (v3_call_expr_string_literal_count(inline_signature_body) > max_string_literal_temps) {
            max_string_literal_temps = v3_call_expr_string_literal_count(inline_signature_body);
        }
        if (!v3_prepare_expr_call_state(plan,
                                        lowering,
                                        function,
                                        aliases,
                                        alias_count,
                                        locals,
                                        &local_count,
                                        CHENG_V3_MAX_ASM_LOCALS,
                                        &next_offset,
                                        inline_signature_body,
                                        0,
                                        &max_call_depth)) {
            fprintf(stderr,
                    "[cheng_v3_seed] prepare inline return call state failed function=%s expr=%s\n",
                    function->symbol_text,
                    inline_signature_body);
            free(lines);
            free(owned_lines);
            return false;
        }
    }
    for (i = fn_line_index + 1U; i < line_count; ) {
        char statement[8192];
        char name[128];
        char type_text[CHENG_V3_MAX_TYPE_TEXT];
        char expr[4096];
        char raw_copy[4096];
        char *trimmed;
        int32_t statement_indent;
        snprintf(raw_copy, sizeof(raw_copy), "%s", lines[i]);
        trimmed = v3_trim_inplace(raw_copy);
        if (*trimmed == '\0') {
            i += 1U;
            continue;
        }
        statement_indent = v3_line_indent(lines[i]);
        if (statement_indent <= 0 && v3_is_top_level_decl_start(trimmed)) {
            break;
        }
        if (!v3_collect_statement_from_lines(lines, line_count, &i, statement, sizeof(statement))) {
            free(lines);
            free(owned_lines);
            return false;
        }
        if (statement_indent <= 0 && v3_is_top_level_decl_start(statement)) {
            break;
        }
        if (v3_startswith(statement, "if ")) {
            if (!v3_prepare_if_statement_state(plan,
                                               lowering,
                                               function,
                                               aliases,
                                               alias_count,
                                               locals,
                                               &local_count,
                                               CHENG_V3_MAX_ASM_LOCALS,
                                               &next_offset,
                                               lines,
                                               line_count,
                                               &i,
                                               statement_indent,
                                               statement,
                                               &max_call_depth,
                                               &max_string_literal_temps)) {
                fprintf(stderr,
                        "[cheng_v3_seed] prepare if state failed function=%s stmt=%s\n",
                        function->symbol_text,
                        statement);
                free(lines);
                free(owned_lines);
                return false;
            }
            continue;
        }
        if (v3_startswith(statement, "while ")) {
            if (!v3_prepare_while_statement_state(plan,
                                                  lowering,
                                                  function,
                                                  aliases,
                                                  alias_count,
                                                  locals,
                                                  &local_count,
                                                  CHENG_V3_MAX_ASM_LOCALS,
                                                  &next_offset,
                                                  lines,
                                                  line_count,
                                                  &i,
                                                  statement_indent,
                                                  statement,
                                                  &max_call_depth,
                                                  &max_string_literal_temps)) {
                fprintf(stderr,
                        "[cheng_v3_seed] prepare while state failed function=%s stmt=%s\n",
                        function->symbol_text,
                        statement);
                free(lines);
                free(owned_lines);
                return false;
            }
            continue;
        }
        if (v3_startswith(statement, "for ")) {
            if (!v3_prepare_for_statement_state(plan,
                                                lowering,
                                                function,
                                                aliases,
                                                alias_count,
                                                locals,
                                                &local_count,
                                                CHENG_V3_MAX_ASM_LOCALS,
                                                &next_offset,
                                                lines,
                                                line_count,
                                                &i,
                                                statement_indent,
                                                statement,
                                                &max_call_depth,
                                                &max_string_literal_temps)) {
                fprintf(stderr,
                        "[cheng_v3_seed] prepare for state failed function=%s stmt=%s\n",
                        function->symbol_text,
                        statement);
                free(lines);
                free(owned_lines);
                return false;
            }
            continue;
        }
        if (v3_startswith(statement, "case ")) {
            if (!v3_prepare_case_const_table_statement(plan,
                                                       lowering,
                                                       function,
                                                       aliases,
                                                       alias_count,
                                                       locals,
                                                       &local_count,
                                                       CHENG_V3_MAX_ASM_LOCALS,
                                                       &next_offset,
                                                       lines,
                                                       line_count,
                                                       &i,
                                                       statement_indent,
                                                       statement,
                                                       &max_call_depth,
                                                       &max_string_literal_temps)) {
                fprintf(stderr,
                        "[cheng_v3_seed] prepare case state failed function=%s stmt=%s\n",
                        function->symbol_text,
                        statement);
                free(lines);
                free(owned_lines);
                return false;
            }
            continue;
        }
        if (v3_is_implicit_return_statement(function,
                                            lines,
                                            line_count,
                                            i,
                                            statement)) {
            if (v3_call_expr_string_literal_count(statement) > max_string_literal_temps) {
                max_string_literal_temps = v3_call_expr_string_literal_count(statement);
            }
            if (!v3_prepare_expr_call_state(plan,
                                            lowering,
                                            function,
                                            aliases,
                                            alias_count,
                                            locals,
                                            &local_count,
                                            CHENG_V3_MAX_ASM_LOCALS,
                                            &next_offset,
                                            statement,
                                            0,
                                            &max_call_depth)) {
                fprintf(stderr,
                        "[cheng_v3_seed] prepare implicit return call state failed function=%s expr=%s\n",
                        function->symbol_text,
                        statement);
                free(lines);
                free(owned_lines);
                return false;
            }
            continue;
        }
        if ((v3_startswith(statement, "let ") || v3_startswith(statement, "var ")) &&
            v3_tuple_binding_expr_omits_items(statement)) {
            fprintf(stderr,
                    "[cheng_v3_seed] tuple literal omission unsupported function=%s stmt=%s\n",
                    function->symbol_text,
                    statement);
            free(lines);
            free(owned_lines);
            return false;
        }
        if ((v3_startswith(statement, "let ") || v3_startswith(statement, "var ")) &&
            v3_parse_binding_decl_meta(statement,
                                       v3_startswith(statement, "let ") ? "let " : "var ",
                                       name,
                                       sizeof(name),
                                       type_text,
                                       sizeof(type_text))) {
            char normalized_type[256];
            const char *abi_class = v3_resolved_type_abi_class(lowering,
                                                               function->owner_module_path,
                                                               aliases,
                                                               alias_count,
                                                               type_text,
                                                               normalized_type,
                                                               sizeof(normalized_type));
            if (v3_find_local_slot(locals, local_count, name) < 0) {
                if (!v3_add_local_slot_for_type(lowering,
                                                function->owner_module_path,
                                                aliases,
                                                alias_count,
                                                locals,
                                                &local_count,
                                                CHENG_V3_MAX_ASM_LOCALS,
                                                name,
                                                type_text,
                                                abi_class,
                                                false,
                                                &next_offset)) {
                    free(lines);
                    free(owned_lines);
                    return false;
                }
            }
            continue;
        }
        if ((v3_startswith(statement, "let ") || v3_startswith(statement, "var ")) &&
            v3_parse_binding_meta(statement,
                                  v3_startswith(statement, "let ") ? "let " : "var ",
                                  name,
                                  sizeof(name),
                                  type_text,
                                  sizeof(type_text),
                                  expr,
                                  sizeof(expr))) {
            const char *abi_class;
            char inferred_type[CHENG_V3_MAX_TYPE_TEXT];
            char inferred_abi[32];
            char normalized_type[256];
            if (type_text[0] == '\0') {
                if (!v3_infer_expr_type(plan,
                                        lowering,
                                        function,
                                        aliases,
                                        alias_count,
                                        locals,
                                        local_count,
                                        expr,
                                        inferred_type,
                                        sizeof(inferred_type),
                                        inferred_abi,
                                        sizeof(inferred_abi))) {
                    fprintf(stderr,
                            "[cheng_v3_seed] prepare binding infer type failed function=%s local=%s expr=%s\n",
                            function->symbol_text,
                            name,
                            expr);
                    free(lines);
                    free(owned_lines);
                    return false;
                }
                snprintf(type_text, sizeof(type_text), "%s", inferred_type);
            }
            if (!v3_tuple_binding_expr_complete(type_text, expr)) {
                fprintf(stderr,
                        "[cheng_v3_seed] tuple literal omission unsupported function=%s local=%s type=%s expr=%s\n",
                        function->symbol_text,
                        name,
                        type_text,
                        expr);
                free(lines);
                free(owned_lines);
                return false;
            }
            abi_class = v3_resolved_type_abi_class(lowering,
                                                   function->owner_module_path,
                                                   aliases,
                                                   alias_count,
                                                   type_text,
                                                   normalized_type,
                                                   sizeof(normalized_type));
            if (type_text[0] == '\0' || strcmp(abi_class, "void") == 0) {
                fprintf(stderr,
                        "[cheng_v3_seed] prepare binding invalid type function=%s local=%s type=%s expr=%s\n",
                        function->symbol_text,
                        name,
                        type_text,
                        expr);
                free(lines);
                free(owned_lines);
                return false;
            }
            if (v3_find_local_slot(locals, local_count, name) < 0) {
                if (!v3_add_local_slot_for_type(lowering,
                                                function->owner_module_path,
                                                aliases,
                                                alias_count,
                                                locals,
                                                &local_count,
                                                CHENG_V3_MAX_ASM_LOCALS,
                                                name,
                                                type_text,
                                                abi_class,
                                                false,
                                                &next_offset)) {
                    fprintf(stderr,
                            "[cheng_v3_seed] emit local slot failed function=%s local=%s type=%s abi=%s stmt=%s\n",
                            function->symbol_text,
                            name,
                            type_text,
                            abi_class,
                            statement);
                    free(lines);
                    free(owned_lines);
                    return false;
                }
            }
            if (v3_call_expr_string_literal_count(expr) > max_string_literal_temps) {
                max_string_literal_temps = v3_call_expr_string_literal_count(expr);
            }
            if (!v3_prepare_expr_call_state(plan,
                                            lowering,
                                            function,
                                            aliases,
                                            alias_count,
                                            locals,
                                            &local_count,
                                            CHENG_V3_MAX_ASM_LOCALS,
                                            &next_offset,
                                            expr,
                                            1,
                                            &max_call_depth)) {
                fprintf(stderr,
                        "[cheng_v3_seed] prepare expr call state failed function=%s expr=%s\n",
                        function->symbol_text,
                        expr);
                free(lines);
                free(owned_lines);
                return false;
            }
            continue;
        }
        if (strcmp(statement, "return") == 0) {
            continue;
        }
        if (v3_startswith(statement, "return ")) {
            if (v3_call_expr_string_literal_count(statement + 7) > max_string_literal_temps) {
                max_string_literal_temps = v3_call_expr_string_literal_count(statement + 7);
            }
            if (!v3_prepare_expr_call_state(plan,
                                            lowering,
                                            function,
                                            aliases,
                                            alias_count,
                                            locals,
                                            &local_count,
                                            CHENG_V3_MAX_ASM_LOCALS,
                                            &next_offset,
                                            statement + 7,
                                            0,
                                            &max_call_depth)) {
                fprintf(stderr,
                        "[cheng_v3_seed] prepare return call state failed function=%s expr=%s\n",
                        function->symbol_text,
                        statement + 7);
                free(lines);
                free(owned_lines);
                return false;
            }
            continue;
        }
        if (v3_parse_assignment_meta(statement,
                                     name,
                                     sizeof(name),
                                     expr,
                                     sizeof(expr))) {
            int32_t string_literal_count = v3_assignment_string_literal_count(name, expr);
            bool handled = false;
            if (string_literal_count > max_string_literal_temps) {
                max_string_literal_temps = string_literal_count;
            }
            if (!v3_try_prepare_index_assignment_call_state(plan,
                                                            lowering,
                                                            function,
                                                            aliases,
                                                            alias_count,
                                                            locals,
                                                            &local_count,
                                                            CHENG_V3_MAX_ASM_LOCALS,
                                                            &next_offset,
                                                            name,
                                                            expr,
                                                            0,
                                                            &max_call_depth,
                                                            &handled)) {
                fprintf(stderr,
                        "[cheng_v3_seed] prepare assignment call state failed function=%s target=%s expr=%s stmt=%s\n",
                        function->symbol_text,
                        name,
                        expr,
                        statement);
                free(lines);
                free(owned_lines);
                return false;
            }
            if (handled) {
                continue;
            }
            if (!v3_prepare_expr_call_state(plan,
                                            lowering,
                                            function,
                                            aliases,
                                            alias_count,
                                            locals,
                                            &local_count,
                                            CHENG_V3_MAX_ASM_LOCALS,
                                            &next_offset,
                                            expr,
                                            0,
                                            &max_call_depth)) {
                fprintf(stderr,
                        "[cheng_v3_seed] prepare assignment call state failed function=%s target=%s expr=%s stmt=%s\n",
                        function->symbol_text,
                        name,
                        expr,
                        statement);
                free(lines);
                free(owned_lines);
                return false;
            }
            continue;
        }
        if (v3_is_noop_local_reference_statement(locals, local_count, statement)) {
            continue;
        }
        if (strchr(statement, '(') != NULL) {
            if (v3_call_expr_string_literal_count(statement) > max_string_literal_temps) {
                max_string_literal_temps = v3_call_expr_string_literal_count(statement);
            }
            if (!v3_prepare_expr_call_state(plan,
                                            lowering,
                                            function,
                                            aliases,
                                            alias_count,
                                            locals,
                                            &local_count,
                                            CHENG_V3_MAX_ASM_LOCALS,
                                            &next_offset,
                                            statement,
                                            0,
                                            &max_call_depth)) {
                fprintf(stderr,
                        "[cheng_v3_seed] prepare stmt call state failed function=%s stmt=%s\n",
                        function->symbol_text,
                        statement);
                free(lines);
                free(owned_lines);
                return false;
            }
            continue;
        }
        {
            char expr_type[256];
            char expr_abi[32];
            if (v3_infer_expr_type(plan,
                                   lowering,
                                   function,
                                   aliases,
                                   alias_count,
                                   locals,
                                   local_count,
                                   statement,
                                   expr_type,
                                   sizeof(expr_type),
                                   expr_abi,
                                   sizeof(expr_abi)) &&
                v3_abi_class_scalar_or_ptr(expr_abi)) {
                if (v3_call_expr_string_literal_count(statement) > max_string_literal_temps) {
                    max_string_literal_temps = v3_call_expr_string_literal_count(statement);
                }
                if (!v3_prepare_expr_call_state(plan,
                                                lowering,
                                                function,
                                                aliases,
                                                alias_count,
                                                locals,
                                                &local_count,
                                                CHENG_V3_MAX_ASM_LOCALS,
                                                &next_offset,
                                                statement,
                                                0,
                                                &max_call_depth)) {
                    fprintf(stderr,
                            "[cheng_v3_seed] prepare scalar stmt failed function=%s stmt=%s abi=%s\n",
                            function->symbol_text,
                            statement,
                            expr_abi);
                    free(lines);
                    free(owned_lines);
                    return false;
                }
                continue;
            }
        }
        fprintf(stderr,
                "[cheng_v3_seed] prepare top-level stmt unsupported function=%s stmt=%s\n",
                function->symbol_text,
                statement);
        free(lines);
        free(owned_lines);
        return false;
    }
    {
        /* Codegen materializes call arguments and string temps one level below the current call site. */
        int32_t spill_layers = max_call_depth + 2;
        if (spill_layers < 2) {
            spill_layers = 2;
        }
        call_arg_base = next_offset;
        next_offset += spill_layers * CHENG_V3_CALL_ARG_SPILL_STRIDE;
        string_temp_base = next_offset;
        {
            int32_t string_temp_slots = max_string_literal_temps > 0 ? (max_string_literal_temps + 1) : 2;
            if (string_temp_slots < 3) {
                string_temp_slots = 3;
            }
            string_temp_stride = string_temp_slots * v3_active_target_str_size_i32();
        }
        next_offset += string_temp_stride * spill_layers;
    }
    /* Keep a 16-byte guard gap between scratch spill space and the saved FP/LR pair. */
    frame_size = ((next_offset + 15 + 32) / 16) * 16;
    if (v3_active_target_is_linux_x86_64()) {
        frame_size += 8;
    }
    if (frame_size < 16) {
        frame_size = 16;
    }
    v3_maybe_debug_scalar_function_frame_layout(function,
                                                next_offset,
                                                max_call_depth,
                                                max_string_literal_temps,
                                                call_arg_base,
                                                string_temp_base,
                                                string_temp_stride,
                                                frame_size);
    v3_maybe_debug_scalar_function_locals(function,
                                          locals,
                                          local_count,
                                          frame_size);
    if (!v3_validate_scalar_function_frame_layout(function,
                                                  max_call_depth,
                                                  call_arg_base,
                                                  string_temp_base,
                                                  string_temp_stride,
                                                  frame_size)) {
        if (trace_emit_scalar &&
            trace_emit_scalar[0] != '\0' &&
            strcmp(trace_emit_scalar, "0") != 0) {
            fprintf(stderr,
                    "[cheng_v3_seed] emit scalar frame validation failed function=%s\n",
                    function->symbol_text);
        }
        free(lines);
        free(owned_lines);
        return false;
    }
    out[0] = '\0';
    v3_text_appendf(out, cap,
                    ".globl ");
    {
        char symbol_name[PATH_MAX];
        v3_primary_symbol_name(plan, function, symbol_name, sizeof(symbol_name));
        int32_t save_offset = frame_size - 16;
        v3_text_appendf(out, cap,
                        "%s\n"
                        "%s:\n",
                        symbol_name,
                        symbol_name);
        if (v3_active_target_is_linux_x86_64()) {
            v3_text_append(out, cap,
                           "  pushq %rbp\n"
                           "  pushq %rbx\n"
                           "  pushq %r12\n"
                           "  pushq %r13\n"
                           "  pushq %r14\n"
                           "  pushq %r15\n");
            if (!v3_emit_sp_adjust(out, cap, true, frame_size)) {
                free(lines);
                free(owned_lines);
                return false;
            }
        } else {
            if (!v3_emit_sp_adjust(out, cap, true, frame_size)) {
                free(lines);
                free(owned_lines);
                return false;
            }
            if (save_offset <= 504 && (save_offset % 8) == 0) {
                v3_text_appendf(out, cap, "  stp x29, x30, [sp, #%d]\n", save_offset);
            } else if (!v3_emit_sp_offset_address(out, cap, 16, save_offset)) {
                free(lines);
                free(owned_lines);
                return false;
            } else {
                v3_text_appendf(out, cap, "  stp x29, x30, [x16]\n");
            }
            if (!v3_emit_sp_offset_address(out, cap, 29, save_offset)) {
                free(lines);
                free(owned_lines);
                return false;
            }
        }
    }
    for (i = 0; i < function->param_count; ++i) {
        const char *pass_abi = v3_param_pass_abi_class(&locals[i], effective_param_abis[i]);
        int call_reg_count = v3_call_arg_reg_count_for_target(plan, composite_return);
        if (v3_active_target_is_linux_x86_64() && (int)i < call_reg_count) {
            int arg_reg = v3_x64_call_arg_virtual_reg(i, composite_return);
            if (arg_reg < 0 ||
                !v3_emit_sp_store_reg(out,
                                      cap,
                                      locals[i].stack_offset,
                                      pass_abi,
                                      arg_reg)) {
                if (trace_emit_scalar &&
                    trace_emit_scalar[0] != '\0' &&
                    strcmp(trace_emit_scalar, "0") != 0) {
                    fprintf(stderr,
                            "[cheng_v3_seed] emit scalar param spill failed function=%s param=%s abi=%s stack_offset=%d arg_reg=%d\n",
                            function->symbol_text,
                            locals[i].name,
                            pass_abi,
                            locals[i].stack_offset,
                            arg_reg);
                }
                free(lines);
                free(owned_lines);
                return false;
            }
            continue;
        }
        if (!v3_active_target_is_linux_x86_64() && i < CHENG_V3_CALL_ARG_REGS) {
            if (!v3_emit_sp_store_reg(out,
                                      cap,
                                      locals[i].stack_offset,
                                      pass_abi,
                                      (int)i)) {
                if (trace_emit_scalar &&
                    trace_emit_scalar[0] != '\0' &&
                    strcmp(trace_emit_scalar, "0") != 0) {
                    fprintf(stderr,
                            "[cheng_v3_seed] emit scalar param spill failed function=%s param=%s abi=%s stack_offset=%d arg_reg=%zu\n",
                            function->symbol_text,
                            locals[i].name,
                            pass_abi,
                            locals[i].stack_offset,
                            i);
                    }
                free(lines);
                free(owned_lines);
                return false;
            }
            continue;
        }
        if (!v3_emit_sp_load_reg(out,
                                 cap,
                                 v3_incoming_stack_arg_offset_for_target(plan,
                                                                         frame_size,
                                                                         (int32_t)(i - (size_t)call_reg_count)),
                                 pass_abi,
                                 9) ||
            !v3_emit_sp_store_reg(out,
                                  cap,
                                  locals[i].stack_offset,
                                  pass_abi,
                                  9)) {
            if (trace_emit_scalar &&
                trace_emit_scalar[0] != '\0' &&
                strcmp(trace_emit_scalar, "0") != 0) {
                fprintf(stderr,
                        "[cheng_v3_seed] emit scalar stack param reload failed function=%s param=%s abi=%s stack_offset=%d frame_size=%d\n",
                        function->symbol_text,
                        locals[i].name,
                        pass_abi,
                        locals[i].stack_offset,
                        frame_size);
            }
            free(lines);
            free(owned_lines);
            return false;
        }
    }
    if (composite_return && sret_local_index >= 0) {
        if (!v3_emit_sp_store_reg(out,
                                  cap,
                                  locals[sret_local_index].stack_offset,
                                  "ptr",
                                  v3_active_target_is_linux_x86_64() ? 14 : 8)) {
            free(lines);
            free(owned_lines);
            return false;
        }
    }
    if (has_inline_signature_body &&
        strcmp(function->body_kind, "return_expr") == 0) {
        if (composite_return) {
            if (sret_local_index < 0 ||
                !v3_emit_slot_address(out, cap, &locals[sret_local_index], 15) ||
                !v3_materialize_composite_expr_into_address(plan,
                                                            lowering,
                                                            function,
                                                            aliases,
                                                            alias_count,
                                                            locals,
                                                            local_count,
                                                            inline_signature_body,
                                                            function->return_type,
                                                            15,
                                                            call_arg_base,
                                                            string_temp_base,
                                                            string_temp_stride,
                                                            &string_label_index,
                                                            0,
                                                            out,
                                                            cap)) {
                fprintf(stderr,
                        "[cheng_v3_seed] emit inline composite return failed function=%s expr=%s type=%s\n",
                        function->symbol_text,
                        inline_signature_body,
                        function->return_type);
                free(lines);
                free(owned_lines);
                return false;
            }
        } else if (!v3_codegen_expr_scalar(plan,
                                           lowering,
                                           function,
                                           aliases,
                                           alias_count,
                                           locals,
                                           local_count,
                                           inline_signature_body,
                                           function->return_abi_class,
                                           9,
                                           call_arg_base,
                                           string_temp_base,
                                           string_temp_stride,
                                           0,
                                           out,
                                           cap)) {
            fprintf(stderr,
                    "[cheng_v3_seed] emit inline return failed function=%s expr=%s abi=%s\n",
                    function->symbol_text,
                    inline_signature_body,
                    function->return_abi_class);
            free(lines);
            free(owned_lines);
            return false;
        } else {
            v3_emit_reg_to_return(out, cap, 9, function->return_abi_class);
        }
        if (!v3_emit_function_epilogue(out, cap, frame_size)) {
            free(lines);
            free(owned_lines);
            return false;
        }
        saw_return = true;
    }
    for (i = fn_line_index + 1U; !saw_return && i < line_count; ) {
        char statement[8192];
        char name[128];
        char type_text[CHENG_V3_MAX_TYPE_TEXT];
        char expr[4096];
        char raw_copy[4096];
        char *trimmed;
        int32_t statement_indent;
        snprintf(raw_copy, sizeof(raw_copy), "%s", lines[i]);
        trimmed = v3_trim_inplace(raw_copy);
        if (*trimmed == '\0') {
            i += 1U;
            continue;
        }
        statement_indent = v3_line_indent(lines[i]);
        if (statement_indent <= 0 && v3_is_top_level_decl_start(trimmed)) {
            break;
        }
        if (!v3_collect_statement_from_lines(lines, line_count, &i, statement, sizeof(statement))) {
            free(lines);
            free(owned_lines);
            return false;
        }
        if (statement_indent <= 0 && v3_is_top_level_decl_start(statement)) {
            break;
        }
        if (trace_emit_scalar &&
            trace_emit_scalar[0] != '\0' &&
            strcmp(trace_emit_scalar, "0") != 0) {
            fprintf(stderr,
                    "[cheng_v3_seed] emit scalar stmt function=%s stmt=%s\n",
                    function->symbol_text,
                    statement);
        }
        if (v3_startswith(statement, "if ")) {
            if (!v3_emit_if_statement(plan,
                                      lowering,
                                      function,
                                      aliases,
                                      alias_count,
                                      locals,
                                      local_count,
                                      lines,
                                      line_count,
                                      &i,
                                      statement_indent,
                                      statement,
                                      call_arg_base,
                                      string_temp_base,
                                      string_temp_stride,
                                      &string_label_index,
                                      &control_label_index,
                                      frame_size,
                                      sret_local_index,
                                      out,
                                      cap)) {
                fprintf(stderr,
                        "[cheng_v3_seed] emit if statement failed function=%s stmt=%s\n",
                        function->symbol_text,
                        statement);
                free(lines);
                free(owned_lines);
                return false;
            }
            continue;
        }
        if (v3_startswith(statement, "while ")) {
            char while_cond[4096];
            char while_inline_stmt[8192];
            if (!v3_emit_while_statement(plan,
                                         lowering,
                                         function,
                                         aliases,
                                         alias_count,
                                         locals,
                                         local_count,
                                         lines,
                                         line_count,
                                         &i,
                                         statement_indent,
                                         statement,
                                         call_arg_base,
                                         string_temp_base,
                                         string_temp_stride,
                                         &string_label_index,
                                         &control_label_index,
                                         frame_size,
                                         sret_local_index,
                                         out,
                                         cap)) {
                fprintf(stderr,
                        "[cheng_v3_seed] emit while statement failed function=%s stmt=%s\n",
                        function->symbol_text,
                        statement);
                free(lines);
                free(owned_lines);
                return false;
            }
            if (!void_return &&
                v3_is_last_logical_statement_in_function(lines, line_count, i) &&
                v3_parse_while_header(statement,
                                      while_cond,
                                      sizeof(while_cond),
                                      while_inline_stmt,
                                      sizeof(while_inline_stmt)) &&
                strcmp(while_cond, "true") == 0) {
                saw_return = true;
                break;
            }
            continue;
        }
        if (v3_startswith(statement, "for ")) {
            if (!v3_emit_for_statement(plan,
                                       lowering,
                                       function,
                                       aliases,
                                       alias_count,
                                       locals,
                                       local_count,
                                       lines,
                                       line_count,
                                       &i,
                                       statement_indent,
                                       statement,
                                       call_arg_base,
                                       string_temp_base,
                                       string_temp_stride,
                                       &string_label_index,
                                       &control_label_index,
                                       frame_size,
                                       sret_local_index,
                                       out,
                                       cap)) {
                fprintf(stderr,
                        "[cheng_v3_seed] emit for statement failed function=%s stmt=%s\n",
                        function->symbol_text,
                        statement);
                free(lines);
                free(owned_lines);
                return false;
            }
            continue;
        }
        if (v3_startswith(statement, "case ")) {
            bool case_terminated = false;
            if (!v3_emit_case_const_table_statement(plan,
                                                    lowering,
                                                    function,
                                                    aliases,
                                                    alias_count,
                                                    locals,
                                                    local_count,
                                                    lines,
                                                    line_count,
                                                    &i,
                                                    statement_indent,
                                                    statement,
                                                    call_arg_base,
                                                    string_temp_base,
                                                    string_temp_stride,
                                                    &string_label_index,
                                                    &control_label_index,
                                                    frame_size,
                                                    sret_local_index,
                                                    &case_terminated,
                                                    out,
                                                    cap)) {
                fprintf(stderr,
                        "[cheng_v3_seed] emit case statement failed function=%s stmt=%s\n",
                        function->symbol_text,
                        statement);
                free(lines);
                free(owned_lines);
                return false;
            }
            if (case_terminated) {
                saw_return = true;
                break;
            }
            continue;
        }
        if ((v3_startswith(statement, "let ") || v3_startswith(statement, "var ")) &&
            v3_parse_binding_decl_meta(statement,
                                       v3_startswith(statement, "let ") ? "let " : "var ",
                                       name,
                                       sizeof(name),
                                       type_text,
                                       sizeof(type_text))) {
            int32_t local_index = v3_find_local_slot(locals, local_count, name);
            if (local_index < 0 ||
                !v3_emit_slot_address(out, cap, &locals[local_index], 15) ||
                !v3_emit_implicit_default_into_address(plan,
                                                       lowering,
                                                       function,
                                                       aliases,
                                                       alias_count,
                                                       function->owner_module_path,
                                                       function->source_path,
                                                       locals[local_index].type_text,
                                                       15,
                                                       call_arg_base,
                                                       string_temp_base,
                                                       string_temp_stride,
                                                       &string_label_index,
                                                       0,
                                                       out,
                                                       cap,
                                                       0U)) {
                free(lines);
                free(owned_lines);
                return false;
            }
            continue;
        }
        if ((v3_startswith(statement, "let ") || v3_startswith(statement, "var ")) &&
            v3_parse_binding_meta(statement,
                                  v3_startswith(statement, "let ") ? "let " : "var ",
                                  name,
                                  sizeof(name),
                                  type_text,
                                  sizeof(type_text),
                                  expr,
                                  sizeof(expr))) {
            int32_t local_index = v3_find_local_slot(locals, local_count, name);
            const char *abi_class;
            char inferred_type[CHENG_V3_MAX_TYPE_TEXT];
            char inferred_abi[32];
            char normalized_type[256];
            if (type_text[0] == '\0') {
                if (!v3_infer_expr_type(plan,
                                        lowering,
                                        function,
                                        aliases,
                                        alias_count,
                                        locals,
                                        local_count,
                                        expr,
                                        inferred_type,
                                        sizeof(inferred_type),
                                        inferred_abi,
                                        sizeof(inferred_abi))) {
                    fprintf(stderr,
                            "[cheng_v3_seed] emit binding infer type failed function=%s local=%s expr=%s\n",
                            function->symbol_text,
                            name,
                            expr);
                    free(lines);
                    free(owned_lines);
                    return false;
                }
                snprintf(type_text, sizeof(type_text), "%s", inferred_type);
            }
            if (!v3_tuple_binding_expr_complete(type_text, expr)) {
                fprintf(stderr,
                        "[cheng_v3_seed] tuple literal omission unsupported function=%s local=%s type=%s expr=%s\n",
                        function->symbol_text,
                        name,
                        type_text,
                        expr);
                free(lines);
                free(owned_lines);
                return false;
            }
            abi_class = v3_resolved_type_abi_class(lowering,
                                                   function->owner_module_path,
                                                   aliases,
                                                   alias_count,
                                                   type_text,
                                                   normalized_type,
                                                   sizeof(normalized_type));
            if (local_index < 0 || locals[local_index].stack_offset < 0) {
                fprintf(stderr,
                        "[cheng_v3_seed] emit local missing function=%s local=%s stmt=%s\n",
                        function->symbol_text,
                        name,
                        statement);
                free(lines);
                free(owned_lines);
                return false;
            }
            if (v3_abi_class_scalar_or_ptr(abi_class)) {
                if (!v3_codegen_expr_scalar(plan,
                                            lowering,
                                            function,
                                            aliases,
                                            alias_count,
                                            locals,
                                            local_count,
                                            expr,
                                            abi_class,
                                            9,
                                            call_arg_base,
                                            string_temp_base,
                                            string_temp_stride,
                                            1,
                                            out,
                                            cap)) {
                    fprintf(stderr,
                            "[cheng_v3_seed] emit scalar binding failed function=%s local=%s abi=%s expr=%s\n",
                            function->symbol_text,
                            name,
                            abi_class,
                            expr);
                    free(lines);
                    free(owned_lines);
                    return false;
                }
                v3_emit_store_slot(out, cap, &locals[local_index], 9);
                continue;
            }
            if (!v3_materialize_composite_expr_into_slot(plan,
                                                         lowering,
                                                         function,
                                                         aliases,
                                                         alias_count,
                                                         locals,
                                                         local_count,
                                                         name,
                                                         0U,
                                                         expr,
                                                         &locals[local_index],
                                                         call_arg_base,
                                                         string_temp_base,
                                                         string_temp_stride,
                                                         &string_label_index,
                                                         0,
                                                         out,
                                                         cap)) {
                fprintf(stderr,
                        "[cheng_v3_seed] emit composite binding failed function=%s local=%s type=%s expr=%s\n",
                        function->symbol_text,
                        name,
                        type_text,
                        expr);
                free(lines);
                free(owned_lines);
                return false;
            }
            continue;
        }
        if (v3_parse_assignment_meta(statement,
                                     name,
                                     sizeof(name),
                                     expr,
                                     sizeof(expr))) {
            int32_t local_index = v3_find_local_slot(locals, local_count, name);
            char field_type[256];
            char field_abi[32];
            bool handled = false;
            if (!v3_try_codegen_index_assignment_call(plan,
                                                      lowering,
                                                      function,
                                                      aliases,
                                                      alias_count,
                                                      locals,
                                                      local_count,
                                                      name,
                                                      expr,
                                                      call_arg_base,
                                                      string_temp_base,
                                                      string_temp_stride,
                                                      &string_label_index,
                                                      0,
                                                      out,
                                                      cap,
                                                      &handled)) {
                fprintf(stderr,
                        "[cheng_v3_seed] emit index assignment failed function=%s target=%s expr=%s stmt=%s\n",
                        function->symbol_text,
                        name,
                        expr,
                        statement);
                free(lines);
                free(owned_lines);
                return false;
            }
            if (handled) {
                continue;
            }
            if (local_index >= 0) {
                if (locals[local_index].indirect_value) {
                    const char *value_type = locals[local_index].type_text;
                    const char *value_abi = locals[local_index].abi_class;
                    if (v3_abi_class_scalar_or_ptr(value_abi)) {
                        if (!v3_emit_local_value_address(out, cap, &locals[local_index], 15)) {
                            fprintf(stderr,
                                    "[cheng_v3_seed] emit indirect local scalar assignment failed function=%s target=%s expr=%s abi=%s stmt=%s\n",
                                    function->symbol_text,
                                    name,
                                    expr,
                                    value_abi,
                                    statement);
                            free(lines);
                            free(owned_lines);
                            return false;
                        }
                        v3_emit_address_spill_store(out, cap, call_arg_base, 0, 15);
                        if (!v3_codegen_expr_scalar(plan,
                                                    lowering,
                                                    function,
                                                    aliases,
                                                    alias_count,
                                                    locals,
                                                    local_count,
                                                    expr,
                                                    value_abi,
                                                    9,
                                                    call_arg_base,
                                                    string_temp_base,
                                                    string_temp_stride,
                                                    0,
                                                    out,
                                                    cap)) {
                            fprintf(stderr,
                                    "[cheng_v3_seed] emit indirect local scalar assignment failed function=%s target=%s expr=%s abi=%s stmt=%s\n",
                                    function->symbol_text,
                                    name,
                                    expr,
                                    value_abi,
                                    statement);
                            free(lines);
                            free(owned_lines);
                            return false;
                        }
                        v3_emit_address_spill_load(out, cap, call_arg_base, 0, 15);
                        v3_emit_store_scalar_to_address(out, cap, 15, 0, value_abi, 9);
                        continue;
                    }
                    if (!v3_emit_local_value_address(out, cap, &locals[local_index], 15)) {
                        fprintf(stderr,
                                "[cheng_v3_seed] emit indirect local composite assignment failed function=%s target=%s expr=%s type=%s stmt=%s\n",
                                function->symbol_text,
                                name,
                                expr,
                                value_type,
                                statement);
                        free(lines);
                        free(owned_lines);
                        return false;
                    }
                    v3_emit_address_spill_store(out, cap, call_arg_base, 0, 15);
                    if (!v3_materialize_composite_expr_into_address(plan,
                                                                    lowering,
                                                                    function,
                                                                    aliases,
                                                                    alias_count,
                                                                    locals,
                                                                    local_count,
                                                                    expr,
                                                                    value_type,
                                                                    15,
                                                                    call_arg_base,
                                                                    string_temp_base,
                                                                    string_temp_stride,
                                                                    &string_label_index,
                                                                    0,
                                                                    out,
                                                                    cap)) {
                        fprintf(stderr,
                                "[cheng_v3_seed] emit indirect local composite assignment failed function=%s target=%s expr=%s type=%s stmt=%s\n",
                                function->symbol_text,
                                name,
                                expr,
                                value_type,
                                statement);
                        free(lines);
                        free(owned_lines);
                        return false;
                    }
                    v3_emit_address_spill_load(out, cap, call_arg_base, 0, 15);
                    continue;
                }
                const char *abi_class = locals[local_index].abi_class;
                if (v3_abi_class_scalar_or_ptr(abi_class)) {
                    if (!v3_codegen_expr_scalar(plan,
                                                lowering,
                                                function,
                                                aliases,
                                                alias_count,
                                                locals,
                                                local_count,
                                                expr,
                                                abi_class,
                                                9,
                                                call_arg_base,
                                                string_temp_base,
                                                string_temp_stride,
                                                0,
                                                out,
                                                cap)) {
                        fprintf(stderr,
                                "[cheng_v3_seed] emit local scalar assignment failed function=%s target=%s expr=%s abi=%s stmt=%s\n",
                                function->symbol_text,
                                name,
                                expr,
                                abi_class,
                                statement);
                        free(lines);
                        free(owned_lines);
                        return false;
                    }
                    v3_emit_store_slot(out, cap, &locals[local_index], 9);
                    continue;
                }
                if (!v3_materialize_composite_expr_into_slot(plan,
                                                             lowering,
                                                             function,
                                                             aliases,
                                                             alias_count,
                                                             locals,
                                                             local_count,
                                                             name,
                                                             0U,
                                                             expr,
                                                             &locals[local_index],
                                                             call_arg_base,
                                                             string_temp_base,
                                                             string_temp_stride,
                                                             &string_label_index,
                                                             0,
                                                             out,
                                                             cap)) {
                    fprintf(stderr,
                            "[cheng_v3_seed] emit local composite assignment failed function=%s target=%s expr=%s stmt=%s\n",
                            function->symbol_text,
                            name,
                            expr,
                            statement);
                    free(lines);
                    free(owned_lines);
                    return false;
                }
                continue;
            }
            if (v3_infer_lvalue_expr_type(plan,
                                          lowering,
                                          function,
                                          aliases,
                                          alias_count,
                                          locals,
                                          local_count,
                                          name,
                                          field_type,
                                          sizeof(field_type),
                                          field_abi,
                                          sizeof(field_abi))) {
                if (v3_abi_class_scalar_or_ptr(field_abi)) {
                    if (!v3_emit_lvalue_address(plan,
                                                lowering,
                                                function,
                                                aliases,
                                                alias_count,
                                                locals,
                                                local_count,
                                                name,
                                                field_type,
                                                sizeof(field_type),
                                                field_abi,
                                                sizeof(field_abi),
                                                15,
                                                call_arg_base,
                                                string_temp_base,
                                                string_temp_stride,
                                                0,
                                                out,
                                                cap)) {
                        fprintf(stderr,
                                "[cheng_v3_seed] emit lvalue scalar address failed function=%s target=%s type=%s abi=%s stmt=%s\n",
                                function->symbol_text,
                                name,
                                field_type,
                                field_abi,
                                statement);
                        free(lines);
                        free(owned_lines);
                        return false;
                    }
                    v3_emit_address_spill_store(out, cap, call_arg_base, 0, 15);
                    if (!v3_codegen_expr_scalar(plan,
                                                lowering,
                                                function,
                                                aliases,
                                                alias_count,
                                                locals,
                                                local_count,
                                                expr,
                                                field_abi,
                                                9,
                                                call_arg_base,
                                                string_temp_base,
                                                string_temp_stride,
                                                1,
                                                out,
                                                cap)) {
                        fprintf(stderr,
                                "[cheng_v3_seed] emit lvalue scalar expr failed function=%s target=%s expr=%s abi=%s stmt=%s\n",
                                function->symbol_text,
                                name,
                                expr,
                                field_abi,
                                statement);
                        free(lines);
                        free(owned_lines);
                        return false;
                    }
                    v3_emit_address_spill_load(out, cap, call_arg_base, 0, 15);
                    v3_emit_store_scalar_to_address(out, cap, 15, 0, field_abi, 9);
                    continue;
                }
                if (!v3_emit_lvalue_address(plan,
                                            lowering,
                                            function,
                                            aliases,
                                            alias_count,
                                            locals,
                                            local_count,
                                            name,
                                            field_type,
                                            sizeof(field_type),
                                            field_abi,
                                            sizeof(field_abi),
                                            15,
                                            call_arg_base,
                                            string_temp_base,
                                            string_temp_stride,
                                            0,
                                            out,
                                            cap)) {
                    fprintf(stderr,
                            "[cheng_v3_seed] emit lvalue composite address failed function=%s target=%s type=%s abi=%s stmt=%s\n",
                            function->symbol_text,
                            name,
                            field_type,
                            field_abi,
                            statement);
                    free(lines);
                    free(owned_lines);
                    return false;
                }
                if (!v3_materialize_composite_expr_into_address(plan,
                                                                lowering,
                                                                function,
                                                                aliases,
                                                                alias_count,
                                                                locals,
                                                                local_count,
                                                                expr,
                                                                field_type,
                                                                15,
                                                                call_arg_base,
                                                                string_temp_base,
                                                                string_temp_stride,
                                                                &string_label_index,
                                                                0,
                                                                out,
                                                                cap)) {
                    fprintf(stderr,
                            "[cheng_v3_seed] emit lvalue composite materialize failed function=%s target=%s expr=%s type=%s stmt=%s\n",
                            function->symbol_text,
                            name,
                            expr,
                            field_type,
                            statement);
                    free(lines);
                    free(owned_lines);
                    return false;
                }
                continue;
            }
            fprintf(stderr,
                    "[cheng_v3_seed] emit lvalue infer failed function=%s target=%s expr=%s stmt=%s\n",
                    function->symbol_text,
                    name,
                    expr,
                    statement);
            free(lines);
            free(owned_lines);
            return false;
        }
        if (strcmp(statement, "return") == 0) {
            if (!void_return) {
                free(lines);
                free(owned_lines);
                return false;
            }
            if (!v3_emit_function_epilogue(out, cap, frame_size)) {
                free(lines);
                free(owned_lines);
                return false;
            }
            saw_return = true;
            break;
        }
        if (v3_is_implicit_return_statement(function,
                                            lines,
                                            line_count,
                                            i,
                                            statement)) {
            if (composite_return) {
                if (sret_local_index < 0 ||
                    !v3_emit_slot_address(out, cap, &locals[sret_local_index], 15) ||
                    !v3_materialize_composite_expr_into_address(plan,
                                                                lowering,
                                                                function,
                                                                aliases,
                                                                alias_count,
                                                                locals,
                                                                local_count,
                                                                statement,
                                                                function->return_type,
                                                                15,
                                                                call_arg_base,
                                                                string_temp_base,
                                                                string_temp_stride,
                                                                &string_label_index,
                                                                0,
                                                                out,
                                                                cap)) {
                    fprintf(stderr,
                            "[cheng_v3_seed] emit implicit composite return failed function=%s expr=%s type=%s\n",
                            function->symbol_text,
                            statement,
                            function->return_type);
                    free(lines);
                    free(owned_lines);
                    return false;
                }
            } else if (!v3_codegen_expr_scalar(plan,
                                               lowering,
                                               function,
                                               aliases,
                                               alias_count,
                                               locals,
                                               local_count,
                                               statement,
                                               function->return_abi_class,
                                               9,
                                               call_arg_base,
                                               string_temp_base,
                                               string_temp_stride,
                                               0,
                                               out,
                                               cap)) {
                fprintf(stderr,
                        "[cheng_v3_seed] emit implicit return failed function=%s expr=%s abi=%s\n",
                        function->symbol_text,
                        statement,
                        function->return_abi_class);
                free(lines);
                free(owned_lines);
                return false;
            } else {
                v3_emit_reg_to_return(out, cap, 9, function->return_abi_class);
            }
            if (!v3_emit_function_epilogue(out, cap, frame_size)) {
                free(lines);
                free(owned_lines);
                return false;
            }
            saw_return = true;
            break;
        }
        if (v3_startswith(statement, "return ")) {
            if (composite_return) {
                if (sret_local_index < 0 ||
                    !v3_emit_slot_address(out, cap, &locals[sret_local_index], 15) ||
                    !v3_materialize_composite_expr_into_address(plan,
                                                                lowering,
                                                                function,
                                                                aliases,
                                                                alias_count,
                                                                locals,
                                                                local_count,
                                                                statement + 7,
                                                                function->return_type,
                                                                15,
                                                                call_arg_base,
                                                                string_temp_base,
                                                                string_temp_stride,
                                                                &string_label_index,
                                                                0,
                                                                out,
                                                                cap)) {
                    fprintf(stderr,
                            "[cheng_v3_seed] emit composite return failed function=%s expr=%s type=%s\n",
                            function->symbol_text,
                            statement + 7,
                            function->return_type);
                    free(lines);
                    free(owned_lines);
                    return false;
                }
            } else if (!v3_codegen_expr_scalar(plan,
                                               lowering,
                                               function,
                                               aliases,
                                               alias_count,
                                               locals,
                                               local_count,
                                               statement + 7,
                                               function->return_abi_class,
                                               9,
                                               call_arg_base,
                                               string_temp_base,
                                               string_temp_stride,
                                               0,
                                               out,
                                               cap)) {
                fprintf(stderr,
                        "[cheng_v3_seed] emit return failed function=%s expr=%s abi=%s\n",
                        function->symbol_text,
                        statement + 7,
                        function->return_abi_class);
                free(lines);
                free(owned_lines);
                return false;
            } else {
                v3_emit_reg_to_return(out, cap, 9, function->return_abi_class);
            }
            if (!v3_emit_function_epilogue(out, cap, frame_size)) {
                free(lines);
                free(owned_lines);
                return false;
            }
            saw_return = true;
            break;
        }
        if (v3_is_noop_local_reference_statement(locals, local_count, statement)) {
            continue;
        }
        if (strchr(statement, '(') != NULL) {
        bool handled = false;
        char callee[PATH_MAX];
        char (*args)[4096] = v3_alloc_call_arg_scratch();
        size_t arg_count = 0U;
        callee[0] = '\0';
        v3_parse_call_text(statement, callee, sizeof(callee), args, &arg_count, CHENG_V3_MAX_CALL_ARGS);
        if (!v3_try_emit_builtin_statement(plan,
                                           lowering,
                                           function,
                                           aliases,
                                               alias_count,
                                               locals,
                                               local_count,
                                               statement,
                                               call_arg_base,
                                               string_temp_base,
                                               string_temp_stride,
                                               &string_label_index,
                                               &control_label_index,
                                               out,
                                               cap,
                                               &handled)) {
                fprintf(stderr,
                        "[cheng_v3_seed] emit builtin statement failed function=%s stmt=%s\n",
                        function->symbol_text,
                        statement);
                free(args);
                free(lines);
                free(owned_lines);
                return false;
            }
            if (handled) {
                if (strcmp(callee, "panic") == 0 || strcmp(callee, "system.panic") == 0) {
                    saw_return = true;
                    free(args);
                    break;
                }
                free(args);
                continue;
            }
            if (!v3_parse_call_text(statement, callee, sizeof(callee), args, &arg_count, CHENG_V3_MAX_CALL_ARGS) ||
                !v3_codegen_call_scalar(plan,
                                        lowering,
                                        function,
                                        aliases,
                                        alias_count,
                                        locals,
                                        local_count,
                                        callee,
                                        args,
                                        arg_count,
                                        "void",
                                        9,
                                        call_arg_base,
                                        string_temp_base,
                                        string_temp_stride,
                                        &string_label_index,
                                        0,
                                        out,
                                        cap)) {
                fprintf(stderr,
                        "[cheng_v3_seed] emit stmt call failed function=%s stmt=%s\n",
                        function->symbol_text,
                        statement);
                free(args);
                free(lines);
                free(owned_lines);
                return false;
            }
            free(args);
            continue;
        }
        {
            char expr_type[256];
            char expr_abi[32];
            if (v3_infer_expr_type(plan,
                                   lowering,
                                   function,
                                   aliases,
                                   alias_count,
                                   locals,
                                   local_count,
                                   statement,
                                   expr_type,
                                   sizeof(expr_type),
                                   expr_abi,
                                   sizeof(expr_abi)) &&
                v3_abi_class_scalar_or_ptr(expr_abi)) {
                if (!v3_codegen_expr_scalar(plan,
                                            lowering,
                                            function,
                                            aliases,
                                            alias_count,
                                            locals,
                                            local_count,
                                            statement,
                                            expr_abi,
                                            9,
                                            call_arg_base,
                                            string_temp_base,
                                            string_temp_stride,
                                            0,
                                            out,
                                            cap)) {
                    fprintf(stderr,
                            "[cheng_v3_seed] emit scalar stmt failed function=%s stmt=%s abi=%s\n",
                            function->symbol_text,
                            statement,
                            expr_abi);
                    free(lines);
                    free(owned_lines);
                    return false;
                }
                continue;
            }
        }
        free(lines);
        free(owned_lines);
        fprintf(stderr,
                "[cheng_v3_seed] emit top-level stmt unsupported function=%s stmt=%s\n",
                function->symbol_text,
                statement);
        return false;
    }
    if (!saw_return) {
        if (!void_return) {
            free(lines);
            free(owned_lines);
            fprintf(stderr,
                    "[cheng_v3_seed] emit function fell through without terminal return function=%s return_type=%s body_kind=%s\n",
                    function->symbol_text,
                    function->return_type,
                    function->body_kind);
            return false;
        }
        if (!v3_emit_function_epilogue(out, cap, frame_size)) {
            free(lines);
            free(owned_lines);
            return false;
        }
    }
    free(lines);
    free(owned_lines);
    if (instruction_words_out) {
        *instruction_words_out = 16U;
    }
    return true;
}

static bool v3_append_module_global_init_function(const V3SystemLinkPlanStub *plan,
                                                  const V3LoweringPlanStub *lowering,
                                                  const char *source_path,
                                                  char *symbol_out,
                                                  size_t symbol_cap,
                                                  char *out,
                                                  size_t cap,
                                                  bool *emitted_out) {
    char owner_module_path[PATH_MAX];
    char **lines = NULL;
    char *owned = NULL;
    size_t line_count = 0U;
    V3ImportAlias aliases[CHENG_V3_MAX_IMPORT_ALIASES];
    size_t alias_count = 0U;
    V3AsmLocalSlot locals[CHENG_V3_MAX_ASM_LOCALS];
    size_t local_count = 0U;
    int32_t next_offset = 0;
    int32_t max_call_depth = 0;
    int32_t max_string_literal_temps = 0;
    int32_t call_arg_base = 0;
    int32_t string_temp_base = 0;
    int32_t string_temp_stride = 0;
    int32_t frame_size = 0;
    int32_t string_label_index = 0;
    size_t i;
    bool in_var_block = false;
    bool saw_init = false;
    V3LoweredFunctionStub synthetic_function;
    char symbol_name[PATH_MAX];

    if (emitted_out) {
        *emitted_out = false;
    }
    if (symbol_out && symbol_cap > 0U) {
        symbol_out[0] = '\0';
    }
    v3_source_path_to_module_path(plan->workspace_root,
                                  plan->package_root,
                                  plan->package_id,
                                  source_path,
                                  owner_module_path,
                                  sizeof(owner_module_path));
    if (!v3_path_exists_nonempty(source_path) ||
        !v3_load_source_lines(source_path, &lines, &owned, &line_count)) {
        return false;
    }
    v3_parse_import_aliases_from_lines(lines,
                                       line_count,
                                       aliases,
                                       &alias_count,
                                       CHENG_V3_MAX_IMPORT_ALIASES);
    memset(&synthetic_function, 0, sizeof(synthetic_function));
    snprintf(synthetic_function.source_path, sizeof(synthetic_function.source_path), "%s", source_path);
    snprintf(synthetic_function.owner_module_path,
             sizeof(synthetic_function.owner_module_path),
             "%s",
             owner_module_path);
    snprintf(synthetic_function.function_name,
             sizeof(synthetic_function.function_name),
             "%s",
             "__v3_module_init");
    snprintf(synthetic_function.symbol_text,
             sizeof(synthetic_function.symbol_text),
             "%s::__v3_module_init",
             owner_module_path);
    snprintf(synthetic_function.return_type, sizeof(synthetic_function.return_type), "%s", "void");
    snprintf(synthetic_function.return_abi_class, sizeof(synthetic_function.return_abi_class), "%s", "void");
    memset(locals, 0, sizeof(locals));

    for (i = 0; i < line_count; ++i) {
        char line_copy[4096];
        char name[128];
        char raw_type[256];
        char expr[4096];
        char normalized_type[256];
        bool has_init = false;
        int32_t indent;
        char *trimmed;
        snprintf(line_copy, sizeof(line_copy), "%s", lines[i]);
        indent = v3_line_indent(line_copy);
        trimmed = v3_trim_inplace(line_copy);
        if (*trimmed == '\0' || *trimmed == '#') {
            continue;
        }
        if (indent <= 0 && strcmp(trimmed, "var") == 0) {
            in_var_block = true;
            continue;
        }
        if (indent <= 0 && !v3_startswith(trimmed, "var ")) {
            in_var_block = false;
        }
        if (indent <= 0 && v3_startswith(trimmed, "var ")) {
            trimmed += 4;
        } else if (!in_var_block || indent <= 0) {
            continue;
        }
        if (!v3_parse_top_level_var_binding_text(trimmed,
                                                 name,
                                                 sizeof(name),
                                                 raw_type,
                                                 sizeof(raw_type),
                                                 &has_init,
                                                 expr,
                                                 sizeof(expr)) ||
            !has_init ||
            raw_type[0] == '\0' ||
            !v3_normalize_type_text(lowering,
                                    owner_module_path,
                                    aliases,
                                    alias_count,
                                    raw_type,
                                    normalized_type,
                                    sizeof(normalized_type))) {
            continue;
        }
        saw_init = true;
        if (v3_call_expr_string_literal_count(expr) > max_string_literal_temps) {
            max_string_literal_temps = v3_call_expr_string_literal_count(expr);
        }
        if (!v3_prepare_expr_call_state(plan,
                                        lowering,
                                        &synthetic_function,
                                        aliases,
                                        alias_count,
                                        locals,
                                        &local_count,
                                        CHENG_V3_MAX_ASM_LOCALS,
                                        &next_offset,
                                        expr,
                                        0,
                                        &max_call_depth)) {
            fprintf(stderr,
                    "[cheng_v3_seed] prepare module init expr failed module=%s var=%s expr=%s\n",
                    owner_module_path,
                    name,
                    expr);
            free(lines);
            free(owned);
            return false;
        }
    }
    if (!saw_init) {
        free(lines);
        free(owned);
        return true;
    }

    {
        int32_t spill_layers = max_call_depth + 2;
        int32_t string_temp_slots;
        if (spill_layers < 2) {
            spill_layers = 2;
        }
        call_arg_base = next_offset;
        next_offset += spill_layers * CHENG_V3_CALL_ARG_SPILL_STRIDE;
        string_temp_base = next_offset;
        string_temp_slots = max_string_literal_temps > 0 ? (max_string_literal_temps + 1) : 2;
        if (string_temp_slots < 3) {
            string_temp_slots = 3;
        }
        string_temp_stride = string_temp_slots * v3_active_target_str_size_i32();
        next_offset += string_temp_stride * spill_layers;
        frame_size = ((next_offset + 15 + 32) / 16) * 16;
        if (v3_active_target_is_linux_x86_64()) {
            frame_size += 8;
        }
        if (frame_size < 16) {
            frame_size = 16;
        }
    }
    if (!v3_validate_scalar_function_frame_layout(&synthetic_function,
                                                  max_call_depth,
                                                  call_arg_base,
                                                  string_temp_base,
                                                  string_temp_stride,
                                                  frame_size)) {
        free(lines);
        free(owned);
        return false;
    }

    v3_module_init_symbol_name(plan, owner_module_path, symbol_name, sizeof(symbol_name));
    if (symbol_out && symbol_cap > 0U) {
        snprintf(symbol_out, symbol_cap, "%s", symbol_name);
    }
    v3_text_appendf(out, cap,
                    ".globl %s\n"
                    "%s:\n",
                    symbol_name,
                    symbol_name);
    {
        int32_t save_offset = frame_size - 16;
        if (v3_active_target_is_linux_x86_64()) {
            v3_text_append(out, cap,
                           "  pushq %rbp\n"
                           "  pushq %rbx\n"
                           "  pushq %r12\n"
                           "  pushq %r13\n"
                           "  pushq %r14\n"
                           "  pushq %r15\n");
            if (!v3_emit_sp_adjust(out, cap, true, frame_size)) {
                free(lines);
                free(owned);
                return false;
            }
        } else {
            if (!v3_emit_sp_adjust(out, cap, true, frame_size)) {
                free(lines);
                free(owned);
                return false;
            }
            if (save_offset <= 504 && (save_offset % 8) == 0) {
                v3_text_appendf(out, cap, "  stp x29, x30, [sp, #%d]\n", save_offset);
            } else if (!v3_emit_sp_offset_address(out, cap, 16, save_offset)) {
                free(lines);
                free(owned);
                return false;
            } else {
                v3_text_appendf(out, cap, "  stp x29, x30, [x16]\n");
            }
            if (!v3_emit_sp_offset_address(out, cap, 29, save_offset)) {
                free(lines);
                free(owned);
                return false;
            }
        }
    }

    in_var_block = false;
    for (i = 0; i < line_count; ++i) {
        char line_copy[4096];
        char name[128];
        char raw_type[256];
        char expr[4096];
        char normalized_type[256];
        char global_symbol[PATH_MAX];
        const char *abi_class;
        bool has_init = false;
        int32_t indent;
        char *trimmed;
        snprintf(line_copy, sizeof(line_copy), "%s", lines[i]);
        indent = v3_line_indent(line_copy);
        trimmed = v3_trim_inplace(line_copy);
        if (*trimmed == '\0' || *trimmed == '#') {
            continue;
        }
        if (indent <= 0 && strcmp(trimmed, "var") == 0) {
            in_var_block = true;
            continue;
        }
        if (indent <= 0 && !v3_startswith(trimmed, "var ")) {
            in_var_block = false;
        }
        if (indent <= 0 && v3_startswith(trimmed, "var ")) {
            trimmed += 4;
        } else if (!in_var_block || indent <= 0) {
            continue;
        }
        if (!v3_parse_top_level_var_binding_text(trimmed,
                                                 name,
                                                 sizeof(name),
                                                 raw_type,
                                                 sizeof(raw_type),
                                                 &has_init,
                                                 expr,
                                                 sizeof(expr)) ||
            !has_init ||
            raw_type[0] == '\0' ||
            !v3_normalize_type_text(lowering,
                                    owner_module_path,
                                    aliases,
                                    alias_count,
                                    raw_type,
                                    normalized_type,
                                    sizeof(normalized_type))) {
            continue;
        }
        abi_class = v3_type_abi_class(normalized_type);
        v3_global_var_symbol_name(plan, owner_module_path, name, global_symbol, sizeof(global_symbol));
        if (v3_abi_class_scalar_or_ptr(abi_class)) {
            if (!v3_codegen_expr_scalar(plan,
                                        lowering,
                                        &synthetic_function,
                                        aliases,
                                        alias_count,
                                        locals,
                                        local_count,
                                        expr,
                                        abi_class,
                                        9,
                                        call_arg_base,
                                        string_temp_base,
                                        string_temp_stride,
                                        0,
                                        out,
                                        cap)) {
                fprintf(stderr,
                        "[cheng_v3_seed] emit module init scalar failed module=%s var=%s expr=%s abi=%s\n",
                        owner_module_path,
                        name,
                        expr,
                        abi_class);
                free(lines);
                free(owned);
                return false;
            }
            if (!v3_emit_symbol_address_for_target(plan, out, cap, 15, global_symbol)) {
                free(lines);
                free(owned);
                return false;
            }
            v3_emit_store_scalar_to_address(out, cap, 15, 0, abi_class, 9);
            continue;
        }
        if (!v3_emit_symbol_address_for_target(plan, out, cap, 15, global_symbol)) {
            free(lines);
            free(owned);
            return false;
        }
        if (!v3_materialize_composite_expr_into_address(plan,
                                                        lowering,
                                                        &synthetic_function,
                                                        aliases,
                                                        alias_count,
                                                        locals,
                                                        local_count,
                                                        expr,
                                                        normalized_type,
                                                        15,
                                                        call_arg_base,
                                                        string_temp_base,
                                                        string_temp_stride,
                                                        &string_label_index,
                                                        0,
                                                        out,
                                                        cap)) {
            fprintf(stderr,
                    "[cheng_v3_seed] emit module init composite failed module=%s var=%s expr=%s type=%s\n",
                    owner_module_path,
                    name,
                    expr,
                    normalized_type);
            free(lines);
            free(owned);
            return false;
        }
    }

    if (!v3_emit_function_epilogue(out, cap, frame_size)) {
        free(lines);
        free(owned);
        return false;
    }
    if (emitted_out) {
        *emitted_out = true;
    }
    free(lines);
    free(owned);
    return true;
}

static void v3_append_entry_bridge_with_module_inits(const V3SystemLinkPlanStub *plan,
                                                     char *out,
                                                     size_t cap,
                                                     const char *bridge_symbol,
                                                     const char *entry_symbol,
                                                     bool entry_returns_void,
                                                     char module_init_symbols[][PATH_MAX],
                                                     size_t module_init_count,
                                                     const char *end_label) {
    size_t i;
    char setcmd_symbol[PATH_MAX];
    v3_copy_target_symbol_name(plan, "__cheng_setCmdLine", setcmd_symbol, sizeof(setcmd_symbol));
    v3_text_appendf(out, cap,
                    ".globl %s\n"
                    ".extern %s\n"
                    "%s:\n",
                    bridge_symbol,
                    setcmd_symbol,
                    bridge_symbol);
    if (v3_target_is_linux_x86_64(plan->target_triple)) {
        v3_text_append(out, cap,
                       "  subq $24, %rsp\n"
                       "  movq %rdi, 0(%rsp)\n"
                       "  movq %rsi, 8(%rsp)\n");
    } else {
        v3_text_append(out, cap,
                       "  sub sp, sp, #32\n"
                       "  stp x0, x1, [sp]\n"
                       "  str x30, [sp, #24]\n");
    }
    if (v3_target_is_linux_x86_64(plan->target_triple)) {
        v3_text_appendf(out, cap, "  call %s\n", setcmd_symbol);
    } else {
        v3_text_appendf(out, cap, "  bl %s\n", setcmd_symbol);
    }
    if (module_init_count > 0U) {
        if (!v3_target_is_linux_x86_64(plan->target_triple)) {
            v3_text_append(out, cap,
                           "  ldr x30, [sp, #24]\n");
        }
        for (i = 0; i < module_init_count; ++i) {
            if (v3_target_is_linux_x86_64(plan->target_triple)) {
                v3_text_appendf(out, cap, "  call %s\n", module_init_symbols[i]);
            } else {
                v3_text_appendf(out, cap, "  bl %s\n", module_init_symbols[i]);
            }
        }
    }
    if (v3_target_is_linux_x86_64(plan->target_triple)) {
        v3_text_appendf(out, cap,
                        "  movq 0(%%rsp), %%rdi\n"
                        "  movq 8(%%rsp), %%rsi\n"
                        "  call %s\n"
                        "  addq $24, %%rsp\n"
                        "%s"
                        "  ret\n"
                        "%s:\n",
                        entry_symbol,
                        entry_returns_void ? "  xorl %eax, %eax\n" : "",
                        end_label);
    } else {
        v3_text_appendf(out, cap,
                        "  ldp x0, x1, [sp]\n"
                        "  bl %s\n"
                        "  ldr x30, [sp, #24]\n"
                        "  add sp, sp, #32\n"
                        "%s"
                        "  ret\n"
                        "%s:\n",
                        entry_symbol,
                        entry_returns_void ? "  mov x0, #0\n" : "",
                        end_label);
    }
}

static void v3_provider_source_for_module(const V3SystemLinkPlanStub *plan,
                                          const char *module_path,
                                          char *out,
                                          size_t cap) {
    if (v3_streq(module_path, "runtime/core_runtime_v3")) {
        if (v3_streq(plan->target_triple, "arm64-apple-darwin")) {
            v3_join_path(out,
                         cap,
                         plan->workspace_root,
                         "v3/src/runtime/core_runtime_provider_darwin_v3.cheng");
        } else {
            v3_join_path(out,
                         cap,
                         plan->workspace_root,
                         "v3/src/runtime/core_runtime_provider_linux_v3.cheng");
        }
        return;
    }
    if (v3_streq(module_path, "runtime/compiler_runtime_v3")) {
        if (v3_streq(plan->module_kind, "compiler_control_plane")) {
            v3_join_path(out,
                         cap,
                         plan->workspace_root,
                         "v3/src/runtime/compiler_runtime_tooling_entry_provider_v3.cheng");
        } else {
            v3_join_path(out,
                         cap,
                         plan->workspace_root,
                         "v3/src/runtime/compiler_runtime_program_entry_provider_v3.cheng");
        }
        return;
    }
    if (v3_streq(module_path, "runtime/debug_runtime_v3")) {
        v3_join_path(out,
                     cap,
                     plan->workspace_root,
                     "v3/src/runtime/debug_runtime_stub_v3.cheng");
        return;
    }
    if (v3_streq(module_path, "runtime/program_support_v3")) {
        v3_join_path(out, cap, plan->workspace_root, "v3/src/runtime/program_support_backend_v3.cheng");
        return;
    }
    if (v3_streq(module_path, "runtime/program_support_host_v3")) {
        v3_join_path(out, cap, plan->workspace_root, "v3/src/runtime/program_support_host_runtime_v3.cheng");
        return;
    }
    out[0] = '\0';
}

static bool CHENG_V3_UNUSED v3_body_kind_is_simple_object_compatible(const char *body_kind) {
    return body_kind != NULL &&
           (strcmp(body_kind, "return_zero_i32") == 0 ||
            strcmp(body_kind, "return_call_noarg_i32") == 0 ||
            strcmp(body_kind, "return_call_arg0_i32") == 0);
}

static bool v3_target_supports_primary_object_for_plan(const V3SystemLinkPlanStub *plan,
                                                       const V3LoweringPlanStub *lowering) {
    if (plan == NULL) {
        return false;
    }
    if (v3_target_supports_primary_object_codegen(plan->target_triple)) {
        return true;
    }
    (void)lowering;
    return false;
}

static bool v3_build_primary_object_plan_stub(const V3SystemLinkPlanStub *plan,
                                              const V3LoweringPlanStub *lowering,
                                              V3PrimaryObjectPlanStub *primary) {
    size_t i;
    const V3LoweredFunctionStub *entry_function;
    V3ConstEntryProgram consteval_entry;
    memset(primary, 0, sizeof(*primary));
    memset(&consteval_entry, 0, sizeof(consteval_entry));
    snprintf(primary->object_format,
             sizeof(primary->object_format),
             "%s",
             v3_target_is_darwin_arm64(plan->target_triple) ? "macho" :
             (v3_target_is_elf_aarch64(plan->target_triple) ||
              v3_target_is_linux_x86_64(plan->target_triple) ||
              v3_target_is_linux_riscv64(plan->target_triple)) ? "elf" :
             v3_target_is_windows_aarch64(plan->target_triple) ? "coff" :
             v3_target_is_wasm32_unknown_unknown(plan->target_triple) ? "wasm" : "unknown");
    snprintf(primary->output_path,
             sizeof(primary->output_path),
             "%s",
             v3_streq(plan->emit_kind, "obj") ?
                 plan->output_path :
                 "");
    if (!v3_streq(plan->emit_kind, "obj")) {
        snprintf(primary->output_path,
                 sizeof(primary->output_path),
                 "%s.primary.%s",
                 plan->output_path,
                 v3_target_is_wasm32_unknown_unknown(plan->target_triple) ? "wasm" : "o");
    }
    snprintf(primary->entry_path,
             sizeof(primary->entry_path),
             "%s",
             plan->entry_path);
    snprintf(primary->entry_symbol,
             sizeof(primary->entry_symbol),
             "%s",
             plan->entry_symbol);
    if (!v3_target_supports_primary_object_for_plan(plan, lowering)) {
        v3_primary_add_reason(primary, "primary_object_target_codegen_not_supported");
        if (plan->missing_reason_count > 0U) {
            v3_primary_add_reason(primary, "system_link_plan_not_ready_for_primary_object");
        }
        if (lowering->missing_reason_count > 0U) {
            v3_primary_add_reason(primary, "lowering_plan_not_ready_for_primary_object");
        }
        if (v3_streq(plan->emit_kind, "exe") && primary->entry_symbol[0] == '\0') {
            v3_primary_add_reason(primary, "primary_object_entry_symbol_missing");
        }
        return true;
    }
    entry_function = v3_find_entry_lowered_function(lowering);
    if (entry_function &&
        v3_streq(plan->emit_kind, "exe") &&
        !v3_plan_has_module_global_inits(plan) &&
        !v3_target_is_wasm32_unknown_unknown(plan->target_triple) &&
        v3_try_consteval_entry_program(plan, lowering, &consteval_entry)) {
        primary->consteval_entry_ready = true;
        if (v3_target_uses_consteval_builtin_executable(plan->target_triple)) {
            primary->output_path[0] = '\0';
        }
        primary->consteval_echo_count = consteval_entry.echo_count;
        primary->consteval_return_code = consteval_entry.return_code;
        for (i = 0; i < consteval_entry.echo_count; ++i) {
            snprintf(primary->consteval_echo_texts[i].text,
                     sizeof(primary->consteval_echo_texts[i].text),
                     "%s",
                     consteval_entry.echo_texts[i].text);
        }
        if (primary->item_count < CHENG_V3_MAX_PLAN_FUNCTIONS) {
            primary->item_ids[primary->item_count] = (int32_t)(100000 + (int32_t)primary->item_count);
            v3_primary_symbol_name(plan,
                                   entry_function,
                                   primary->symbol_names[primary->item_count].text,
                                   sizeof(primary->symbol_names[primary->item_count].text));
            primary->instruction_word_count += (consteval_entry.echo_count * 3U) + 2U;
            primary->data_label_count += consteval_entry.echo_count;
            primary->reloc_count += consteval_entry.echo_count * 2U;
            primary->item_count += 1U;
        } else {
            v3_primary_add_reason(primary, "primary_object_items_missing");
        }
        if (primary->item_count < CHENG_V3_MAX_PLAN_FUNCTIONS) {
            primary->item_ids[primary->item_count] = (int32_t)(900000 + (int32_t)primary->item_count);
            v3_runtime_entry_bridge_symbol(plan,
                                           primary->symbol_names[primary->item_count].text,
                                           sizeof(primary->symbol_names[primary->item_count].text));
            primary->instruction_word_count += 1U;
            primary->item_count += 1U;
        } else {
            v3_primary_add_reason(primary, "primary_object_entry_bridge_slot_missing");
        }
        if (plan->missing_reason_count > 0U) {
            v3_primary_add_reason(primary, "system_link_plan_not_ready_for_primary_object");
        }
        if (lowering->missing_reason_count > 0U) {
            v3_primary_add_reason(primary, "lowering_plan_not_ready_for_primary_object");
        }
        if (v3_streq(plan->emit_kind, "exe") && primary->entry_symbol[0] == '\0') {
            v3_primary_add_reason(primary, "primary_object_entry_symbol_missing");
        }
        return true;
    }
    for (i = 0; i < lowering->function_count && i < CHENG_V3_MAX_PLAN_FUNCTIONS; ++i) {
        if (v3_function_has_unbound_generics(lowering, &lowering->functions[i]) ||
            v3_function_has_obvious_template_text(&lowering->functions[i])) {
            continue;
        }
        primary->item_ids[primary->item_count] = (int32_t)(100000 + (int32_t)i);
        v3_primary_symbol_name(plan,
                               &lowering->functions[i],
                               primary->symbol_names[primary->item_count].text,
                               sizeof(primary->symbol_names[primary->item_count].text));
        if (strcmp(lowering->functions[i].body_kind, "return_zero_i32") == 0) {
            primary->instruction_word_count += 2U;
        } else if (strcmp(lowering->functions[i].body_kind, "return_call_noarg_i32") == 0) {
            V3AsmCallTarget target;
            bool target_ok = v3_resolve_return_call_noarg_i32_target(plan,
                                                                     lowering,
                                                                     &lowering->functions[i],
                                                                     &target);
            if (!target_ok && v3_target_is_wasm32_unknown_unknown(plan->target_triple)) {
                char wasm_symbol[PATH_MAX];
                wasm_symbol[0] = '\0';
                target_ok = v3_wasm_resolve_external_noarg_i32_from_source_path(plan,
                                                                                 lowering->functions[i].source_path,
                                                                                 lowering->functions[i].body_call_target,
                                                                                 wasm_symbol,
                                                                                 sizeof(wasm_symbol));
            }
            if (target_ok) {
                primary->instruction_word_count += 1U;
            } else {
                fprintf(stderr,
                        "[cheng_v3_seed] unresolved return_call_noarg target function=%s target=%s return_type=%s source=%s\n",
                        lowering->functions[i].symbol_text,
                        lowering->functions[i].body_call_target,
                        lowering->functions[i].return_type,
                        lowering->functions[i].source_path);
                v3_primary_add_reason(primary, "primary_object_call_target_missing");
            }
        } else if (strcmp(lowering->functions[i].body_kind, "return_call_arg0_i32") == 0) {
            V3AsmCallTarget target;
            if (v3_resolve_return_call_arg0_i32_target(plan,
                                                       lowering,
                                                       &lowering->functions[i],
                                                       &target)) {
                primary->instruction_word_count += 2U;
            } else {
                fprintf(stderr,
                        "[cheng_v3_seed] unresolved return_call_arg0 target function=%s target=%s return_type=%s source=%s\n",
                        lowering->functions[i].symbol_text,
                        lowering->functions[i].body_call_target,
                        lowering->functions[i].return_type,
                        lowering->functions[i].source_path);
                v3_primary_add_reason(primary, "primary_object_call_target_missing");
            }
        } else {
            size_t instruction_words = 0U;
            char *scratch = (char *)v3_xmalloc(1024U * 1024U);
            scratch[0] = '\0';
            if (v3_try_emit_scalar_function(plan,
                                            lowering,
                                            &lowering->functions[i],
                                            scratch,
                                            1024U * 1024U,
                                            &instruction_words)) {
                primary->instruction_word_count += instruction_words;
            } else {
                v3_primary_add_unsupported(primary, &lowering->functions[i]);
                primary->missing_reason_count += 0U;
            }
            free(scratch);
        }
        primary->item_count += 1U;
    }
    if (entry_function && v3_streq(plan->emit_kind, "exe")) {
        if (primary->item_count < CHENG_V3_MAX_PLAN_FUNCTIONS) {
            primary->item_ids[primary->item_count] = (int32_t)(900000 + (int32_t)primary->item_count);
            v3_runtime_entry_bridge_symbol(plan,
                                           primary->symbol_names[primary->item_count].text,
                                           sizeof(primary->symbol_names[primary->item_count].text));
            primary->instruction_word_count += 1U;
            primary->item_count += 1U;
        } else {
            v3_primary_add_reason(primary, "primary_object_entry_bridge_slot_missing");
        }
    }
    if (plan->missing_reason_count > 0U) {
        v3_primary_add_reason(primary, "system_link_plan_not_ready_for_primary_object");
    }
    if (lowering->missing_reason_count > 0U) {
        v3_primary_add_reason(primary, "lowering_plan_not_ready_for_primary_object");
    }
    if (primary->item_count <= 0U) {
        v3_primary_add_reason(primary, "primary_object_items_missing");
    }
    for (i = 0; i < lowering->function_count && i < CHENG_V3_MAX_PLAN_FUNCTIONS; ++i) {
        if (v3_function_has_unbound_generics(lowering, &lowering->functions[i]) ||
            v3_function_has_obvious_template_text(&lowering->functions[i])) {
            continue;
        }
        if (strcmp(lowering->functions[i].body_kind, "return_zero_i32") != 0 &&
            strcmp(lowering->functions[i].body_kind, "return_call_noarg_i32") != 0 &&
            strcmp(lowering->functions[i].body_kind, "return_call_arg0_i32") != 0) {
            char *scratch = (char *)v3_xmalloc(1024U * 1024U);
            bool ok;
            scratch[0] = '\0';
            ok = v3_try_emit_scalar_function(plan,
                                             lowering,
                                             &lowering->functions[i],
                                             scratch,
                                             1024U * 1024U,
                                             NULL);
            free(scratch);
            if (!ok) {
                v3_primary_add_unsupported(primary, &lowering->functions[i]);
                v3_primary_add_reason(primary, "primary_object_body_semantics_missing");
                break;
            }
        }
    }
    if (primary->instruction_word_count <= 0U) {
        v3_primary_add_reason(primary, "primary_object_machine_words_missing");
    }
    if (v3_streq(plan->emit_kind, "exe") && primary->entry_symbol[0] == '\0') {
        v3_primary_add_reason(primary, "primary_object_entry_symbol_missing");
    }
    return true;
}

static bool v3_build_object_plan_stub(const V3SystemLinkPlanStub *plan,
                                      const V3LoweringPlanStub *lowering,
                                      const V3PrimaryObjectPlanStub *primary,
                                      V3ObjectPlanStub *object_plan) {
    size_t i;
    bool consteval_builtin_direct = v3_streq(plan->emit_kind, "exe") &&
                                    v3_target_uses_consteval_builtin_executable(plan->target_triple) &&
                                    primary->consteval_entry_ready;
    memset(object_plan, 0, sizeof(*object_plan));
    snprintf(object_plan->primary_object_path,
             sizeof(object_plan->primary_object_path),
             "%s",
             primary->output_path);
    if (!consteval_builtin_direct &&
        object_plan->primary_object_path[0] != '\0') {
        v3_plan_add_path(object_plan->link_input_paths,
                         &object_plan->link_input_count,
                         CHENG_V3_MAX_PLAN_PATHS,
                         object_plan->primary_object_path);
    }
    if (plan->extra_link_input_path[0] != '\0') {
        v3_plan_add_path(object_plan->link_input_paths,
                         &object_plan->link_input_count,
                         CHENG_V3_MAX_PLAN_PATHS,
                         plan->extra_link_input_path);
    }
    if (v3_streq(plan->emit_kind, "exe") &&
        v3_target_uses_linux_nolibc_runtime(plan->target_triple)) {
        object_plan->linux_nolibc_support_enabled = true;
        v3_join_path(object_plan->linux_nolibc_runtime_source_path,
                     sizeof(object_plan->linux_nolibc_runtime_source_path),
                     plan->workspace_root,
                     "src/std/system_helpers_backend_nolibc_linux_aarch64.cheng");
        snprintf(object_plan->linux_nolibc_runtime_object_path,
                 sizeof(object_plan->linux_nolibc_runtime_object_path),
                 "%s.runtime.system_helpers_backend_nolibc_linux_aarch64.o",
                 plan->output_path);
        v3_join_path(object_plan->linux_nolibc_startup_source_path,
                     sizeof(object_plan->linux_nolibc_startup_source_path),
                     plan->workspace_root,
                     "v3/src/runtime/v3_linux_nolibc_aarch64_entry.S");
        snprintf(object_plan->linux_nolibc_startup_object_path,
                 sizeof(object_plan->linux_nolibc_startup_object_path),
                 "%s.startup.v3_linux_nolibc_aarch64_entry.o",
                 plan->output_path);
        v3_plan_add_path(object_plan->link_input_paths,
                         &object_plan->link_input_count,
                         CHENG_V3_MAX_PLAN_PATHS,
                         object_plan->linux_nolibc_startup_object_path);
        v3_plan_add_path(object_plan->link_input_paths,
                         &object_plan->link_input_count,
                         CHENG_V3_MAX_PLAN_PATHS,
                         object_plan->linux_nolibc_runtime_object_path);
    }
    for (i = 0; i < plan->provider_module_count; ++i) {
        char safe[PATH_MAX];
        char provider_source_path[PATH_MAX];
        char provider_object_path[PATH_MAX];
        v3_provider_source_for_module(plan,
                                      plan->provider_modules[i].text,
                                      provider_source_path,
                                      sizeof(provider_source_path));
        if (provider_source_path[0] != '\0') {
            v3_plan_add_path(object_plan->provider_source_paths,
                             &object_plan->provider_source_count,
                             CHENG_V3_MAX_PROVIDER_MODULES,
                             provider_source_path);
        }
        v3_sanitize_path_part(plan->provider_modules[i].text, safe, sizeof(safe));
        snprintf(provider_object_path,
                 sizeof(provider_object_path),
                 "%s.provider.%s.o",
                 plan->output_path,
                 safe);
        v3_plan_add_path(object_plan->provider_object_paths,
                         &object_plan->provider_object_count,
                         CHENG_V3_MAX_PROVIDER_MODULES,
                         provider_object_path);
        v3_plan_add_path(object_plan->link_input_paths,
                         &object_plan->link_input_count,
                         CHENG_V3_MAX_PLAN_PATHS,
                         provider_object_path);
    }
    if (plan->missing_reason_count > 0U) {
        v3_object_add_reason(object_plan, "system_link_plan_not_ready_for_object_plan");
    }
    if (lowering->missing_reason_count > 0U) {
        v3_object_add_reason(object_plan, "lowering_plan_not_ready_for_object_plan");
    }
    if (primary->missing_reason_count > 0U) {
        v3_object_add_reason(object_plan, "primary_object_not_ready_for_object_plan");
    }
    if (!consteval_builtin_direct &&
        object_plan->primary_object_path[0] == '\0') {
        v3_object_add_reason(object_plan, "primary_object_path_missing");
    }
    if (lowering->function_count <= 0U) {
        v3_object_add_reason(object_plan, "primary_object_functions_missing");
    }
    if (plan->provider_module_count > 0U &&
        object_plan->provider_object_count != plan->provider_module_count) {
        v3_object_add_reason(object_plan, "provider_object_paths_incomplete");
    }
    if (plan->provider_module_count > 0U &&
        object_plan->provider_source_count != plan->provider_module_count) {
        v3_object_add_reason(object_plan, "provider_source_paths_incomplete");
    }
    if (object_plan->linux_nolibc_support_enabled) {
        if (!v3_path_exists_nonempty(object_plan->linux_nolibc_runtime_source_path)) {
            v3_object_add_reason(object_plan, "linux_nolibc_runtime_source_missing");
        }
        if (!v3_path_exists_nonempty(object_plan->linux_nolibc_startup_source_path)) {
            v3_object_add_reason(object_plan, "linux_nolibc_startup_source_missing");
        }
        if (object_plan->linux_nolibc_runtime_object_path[0] == '\0') {
            v3_object_add_reason(object_plan, "linux_nolibc_runtime_object_path_missing");
        }
        if (object_plan->linux_nolibc_startup_object_path[0] == '\0') {
            v3_object_add_reason(object_plan, "linux_nolibc_startup_object_path_missing");
        }
    }
    return true;
}

static bool v3_path_is_directory(const char *path) {
    struct stat st;
    return path != NULL && path[0] != '\0' && stat(path, &st) == 0 && S_ISDIR(st.st_mode);
}

static bool v3_path_is_executable(const char *path) {
    return path != NULL && path[0] != '\0' && access(path, X_OK) == 0;
}

static bool v3_resolve_self_path(char *out, size_t cap) {
    char raw[PATH_MAX];
    char *resolved = NULL;
    if (out == NULL || cap == 0U) {
        return false;
    }
    out[0] = '\0';
#if defined(__APPLE__)
    {
        uint32_t size = (uint32_t)sizeof(raw);
        if (_NSGetExecutablePath(raw, &size) == 0) {
            resolved = realpath(raw, out);
            if (resolved != NULL) {
                return true;
            }
            if (strlen(raw) < cap) {
                snprintf(out, cap, "%s", raw);
                return true;
            }
        }
    }
#elif !defined(_WIN32)
    {
        ssize_t n = readlink("/proc/self/exe", raw, sizeof(raw) - 1U);
        if (n > 0 && (size_t)n < sizeof(raw)) {
            raw[n] = '\0';
            resolved = realpath(raw, out);
            if (resolved != NULL) {
                return true;
            }
            if ((size_t)n < cap) {
                snprintf(out, cap, "%s", raw);
                return true;
            }
        }
    }
#endif
    return false;
}

static bool v3_bootstrap_current_compiler_path(const V3BootstrapPaths *paths,
                                               char *out,
                                               size_t cap) {
    if (paths == NULL || out == NULL || cap == 0U) {
        return false;
    }
    out[0] = '\0';
    if (v3_resolve_self_path(out, cap) && v3_path_is_executable(out)) {
        return true;
    }
    if (v3_path_is_executable(paths->stage3)) {
        snprintf(out, cap, "%s", paths->stage3);
        return true;
    }
    if (v3_path_is_executable(paths->stage2)) {
        snprintf(out, cap, "%s", paths->stage2);
        return true;
    }
    if (v3_path_is_executable(paths->stage1)) {
        snprintf(out, cap, "%s", paths->stage1);
        return true;
    }
    if (v3_path_is_executable(paths->stage0)) {
        snprintf(out, cap, "%s", paths->stage0);
        return true;
    }
    return false;
}

static bool v3_bootstrap_materialize_stage0(const V3BootstrapPaths *paths,
                                            const char *compiler_path,
                                            const char *output_path) {
    char note[PATH_MAX * 2U];
    char *quoted_compiler = NULL;
    char *quoted_output = NULL;
    char command[PATH_MAX * 4U];
    if (paths == NULL || compiler_path == NULL || compiler_path[0] == '\0' ||
        output_path == NULL || output_path[0] == '\0') {
        return false;
    }
    if (!v3_path_is_executable(compiler_path)) {
        return false;
    }
    if (strcmp(compiler_path, paths->stage0) == 0) {
        if (strcmp(output_path, compiler_path) == 0) {
            snprintf(note, sizeof(note), "stage0_reused_from=%s\n", compiler_path);
            return v3_write_text_file(paths->stage0_build_log, note);
        }
        unlink(output_path);
        if (!v3_copy_file_bytes(compiler_path, output_path)) {
            return false;
        }
        if (chmod(output_path, 0755) != 0) {
            perror("chmod");
            return false;
        }
        snprintf(note, sizeof(note), "stage0_candidate_copied_from=%s\n", compiler_path);
        return v3_write_text_file(paths->stage0_build_log, note);
    }
    {
        quoted_compiler = v3_shell_quote(compiler_path);
        quoted_output = v3_shell_quote(output_path);
        if (quoted_compiler == NULL || quoted_output == NULL) {
            free(quoted_compiler);
            free(quoted_output);
            return false;
        }
        snprintf(command,
                 sizeof(command),
                 "rm -f %s && ln -s %s %s",
                 quoted_output,
                 quoted_compiler,
                 quoted_output);
        free(quoted_compiler);
        free(quoted_output);
        if (!v3_run_shell_command_logged(command,
                                         paths->stage0_build_log,
                                         "bootstrap-bridge stage0-symlink")) {
            return false;
        }
        snprintf(note, sizeof(note), "stage0_symlinked_from=%s\n", compiler_path);
        return v3_write_text_file(paths->stage0_build_log, note);
    }
}

static bool CHENG_V3_UNUSED v3_resolve_executable_program(const char *program, char *out, size_t cap) {
    const char *path_env;
    char *copy;
    char *cursor;
    if (!program || program[0] == '\0') {
        return false;
    }
    if (strchr(program, '/') != NULL) {
        if (!v3_path_is_executable(program)) {
            return false;
        }
        snprintf(out, cap, "%s", program);
        return true;
    }
    path_env = getenv("PATH");
    if (!path_env || path_env[0] == '\0') {
        return false;
    }
    copy = (char *)v3_xmalloc(strlen(path_env) + 1U);
    strcpy(copy, path_env);
    cursor = copy;
    while (cursor != NULL) {
        char candidate[PATH_MAX];
        char *next = strchr(cursor, ':');
        if (next != NULL) {
            *next = '\0';
        }
        if (cursor[0] == '\0') {
            v3_join_path(candidate, sizeof(candidate), ".", program);
        } else {
            v3_join_path(candidate, sizeof(candidate), cursor, program);
        }
        if (v3_path_is_executable(candidate)) {
            snprintf(out, cap, "%s", candidate);
            free(copy);
            return true;
        }
        cursor = next != NULL ? next + 1 : NULL;
    }
    free(copy);
    return false;
}

static const char *CHENG_V3_UNUSED v3_linux_nolibc_linker_flavor_for_program(const char *program) {
    const char *name = strrchr(program != NULL ? program : "", '/');
    name = name != NULL ? name + 1 : program;
    if (name == NULL) {
        return NULL;
    }
    if (strcmp(name, "aarch64-linux-gnu-gcc") == 0) {
        return "gnu_gcc_driver";
    }
    if (strcmp(name, "aarch64-linux-gnu-ld") == 0) {
        return "gnu_ld";
    }
    if (strcmp(name, "ld.lld") == 0) {
        return "ld.lld";
    }
    if (strcmp(name, "llvm-lld") == 0) {
        return "llvm-lld";
    }
    if (strcmp(name, "lld") == 0) {
        return "lld";
    }
    return NULL;
}

static int v3_compare_version_like_text(const char *left, const char *right) {
    const char *a = left != NULL ? left : "";
    const char *b = right != NULL ? right : "";
    while (*a != '\0' || *b != '\0') {
        unsigned long long av = 0ULL;
        unsigned long long bv = 0ULL;
        while (*a != '\0' && !isdigit((unsigned char)*a)) {
            a += 1;
        }
        while (*b != '\0' && !isdigit((unsigned char)*b)) {
            b += 1;
        }
        while (isdigit((unsigned char)*a)) {
            av = av * 10ULL + (unsigned long long)(*a - '0');
            a += 1;
        }
        while (isdigit((unsigned char)*b)) {
            bv = bv * 10ULL + (unsigned long long)(*b - '0');
            b += 1;
        }
        if (av != bv) {
            return av < bv ? -1 : 1;
        }
        while (*a != '\0' && *a != '.') {
            a += 1;
        }
        while (*b != '\0' && *b != '.') {
            b += 1;
        }
        if (*a == '.') {
            a += 1;
        }
        if (*b == '.') {
            b += 1;
        }
    }
    return 0;
}

static bool v3_find_latest_directory_under(const char *root, char *out, size_t cap) {
    DIR *dir;
    struct dirent *entry;
    char best_path[PATH_MAX];
    char best_name[256];
    bool found = false;
    if (!v3_path_is_directory(root)) {
        return false;
    }
    dir = opendir(root);
    if (!dir) {
        return false;
    }
    best_path[0] = '\0';
    best_name[0] = '\0';
    while ((entry = readdir(dir)) != NULL) {
        char candidate[PATH_MAX];
        if (entry->d_name[0] == '.') {
            continue;
        }
        v3_join_path(candidate, sizeof(candidate), root, entry->d_name);
        if (!v3_path_is_directory(candidate)) {
            continue;
        }
        if (!found || v3_compare_version_like_text(entry->d_name, best_name) > 0) {
            snprintf(best_name, sizeof(best_name), "%s", entry->d_name);
            snprintf(best_path, sizeof(best_path), "%s", candidate);
            found = true;
        }
    }
    closedir(dir);
    if (!found) {
        return false;
    }
    snprintf(out, cap, "%s", best_path);
    return true;
}

static bool v3_resolve_android_ndk_root(char *out, size_t cap) {
    static const char *NDK_ENVS[] = {"ANDROID_NDK_HOME", "ANDROID_NDK_ROOT"};
    static const char *SDK_ENVS[] = {"ANDROID_SDK_ROOT", "ANDROID_HOME"};
    size_t i;
    for (i = 0; i < sizeof(NDK_ENVS) / sizeof(NDK_ENVS[0]); ++i) {
        const char *value = getenv(NDK_ENVS[i]);
        if (v3_path_is_directory(value)) {
            snprintf(out, cap, "%s", value);
            return true;
        }
    }
    for (i = 0; i < sizeof(SDK_ENVS) / sizeof(SDK_ENVS[0]); ++i) {
        const char *value = getenv(SDK_ENVS[i]);
        char ndk_root[PATH_MAX];
        if (!v3_path_is_directory(value)) {
            continue;
        }
        v3_join_path(ndk_root, sizeof(ndk_root), value, "ndk");
        if (v3_find_latest_directory_under(ndk_root, out, cap)) {
            return true;
        }
    }
    {
        const char *home = getenv("HOME");
        char sdk_root[PATH_MAX];
        char ndk_root[PATH_MAX];
        if (!home || home[0] == '\0') {
            return false;
        }
        v3_join_path(sdk_root, sizeof(sdk_root), home, "Library/Android/sdk");
        v3_join_path(ndk_root, sizeof(ndk_root), sdk_root, "ndk");
        return v3_find_latest_directory_under(ndk_root, out, cap);
    }
}

static bool v3_resolve_android_llvm_bin_dir(char *out, size_t cap) {
    char ndk_root[PATH_MAX];
    char prebuilt_root[PATH_MAX];
    char candidate[PATH_MAX];
    if (!v3_resolve_android_ndk_root(ndk_root, sizeof(ndk_root))) {
        return false;
    }
    v3_join_path(prebuilt_root, sizeof(prebuilt_root), ndk_root, "toolchains/llvm/prebuilt");
    if (!v3_path_is_directory(prebuilt_root)) {
        return false;
    }
    v3_join_path(candidate, sizeof(candidate), prebuilt_root, "darwin-arm64/bin");
    if (v3_path_is_directory(candidate)) {
        snprintf(out, cap, "%s", candidate);
        return true;
    }
    v3_join_path(candidate, sizeof(candidate), prebuilt_root, "darwin-x86_64/bin");
    if (v3_path_is_directory(candidate)) {
        snprintf(out, cap, "%s", candidate);
        return true;
    }
    {
        DIR *dir = opendir(prebuilt_root);
        struct dirent *entry;
        if (!dir) {
            return false;
        }
        while ((entry = readdir(dir)) != NULL) {
            char bin_dir[PATH_MAX];
            char clang_path[PATH_MAX];
            if (entry->d_name[0] == '.') {
                continue;
            }
            v3_join_path(bin_dir, sizeof(bin_dir), prebuilt_root, entry->d_name);
            v3_join_path(bin_dir, sizeof(bin_dir), bin_dir, "bin");
            v3_join_path(clang_path, sizeof(clang_path), bin_dir, "aarch64-linux-android24-clang");
            if (v3_path_is_executable(clang_path)) {
                closedir(dir);
                snprintf(out, cap, "%s", bin_dir);
                return true;
            }
        }
        closedir(dir);
    }
    return false;
}

static bool v3_resolve_ohos_llvm_bin_dir(char *out, size_t cap) {
    const char *clang = getenv("OH_CLANG");
    const char *llvm_root = getenv("OH_LLVM_ROOT");
    char candidate[PATH_MAX];
    if (v3_path_is_executable(clang)) {
        snprintf(out, cap, "%s", clang);
        return true;
    }
    if (v3_path_is_directory(llvm_root)) {
        v3_join_path(candidate, sizeof(candidate), llvm_root, "aarch64-unknown-linux-ohos-clang");
        if (v3_path_is_executable(candidate)) {
            snprintf(out, cap, "%s", candidate);
            return true;
        }
    }
    snprintf(candidate,
             sizeof(candidate),
             "%s",
             "/Applications/DevEco-Studio.app/Contents/sdk/default/openharmony/native/llvm/bin/aarch64-unknown-linux-ohos-clang");
    if (v3_path_is_executable(candidate)) {
        snprintf(out, cap, "%s", candidate);
        return true;
    }
    return false;
}

static bool v3_resolve_target_cc_program(const char *target_triple, char *out, size_t cap) {
    const char *cc = getenv("CC");
    if (v3_target_is_android_aarch64(target_triple)) {
        char llvm_bin_dir[PATH_MAX];
        char clang_path[PATH_MAX];
        if (!v3_resolve_android_llvm_bin_dir(llvm_bin_dir, sizeof(llvm_bin_dir))) {
            return false;
        }
        v3_join_path(clang_path, sizeof(clang_path), llvm_bin_dir, "aarch64-linux-android24-clang");
        if (!v3_path_is_executable(clang_path)) {
            return false;
        }
        snprintf(out, cap, "%s", clang_path);
        return true;
    }
    if (v3_target_is_ohos_aarch64(target_triple)) {
        return v3_resolve_ohos_llvm_bin_dir(out, cap);
    }
    snprintf(out, cap, "%s", (cc && *cc != '\0') ? cc : "cc");
    return true;
}

static bool v3_build_native_link_plan_stub(const V3SystemLinkPlanStub *plan,
                                           const V3PrimaryObjectPlanStub *primary,
                                           const V3ObjectPlanStub *object_plan,
                                           V3NativeLinkPlanStub *native_link) {
    size_t i;
    memset(native_link, 0, sizeof(*native_link));
    snprintf(native_link->target_triple,
             sizeof(native_link->target_triple),
             "%s",
             plan->target_triple);
    if (v3_target_is_wasm32_unknown_unknown(plan->target_triple)) {
        snprintf(native_link->linker_program,
                 sizeof(native_link->linker_program),
                 "%s",
                 "internal_wasm_link");
    } else if (v3_target_is_linux_riscv64(plan->target_triple)) {
        snprintf(native_link->linker_program,
                 sizeof(native_link->linker_program),
                 "%s",
                 "internal_elf_consteval_linker");
        snprintf(native_link->linker_flavor,
                 sizeof(native_link->linker_flavor),
                 "%s",
                 "elf_riscv64_consteval_internal");
    } else if (v3_target_is_windows_aarch64(plan->target_triple)) {
        snprintf(native_link->linker_program,
                 sizeof(native_link->linker_program),
                 "%s",
                 "internal_pe_consteval_linker");
        snprintf(native_link->linker_flavor,
                 sizeof(native_link->linker_flavor),
                 "%s",
                 "pe_aarch64_consteval_internal");
    } else if (v3_target_uses_linux_nolibc_runtime(plan->target_triple)) {
        snprintf(native_link->linker_program,
                 sizeof(native_link->linker_program),
                 "%s",
                 "internal_elf_linker");
        snprintf(native_link->linker_flavor,
                 sizeof(native_link->linker_flavor),
                 "%s",
                 "elf_aarch64_nolibc_internal");
    } else if (!v3_resolve_target_cc_program(plan->target_triple,
                                             native_link->linker_program,
                                             sizeof(native_link->linker_program))) {
        snprintf(native_link->linker_program,
                 sizeof(native_link->linker_program),
                 "%s",
                 "cc");
        if (v3_target_is_android_aarch64(plan->target_triple)) {
            v3_native_link_add_reason(native_link, "android_toolchain_missing");
        } else if (v3_target_is_ohos_aarch64(plan->target_triple)) {
            v3_native_link_add_reason(native_link, "ohos_toolchain_missing");
        }
    }
    if (native_link->linker_flavor[0] == '\0') {
        snprintf(native_link->linker_flavor,
                 sizeof(native_link->linker_flavor),
                 "%s",
                 v3_target_is_darwin_arm64(plan->target_triple) ? "ld64" :
                 v3_target_is_android_aarch64(plan->target_triple) ? "android_clang" :
                 v3_target_is_ohos_aarch64(plan->target_triple) ? "ohos_clang" :
                 v3_target_is_wasm32_unknown_unknown(plan->target_triple) ? "wasm_internal" :
                 "system_linker");
    }
    snprintf(native_link->output_kind,
             sizeof(native_link->output_kind),
             "%s",
             v3_streq(plan->emit_kind, "exe") ? "executable" : "artifact");
    snprintf(native_link->primary_object_path,
             sizeof(native_link->primary_object_path),
             "%s",
             object_plan->primary_object_path);
    snprintf(native_link->entry_symbol,
             sizeof(native_link->entry_symbol),
             "%s",
             plan->entry_symbol[0] != '\0' ? plan->entry_symbol : "");
    if (primary != NULL && primary->consteval_entry_ready &&
        v3_target_uses_consteval_builtin_executable(plan->target_triple) &&
        v3_streq(plan->emit_kind, "exe")) {
        native_link->consteval_builtin_ready = true;
        native_link->consteval_echo_count = primary->consteval_echo_count;
        native_link->consteval_return_code = primary->consteval_return_code;
        for (i = 0U; i < primary->consteval_echo_count; ++i) {
            snprintf(native_link->consteval_echo_texts[i].text,
                     sizeof(native_link->consteval_echo_texts[i].text),
                     "%s",
                     primary->consteval_echo_texts[i].text);
        }
    }
    for (i = 0; i < object_plan->link_input_count; ++i) {
        v3_plan_add_path(native_link->link_input_paths,
                         &native_link->link_input_count,
                         CHENG_V3_MAX_PLAN_PATHS,
                         object_plan->link_input_paths[i].text);
    }
    if (plan->missing_reason_count > 0U) {
        v3_native_link_add_reason(native_link, "system_link_plan_not_ready_for_native_link");
    }
    if (!v3_target_supports_native_link_for_emit(plan->target_triple, plan->emit_kind)) {
        v3_native_link_add_reason(native_link, "native_link_target_codegen_not_supported");
    }
    if (object_plan->missing_reason_count > 0U) {
        v3_native_link_add_reason(native_link, "object_plan_not_ready_for_native_link");
    }
    if (native_link->link_input_count <= 0U &&
        !native_link->consteval_builtin_ready) {
        v3_native_link_add_reason(native_link, "native_link_inputs_missing");
    }
    if (plan->output_path[0] == '\0') {
        v3_native_link_add_reason(native_link, "native_link_output_missing");
    }
    if (v3_streq(plan->emit_kind, "exe") && native_link->entry_symbol[0] == '\0') {
        v3_native_link_add_reason(native_link, "native_link_entry_symbol_missing");
    }
    if (v3_target_uses_consteval_builtin_executable(plan->target_triple) &&
        v3_streq(plan->emit_kind, "exe") &&
        !native_link->consteval_builtin_ready) {
        v3_native_link_add_reason(native_link, "native_link_consteval_entry_missing");
    }
    return true;
}

typedef struct {
    unsigned char *data;
    size_t len;
    size_t cap;
} V3WasmBuffer;

typedef struct {
    V3WasmBuffer bytes;
    int32_t base_addr;
    bool initialized;
} V3WasmStringLiteralPool;

static V3WasmStringLiteralPool g_v3_wasm_string_literal_pool;

static bool v3_wasm_append_memory_arg(V3WasmBuffer *body,
                                      uint32_t align,
                                      uint32_t offset);

static void v3_wasm_buffer_init(V3WasmBuffer *buf) {
    memset(buf, 0, sizeof(*buf));
}

static void v3_wasm_buffer_free(V3WasmBuffer *buf) {
    free(buf->data);
    buf->data = NULL;
    buf->len = 0U;
    buf->cap = 0U;
}

static bool v3_wasm_buffer_reserve(V3WasmBuffer *buf, size_t extra) {
    unsigned char *next;
    size_t need = buf->len + extra;
    size_t cap = buf->cap > 0U ? buf->cap : 256U;
    if (need <= buf->cap) {
        return true;
    }
    while (cap < need) {
        cap *= 2U;
    }
    next = (unsigned char *)realloc(buf->data, cap);
    if (!next) {
        fprintf(stderr, "[cheng_v3_seed] wasm buffer alloc failed size=%zu\n", cap);
        return false;
    }
    buf->data = next;
    buf->cap = cap;
    return true;
}

static bool v3_wasm_append_u8(V3WasmBuffer *buf, unsigned char byte) {
    if (!v3_wasm_buffer_reserve(buf, 1U)) {
        return false;
    }
    buf->data[buf->len++] = byte;
    return true;
}

static bool v3_wasm_append_bytes(V3WasmBuffer *buf, const unsigned char *data, size_t len) {
    if (len <= 0U) {
        return true;
    }
    if (!v3_wasm_buffer_reserve(buf, len)) {
        return false;
    }
    memcpy(buf->data + buf->len, data, len);
    buf->len += len;
    return true;
}

static bool v3_wasm_append_uleb_u32(V3WasmBuffer *buf, uint32_t value) {
    do {
        unsigned char byte = (unsigned char)(value & 0x7fU);
        value >>= 7U;
        if (value != 0U) {
            byte |= 0x80U;
        }
        if (!v3_wasm_append_u8(buf, byte)) {
            return false;
        }
    } while (value != 0U);
    return true;
}

static bool v3_wasm_append_name(V3WasmBuffer *buf, const char *text) {
    size_t len = text != NULL ? strlen(text) : 0U;
    if (!v3_wasm_append_uleb_u32(buf, (uint32_t)len)) {
        return false;
    }
    return v3_wasm_append_bytes(buf, (const unsigned char *)(text != NULL ? text : ""), len);
}

static bool v3_wasm_function_should_export(const V3SystemLinkPlanStub *plan,
                                           const V3LoweringPlanStub *lowering,
                                           const V3LoweredFunctionStub *function) {
    bool entry_has_explicit_exports;
    if (plan == NULL || function == NULL) {
        return false;
    }
    entry_has_explicit_exports = v3_lowering_entry_has_explicit_exports(plan, lowering);
    if ((v3_streq(plan->emit_kind, "shared") || v3_streq(plan->emit_kind, "obj")) &&
        plan->entry_path[0] != '\0') {
        return v3_lowered_function_is_entry_export_root(plan,
                                                        lowering,
                                                        function,
                                                        entry_has_explicit_exports);
    }
    return true;
}

static bool v3_wasm_append_section(V3WasmBuffer *module,
                                   unsigned char section_id,
                                   const V3WasmBuffer *payload) {
    if (!v3_wasm_append_u8(module, section_id)) {
        return false;
    }
    if (!v3_wasm_append_uleb_u32(module, (uint32_t)payload->len)) {
        return false;
    }
    return v3_wasm_append_bytes(module, payload->data, payload->len);
}

static bool v3_wasm_append_sleb_i32(V3WasmBuffer *buf, int32_t value);

static void v3_wasm_string_literal_pool_reset(int32_t base_addr) {
    if (g_v3_wasm_string_literal_pool.initialized) {
        v3_wasm_buffer_free(&g_v3_wasm_string_literal_pool.bytes);
    }
    v3_wasm_buffer_init(&g_v3_wasm_string_literal_pool.bytes);
    g_v3_wasm_string_literal_pool.base_addr = base_addr;
    g_v3_wasm_string_literal_pool.initialized = true;
}

static void v3_wasm_string_literal_pool_dispose(void) {
    if (g_v3_wasm_string_literal_pool.initialized) {
        v3_wasm_buffer_free(&g_v3_wasm_string_literal_pool.bytes);
        memset(&g_v3_wasm_string_literal_pool, 0, sizeof(g_v3_wasm_string_literal_pool));
    }
}

static bool v3_wasm_register_string_literal(const char *text,
                                            int32_t *addr_out,
                                            int32_t *len_out) {
    size_t len = 0U;
    if (text == NULL || addr_out == NULL || !g_v3_wasm_string_literal_pool.initialized) {
        return false;
    }
    len = strlen(text);
    if (g_v3_wasm_string_literal_pool.bytes.len > (size_t)INT32_MAX - len) {
        return false;
    }
    *addr_out = g_v3_wasm_string_literal_pool.base_addr + (int32_t)g_v3_wasm_string_literal_pool.bytes.len;
    if (*addr_out < 0 || *addr_out > (1024 * 1024) - (int32_t)len) {
        return false;
    }
    if (len_out != NULL) {
        *len_out = (int32_t)len;
    }
    if (len == 0U) {
        return true;
    }
    return v3_wasm_append_bytes(&g_v3_wasm_string_literal_pool.bytes,
                                (const unsigned char *)text,
                                len);
}

static bool v3_wasm_literal_pool_append_u32le(uint32_t value) {
    unsigned char bytes[4];
    bytes[0] = (unsigned char)(value & 0xffU);
    bytes[1] = (unsigned char)((value >> 8) & 0xffU);
    bytes[2] = (unsigned char)((value >> 16) & 0xffU);
    bytes[3] = (unsigned char)((value >> 24) & 0xffU);
    return v3_wasm_append_bytes(&g_v3_wasm_string_literal_pool.bytes, bytes, sizeof(bytes));
}

static bool v3_wasm_register_string_literal_object(const char *text,
                                                   int32_t *object_addr_out,
                                                   int32_t *data_addr_out,
                                                   int32_t *len_out) {
    int32_t data_addr = 0;
    int32_t literal_len = 0;
    while (g_v3_wasm_string_literal_pool.initialized &&
           (g_v3_wasm_string_literal_pool.bytes.len % 4U) != 0U) {
        if (!v3_wasm_append_u8(&g_v3_wasm_string_literal_pool.bytes, 0U)) {
            return false;
        }
    }
    if (!v3_wasm_register_string_literal(text, &data_addr, &literal_len)) {
        return false;
    }
    while ((g_v3_wasm_string_literal_pool.bytes.len % 4U) != 0U) {
        if (!v3_wasm_append_u8(&g_v3_wasm_string_literal_pool.bytes, 0U)) {
            return false;
        }
    }
    if (object_addr_out != NULL) {
        *object_addr_out = g_v3_wasm_string_literal_pool.base_addr + (int32_t)g_v3_wasm_string_literal_pool.bytes.len;
    }
    if (data_addr_out != NULL) {
        *data_addr_out = data_addr;
    }
    if (len_out != NULL) {
        *len_out = literal_len;
    }
    return v3_wasm_literal_pool_append_u32le((uint32_t)data_addr) &&
           v3_wasm_literal_pool_append_u32le((uint32_t)literal_len) &&
           v3_wasm_literal_pool_append_u32le(0U) &&
           v3_wasm_literal_pool_append_u32le(0U);
}

static bool v3_wasm_append_string_literal_data_section(V3WasmBuffer *module) {
    V3WasmBuffer payload;
    bool ok = true;
    if (module == NULL ||
        !g_v3_wasm_string_literal_pool.initialized ||
        g_v3_wasm_string_literal_pool.bytes.len == 0U) {
        return true;
    }
    v3_wasm_buffer_init(&payload);
    ok = v3_wasm_append_uleb_u32(&payload, 1U) &&
         v3_wasm_append_u8(&payload, 0x00U) &&
         v3_wasm_append_u8(&payload, 0x41U) &&
         v3_wasm_append_sleb_i32(&payload, g_v3_wasm_string_literal_pool.base_addr) &&
         v3_wasm_append_u8(&payload, 0x0bU) &&
         v3_wasm_append_uleb_u32(&payload, (uint32_t)g_v3_wasm_string_literal_pool.bytes.len) &&
         v3_wasm_append_bytes(&payload,
                              g_v3_wasm_string_literal_pool.bytes.data,
                              g_v3_wasm_string_literal_pool.bytes.len) &&
         v3_wasm_append_section(module, 11U, &payload);
    v3_wasm_buffer_free(&payload);
    return ok;
}

typedef struct {
    char symbol_name[PATH_MAX];
    uint32_t type_index;
} V3WasmImportStub;

typedef struct {
    V3AsmLocalSlot locals[CHENG_V3_MAX_ASM_LOCALS];
    uint32_t local_indices[CHENG_V3_MAX_ASM_LOCALS];
    size_t local_count;
    size_t param_count;
    V3ImportAlias aliases[CHENG_V3_MAX_IMPORT_ALIASES];
    size_t alias_count;
    int32_t frame_size;
    int32_t str_temp0_offset;
    int32_t str_temp1_offset;
    uint32_t frame_base_local_index;
    uint32_t scratch_local_index;
    uint32_t scratch2_local_index;
    uint32_t scratch3_local_index;
    uint32_t scratch4_local_index;
    uint32_t scratch5_local_index;
    bool has_frame_base_local;
    bool has_scratch_locals;
    bool needs_str_temps;
} V3WasmFunctionContext;

static bool v3_wasm_register_import(const char *function_symbol,
                                    const V3AsmCallTarget *target,
                                    V3WasmImportStub *imports,
                                    size_t *import_count);

static bool v3_wasm_analyze_expr(const V3SystemLinkPlanStub *plan,
                                 const V3LoweringPlanStub *lowering,
                                 const V3LoweredFunctionStub *function,
                                 V3WasmFunctionContext *ctx,
                                 const char *expr_text,
                                 V3WasmImportStub *imports,
                                 size_t *import_count);

#define CHENG_V3_WASM_STATIC_BASE_I32 4096
#define CHENG_V3_WASM_STACK_INIT_I32 (1024 * 1024)
#define CHENG_V3_WASM_STACK_GLOBAL_INDEX 0U

static bool v3_wasm_abi_uses_i32_value(const char *abi_class) {
    return v3_abi_descriptor_from_class(abi_class).wasm_i32_value;
}

static bool v3_wasm_abi_is_void(const char *abi_class) {
    return v3_abi_descriptor_from_class(abi_class).value_kind == V3_ABI_VALUE_VOID;
}

static bool v3_wasm_abi_uses_i32_return_slot(const char *abi_class) {
    return v3_abi_descriptor_from_class(abi_class).wasm_i32_return_slot;
}

static bool v3_wasm_abi_uses_i32_param_slot(const char *abi_class) {
    return v3_abi_descriptor_from_class(abi_class).wasm_i32_param_slot;
}

static bool v3_wasm_function_return_supported(const char *abi_class) {
    return v3_wasm_abi_is_void(abi_class) ||
           v3_wasm_abi_uses_i32_return_slot(abi_class);
}

static bool v3_wasm_signature_has_result(const char *return_abi_class) {
    return !v3_wasm_abi_is_void(return_abi_class);
}

static uint32_t v3_wasm_signature_type_slot(size_t param_count,
                                            bool has_result) {
    return (uint32_t)(param_count * 2U + (has_result ? 1U : 0U));
}

static uint32_t v3_wasm_signature_param_count_from_type_index(uint32_t type_index) {
    return type_index / 2U;
}

static bool v3_wasm_signature_type_index(size_t param_count,
                                         const char *return_abi_class,
                                         const char param_abi_classes[][32],
                                         uint32_t *type_index_out) {
    size_t i;
    if (return_abi_class == NULL || type_index_out == NULL) {
        return false;
    }
    if (!v3_wasm_function_return_supported(return_abi_class)) {
        return false;
    }
    if (param_count > CHENG_V3_MAX_CALL_ARGS) {
        return false;
    }
    for (i = 0U; i < param_count; ++i) {
        if (!v3_wasm_abi_uses_i32_param_slot(param_abi_classes[i])) {
            return false;
        }
    }
    *type_index_out = v3_wasm_signature_type_slot(param_count,
                                                  v3_wasm_signature_has_result(return_abi_class));
    return true;
}

static uint32_t v3_wasm_max_signature_param_count(const V3LoweringPlanStub *lowering,
                                                  const V3WasmImportStub *imports,
                                                  size_t import_count) {
    uint32_t max_param_count = 0U;
    size_t i;
    if (lowering != NULL) {
        for (i = 0U; i < lowering->function_count; ++i) {
            if (lowering->functions[i].param_count > max_param_count) {
                max_param_count = (uint32_t)lowering->functions[i].param_count;
            }
        }
    }
    for (i = 0U; i < import_count; ++i) {
        uint32_t param_count = v3_wasm_signature_param_count_from_type_index(imports[i].type_index);
        if (param_count > max_param_count) {
            max_param_count = param_count;
        }
    }
    return max_param_count;
}

static int32_t v3_wasm_find_import_index(const V3WasmImportStub *imports,
                                         size_t import_count,
                                         const char *symbol_name,
                                         uint32_t type_index);

static void v3_wasm_debug_dump_imports(const V3WasmImportStub *imports,
                                       size_t import_count);

static bool v3_wasm_append_memory_arg(V3WasmBuffer *body,
                                      uint32_t align_log2,
                                      uint32_t offset);

static bool v3_wasm_append_sleb_i32(V3WasmBuffer *buf, int32_t value) {
    bool more = true;
    while (more) {
        unsigned char byte = (unsigned char)(value & 0x7f);
        const bool sign_bit = (byte & 0x40U) != 0U;
        value >>= 7;
        if ((value == 0 && !sign_bit) || (value == -1 && sign_bit)) {
            more = false;
        } else {
            byte |= 0x80U;
        }
        if (!v3_wasm_append_u8(buf, byte)) {
            return false;
        }
    }
    return true;
}

static bool v3_wasm_trim_wrapping_parens(const char *text,
                                         char *out,
                                         size_t cap) {
    char trimmed[4096];
    bool changed = false;
    v3_trim_copy_text(text, trimmed, sizeof(trimmed));
    while (trimmed[0] == '(') {
        size_t len = strlen(trimmed);
        size_t i;
        int32_t paren_depth = 0;
        bool in_string = false;
        bool wraps = len > 1U && trimmed[len - 1U] == ')';
        if (!wraps) {
            break;
        }
        for (i = 0U; i < len; ++i) {
            char ch = trimmed[i];
            if (ch == '"' && !v3_quote_is_escaped(trimmed, i)) {
                in_string = !in_string;
                continue;
            }
            if (in_string) {
                continue;
            }
            if (ch == '(') {
                paren_depth += 1;
            } else if (ch == ')') {
                paren_depth -= 1;
                if (paren_depth == 0 && i + 1U < len) {
                    wraps = false;
                    break;
                }
            }
        }
        if (!wraps || paren_depth != 0) {
            break;
        }
        snprintf(trimmed, sizeof(trimmed), "%.*s", (int)(len - 2U), trimmed + 1);
        v3_trim_copy_text(trimmed, trimmed, sizeof(trimmed));
        changed = true;
    }
    snprintf(out, cap, "%s", trimmed);
    return changed || out[0] != '\0';
}

static bool v3_wasm_flatten_str_concat_terms(const char *expr_text,
                                             char terms[][4096],
                                             size_t *count_io,
                                             size_t term_cap) {
    char expr[4096];
    int32_t plus_index = -1;
    if (expr_text == NULL || terms == NULL || count_io == NULL || term_cap == 0U) {
        return false;
    }
    v3_wasm_trim_wrapping_parens(expr_text, expr, sizeof(expr));
    plus_index = v3_find_top_level_binary_op_last(expr, "+");
    if (plus_index < 0) {
        if (*count_io >= term_cap) {
            return false;
        }
        snprintf(terms[*count_io], 4096, "%s", expr);
        *count_io += 1U;
        return true;
    }
    {
        char left_expr[4096];
        char right_expr[4096];
        snprintf(left_expr, sizeof(left_expr), "%.*s", plus_index, expr);
        snprintf(right_expr, sizeof(right_expr), "%s", expr + plus_index + 1);
        v3_trim_copy_text(left_expr, left_expr, sizeof(left_expr));
        v3_trim_copy_text(right_expr, right_expr, sizeof(right_expr));
        return v3_wasm_flatten_str_concat_terms(left_expr, terms, count_io, term_cap) &&
               v3_wasm_flatten_str_concat_terms(right_expr, terms, count_io, term_cap);
    }
}

static int32_t v3_wasm_find_local_slot_index(const V3WasmFunctionContext *ctx,
                                             const char *name) {
    size_t i;
    if (ctx == NULL || name == NULL || name[0] == '\0') {
        return -1;
    }
    for (i = 0U; i < ctx->local_count; ++i) {
        if (strcmp(ctx->locals[i].name, name) == 0) {
            return (int32_t)i;
        }
    }
    return -1;
}

static bool v3_wasm_context_add_local(V3WasmFunctionContext *ctx,
                                      const char *name,
                                      const char *type_text,
                                      const char *abi_class,
                                      uint32_t local_index,
                                      int32_t stack_offset,
                                      int32_t stack_size,
                                      int32_t stack_align) {
    if (ctx == NULL ||
        name == NULL || name[0] == '\0' ||
        type_text == NULL || type_text[0] == '\0' ||
        abi_class == NULL || !v3_wasm_abi_uses_i32_param_slot(abi_class)) {
        return false;
    }
    if (v3_wasm_find_local_slot_index(ctx, name) >= 0) {
        return true;
    }
    if (ctx->local_count >= CHENG_V3_MAX_ASM_LOCALS) {
        fprintf(stderr, "[cheng_v3_seed] wasm local table overflow local=%s\n", name);
        return false;
    }
    memset(&ctx->locals[ctx->local_count], 0, sizeof(ctx->locals[ctx->local_count]));
    snprintf(ctx->locals[ctx->local_count].name, sizeof(ctx->locals[ctx->local_count].name), "%s", name);
    snprintf(ctx->locals[ctx->local_count].type_text, sizeof(ctx->locals[ctx->local_count].type_text), "%s", type_text);
    snprintf(ctx->locals[ctx->local_count].abi_class, sizeof(ctx->locals[ctx->local_count].abi_class), "%s", abi_class);
    ctx->locals[ctx->local_count].stack_offset = stack_offset;
    ctx->locals[ctx->local_count].stack_size = stack_size;
    ctx->locals[ctx->local_count].stack_align = stack_align;
    ctx->local_indices[ctx->local_count] = local_index;
    ctx->local_count += 1U;
    return true;
}

static bool v3_wasm_context_add_storage_local(const V3LoweringPlanStub *lowering,
                                              V3WasmFunctionContext *ctx,
                                              const char *name,
                                              const char *type_text,
                                              const char *abi_class,
                                              uint32_t local_index) {
    int32_t stack_offset = -1;
    int32_t stack_size = 0;
    int32_t stack_align = 0;
    if (strcmp(abi_class, "composite") == 0) {
        V3TypeLayoutStub layout;
        if (!v3_compute_type_layout_impl(lowering, type_text, &layout, 0U)) {
            return false;
        }
        stack_align = layout.align > 0 ? layout.align : 4;
        stack_size = layout.size > 0 ? layout.size : stack_align;
        stack_offset = v3_align_up_i32(ctx->frame_size, stack_align);
        ctx->frame_size = v3_align_up_i32(stack_offset + stack_size, 8);
    }
    return v3_wasm_context_add_local(ctx,
                                     name,
                                     type_text,
                                     abi_class,
                                     local_index,
                                     stack_offset,
                                     stack_size,
                                     stack_align);
}

static uint32_t v3_wasm_extra_local_count(const V3WasmFunctionContext *ctx) {
    if (ctx == NULL) {
        return 0U;
    }
    return (ctx->has_frame_base_local ? 1U : 0U) +
           (ctx->has_scratch_locals ? 5U : 0U);
}

static uint32_t v3_wasm_total_local_count(const V3WasmFunctionContext *ctx) {
    if (ctx == NULL) {
        return 0U;
    }
    return (uint32_t)ctx->local_count + v3_wasm_extra_local_count(ctx);
}

static bool v3_wasm_context_finalize(V3WasmFunctionContext *ctx) {
    if (ctx == NULL) {
        return false;
    }
    ctx->str_temp0_offset = -1;
    ctx->str_temp1_offset = -1;
    if (ctx->needs_str_temps) {
        const int32_t str_size = v3_active_target_str_size_i32();
        ctx->str_temp0_offset = v3_align_up_i32(ctx->frame_size, 4);
        ctx->frame_size = v3_align_up_i32(ctx->str_temp0_offset + str_size, 8);
        ctx->str_temp1_offset = v3_align_up_i32(ctx->frame_size, 4);
        ctx->frame_size = v3_align_up_i32(ctx->str_temp1_offset + str_size, 8);
    }
    if (ctx->frame_size > 0) {
        ctx->frame_size = v3_align_up_i32(ctx->frame_size, 16);
        ctx->frame_base_local_index = (uint32_t)ctx->local_count;
        ctx->has_frame_base_local = true;
    }
    ctx->scratch_local_index = (uint32_t)ctx->local_count + (ctx->has_frame_base_local ? 1U : 0U);
    ctx->scratch2_local_index = ctx->scratch_local_index + 1U;
    ctx->scratch3_local_index = ctx->scratch2_local_index + 1U;
    ctx->scratch4_local_index = ctx->scratch3_local_index + 1U;
    ctx->scratch5_local_index = ctx->scratch4_local_index + 1U;
    ctx->has_scratch_locals = true;
    return true;
}

static bool v3_wasm_emit_local_get(V3WasmBuffer *body,
                                   uint32_t local_index) {
    return v3_wasm_append_u8(body, 0x20U) &&
           v3_wasm_append_uleb_u32(body, local_index);
}

static bool v3_wasm_emit_local_set(V3WasmBuffer *body,
                                   uint32_t local_index) {
    return v3_wasm_append_u8(body, 0x21U) &&
           v3_wasm_append_uleb_u32(body, local_index);
}

static bool CHENG_V3_UNUSED v3_wasm_emit_local_tee(V3WasmBuffer *body,
                                                   uint32_t local_index) {
    return v3_wasm_append_u8(body, 0x22U) &&
           v3_wasm_append_uleb_u32(body, local_index);
}

static bool v3_wasm_emit_i32_const(V3WasmBuffer *body,
                                   int32_t value) {
    return v3_wasm_append_u8(body, 0x41U) &&
           v3_wasm_append_sleb_i32(body, value);
}

static bool v3_wasm_emit_i32_add(V3WasmBuffer *body) {
    return v3_wasm_append_u8(body, 0x6aU);
}

static bool v3_wasm_emit_global_get(V3WasmBuffer *body,
                                    uint32_t global_index) {
    return v3_wasm_append_u8(body, 0x23U) &&
           v3_wasm_append_uleb_u32(body, global_index);
}

static bool v3_wasm_emit_global_set(V3WasmBuffer *body,
                                    uint32_t global_index) {
    return v3_wasm_append_u8(body, 0x24U) &&
           v3_wasm_append_uleb_u32(body, global_index);
}

static bool v3_wasm_emit_store_scalar_to_top_address(V3WasmBuffer *body,
                                                     const char *abi_class) {
    if (body == NULL || abi_class == NULL) {
        return false;
    }
    if (strcmp(abi_class, "ptr") == 0 || strcmp(abi_class, "i32") == 0 || strcmp(abi_class, "u32") == 0) {
        return v3_wasm_append_u8(body, 0x36U) &&
               v3_wasm_append_memory_arg(body, 2U, 0U);
    }
    if (strcmp(abi_class, "bool") == 0 ||
        strcmp(abi_class, "u8") == 0 ||
        strcmp(abi_class, "i8") == 0) {
        return v3_wasm_append_u8(body, 0x3aU) &&
               v3_wasm_append_memory_arg(body, 0U, 0U);
    }
    if (strcmp(abi_class, "u16") == 0 ||
        strcmp(abi_class, "i16") == 0) {
        return v3_wasm_append_u8(body, 0x3bU) &&
               v3_wasm_append_memory_arg(body, 1U, 0U);
    }
    return false;
}

static bool v3_wasm_emit_restore_frame_base(const V3WasmFunctionContext *ctx,
                                            V3WasmBuffer *body) {
    if (ctx == NULL || body == NULL) {
        return false;
    }
    if (!ctx->has_frame_base_local) {
        return true;
    }
    return v3_wasm_emit_local_get(body, ctx->frame_base_local_index) &&
           v3_wasm_emit_global_set(body, CHENG_V3_WASM_STACK_GLOBAL_INDEX);
}

static bool v3_wasm_emit_zero_region_from_local(V3WasmBuffer *body,
                                                uint32_t local_index,
                                                int32_t size) {
    int32_t offset = 0;
    if (body == NULL || size < 0) {
        return false;
    }
    while (offset + 4 <= size) {
        if (!v3_wasm_emit_local_get(body, local_index) ||
            (offset != 0 && (!v3_wasm_emit_i32_const(body, offset) ||
                             !v3_wasm_emit_i32_add(body))) ||
            !v3_wasm_emit_i32_const(body, 0) ||
            !v3_wasm_append_u8(body, 0x36U) ||
            !v3_wasm_append_memory_arg(body, 2U, 0U)) {
            return false;
        }
        offset += 4;
    }
    while (offset < size) {
        if (!v3_wasm_emit_local_get(body, local_index) ||
            (offset != 0 && (!v3_wasm_emit_i32_const(body, offset) ||
                             !v3_wasm_emit_i32_add(body))) ||
            !v3_wasm_emit_i32_const(body, 0) ||
            !v3_wasm_append_u8(body, 0x3aU) ||
            !v3_wasm_append_memory_arg(body, 0U, 0U)) {
            return false;
        }
        offset += 1;
    }
    return true;
}

static bool v3_wasm_emit_copy_region_between_locals(V3WasmBuffer *body,
                                                    uint32_t dst_local_index,
                                                    uint32_t src_local_index,
                                                    int32_t size) {
    int32_t offset = 0;
    if (body == NULL || size < 0) {
        return false;
    }
    while (offset + 4 <= size) {
        if (!v3_wasm_emit_local_get(body, dst_local_index) ||
            (offset != 0 && (!v3_wasm_emit_i32_const(body, offset) ||
                             !v3_wasm_emit_i32_add(body))) ||
            !v3_wasm_emit_local_get(body, src_local_index) ||
            (offset != 0 && (!v3_wasm_emit_i32_const(body, offset) ||
                             !v3_wasm_emit_i32_add(body))) ||
            !v3_wasm_append_u8(body, 0x28U) ||
            !v3_wasm_append_memory_arg(body, 2U, 0U) ||
            !v3_wasm_append_u8(body, 0x36U) ||
            !v3_wasm_append_memory_arg(body, 2U, 0U)) {
            return false;
        }
        offset += 4;
    }
    while (offset < size) {
        if (!v3_wasm_emit_local_get(body, dst_local_index) ||
            (offset != 0 && (!v3_wasm_emit_i32_const(body, offset) ||
                             !v3_wasm_emit_i32_add(body))) ||
            !v3_wasm_emit_local_get(body, src_local_index) ||
            (offset != 0 && (!v3_wasm_emit_i32_const(body, offset) ||
                             !v3_wasm_emit_i32_add(body))) ||
            !v3_wasm_append_u8(body, 0x2dU) ||
            !v3_wasm_append_memory_arg(body, 0U, 0U) ||
            !v3_wasm_append_u8(body, 0x3aU) ||
            !v3_wasm_append_memory_arg(body, 0U, 0U)) {
            return false;
        }
        offset += 1;
    }
    return true;
}

static bool CHENG_V3_UNUSED v3_wasm_emit_i32_store_at_local_offset(V3WasmBuffer *body,
                                                                   uint32_t local_index,
                                                                   int32_t offset,
                                                                   int32_t value) {
    return v3_wasm_emit_local_get(body, local_index) &&
           (offset == 0 || (v3_wasm_emit_i32_const(body, offset) &&
                            v3_wasm_emit_i32_add(body))) &&
           v3_wasm_emit_i32_const(body, value) &&
           v3_wasm_append_u8(body, 0x36U) &&
           v3_wasm_append_memory_arg(body, 2U, 0U);
}

static bool v3_wasm_emit_frame_temp_address(const V3WasmFunctionContext *ctx,
                                            V3WasmBuffer *body,
                                            uint32_t target_local_index,
                                            int32_t frame_offset) {
    if (ctx == NULL || body == NULL || !ctx->has_frame_base_local || frame_offset < 0) {
        return false;
    }
    return v3_wasm_emit_local_get(body, ctx->frame_base_local_index) &&
           (frame_offset == 0 || (v3_wasm_emit_i32_const(body, frame_offset) &&
                                  v3_wasm_emit_i32_add(body))) &&
           v3_wasm_emit_local_set(body, target_local_index);
}

static bool v3_wasm_build_str_concat_target(const V3SystemLinkPlanStub *plan,
                                            V3AsmCallTarget *target) {
    if (plan == NULL || target == NULL) {
        return false;
    }
    memset(target, 0, sizeof(*target));
    target->external = true;
    target->param_count = 2U;
    snprintf(target->callee_name, sizeof(target->callee_name), "%s", "driver_c_str_concat_bridge");
    v3_copy_target_symbol_name(plan,
                               "driver_c_str_concat_bridge",
                               target->symbol_name,
                               sizeof(target->symbol_name));
    snprintf(target->return_type, sizeof(target->return_type), "%s", "str");
    snprintf(target->return_abi_class, sizeof(target->return_abi_class), "%s", "composite");
    snprintf(target->param_types[0], sizeof(target->param_types[0]), "%s", "str");
    snprintf(target->param_types[1], sizeof(target->param_types[1]), "%s", "str");
    snprintf(target->param_abi_classes[0], sizeof(target->param_abi_classes[0]), "%s", "composite");
    snprintf(target->param_abi_classes[1], sizeof(target->param_abi_classes[1]), "%s", "composite");
    return true;
}

static bool v3_wasm_build_str_eq_target(const V3SystemLinkPlanStub *plan,
                                        V3AsmCallTarget *target) {
    if (plan == NULL || target == NULL) {
        return false;
    }
    memset(target, 0, sizeof(*target));
    target->external = true;
    target->param_count = 2U;
    snprintf(target->callee_name, sizeof(target->callee_name), "%s", "driver_c_str_eq_bridge");
    v3_copy_target_symbol_name(plan,
                               "driver_c_str_eq_bridge",
                               target->symbol_name,
                               sizeof(target->symbol_name));
    snprintf(target->return_type, sizeof(target->return_type), "%s", "int32");
    snprintf(target->return_abi_class, sizeof(target->return_abi_class), "%s", "i32");
    snprintf(target->param_types[0], sizeof(target->param_types[0]), "%s", "str");
    snprintf(target->param_types[1], sizeof(target->param_types[1]), "%s", "str");
    snprintf(target->param_abi_classes[0], sizeof(target->param_abi_classes[0]), "%s", "composite");
    snprintf(target->param_abi_classes[1], sizeof(target->param_abi_classes[1]), "%s", "composite");
    return true;
}

static bool v3_wasm_prepare_composite_expr(const V3SystemLinkPlanStub *plan,
                                           const V3LoweringPlanStub *lowering,
                                           const V3LoweredFunctionStub *function,
                                           V3WasmFunctionContext *ctx,
                                           const char *expr_text,
                                           const char *dest_type_text,
                                           V3WasmImportStub *imports,
                                           size_t *import_count) {
    char expr[4096];
    char normalized_dest_type[CHENG_V3_MAX_TYPE_TEXT];
    if (plan == NULL || lowering == NULL || function == NULL || ctx == NULL ||
        expr_text == NULL || dest_type_text == NULL) {
        return false;
    }
    v3_wasm_trim_wrapping_parens(expr_text, expr, sizeof(expr));
    if (!v3_normalize_type_text(lowering,
                                function->owner_module_path,
                                ctx->aliases,
                                ctx->alias_count,
                                dest_type_text,
                                normalized_dest_type,
                                sizeof(normalized_dest_type))) {
        return false;
    }
    if (strcmp(expr, "nil") == 0 ||
        (strcmp(expr, "[]") == 0 && v3_is_seq_type_text(normalized_dest_type)) ||
        (strcmp(expr, "\"\"") == 0 && strcmp(normalized_dest_type, "str") == 0)) {
        return true;
    }
    if (strcmp(normalized_dest_type, "str") == 0) {
        char literal[4096];
        int32_t plus_index = -1;
        if (v3_parse_string_literal_text(expr, literal, sizeof(literal))) {
            return true;
        }
        plus_index = v3_find_top_level_binary_op_last(expr, "+");
        if (plus_index >= 0) {
            char left_expr[4096];
            char right_expr[4096];
            V3AsmCallTarget target;
            snprintf(left_expr, sizeof(left_expr), "%.*s", plus_index, expr);
            snprintf(right_expr, sizeof(right_expr), "%s", expr + plus_index + 1);
            v3_trim_copy_text(left_expr, left_expr, sizeof(left_expr));
            v3_trim_copy_text(right_expr, right_expr, sizeof(right_expr));
            ctx->needs_str_temps = true;
            return v3_wasm_prepare_composite_expr(plan,
                                                  lowering,
                                                  function,
                                                  ctx,
                                                  left_expr,
                                                  "str",
                                                  imports,
                                                  import_count) &&
                   v3_wasm_prepare_composite_expr(plan,
                                                  lowering,
                                                  function,
                                                  ctx,
                                                  right_expr,
                                                  "str",
                                                  imports,
                                                  import_count) &&
                   v3_wasm_build_str_concat_target(plan, &target) &&
                   v3_wasm_register_import(function->symbol_text, &target, imports, import_count);
        }
    }
    return v3_wasm_analyze_expr(plan, lowering, function, ctx, expr, imports, import_count);
}

static bool v3_wasm_emit_str_literal_into_local_address(const V3WasmFunctionContext *ctx,
                                                        V3WasmBuffer *body,
                                                        uint32_t dest_addr_local_index,
                                                        const char *literal_text) {
    int32_t object_addr = 0;
    if (ctx == NULL || body == NULL || literal_text == NULL ||
        !v3_wasm_register_string_literal_object(literal_text, &object_addr, NULL, NULL)) {
        return false;
    }
    return v3_wasm_emit_i32_const(body, object_addr) &&
           v3_wasm_emit_local_set(body, ctx->scratch3_local_index) &&
           v3_wasm_emit_copy_region_between_locals(body,
                                                   dest_addr_local_index,
                                                   ctx->scratch3_local_index,
                                                   v3_active_target_str_size_i32());
}

static bool v3_wasm_emit_str_literal_expr(V3WasmBuffer *body,
                                          const char *literal_text) {
    int32_t object_addr = 0;
    if (body == NULL || literal_text == NULL ||
        !v3_wasm_register_string_literal_object(literal_text, &object_addr, NULL, NULL)) {
        return false;
    }
    return v3_wasm_emit_i32_const(body, object_addr);
}

static bool v3_wasm_compute_static_return_slot_offset(const V3LoweringPlanStub *lowering,
                                                      const V3LoweredFunctionStub *function,
                                                      int32_t *offset_out,
                                                      int32_t *size_out) {
    size_t i;
    int32_t offset = 0;
    if (lowering == NULL || function == NULL || offset_out == NULL || size_out == NULL) {
        return false;
    }
    for (i = 0U; i < lowering->function_count; ++i) {
        const V3LoweredFunctionStub *it = &lowering->functions[i];
        if (strcmp(it->return_abi_class, "composite") == 0) {
            V3TypeLayoutStub layout;
            if (!v3_compute_type_layout_impl(lowering, it->return_type, &layout, 0U)) {
                return false;
            }
            offset = v3_align_up_i32(offset, layout.align > 0 ? layout.align : 8);
            if (it == function) {
                *offset_out = offset;
                *size_out = layout.size;
                return true;
            }
            offset += v3_align_up_i32(layout.size, 8);
        }
    }
    return false;
}

static int32_t v3_wasm_total_static_return_bytes(const V3LoweringPlanStub *lowering) {
    size_t i;
    int32_t offset = 0;
    if (lowering == NULL) {
        return 0;
    }
    for (i = 0U; i < lowering->function_count; ++i) {
        const V3LoweredFunctionStub *it = &lowering->functions[i];
        if (strcmp(it->return_abi_class, "composite") == 0) {
            V3TypeLayoutStub layout;
            if (!v3_compute_type_layout_impl(lowering, it->return_type, &layout, 0U)) {
                return 0;
            }
            offset = v3_align_up_i32(offset, layout.align > 0 ? layout.align : 8);
            offset += v3_align_up_i32(layout.size, 8);
        }
    }
    return offset;
}

static bool v3_wasm_context_init(const V3SystemLinkPlanStub *plan,
                                 const V3LoweringPlanStub *lowering,
                                 const V3LoweredFunctionStub *function,
                                 V3WasmFunctionContext *ctx) {
    size_t i;
    (void)plan;
    (void)lowering;
    memset(ctx, 0, sizeof(*ctx));
    if (!v3_load_import_aliases_from_source_path(function->source_path,
                                                 ctx->aliases,
                                                 &ctx->alias_count,
                                                 CHENG_V3_MAX_IMPORT_ALIASES)) {
        fprintf(stderr, "[cheng_v3_seed] wasm alias load failed: %s\n", function->source_path);
        return false;
    }
    if (!v3_wasm_function_return_supported(function->return_abi_class) ||
        function->param_count > CHENG_V3_MAX_CALL_ARGS) {
        return false;
    }
    for (i = 0U; i < function->param_count; ++i) {
        if (!v3_wasm_abi_uses_i32_param_slot(function->param_abi_classes[i]) ||
            !v3_wasm_context_add_local(ctx,
                                       function->param_names[i],
                                       function->param_types[i],
                                       function->param_abi_classes[i],
                                       (uint32_t)i,
                                       -1,
                                       0,
                                       0)) {
            fprintf(stderr,
                    "[cheng_v3_seed] wasm param local init failed function=%s param=%s type=%s abi=%s\n",
                    function->symbol_text,
                    function->param_names[i],
                    function->param_types[i],
                    function->param_abi_classes[i]);
            return false;
        }
    }
    ctx->param_count = function->param_count;
    return true;
}

static bool v3_wasm_register_import(const char *function_symbol,
                                    const V3AsmCallTarget *target,
                                    V3WasmImportStub *imports,
                                    size_t *import_count) {
    uint32_t type_index = 0U;
    if (target == NULL || !target->external) {
        return true;
    }
    if (!v3_wasm_signature_type_index(target->param_count,
                                      target->return_abi_class,
                                      target->param_abi_classes,
                                      &type_index)) {
        fprintf(stderr,
                "[cheng_v3_seed] wasm unsupported external call signature function=%s symbol=%s ret=%s argc=%zu\n",
                function_symbol,
                target->symbol_name,
                target->return_abi_class,
                target->param_count);
        return false;
    }
    if (imports == NULL || import_count == NULL) {
        return true;
    }
    if (v3_wasm_find_import_index(imports, *import_count, target->symbol_name, type_index) >= 0) {
        return true;
    }
    if (*import_count >= CHENG_V3_MAX_PLAN_FUNCTIONS) {
        fprintf(stderr, "[cheng_v3_seed] wasm import table overflow\n");
        return false;
    }
    snprintf(imports[*import_count].symbol_name,
             sizeof(imports[*import_count].symbol_name),
             "%s",
             target->symbol_name);
    imports[*import_count].type_index = type_index;
    *import_count += 1U;
    return true;
}

static bool v3_wasm_analyze_expr(const V3SystemLinkPlanStub *plan,
                                 const V3LoweringPlanStub *lowering,
                                 const V3LoweredFunctionStub *function,
                                 V3WasmFunctionContext *ctx,
                                 const char *expr_text,
                                 V3WasmImportStub *imports,
                                 size_t *import_count);

static bool v3_wasm_emit_expr(const V3SystemLinkPlanStub *plan,
                              const V3LoweringPlanStub *lowering,
                              const V3LoweredFunctionStub *function,
                              const V3WasmFunctionContext *ctx,
                              const char *expr_text,
                              const V3WasmImportStub *imports,
                              size_t import_count,
                              V3WasmBuffer *body);

static bool v3_wasm_emit_composite_expr_into_local_address(const V3SystemLinkPlanStub *plan,
                                                           const V3LoweringPlanStub *lowering,
                                                           const V3LoweredFunctionStub *function,
                                                           const V3WasmFunctionContext *ctx,
                                                           const char *expr_text,
                                                           const char *dest_type_text,
                                                           uint32_t dest_addr_local_index,
                                                           const V3WasmImportStub *imports,
                                                           size_t import_count,
                                                           V3WasmBuffer *body);

static bool v3_wasm_emit_field_access_from_base(V3WasmBuffer *body,
                                                const char *field_abi,
                                                int32_t field_offset);

static bool v3_wasm_resolve_len_expr_meta(const V3SystemLinkPlanStub *plan,
                                          const V3LoweringPlanStub *lowering,
                                          const V3LoweredFunctionStub *function,
                                          const V3WasmFunctionContext *ctx,
                                          const char *expr_text,
                                          char *base_type_out,
                                          size_t base_type_cap,
                                          char *base_abi_out,
                                          size_t base_abi_cap,
                                          char *field_abi_out,
                                          size_t field_abi_cap,
                                          int32_t *field_offset_out) {
    if (plan == NULL || lowering == NULL || function == NULL || ctx == NULL || expr_text == NULL ||
        base_type_out == NULL || base_abi_out == NULL || field_abi_out == NULL || field_offset_out == NULL) {
        return false;
    }
    if (!v3_infer_expr_type(plan,
                            lowering,
                            function,
                            ctx->aliases,
                            ctx->alias_count,
                            ctx->locals,
                            ctx->local_count,
                            expr_text,
                            base_type_out,
                            base_type_cap,
                            base_abi_out,
                            base_abi_cap)) {
        return false;
    }
    return v3_resolve_field_meta(lowering,
                                 base_type_out,
                                 "len",
                                 NULL,
                                 0U,
                                 field_abi_out,
                                 field_abi_cap,
                                 field_offset_out);
}

static bool v3_wasm_seq_elem_size(const V3LoweringPlanStub *lowering,
                                  const char *seq_type_text,
                                  char *elem_type_out,
                                  size_t elem_type_cap,
                                  char *elem_abi_out,
                                  size_t elem_abi_cap,
                                  int32_t *elem_size_out);

static bool v3_wasm_build_bounds_check_target(V3AsmCallTarget *target) {
    if (target == NULL) {
        return false;
    }
    memset(target, 0, sizeof(*target));
    target->external = true;
    target->param_count = 2U;
    snprintf(target->callee_name, sizeof(target->callee_name), "%s", "cheng_bounds_check");
    snprintf(target->symbol_name, sizeof(target->symbol_name), "%s", "cheng_bounds_check");
    snprintf(target->return_type, sizeof(target->return_type), "%s", "void");
    snprintf(target->return_abi_class, sizeof(target->return_abi_class), "%s", "void");
    snprintf(target->param_types[0], sizeof(target->param_types[0]), "%s", "int32");
    snprintf(target->param_types[1], sizeof(target->param_types[1]), "%s", "int32");
    snprintf(target->param_abi_classes[0], sizeof(target->param_abi_classes[0]), "%s", "i32");
    snprintf(target->param_abi_classes[1], sizeof(target->param_abi_classes[1]), "%s", "i32");
    return true;
}

static bool v3_wasm_resolve_index_expr_meta(const V3SystemLinkPlanStub *plan,
                                            const V3LoweringPlanStub *lowering,
                                            const V3LoweredFunctionStub *function,
                                            const V3WasmFunctionContext *ctx,
                                            const char *expr_text,
                                            char *base_expr_out,
                                            size_t base_expr_cap,
                                            char *index_expr_out,
                                            size_t index_expr_cap,
                                            char *base_type_out,
                                            size_t base_type_cap,
                                            char *base_abi_out,
                                            size_t base_abi_cap,
                                            char *normalized_base_type_out,
                                            size_t normalized_base_type_cap,
                                            char *elem_type_out,
                                            size_t elem_type_cap,
                                            char *elem_abi_out,
                                            size_t elem_abi_cap,
                                            int32_t *elem_size_out,
                                            int32_t *data_field_offset_out,
                                            int32_t *len_field_offset_out,
                                            int32_t *fixed_len_out,
                                            bool *base_is_fixed_array_out) {
    char data_field_type[256];
    char data_field_abi[32];
    char len_field_type[256];
    char len_field_abi[32];
    V3TypeLayoutStub elem_layout;
    if (plan == NULL || lowering == NULL || function == NULL || ctx == NULL || expr_text == NULL ||
        base_expr_out == NULL || index_expr_out == NULL || base_type_out == NULL || base_abi_out == NULL ||
        normalized_base_type_out == NULL || elem_type_out == NULL || elem_abi_out == NULL ||
        elem_size_out == NULL || data_field_offset_out == NULL || len_field_offset_out == NULL ||
        fixed_len_out == NULL || base_is_fixed_array_out == NULL) {
        return false;
    }
    *elem_size_out = 0;
    *data_field_offset_out = 0;
    *len_field_offset_out = 0;
    *fixed_len_out = 0;
    *base_is_fixed_array_out = false;
    if (!v3_parse_index_access_expr(expr_text,
                                    base_expr_out,
                                    base_expr_cap,
                                    index_expr_out,
                                    index_expr_cap) ||
        !v3_infer_expr_type(plan,
                            lowering,
                            function,
                            ctx->aliases,
                            ctx->alias_count,
                            ctx->locals,
                            ctx->local_count,
                            base_expr_out,
                            base_type_out,
                            base_type_cap,
                            base_abi_out,
                            base_abi_cap) ||
        !v3_normalize_type_text(lowering,
                                function->owner_module_path,
                                ctx->aliases,
                                ctx->alias_count,
                                base_type_out,
                                normalized_base_type_out,
                                normalized_base_type_cap)) {
        return false;
    }
    if (strcmp(normalized_base_type_out, "Bytes") == 0) {
        snprintf(elem_type_out, elem_type_cap, "%s", "int32");
        snprintf(elem_abi_out, elem_abi_cap, "%s", "i32");
        *elem_size_out = 1;
        return v3_resolve_field_meta(lowering,
                                     normalized_base_type_out,
                                     "data",
                                     data_field_type,
                                     sizeof(data_field_type),
                                     data_field_abi,
                                     sizeof(data_field_abi),
                                     data_field_offset_out) &&
               strcmp(data_field_abi, "ptr") == 0 &&
               v3_resolve_field_meta(lowering,
                                     normalized_base_type_out,
                                     "len",
                                     len_field_type,
                                     sizeof(len_field_type),
                                     len_field_abi,
                                     sizeof(len_field_abi),
                                     len_field_offset_out) &&
               strcmp(len_field_abi, "i32") == 0;
    }
    if (strcmp(normalized_base_type_out, "str") == 0) {
        snprintf(elem_type_out, elem_type_cap, "%s", "char");
        snprintf(elem_abi_out, elem_abi_cap, "%s", "i32");
        *elem_size_out = 1;
        return v3_resolve_field_meta(lowering,
                                     normalized_base_type_out,
                                     "data",
                                     data_field_type,
                                     sizeof(data_field_type),
                                     data_field_abi,
                                     sizeof(data_field_abi),
                                     data_field_offset_out) &&
               strcmp(data_field_abi, "ptr") == 0 &&
               v3_resolve_field_meta(lowering,
                                     normalized_base_type_out,
                                     "len",
                                     len_field_type,
                                     sizeof(len_field_type),
                                     len_field_abi,
                                     sizeof(len_field_abi),
                                     len_field_offset_out) &&
               strcmp(len_field_abi, "i32") == 0;
    }
    if (strlen(normalized_base_type_out) > 2U &&
        strcmp(normalized_base_type_out + strlen(normalized_base_type_out) - 2U, "[]") == 0) {
        return v3_wasm_seq_elem_size(lowering,
                                     normalized_base_type_out,
                                     elem_type_out,
                                     elem_type_cap,
                                     elem_abi_out,
                                     elem_abi_cap,
                                     elem_size_out) &&
               v3_resolve_field_meta(lowering,
                                     normalized_base_type_out,
                                     "buffer",
                                     data_field_type,
                                     sizeof(data_field_type),
                                     data_field_abi,
                                     sizeof(data_field_abi),
                                     data_field_offset_out) &&
               strcmp(data_field_abi, "ptr") == 0 &&
               v3_resolve_field_meta(lowering,
                                     normalized_base_type_out,
                                     "len",
                                     len_field_type,
                                     sizeof(len_field_type),
                                     len_field_abi,
                                     sizeof(len_field_abi),
                                     len_field_offset_out) &&
               strcmp(len_field_abi, "i32") == 0;
    }
    if (!v3_parse_fixed_array_type(normalized_base_type_out,
                                   elem_type_out,
                                   elem_type_cap,
                                   fixed_len_out)) {
        return false;
    }
    *base_is_fixed_array_out = true;
    snprintf(elem_abi_out, elem_abi_cap, "%s", v3_type_abi_class(elem_type_out));
    if (strcmp(elem_abi_out, "i8") == 0 ||
        strcmp(elem_abi_out, "u8") == 0 ||
        strcmp(elem_abi_out, "bool") == 0) {
        *elem_size_out = 1;
        return true;
    }
    if (strcmp(elem_abi_out, "i16") == 0 ||
        strcmp(elem_abi_out, "u16") == 0) {
        *elem_size_out = 2;
        return true;
    }
    if (strcmp(elem_abi_out, "i32") == 0 ||
        strcmp(elem_abi_out, "u32") == 0) {
        *elem_size_out = 4;
        return true;
    }
    if (strcmp(elem_abi_out, "i64") == 0 || strcmp(elem_abi_out, "ptr") == 0) {
        *elem_size_out = 8;
        return true;
    }
    return v3_compute_type_layout_impl(lowering, elem_type_out, &elem_layout, 0U) &&
           elem_layout.size > 0 &&
           ((*elem_size_out = elem_layout.size), true);
}

static bool v3_wasm_emit_i32_cast(V3WasmBuffer *body,
                                  const char *src_type_text,
                                  const char *src_abi,
                                  const char *dst_type_text,
                                  const char *dst_abi) {
    (void)src_type_text;
    (void)dst_type_text;
    if (body == NULL || src_abi == NULL || dst_abi == NULL) {
        return false;
    }
    if (!v3_wasm_abi_uses_i32_param_slot(src_abi) || !v3_wasm_abi_uses_i32_param_slot(dst_abi)) {
        return false;
    }
    if (strcmp(dst_abi, "bool") == 0) {
        return v3_wasm_emit_i32_const(body, 0) &&
               v3_wasm_append_u8(body, 0x47U);
    }
    if (strcmp(dst_abi, "u8") == 0) {
        return v3_wasm_emit_i32_const(body, 0xff) &&
               v3_wasm_append_u8(body, 0x71U);
    }
    if (strcmp(dst_abi, "i8") == 0) {
        return v3_wasm_emit_i32_const(body, 24) &&
               v3_wasm_append_u8(body, 0x74U) &&
               v3_wasm_emit_i32_const(body, 24) &&
               v3_wasm_append_u8(body, 0x75U);
    }
    if (strcmp(dst_abi, "u16") == 0) {
        return v3_wasm_emit_i32_const(body, 0xffff) &&
               v3_wasm_append_u8(body, 0x71U);
    }
    if (strcmp(dst_abi, "i16") == 0) {
        return v3_wasm_emit_i32_const(body, 16) &&
               v3_wasm_append_u8(body, 0x74U) &&
               v3_wasm_emit_i32_const(body, 16) &&
               v3_wasm_append_u8(body, 0x75U);
    }
    return true;
}

static bool v3_wasm_emit_len_expr(const V3SystemLinkPlanStub *plan,
                                  const V3LoweringPlanStub *lowering,
                                  const V3LoweredFunctionStub *function,
                                  const V3WasmFunctionContext *ctx,
                                  const char *expr_text,
                                  const V3WasmImportStub *imports,
                                  size_t import_count,
                                  V3WasmBuffer *body) {
    char base_type[256];
    char base_abi[32];
    char field_abi[32];
    int32_t field_offset = 0;
    if (!v3_wasm_resolve_len_expr_meta(plan,
                                       lowering,
                                       function,
                                       ctx,
                                       expr_text,
                                       base_type,
                                       sizeof(base_type),
                                       base_abi,
                                       sizeof(base_abi),
                                       field_abi,
                                       sizeof(field_abi),
                                       &field_offset) ||
        !v3_wasm_abi_uses_i32_param_slot(base_abi) ||
        !v3_wasm_abi_uses_i32_param_slot(field_abi) ||
        !v3_wasm_emit_expr(plan, lowering, function, ctx, expr_text, imports, import_count, body)) {
        return false;
    }
    return v3_wasm_emit_field_access_from_base(body, field_abi, field_offset);
}

static uint32_t v3_wasm_pick_temp_local_excluding(const V3WasmFunctionContext *ctx,
                                                  uint32_t avoid_local_index);

static bool v3_wasm_emit_result_constructor_into_local_address(const V3SystemLinkPlanStub *plan,
                                                               const V3LoweringPlanStub *lowering,
                                                               const V3LoweredFunctionStub *function,
                                                               const V3WasmFunctionContext *ctx,
                                                               V3ResultIntrinsicKind kind,
                                                               const char *callee,
                                                               char args[][4096],
                                                               size_t arg_count,
                                                               const char *dest_type_text,
                                                               uint32_t dest_addr_local_index,
                                                               const V3WasmImportStub *imports,
                                                               size_t import_count,
                                                               V3WasmBuffer *body) {
    char normalized_dest_type[256];
    char explicit_inner_type[256];
    char normalized_explicit_inner_type[256];
    char result_inner_type[256];
    char ok_type[256];
    char ok_abi[32];
    char value_type[256];
    char value_abi[32];
    char err_type[256];
    char err_abi[32];
    char err_code_type[256];
    char err_code_abi[32];
    char err_msg_type[256];
    char err_msg_abi[32];
    V3TypeLayoutStub result_layout;
    uint32_t field_addr_local_index = 0U;
    int32_t ok_offset = 0;
    int32_t value_offset = 0;
    int32_t err_offset = 0;
    int32_t err_code_offset = 0;
    int32_t err_msg_offset = 0;
    if (plan == NULL || lowering == NULL || function == NULL || ctx == NULL || callee == NULL ||
        args == NULL || dest_type_text == NULL || body == NULL || !ctx->has_scratch_locals) {
        return false;
    }
    if (!v3_normalize_type_text(lowering,
                                function->owner_module_path,
                                ctx->aliases,
                                ctx->alias_count,
                                dest_type_text,
                                normalized_dest_type,
                                sizeof(normalized_dest_type)) ||
        !v3_startswith(normalized_dest_type, "Result[") ||
        !v3_parse_result_inner_type(normalized_dest_type,
                                    result_inner_type,
                                    sizeof(result_inner_type)) ||
        !v3_compute_type_layout_impl(lowering, normalized_dest_type, &result_layout, 0U) ||
        !v3_resolve_field_meta(lowering,
                               normalized_dest_type,
                               "ok",
                               ok_type,
                               sizeof(ok_type),
                               ok_abi,
                               sizeof(ok_abi),
                               &ok_offset) ||
        !v3_resolve_field_meta(lowering,
                               normalized_dest_type,
                               "value",
                               value_type,
                               sizeof(value_type),
                               value_abi,
                               sizeof(value_abi),
                               &value_offset) ||
        !v3_resolve_field_meta(lowering,
                               normalized_dest_type,
                               "err",
                               err_type,
                               sizeof(err_type),
                               err_abi,
                               sizeof(err_abi),
                               &err_offset) ||
        strcmp(ok_abi, "bool") != 0 ||
        strcmp(err_abi, "composite") != 0) {
        return false;
    }
    field_addr_local_index = v3_wasm_pick_temp_local_excluding(ctx, dest_addr_local_index);
    if (field_addr_local_index == 0U) {
        return false;
    }
    if (v3_extract_trailing_type_arg_text(callee,
                                          explicit_inner_type,
                                          sizeof(explicit_inner_type)) &&
        (!v3_normalize_type_text(lowering,
                                 function->owner_module_path,
                                 ctx->aliases,
                                 ctx->alias_count,
                                 explicit_inner_type,
                                 normalized_explicit_inner_type,
                                 sizeof(normalized_explicit_inner_type)) ||
         strcmp(normalized_explicit_inner_type, result_inner_type) != 0)) {
        return false;
    }
    if (!v3_wasm_emit_zero_region_from_local(body, dest_addr_local_index, result_layout.size)) {
        return false;
    }
    if (kind == V3_RESULT_INTRINSIC_OK) {
        if (arg_count != 1U ||
            !v3_wasm_emit_local_get(body, dest_addr_local_index) ||
            (ok_offset != 0 && (!v3_wasm_emit_i32_const(body, ok_offset) ||
                                !v3_wasm_emit_i32_add(body))) ||
            !v3_wasm_emit_i32_const(body, 1) ||
            !v3_wasm_emit_store_scalar_to_top_address(body, "bool")) {
            return false;
        }
        if (strcmp(value_abi, "composite") == 0) {
            return v3_wasm_emit_local_get(body, dest_addr_local_index) &&
                   (value_offset == 0 ||
                    (v3_wasm_emit_i32_const(body, value_offset) &&
                     v3_wasm_emit_i32_add(body))) &&
                   v3_wasm_emit_local_set(body, field_addr_local_index) &&
                   v3_wasm_emit_composite_expr_into_local_address(plan,
                                                                  lowering,
                                                                  function,
                                                                  ctx,
                                                                  args[0],
                                                                  value_type,
                                                                  field_addr_local_index,
                                                                  imports,
                                                                  import_count,
                                                                  body);
        }
        if (!v3_wasm_abi_uses_i32_param_slot(value_abi)) {
            return false;
        }
        return v3_wasm_emit_local_get(body, dest_addr_local_index) &&
               (value_offset == 0 ||
                (v3_wasm_emit_i32_const(body, value_offset) &&
                 v3_wasm_emit_i32_add(body))) &&
               v3_wasm_emit_expr(plan, lowering, function, ctx, args[0], imports, import_count, body) &&
               v3_wasm_emit_store_scalar_to_top_address(body, value_abi);
    }
    if (kind == V3_RESULT_INTRINSIC_ERR_INFO) {
        if (arg_count != 1U) {
            return false;
        }
        return v3_wasm_emit_local_get(body, dest_addr_local_index) &&
               (err_offset == 0 ||
                (v3_wasm_emit_i32_const(body, err_offset) &&
                 v3_wasm_emit_i32_add(body))) &&
               v3_wasm_emit_local_set(body, field_addr_local_index) &&
               v3_wasm_emit_composite_expr_into_local_address(plan,
                                                              lowering,
                                                              function,
                                                              ctx,
                                                              args[0],
                                                              err_type,
                                                              field_addr_local_index,
                                                              imports,
                                                              import_count,
                                                              body);
    }
    if (!v3_resolve_field_meta(lowering,
                               err_type,
                               "code",
                               err_code_type,
                               sizeof(err_code_type),
                               err_code_abi,
                               sizeof(err_code_abi),
                               &err_code_offset) ||
        !v3_resolve_field_meta(lowering,
                               err_type,
                               "msg",
                               err_msg_type,
                               sizeof(err_msg_type),
                               err_msg_abi,
                               sizeof(err_msg_abi),
                               &err_msg_offset) ||
        strcmp(err_code_abi, "i32") != 0 ||
        strcmp(err_msg_abi, "composite") != 0) {
        return false;
    }
    if (kind == V3_RESULT_INTRINSIC_ERR) {
        if (arg_count != 1U ||
            !v3_wasm_emit_local_get(body, dest_addr_local_index) ||
            ((err_offset + err_code_offset) != 0 &&
             (!v3_wasm_emit_i32_const(body, err_offset + err_code_offset) ||
              !v3_wasm_emit_i32_add(body))) ||
            !v3_wasm_emit_i32_const(body, 1) ||
            !v3_wasm_emit_store_scalar_to_top_address(body, "i32")) {
            return false;
        }
        return v3_wasm_emit_local_get(body, dest_addr_local_index) &&
               ((err_offset + err_msg_offset) == 0 ||
                (v3_wasm_emit_i32_const(body, err_offset + err_msg_offset) &&
                 v3_wasm_emit_i32_add(body))) &&
               v3_wasm_emit_local_set(body, field_addr_local_index) &&
               v3_wasm_emit_composite_expr_into_local_address(plan,
                                                              lowering,
                                                              function,
                                                              ctx,
                                                              args[0],
                                                              err_msg_type,
                                                              field_addr_local_index,
                                                              imports,
                                                              import_count,
                                                              body);
    }
    if (kind == V3_RESULT_INTRINSIC_ERR_CODE) {
        if (arg_count != 2U ||
            !v3_wasm_emit_local_get(body, dest_addr_local_index) ||
            ((err_offset + err_code_offset) != 0 &&
             (!v3_wasm_emit_i32_const(body, err_offset + err_code_offset) ||
              !v3_wasm_emit_i32_add(body))) ||
            !v3_wasm_emit_expr(plan, lowering, function, ctx, args[0], imports, import_count, body) ||
            !v3_wasm_emit_store_scalar_to_top_address(body, "i32")) {
            return false;
        }
        return v3_wasm_emit_local_get(body, dest_addr_local_index) &&
               ((err_offset + err_msg_offset) == 0 ||
                (v3_wasm_emit_i32_const(body, err_offset + err_msg_offset) &&
                 v3_wasm_emit_i32_add(body))) &&
               v3_wasm_emit_local_set(body, field_addr_local_index) &&
               v3_wasm_emit_composite_expr_into_local_address(plan,
                                                              lowering,
                                                              function,
                                                              ctx,
                                                              args[1],
                                                              err_msg_type,
                                                              field_addr_local_index,
                                                              imports,
                                                              import_count,
                                                              body);
    }
    return false;
}

static bool v3_wasm_emit_constructor_into_local_address(const V3SystemLinkPlanStub *plan,
                                                        const V3LoweringPlanStub *lowering,
                                                        const V3LoweredFunctionStub *function,
                                                        const V3WasmFunctionContext *ctx,
                                                        const char *callee,
                                                        char args[][4096],
                                                        size_t arg_count,
                                                        const char *dest_type_text,
                                                        uint32_t dest_addr_local_index,
                                                        const V3WasmImportStub *imports,
                                                        size_t import_count,
                                                        V3WasmBuffer *body) {
    size_t i;
    uint32_t field_addr_local_index = 0U;
    char normalized_dest_type[256];
    char seen_fields[CHENG_V3_MAX_CALL_ARGS][128];
    size_t seen_count = 0U;
    V3TypeLayoutStub layout;
    if (plan == NULL || lowering == NULL || function == NULL || ctx == NULL || callee == NULL ||
        args == NULL || dest_type_text == NULL || body == NULL || !ctx->has_scratch_locals) {
        return false;
    }
    if (!v3_normalize_type_text(lowering,
                                function->owner_module_path,
                                ctx->aliases,
                                ctx->alias_count,
                                dest_type_text,
                                normalized_dest_type,
                                sizeof(normalized_dest_type)) ||
        !v3_compute_type_layout_impl(lowering, normalized_dest_type, &layout, 0U)) {
        return false;
    }
    field_addr_local_index = v3_wasm_pick_temp_local_excluding(ctx, dest_addr_local_index);
    if (field_addr_local_index == 0U) {
        return false;
    }
    if (v3_is_tuple_type_text(normalized_dest_type) &&
        !v3_tuple_args_cover_all_items(normalized_dest_type,
                                       (const char (*)[4096])args,
                                       arg_count)) {
        return false;
    }
    if (!v3_wasm_emit_zero_region_from_local(body, dest_addr_local_index, layout.size)) {
        return false;
    }
    (void)callee;
    for (i = 0U; i < arg_count; ++i) {
        char field_name[128];
        char field_expr[4096];
        char field_type[256];
        char field_abi[32];
        int32_t field_offset = 0;
        size_t seen_index;
        if (!v3_parse_named_arg_meta(args[i],
                                     field_name,
                                     sizeof(field_name),
                                     field_expr,
                                     sizeof(field_expr)) ||
            !v3_resolve_field_meta(lowering,
                                   normalized_dest_type,
                                   field_name,
                                   field_type,
                                   sizeof(field_type),
                                   field_abi,
                                   sizeof(field_abi),
                                   &field_offset)) {
            return false;
        }
        for (seen_index = 0U; seen_index < seen_count; ++seen_index) {
            if (strcmp(seen_fields[seen_index], field_name) == 0) {
                return false;
            }
        }
        if (seen_count >= CHENG_V3_MAX_CALL_ARGS) {
            return false;
        }
        snprintf(seen_fields[seen_count], sizeof(seen_fields[seen_count]), "%s", field_name);
        seen_count += 1U;
        if (strcmp(field_abi, "composite") == 0) {
            if (!v3_wasm_emit_local_get(body, dest_addr_local_index) ||
                (field_offset != 0 &&
                 (!v3_wasm_emit_i32_const(body, field_offset) ||
                  !v3_wasm_emit_i32_add(body))) ||
                !v3_wasm_emit_local_set(body, field_addr_local_index) ||
                !v3_wasm_emit_composite_expr_into_local_address(plan,
                                                                lowering,
                                                                function,
                                                                ctx,
                                                                field_expr,
                                                                field_type,
                                                                field_addr_local_index,
                                                                imports,
                                                                import_count,
                                                                body)) {
                return false;
            }
            continue;
        }
        if (!v3_wasm_abi_uses_i32_param_slot(field_abi) ||
            !v3_wasm_emit_local_get(body, dest_addr_local_index) ||
            (field_offset != 0 &&
             (!v3_wasm_emit_i32_const(body, field_offset) ||
              !v3_wasm_emit_i32_add(body))) ||
            !v3_wasm_emit_expr(plan,
                               lowering,
                               function,
                               ctx,
                               field_expr,
                               imports,
                               import_count,
                               body) ||
            !v3_wasm_emit_store_scalar_to_top_address(body, field_abi)) {
            return false;
        }
    }
    return true;
}

static bool v3_wasm_analyze_result_intrinsic_expr(const V3SystemLinkPlanStub *plan,
                                                  const V3LoweringPlanStub *lowering,
                                                  const V3LoweredFunctionStub *function,
                                                  V3WasmFunctionContext *ctx,
                                                  V3ResultIntrinsicKind kind,
                                                  const char *arg_expr,
                                                  V3WasmImportStub *imports,
                                                  size_t *import_count) {
    char arg_type[256];
    char arg_abi[32];
    if (plan == NULL || lowering == NULL || function == NULL || ctx == NULL || arg_expr == NULL) {
        return false;
    }
    if (!v3_infer_expr_type(plan,
                            lowering,
                            function,
                            ctx->aliases,
                            ctx->alias_count,
                            ctx->locals,
                            ctx->local_count,
                            arg_expr,
                            arg_type,
                            sizeof(arg_type),
                            arg_abi,
                            sizeof(arg_abi)) ||
        strcmp(arg_abi, "composite") != 0 ||
        !v3_startswith(arg_type, "Result[")) {
        return false;
    }
    switch (kind) {
        case V3_RESULT_INTRINSIC_IS_OK:
        case V3_RESULT_INTRINSIC_IS_ERR:
        case V3_RESULT_INTRINSIC_VALUE:
        case V3_RESULT_INTRINSIC_ERROR:
        case V3_RESULT_INTRINSIC_ERROR_TEXT:
        case V3_RESULT_INTRINSIC_ERROR_INFO_OF:
            return v3_wasm_analyze_expr(plan, lowering, function, ctx, arg_expr, imports, import_count);
        default:
            return false;
    }
}

static bool v3_wasm_emit_result_intrinsic_expr(const V3SystemLinkPlanStub *plan,
                                               const V3LoweringPlanStub *lowering,
                                               const V3LoweredFunctionStub *function,
                                               const V3WasmFunctionContext *ctx,
                                               V3ResultIntrinsicKind kind,
                                               const char *arg_expr,
                                               const V3WasmImportStub *imports,
                                               size_t import_count,
                                               V3WasmBuffer *body) {
    char arg_type[256];
    char arg_abi[32];
    char field_type[256];
    char field_abi[32];
    char err_type[256];
    char err_abi[32];
    char msg_type[256];
    char msg_abi[32];
    int32_t field_offset = 0;
    int32_t err_offset = 0;
    int32_t msg_offset = 0;
    if (plan == NULL || lowering == NULL || function == NULL || ctx == NULL || arg_expr == NULL || body == NULL) {
        return false;
    }
    if (!v3_infer_expr_type(plan,
                            lowering,
                            function,
                            ctx->aliases,
                            ctx->alias_count,
                            ctx->locals,
                            ctx->local_count,
                            arg_expr,
                            arg_type,
                            sizeof(arg_type),
                            arg_abi,
                            sizeof(arg_abi)) ||
        strcmp(arg_abi, "composite") != 0 ||
        !v3_startswith(arg_type, "Result[") ||
        !v3_wasm_emit_expr(plan, lowering, function, ctx, arg_expr, imports, import_count, body)) {
        return false;
    }
    if (kind == V3_RESULT_INTRINSIC_IS_OK || kind == V3_RESULT_INTRINSIC_IS_ERR) {
        if (!v3_resolve_field_meta(lowering,
                                   arg_type,
                                   "ok",
                                   field_type,
                                   sizeof(field_type),
                                   field_abi,
                                   sizeof(field_abi),
                                   &field_offset) ||
            strcmp(field_abi, "bool") != 0 ||
            !v3_wasm_emit_field_access_from_base(body, field_abi, field_offset)) {
            return false;
        }
        if (kind == V3_RESULT_INTRINSIC_IS_ERR) {
            return v3_wasm_append_u8(body, 0x45U);
        }
        return true;
    }
    if (kind == V3_RESULT_INTRINSIC_VALUE) {
        return v3_resolve_field_meta(lowering,
                                     arg_type,
                                     "value",
                                     field_type,
                                     sizeof(field_type),
                                     field_abi,
                                     sizeof(field_abi),
                                     &field_offset) &&
               v3_wasm_emit_field_access_from_base(body, field_abi, field_offset);
    }
    if (kind == V3_RESULT_INTRINSIC_ERROR_INFO_OF) {
        return v3_resolve_field_meta(lowering,
                                     arg_type,
                                     "err",
                                     field_type,
                                     sizeof(field_type),
                                     field_abi,
                                     sizeof(field_abi),
                                     &field_offset) &&
               strcmp(field_abi, "composite") == 0 &&
               v3_wasm_emit_field_access_from_base(body, field_abi, field_offset);
    }
    if (kind != V3_RESULT_INTRINSIC_ERROR && kind != V3_RESULT_INTRINSIC_ERROR_TEXT) {
        return false;
    }
    if (!v3_resolve_field_meta(lowering,
                               arg_type,
                               "err",
                               err_type,
                               sizeof(err_type),
                               err_abi,
                               sizeof(err_abi),
                               &err_offset) ||
        strcmp(err_abi, "composite") != 0 ||
        !v3_wasm_emit_field_access_from_base(body, err_abi, err_offset) ||
        !v3_resolve_field_meta(lowering,
                               err_type,
                               "msg",
                               msg_type,
                               sizeof(msg_type),
                               msg_abi,
                               sizeof(msg_abi),
                               &msg_offset) ||
        strcmp(msg_abi, "composite") != 0) {
        return false;
    }
    return v3_wasm_emit_field_access_from_base(body, msg_abi, msg_offset);
}

static bool v3_wasm_emit_composite_pointer_value_into_local_address(const V3WasmFunctionContext *ctx,
                                                                    uint32_t dest_addr_local_index,
                                                                    int32_t copy_size,
                                                                    V3WasmBuffer *body) {
    uint32_t result_local_index = 0U;
    if (ctx == NULL || body == NULL || copy_size < 0) {
        return false;
    }
    result_local_index =
        dest_addr_local_index == ctx->scratch3_local_index
        ? ctx->scratch2_local_index
        : ctx->scratch3_local_index;
    return v3_wasm_emit_local_set(body, result_local_index) &&
           v3_wasm_emit_copy_region_between_locals(body,
                                                   dest_addr_local_index,
                                                   result_local_index,
                                                   copy_size);
}

static uint32_t v3_wasm_pick_temp_local_excluding(const V3WasmFunctionContext *ctx,
                                                  uint32_t avoid_local_index) {
    if (ctx == NULL) {
        return 0U;
    }
    if (ctx->scratch_local_index != avoid_local_index) {
        return ctx->scratch_local_index;
    }
    if (ctx->scratch2_local_index != avoid_local_index) {
        return ctx->scratch2_local_index;
    }
    if (ctx->scratch3_local_index != avoid_local_index) {
        return ctx->scratch3_local_index;
    }
    if (ctx->scratch4_local_index != 0U &&
        ctx->scratch4_local_index != avoid_local_index) {
        return ctx->scratch4_local_index;
    }
    if (ctx->scratch5_local_index != 0U &&
        ctx->scratch5_local_index != avoid_local_index) {
        return ctx->scratch5_local_index;
    }
    return 0U;
}

static uint32_t v3_wasm_pick_lvalue_preserve_local(const V3WasmFunctionContext *ctx,
                                                   uint32_t avoid_local_index) {
    if (ctx == NULL) {
        return 0U;
    }
    if (ctx->scratch5_local_index != 0U &&
        ctx->scratch5_local_index != avoid_local_index) {
        return ctx->scratch5_local_index;
    }
    if (ctx->scratch4_local_index != 0U &&
        ctx->scratch4_local_index != avoid_local_index) {
        return ctx->scratch4_local_index;
    }
    if (ctx->scratch3_local_index != avoid_local_index) {
        return ctx->scratch3_local_index;
    }
    return 0U;
}

static bool v3_wasm_emit_composite_result_intrinsic_into_local_address(const V3SystemLinkPlanStub *plan,
                                                                       const V3LoweringPlanStub *lowering,
                                                                       const V3LoweredFunctionStub *function,
                                                                       const V3WasmFunctionContext *ctx,
                                                                       V3ResultIntrinsicKind kind,
                                                                       const char *arg_expr,
                                                                       uint32_t dest_addr_local_index,
                                                                       int32_t copy_size,
                                                                       const V3WasmImportStub *imports,
                                                                       size_t import_count,
                                                                       V3WasmBuffer *body) {
    return v3_wasm_emit_result_intrinsic_expr(plan,
                                              lowering,
                                              function,
                                              ctx,
                                              kind,
                                              arg_expr,
                                              imports,
                                              import_count,
                                              body) &&
           v3_wasm_emit_composite_pointer_value_into_local_address(ctx,
                                                                   dest_addr_local_index,
                                                                   copy_size,
                                                                   body);
}

static bool v3_wasm_expr_matches_composite_default(const V3LoweringPlanStub *lowering,
                                                   const V3LoweredFunctionStub *function,
                                                   const V3WasmFunctionContext *ctx,
                                                   const char *expr_text,
                                                   const char *target_type_text) {
    char expr[4096];
    char default_type[CHENG_V3_MAX_TYPE_TEXT];
    char normalized_target_type[CHENG_V3_MAX_TYPE_TEXT];
    char normalized_default_type[CHENG_V3_MAX_TYPE_TEXT];
    if (lowering == NULL || function == NULL || ctx == NULL || expr_text == NULL || target_type_text == NULL) {
        return false;
    }
    v3_wasm_trim_wrapping_parens(expr_text, expr, sizeof(expr));
    if (!v3_normalize_type_text(lowering,
                                function->owner_module_path,
                                ctx->aliases,
                                ctx->alias_count,
                                target_type_text,
                                normalized_target_type,
                                sizeof(normalized_target_type))) {
        return false;
    }
    if ((strcmp(expr, "[]") == 0 && v3_is_seq_type_text(normalized_target_type)) ||
        (strcmp(expr, "\"\"") == 0 && strcmp(normalized_target_type, "str") == 0)) {
        return true;
    }
    if (!v3_parse_default_expr_text(expr, default_type, sizeof(default_type))) {
        return false;
    }
    return v3_normalize_type_text(lowering,
                                  function->owner_module_path,
                                  ctx->aliases,
                                  ctx->alias_count,
                                  default_type,
                                  normalized_default_type,
                                  sizeof(normalized_default_type)) &&
           strcmp(normalized_default_type, normalized_target_type) == 0;
}

static bool v3_wasm_seq_elem_size(const V3LoweringPlanStub *lowering,
                                  const char *seq_type_text,
                                  char *elem_type_out,
                                  size_t elem_type_cap,
                                  char *elem_abi_out,
                                  size_t elem_abi_cap,
                                  int32_t *elem_size_out) {
    V3TypeLayoutStub elem_layout;
    if (lowering == NULL || seq_type_text == NULL || elem_type_out == NULL || elem_abi_out == NULL ||
        elem_size_out == NULL || strlen(seq_type_text) <= 2U ||
        strcmp(seq_type_text + strlen(seq_type_text) - 2U, "[]") != 0) {
        return false;
    }
    snprintf(elem_type_out,
             elem_type_cap,
             "%.*s",
             (int)(strlen(seq_type_text) - 2U),
             seq_type_text);
    snprintf(elem_abi_out, elem_abi_cap, "%s", v3_type_abi_class(elem_type_out));
    if (strcmp(elem_abi_out, "i8") == 0 ||
        strcmp(elem_abi_out, "u8") == 0 ||
        strcmp(elem_abi_out, "bool") == 0) {
        *elem_size_out = 1;
        return true;
    }
    if (strcmp(elem_abi_out, "i16") == 0 ||
        strcmp(elem_abi_out, "u16") == 0) {
        *elem_size_out = 2;
        return true;
    }
    if (strcmp(elem_abi_out, "i32") == 0 ||
        strcmp(elem_abi_out, "u32") == 0) {
        *elem_size_out = 4;
        return true;
    }
    if (strcmp(elem_abi_out, "i64") == 0 || strcmp(elem_abi_out, "ptr") == 0) {
        *elem_size_out = 8;
        return true;
    }
    if (!v3_compute_type_layout_impl(lowering, elem_type_out, &elem_layout, 0U) ||
        elem_layout.size <= 0) {
        return false;
    }
    *elem_size_out = elem_layout.size;
    return true;
}

static bool v3_wasm_emit_call_target(const V3LoweringPlanStub *lowering,
                                     const V3AsmCallTarget *target,
                                     const V3WasmImportStub *imports,
                                     size_t import_count,
                                     V3WasmBuffer *body);

static bool v3_wasm_emit_lvalue_address(const V3SystemLinkPlanStub *plan,
                                        const V3LoweringPlanStub *lowering,
                                        const V3LoweredFunctionStub *function,
                                        const V3WasmFunctionContext *ctx,
                                        const char *expr_text,
                                        const V3WasmImportStub *imports,
                                        size_t import_count,
                                        V3WasmBuffer *body);

static bool v3_wasm_ensure_composite_call_arg_local(const V3SystemLinkPlanStub *plan,
                                                    const V3LoweringPlanStub *lowering,
                                                    const V3LoweredFunctionStub *function,
                                                    V3WasmFunctionContext *ctx,
                                                    const char *callee,
                                                    size_t arg_index,
                                                    const char *arg_expr,
                                                    const char *preferred_type,
                                                    const char *preferred_abi);

static bool v3_wasm_register_builtin_add_import(V3WasmImportStub *imports,
                                                size_t *import_count) {
    V3AsmCallTarget target;
    memset(&target, 0, sizeof(target));
    target.external = true;
    target.param_count = 3U;
    snprintf(target.symbol_name, sizeof(target.symbol_name), "%s", "cheng_seq_set_grow");
    snprintf(target.return_type, sizeof(target.return_type), "%s", "ptr");
    snprintf(target.return_abi_class, sizeof(target.return_abi_class), "%s", "ptr");
    snprintf(target.param_types[0], sizeof(target.param_types[0]), "%s", "ptr");
    snprintf(target.param_types[1], sizeof(target.param_types[1]), "%s", "int32");
    snprintf(target.param_types[2], sizeof(target.param_types[2]), "%s", "int32");
    snprintf(target.param_abi_classes[0], sizeof(target.param_abi_classes[0]), "%s", "ptr");
    snprintf(target.param_abi_classes[1], sizeof(target.param_abi_classes[1]), "%s", "i32");
    snprintf(target.param_abi_classes[2], sizeof(target.param_abi_classes[2]), "%s", "i32");
    return v3_wasm_register_import("wasm_builtin_add", &target, imports, import_count);
}

static bool v3_wasm_analyze_builtin_statement(const V3SystemLinkPlanStub *plan,
                                              const V3LoweringPlanStub *lowering,
                                              const V3LoweredFunctionStub *function,
                                              V3WasmFunctionContext *ctx,
                                              const char *callee,
                                              char args[][4096],
                                              size_t arg_count,
                                              V3WasmImportStub *imports,
                                              size_t *import_count,
                                              bool *handled_out) {
    char seq_type[256];
    char seq_abi[32];
    char elem_type[256];
    char elem_abi[32];
    int32_t elem_size = 0;
    if (handled_out == NULL) {
        return false;
    }
    *handled_out = false;
    if (strcmp(callee, "add") != 0 || arg_count != 2U) {
        return true;
    }
    *handled_out = true;
    if (!v3_infer_expr_type(plan,
                            lowering,
                            function,
                            ctx->aliases,
                            ctx->alias_count,
                            ctx->locals,
                            ctx->local_count,
                            args[0],
                            seq_type,
                            sizeof(seq_type),
                            seq_abi,
                            sizeof(seq_abi)) ||
        strcmp(seq_abi, "composite") != 0 ||
        !v3_wasm_seq_elem_size(lowering,
                               seq_type,
                               elem_type,
                               sizeof(elem_type),
                               elem_abi,
                               sizeof(elem_abi),
                               &elem_size) ||
        elem_size <= 0 ||
        !v3_wasm_analyze_expr(plan, lowering, function, ctx, args[0], imports, import_count) ||
        !v3_wasm_register_builtin_add_import(imports, import_count)) {
        return false;
    }
    if (strcmp(elem_abi, "composite") == 0) {
        return v3_wasm_ensure_composite_call_arg_local(plan,
                                                       lowering,
                                                       function,
                                                       ctx,
                                                       callee,
                                                       1U,
                                                       args[1],
                                                       elem_type,
                                                       elem_abi) &&
               v3_wasm_prepare_composite_expr(plan,
                                              lowering,
                                              function,
                                              ctx,
                                              args[1],
                                              elem_type,
                                              imports,
                                              import_count);
    }
    if (!v3_wasm_analyze_expr(plan, lowering, function, ctx, args[1], imports, import_count)) {
        return false;
    }
    return true;
}

static bool v3_wasm_emit_builtin_statement(const V3SystemLinkPlanStub *plan,
                                           const V3LoweringPlanStub *lowering,
                                           const V3LoweredFunctionStub *function,
                                           const V3WasmFunctionContext *ctx,
                                           const char *callee,
                                           char args[][4096],
                                           size_t arg_count,
                                           const V3WasmImportStub *imports,
                                           size_t import_count,
                                           V3WasmBuffer *body,
                                           bool *handled_out) {
    char seq_type[256];
    char seq_abi[32];
    char elem_type[256];
    char elem_abi[32];
    char len_field_type[256];
    char len_field_abi[32];
    int32_t len_field_offset = 0;
    int32_t elem_size = 0;
    V3AsmCallTarget target;
    if (handled_out == NULL || plan == NULL || lowering == NULL || function == NULL || ctx == NULL ||
        callee == NULL || args == NULL || body == NULL || !ctx->has_scratch_locals) {
        return false;
    }
    *handled_out = false;
    if (strcmp(callee, "add") != 0 || arg_count != 2U) {
        return true;
    }
    *handled_out = true;
    if (!v3_infer_expr_type(plan,
                            lowering,
                            function,
                            ctx->aliases,
                            ctx->alias_count,
                            ctx->locals,
                            ctx->local_count,
                            args[0],
                            seq_type,
                            sizeof(seq_type),
                            seq_abi,
                            sizeof(seq_abi)) ||
        strcmp(seq_abi, "composite") != 0 ||
        !v3_wasm_seq_elem_size(lowering,
                               seq_type,
                               elem_type,
                               sizeof(elem_type),
                               elem_abi,
                               sizeof(elem_abi),
                               &elem_size) ||
        !v3_resolve_field_meta(lowering,
                               seq_type,
                               "len",
                               len_field_type,
                               sizeof(len_field_type),
                               len_field_abi,
                               sizeof(len_field_abi),
                               &len_field_offset) ||
        strcmp(len_field_abi, "i32") != 0 ||
        !v3_wasm_emit_lvalue_address(plan,
                                     lowering,
                                     function,
                                     ctx,
                                     args[0],
                                     imports,
                                     import_count,
                                     body) ||
        !v3_wasm_emit_local_set(body, ctx->scratch_local_index) ||
        !v3_wasm_emit_local_get(body, ctx->scratch_local_index) ||
        !v3_wasm_emit_local_get(body, ctx->scratch_local_index) ||
        !v3_wasm_emit_field_access_from_base(body, len_field_abi, len_field_offset) ||
        !v3_wasm_emit_i32_const(body, elem_size)) {
        return false;
    }
    memset(&target, 0, sizeof(target));
    target.external = true;
    target.param_count = 3U;
    snprintf(target.symbol_name, sizeof(target.symbol_name), "%s", "cheng_seq_set_grow");
    snprintf(target.return_type, sizeof(target.return_type), "%s", "ptr");
    snprintf(target.return_abi_class, sizeof(target.return_abi_class), "%s", "ptr");
    snprintf(target.param_abi_classes[0], sizeof(target.param_abi_classes[0]), "%s", "ptr");
    snprintf(target.param_abi_classes[1], sizeof(target.param_abi_classes[1]), "%s", "i32");
    snprintf(target.param_abi_classes[2], sizeof(target.param_abi_classes[2]), "%s", "i32");
    if (!v3_wasm_emit_call_target(lowering, &target, imports, import_count, body) ||
        !v3_wasm_emit_local_set(body, ctx->scratch2_local_index) ||
        !v3_wasm_emit_local_get(body, ctx->scratch2_local_index) ||
        !v3_wasm_emit_local_set(body, ctx->scratch5_local_index)) {
        return false;
    }
    if (strcmp(elem_abi, "composite") == 0) {
        char temp_name[128];
        char value_type[256];
        char value_abi[32];
        int32_t temp_local_slot = -1;
        int32_t value_local_slot = v3_wasm_find_local_slot_index(ctx, args[1]);
        uint32_t src_addr_local_index = ctx->scratch4_local_index;
        if (value_local_slot >= 0 &&
            strcmp(ctx->locals[value_local_slot].abi_class, "composite") == 0) {
            return v3_wasm_emit_copy_region_between_locals(body,
                                                           ctx->scratch5_local_index,
                                                           ctx->local_indices[value_local_slot],
                                                           elem_size);
        }
        if (src_addr_local_index != 0U &&
            v3_infer_lvalue_expr_type(plan,
                                      lowering,
                                      function,
                                      ctx->aliases,
                                      ctx->alias_count,
                                      ctx->locals,
                                      ctx->local_count,
                                      args[1],
                                      value_type,
                                      sizeof(value_type),
                                      value_abi,
                                      sizeof(value_abi)) &&
            strcmp(value_abi, "composite") == 0 &&
            v3_wasm_emit_lvalue_address(plan,
                                        lowering,
                                        function,
                                        ctx,
                                        args[1],
                                        imports,
                                        import_count,
                                        body) &&
            v3_wasm_emit_local_set(body, src_addr_local_index)) {
            return v3_wasm_emit_copy_region_between_locals(body,
                                                           ctx->scratch5_local_index,
                                                           src_addr_local_index,
                                                           elem_size);
        }
        v3_call_arg_temp_name(function,
                              callee,
                              1U,
                              args[1],
                              temp_name,
                              sizeof(temp_name));
        temp_local_slot = v3_wasm_find_local_slot_index(ctx, temp_name);
        if (temp_local_slot < 0 ||
            strcmp(ctx->locals[temp_local_slot].abi_class, "composite") != 0) {
            fprintf(stderr,
                    "[cheng_v3_seed] wasm builtin add composite temp missing function=%s expr=%s type=%s\n",
                    function->symbol_text,
                    args[1],
                    elem_type);
            return false;
        }
        return v3_wasm_emit_composite_expr_into_local_address(plan,
                                                              lowering,
                                                              function,
                                                              ctx,
                                                              args[1],
                                                              elem_type,
                                                              ctx->local_indices[temp_local_slot],
                                                              imports,
                                                              import_count,
                                                              body) &&
               v3_wasm_emit_copy_region_between_locals(body,
                                                       ctx->scratch5_local_index,
                                                       ctx->local_indices[temp_local_slot],
                                                       elem_size);
    }
    if (!v3_wasm_abi_uses_i32_param_slot(elem_abi)) {
        return false;
    }
    return v3_wasm_emit_local_get(body, ctx->scratch5_local_index) &&
           v3_wasm_emit_expr(plan, lowering, function, ctx, args[1], imports, import_count, body) &&
           v3_wasm_emit_store_scalar_to_top_address(body, elem_abi);
}

static bool v3_wasm_ensure_composite_call_arg_local(const V3SystemLinkPlanStub *plan,
                                                    const V3LoweringPlanStub *lowering,
                                                    const V3LoweredFunctionStub *function,
                                                    V3WasmFunctionContext *ctx,
                                                    const char *callee,
                                                    size_t arg_index,
                                                    const char *arg_expr,
                                                    const char *preferred_type,
                                                    const char *preferred_abi) {
    char named_field[128];
    char named_expr[4096];
    char temp_name[128];
    char value_type[CHENG_V3_MAX_TYPE_TEXT];
    char value_abi[32];
    const char *value_expr = arg_expr;
    if (plan == NULL || lowering == NULL || function == NULL || ctx == NULL ||
        callee == NULL || arg_expr == NULL) {
        return false;
    }
    if (v3_parse_named_arg_meta(arg_expr,
                                named_field,
                                sizeof(named_field),
                                named_expr,
                                sizeof(named_expr))) {
        value_expr = named_expr;
    }
    if (v3_wasm_find_local_slot_index(ctx, value_expr) >= 0) {
        return true;
    }
    v3_call_arg_temp_name(function,
                          callee,
                          arg_index,
                          value_expr,
                          temp_name,
                          sizeof(temp_name));
    if (v3_wasm_find_local_slot_index(ctx, temp_name) >= 0) {
        return true;
    }
    if (preferred_type != NULL && preferred_type[0] != '\0' &&
        preferred_abi != NULL && preferred_abi[0] != '\0' &&
        !v3_abi_class_scalar_or_ptr(preferred_abi)) {
        snprintf(value_type, sizeof(value_type), "%s", preferred_type);
        snprintf(value_abi, sizeof(value_abi), "%s", preferred_abi);
    } else if (!v3_infer_expr_type(plan,
                                   lowering,
                                   function,
                                   ctx->aliases,
                                   ctx->alias_count,
                                   ctx->locals,
                                   ctx->local_count,
                                   value_expr,
                                   value_type,
                                   sizeof(value_type),
                                   value_abi,
                                   sizeof(value_abi))) {
        return false;
    }
    if (v3_abi_class_scalar_or_ptr(value_abi)) {
        return true;
    }
    return v3_wasm_context_add_storage_local(lowering,
                                             ctx,
                                             temp_name,
                                             value_type,
                                             value_abi,
                                             (uint32_t)ctx->local_count);
}

static bool v3_wasm_emit_call_arg_value(const V3SystemLinkPlanStub *plan,
                                        const V3LoweringPlanStub *lowering,
                                        const V3LoweredFunctionStub *function,
                                        const V3WasmFunctionContext *ctx,
                                        const char *callee,
                                        size_t arg_index,
                                        const char *arg_expr,
                                        const char *param_type,
                                        const char *param_abi,
                                        const V3WasmImportStub *imports,
                                        size_t import_count,
                                        V3WasmBuffer *body) {
    char named_field[128];
    char named_expr[4096];
    char temp_name[128];
    const char *value_expr = arg_expr;
    int32_t local_slot = -1;
    if (plan == NULL || lowering == NULL || function == NULL || ctx == NULL ||
        callee == NULL || arg_expr == NULL || param_abi == NULL || body == NULL) {
        return false;
    }
    if (v3_parse_named_arg_meta(arg_expr,
                                named_field,
                                sizeof(named_field),
                                named_expr,
                                sizeof(named_expr))) {
        value_expr = named_expr;
    }
    if (v3_abi_class_scalar_or_ptr(param_abi)) {
        return v3_wasm_emit_expr(plan,
                                 lowering,
                                 function,
                                 ctx,
                                 value_expr,
                                 imports,
                                 import_count,
                                 body);
    }
    local_slot = v3_wasm_find_local_slot_index(ctx, value_expr);
    if (local_slot >= 0 &&
        strcmp(ctx->locals[local_slot].abi_class, "composite") == 0) {
        return v3_wasm_emit_local_get(body, ctx->local_indices[local_slot]);
    }
    {
        char value_type[256];
        char value_abi[32];
        if (v3_infer_lvalue_expr_type(plan,
                                      lowering,
                                      function,
                                      ctx->aliases,
                                      ctx->alias_count,
                                      ctx->locals,
                                      ctx->local_count,
                                      value_expr,
                                      value_type,
                                      sizeof(value_type),
                                      value_abi,
                                      sizeof(value_abi)) &&
            strcmp(value_abi, "composite") == 0) {
            return v3_wasm_emit_lvalue_address(plan,
                                               lowering,
                                               function,
                                               ctx,
                                               value_expr,
                                               imports,
                                               import_count,
                                               body);
        }
    }
    v3_call_arg_temp_name(function,
                          callee,
                          arg_index,
                          value_expr,
                          temp_name,
                          sizeof(temp_name));
    local_slot = v3_wasm_find_local_slot_index(ctx, temp_name);
    if (local_slot >= 0 &&
        strcmp(ctx->locals[local_slot].abi_class, "composite") == 0) {
        return v3_wasm_emit_composite_expr_into_local_address(plan,
                                                              lowering,
                                                              function,
                                                              ctx,
                                                              value_expr,
                                                              param_type,
                                                              ctx->local_indices[local_slot],
                                                              imports,
                                                              import_count,
                                                              body) &&
               v3_wasm_emit_local_get(body, ctx->local_indices[local_slot]);
    }
    return v3_wasm_emit_expr(plan,
                             lowering,
                             function,
                             ctx,
                             value_expr,
                             imports,
                             import_count,
                             body);
}

static bool v3_wasm_analyze_call(const V3SystemLinkPlanStub *plan,
                                 const V3LoweringPlanStub *lowering,
                                 const V3LoweredFunctionStub *function,
                                 V3WasmFunctionContext *ctx,
                                 const char *callee,
                                 char args[][4096],
                                 size_t arg_count,
                                 V3WasmImportStub *imports,
                                 size_t *import_count) {
    V3AsmCallTarget target;
    size_t i;
    if (!v3_resolve_call_target_with_args(plan,
                                          lowering,
                                          function,
                                          ctx->aliases,
                                          ctx->alias_count,
                                          ctx->locals,
                                          ctx->local_count,
                                          callee,
                                          args,
                                          arg_count,
                                          &target)) {
        fprintf(stderr,
                "[cheng_v3_seed] wasm call target resolve failed function=%s callee=%s\n",
                function->symbol_text,
                callee);
        return false;
    }
    if (!v3_wasm_abi_uses_i32_return_slot(target.return_abi_class) || target.param_count != arg_count) {
        fprintf(stderr,
                "[cheng_v3_seed] wasm call signature reject function=%s callee=%s ret=%s argc=%zu expected=%zu\n",
                function->symbol_text,
                callee,
                target.return_abi_class,
                target.param_count,
                arg_count);
        return false;
    }
    for (i = 0U; i < arg_count; ++i) {
        if (!v3_wasm_abi_uses_i32_param_slot(target.param_abi_classes[i])) {
            return false;
        }
        if (strcmp(target.param_abi_classes[i], "composite") == 0) {
            if (!v3_wasm_ensure_composite_call_arg_local(plan,
                                                         lowering,
                                                         function,
                                                         ctx,
                                                         callee,
                                                         i,
                                                         args[i],
                                                         target.param_types[i],
                                                         target.param_abi_classes[i]) ||
                !v3_wasm_prepare_composite_expr(plan,
                                                lowering,
                                                function,
                                                ctx,
                                                args[i],
                                                target.param_types[i],
                                                imports,
                                                import_count)) {
                return false;
            }
            continue;
        }
        if (!v3_wasm_analyze_expr(plan, lowering, function, ctx, args[i], imports, import_count)) {
            return false;
        }
    }
    return v3_wasm_register_import(function->symbol_text, &target, imports, import_count);
}

static bool v3_wasm_analyze_call_statement(const V3SystemLinkPlanStub *plan,
                                           const V3LoweringPlanStub *lowering,
                                           const V3LoweredFunctionStub *function,
                                           V3WasmFunctionContext *ctx,
                                           const char *callee,
                                           char args[][4096],
                                           size_t arg_count,
                                           V3WasmImportStub *imports,
                                           size_t *import_count) {
    bool handled = false;
    V3AsmCallTarget target;
    size_t i;
    if (!v3_wasm_analyze_builtin_statement(plan,
                                           lowering,
                                           function,
                                           ctx,
                                           callee,
                                           args,
                                           arg_count,
                                           imports,
                                           import_count,
                                           &handled)) {
        return false;
    }
    if (handled) {
        return true;
    }
    if (!v3_resolve_call_target_with_args(plan,
                                          lowering,
                                          function,
                                          ctx->aliases,
                                          ctx->alias_count,
                                          ctx->locals,
                                          ctx->local_count,
                                          callee,
                                          args,
                                          arg_count,
                                          &target)) {
        fprintf(stderr,
                "[cheng_v3_seed] wasm statement call target resolve failed function=%s callee=%s argc=%zu\n",
                function->symbol_text,
                callee,
                arg_count);
        return false;
    }
    if (target.param_count != arg_count) {
        fprintf(stderr,
                "[cheng_v3_seed] wasm statement call argc mismatch function=%s callee=%s got=%zu expected=%zu\n",
                function->symbol_text,
                callee,
                arg_count,
                target.param_count);
        return false;
    }
    if (strcmp(target.return_abi_class, "void") != 0 &&
        !v3_wasm_abi_uses_i32_return_slot(target.return_abi_class)) {
        fprintf(stderr,
                "[cheng_v3_seed] wasm statement call ret reject function=%s callee=%s ret=%s\n",
                function->symbol_text,
                callee,
                target.return_abi_class);
        return false;
    }
    for (i = 0U; i < arg_count; ++i) {
        if (!v3_wasm_abi_uses_i32_param_slot(target.param_abi_classes[i])) {
            fprintf(stderr,
                    "[cheng_v3_seed] wasm statement call arg reject function=%s callee=%s index=%zu abi=%s\n",
                    function->symbol_text,
                    callee,
                    i,
                    target.param_abi_classes[i]);
            return false;
        }
        if (strcmp(target.param_abi_classes[i], "composite") == 0) {
            if (!v3_wasm_ensure_composite_call_arg_local(plan,
                                                         lowering,
                                                         function,
                                                         ctx,
                                                         callee,
                                                         i,
                                                         args[i],
                                                         target.param_types[i],
                                                         target.param_abi_classes[i]) ||
                !v3_wasm_prepare_composite_expr(plan,
                                                lowering,
                                                function,
                                                ctx,
                                                args[i],
                                                target.param_types[i],
                                                imports,
                                                import_count)) {
                fprintf(stderr,
                        "[cheng_v3_seed] wasm statement call composite arg reject function=%s callee=%s index=%zu type=%s expr=%s\n",
                        function->symbol_text,
                        callee,
                        i,
                        target.param_types[i],
                        args[i]);
                return false;
            }
            continue;
        }
        if (!v3_wasm_analyze_expr(plan, lowering, function, ctx, args[i], imports, import_count)) {
            fprintf(stderr,
                    "[cheng_v3_seed] wasm statement call arg reject function=%s callee=%s index=%zu abi=%s\n",
                    function->symbol_text,
                    callee,
                    i,
                    target.param_abi_classes[i]);
            return false;
        }
    }
    return v3_wasm_register_import(function->symbol_text, &target, imports, import_count);
}

static bool v3_wasm_analyze_expr(const V3SystemLinkPlanStub *plan,
                                 const V3LoweringPlanStub *lowering,
                                 const V3LoweredFunctionStub *function,
                                 V3WasmFunctionContext *ctx,
                                 const char *expr_text,
                                 V3WasmImportStub *imports,
                                 size_t *import_count) {
    static const char *OPS[] = {
        "||", "&&", "==", "!=", "<=", ">=", "<<", ">>", "<", ">", "|", "^", "&", "+", "-", "*", "/", "%"
    };
    char expr[4096];
    char inferred_type[256];
    char inferred_abi[32];
    size_t i;
    char callee[PATH_MAX];
    char arg_text[4096];
    char base_expr[4096];
    char index_expr[4096];
    char field_name[128];
    char surface_error[256];
    V3ExprSurface surface;
    v3_wasm_trim_wrapping_parens(expr_text, expr, sizeof(expr));
    if (!v3_classify_expr_surface(expr, &surface, surface_error, sizeof(surface_error))) {
        fprintf(stderr,
                "[cheng_v3_seed] wasm expr surface classify failed function=%s expr=%s reason=%s\n",
                function->symbol_text,
                expr,
                surface_error[0] != '\0' ? surface_error : "invalid expr");
        return false;
    }
    snprintf(expr, sizeof(expr), "%s", surface.text);
    if (!v3_infer_expr_type(plan,
                            lowering,
                            function,
                            ctx->aliases,
                            ctx->alias_count,
                            ctx->locals,
                            ctx->local_count,
                            expr,
                            inferred_type,
                            sizeof(inferred_type),
                            inferred_abi,
                            sizeof(inferred_abi)) ||
        !v3_wasm_abi_uses_i32_param_slot(inferred_abi)) {
        fprintf(stderr,
                "[cheng_v3_seed] wasm expr infer/type reject function=%s expr=%s abi=%s type=%s\n",
                function->symbol_text,
                expr,
                inferred_abi,
                inferred_type);
        return false;
    }
    if (surface.kind == V3_EXPR_SURFACE_IF || surface.kind == V3_EXPR_SURFACE_TERNARY) {
        return v3_wasm_analyze_expr(plan, lowering, function, ctx, surface.cond, imports, import_count) &&
               v3_wasm_analyze_expr(plan, lowering, function, ctx, surface.when_true, imports, import_count) &&
               v3_wasm_analyze_expr(plan, lowering, function, ctx, surface.when_false, imports, import_count);
    }
    if (surface.kind == V3_EXPR_SURFACE_RANGE) {
        return v3_wasm_analyze_expr(plan, lowering, function, ctx, surface.when_true, imports, import_count) &&
               v3_wasm_analyze_expr(plan, lowering, function, ctx, surface.when_false, imports, import_count);
    }
    if (expr[0] == '!' && expr[1] != '\0') {
        return v3_wasm_analyze_expr(plan, lowering, function, ctx, expr + 1, imports, import_count);
    }
    {
        char literal[4096];
        if (v3_parse_string_literal_text(expr, literal, sizeof(literal))) {
            return true;
        }
    }
    for (i = 0U; i < sizeof(OPS) / sizeof(OPS[0]); ++i) {
        int32_t op_index = v3_find_top_level_binary_op_last(expr, OPS[i]);
        if (op_index > 0) {
            char left[4096];
            char right[4096];
            char left_type[256];
            char left_abi[32];
            char right_type[256];
            char right_abi[32];
            snprintf(left, sizeof(left), "%.*s", op_index, expr);
            snprintf(right, sizeof(right), "%s", expr + op_index + strlen(OPS[i]));
            v3_trim_copy_text(left, left, sizeof(left));
            v3_trim_copy_text(right, right, sizeof(right));
            if (!v3_infer_expr_type(plan,
                                    lowering,
                                    function,
                                    ctx->aliases,
                                    ctx->alias_count,
                                    ctx->locals,
                                    ctx->local_count,
                                    left,
                                    left_type,
                                    sizeof(left_type),
                                    left_abi,
                                    sizeof(left_abi)) ||
                !v3_infer_expr_type(plan,
                                    lowering,
                                    function,
                                    ctx->aliases,
                                    ctx->alias_count,
                                    ctx->locals,
                                    ctx->local_count,
                                    right,
                                    right_type,
                                    sizeof(right_type),
                                    right_abi,
                                    sizeof(right_abi))) {
                return false;
            }
            if ((strcmp(OPS[i], "==") == 0 || strcmp(OPS[i], "!=") == 0) &&
                strcmp(left_type, "str") == 0 &&
                strcmp(right_type, "str") == 0 &&
                strcmp(left_abi, "composite") == 0 &&
                strcmp(right_abi, "composite") == 0) {
                V3AsmCallTarget target;
                ctx->needs_str_temps = true;
                return v3_wasm_prepare_composite_expr(plan,
                                                      lowering,
                                                      function,
                                                      ctx,
                                                      left,
                                                      "str",
                                                      imports,
                                                      import_count) &&
                       v3_wasm_prepare_composite_expr(plan,
                                                      lowering,
                                                      function,
                                                      ctx,
                                                      right,
                                                      "str",
                                                      imports,
                                                      import_count) &&
                       v3_wasm_build_str_eq_target(plan, &target) &&
                       v3_wasm_register_import(function->symbol_text, &target, imports, import_count);
            }
            return v3_wasm_analyze_expr(plan, lowering, function, ctx, left, imports, import_count) &&
                   v3_wasm_analyze_expr(plan, lowering, function, ctx, right, imports, import_count);
        }
    }
    if (v3_parse_index_access_expr(expr,
                                   base_expr,
                                   sizeof(base_expr),
                                   index_expr,
                                   sizeof(index_expr))) {
        char base_type[256];
        char base_abi[32];
        char normalized_base_type[256];
        char elem_type[256];
        char elem_abi[32];
        int32_t elem_size = 0;
        int32_t data_field_offset = 0;
        int32_t len_field_offset = 0;
        int32_t fixed_len = 0;
        bool base_is_fixed_array = false;
        V3AsmCallTarget bounds_target;
        if (!v3_wasm_resolve_index_expr_meta(plan,
                                             lowering,
                                             function,
                                             ctx,
                                             expr,
                                             base_expr,
                                             sizeof(base_expr),
                                             index_expr,
                                             sizeof(index_expr),
                                             base_type,
                                             sizeof(base_type),
                                             base_abi,
                                             sizeof(base_abi),
                                             normalized_base_type,
                                             sizeof(normalized_base_type),
                                             elem_type,
                                             sizeof(elem_type),
                                             elem_abi,
                                             sizeof(elem_abi),
                                             &elem_size,
                                             &data_field_offset,
                                             &len_field_offset,
                                             &fixed_len,
                                             &base_is_fixed_array)) {
            fprintf(stderr,
                    "[cheng_v3_seed] wasm index meta failed function=%s expr=%s base=%s index=%s\n",
                    function->symbol_text,
                    expr,
                    base_expr,
                    index_expr);
            return false;
        }
        if (!v3_wasm_analyze_expr(plan, lowering, function, ctx, base_expr, imports, import_count) ||
            !v3_wasm_analyze_expr(plan, lowering, function, ctx, index_expr, imports, import_count)) {
            fprintf(stderr,
                    "[cheng_v3_seed] wasm index arg analyze failed function=%s expr=%s base=%s index=%s base_type=%s normalized=%s elem=%s abi=%s\n",
                    function->symbol_text,
                    expr,
                    base_expr,
                    index_expr,
                    base_type,
                    normalized_base_type,
                    elem_type,
                    elem_abi);
            return false;
        }
        if (!v3_wasm_build_bounds_check_target(&bounds_target) ||
            !v3_wasm_register_import(function->symbol_text, &bounds_target, imports, import_count)) {
            fprintf(stderr,
                    "[cheng_v3_seed] wasm index bounds import failed function=%s expr=%s\n",
                    function->symbol_text,
                    expr);
            return false;
        }
        return true;
    }
    {
        char (*args)[4096] = v3_alloc_call_arg_scratch();
        size_t arg_count = 0U;
        bool parsed = v3_parse_call_text(expr, callee, sizeof(callee), args, &arg_count, CHENG_V3_MAX_CALL_ARGS);
        if (parsed) {
            V3ResultIntrinsicKind result_kind =
                v3_resolve_result_intrinsic_kind(lowering,
                                                 function->owner_module_path,
                                                 ctx->aliases,
                                                 ctx->alias_count,
                                                 callee);
            bool is_constructor_call = false;
            is_constructor_call = v3_callee_is_constructor_like(lowering,
                                                                function->owner_module_path,
                                                                ctx->aliases,
                                                                ctx->alias_count,
                                                                callee);
            if ((result_kind == V3_RESULT_INTRINSIC_IS_OK ||
                 result_kind == V3_RESULT_INTRINSIC_IS_ERR ||
                 result_kind == V3_RESULT_INTRINSIC_VALUE ||
                 result_kind == V3_RESULT_INTRINSIC_ERROR ||
                 result_kind == V3_RESULT_INTRINSIC_ERROR_TEXT ||
                 result_kind == V3_RESULT_INTRINSIC_ERROR_INFO_OF) &&
                arg_count == 1U) {
                bool ok = v3_wasm_analyze_result_intrinsic_expr(plan,
                                                                lowering,
                                                                function,
                                                                ctx,
                                                                result_kind,
                                                                args[0],
                                                                imports,
                                                                import_count);
                free(args);
                return ok;
            }
            if ((result_kind == V3_RESULT_INTRINSIC_OK && arg_count == 1U) ||
                (result_kind == V3_RESULT_INTRINSIC_ERR && arg_count == 1U) ||
                (result_kind == V3_RESULT_INTRINSIC_ERR_INFO && arg_count == 1U) ||
                (result_kind == V3_RESULT_INTRINSIC_ERR_CODE && arg_count == 2U)) {
                size_t arg_index;
                bool ok = true;
                for (arg_index = 0U; arg_index < arg_count; ++arg_index) {
                    if (!v3_wasm_analyze_expr(plan,
                                              lowering,
                                              function,
                                              ctx,
                                              args[arg_index],
                                              imports,
                                              import_count)) {
                        ok = false;
                        break;
                    }
                }
                free(args);
                return ok;
            }
            if (strcmp(inferred_abi, "composite") == 0 && is_constructor_call) {
                size_t arg_index;
                bool ok = true;
                for (arg_index = 0U; arg_index < arg_count; ++arg_index) {
                    char field_name[128];
                    char value_expr[4096];
                    const char *arg_expr = args[arg_index];
                    if (v3_parse_named_arg_meta(args[arg_index],
                                                field_name,
                                                sizeof(field_name),
                                                value_expr,
                                                sizeof(value_expr))) {
                        arg_expr = value_expr;
                    }
                    if (!v3_wasm_analyze_expr(plan,
                                              lowering,
                                              function,
                                              ctx,
                                              arg_expr,
                                              imports,
                                              import_count)) {
                        ok = false;
                        break;
                    }
                }
                free(args);
                return ok;
            }
            if (strcmp(callee, "len") == 0 && arg_count == 1U) {
                char base_type[256];
                char base_abi[32];
                char field_abi[32];
                int32_t field_offset = 0;
                bool ok = v3_wasm_resolve_len_expr_meta(plan,
                                                        lowering,
                                                        function,
                                                        ctx,
                                                        args[0],
                                                        base_type,
                                                        sizeof(base_type),
                                                        base_abi,
                                                        sizeof(base_abi),
                                                        field_abi,
                                                        sizeof(field_abi),
                                                        &field_offset) &&
                          v3_wasm_abi_uses_i32_param_slot(field_abi) &&
                          v3_wasm_analyze_expr(plan, lowering, function, ctx, args[0], imports, import_count);
                free(args);
                return ok;
            }
            if (strcmp(callee, "sizeof") == 0 && arg_count == 1U) {
                int32_t sizeof_value = 0;
                bool ok = v3_resolve_sizeof_expr_value(lowering,
                                                       function->owner_module_path,
                                                       ctx->aliases,
                                                       ctx->alias_count,
                                                       args[0],
                                                       &sizeof_value);
                free(args);
                return ok;
            }
            if (arg_count == 1U) {
                char cast_target_type[256];
                char cast_target_abi[32];
                if (v3_resolve_scalar_or_pointer_cast_target(lowering,
                                                             function->owner_module_path,
                                                             ctx->aliases,
                                                             ctx->alias_count,
                                                             callee,
                                                             cast_target_type,
                                                             sizeof(cast_target_type),
                                                             cast_target_abi,
                                                             sizeof(cast_target_abi))) {
                    char arg_type[256];
                    char arg_abi[32];
                    bool ok = v3_infer_expr_type(plan,
                                                 lowering,
                                                 function,
                                                 ctx->aliases,
                                                 ctx->alias_count,
                                                 ctx->locals,
                                                 ctx->local_count,
                                                 args[0],
                                                 arg_type,
                                                 sizeof(arg_type),
                                                 arg_abi,
                                                 sizeof(arg_abi)) &&
                              v3_wasm_abi_uses_i32_param_slot(arg_abi) &&
                              v3_wasm_abi_uses_i32_param_slot(cast_target_abi) &&
                              v3_wasm_analyze_expr(plan, lowering, function, ctx, args[0], imports, import_count);
                    free(args);
                    return ok;
                }
            }
            {
                bool ok =
                    v3_wasm_analyze_call(plan, lowering, function, ctx, callee, args, arg_count, imports, import_count);
                free(args);
                return ok;
            }
        }
        free(args);
    }
    if (v3_parse_prefix_single_arg_call(expr, callee, sizeof(callee), arg_text, sizeof(arg_text))) {
        {
            V3ResultIntrinsicKind result_kind =
                v3_resolve_result_intrinsic_kind(lowering,
                                                 function->owner_module_path,
                                                 ctx->aliases,
                                                 ctx->alias_count,
                                                 callee);
            if ((result_kind == V3_RESULT_INTRINSIC_IS_OK ||
                 result_kind == V3_RESULT_INTRINSIC_IS_ERR ||
                 result_kind == V3_RESULT_INTRINSIC_VALUE ||
                 result_kind == V3_RESULT_INTRINSIC_ERROR ||
                 result_kind == V3_RESULT_INTRINSIC_ERROR_TEXT ||
                 result_kind == V3_RESULT_INTRINSIC_ERROR_INFO_OF) &&
                v3_wasm_analyze_result_intrinsic_expr(plan,
                                                      lowering,
                                                      function,
                                                      ctx,
                                                      result_kind,
                                                      arg_text,
                                                      imports,
                                                      import_count)) {
                return true;
            }
            if ((result_kind == V3_RESULT_INTRINSIC_OK ||
                 result_kind == V3_RESULT_INTRINSIC_ERR ||
                 result_kind == V3_RESULT_INTRINSIC_ERR_INFO) &&
                v3_wasm_analyze_expr(plan, lowering, function, ctx, arg_text, imports, import_count)) {
                return true;
            }
        }
        if (strcmp(callee, "len") == 0) {
            char base_type[256];
            char base_abi[32];
            char field_abi[32];
            int32_t field_offset = 0;
            return v3_wasm_resolve_len_expr_meta(plan,
                                                 lowering,
                                                 function,
                                                 ctx,
                                                 arg_text,
                                                 base_type,
                                                 sizeof(base_type),
                                                 base_abi,
                                                 sizeof(base_abi),
                                                 field_abi,
                                                 sizeof(field_abi),
                                                 &field_offset) &&
                   v3_wasm_abi_uses_i32_param_slot(field_abi) &&
                   v3_wasm_analyze_expr(plan, lowering, function, ctx, arg_text, imports, import_count);
        }
        if (strcmp(callee, "sizeof") == 0) {
            int32_t sizeof_value = 0;
            return v3_resolve_sizeof_expr_value(lowering,
                                                function->owner_module_path,
                                                ctx->aliases,
                                                ctx->alias_count,
                                                arg_text,
                                                &sizeof_value);
        }
        {
            char cast_target_type[256];
            char cast_target_abi[32];
            if (v3_resolve_scalar_or_pointer_cast_target(lowering,
                                                         function->owner_module_path,
                                                         ctx->aliases,
                                                         ctx->alias_count,
                                                         callee,
                                                         cast_target_type,
                                                         sizeof(cast_target_type),
                                                         cast_target_abi,
                                                         sizeof(cast_target_abi))) {
                char arg_type[256];
                char arg_abi[32];
                return v3_infer_expr_type(plan,
                                          lowering,
                                          function,
                                          ctx->aliases,
                                          ctx->alias_count,
                                          ctx->locals,
                                          ctx->local_count,
                                          arg_text,
                                          arg_type,
                                          sizeof(arg_type),
                                          arg_abi,
                                          sizeof(arg_abi)) &&
                       v3_wasm_abi_uses_i32_param_slot(arg_abi) &&
                       v3_wasm_abi_uses_i32_param_slot(cast_target_abi) &&
                       v3_wasm_analyze_expr(plan, lowering, function, ctx, arg_text, imports, import_count);
            }
        }
        {
            char (*args)[4096] = v3_alloc_call_arg_scratch();
            bool ok;
            snprintf(args[0], sizeof(args[0]), "%s", arg_text);
            ok = v3_wasm_analyze_call(plan, lowering, function, ctx, callee, args, 1U, imports, import_count);
            free(args);
            return ok;
        }
    }
    if (v3_parse_member_access_expr(expr, base_expr, sizeof(base_expr), field_name, sizeof(field_name))) {
        char base_type[256];
        char base_abi[32];
        char field_type[256];
        char field_abi[32];
        int32_t field_offset = 0;
        return v3_infer_expr_type(plan,
                                  lowering,
                                  function,
                                  ctx->aliases,
                                  ctx->alias_count,
                                  ctx->locals,
                                  ctx->local_count,
                                  base_expr,
                                  base_type,
                                  sizeof(base_type),
                                  base_abi,
                                  sizeof(base_abi)) &&
               v3_wasm_abi_uses_i32_param_slot(base_abi) &&
               v3_resolve_field_meta(lowering,
                                     base_type,
                                     field_name,
                                     field_type,
                                     sizeof(field_type),
                                     field_abi,
                                     sizeof(field_abi),
                                     &field_offset) &&
               v3_wasm_analyze_expr(plan, lowering, function, ctx, base_expr, imports, import_count);
    }
    return true;
}

static bool v3_wasm_prepare_statement(const V3SystemLinkPlanStub *plan,
                                      const V3LoweringPlanStub *lowering,
                                      const V3LoweredFunctionStub *function,
                                      V3WasmFunctionContext *ctx,
                                      const char *statement,
                                      V3WasmImportStub *imports,
                                      size_t *import_count) {
    char name[128];
    char type_text[256];
    char expr[4096];
    char cond[4096];
    char inline_stmt[8192];
    if (v3_startswith(statement, "if ")) {
        if (!v3_parse_if_header(statement, cond, sizeof(cond), inline_stmt, sizeof(inline_stmt)) ||
            !v3_wasm_analyze_expr(plan, lowering, function, ctx, cond, imports, import_count)) {
            return false;
        }
        if (inline_stmt[0] != '\0') {
            return v3_wasm_prepare_statement(plan, lowering, function, ctx, inline_stmt, imports, import_count);
        }
        return true;
    }
    if (v3_startswith(statement, "elif ")) {
        if (!v3_parse_elif_header(statement, cond, sizeof(cond), inline_stmt, sizeof(inline_stmt)) ||
            !v3_wasm_analyze_expr(plan, lowering, function, ctx, cond, imports, import_count)) {
            return false;
        }
        if (inline_stmt[0] != '\0') {
            return v3_wasm_prepare_statement(plan, lowering, function, ctx, inline_stmt, imports, import_count);
        }
        return true;
    }
    if (v3_startswith(statement, "else")) {
        if (!v3_parse_else_header(statement, inline_stmt, sizeof(inline_stmt))) {
            return false;
        }
        if (inline_stmt[0] != '\0') {
            return v3_wasm_prepare_statement(plan, lowering, function, ctx, inline_stmt, imports, import_count);
        }
        return true;
    }
    if (v3_startswith(statement, "while ")) {
        if (!v3_parse_while_header(statement, cond, sizeof(cond), inline_stmt, sizeof(inline_stmt)) ||
            !v3_wasm_analyze_expr(plan, lowering, function, ctx, cond, imports, import_count)) {
            return false;
        }
        if (inline_stmt[0] != '\0') {
            return v3_wasm_prepare_statement(plan, lowering, function, ctx, inline_stmt, imports, import_count);
        }
        return true;
    }
    if (v3_startswith(statement, "for ")) {
        char iter_name[128];
        char start_expr[4096];
        char end_expr[4096];
        bool end_inclusive = false;
        if (!v3_parse_for_header(statement,
                                 iter_name,
                                 sizeof(iter_name),
                                 start_expr,
                                 sizeof(start_expr),
                                 end_expr,
                                 sizeof(end_expr),
                                 &end_inclusive,
                                 inline_stmt,
                                 sizeof(inline_stmt)) ||
            !v3_wasm_analyze_expr(plan, lowering, function, ctx, start_expr, imports, import_count) ||
            !v3_wasm_analyze_expr(plan, lowering, function, ctx, end_expr, imports, import_count)) {
            return false;
        }
        if (v3_wasm_find_local_slot_index(ctx, iter_name) < 0 &&
            !v3_wasm_context_add_storage_local(lowering,
                                               ctx,
                                               iter_name,
                                               "int32",
                                               "i32",
                                               (uint32_t)ctx->local_count)) {
            return false;
        }
        if (inline_stmt[0] != '\0') {
            return v3_wasm_prepare_statement(plan, lowering, function, ctx, inline_stmt, imports, import_count);
        }
        return true;
    }
    if (v3_startswith(statement, "case ")) {
        fprintf(stderr,
                "[cheng_v3_seed] wasm generic statement not supported function=%s stmt=%s\n",
                function->symbol_text,
                statement);
        return false;
    }
    if (strcmp(statement, "break") == 0 || strcmp(statement, "continue") == 0) {
        return true;
    }
    if ((v3_startswith(statement, "let ") || v3_startswith(statement, "var ")) &&
        v3_parse_binding_decl_meta(statement,
                                   v3_startswith(statement, "let ") ? "let " : "var ",
                                   name,
                                   sizeof(name),
                                   type_text,
                                   sizeof(type_text))) {
        char normalized_type[256];
        const char *abi_class = v3_resolved_type_abi_class(lowering,
                                                           function->owner_module_path,
                                                           ctx->aliases,
                                                           ctx->alias_count,
                                                           type_text,
                                                           normalized_type,
                                                           sizeof(normalized_type));
        return v3_wasm_context_add_storage_local(lowering,
                                                 ctx,
                                                 name,
                                                 normalized_type[0] != '\0' ? normalized_type : type_text,
                                                 abi_class,
                                                 (uint32_t)ctx->local_count);
    }
    if ((v3_startswith(statement, "let ") || v3_startswith(statement, "var ")) &&
        v3_parse_binding_meta(statement,
                              v3_startswith(statement, "let ") ? "let " : "var ",
                              name,
                              sizeof(name),
                              type_text,
                              sizeof(type_text),
                              expr,
                              sizeof(expr))) {
        char inferred_type[256];
        char inferred_abi[32];
        char normalized_type[256];
        const char *abi_class = NULL;
        if (type_text[0] == '\0') {
            if (!v3_infer_expr_type(plan,
                                    lowering,
                                    function,
                                    ctx->aliases,
                                    ctx->alias_count,
                                    ctx->locals,
                                    ctx->local_count,
                                    expr,
                                    inferred_type,
                                    sizeof(inferred_type),
                                    inferred_abi,
                                    sizeof(inferred_abi))) {
                return false;
            }
            if (strcmp(inferred_abi, "composite") == 0) {
                if (!v3_wasm_prepare_composite_expr(plan,
                                                    lowering,
                                                    function,
                                                    ctx,
                                                    expr,
                                                    inferred_type,
                                                    imports,
                                                    import_count)) {
                    return false;
                }
            } else if (!v3_wasm_analyze_expr(plan, lowering, function, ctx, expr, imports, import_count)) {
                return false;
            }
            snprintf(type_text, sizeof(type_text), "%s", inferred_type);
        } else {
            const char *declared_abi = v3_type_abi_class(type_text);
            if (strcmp(declared_abi, "composite") == 0) {
                if (!v3_wasm_expr_matches_composite_default(lowering, function, ctx, expr, type_text) &&
                    !v3_wasm_prepare_composite_expr(plan,
                                                    lowering,
                                                    function,
                                                    ctx,
                                                    expr,
                                                    type_text,
                                                    imports,
                                                    import_count)) {
                    return false;
                }
            } else if (!v3_wasm_analyze_expr(plan, lowering, function, ctx, expr, imports, import_count)) {
                return false;
            }
        }
        if (!v3_tuple_binding_expr_complete(type_text, expr)) {
            return false;
        }
        abi_class = v3_resolved_type_abi_class(lowering,
                                               function->owner_module_path,
                                               ctx->aliases,
                                               ctx->alias_count,
                                               type_text,
                                               normalized_type,
                                               sizeof(normalized_type));
        return v3_wasm_context_add_storage_local(lowering,
                                                 ctx,
                                                 name,
                                                 normalized_type[0] != '\0' ? normalized_type : type_text,
                                                 abi_class,
                                                 (uint32_t)ctx->local_count);
    }
    if (strcmp(statement, "return") == 0) {
        return v3_wasm_abi_is_void(function->return_abi_class);
    }
    if (v3_startswith(statement, "return ")) {
        if (strcmp(function->return_abi_class, "composite") == 0) {
            return v3_wasm_prepare_composite_expr(plan,
                                                  lowering,
                                                  function,
                                                  ctx,
                                                  statement + 7,
                                                  function->return_type,
                                                  imports,
                                                  import_count);
        }
        return v3_wasm_analyze_expr(plan, lowering, function, ctx, statement + 7, imports, import_count);
    }
    if (v3_parse_assignment_meta(statement, name, sizeof(name), expr, sizeof(expr))) {
        char field_type[256];
        char field_abi[32];
        int32_t local_slot = v3_wasm_find_local_slot_index(ctx, name);
        if (local_slot >= 0) {
            snprintf(field_type, sizeof(field_type), "%s", ctx->locals[local_slot].type_text);
            snprintf(field_abi, sizeof(field_abi), "%s", ctx->locals[local_slot].abi_class);
        } else if (!v3_infer_lvalue_expr_type(plan,
                                              lowering,
                                              function,
                                              ctx->aliases,
                                              ctx->alias_count,
                                              ctx->locals,
                                              ctx->local_count,
                                              name,
                                              field_type,
                                              sizeof(field_type),
                                              field_abi,
                                              sizeof(field_abi))) {
            return false;
        }
        if (strcmp(field_abi, "composite") == 0 &&
            v3_wasm_expr_matches_composite_default(lowering, function, ctx, expr, field_type)) {
            return true;
        }
        if (strcmp(field_abi, "composite") == 0) {
            return v3_wasm_prepare_composite_expr(plan,
                                                  lowering,
                                                  function,
                                                  ctx,
                                                  expr,
                                                  field_type,
                                                  imports,
                                                  import_count);
        }
        return v3_wasm_analyze_expr(plan, lowering, function, ctx, expr, imports, import_count);
    }
    {
        char callee[PATH_MAX];
        char arg_text[4096];
        callee[0] = '\0';
        arg_text[0] = '\0';
        {
            char (*args)[4096] = v3_alloc_call_arg_scratch();
            size_t arg_count = 0U;
            bool parsed = v3_parse_call_text(statement, callee, sizeof(callee), args, &arg_count, CHENG_V3_MAX_CALL_ARGS);
            bool ok = parsed &&
                      v3_wasm_analyze_call_statement(plan, lowering, function, ctx, callee, args, arg_count, imports, import_count);
            free(args);
            if (parsed) {
                return ok;
            }
        }
        if (v3_parse_prefix_single_arg_call(statement, callee, sizeof(callee), arg_text, sizeof(arg_text))) {
            char (*args)[4096] = v3_alloc_call_arg_scratch();
            bool ok;
            snprintf(args[0], sizeof(args[0]), "%s", arg_text);
            ok = v3_wasm_analyze_call_statement(plan, lowering, function, ctx, callee, args, 1U, imports, import_count);
            free(args);
            if (ok) {
                return true;
            }
        }
    }
    return v3_wasm_analyze_expr(plan, lowering, function, ctx, statement, imports, import_count);
}

static bool v3_wasm_prepare_function_context(const V3SystemLinkPlanStub *plan,
                                             const V3LoweringPlanStub *lowering,
                                             const V3LoweredFunctionStub *function,
                                             V3WasmFunctionContext *ctx,
                                             V3WasmImportStub *imports,
                                             size_t *import_count) {
    char **lines = NULL;
    char *owned_lines = NULL;
    size_t line_count = 0U;
    size_t fn_line_index = 0U;
    char param_names[CHENG_V3_CONST_MAX_BINDINGS][128];
    size_t param_count = 0U;
    char owned_path[PATH_MAX];
    char inline_signature_body[8192];
    bool has_inline_signature_body = false;
    size_t i;
    if (!v3_wasm_context_init(plan, lowering, function, ctx) ||
        !v3_load_function_source_lines(function,
                                       &lines,
                                       &owned_lines,
                                       &line_count,
                                       &fn_line_index,
                                       param_names,
                                       &param_count,
                                       owned_path)) {
        return false;
    }
    inline_signature_body[0] = '\0';
    {
        char signature_copy[8192];
        char *eq;
        snprintf(signature_copy, sizeof(signature_copy), "%s", lines[fn_line_index]);
        eq = strrchr(signature_copy, '=');
        if (eq != NULL) {
            v3_trim_copy_text(eq + 1, inline_signature_body, sizeof(inline_signature_body));
            has_inline_signature_body = inline_signature_body[0] != '\0';
        }
    }
    if (has_inline_signature_body) {
        bool ok = v3_wasm_prepare_statement(plan,
                                            lowering,
                                            function,
                                            ctx,
                                            inline_signature_body,
                                            imports,
                                            import_count);
        if (ok) {
            ok = v3_wasm_context_finalize(ctx);
        }
        free(lines);
        free(owned_lines);
        return ok;
    }
    for (i = fn_line_index + 1U; i < line_count; ) {
        char raw_copy[4096];
        char *trimmed;
        int32_t indent;
        char statement[8192];
        snprintf(raw_copy, sizeof(raw_copy), "%s", lines[i]);
        trimmed = v3_trim_inplace(raw_copy);
        if (*trimmed == '\0') {
            i += 1U;
            continue;
        }
        indent = v3_line_indent(lines[i]);
        if (indent <= 0 && v3_is_top_level_decl_start(trimmed)) {
            break;
        }
        if (!v3_collect_statement_from_lines(lines, line_count, &i, statement, sizeof(statement)) ||
            !v3_wasm_prepare_statement(plan, lowering, function, ctx, statement, imports, import_count)) {
            fprintf(stderr,
                    "[cheng_v3_seed] wasm prepare statement failed function=%s stmt=%s\n",
                    function->symbol_text,
                    statement);
            free(lines);
            free(owned_lines);
            return false;
        }
    }
    if (!v3_wasm_context_finalize(ctx)) {
        free(lines);
        free(owned_lines);
        return false;
    }
    free(lines);
    free(owned_lines);
    return true;
}

static bool v3_wasm_emit_expr(const V3SystemLinkPlanStub *plan,
                              const V3LoweringPlanStub *lowering,
                              const V3LoweredFunctionStub *function,
                              const V3WasmFunctionContext *ctx,
                              const char *expr_text,
                              const V3WasmImportStub *imports,
                              size_t import_count,
                              V3WasmBuffer *body);

static bool v3_wasm_emit_statement(const V3SystemLinkPlanStub *plan,
                                   const V3LoweringPlanStub *lowering,
                                   const V3LoweredFunctionStub *function,
                                   const V3WasmFunctionContext *ctx,
                                   char **lines,
                                   size_t line_count,
                                   size_t *index_io,
                                   int32_t statement_indent,
                                   int32_t loop_continue_depth,
                                   int32_t loop_break_depth,
                                   const char *statement,
                                   const V3WasmImportStub *imports,
                                   size_t import_count,
                                   bool allow_implicit_return,
                                   V3WasmBuffer *body,
                                   bool *terminated_out);

static bool v3_wasm_emit_for_statement(const V3SystemLinkPlanStub *plan,
                                       const V3LoweringPlanStub *lowering,
                                       const V3LoweredFunctionStub *function,
                                       const V3WasmFunctionContext *ctx,
                                       char **lines,
                                       size_t line_count,
                                       size_t *index_io,
                                       int32_t statement_indent,
                                       const char *statement,
                                       const V3WasmImportStub *imports,
                                       size_t import_count,
                                       V3WasmBuffer *body);

static bool v3_wasm_emit_block_statements(const V3SystemLinkPlanStub *plan,
                                          const V3LoweringPlanStub *lowering,
                                          const V3LoweredFunctionStub *function,
                                          const V3WasmFunctionContext *ctx,
                                          char **lines,
                                          size_t line_count,
                                          size_t *index_io,
                                          int32_t parent_indent,
                                          int32_t loop_continue_depth,
                                          int32_t loop_break_depth,
                                          const V3WasmImportStub *imports,
                                          size_t import_count,
                                          V3WasmBuffer *body,
                                          bool *terminated_out) {
    bool terminated = false;
    while (*index_io < line_count) {
        char raw_copy[4096];
        char *trimmed;
        int32_t indent;
        char statement[8192];
        snprintf(raw_copy, sizeof(raw_copy), "%s", lines[*index_io]);
        trimmed = v3_trim_inplace(raw_copy);
        if (*trimmed == '\0') {
            *index_io += 1U;
            continue;
        }
        indent = v3_line_indent(lines[*index_io]);
        if (indent <= parent_indent) {
            break;
        }
        if (!v3_collect_statement_from_lines(lines, line_count, index_io, statement, sizeof(statement))) {
            return false;
        }
        if (terminated) {
            continue;
        }
        if (!v3_wasm_emit_statement(plan,
                                    lowering,
                                    function,
                                    ctx,
                                    lines,
                                    line_count,
                                    index_io,
                                    indent,
                                    loop_continue_depth,
                                    loop_break_depth,
                                    statement,
                                    imports,
                                    import_count,
                                    false,
                                    body,
                                    &terminated)) {
            return false;
        }
    }
    *terminated_out = terminated;
    return true;
}

static bool v3_wasm_emit_call_target(const V3LoweringPlanStub *lowering,
                                     const V3AsmCallTarget *target,
                                     const V3WasmImportStub *imports,
                                     size_t import_count,
                                     V3WasmBuffer *body) {
    uint32_t callee_index = 0U;
    if (target->external) {
        uint32_t type_index = 0U;
        int32_t import_index;
        if (!v3_wasm_signature_type_index(target->param_count,
                                          target->return_abi_class,
                                          target->param_abi_classes,
                                          &type_index)) {
            return false;
        }
        import_index = v3_wasm_find_import_index(imports, import_count, target->symbol_name, type_index);
        if (import_index < 0) {
            fprintf(stderr,
                    "[cheng_v3_seed] wasm missing import symbol=%s type_index=%u\n",
                    target->symbol_name,
                    type_index);
            v3_wasm_debug_dump_imports(imports, import_count);
            return false;
        }
        callee_index = (uint32_t)import_index;
    } else {
        int32_t local_index = v3_find_lowered_function_index_by_symbol(lowering,
                                                                       target->lowered_function->symbol_text);
        if (local_index < 0) {
            fprintf(stderr, "[cheng_v3_seed] wasm local callee missing symbol=%s\n",
                    target->lowered_function->symbol_text);
            return false;
        }
        callee_index = (uint32_t)import_count + (uint32_t)local_index;
    }
    return v3_wasm_append_u8(body, 0x10U) &&
           v3_wasm_append_uleb_u32(body, callee_index);
}

static bool v3_wasm_emit_call_expr(const V3SystemLinkPlanStub *plan,
                                   const V3LoweringPlanStub *lowering,
                                   const V3LoweredFunctionStub *function,
                                   const V3WasmFunctionContext *ctx,
                                   const char *callee,
                                   char args[][4096],
                                   size_t arg_count,
                                   const V3WasmImportStub *imports,
                                   size_t import_count,
                                   V3WasmBuffer *body) {
    V3AsmCallTarget target;
    size_t i;
    if (!v3_resolve_call_target_with_args(plan,
                                          lowering,
                                          function,
                                          ctx->aliases,
                                          ctx->alias_count,
                                          ctx->locals,
                                          ctx->local_count,
                                          callee,
                                          args,
                                          arg_count,
                                          &target)) {
        fprintf(stderr,
                "[cheng_v3_seed] wasm emit call resolve failed function=%s callee=%s argc=%zu\n",
                function->symbol_text,
                callee,
                arg_count);
        return false;
    }
    if (!v3_wasm_abi_uses_i32_return_slot(target.return_abi_class) ||
        target.param_count != arg_count) {
        fprintf(stderr,
                "[cheng_v3_seed] wasm emit call signature reject function=%s callee=%s ret=%s argc=%zu expected=%zu\n",
                function->symbol_text,
                callee,
                target.return_abi_class,
                target.param_count,
                arg_count);
        return false;
    }
    for (i = 0U; i < arg_count; ++i) {
        if (!v3_wasm_abi_uses_i32_param_slot(target.param_abi_classes[i]) ||
            !v3_wasm_emit_call_arg_value(plan,
                                         lowering,
                                         function,
                                         ctx,
                                         callee,
                                         i,
                                         args[i],
                                         target.param_types[i],
                                         target.param_abi_classes[i],
                                         imports,
                                         import_count,
                                         body)) {
            fprintf(stderr,
                    "[cheng_v3_seed] wasm emit call arg failed function=%s callee=%s index=%zu abi=%s expr=%s\n",
                    function->symbol_text,
                    callee,
                    i,
                    target.param_abi_classes[i],
                    args[i]);
            return false;
        }
    }
    return v3_wasm_emit_call_target(lowering, &target, imports, import_count, body);
}

static bool v3_wasm_emit_call_statement(const V3SystemLinkPlanStub *plan,
                                        const V3LoweringPlanStub *lowering,
                                        const V3LoweredFunctionStub *function,
                                        const V3WasmFunctionContext *ctx,
                                        const char *callee,
                                        char args[][4096],
                                        size_t arg_count,
                                        const V3WasmImportStub *imports,
                                        size_t import_count,
                                        V3WasmBuffer *body) {
    bool handled = false;
    V3AsmCallTarget target;
    size_t i;
    if (!v3_wasm_emit_builtin_statement(plan,
                                        lowering,
                                        function,
                                        ctx,
                                        callee,
                                        args,
                                        arg_count,
                                        imports,
                                        import_count,
                                        body,
                                        &handled)) {
        return false;
    }
    if (handled) {
        return true;
    }
    if (!v3_resolve_call_target_with_args(plan,
                                          lowering,
                                          function,
                                          ctx->aliases,
                                          ctx->alias_count,
                                          ctx->locals,
                                          ctx->local_count,
                                          callee,
                                          args,
                                          arg_count,
                                          &target) ||
        target.param_count != arg_count) {
        return false;
    }
    if (strcmp(target.return_abi_class, "void") != 0 &&
        !v3_wasm_abi_uses_i32_return_slot(target.return_abi_class)) {
        return false;
    }
    for (i = 0U; i < arg_count; ++i) {
        if (!v3_wasm_abi_uses_i32_param_slot(target.param_abi_classes[i]) ||
            !v3_wasm_emit_call_arg_value(plan,
                                         lowering,
                                         function,
                                         ctx,
                                         callee,
                                         i,
                                         args[i],
                                         target.param_types[i],
                                         target.param_abi_classes[i],
                                         imports,
                                         import_count,
                                         body)) {
            return false;
        }
    }
    if (!v3_wasm_emit_call_target(lowering, &target, imports, import_count, body)) {
        return false;
    }
    if (strcmp(target.return_abi_class, "void") == 0) {
        return true;
    }
    return v3_wasm_append_u8(body, 0x1aU);
}

static bool v3_wasm_append_memory_arg(V3WasmBuffer *body,
                                      uint32_t align_log2,
                                      uint32_t offset) {
    return v3_wasm_append_uleb_u32(body, align_log2) &&
           v3_wasm_append_uleb_u32(body, offset);
}

static bool v3_wasm_emit_field_access_from_base(V3WasmBuffer *body,
                                                const char *field_abi,
                                                int32_t field_offset) {
    uint32_t wasm_offset = field_offset > 0 ? (uint32_t)field_offset : 0U;
    if (field_abi == NULL || body == NULL) {
        return false;
    }
    if (strcmp(field_abi, "composite") == 0) {
        if (wasm_offset == 0U) {
            return true;
        }
        return v3_wasm_append_u8(body, 0x41U) &&
               v3_wasm_append_sleb_i32(body, field_offset) &&
               v3_wasm_append_u8(body, 0x6aU);
    }
    if (strcmp(field_abi, "ptr") == 0 ||
        strcmp(field_abi, "i32") == 0 ||
        strcmp(field_abi, "u32") == 0) {
        return v3_wasm_append_u8(body, 0x28U) &&
               v3_wasm_append_memory_arg(body, 2U, wasm_offset);
    }
    if (strcmp(field_abi, "bool") == 0 ||
        strcmp(field_abi, "u8") == 0) {
        return v3_wasm_append_u8(body, 0x2dU) &&
               v3_wasm_append_memory_arg(body, 0U, wasm_offset);
    }
    if (strcmp(field_abi, "i8") == 0) {
        return v3_wasm_append_u8(body, 0x2cU) &&
               v3_wasm_append_memory_arg(body, 0U, wasm_offset);
    }
    if (strcmp(field_abi, "u16") == 0) {
        return v3_wasm_append_u8(body, 0x2fU) &&
               v3_wasm_append_memory_arg(body, 1U, wasm_offset);
    }
    if (strcmp(field_abi, "i16") == 0) {
        return v3_wasm_append_u8(body, 0x2eU) &&
               v3_wasm_append_memory_arg(body, 1U, wasm_offset);
    }
    return false;
}

static bool v3_wasm_emit_expr(const V3SystemLinkPlanStub *plan,
                              const V3LoweringPlanStub *lowering,
                              const V3LoweredFunctionStub *function,
                              const V3WasmFunctionContext *ctx,
                              const char *expr_text,
                              const V3WasmImportStub *imports,
                              size_t import_count,
                              V3WasmBuffer *body) {
    static const char *OPS[] = {
        "||", "&&", "==", "!=", "<=", ">=", "<<", ">>", "<", ">", "|", "^", "&", "+", "-", "*", "/", "%"
    };
    char expr[4096];
    char callee[PATH_MAX];
    char arg_text[4096];
    char cond_expr[4096];
    char true_expr[4096];
    char false_expr[4096];
    char base_expr[4096];
    char index_expr[4096];
    char field_name[256];
    int64_t literal_value = 0;
    int32_t char_value = 0;
    size_t i;
    v3_wasm_trim_wrapping_parens(expr_text, expr, sizeof(expr));
    if (v3_parse_if_expr(expr,
                         cond_expr,
                         sizeof(cond_expr),
                         true_expr,
                         sizeof(true_expr),
                         false_expr,
                         sizeof(false_expr)) ||
        v3_parse_ternary_expr(expr,
                              cond_expr,
                              sizeof(cond_expr),
                              true_expr,
                              sizeof(true_expr),
                              false_expr,
                              sizeof(false_expr))) {
        return v3_wasm_emit_expr(plan, lowering, function, ctx, cond_expr, imports, import_count, body) &&
               v3_wasm_append_u8(body, 0x04U) &&
               v3_wasm_append_u8(body, 0x7fU) &&
               v3_wasm_emit_expr(plan, lowering, function, ctx, true_expr, imports, import_count, body) &&
               v3_wasm_append_u8(body, 0x05U) &&
               v3_wasm_emit_expr(plan, lowering, function, ctx, false_expr, imports, import_count, body) &&
               v3_wasm_append_u8(body, 0x0bU);
    }
    if (expr[0] == '!' && expr[1] != '\0') {
        return v3_wasm_emit_expr(plan, lowering, function, ctx, expr + 1, imports, import_count, body) &&
               v3_wasm_append_u8(body, 0x45U);
    }
    {
        char literal[4096];
        if (v3_parse_string_literal_text(expr, literal, sizeof(literal))) {
            return v3_wasm_emit_str_literal_expr(body, literal);
        }
    }
    if (expr[0] == '-' && expr[1] != '\0' && !v3_parse_int_literal_text(expr, &literal_value)) {
        return v3_wasm_append_u8(body, 0x41U) &&
               v3_wasm_append_sleb_i32(body, 0) &&
               v3_wasm_emit_expr(plan, lowering, function, ctx, expr + 1, imports, import_count, body) &&
               v3_wasm_append_u8(body, 0x6bU);
    }
    for (i = 0U; i < sizeof(OPS) / sizeof(OPS[0]); ++i) {
        int32_t op_index = v3_find_top_level_binary_op_last(expr, OPS[i]);
        if (op_index > 0) {
            char left[4096];
            char right[4096];
            char left_type[256];
            char left_abi[32];
            char right_type[256];
            char right_abi[32];
            bool left_unsigned = false;
            snprintf(left, sizeof(left), "%.*s", op_index, expr);
            snprintf(right, sizeof(right), "%s", expr + op_index + strlen(OPS[i]));
            v3_trim_copy_text(left, left, sizeof(left));
            v3_trim_copy_text(right, right, sizeof(right));
            if (!v3_infer_expr_type(plan,
                                    lowering,
                                    function,
                                    ctx->aliases,
                                    ctx->alias_count,
                                    ctx->locals,
                                    ctx->local_count,
                                    left,
                                    left_type,
                                    sizeof(left_type),
                                    left_abi,
                                    sizeof(left_abi)) ||
                !v3_infer_expr_type(plan,
                                    lowering,
                                    function,
                                    ctx->aliases,
                                    ctx->alias_count,
                                    ctx->locals,
                                    ctx->local_count,
                                    right,
                                    right_type,
                                    sizeof(right_type),
                                    right_abi,
                                    sizeof(right_abi))) {
                return false;
            }
            if ((strcmp(OPS[i], "==") == 0 || strcmp(OPS[i], "!=") == 0) &&
                strcmp(left_type, "str") == 0 &&
                strcmp(right_type, "str") == 0 &&
                strcmp(left_abi, "composite") == 0 &&
                strcmp(right_abi, "composite") == 0) {
                V3AsmCallTarget target;
                uint32_t left_addr_local_index = ctx->scratch3_local_index;
                uint32_t right_addr_local_index = ctx->scratch4_local_index;
                if (!ctx->has_frame_base_local ||
                    ctx->str_temp0_offset < 0 ||
                    ctx->str_temp1_offset < 0 ||
                    !v3_wasm_build_str_eq_target(plan, &target) ||
                    !v3_wasm_emit_frame_temp_address(ctx,
                                                     body,
                                                     left_addr_local_index,
                                                     ctx->str_temp0_offset) ||
                    !v3_wasm_emit_composite_expr_into_local_address(plan,
                                                                    lowering,
                                                                    function,
                                                                    ctx,
                                                                    left,
                                                                    "str",
                                                                    left_addr_local_index,
                                                                    imports,
                                                                    import_count,
                                                                    body) ||
                    !v3_wasm_emit_frame_temp_address(ctx,
                                                     body,
                                                     right_addr_local_index,
                                                     ctx->str_temp1_offset) ||
                    !v3_wasm_emit_composite_expr_into_local_address(plan,
                                                                    lowering,
                                                                    function,
                                                                    ctx,
                                                                    right,
                                                                    "str",
                                                                    right_addr_local_index,
                                                                    imports,
                                                                    import_count,
                                                                    body) ||
                    !v3_wasm_emit_frame_temp_address(ctx,
                                                     body,
                                                     left_addr_local_index,
                                                     ctx->str_temp0_offset) ||
                    !v3_wasm_emit_frame_temp_address(ctx,
                                                     body,
                                                     right_addr_local_index,
                                                     ctx->str_temp1_offset) ||
                    !v3_wasm_emit_local_get(body, left_addr_local_index) ||
                    !v3_wasm_emit_local_get(body, right_addr_local_index) ||
                    !v3_wasm_emit_call_target(lowering, &target, imports, import_count, body)) {
                    return false;
                }
                if (strcmp(OPS[i], "!=") == 0) {
                    return v3_wasm_append_u8(body, 0x45U);
                }
                return true;
            }
            if (!v3_wasm_emit_expr(plan, lowering, function, ctx, left, imports, import_count, body) ||
                !v3_wasm_emit_expr(plan, lowering, function, ctx, right, imports, import_count, body)) {
                return false;
            }
            left_unsigned = v3_scalar_type_is_unsigned(left_type);
            if (strcmp(OPS[i], "||") == 0) {
                return v3_wasm_append_u8(body, 0x72U) &&
                       v3_wasm_append_u8(body, 0x45U) &&
                       v3_wasm_append_u8(body, 0x45U);
            }
            if (strcmp(OPS[i], "&&") == 0) {
                return v3_wasm_append_u8(body, 0x71U) &&
                       v3_wasm_append_u8(body, 0x45U) &&
                       v3_wasm_append_u8(body, 0x45U);
            }
            if (strcmp(OPS[i], "==") == 0) return v3_wasm_append_u8(body, 0x46U);
            if (strcmp(OPS[i], "!=") == 0) return v3_wasm_append_u8(body, 0x47U);
            if (strcmp(OPS[i], "<") == 0) return v3_wasm_append_u8(body, left_unsigned ? 0x49U : 0x48U);
            if (strcmp(OPS[i], ">") == 0) return v3_wasm_append_u8(body, left_unsigned ? 0x4bU : 0x4aU);
            if (strcmp(OPS[i], "<=") == 0) return v3_wasm_append_u8(body, left_unsigned ? 0x4dU : 0x4cU);
            if (strcmp(OPS[i], ">=") == 0) return v3_wasm_append_u8(body, left_unsigned ? 0x4fU : 0x4eU);
            if (strcmp(OPS[i], "|") == 0) return v3_wasm_append_u8(body, 0x72U);
            if (strcmp(OPS[i], "^") == 0) return v3_wasm_append_u8(body, 0x73U);
            if (strcmp(OPS[i], "&") == 0) return v3_wasm_append_u8(body, 0x71U);
            if (strcmp(OPS[i], "+") == 0) return v3_wasm_append_u8(body, 0x6aU);
            if (strcmp(OPS[i], "-") == 0) return v3_wasm_append_u8(body, 0x6bU);
            if (strcmp(OPS[i], "*") == 0) return v3_wasm_append_u8(body, 0x6cU);
            if (strcmp(OPS[i], "/") == 0) return v3_wasm_append_u8(body, left_unsigned ? 0x6eU : 0x6dU);
            if (strcmp(OPS[i], "%") == 0) return v3_wasm_append_u8(body, left_unsigned ? 0x70U : 0x6fU);
            if (strcmp(OPS[i], "<<") == 0) return v3_wasm_append_u8(body, 0x74U);
            if (strcmp(OPS[i], ">>") == 0) return v3_wasm_append_u8(body, left_unsigned ? 0x76U : 0x75U);
        }
    }
    if (v3_parse_index_access_expr(expr,
                                   base_expr,
                                   sizeof(base_expr),
                                   index_expr,
                                   sizeof(index_expr))) {
        char base_type[256];
        char base_abi[32];
        char normalized_base_type[256];
        char field_type[256];
        char field_abi[32];
        int32_t elem_size = 0;
        int32_t data_field_offset = 0;
        int32_t len_field_offset = 0;
        int32_t fixed_len = 0;
        bool base_is_fixed_array = false;
        if (!v3_wasm_resolve_index_expr_meta(plan,
                                             lowering,
                                             function,
                                             ctx,
                                             expr,
                                             base_expr,
                                             sizeof(base_expr),
                                             index_expr,
                                             sizeof(index_expr),
                                             base_type,
                                             sizeof(base_type),
                                             base_abi,
                                             sizeof(base_abi),
                                             normalized_base_type,
                                             sizeof(normalized_base_type),
                                             field_type,
                                             sizeof(field_type),
                                             field_abi,
                                             sizeof(field_abi),
                                             &elem_size,
                                             &data_field_offset,
                                             &len_field_offset,
                                             &fixed_len,
                                             &base_is_fixed_array) ||
            !v3_wasm_emit_lvalue_address(plan,
                                         lowering,
                                         function,
                                         ctx,
                                         expr,
                                         imports,
                                         import_count,
                                         body)) {
            return false;
        }
        if (strcmp(field_abi, "composite") == 0) {
            return true;
        }
        if (strcmp(normalized_base_type, "Bytes") == 0 ||
            strcmp(normalized_base_type, "str") == 0 ||
            strcmp(field_type, "char") == 0) {
            return v3_wasm_emit_field_access_from_base(body, "u8", 0);
        }
        return v3_wasm_emit_field_access_from_base(body, field_abi, 0);
    }
    {
        char (*args)[4096] = v3_alloc_call_arg_scratch();
        size_t arg_count = 0U;
        bool parsed = v3_parse_call_text(expr, callee, sizeof(callee), args, &arg_count, CHENG_V3_MAX_CALL_ARGS);
        if (parsed) {
            V3ResultIntrinsicKind result_kind =
                v3_resolve_result_intrinsic_kind(lowering,
                                                 function->owner_module_path,
                                                 ctx->aliases,
                                                 ctx->alias_count,
                                                 callee);
            if ((result_kind == V3_RESULT_INTRINSIC_IS_OK ||
                 result_kind == V3_RESULT_INTRINSIC_IS_ERR ||
                 result_kind == V3_RESULT_INTRINSIC_VALUE ||
                 result_kind == V3_RESULT_INTRINSIC_ERROR ||
                 result_kind == V3_RESULT_INTRINSIC_ERROR_TEXT ||
                 result_kind == V3_RESULT_INTRINSIC_ERROR_INFO_OF) &&
                arg_count == 1U) {
                bool ok = v3_wasm_emit_result_intrinsic_expr(plan,
                                                             lowering,
                                                             function,
                                                             ctx,
                                                             result_kind,
                                                             args[0],
                                                             imports,
                                                             import_count,
                                                             body);
                free(args);
                return ok;
            }
            if (strcmp(callee, "len") == 0 && arg_count == 1U) {
                bool ok = v3_wasm_emit_len_expr(plan,
                                                lowering,
                                                function,
                                                ctx,
                                                args[0],
                                                imports,
                                                import_count,
                                                body);
                free(args);
                return ok;
            }
            if (strcmp(callee, "sizeof") == 0 && arg_count == 1U) {
                int32_t sizeof_value = 0;
                bool ok = v3_resolve_sizeof_expr_value(lowering,
                                                       function->owner_module_path,
                                                       ctx->aliases,
                                                       ctx->alias_count,
                                                       args[0],
                                                       &sizeof_value) &&
                          v3_wasm_emit_i32_const(body, sizeof_value);
                free(args);
                return ok;
            }
            if (arg_count == 1U) {
                char cast_target_type[256];
                char cast_target_abi[32];
                if (v3_resolve_scalar_or_pointer_cast_target(lowering,
                                                             function->owner_module_path,
                                                             ctx->aliases,
                                                             ctx->alias_count,
                                                             callee,
                                                             cast_target_type,
                                                             sizeof(cast_target_type),
                                                             cast_target_abi,
                                                             sizeof(cast_target_abi))) {
                    char arg_type[256];
                    char arg_abi[32];
                    bool ok = v3_infer_expr_type(plan,
                                                 lowering,
                                                 function,
                                                 ctx->aliases,
                                                 ctx->alias_count,
                                                 ctx->locals,
                                                 ctx->local_count,
                                                 args[0],
                                                 arg_type,
                                                 sizeof(arg_type),
                                                 arg_abi,
                                                 sizeof(arg_abi)) &&
                              v3_wasm_emit_expr(plan,
                                                lowering,
                                                function,
                                                ctx,
                                                args[0],
                                                imports,
                                                import_count,
                                                body) &&
                              v3_wasm_emit_i32_cast(body,
                                                    arg_type,
                                                    arg_abi,
                                                    cast_target_type,
                                                    cast_target_abi);
                    free(args);
                    return ok;
                }
            }
            {
                bool ok = v3_wasm_emit_call_expr(plan,
                                                 lowering,
                                                 function,
                                                 ctx,
                                                 callee,
                                                 args,
                                                 arg_count,
                                                 imports,
                                                 import_count,
                                                 body);
                free(args);
                return ok;
            }
        }
        free(args);
    }
    if (v3_parse_prefix_single_arg_call(expr, callee, sizeof(callee), arg_text, sizeof(arg_text))) {
        V3ResultIntrinsicKind result_kind =
            v3_resolve_result_intrinsic_kind(lowering,
                                             function->owner_module_path,
                                             ctx->aliases,
                                             ctx->alias_count,
                                             callee);
        if (result_kind == V3_RESULT_INTRINSIC_IS_OK ||
            result_kind == V3_RESULT_INTRINSIC_IS_ERR ||
            result_kind == V3_RESULT_INTRINSIC_VALUE ||
            result_kind == V3_RESULT_INTRINSIC_ERROR ||
            result_kind == V3_RESULT_INTRINSIC_ERROR_TEXT ||
            result_kind == V3_RESULT_INTRINSIC_ERROR_INFO_OF) {
            return v3_wasm_emit_result_intrinsic_expr(plan,
                                                      lowering,
                                                      function,
                                                      ctx,
                                                      result_kind,
                                                      arg_text,
                                                      imports,
                                                      import_count,
                                                      body);
        }
        if (strcmp(callee, "len") == 0) {
            return v3_wasm_emit_len_expr(plan,
                                         lowering,
                                         function,
                                         ctx,
                                         arg_text,
                                         imports,
                                         import_count,
                                         body);
        }
        if (strcmp(callee, "sizeof") == 0) {
            int32_t sizeof_value = 0;
            return v3_resolve_sizeof_expr_value(lowering,
                                                function->owner_module_path,
                                                ctx->aliases,
                                                ctx->alias_count,
                                                arg_text,
                                                &sizeof_value) &&
                   v3_wasm_emit_i32_const(body, sizeof_value);
        }
        {
            char cast_target_type[256];
            char cast_target_abi[32];
            if (v3_resolve_scalar_or_pointer_cast_target(lowering,
                                                         function->owner_module_path,
                                                         ctx->aliases,
                                                         ctx->alias_count,
                                                         callee,
                                                         cast_target_type,
                                                         sizeof(cast_target_type),
                                                         cast_target_abi,
                                                         sizeof(cast_target_abi))) {
                char arg_type[256];
                char arg_abi[32];
                return v3_infer_expr_type(plan,
                                          lowering,
                                          function,
                                          ctx->aliases,
                                          ctx->alias_count,
                                          ctx->locals,
                                          ctx->local_count,
                                          arg_text,
                                          arg_type,
                                          sizeof(arg_type),
                                          arg_abi,
                                          sizeof(arg_abi)) &&
                       v3_wasm_emit_expr(plan,
                                         lowering,
                                         function,
                                         ctx,
                                         arg_text,
                                         imports,
                                         import_count,
                                         body) &&
                       v3_wasm_emit_i32_cast(body,
                                             arg_type,
                                             arg_abi,
                                             cast_target_type,
                                             cast_target_abi);
            }
        }
        {
            char (*args)[4096] = v3_alloc_call_arg_scratch();
            bool ok;
            snprintf(args[0], sizeof(args[0]), "%s", arg_text);
            ok = v3_wasm_emit_call_expr(plan, lowering, function, ctx, callee, args, 1U, imports, import_count, body);
            free(args);
            return ok;
        }
    }
    {
        int32_t base_local_index = -1;
        int32_t field_offset = 0;
        char field_type[256];
        char field_abi[32];
        if (v3_resolve_field_path_expr(lowering,
                                       ctx->locals,
                                       ctx->local_count,
                                       expr,
                                       &base_local_index,
                                       field_type,
                                       sizeof(field_type),
                                       field_abi,
                                       sizeof(field_abi),
                                       &field_offset)) {
            return v3_wasm_append_u8(body, 0x20U) &&
                   v3_wasm_append_uleb_u32(body, ctx->local_indices[base_local_index]) &&
                   v3_wasm_emit_field_access_from_base(body, field_abi, field_offset);
        }
    }
    if (v3_parse_member_access_expr(expr, base_expr, sizeof(base_expr), field_name, sizeof(field_name))) {
        char base_type[256];
        char base_abi[32];
        char field_type[256];
        char field_abi[32];
        int32_t field_offset = 0;
        if (!v3_infer_expr_type(plan,
                                lowering,
                                function,
                                ctx->aliases,
                                ctx->alias_count,
                                ctx->locals,
                                ctx->local_count,
                                base_expr,
                                base_type,
                                sizeof(base_type),
                                base_abi,
                                sizeof(base_abi)) ||
            !v3_wasm_abi_uses_i32_param_slot(base_abi) ||
            !v3_resolve_field_meta(lowering,
                                   base_type,
                                   field_name,
                                   field_type,
                                   sizeof(field_type),
                                   field_abi,
                                   sizeof(field_abi),
                                   &field_offset) ||
            !v3_wasm_emit_expr(plan, lowering, function, ctx, base_expr, imports, import_count, body)) {
            return false;
        }
        return v3_wasm_emit_field_access_from_base(body, field_abi, field_offset);
    }
    {
        int32_t local_slot = v3_wasm_find_local_slot_index(ctx, expr);
        if (local_slot >= 0) {
            return v3_wasm_append_u8(body, 0x20U) &&
                   v3_wasm_append_uleb_u32(body, ctx->local_indices[local_slot]);
        }
    }
    {
        const V3TopLevelConstStub *top_level_const =
            v3_find_expr_top_level_const(lowering,
                                         function->owner_module_path,
                                         ctx->aliases,
                                         ctx->alias_count,
                                         expr);
        if (top_level_const && v3_wasm_abi_uses_i32_value(top_level_const->abi_class)) {
            int32_t value =
                strcmp(top_level_const->abi_class, "bool") == 0
                    ? (top_level_const->bool_value ? 1 : 0)
                    : (int32_t)top_level_const->i64_value;
            return v3_wasm_append_u8(body, 0x41U) &&
                   v3_wasm_append_sleb_i32(body, value);
        }
    }
    if (v3_parse_char_literal_text(expr, &char_value)) {
        return v3_wasm_append_u8(body, 0x41U) &&
               v3_wasm_append_sleb_i32(body, char_value);
    }
    if (v3_parse_int_literal_text(expr, &literal_value)) {
        return v3_wasm_append_u8(body, 0x41U) &&
               v3_wasm_append_sleb_i32(body, (int32_t)literal_value);
    }
    if (strcmp(expr, "true") == 0) {
        return v3_wasm_append_u8(body, 0x41U) &&
               v3_wasm_append_sleb_i32(body, 1);
    }
    if (strcmp(expr, "false") == 0 || strcmp(expr, "nil") == 0) {
        return v3_wasm_append_u8(body, 0x41U) &&
               v3_wasm_append_sleb_i32(body, 0);
    }
    fprintf(stderr,
            "[cheng_v3_seed] wasm expr emit unsupported function=%s expr=%s\n",
            function->symbol_text,
            expr);
    return false;
}

static bool v3_wasm_emit_lvalue_address(const V3SystemLinkPlanStub *plan,
                                        const V3LoweringPlanStub *lowering,
                                        const V3LoweredFunctionStub *function,
                                        const V3WasmFunctionContext *ctx,
                                        const char *expr_text,
                                        const V3WasmImportStub *imports,
                                        size_t import_count,
                                        V3WasmBuffer *body) {
    char expr[4096];
    char base_expr[4096];
    char index_expr[4096];
    char field_name[256];
    char field_type[256];
    char field_abi[32];
    int32_t base_local_index = -1;
    int32_t field_offset = 0;
    v3_wasm_trim_wrapping_parens(expr_text, expr, sizeof(expr));
    if (v3_resolve_field_path_expr(lowering,
                                   ctx->locals,
                                   ctx->local_count,
                                   expr,
                                   &base_local_index,
                                   field_type,
                                   sizeof(field_type),
                                   field_abi,
                                   sizeof(field_abi),
                                   &field_offset)) {
        return v3_wasm_emit_local_get(body, ctx->local_indices[base_local_index]) &&
               (field_offset == 0 ||
                (v3_wasm_emit_i32_const(body, field_offset) &&
                 v3_wasm_emit_i32_add(body)));
    }
    if (v3_parse_member_access_expr(expr, base_expr, sizeof(base_expr), field_name, sizeof(field_name))) {
        char base_type[256];
        char base_abi[32];
        if (!v3_infer_expr_type(plan,
                                lowering,
                                function,
                                ctx->aliases,
                                ctx->alias_count,
                                ctx->locals,
                                ctx->local_count,
                                base_expr,
                                base_type,
                                sizeof(base_type),
                                base_abi,
                                sizeof(base_abi)) ||
            !v3_wasm_abi_uses_i32_param_slot(base_abi) ||
            !v3_resolve_field_meta(lowering,
                                   base_type,
                                   field_name,
                                   field_type,
                                   sizeof(field_type),
                                   field_abi,
                                   sizeof(field_abi),
                                   &field_offset) ||
            !v3_wasm_emit_lvalue_address(plan,
                                         lowering,
                                         function,
                                         ctx,
                                         base_expr,
                                         imports,
                                         import_count,
                                         body)) {
            return false;
        }
        return field_offset == 0 ||
               (v3_wasm_emit_i32_const(body, field_offset) &&
                v3_wasm_emit_i32_add(body));
    }
    if (v3_parse_index_access_expr(expr,
                                   base_expr,
                                   sizeof(base_expr),
                                   index_expr,
                                   sizeof(index_expr))) {
        char base_type[256];
        char base_abi[32];
        char normalized_base_type[256];
        char elem_type[256];
        char elem_abi[32];
        int32_t elem_size = 0;
        int32_t data_field_offset = 0;
        int32_t len_field_offset = 0;
        int32_t fixed_len = 0;
        bool base_is_fixed_array = false;
        V3AsmCallTarget bounds_target;
        if (body == NULL || !ctx->has_scratch_locals ||
            !v3_wasm_resolve_index_expr_meta(plan,
                                             lowering,
                                             function,
                                             ctx,
                                             expr,
                                             base_expr,
                                             sizeof(base_expr),
                                             index_expr,
                                             sizeof(index_expr),
                                             base_type,
                                             sizeof(base_type),
                                             base_abi,
                                             sizeof(base_abi),
                                             normalized_base_type,
                                             sizeof(normalized_base_type),
                                             elem_type,
                                             sizeof(elem_type),
                                             elem_abi,
                                             sizeof(elem_abi),
                                             &elem_size,
                                             &data_field_offset,
                                             &len_field_offset,
                                             &fixed_len,
                                             &base_is_fixed_array) ||
            elem_size <= 0 ||
            !v3_wasm_build_bounds_check_target(&bounds_target) ||
            !v3_wasm_emit_expr(plan, lowering, function, ctx, base_expr, imports, import_count, body) ||
            !v3_wasm_emit_local_set(body, ctx->scratch_local_index) ||
            !v3_wasm_emit_expr(plan, lowering, function, ctx, index_expr, imports, import_count, body) ||
            !v3_wasm_emit_local_set(body, ctx->scratch2_local_index)) {
            return false;
        }
        if (base_is_fixed_array) {
            if (!v3_wasm_emit_i32_const(body, fixed_len)) {
                return false;
            }
        } else if (!v3_wasm_emit_local_get(body, ctx->scratch_local_index) ||
                   !v3_wasm_emit_field_access_from_base(body, "i32", len_field_offset)) {
            return false;
        }
        if (!v3_wasm_emit_local_get(body, ctx->scratch2_local_index) ||
            !v3_wasm_emit_call_target(lowering, &bounds_target, imports, import_count, body) ||
            !v3_wasm_emit_local_get(body, ctx->scratch_local_index) ||
            (!base_is_fixed_array &&
             !v3_wasm_emit_field_access_from_base(body, "ptr", data_field_offset))) {
            return false;
        }
        if (elem_size == 1) {
            return v3_wasm_emit_local_get(body, ctx->scratch2_local_index) &&
                   v3_wasm_emit_i32_add(body);
        }
        return v3_wasm_emit_local_get(body, ctx->scratch2_local_index) &&
               v3_wasm_emit_i32_const(body, elem_size) &&
               v3_wasm_append_u8(body, 0x6cU) &&
               v3_wasm_emit_i32_add(body);
    }
    {
        int32_t local_slot = v3_wasm_find_local_slot_index(ctx, expr);
        if (local_slot >= 0 &&
            strcmp(ctx->locals[local_slot].abi_class, "composite") == 0) {
            return v3_wasm_emit_local_get(body, ctx->local_indices[local_slot]);
        }
    }
    return false;
}

static bool v3_wasm_emit_composite_expr_into_local_address(const V3SystemLinkPlanStub *plan,
                                                           const V3LoweringPlanStub *lowering,
                                                           const V3LoweredFunctionStub *function,
                                                           const V3WasmFunctionContext *ctx,
                                                           const char *expr_text,
                                                           const char *dest_type_text,
                                                           uint32_t dest_addr_local_index,
                                                           const V3WasmImportStub *imports,
                                                           size_t import_count,
                                                           V3WasmBuffer *body) {
    V3TypeLayoutStub layout;
    char expr[4096];
    char default_type[CHENG_V3_MAX_TYPE_TEXT];
    char normalized_dest_type[CHENG_V3_MAX_TYPE_TEXT];
    char normalized_default_type[CHENG_V3_MAX_TYPE_TEXT];
    if (!v3_compute_type_layout_impl(lowering, dest_type_text, &layout, 0U)) {
        return false;
    }
    v3_wasm_trim_wrapping_parens(expr_text, expr, sizeof(expr));
    if (!v3_normalize_type_text(lowering,
                                function->owner_module_path,
                                ctx->aliases,
                                ctx->alias_count,
                                dest_type_text,
                                normalized_dest_type,
                                sizeof(normalized_dest_type))) {
        return false;
    }
    if (strcmp(expr, "nil") == 0 ||
        (strcmp(expr, "[]") == 0 && v3_is_seq_type_text(normalized_dest_type)) ||
        (strcmp(expr, "\"\"") == 0 && strcmp(normalized_dest_type, "str") == 0)) {
        return v3_wasm_emit_zero_region_from_local(body, dest_addr_local_index, layout.size);
    }
    if (strcmp(normalized_dest_type, "str") == 0) {
        char literal[4096];
        int32_t plus_index = -1;
        if (v3_parse_string_literal_text(expr, literal, sizeof(literal))) {
            return v3_wasm_emit_str_literal_into_local_address(ctx,
                                                               body,
                                                               dest_addr_local_index,
                                                               literal);
        }
        plus_index = v3_find_top_level_binary_op_last(expr, "+");
        if (plus_index >= 0) {
            char concat_terms[32][4096];
            size_t term_count = 0U;
            size_t term_index = 0U;
            V3AsmCallTarget target;
            uint32_t term_addr_local_index = ctx->scratch3_local_index;
            uint32_t accum_local_index = ctx->scratch4_local_index;
            if (!v3_wasm_flatten_str_concat_terms(expr,
                                                  concat_terms,
                                                  &term_count,
                                                  sizeof(concat_terms) / sizeof(concat_terms[0]))) {
                return false;
            }
            if (term_count == 0U) {
                return v3_wasm_emit_zero_region_from_local(body, dest_addr_local_index, layout.size);
            }
            if (term_count == 1U) {
                return v3_wasm_emit_composite_expr_into_local_address(plan,
                                                                      lowering,
                                                                      function,
                                                                      ctx,
                                                                      concat_terms[0],
                                                                      "str",
                                                                      dest_addr_local_index,
                                                                      imports,
                                                                      import_count,
                                                                      body);
            }
            if (!ctx->has_frame_base_local ||
                ctx->str_temp0_offset < 0 ||
                ctx->str_temp1_offset < 0 ||
                !v3_wasm_build_str_concat_target(plan, &target) ||
                !v3_wasm_emit_frame_temp_address(ctx,
                                                 body,
                                                 term_addr_local_index,
                                                 ctx->str_temp0_offset) ||
                !v3_wasm_emit_composite_expr_into_local_address(plan,
                                                                lowering,
                                                                function,
                                                                ctx,
                                                                concat_terms[0],
                                                                "str",
                                                                term_addr_local_index,
                                                                imports,
                                                                import_count,
                                                                body) ||
                !v3_wasm_emit_local_get(body, term_addr_local_index) ||
                !v3_wasm_emit_local_set(body, accum_local_index)) {
                return false;
            }
            for (term_index = 1U; term_index < term_count; ++term_index) {
                if (!v3_wasm_emit_frame_temp_address(ctx,
                                                     body,
                                                     term_addr_local_index,
                                                     ctx->str_temp1_offset) ||
                    !v3_wasm_emit_composite_expr_into_local_address(plan,
                                                                    lowering,
                                                                    function,
                                                                    ctx,
                                                                    concat_terms[term_index],
                                                                    "str",
                                                                    term_addr_local_index,
                                                                    imports,
                                                                    import_count,
                                                                    body) ||
                    !v3_wasm_emit_local_get(body, accum_local_index) ||
                    !v3_wasm_emit_local_get(body, term_addr_local_index) ||
                    !v3_wasm_emit_call_target(lowering, &target, imports, import_count, body) ||
                    !v3_wasm_emit_local_set(body, accum_local_index)) {
                    return false;
                }
            }
            return v3_wasm_emit_copy_region_between_locals(body,
                                                           dest_addr_local_index,
                                                           accum_local_index,
                                                           layout.size);
        }
    }
    if (v3_parse_default_expr_text(expr, default_type, sizeof(default_type))) {
        if (!v3_normalize_type_text(lowering,
                                    function->owner_module_path,
                                    ctx->aliases,
                                    ctx->alias_count,
                                    default_type,
                                    normalized_default_type,
                                    sizeof(normalized_default_type)) ||
            strcmp(normalized_default_type, normalized_dest_type) != 0) {
            return false;
        }
        return v3_wasm_emit_zero_region_from_local(body, dest_addr_local_index, layout.size);
    }
    {
        char base_expr[4096];
        char index_expr[4096];
        char field_name[256];
        char value_type[256];
        char value_abi[32];
        uint32_t copy_dest_local_index = dest_addr_local_index;
        uint32_t src_addr_local_index =
            v3_wasm_pick_temp_local_excluding(ctx, dest_addr_local_index);
        if (src_addr_local_index != 0U &&
            (v3_parse_member_access_expr(expr,
                                         base_expr,
                                         sizeof(base_expr),
                                         field_name,
                                         sizeof(field_name)) ||
             v3_parse_index_access_expr(expr,
                                        base_expr,
                                        sizeof(base_expr),
                                        index_expr,
                                        sizeof(index_expr)))) {
            /* Index/member lvalue lowering reuses scratch0/scratch1 internally. */
            if ((dest_addr_local_index == ctx->scratch_local_index ||
                 dest_addr_local_index == ctx->scratch2_local_index) &&
                ((copy_dest_local_index =
                      v3_wasm_pick_lvalue_preserve_local(ctx, dest_addr_local_index)) == 0U ||
                 !v3_wasm_emit_local_get(body, dest_addr_local_index) ||
                 !v3_wasm_emit_local_set(body, copy_dest_local_index))) {
                return false;
            }
            if (!v3_infer_expr_type(plan,
                                    lowering,
                                    function,
                                    ctx->aliases,
                                    ctx->alias_count,
                                    ctx->locals,
                                    ctx->local_count,
                                    expr,
                                    value_type,
                                    sizeof(value_type),
                                    value_abi,
                                    sizeof(value_abi)) ||
                strcmp(value_abi, "composite") != 0 ||
                !v3_wasm_emit_lvalue_address(plan,
                                             lowering,
                                             function,
                                             ctx,
                                             expr,
                                             imports,
                                             import_count,
                                             body) ||
                !v3_wasm_emit_local_set(body, src_addr_local_index)) {
                return false;
            }
            return v3_wasm_emit_copy_region_between_locals(body,
                                                           copy_dest_local_index,
                                                           src_addr_local_index,
                                                           layout.size);
        }
    }
    {
        size_t arg_count = 0U;
        char inner_callee[PATH_MAX];
        char (*args)[4096] = v3_alloc_call_arg_scratch();
        if (v3_parse_call_text(expr,
                               inner_callee,
                               sizeof(inner_callee),
                               args,
                               &arg_count,
                               CHENG_V3_MAX_CALL_ARGS)) {
            V3ResultIntrinsicKind result_kind =
                v3_resolve_result_intrinsic_kind(lowering,
                                                 function->owner_module_path,
                                                 ctx->aliases,
                                                 ctx->alias_count,
                                                 inner_callee);
            if (result_kind == V3_RESULT_INTRINSIC_OK ||
                result_kind == V3_RESULT_INTRINSIC_ERR ||
                result_kind == V3_RESULT_INTRINSIC_ERR_CODE ||
                result_kind == V3_RESULT_INTRINSIC_ERR_INFO) {
                bool ok = v3_wasm_emit_result_constructor_into_local_address(plan,
                                                                             lowering,
                                                                             function,
                                                                             ctx,
                                                                             result_kind,
                                                                             inner_callee,
                                                                             args,
                                                                             arg_count,
                                                                             normalized_dest_type,
                                                                             dest_addr_local_index,
                                                                             imports,
                                                                             import_count,
                                                                             body);
                free(args);
                return ok;
            }
            if ((result_kind == V3_RESULT_INTRINSIC_VALUE ||
                 result_kind == V3_RESULT_INTRINSIC_ERROR ||
                 result_kind == V3_RESULT_INTRINSIC_ERROR_TEXT ||
                 result_kind == V3_RESULT_INTRINSIC_ERROR_INFO_OF) &&
                arg_count == 1U) {
                bool ok = v3_wasm_emit_composite_result_intrinsic_into_local_address(plan,
                                                                                     lowering,
                                                                                     function,
                                                                                     ctx,
                                                                                     result_kind,
                                                                                     args[0],
                                                                                     dest_addr_local_index,
                                                                                     layout.size,
                                                                                     imports,
                                                                                     import_count,
                                                                                     body);
                if (!ok) {
                    fprintf(stderr,
                            "[cheng_v3_seed] wasm composite result intrinsic emit failed function=%s callee=%s expr=%s\n",
                            function->symbol_text,
                            inner_callee,
                            expr);
                }
                free(args);
                return ok;
            }
            {
                V3AsmCallTarget target;
                uint32_t result_local_index =
                    dest_addr_local_index == ctx->scratch3_local_index
                    ? ctx->scratch2_local_index
                    : ctx->scratch3_local_index;
                if (v3_resolve_call_target_with_args(plan,
                                                     lowering,
                                                     function,
                                                     ctx->aliases,
                                                     ctx->alias_count,
                                                     ctx->locals,
                                                     ctx->local_count,
                                                     inner_callee,
                                                     args,
                                                     arg_count,
                                                     &target)) {
                    if (strcmp(target.return_abi_class, "composite") != 0) {
                        fprintf(stderr,
                                "[cheng_v3_seed] wasm composite call return reject function=%s callee=%s ret=%s expr=%s\n",
                                function->symbol_text,
                                inner_callee,
                                target.return_abi_class,
                                expr);
                        free(args);
                        return false;
                    }
                    if (!v3_wasm_emit_expr(plan,
                                           lowering,
                                           function,
                                           ctx,
                                           expr,
                                           imports,
                                           import_count,
                                           body) ||
                        !v3_wasm_emit_local_set(body, result_local_index)) {
                        fprintf(stderr,
                                "[cheng_v3_seed] wasm composite call emit failed function=%s callee=%s expr=%s\n",
                                function->symbol_text,
                                inner_callee,
                                expr);
                        free(args);
                        return false;
                    }
                    free(args);
                    return v3_wasm_emit_copy_region_between_locals(body,
                                                                   dest_addr_local_index,
                                                                   result_local_index,
                                                                   layout.size);
                }
                if (v3_callee_is_constructor_like(lowering,
                                                  function->owner_module_path,
                                                  ctx->aliases,
                                                  ctx->alias_count,
                                                  inner_callee)) {
                    bool ok = v3_wasm_emit_constructor_into_local_address(plan,
                                                                          lowering,
                                                                          function,
                                                                          ctx,
                                                                          inner_callee,
                                                                          args,
                                                                          arg_count,
                                                                          normalized_dest_type,
                                                                          dest_addr_local_index,
                                                                          imports,
                                                                          import_count,
                                                                          body);
                    if (!ok) {
                        fprintf(stderr,
                                "[cheng_v3_seed] wasm composite constructor emit failed function=%s callee=%s expr=%s\n",
                                function->symbol_text,
                                inner_callee,
                                expr);
                    }
                    free(args);
                    return ok;
                }
            }
        }
        free(args);
    }
    {
        char inner_callee[PATH_MAX];
        char arg_text[4096];
        if (v3_parse_prefix_single_arg_call(expr,
                                            inner_callee,
                                            sizeof(inner_callee),
                                            arg_text,
                                            sizeof(arg_text))) {
            V3ResultIntrinsicKind result_kind =
                v3_resolve_result_intrinsic_kind(lowering,
                                                 function->owner_module_path,
                                                 ctx->aliases,
                                                 ctx->alias_count,
                                                 inner_callee);
            if (result_kind == V3_RESULT_INTRINSIC_OK ||
                result_kind == V3_RESULT_INTRINSIC_ERR ||
                result_kind == V3_RESULT_INTRINSIC_ERR_INFO) {
                char args[1][4096];
                snprintf(args[0], sizeof(args[0]), "%s", arg_text);
                return v3_wasm_emit_result_constructor_into_local_address(plan,
                                                                          lowering,
                                                                          function,
                                                                          ctx,
                                                                          result_kind,
                                                                          inner_callee,
                                                                          args,
                                                                          1U,
                                                                          normalized_dest_type,
                                                                          dest_addr_local_index,
                                                                          imports,
                                                                          import_count,
                                                                          body);
            }
            if (result_kind == V3_RESULT_INTRINSIC_VALUE ||
                result_kind == V3_RESULT_INTRINSIC_ERROR ||
                result_kind == V3_RESULT_INTRINSIC_ERROR_TEXT ||
                result_kind == V3_RESULT_INTRINSIC_ERROR_INFO_OF) {
                bool ok = v3_wasm_emit_composite_result_intrinsic_into_local_address(plan,
                                                                                     lowering,
                                                                                     function,
                                                                                     ctx,
                                                                                     result_kind,
                                                                                     arg_text,
                                                                                     dest_addr_local_index,
                                                                                     layout.size,
                                                                                     imports,
                                                                                     import_count,
                                                                                     body);
                if (!ok) {
                    fprintf(stderr,
                            "[cheng_v3_seed] wasm composite prefix result intrinsic emit failed function=%s callee=%s expr=%s\n",
                            function->symbol_text,
                            inner_callee,
                            expr);
                }
                return ok;
            }
        }
    }
    fprintf(stderr,
            "[cheng_v3_seed] wasm composite expr fallback function=%s type=%s expr=%s\n",
            function->symbol_text,
            normalized_dest_type,
            expr);
    if (!v3_wasm_emit_expr(plan,
                           lowering,
                           function,
                           ctx,
                           expr,
                           imports,
                           import_count,
                           body)) {
        fprintf(stderr,
                "[cheng_v3_seed] wasm composite fallback emit expr failed function=%s type=%s expr=%s\n",
                function->symbol_text,
                normalized_dest_type,
                expr);
        return false;
    }
    if (!v3_wasm_emit_composite_pointer_value_into_local_address(ctx,
                                                                 dest_addr_local_index,
                                                                 layout.size,
                                                                 body)) {
        fprintf(stderr,
                "[cheng_v3_seed] wasm composite fallback copy failed function=%s type=%s expr=%s size=%d\n",
                function->symbol_text,
                normalized_dest_type,
                expr,
                layout.size);
        return false;
    }
    return true;
}

static bool v3_wasm_emit_function_prologue(const V3WasmFunctionContext *ctx,
                                           V3WasmBuffer *body) {
    size_t i;
    if (ctx == NULL || body == NULL) {
        return false;
    }
    if (!ctx->has_frame_base_local || ctx->frame_size <= 0) {
        return true;
    }
    if (!v3_wasm_emit_global_get(body, CHENG_V3_WASM_STACK_GLOBAL_INDEX) ||
        !v3_wasm_emit_local_set(body, ctx->frame_base_local_index) ||
        !v3_wasm_emit_global_get(body, CHENG_V3_WASM_STACK_GLOBAL_INDEX) ||
        !v3_wasm_emit_i32_const(body, ctx->frame_size) ||
        !v3_wasm_emit_i32_add(body) ||
        !v3_wasm_emit_global_set(body, CHENG_V3_WASM_STACK_GLOBAL_INDEX)) {
        return false;
    }
    for (i = ctx->param_count; i < ctx->local_count; ++i) {
        const V3AsmLocalSlot *slot = &ctx->locals[i];
        if (strcmp(slot->abi_class, "composite") != 0 || slot->stack_offset < 0) {
            continue;
        }
        if (!v3_wasm_emit_local_get(body, ctx->frame_base_local_index) ||
            (slot->stack_offset != 0 && (!v3_wasm_emit_i32_const(body, slot->stack_offset) ||
                                         !v3_wasm_emit_i32_add(body))) ||
            !v3_wasm_emit_local_set(body, ctx->local_indices[i]) ||
            !v3_wasm_emit_zero_region_from_local(body, ctx->local_indices[i], slot->stack_size)) {
            return false;
        }
    }
    return true;
}

static bool v3_wasm_emit_if_statement(const V3SystemLinkPlanStub *plan,
                                      const V3LoweringPlanStub *lowering,
                                      const V3LoweredFunctionStub *function,
                                      const V3WasmFunctionContext *ctx,
                                      char **lines,
                                      size_t line_count,
                                      size_t *index_io,
                                      int32_t statement_indent,
                                      int32_t loop_continue_depth,
                                      int32_t loop_break_depth,
                                      const char *statement,
                                      const V3WasmImportStub *imports,
                                      size_t import_count,
                                      V3WasmBuffer *body,
                                      bool *terminated_out) {
    char cond[4096];
    char inline_stmt[8192];
    bool then_terminated = false;
    bool else_terminated = false;
    int32_t branch_continue_depth = loop_continue_depth >= 0 ? loop_continue_depth + 1 : -1;
    int32_t branch_break_depth = loop_break_depth >= 0 ? loop_break_depth + 1 : -1;
    if (!v3_parse_if_header(statement, cond, sizeof(cond), inline_stmt, sizeof(inline_stmt)) ||
        !v3_wasm_emit_expr(plan, lowering, function, ctx, cond, imports, import_count, body) ||
        !v3_wasm_append_u8(body, 0x04U) ||
        !v3_wasm_append_u8(body, 0x40U)) {
        return false;
    }
    if (inline_stmt[0] != '\0') {
        if (!v3_wasm_emit_statement(plan,
                                    lowering,
                                    function,
                                    ctx,
                                    NULL,
                                    0U,
                                    NULL,
                                    statement_indent + 1,
                                    branch_continue_depth,
                                    branch_break_depth,
                                    inline_stmt,
                                    imports,
                                    import_count,
                                    false,
                                    body,
                                    &then_terminated)) {
            return false;
        }
    } else if (lines != NULL && index_io != NULL) {
        if (!v3_wasm_emit_block_statements(plan,
                                           lowering,
                                           function,
                                           ctx,
                                           lines,
                                           line_count,
                                           index_io,
                                           statement_indent,
                                           branch_continue_depth,
                                           branch_break_depth,
                                           imports,
                                           import_count,
                                           body,
                                           &then_terminated)) {
            return false;
        }
    }
    if (lines != NULL && index_io != NULL) {
        while (*index_io < line_count) {
            char raw_copy[4096];
            char *trimmed;
            int32_t indent;
            snprintf(raw_copy, sizeof(raw_copy), "%s", lines[*index_io]);
            trimmed = v3_trim_inplace(raw_copy);
            if (*trimmed == '\0') {
                *index_io += 1U;
                continue;
            }
            indent = v3_line_indent(lines[*index_io]);
            if (indent != statement_indent) {
                break;
            }
            if (v3_startswith(trimmed, "elif ")) {
                char elif_stmt[8192];
                char elif_as_if[8192];
                if (!v3_collect_statement_from_lines(lines, line_count, index_io, elif_stmt, sizeof(elif_stmt))) {
                    return false;
                }
                snprintf(elif_as_if, sizeof(elif_as_if), "if %s", elif_stmt + 5);
                if (!v3_wasm_append_u8(body, 0x05U) ||
                    !v3_wasm_emit_statement(plan,
                                            lowering,
                                            function,
                                            ctx,
                                            lines,
                                            line_count,
                                            index_io,
                                            statement_indent,
                                            branch_continue_depth,
                                            branch_break_depth,
                                            elif_as_if,
                                            imports,
                                            import_count,
                                            false,
                                            body,
                                            &else_terminated)) {
                    return false;
                }
                return v3_wasm_append_u8(body, 0x0bU) &&
                       ((*terminated_out = then_terminated && else_terminated), true);
            }
            if (v3_startswith(trimmed, "else")) {
                char else_stmt[8192];
                char else_inline[8192];
                if (!v3_collect_statement_from_lines(lines, line_count, index_io, else_stmt, sizeof(else_stmt)) ||
                    !v3_parse_else_header(else_stmt, else_inline, sizeof(else_inline)) ||
                    !v3_wasm_append_u8(body, 0x05U)) {
                    return false;
                }
                if (else_inline[0] != '\0') {
                    if (!v3_wasm_emit_statement(plan,
                                                lowering,
                                                function,
                                                ctx,
                                                NULL,
                                                0U,
                                                NULL,
                                                statement_indent + 1,
                                                branch_continue_depth,
                                                branch_break_depth,
                                                else_inline,
                                                imports,
                                                import_count,
                                                false,
                                                body,
                                                &else_terminated)) {
                        return false;
                    }
                } else if (!v3_wasm_emit_block_statements(plan,
                                                          lowering,
                                                          function,
                                                          ctx,
                                                          lines,
                                                          line_count,
                                                          index_io,
                                                          statement_indent,
                                                          branch_continue_depth,
                                                          branch_break_depth,
                                                          imports,
                                                          import_count,
                                                          body,
                                                          &else_terminated)) {
                    return false;
                }
                return v3_wasm_append_u8(body, 0x0bU) &&
                       ((*terminated_out = then_terminated && else_terminated), true);
            }
            break;
        }
    }
    *terminated_out = false;
    return v3_wasm_append_u8(body, 0x0bU);
}

static bool v3_wasm_emit_while_statement(const V3SystemLinkPlanStub *plan,
                                         const V3LoweringPlanStub *lowering,
                                         const V3LoweredFunctionStub *function,
                                         const V3WasmFunctionContext *ctx,
                                         char **lines,
                                         size_t line_count,
                                         size_t *index_io,
                                         int32_t statement_indent,
                                         const char *statement,
                                         const V3WasmImportStub *imports,
                                         size_t import_count,
                                         V3WasmBuffer *body) {
    char cond[4096];
    char inline_stmt[8192];
    bool branch_terminated = false;
    if (!v3_parse_while_header(statement, cond, sizeof(cond), inline_stmt, sizeof(inline_stmt)) ||
        !v3_wasm_append_u8(body, 0x02U) ||
        !v3_wasm_append_u8(body, 0x40U) ||
        !v3_wasm_append_u8(body, 0x03U) ||
        !v3_wasm_append_u8(body, 0x40U) ||
        !v3_wasm_emit_expr(plan, lowering, function, ctx, cond, imports, import_count, body) ||
        !v3_wasm_append_u8(body, 0x45U) ||
        !v3_wasm_append_u8(body, 0x0dU) ||
        !v3_wasm_append_uleb_u32(body, 1U)) {
        return false;
    }
    if (inline_stmt[0] != '\0') {
        if (!v3_wasm_emit_statement(plan,
                                    lowering,
                                    function,
                                    ctx,
                                    NULL,
                                    0U,
                                    NULL,
                                    statement_indent + 1,
                                    0,
                                    1,
                                    inline_stmt,
                                    imports,
                                    import_count,
                                    false,
                                    body,
                                    &branch_terminated)) {
            return false;
        }
    } else if (lines != NULL && index_io != NULL) {
        if (!v3_wasm_emit_block_statements(plan,
                                           lowering,
                                           function,
                                           ctx,
                                           lines,
                                           line_count,
                                           index_io,
                                           statement_indent,
                                           0,
                                           1,
                                           imports,
                                           import_count,
                                           body,
                                           &branch_terminated)) {
            return false;
        }
    }
    return v3_wasm_append_u8(body, 0x0cU) &&
           v3_wasm_append_uleb_u32(body, 0U) &&
           v3_wasm_append_u8(body, 0x0bU) &&
           v3_wasm_append_u8(body, 0x0bU);
}

static bool v3_wasm_emit_for_statement(const V3SystemLinkPlanStub *plan,
                                       const V3LoweringPlanStub *lowering,
                                       const V3LoweredFunctionStub *function,
                                       const V3WasmFunctionContext *ctx,
                                       char **lines,
                                       size_t line_count,
                                       size_t *index_io,
                                       int32_t statement_indent,
                                       const char *statement,
                                       const V3WasmImportStub *imports,
                                       size_t import_count,
                                       V3WasmBuffer *body) {
    char iter_name[128];
    char start_expr[4096];
    char end_expr[4096];
    char inline_stmt[8192];
    bool end_inclusive = false;
    bool branch_terminated = false;
    int32_t iter_local_slot = -1;
    if (!v3_parse_for_header(statement,
                             iter_name,
                             sizeof(iter_name),
                             start_expr,
                             sizeof(start_expr),
                             end_expr,
                             sizeof(end_expr),
                             &end_inclusive,
                             inline_stmt,
                             sizeof(inline_stmt))) {
        return false;
    }
    iter_local_slot = v3_wasm_find_local_slot_index(ctx, iter_name);
    if (iter_local_slot < 0 ||
        !v3_wasm_emit_expr(plan, lowering, function, ctx, start_expr, imports, import_count, body) ||
        !v3_wasm_emit_local_set(body, ctx->local_indices[iter_local_slot]) ||
        !v3_wasm_append_u8(body, 0x02U) ||
        !v3_wasm_append_u8(body, 0x40U) ||
        !v3_wasm_append_u8(body, 0x03U) ||
        !v3_wasm_append_u8(body, 0x40U) ||
        !v3_wasm_emit_local_get(body, ctx->local_indices[iter_local_slot]) ||
        !v3_wasm_emit_expr(plan, lowering, function, ctx, end_expr, imports, import_count, body) ||
        !v3_wasm_append_u8(body, end_inclusive ? 0x4aU : 0x4eU) ||
        !v3_wasm_append_u8(body, 0x0dU) ||
        !v3_wasm_append_uleb_u32(body, 1U)) {
        return false;
    }
    if (!v3_wasm_append_u8(body, 0x02U) ||
        !v3_wasm_append_u8(body, 0x40U)) {
        return false;
    }
    if (inline_stmt[0] != '\0') {
        if (!v3_wasm_emit_statement(plan,
                                    lowering,
                                    function,
                                    ctx,
                                    NULL,
                                    0U,
                                    NULL,
                                    statement_indent + 1,
                                    0,
                                    2,
                                    inline_stmt,
                                    imports,
                                    import_count,
                                    false,
                                    body,
                                    &branch_terminated)) {
            return false;
        }
    } else if (lines != NULL && index_io != NULL) {
        if (!v3_wasm_emit_block_statements(plan,
                                           lowering,
                                           function,
                                           ctx,
                                           lines,
                                           line_count,
                                           index_io,
                                           statement_indent,
                                           0,
                                           2,
                                           imports,
                                           import_count,
                                           body,
                                           &branch_terminated)) {
            return false;
        }
    }
    return v3_wasm_append_u8(body, 0x0bU) &&
           v3_wasm_emit_local_get(body, ctx->local_indices[iter_local_slot]) &&
           v3_wasm_emit_i32_const(body, 1) &&
           v3_wasm_emit_i32_add(body) &&
           v3_wasm_emit_local_set(body, ctx->local_indices[iter_local_slot]) &&
           v3_wasm_append_u8(body, 0x0cU) &&
           v3_wasm_append_uleb_u32(body, 0U) &&
           v3_wasm_append_u8(body, 0x0bU) &&
           v3_wasm_append_u8(body, 0x0bU);
}

static bool v3_wasm_emit_statement(const V3SystemLinkPlanStub *plan,
                                   const V3LoweringPlanStub *lowering,
                                   const V3LoweredFunctionStub *function,
                                   const V3WasmFunctionContext *ctx,
                                   char **lines,
                                   size_t line_count,
                                   size_t *index_io,
                                   int32_t statement_indent,
                                   int32_t loop_continue_depth,
                                   int32_t loop_break_depth,
                                   const char *statement,
                                   const V3WasmImportStub *imports,
                                   size_t import_count,
                                   bool allow_implicit_return,
                                   V3WasmBuffer *body,
                                   bool *terminated_out) {
    char name[128];
    char type_text[256];
    char expr[4096];
    int32_t local_slot = -1;
    *terminated_out = false;
    if (v3_startswith(statement, "if ")) {
        return v3_wasm_emit_if_statement(plan,
                                         lowering,
                                         function,
                                         ctx,
                                         lines,
                                         line_count,
                                         index_io,
                                         statement_indent,
                                         loop_continue_depth,
                                         loop_break_depth,
                                         statement,
                                         imports,
                                         import_count,
                                         body,
                                         terminated_out);
    }
    if (v3_startswith(statement, "while ")) {
        return v3_wasm_emit_while_statement(plan,
                                            lowering,
                                            function,
                                            ctx,
                                            lines,
                                            line_count,
                                            index_io,
                                            statement_indent,
                                            statement,
                                            imports,
                                            import_count,
                                            body);
    }
    if (v3_startswith(statement, "for ")) {
        return v3_wasm_emit_for_statement(plan,
                                          lowering,
                                          function,
                                          ctx,
                                          lines,
                                          line_count,
                                          index_io,
                                          statement_indent,
                                          statement,
                                          imports,
                                          import_count,
                                          body);
    }
    if (strcmp(statement, "continue") == 0) {
        if (loop_continue_depth < 0) {
            return false;
        }
        *terminated_out = true;
        return v3_wasm_append_u8(body, 0x0cU) &&
               v3_wasm_append_uleb_u32(body, (uint32_t)loop_continue_depth);
    }
    if (strcmp(statement, "break") == 0) {
        if (loop_break_depth < 0) {
            return false;
        }
        *terminated_out = true;
        return v3_wasm_append_u8(body, 0x0cU) &&
               v3_wasm_append_uleb_u32(body, (uint32_t)loop_break_depth);
    }
    if ((v3_startswith(statement, "let ") || v3_startswith(statement, "var ")) &&
        v3_parse_binding_decl_meta(statement,
                                   v3_startswith(statement, "let ") ? "let " : "var ",
                                   name,
                                   sizeof(name),
                                   type_text,
                                   sizeof(type_text))) {
        local_slot = v3_wasm_find_local_slot_index(ctx, name);
        if (local_slot < 0) {
            return false;
        }
        if (strcmp(ctx->locals[local_slot].abi_class, "composite") == 0) {
            return true;
        }
        return v3_wasm_emit_i32_const(body, 0) &&
               v3_wasm_emit_local_set(body, ctx->local_indices[local_slot]);
    }
    if ((v3_startswith(statement, "let ") || v3_startswith(statement, "var ")) &&
        v3_parse_binding_meta(statement,
                              v3_startswith(statement, "let ") ? "let " : "var ",
                              name,
                              sizeof(name),
                              type_text,
                              sizeof(type_text),
                              expr,
                              sizeof(expr))) {
        local_slot = v3_wasm_find_local_slot_index(ctx, name);
        if (!v3_tuple_binding_expr_complete(type_text, expr)) {
            return false;
        }
        if (local_slot < 0) {
            return false;
        }
        if (strcmp(ctx->locals[local_slot].abi_class, "composite") == 0) {
            return v3_wasm_emit_composite_expr_into_local_address(plan,
                                                                  lowering,
                                                                  function,
                                                                  ctx,
                                                                  expr,
                                                                  ctx->locals[local_slot].type_text,
                                                                  ctx->local_indices[local_slot],
                                                                  imports,
                                                                  import_count,
                                                                  body);
        }
        return v3_wasm_emit_expr(plan, lowering, function, ctx, expr, imports, import_count, body) &&
               v3_wasm_emit_local_set(body, ctx->local_indices[local_slot]);
    }
    if (v3_startswith(statement, "return ")) {
        *terminated_out = true;
        if (strcmp(function->return_abi_class, "composite") == 0) {
            int32_t return_slot_offset = 0;
            int32_t return_slot_size = 0;
            int32_t return_slot_addr = 0;
            if (!v3_wasm_compute_static_return_slot_offset(lowering,
                                                           function,
                                                           &return_slot_offset,
                                                           &return_slot_size)) {
                return false;
            }
            return_slot_addr = CHENG_V3_WASM_STATIC_BASE_I32 + return_slot_offset;
            (void)return_slot_size;
            if (!v3_wasm_emit_i32_const(body, return_slot_addr) ||
                !v3_wasm_emit_local_set(body, ctx->scratch_local_index) ||
                !v3_wasm_emit_composite_expr_into_local_address(plan,
                                                                lowering,
                                                                function,
                                                                ctx,
                                                                statement + 7,
                                                                function->return_type,
                                                                ctx->scratch_local_index,
                                                                imports,
                                                                import_count,
                                                                body)) {
                return false;
            }
            if (ctx->has_frame_base_local &&
                (!v3_wasm_emit_local_get(body, ctx->frame_base_local_index) ||
                 !v3_wasm_emit_global_set(body, CHENG_V3_WASM_STACK_GLOBAL_INDEX))) {
                return false;
            }
            return v3_wasm_emit_i32_const(body, return_slot_addr) &&
                   v3_wasm_append_u8(body, 0x0fU);
        }
        if (!v3_wasm_emit_expr(plan, lowering, function, ctx, statement + 7, imports, import_count, body) ||
            !v3_wasm_emit_local_set(body, ctx->scratch_local_index)) {
            return false;
        }
        if (!v3_wasm_emit_restore_frame_base(ctx, body)) {
            return false;
        }
        return v3_wasm_emit_local_get(body, ctx->scratch_local_index) &&
               v3_wasm_append_u8(body, 0x0fU);
    }
    if (strcmp(statement, "return") == 0) {
        *terminated_out = true;
        return v3_wasm_emit_restore_frame_base(ctx, body) &&
               v3_wasm_append_u8(body, 0x0fU);
    }
    if (v3_parse_assignment_meta(statement, name, sizeof(name), expr, sizeof(expr))) {
        char field_type[256];
        char field_abi[32];
        local_slot = v3_wasm_find_local_slot_index(ctx, name);
        if (local_slot >= 0) {
            if (strcmp(ctx->locals[local_slot].abi_class, "composite") == 0) {
                return v3_wasm_emit_composite_expr_into_local_address(plan,
                                                                      lowering,
                                                                      function,
                                                                      ctx,
                                                                      expr,
                                                                      ctx->locals[local_slot].type_text,
                                                                      ctx->local_indices[local_slot],
                                                                      imports,
                                                                      import_count,
                                                                      body);
            }
            return v3_wasm_emit_expr(plan, lowering, function, ctx, expr, imports, import_count, body) &&
                   v3_wasm_emit_local_set(body, ctx->local_indices[local_slot]);
        }
        if (!v3_infer_lvalue_expr_type(plan,
                                       lowering,
                                       function,
                                       ctx->aliases,
                                       ctx->alias_count,
                                       ctx->locals,
                                       ctx->local_count,
                                       name,
                                       field_type,
                                       sizeof(field_type),
                                       field_abi,
                                       sizeof(field_abi)) ||
            !v3_wasm_emit_lvalue_address(plan,
                                         lowering,
                                         function,
                                         ctx,
                                         name,
                                         imports,
                                         import_count,
                                         body) ||
            !v3_wasm_emit_local_set(body, ctx->scratch_local_index)) {
            return false;
        }
        if (strcmp(field_abi, "composite") == 0) {
            return v3_wasm_emit_composite_expr_into_local_address(plan,
                                                                  lowering,
                                                                  function,
                                                                  ctx,
                                                                  expr,
                                                                  field_type,
                                                                  ctx->scratch_local_index,
                                                                  imports,
                                                                  import_count,
                                                                  body);
        }
        if (!v3_wasm_emit_expr(plan, lowering, function, ctx, expr, imports, import_count, body) ||
            !v3_wasm_emit_local_set(body, ctx->scratch2_local_index) ||
            !v3_wasm_emit_local_get(body, ctx->scratch_local_index) ||
            !v3_wasm_emit_local_get(body, ctx->scratch2_local_index) ||
            !v3_wasm_emit_store_scalar_to_top_address(body, field_abi)) {
            return false;
        }
        return true;
    }
    if (!allow_implicit_return || v3_wasm_abi_is_void(function->return_abi_class)) {
        char callee[PATH_MAX];
        char arg_text[4096];
        callee[0] = '\0';
        arg_text[0] = '\0';
        {
            char (*args)[4096] = v3_alloc_call_arg_scratch();
            size_t arg_count = 0U;
            bool parsed = v3_parse_call_text(statement, callee, sizeof(callee), args, &arg_count, CHENG_V3_MAX_CALL_ARGS);
            bool ok = parsed &&
                      v3_wasm_emit_call_statement(plan,
                                                  lowering,
                                                  function,
                                                  ctx,
                                                  callee,
                                                  args,
                                                  arg_count,
                                                  imports,
                                                  import_count,
                                                  body);
            free(args);
            if (parsed) {
                return ok;
            }
        }
        if (v3_parse_prefix_single_arg_call(statement, callee, sizeof(callee), arg_text, sizeof(arg_text))) {
            char (*args)[4096] = v3_alloc_call_arg_scratch();
            bool ok;
            snprintf(args[0], sizeof(args[0]), "%s", arg_text);
            ok = v3_wasm_emit_call_statement(plan,
                                             lowering,
                                             function,
                                             ctx,
                                             callee,
                                             args,
                                             1U,
                                             imports,
                                             import_count,
                                             body);
            free(args);
            if (ok) {
                return true;
            }
        }
    }
    if (!v3_wasm_emit_expr(plan, lowering, function, ctx, statement, imports, import_count, body)) {
        return false;
    }
    if (allow_implicit_return) {
        if (v3_wasm_abi_is_void(function->return_abi_class)) {
            return true;
        }
        *terminated_out = true;
        if (strcmp(function->return_abi_class, "composite") == 0) {
            int32_t return_slot_offset = 0;
            int32_t return_slot_size = 0;
            int32_t return_slot_addr = 0;
            if (!v3_wasm_compute_static_return_slot_offset(lowering,
                                                           function,
                                                           &return_slot_offset,
                                                           &return_slot_size)) {
                return false;
            }
            return_slot_addr = CHENG_V3_WASM_STATIC_BASE_I32 + return_slot_offset;
            if (!v3_wasm_emit_local_set(body, ctx->scratch2_local_index) ||
                !v3_wasm_emit_i32_const(body, return_slot_addr) ||
                !v3_wasm_emit_local_set(body, ctx->scratch_local_index) ||
                !v3_wasm_emit_copy_region_between_locals(body,
                                                         ctx->scratch_local_index,
                                                         ctx->scratch2_local_index,
                                                         return_slot_size)) {
                return false;
            }
            if (!v3_wasm_emit_restore_frame_base(ctx, body)) {
                return false;
            }
            return v3_wasm_emit_i32_const(body, return_slot_addr) &&
                   v3_wasm_append_u8(body, 0x0fU);
        }
        if (ctx->has_frame_base_local &&
            (!v3_wasm_emit_local_set(body, ctx->scratch_local_index) ||
             !v3_wasm_emit_restore_frame_base(ctx, body) ||
             !v3_wasm_emit_local_get(body, ctx->scratch_local_index))) {
            return false;
        }
        return v3_wasm_append_u8(body, 0x0fU);
    }
    return v3_wasm_append_u8(body, 0x1aU);
}

static bool v3_wasm_encode_function_body_generic(const V3SystemLinkPlanStub *plan,
                                                 const V3LoweringPlanStub *lowering,
                                                 const V3LoweredFunctionStub *function,
                                                 const V3WasmImportStub *imports,
                                                 size_t import_count,
                                                 V3WasmBuffer *code_section) {
    V3WasmFunctionContext ctx;
    V3WasmBuffer body;
    char **lines = NULL;
    char *owned_lines = NULL;
    size_t line_count = 0U;
    size_t fn_line_index = 0U;
    char param_names[CHENG_V3_CONST_MAX_BINDINGS][128];
    size_t param_count = 0U;
    char owned_path[PATH_MAX];
    char inline_signature_body[8192];
    bool has_inline_signature_body = false;
    bool terminated = false;
    if (!v3_wasm_prepare_function_context(plan, lowering, function, &ctx, NULL, NULL) ||
        !v3_load_function_source_lines(function,
                                       &lines,
                                       &owned_lines,
                                       &line_count,
                                       &fn_line_index,
                                       param_names,
                                       &param_count,
                                       owned_path)) {
        return false;
    }
    v3_wasm_buffer_init(&body);
    if (v3_wasm_total_local_count(&ctx) > ctx.param_count) {
        if (!v3_wasm_append_uleb_u32(&body, 1U) ||
            !v3_wasm_append_uleb_u32(&body, (uint32_t)(v3_wasm_total_local_count(&ctx) - ctx.param_count)) ||
            !v3_wasm_append_u8(&body, 0x7fU)) {
            goto fail;
        }
    } else if (!v3_wasm_append_u8(&body, 0x00U)) {
        goto fail;
    }
    if (!v3_wasm_emit_function_prologue(&ctx, &body)) {
        goto fail;
    }
    inline_signature_body[0] = '\0';
    {
        char signature_copy[8192];
        char *eq;
        snprintf(signature_copy, sizeof(signature_copy), "%s", lines[fn_line_index]);
        eq = strrchr(signature_copy, '=');
        if (eq != NULL) {
            v3_trim_copy_text(eq + 1, inline_signature_body, sizeof(inline_signature_body));
            has_inline_signature_body = inline_signature_body[0] != '\0';
        }
    }
    if (has_inline_signature_body) {
        if (!v3_wasm_emit_statement(plan,
                                    lowering,
                                    function,
                                    &ctx,
                                    NULL,
                                    0U,
                                    NULL,
                                    0,
                                    -1,
                                    -1,
                                    inline_signature_body,
                                    imports,
                                    import_count,
                                    true,
                                    &body,
                                    &terminated)) {
            fprintf(stderr,
                    "[cheng_v3_seed] wasm emit inline stmt failed function=%s stmt=%s\n",
                    function->symbol_text,
                    inline_signature_body);
            goto fail;
        }
    } else {
        size_t i;
        for (i = fn_line_index + 1U; i < line_count; ) {
            char raw_copy[4096];
            char *trimmed;
            int32_t indent;
            char statement[8192];
            bool allow_implicit_return = false;
            snprintf(raw_copy, sizeof(raw_copy), "%s", lines[i]);
            trimmed = v3_trim_inplace(raw_copy);
            if (*trimmed == '\0') {
                i += 1U;
                continue;
            }
            indent = v3_line_indent(lines[i]);
            if (indent <= 0 && v3_is_top_level_decl_start(trimmed)) {
                break;
            }
            if (!v3_collect_statement_from_lines(lines, line_count, &i, statement, sizeof(statement))) {
                goto fail;
            }
            if (terminated) {
                continue;
            }
            allow_implicit_return = v3_is_last_logical_statement_in_function(lines, line_count, i);
            if (!v3_wasm_emit_statement(plan,
                                        lowering,
                                        function,
                                        &ctx,
                                        lines,
                                        line_count,
                                        &i,
                                        indent,
                                        -1,
                                        -1,
                                        statement,
                                        imports,
                                        import_count,
                                        allow_implicit_return,
                                        &body,
                                        &terminated)) {
                fprintf(stderr,
                        "[cheng_v3_seed] wasm emit stmt failed function=%s stmt=%s implicit=%d\n",
                        function->symbol_text,
                        statement,
                        allow_implicit_return ? 1 : 0);
                goto fail;
            }
        }
    }
    if (!terminated &&
        (!v3_wasm_abi_is_void(function->return_abi_class) ||
         !v3_wasm_emit_restore_frame_base(&ctx, &body))) {
        fprintf(stderr,
                "[cheng_v3_seed] wasm generic body fell through function=%s\n",
                function->symbol_text);
        goto fail;
    }
    if (!v3_wasm_append_u8(&body, 0x0bU) ||
        !v3_wasm_append_uleb_u32(code_section, (uint32_t)body.len) ||
        !v3_wasm_append_bytes(code_section, body.data, body.len)) {
        goto fail;
    }
    v3_wasm_buffer_free(&body);
    free(lines);
    free(owned_lines);
    return true;
fail:
    v3_wasm_buffer_free(&body);
    free(lines);
    free(owned_lines);
    return false;
}

static int32_t v3_wasm_find_import_index(const V3WasmImportStub *imports,
                                         size_t import_count,
                                         const char *symbol_name,
                                         uint32_t type_index) {
    size_t i;
    for (i = 0; i < import_count; ++i) {
        if (strcmp(imports[i].symbol_name, symbol_name) == 0 &&
            imports[i].type_index == type_index) {
            return (int32_t)i;
        }
    }
    return -1;
}

static void v3_wasm_debug_dump_imports(const V3WasmImportStub *imports,
                                       size_t import_count) {
    size_t i;
    if (imports == NULL) {
        fprintf(stderr, "[cheng_v3_seed] wasm import dump: (null)\n");
        return;
    }
    fprintf(stderr, "[cheng_v3_seed] wasm import dump count=%zu\n", import_count);
    for (i = 0U; i < import_count; ++i) {
        fprintf(stderr,
                "[cheng_v3_seed] wasm import dump[%zu] symbol=%s type_index=%u\n",
                i,
                imports[i].symbol_name,
                imports[i].type_index);
    }
}

static bool v3_wasm_resolve_external_noarg_i32_from_source_path(const V3SystemLinkPlanStub *plan,
                                                                const char *source_path,
                                                                const char *callee,
                                                                char *symbol_name_out,
                                                                size_t symbol_name_cap) {
    char *owned = NULL;
    char *cursor = NULL;
    char *line = NULL;
    char pending_symbol[PATH_MAX];
    if (plan == NULL || source_path == NULL || source_path[0] == '\0' ||
        callee == NULL || callee[0] == '\0' ||
        symbol_name_out == NULL || symbol_name_cap == 0U) {
        return false;
    }
    owned = v3_read_file(source_path);
    if (owned == NULL) {
        return false;
    }
    pending_symbol[0] = '\0';
    cursor = owned;
    while ((line = strsep(&cursor, "\n")) != NULL) {
        char line_copy[4096];
        char signature_copy[4096];
        char function_name[128];
        V3LoweredFunctionStub probe;
        char *trimmed;
        char *signature_trimmed;
        bool matched_decl = false;
        snprintf(line_copy, sizeof(line_copy), "%s", line);
        trimmed = v3_trim_inplace(line_copy);
        if (*trimmed == '\0' || *trimmed == '#') {
            continue;
        }
        if (v3_importc_symbol_from_annotation(trimmed, pending_symbol, sizeof(pending_symbol))) {
            continue;
        }
        if (pending_symbol[0] != '\0' && v3_is_annotation_line(trimmed)) {
            continue;
        }
        function_name[0] = '\0';
        if (v3_importc_function_name_from_line(trimmed, function_name, sizeof(function_name))) {
            matched_decl = true;
        } else if (pending_symbol[0] != '\0' &&
                   v3_lowering_function_name_from_line(trimmed, function_name, sizeof(function_name))) {
            matched_decl = true;
        }
        if (!matched_decl) {
            pending_symbol[0] = '\0';
            continue;
        }
        if (strcmp(function_name, callee) != 0) {
            if (pending_symbol[0] == '\0') {
                continue;
            }
            pending_symbol[0] = '\0';
            continue;
        }
        memset(&probe, 0, sizeof(probe));
        snprintf(signature_copy, sizeof(signature_copy), "%s", line);
        signature_trimmed = v3_trim_inplace(signature_copy);
        if (!v3_parse_importc_signature_meta(signature_trimmed, function_name, &probe)) {
            free(owned);
            return false;
        }
        if (probe.param_count != 0U || strcmp(probe.return_abi_class, "i32") != 0) {
            free(owned);
            return false;
        }
        v3_copy_target_symbol_name(plan,
                                   pending_symbol[0] != '\0' && v3_function_decl_prefix_len(trimmed) != 0U ? pending_symbol : function_name,
                                   symbol_name_out,
                                   symbol_name_cap);
        free(owned);
        return true;
    }
    free(owned);
    return false;
}

static bool v3_wasm_resolve_external_arg0_i32_from_source_path(const V3SystemLinkPlanStub *plan,
                                                               const char *source_path,
                                                               const char *callee,
                                                               char *symbol_name_out,
                                                               size_t symbol_name_cap) {
    char *owned = NULL;
    char *cursor = NULL;
    char *line = NULL;
    char pending_symbol[PATH_MAX];
    if (plan == NULL || source_path == NULL || source_path[0] == '\0' ||
        callee == NULL || callee[0] == '\0' ||
        symbol_name_out == NULL || symbol_name_cap == 0U) {
        return false;
    }
    owned = v3_read_file(source_path);
    if (owned == NULL) {
        return false;
    }
    pending_symbol[0] = '\0';
    cursor = owned;
    while ((line = strsep(&cursor, "\n")) != NULL) {
        char line_copy[4096];
        char signature_copy[4096];
        char function_name[128];
        V3LoweredFunctionStub probe;
        char *trimmed;
        char *signature_trimmed;
        bool matched_decl = false;
        snprintf(line_copy, sizeof(line_copy), "%s", line);
        trimmed = v3_trim_inplace(line_copy);
        if (*trimmed == '\0' || *trimmed == '#') {
            continue;
        }
        if (v3_importc_symbol_from_annotation(trimmed, pending_symbol, sizeof(pending_symbol))) {
            continue;
        }
        if (pending_symbol[0] != '\0' && v3_is_annotation_line(trimmed)) {
            continue;
        }
        function_name[0] = '\0';
        if (v3_importc_function_name_from_line(trimmed, function_name, sizeof(function_name))) {
            matched_decl = true;
        } else if (pending_symbol[0] != '\0' &&
                   v3_lowering_function_name_from_line(trimmed, function_name, sizeof(function_name))) {
            matched_decl = true;
        }
        if (!matched_decl) {
            pending_symbol[0] = '\0';
            continue;
        }
        if (strcmp(function_name, callee) != 0) {
            if (pending_symbol[0] != '\0') {
                pending_symbol[0] = '\0';
            }
            continue;
        }
        memset(&probe, 0, sizeof(probe));
        snprintf(signature_copy, sizeof(signature_copy), "%s", line);
        signature_trimmed = v3_trim_inplace(signature_copy);
        if (!v3_parse_importc_signature_meta(signature_trimmed, function_name, &probe)) {
            free(owned);
            return false;
        }
        if (probe.param_count != 1U ||
            strcmp(probe.return_abi_class, "i32") != 0 ||
            strcmp(probe.param_abi_classes[0], "i32") != 0) {
            free(owned);
            return false;
        }
        v3_copy_target_symbol_name(plan,
                                   pending_symbol[0] != '\0' && v3_function_decl_prefix_len(trimmed) != 0U ? pending_symbol : function_name,
                                   symbol_name_out,
                                   symbol_name_cap);
        free(owned);
        return true;
    }
    free(owned);
    return false;
}

static bool v3_wasm_resolve_external_i32_params_from_source_path(const V3SystemLinkPlanStub *plan,
                                                                 const char *source_path,
                                                                 const char *callee,
                                                                 size_t expected_argc,
                                                                 char *symbol_name_out,
                                                                 size_t symbol_name_cap) {
    char *owned = NULL;
    char *cursor = NULL;
    char *line = NULL;
    char pending_symbol[PATH_MAX];
    if (plan == NULL || source_path == NULL || source_path[0] == '\0' ||
        callee == NULL || callee[0] == '\0' ||
        expected_argc < 2U || expected_argc > CHENG_V3_MAX_CALL_ARGS ||
        symbol_name_out == NULL || symbol_name_cap == 0U) {
        return false;
    }
    owned = v3_read_file(source_path);
    if (owned == NULL) {
        return false;
    }
    pending_symbol[0] = '\0';
    cursor = owned;
    while ((line = strsep(&cursor, "\n")) != NULL) {
        char line_copy[4096];
        char signature_copy[4096];
        char function_name[128];
        V3LoweredFunctionStub probe;
        char *trimmed;
        char *signature_trimmed;
        bool matched_decl = false;
        size_t arg_index;
        snprintf(line_copy, sizeof(line_copy), "%s", line);
        trimmed = v3_trim_inplace(line_copy);
        if (*trimmed == '\0' || *trimmed == '#') {
            continue;
        }
        if (v3_importc_symbol_from_annotation(trimmed, pending_symbol, sizeof(pending_symbol))) {
            continue;
        }
        if (pending_symbol[0] != '\0' && v3_is_annotation_line(trimmed)) {
            continue;
        }
        function_name[0] = '\0';
        if (v3_importc_function_name_from_line(trimmed, function_name, sizeof(function_name))) {
            matched_decl = true;
        } else if (pending_symbol[0] != '\0' &&
                   v3_lowering_function_name_from_line(trimmed, function_name, sizeof(function_name))) {
            matched_decl = true;
        }
        if (!matched_decl) {
            pending_symbol[0] = '\0';
            continue;
        }
        if (strcmp(function_name, callee) != 0) {
            if (pending_symbol[0] != '\0') {
                pending_symbol[0] = '\0';
            }
            continue;
        }
        memset(&probe, 0, sizeof(probe));
        snprintf(signature_copy, sizeof(signature_copy), "%s", line);
        signature_trimmed = v3_trim_inplace(signature_copy);
        if (!v3_parse_importc_signature_meta(signature_trimmed, function_name, &probe)) {
            free(owned);
            return false;
        }
        if (probe.param_count != expected_argc || strcmp(probe.return_abi_class, "i32") != 0) {
            free(owned);
            return false;
        }
        for (arg_index = 0U; arg_index < expected_argc; ++arg_index) {
            if (strcmp(probe.param_abi_classes[arg_index], "i32") != 0) {
                free(owned);
                return false;
            }
        }
        v3_copy_target_symbol_name(plan,
                                   pending_symbol[0] != '\0' && v3_function_decl_prefix_len(trimmed) != 0U ? pending_symbol : function_name,
                                   symbol_name_out,
                                   symbol_name_cap);
        free(owned);
        return true;
    }
    free(owned);
    return false;
}

static bool v3_wasm_collect_imports(const V3SystemLinkPlanStub *plan,
                                    const V3LoweringPlanStub *lowering,
                                    V3WasmImportStub *imports,
                                    size_t *import_count) {
    size_t i;
    *import_count = 0U;
    for (i = 0; i < lowering->function_count; ++i) {
        const V3LoweredFunctionStub *function = &lowering->functions[i];
        if (!v3_wasm_function_return_supported(function->return_abi_class) ||
            function->param_count > CHENG_V3_MAX_CALL_ARGS) {
            fprintf(stderr,
                    "[cheng_v3_seed] wasm target only supports void/i32-slot functions with argc<=%d today: %s abi=%s argc=%zu\n",
                    CHENG_V3_MAX_CALL_ARGS,
                    function->symbol_text,
                    function->return_abi_class,
                    function->param_count);
            return false;
        }
        {
            size_t param_index;
            for (param_index = 0U; param_index < function->param_count; ++param_index) {
                if (!v3_wasm_abi_uses_i32_param_slot(function->param_abi_classes[param_index])) {
                    fprintf(stderr,
                            "[cheng_v3_seed] wasm target only supports i32-slot params today: %s param=%zu abi=%s\n",
                            function->symbol_text,
                            param_index,
                            function->param_abi_classes[param_index]);
                    return false;
                }
            }
        }
        if (strcmp(function->body_kind, "return_zero_i32") == 0) {
            continue;
        }
        if (strcmp(function->body_kind, "return_call_noarg_i32") == 0) {
            V3AsmCallTarget target;
            bool target_ok = v3_resolve_return_call_noarg_i32_target(plan, lowering, function, &target);
            if (!target_ok) {
                char wasm_symbol[PATH_MAX];
                wasm_symbol[0] = '\0';
                if (!v3_wasm_resolve_external_noarg_i32_from_source_path(plan,
                                                                         function->source_path,
                                                                         function->body_call_target,
                                                                         wasm_symbol,
                                                                         sizeof(wasm_symbol))) {
                    fprintf(stderr,
                            "[cheng_v3_seed] wasm return_call target resolve failed function=%s target=%s\n",
                            function->symbol_text,
                            function->body_call_target);
                    return false;
                }
                memset(&target, 0, sizeof(target));
                target.external = true;
                snprintf(target.symbol_name, sizeof(target.symbol_name), "%s", wasm_symbol);
                snprintf(target.return_type, sizeof(target.return_type), "%s", "int32");
                snprintf(target.return_abi_class, sizeof(target.return_abi_class), "%s", "i32");
                target.param_count = 0U;
            }
            if (strcmp(target.return_abi_class, "i32") != 0 || target.param_count != 0U) {
                fprintf(stderr,
                        "[cheng_v3_seed] wasm target only supports noarg i32 call target function=%s target=%s ret=%s argc=%zu\n",
                        function->symbol_text,
                        target.symbol_name,
                        target.return_abi_class,
                        target.param_count);
                return false;
            }
            if (target.external) {
                uint32_t type_index = 0U;
                if (!v3_wasm_signature_type_index(target.param_count,
                                                  target.return_abi_class,
                                                  target.param_abi_classes,
                                                  &type_index)) {
                    fprintf(stderr,
                            "[cheng_v3_seed] wasm unsupported import signature function=%s symbol=%s\n",
                            function->symbol_text,
                            target.symbol_name);
                    return false;
                }
                if (v3_wasm_find_import_index(imports, *import_count, target.symbol_name, type_index) < 0) {
                    if (*import_count >= CHENG_V3_MAX_PLAN_FUNCTIONS) {
                        fprintf(stderr, "[cheng_v3_seed] wasm import table overflow\n");
                        return false;
                    }
                    snprintf(imports[*import_count].symbol_name,
                             sizeof(imports[*import_count].symbol_name),
                             "%s",
                             target.symbol_name);
                    imports[*import_count].type_index = type_index;
                    *import_count += 1U;
                }
            }
            continue;
        }
        if (strcmp(function->body_kind, "return_call_arg0_i32") == 0) {
            V3AsmCallTarget target;
            if (function->param_count != 1U ||
                strcmp(function->param_abi_classes[0], "i32") != 0 ||
                strcmp(function->body_call_arg0_name, function->param_names[0]) != 0) {
                fprintf(stderr,
                        "[cheng_v3_seed] wasm return_call_arg0 requires one i32 param bound to arg0: %s arg=%s param0=%s\n",
                        function->symbol_text,
                        function->body_call_arg0_name,
                        function->param_count > 0U ? function->param_names[0] : "-");
                return false;
            }
            if (!v3_resolve_return_call_arg0_i32_target(plan, lowering, function, &target)) {
                fprintf(stderr,
                        "[cheng_v3_seed] wasm target only supports arg0 i32->i32 call target function=%s target=%s\n",
                        function->symbol_text,
                        function->body_call_target);
                return false;
            }
            if (target.external) {
                uint32_t type_index = 0U;
                if (!v3_wasm_signature_type_index(target.param_count,
                                                  target.return_abi_class,
                                                  target.param_abi_classes,
                                                  &type_index)) {
                    fprintf(stderr,
                            "[cheng_v3_seed] wasm unsupported import signature function=%s symbol=%s\n",
                            function->symbol_text,
                            target.symbol_name);
                    return false;
                }
                if (v3_wasm_find_import_index(imports, *import_count, target.symbol_name, type_index) < 0) {
                    if (*import_count >= CHENG_V3_MAX_PLAN_FUNCTIONS) {
                        fprintf(stderr, "[cheng_v3_seed] wasm import table overflow\n");
                        return false;
                    }
                    snprintf(imports[*import_count].symbol_name,
                             sizeof(imports[*import_count].symbol_name),
                             "%s",
                             target.symbol_name);
                    imports[*import_count].type_index = type_index;
                    *import_count += 1U;
                }
            }
            continue;
        }
        if (strcmp(function->body_kind, "return_call_params_i32") == 0) {
            V3AsmCallTarget target;
            if (!v3_resolve_return_call_params_i32_target(plan, lowering, function, &target)) {
                fprintf(stderr,
                        "[cheng_v3_seed] wasm target only supports params i32->i32 call target function=%s target=%s argc=%zu\n",
                        function->symbol_text,
                        function->body_call_target,
                        function->body_call_arg_count);
                return false;
            }
            if (target.external) {
                uint32_t type_index = 0U;
                if (!v3_wasm_signature_type_index(target.param_count,
                                                  target.return_abi_class,
                                                  target.param_abi_classes,
                                                  &type_index)) {
                    fprintf(stderr,
                            "[cheng_v3_seed] wasm unsupported import signature function=%s symbol=%s\n",
                            function->symbol_text,
                            target.symbol_name);
                    return false;
                }
                if (v3_wasm_find_import_index(imports, *import_count, target.symbol_name, type_index) < 0) {
                    if (*import_count >= CHENG_V3_MAX_PLAN_FUNCTIONS) {
                        fprintf(stderr, "[cheng_v3_seed] wasm import table overflow\n");
                        return false;
                    }
                    snprintf(imports[*import_count].symbol_name,
                             sizeof(imports[*import_count].symbol_name),
                             "%s",
                             target.symbol_name);
                    imports[*import_count].type_index = type_index;
                    *import_count += 1U;
                }
            }
            continue;
        }
        {
            V3WasmFunctionContext ctx;
            if (!v3_wasm_prepare_function_context(plan,
                                                  lowering,
                                                  function,
                                                  &ctx,
                                                  imports,
                                                  import_count)) {
                fprintf(stderr,
                        "[cheng_v3_seed] wasm generic prepare failed function=%s body=%s first_line=%s\n",
                        function->symbol_text,
                        function->body_kind,
                        function->first_body_line);
                return false;
            }
        }
    }
    return true;
}

static bool v3_wasm_encode_function_body(const V3SystemLinkPlanStub *plan,
                                         const V3LoweringPlanStub *lowering,
                                         const V3LoweredFunctionStub *function,
                                         const V3WasmImportStub *imports,
                                         size_t import_count,
                                         V3WasmBuffer *code_section) {
    V3WasmBuffer body;
    uint32_t callee_index = 0U;
    if (strcmp(function->body_kind, "return_zero_i32") != 0 &&
        strcmp(function->body_kind, "return_call_noarg_i32") != 0 &&
        strcmp(function->body_kind, "return_call_arg0_i32") != 0 &&
        strcmp(function->body_kind, "return_call_params_i32") != 0) {
        return v3_wasm_encode_function_body_generic(plan,
                                                    lowering,
                                                    function,
                                                    imports,
                                                    import_count,
                                                    code_section);
    }
    v3_wasm_buffer_init(&body);
    if (!v3_wasm_append_u8(&body, 0x00U)) {
        v3_wasm_buffer_free(&body);
        return false;
    }
    if (strcmp(function->body_kind, "return_zero_i32") == 0) {
        if (!v3_wasm_append_u8(&body, 0x41U) ||
            !v3_wasm_append_uleb_u32(&body, 0U)) {
            v3_wasm_buffer_free(&body);
            return false;
        }
    } else if (strcmp(function->body_kind, "return_call_noarg_i32") == 0) {
        V3AsmCallTarget target;
        bool target_ok = v3_resolve_return_call_noarg_i32_target(plan, lowering, function, &target);
        if (!target_ok) {
            char wasm_symbol[PATH_MAX];
            wasm_symbol[0] = '\0';
            if (!v3_wasm_resolve_external_noarg_i32_from_source_path(plan,
                                                                     function->source_path,
                                                                     function->body_call_target,
                                                                     wasm_symbol,
                                                                     sizeof(wasm_symbol))) {
                fprintf(stderr,
                        "[cheng_v3_seed] wasm return_call encode resolve failed function=%s target=%s\n",
                        function->symbol_text,
                        function->body_call_target);
                v3_wasm_buffer_free(&body);
                return false;
            }
            memset(&target, 0, sizeof(target));
            target.external = true;
            snprintf(target.symbol_name, sizeof(target.symbol_name), "%s", wasm_symbol);
            snprintf(target.return_type, sizeof(target.return_type), "%s", "int32");
            snprintf(target.return_abi_class, sizeof(target.return_abi_class), "%s", "i32");
            target.param_count = 0U;
        }
        if (target.external) {
            uint32_t type_index = 0U;
            int32_t import_index;
            if (!v3_wasm_signature_type_index(target.param_count,
                                              target.return_abi_class,
                                              target.param_abi_classes,
                                              &type_index)) {
                fprintf(stderr,
                        "[cheng_v3_seed] wasm import signature resolve failed symbol=%s function=%s\n",
                        target.symbol_name,
                        function->symbol_text);
                v3_wasm_buffer_free(&body);
                return false;
            }
            import_index = v3_wasm_find_import_index(imports, import_count, target.symbol_name, type_index);
            if (import_index < 0) {
                fprintf(stderr,
                        "[cheng_v3_seed] wasm import index missing symbol=%s function=%s\n",
                        target.symbol_name,
                        function->symbol_text);
                v3_wasm_buffer_free(&body);
                return false;
            }
            callee_index = (uint32_t)import_index;
        } else {
            int32_t local_index = v3_find_lowered_function_index_by_symbol(lowering, target.lowered_function->symbol_text);
            if (local_index < 0) {
                fprintf(stderr,
                        "[cheng_v3_seed] wasm local callee missing symbol=%s function=%s\n",
                        target.lowered_function != NULL ? target.lowered_function->symbol_text : "<nil>",
                        function->symbol_text);
                v3_wasm_buffer_free(&body);
                return false;
            }
            callee_index = (uint32_t)import_count + (uint32_t)local_index;
        }
        if (!v3_wasm_append_u8(&body, 0x10U) ||
            !v3_wasm_append_uleb_u32(&body, callee_index)) {
            v3_wasm_buffer_free(&body);
            return false;
        }
    } else if (strcmp(function->body_kind, "return_call_arg0_i32") == 0) {
        V3AsmCallTarget target;
        if (function->param_count != 1U ||
            strcmp(function->param_abi_classes[0], "i32") != 0 ||
            strcmp(function->body_call_arg0_name, function->param_names[0]) != 0) {
            fprintf(stderr,
                    "[cheng_v3_seed] wasm encode arg0 call requires one i32 param function=%s arg=%s param0=%s\n",
                    function->symbol_text,
                    function->body_call_arg0_name,
                    function->param_count > 0U ? function->param_names[0] : "-");
            v3_wasm_buffer_free(&body);
            return false;
        }
        if (!v3_resolve_return_call_arg0_i32_target(plan, lowering, function, &target)) {
            fprintf(stderr,
                    "[cheng_v3_seed] wasm encode arg0 target resolve failed function=%s target=%s\n",
                    function->symbol_text,
                    function->body_call_target);
            v3_wasm_buffer_free(&body);
            return false;
        }
        if (!v3_wasm_append_u8(&body, 0x20U) ||
            !v3_wasm_append_uleb_u32(&body, 0U)) {
            v3_wasm_buffer_free(&body);
            return false;
        }
        if (target.external) {
            uint32_t type_index = 0U;
            int32_t import_index;
            if (!v3_wasm_signature_type_index(target.param_count,
                                              target.return_abi_class,
                                              target.param_abi_classes,
                                              &type_index)) {
                fprintf(stderr,
                        "[cheng_v3_seed] wasm import signature resolve failed symbol=%s function=%s\n",
                        target.symbol_name,
                        function->symbol_text);
                v3_wasm_buffer_free(&body);
                return false;
            }
            import_index = v3_wasm_find_import_index(imports, import_count, target.symbol_name, type_index);
            if (import_index < 0) {
                fprintf(stderr,
                        "[cheng_v3_seed] wasm import index missing symbol=%s function=%s\n",
                        target.symbol_name,
                        function->symbol_text);
                v3_wasm_buffer_free(&body);
                return false;
            }
            callee_index = (uint32_t)import_index;
        } else {
            int32_t local_index = v3_find_lowered_function_index_by_symbol(lowering, target.lowered_function->symbol_text);
            if (local_index < 0) {
                fprintf(stderr,
                        "[cheng_v3_seed] wasm local callee missing symbol=%s function=%s\n",
                        target.lowered_function != NULL ? target.lowered_function->symbol_text : "<nil>",
                        function->symbol_text);
                v3_wasm_buffer_free(&body);
                return false;
            }
            callee_index = (uint32_t)import_count + (uint32_t)local_index;
        }
        if (!v3_wasm_append_u8(&body, 0x10U) ||
            !v3_wasm_append_uleb_u32(&body, callee_index)) {
            v3_wasm_buffer_free(&body);
            return false;
        }
    } else if (strcmp(function->body_kind, "return_call_params_i32") == 0) {
        V3AsmCallTarget target;
        size_t arg_index;
        if (!v3_resolve_return_call_params_i32_target(plan, lowering, function, &target)) {
            fprintf(stderr,
                    "[cheng_v3_seed] wasm encode params target resolve failed function=%s target=%s argc=%zu\n",
                    function->symbol_text,
                    function->body_call_target,
                    function->body_call_arg_count);
            v3_wasm_buffer_free(&body);
            return false;
        }
        for (arg_index = 0U; arg_index < function->body_call_arg_count; ++arg_index) {
            if (!v3_wasm_append_u8(&body, 0x20U) ||
                !v3_wasm_append_uleb_u32(&body, (uint32_t)arg_index)) {
                v3_wasm_buffer_free(&body);
                return false;
            }
        }
        if (target.external) {
            uint32_t type_index = 0U;
            int32_t import_index;
            if (!v3_wasm_signature_type_index(target.param_count,
                                              target.return_abi_class,
                                              target.param_abi_classes,
                                              &type_index)) {
                v3_wasm_buffer_free(&body);
                return false;
            }
            import_index = v3_wasm_find_import_index(imports, import_count, target.symbol_name, type_index);
            if (import_index < 0) {
                fprintf(stderr,
                        "[cheng_v3_seed] wasm import index missing symbol=%s function=%s\n",
                        target.symbol_name,
                        function->symbol_text);
                v3_wasm_buffer_free(&body);
                return false;
            }
            callee_index = (uint32_t)import_index;
        } else {
            int32_t local_index = v3_find_lowered_function_index_by_symbol(lowering, target.lowered_function->symbol_text);
            if (local_index < 0) {
                fprintf(stderr,
                        "[cheng_v3_seed] wasm local callee missing symbol=%s function=%s\n",
                        target.lowered_function != NULL ? target.lowered_function->symbol_text : "<nil>",
                        function->symbol_text);
                v3_wasm_buffer_free(&body);
                return false;
            }
            callee_index = (uint32_t)import_count + (uint32_t)local_index;
        }
        if (!v3_wasm_append_u8(&body, 0x10U) ||
            !v3_wasm_append_uleb_u32(&body, callee_index)) {
            v3_wasm_buffer_free(&body);
            return false;
        }
    } else {
        fprintf(stderr,
                "[cheng_v3_seed] wasm encode unsupported body kind function=%s body=%s\n",
                function->symbol_text,
                function->body_kind);
        v3_wasm_buffer_free(&body);
        return false;
    }
    if (!v3_wasm_append_u8(&body, 0x0bU) ||
        !v3_wasm_append_uleb_u32(code_section, (uint32_t)body.len) ||
        !v3_wasm_append_bytes(code_section, body.data, body.len)) {
        v3_wasm_buffer_free(&body);
        return false;
    }
    v3_wasm_buffer_free(&body);
    return true;
}

static bool v3_materialize_wasm_module(const V3SystemLinkPlanStub *plan,
                                       const V3LoweringPlanStub *lowering,
                                       const V3PrimaryObjectPlanStub *primary) {
    static const unsigned char wasm_magic[8] = {0x00U, 0x61U, 0x73U, 0x6dU, 0x01U, 0x00U, 0x00U, 0x00U};
    V3WasmImportStub *imports = NULL;
    size_t import_count = 0U;
    size_t i;
    int32_t entry_index = -1;
    V3WasmBuffer module;
    V3WasmBuffer payload;
    v3_wasm_buffer_init(&module);
    v3_wasm_buffer_init(&payload);
    imports = (V3WasmImportStub *)calloc(CHENG_V3_MAX_PLAN_FUNCTIONS, sizeof(V3WasmImportStub));
    if (imports == NULL) {
        fprintf(stderr, "[cheng_v3_seed] wasm imports alloc failed\n");
        return false;
    }
    if (primary->missing_reason_count > 0U) {
        free(imports);
        return false;
    }
    if (!v3_wasm_collect_imports(plan, lowering, imports, &import_count)) {
        goto fail;
    }
    v3_wasm_string_literal_pool_reset(v3_align_up_i32(CHENG_V3_WASM_STATIC_BASE_I32 +
                                                      v3_wasm_total_static_return_bytes(lowering),
                                                      16));
    for (i = 0; i < lowering->function_count; ++i) {
        if (lowering->functions[i].is_entry) {
            entry_index = (int32_t)i;
            break;
        }
    }
    if (v3_streq(plan->emit_kind, "exe") && entry_index < 0) {
        fprintf(stderr, "[cheng_v3_seed] wasm target missing entry function\n");
        return false;
    }
    if (!v3_wasm_append_bytes(&module, wasm_magic, sizeof(wasm_magic))) {
        goto fail;
    }
    {
        uint32_t max_param_count = v3_wasm_max_signature_param_count(lowering, imports, import_count);
        uint32_t param_count = 0U;
        if (!v3_wasm_append_uleb_u32(&payload, (max_param_count + 1U) * 2U)) {
            goto fail;
        }
        for (param_count = 0U; param_count <= max_param_count; ++param_count) {
            uint32_t param_index = 0U;
            size_t pass = 0U;
            for (pass = 0U; pass < 2U; ++pass) {
                bool has_result = pass == 1U;
                if (!v3_wasm_append_u8(&payload, 0x60U) ||
                    !v3_wasm_append_uleb_u32(&payload, param_count)) {
                    goto fail;
                }
                for (param_index = 0U; param_index < param_count; ++param_index) {
                    if (!v3_wasm_append_u8(&payload, 0x7fU)) {
                        goto fail;
                    }
                }
                if (!v3_wasm_append_uleb_u32(&payload, has_result ? 1U : 0U)) {
                    goto fail;
                }
                if (has_result && !v3_wasm_append_u8(&payload, 0x7fU)) {
                    goto fail;
                }
            }
        }
        if (!v3_wasm_append_section(&module, 1U, &payload)) {
            goto fail;
        }
    }
    payload.len = 0U;
    {
        uint32_t total_import_count = (uint32_t)import_count + 1U;
        if (!v3_wasm_append_uleb_u32(&payload, total_import_count) ||
            !v3_wasm_append_name(&payload, "env") ||
            !v3_wasm_append_name(&payload, "memory") ||
            !v3_wasm_append_u8(&payload, 0x02U) ||
            !v3_wasm_append_u8(&payload, 0x01U) ||
            !v3_wasm_append_uleb_u32(&payload, 32U) ||
            !v3_wasm_append_uleb_u32(&payload, 512U)) {
            goto fail;
        }
        for (i = 0; i < import_count; ++i) {
            if (!v3_wasm_append_name(&payload, "env") ||
                !v3_wasm_append_name(&payload, imports[i].symbol_name) ||
                !v3_wasm_append_u8(&payload, 0x00U) ||
                !v3_wasm_append_uleb_u32(&payload, imports[i].type_index)) {
                goto fail;
            }
        }
        if (!v3_wasm_append_section(&module, 2U, &payload)) {
            goto fail;
        }
    }
    payload.len = 0U;
    if (!v3_wasm_append_uleb_u32(&payload, (uint32_t)lowering->function_count)) {
        goto fail;
    }
    for (i = 0; i < lowering->function_count; ++i) {
        uint32_t type_index = 0U;
        if (!v3_wasm_signature_type_index(lowering->functions[i].param_count,
                                          lowering->functions[i].return_abi_class,
                                          lowering->functions[i].param_abi_classes,
                                          &type_index) ||
            !v3_wasm_append_uleb_u32(&payload, type_index)) {
            goto fail;
        }
    }
    if (!v3_wasm_append_section(&module, 3U, &payload)) {
        goto fail;
    }
    payload.len = 0U;
    {
        int32_t stack_init = CHENG_V3_WASM_STACK_INIT_I32;
        if (!v3_wasm_append_uleb_u32(&payload, 1U) ||
            !v3_wasm_append_u8(&payload, 0x7fU) ||
            !v3_wasm_append_u8(&payload, 0x01U) ||
            !v3_wasm_append_u8(&payload, 0x41U) ||
            !v3_wasm_append_sleb_i32(&payload, stack_init) ||
            !v3_wasm_append_u8(&payload, 0x0bU)) {
            goto fail;
        }
        if (!v3_wasm_append_section(&module, 6U, &payload)) {
            goto fail;
        }
    }
    payload.len = 0U;
    {
        uint32_t exported_count = 0U;
        for (i = 0; i < lowering->function_count; ++i) {
            if (v3_wasm_function_should_export(plan, lowering, &lowering->functions[i])) {
                exported_count += 1U;
            }
        }
        if (!v3_wasm_append_uleb_u32(&payload, exported_count)) {
            goto fail;
        }
    }
    for (i = 0; i < lowering->function_count; ++i) {
        if (!v3_wasm_function_should_export(plan, lowering, &lowering->functions[i])) {
            continue;
        }
        if (!v3_wasm_append_name(&payload, lowering->functions[i].function_name) ||
            !v3_wasm_append_u8(&payload, 0x00U) ||
            !v3_wasm_append_uleb_u32(&payload, (uint32_t)import_count + (uint32_t)i)) {
            goto fail;
        }
    }
    if (!v3_wasm_append_section(&module, 7U, &payload)) {
        goto fail;
    }
    payload.len = 0U;
    if (!v3_wasm_append_uleb_u32(&payload, (uint32_t)lowering->function_count)) {
        goto fail;
    }
    for (i = 0; i < lowering->function_count; ++i) {
        if (!v3_wasm_encode_function_body(plan,
                                          lowering,
                                          &lowering->functions[i],
                                          imports,
                                          import_count,
                                          &payload)) {
            fprintf(stderr,
                    "[cheng_v3_seed] wasm encode body failed function=%s body=%s first_line=%s\n",
                    lowering->functions[i].symbol_text,
                    lowering->functions[i].body_kind,
                    lowering->functions[i].first_body_line);
            goto fail;
        }
    }
    if (!v3_wasm_append_section(&module, 10U, &payload)) {
        goto fail;
    }
    if (!v3_wasm_append_string_literal_data_section(&module)) {
        goto fail;
    }
    if (!v3_ensure_parent_dir(primary->output_path) ||
        !v3_write_binary_file(primary->output_path, module.data, module.len)) {
        goto fail;
    }
    v3_wasm_buffer_free(&module);
    v3_wasm_buffer_free(&payload);
    v3_wasm_string_literal_pool_dispose();
    free(imports);
    return true;
fail:
    v3_wasm_buffer_free(&module);
    v3_wasm_buffer_free(&payload);
    v3_wasm_string_literal_pool_dispose();
    free(imports);
    return false;
}

static bool v3_compile_c_object(const char *target_triple,
                                const char *emit_kind,
                                const char *source_path,
                                const char *out_path) {
    char cc[PATH_MAX];
    char command[PATH_MAX * 8];
    char target_clause[PATH_MAX];
    char *quoted_cc;
    char *quoted_source;
    char *quoted_out;
    if (!v3_resolve_target_cc_program(target_triple, cc, sizeof(cc))) {
        fprintf(stderr, "[cheng_v3_seed] target compiler missing for %s\n", target_triple);
        return false;
    }
    if (!v3_ensure_parent_dir(out_path)) {
        return false;
    }
    target_clause[0] = '\0';
    if ((v3_target_is_linux_x86_64(target_triple) ||
         v3_target_is_linux_generic_aarch64(target_triple)) &&
        !v3_target_matches_native_host(target_triple)) {
        snprintf(target_clause, sizeof(target_clause), " -target %s", target_triple);
    }
    quoted_cc = v3_shell_quote(cc);
    quoted_source = v3_shell_quote(source_path);
    quoted_out = v3_shell_quote(out_path);
    snprintf(command, sizeof(command),
             v3_target_is_elf_aarch64(target_triple) ?
                 ((v3_streq(emit_kind, "shared") || v3_streq(emit_kind, "obj")) ?
                     "%s%s -std=c11 -O2 -Wall -Wextra -pedantic -fPIC -c %s -o %s" :
                         "%s%s -std=c11 -O2 -Wall -Wextra -pedantic -fPIE -c %s -o %s") :
                 "%s%s -std=c11 -O2 -Wall -Wextra -pedantic -c %s -o %s",
             quoted_cc,
             target_clause,
             quoted_source,
             quoted_out);
    free(quoted_cc);
    free(quoted_source);
    free(quoted_out);
    if (!v3_run_shell_command_status(command, "provider-object-compile")) {
        fprintf(stderr, "[cheng_v3_seed] provider object compile failed command=%s\n", command);
        return false;
    }
    return true;
}

static bool v3_compile_asm_object(const char *target_triple,
                                  const char *emit_kind,
                                  const char *source_path,
                                  const char *out_path) {
    char cc[PATH_MAX];
    char command[PATH_MAX * 8];
    char target_clause[PATH_MAX];
    char *quoted_cc;
    char *quoted_source;
    char *quoted_out;
    if (!v3_resolve_target_cc_program(target_triple, cc, sizeof(cc))) {
        fprintf(stderr, "[cheng_v3_seed] target assembler missing for %s\n", target_triple);
        return false;
    }
    if (!v3_ensure_parent_dir(out_path)) {
        return false;
    }
    target_clause[0] = '\0';
    if ((v3_target_is_linux_x86_64(target_triple) ||
         v3_target_is_linux_generic_aarch64(target_triple)) &&
        !v3_target_matches_native_host(target_triple)) {
        snprintf(target_clause, sizeof(target_clause), " -target %s", target_triple);
    }
    quoted_cc = v3_shell_quote(cc);
    quoted_source = v3_shell_quote(source_path);
    quoted_out = v3_shell_quote(out_path);
    snprintf(command, sizeof(command),
             v3_target_is_elf_aarch64(target_triple) ?
                 ((v3_streq(emit_kind, "shared") || v3_streq(emit_kind, "obj")) ?
                     "%s%s -fPIC -c %s -o %s" :
                         "%s%s -fPIE -c %s -o %s") :
             v3_target_is_linux_x86_64(target_triple) ?
                 "%s%s -c %s -o %s" :
                 "%s%s -arch arm64 -c %s -o %s",
             quoted_cc,
             target_clause,
             quoted_source,
             quoted_out);
    free(quoted_cc);
    free(quoted_source);
    free(quoted_out);
    if (!v3_run_shell_command_status(command, "primary-object-compile")) {
        fprintf(stderr, "[cheng_v3_seed] primary object compile failed command=%s\n", command);
        return false;
    }
    return true;
}

static bool v3_emit_simple_return_zero_i32_chunk(const V3SystemLinkPlanStub *plan,
                                                 const char *symbol_name,
                                                 const char *end_label,
                                                 char *out,
                                                 size_t cap) {
    if (v3_target_is_linux_x86_64(plan->target_triple)) {
        snprintf(out, cap,
                 ".globl %s\n"
                 "%s:\n"
                 "  xorl %%eax, %%eax\n"
                 "  retq\n"
                 "%s:\n",
                 symbol_name,
                 symbol_name,
                 end_label);
        return true;
    }
    snprintf(out, cap,
             ".globl %s\n"
             "%s:\n"
             "  mov w0, #0\n"
             "  ret\n"
             "%s:\n",
             symbol_name,
             symbol_name,
             end_label);
    return true;
}

static bool v3_emit_simple_return_call_chunk(const V3SystemLinkPlanStub *plan,
                                             const char *symbol_name,
                                             const char *target_symbol,
                                             const char *end_label,
                                             char *out,
                                             size_t cap) {
    if (v3_target_is_linux_x86_64(plan->target_triple)) {
        snprintf(out, cap,
                 ".globl %s\n"
                 "%s:\n"
                 "  jmp %s\n"
                 "%s:\n",
                 symbol_name,
                 symbol_name,
                 target_symbol,
                 end_label);
        return true;
    }
    snprintf(out, cap,
             ".globl %s\n"
             "%s:\n"
             "  b %s\n"
             "%s:\n",
             symbol_name,
             symbol_name,
             target_symbol,
             end_label);
    return true;
}

static bool v3_materialize_primary_object_internal(const V3SystemLinkPlanStub *plan,
                                                   const V3LoweringPlanStub *lowering,
                                                   const V3PrimaryObjectPlanStub *primary,
                                                   bool compile_object) {
    size_t i;
    char asm_path[PATH_MAX];
    char module_init_symbols[CHENG_V3_MAX_PLAN_PATHS][PATH_MAX];
    size_t module_init_count = 0U;
    size_t cap = 65536U + lowering->function_count * 32768U + plan->source_closure_count * 16384U;
    char *text;
    if (primary->missing_reason_count > 0U) {
        return false;
    }
    v3_active_codegen_target_triple = plan != NULL ? plan->target_triple : NULL;
    if (v3_target_is_wasm32_unknown_unknown(plan->target_triple)) {
        return v3_materialize_wasm_module(plan, lowering, primary);
    }
    if (primary->consteval_entry_ready &&
        v3_target_uses_consteval_builtin_executable(plan->target_triple) &&
        v3_streq(plan->emit_kind, "exe")) {
        if (!compile_object) {
            fprintf(stderr,
                    "[cheng_v3_seed] print-asm unsupported for builtin consteval target: %s\n",
                    plan->target_triple);
            return false;
        }
        return true;
    }
    snprintf(asm_path, sizeof(asm_path), "%s.s", primary->output_path);
    if (primary->consteval_entry_ready) {
        const V3LoweredFunctionStub *entry_function = v3_find_entry_lowered_function(lowering);
        char entry_symbol[PATH_MAX];
        char bridge_symbol[PATH_MAX];
        if (!entry_function) {
            return false;
        }
        cap = 4096U + primary->consteval_echo_count * (PATH_MAX * 2U);
        text = (char *)v3_xmalloc(cap);
        text[0] = '\0';
        for (i = 0; i < plan->source_closure_count; ++i) {
            if (!v3_append_module_global_storage(plan,
                                                 lowering,
                                                 plan->source_closure_paths[i].text,
                                                 text,
                                                 cap)) {
                free(text);
                return false;
            }
        }
        v3_append_cstring_section_for_target(plan, text, cap);
        for (i = 0; i < primary->consteval_echo_count; ++i) {
            char label[64];
            char *escaped = v3_c_escape(primary->consteval_echo_texts[i].text);
            char chunk[PATH_MAX * 2];
            snprintf(label, sizeof(label), "L_v3_const_echo_%zu", i);
            snprintf(chunk, sizeof(chunk),
                     "%s:\n"
                     "  .asciz \"%s\"\n",
                     label,
                     escaped);
            free(escaped);
            strncat(text, chunk, cap - strlen(text) - 1U);
        }
        strncat(text, ".text\n.p2align 2\n", cap - strlen(text) - 1U);
        for (i = 0; i < plan->source_closure_count; ++i) {
            bool emitted_init = false;
            if (!v3_append_module_global_init_function(plan,
                                                       lowering,
                                                       plan->source_closure_paths[i].text,
                                                       module_init_symbols[module_init_count],
                                                       sizeof(module_init_symbols[module_init_count]),
                                                       text,
                                                       cap,
                                                       &emitted_init)) {
                free(text);
                return false;
            }
            if (emitted_init) {
                module_init_count += 1U;
            }
        }
        v3_primary_symbol_name(plan, entry_function, entry_symbol, sizeof(entry_symbol));
        snprintf(text + strlen(text), cap - strlen(text),
                 ".globl %s\n"
                 "%s:\n",
                 entry_symbol,
                 entry_symbol);
        if (v3_target_is_linux_x86_64(plan->target_triple)) {
            strncat(text,
                    "  subq $8, %rsp\n",
                    cap - strlen(text) - 1U);
        } else {
            strncat(text,
                    "  sub sp, sp, #16\n"
                    "  str x30, [sp, #8]\n",
                    cap - strlen(text) - 1U);
        }
        for (i = 0; i < primary->consteval_echo_count; ++i) {
            char label[64];
            snprintf(label, sizeof(label), "L_v3_const_echo_%zu", i);
            if (!v3_emit_symbol_address_for_target(plan, text, cap, 0, label)) {
                free(text);
                return false;
            }
            v3_emit_call_named_symbol(plan, text, cap, "puts");
        }
        {
            char chunk[256];
            char end_label[64];
            v3_embedded_line_map_end_label(0U, end_label, sizeof(end_label));
            if (v3_target_is_linux_x86_64(plan->target_triple)) {
                snprintf(chunk, sizeof(chunk),
                         "  addq $8, %%rsp\n"
                         "  movl $%d, %%eax\n"
                         "  retq\n"
                         "%s:\n",
                         primary->consteval_return_code,
                         end_label);
            } else {
                snprintf(chunk, sizeof(chunk),
                         "  ldr x30, [sp, #8]\n"
                         "  add sp, sp, #16\n"
                         "  mov w0, #%d\n"
                         "  ret\n"
                         "%s:\n",
                         primary->consteval_return_code,
                         end_label);
            }
            strncat(text, chunk, cap - strlen(text) - 1U);
        }
        if (v3_streq(plan->emit_kind, "exe")) {
            char end_label[64];
            v3_runtime_entry_bridge_symbol(plan, bridge_symbol, sizeof(bridge_symbol));
            v3_embedded_line_map_end_label(1U, end_label, sizeof(end_label));
            v3_append_entry_bridge_with_module_inits(plan,
                                                     text,
                                                     cap,
                                                     bridge_symbol,
                                                     entry_symbol,
                                                     v3_streq(entry_function->return_type, "void"),
                                                     module_init_symbols,
                                                     module_init_count,
                                                     end_label);
        }
        if (!v3_append_embedded_line_map(text, cap, plan, lowering, entry_function)) {
            free(text);
            return false;
        }
        v3_append_symbol_trailer_for_target(plan, text, cap);
        if (!v3_ensure_parent_dir(asm_path) || !v3_write_text_file(asm_path, text)) {
            free(text);
            return false;
        }
        free(text);
        if (!compile_object) {
            return true;
        }
        return v3_compile_asm_object(plan->target_triple, plan->emit_kind, asm_path, primary->output_path);
    }
    text = (char *)v3_xmalloc(cap);
    text[0] = '\0';
    for (i = 0; i < plan->source_closure_count; ++i) {
        if (!v3_append_module_global_storage(plan,
                                             lowering,
                                             plan->source_closure_paths[i].text,
                                             text,
                                             cap)) {
            free(text);
            return false;
        }
    }
    strncat(text, ".text\n", cap - strlen(text) - 1U);
    strncat(text, ".p2align 2\n", cap - strlen(text) - 1U);
    for (i = 0; i < plan->source_closure_count; ++i) {
        bool emitted_init = false;
        if (!v3_append_module_global_init_function(plan,
                                                   lowering,
                                                   plan->source_closure_paths[i].text,
                                                   module_init_symbols[module_init_count],
                                                   sizeof(module_init_symbols[module_init_count]),
                                                   text,
                                                   cap,
                                                   &emitted_init)) {
            free(text);
            return false;
        }
        if (emitted_init) {
            module_init_count += 1U;
        }
    }
    for (i = 0; i < lowering->function_count; ++i) {
        if (v3_function_has_unbound_generics(lowering, &lowering->functions[i]) ||
            v3_function_has_obvious_template_text(&lowering->functions[i])) {
            continue;
        }
        char symbol_name[PATH_MAX];
        char end_label[64];
        char chunk[PATH_MAX + 128];
        v3_embedded_line_map_end_label(i, end_label, sizeof(end_label));
        if (strcmp(lowering->functions[i].body_kind, "return_zero_i32") != 0 &&
            strcmp(lowering->functions[i].body_kind, "return_call_noarg_i32") != 0 &&
            strcmp(lowering->functions[i].body_kind, "return_call_arg0_i32") != 0) {
            char *scalar_chunk = (char *)v3_xmalloc(1024U * 1024U);
            scalar_chunk[0] = '\0';
            if (!v3_try_emit_scalar_function(plan,
                                             lowering,
                                             &lowering->functions[i],
                                             scalar_chunk,
                                             1024U * 1024U,
                                             NULL)) {
                free(scalar_chunk);
                free(text);
                return false;
            }
            v3_text_append(text, cap, scalar_chunk);
            v3_text_appendf(text, cap, "%s:\n", end_label);
            free(scalar_chunk);
            continue;
        }
        v3_primary_symbol_name(plan,
                               &lowering->functions[i],
                               symbol_name,
                               sizeof(symbol_name));
        if (strcmp(lowering->functions[i].body_kind, "return_zero_i32") == 0) {
            if (!v3_emit_simple_return_zero_i32_chunk(plan,
                                                      symbol_name,
                                                      end_label,
                                                      chunk,
                                                      sizeof(chunk))) {
                free(text);
                return false;
            }
        } else if (strcmp(lowering->functions[i].body_kind, "return_call_noarg_i32") == 0) {
            V3AsmCallTarget target;
            if (!v3_resolve_return_call_noarg_i32_target(plan,
                                                         lowering,
                                                         &lowering->functions[i],
                                                         &target)) {
                fprintf(stderr,
                        "[cheng_v3_seed] emit unresolved return_call_noarg target function=%s target=%s return_type=%s source=%s\n",
                        lowering->functions[i].symbol_text,
                        lowering->functions[i].body_call_target,
                        lowering->functions[i].return_type,
                        lowering->functions[i].source_path);
                free(text);
                return false;
            }
            if (!v3_emit_simple_return_call_chunk(plan,
                                                  symbol_name,
                                                  target.symbol_name,
                                                  end_label,
                                                  chunk,
                                                  sizeof(chunk))) {
                free(text);
                return false;
            }
        } else {
            V3AsmCallTarget target;
            if (!v3_resolve_return_call_arg0_i32_target(plan,
                                                        lowering,
                                                        &lowering->functions[i],
                                                        &target)) {
                fprintf(stderr,
                        "[cheng_v3_seed] emit unresolved return_call_arg0 target function=%s target=%s return_type=%s source=%s\n",
                        lowering->functions[i].symbol_text,
                        lowering->functions[i].body_call_target,
                        lowering->functions[i].return_type,
                        lowering->functions[i].source_path);
                free(text);
                return false;
            }
            if (!v3_emit_simple_return_call_chunk(plan,
                                                  symbol_name,
                                                  target.symbol_name,
                                                  end_label,
                                                  chunk,
                                                  sizeof(chunk))) {
                free(text);
                return false;
            }
        }
        strncat(text, chunk, cap - strlen(text) - 1U);
    }
    if (v3_streq(plan->emit_kind, "exe")) {
        const V3LoweredFunctionStub *entry_function = v3_find_entry_lowered_function(lowering);
        if (entry_function) {
            char bridge_symbol[PATH_MAX];
            char entry_symbol[PATH_MAX];
            char end_label[64];
            v3_runtime_entry_bridge_symbol(plan, bridge_symbol, sizeof(bridge_symbol));
            v3_primary_symbol_name(plan, entry_function, entry_symbol, sizeof(entry_symbol));
            v3_embedded_line_map_end_label(lowering->function_count, end_label, sizeof(end_label));
            v3_append_entry_bridge_with_module_inits(plan,
                                                     text,
                                                     cap,
                                                     bridge_symbol,
                                                     entry_symbol,
                                                     v3_streq(entry_function->return_type, "void"),
                                                     module_init_symbols,
                                                     module_init_count,
                                                     end_label);
        }
    }
    if (!v3_streq(plan->emit_kind, "obj") &&
        !v3_append_embedded_line_map(text, cap, plan, lowering, NULL)) {
        free(text);
        return false;
    }
    v3_append_symbol_trailer_for_target(plan, text, cap);
    if (!v3_ensure_parent_dir(asm_path) || !v3_write_text_file(asm_path, text)) {
        free(text);
        return false;
    }
    free(text);
    if (!compile_object) {
        return true;
    }
    return v3_compile_asm_object(plan->target_triple, plan->emit_kind, asm_path, primary->output_path);
}

static bool v3_materialize_primary_object(const V3SystemLinkPlanStub *plan,
                                          const V3LoweringPlanStub *lowering,
                                          const V3PrimaryObjectPlanStub *primary) {
    return v3_materialize_primary_object_internal(plan, lowering, primary, true);
}

static bool v3_materialize_primary_asm_only(const V3SystemLinkPlanStub *plan,
                                            const V3LoweringPlanStub *lowering,
                                            const V3PrimaryObjectPlanStub *primary) {
    return v3_materialize_primary_object_internal(plan, lowering, primary, false);
}

static bool v3_materialize_provider_objects(const V3BootstrapContract *contract,
                                            const V3SystemLinkPlanStub *plan,
                                            const V3ObjectPlanStub *object_plan) {
    size_t i;
    if (object_plan->provider_source_count != object_plan->provider_object_count) {
        return false;
    }
    for (i = 0; i < object_plan->provider_source_count; ++i) {
        const char *source_path = object_plan->provider_source_paths[i].text;
        const char *ext = strrchr(source_path != NULL ? source_path : "", '.');
        if (ext != NULL && strcmp(ext, ".cheng") == 0) {
            if (!v3_materialize_cheng_object(contract,
                                             plan->package_root,
                                             source_path,
                                             plan->target_triple,
                                             object_plan->provider_object_paths[i].text)) {
                return false;
            }
        } else {
            if (!v3_compile_c_object(plan->target_triple,
                                     plan->emit_kind,
                                     source_path,
                                     object_plan->provider_object_paths[i].text)) {
                return false;
            }
        }
    }
    return true;
}

static bool v3_build_exec_context(const V3BootstrapContract *contract,
                                  int argc,
                                  char **argv,
                                  bool require_output,
                                  bool enable_baseline_surface_check,
                                  V3SystemLinkPlanStub *plan,
                                  V3CompilerWorldArtifacts *world,
                                  V3LoweringPlanStub **lowering_out,
                                  V3PrimaryObjectPlanStub *primary,
                                  V3ObjectPlanStub *object_plan,
                                  V3NativeLinkPlanStub *native_link);
static void v3_compiler_world_artifacts_free(V3CompilerWorldArtifacts *artifacts);

static bool v3_materialize_cheng_object(const V3BootstrapContract *contract,
                                        const char *package_root,
                                        const char *source_path,
                                        const char *target_triple,
                                        const char *out_path) {
    char root_flag[PATH_MAX + 16];
    char in_flag[PATH_MAX + 16];
    char target_flag[256];
    char out_flag[PATH_MAX + 16];
    char *argv_local[7];
    V3SystemLinkPlanStub *plan;
    V3CompilerWorldArtifacts *world;
    V3LoweringPlanStub *lowering = NULL;
    V3PrimaryObjectPlanStub *primary;
    V3ObjectPlanStub *object_plan;
    V3NativeLinkPlanStub *native_link;
    bool ok = false;
    plan = (V3SystemLinkPlanStub *)v3_xmalloc(sizeof(*plan));
    world = (V3CompilerWorldArtifacts *)v3_xmalloc(sizeof(*world));
    primary = (V3PrimaryObjectPlanStub *)v3_xmalloc(sizeof(*primary));
    object_plan = (V3ObjectPlanStub *)v3_xmalloc(sizeof(*object_plan));
    native_link = (V3NativeLinkPlanStub *)v3_xmalloc(sizeof(*native_link));
    memset(world, 0, sizeof(*world));
    snprintf(root_flag, sizeof(root_flag), "--root:%s", package_root);
    snprintf(in_flag, sizeof(in_flag), "--in:%s", source_path);
    snprintf(target_flag, sizeof(target_flag), "--target:%s", target_triple);
    snprintf(out_flag, sizeof(out_flag), "--out:%s", out_path);
    argv_local[0] = "cheng_v3_seed";
    argv_local[1] = "system-link-exec";
    argv_local[2] = root_flag;
    argv_local[3] = in_flag;
    argv_local[4] = "--emit:obj";
    argv_local[5] = target_flag;
    argv_local[6] = out_flag;
    if (!v3_build_exec_context(contract,
                               7,
                               argv_local,
                               true,
                               false,
                               plan,
                               world,
                               &lowering,
                               primary,
                               object_plan,
                               native_link)) {
        goto done;
    }
    if (primary->missing_reason_count > 0U) {
        fprintf(stderr, "[cheng_v3_seed] cheng object materialize blocked source=%s out=%s\n", source_path, out_path);
        goto done;
    }
    ok = v3_materialize_primary_object(plan, lowering, primary);
done:
    free(lowering);
    v3_compiler_world_artifacts_free(world);
    free(native_link);
    free(object_plan);
    free(primary);
    free(world);
    free(plan);
    return ok;
}

static bool v3_compile_linux_nolibc_startup_object(const char *target_triple,
                                                   const char *entry_symbol,
                                                   const char *source_path,
                                                   const char *out_path) {
    char cc[PATH_MAX];
    char command[PATH_MAX * 8];
    char target_clause[PATH_MAX];
    char *quoted_cc;
    char *quoted_source;
    char *quoted_out;
    if (!v3_resolve_target_cc_program(target_triple, cc, sizeof(cc))) {
        fprintf(stderr, "[cheng_v3_seed] linux nolibc startup assembler missing for %s\n", target_triple);
        return false;
    }
    if (!v3_ensure_parent_dir(out_path)) {
        return false;
    }
    target_clause[0] = '\0';
    if ((v3_target_is_linux_x86_64(target_triple) ||
         v3_target_is_linux_generic_aarch64(target_triple)) &&
        !v3_target_matches_native_host(target_triple)) {
        snprintf(target_clause, sizeof(target_clause), " -target %s", target_triple);
    }
    quoted_cc = v3_shell_quote(cc);
    quoted_source = v3_shell_quote(source_path);
    quoted_out = v3_shell_quote(out_path);
    snprintf(command,
             sizeof(command),
             "%s%s -DCHENG_V3_LINUX_ENTRY_SYMBOL=%s -c %s -o %s",
             quoted_cc,
             target_clause,
             entry_symbol,
             quoted_source,
             quoted_out);
    free(quoted_cc);
    free(quoted_source);
    free(quoted_out);
    if (!v3_run_shell_command_status(command, "linux-nolibc-startup-compile")) {
        fprintf(stderr, "[cheng_v3_seed] linux nolibc startup compile failed command=%s\n", command);
        return false;
    }
    return true;
}

static bool v3_materialize_linux_nolibc_support_objects(const V3BootstrapContract *contract,
                                                        const V3SystemLinkPlanStub *plan,
                                                        const V3ObjectPlanStub *object_plan) {
    char bridge_symbol[PATH_MAX];
    if (!object_plan->linux_nolibc_support_enabled) {
        return true;
    }
    if (object_plan->linux_nolibc_runtime_source_path[0] == '\0' ||
        object_plan->linux_nolibc_runtime_object_path[0] == '\0' ||
        object_plan->linux_nolibc_startup_source_path[0] == '\0' ||
        object_plan->linux_nolibc_startup_object_path[0] == '\0') {
        return false;
    }
    if (!v3_materialize_cheng_object(contract,
                                     plan->package_root,
                                     object_plan->linux_nolibc_runtime_source_path,
                                     plan->target_triple,
                                     object_plan->linux_nolibc_runtime_object_path)) {
        return false;
    }
    v3_runtime_entry_bridge_symbol(plan, bridge_symbol, sizeof(bridge_symbol));
    return v3_compile_linux_nolibc_startup_object(plan->target_triple,
                                                  bridge_symbol,
                                                  object_plan->linux_nolibc_startup_source_path,
                                                  object_plan->linux_nolibc_startup_object_path);
}

enum {
    V3_ELF_LAYOUT_NONE = 0,
    V3_ELF_LAYOUT_TEXT = 1,
    V3_ELF_LAYOUT_RODATA = 2,
    V3_ELF_LAYOUT_DATA = 3,
    V3_ELF_LAYOUT_BSS = 4
};

#define V3_ELF_PAGE_SIZE 0x1000ULL
#define V3_ELF_BASE_ADDR 0x400000ULL
#define V3_ELF_ET_REL 1U
#define V3_ELF_ET_EXEC 2U
#define V3_ELF_EM_AARCH64 183U
#define V3_ELF_EM_RISCV 243U
#define V3_ELF_SHT_SYMTAB 2U
#define V3_ELF_SHT_RELA 4U
#define V3_ELF_SHT_NOBITS 8U
#define V3_ELF_SHN_UNDEF 0U
#define V3_ELF_STB_GLOBAL 1U
#define V3_ELF_STT_SECTION 3U
#define V3_ELF_PT_LOAD 1U
#define V3_ELF_PT_PHDR 6U
#define V3_ELF_PT_GNU_STACK 0x6474e551U
#define V3_ELF_PF_X 1U
#define V3_ELF_PF_W 2U
#define V3_ELF_PF_R 4U
#define V3_ELF_R_AARCH64_ABS64 257U
#define V3_ELF_R_AARCH64_ADR_PREL_PG_HI21 275U
#define V3_ELF_R_AARCH64_ADD_ABS_LO12_NC 277U
#define V3_ELF_R_AARCH64_JUMP26 282U
#define V3_ELF_R_AARCH64_CALL26 283U
#define V3_PE_FILE_ALIGNMENT 0x200U
#define V3_PE_SECTION_ALIGNMENT 0x1000U
#define V3_PE_IMAGE_BASE_AARCH64 0x140000000ULL
#define V3_PE_MACHINE_ARM64 0xaa64U
#define V3_PE_CHARACTERISTICS_RELOCS_STRIPPED 0x0001U
#define V3_PE_CHARACTERISTICS_EXECUTABLE_IMAGE 0x0002U
#define V3_PE_CHARACTERISTICS_LARGE_ADDRESS_AWARE 0x0020U
#define V3_PE_OPTIONAL_MAGIC_PE32_PLUS 0x20bU
#define V3_PE_SUBSYSTEM_WINDOWS_CUI 3U
#define V3_PE_SECTION_CNT_CODE 0x00000020U
#define V3_PE_SECTION_CNT_INITIALIZED_DATA 0x00000040U
#define V3_PE_SECTION_MEM_EXECUTE 0x20000000U
#define V3_PE_SECTION_MEM_READ 0x40000000U
#define V3_PE_SECTION_MEM_WRITE 0x80000000U
#define V3_MACHO_MAGIC_64 0xfeedfacfU
#define V3_MACHO_LC_SEGMENT_64 0x19U
#define V3_MACHO_LC_SYMTAB 0x2U

static void v3_bytebuf_append_zero_fill(V3ByteBuf *buf, size_t len) {
    static const uint8_t ZEROES[32] = {0};
    while (len > 0U) {
        size_t chunk = len > sizeof(ZEROES) ? sizeof(ZEROES) : len;
        v3_bytebuf_append_bytes(buf, ZEROES, chunk);
        len -= chunk;
    }
}

static void v3_bytebuf_append_cstring_raw(V3ByteBuf *buf, const char *text) {
    size_t len = text != NULL ? strlen(text) : 0U;
    if (len > 0U) {
        v3_bytebuf_append_bytes(buf, (const uint8_t *)text, len);
    }
    v3_bytebuf_append_zero_fill(buf, 1U);
}

static uint16_t v3_read_u16_le_at(const unsigned char *data, size_t off) {
    return (uint16_t)((uint16_t)data[off] |
                      ((uint16_t)data[off + 1U] << 8U));
}

static uint32_t v3_read_u32_le_at(const unsigned char *data, size_t off) {
    return (uint32_t)data[off] |
           ((uint32_t)data[off + 1U] << 8U) |
           ((uint32_t)data[off + 2U] << 16U) |
           ((uint32_t)data[off + 3U] << 24U);
}

static uint64_t v3_read_u64_le_at(const unsigned char *data, size_t off) {
    uint64_t lo = (uint64_t)v3_read_u32_le_at(data, off);
    uint64_t hi = (uint64_t)v3_read_u32_le_at(data, off + 4U);
    return lo | (hi << 32U);
}

static int64_t v3_read_i64_le_at(const unsigned char *data, size_t off) {
    return (int64_t)v3_read_u64_le_at(data, off);
}

static void v3_copy_fixed_name_bytes(const unsigned char *src,
                                     size_t len,
                                     char *out,
                                     size_t cap) {
    size_t i = 0U;
    if (cap == 0U) {
        return;
    }
    out[0] = '\0';
    if (src == NULL) {
        return;
    }
    while (i < len && src[i] != 0U && i + 1U < cap) {
        out[i] = (char)src[i];
        i += 1U;
    }
    out[i] = '\0';
    while (i > 0U && (out[i - 1U] == ' ' || out[i - 1U] == '\t')) {
        out[i - 1U] = '\0';
        i -= 1U;
    }
}

static bool v3_range_fits(size_t len, size_t off, size_t need) {
    return off <= len && need <= len - off;
}

static void v3_write_u16_le_at(unsigned char *data, size_t off, uint16_t value) {
    data[off] = (unsigned char)(value & 0xffU);
    data[off + 1U] = (unsigned char)((value >> 8U) & 0xffU);
}

static void v3_write_u32_le_at(unsigned char *data, size_t off, uint32_t value) {
    data[off] = (unsigned char)(value & 0xffU);
    data[off + 1U] = (unsigned char)((value >> 8U) & 0xffU);
    data[off + 2U] = (unsigned char)((value >> 16U) & 0xffU);
    data[off + 3U] = (unsigned char)((value >> 24U) & 0xffU);
}

static void v3_write_u64_le_at(unsigned char *data, size_t off, uint64_t value) {
    v3_write_u32_le_at(data, off, (uint32_t)(value & 0xffffffffU));
    v3_write_u32_le_at(data, off + 4U, (uint32_t)(value >> 32U));
}

static void v3_bytebuf_append_u16_le(V3ByteBuf *buf, uint16_t value) {
    uint8_t bytes[2];
    bytes[0] = (uint8_t)(value & 0xffU);
    bytes[1] = (uint8_t)((value >> 8U) & 0xffU);
    v3_bytebuf_append_bytes(buf, bytes, 2U);
}

static void v3_bytebuf_append_u32_le(V3ByteBuf *buf, uint32_t value) {
    uint8_t bytes[4];
    bytes[0] = (uint8_t)(value & 0xffU);
    bytes[1] = (uint8_t)((value >> 8U) & 0xffU);
    bytes[2] = (uint8_t)((value >> 16U) & 0xffU);
    bytes[3] = (uint8_t)((value >> 24U) & 0xffU);
    v3_bytebuf_append_bytes(buf, bytes, 4U);
}

static void v3_bytebuf_append_u64_le(V3ByteBuf *buf, uint64_t value) {
    v3_bytebuf_append_u32_le(buf, (uint32_t)(value & 0xffffffffU));
    v3_bytebuf_append_u32_le(buf, (uint32_t)(value >> 32U));
}

static uint64_t v3_align_u64(uint64_t value, uint64_t align) {
    uint64_t rem;
    if (align <= 1U) {
        return value;
    }
    rem = value % align;
    return rem == 0U ? value : value + (align - rem);
}

static void v3_bytebuf_align_zero(V3ByteBuf *buf, uint64_t align) {
    uint64_t padded = v3_align_u64((uint64_t)buf->len, align);
    while ((uint64_t)buf->len < padded) {
        uint8_t zero = 0U;
        v3_bytebuf_append_bytes(buf, &zero, 1U);
    }
}

static int v3_elf_section_layout_kind(const char *name, uint32_t type) {
    if (name == NULL || name[0] == '\0') {
        return V3_ELF_LAYOUT_NONE;
    }
    if (strcmp(name, ".text") == 0) {
        return V3_ELF_LAYOUT_TEXT;
    }
    if (strcmp(name, ".rodata") == 0 || strcmp(name, ".data.rel.ro") == 0) {
        return V3_ELF_LAYOUT_RODATA;
    }
    if (strcmp(name, ".data") == 0) {
        return V3_ELF_LAYOUT_DATA;
    }
    if (strcmp(name, ".bss") == 0 || type == V3_ELF_SHT_NOBITS) {
        return strcmp(name, ".bss") == 0 ? V3_ELF_LAYOUT_BSS : V3_ELF_LAYOUT_NONE;
    }
    return V3_ELF_LAYOUT_NONE;
}

static bool v3_read_binary_file_alloc(const char *path,
                                      unsigned char **data_out,
                                      size_t *len_out) {
    FILE *fp = NULL;
    long size_long;
    size_t size;
    unsigned char *data = NULL;
    fp = fopen(path, "rb");
    if (!fp) {
        fprintf(stderr, "[cheng_v3_seed] failed to open binary: %s\n", path);
        return false;
    }
    if (fseek(fp, 0L, SEEK_END) != 0) {
        fclose(fp);
        return false;
    }
    size_long = ftell(fp);
    if (size_long < 0L) {
        fclose(fp);
        return false;
    }
    size = (size_t)size_long;
    if (fseek(fp, 0L, SEEK_SET) != 0) {
        fclose(fp);
        return false;
    }
    data = (unsigned char *)v3_xmalloc(size > 0U ? size : 1U);
    if (size > 0U && fread(data, 1U, size, fp) != size) {
        fclose(fp);
        free(data);
        return false;
    }
    fclose(fp);
    *data_out = data;
    *len_out = size;
    return true;
}

static void v3_elf_object_free(V3ElfObjectStub *obj) {
    if (obj == NULL) {
        return;
    }
    free(obj->bytes);
    free(obj->sections);
    free(obj->placements);
    free(obj->symbols);
    free(obj->relocs);
    memset(obj, 0, sizeof(*obj));
}

static bool v3_elf_copy_name_from_table(const unsigned char *table,
                                        size_t table_size,
                                        uint32_t name_off,
                                        char *out,
                                        size_t cap) {
    size_t i = 0U;
    if (cap == 0U) {
        return false;
    }
    out[0] = '\0';
    if (table == NULL || name_off >= table_size) {
        return false;
    }
    while ((size_t)name_off + i < table_size &&
           table[name_off + i] != 0U &&
           i + 1U < cap) {
        out[i] = (char)table[name_off + i];
        i += 1U;
    }
    out[i] = '\0';
    return true;
}

static bool v3_parse_elf_relocatable_object(const char *path, V3ElfObjectStub *out) {
    size_t i;
    uint64_t shoff;
    uint16_t shentsize;
    uint16_t shnum;
    uint16_t shstrndx;
    uint16_t elf_type;
    const V3ElfSectionStub *shstr = NULL;
    const unsigned char *shstrtab = NULL;
    size_t sym_count = 0U;
    size_t reloc_count = 0U;
    memset(out, 0, sizeof(*out));
    snprintf(out->path, sizeof(out->path), "%s", path);
    if (!v3_read_binary_file_alloc(path, &out->bytes, &out->len)) {
        return false;
    }
    if (out->len < 64U ||
        out->bytes[0] != 0x7fU ||
        out->bytes[1] != 'E' ||
        out->bytes[2] != 'L' ||
        out->bytes[3] != 'F' ||
        out->bytes[4] != 2U ||
        out->bytes[5] != 1U) {
        fprintf(stderr, "[cheng_v3_seed] unsupported ELF object: %s\n", path);
        v3_elf_object_free(out);
        return false;
    }
    elf_type = v3_read_u16_le_at(out->bytes, 16U);
    if (elf_type != V3_ELF_ET_REL && elf_type != V3_ELF_ET_EXEC) {
        fprintf(stderr, "[cheng_v3_seed] unsupported ELF type: %s\n", path);
        v3_elf_object_free(out);
        return false;
    }
    out->file_type = elf_type;
    out->machine = v3_read_u16_le_at(out->bytes, 18U);
    shoff = v3_read_u64_le_at(out->bytes, 40U);
    shentsize = v3_read_u16_le_at(out->bytes, 58U);
    shnum = v3_read_u16_le_at(out->bytes, 60U);
    shstrndx = v3_read_u16_le_at(out->bytes, 62U);
    if (shoff == 0U || shentsize < 64U || shnum == 0U ||
        shoff + (uint64_t)shentsize * (uint64_t)shnum > (uint64_t)out->len) {
        if (elf_type == V3_ELF_ET_EXEC) {
            out->section_count = 0U;
            out->symbol_count = 0U;
            out->reloc_count = 0U;
            return true;
        }
        fprintf(stderr, "[cheng_v3_seed] malformed ELF section table: %s\n", path);
        v3_elf_object_free(out);
        return false;
    }
    out->section_count = (size_t)shnum;
    out->sections = (V3ElfSectionStub *)calloc(out->section_count, sizeof(V3ElfSectionStub));
    out->placements = (V3ElfSectionPlacementStub *)calloc(out->section_count, sizeof(V3ElfSectionPlacementStub));
    if (out->sections == NULL || out->placements == NULL) {
        v3_elf_object_free(out);
        return false;
    }
    for (i = 0U; i < out->section_count; ++i) {
        size_t base = (size_t)shoff + (size_t)shentsize * i;
        uint32_t name_off = v3_read_u32_le_at(out->bytes, base + 0U);
        V3ElfSectionStub *sec = &out->sections[i];
        sec->type = v3_read_u32_le_at(out->bytes, base + 4U);
        sec->offset = v3_read_u64_le_at(out->bytes, base + 24U);
        sec->size = v3_read_u64_le_at(out->bytes, base + 32U);
        sec->link = v3_read_u32_le_at(out->bytes, base + 40U);
        sec->info = v3_read_u32_le_at(out->bytes, base + 44U);
        sec->addralign = v3_read_u64_le_at(out->bytes, base + 48U);
        sec->entsize = v3_read_u64_le_at(out->bytes, base + 56U);
        snprintf(sec->name, sizeof(sec->name), "%u", name_off);
    }
    if (shstrndx >= out->section_count) {
        fprintf(stderr, "[cheng_v3_seed] malformed shstrndx: %s\n", path);
        v3_elf_object_free(out);
        return false;
    }
    shstr = &out->sections[shstrndx];
    if (shstr->offset + shstr->size > (uint64_t)out->len) {
        fprintf(stderr, "[cheng_v3_seed] malformed shstrtab: %s\n", path);
        v3_elf_object_free(out);
        return false;
    }
    shstrtab = out->bytes + shstr->offset;
    for (i = 0U; i < out->section_count; ++i) {
        size_t base = (size_t)shoff + (size_t)shentsize * i;
        uint32_t name_off = v3_read_u32_le_at(out->bytes, base + 0U);
        V3ElfSectionStub *sec = &out->sections[i];
        v3_elf_copy_name_from_table(shstrtab, (size_t)shstr->size, name_off, sec->name, sizeof(sec->name));
        sec->layout_kind = v3_elf_section_layout_kind(sec->name, sec->type);
    }
    for (i = 0U; i < out->section_count; ++i) {
        V3ElfSectionStub *sec = &out->sections[i];
        if (sec->type == V3_ELF_SHT_SYMTAB && sec->entsize >= 24U) {
            const unsigned char *strtab = NULL;
            size_t strtab_size = 0U;
            size_t j;
            if (sec->offset + sec->size > (uint64_t)out->len ||
                sec->link >= out->section_count ||
                out->sections[sec->link].offset + out->sections[sec->link].size > (uint64_t)out->len) {
                fprintf(stderr, "[cheng_v3_seed] malformed symtab: %s\n", path);
                v3_elf_object_free(out);
                return false;
            }
            sym_count = (size_t)(sec->size / sec->entsize);
            out->symbol_count = sym_count;
            out->symbols = (V3ElfSymbolStub *)calloc(sym_count > 0U ? sym_count : 1U, sizeof(V3ElfSymbolStub));
            if (out->symbols == NULL) {
                v3_elf_object_free(out);
                return false;
            }
            strtab = out->bytes + out->sections[sec->link].offset;
            strtab_size = (size_t)out->sections[sec->link].size;
            for (j = 0U; j < sym_count; ++j) {
                size_t sbase = (size_t)sec->offset + (size_t)sec->entsize * j;
                uint32_t st_name = v3_read_u32_le_at(out->bytes, sbase + 0U);
                V3ElfSymbolStub *sym = &out->symbols[j];
                sym->bind = (uint8_t)(out->bytes[sbase + 4U] >> 4U);
                sym->type = (uint8_t)(out->bytes[sbase + 4U] & 0x0fU);
                sym->shndx = v3_read_u16_le_at(out->bytes, sbase + 6U);
                sym->value = v3_read_u64_le_at(out->bytes, sbase + 8U);
                sym->size = v3_read_u64_le_at(out->bytes, sbase + 16U);
                if (!v3_elf_copy_name_from_table(strtab, strtab_size, st_name, sym->name, sizeof(sym->name)) &&
                    sym->type == V3_ELF_STT_SECTION &&
                    sym->shndx < out->section_count) {
                    snprintf(sym->name, sizeof(sym->name), "%s", out->sections[sym->shndx].name);
                }
            }
            break;
        }
    }
    for (i = 0U; i < out->section_count; ++i) {
        const V3ElfSectionStub *sec = &out->sections[i];
        if (sec->type == V3_ELF_SHT_RELA &&
            sec->entsize >= 24U &&
            sec->info < out->section_count &&
            out->sections[sec->info].layout_kind != V3_ELF_LAYOUT_NONE) {
            reloc_count += (size_t)(sec->size / sec->entsize);
        }
    }
    out->reloc_count = reloc_count;
    out->relocs = (V3ElfRelocStub *)calloc(reloc_count > 0U ? reloc_count : 1U, sizeof(V3ElfRelocStub));
    if (out->relocs == NULL) {
        v3_elf_object_free(out);
        return false;
    }
    if (reloc_count > 0U) {
        size_t cursor = 0U;
        for (i = 0U; i < out->section_count; ++i) {
            const V3ElfSectionStub *sec = &out->sections[i];
            size_t j;
            if (sec->type != V3_ELF_SHT_RELA ||
                sec->entsize < 24U ||
                sec->info >= out->section_count ||
                out->sections[sec->info].layout_kind == V3_ELF_LAYOUT_NONE) {
                continue;
            }
            if (sec->offset + sec->size > (uint64_t)out->len) {
                fprintf(stderr, "[cheng_v3_seed] malformed relocation section: %s\n", path);
                v3_elf_object_free(out);
                return false;
            }
            for (j = 0U; j < (size_t)(sec->size / sec->entsize); ++j) {
                size_t rbase = (size_t)sec->offset + (size_t)sec->entsize * j;
                uint64_t info = v3_read_u64_le_at(out->bytes, rbase + 8U);
                V3ElfRelocStub *rel = &out->relocs[cursor++];
                rel->target_shndx = (uint16_t)sec->info;
                rel->offset = v3_read_u64_le_at(out->bytes, rbase + 0U);
                rel->sym_index = (uint32_t)(info >> 32U);
                rel->type = (uint32_t)(info & 0xffffffffU);
                rel->addend = v3_read_i64_le_at(out->bytes, rbase + 16U);
            }
        }
    }
    return true;
}

static int v3_elf_global_def_cmp(const void *left, const void *right) {
    const V3ElfGlobalDefStub *a = (const V3ElfGlobalDefStub *)left;
    const V3ElfGlobalDefStub *b = (const V3ElfGlobalDefStub *)right;
    return strcmp(a->name, b->name);
}

static int32_t v3_elf_find_global_def(V3ElfGlobalDefStub *defs,
                                      size_t def_count,
                                      const char *name) {
    size_t lo = 0U;
    size_t hi = def_count;
    while (lo < hi) {
        size_t mid = lo + (hi - lo) / 2U;
        int cmp = strcmp(name, defs[mid].name);
        if (cmp == 0) {
            return (int32_t)mid;
        }
        if (cmp < 0) {
            hi = mid;
        } else {
            lo = mid + 1U;
        }
    }
    return -1;
}

static uint64_t v3_elf_layout_base_addr(int kind,
                                        uint64_t text_vaddr,
                                        uint64_t rodata_vaddr,
                                        uint64_t data_vaddr,
                                        uint64_t bss_vaddr) {
    if (kind == V3_ELF_LAYOUT_TEXT) {
        return text_vaddr;
    }
    if (kind == V3_ELF_LAYOUT_RODATA) {
        return rodata_vaddr;
    }
    if (kind == V3_ELF_LAYOUT_DATA) {
        return data_vaddr;
    }
    if (kind == V3_ELF_LAYOUT_BSS) {
        return bss_vaddr;
    }
    return 0U;
}

static bool v3_elf_resolve_symbol_addr(const V3ElfObjectStub *objects,
                                       size_t object_count,
                                       V3ElfGlobalDefStub *defs,
                                       size_t def_count,
                                       size_t object_index,
                                       const V3ElfSymbolStub *sym,
                                       uint64_t text_vaddr,
                                       uint64_t rodata_vaddr,
                                       uint64_t data_vaddr,
                                       uint64_t bss_vaddr,
                                       uint64_t *out_addr) {
    const V3ElfObjectStub *obj;
    const V3ElfSectionPlacementStub *placement;
    if (sym == NULL || object_index >= object_count) {
        return false;
    }
    if (sym->shndx == V3_ELF_SHN_UNDEF) {
        int32_t def_index;
        if (sym->name[0] == '\0') {
            return false;
        }
        def_index = v3_elf_find_global_def(defs, def_count, sym->name);
        if (def_index < 0) {
            return false;
        }
        obj = &objects[defs[def_index].object_index];
        sym = &obj->symbols[defs[def_index].symbol_index];
        object_index = defs[def_index].object_index;
    }
    obj = &objects[object_index];
    if (sym->shndx >= obj->section_count) {
        return false;
    }
    placement = &obj->placements[sym->shndx];
    if (placement->out_kind == V3_ELF_LAYOUT_NONE) {
        return false;
    }
    *out_addr = v3_elf_layout_base_addr(placement->out_kind,
                                        text_vaddr,
                                        rodata_vaddr,
                                        data_vaddr,
                                        bss_vaddr) +
                placement->out_off +
                sym->value;
    return true;
}

static bool v3_elf_patch_branch26(unsigned char *data,
                                  size_t len,
                                  uint64_t off,
                                  uint64_t place_addr,
                                  uint64_t target_addr,
                                  uint32_t opcode_base) {
    int64_t delta;
    int64_t imm26;
    uint32_t encoded;
    if (off + 4U > (uint64_t)len) {
        return false;
    }
    delta = (int64_t)target_addr - (int64_t)place_addr;
    if ((delta & 3LL) != 0LL) {
        return false;
    }
    imm26 = delta >> 2U;
    if (imm26 < -(1LL << 25U) || imm26 >= (1LL << 25U)) {
        return false;
    }
    encoded = opcode_base | ((uint32_t)imm26 & 0x03ffffffU);
    v3_write_u32_le_at(data, (size_t)off, encoded);
    return true;
}

static bool v3_elf_patch_adrp(unsigned char *data,
                              size_t len,
                              uint64_t off,
                              uint64_t place_addr,
                              uint64_t target_addr) {
    int64_t delta_pages;
    uint64_t imm21;
    uint32_t insn;
    if (off + 4U > (uint64_t)len) {
        return false;
    }
    delta_pages = (int64_t)((target_addr & ~0xfffULL) >> 12U) -
                  (int64_t)((place_addr & ~0xfffULL) >> 12U);
    if (delta_pages < -(1LL << 20U) || delta_pages >= (1LL << 20U)) {
        return false;
    }
    imm21 = (uint64_t)delta_pages & 0x1fffffULL;
    insn = v3_read_u32_le_at(data, (size_t)off);
    insn &= ~(uint32_t)(((uint32_t)3U << 29U) | ((uint32_t)0x7ffffU << 5U));
    insn |= (uint32_t)((imm21 & 0x3ULL) << 29U);
    insn |= (uint32_t)(((imm21 >> 2U) & 0x7ffffULL) << 5U);
    v3_write_u32_le_at(data, (size_t)off, insn);
    return true;
}

static bool v3_elf_patch_add_lo12(unsigned char *data,
                                  size_t len,
                                  uint64_t off,
                                  uint64_t target_addr) {
    uint32_t insn;
    uint32_t imm12;
    if (off + 4U > (uint64_t)len) {
        return false;
    }
    imm12 = (uint32_t)(target_addr & 0xfffULL);
    insn = v3_read_u32_le_at(data, (size_t)off);
    insn &= ~(uint32_t)(0xfffU << 10U);
    insn |= imm12 << 10U;
    v3_write_u32_le_at(data, (size_t)off, insn);
    return true;
}

static void v3_elf_write_phdr(V3ByteBuf *buf,
                              uint32_t p_type,
                              uint32_t p_flags,
                              uint64_t p_offset,
                              uint64_t p_vaddr,
                              uint64_t p_filesz,
                              uint64_t p_memsz,
                              uint64_t p_align) {
    v3_bytebuf_append_u32_le(buf, p_type);
    v3_bytebuf_append_u32_le(buf, p_flags);
    v3_bytebuf_append_u64_le(buf, p_offset);
    v3_bytebuf_append_u64_le(buf, p_vaddr);
    v3_bytebuf_append_u64_le(buf, p_vaddr);
    v3_bytebuf_append_u64_le(buf, p_filesz);
    v3_bytebuf_append_u64_le(buf, p_memsz);
    v3_bytebuf_append_u64_le(buf, p_align);
}

static uint32_t v3_riscv_encode_addi(uint32_t rd,
                                     uint32_t rs1,
                                     int32_t imm12) {
    return ((((uint32_t)imm12) & 0xfffU) << 20U) |
           ((rs1 & 31U) << 15U) |
           (0U << 12U) |
           ((rd & 31U) << 7U) |
           0x13U;
}

static uint32_t v3_riscv_encode_lui(uint32_t rd,
                                    int32_t hi20) {
    return ((((uint32_t)hi20) & 0xfffffU) << 12U) |
           ((rd & 31U) << 7U) |
           0x37U;
}

static uint32_t v3_riscv_encode_auipc(uint32_t rd,
                                      int32_t hi20) {
    return ((((uint32_t)hi20) & 0xfffffU) << 12U) |
           ((rd & 31U) << 7U) |
           0x17U;
}

static uint32_t v3_riscv_encode_ecall(void) {
    return 0x00000073U;
}

static bool v3_riscv_patch_pcrel_pair(unsigned char *data,
                                      size_t len,
                                      uint64_t auipc_off,
                                      uint64_t addi_off,
                                      uint64_t place_addr,
                                      uint64_t target_addr) {
    int64_t delta = (int64_t)target_addr - (int64_t)place_addr;
    int64_t hi20 = (delta + 0x800LL) >> 12;
    int64_t lo12 = delta - hi20 * 4096LL;
    uint32_t auipc_insn;
    uint32_t addi_insn;
    if (auipc_off + 4U > (uint64_t)len || addi_off + 4U > (uint64_t)len) {
        return false;
    }
    if (hi20 < -(1LL << 19U) || hi20 >= (1LL << 19U) ||
        lo12 < -2048LL || lo12 > 2047LL) {
        return false;
    }
    auipc_insn = v3_read_u32_le_at(data, (size_t)auipc_off);
    addi_insn = v3_read_u32_le_at(data, (size_t)addi_off);
    auipc_insn = (auipc_insn & 0x00000fffU) |
                 ((((uint32_t)hi20) & 0xfffffU) << 12U);
    addi_insn = (addi_insn & 0x000fffffU) |
                ((((uint32_t)lo12) & 0xfffU) << 20U);
    v3_write_u32_le_at(data, (size_t)auipc_off, auipc_insn);
    v3_write_u32_le_at(data, (size_t)addi_off, addi_insn);
    return true;
}

static void v3_riscv_emit_li32(V3ByteBuf *buf,
                               uint32_t rd,
                               int32_t value) {
    int32_t hi20 = (value + 0x800) >> 12;
    int32_t lo12 = value - hi20 * 4096;
    if (hi20 == 0) {
        v3_bytebuf_append_u32_le(buf, v3_riscv_encode_addi(rd, 0U, lo12));
        return;
    }
    v3_bytebuf_append_u32_le(buf, v3_riscv_encode_lui(rd, hi20));
    if (lo12 != 0) {
        v3_bytebuf_append_u32_le(buf, v3_riscv_encode_addi(rd, rd, lo12));
    }
}

static uint32_t v3_aarch64_encode_adrp(uint32_t rd) {
    return 0x90000000U | (rd & 31U);
}

static uint32_t v3_aarch64_encode_add_imm12(uint32_t rd,
                                            uint32_t rn,
                                            uint32_t imm12) {
    return 0x91000000U |
           ((imm12 & 0xfffU) << 10U) |
           ((rn & 31U) << 5U) |
           (rd & 31U);
}

static uint32_t v3_aarch64_encode_ldr_u64(uint32_t rt,
                                          uint32_t rn) {
    return 0xF9400000U |
           ((rn & 31U) << 5U) |
           (rt & 31U);
}

static uint32_t v3_aarch64_encode_blr(uint32_t rn) {
    return 0xD63F0000U | ((rn & 31U) << 5U);
}

static uint32_t v3_aarch64_encode_ret(void) {
    return 0xD65F03C0U;
}

static uint32_t v3_aarch64_encode_movz_w(uint32_t rd,
                                         uint16_t imm16,
                                         uint32_t shift_bits) {
    return 0x52800000U |
           ((((shift_bits / 16U) & 3U) << 21U)) |
           (((uint32_t)imm16) << 5U) |
           (rd & 31U);
}

static uint32_t v3_aarch64_encode_movk_w(uint32_t rd,
                                         uint16_t imm16,
                                         uint32_t shift_bits) {
    return 0x72800000U |
           ((((shift_bits / 16U) & 3U) << 21U)) |
           (((uint32_t)imm16) << 5U) |
           (rd & 31U);
}

static void v3_aarch64_emit_i32_return_code(V3ByteBuf *buf,
                                            int32_t value) {
    uint32_t raw = (uint32_t)value;
    uint16_t lo = (uint16_t)(raw & 0xffffU);
    uint16_t hi = (uint16_t)((raw >> 16U) & 0xffffU);
    v3_bytebuf_append_u32_le(buf, v3_aarch64_encode_movz_w(0U, lo, 0U));
    if (hi != 0U) {
        v3_bytebuf_append_u32_le(buf, v3_aarch64_encode_movk_w(0U, hi, 16U));
    }
}

static bool v3_link_internal_riscv64_consteval_executable(const V3NativeLinkPlanStub *native_link,
                                                          const char *output_path) {
    V3ByteBuf text;
    V3ByteBuf rodata;
    unsigned char *out_bytes = NULL;
    uint64_t *string_offsets = NULL;
    uint64_t *string_auipc_offsets = NULL;
    uint64_t *string_addi_offsets = NULL;
    uint64_t text_file_off;
    uint64_t rodata_file_off;
    uint64_t entry_addr;
    uint64_t file_size;
    size_t i;
    bool ok = false;
    memset(&text, 0, sizeof(text));
    memset(&rodata, 0, sizeof(rodata));
    if (!native_link->consteval_builtin_ready) {
        fprintf(stderr, "[cheng_v3_seed] riscv64 consteval link missing entry program\n");
        return false;
    }
    string_offsets = (uint64_t *)calloc(native_link->consteval_echo_count > 0U ? native_link->consteval_echo_count : 1U,
                                        sizeof(uint64_t));
    string_auipc_offsets = (uint64_t *)calloc(native_link->consteval_echo_count > 0U ? native_link->consteval_echo_count : 1U,
                                              sizeof(uint64_t));
    string_addi_offsets = (uint64_t *)calloc(native_link->consteval_echo_count > 0U ? native_link->consteval_echo_count : 1U,
                                             sizeof(uint64_t));
    if (string_offsets == NULL || string_auipc_offsets == NULL || string_addi_offsets == NULL) {
        goto done;
    }
    v3_bytebuf_init(&text, 256U);
    v3_bytebuf_init(&rodata, 256U);
    for (i = 0U; i < native_link->consteval_echo_count; ++i) {
        string_auipc_offsets[i] = (uint64_t)text.len;
        v3_bytebuf_append_u32_le(&text, v3_riscv_encode_auipc(11U, 0));
        string_addi_offsets[i] = (uint64_t)text.len;
        v3_bytebuf_append_u32_le(&text, v3_riscv_encode_addi(11U, 11U, 0));
        v3_riscv_emit_li32(&text, 10U, 1);
        v3_riscv_emit_li32(&text,
                           12U,
                           (int32_t)(strlen(native_link->consteval_echo_texts[i].text) + 1U));
        v3_riscv_emit_li32(&text, 17U, 64);
        v3_bytebuf_append_u32_le(&text, v3_riscv_encode_ecall());
    }
    v3_riscv_emit_li32(&text, 10U, native_link->consteval_return_code);
    v3_riscv_emit_li32(&text, 17U, 93);
    v3_bytebuf_append_u32_le(&text, v3_riscv_encode_ecall());
    for (i = 0U; i < native_link->consteval_echo_count; ++i) {
        uint8_t newline = (uint8_t)'\n';
        string_offsets[i] = (uint64_t)rodata.len;
        if (native_link->consteval_echo_texts[i].text[0] != '\0') {
            v3_bytebuf_append_bytes(&rodata,
                                    (const uint8_t *)native_link->consteval_echo_texts[i].text,
                                    strlen(native_link->consteval_echo_texts[i].text));
        }
        v3_bytebuf_append_bytes(&rodata, &newline, 1U);
    }
    text_file_off = v3_align_u64(64U + 56U * 2U, V3_ELF_PAGE_SIZE);
    rodata_file_off = v3_align_u64(text_file_off + (uint64_t)text.len, 16U);
    entry_addr = V3_ELF_BASE_ADDR + text_file_off;
    for (i = 0U; i < native_link->consteval_echo_count; ++i) {
        uint64_t target_addr = V3_ELF_BASE_ADDR + rodata_file_off + string_offsets[i];
        uint64_t place_addr = V3_ELF_BASE_ADDR + text_file_off + string_auipc_offsets[i];
        if (!v3_riscv_patch_pcrel_pair(text.data,
                                       text.len,
                                       string_auipc_offsets[i],
                                       string_addi_offsets[i],
                                       place_addr,
                                       target_addr)) {
            fprintf(stderr, "[cheng_v3_seed] riscv64 consteval patch failed index=%zu\n", i);
            goto done;
        }
    }
    file_size = rodata_file_off + (uint64_t)rodata.len;
    out_bytes = (unsigned char *)calloc((size_t)file_size, 1U);
    if (out_bytes == NULL) {
        goto done;
    }
    if (text.len > 0U) {
        memcpy(out_bytes + text_file_off, text.data, text.len);
    }
    if (rodata.len > 0U) {
        memcpy(out_bytes + rodata_file_off, rodata.data, rodata.len);
    }
    {
        V3ByteBuf header;
        v3_bytebuf_init(&header, 256U);
        v3_bytebuf_append_bytes(&header, (const uint8_t *)"\x7f""ELF", 4U);
        {
            uint8_t ident[12];
            memset(ident, 0, sizeof(ident));
            ident[0] = 2U;
            ident[1] = 1U;
            ident[2] = 1U;
            v3_bytebuf_append_bytes(&header, ident, sizeof(ident));
        }
        v3_bytebuf_append_u16_le(&header, V3_ELF_ET_EXEC);
        v3_bytebuf_append_u16_le(&header, V3_ELF_EM_RISCV);
        v3_bytebuf_append_u32_le(&header, 1U);
        v3_bytebuf_append_u64_le(&header, entry_addr);
        v3_bytebuf_append_u64_le(&header, 64U);
        v3_bytebuf_append_u64_le(&header, 0U);
        v3_bytebuf_append_u32_le(&header, 0U);
        v3_bytebuf_append_u16_le(&header, 64U);
        v3_bytebuf_append_u16_le(&header, 56U);
        v3_bytebuf_append_u16_le(&header, 2U);
        v3_bytebuf_append_u16_le(&header, 0U);
        v3_bytebuf_append_u16_le(&header, 0U);
        v3_bytebuf_append_u16_le(&header, 0U);
        v3_elf_write_phdr(&header,
                          V3_ELF_PT_LOAD,
                          V3_ELF_PF_R | V3_ELF_PF_X,
                          0U,
                          V3_ELF_BASE_ADDR,
                          file_size,
                          file_size,
                          V3_ELF_PAGE_SIZE);
        v3_elf_write_phdr(&header,
                          V3_ELF_PT_GNU_STACK,
                          V3_ELF_PF_R | V3_ELF_PF_W,
                          0U,
                          0U,
                          0U,
                          0U,
                          16U);
        memcpy(out_bytes, header.data, header.len);
        free(header.data);
    }
    if (!v3_ensure_parent_dir(output_path) ||
        !v3_write_binary_file(output_path, out_bytes, (size_t)file_size) ||
        chmod(output_path, 0755) != 0) {
        goto done;
    }
    ok = true;
done:
    free(out_bytes);
    free(string_offsets);
    free(string_auipc_offsets);
    free(string_addi_offsets);
    free(text.data);
    free(rodata.data);
    return ok;
}

static bool v3_link_internal_windows_aarch64_consteval_executable(const V3NativeLinkPlanStub *native_link,
                                                                  const char *output_path) {
    V3ByteBuf section;
    unsigned char *out_bytes = NULL;
    uint64_t *string_rvas = NULL;
    uint64_t *string_adrp_offsets = NULL;
    uint64_t *string_add_offsets = NULL;
    uint64_t *puts_adrp_offsets = NULL;
    uint64_t *puts_add_offsets = NULL;
    uint32_t section_rva = V3_PE_SECTION_ALIGNMENT;
    uint32_t headers_size = (uint32_t)v3_align_u64(0x80U + 4U + 20U + 0xF0U + 40U, V3_PE_FILE_ALIGNMENT);
    uint32_t section_raw_size;
    uint32_t size_of_image;
    uint32_t size_of_code;
    uint32_t size_of_init_data;
    uint32_t code_size;
    uint32_t import_desc_rva;
    uint32_t ucrt_int_rva;
    uint32_t kernel_int_rva;
    uint32_t ucrt_iat_rva;
    uint32_t kernel_iat_rva;
    uint32_t puts_name_rva;
    uint32_t exit_name_rva;
    uint32_t ucrt_dll_rva;
    uint32_t kernel_dll_rva;
    uint32_t import_dir_size = 60U;
    uint32_t iat_dir_rva;
    uint32_t iat_dir_size = 32U;
    uint64_t exit_adrp_off;
    uint64_t exit_add_off;
    size_t i;
    bool ok = false;
    memset(&section, 0, sizeof(section));
    if (!native_link->consteval_builtin_ready) {
        fprintf(stderr, "[cheng_v3_seed] windows consteval link missing entry program\n");
        return false;
    }
    string_rvas = (uint64_t *)calloc(native_link->consteval_echo_count > 0U ? native_link->consteval_echo_count : 1U,
                                     sizeof(uint64_t));
    string_adrp_offsets = (uint64_t *)calloc(native_link->consteval_echo_count > 0U ? native_link->consteval_echo_count : 1U,
                                             sizeof(uint64_t));
    string_add_offsets = (uint64_t *)calloc(native_link->consteval_echo_count > 0U ? native_link->consteval_echo_count : 1U,
                                            sizeof(uint64_t));
    puts_adrp_offsets = (uint64_t *)calloc(native_link->consteval_echo_count > 0U ? native_link->consteval_echo_count : 1U,
                                           sizeof(uint64_t));
    puts_add_offsets = (uint64_t *)calloc(native_link->consteval_echo_count > 0U ? native_link->consteval_echo_count : 1U,
                                          sizeof(uint64_t));
    if (string_rvas == NULL ||
        string_adrp_offsets == NULL ||
        string_add_offsets == NULL ||
        puts_adrp_offsets == NULL ||
        puts_add_offsets == NULL) {
        goto done;
    }
    v3_bytebuf_init(&section, 512U);
    for (i = 0U; i < native_link->consteval_echo_count; ++i) {
        string_adrp_offsets[i] = (uint64_t)section.len;
        v3_bytebuf_append_u32_le(&section, v3_aarch64_encode_adrp(0U));
        string_add_offsets[i] = (uint64_t)section.len;
        v3_bytebuf_append_u32_le(&section, v3_aarch64_encode_add_imm12(0U, 0U, 0U));
        puts_adrp_offsets[i] = (uint64_t)section.len;
        v3_bytebuf_append_u32_le(&section, v3_aarch64_encode_adrp(16U));
        puts_add_offsets[i] = (uint64_t)section.len;
        v3_bytebuf_append_u32_le(&section, v3_aarch64_encode_add_imm12(16U, 16U, 0U));
        v3_bytebuf_append_u32_le(&section, v3_aarch64_encode_ldr_u64(16U, 16U));
        v3_bytebuf_append_u32_le(&section, v3_aarch64_encode_blr(16U));
    }
    v3_aarch64_emit_i32_return_code(&section, native_link->consteval_return_code);
    exit_adrp_off = (uint64_t)section.len;
    v3_bytebuf_append_u32_le(&section, v3_aarch64_encode_adrp(16U));
    exit_add_off = (uint64_t)section.len;
    v3_bytebuf_append_u32_le(&section, v3_aarch64_encode_add_imm12(16U, 16U, 0U));
    v3_bytebuf_append_u32_le(&section, v3_aarch64_encode_ldr_u64(16U, 16U));
    v3_bytebuf_append_u32_le(&section, v3_aarch64_encode_blr(16U));
    v3_bytebuf_append_u32_le(&section, v3_aarch64_encode_ret());
    code_size = (uint32_t)section.len;
    v3_bytebuf_align_zero(&section, 8U);
    for (i = 0U; i < native_link->consteval_echo_count; ++i) {
        string_rvas[i] = (uint64_t)(section_rva + (uint32_t)section.len);
        v3_bytebuf_append_cstring_raw(&section, native_link->consteval_echo_texts[i].text);
    }
    v3_bytebuf_align_zero(&section, 8U);
    import_desc_rva = section_rva + (uint32_t)section.len;
    v3_bytebuf_append_zero_fill(&section, 60U);
    ucrt_int_rva = section_rva + (uint32_t)section.len;
    v3_bytebuf_append_zero_fill(&section, 16U);
    kernel_int_rva = section_rva + (uint32_t)section.len;
    v3_bytebuf_append_zero_fill(&section, 16U);
    ucrt_iat_rva = section_rva + (uint32_t)section.len;
    v3_bytebuf_append_zero_fill(&section, 16U);
    kernel_iat_rva = section_rva + (uint32_t)section.len;
    v3_bytebuf_append_zero_fill(&section, 16U);
    iat_dir_rva = ucrt_iat_rva;
    v3_bytebuf_align_zero(&section, 2U);
    puts_name_rva = section_rva + (uint32_t)section.len;
    v3_bytebuf_append_zero_fill(&section, 2U);
    v3_bytebuf_append_cstring_raw(&section, "puts");
    v3_bytebuf_align_zero(&section, 2U);
    exit_name_rva = section_rva + (uint32_t)section.len;
    v3_bytebuf_append_zero_fill(&section, 2U);
    v3_bytebuf_append_cstring_raw(&section, "ExitProcess");
    ucrt_dll_rva = section_rva + (uint32_t)section.len;
    v3_bytebuf_append_cstring_raw(&section, "UCRTBASE.dll");
    kernel_dll_rva = section_rva + (uint32_t)section.len;
    v3_bytebuf_append_cstring_raw(&section, "KERNEL32.dll");
    v3_write_u32_le_at(section.data, (size_t)(import_desc_rva - section_rva) + 0U, ucrt_int_rva);
    v3_write_u32_le_at(section.data, (size_t)(import_desc_rva - section_rva) + 12U, ucrt_dll_rva);
    v3_write_u32_le_at(section.data, (size_t)(import_desc_rva - section_rva) + 16U, ucrt_iat_rva);
    v3_write_u32_le_at(section.data, (size_t)(import_desc_rva - section_rva) + 20U, kernel_int_rva);
    v3_write_u32_le_at(section.data, (size_t)(import_desc_rva - section_rva) + 32U, kernel_dll_rva);
    v3_write_u32_le_at(section.data, (size_t)(import_desc_rva - section_rva) + 36U, kernel_iat_rva);
    v3_write_u64_le_at(section.data, (size_t)(ucrt_int_rva - section_rva), puts_name_rva);
    v3_write_u64_le_at(section.data, (size_t)(kernel_int_rva - section_rva), exit_name_rva);
    v3_write_u64_le_at(section.data, (size_t)(ucrt_iat_rva - section_rva), puts_name_rva);
    v3_write_u64_le_at(section.data, (size_t)(kernel_iat_rva - section_rva), exit_name_rva);
    for (i = 0U; i < native_link->consteval_echo_count; ++i) {
        uint64_t place_addr = V3_PE_IMAGE_BASE_AARCH64 + section_rva + string_adrp_offsets[i];
        uint64_t string_addr = V3_PE_IMAGE_BASE_AARCH64 + string_rvas[i];
        uint64_t puts_iat_addr = V3_PE_IMAGE_BASE_AARCH64 + ucrt_iat_rva;
        if (!v3_elf_patch_adrp(section.data, section.len, string_adrp_offsets[i], place_addr, string_addr) ||
            !v3_elf_patch_add_lo12(section.data, section.len, string_add_offsets[i], string_addr) ||
            !v3_elf_patch_adrp(section.data,
                               section.len,
                               puts_adrp_offsets[i],
                               V3_PE_IMAGE_BASE_AARCH64 + section_rva + puts_adrp_offsets[i],
                               puts_iat_addr) ||
            !v3_elf_patch_add_lo12(section.data, section.len, puts_add_offsets[i], puts_iat_addr)) {
            fprintf(stderr, "[cheng_v3_seed] windows consteval puts patch failed index=%zu\n", i);
            goto done;
        }
    }
    if (!v3_elf_patch_adrp(section.data,
                           section.len,
                           exit_adrp_off,
                           V3_PE_IMAGE_BASE_AARCH64 + section_rva + exit_adrp_off,
                           V3_PE_IMAGE_BASE_AARCH64 + kernel_iat_rva) ||
        !v3_elf_patch_add_lo12(section.data,
                               section.len,
                               exit_add_off,
                               V3_PE_IMAGE_BASE_AARCH64 + kernel_iat_rva)) {
        fprintf(stderr, "[cheng_v3_seed] windows consteval exit patch failed\n");
        goto done;
    }
    section_raw_size = (uint32_t)v3_align_u64((uint64_t)section.len, V3_PE_FILE_ALIGNMENT);
    size_of_code = (uint32_t)v3_align_u64((uint64_t)code_size, V3_PE_FILE_ALIGNMENT);
    size_of_init_data = section_raw_size > size_of_code ? (section_raw_size - size_of_code) : 0U;
    size_of_image = (uint32_t)v3_align_u64((uint64_t)section_rva + (uint64_t)section.len, V3_PE_SECTION_ALIGNMENT);
    out_bytes = (unsigned char *)calloc((size_t)headers_size + (size_t)section_raw_size, 1U);
    if (out_bytes == NULL) {
        goto done;
    }
    memcpy(out_bytes + headers_size, section.data, section.len);
    out_bytes[0] = 'M';
    out_bytes[1] = 'Z';
    v3_write_u32_le_at(out_bytes, 0x3cU, 0x80U);
    memcpy(out_bytes + 0x80U, "PE\0\0", 4U);
    v3_write_u16_le_at(out_bytes, 0x84U, V3_PE_MACHINE_ARM64);
    v3_write_u16_le_at(out_bytes, 0x86U, 1U);
    v3_write_u32_le_at(out_bytes, 0x94U, 0xF0U);
    v3_write_u16_le_at(out_bytes,
                       0x96U,
                       V3_PE_CHARACTERISTICS_RELOCS_STRIPPED |
                           V3_PE_CHARACTERISTICS_EXECUTABLE_IMAGE |
                           V3_PE_CHARACTERISTICS_LARGE_ADDRESS_AWARE);
    v3_write_u16_le_at(out_bytes, 0x98U, V3_PE_OPTIONAL_MAGIC_PE32_PLUS);
    out_bytes[0x9aU] = 1U;
    out_bytes[0x9bU] = 0U;
    v3_write_u32_le_at(out_bytes, 0x9cU, size_of_code);
    v3_write_u32_le_at(out_bytes, 0xa0U, size_of_init_data);
    v3_write_u32_le_at(out_bytes, 0xa4U, 0U);
    v3_write_u32_le_at(out_bytes, 0xa8U, section_rva);
    v3_write_u32_le_at(out_bytes, 0xacU, section_rva);
    v3_write_u64_le_at(out_bytes, 0xb0U, V3_PE_IMAGE_BASE_AARCH64);
    v3_write_u32_le_at(out_bytes, 0xb8U, V3_PE_SECTION_ALIGNMENT);
    v3_write_u32_le_at(out_bytes, 0xbcU, V3_PE_FILE_ALIGNMENT);
    v3_write_u16_le_at(out_bytes, 0xc0U, 6U);
    v3_write_u16_le_at(out_bytes, 0xc2U, 0U);
    v3_write_u16_le_at(out_bytes, 0xc4U, 0U);
    v3_write_u16_le_at(out_bytes, 0xc6U, 0U);
    v3_write_u16_le_at(out_bytes, 0xc8U, 6U);
    v3_write_u16_le_at(out_bytes, 0xcaU, 0U);
    v3_write_u32_le_at(out_bytes, 0xccU, 0U);
    v3_write_u32_le_at(out_bytes, 0xd0U, size_of_image);
    v3_write_u32_le_at(out_bytes, 0xd4U, headers_size);
    v3_write_u32_le_at(out_bytes, 0xd8U, 0U);
    v3_write_u16_le_at(out_bytes, 0xdcU, V3_PE_SUBSYSTEM_WINDOWS_CUI);
    v3_write_u16_le_at(out_bytes, 0xdeU, 0U);
    v3_write_u64_le_at(out_bytes, 0xe0U, 0x100000ULL);
    v3_write_u64_le_at(out_bytes, 0xe8U, 0x1000ULL);
    v3_write_u64_le_at(out_bytes, 0xf0U, 0x100000ULL);
    v3_write_u64_le_at(out_bytes, 0xf8U, 0x1000ULL);
    v3_write_u32_le_at(out_bytes, 0x100U, 0U);
    v3_write_u32_le_at(out_bytes, 0x104U, 16U);
    v3_write_u32_le_at(out_bytes, 0x110U, import_desc_rva);
    v3_write_u32_le_at(out_bytes, 0x114U, import_dir_size);
    v3_write_u32_le_at(out_bytes, 0x168U, iat_dir_rva);
    v3_write_u32_le_at(out_bytes, 0x16cU, iat_dir_size);
    memcpy(out_bytes + 0x188U, ".text\0\0\0", 8U);
    v3_write_u32_le_at(out_bytes, 0x190U, (uint32_t)section.len);
    v3_write_u32_le_at(out_bytes, 0x194U, section_rva);
    v3_write_u32_le_at(out_bytes, 0x198U, section_raw_size);
    v3_write_u32_le_at(out_bytes, 0x19cU, headers_size);
    v3_write_u32_le_at(out_bytes,
                       0x1acU,
                       V3_PE_SECTION_CNT_CODE |
                           V3_PE_SECTION_CNT_INITIALIZED_DATA |
                           V3_PE_SECTION_MEM_EXECUTE |
                           V3_PE_SECTION_MEM_READ |
                           V3_PE_SECTION_MEM_WRITE);
    if (!v3_ensure_parent_dir(output_path) ||
        !v3_write_binary_file(output_path,
                              out_bytes,
                              (size_t)headers_size + (size_t)section_raw_size)) {
        goto done;
    }
    ok = true;
done:
    free(out_bytes);
    free(string_rvas);
    free(string_adrp_offsets);
    free(string_add_offsets);
    free(puts_adrp_offsets);
    free(puts_add_offsets);
    free(section.data);
    return ok;
}

static bool v3_link_internal_linux_nolibc_executable(const V3NativeLinkPlanStub *native_link,
                                                     const char *output_path) {
    V3ElfObjectStub *objects = NULL;
    V3ElfGlobalDefStub *defs = NULL;
    size_t object_count = native_link->link_input_count;
    size_t object_index;
    size_t def_count = 0U;
    V3ByteBuf text;
    V3ByteBuf rodata;
    V3ByteBuf data;
    uint64_t bss_size = 0U;
    uint64_t max_bss_align = 1U;
    uint64_t text_file_off;
    uint64_t rodata_file_off;
    uint64_t data_file_off;
    uint64_t text_vaddr;
    uint64_t rodata_vaddr;
    uint64_t data_vaddr;
    uint64_t bss_vaddr;
    uint64_t data_mem_size;
    uint64_t text_file_size;
    uint64_t file_size;
    uint64_t entry_addr = 0U;
    unsigned char *out_bytes = NULL;
    bool ok = false;
    memset(&text, 0, sizeof(text));
    memset(&rodata, 0, sizeof(rodata));
    memset(&data, 0, sizeof(data));
    if (object_count == 0U) {
        fprintf(stderr, "[cheng_v3_seed] internal ELF link missing inputs\n");
        return false;
    }
    objects = (V3ElfObjectStub *)calloc(object_count, sizeof(V3ElfObjectStub));
    if (objects == NULL) {
        return false;
    }
    for (object_index = 0U; object_index < object_count; ++object_index) {
        if (!v3_parse_elf_relocatable_object(native_link->link_input_paths[object_index].text,
                                             &objects[object_index])) {
            goto done;
        }
    }
    v3_bytebuf_init(&text, 4096U);
    v3_bytebuf_init(&rodata, 4096U);
    v3_bytebuf_init(&data, 4096U);
    for (object_index = 0U; object_index < object_count; ++object_index) {
        size_t sec_index;
        V3ElfObjectStub *obj = &objects[object_index];
        for (sec_index = 0U; sec_index < obj->section_count; ++sec_index) {
            const V3ElfSectionStub *sec = &obj->sections[sec_index];
            V3ElfSectionPlacementStub *placement = &obj->placements[sec_index];
            uint64_t align = sec->addralign > 0U ? sec->addralign : 1U;
            placement->out_kind = V3_ELF_LAYOUT_NONE;
            placement->out_off = 0U;
            if (sec->layout_kind == V3_ELF_LAYOUT_TEXT) {
                if (sec->offset + sec->size > (uint64_t)obj->len) {
                    goto done;
                }
                v3_bytebuf_align_zero(&text, align);
                placement->out_kind = V3_ELF_LAYOUT_TEXT;
                placement->out_off = (uint64_t)text.len;
                v3_bytebuf_append_bytes(&text, obj->bytes + sec->offset, (size_t)sec->size);
            } else if (sec->layout_kind == V3_ELF_LAYOUT_RODATA) {
                if (sec->offset + sec->size > (uint64_t)obj->len) {
                    goto done;
                }
                v3_bytebuf_align_zero(&rodata, align);
                placement->out_kind = V3_ELF_LAYOUT_RODATA;
                placement->out_off = (uint64_t)rodata.len;
                v3_bytebuf_append_bytes(&rodata, obj->bytes + sec->offset, (size_t)sec->size);
            } else if (sec->layout_kind == V3_ELF_LAYOUT_DATA) {
                if (sec->offset + sec->size > (uint64_t)obj->len) {
                    goto done;
                }
                v3_bytebuf_align_zero(&data, align);
                placement->out_kind = V3_ELF_LAYOUT_DATA;
                placement->out_off = (uint64_t)data.len;
                v3_bytebuf_append_bytes(&data, obj->bytes + sec->offset, (size_t)sec->size);
            } else if (sec->layout_kind == V3_ELF_LAYOUT_BSS) {
                placement->out_kind = V3_ELF_LAYOUT_BSS;
                bss_size = v3_align_u64(bss_size, align);
                placement->out_off = bss_size;
                bss_size += sec->size;
                if (align > max_bss_align) {
                    max_bss_align = align;
                }
            }
        }
    }
    for (object_index = 0U; object_index < object_count; ++object_index) {
        size_t sym_index;
        const V3ElfObjectStub *obj = &objects[object_index];
        for (sym_index = 0U; sym_index < obj->symbol_count; ++sym_index) {
            const V3ElfSymbolStub *sym = &obj->symbols[sym_index];
            if (sym->bind == V3_ELF_STB_GLOBAL &&
                sym->shndx != V3_ELF_SHN_UNDEF &&
                sym->name[0] != '\0' &&
                sym->shndx < obj->section_count &&
                obj->placements[sym->shndx].out_kind != V3_ELF_LAYOUT_NONE) {
                def_count += 1U;
            }
        }
    }
    defs = (V3ElfGlobalDefStub *)calloc(def_count > 0U ? def_count : 1U, sizeof(V3ElfGlobalDefStub));
    if (defs == NULL) {
        goto done;
    }
    if (def_count > 0U) {
        size_t cursor = 0U;
        for (object_index = 0U; object_index < object_count; ++object_index) {
            size_t sym_index;
            const V3ElfObjectStub *obj = &objects[object_index];
            for (sym_index = 0U; sym_index < obj->symbol_count; ++sym_index) {
                const V3ElfSymbolStub *sym = &obj->symbols[sym_index];
                if (sym->bind == V3_ELF_STB_GLOBAL &&
                    sym->shndx != V3_ELF_SHN_UNDEF &&
                    sym->name[0] != '\0' &&
                    sym->shndx < obj->section_count &&
                    obj->placements[sym->shndx].out_kind != V3_ELF_LAYOUT_NONE) {
                    snprintf(defs[cursor].name, sizeof(defs[cursor].name), "%s", sym->name);
                    defs[cursor].object_index = object_index;
                    defs[cursor].symbol_index = (uint32_t)sym_index;
                    cursor += 1U;
                }
            }
        }
        qsort(defs, def_count, sizeof(V3ElfGlobalDefStub), v3_elf_global_def_cmp);
        for (object_index = 1U; object_index < def_count; ++object_index) {
            if (strcmp(defs[object_index - 1U].name, defs[object_index].name) == 0) {
                fprintf(stderr, "[cheng_v3_seed] duplicate ELF symbol: %s\n", defs[object_index].name);
                goto done;
            }
        }
    }
    text_file_off = v3_align_u64(64U + 56U * 4U, V3_ELF_PAGE_SIZE);
    rodata_file_off = v3_align_u64(text_file_off + (uint64_t)text.len, 16U);
    data_file_off = v3_align_u64(rodata_file_off + (uint64_t)rodata.len, V3_ELF_PAGE_SIZE);
    text_vaddr = V3_ELF_BASE_ADDR + text_file_off;
    rodata_vaddr = V3_ELF_BASE_ADDR + rodata_file_off;
    data_vaddr = V3_ELF_BASE_ADDR + data_file_off;
    bss_vaddr = v3_align_u64(data_vaddr + (uint64_t)data.len, max_bss_align);
    data_mem_size = (bss_vaddr - data_vaddr) + bss_size;
    for (object_index = 0U; object_index < object_count; ++object_index) {
        size_t reloc_index;
        const V3ElfObjectStub *obj = &objects[object_index];
        for (reloc_index = 0U; reloc_index < obj->reloc_count; ++reloc_index) {
            const V3ElfRelocStub *rel = &obj->relocs[reloc_index];
            const V3ElfSectionPlacementStub *target_place;
            const V3ElfSymbolStub *sym;
            unsigned char *buf = NULL;
            size_t buf_len = 0U;
            uint64_t patch_off = 0U;
            uint64_t place_addr = 0U;
            uint64_t target_addr = 0U;
            int64_t resolved_addr = 0LL;
            if (rel->sym_index >= obj->symbol_count || rel->target_shndx >= obj->section_count) {
                goto done;
            }
            target_place = &obj->placements[rel->target_shndx];
            if (target_place->out_kind == V3_ELF_LAYOUT_NONE) {
                goto done;
            }
            patch_off = target_place->out_off + rel->offset;
            if (target_place->out_kind == V3_ELF_LAYOUT_TEXT) {
                buf = text.data;
                buf_len = text.len;
                place_addr = text_vaddr + patch_off;
            } else if (target_place->out_kind == V3_ELF_LAYOUT_RODATA) {
                buf = rodata.data;
                buf_len = rodata.len;
                place_addr = rodata_vaddr + patch_off;
            } else if (target_place->out_kind == V3_ELF_LAYOUT_DATA) {
                buf = data.data;
                buf_len = data.len;
                place_addr = data_vaddr + patch_off;
            } else {
                goto done;
            }
            sym = &obj->symbols[rel->sym_index];
            if (!v3_elf_resolve_symbol_addr(objects,
                                            object_count,
                                            defs,
                                            def_count,
                                            object_index,
                                            sym,
                                            text_vaddr,
                                            rodata_vaddr,
                                            data_vaddr,
                                            bss_vaddr,
                                            &target_addr)) {
                fprintf(stderr,
                        "[cheng_v3_seed] unresolved ELF symbol while linking: %s\n",
                        sym->name[0] != '\0' ? sym->name : "<anonymous>");
                goto done;
            }
            resolved_addr = (int64_t)target_addr + rel->addend;
            if (resolved_addr < 0LL) {
                goto done;
            }
            target_addr = (uint64_t)resolved_addr;
            if (rel->type == V3_ELF_R_AARCH64_ADR_PREL_PG_HI21) {
                if (!v3_elf_patch_adrp(buf, buf_len, patch_off, place_addr, target_addr)) {
                    goto done;
                }
            } else if (rel->type == V3_ELF_R_AARCH64_ADD_ABS_LO12_NC) {
                if (!v3_elf_patch_add_lo12(buf, buf_len, patch_off, target_addr)) {
                    goto done;
                }
            } else if (rel->type == V3_ELF_R_AARCH64_CALL26) {
                if (!v3_elf_patch_branch26(buf, buf_len, patch_off, place_addr, target_addr, 0x94000000U)) {
                    goto done;
                }
            } else if (rel->type == V3_ELF_R_AARCH64_JUMP26) {
                if (!v3_elf_patch_branch26(buf, buf_len, patch_off, place_addr, target_addr, 0x14000000U)) {
                    goto done;
                }
            } else if (rel->type == V3_ELF_R_AARCH64_ABS64) {
                if (patch_off + 8U > (uint64_t)buf_len) {
                    goto done;
                }
                v3_write_u64_le_at(buf, (size_t)patch_off, target_addr);
            } else {
                fprintf(stderr, "[cheng_v3_seed] unsupported AArch64 ELF relocation=%u\n", rel->type);
                goto done;
            }
        }
    }
    {
        int32_t start_index = v3_elf_find_global_def(defs, def_count, "_start");
        if (start_index < 0 ||
            !v3_elf_resolve_symbol_addr(objects,
                                        object_count,
                                        defs,
                                        def_count,
                                        defs[start_index].object_index,
                                        &objects[defs[start_index].object_index].symbols[defs[start_index].symbol_index],
                                        text_vaddr,
                                        rodata_vaddr,
                                        data_vaddr,
                                        bss_vaddr,
                                        &entry_addr)) {
            fprintf(stderr, "[cheng_v3_seed] internal ELF link missing _start symbol\n");
            goto done;
        }
    }
    text_file_size = rodata_file_off + (uint64_t)rodata.len;
    file_size = text_file_size;
    if (data_file_off + (uint64_t)data.len > file_size) {
        file_size = data_file_off + (uint64_t)data.len;
    }
    out_bytes = (unsigned char *)calloc((size_t)file_size, 1U);
    if (out_bytes == NULL) {
        goto done;
    }
    if (text.len > 0U) {
        memcpy(out_bytes + text_file_off, text.data, text.len);
    }
    if (rodata.len > 0U) {
        memcpy(out_bytes + rodata_file_off, rodata.data, rodata.len);
    }
    if (data.len > 0U) {
        memcpy(out_bytes + data_file_off, data.data, data.len);
    }
    {
        V3ByteBuf header;
        v3_bytebuf_init(&header, 512U);
        v3_bytebuf_append_bytes(&header, (const uint8_t *)"\x7f""ELF", 4U);
        {
            uint8_t ident[12];
            memset(ident, 0, sizeof(ident));
            ident[0] = 2U;
            ident[1] = 1U;
            ident[2] = 1U;
            v3_bytebuf_append_bytes(&header, ident, sizeof(ident));
        }
        v3_bytebuf_append_u16_le(&header, V3_ELF_ET_EXEC);
        v3_bytebuf_append_u16_le(&header, V3_ELF_EM_AARCH64);
        v3_bytebuf_append_u32_le(&header, 1U);
        v3_bytebuf_append_u64_le(&header, entry_addr);
        v3_bytebuf_append_u64_le(&header, 64U);
        v3_bytebuf_append_u64_le(&header, 0U);
        v3_bytebuf_append_u32_le(&header, 0U);
        v3_bytebuf_append_u16_le(&header, 64U);
        v3_bytebuf_append_u16_le(&header, 56U);
        v3_bytebuf_append_u16_le(&header, 4U);
        v3_bytebuf_append_u16_le(&header, 0U);
        v3_bytebuf_append_u16_le(&header, 0U);
        v3_bytebuf_append_u16_le(&header, 0U);
        v3_elf_write_phdr(&header,
                          V3_ELF_PT_PHDR,
                          V3_ELF_PF_R,
                          64U,
                          V3_ELF_BASE_ADDR + 64U,
                          56U * 4U,
                          56U * 4U,
                          8U);
        v3_elf_write_phdr(&header,
                          V3_ELF_PT_LOAD,
                          V3_ELF_PF_R | V3_ELF_PF_X,
                          0U,
                          V3_ELF_BASE_ADDR,
                          text_file_size,
                          text_file_size,
                          V3_ELF_PAGE_SIZE);
        v3_elf_write_phdr(&header,
                          V3_ELF_PT_LOAD,
                          V3_ELF_PF_R | V3_ELF_PF_W,
                          data_file_off,
                          data_vaddr,
                          (uint64_t)data.len,
                          data_mem_size,
                          V3_ELF_PAGE_SIZE);
        v3_elf_write_phdr(&header,
                          V3_ELF_PT_GNU_STACK,
                          V3_ELF_PF_R | V3_ELF_PF_W,
                          0U,
                          0U,
                          0U,
                          0U,
                          16U);
        memcpy(out_bytes, header.data, header.len);
        free(header.data);
    }
    if (!v3_ensure_parent_dir(output_path) ||
        !v3_write_binary_file(output_path, out_bytes, (size_t)file_size) ||
        chmod(output_path, 0755) != 0) {
        goto done;
    }
    ok = true;
done:
    if (objects != NULL) {
        for (object_index = 0U; object_index < object_count; ++object_index) {
            v3_elf_object_free(&objects[object_index]);
        }
    }
    free(objects);
    free(defs);
    free(out_bytes);
    free(text.data);
    free(rodata.data);
    free(data.data);
    return ok;
}

static bool v3_link_native_executable(const V3NativeLinkPlanStub *native_link,
                                      const char *output_path) {
    size_t i;
    size_t cap = PATH_MAX * (size_t)(native_link->link_input_count + 16U);
    char *command = NULL;
    if (!v3_ensure_parent_dir(output_path)) {
        return false;
    }
    if (v3_target_is_wasm32_unknown_unknown(native_link->target_triple)) {
        if (native_link->primary_object_path[0] == '\0') {
            fprintf(stderr, "[cheng_v3_seed] wasm link missing primary module\n");
            return false;
        }
        return v3_copy_file_bytes(native_link->primary_object_path, output_path);
    }
    if (v3_streq(native_link->linker_flavor, "elf_riscv64_consteval_internal")) {
        return v3_link_internal_riscv64_consteval_executable(native_link, output_path);
    }
    if (v3_streq(native_link->linker_flavor, "pe_aarch64_consteval_internal")) {
        return v3_link_internal_windows_aarch64_consteval_executable(native_link, output_path);
    }
    if (v3_target_uses_linux_nolibc_runtime(native_link->target_triple)) {
        if (v3_streq(native_link->linker_flavor, "elf_aarch64_nolibc_internal")) {
            return v3_link_internal_linux_nolibc_executable(native_link, output_path);
        }
        command = (char *)v3_xmalloc(cap);
        command[0] = '\0';
        {
            const char *linker = native_link->linker_program[0] != '\0' ?
                                     native_link->linker_program :
                                     "aarch64-linux-gnu-gcc";
            char *quoted_linker = v3_shell_quote(linker);
            strncat(command, quoted_linker, cap - strlen(command) - 1U);
            free(quoted_linker);
        }
        if (v3_streq(native_link->linker_flavor, "gnu_gcc_driver")) {
            strncat(command, " -nostdlib -static", cap - strlen(command) - 1U);
            for (i = 0; i < native_link->link_input_count; ++i) {
                char *quoted_input = v3_shell_quote(native_link->link_input_paths[i].text);
                strncat(command, " ", cap - strlen(command) - 1U);
                strncat(command, quoted_input, cap - strlen(command) - 1U);
                free(quoted_input);
            }
            {
                char *quoted_out = v3_shell_quote(output_path);
                strncat(command, " -o ", cap - strlen(command) - 1U);
                strncat(command, quoted_out, cap - strlen(command) - 1U);
                free(quoted_out);
            }
        } else if (v3_streq(native_link->linker_flavor, "gnu_ld")) {
            strncat(command, " -m aarch64linux -e _start -static -o ", cap - strlen(command) - 1U);
            {
                char *quoted_out = v3_shell_quote(output_path);
                strncat(command, quoted_out, cap - strlen(command) - 1U);
                free(quoted_out);
            }
            for (i = 0; i < native_link->link_input_count; ++i) {
                char *quoted_input = v3_shell_quote(native_link->link_input_paths[i].text);
                strncat(command, " ", cap - strlen(command) - 1U);
                strncat(command, quoted_input, cap - strlen(command) - 1U);
                free(quoted_input);
            }
        } else if (v3_streq(native_link->linker_flavor, "ld.lld")) {
            strncat(command, " -m aarch64elf -e _start -static -o ", cap - strlen(command) - 1U);
            {
                char *quoted_out = v3_shell_quote(output_path);
                strncat(command, quoted_out, cap - strlen(command) - 1U);
                free(quoted_out);
            }
            for (i = 0; i < native_link->link_input_count; ++i) {
                char *quoted_input = v3_shell_quote(native_link->link_input_paths[i].text);
                strncat(command, " ", cap - strlen(command) - 1U);
                strncat(command, quoted_input, cap - strlen(command) - 1U);
                free(quoted_input);
            }
        } else if (v3_streq(native_link->linker_flavor, "llvm-lld") ||
                   v3_streq(native_link->linker_flavor, "lld")) {
            strncat(command, " -flavor gnu -m aarch64elf -e _start -static -o ", cap - strlen(command) - 1U);
            {
                char *quoted_out = v3_shell_quote(output_path);
                strncat(command, quoted_out, cap - strlen(command) - 1U);
                free(quoted_out);
            }
            for (i = 0; i < native_link->link_input_count; ++i) {
                char *quoted_input = v3_shell_quote(native_link->link_input_paths[i].text);
                strncat(command, " ", cap - strlen(command) - 1U);
                strncat(command, quoted_input, cap - strlen(command) - 1U);
                free(quoted_input);
            }
        } else {
            fprintf(stderr,
                    "[cheng_v3_seed] linux nolibc linker flavor unsupported: %s\n",
                    native_link->linker_flavor);
            free(command);
            return false;
        }
        if (!v3_run_shell_command_status(command, "native-link")) {
            fprintf(stderr, "[cheng_v3_seed] native link failed command=%s\n", command);
            free(command);
            return false;
        }
        free(command);
        return true;
    }
    command = (char *)v3_xmalloc(cap);
    command[0] = '\0';
    {
        const char *cc = native_link->linker_program[0] != '\0' ? native_link->linker_program : "cc";
        char *quoted_cc = v3_shell_quote(cc);
        strncat(command, quoted_cc, cap - strlen(command) - 1U);
        free(quoted_cc);
    }
    if (v3_target_is_darwin_arm64(native_link->target_triple)) {
        strncat(command, " -arch arm64", cap - strlen(command) - 1U);
    } else if (v3_target_is_elf_aarch64(native_link->target_triple)) {
        strncat(command, " -fPIE -pie", cap - strlen(command) - 1U);
    }
    for (i = 0; i < native_link->link_input_count; ++i) {
        char *quoted_input = v3_shell_quote(native_link->link_input_paths[i].text);
        strncat(command, " ", cap - strlen(command) - 1U);
        strncat(command, quoted_input, cap - strlen(command) - 1U);
        free(quoted_input);
    }
    {
        char *quoted_out = v3_shell_quote(output_path);
        strncat(command, " -o ", cap - strlen(command) - 1U);
        strncat(command, quoted_out, cap - strlen(command) - 1U);
        free(quoted_out);
    }
    if (v3_target_is_android_aarch64(native_link->target_triple)) {
        strncat(command,
                " -llog -ldl -lm -Wl,-z,max-page-size=16384 -Wl,-z,common-page-size=16384",
                cap - strlen(command) - 1U);
    } else if (v3_target_is_ohos_aarch64(native_link->target_triple)) {
        strncat(command, " -ldl -lm", cap - strlen(command) - 1U);
    }
    if (!v3_run_shell_command_status(command, "native-link")) {
        fprintf(stderr, "[cheng_v3_seed] native link failed command=%s\n", command);
        free(command);
        return false;
    }
    free(command);
    return true;
}

static bool v3_link_native_shared_library(const V3NativeLinkPlanStub *native_link,
                                          const char *output_path) {
    size_t i;
    size_t cap = PATH_MAX * (size_t)(native_link->link_input_count + 24U);
    char *command = NULL;
    const char *base_name = output_path;
    if (!v3_ensure_parent_dir(output_path)) {
        return false;
    }
    if (v3_target_is_wasm32_unknown_unknown(native_link->target_triple)) {
        if (native_link->primary_object_path[0] == '\0') {
            fprintf(stderr, "[cheng_v3_seed] wasm shared link missing primary module\n");
            return false;
        }
        return v3_copy_file_bytes(native_link->primary_object_path, output_path);
    }
    command = (char *)v3_xmalloc(cap);
    command[0] = '\0';
    {
        const char *cc = native_link->linker_program[0] != '\0' ? native_link->linker_program : "cc";
        char *quoted_cc = v3_shell_quote(cc);
        strncat(command, quoted_cc, cap - strlen(command) - 1U);
        free(quoted_cc);
    }
    if (v3_target_is_darwin_arm64(native_link->target_triple)) {
        strncat(command, " -dynamiclib -arch arm64", cap - strlen(command) - 1U);
    } else if (v3_target_is_elf_aarch64(native_link->target_triple)) {
        const char *slash = strrchr(output_path, '/');
        if (slash != NULL && slash[1] != '\0') {
            base_name = slash + 1;
        }
        strncat(command, " -shared -fPIC -Wl,-soname,", cap - strlen(command) - 1U);
        strncat(command, base_name, cap - strlen(command) - 1U);
    } else {
        strncat(command, " -shared", cap - strlen(command) - 1U);
    }
    for (i = 0; i < native_link->link_input_count; ++i) {
        char *quoted_input = v3_shell_quote(native_link->link_input_paths[i].text);
        strncat(command, " ", cap - strlen(command) - 1U);
        strncat(command, quoted_input, cap - strlen(command) - 1U);
        free(quoted_input);
    }
    {
        char *quoted_out = v3_shell_quote(output_path);
        strncat(command, " -o ", cap - strlen(command) - 1U);
        strncat(command, quoted_out, cap - strlen(command) - 1U);
        free(quoted_out);
    }
    if (v3_target_is_android_aarch64(native_link->target_triple)) {
        strncat(command,
                " -llog -ldl -lm -Wl,-z,max-page-size=16384 -Wl,-z,common-page-size=16384",
                cap - strlen(command) - 1U);
    } else if (v3_target_is_ohos_aarch64(native_link->target_triple)) {
        strncat(command, " -ldl -lm", cap - strlen(command) - 1U);
    }
    if (!v3_run_shell_command_status(command, "shared-native-link")) {
        fprintf(stderr, "[cheng_v3_seed] shared native link failed command=%s\n", command);
        free(command);
        return false;
    }
    free(command);
    return true;
}

static bool v3_collect_source_closure(V3SystemLinkPlanStub *plan,
                                      const char *workspace_root,
                                      const char *package_root,
                                      const char *package_id,
                                      const char *source_path,
                                      V3PlanPath *seen,
                                      size_t *seen_len) {
    char owner_module_path[PATH_MAX];
    char *text;
    V3PlanImportEdge *edges;
    size_t edge_len = 0U;
    size_t i;
    if (v3_plan_paths_contains(seen, *seen_len, source_path)) {
        return true;
    }
    if (!v3_path_exists_nonempty(source_path)) {
        fprintf(stderr, "[cheng_v3_seed] missing source: %s\n", source_path);
        return false;
    }
    if (*seen_len >= CHENG_V3_MAX_PLAN_PATHS) {
        fprintf(stderr, "[cheng_v3_seed] source closure too large\n");
        return false;
    }
    v3_plan_add_path(seen, seen_len, CHENG_V3_MAX_PLAN_PATHS, source_path);
    v3_plan_add_path(plan->source_closure_paths,
                     &plan->source_closure_count,
                     CHENG_V3_MAX_PLAN_PATHS,
                     source_path);
    text = v3_read_file(source_path);
    if (!text) {
        return false;
    }
    edges = (V3PlanImportEdge *)v3_xmalloc(sizeof(V3PlanImportEdge) * CHENG_V3_MAX_PLAN_PATHS);
    v3_source_path_to_module_path(workspace_root, package_root, package_id, source_path, owner_module_path, sizeof(owner_module_path));
    if (!v3_parse_import_edges(workspace_root,
                               package_root,
                               package_id,
                               owner_module_path,
                               text,
                               edges,
                               &edge_len,
                               CHENG_V3_MAX_PLAN_PATHS)) {
        free(text);
        free(edges);
        fprintf(stderr, "[cheng_v3_seed] malformed import in %s\n", source_path);
        return false;
    }
    free(text);
    for (i = 0; i < edge_len; ++i) {
        if (!edges[i].resolved) {
            plan->unresolved_import_count += 1U;
            continue;
        }
        if (plan->import_edge_count < CHENG_V3_MAX_PLAN_PATHS &&
            !v3_plan_import_edge_exists(plan, &edges[i])) {
            plan->import_edges[plan->import_edge_count] = edges[i];
            plan->import_edge_count += 1U;
        }
        if (!v3_collect_source_closure(plan,
                                       workspace_root,
                                       package_root,
                                       package_id,
                                       edges[i].target_source_path,
                                       seen,
                                       seen_len)) {
            free(edges);
            return false;
        }
    }
    free(edges);
    return true;
}

static bool v3_build_system_link_plan_stub(const V3BootstrapContract *contract,
                                           int argc,
                                           char **argv,
                                           bool require_output,
                                           V3SystemLinkPlanStub *plan) {
    const char *source_path = v3_flag_value(argc, argv, "--in");
    const char *out_path = v3_flag_value(argc, argv, "--out");
    const char *emit = v3_flag_value(argc, argv, "--emit");
    const char *target = v3_flag_value(argc, argv, "--target");
    const char *package_root_flag = v3_flag_value(argc, argv, "--root");
    const char *link_input_path = v3_flag_value(argc, argv, "--link-input");
    char seen_paths[1][1];
    V3PlanPath seen[CHENG_V3_MAX_PLAN_PATHS];
    size_t seen_len = 0U;
    char *text;
    V3PlanImportEdge direct_edges[CHENG_V3_MAX_PLAN_PATHS];
    size_t direct_edge_len = 0U;
    char owner_module_path[PATH_MAX];
    char compiler_entry[PATH_MAX];
    char last_component[PATH_MAX];
    const char *slash;
    memset(plan, 0, sizeof(*plan));
    (void)seen_paths;
    if (!source_path || *source_path == '\0') {
        fprintf(stderr, "[cheng_v3_seed] missing --in:<path>\n");
        return false;
    }
    if (require_output && (!out_path || *out_path == '\0')) {
        fprintf(stderr, "[cheng_v3_seed] missing --out:<path>\n");
        return false;
    }
    if (!emit || *emit == '\0') {
        emit = "exe";
    }
    if (!v3_streq(emit, "exe") &&
        !v3_streq(emit, "shared") &&
        !v3_streq(emit, "obj")) {
        fprintf(stderr, "[cheng_v3_seed] unsupported --emit (only exe/shared/obj)\n");
        return false;
    }
    if (!target || *target == '\0') {
        target = v3_contract_get(contract, "target");
    }
    v3_resolve_effective_package_root(contract,
                                      package_root_flag,
                                      source_path,
                                      plan->package_root,
                                      sizeof(plan->package_root));
    v3_workspace_root_from_package_root(plan->package_root, plan->workspace_root, sizeof(plan->workspace_root));
    v3_package_id_from_root(plan->package_root, plan->package_id, sizeof(plan->package_id));
    v3_copy_text(plan->entry_path, sizeof(plan->entry_path), source_path);
    v3_copy_text(plan->output_path, sizeof(plan->output_path), out_path ? out_path : "");
    v3_copy_text(plan->extra_link_input_path,
                 sizeof(plan->extra_link_input_path),
                 link_input_path != NULL ? link_input_path : "");
    v3_copy_text(plan->target_triple, sizeof(plan->target_triple), target);
    v3_copy_text(plan->emit_kind, sizeof(plan->emit_kind), emit);
    if (plan->extra_link_input_path[0] != '\0' &&
        !v3_path_exists_nonempty(plan->extra_link_input_path)) {
        fprintf(stderr, "[cheng_v3_seed] explicit link input missing: %s\n", plan->extra_link_input_path);
        return false;
    }
    if (!v3_target_supports_requested_emit(plan->target_triple, plan->emit_kind)) {
        v3_plan_add_reason(plan, "target_codegen_not_supported");
    }
    slash = strrchr(source_path, '/');
    snprintf(last_component, sizeof(last_component), "%s", slash ? slash + 1 : source_path);
    v3_strip_ext(last_component);
    v3_copy_text(plan->module_stem, sizeof(plan->module_stem), last_component);
    v3_copy_text(plan->module_kind, sizeof(plan->module_kind), "ordinary_program");
    if (!v3_path_exists_nonempty(source_path)) {
        fprintf(stderr, "[cheng_v3_seed] source file missing: %s\n", source_path);
        return false;
    }
    text = v3_read_file(source_path);
    if (!text) {
        return false;
    }
    plan->has_compiler_top_level = v3_source_has_top_level(text);
    plan->main_function_present = v3_source_has_main(text);
    if (v3_source_has_legacy_func_alias(text)) {
        v3_plan_add_reason(plan, "legacy_func_alias_unsupported");
    }
    if (plan->main_function_present) {
        v3_copy_text(plan->entry_symbol, sizeof(plan->entry_symbol), "main");
    }
    v3_source_path_to_module_path(plan->workspace_root,
                                  plan->package_root,
                                  plan->package_id,
                                  source_path,
                                  owner_module_path,
                                  sizeof(owner_module_path));
    if (v3_streq(owner_module_path, "cheng/v3/tooling/compiler_main")) {
        v3_copy_text(plan->module_kind, sizeof(plan->module_kind), "compiler_control_plane");
    }
    v3_plan_add_path(plan->owner_modules, &plan->owner_module_count, 8U, owner_module_path);
    v3_populate_runtime_requirements(plan);
    if (!v3_parse_import_edges(plan->workspace_root,
                               plan->package_root,
                               plan->package_id,
                               owner_module_path,
                               text,
                               direct_edges,
                               &direct_edge_len,
                               CHENG_V3_MAX_PLAN_PATHS)) {
        free(text);
        fprintf(stderr, "[cheng_v3_seed] malformed import in %s\n", source_path);
        return false;
    }
    free(text);
    (void)direct_edge_len;
    plan->import_edge_count = 0U;
    if (!v3_collect_source_closure(plan,
                                   plan->workspace_root,
                                   plan->package_root,
                                   plan->package_id,
                                   source_path,
                                   seen,
                                   &seen_len)) {
        return false;
    }
    if (plan->source_closure_count <= 0U) {
        v3_plan_add_reason(plan, "source_closure_missing");
    }
    if (plan->unresolved_import_count > 0U) {
        v3_plan_add_reason(plan, "source_closure_unresolved_imports");
    }
    if (!plan->has_compiler_top_level) {
        v3_plan_add_reason(plan, "compiler_top_level_missing");
    }
    if (v3_streq(plan->emit_kind, "exe") && plan->entry_symbol[0] == '\0') {
        v3_plan_add_reason(plan, "program_entry_symbol_missing");
    }
    if (v3_streq(plan->emit_kind, "exe") && plan->runtime_target_count <= 0U) {
        v3_plan_add_reason(plan, "runtime_targets_not_lowered");
    }
    if (v3_streq(plan->emit_kind, "exe") &&
        v3_target_requires_native_provider_modules(plan->target_triple) &&
        plan->provider_module_count <= 0U) {
        v3_plan_add_reason(plan, "runtime_provider_modules_not_selected");
    }
    v3_resolve_contract_path(contract, "compiler_entry_source", compiler_entry, sizeof(compiler_entry));
    (void)compiler_entry;
    return true;
}

static void v3_report_append(char **out, size_t *cap, size_t *used, const char *line) {
    size_t need = strlen(line) + 1U;
    if (*used + need + 1U >= *cap) {
        *cap *= 2U;
        *out = (char *)realloc(*out, *cap);
        if (!*out) {
            fprintf(stderr, "[cheng_v3_seed] out of memory while building report\n");
            exit(1);
        }
    }
    memcpy(*out + *used, line, need - 1U);
    *used += need - 1U;
    (*out)[(*used)++] = '\n';
    (*out)[*used] = '\0';
}

static void v3_report_append_raw(char **out, size_t *cap, size_t *used, const char *text) {
    size_t need = strlen(text);
    while (*used + need + 1U >= *cap) {
        *cap *= 2U;
        *out = (char *)realloc(*out, *cap);
        if (!*out) {
            fprintf(stderr, "[cheng_v3_seed] out of memory while building report\n");
            exit(1);
        }
    }
    memcpy(*out + *used, text, need);
    *used += need;
    (*out)[*used] = '\0';
}

static void v3_report_finish_line(char **out, size_t *cap, size_t *used) {
    while (*used + 2U >= *cap) {
        *cap *= 2U;
        *out = (char *)realloc(*out, *cap);
        if (!*out) {
            fprintf(stderr, "[cheng_v3_seed] out of memory while building report\n");
            exit(1);
        }
    }
    (*out)[(*used)++] = '\n';
    (*out)[*used] = '\0';
}

static void v3_report_append_csv_paths(char **out,
                                       size_t *cap,
                                       size_t *used,
                                       const char *label,
                                       const V3PlanPath *paths,
                                       size_t path_len) {
    size_t i;
    v3_report_append_raw(out, cap, used, label);
    v3_report_append_raw(out, cap, used, "=");
    if (path_len <= 0U) {
        v3_report_append_raw(out, cap, used, "-");
        v3_report_finish_line(out, cap, used);
        return;
    }
    for (i = 0; i < path_len; ++i) {
        if (i > 0U) {
            v3_report_append_raw(out, cap, used, ",");
        }
        v3_report_append_raw(out, cap, used, paths[i].text);
    }
    v3_report_finish_line(out, cap, used);
}

static void v3_report_append_csv_reasons(char **out,
                                         size_t *cap,
                                         size_t *used,
                                         const char *label,
                                         const char items[][128],
                                         size_t item_len) {
    size_t i;
    v3_report_append_raw(out, cap, used, label);
    v3_report_append_raw(out, cap, used, "=");
    if (item_len <= 0U) {
        v3_report_append_raw(out, cap, used, "-");
        v3_report_finish_line(out, cap, used);
        return;
    }
    for (i = 0; i < item_len; ++i) {
        if (i > 0U) {
            v3_report_append_raw(out, cap, used, ",");
        }
        v3_report_append_raw(out, cap, used, items[i]);
    }
    v3_report_finish_line(out, cap, used);
}

static void v3_report_append_csv_text256(char **out,
                                         size_t *cap,
                                         size_t *used,
                                         const char *label,
                                         const char items[][256],
                                         size_t item_len) {
    size_t i;
    v3_report_append_raw(out, cap, used, label);
    v3_report_append_raw(out, cap, used, "=");
    if (item_len <= 0U) {
        v3_report_append_raw(out, cap, used, "-");
        v3_report_finish_line(out, cap, used);
        return;
    }
    for (i = 0; i < item_len; ++i) {
        if (i > 0U) {
            v3_report_append_raw(out, cap, used, ",");
        }
        v3_report_append_raw(out, cap, used, items[i]);
    }
    v3_report_finish_line(out, cap, used);
}

static void v3_report_append_csv_functions(char **out,
                                           size_t *cap,
                                           size_t *used,
                                           const char *label,
                                           const V3LoweredFunctionStub *functions,
                                           size_t function_len) {
    size_t i;
    v3_report_append_raw(out, cap, used, label);
    v3_report_append_raw(out, cap, used, "=");
    if (function_len <= 0U) {
        v3_report_append_raw(out, cap, used, "-");
        v3_report_finish_line(out, cap, used);
        return;
    }
    for (i = 0; i < function_len; ++i) {
        if (i > 0U) {
            v3_report_append_raw(out, cap, used, ",");
        }
        v3_report_append_raw(out, cap, used, functions[i].symbol_text);
    }
    v3_report_finish_line(out, cap, used);
}

static void v3_report_append_csv_function_abi(char **out,
                                              size_t *cap,
                                              size_t *used,
                                              const char *label,
                                              const V3LoweredFunctionStub *functions,
                                              size_t function_len) {
    size_t i;
    char abi_summary[256];
    v3_report_append_raw(out, cap, used, label);
    v3_report_append_raw(out, cap, used, "=");
    if (function_len <= 0U) {
        v3_report_append_raw(out, cap, used, "-");
        v3_report_finish_line(out, cap, used);
        return;
    }
    for (i = 0; i < function_len; ++i) {
        if (i > 0U) {
            v3_report_append_raw(out, cap, used, ",");
        }
        v3_lowered_function_abi_summary(&functions[i], abi_summary, sizeof(abi_summary));
        v3_report_append_raw(out, cap, used, abi_summary);
    }
    v3_report_finish_line(out, cap, used);
}

static void v3_compiler_world_artifacts_free(V3CompilerWorldArtifacts *artifacts) {
    if (!artifacts) {
        return;
    }
    free(artifacts->exports);
    free(artifacts->csg_report);
    free(artifacts->surface_report);
    free(artifacts->compiler_snapshot_report);
    free(artifacts->std_snapshot_report);
    free(artifacts->runtime_snapshot_report);
    free(artifacts->world_report);
    free(artifacts->receipt_report);
    free(artifacts->lock_text);
    memset(artifacts, 0, sizeof(*artifacts));
}

static int32_t v3_plan_path_find_index(const V3PlanPath *items,
                                       size_t len,
                                       const char *text) {
    size_t i;
    for (i = 0U; i < len; ++i) {
        if (v3_streq(items[i].text, text)) {
            return (int32_t)i;
        }
    }
    return -1;
}

static void v3_plan_path_add_unique(V3PlanPath *items,
                                    size_t *len,
                                    size_t cap,
                                    const char *text) {
    if (v3_plan_path_find_index(items, *len, text) >= 0) {
        return;
    }
    v3_plan_add_path(items, len, cap, text);
}

static bool v3_compiler_symbol_exported(const char *function_name,
                                        int32_t entry_flag) {
    (void)entry_flag;
    if (!function_name || function_name[0] == '\0') {
        return false;
    }
    return function_name[0] >= 'A' && function_name[0] <= 'Z';
}

static bool v3_compiler_world_should_export_symbol(const V3SystemLinkPlanStub *plan,
                                                   int32_t entry_source_flag,
                                                   bool entry_has_explicit_exports,
                                                   const char *explicit_export_symbol,
                                                   const char *function_name,
                                                   int32_t entry_flag) {
    bool shared_like;
    bool has_explicit_export;
    if (!plan || !function_name) {
        return false;
    }
    has_explicit_export = explicit_export_symbol != NULL && explicit_export_symbol[0] != '\0';
    shared_like = v3_streq(plan->emit_kind, "shared") || v3_streq(plan->emit_kind, "obj");
    if (!shared_like) {
        if (has_explicit_export) {
            return true;
        }
        return v3_compiler_symbol_exported(function_name, entry_flag);
    }
    if (entry_source_flag == 0) {
        return false;
    }
    if (entry_has_explicit_exports) {
        return has_explicit_export;
    }
    return true;
}

static void v3_compiler_target_export_name(const char *symbol_text,
                                           const char *explicit_export_symbol,
                                           const char *function_name,
                                           char *out,
                                           size_t cap) {
    const char *tail;
    if (out != NULL && cap > 0U) {
        out[0] = '\0';
    }
    if (out == NULL || cap == 0U) {
        return;
    }
    if (explicit_export_symbol != NULL && explicit_export_symbol[0] != '\0') {
        v3_copy_text(out, cap, explicit_export_symbol);
        return;
    }
    if (function_name != NULL && function_name[0] != '\0') {
        v3_copy_text(out, cap, function_name);
        return;
    }
    tail = symbol_text != NULL ? strstr(symbol_text, "::") : NULL;
    if (tail != NULL && tail[2] != '\0') {
        v3_copy_text(out, cap, tail + 2);
        return;
    }
    v3_copy_text(out, cap, symbol_text ? symbol_text : "");
}

static V3Cid32 v3_compiler_hash_node(int32_t node_kind,
                                     int32_t owner_node_id,
                                     const char *module_path,
                                     const char *symbol_text,
                                     const char *fact_text,
                                     int32_t result_layout_kind,
                                     int32_t effect_kind,
                                     int32_t capability_kind,
                                     int32_t call_conv,
                                     int32_t entry_flag,
                                     int32_t exported_flag,
                                     const char *export_symbol_name) {
    V3ByteBuf buf;
    V3Cid32 out;
    v3_bytebuf_init(&buf, 256U);
    v3_bytebuf_append_text(&buf, "v3.compiler.csg.node");
    v3_bytebuf_append_u32_be(&buf, (uint32_t)node_kind);
    v3_bytebuf_append_u32_be(&buf, (uint32_t)owner_node_id);
    v3_bytebuf_append_text(&buf, module_path ? module_path : "");
    v3_bytebuf_append_text(&buf, symbol_text ? symbol_text : "");
    v3_bytebuf_append_text(&buf, fact_text ? fact_text : "");
    v3_bytebuf_append_u32_be(&buf, (uint32_t)result_layout_kind);
    v3_bytebuf_append_u32_be(&buf, (uint32_t)effect_kind);
    v3_bytebuf_append_u32_be(&buf, (uint32_t)capability_kind);
    v3_bytebuf_append_u32_be(&buf, (uint32_t)call_conv);
    v3_bytebuf_append_u32_be(&buf, (uint32_t)entry_flag);
    v3_bytebuf_append_u32_be(&buf, (uint32_t)exported_flag);
    v3_bytebuf_append_text(&buf, export_symbol_name ? export_symbol_name : "");
    out = v3_cid_from_bytebuf(&buf);
    v3_bytebuf_free(&buf);
    return out;
}

static V3Cid32 v3_compiler_hash_edge(int32_t from_node_id,
                                     int32_t to_node_id,
                                     int32_t edge_kind) {
    V3ByteBuf buf;
    V3Cid32 out;
    v3_bytebuf_init(&buf, 96U);
    v3_bytebuf_append_text(&buf, "v3.compiler.csg.edge");
    v3_bytebuf_append_u32_be(&buf, (uint32_t)from_node_id);
    v3_bytebuf_append_u32_be(&buf, (uint32_t)to_node_id);
    v3_bytebuf_append_u32_be(&buf, (uint32_t)edge_kind);
    out = v3_cid_from_bytebuf(&buf);
    v3_bytebuf_free(&buf);
    return out;
}

static void v3_compiler_csg_append_node(V3ByteBuf *graph_buf,
                                        char **node_report,
                                        size_t *node_report_cap,
                                        size_t *node_report_used,
                                        size_t *node_index,
                                        int32_t node_id,
                                        int32_t node_kind,
                                        int32_t owner_node_id,
                                        const char *module_path,
                                        const char *symbol_text,
                                        const char *fact_text,
                                        int32_t result_layout_kind,
                                        int32_t effect_kind,
                                        int32_t capability_kind,
                                        int32_t call_conv,
                                        int32_t entry_flag,
                                        int32_t exported_flag,
                                        const char *export_symbol_name) {
    char line[PATH_MAX * 4];
    V3Cid32 fact_cid = v3_compiler_hash_node(node_kind,
                                             owner_node_id,
                                             module_path,
                                             symbol_text,
                                             fact_text,
                                             result_layout_kind,
                                             effect_kind,
                                             capability_kind,
                                             call_conv,
                                             entry_flag,
                                             exported_flag,
                                             export_symbol_name);
    v3_bytebuf_append_u32_be(graph_buf, (uint32_t)node_id);
    v3_bytebuf_append_u32_be(graph_buf, (uint32_t)node_kind);
    v3_bytebuf_append_u32_be(graph_buf, (uint32_t)owner_node_id);
    v3_bytebuf_append_text(graph_buf, module_path ? module_path : "");
    v3_bytebuf_append_text(graph_buf, symbol_text ? symbol_text : "");
    v3_bytebuf_append_text(graph_buf, fact_text ? fact_text : "");
    v3_bytebuf_append_u32_be(graph_buf, (uint32_t)result_layout_kind);
    v3_bytebuf_append_u32_be(graph_buf, (uint32_t)effect_kind);
    v3_bytebuf_append_u32_be(graph_buf, (uint32_t)capability_kind);
    v3_bytebuf_append_u32_be(graph_buf, (uint32_t)call_conv);
    v3_bytebuf_append_u32_be(graph_buf, (uint32_t)entry_flag);
    v3_bytebuf_append_u32_be(graph_buf, (uint32_t)exported_flag);
    v3_bytebuf_append_text(graph_buf, export_symbol_name ? export_symbol_name : "");
    v3_bytebuf_append_cid(graph_buf, fact_cid);
    if (node_report && node_report_cap && node_report_used && node_index) {
        snprintf(line,
                 sizeof(line),
                 "node[%zu]=%d|%d|%s|%s|%s|%d|%d|%d|%d|%d|%d|%s|%s",
                 *node_index,
                 node_kind,
                 owner_node_id,
                 module_path ? module_path : "",
                 symbol_text ? symbol_text : "",
                 fact_text ? fact_text : "",
                 result_layout_kind,
                 effect_kind,
                 capability_kind,
                 call_conv,
                 entry_flag,
                 exported_flag,
                 export_symbol_name ? export_symbol_name : "",
                 fact_cid.hex);
        v3_report_append(node_report, node_report_cap, node_report_used, line);
        *node_index += 1U;
    }
}

static void v3_compiler_csg_append_edge(V3ByteBuf *edge_buf,
                                        char **edge_report,
                                        size_t *edge_report_cap,
                                        size_t *edge_report_used,
                                        size_t *edge_index,
                                        int32_t edge_id,
                                        int32_t from_node_id,
                                        int32_t to_node_id,
                                        int32_t edge_kind) {
    char line[512];
    V3Cid32 edge_cid = v3_compiler_hash_edge(from_node_id, to_node_id, edge_kind);
    v3_bytebuf_append_u32_be(edge_buf, (uint32_t)edge_id);
    v3_bytebuf_append_u32_be(edge_buf, (uint32_t)from_node_id);
    v3_bytebuf_append_u32_be(edge_buf, (uint32_t)to_node_id);
    v3_bytebuf_append_u32_be(edge_buf, (uint32_t)edge_kind);
    v3_bytebuf_append_cid(edge_buf, edge_cid);
    if (edge_report && edge_report_cap && edge_report_used && edge_index) {
        snprintf(line,
                 sizeof(line),
                 "edge[%zu]=%d|%d|%d|%s",
                 *edge_index,
                 edge_kind,
                 from_node_id,
                 to_node_id,
                 edge_cid.hex);
        v3_report_append(edge_report, edge_report_cap, edge_report_used, line);
        *edge_index += 1U;
    }
}

static bool CHENG_V3_UNUSED v3_compiler_source_bundle_cid(const V3SystemLinkPlanStub *plan,
                                                          V3Cid32 *out) {
    return v3_compiler_source_bundle_cid_with_overrides(plan, NULL, 0U, out);
}

static bool v3_compiler_source_text_lookup(const V3CompilerSourceOverride *overrides,
                                           size_t override_count,
                                           const char *source_path,
                                           char **text_out) {
    size_t i;
    for (i = 0U; i < override_count; ++i) {
        if (!v3_streq(overrides[i].source_path, source_path)) {
            continue;
        }
        *text_out = overrides[i].text ? overrides[i].text : "";
        return true;
    }
    return false;
}

static void v3_compiler_source_module_path(const V3SystemLinkPlanStub *plan,
                                           const V3CompilerSourceOverride *overrides,
                                           size_t override_count,
                                           const char *source_path,
                                           char *out,
                                           size_t cap) {
    size_t i;
    for (i = 0U; i < override_count; ++i) {
        if (!v3_streq(overrides[i].source_path, source_path)) {
            continue;
        }
        if (overrides[i].module_path[0] != '\0') {
            v3_copy_text(out, cap, overrides[i].module_path);
            return;
        }
    }
    v3_source_path_to_module_path(plan->workspace_root,
                                  plan->package_root,
                                  plan->package_id,
                                  source_path,
                                  out,
                                  cap);
}

static bool v3_compiler_source_bundle_cid_from_texts(const char *package_id,
                                                     const V3PlanPath *source_paths,
                                                     char *const *texts,
                                                     size_t source_count,
                                                     V3Cid32 *out) {
    V3ByteBuf buf;
    size_t i;
    v3_bytebuf_init(&buf, 512U);
    v3_bytebuf_append_text(&buf, "v3.compiler.source_bundle");
    v3_bytebuf_append_text(&buf, package_id);
    v3_bytebuf_append_u32_be(&buf, (uint32_t)source_count);
    for (i = 0U; i < source_count; ++i) {
        v3_bytebuf_append_text(&buf, source_paths[i].text);
        v3_bytebuf_append_text(&buf, texts[i] ? texts[i] : "");
    }
    *out = v3_cid_from_bytebuf(&buf);
    v3_bytebuf_free(&buf);
    return true;
}

static bool v3_compiler_source_bundle_cid_with_overrides(const V3SystemLinkPlanStub *plan,
                                                         const V3CompilerSourceOverride *overrides,
                                                         size_t override_count,
                                                         V3Cid32 *out) {
    V3ByteBuf buf;
    size_t i;
    v3_bytebuf_init(&buf, 512U);
    v3_bytebuf_append_text(&buf, "v3.compiler.source_bundle");
    v3_bytebuf_append_text(&buf, plan->package_id);
    v3_bytebuf_append_u32_be(&buf, (uint32_t)plan->source_closure_count);
    for (i = 0U; i < plan->source_closure_count; ++i) {
        char *owned = NULL;
        char *text = NULL;
        if (!v3_compiler_source_text_lookup(overrides,
                                            override_count,
                                            plan->source_closure_paths[i].text,
                                            &text)) {
            owned = v3_read_file(plan->source_closure_paths[i].text);
            if (!owned) {
                v3_bytebuf_free(&buf);
                return false;
            }
            text = owned;
        }
        v3_bytebuf_append_text(&buf, plan->source_closure_paths[i].text);
        v3_bytebuf_append_text(&buf, text ? text : "");
        free(owned);
    }
    *out = v3_cid_from_bytebuf(&buf);
    v3_bytebuf_free(&buf);
    return true;
}

static V3Cid32 v3_compiler_package_snapshot_cid(const char *package_id,
                                                const char *channel,
                                                V3Cid32 source_bundle_cid,
                                                V3Cid32 csg_root_cid,
                                                V3Cid32 surface_cid,
                                                const char *signature) {
    V3ByteBuf buf;
    V3Cid32 out;
    v3_bytebuf_init(&buf, 256U);
    v3_bytebuf_append_text(&buf, "v3.compiler.package_snapshot");
    v3_bytebuf_append_text(&buf, package_id);
    v3_bytebuf_append_text(&buf, channel);
    v3_bytebuf_append_cid(&buf, source_bundle_cid);
    v3_bytebuf_append_cid(&buf, csg_root_cid);
    v3_bytebuf_append_cid(&buf, surface_cid);
    v3_bytebuf_append_text(&buf, signature);
    out = v3_cid_from_bytebuf(&buf);
    v3_bytebuf_free(&buf);
    return out;
}

static V3Cid32 v3_compiler_manifest_root_cid(V3Cid32 compiler_pkg_cid,
                                             V3Cid32 std_pkg_cid,
                                             V3Cid32 runtime_pkg_cid) {
    V3ByteBuf buf;
    V3Cid32 out;
    v3_bytebuf_init(&buf, 192U);
    v3_bytebuf_append_text(&buf, "v3.compiler.manifest_root");
    v3_bytebuf_append_cid(&buf, compiler_pkg_cid);
    v3_bytebuf_append_cid(&buf, std_pkg_cid);
    v3_bytebuf_append_cid(&buf, runtime_pkg_cid);
    out = v3_cid_from_bytebuf(&buf);
    v3_bytebuf_free(&buf);
    return out;
}

static V3Cid32 v3_compiler_world_head_cid(const char *universe_id,
                                          const char *channel,
                                          int32_t head_epoch,
                                          V3Cid32 prev_head_cid,
                                          V3Cid32 manifest_root_cid,
                                          V3Cid32 csg_root_cid,
                                          V3Cid32 compiler_pkg_cid,
                                          V3Cid32 std_pkg_cid,
                                          V3Cid32 runtime_pkg_cid,
                                          const char *signature) {
    V3ByteBuf buf;
    V3Cid32 out;
    v3_bytebuf_init(&buf, 256U);
    v3_bytebuf_append_text(&buf, "v3.compiler.world_head");
    v3_bytebuf_append_text(&buf, universe_id);
    v3_bytebuf_append_text(&buf, channel);
    v3_bytebuf_append_u32_be(&buf, (uint32_t)head_epoch);
    v3_bytebuf_append_cid(&buf, prev_head_cid);
    v3_bytebuf_append_cid(&buf, manifest_root_cid);
    v3_bytebuf_append_cid(&buf, csg_root_cid);
    v3_bytebuf_append_cid(&buf, compiler_pkg_cid);
    v3_bytebuf_append_cid(&buf, std_pkg_cid);
    v3_bytebuf_append_cid(&buf, runtime_pkg_cid);
    v3_bytebuf_append_text(&buf, signature);
    out = v3_cid_from_bytebuf(&buf);
    v3_bytebuf_free(&buf);
    return out;
}

static V3Cid32 v3_compiler_output_digest_cid(const V3SystemLinkPlanStub *plan,
                                             V3Cid32 csg_cid) {
    V3ByteBuf buf;
    V3Cid32 out;
    size_t i;
    v3_bytebuf_init(&buf, 256U);
    v3_bytebuf_append_text(&buf, "v3.compiler.output_digest");
    v3_bytebuf_append_text(&buf, plan->target_triple);
    v3_bytebuf_append_cid(&buf, csg_cid);
    v3_bytebuf_append_u32_be(&buf, (uint32_t)plan->provider_module_count);
    for (i = 0U; i < plan->provider_module_count; ++i) {
        v3_bytebuf_append_text(&buf, plan->provider_modules[i].text);
    }
    out = v3_cid_from_bytebuf(&buf);
    v3_bytebuf_free(&buf);
    return out;
}

static V3Cid32 v3_compiler_receipt_cid(V3Cid32 world_head_cid,
                                       V3Cid32 compiler_csg_cid,
                                       V3Cid32 canonical_compiler_csg_cid,
                                       const V3SystemLinkPlanStub *plan,
                                       V3Cid32 output_digest_cid,
                                       const char *bootstrap_stage,
                                       V3Cid32 canonical_output_digest_cid) {
    V3ByteBuf buf;
    V3Cid32 out;
    size_t i;
    v3_bytebuf_init(&buf, 256U);
    v3_bytebuf_append_text(&buf, "v3.compiler.compile_receipt");
    v3_bytebuf_append_cid(&buf, world_head_cid);
    v3_bytebuf_append_cid(&buf, compiler_csg_cid);
    v3_bytebuf_append_cid(&buf, canonical_compiler_csg_cid);
    v3_bytebuf_append_text(&buf, plan->target_triple);
    v3_bytebuf_append_cid(&buf, output_digest_cid);
    v3_bytebuf_append_cid(&buf, canonical_output_digest_cid);
    v3_bytebuf_append_text(&buf, bootstrap_stage ? bootstrap_stage : "stage3_local");
    v3_bytebuf_append_u32_be(&buf, (uint32_t)plan->provider_module_count);
    for (i = 0U; i < plan->provider_module_count; ++i) {
        v3_bytebuf_append_text(&buf, plan->provider_modules[i].text);
    }
    out = v3_cid_from_bytebuf(&buf);
    v3_bytebuf_free(&buf);
    return out;
}

static char *v3_compiler_package_snapshot_report(const char *package_id,
                                                 const char *channel,
                                                 V3Cid32 source_bundle_cid,
                                                 V3Cid32 csg_root_cid,
                                                 V3Cid32 surface_cid,
                                                 const char *signature,
                                                 V3Cid32 snapshot_cid) {
    char *out = (char *)v3_xmalloc(4096U);
    snprintf(out,
             4096U,
             "version=1\npackage_id=%s\nchannel=%s\nsource_bundle_cid=%s\ncsg_root_cid=%s\nexport_surface_cid=%s\nsignature=%s\nsnapshot_cid=%s",
             package_id,
             channel,
             source_bundle_cid.hex,
             csg_root_cid.hex,
             surface_cid.hex,
             signature,
             snapshot_cid.hex);
    return out;
}

static char *v3_compiler_world_head_report(const char *channel,
                                           V3Cid32 manifest_root_cid,
                                           V3Cid32 world_head_cid,
                                           V3Cid32 csg_root_cid,
                                           V3Cid32 compiler_pkg_cid,
                                           V3Cid32 std_pkg_cid,
                                           V3Cid32 runtime_pkg_cid) {
    char *out = (char *)v3_xmalloc(4096U);
    snprintf(out,
             4096U,
             "version=1\nuniverse_id=cheng/v3/compiler_universe\nchannel=%s\nhead_epoch=1\nprev_head_cid=%s\nmanifest_root_cid=%s\ncsg_root_cid=%s\ncompiler_pkg_cid=%s\nstd_pkg_cid=%s\nruntime_pkg_cid=%s\nsignature=local:world_head\nhead_cid=%s",
             channel,
             v3_cid_zero().hex,
             manifest_root_cid.hex,
             csg_root_cid.hex,
             compiler_pkg_cid.hex,
             std_pkg_cid.hex,
             runtime_pkg_cid.hex,
             world_head_cid.hex);
    return out;
}

static char *v3_compiler_receipt_report(V3Cid32 world_head_cid,
                                        V3Cid32 csg_cid,
                                        V3Cid32 canonical_csg_cid,
                                        const V3SystemLinkPlanStub *plan,
                                        V3Cid32 output_digest_cid,
                                        const char *bootstrap_stage,
                                        V3Cid32 canonical_output_digest_cid,
                                        V3Cid32 receipt_cid) {
    size_t i;
    size_t cap = 4096U + plan->provider_module_count * PATH_MAX;
    size_t used = 0U;
    char *out = (char *)v3_xmalloc(cap);
    char line[PATH_MAX * 2];
    out[0] = '\0';
    snprintf(line, sizeof(line), "version=1");
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "world_head_cid=%s", world_head_cid.hex);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "compiler_csg_cid=%s", csg_cid.hex);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "canonical_compiler_csg_cid=%s", canonical_csg_cid.hex);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "target=%s", plan->target_triple);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "output_digest=%s", output_digest_cid.hex);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "canonical_output_digest=%s", canonical_output_digest_cid.hex);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "bootstrap_stage=%s", bootstrap_stage ? bootstrap_stage : "stage3_local");
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "runtime_provider_count=%zu", plan->provider_module_count);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "receipt_cid=%s", receipt_cid.hex);
    v3_report_append(&out, &cap, &used, line);
    for (i = 0U; i < plan->provider_module_count; ++i) {
        snprintf(line, sizeof(line), "runtime_provider[%zu]=%s", i, plan->provider_modules[i].text);
        v3_report_append(&out, &cap, &used, line);
    }
    return out;
}

static char *v3_compiler_lock_text(V3Cid32 world_head_cid,
                                   V3Cid32 compiler_pkg_cid,
                                   V3Cid32 std_pkg_cid,
                                   V3Cid32 runtime_pkg_cid) {
    char *out = (char *)v3_xmalloc(1024U);
    snprintf(out,
             1024U,
             "world_head_cid = \"%s\"\ncompiler_pkg_cid = \"%s\"\nstd_pkg_cid = \"%s\"\nruntime_pkg_cid = \"%s\"\n",
             world_head_cid.hex,
             compiler_pkg_cid.hex,
             std_pkg_cid.hex,
             runtime_pkg_cid.hex);
    return out;
}

static char *v3_trimmed_text_dup(const char *text) {
    char *copy;
    char *trimmed;
    if (!text) {
        return v3_strdup("");
    }
    copy = v3_strdup(text);
    trimmed = v3_trim_inplace(copy);
    if (trimmed != copy) {
        char *owned = v3_strdup(trimmed);
        free(copy);
        return owned;
    }
    return copy;
}

static bool v3_trimmed_file_equals(const char *path, const char *expected) {
    char *file_text = v3_read_file(path);
    char *file_trimmed;
    char *expected_trimmed;
    bool ok;
    if (!file_text) {
        return false;
    }
    file_trimmed = v3_trimmed_text_dup(file_text);
    expected_trimmed = v3_trimmed_text_dup(expected);
    ok = v3_streq(file_trimmed, expected_trimmed);
    free(file_text);
    free(file_trimmed);
    free(expected_trimmed);
    return ok;
}

static bool v3_report_line_value(const char *text,
                                 const char *key,
                                 char *out,
                                 size_t out_cap) {
    char *copy;
    char *cursor;
    char *line;
    if (!text) {
        out[0] = '\0';
        return false;
    }
    copy = v3_strdup(text);
    cursor = copy;
    while ((line = strsep(&cursor, "\n")) != NULL) {
        char *eq;
        char *line_key;
        char *line_value;
        line = v3_trim_inplace(line);
        if (*line == '\0') {
            continue;
        }
        eq = strchr(line, '=');
        if (!eq) {
            continue;
        }
        *eq = '\0';
        line_key = v3_trim_inplace(line);
        line_value = v3_trim_inplace(eq + 1);
        if (!v3_streq(line_key, key)) {
            continue;
        }
        v3_copy_text(out, out_cap, line_value);
        free(copy);
        return true;
    }
    free(copy);
    out[0] = '\0';
    return false;
}

static bool v3_parse_i32_text(const char *text, int32_t *out) {
    char *end = NULL;
    long value;
    if (!text || *text == '\0') {
        return false;
    }
    value = strtol(text, &end, 10);
    if (!end || *v3_trim_inplace(end) != '\0') {
        return false;
    }
    *out = (int32_t)value;
    return true;
}

static bool v3_report_collect_prefixed_values(const char *text,
                                              const char *prefix,
                                              V3PlanPath *items,
                                              size_t *count,
                                              size_t cap) {
    char *copy;
    char *cursor;
    char *line;
    *count = 0U;
    if (!text) {
        return false;
    }
    copy = v3_strdup(text);
    cursor = copy;
    while ((line = strsep(&cursor, "\n")) != NULL) {
        char *eq;
        char *value;
        line = v3_trim_inplace(line);
        if (!v3_startswith(line, prefix)) {
            continue;
        }
        eq = strchr(line, '=');
        if (!eq) {
            free(copy);
            return false;
        }
        value = v3_trim_inplace(eq + 1);
        if (*count >= cap) {
            free(copy);
            return false;
        }
        v3_copy_text(items[*count].text, sizeof(items[*count].text), value);
        *count += 1U;
    }
    free(copy);
    return true;
}

static bool v3_report_values_equal(const V3PlanPath *left,
                                   size_t left_count,
                                   const V3PlanPath *right,
                                   size_t right_count) {
    size_t i;
    if (left_count != right_count) {
        return false;
    }
    for (i = 0U; i < left_count; ++i) {
        if (!v3_streq(left[i].text, right[i].text)) {
            return false;
        }
    }
    return true;
}

static bool v3_compiler_equivalence_graph_compare_reports(const char *baseline_text,
                                                          const char *migrated_text,
                                                          V3Cid32 *baseline_cid,
                                                          V3Cid32 *migrated_cid,
                                                          int32_t *out_equivalent) {
    char value[CHENG_V3_CID_HEX_CAP];
    if ((!v3_report_line_value(baseline_text, "canonical_graph_cid", value, sizeof(value)) &&
         !v3_report_line_value(baseline_text, "graph_cid", value, sizeof(value))) ||
        !v3_cid_from_hex_text(value, baseline_cid)) {
        fprintf(stderr, "v3 compiler equivalence: invalid baseline graph_cid\n");
        return false;
    }
    if ((!v3_report_line_value(migrated_text, "canonical_graph_cid", value, sizeof(value)) &&
         !v3_report_line_value(migrated_text, "graph_cid", value, sizeof(value))) ||
        !v3_cid_from_hex_text(value, migrated_cid)) {
        fprintf(stderr, "v3 compiler equivalence: invalid migrated graph_cid\n");
        return false;
    }
    *out_equivalent = v3_cid_equal(*baseline_cid, *migrated_cid) ? 1 : 0;
    return true;
}

static bool v3_compiler_equivalence_surface_compare_reports(const char *baseline_text,
                                                            const char *migrated_text,
                                                            V3Cid32 *baseline_cid,
                                                            V3Cid32 *migrated_cid,
                                                            int32_t *out_compatible) {
    char base_package[PATH_MAX];
    char next_package[PATH_MAX];
    char count_text[64];
    char cid_text[CHENG_V3_CID_HEX_CAP];
    int32_t base_count = 0;
    int32_t next_count = 0;
    V3PlanPath base_exports[CHENG_V3_MAX_TYPE_FIELDS];
    V3PlanPath next_exports[CHENG_V3_MAX_TYPE_FIELDS];
    size_t base_export_count = 0U;
    size_t next_export_count = 0U;
    if (!v3_report_line_value(baseline_text, "package_id", base_package, sizeof(base_package)) ||
        !v3_report_line_value(migrated_text, "package_id", next_package, sizeof(next_package))) {
        fprintf(stderr, "v3 compiler equivalence: surface package_id missing\n");
        return false;
    }
    if (!v3_report_line_value(baseline_text, "surface_cid", cid_text, sizeof(cid_text)) ||
        !v3_cid_from_hex_text(cid_text, baseline_cid)) {
        fprintf(stderr, "v3 compiler equivalence: invalid baseline surface_cid\n");
        return false;
    }
    if (!v3_report_line_value(migrated_text, "surface_cid", cid_text, sizeof(cid_text)) ||
        !v3_cid_from_hex_text(cid_text, migrated_cid)) {
        fprintf(stderr, "v3 compiler equivalence: invalid migrated surface_cid\n");
        return false;
    }
    if (!v3_report_line_value(baseline_text, "exported_count", count_text, sizeof(count_text)) ||
        !v3_parse_i32_text(count_text, &base_count)) {
        fprintf(stderr, "v3 compiler equivalence: invalid baseline exported_count\n");
        return false;
    }
    if (!v3_report_line_value(migrated_text, "exported_count", count_text, sizeof(count_text)) ||
        !v3_parse_i32_text(count_text, &next_count)) {
        fprintf(stderr, "v3 compiler equivalence: invalid migrated exported_count\n");
        return false;
    }
    if (!v3_report_collect_prefixed_values(baseline_text, "export[", base_exports, &base_export_count, CHENG_V3_MAX_TYPE_FIELDS) ||
        !v3_report_collect_prefixed_values(migrated_text, "export[", next_exports, &next_export_count, CHENG_V3_MAX_TYPE_FIELDS)) {
        fprintf(stderr, "v3 compiler equivalence: malformed export rows\n");
        return false;
    }
    *out_compatible = (v3_streq(base_package, next_package) &&
                       base_count == next_count &&
                       (size_t)base_count == base_export_count &&
                       (size_t)next_count == next_export_count &&
                       v3_report_values_equal(base_exports, base_export_count, next_exports, next_export_count)) ? 1 : 0;
    return true;
}

static bool v3_compiler_equivalence_receipt_compare_reports(const char *baseline_text,
                                                            const char *migrated_text,
                                                            V3Cid32 *baseline_cid,
                                                            V3Cid32 *migrated_cid,
                                                            char *target_out,
                                                            size_t target_cap,
                                                            int32_t *out_equivalent) {
    char base_csg[CHENG_V3_CID_HEX_CAP];
    char next_csg[CHENG_V3_CID_HEX_CAP];
    char base_digest[CHENG_V3_CID_HEX_CAP];
    char next_digest[CHENG_V3_CID_HEX_CAP];
    char base_stage[128];
    char next_stage[128];
    char base_target[128];
    char next_target[128];
    V3Cid32 base_csg_cid;
    V3Cid32 next_csg_cid;
    V3Cid32 base_digest_cid;
    V3Cid32 next_digest_cid;
    V3PlanPath base_providers[CHENG_V3_MAX_CALL_ARGS];
    V3PlanPath next_providers[CHENG_V3_MAX_CALL_ARGS];
    size_t base_provider_count = 0U;
    size_t next_provider_count = 0U;
    char value[CHENG_V3_CID_HEX_CAP];
    if (!v3_report_line_value(baseline_text, "receipt_cid", value, sizeof(value)) ||
        !v3_cid_from_hex_text(value, baseline_cid)) {
        fprintf(stderr, "v3 compiler equivalence: invalid baseline receipt_cid\n");
        return false;
    }
    if (!v3_report_line_value(migrated_text, "receipt_cid", value, sizeof(value)) ||
        !v3_cid_from_hex_text(value, migrated_cid)) {
        fprintf(stderr, "v3 compiler equivalence: invalid migrated receipt_cid\n");
        return false;
    }
    if ((!v3_report_line_value(baseline_text, "canonical_compiler_csg_cid", base_csg, sizeof(base_csg)) &&
         !v3_report_line_value(baseline_text, "compiler_csg_cid", base_csg, sizeof(base_csg))) ||
        (!v3_report_line_value(migrated_text, "canonical_compiler_csg_cid", next_csg, sizeof(next_csg)) &&
         !v3_report_line_value(migrated_text, "compiler_csg_cid", next_csg, sizeof(next_csg))) ||
        !v3_cid_from_hex_text(base_csg, &base_csg_cid) ||
        !v3_cid_from_hex_text(next_csg, &next_csg_cid)) {
        fprintf(stderr, "v3 compiler equivalence: invalid compiler_csg_cid\n");
        return false;
    }
    if ((!v3_report_line_value(baseline_text, "canonical_output_digest", base_digest, sizeof(base_digest)) &&
         !v3_report_line_value(baseline_text, "output_digest", base_digest, sizeof(base_digest))) ||
        (!v3_report_line_value(migrated_text, "canonical_output_digest", next_digest, sizeof(next_digest)) &&
         !v3_report_line_value(migrated_text, "output_digest", next_digest, sizeof(next_digest))) ||
        !v3_cid_from_hex_text(base_digest, &base_digest_cid) ||
        !v3_cid_from_hex_text(next_digest, &next_digest_cid)) {
        fprintf(stderr, "v3 compiler equivalence: invalid output_digest\n");
        return false;
    }
    if (!v3_report_line_value(baseline_text, "bootstrap_stage", base_stage, sizeof(base_stage)) ||
        !v3_report_line_value(migrated_text, "bootstrap_stage", next_stage, sizeof(next_stage)) ||
        !v3_report_line_value(baseline_text, "target", base_target, sizeof(base_target)) ||
        !v3_report_line_value(migrated_text, "target", next_target, sizeof(next_target))) {
        fprintf(stderr, "v3 compiler equivalence: receipt fields missing\n");
        return false;
    }
    if (!v3_report_collect_prefixed_values(baseline_text, "runtime_provider[", base_providers, &base_provider_count, CHENG_V3_MAX_CALL_ARGS) ||
        !v3_report_collect_prefixed_values(migrated_text, "runtime_provider[", next_providers, &next_provider_count, CHENG_V3_MAX_CALL_ARGS)) {
        fprintf(stderr, "v3 compiler equivalence: malformed runtime_provider rows\n");
        return false;
    }
    v3_copy_text(target_out, target_cap, next_target);
    *out_equivalent = (v3_cid_equal(base_csg_cid, next_csg_cid) &&
                       v3_streq(base_target, next_target) &&
                       v3_cid_equal(base_digest_cid, next_digest_cid) &&
                       v3_streq(base_stage, next_stage) &&
                       v3_report_values_equal(base_providers, base_provider_count, next_providers, next_provider_count)) ? 1 : 0;
    return true;
}

static const char *v3_compiler_equivalence_kind(int32_t graph_equivalent,
                                                int32_t export_surface_compatible,
                                                int32_t compile_receipt_equivalent) {
    if (graph_equivalent != 0 &&
        export_surface_compatible != 0 &&
        compile_receipt_equivalent != 0) {
        return "canonical_semantic_identity";
    }
    return "incompatible";
}

static bool v3_compiler_equivalence_proof_make(const char *package_id,
                                               const char *channel_raw,
                                               V3Cid32 baseline_graph_cid,
                                               V3Cid32 migrated_graph_cid,
                                               V3Cid32 baseline_surface_cid,
                                               V3Cid32 migrated_surface_cid,
                                               V3Cid32 baseline_receipt_cid,
                                               V3Cid32 migrated_receipt_cid,
                                               const char *target_triple,
                                               int32_t graph_equivalent,
                                               int32_t export_surface_compatible,
                                               int32_t compile_receipt_equivalent,
                                               V3CompilerEquivalenceProof *out) {
    V3ByteBuf buf;
    const char *equivalence_kind;
    if (!v3_compiler_normalize_channel(channel_raw, out->channel, sizeof(out->channel))) {
        return false;
    }
    equivalence_kind = v3_compiler_equivalence_kind(graph_equivalent,
                                                    export_surface_compatible,
                                                    compile_receipt_equivalent);
    v3_copy_text(out->package_id, sizeof(out->package_id), package_id);
    out->baseline_graph_cid = baseline_graph_cid;
    out->migrated_graph_cid = migrated_graph_cid;
    out->baseline_surface_cid = baseline_surface_cid;
    out->migrated_surface_cid = migrated_surface_cid;
    out->baseline_receipt_cid = baseline_receipt_cid;
    out->migrated_receipt_cid = migrated_receipt_cid;
    v3_copy_text(out->target_triple, sizeof(out->target_triple), target_triple);
    out->graph_equivalent = graph_equivalent;
    out->export_surface_compatible = export_surface_compatible;
    out->compile_receipt_equivalent = compile_receipt_equivalent;
    v3_copy_text(out->equivalence_kind, sizeof(out->equivalence_kind), equivalence_kind);
    v3_bytebuf_init(&buf, 256U);
    v3_bytebuf_append_text(&buf, "v3.compiler.equivalence_proof");
    v3_bytebuf_append_text(&buf, package_id);
    v3_bytebuf_append_text(&buf, out->channel);
    v3_bytebuf_append_cid(&buf, baseline_graph_cid);
    v3_bytebuf_append_cid(&buf, migrated_graph_cid);
    v3_bytebuf_append_cid(&buf, baseline_surface_cid);
    v3_bytebuf_append_cid(&buf, migrated_surface_cid);
    v3_bytebuf_append_cid(&buf, baseline_receipt_cid);
    v3_bytebuf_append_cid(&buf, migrated_receipt_cid);
    v3_bytebuf_append_text(&buf, target_triple);
    v3_bytebuf_append_u32_be(&buf, (uint32_t)graph_equivalent);
    v3_bytebuf_append_u32_be(&buf, (uint32_t)export_surface_compatible);
    v3_bytebuf_append_u32_be(&buf, (uint32_t)compile_receipt_equivalent);
    v3_bytebuf_append_text(&buf, out->equivalence_kind);
    out->proof_cid = v3_cid_from_bytebuf(&buf);
    v3_bytebuf_free(&buf);
    return true;
}

static bool v3_compiler_equivalence_passed(const V3CompilerEquivalenceProof *proof) {
    return proof->graph_equivalent != 0 &&
           proof->export_surface_compatible != 0 &&
           proof->compile_receipt_equivalent != 0 &&
           v3_streq(proof->equivalence_kind, "canonical_semantic_identity");
}

static char *v3_compiler_equivalence_proof_report(const V3CompilerEquivalenceProof *proof) {
    char *out = (char *)v3_xmalloc(4096U);
    snprintf(out,
             4096U,
             "version=1\npackage_id=%s\nchannel=%s\nbaseline_graph_cid=%s\nmigrated_graph_cid=%s\nbaseline_surface_cid=%s\nmigrated_surface_cid=%s\nbaseline_receipt_cid=%s\nmigrated_receipt_cid=%s\ntarget=%s\ngraph_equivalent=%d\nexport_surface_compatible=%d\ncompile_receipt_equivalent=%d\nequivalence_kind=%s\nproof_cid=%s",
             proof->package_id,
             proof->channel,
             proof->baseline_graph_cid.hex,
             proof->migrated_graph_cid.hex,
             proof->baseline_surface_cid.hex,
             proof->migrated_surface_cid.hex,
             proof->baseline_receipt_cid.hex,
             proof->migrated_receipt_cid.hex,
             proof->target_triple,
             proof->graph_equivalent,
             proof->export_surface_compatible,
             proof->compile_receipt_equivalent,
             proof->equivalence_kind,
             proof->proof_cid.hex);
    return out;
}

static bool v3_compiler_publish_decision_make(const char *channel_raw,
                                              V3Cid32 world_head_cid,
                                              bool equivalence_checked,
                                              const V3CompilerEquivalenceProof *proof,
                                              V3CompilerPublishDecision *out) {
    int32_t stable_gate_required;
    if (!v3_compiler_normalize_channel(channel_raw, out->channel, sizeof(out->channel))) {
        return false;
    }
    stable_gate_required = v3_streq(out->channel, "stable") ? 1 : 0;
    out->stable_gate_required = stable_gate_required;
    out->equivalence_checked = equivalence_checked ? 1 : 0;
    out->equivalence_passed = (equivalence_checked && v3_compiler_equivalence_passed(proof)) ? 1 : 0;
    out->publish_allowed = stable_gate_required != 0 ? out->equivalence_passed : 1;
    out->world_head_cid = world_head_cid;
    out->proof_cid = equivalence_checked ? proof->proof_cid : v3_cid_zero();
    return true;
}

static char *v3_compiler_publish_decision_report(const V3CompilerPublishDecision *decision) {
    char *out = (char *)v3_xmalloc(2048U);
    snprintf(out,
             2048U,
             "version=1\nchannel=%s\nstable_gate_required=%d\nequivalence_checked=%d\nequivalence_passed=%d\npublish_allowed=%d\nworld_head_cid=%s\nproof_cid=%s",
             decision->channel,
             decision->stable_gate_required,
             decision->equivalence_checked,
             decision->equivalence_passed,
             decision->publish_allowed,
             decision->world_head_cid.hex,
             decision->proof_cid.hex);
    return out;
}

static bool v3_compiler_equivalence_inputs_ready(int argc,
                                                 char **argv,
                                                 bool required,
                                                 bool *enabled_out) {
    const char *baseline_csg = v3_flag_value(argc, argv, "--baseline-csg");
    const char *baseline_surface = v3_flag_value(argc, argv, "--baseline-surface");
    const char *baseline_receipt = v3_flag_value(argc, argv, "--baseline-receipt");
    bool any_provided = (baseline_csg && *baseline_csg != '\0') ||
                        (baseline_surface && *baseline_surface != '\0') ||
                        (baseline_receipt && *baseline_receipt != '\0');
    bool all_provided = (baseline_csg && *baseline_csg != '\0') &&
                        (baseline_surface && *baseline_surface != '\0') &&
                        (baseline_receipt && *baseline_receipt != '\0');
    if (any_provided && !all_provided) {
        fprintf(stderr, "v3 compiler equivalence: require --baseline-csg --baseline-surface --baseline-receipt together\n");
        return false;
    }
    if (required && !all_provided) {
        fprintf(stderr, "v3 compiler equivalence: missing --baseline-csg/--baseline-surface/--baseline-receipt\n");
        return false;
    }
    *enabled_out = all_provided;
    return true;
}

static bool v3_compiler_equivalence_proof_from_args(const V3SystemLinkPlanStub *plan,
                                                    const V3CompilerWorldArtifacts *world,
                                                    int argc,
                                                    char **argv,
                                                    V3CompilerEquivalenceProof *out) {
    const char *baseline_csg_path = v3_flag_value(argc, argv, "--baseline-csg");
    const char *baseline_surface_path = v3_flag_value(argc, argv, "--baseline-surface");
    const char *baseline_receipt_path = v3_flag_value(argc, argv, "--baseline-receipt");
    char *baseline_csg_text;
    char *baseline_surface_text;
    char *baseline_receipt_text;
    V3Cid32 baseline_graph_cid;
    V3Cid32 migrated_graph_cid;
    V3Cid32 baseline_surface_cid;
    V3Cid32 migrated_surface_cid;
    V3Cid32 baseline_receipt_cid;
    V3Cid32 migrated_receipt_cid;
    char target_triple[128];
    int32_t graph_equivalent = 0;
    int32_t export_surface_compatible = 0;
    int32_t compile_receipt_equivalent = 0;
    bool ok = false;
    baseline_csg_text = v3_read_file(baseline_csg_path);
    baseline_surface_text = v3_read_file(baseline_surface_path);
    baseline_receipt_text = v3_read_file(baseline_receipt_path);
    if (!baseline_csg_text || !baseline_surface_text || !baseline_receipt_text) {
        fprintf(stderr, "v3 compiler equivalence: failed to read baseline reports\n");
        goto cleanup;
    }
    if (!v3_compiler_equivalence_graph_compare_reports(baseline_csg_text,
                                                       world->csg_report,
                                                       &baseline_graph_cid,
                                                       &migrated_graph_cid,
                                                       &graph_equivalent)) {
        goto cleanup;
    }
    if (!v3_compiler_equivalence_surface_compare_reports(baseline_surface_text,
                                                         world->surface_report,
                                                         &baseline_surface_cid,
                                                         &migrated_surface_cid,
                                                         &export_surface_compatible)) {
        goto cleanup;
    }
    if (!v3_compiler_equivalence_receipt_compare_reports(baseline_receipt_text,
                                                         world->receipt_report,
                                                         &baseline_receipt_cid,
                                                         &migrated_receipt_cid,
                                                         target_triple,
                                                         sizeof(target_triple),
                                                         &compile_receipt_equivalent)) {
        goto cleanup;
    }
    ok = v3_compiler_equivalence_proof_make(plan->package_id,
                                            world->channel,
                                            baseline_graph_cid,
                                            migrated_graph_cid,
                                            baseline_surface_cid,
                                            migrated_surface_cid,
                                            baseline_receipt_cid,
                                            migrated_receipt_cid,
                                            target_triple,
                                            graph_equivalent,
                                            export_surface_compatible,
                                            compile_receipt_equivalent,
                                            out);
cleanup:
    free(baseline_csg_text);
    free(baseline_surface_text);
    free(baseline_receipt_text);
    return ok;
}

static char *v3_compiler_publish_summary(const char *bundle_prefix,
                                         const char *proof_mode,
                                         const V3CompilerWorldArtifacts *world,
                                         const V3CompilerPublishDecision *decision) {
    char *out = (char *)v3_xmalloc(2304U);
    snprintf(out,
             2304U,
             "bundle_prefix=%s\nchannel=%s\nworld_head_cid=%s\ncsg_root_cid=%s\nsurface_cid=%s\nreceipt_cid=%s\nproof_mode=%s\nequivalence_checked=%d\nequivalence_passed=%d\npublish_allowed=%d\nproof_cid=%s",
             bundle_prefix,
             world->channel,
             world->world_head_cid.hex,
             world->graph_cid.hex,
             world->surface_cid.hex,
             world->receipt_cid.hex,
             proof_mode && *proof_mode != '\0' ? proof_mode : "-",
             decision->equivalence_checked,
             decision->equivalence_passed,
             decision->publish_allowed,
             decision->proof_cid.hex);
    return out;
}

static void v3_compiler_legacy_units_free(V3CompilerLegacySourceUnit *units,
                                          size_t unit_count) {
    size_t i;
    for (i = 0U; i < unit_count; ++i) {
        free(units[i].legacy_text);
        free(units[i].migrated_text);
        units[i].legacy_text = NULL;
        units[i].migrated_text = NULL;
    }
}

static char *v3_text_dup_range(const char *text,
                               size_t start,
                               size_t len) {
    char *out = (char *)v3_xmalloc(len + 1U);
    memcpy(out, text + start, len);
    out[len] = '\0';
    return out;
}

static void v3_text_builder_append_n(char **out,
                                     size_t *cap,
                                     size_t *used,
                                     const char *text,
                                     size_t len) {
    while (*used + len + 1U >= *cap) {
        *cap *= 2U;
        *out = (char *)realloc(*out, *cap);
        if (!*out) {
            fprintf(stderr, "[cheng_v3_seed] out of memory while building migration text\n");
            exit(1);
        }
    }
    memcpy(*out + *used, text, len);
    *used += len;
    (*out)[*used] = '\0';
}

static void v3_text_builder_append(char **out,
                                   size_t *cap,
                                   size_t *used,
                                   const char *text) {
    v3_text_builder_append_n(out, cap, used, text, strlen(text));
}

static void v3_text_builder_append_char(char **out,
                                        size_t *cap,
                                        size_t *used,
                                        char ch) {
    v3_text_builder_append_n(out, cap, used, &ch, 1U);
}

static char *v3_text_replace_all(const char *text,
                                 const char *needle,
                                 const char *replacement) {
    size_t text_len = strlen(text);
    size_t needle_len = strlen(needle);
    size_t replacement_len = strlen(replacement);
    size_t cap = text_len + 64U;
    size_t used = 0U;
    size_t i = 0U;
    char *out = (char *)v3_xmalloc(cap);
    out[0] = '\0';
    if (needle_len == 0U) {
        v3_copy_text(out, cap, text);
        return out;
    }
    while (i < text_len) {
        if (i + needle_len <= text_len &&
            strncmp(text + i, needle, needle_len) == 0) {
            v3_text_builder_append_n(&out, &cap, &used, replacement, replacement_len);
            i += needle_len;
            continue;
        }
        v3_text_builder_append_char(&out, &cap, &used, text[i]);
        i += 1U;
    }
    return out;
}

static size_t v3_compiler_legacy_line_indent_len(const char *raw_line) {
    size_t i = 0U;
    while (raw_line[i] == ' ' || raw_line[i] == '\t') {
        i += 1U;
    }
    return i;
}

static int32_t v3_compiler_legacy_find_matching_bracket(const char *text,
                                                        size_t open_index) {
    size_t i;
    int32_t depth = 0;
    for (i = open_index; text[i] != '\0'; ++i) {
        if (text[i] == '[') {
            depth += 1;
            continue;
        }
        if (text[i] == ']') {
            depth -= 1;
            if (depth == 0) {
                return (int32_t)i;
            }
        }
    }
    return -1;
}

static int32_t v3_compiler_legacy_top_level_comma(const char *text) {
    size_t i;
    int32_t depth = 0;
    for (i = 0U; text[i] != '\0'; ++i) {
        if (text[i] == '[') {
            depth += 1;
            continue;
        }
        if (text[i] == ']') {
            depth -= 1;
            continue;
        }
        if (text[i] == ',' && depth == 0) {
            return (int32_t)i;
        }
    }
    return -1;
}

static char *v3_compiler_legacy_rewrite_seq_types(const char *text_raw,
                                                  int32_t *out_applied_count) {
    char *text = v3_strdup(text_raw);
    size_t i = 0U;
    *out_applied_count = 0;
    while (text[i] != '\0') {
        int32_t close_pos;
        char *inner;
        char *inner_trimmed;
        char *replacement;
        char *next;
        size_t replacement_len;
        size_t prefix_len;
        size_t suffix_start;
        if (strncmp(text + i, "seq[", 4) != 0) {
            i += 1U;
            continue;
        }
        close_pos = v3_compiler_legacy_find_matching_bracket(text, i + 3U);
        if (close_pos <= (int32_t)(i + 3U)) {
            i += 1U;
            continue;
        }
        inner = v3_text_dup_range(text, i + 4U, (size_t)close_pos - (i + 4U));
        inner_trimmed = v3_strdup(v3_trim_inplace(inner));
        replacement_len = strlen(inner_trimmed) + 2U;
        replacement = (char *)v3_xmalloc(replacement_len + 1U);
        snprintf(replacement, replacement_len + 1U, "%s[]", inner_trimmed);
        prefix_len = i;
        suffix_start = (size_t)close_pos + 1U;
        next = (char *)v3_xmalloc(prefix_len + replacement_len + strlen(text + suffix_start) + 1U);
        memcpy(next, text, prefix_len);
        memcpy(next + prefix_len, replacement, replacement_len);
        strcpy(next + prefix_len + replacement_len, text + suffix_start);
        free(text);
        free(inner);
        free(inner_trimmed);
        free(replacement);
        text = next;
        *out_applied_count += 1;
        i = prefix_len + replacement_len;
    }
    return text;
}

static char *v3_compiler_legacy_rewrite_array_types(const char *text_raw,
                                                    int32_t *out_applied_count) {
    char *text = v3_strdup(text_raw);
    size_t i = 0U;
    *out_applied_count = 0;
    while (text[i] != '\0') {
        int32_t close_pos;
        char *inner;
        int32_t comma_pos;
        char *elem;
        char *elem_trimmed;
        char *len_text;
        char *len_trimmed;
        char *replacement;
        char *next;
        size_t replacement_len;
        size_t prefix_len;
        size_t suffix_start;
        if (strncmp(text + i, "array[", 6) != 0) {
            i += 1U;
            continue;
        }
        close_pos = v3_compiler_legacy_find_matching_bracket(text, i + 5U);
        if (close_pos <= (int32_t)(i + 5U)) {
            i += 1U;
            continue;
        }
        inner = v3_text_dup_range(text, i + 6U, (size_t)close_pos - (i + 6U));
        comma_pos = v3_compiler_legacy_top_level_comma(inner);
        if (comma_pos <= 0) {
            free(inner);
            i += 1U;
            continue;
        }
        elem = v3_text_dup_range(inner, 0U, (size_t)comma_pos);
        elem_trimmed = v3_strdup(v3_trim_inplace(elem));
        len_text = v3_text_dup_range(inner, (size_t)comma_pos + 1U, strlen(inner) - ((size_t)comma_pos + 1U));
        len_trimmed = v3_strdup(v3_trim_inplace(len_text));
        replacement_len = strlen(elem_trimmed) + strlen(len_trimmed) + 2U;
        replacement = (char *)v3_xmalloc(replacement_len + 1U);
        snprintf(replacement, replacement_len + 1U, "%s[%s]", elem_trimmed, len_trimmed);
        prefix_len = i;
        suffix_start = (size_t)close_pos + 1U;
        next = (char *)v3_xmalloc(prefix_len + replacement_len + strlen(text + suffix_start) + 1U);
        memcpy(next, text, prefix_len);
        memcpy(next + prefix_len, replacement, replacement_len);
        strcpy(next + prefix_len + replacement_len, text + suffix_start);
        free(text);
        free(inner);
        free(elem);
        free(elem_trimmed);
        free(len_text);
        free(len_trimmed);
        free(replacement);
        text = next;
        *out_applied_count += 1;
        i = prefix_len + replacement_len;
    }
    return text;
}

static char *v3_compiler_legacy_normalize_text(const char *text_raw,
                                               V3PlanPath *applied_rules,
                                               size_t *applied_rule_len,
                                               int32_t *out_applied_count) {
    char *text;
    char *next;
    int32_t seq_applied = 0;
    int32_t array_applied = 0;
    char *owned;
    char *cursor;
    char *line;
    size_t cap = strlen(text_raw) + 128U;
    size_t used = 0U;
    char *out = (char *)v3_xmalloc(cap);
    *applied_rule_len = 0U;
    *out_applied_count = 0;
    out[0] = '\0';
    text = v3_text_replace_all(text_raw, "else if", "elif");
    if (!v3_streq(text, text_raw)) {
        v3_plan_path_add_unique(applied_rules, applied_rule_len, CHENG_V3_MAX_PLAN_PATHS, "else_if_to_elif");
        *out_applied_count += 1;
    }
    next = v3_text_replace_all(text, "@[]", "[]");
    if (!v3_streq(next, text)) {
        v3_plan_path_add_unique(applied_rules, applied_rule_len, CHENG_V3_MAX_PLAN_PATHS, "at_list_to_list");
        *out_applied_count += 1;
    }
    free(text);
    text = next;
    next = v3_compiler_legacy_rewrite_seq_types(text, &seq_applied);
    if (seq_applied > 0) {
        v3_plan_path_add_unique(applied_rules, applied_rule_len, CHENG_V3_MAX_PLAN_PATHS, "seq_type_to_suffix");
        *out_applied_count += seq_applied;
    }
    free(text);
    text = next;
    next = v3_compiler_legacy_rewrite_array_types(text, &array_applied);
    if (array_applied > 0) {
        v3_plan_path_add_unique(applied_rules, applied_rule_len, CHENG_V3_MAX_PLAN_PATHS, "array_type_to_suffix");
        *out_applied_count += array_applied;
    }
    free(text);
    text = next;
    owned = text;
    cursor = owned;
    while ((line = strsep(&cursor, "\n")) != NULL) {
        char *trimmed = v3_trim_inplace(line);
        size_t indent_len = v3_compiler_legacy_line_indent_len(line);
        if (used > 0U) {
            v3_text_builder_append_char(&out, &cap, &used, '\n');
        }
        if (v3_startswith(trimmed, "proc ")) {
            v3_plan_path_add_unique(applied_rules, applied_rule_len, CHENG_V3_MAX_PLAN_PATHS, "proc_to_fn");
            *out_applied_count += 1;
            v3_text_builder_append_n(&out, &cap, &used, line, indent_len);
            v3_text_builder_append(&out, &cap, &used, "fn ");
            v3_text_builder_append(&out, &cap, &used, trimmed + strlen("proc "));
            continue;
        }
        if (v3_startswith(trimmed, "method ")) {
            v3_plan_path_add_unique(applied_rules, applied_rule_len, CHENG_V3_MAX_PLAN_PATHS, "method_to_fn");
            *out_applied_count += 1;
            v3_text_builder_append_n(&out, &cap, &used, line, indent_len);
            v3_text_builder_append(&out, &cap, &used, "fn ");
            v3_text_builder_append(&out, &cap, &used, trimmed + strlen("method "));
            continue;
        }
        if (v3_startswith(trimmed, "converter ")) {
            v3_plan_path_add_unique(applied_rules, applied_rule_len, CHENG_V3_MAX_PLAN_PATHS, "converter_to_fn");
            *out_applied_count += 1;
            v3_text_builder_append_n(&out, &cap, &used, line, indent_len);
            v3_text_builder_append(&out, &cap, &used, "fn ");
            v3_text_builder_append(&out, &cap, &used, trimmed + strlen("converter "));
            continue;
        }
        v3_text_builder_append(&out, &cap, &used, line);
    }
    free(text);
    return out;
}

static bool v3_compiler_import_edge_add_unique(V3PlanImportEdge *edges,
                                               size_t *edge_len,
                                               size_t edge_cap,
                                               const V3PlanImportEdge *edge) {
    size_t i;
    for (i = 0U; i < *edge_len; ++i) {
        if (v3_streq(edges[i].owner_module_path, edge->owner_module_path) &&
            v3_streq(edges[i].target_module_path, edge->target_module_path) &&
            v3_streq(edges[i].target_source_path, edge->target_source_path) &&
            edges[i].resolved == edge->resolved) {
            return true;
        }
    }
    if (*edge_len >= edge_cap) {
        return false;
    }
    edges[*edge_len] = *edge;
    *edge_len += 1U;
    return true;
}

static void v3_compiler_merge_applied_rules(V3PlanPath *dst,
                                            size_t *dst_len,
                                            const V3PlanPath *src,
                                            size_t src_len) {
    size_t i;
    for (i = 0U; i < src_len; ++i) {
        v3_plan_path_add_unique(dst, dst_len, CHENG_V3_MAX_PLAN_PATHS, src[i].text);
    }
}

static bool v3_compiler_legacy_collect_closure(const char *workspace_root,
                                               const char *package_root,
                                               const char *package_id,
                                               const char *source_path,
                                               const char *entry_module_override,
                                               V3PlanPath *seen,
                                               size_t *seen_len,
                                               V3CompilerLegacySourceUnit *units,
                                               size_t *unit_count,
                                               V3PlanPath *owner_modules,
                                               size_t *owner_module_count,
                                               V3PlanImportEdge *import_edges,
                                               size_t *import_edge_count,
                                               size_t *unresolved_import_count,
                                               V3PlanPath *applied_rules,
                                               size_t *applied_rule_len,
                                               int32_t *applied_rule_count) {
    char module_path[PATH_MAX];
    char *legacy_text;
    char *migrated_text;
    V3PlanPath local_rules[CHENG_V3_MAX_PLAN_PATHS];
    size_t local_rule_len = 0U;
    int32_t local_rule_count = 0;
    V3PlanImportEdge edges[CHENG_V3_MAX_PLAN_PATHS];
    size_t edge_len = 0U;
    size_t i;
    bool is_entry = (*seen_len == 0U);
    if (v3_plan_paths_contains(seen, *seen_len, source_path)) {
        return true;
    }
    if (!v3_path_exists_nonempty(source_path)) {
        fprintf(stderr, "v3 compiler migration: missing legacy source: %s\n", source_path);
        return false;
    }
    if (*seen_len >= CHENG_V3_MAX_PLAN_PATHS || *unit_count >= CHENG_V3_MAX_PLAN_PATHS) {
        fprintf(stderr, "v3 compiler migration: source closure too large\n");
        return false;
    }
    legacy_text = v3_read_file(source_path);
    if (!legacy_text) {
        return false;
    }
    migrated_text = v3_compiler_legacy_normalize_text(legacy_text,
                                                      local_rules,
                                                      &local_rule_len,
                                                      &local_rule_count);
    v3_source_path_to_module_path(workspace_root,
                                  package_root,
                                  package_id,
                                  source_path,
                                  module_path,
                                  sizeof(module_path));
    if (is_entry && entry_module_override && *entry_module_override != '\0') {
        v3_copy_text(module_path, sizeof(module_path), entry_module_override);
    }
    v3_plan_add_path(seen, seen_len, CHENG_V3_MAX_PLAN_PATHS, source_path);
    v3_copy_text(units[*unit_count].source_path, sizeof(units[*unit_count].source_path), source_path);
    v3_copy_text(units[*unit_count].module_path, sizeof(units[*unit_count].module_path), module_path);
    units[*unit_count].legacy_text = legacy_text;
    units[*unit_count].migrated_text = migrated_text;
    *unit_count += 1U;
    v3_plan_path_add_unique(owner_modules, owner_module_count, 8U, module_path);
    v3_compiler_merge_applied_rules(applied_rules, applied_rule_len, local_rules, local_rule_len);
    *applied_rule_count += local_rule_count;
    if (!v3_parse_import_edges(workspace_root,
                               package_root,
                               package_id,
                               module_path,
                               migrated_text,
                               edges,
                               &edge_len,
                               CHENG_V3_MAX_PLAN_PATHS)) {
        fprintf(stderr, "v3 compiler migration: malformed import in %s\n", source_path);
        return false;
    }
    for (i = 0U; i < edge_len; ++i) {
        if (!v3_compiler_import_edge_add_unique(import_edges,
                                                import_edge_count,
                                                CHENG_V3_MAX_PLAN_PATHS,
                                                &edges[i])) {
            fprintf(stderr, "v3 compiler migration: import edge overflow\n");
            return false;
        }
        if (!edges[i].resolved) {
            *unresolved_import_count += 1U;
            continue;
        }
        if (!v3_compiler_legacy_collect_closure(workspace_root,
                                                package_root,
                                                package_id,
                                                edges[i].target_source_path,
                                                "",
                                                seen,
                                                seen_len,
                                                units,
                                                unit_count,
                                                owner_modules,
                                                owner_module_count,
                                                import_edges,
                                                import_edge_count,
                                                unresolved_import_count,
                                                applied_rules,
                                                applied_rule_len,
                                                applied_rule_count)) {
            return false;
        }
    }
    return true;
}

static bool v3_compiler_legacy_build_plan(const V3BootstrapContract *contract,
                                          int argc,
                                          char **argv,
                                          const char *package_root,
                                          const char *package_id,
                                          const V3CompilerLegacySourceUnit *units,
                                          size_t unit_count,
                                          const V3PlanPath *owner_modules,
                                          size_t owner_module_count,
                                          const V3PlanImportEdge *import_edges,
                                          size_t import_edge_count,
                                          size_t unresolved_import_count,
                                          V3SystemLinkPlanStub *out) {
    const char *target = v3_flag_value(argc, argv, "--target");
    const char *emit = v3_flag_value(argc, argv, "--emit");
    const char *out_path = v3_flag_value(argc, argv, "--out");
    const char *slash;
    char last_component[PATH_MAX];
    size_t i;
    char compiler_entry[PATH_MAX];
    char workspace_root[PATH_MAX];
    memset(out, 0, sizeof(*out));
    if (unit_count <= 0U) {
        return false;
    }
    if (!target || *target == '\0') {
        target = v3_contract_get(contract, "target");
    }
    if (!emit || *emit == '\0') {
        emit = "exe";
    }
    if (!v3_streq(emit, "exe") &&
        !v3_streq(emit, "shared") &&
        !v3_streq(emit, "obj")) {
        fprintf(stderr, "v3 compiler migration: unsupported --emit (only exe/shared/obj)\n");
        return false;
    }
    v3_workspace_root_from_package_root(package_root, workspace_root, sizeof(workspace_root));
    v3_copy_text(out->workspace_root, sizeof(out->workspace_root), workspace_root);
    v3_copy_text(out->package_root, sizeof(out->package_root), package_root);
    v3_copy_text(out->package_id, sizeof(out->package_id), package_id);
    v3_copy_text(out->entry_path, sizeof(out->entry_path), units[0].source_path);
    v3_copy_text(out->output_path, sizeof(out->output_path), out_path ? out_path : "");
    v3_copy_text(out->target_triple, sizeof(out->target_triple), target ? target : "arm64-apple-darwin");
    v3_copy_text(out->emit_kind, sizeof(out->emit_kind), emit);
    slash = strrchr(units[0].source_path, '/');
    snprintf(last_component, sizeof(last_component), "%s", slash ? slash + 1 : units[0].source_path);
    v3_strip_ext(last_component);
    v3_copy_text(out->module_stem, sizeof(out->module_stem), last_component);
    v3_copy_text(out->module_kind,
                 sizeof(out->module_kind),
                 v3_streq(units[0].module_path, "cheng/v3/tooling/compiler_main") ?
                    "compiler_control_plane" :
                    "ordinary_program");
    out->has_compiler_top_level = v3_source_has_top_level(units[0].migrated_text);
    out->main_function_present = v3_source_has_main(units[0].migrated_text);
    if (v3_source_has_legacy_func_alias(units[0].migrated_text)) {
        v3_plan_add_reason(out, "legacy_func_alias_unsupported");
    }
    if (out->main_function_present) {
        v3_copy_text(out->entry_symbol, sizeof(out->entry_symbol), "main");
    }
    for (i = 0U; i < unit_count; ++i) {
        v3_plan_add_path(out->source_closure_paths,
                         &out->source_closure_count,
                         CHENG_V3_MAX_PLAN_PATHS,
                         units[i].source_path);
    }
    for (i = 0U; i < owner_module_count; ++i) {
        v3_plan_add_path(out->owner_modules,
                         &out->owner_module_count,
                         8U,
                         owner_modules[i].text);
    }
    for (i = 0U; i < import_edge_count && i < CHENG_V3_MAX_PLAN_PATHS; ++i) {
        out->import_edges[i] = import_edges[i];
    }
    out->import_edge_count = import_edge_count;
    out->unresolved_import_count = unresolved_import_count;
    v3_populate_runtime_requirements(out);
    if (out->source_closure_count <= 0U) {
        v3_plan_add_reason(out, "source_closure_missing");
    }
    if (out->unresolved_import_count > 0U) {
        v3_plan_add_reason(out, "source_closure_unresolved_imports");
    }
    if (!out->has_compiler_top_level) {
        v3_plan_add_reason(out, "compiler_top_level_missing");
    }
    if (v3_streq(out->emit_kind, "exe") && out->entry_symbol[0] == '\0') {
        v3_plan_add_reason(out, "program_entry_symbol_missing");
    }
    if (v3_streq(out->emit_kind, "exe") && out->runtime_target_count <= 0U) {
        v3_plan_add_reason(out, "runtime_targets_not_lowered");
    }
    if (v3_streq(out->emit_kind, "exe") &&
        v3_target_requires_native_provider_modules(out->target_triple) &&
        out->provider_module_count <= 0U) {
        v3_plan_add_reason(out, "runtime_provider_modules_not_selected");
    }
    v3_resolve_contract_path(contract, "compiler_entry_source", compiler_entry, sizeof(compiler_entry));
    (void)compiler_entry;
    return true;
}

static bool v3_compiler_legacy_build(int argc,
                                     char **argv,
                                     const V3BootstrapContract *contract,
                                     const char *package_id_hint,
                                     const char *entry_module_override,
                                     const char *channel_override,
                                     V3SystemLinkPlanStub *plan_out,
                                     V3CompilerLegacyMigration *migration,
                                     V3CompilerWorldArtifacts *world) {
    const char *legacy_source_path = v3_flag_value(argc, argv, "--legacy-in");
    const char *package_root_flag = v3_flag_value(argc, argv, "--root");
    const char *channel = channel_override;
    char package_root[PATH_MAX];
    char workspace_root[PATH_MAX];
    char package_id[PATH_MAX];
    V3PlanPath seen[CHENG_V3_MAX_PLAN_PATHS];
    size_t seen_len = 0U;
    V3CompilerLegacySourceUnit units[CHENG_V3_MAX_PLAN_PATHS];
    size_t unit_count = 0U;
    V3PlanPath owner_modules[8];
    size_t owner_module_count = 0U;
    V3PlanImportEdge import_edges[CHENG_V3_MAX_PLAN_PATHS];
    size_t import_edge_count = 0U;
    size_t unresolved_import_count = 0U;
    V3PlanPath applied_rules[CHENG_V3_MAX_PLAN_PATHS];
    size_t applied_rule_len = 0U;
    int32_t applied_rule_count = 0;
    V3SystemLinkPlanStub plan;
    V3CompilerSourceOverride overrides[CHENG_V3_MAX_PLAN_PATHS];
    size_t i;
    char *legacy_texts[CHENG_V3_MAX_PLAN_PATHS];
    char *migrated_texts[CHENG_V3_MAX_PLAN_PATHS];
    V3Cid32 legacy_source_bundle_cid;
    V3Cid32 migrated_source_bundle_cid;
    V3ByteBuf mig_buf;
    if (!legacy_source_path || *legacy_source_path == '\0') {
        fprintf(stderr, "v3 compiler migration: missing --legacy-in\n");
        return false;
    }
    memset(units, 0, sizeof(units));
    memset(migration, 0, sizeof(*migration));
    memset(world, 0, sizeof(*world));
    v3_resolve_effective_package_root(contract,
                                      package_root_flag,
                                      legacy_source_path,
                                      package_root,
                                      sizeof(package_root));
    v3_workspace_root_from_package_root(package_root, workspace_root, sizeof(workspace_root));
    if (package_id_hint && *package_id_hint != '\0') {
        v3_copy_text(package_id, sizeof(package_id), package_id_hint);
    } else {
        v3_package_id_from_root(package_root, package_id, sizeof(package_id));
    }
    if (!v3_compiler_legacy_collect_closure(workspace_root,
                                            package_root,
                                            package_id,
                                            legacy_source_path,
                                            entry_module_override ? entry_module_override : "",
                                            seen,
                                            &seen_len,
                                            units,
                                            &unit_count,
                                            owner_modules,
                                            &owner_module_count,
                                            import_edges,
                                            &import_edge_count,
                                            &unresolved_import_count,
                                            applied_rules,
                                            &applied_rule_len,
                                            &applied_rule_count)) {
        v3_compiler_legacy_units_free(units, unit_count);
        return false;
    }
    for (i = 0U; i < unit_count; ++i) {
        legacy_texts[i] = units[i].legacy_text;
        migrated_texts[i] = units[i].migrated_text;
        snprintf(overrides[i].source_path, sizeof(overrides[i].source_path), "%s", units[i].source_path);
        snprintf(overrides[i].module_path, sizeof(overrides[i].module_path), "%s", units[i].module_path);
        overrides[i].text = units[i].migrated_text;
    }
    if (!v3_compiler_source_bundle_cid_from_texts(package_id,
                                                  seen,
                                                  legacy_texts,
                                                  unit_count,
                                                  &legacy_source_bundle_cid) ||
        !v3_compiler_source_bundle_cid_from_texts(package_id,
                                                  seen,
                                                  migrated_texts,
                                                  unit_count,
                                                  &migrated_source_bundle_cid)) {
        fprintf(stderr, "v3 compiler migration: failed to hash legacy bundle\n");
        v3_compiler_legacy_units_free(units, unit_count);
        return false;
    }
    if (!v3_compiler_legacy_build_plan(contract,
                                       argc,
                                       argv,
                                       package_root,
                                       package_id,
                                       units,
                                       unit_count,
                                       owner_modules,
                                       owner_module_count,
                                       import_edges,
                                       import_edge_count,
                                       unresolved_import_count,
                                       &plan)) {
        v3_compiler_legacy_units_free(units, unit_count);
        return false;
    }
    if (!channel || *channel == '\0') {
        channel = v3_flag_value(argc, argv, "--channel");
    }
    if (!v3_build_compiler_world_artifacts_with_overrides(&plan,
                                                          channel ? channel : "",
                                                          "",
                                                          "",
                                                          "",
                                                          overrides,
                                                          unit_count,
                                                          world)) {
        v3_compiler_legacy_units_free(units, unit_count);
        return false;
    }
    v3_copy_text(migration->package_id, sizeof(migration->package_id), package_id);
    v3_copy_text(migration->entry_source_path, sizeof(migration->entry_source_path), legacy_source_path);
    v3_copy_text(migration->entry_module_path, sizeof(migration->entry_module_path), units[0].module_path);
    migration->unit_count = unit_count;
    migration->applied_rule_count = applied_rule_count;
    for (i = 0U; i < applied_rule_len; ++i) {
        snprintf(migration->applied_rules[i].text, sizeof(migration->applied_rules[i].text), "%s", applied_rules[i].text);
    }
    migration->legacy_source_bundle_cid = legacy_source_bundle_cid;
    migration->migrated_source_bundle_cid = migrated_source_bundle_cid;
    v3_bytebuf_init(&mig_buf, 256U);
    v3_bytebuf_append_text(&mig_buf, "v3.compiler.legacy_migration");
    v3_bytebuf_append_text(&mig_buf, package_id);
    v3_bytebuf_append_text(&mig_buf, legacy_source_path);
    v3_bytebuf_append_cid(&mig_buf, legacy_source_bundle_cid);
    v3_bytebuf_append_cid(&mig_buf, migrated_source_bundle_cid);
    v3_bytebuf_append_cid(&mig_buf, world->canonical_graph_cid);
    v3_bytebuf_append_cid(&mig_buf, world->surface_cid);
    v3_bytebuf_append_u32_be(&mig_buf, (uint32_t)applied_rule_len);
    for (i = 0U; i < applied_rule_len; ++i) {
        v3_bytebuf_append_text(&mig_buf, applied_rules[i].text);
    }
    migration->migration_cid = v3_cid_from_bytebuf(&mig_buf);
    v3_bytebuf_free(&mig_buf);
    if (plan_out) {
        *plan_out = plan;
    }
    v3_compiler_legacy_units_free(units, unit_count);
    return true;
}

static char *v3_compiler_legacy_migration_report(const V3CompilerLegacyMigration *migration,
                                                 const V3CompilerWorldArtifacts *world) {
    size_t cap = 4096U + migration->unit_count * PATH_MAX;
    size_t used = 0U;
    size_t i;
    char *out = (char *)v3_xmalloc(cap);
    char line[PATH_MAX * 2];
    out[0] = '\0';
    v3_report_append(&out, &cap, &used, "version=1");
    snprintf(line, sizeof(line), "package_id=%s", migration->package_id);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "entry_source_path=%s", migration->entry_source_path);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "entry_module_path=%s", migration->entry_module_path);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "unit_count=%zu", migration->unit_count);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "applied_rule_count=%d", migration->applied_rule_count);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "legacy_source_bundle_cid=%s", migration->legacy_source_bundle_cid.hex);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "migrated_source_bundle_cid=%s", migration->migrated_source_bundle_cid.hex);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "canonical_graph_cid=%s", world->canonical_graph_cid.hex);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "graph_cid=%s", world->graph_cid.hex);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "surface_cid=%s", world->surface_cid.hex);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "migration_cid=%s", migration->migration_cid.hex);
    v3_report_append(&out, &cap, &used, line);
    for (i = 0U; i < CHENG_V3_MAX_PLAN_PATHS; ++i) {
        if (migration->applied_rules[i].text[0] == '\0') {
            continue;
        }
        snprintf(line, sizeof(line), "applied_rule[%zu]=%s", i, migration->applied_rules[i].text);
        v3_report_append(&out, &cap, &used, line);
    }
    return out;
}

static bool v3_compiler_write_legacy_artifacts(const char *prefix,
                                               const char *migration_report,
                                               const char *csg_report,
                                               const char *surface_report,
                                               const char *receipt_report) {
    if (!prefix || *prefix == '\0') {
        return true;
    }
    return v3_write_world_bundle_text(prefix, ".migration.txt", migration_report) &&
           v3_write_world_bundle_text(prefix, ".migration.csg.txt", csg_report) &&
           v3_write_world_bundle_text(prefix, ".migration.surface.txt", surface_report) &&
           v3_write_world_bundle_text(prefix, ".migration.receipt.txt", receipt_report);
}

static bool v3_compiler_migration_receipt_report(const V3CompilerWorldArtifacts *baseline_world,
                                                 const V3SystemLinkPlanStub *plan,
                                                 const V3CompilerWorldArtifacts *migration_world,
                                                 const char *bootstrap_stage,
                                                 V3Cid32 *receipt_cid_out,
                                                 char **report_out) {
    V3Cid32 output_digest_cid = v3_compiler_output_digest_cid(plan, migration_world->graph_cid);
    V3Cid32 canonical_output_digest_cid = v3_compiler_output_digest_cid(plan, migration_world->canonical_graph_cid);
    V3Cid32 receipt_cid = v3_compiler_receipt_cid(baseline_world->world_head_cid,
                                                  migration_world->graph_cid,
                                                  migration_world->canonical_graph_cid,
                                                  plan,
                                                  output_digest_cid,
                                                  bootstrap_stage ? bootstrap_stage : "stage3_local",
                                                  canonical_output_digest_cid);
    *receipt_cid_out = receipt_cid;
    *report_out = v3_compiler_receipt_report(baseline_world->world_head_cid,
                                             migration_world->graph_cid,
                                             migration_world->canonical_graph_cid,
                                             plan,
                                             output_digest_cid,
                                             bootstrap_stage ? bootstrap_stage : "stage3_local",
                                             canonical_output_digest_cid,
                                             receipt_cid);
    return true;
}

static bool v3_compiler_migration_proof_from_reports(const V3SystemLinkPlanStub *plan,
                                                     const V3CompilerWorldArtifacts *baseline_world,
                                                     const V3CompilerWorldArtifacts *migration_world,
                                                     V3CompilerEquivalenceProof *out,
                                                     char **migration_receipt_report_out) {
    V3Cid32 baseline_graph_cid;
    V3Cid32 migrated_graph_cid;
    V3Cid32 baseline_surface_cid;
    V3Cid32 migrated_surface_cid;
    V3Cid32 baseline_receipt_cid;
    V3Cid32 migrated_receipt_cid;
    char target_triple[128];
    int32_t graph_equivalent = 0;
    int32_t export_surface_compatible = 0;
    int32_t compile_receipt_equivalent = 0;
    if (!v3_compiler_migration_receipt_report(baseline_world,
                                              plan,
                                              migration_world,
                                              "stage3_local",
                                              &migrated_receipt_cid,
                                              migration_receipt_report_out)) {
        return false;
    }
    if (!v3_compiler_equivalence_graph_compare_reports(baseline_world->csg_report,
                                                       migration_world->csg_report,
                                                       &baseline_graph_cid,
                                                       &migrated_graph_cid,
                                                       &graph_equivalent)) {
        return false;
    }
    if (!v3_compiler_equivalence_surface_compare_reports(baseline_world->surface_report,
                                                         migration_world->surface_report,
                                                         &baseline_surface_cid,
                                                         &migrated_surface_cid,
                                                         &export_surface_compatible)) {
        return false;
    }
    if (!v3_compiler_equivalence_receipt_compare_reports(baseline_world->receipt_report,
                                                         *migration_receipt_report_out,
                                                         &baseline_receipt_cid,
                                                         &migrated_receipt_cid,
                                                         target_triple,
                                                         sizeof(target_triple),
                                                         &compile_receipt_equivalent)) {
        return false;
    }
    return v3_compiler_equivalence_proof_make(plan->package_id,
                                              baseline_world->channel,
                                              baseline_graph_cid,
                                              migrated_graph_cid,
                                              baseline_surface_cid,
                                              migrated_surface_cid,
                                              baseline_receipt_cid,
                                              migrated_receipt_cid,
                                              target_triple,
                                              graph_equivalent,
                                              export_surface_compatible,
                                              compile_receipt_equivalent,
                                              out);
}

static bool v3_compiler_proof_mode_from_args(int argc,
                                             char **argv,
                                             bool required,
                                             bool *enabled_out,
                                             char *mode_out,
                                             size_t mode_cap) {
    bool equivalence_enabled = false;
    bool migration_enabled = false;
    const char *legacy_in = v3_flag_value(argc, argv, "--legacy-in");
    if (!v3_compiler_equivalence_inputs_ready(argc, argv, false, &equivalence_enabled)) {
        return false;
    }
    migration_enabled = legacy_in && *legacy_in != '\0';
    if (equivalence_enabled && migration_enabled) {
        fprintf(stderr, "v3 compiler proof: choose either --legacy-in or --baseline-csg/--baseline-surface/--baseline-receipt\n");
        return false;
    }
    if (required && !equivalence_enabled && !migration_enabled) {
        fprintf(stderr, "v3 compiler proof: stable requires --legacy-in or --baseline-csg/--baseline-surface/--baseline-receipt\n");
        return false;
    }
    *enabled_out = equivalence_enabled || migration_enabled;
    v3_copy_text(mode_out, mode_cap, migration_enabled ? "migration" : (equivalence_enabled ? "equivalence" : ""));
    return true;
}

static bool v3_build_compiler_world_artifacts_with_overrides(const V3SystemLinkPlanStub *plan,
                                                             const char *channel_raw,
                                                             const char *requested_world_head,
                                                             const char *lock_path,
                                                             const char *baseline_surface_path,
                                                             const V3CompilerSourceOverride *overrides,
                                                             size_t override_count,
                                                             V3CompilerWorldArtifacts *out) {
    V3PlanPath modules[CHENG_V3_MAX_PLAN_PATHS];
    size_t module_count = 0U;
    size_t function_count = 0U;
    size_t i;
    size_t node_index = 0U;
    size_t edge_index = 0U;
    size_t node_cap = 4096U;
    size_t node_used = 0U;
    size_t edge_cap = 4096U;
    size_t edge_used = 0U;
    char *node_report = (char *)v3_xmalloc(node_cap);
    char *edge_report = (char *)v3_xmalloc(edge_cap);
    char entry_module_path[PATH_MAX];
    V3ByteBuf graph_buf;
    V3ByteBuf canonical_buf;
    V3ByteBuf edge_buf;
    int32_t next_node_id;
    int32_t next_edge_id;
    node_report[0] = '\0';
    edge_report[0] = '\0';
    memset(out, 0, sizeof(*out));
    if (!v3_compiler_normalize_channel(channel_raw, out->channel, sizeof(out->channel))) {
        free(node_report);
        free(edge_report);
        return false;
    }
    v3_copy_text(out->package_id, sizeof(out->package_id), plan->package_id);
    v3_compiler_source_module_path(plan,
                                   overrides,
                                   override_count,
                                   plan->entry_path,
                                   entry_module_path,
                                   sizeof(entry_module_path));
    if (!v3_compiler_source_bundle_cid_with_overrides(plan, overrides, override_count, &out->source_bundle_cid)) {
        free(node_report);
        free(edge_report);
        return false;
    }
    for (i = 0U; i < plan->source_closure_count; ++i) {
        char module_path[PATH_MAX];
        char *owned = NULL;
        char *text = NULL;
        char *cursor;
        char *line;
        bool pending_importc = false;
        if (!v3_compiler_source_text_lookup(overrides,
                                            override_count,
                                            plan->source_closure_paths[i].text,
                                            &text)) {
            owned = v3_read_file(plan->source_closure_paths[i].text);
            text = owned;
        } else {
            owned = v3_strdup(text);
            text = owned;
        }
        if (!text) {
            free(node_report);
            free(edge_report);
            return false;
        }
        v3_compiler_source_module_path(plan,
                                       overrides,
                                       override_count,
                                       plan->source_closure_paths[i].text,
                                       module_path,
                                       sizeof(module_path));
        v3_plan_path_add_unique(modules, &module_count, CHENG_V3_MAX_PLAN_PATHS, module_path);
        cursor = text;
        while ((line = strsep(&cursor, "\n")) != NULL) {
            char trimmed[4096];
            char function_name[256];
            v3_trim_copy_text(line, trimmed, sizeof(trimmed));
            if (trimmed[0] == '\0') {
                pending_importc = false;
                continue;
            }
            if (v3_startswith(trimmed, "@importc")) {
                pending_importc = true;
                continue;
            }
            if (pending_importc && v3_is_annotation_line(trimmed)) {
                continue;
            }
            if (v3_importc_function_name_from_line(trimmed, function_name, sizeof(function_name))) {
                pending_importc = false;
                continue;
            }
            if (!v3_lowering_function_name_from_line(trimmed, function_name, sizeof(function_name))) {
                pending_importc = false;
                continue;
            }
            if (!pending_importc) {
                function_count += 1U;
            }
            pending_importc = false;
        }
        free(owned);
    }
    out->node_count = 1U + module_count + function_count * 4U;
    out->edge_count = module_count + plan->import_edge_count + function_count * 4U;
    out->exports = (V3CompilerExportEntry *)calloc(function_count > 0U ? function_count : 1U,
                                                   sizeof(V3CompilerExportEntry));
    if (!out->exports) {
        fprintf(stderr, "[cheng_v3_seed] out of memory while allocating exports\n");
        exit(1);
    }
    v3_bytebuf_init(&graph_buf, 65536U);
    v3_bytebuf_init(&canonical_buf, 65536U);
    v3_bytebuf_init(&edge_buf, 65536U);
    v3_bytebuf_append_text(&graph_buf, "v3.compiler.csg.graph");
    v3_bytebuf_append_text(&graph_buf, plan->package_id);
    v3_bytebuf_append_cid(&graph_buf, out->source_bundle_cid);
    v3_bytebuf_append_u32_be(&graph_buf, (uint32_t)out->node_count);
    v3_bytebuf_append_text(&canonical_buf, "v3.compiler.csg.canonical_graph");
    v3_bytebuf_append_text(&canonical_buf, plan->package_id);
    v3_bytebuf_append_u32_be(&canonical_buf, (uint32_t)out->node_count);
    v3_compiler_csg_append_node(&graph_buf,
                                &node_report,
                                &node_cap,
                                &node_used,
                                &node_index,
                                1,
                                1,
                                0,
                                "",
                                "",
                                plan->package_id,
                                0,
                                0,
                                0,
                                0,
                                0,
                                0,
                                "");
    v3_compiler_csg_append_node(&canonical_buf,
                                NULL,
                                NULL,
                                NULL,
                                NULL,
                                1,
                                1,
                                0,
                                "",
                                "",
                                plan->package_id,
                                0,
                                0,
                                0,
                                0,
                                0,
                                0,
                                "");
    next_node_id = 2;
    next_edge_id = 1;
    for (i = 0U; i < module_count; ++i) {
        int32_t module_node_id = next_node_id;
        next_node_id += 1;
        v3_compiler_csg_append_node(&graph_buf,
                                    &node_report,
                                    &node_cap,
                                    &node_used,
                                    &node_index,
                                    module_node_id,
                                    2,
                                    1,
                                    modules[i].text,
                                    "",
                                    modules[i].text,
                                    0,
                                    0,
                                    0,
                                    0,
                                    0,
                                    0,
                                    "");
        v3_compiler_csg_append_node(&canonical_buf,
                                    NULL,
                                    NULL,
                                    NULL,
                                    NULL,
                                    module_node_id,
                                    2,
                                    1,
                                    modules[i].text,
                                    "",
                                    modules[i].text,
                                    0,
                                    0,
                                    0,
                                    0,
                                    0,
                                    0,
                                    "");
        v3_compiler_csg_append_edge(&edge_buf,
                                    &edge_report,
                                    &edge_cap,
                                    &edge_used,
                                    &edge_index,
                                    next_edge_id,
                                    1,
                                    module_node_id,
                                    1);
        next_edge_id += 1;
    }
    for (i = 0U; i < plan->import_edge_count; ++i) {
        int32_t from_index = v3_plan_path_find_index(modules, module_count, plan->import_edges[i].owner_module_path);
        int32_t to_index = v3_plan_path_find_index(modules, module_count, plan->import_edges[i].target_module_path);
        if (from_index < 0 || to_index < 0) {
            continue;
        }
        v3_compiler_csg_append_edge(&edge_buf,
                                    &edge_report,
                                    &edge_cap,
                                    &edge_used,
                                    &edge_index,
                                    next_edge_id,
                                    2 + from_index,
                                    2 + to_index,
                                    2);
        next_edge_id += 1;
    }
    for (i = 0U; i < plan->source_closure_count; ++i) {
        char module_path[PATH_MAX];
        int32_t owner_module_index;
        int32_t owner_module_node_id;
        int32_t entry_source_flag = 0;
        char *owned = NULL;
        char *text = NULL;
        char *cursor;
        char *line;
        char *lines[CHENG_V3_MAX_SOURCE_LINES];
        size_t line_count = 0U;
        size_t line_index;
        bool pending_importc = false;
        bool entry_has_explicit_exports = false;
        if (!v3_compiler_source_text_lookup(overrides,
                                            override_count,
                                            plan->source_closure_paths[i].text,
                                            &text)) {
            owned = v3_read_file(plan->source_closure_paths[i].text);
            text = owned;
        } else {
            owned = v3_strdup(text);
            text = owned;
        }
        if (!text) {
            v3_bytebuf_free(&graph_buf);
            v3_bytebuf_free(&canonical_buf);
            v3_bytebuf_free(&edge_buf);
            free(node_report);
            free(edge_report);
            v3_compiler_world_artifacts_free(out);
            return false;
        }
        v3_compiler_source_module_path(plan,
                                       overrides,
                                       override_count,
                                       plan->source_closure_paths[i].text,
                                       module_path,
                                       sizeof(module_path));
        owner_module_index = v3_plan_path_find_index(modules, module_count, module_path);
        if (owner_module_index < 0) {
            free(owned);
            continue;
        }
        entry_source_flag =
            v3_streq(plan->source_closure_paths[i].text, plan->entry_path) ||
            v3_streq(module_path, entry_module_path);
        owner_module_node_id = 2 + owner_module_index;
        entry_has_explicit_exports =
            (v3_streq(plan->emit_kind, "shared") || v3_streq(plan->emit_kind, "obj")) &&
            entry_source_flag != 0 &&
            v3_source_text_has_explicit_compiler_exports(text);
        cursor = text;
        while ((line = strsep(&cursor, "\n")) != NULL) {
            if (line_count >= CHENG_V3_MAX_SOURCE_LINES) {
                v3_bytebuf_free(&graph_buf);
                v3_bytebuf_free(&canonical_buf);
                v3_bytebuf_free(&edge_buf);
                free(node_report);
                free(edge_report);
                v3_compiler_world_artifacts_free(out);
                free(owned);
                fprintf(stderr, "[cheng_v3_seed] compiler world source line inventory too large\n");
                return false;
            }
            lines[line_count++] = line;
        }
        for (line_index = 0U; line_index < line_count; ++line_index) {
            char trimmed[4096];
            char function_name[256];
            int32_t entry_flag;
            int32_t result_layout_kind;
            int32_t effect_kind;
            int32_t capability_kind;
            int32_t symbol_node_id;
            int32_t layout_node_id;
            int32_t effect_node_id;
            int32_t capability_node_id;
            char symbol_text[PATH_MAX];
            char layout_fact[64];
            char effect_fact[64];
            char capability_fact[64];
            char captured_export_symbol[PATH_MAX];
            char export_symbol_name[PATH_MAX];
            int32_t exported_flag;
            v3_trim_copy_text(lines[line_index], trimmed, sizeof(trimmed));
            captured_export_symbol[0] = '\0';
            if (trimmed[0] == '\0') {
                pending_importc = false;
                continue;
            }
            if (v3_startswith(trimmed, "@importc")) {
                pending_importc = true;
                continue;
            }
            if (pending_importc && v3_is_annotation_line(trimmed)) {
                continue;
            }
            if (v3_importc_function_name_from_line(trimmed, function_name, sizeof(function_name))) {
                pending_importc = false;
                continue;
            }
            if (!v3_lowering_function_name_from_line(trimmed, function_name, sizeof(function_name))) {
                pending_importc = false;
                continue;
            }
            if (pending_importc) {
                pending_importc = false;
                continue;
            }
            v3_top_level_function_export_symbol_from_lines(lines,
                                                           line_count,
                                                           line_index,
                                                           captured_export_symbol,
                                                           sizeof(captured_export_symbol));
            entry_flag = (v3_streq(plan->source_closure_paths[i].text, plan->entry_path) &&
                          v3_streq(function_name, plan->entry_symbol)) ? 1 : 0;
            result_layout_kind = v3_streq(function_name, "main") ? 5 : 0;
            effect_kind = entry_flag != 0 ? 2 : 1;
            capability_kind = entry_flag != 0 ? 2 : 1;
            snprintf(symbol_text, sizeof(symbol_text), "%s::%s", module_path, function_name);
            exported_flag = v3_compiler_world_should_export_symbol(plan,
                                                                   entry_source_flag,
                                                                   entry_has_explicit_exports,
                                                                   captured_export_symbol,
                                                                   function_name,
                                                                   entry_flag) ? 1 : 0;
            v3_compiler_target_export_name(symbol_text,
                                           exported_flag != 0 ? captured_export_symbol : "",
                                           function_name,
                                           export_symbol_name,
                                           sizeof(export_symbol_name));
            symbol_node_id = next_node_id++;
            layout_node_id = next_node_id++;
            effect_node_id = next_node_id++;
            capability_node_id = next_node_id++;
            snprintf(layout_fact, sizeof(layout_fact), "layout=%d", result_layout_kind);
            snprintf(effect_fact, sizeof(effect_fact), "effect=%d", effect_kind);
            snprintf(capability_fact, sizeof(capability_fact), "capability=%d", capability_kind);
            v3_compiler_csg_append_node(&graph_buf,
                                        &node_report,
                                        &node_cap,
                                        &node_used,
                                        &node_index,
                                        symbol_node_id,
                                        3,
                                        owner_module_node_id,
                                        module_path,
                                        symbol_text,
                                        function_name,
                                        result_layout_kind,
                                        effect_kind,
                                        capability_kind,
                                        1,
                                        entry_flag,
                                        exported_flag,
                                        exported_flag != 0 ? export_symbol_name : "");
            v3_compiler_csg_append_node(&canonical_buf,
                                        NULL,
                                        NULL,
                                        NULL,
                                        NULL,
                                        symbol_node_id,
                                        3,
                                        owner_module_node_id,
                                        module_path,
                                        symbol_text,
                                        function_name,
                                        result_layout_kind,
                                        effect_kind,
                                        capability_kind,
                                        1,
                                        entry_flag,
                                        exported_flag,
                                        exported_flag != 0 ? export_symbol_name : "");
            v3_compiler_csg_append_node(&graph_buf,
                                        &node_report,
                                        &node_cap,
                                        &node_used,
                                        &node_index,
                                        layout_node_id,
                                        4,
                                        symbol_node_id,
                                        module_path,
                                        symbol_text,
                                        layout_fact,
                                        result_layout_kind,
                                        0,
                                        0,
                                        1,
                                        entry_flag,
                                        0,
                                        "");
            v3_compiler_csg_append_node(&canonical_buf,
                                        NULL,
                                        NULL,
                                        NULL,
                                        NULL,
                                        layout_node_id,
                                        4,
                                        symbol_node_id,
                                        module_path,
                                        symbol_text,
                                        layout_fact,
                                        result_layout_kind,
                                        0,
                                        0,
                                        1,
                                        entry_flag,
                                        0,
                                        "");
            v3_compiler_csg_append_node(&graph_buf,
                                        &node_report,
                                        &node_cap,
                                        &node_used,
                                        &node_index,
                                        effect_node_id,
                                        5,
                                        symbol_node_id,
                                        module_path,
                                        symbol_text,
                                        effect_fact,
                                        result_layout_kind,
                                        effect_kind,
                                        0,
                                        1,
                                        entry_flag,
                                        0,
                                        "");
            v3_compiler_csg_append_node(&canonical_buf,
                                        NULL,
                                        NULL,
                                        NULL,
                                        NULL,
                                        effect_node_id,
                                        5,
                                        symbol_node_id,
                                        module_path,
                                        symbol_text,
                                        effect_fact,
                                        result_layout_kind,
                                        effect_kind,
                                        0,
                                        1,
                                        entry_flag,
                                        0,
                                        "");
            v3_compiler_csg_append_node(&graph_buf,
                                        &node_report,
                                        &node_cap,
                                        &node_used,
                                        &node_index,
                                        capability_node_id,
                                        6,
                                        symbol_node_id,
                                        module_path,
                                        symbol_text,
                                        capability_fact,
                                        result_layout_kind,
                                        effect_kind,
                                        capability_kind,
                                        1,
                                        entry_flag,
                                        0,
                                        "");
            v3_compiler_csg_append_node(&canonical_buf,
                                        NULL,
                                        NULL,
                                        NULL,
                                        NULL,
                                        capability_node_id,
                                        6,
                                        symbol_node_id,
                                        module_path,
                                        symbol_text,
                                        capability_fact,
                                        result_layout_kind,
                                        effect_kind,
                                        capability_kind,
                                        1,
                                        entry_flag,
                                        0,
                                        "");
            v3_compiler_csg_append_edge(&edge_buf, &edge_report, &edge_cap, &edge_used, &edge_index,
                                        next_edge_id++, owner_module_node_id, symbol_node_id, 1);
            v3_compiler_csg_append_edge(&edge_buf, &edge_report, &edge_cap, &edge_used, &edge_index,
                                        next_edge_id++, symbol_node_id, layout_node_id, 3);
            v3_compiler_csg_append_edge(&edge_buf, &edge_report, &edge_cap, &edge_used, &edge_index,
                                        next_edge_id++, symbol_node_id, effect_node_id, 4);
            v3_compiler_csg_append_edge(&edge_buf, &edge_report, &edge_cap, &edge_used, &edge_index,
                                        next_edge_id++, symbol_node_id, capability_node_id, 5);
            if (exported_flag != 0) {
                v3_copy_text(out->exports[out->exported_count].symbol_text,
                             sizeof(out->exports[out->exported_count].symbol_text),
                             symbol_text);
                v3_copy_text(out->exports[out->exported_count].export_symbol_name,
                             sizeof(out->exports[out->exported_count].export_symbol_name),
                             export_symbol_name);
                out->exports[out->exported_count].layout_kind = result_layout_kind;
                out->exports[out->exported_count].effect_kind = effect_kind;
                out->exports[out->exported_count].capability_kind = capability_kind;
                out->exported_count += 1U;
            }
        }
        free(owned);
    }
    v3_bytebuf_append_u32_be(&graph_buf, (uint32_t)out->edge_count);
    v3_bytebuf_append_bytes(&graph_buf, edge_buf.data, edge_buf.len);
    v3_bytebuf_append_u32_be(&canonical_buf, (uint32_t)out->edge_count);
    v3_bytebuf_append_bytes(&canonical_buf, edge_buf.data, edge_buf.len);
    out->canonical_graph_cid = v3_cid_from_bytebuf(&canonical_buf);
    out->graph_cid = v3_cid_from_bytebuf(&graph_buf);
    {
        size_t cap = 8192U + node_used + edge_used;
        size_t used = 0U;
        char *report = (char *)v3_xmalloc(cap);
        char line[PATH_MAX * 2];
        report[0] = '\0';
        v3_report_append(&report, &cap, &used, "version=1");
        snprintf(line, sizeof(line), "package_id=%s", plan->package_id);
        v3_report_append(&report, &cap, &used, line);
        snprintf(line, sizeof(line), "source_bundle_cid=%s", out->source_bundle_cid.hex);
        v3_report_append(&report, &cap, &used, line);
        snprintf(line, sizeof(line), "canonical_graph_cid=%s", out->canonical_graph_cid.hex);
        v3_report_append(&report, &cap, &used, line);
        snprintf(line, sizeof(line), "graph_cid=%s", out->graph_cid.hex);
        v3_report_append(&report, &cap, &used, line);
        snprintf(line, sizeof(line), "node_count=%zu", out->node_count);
        v3_report_append(&report, &cap, &used, line);
        snprintf(line, sizeof(line), "edge_count=%zu", out->edge_count);
        v3_report_append(&report, &cap, &used, line);
        v3_report_append_raw(&report, &cap, &used, node_report);
        v3_report_append_raw(&report, &cap, &used, edge_report);
        out->csg_report = report;
    }
    {
        V3ByteBuf surface_buf;
        size_t cap = 4096U + out->exported_count * (PATH_MAX + 64U);
        size_t used = 0U;
        char *report = (char *)v3_xmalloc(cap);
        char line[PATH_MAX * 2];
        v3_bytebuf_init(&surface_buf, 256U);
        v3_bytebuf_append_text(&surface_buf, "v3.compiler.export_surface");
        v3_bytebuf_append_text(&surface_buf, plan->package_id);
        v3_bytebuf_append_u32_be(&surface_buf, (uint32_t)out->exported_count);
        report[0] = '\0';
        v3_report_append(&report, &cap, &used, "version=1");
        snprintf(line, sizeof(line), "package_id=%s", plan->package_id);
        v3_report_append(&report, &cap, &used, line);
        snprintf(line, sizeof(line), "exported_count=%zu", out->exported_count);
        v3_report_append(&report, &cap, &used, line);
        for (i = 0U; i < out->exported_count; ++i) {
            v3_bytebuf_append_text(&surface_buf, out->exports[i].symbol_text);
            v3_bytebuf_append_text(&surface_buf, out->exports[i].export_symbol_name);
            v3_bytebuf_append_u32_be(&surface_buf, (uint32_t)out->exports[i].layout_kind);
            v3_bytebuf_append_u32_be(&surface_buf, (uint32_t)out->exports[i].effect_kind);
            v3_bytebuf_append_u32_be(&surface_buf, (uint32_t)out->exports[i].capability_kind);
        }
        out->surface_cid = v3_cid_from_bytebuf(&surface_buf);
        snprintf(line, sizeof(line), "surface_cid=%s", out->surface_cid.hex);
        v3_report_append(&report, &cap, &used, line);
        for (i = 0U; i < out->exported_count; ++i) {
            snprintf(line,
                     sizeof(line),
                     "export[%zu]=%s|%s|%d|%d|%d",
                     i,
                     out->exports[i].symbol_text,
                     out->exports[i].export_symbol_name,
                     out->exports[i].layout_kind,
                     out->exports[i].effect_kind,
                     out->exports[i].capability_kind);
            v3_report_append(&report, &cap, &used, line);
        }
        out->surface_report = report;
        v3_bytebuf_free(&surface_buf);
    }
    out->compiler_pkg_cid = v3_compiler_package_snapshot_cid("cheng/v3/compiler",
                                                             out->channel,
                                                             out->source_bundle_cid,
                                                             out->graph_cid,
                                                             out->surface_cid,
                                                             "local:compiler");
    out->std_pkg_cid = v3_compiler_package_snapshot_cid("cheng/v3/std",
                                                        out->channel,
                                                        out->source_bundle_cid,
                                                        out->graph_cid,
                                                        out->surface_cid,
                                                        "local:std");
    out->runtime_pkg_cid = v3_compiler_package_snapshot_cid("cheng/v3/runtime",
                                                            out->channel,
                                                            out->source_bundle_cid,
                                                            out->graph_cid,
                                                            out->surface_cid,
                                                            "local:runtime");
    out->manifest_root_cid = v3_compiler_manifest_root_cid(out->compiler_pkg_cid,
                                                           out->std_pkg_cid,
                                                           out->runtime_pkg_cid);
    out->world_head_cid = v3_compiler_world_head_cid("cheng/v3/compiler_universe",
                                                     out->channel,
                                                     1,
                                                     v3_cid_zero(),
                                                     out->manifest_root_cid,
                                                     out->graph_cid,
                                                     out->compiler_pkg_cid,
                                                     out->std_pkg_cid,
                                                     out->runtime_pkg_cid,
                                                     "local:world_head");
    out->canonical_output_digest_cid = v3_compiler_output_digest_cid(plan, out->canonical_graph_cid);
    out->output_digest_cid = v3_compiler_output_digest_cid(plan, out->graph_cid);
    out->receipt_cid = v3_compiler_receipt_cid(out->world_head_cid,
                                               out->graph_cid,
                                               out->canonical_graph_cid,
                                               plan,
                                               out->output_digest_cid,
                                               "stage3_local",
                                               out->canonical_output_digest_cid);
    out->compiler_snapshot_report = v3_compiler_package_snapshot_report("cheng/v3/compiler",
                                                                        out->channel,
                                                                        out->source_bundle_cid,
                                                                        out->graph_cid,
                                                                        out->surface_cid,
                                                                        "local:compiler",
                                                                        out->compiler_pkg_cid);
    out->std_snapshot_report = v3_compiler_package_snapshot_report("cheng/v3/std",
                                                                   out->channel,
                                                                   out->source_bundle_cid,
                                                                   out->graph_cid,
                                                                   out->surface_cid,
                                                                   "local:std",
                                                                   out->std_pkg_cid);
    out->runtime_snapshot_report = v3_compiler_package_snapshot_report("cheng/v3/runtime",
                                                                       out->channel,
                                                                       out->source_bundle_cid,
                                                                       out->graph_cid,
                                                                       out->surface_cid,
                                                                       "local:runtime",
                                                                       out->runtime_pkg_cid);
    out->world_report = v3_compiler_world_head_report(out->channel,
                                                      out->manifest_root_cid,
                                                      out->world_head_cid,
                                                      out->graph_cid,
                                                      out->compiler_pkg_cid,
                                                      out->std_pkg_cid,
                                                      out->runtime_pkg_cid);
    out->receipt_report = v3_compiler_receipt_report(out->world_head_cid,
                                                     out->graph_cid,
                                                     out->canonical_graph_cid,
                                                     plan,
                                                     out->output_digest_cid,
                                                     "stage3_local",
                                                     out->canonical_output_digest_cid,
                                                     out->receipt_cid);
    out->lock_text = v3_compiler_lock_text(out->world_head_cid,
                                           out->compiler_pkg_cid,
                                           out->std_pkg_cid,
                                           out->runtime_pkg_cid);
    out->world_head_pinned = false;
    out->lock_check_enabled = false;
    out->lock_verified = false;
    out->baseline_surface_checked = false;
    out->baseline_surface_compatible = false;
    if (requested_world_head && *requested_world_head != '\0') {
        char requested_copy[128];
        v3_trim_copy_text(requested_world_head, requested_copy, sizeof(requested_copy));
        if (!v3_streq(requested_copy, out->world_head_cid.hex)) {
            fprintf(stderr, "v3 compiler world: requested world head mismatch\n");
            v3_bytebuf_free(&graph_buf);
            v3_bytebuf_free(&canonical_buf);
            v3_bytebuf_free(&edge_buf);
            free(node_report);
            free(edge_report);
            v3_compiler_world_artifacts_free(out);
            return false;
        }
        out->world_head_pinned = true;
    }
    if (lock_path && *lock_path != '\0') {
        v3_copy_text(out->lock_path, sizeof(out->lock_path), lock_path);
        out->lock_check_enabled = true;
        if (!v3_trimmed_file_equals(lock_path, out->lock_text)) {
            fprintf(stderr, "v3 compiler world: lock world head mismatch\n");
            v3_bytebuf_free(&graph_buf);
            v3_bytebuf_free(&canonical_buf);
            v3_bytebuf_free(&edge_buf);
            free(node_report);
            free(edge_report);
            v3_compiler_world_artifacts_free(out);
            return false;
        }
        out->lock_verified = true;
    }
    if (baseline_surface_path && *baseline_surface_path != '\0') {
        v3_copy_text(out->baseline_surface_path, sizeof(out->baseline_surface_path), baseline_surface_path);
        out->baseline_surface_checked = true;
        if (!v3_trimmed_file_equals(baseline_surface_path, out->surface_report)) {
            fprintf(stderr, "v3 compiler csg: baseline export surface mismatch\n");
            v3_bytebuf_free(&graph_buf);
            v3_bytebuf_free(&canonical_buf);
            v3_bytebuf_free(&edge_buf);
            free(node_report);
            free(edge_report);
            v3_compiler_world_artifacts_free(out);
            return false;
        }
        out->baseline_surface_compatible = true;
    }
    v3_bytebuf_free(&graph_buf);
    v3_bytebuf_free(&canonical_buf);
    v3_bytebuf_free(&edge_buf);
    free(node_report);
    free(edge_report);
    return true;
}

static bool v3_build_compiler_world_artifacts(const V3SystemLinkPlanStub *plan,
                                              const char *channel_raw,
                                              const char *requested_world_head,
                                              const char *lock_path,
                                              const char *baseline_surface_path,
                                              V3CompilerWorldArtifacts *out) {
    return v3_build_compiler_world_artifacts_with_overrides(plan,
                                                            channel_raw,
                                                            requested_world_head,
                                                            lock_path,
                                                            baseline_surface_path,
                                                            NULL,
                                                            0U,
                                                            out);
}

static char *v3_system_link_plan_report(const V3BootstrapContract *contract,
                                        const V3SystemLinkPlanStub *plan) {
    size_t cap = 65536U;
    size_t used = 0U;
    char *out = (char *)v3_xmalloc(cap);
    char line[PATH_MAX * 2];
    char compiler_entry[PATH_MAX];
    out[0] = '\0';
    v3_resolve_contract_path(contract, "compiler_entry_source", compiler_entry, sizeof(compiler_entry));
    snprintf(line, sizeof(line), "entry=%s", plan->entry_path);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "output=%s", plan->output_path);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "extra_link_input=%s",
             plan->extra_link_input_path[0] != '\0' ? plan->extra_link_input_path : "-");
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "target=%s", plan->target_triple);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "supported_targets=%s", v3_supported_seed_target_csv());
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "target_support=%s", v3_target_support_state(plan->target_triple));
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "target_support_detail=%s", v3_target_support_detail(plan->target_triple));
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "emit=%s", plan->emit_kind);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "requested_emit_support=%s",
             v3_requested_emit_support_state(plan->target_triple, plan->emit_kind));
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "requested_emit_support_detail=%s",
             v3_requested_emit_support_detail(plan->target_triple, plan->emit_kind));
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "workspace_root=%s", plan->workspace_root);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "package_root=%s", plan->package_root);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "package_id=%s", plan->package_id);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "module_stem=%s", plan->module_stem);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "module_kind=%s", plan->module_kind);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "source_closure_count=%zu", plan->source_closure_count);
    v3_report_append(&out, &cap, &used, line);
    v3_report_append_csv_paths(&out, &cap, &used, "source_closure_paths", plan->source_closure_paths, plan->source_closure_count);
    v3_report_append_csv_paths(&out, &cap, &used, "owner_modules", plan->owner_modules, plan->owner_module_count);
    snprintf(line, sizeof(line), "import_edge_count=%zu", plan->import_edge_count);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "unresolved_import_count=%zu", plan->unresolved_import_count);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "has_compiler_top_level=%d", plan->has_compiler_top_level ? 1 : 0);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "main_function_present=%d", plan->main_function_present ? 1 : 0);
    v3_report_append(&out, &cap, &used, line);
    v3_report_append_csv_paths(&out, &cap, &used, "runtime_targets", plan->runtime_targets, plan->runtime_target_count);
    v3_report_append_csv_paths(&out, &cap, &used, "provider_modules", plan->provider_modules, plan->provider_module_count);
    snprintf(line, sizeof(line), "entry_symbol=%s", plan->entry_symbol[0] != '\0' ? plan->entry_symbol : "-");
    v3_report_append(&out, &cap, &used, line);
    v3_report_append_csv_reasons(&out, &cap, &used, "missing_reasons", plan->missing_reasons, plan->missing_reason_count);
    v3_report_append(&out, &cap, &used, "parser_syntax=v3_source_stub_v1");
    v3_report_append(&out, &cap, &used, "parser_source_kind=ordinary_cheng_source");
    snprintf(line, sizeof(line), "build_entry=%s", compiler_entry);
    v3_report_append(&out, &cap, &used, line);
    v3_report_append(&out, &cap, &used, "build_source_unit_count=22");
    v3_report_append(&out, &cap, &used, "link_mode=system_link");
    v3_report_append(&out, &cap, &used, "pipeline_stage=parser_stub_to_system_link_plan_stub");
    return out;
}

static char *v3_system_link_exec_report(const V3BootstrapContract *contract,
                                        const V3SystemLinkPlanStub *plan,
                                        const V3CompilerWorldArtifacts *world,
                                        const V3LoweringPlanStub *lowering,
                                        const V3PrimaryObjectPlanStub *primary,
                                        const V3ObjectPlanStub *object_plan,
                                        const V3NativeLinkPlanStub *native_link) {
    char *out = v3_system_link_plan_report(contract, plan);
    size_t used = strlen(out);
    size_t cap = used + 8192U;
    char line[256];
    char line_map_path[PATH_MAX];
    out = (char *)realloc(out, cap);
    if (!out) {
        fprintf(stderr, "[cheng_v3_seed] out of memory while extending exec report\n");
        exit(1);
    }
    if (world) {
        snprintf(line, sizeof(line), "source_bundle_cid=%s", world->source_bundle_cid.hex);
        v3_report_append(&out, &cap, &used, line);
        snprintf(line, sizeof(line), "compiler_csg_cid=%s", world->graph_cid.hex);
        v3_report_append(&out, &cap, &used, line);
        snprintf(line, sizeof(line), "canonical_compiler_csg_cid=%s", world->canonical_graph_cid.hex);
        v3_report_append(&out, &cap, &used, line);
        snprintf(line, sizeof(line), "compiler_csg_node_count=%zu", world->node_count);
        v3_report_append(&out, &cap, &used, line);
        snprintf(line, sizeof(line), "compiler_csg_edge_count=%zu", world->edge_count);
        v3_report_append(&out, &cap, &used, line);
        snprintf(line, sizeof(line), "export_surface_cid=%s", world->surface_cid.hex);
        v3_report_append(&out, &cap, &used, line);
        snprintf(line, sizeof(line), "export_surface_symbol_count=%zu", world->exported_count);
        v3_report_append(&out, &cap, &used, line);
        snprintf(line, sizeof(line), "world_head_cid=%s", world->world_head_cid.hex);
        v3_report_append(&out, &cap, &used, line);
        snprintf(line, sizeof(line), "compiler_pkg_cid=%s", world->compiler_pkg_cid.hex);
        v3_report_append(&out, &cap, &used, line);
        snprintf(line, sizeof(line), "std_pkg_cid=%s", world->std_pkg_cid.hex);
        v3_report_append(&out, &cap, &used, line);
        snprintf(line, sizeof(line), "runtime_pkg_cid=%s", world->runtime_pkg_cid.hex);
        v3_report_append(&out, &cap, &used, line);
        snprintf(line, sizeof(line), "world_head_pinned=%d", world->world_head_pinned ? 1 : 0);
        v3_report_append(&out, &cap, &used, line);
        snprintf(line, sizeof(line), "lock_check_enabled=%d", world->lock_check_enabled ? 1 : 0);
        v3_report_append(&out, &cap, &used, line);
        snprintf(line, sizeof(line), "lock_verified=%d", world->lock_verified ? 1 : 0);
        v3_report_append(&out, &cap, &used, line);
        snprintf(line, sizeof(line), "lock_path=%s", world->lock_path[0] != '\0' ? world->lock_path : "-");
        v3_report_append(&out, &cap, &used, line);
        snprintf(line, sizeof(line), "baseline_surface_checked=%d", world->baseline_surface_checked ? 1 : 0);
        v3_report_append(&out, &cap, &used, line);
        snprintf(line, sizeof(line), "baseline_surface_compatible=%d", world->baseline_surface_compatible ? 1 : 0);
        v3_report_append(&out, &cap, &used, line);
        snprintf(line, sizeof(line), "baseline_surface_path=%s",
                 world->baseline_surface_path[0] != '\0' ? world->baseline_surface_path : "-");
        v3_report_append(&out, &cap, &used, line);
        snprintf(line, sizeof(line), "compile_receipt_cid=%s", world->receipt_cid.hex);
        v3_report_append(&out, &cap, &used, line);
        snprintf(line, sizeof(line), "compile_output_digest=%s", world->output_digest_cid.hex);
        v3_report_append(&out, &cap, &used, line);
        snprintf(line, sizeof(line), "compile_canonical_output_digest=%s", world->canonical_output_digest_cid.hex);
        v3_report_append(&out, &cap, &used, line);
        v3_report_append(&out, &cap, &used, "compile_bootstrap_stage=stage3_local");
    }
    snprintf(line, sizeof(line), "lowering_function_count=%zu", lowering->function_count);
    v3_report_append(&out, &cap, &used, line);
    v3_report_append_csv_functions(&out, &cap, &used, "lowering_functions", lowering->functions, lowering->function_count);
    v3_report_append_csv_function_abi(&out, &cap, &used, "lowering_function_abi", lowering->functions, lowering->function_count);
    snprintf(line, sizeof(line), "lowering_entry_function=%s", plan->entry_symbol[0] != '\0' ? plan->entry_symbol : "-");
    v3_report_append(&out, &cap, &used, line);
    v3_report_append_csv_reasons(&out, &cap, &used, "lowering_missing_reasons", lowering->missing_reasons, lowering->missing_reason_count);
    snprintf(line, sizeof(line), "primary_object_format=%s",
             primary->object_format[0] != '\0' ? primary->object_format : "-");
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "primary_object_item_count=%zu", primary->item_count);
    v3_report_append(&out, &cap, &used, line);
    v3_report_append_csv_paths(&out, &cap, &used, "primary_object_symbols", primary->symbol_names, primary->item_count);
    snprintf(line, sizeof(line), "primary_object_instruction_word_count=%zu", primary->instruction_word_count);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "primary_object_data_label_count=%zu", primary->data_label_count);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "primary_object_reloc_count=%zu", primary->reloc_count);
    v3_report_append(&out, &cap, &used, line);
    v3_report_append_csv_reasons(&out, &cap, &used, "primary_object_missing_reasons", primary->missing_reasons, primary->missing_reason_count);
    snprintf(line, sizeof(line), "primary_object_unsupported_function_count=%zu", primary->unsupported_function_count);
    v3_report_append(&out, &cap, &used, line);
    v3_report_append_csv_paths(&out, &cap, &used, "primary_object_unsupported_functions",
                               primary->unsupported_function_symbols,
                               primary->unsupported_function_count);
    v3_report_append_csv_reasons(&out, &cap, &used, "primary_object_unsupported_body_kinds",
                                 primary->unsupported_body_kinds,
                                 primary->unsupported_function_count);
    v3_report_append_csv_reasons(&out, &cap, &used, "primary_object_unsupported_first_lines",
                                 primary->unsupported_first_lines,
                                 primary->unsupported_function_count);
    v3_report_append_csv_text256(&out, &cap, &used, "primary_object_unsupported_abi",
                                 primary->unsupported_abi_summaries,
                                 primary->unsupported_function_count);
    v3_report_append(&out, &cap, &used, "primary_object_pipeline_stage=lowering_plan_to_primary_object_plan");
    snprintf(line, sizeof(line), "primary_object_path=%s",
             object_plan->primary_object_path[0] != '\0' ? object_plan->primary_object_path : "-");
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "provider_object_count=%zu", object_plan->provider_object_count);
    v3_report_append(&out, &cap, &used, line);
    v3_report_append_csv_paths(&out, &cap, &used, "provider_source_paths",
                               object_plan->provider_source_paths,
                               object_plan->provider_source_count);
    v3_report_append_csv_paths(&out, &cap, &used, "provider_object_paths",
                               object_plan->provider_object_paths,
                               object_plan->provider_object_count);
    snprintf(line, sizeof(line), "linux_nolibc_support_enabled=%d",
             object_plan->linux_nolibc_support_enabled ? 1 : 0);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "linux_nolibc_runtime_source=%s",
             object_plan->linux_nolibc_runtime_source_path[0] != '\0' ?
                 object_plan->linux_nolibc_runtime_source_path :
                 "-");
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "linux_nolibc_runtime_object=%s",
             object_plan->linux_nolibc_runtime_object_path[0] != '\0' ?
                 object_plan->linux_nolibc_runtime_object_path :
                 "-");
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "linux_nolibc_startup_source=%s",
             object_plan->linux_nolibc_startup_source_path[0] != '\0' ?
                 object_plan->linux_nolibc_startup_source_path :
                 "-");
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "linux_nolibc_startup_object=%s",
             object_plan->linux_nolibc_startup_object_path[0] != '\0' ?
                 object_plan->linux_nolibc_startup_object_path :
                 "-");
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "object_link_input_count=%zu", object_plan->link_input_count);
    v3_report_append(&out, &cap, &used, line);
    v3_report_append_csv_paths(&out, &cap, &used, "object_link_inputs",
                               object_plan->link_input_paths,
                               object_plan->link_input_count);
    v3_report_append_csv_reasons(&out, &cap, &used, "object_missing_reasons",
                                 object_plan->missing_reasons,
                                 object_plan->missing_reason_count);
    v3_report_append(&out, &cap, &used, "object_pipeline_stage=primary_object_plan_to_object_plan");
    snprintf(line, sizeof(line), "native_linker_program=%s",
             native_link->linker_program[0] != '\0' ? native_link->linker_program : "-");
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "native_linker_flavor=%s",
             native_link->linker_flavor[0] != '\0' ? native_link->linker_flavor : "-");
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "native_output_kind=%s",
             native_link->output_kind[0] != '\0' ? native_link->output_kind : "-");
    v3_report_append(&out, &cap, &used, line);
    if (v3_streq(plan->emit_kind, "exe") && plan->output_path[0] != '\0') {
        v3_line_map_path_for_output(plan->output_path, line_map_path, sizeof(line_map_path));
        snprintf(line, sizeof(line), "debug_line_map_path=%s", line_map_path);
        v3_report_append(&out, &cap, &used, line);
    }
    snprintf(line, sizeof(line), "native_primary_object=%s",
             native_link->primary_object_path[0] != '\0' ? native_link->primary_object_path : "-");
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "native_link_input_count=%zu", native_link->link_input_count);
    v3_report_append(&out, &cap, &used, line);
    v3_report_append_csv_paths(&out, &cap, &used, "native_link_inputs",
                               native_link->link_input_paths,
                               native_link->link_input_count);
    snprintf(line, sizeof(line), "native_link_entry_symbol=%s",
             native_link->entry_symbol[0] != '\0' ? native_link->entry_symbol : "-");
    v3_report_append(&out, &cap, &used, line);
    v3_report_append_csv_reasons(&out, &cap, &used, "native_link_missing_reasons",
                                 native_link->missing_reasons,
                                 native_link->missing_reason_count);
    v3_report_append(&out, &cap, &used, "native_link_pipeline_stage=primary_object_plan_to_native_link_plan");
    v3_report_append(&out, &cap, &used, "exec_pipeline_stage=primary_object_plan_to_native_link_plan");
    return out;
}

static bool v3_load_contract_from_cli(int argc, char **argv, V3BootstrapContract *contract) {
    const char *in_path = v3_flag_value(argc, argv, "--in");
    if (in_path && *in_path != '\0') {
        return v3_parse_contract_file(contract, in_path);
    }
    if (CHENG_V3_EMBEDDED_CONTRACT_TEXT[0] != '\0') {
        return v3_parse_contract_text(contract,
                                      CHENG_V3_EMBEDDED_SOURCE_PATH[0] != '\0' ? CHENG_V3_EMBEDDED_SOURCE_PATH : "<embedded>",
                                      CHENG_V3_EMBEDDED_CONTRACT_TEXT);
    }
    fprintf(stderr, "[cheng_v3_seed] missing --in:<path>\n");
    return false;
}

static bool v3_load_runtime_contract(int argc, char **argv, V3BootstrapContract *contract) {
    const char *contract_path = v3_flag_value(argc, argv, "--contract-in");
    if (contract_path && *contract_path != '\0') {
        return v3_parse_contract_file(contract, contract_path);
    }
    if (CHENG_V3_EMBEDDED_CONTRACT_TEXT[0] != '\0') {
        return v3_parse_contract_text(contract,
                                      CHENG_V3_EMBEDDED_SOURCE_PATH[0] != '\0' ? CHENG_V3_EMBEDDED_SOURCE_PATH : "<embedded>",
                                      CHENG_V3_EMBEDDED_CONTRACT_TEXT);
    }
    fprintf(stderr, "[cheng_v3_seed] missing embedded bootstrap contract\n");
    return false;
}

static int v3_cmd_print_contract(int argc, char **argv) {
    V3BootstrapContract contract;
    char *normalized;
    if (!v3_load_contract_from_cli(argc, argv, &contract)) {
        return 1;
    }
    if (!v3_validate_contract(&contract)) {
        v3_contract_free(&contract);
        return 1;
    }
    normalized = v3_contract_normalized_text(&contract);
    if (!normalized) {
        v3_contract_free(&contract);
        return 1;
    }
    fputs(normalized, stdout);
    free(normalized);
    v3_contract_free(&contract);
    return 0;
}

static int v3_cmd_self_check(int argc, char **argv) {
    V3BootstrapContract contract;
    char *normalized;
    uint64_t hash;
    if (!v3_load_contract_from_cli(argc, argv, &contract)) {
        return 1;
    }
    if (!v3_validate_contract(&contract)) {
        v3_contract_free(&contract);
        return 1;
    }
    normalized = v3_contract_normalized_text(&contract);
    if (!normalized) {
        v3_contract_free(&contract);
        return 1;
    }
    hash = v3_fnv1a64(normalized);
    printf("bootstrap_name=%s\n", v3_contract_get(&contract, "bootstrap_name"));
    printf("target=%s\n", v3_contract_get(&contract, "target"));
    printf("compiler_parallel_model=%s\n", v3_contract_get(&contract, "compiler_parallel_model"));
    printf("program_parallel_model=%s\n", v3_contract_get(&contract, "program_parallel_model"));
    printf("contract_hash=%016llx\n", (unsigned long long)hash);
    puts("cheng_v3_bootstrap_self_check=ok");
    free(normalized);
    v3_contract_free(&contract);
    return 0;
}

static int v3_cmd_compile_bootstrap(int argc, char **argv) {
    V3BootstrapContract contract;
    const char *out_path = v3_flag_value(argc, argv, "--out");
    const char *report_path = v3_flag_value(argc, argv, "--report-out");
    char *normalized;
    char wrapper_path[PATH_MAX];
    if (!out_path || *out_path == '\0') {
        fprintf(stderr, "[cheng_v3_seed] missing --out:<path>\n");
        return 1;
    }
    if (!v3_load_contract_from_cli(argc, argv, &contract)) {
        return 1;
    }
    if (!v3_validate_contract(&contract)) {
        v3_contract_free(&contract);
        return 1;
    }
    normalized = v3_contract_normalized_text(&contract);
    if (!normalized) {
        v3_contract_free(&contract);
        return 1;
    }
    snprintf(wrapper_path, sizeof(wrapper_path), "%s.generated.c", out_path);
    if (!v3_write_wrapper_source(wrapper_path, contract.source_path, normalized)) {
        free(normalized);
        v3_contract_free(&contract);
        return 1;
    }
    if (!v3_ensure_parent_dir(out_path) || !v3_compile_wrapper(wrapper_path, out_path)) {
        free(normalized);
        v3_contract_free(&contract);
        return 1;
    }
    if (report_path && *report_path != '\0') {
        if (!v3_write_compile_report(report_path, &contract, contract.source_path, out_path, normalized)) {
            free(normalized);
            v3_contract_free(&contract);
            return 1;
        }
    }
    printf("compiled=%s\n", out_path);
    printf("contract_hash=%016llx\n", (unsigned long long)v3_fnv1a64(normalized));
    free(normalized);
    v3_contract_free(&contract);
    return 0;
}

static int v3_cmd_status(int argc, char **argv) {
    V3BootstrapContract contract;
    char compiler_entry[PATH_MAX];
    const char *target;
    if (!v3_load_runtime_contract(argc, argv, &contract)) {
        return 1;
    }
    if (!v3_validate_contract(&contract)) {
        v3_contract_free(&contract);
        return 1;
    }
    target = v3_contract_get(&contract, "target");
    v3_resolve_contract_path(&contract, "compiler_entry_source", compiler_entry, sizeof(compiler_entry));
    puts("cheng_v3c");
    puts("version=v3.compiler_runtime.v1");
    puts("execution=argv_control_plane");
    puts("bootstrap_mode=v3_selfhost");
    printf("contract_target=%s\n", target);
    printf("supported_targets=%s\n", v3_supported_seed_target_csv());
    printf("target_support=%s\n", v3_target_support_state(target));
    printf("target_support_detail=%s\n", v3_target_support_detail(target));
    printf("compiler_entry=%s\n", compiler_entry);
    printf("ordinary_command=%s\n", v3_contract_get(&contract, "ordinary_command"));
    printf("ordinary_pipeline=%s\n", v3_contract_get(&contract, "ordinary_pipeline_state"));
    v3_contract_free(&contract);
    return 0;
}

static int v3_try_exec_backend_driver_cli_passthrough(const V3BootstrapPaths *paths,
                                                      int argc,
                                                      char **argv,
                                                      const char *label,
                                                      bool require_ready);

static int v3_cmd_print_build_plan(int argc, char **argv) {
    V3BootstrapPaths paths;
    V3BootstrapContract contract;
    char workspace_root[PATH_MAX];
    char stage2_path[PATH_MAX];
    char output_path[PATH_MAX];
    char resolved[PATH_MAX];
    const char *target;
    int passthrough_rc;
    const char *keys[] = {
        NULL,
        "compiler_runtime_source",
        "compiler_request_source",
        NULL,
        NULL,
        NULL,
        NULL,
        "lang_parser_source",
        "backend_system_link_plan_source",
        "backend_lowering_plan_source",
        "backend_primary_object_plan_source",
        "backend_object_plan_source",
        "backend_native_link_plan_source",
        "backend_system_link_exec_source",
        "runtime_core_runtime_source",
        "runtime_compiler_runtime_source",
        "runtime_debug_runtime_source",
        "tooling_bootstrap_contract_source",
        "backend_build_plan_source"
    };
    const char *labels[] = {
        "backend_driver_entry_source",
        "compiler_runtime_source",
        "compiler_request_source",
        "tooling_source",
        "tooling_source",
        "tooling_source",
        "tooling_source",
        "lang_parser_source",
        "backend_system_link_plan_source",
        "backend_lowering_plan_source",
        "backend_primary_object_plan_source",
        "backend_object_plan_source",
        "backend_native_link_plan_source",
        "backend_system_link_exec_source",
        "runtime_core_runtime_source",
        "runtime_compiler_runtime_source",
        "runtime_debug_runtime_source",
        "tooling_source",
        "tooling_source"
    };
    const char *manual_paths[] = {
        "v3/src/tooling/backend_driver_main.cheng",
        NULL,
        NULL,
        "v3/src/tooling/compiler_world.cheng",
        "v3/src/tooling/compiler_csg.cheng",
        "v3/src/tooling/compiler_world_libp2p.cheng",
        "v3/src/tooling/compiler_equivalence.cheng",
        NULL,
        NULL,
        NULL,
        NULL,
        NULL,
        NULL,
        NULL,
        NULL,
        NULL,
        NULL,
        NULL,
        NULL,
        NULL,
        NULL
    };
    size_t i;
    v3_bootstrap_paths_init(&paths);
    if (v3_backend_driver_handoff_enabled()) {
        passthrough_rc = v3_try_exec_backend_driver_cli_passthrough(&paths,
                                                                    argc,
                                                                    argv,
                                                                    "cheng.backend_driver",
                                                                    true);
        if (passthrough_rc >= 0) {
            return passthrough_rc;
        }
    }
    if (!v3_load_runtime_contract(argc, argv, &contract)) {
        return 1;
    }
    if (!v3_validate_contract(&contract)) {
        v3_contract_free(&contract);
        return 1;
    }
    v3_contract_workspace_root(&contract, workspace_root, sizeof(workspace_root));
    v3_join_path(stage2_path, sizeof(stage2_path), workspace_root, "artifacts/v3_bootstrap/cheng.stage2");
    v3_join_path(output_path, sizeof(output_path), workspace_root, "artifacts/v3_backend_driver/cheng");
    target = v3_flag_value(argc, argv, "--target");
    if (target == NULL || *target == '\0') {
        target = v3_contract_get(&contract, "target");
    }
    printf("target=%s\n", target);
    printf("supported_targets=%s\n", v3_supported_seed_target_csv());
    printf("target_support=%s\n", v3_target_support_state(target));
    printf("target_support_detail=%s\n", v3_target_support_detail(target));
    puts("linker=system_link");
    printf("stage2_compiler=%s\n", stage2_path);
    printf("entry=%s\n", paths.backend_driver_entry_source);
    printf("output=%s\n", output_path);
    printf("source[0]=bootstrap_contract_source:%s\n", contract.source_path);
    for (i = 0; i < sizeof(keys) / sizeof(keys[0]); ++i) {
        if (manual_paths[i] != NULL) {
            v3_join_path(resolved, sizeof(resolved), workspace_root, manual_paths[i]);
        } else {
            v3_resolve_contract_path(&contract, keys[i], resolved, sizeof(resolved));
        }
        printf("source[%zu]=%s:%s\n", i + 1U, labels[i], resolved);
    }
    v3_contract_free(&contract);
    return 0;
}

static int v3_cmd_bootstrap_bridge(int argc, char **argv);
static bool v3_run_binary_capture_output(char *const argv_local[],
                                         const char *log_path,
                                         int *status_out);
static bool v3_status_is_exit_code(int status, int code);

static int v3_cmd_print_bootstrap(int argc, char **argv) {
    V3BootstrapPaths paths;
    char *snapshot_text;
    (void)argc;
    (void)argv;
    v3_bootstrap_paths_init(&paths);
    if (!v3_bootstrap_artifacts_ready(&paths)) {
        if (v3_cmd_bootstrap_bridge(0, NULL) != 0) {
            return 1;
        }
        v3_bootstrap_paths_init(&paths);
    }
    snapshot_text = v3_read_file(paths.snapshot_path);
    if (snapshot_text == NULL) {
        fprintf(stderr, "[cheng_v3_seed] missing bootstrap snapshot: %s\n", paths.snapshot_path);
        return 1;
    }
    fputs(snapshot_text, stdout);
    free(snapshot_text);
    return 0;
}

static int v3_cmd_bootstrap_bridge(int argc, char **argv) {
    V3BootstrapPaths paths;
    char lock_path[PATH_MAX];
    char bootstrap_compiler[PATH_MAX];
    char stage0_candidate[PATH_MAX];
    char in_flag[PATH_MAX + 16];
    char out_flag[PATH_MAX + 16];
    char report_flag[PATH_MAX + 16];
    bool lock_held = false;
    int rc = 1;
    (void)argc;
    (void)argv;
    v3_bootstrap_paths_init(&paths);
    if (paths.root[0] == '\0') {
        fprintf(stderr, "[cheng_v3_seed] bootstrap root detect failed\n");
        return 1;
    }
    if (!v3_mkdir_p(paths.out_dir)) {
        return 1;
    }
    v3_join_path(stage0_candidate, sizeof(stage0_candidate), paths.out_dir, "cheng.stage0.next");
    v3_join_path(lock_path, sizeof(lock_path), paths.out_dir, ".bootstrap_bridge.lock");
    if (v3_bootstrap_artifacts_fresh(&paths)) {
        printf("v3 bootstrap bridge ok: %s\n", paths.stage2);
        return 0;
    }
    if (!v3_acquire_pid_lock(lock_path, &lock_held, v3_bootstrap_ready_check, &paths, "bootstrap-bridge")) {
        return 1;
    }
    if (v3_bootstrap_artifacts_fresh(&paths)) {
        rc = 0;
        goto done;
    }
    if (access(paths.stage1_source, R_OK) != 0 ||
        access(paths.compiler_entry_source, R_OK) != 0 ||
        access(paths.compiler_runtime_source, R_OK) != 0 ||
        access(paths.compiler_request_source, R_OK) != 0 ||
        access(paths.tooling_gate_source, R_OK) != 0) {
        fprintf(stderr, "[cheng_v3_seed] bootstrap-bridge missing source input under %s\n", paths.root);
        goto done;
    }
    if (!v3_bootstrap_current_compiler_path(&paths, bootstrap_compiler, sizeof(bootstrap_compiler))) {
        fprintf(stderr, "[cheng_v3_seed] bootstrap-bridge missing live bootstrap compiler\n");
        goto done;
    }
    if (!v3_bootstrap_materialize_stage0(&paths, bootstrap_compiler, stage0_candidate)) {
        fprintf(stderr, "[cheng_v3_seed] bootstrap-bridge failed to materialize stage0 candidate from %s\n",
                bootstrap_compiler);
        goto done;
    }
    if (access(stage0_candidate, X_OK) != 0) {
        fprintf(stderr, "[cheng_v3_seed] bootstrap-bridge missing stage0 candidate: %s\n", stage0_candidate);
        goto done;
    }

    snprintf(in_flag, sizeof(in_flag), "--in:%s", paths.stage1_source);
    if (!v3_run_command_logged_argv(3U,
                                    (const char *[3]){ stage0_candidate, "self-check", in_flag },
                                    paths.stage0_self_check_log,
                                    "bootstrap-bridge stage0_self_check")) {
        goto done;
    }

    snprintf(out_flag, sizeof(out_flag), "--out:%s", paths.stage1);
    snprintf(report_flag, sizeof(report_flag), "--report-out:%s", paths.stage1_report);
    if (!v3_run_command_logged_argv(5U,
                                    (const char *[5]){ stage0_candidate,
                                                       "compile-bootstrap",
                                                       in_flag,
                                                       out_flag,
                                                       report_flag },
                                    paths.stage1_build_log,
                                    "bootstrap-bridge stage1_compile")) {
        goto done;
    }

    if (!v3_run_command_logged_argv(3U,
                                    (const char *[3]){ paths.stage1, "self-check", in_flag },
                                    paths.stage1_self_check_log,
                                    "bootstrap-bridge stage1_self_check")) {
        goto done;
    }

    snprintf(out_flag, sizeof(out_flag), "--out:%s", paths.stage2);
    snprintf(report_flag, sizeof(report_flag), "--report-out:%s", paths.stage2_report);
    if (!v3_run_command_logged_argv(5U,
                                    (const char *[5]){ paths.stage1,
                                                       "compile-bootstrap",
                                                       in_flag,
                                                       out_flag,
                                                       report_flag },
                                    paths.stage2_build_log,
                                    "bootstrap-bridge stage2_compile")) {
        goto done;
    }

    if (!v3_run_command_logged_argv(3U,
                                    (const char *[3]){ paths.stage2, "self-check", in_flag },
                                    paths.stage2_self_check_log,
                                    "bootstrap-bridge stage2_self_check")) {
        goto done;
    }

    snprintf(out_flag, sizeof(out_flag), "--out:%s", paths.stage3);
    snprintf(report_flag, sizeof(report_flag), "--report-out:%s", paths.stage3_report);
    if (!v3_run_command_logged_argv(5U,
                                    (const char *[5]){ paths.stage2,
                                                       "compile-bootstrap",
                                                       in_flag,
                                                       out_flag,
                                                       report_flag },
                                    paths.stage3_build_log,
                                    "bootstrap-bridge stage3_compile")) {
        goto done;
    }

    if (!v3_run_command_logged_argv(3U,
                                    (const char *[3]){ paths.stage3, "self-check", in_flag },
                                    paths.stage3_self_check_log,
                                    "bootstrap-bridge stage3_self_check")) {
        goto done;
    }

    if (!v3_run_command_logged_argv(3U,
                                    (const char *[3]){ paths.stage2, "print-contract", in_flag },
                                    paths.stage2_contract_log,
                                    "bootstrap-bridge stage2_contract")) {
        goto done;
    }

    if (!v3_run_command_logged_argv(3U,
                                    (const char *[3]){ paths.stage3, "print-contract", in_flag },
                                    paths.stage3_contract_log,
                                    "bootstrap-bridge stage3_contract")) {
        goto done;
    }

    if (!v3_file_text_equal(paths.stage2_contract_log, paths.stage3_contract_log)) {
        fprintf(stderr, "[cheng_v3_seed] bootstrap-bridge fixed point mismatch stage2/stage3\n");
        goto done;
    }
    if (strcmp(bootstrap_compiler, paths.stage0) != 0) {
        if (rename(stage0_candidate, paths.stage0) != 0) {
            perror("rename");
            goto done;
        }
        stage0_candidate[0] = '\0';
    } else {
        if (unlink(stage0_candidate) != 0 && errno != ENOENT) {
            perror("unlink");
            goto done;
        }
        stage0_candidate[0] = '\0';
    }
    if (!v3_bootstrap_write_env_and_snapshot(&paths)) {
        fprintf(stderr, "[cheng_v3_seed] bootstrap-bridge failed to write snapshot/env\n");
        goto done;
    }
    rc = 0;
done:
    if (stage0_candidate[0] != '\0') {
        unlink(stage0_candidate);
    }
    v3_release_pid_lock(lock_path, &lock_held);
    if (rc == 0) {
        printf("v3 bootstrap bridge ok: %s\n", paths.stage2);
    }
    return rc;
}

static int v3_cmd_build_backend_driver(int argc, char **argv) {
    V3BootstrapPaths paths;
    char lock_path[PATH_MAX];
    char log_path[PATH_MAX];
    char report_path[PATH_MAX];
    char compile_report_path[PATH_MAX];
    char status_log[PATH_MAX];
    char plan_log[PATH_MAX];
    char reference_plan_log[PATH_MAX];
    char package_root[PATH_MAX];
    char smoke_src[PATH_MAX];
    char smoke_bin[PATH_MAX];
    char smoke_compile_log[PATH_MAX];
    char smoke_run_log[PATH_MAX];
    char smoke_report_path[PATH_MAX];
    char root_flag[PATH_MAX + 16];
    char in_flag[PATH_MAX + 16];
    char target_flag[256];
    char out_flag[PATH_MAX + 16];
    char candidate_out[PATH_MAX];
    char candidate_build_out[PATH_MAX];
    char candidate_flag[PATH_MAX + 16];
    char report_flag[PATH_MAX + 16];
    char *run_argv[2];
    char *report_text = NULL;
    const char *target;
    bool default_target_request;
    bool lock_held = false;
    int rc = 1;
    int status = 0;
    v3_bootstrap_paths_init(&paths);
    candidate_out[0] = '\0';
    candidate_build_out[0] = '\0';
    target = v3_flag_value(argc, argv, "--target");
    if (target == NULL || *target == '\0') {
        target = paths.target;
    }
    default_target_request = v3_streq(target, paths.target);
    if (!v3_ensure_parent_dir(paths.backend_driver_out)) {
        return 1;
    }
    v3_join_path(lock_path, sizeof(lock_path), paths.root, "artifacts/v3_backend_driver/.build_backend_driver_v3.lock");
    if (!v3_acquire_pid_lock(lock_path, &lock_held, v3_backend_driver_ready_check, &paths, "build-backend-driver")) {
        return 1;
    }
    if (default_target_request && v3_backend_driver_ready(&paths)) {
        rc = 0;
        goto done;
    }
    if (!v3_bootstrap_artifacts_fresh(&paths) && v3_cmd_bootstrap_bridge(0, NULL) != 0) {
        goto done;
    }
    v3_join_path(log_path, sizeof(log_path), paths.root, "artifacts/v3_backend_driver/build_backend_driver_v3.log");
    v3_join_path(report_path, sizeof(report_path), paths.root, "artifacts/v3_backend_driver/build_backend_driver_v3.report.txt");
    v3_join_path(compile_report_path, sizeof(compile_report_path), paths.root, "artifacts/v3_backend_driver/build_backend_driver_v3.compile.report.txt");
    v3_join_path(status_log, sizeof(status_log), paths.root, "artifacts/v3_backend_driver/build_backend_driver_v3.status.txt");
    v3_join_path(plan_log, sizeof(plan_log), paths.root, "artifacts/v3_backend_driver/build_backend_driver_v3.plan.txt");
    v3_join_path(reference_plan_log, sizeof(reference_plan_log), paths.root, "artifacts/v3_backend_driver/reference_stage3.plan.txt");
    v3_join_path(package_root, sizeof(package_root), paths.root, "v3");
    v3_join_path(smoke_src, sizeof(smoke_src), paths.root, "v3/src/tests/ordinary_zero_exit_fixture.cheng");
    v3_join_path(smoke_bin, sizeof(smoke_bin), paths.root, "artifacts/v3_backend_driver/ordinary_zero_exit_fixture.selftest");
    snprintf(smoke_report_path, sizeof(smoke_report_path), "%s.report.txt", smoke_bin);
    v3_join_path(smoke_compile_log, sizeof(smoke_compile_log), paths.root, "artifacts/v3_backend_driver/ordinary_zero_exit_fixture.selftest.compile.log");
    v3_join_path(smoke_run_log, sizeof(smoke_run_log), paths.root, "artifacts/v3_backend_driver/ordinary_zero_exit_fixture.selftest.run.log");
    snprintf(candidate_out, sizeof(candidate_out), "%s.next", paths.backend_driver_out);
    v3_join_path(candidate_build_out,
                 sizeof(candidate_build_out),
                 paths.root,
                 "artifacts/v3_backend_driver/cheng_candidate");
    unlink(candidate_out);
    unlink(candidate_build_out);

    snprintf(root_flag, sizeof(root_flag), "--root:%s", package_root);
    snprintf(in_flag, sizeof(in_flag), "--in:%s", paths.backend_driver_entry_source);
    snprintf(target_flag, sizeof(target_flag), "--target:%s", target);
    snprintf(out_flag, sizeof(out_flag), "--out:%s", paths.backend_driver_out);
    snprintf(candidate_flag, sizeof(candidate_flag), "--out:%s", candidate_build_out);
    snprintf(report_flag, sizeof(report_flag), "--report-out:%s", compile_report_path);
    if (!v3_run_command_logged_argv(8U,
                                    (const char *[8]){ paths.stage3,
                                                       "system-link-exec",
                                                       root_flag,
                                                       in_flag,
                                                       "--emit:exe",
                                                       target_flag,
                                                       candidate_flag,
                                                       report_flag },
                                    log_path,
                                    "build-backend-driver compile")) {
        goto done;
    }
    if (access(candidate_build_out, X_OK) != 0) {
        fprintf(stderr, "[cheng_v3_seed] build-backend-driver missing output: %s\n", candidate_build_out);
        goto done;
    }
    if (rename(candidate_build_out, candidate_out) != 0) {
        perror("rename");
        fprintf(stderr, "[cheng_v3_seed] build-backend-driver failed to stage candidate: %s\n", candidate_out);
        goto done;
    }

    if (!v3_run_command_logged_argv(2U,
                                    (const char *[2]){ candidate_out, "status" },
                                    status_log,
                                    "build-backend-driver status")) {
        goto done;
    }

    {
        char verify_export_log[PATH_MAX];
        v3_join_path(verify_export_log,
                     sizeof(verify_export_log),
                     paths.root,
                     "artifacts/v3_backend_driver/build_backend_driver_v3.verify_export_visibility.txt");
        if (!v3_run_command_logged_argv(2U,
                                        (const char *[2]){ candidate_out, "verify-export-visibility" },
                                        verify_export_log,
                                        "build-backend-driver verify-export-visibility")) {
            goto done;
        }
    }

    if (!v3_run_command_logged_argv(3U,
                                    (const char *[3]){ candidate_out, "print-build-plan", target_flag },
                                    plan_log,
                                    "build-backend-driver print-build-plan")) {
        goto done;
    }

    if (!v3_run_command_logged_argv(3U,
                                    (const char *[3]){ paths.stage3, "print-build-plan", target_flag },
                                    reference_plan_log,
                                    "build-backend-driver reference-stage3")) {
        goto done;
    }

    if (!v3_build_plan_files_equal(plan_log, reference_plan_log)) {
        fprintf(stderr, "[cheng_v3_seed] build-backend-driver plan drift vs stage3: %s\n", paths.backend_driver_out);
        goto done;
    }

    snprintf(in_flag, sizeof(in_flag), "--in:%s", smoke_src);
    snprintf(root_flag, sizeof(root_flag), "--root:%s", package_root);
    snprintf(out_flag, sizeof(out_flag), "--out:%s", smoke_bin);
    snprintf(report_flag, sizeof(report_flag), "--report-out:%s", smoke_report_path);
    if (!v3_run_command_logged_argv(8U,
                                    (const char *[8]){ candidate_out,
                                                       "system-link-exec",
                                                       root_flag,
                                                       in_flag,
                                                       "--emit:exe",
                                                       target_flag,
                                                       out_flag,
                                                       report_flag },
                                    smoke_compile_log,
                                    "build-backend-driver zero-exit compile")) {
        goto done;
    }
    if (access(smoke_bin, X_OK) != 0) {
        fprintf(stderr, "[cheng_v3_seed] build-backend-driver zero-exit smoke missing output: %s\n", smoke_bin);
        goto done;
    }
    run_argv[0] = smoke_bin;
    run_argv[1] = NULL;
    if (!v3_run_binary_capture_output(run_argv, smoke_run_log, &status) ||
        !v3_status_is_exit_code(status, 0)) {
        fprintf(stderr, "[cheng_v3_seed] build-backend-driver zero-exit smoke failed log=%s\n", smoke_run_log);
        goto done;
    }
    if (rename(candidate_out, paths.backend_driver_out) != 0) {
        perror("rename");
        fprintf(stderr, "[cheng_v3_seed] build-backend-driver failed to install candidate: %s\n",
                paths.backend_driver_out);
        goto done;
    }
    {
        char verify_export_parallel_log[PATH_MAX];
        v3_join_path(verify_export_parallel_log,
                     sizeof(verify_export_parallel_log),
                     paths.root,
                     "artifacts/v3_backend_driver/build_backend_driver_v3.verify_export_visibility_parallel.txt");
        if (!v3_run_command_logged_argv(2U,
                                        (const char *[2]){ paths.backend_driver_out, "verify-export-visibility-parallel" },
                                        verify_export_parallel_log,
                                        "build-backend-driver installed verify-export-visibility-parallel")) {
            goto done;
        }
    }

    report_text = (char *)v3_xmalloc(12288U);
    snprintf(report_text,
             12288U,
             "target=%s\n"
             "bootstrap_kind=v3_selfhost\n"
             "compiler_class=ordinary_compiler\n"
             "stage0_compiler=%s\n"
             "stage1_compiler=%s\n"
             "stage2_compiler=%s\n"
             "stage3_compiler=%s\n"
             "bootstrap_contract_source=%s\n"
             "planned_entry_source=%s\n"
             "planned_runtime_source=%s\n"
             "planned_request_source=%s\n"
             "materialized_source=%s\n"
             "output=%s\n"
             "status_log=%s\n"
             "verify_export_visibility_parallel_log=%s\n"
             "plan_log=%s\n"
             "reference_plan_log=%s\n"
             "log=%s\n"
             "zero_exit_smoke_source=%s\n"
             "zero_exit_smoke_output=%s\n"
             "zero_exit_smoke_report=%s\n"
             "zero_exit_smoke_compile_log=%s\n"
             "zero_exit_smoke_run_log=%s\n",
             target,
             paths.stage0,
             paths.stage1,
             paths.stage2,
             paths.stage3,
             paths.stage1_source,
             paths.backend_driver_entry_source,
             paths.compiler_runtime_source,
             paths.compiler_request_source,
             paths.backend_driver_entry_source,
             paths.backend_driver_out,
             status_log,
             "artifacts/v3_backend_driver/build_backend_driver_v3.verify_export_visibility_parallel.txt",
             plan_log,
             reference_plan_log,
             log_path,
             smoke_src,
             smoke_bin,
             smoke_report_path,
             smoke_compile_log,
             smoke_run_log);
    if (!v3_ensure_parent_dir(report_path) || !v3_write_text_file(report_path, report_text)) {
        fprintf(stderr, "[cheng_v3_seed] build-backend-driver failed to write report: %s\n", report_path);
        goto done;
    }
    rc = 0;
done:
    free(report_text);
    if (rc != 0 && candidate_out[0] != '\0') {
        unlink(candidate_out);
    }
    if (rc != 0 && candidate_build_out[0] != '\0') {
        unlink(candidate_build_out);
    }
    v3_release_pid_lock(lock_path, &lock_held);
    if (rc == 0) {
        printf("v3 backend driver ok: %s\n", paths.backend_driver_out);
    }
    return rc;
}

static int v3_cmd_run_smokes(int argc, char **argv) {
    V3BootstrapPaths paths;
    char log_path[PATH_MAX];
    char *command;
    char *quoted_bin;
    char *quoted_in;
    const char *bins[4];
    size_t i;
    size_t cap = PATH_MAX * 4U;
    (void)argc;
    (void)argv;
    v3_bootstrap_paths_init(&paths);
    if (!v3_bootstrap_artifacts_ready(&paths) && v3_cmd_bootstrap_bridge(0, NULL) != 0) {
        return 1;
    }
    bins[0] = paths.stage0;
    bins[1] = paths.stage1;
    bins[2] = paths.stage2;
    bins[3] = paths.stage3;
    command = (char *)v3_xmalloc(cap);
    for (i = 0; i < 4U; ++i) {
        const char *bin = bins[i];
        const char *base = strrchr(bin, '/');
        if (access(bin, X_OK) != 0) {
            fprintf(stderr, "[cheng_v3_seed] run-smokes missing bootstrap compiler: %s\n", bin);
            free(command);
            return 1;
        }
        v3_join_path(log_path, sizeof(log_path), paths.root, "artifacts/v3_gate");
        if (!v3_mkdir_p(log_path)) {
            free(command);
            return 1;
        }
        v3_join_path(log_path, sizeof(log_path), log_path, base != NULL ? base + 1 : bin);
        strncat(log_path, ".self-check.log", sizeof(log_path) - strlen(log_path) - 1U);
        quoted_bin = v3_shell_quote(bin);
        quoted_in = v3_shell_quote(paths.stage1_source);
        snprintf(command, cap, "%s self-check --in:%s", quoted_bin, quoted_in);
        free(quoted_bin);
        free(quoted_in);
        if (!v3_run_shell_command_logged(command, log_path, "run-smokes self-check")) {
            free(command);
            return 1;
        }
    }
    free(command);
    return 0;
}

static bool v3_is_ident_char(int ch) {
    return isalnum((unsigned char)ch) || ch == '_';
}

static bool v3_path_has_named_segment(const char *path, const char *segment) {
    size_t len = strlen(segment);
    const char *cursor = path;
    while ((cursor = strstr(cursor, segment)) != NULL) {
        bool left_ok = cursor == path || cursor[-1] == '/';
        bool right_ok = cursor[len] == '\0' || cursor[len] == '/';
        if (left_ok && right_ok) {
            return true;
        }
        cursor += 1;
    }
    return false;
}

static bool v3_path_has_tests_segment(const char *path) {
    return v3_path_has_named_segment(path, "tests");
}

static bool v3_is_cheng_source_path(const char *path) {
    const char *dot = strrchr(path, '.');
    return dot != NULL && strcmp(dot, ".cheng") == 0;
}

static bool v3_text_contains_whole_word(const char *text, const char *word) {
    size_t len = strlen(word);
    const char *cursor = text;
    while ((cursor = strstr(cursor, word)) != NULL) {
        bool left_ok = cursor == text || !v3_is_ident_char((unsigned char)cursor[-1]);
        bool right_ok = cursor[len] == '\0' || !v3_is_ident_char((unsigned char)cursor[len]);
        if (left_ok && right_ok) {
            return true;
        }
        cursor += 1;
    }
    return false;
}

static bool v3_text_contains_label_colon_str(const char *text, const char *label) {
    size_t len = strlen(label);
    const char *cursor = text;
    while ((cursor = strstr(cursor, label)) != NULL) {
        const char *probe = cursor + len;
        if ((cursor == text || !v3_is_ident_char((unsigned char)cursor[-1])) &&
            *probe == ':') {
            probe += 1;
            while (*probe == ' ' || *probe == '\t' || *probe == '\r' || *probe == '\n') {
                probe += 1;
            }
            if (strncmp(probe, "str", 3U) == 0 &&
                (probe[3] == '\0' || !v3_is_ident_char((unsigned char)probe[3]))) {
                return true;
            }
        }
        cursor += 1;
    }
    return false;
}

static bool v3_text_contains_forbidden_pattern(const char *text,
                                               const V3ForbiddenPattern *pattern) {
    if (pattern->kind == V3_FORBIDDEN_WHOLE_WORD) {
        return v3_text_contains_whole_word(text, pattern->needle);
    }
    if (pattern->kind == V3_FORBIDDEN_LABEL_COLON_STR) {
        return v3_text_contains_label_colon_str(text, pattern->needle);
    }
    return strstr(text, pattern->needle) != NULL;
}

static bool v3_scan_forbidden_source_dir(const char *src_dir,
                                         V3ForbiddenHitVec *hits) {
    static const V3ForbiddenPattern patterns[] = {
        { V3_FORBIDDEN_SUBSTRING, "local_payload", "local_payload" },
        { V3_FORBIDDEN_SUBSTRING, "exec_plan_payload", "exec_plan_payload" },
        { V3_FORBIDDEN_SUBSTRING, "entry_bridge", "entry_bridge" },
        { V3_FORBIDDEN_SUBSTRING, "payloadText", "payloadText" },
        { V3_FORBIDDEN_LABEL_COLON_STR, "runtimeTarget", "runtimeTarget: str" },
        { V3_FORBIDDEN_LABEL_COLON_STR, "opName", "opName: str" },
        { V3_FORBIDDEN_LABEL_COLON_STR, "kind", "kind: str" },
        { V3_FORBIDDEN_WHOLE_WORD, "BigInt", "BigInt" }
    };
    V3OwnedStringVec dirs;
    char *dir_path;
    bool ok = true;
    v3_owned_string_vec_init(&dirs);
    v3_owned_string_vec_push_dup(&dirs, src_dir);
    while ((dir_path = v3_owned_string_vec_pop(&dirs)) != NULL) {
        DIR *dir = opendir(dir_path);
        if (dir == NULL) {
            fprintf(stderr, "[cheng_v3_seed] scan-hotpath open failed: %s\n", dir_path);
            free(dir_path);
            ok = false;
            break;
        }
        for (;;) {
            struct dirent *entry = readdir(dir);
            char child_path[PATH_MAX];
            struct stat st;
            size_t i;
            char *text;
            if (entry == NULL) {
                break;
            }
            if (strcmp(entry->d_name, ".") == 0 || strcmp(entry->d_name, "..") == 0) {
                continue;
            }
            v3_join_path(child_path, sizeof(child_path), dir_path, entry->d_name);
            if (stat(child_path, &st) != 0) {
                fprintf(stderr, "[cheng_v3_seed] scan-hotpath stat failed: %s\n", child_path);
                ok = false;
                break;
            }
            if (S_ISDIR(st.st_mode)) {
                if (!v3_path_has_tests_segment(child_path)) {
                    v3_owned_string_vec_push_dup(&dirs, child_path);
                }
                continue;
            }
            if (!S_ISREG(st.st_mode) ||
                v3_path_has_tests_segment(child_path) ||
                !v3_is_cheng_source_path(child_path)) {
                continue;
            }
            text = v3_read_file(child_path);
            if (text == NULL) {
                fprintf(stderr, "[cheng_v3_seed] scan-hotpath read failed: %s\n", child_path);
                ok = false;
                break;
            }
            for (i = 0U; i < sizeof(patterns) / sizeof(patterns[0]); ++i) {
                if (v3_text_contains_forbidden_pattern(text, &patterns[i])) {
                    v3_forbidden_hit_vec_push(hits, child_path, patterns[i].label);
                }
            }
            free(text);
        }
        closedir(dir);
        free(dir_path);
        if (!ok) {
            break;
        }
    }
    while ((dir_path = v3_owned_string_vec_pop(&dirs)) != NULL) {
        free(dir_path);
    }
    v3_owned_string_vec_free(&dirs);
    return ok;
}

static int v3_cmd_scan_hotpath(int argc, char **argv) {
    V3BootstrapPaths paths;
    V3ForbiddenHitVec hits;
    char src_dir[PATH_MAX];
    char *report;
    size_t cap;
    size_t used = 0U;
    size_t i;
    char line[PATH_MAX * 2U];
    (void)argc;
    (void)argv;
    v3_bootstrap_paths_init(&paths);
    if (paths.root[0] == '\0') {
        fprintf(stderr, "[cheng_v3_seed] scan-hotpath root detect failed\n");
        return 1;
    }
    v3_join_path(src_dir, sizeof(src_dir), paths.root, "v3/src");
    if (access(src_dir, R_OK) != 0) {
        fprintf(stderr, "[cheng_v3_seed] scan-hotpath source dir not found: %s\n", src_dir);
        return 1;
    }
    v3_forbidden_hit_vec_init(&hits);
    if (!v3_scan_forbidden_source_dir(src_dir, &hits)) {
        v3_forbidden_hit_vec_free(&hits);
        return 1;
    }
    if (hits.len == 0U) {
        v3_forbidden_hit_vec_free(&hits);
        puts("v3 scan: ok");
        return 0;
    }
    cap = 256U + hits.len * (PATH_MAX + 96U);
    report = (char *)v3_xmalloc(cap);
    report[0] = '\0';
    for (i = 0U; i < hits.len; ++i) {
        snprintf(line,
                 sizeof(line),
                 "v3 scan: hit forbidden pattern: %s @ %s",
                 hits.items[i].label,
                 hits.items[i].path);
        v3_report_append(&report, &cap, &used, line);
    }
    fputs(report, stdout);
    free(report);
    v3_forbidden_hit_vec_free(&hits);
    return 1;
}

typedef int (*V3SeedCommandFn)(int argc, char **argv);

static bool v3_text_contains_substring(const char *text, const char *needle) {
    return text != NULL && needle != NULL && strstr(text, needle) != NULL;
}

static bool v3_text_has_line(const char *text, const char *line) {
    size_t want_len = strlen(line);
    const char *cursor = text;
    if (text == NULL || line == NULL) {
        return false;
    }
    while (cursor != NULL && *cursor != '\0') {
        const char *end = strchr(cursor, '\n');
        size_t len = end != NULL ? (size_t)(end - cursor) : strlen(cursor);
        if (len == want_len && strncmp(cursor, line, want_len) == 0) {
            return true;
        }
        cursor = end != NULL ? end + 1 : NULL;
    }
    return false;
}

static bool v3_text_has_line_with_prefix(const char *text, const char *prefix) {
    size_t prefix_len = strlen(prefix);
    const char *cursor = text;
    if (text == NULL || prefix == NULL) {
        return false;
    }
    while (cursor != NULL && *cursor != '\0') {
        const char *end = strchr(cursor, '\n');
        size_t len = end != NULL ? (size_t)(end - cursor) : strlen(cursor);
        if (len >= prefix_len && strncmp(cursor, prefix, prefix_len) == 0) {
            return true;
        }
        cursor = end != NULL ? end + 1 : NULL;
    }
    return false;
}

static bool v3_text_has_positive_integer_line(const char *text, const char *prefix) {
    size_t prefix_len = strlen(prefix);
    const char *cursor = text;
    if (text == NULL || prefix == NULL) {
        return false;
    }
    while (cursor != NULL && *cursor != '\0') {
        const char *end = strchr(cursor, '\n');
        size_t len = end != NULL ? (size_t)(end - cursor) : strlen(cursor);
        if (len > prefix_len && strncmp(cursor, prefix, prefix_len) == 0) {
            size_t i;
            long long value = 0;
            bool all_digits = true;
            for (i = prefix_len; i < len; ++i) {
                if (cursor[i] < '0' || cursor[i] > '9') {
                    all_digits = false;
                    break;
                }
                value = value * 10 + (long long)(cursor[i] - '0');
            }
            if (all_digits && value > 0) {
                return true;
            }
        }
        cursor = end != NULL ? end + 1 : NULL;
    }
    return false;
}

static const char *v3_text_after_marker(const char *text, const char *marker) {
    const char *hit;
    if (text == NULL || marker == NULL) {
        return NULL;
    }
    hit = strstr(text, marker);
    if (hit == NULL) {
        return NULL;
    }
    return hit + strlen(marker);
}

static bool v3_expect_text_has_line(const char *label, const char *text, const char *line) {
    if (v3_text_has_line(text, line)) {
        return true;
    }
    fprintf(stderr, "[cheng_v3_seed] %s missing line: %s\n", label, line);
    return false;
}

static bool v3_expect_text_has_prefix(const char *label, const char *text, const char *prefix) {
    if (v3_text_has_line_with_prefix(text, prefix)) {
        return true;
    }
    fprintf(stderr, "[cheng_v3_seed] %s missing line prefix: %s\n", label, prefix);
    return false;
}

static bool v3_expect_text_contains(const char *label, const char *text, const char *needle) {
    if (v3_text_contains_substring(text, needle)) {
        return true;
    }
    fprintf(stderr, "[cheng_v3_seed] %s missing text: %s\n", label, needle);
    return false;
}

static bool v3_expect_text_not_contains(const char *label, const char *text, const char *needle) {
    if (!v3_text_contains_substring(text, needle)) {
        return true;
    }
    fprintf(stderr, "[cheng_v3_seed] %s unexpected text: %s\n", label, needle);
    return false;
}

static bool v3_text_line_value(const char *text,
                               const char *key,
                               char *out,
                               size_t cap) {
    size_t key_len = strlen(key);
    const char *cursor = text;
    if (out == NULL || cap == 0U) {
        return false;
    }
    out[0] = '\0';
    if (text == NULL || key == NULL) {
        return false;
    }
    while (cursor != NULL && *cursor != '\0') {
        const char *end = strchr(cursor, '\n');
        size_t len = end != NULL ? (size_t)(end - cursor) : strlen(cursor);
        if (len > key_len + 1U &&
            strncmp(cursor, key, key_len) == 0 &&
            cursor[key_len] == '=') {
            size_t value_len = len - key_len - 1U;
            if (value_len >= cap) {
                value_len = cap - 1U;
            }
            memcpy(out, cursor + key_len + 1U, value_len);
            out[value_len] = '\0';
            return true;
        }
        cursor = end != NULL ? end + 1 : NULL;
    }
    return false;
}

static bool v3_expect_file_exists_nonempty(const char *label, const char *path) {
    if (v3_path_exists_nonempty(path)) {
        return true;
    }
    fprintf(stderr, "[cheng_v3_seed] %s missing file: %s\n", label, path);
    return false;
}

static bool v3_report_extract_symbol_name(const char *line,
                                          char *out,
                                          size_t cap) {
    const char *eq;
    const char *bar;
    size_t len;
    if (line == NULL || out == NULL || cap == 0U || !v3_startswith(line, "symbol[")) {
        return false;
    }
    eq = strchr(line, '=');
    if (eq == NULL) {
        return false;
    }
    eq += 1;
    bar = strchr(eq, '|');
    if (bar == NULL || bar <= eq) {
        return false;
    }
    len = (size_t)(bar - eq);
    if (len >= cap) {
        len = cap - 1U;
    }
    memcpy(out, eq, len);
    out[len] = '\0';
    return true;
}

static void v3_normalize_report_symbol_name(const char *name,
                                            char *out,
                                            size_t cap) {
    if (out == NULL || cap == 0U) {
        return;
    }
    if (name == NULL) {
        out[0] = '\0';
        return;
    }
    if (name[0] == '_') {
        v3_copy_text(out, cap, name + 1);
        return;
    }
    v3_copy_text(out, cap, name);
}

static bool v3_report_symbol_line_is_export(const char *line) {
    if (line == NULL || !v3_startswith(line, "symbol[")) {
        return false;
    }
    if (v3_text_contains_substring(line, "|type=0xf|")) {
        return true;
    }
    if (v3_text_contains_substring(line, "|bind=1|") &&
        !v3_text_contains_substring(line, "|shndx=0|")) {
        return true;
    }
    return false;
}

static bool v3_report_symbol_line_is_undefined(const char *line) {
    if (line == NULL || !v3_startswith(line, "symbol[")) {
        return false;
    }
    if (v3_text_contains_substring(line, "|type=0x1|") &&
        v3_text_contains_substring(line, "|sect=0|")) {
        return true;
    }
    if (v3_text_contains_substring(line, "|bind=1|") &&
        v3_text_contains_substring(line, "|shndx=0|")) {
        return true;
    }
    return false;
}

static bool CHENG_V3_UNUSED v3_expect_object_export_whitelist(const char *label,
                                                              const char *object_path,
                                                              const char *const *allowed,
                                                              size_t allowed_count) {
    char *report = NULL;
    const char *cursor;
    bool *seen = NULL;
    bool ok = true;
    size_t export_count = 0U;
    size_t i;
    report = v3_debug_object_report(object_path);
    if (report == NULL) {
        fprintf(stderr, "[cheng_v3_seed] %s failed to inspect object: %s\n", label, object_path);
        return false;
    }
    seen = (bool *)v3_xmalloc(sizeof(bool) * allowed_count);
    memset(seen, 0, sizeof(bool) * allowed_count);
    cursor = report;
    while (cursor != NULL && *cursor != '\0') {
        const char *end = strchr(cursor, '\n');
        size_t len = end != NULL ? (size_t)(end - cursor) : strlen(cursor);
        char line[PATH_MAX * 2];
        size_t copy_len = len < sizeof(line) - 1U ? len : sizeof(line) - 1U;
        memcpy(line, cursor, copy_len);
        line[copy_len] = '\0';
        if (v3_report_symbol_line_is_export(line)) {
            char raw_name[PATH_MAX];
            char normalized[PATH_MAX];
            bool matched = false;
            if (!v3_report_extract_symbol_name(line, raw_name, sizeof(raw_name))) {
                fprintf(stderr, "[cheng_v3_seed] %s malformed symbol line: %s\n", label, line);
                ok = false;
                break;
            }
            v3_normalize_report_symbol_name(raw_name, normalized, sizeof(normalized));
            for (i = 0U; i < allowed_count; ++i) {
                if (strcmp(normalized, allowed[i]) == 0) {
                    if (seen[i]) {
                        fprintf(stderr, "[cheng_v3_seed] %s duplicate export: %s\n", label, normalized);
                        ok = false;
                    }
                    seen[i] = true;
                    matched = true;
                    break;
                }
            }
            if (!matched) {
                fprintf(stderr, "[cheng_v3_seed] %s unexpected export: %s (%s)\n", label, normalized, object_path);
                ok = false;
            }
            export_count += 1U;
        }
        cursor = end != NULL ? end + 1 : NULL;
    }
    if (ok && export_count != allowed_count) {
        fprintf(stderr,
                "[cheng_v3_seed] %s export count mismatch: expected=%zu actual=%zu\n",
                label,
                allowed_count,
                export_count);
        ok = false;
    }
    for (i = 0U; i < allowed_count; ++i) {
        if (!seen[i]) {
            fprintf(stderr, "[cheng_v3_seed] %s missing export: %s\n", label, allowed[i]);
            ok = false;
        }
    }
    free(seen);
    free(report);
    return ok;
}

static bool v3_expect_object_export_contract(const char *label,
                                             const char *object_path,
                                             const char *const *public_exports,
                                             size_t public_export_count,
                                             const char *internal_prefix) {
    char *report = NULL;
    const char *cursor;
    bool *seen = NULL;
    bool ok = true;
    size_t export_count = 0U;
    size_t i;
    report = v3_debug_object_report(object_path);
    if (report == NULL) {
        fprintf(stderr, "[cheng_v3_seed] %s failed to inspect object: %s\n", label, object_path);
        return false;
    }
    seen = (bool *)v3_xmalloc(sizeof(bool) * public_export_count);
    memset(seen, 0, sizeof(bool) * public_export_count);
    cursor = report;
    while (cursor != NULL && *cursor != '\0') {
        const char *end = strchr(cursor, '\n');
        size_t len = end != NULL ? (size_t)(end - cursor) : strlen(cursor);
        char line[PATH_MAX * 2];
        size_t copy_len = len < sizeof(line) - 1U ? len : sizeof(line) - 1U;
        memcpy(line, cursor, copy_len);
        line[copy_len] = '\0';
        if (v3_report_symbol_line_is_export(line)) {
            char raw_name[PATH_MAX];
            char normalized[PATH_MAX];
            bool matched_public = false;
            if (!v3_report_extract_symbol_name(line, raw_name, sizeof(raw_name))) {
                fprintf(stderr, "[cheng_v3_seed] %s malformed symbol line: %s\n", label, line);
                ok = false;
                break;
            }
            v3_normalize_report_symbol_name(raw_name, normalized, sizeof(normalized));
            for (i = 0U; i < public_export_count; ++i) {
                if (strcmp(normalized, public_exports[i]) == 0) {
                    if (seen[i]) {
                        fprintf(stderr, "[cheng_v3_seed] %s duplicate export: %s\n", label, normalized);
                        ok = false;
                    }
                    seen[i] = true;
                    matched_public = true;
                    break;
                }
            }
            if (!matched_public &&
                !(internal_prefix != NULL && internal_prefix[0] != '\0' &&
                  v3_startswith(normalized, internal_prefix))) {
                fprintf(stderr, "[cheng_v3_seed] %s unexpected export: %s (%s)\n", label, normalized, object_path);
                ok = false;
            }
            export_count += 1U;
        }
        cursor = end != NULL ? end + 1 : NULL;
    }
    if (ok && export_count < public_export_count) {
        fprintf(stderr,
                "[cheng_v3_seed] %s export count too small: expected_at_least=%zu actual=%zu\n",
                label,
                public_export_count,
                export_count);
        ok = false;
    }
    for (i = 0U; i < public_export_count; ++i) {
        if (!seen[i]) {
            fprintf(stderr, "[cheng_v3_seed] %s missing export: %s\n", label, public_exports[i]);
            ok = false;
        }
    }
    free(seen);
    free(report);
    return ok;
}

static bool v3_expect_object_undefined_whitelist(const char *label,
                                                 const char *object_path,
                                                 const char *const *allowed,
                                                 size_t allowed_count) {
    char *report = NULL;
    const char *cursor;
    bool *seen = NULL;
    bool ok = true;
    size_t undefined_count = 0U;
    size_t i;
    report = v3_debug_object_report(object_path);
    if (report == NULL) {
        fprintf(stderr, "[cheng_v3_seed] %s failed to inspect object: %s\n", label, object_path);
        return false;
    }
    seen = (bool *)v3_xmalloc(sizeof(bool) * allowed_count);
    memset(seen, 0, sizeof(bool) * allowed_count);
    cursor = report;
    while (cursor != NULL && *cursor != '\0') {
        const char *end = strchr(cursor, '\n');
        size_t len = end != NULL ? (size_t)(end - cursor) : strlen(cursor);
        char line[PATH_MAX * 2];
        size_t copy_len = len < sizeof(line) - 1U ? len : sizeof(line) - 1U;
        memcpy(line, cursor, copy_len);
        line[copy_len] = '\0';
        if (v3_report_symbol_line_is_undefined(line)) {
            char raw_name[PATH_MAX];
            bool matched = false;
            if (!v3_report_extract_symbol_name(line, raw_name, sizeof(raw_name))) {
                fprintf(stderr, "[cheng_v3_seed] %s malformed symbol line: %s\n", label, line);
                ok = false;
                break;
            }
            for (i = 0U; i < allowed_count; ++i) {
                if (strcmp(raw_name, allowed[i]) == 0) {
                    if (seen[i]) {
                        fprintf(stderr,
                                "[cheng_v3_seed] %s duplicate undefined symbol: %s\n",
                                label,
                                raw_name);
                        ok = false;
                    }
                    seen[i] = true;
                    matched = true;
                    break;
                }
            }
            if (!matched) {
                fprintf(stderr,
                        "[cheng_v3_seed] %s unexpected undefined symbol: %s (%s)\n",
                        label,
                        raw_name,
                        object_path);
                ok = false;
            }
            undefined_count += 1U;
        }
        cursor = end != NULL ? end + 1 : NULL;
    }
    if (ok && undefined_count != allowed_count) {
        fprintf(stderr,
                "[cheng_v3_seed] %s undefined symbol count mismatch: expected=%zu actual=%zu\n",
                label,
                allowed_count,
                undefined_count);
        ok = false;
    }
    for (i = 0U; i < allowed_count; ++i) {
        if (!seen[i]) {
            fprintf(stderr, "[cheng_v3_seed] %s missing undefined symbol: %s\n", label, allowed[i]);
            ok = false;
        }
    }
    free(seen);
    free(report);
    return ok;
}

static bool v3_run_binary_capture_output(char *const argv_local[],
                                         const char *log_path,
                                         int *status_out) {
    pid_t pid;
    int status = 0;
    size_t arg_count;
    if (argv_local == NULL || argv_local[0] == NULL || access(argv_local[0], X_OK) != 0) {
        if (log_path != NULL && log_path[0] != '\0' && v3_ensure_parent_dir(log_path)) {
            FILE *missing_log = fopen(log_path, "wb");
            if (missing_log != NULL) {
                fprintf(missing_log, "missing executable: %s\n",
                        (argv_local != NULL && argv_local[0] != NULL) ? argv_local[0] : "");
                fclose(missing_log);
            }
        }
        if (status_out != NULL) {
            *status_out = 127 << 8;
        }
        return false;
    }
    if (!v3_ensure_parent_dir(log_path)) {
        return false;
    }
    arg_count = v3_argv_count(argv_local);
    if (arg_count == 0U ||
        !v3_spawn_logged_argv(arg_count, (const char **)argv_local, log_path, false, false, false, &pid)) {
        return false;
    }
    if (!v3_waitpid_nointr(pid, &status)) {
        return false;
    }
    if (status_out != NULL) {
        *status_out = status;
    }
    return true;
}

static bool v3_spawn_binary_capture_output(char *const argv_local[],
                                           const char *log_path,
                                           pid_t *pid_out) {
    size_t arg_count;
    if (pid_out != NULL) {
        *pid_out = -1;
    }
    if (argv_local == NULL || argv_local[0] == NULL || access(argv_local[0], X_OK) != 0) {
        v3_write_spawn_error_log(log_path, argv_local != NULL ? argv_local[0] : "", ENOENT);
        return false;
    }
    if (!v3_ensure_parent_dir(log_path)) {
        return false;
    }
    arg_count = v3_argv_count(argv_local);
    if (arg_count == 0U) {
        return false;
    }
    return v3_spawn_logged_argv(arg_count, (const char **)argv_local, log_path, false, false, false, pid_out);
}

static bool v3_spawn_binary_detached_logged(char *const argv_local[],
                                            const char *log_path,
                                            const char *pid_path,
                                            pid_t *pid_out) {
    pid_t pid;
    int status = 0;
    size_t arg_count;
    char pid_text[64];
    if (pid_out != NULL) {
        *pid_out = -1;
    }
    if (argv_local == NULL || argv_local[0] == NULL || access(argv_local[0], X_OK) != 0) {
        v3_write_spawn_error_log(log_path, argv_local != NULL ? argv_local[0] : "", ENOENT);
        return false;
    }
    if (!v3_ensure_parent_dir(log_path) ||
        !v3_ensure_parent_dir(pid_path)) {
        return false;
    }
    arg_count = v3_argv_count(argv_local);
    if (arg_count == 0U ||
        !v3_spawn_logged_argv(arg_count, (const char **)argv_local, log_path, false, true, true, &pid)) {
        return false;
    }
    snprintf(pid_text, sizeof(pid_text), "%ld\n", (long)pid);
    if (!v3_write_text_file(pid_path, pid_text)) {
        kill(pid, SIGTERM);
        if (!v3_waitpid_nointr(pid, &status) && errno != ECHILD) {
            perror("waitpid");
        }
        return false;
    }
    if (pid_out != NULL) {
        *pid_out = pid;
    }
    return true;
}

static void v3_kill_and_wait_pid(pid_t *pid_io) {
    int status = 0;
    if (pid_io == NULL || *pid_io <= 0) {
        return;
    }
    kill(*pid_io, SIGTERM);
    if (!v3_waitpid_nointr_silent(*pid_io, &status) && errno == ECHILD) {
        status = 0;
    }
    *pid_io = -1;
}

static bool v3_wait_pid_success(pid_t *pid_io,
                                const char *label,
                                const char *log_path) {
    int status = 0;
    if (pid_io == NULL || *pid_io <= 0) {
        return false;
    }
    if (!v3_waitpid_nointr_silent(*pid_io, &status)) {
        perror("waitpid");
        return false;
    }
    *pid_io = -1;
    if (v3_status_is_exit_code(status, 0)) {
        return true;
    }
    fprintf(stderr, "[cheng_v3_seed] %s failed log=%s status=%d\n", label, log_path, status);
    return false;
}

static bool v3_process_alive(pid_t pid) {
    if (pid <= 0) {
        return false;
    }
    if (kill(pid, 0) == 0 || errno == EPERM) {
        return true;
    }
    return false;
}

static bool v3_read_trimmed_file_text(const char *path,
                                      char *out,
                                      size_t out_cap) {
    char *text = v3_read_file(path);
    char *trimmed;
    if (text == NULL) {
        return false;
    }
    trimmed = v3_trim_inplace(text);
    if (*trimmed == '\0') {
        free(text);
        return false;
    }
    v3_copy_text(out, out_cap, trimmed);
    free(text);
    return true;
}

static bool v3_wait_ready_port_file(const char *label,
                                    const char *ready_path,
                                    pid_t pid,
                                    int timeout_ms,
                                    char *port_out,
                                    size_t port_cap) {
    int ticks = timeout_ms / 50;
    int i;
    for (i = 0; i < ticks; ++i) {
        if (v3_path_exists_nonempty(ready_path) &&
            v3_read_trimmed_file_text(ready_path, port_out, port_cap)) {
            return true;
        }
        if (!v3_process_alive(pid)) {
            break;
        }
        usleep(50000);
    }
    fprintf(stderr, "[cheng_v3_seed] %s ready-file timeout: %s\n", label, ready_path);
    return false;
}

static bool v3_status_is_exit_code(int status, int code) {
    return WIFEXITED(status) && WEXITSTATUS(status) == code;
}

static bool v3_exec_sh_script(const char *script_path,
                              size_t arg_count,
                              const char **args,
                              const char *env_key_a,
                              const char *env_value_a,
                              const char *env_key_b,
                              const char *env_value_b,
                              int *exit_code_out) {
    char **exec_argv;
    char **envp;
    pid_t child_pid;
    int status = 0;
    size_t i;
    exec_argv = (char **)v3_xmalloc(sizeof(char *) * (arg_count + 3U));
    exec_argv[0] = (char *)"sh";
    exec_argv[1] = (char *)script_path;
    for (i = 0U; i < arg_count; ++i) {
        exec_argv[i + 2U] = (char *)(args[i] != NULL ? args[i] : "");
    }
    exec_argv[arg_count + 2U] = NULL;
    envp = v3_copy_environ_with_overrides(env_key_a, env_value_a, env_key_b, env_value_b);
    if (!v3_spawn_argv_with_env(arg_count + 2U,
                                (const char **)exec_argv,
                                false,
                                false,
                                envp,
                                &child_pid)) {
        fprintf(stderr, "[cheng_v3_seed] spawn failed for script=%s errno=%d\n", script_path, errno);
        v3_free_spawn_env(envp);
        free(exec_argv);
        return false;
    }
    v3_free_spawn_env(envp);
    free(exec_argv);
    if (!v3_waitpid_nointr_silent(child_pid, &status)) {
        fprintf(stderr, "[cheng_v3_seed] waitpid failed for script=%s errno=%d\n", script_path, errno);
        if (exit_code_out != NULL) {
            *exit_code_out = 1;
        }
        return false;
    }
    if (!WIFEXITED(status)) {
        fprintf(stderr, "[cheng_v3_seed] script command failed status=%d script=%s\n", status, script_path);
        if (exit_code_out != NULL) {
            *exit_code_out = 1;
        }
        return false;
    }
    if (exit_code_out != NULL) {
        *exit_code_out = WEXITSTATUS(status);
    }
    if (WEXITSTATUS(status) != 0) {
        fprintf(stderr, "[cheng_v3_seed] script command failed status=%d exit=%d script=%s\n",
                status,
                WEXITSTATUS(status),
                script_path);
        return false;
    }
    return true;
}

static bool CHENG_V3_UNUSED v3_run_script_passthrough(const char *root,
                                                      const char *script_rel_path,
                                                      int argc,
                                                      char **argv) {
    char script_path[PATH_MAX];
    const char **args;
    size_t arg_count;
    int i;
    v3_join_path(script_path, sizeof(script_path), root, script_rel_path);
    arg_count = argc > 2 ? (size_t)(argc - 2) : 0U;
    args = arg_count > 0U ? (const char **)v3_xmalloc(sizeof(char *) * arg_count) : NULL;
    for (i = 2; i < argc; ++i) {
        args[(size_t)(i - 2)] = argv[i];
    }
    {
        bool ok = v3_exec_sh_script(script_path, arg_count, args, NULL, NULL, NULL, NULL, NULL);
        free(args);
        return ok;
    }
}

static void v3_save_env_value(const char *key,
                              char *out,
                              size_t cap,
                              bool *had_value) {
    const char *value = getenv(key);
    if (had_value != NULL) {
        *had_value = value != NULL && value[0] != '\0';
    }
    if (out != NULL && cap > 0U) {
        out[0] = '\0';
        if (value != NULL && value[0] != '\0') {
            v3_copy_text(out, cap, value);
        }
    }
}

static void v3_restore_env_value(const char *key,
                                 const char *saved,
                                 bool had_value) {
    if (had_value) {
        setenv(key, saved, 1);
    } else {
        unsetenv(key);
    }
}

static bool v3_run_script_args(const char *root,
                               const char *script_rel_path,
                               size_t arg_count,
                               const char **args) {
    char script_path[PATH_MAX];
    v3_join_path(script_path, sizeof(script_path), root, script_rel_path);
    return v3_exec_sh_script(script_path, arg_count, args, NULL, NULL, NULL, NULL, NULL);
}

static bool CHENG_V3_UNUSED v3_run_script_args_with_env(const char *root,
                                                        const char *script_rel_path,
                                                        const char *compiler_env_key,
                                                        const char *compiler_env_value,
                                                        const char *label_env_key,
                                                        const char *label_env_value,
                                                        bool stage23_env,
                                                        size_t arg_count,
                                                        const char **args) {
    char saved_compiler[PATH_MAX];
    char saved_label[256];
    char saved_incremental[64];
    char saved_multi_cache[64];
    bool had_compiler = false;
    bool had_label = false;
    bool had_incremental = false;
    bool had_multi_cache = false;
    bool ok;
    if (compiler_env_key != NULL && compiler_env_value != NULL && compiler_env_value[0] != '\0') {
        v3_save_env_value(compiler_env_key, saved_compiler, sizeof(saved_compiler), &had_compiler);
        setenv(compiler_env_key, compiler_env_value, 1);
    }
    if (label_env_key != NULL && label_env_value != NULL && label_env_value[0] != '\0') {
        v3_save_env_value(label_env_key, saved_label, sizeof(saved_label), &had_label);
        setenv(label_env_key, label_env_value, 1);
    }
    if (stage23_env) {
        v3_save_env_value("BACKEND_INCREMENTAL", saved_incremental, sizeof(saved_incremental), &had_incremental);
        v3_save_env_value("BACKEND_MULTI_MODULE_CACHE", saved_multi_cache, sizeof(saved_multi_cache), &had_multi_cache);
        setenv("BACKEND_INCREMENTAL", "0", 1);
        setenv("BACKEND_MULTI_MODULE_CACHE", "0", 1);
    }
    ok = v3_run_script_args(root, script_rel_path, arg_count, args);
    if (stage23_env) {
        v3_restore_env_value("BACKEND_INCREMENTAL", saved_incremental, had_incremental);
        v3_restore_env_value("BACKEND_MULTI_MODULE_CACHE", saved_multi_cache, had_multi_cache);
    }
    if (label_env_key != NULL && label_env_value != NULL && label_env_value[0] != '\0') {
        v3_restore_env_value(label_env_key, saved_label, had_label);
    }
    if (compiler_env_key != NULL && compiler_env_value != NULL && compiler_env_value[0] != '\0') {
        v3_restore_env_value(compiler_env_key, saved_compiler, had_compiler);
    }
    return ok;
}

static bool v3_compile_host_fixture_with_backend_driver(const char *compiler_bin,
                                                        const char *root_v3,
                                                        const char *src_path,
                                                        const char *out_bin,
                                                        const char *log_path);
static bool v3_leaf_prepare_backend_driver(V3BootstrapPaths *paths,
                                           char *root_v3,
                                           size_t root_v3_cap);
static const char *v3_host_target_triple(void);

static int v3_exec_binary_passthrough_with_env(const char *path,
                                               size_t arg_count,
                                               const char **args,
                                               const char *env_key,
                                               const char *env_value) {
    char **argv_local;
    size_t i;
    if (path == NULL || path[0] == '\0') {
        fprintf(stderr, "[cheng_v3_seed] missing binary for passthrough dispatch\n");
        return 1;
    }
    argv_local = (char **)v3_xmalloc(sizeof(char *) * (arg_count + 2U));
    argv_local[0] = (char *)path;
    for (i = 0U; i < arg_count; ++i) {
        argv_local[i + 1U] = (char *)(args[i] != NULL ? args[i] : "");
    }
    argv_local[arg_count + 1U] = NULL;
    if (env_key != NULL && env_value != NULL && env_key[0] != '\0') {
        setenv(env_key, env_value, 1);
    }
    execv(path, argv_local);
    fprintf(stderr,
            "[cheng_v3_seed] execv failed for passthrough=%s errno=%d\n",
            path,
            errno);
    free(argv_local);
    return 127;
}

static int v3_try_exec_backend_driver_cli_passthrough(const V3BootstrapPaths *paths,
                                                      int argc,
                                                      char **argv,
                                                      const char *label,
                                                      bool require_ready) {
    const char **args;
    size_t arg_count;
    size_t i;
    int rc;
    if (paths == NULL ||
        argc < 2 ||
        argv == NULL ||
        !v3_bootstrap_artifacts_fresh(paths) ||
        !v3_path_is_executable(paths->backend_driver_out)) {
        return -1;
    }
    if (require_ready && !v3_backend_driver_ready(paths)) {
        return -1;
    }
    arg_count = (size_t)(argc - 1);
    args = (const char **)v3_xmalloc(sizeof(char *) * arg_count);
    for (i = 1U; i < (size_t)argc; ++i) {
        args[i - 1U] = argv[i] != NULL ? argv[i] : "";
    }
    rc = v3_exec_binary_passthrough_with_env(paths->backend_driver_out,
                                             arg_count,
                                             args,
                                             "CHENG_V3_CONTROLLER",
                                             label);
    free(args);
    return rc;
}

static bool v3_backend_driver_handoff_enabled(void) {
    const char *disable_handoff = getenv("CHENG_V3_NO_BACKEND_DRIVER_HANDOFF");
    return !(disable_handoff != NULL &&
             disable_handoff[0] != '\0' &&
             !v3_streq(disable_handoff, "0"));
}

static bool v3_command_prefers_backend_driver_passthrough(const char *cmd) {
    if (cmd == NULL || cmd[0] == '\0') {
        return false;
    }
    if (!v3_backend_driver_handoff_enabled()) {
        return false;
    }
    if (v3_streq(cmd, "print-contract") ||
        v3_streq(cmd, "self-check") ||
        v3_streq(cmd, "bootstrap-bridge") ||
        v3_streq(cmd, "build-backend-driver") ||
        v3_streq(cmd, "system-link-exec") ||
        v3_streq(cmd, "compile-bootstrap")) {
        return false;
    }
    return true;
}

static bool v3_command_requires_backend_driver_passthrough(const char *cmd) {
    if (cmd == NULL || cmd[0] == '\0') {
        return false;
    }
    if (!v3_backend_driver_handoff_enabled()) {
        return false;
    }
    return v3_streq(cmd, "debug-report") ||
           v3_streq(cmd, "print-symbols") ||
           v3_streq(cmd, "print-line-map") ||
           v3_streq(cmd, "run-cross-target-smokes") ||
           v3_streq(cmd, "run-host-smokes") ||
           v3_streq(cmd, "run-stage23-libp2p-smokes") ||
           v3_streq(cmd, "r2c-react-v3") ||
           v3_streq(cmd, "vpn-proxy") ||
           v3_streq(cmd, "print-object") ||
           v3_streq(cmd, "print-elf") ||
           v3_streq(cmd, "print-asm") ||
           v3_streq(cmd, "profile-run") ||
           v3_streq(cmd, "profile-report") ||
           v3_streq(cmd, "crash-report") ||
           v3_streq(cmd, "verify-orphan-guard") ||
           v3_streq(cmd, "verify-export-visibility") ||
           v3_streq(cmd, "verify-export-visibility-parallel") ||
           v3_streq(cmd, "verify-backend-driver-command-surface") ||
           v3_streq(cmd, "verify-mobile-shell-build-probe") ||
           v3_streq(cmd, "verify-chain-node-resilience") ||
           v3_streq(cmd, "verify-r2c-react-v3-surface") ||
           v3_streq(cmd, "verify-debug-tools") ||
           v3_streq(cmd, "verify-debug-runtime") ||
           v3_streq(cmd, "verify-debug-profile") ||
           v3_streq(cmd, "build-zero-exit") ||
           v3_streq(cmd, "build-panic-trace") ||
           v3_streq(cmd, "build-bounds-trace") ||
           v3_streq(cmd, "build-signal-trace") ||
           v3_streq(cmd, "build-call-chain") ||
           v3_streq(cmd, "build-ffi-handle") ||
           v3_streq(cmd, "build-program-selfhost") ||
           v3_streq(cmd, "build-bft-state-machine") ||
           v3_streq(cmd, "deploy-bft-validator-three-node") ||
           v3_streq(cmd, "verify-bft-validator-three-node") ||
           v3_streq(cmd, "status-bft-validator-three-node") ||
           v3_streq(cmd, "stop-bft-validator-three-node") ||
           v3_streq(cmd, "bench-bft-validator-three-node") ||
           v3_streq(cmd, "build-browser-host-wasm") ||
           v3_streq(cmd, "build-chain-node") ||
           v3_streq(cmd, "build-rwad-bft-state-machine") ||
           v3_streq(cmd, "build-chain-node-linux") ||
           v3_streq(cmd, "build-linux-nolibc-exe") ||
           v3_streq(cmd, "build-rwad-bft-linux") ||
           v3_streq(cmd, "run-linux-object-smokes") ||
           v3_streq(cmd, "run-tcp-twoproc-smoke") ||
           v3_streq(cmd, "run-quic-twoproc-smoke") ||
           v3_streq(cmd, "run-udp-importc-smoke") ||
           v3_streq(cmd, "run-wasm-smokes") ||
           v3_streq(cmd, "run-browser-host-wasm-smoke") ||
           v3_streq(cmd, "run-chain-node-cli-smoke") ||
           v3_streq(cmd, "run-chain-node-process-smoke") ||
           v3_streq(cmd, "run-chain-node-three-node-smoke") ||
           v3_streq(cmd, "run-chain-node-resilience-smoke") ||
           v3_streq(cmd, "run-tailnet-control-smoke") ||
           v3_streq(cmd, "run-fresh-node-selfhost-gate") ||
           v3_streq(cmd, "run-migration-gate") ||
           v3_streq(cmd, "run-browser-webrtc-smokes");
}

static int v3_require_backend_driver_cli_passthrough(const char *cmd,
                                                     int argc,
                                                     char **argv) {
    V3BootstrapPaths paths;
    const char **args;
    size_t arg_count;
    size_t i;
    int rc;
    v3_bootstrap_paths_init(&paths);
    if (!v3_ensure_backend_driver_ready(&paths, cmd)) {
        return 1;
    }
    arg_count = argc > 1 ? (size_t)(argc - 1) : 0U;
    args = (const char **)v3_xmalloc(sizeof(char *) * arg_count);
    for (i = 1U; i < (size_t)argc; ++i) {
        args[i - 1U] = argv[i] != NULL ? argv[i] : "";
    }
    rc = v3_exec_binary_passthrough_with_env(paths.backend_driver_out,
                                             arg_count,
                                             args,
                                             "CHENG_V3_CONTROLLER",
                                             "cheng.backend_driver");
    free(args);
    return rc;
}

static bool v3_r2c_prepare_controller_bin(V3BootstrapPaths *paths,
                                          char *out_bin,
                                          size_t out_bin_cap) {
    char root_v3[PATH_MAX];
    char out_dir[PATH_MAX];
    char compile_log[PATH_MAX];
    char controller_source[PATH_MAX];
    char controller_impl_source[PATH_MAX];
    char path_source[PATH_MAX];
    char program_argv_source[PATH_MAX];
    char core_runtime_source[PATH_MAX];
    char debug_runtime_source[PATH_MAX];
    char program_support_source[PATH_MAX];
    const char *inputs[8];
    if (paths == NULL || out_bin == NULL || out_bin_cap == 0U) {
        return false;
    }
    v3_bootstrap_paths_init(paths);
    if (!v3_bootstrap_artifacts_fresh(paths) &&
        v3_cmd_bootstrap_bridge(0, NULL) != 0) {
        return false;
    }
    if (access(paths->stage3, X_OK) != 0) {
        fprintf(stderr,
                "[cheng_v3_seed] missing stage3 compiler for r2c controller: %s\n",
                paths->stage3);
        return false;
    }
    v3_join_path(root_v3, sizeof(root_v3), paths->root, "v3");
    v3_join_path(out_dir,
                 sizeof(out_dir),
                 paths->root,
                 "artifacts/v3_r2c_react_v3_controller");
    v3_join_path(out_bin, out_bin_cap, out_dir, "cheng");
    v3_join_path(compile_log, sizeof(compile_log), out_dir, "build.compile.log");
    v3_join_path(controller_source,
                 sizeof(controller_source),
                 paths->root,
                 "v3/src/tooling/r2c_react_v3.cheng");
    v3_join_path(controller_impl_source,
                 sizeof(controller_impl_source),
                 paths->root,
                 "v3/src/tooling/r2c_react_v3_controller_main.cheng");
    v3_join_path(path_source,
                 sizeof(path_source),
                 paths->root,
                 "v3/src/tooling/path.cheng");
    v3_join_path(program_argv_source,
                 sizeof(program_argv_source),
                 paths->root,
                 "v3/src/runtime/compiler_runtime_program_entry_provider_v3.cheng");
#if defined(__APPLE__)
        v3_join_path(core_runtime_source,
                     sizeof(core_runtime_source),
                     paths->root,
                     "v3/src/runtime/core_runtime_provider_darwin_v3.cheng");
#else
        v3_join_path(core_runtime_source,
                     sizeof(core_runtime_source),
                     paths->root,
                     "v3/src/runtime/core_runtime_provider_linux_v3.cheng");
#endif
    v3_join_path(debug_runtime_source,
                 sizeof(debug_runtime_source),
                 paths->root,
                 "v3/src/runtime/debug_runtime_stub_v3.cheng");
    v3_join_path(program_support_source,
                 sizeof(program_support_source),
                 paths->root,
                 "v3/src/runtime/program_support_backend_v3.cheng");
    inputs[0] = paths->stage3;
    inputs[1] = controller_source;
    inputs[2] = controller_impl_source;
    inputs[3] = path_source;
    inputs[4] = program_argv_source;
    inputs[5] = core_runtime_source;
    inputs[6] = debug_runtime_source;
    inputs[7] = program_support_source;
    if (access(out_bin, X_OK) == 0 &&
        v3_output_is_fresh_against_inputs(out_bin, inputs, 8U)) {
        return true;
    }
    if (!v3_mkdir_p(out_dir)) {
        fprintf(stderr,
                "[cheng_v3_seed] failed to create r2c controller out-dir: %s\n",
                out_dir);
        return false;
    }
    if (!v3_compile_host_fixture_with_backend_driver(paths->stage3,
                                                     root_v3,
                                                     controller_source,
                                                     out_bin,
                                                     compile_log)) {
        fprintf(stderr,
                "[cheng_v3_seed] failed to compile r2c controller log=%s\n",
                compile_log);
        return false;
    }
    if (access(out_bin, X_OK) != 0) {
        fprintf(stderr,
                "[cheng_v3_seed] missing r2c controller binary: %s\n",
                out_bin);
        return false;
    }
    return true;
}

static int v3_cmd_verify_orphan_guard_impl(int argc, char **argv);
static int v3_cmd_verify_debug_tools_impl(int argc, char **argv);
static int v3_cmd_verify_debug_runtime_impl(int argc, char **argv);
static int v3_cmd_verify_debug_profile_impl(int argc, char **argv);
static int v3_cmd_verify_windows_builtin_impl(int argc, char **argv);
static int v3_cmd_verify_riscv64_builtin_impl(int argc, char **argv);
static int v3_cmd_run_cross_target_smokes_impl(int argc, char **argv);
static int v3_cmd_run_host_smokes_impl(int argc, char **argv);
static int v3_cmd_run_stage23_libp2p_smokes_impl(int argc, char **argv);
static int v3_cmd_r2c_react_v3_impl(int argc, char **argv);
static int v3_cmd_build_zero_exit_impl(int argc, char **argv);
static int v3_cmd_build_panic_trace_impl(int argc, char **argv);
static int v3_cmd_build_bounds_trace_impl(int argc, char **argv);
static int v3_cmd_build_signal_trace_impl(int argc, char **argv);
static int v3_cmd_build_call_chain_impl(int argc, char **argv);
static int v3_cmd_build_ffi_handle_impl(int argc, char **argv);
static int v3_cmd_build_program_selfhost_impl(int argc, char **argv);
static int v3_cmd_build_bft_state_machine_impl(int argc, char **argv);
static int v3_cmd_build_browser_host_wasm_impl(int argc, char **argv);
static int v3_cmd_build_chain_node_impl(int argc, char **argv);
static int v3_cmd_build_rwad_bft_state_machine_impl(int argc, char **argv);
static int v3_cmd_build_chain_node_linux_impl(int argc, char **argv);
static int v3_cmd_build_linux_nolibc_exe_impl(int argc, char **argv);
static int v3_cmd_build_rwad_bft_linux_impl(int argc, char **argv);
static int v3_cmd_run_linux_object_smokes_impl(int argc, char **argv);
static int v3_cmd_run_tcp_twoproc_smoke_impl(int argc, char **argv);
static int v3_cmd_run_quic_twoproc_smoke_impl(int argc, char **argv);
static int v3_cmd_run_udp_importc_smoke_impl(int argc, char **argv);
static int v3_cmd_run_wasm_smokes_impl(int argc, char **argv);
static int v3_cmd_run_browser_host_wasm_smoke_impl(int argc, char **argv);
static int v3_cmd_run_chain_node_cli_smoke_impl(int argc, char **argv);
static int v3_cmd_run_chain_node_process_smoke_impl(int argc, char **argv);
static int v3_cmd_run_chain_node_three_node_smoke_impl(int argc, char **argv);
static int v3_cmd_run_tailnet_control_smoke_impl(int argc, char **argv);
static int v3_cmd_run_fresh_node_selfhost_gate_impl(int argc, char **argv);
static int v3_cmd_run_migration_gate_impl(int argc, char **argv);
static int v3_cmd_run_browser_webrtc_smokes_impl(int argc, char **argv);
static bool v3_verify_runtime_fixture_output(const char *label,
                                             const char *fixture_path,
                                             const char *run_log_path,
                                             const char *report_path,
                                             const char *message_substring,
                                             const char *reason_line,
                                             bool require_signal_fields);
static const char *v3_leaf_resolve_compiler_bin(int argc,
                                                char **argv,
                                                const char *env_primary,
                                                const char *env_fallback,
                                                const char *default_bin);
static bool v3_leaf_expect_compiler(const char *label,
                                    const char *compiler_bin);
static size_t v3_collect_smoke_positionals(int argc,
                                           char **argv,
                                           const char **out,
                                           size_t cap);
static bool v3_run_fixture_smoke_direct(const char *suite_label,
                                        const char *compile_label,
                                        const char *compiler_bin,
                                        const char *root,
                                        const char *root_v3,
                                        const char *smoke_name,
                                        const char *out_bin);

static int v3_cmd_r2c_react_v3_impl(int argc, char **argv) {
    V3BootstrapPaths paths;
    const char *controller_label = "cheng.stage3";
    char controller_bin[PATH_MAX];
    const char **args;
    size_t arg_count;
    v3_bootstrap_paths_init(&paths);
    if (paths.root[0] == '\0') {
        fprintf(stderr, "[cheng_v3_seed] r2c-react-v3 root detect failed\n");
        return 1;
    }
    if (strstr(argv[0] != NULL ? argv[0] : "", "/v3_backend_driver/") != NULL) {
        controller_label = "cheng.backend_driver";
    }
    if (!v3_r2c_prepare_controller_bin(&paths, controller_bin, sizeof(controller_bin))) {
        return 1;
    }
    setenv("CHENG_V3_ROOT", paths.root, 1);
    arg_count = argc > 2 ? (size_t)(argc - 2) : 0U;
    args = (const char **)v3_xmalloc(sizeof(char *) * arg_count);
    if (argc > 2) {
        int i;
        for (i = 2; i < argc; ++i) {
            args[(size_t)(i - 2)] = argv[i] != NULL ? argv[i] : "";
        }
    }
    return v3_exec_binary_passthrough_with_env(controller_bin,
                                               arg_count,
                                               args,
                                               "R2C_REACT_V3_CONTROLLER",
                                               controller_label);
}

static int v3_invoke_command_with_common_flags(V3SeedCommandFn fn,
                                               const char *cmd_name,
                                               const char *contract_path,
                                               const char *root_path,
                                               const char *source_path,
                                               const char *emit,
                                               const char *target,
                                               const char *out_path,
                                               const char *report_path) {
    char contract_flag[PATH_MAX + 32];
    char root_flag[PATH_MAX + 16];
    char in_flag[PATH_MAX + 16];
    char emit_flag[64];
    char target_flag[256];
    char out_flag[PATH_MAX + 16];
    char report_flag[PATH_MAX + 24];
    char *argv_local[10];
    int argc_local = 0;
    argv_local[argc_local++] = "cheng_v3_seed";
    argv_local[argc_local++] = (char *)cmd_name;
    if (contract_path != NULL && contract_path[0] != '\0') {
        snprintf(contract_flag, sizeof(contract_flag), "--contract-in:%s", contract_path);
        argv_local[argc_local++] = contract_flag;
    }
    if (root_path != NULL && root_path[0] != '\0') {
        snprintf(root_flag, sizeof(root_flag), "--root:%s", root_path);
        argv_local[argc_local++] = root_flag;
    }
    if (source_path != NULL && source_path[0] != '\0') {
        snprintf(in_flag, sizeof(in_flag), "--in:%s", source_path);
        argv_local[argc_local++] = in_flag;
    }
    if (emit != NULL && emit[0] != '\0') {
        snprintf(emit_flag, sizeof(emit_flag), "--emit:%s", emit);
        argv_local[argc_local++] = emit_flag;
    }
    if (target != NULL && target[0] != '\0') {
        snprintf(target_flag, sizeof(target_flag), "--target:%s", target);
        argv_local[argc_local++] = target_flag;
    }
    if (out_path != NULL && out_path[0] != '\0') {
        snprintf(out_flag, sizeof(out_flag), "--out:%s", out_path);
        argv_local[argc_local++] = out_flag;
    }
    if (report_path != NULL && report_path[0] != '\0') {
        snprintf(report_flag, sizeof(report_flag), "--report-out:%s", report_path);
        argv_local[argc_local++] = report_flag;
    }
    return fn(argc_local, argv_local);
}

static int v3_cmd_profile_run(int argc, char **argv);
static bool v3_compile_fixture_with_backend_driver_emit_target(const char *compiler_bin,
                                                               const char *root_v3,
                                                               const char *src_path,
                                                               const char *emit_kind,
                                                               const char *target,
                                                               const char *out_path,
                                                               const char *log_path,
                                                               const char *report_path);
static int v3_cmd_debug_report(int argc, char **argv);
static int v3_cmd_print_symbols(int argc, char **argv);
static int v3_cmd_print_line_map(int argc, char **argv);
static int v3_cmd_print_object(int argc, char **argv);
static int v3_cmd_print_elf(int argc, char **argv);
static int v3_cmd_print_asm(int argc, char **argv);
static const char *v3_host_target_triple(void);
static int v3_cmd_profile_report(int argc, char **argv);
static int v3_cmd_crash_report(int argc, char **argv);
static int v3_cmd_system_link_exec(int argc, char **argv);

static int v3_invoke_profile_run(const char *contract_path,
                                 const char *root_path,
                                 const char *source_path,
                                 const char *target,
                                 const char *out_path,
                                 const char *profile_hz,
                                 const char *report_path) {
    char contract_flag[PATH_MAX + 32];
    char root_flag[PATH_MAX + 16];
    char in_flag[PATH_MAX + 16];
    char emit_flag[64];
    char target_flag[256];
    char out_flag[PATH_MAX + 16];
    char hz_flag[64];
    char report_flag[PATH_MAX + 24];
    char *argv_local[10];
    int argc_local = 0;
    argv_local[argc_local++] = "cheng_v3_seed";
    argv_local[argc_local++] = "profile-run";
    if (contract_path != NULL && contract_path[0] != '\0') {
        snprintf(contract_flag, sizeof(contract_flag), "--contract-in:%s", contract_path);
        argv_local[argc_local++] = contract_flag;
    }
    if (root_path != NULL && root_path[0] != '\0') {
        snprintf(root_flag, sizeof(root_flag), "--root:%s", root_path);
        argv_local[argc_local++] = root_flag;
    }
    snprintf(in_flag, sizeof(in_flag), "--in:%s", source_path);
    argv_local[argc_local++] = in_flag;
    snprintf(emit_flag, sizeof(emit_flag), "--emit:exe");
    argv_local[argc_local++] = emit_flag;
    snprintf(target_flag, sizeof(target_flag), "--target:%s", target);
    argv_local[argc_local++] = target_flag;
    snprintf(out_flag, sizeof(out_flag), "--out:%s", out_path);
    argv_local[argc_local++] = out_flag;
    if (profile_hz != NULL && profile_hz[0] != '\0') {
        snprintf(hz_flag, sizeof(hz_flag), "--profile-hz:%s", profile_hz);
        argv_local[argc_local++] = hz_flag;
    }
    if (report_path != NULL && report_path[0] != '\0') {
        snprintf(report_flag, sizeof(report_flag), "--report-out:%s", report_path);
        argv_local[argc_local++] = report_flag;
    }
    return v3_cmd_profile_run(argc_local, argv_local);
}

static void v3_profile_raw_report_path_from_report(const char *report_path,
                                                   char *out,
                                                   size_t cap) {
    size_t len;
    if (out == NULL || cap == 0U) {
        return;
    }
    out[0] = '\0';
    if (report_path == NULL || report_path[0] == '\0') {
        return;
    }
    len = strlen(report_path);
    if (len > 4U && strcmp(report_path + len - 4U, ".txt") == 0) {
        snprintf(out, cap, "%.*s.raw.txt", (int)(len - 4U), report_path);
        return;
    }
    snprintf(out, cap, "%s.raw.txt", report_path);
}

static void v3_profile_log_error(const char *log_path, const char *message) {
    if (log_path != NULL && log_path[0] != '\0') {
        v3_ensure_parent_dir(log_path);
        v3_write_text_file(log_path, message);
    }
}

static void v3_profile_trim_line(char *line);

static void v3_profile_report_path_from_raw(const char *raw_report_path,
                                            char *out,
                                            size_t cap) {
    size_t len;
    if (out == NULL || cap == 0U) {
        return;
    }
    out[0] = '\0';
    if (raw_report_path == NULL || raw_report_path[0] == '\0') {
        return;
    }
    len = strlen(raw_report_path);
    if (len > 8U && strcmp(raw_report_path + len - 8U, ".raw.txt") == 0) {
        snprintf(out, cap, "%.*s.txt", (int)(len - 8U), raw_report_path);
        return;
    }
    snprintf(out, cap, "%s.report.txt", raw_report_path);
}

static void v3_crash_report_path_from_raw(const char *raw_log_path,
                                          char *out,
                                          size_t cap) {
    size_t len;
    if (out == NULL || cap == 0U) {
        return;
    }
    out[0] = '\0';
    if (raw_log_path == NULL || raw_log_path[0] == '\0') {
        return;
    }
    len = strlen(raw_log_path);
    if (len > 8U && strcmp(raw_log_path + len - 8U, ".run.log") == 0) {
        snprintf(out, cap, "%.*s.report.txt", (int)(len - 8U), raw_log_path);
        return;
    }
    if (len > 8U && strcmp(raw_log_path + len - 8U, ".raw.txt") == 0) {
        snprintf(out, cap, "%.*s.txt", (int)(len - 8U), raw_log_path);
        return;
    }
    snprintf(out, cap, "%s.report.txt", raw_log_path);
}

static void v3_crash_log_error(const char *log_path, const char *message) {
    if (log_path != NULL && log_path[0] != '\0') {
        v3_ensure_parent_dir(log_path);
        v3_write_text_file(log_path, message);
    }
}

static bool v3_crash_report_raw_line_allowed(const char *line) {
    if (line == NULL || line[0] == '\0') {
        return false;
    }
    if (strncmp(line, "reason=", 7) == 0) return true;
    if (strncmp(line, "regs pc=", 8) == 0) return true;
    if (strncmp(line, "m#", 2) == 0) return true;
    if (line[0] == '#' && line[1] >= '0' && line[1] <= '9') return true;
    if (strncmp(line, "source_frames=", 14) == 0) return true;
    return false;
}

static bool v3_crash_report_transform_raw(const char *raw_log_path,
                                          const char *report_path,
                                          const char *log_path) {
    char *raw_text = NULL;
    char *cursor = NULL;
    char *line = NULL;
    char *report = NULL;
    size_t report_cap = 1024U;
    size_t report_used = 0U;
    char line_buf[PATH_MAX * 2];
    bool header_seen = false;
    bool saw_reason = false;
    bool saw_regs = false;
    bool saw_machine = false;
    bool ok = false;
    if (!v3_path_exists_nonempty(raw_log_path)) {
        v3_crash_log_error(log_path, "v3 crash report: missing raw log\n");
        return false;
    }
    if (!v3_ensure_parent_dir(report_path) || !v3_ensure_parent_dir(log_path)) {
        return false;
    }
    raw_text = v3_read_file(raw_log_path);
    if (raw_text == NULL) {
        v3_crash_log_error(log_path, "v3 crash report: failed to read raw log\n");
        goto done;
    }
    report = (char *)v3_xmalloc(report_cap);
    report[0] = '\0';
    cursor = raw_text;
    while ((line = strsep(&cursor, "\n")) != NULL) {
        v3_profile_trim_line(line);
        if (line[0] == '\0') {
            continue;
        }
        if (!header_seen) {
            if (strcmp(line, "v3_crash_raw_v1") != 0) {
                continue;
            }
            header_seen = true;
            v3_report_append(&report, &report_cap, &report_used, "[cheng-v3] v3_crash_report_v1");
            continue;
        }
        if (!v3_crash_report_raw_line_allowed(line)) {
            continue;
        }
        snprintf(line_buf, sizeof(line_buf), "[cheng-v3] %s", line);
        v3_report_append(&report, &report_cap, &report_used, line_buf);
        if (strncmp(line, "reason=", 7) == 0) {
            saw_reason = true;
        } else if (strncmp(line, "regs pc=", 8) == 0) {
            saw_regs = true;
        } else if (strncmp(line, "m#", 2) == 0) {
            saw_machine = true;
        }
    }
    if (!header_seen || !saw_reason || !saw_regs || !saw_machine) {
        v3_crash_log_error(log_path, "v3 crash report: invalid raw log\n");
        goto done;
    }
    if (!v3_write_text_file(report_path, report)) {
        v3_crash_log_error(log_path, "v3 crash report: failed to write report\n");
        goto done;
    }
    ok = true;
done:
    free(report);
    free(raw_text);
    return ok;
}

static int v3_cmd_bootstrap_bridge(int argc, char **argv);

static bool v3_profile_report_live_compiler_bin(const V3BootstrapPaths *paths,
                                                char *out,
                                                size_t cap) {
    if (paths == NULL || out == NULL || cap == 0U) {
        return false;
    }
    out[0] = '\0';
    if (v3_backend_driver_ready(paths)) {
        v3_copy_text(out, cap, paths->backend_driver_out);
        return true;
    }
    if (v3_bootstrap_artifacts_fresh(paths) && v3_path_exists_nonempty(paths->stage3)) {
        v3_copy_text(out, cap, paths->stage3);
        return true;
    }
    if (v3_cmd_bootstrap_bridge(0, NULL) == 0) {
        if (v3_backend_driver_ready(paths)) {
            v3_copy_text(out, cap, paths->backend_driver_out);
            return true;
        }
        if (v3_bootstrap_artifacts_fresh(paths) && v3_path_exists_nonempty(paths->stage3)) {
            v3_copy_text(out, cap, paths->stage3);
            return true;
        }
    }
    return false;
}

typedef struct {
    char *key;
    uint32_t count;
} V3ProfileCount;

static int v3_profile_count_cmp(const void *left, const void *right) {
    const V3ProfileCount *a = (const V3ProfileCount *)left;
    const V3ProfileCount *b = (const V3ProfileCount *)right;
    if (a->count != b->count) {
        return a->count < b->count ? 1 : -1;
    }
    if (a->key == NULL && b->key == NULL) return 0;
    if (a->key == NULL) return 1;
    if (b->key == NULL) return -1;
    return strcmp(a->key, b->key);
}

static bool v3_profile_count_add(V3ProfileCount **items,
                                 int32_t *len,
                                 int32_t *cap,
                                 const char *key) {
    int32_t i;
    int32_t next_cap;
    V3ProfileCount *grown = NULL;
    const char *safe_key = (key != NULL && key[0] != '\0') ? key : "?";
    for (i = 0; i < *len; i += 1) {
        if ((*items)[i].key != NULL && strcmp((*items)[i].key, safe_key) == 0) {
            (*items)[i].count += 1U;
            return true;
        }
    }
    if (*len >= *cap) {
        next_cap = *cap > 0 ? *cap * 2 : 16;
        grown = (V3ProfileCount *)realloc(*items, (size_t)next_cap * sizeof(V3ProfileCount));
        if (grown == NULL) {
            return false;
        }
        *items = grown;
        *cap = next_cap;
    }
    (*items)[*len].key = v3_strdup(safe_key);
    if ((*items)[*len].key == NULL) {
        return false;
    }
    (*items)[*len].count = 1U;
    *len += 1;
    return true;
}

static void v3_profile_counts_free(V3ProfileCount *items, int32_t len) {
    int32_t i;
    if (items == NULL) return;
    for (i = 0; i < len; i += 1) {
        free(items[i].key);
    }
    free(items);
}

static void v3_profile_trim_line(char *line) {
    size_t len;
    if (line == NULL) return;
    len = strlen(line);
    while (len > 0U && (line[len - 1U] == '\r' || line[len - 1U] == '\n')) {
        line[len - 1U] = '\0';
        len -= 1U;
    }
}

static bool v3_profile_report_transform_raw(const char *raw_report_path,
                                            const char *report_path,
                                            const char *log_path) {
    char *raw_text = NULL;
    char *cursor = NULL;
    char *line = NULL;
    bool header_seen = false;
    int32_t total_samples = 0;
    int32_t dropped_samples = 0;
    int32_t sample_count = 0;
    V3ProfileCount *function_counts = NULL;
    V3ProfileCount *source_counts = NULL;
    V3ProfileCount *pc_counts = NULL;
    int32_t function_len = 0;
    int32_t source_len = 0;
    int32_t pc_len = 0;
    int32_t function_cap = 0;
    int32_t source_cap = 0;
    int32_t pc_cap = 0;
    char *report = NULL;
    size_t report_cap = 1024U;
    size_t report_used = 0U;
    char line_buf[PATH_MAX * 2];
    bool ok = false;
    int32_t i;
    (void)log_path;
    if (!v3_path_exists_nonempty(raw_report_path)) {
        v3_profile_log_error(log_path, "v3 profile report: missing raw profile\n");
        return false;
    }
    if (!v3_ensure_parent_dir(report_path) || !v3_ensure_parent_dir(log_path)) {
        return false;
    }
    raw_text = v3_read_file(raw_report_path);
    if (raw_text == NULL) {
        v3_profile_log_error(log_path, "v3 profile report: failed to read raw profile\n");
        goto done;
    }
    cursor = raw_text;
    while ((line = strsep(&cursor, "\n")) != NULL) {
        char *rest;
        char *pc_key;
        char *function_key;
        char *source_key;
        v3_profile_trim_line(line);
        if (line[0] == '\0') {
            continue;
        }
        if (!header_seen) {
            header_seen = true;
            if (strcmp(line, "v3_profile_raw_v1") != 0) {
                v3_profile_log_error(log_path, "v3 profile report: invalid raw header\n");
                goto done;
            }
            continue;
        }
        if (strncmp(line, "total_samples=", 14) == 0) {
            v3_parse_i32_text(line + 14, &total_samples);
            continue;
        }
        if (strncmp(line, "dropped_samples=", 16) == 0) {
            v3_parse_i32_text(line + 16, &dropped_samples);
            continue;
        }
        if (strncmp(line, "sample\t", 7) != 0) {
            continue;
        }
        rest = line + 7;
        pc_key = strsep(&rest, "\t");
        function_key = strsep(&rest, "\t");
        source_key = strsep(&rest, "\t");
        if (!v3_profile_count_add(&pc_counts, &pc_len, &pc_cap, pc_key) ||
            !v3_profile_count_add(&function_counts, &function_len, &function_cap, function_key) ||
            !v3_profile_count_add(&source_counts, &source_len, &source_cap, source_key)) {
            v3_profile_log_error(log_path, "v3 profile report: out of memory while counting samples\n");
            goto done;
        }
        sample_count += 1;
    }
    if (!header_seen) {
        v3_profile_log_error(log_path, "v3 profile report: invalid raw header\n");
        goto done;
    }
    if (total_samples <= 0) {
        total_samples = sample_count;
    }
    if (function_len > 1) {
        qsort(function_counts, (size_t)function_len, sizeof(V3ProfileCount), v3_profile_count_cmp);
    }
    if (source_len > 1) {
        qsort(source_counts, (size_t)source_len, sizeof(V3ProfileCount), v3_profile_count_cmp);
    }
    if (pc_len > 1) {
        qsort(pc_counts, (size_t)pc_len, sizeof(V3ProfileCount), v3_profile_count_cmp);
    }
    report = (char *)v3_xmalloc(report_cap);
    report[0] = '\0';
    v3_report_append(&report, &report_cap, &report_used, "v3_profile_v1");
    snprintf(line_buf, sizeof(line_buf), "total_samples=%d", total_samples);
    v3_report_append(&report, &report_cap, &report_used, line_buf);
    snprintf(line_buf, sizeof(line_buf), "dropped_samples=%d", dropped_samples);
    v3_report_append(&report, &report_cap, &report_used, line_buf);
    for (i = 0; i < function_len && i < 8; i += 1) {
        snprintf(line_buf,
                 sizeof(line_buf),
                 "hot_function[%d]=%u|%s",
                 i,
                 function_counts[i].count,
                 function_counts[i].key != NULL ? function_counts[i].key : "?");
        v3_report_append(&report, &report_cap, &report_used, line_buf);
    }
    for (i = 0; i < source_len && i < 8; i += 1) {
        snprintf(line_buf,
                 sizeof(line_buf),
                 "hot_source[%d]=%u|%s",
                 i,
                 source_counts[i].count,
                 source_counts[i].key != NULL ? source_counts[i].key : "?");
        v3_report_append(&report, &report_cap, &report_used, line_buf);
    }
    for (i = 0; i < pc_len && i < 8; i += 1) {
        snprintf(line_buf,
                 sizeof(line_buf),
                 "hot_pc[%d]=%u|%s",
                 i,
                 pc_counts[i].count,
                 pc_counts[i].key != NULL ? pc_counts[i].key : "?");
        v3_report_append(&report, &report_cap, &report_used, line_buf);
    }
    if (!v3_write_text_file(report_path, report)) {
        v3_profile_log_error(log_path, "v3 profile report: failed to write report\n");
        goto done;
    }
    ok = true;
done:
    free(report);
    free(raw_text);
    v3_profile_counts_free(function_counts, function_len);
    v3_profile_counts_free(source_counts, source_len);
    v3_profile_counts_free(pc_counts, pc_len);
    return ok;
}

static bool v3_compile_fixture_with_backend_driver_emit_target(const char *compiler_bin,
                                                               const char *root_v3,
                                                               const char *src_path,
                                                               const char *emit_kind,
                                                               const char *target,
                                                               const char *out_path,
                                                               const char *log_path,
                                                               const char *report_path) {
    char root_arg[PATH_MAX + 32];
    char in_arg[PATH_MAX + 32];
    char emit_arg[128];
    char target_arg[256];
    char out_arg[PATH_MAX + 32];
    char report_arg[PATH_MAX + 32];
    const char *args[8];
    size_t arg_count = 0U;
    bool ok;
    snprintf(root_arg, sizeof(root_arg), "--root:%s", root_v3);
    snprintf(in_arg, sizeof(in_arg), "--in:%s", src_path);
    snprintf(emit_arg, sizeof(emit_arg), "--emit:%s", emit_kind);
    snprintf(target_arg, sizeof(target_arg), "--target:%s", target);
    snprintf(out_arg, sizeof(out_arg), "--out:%s", out_path);
    args[arg_count++] = compiler_bin;
    args[arg_count++] = "system-link-exec";
    args[arg_count++] = root_arg;
    args[arg_count++] = in_arg;
    args[arg_count++] = emit_arg;
    args[arg_count++] = target_arg;
    args[arg_count++] = out_arg;
    if (report_path != NULL && report_path[0] != '\0') {
        snprintf(report_arg, sizeof(report_arg), "--report-out:%s", report_path);
        args[arg_count++] = report_arg;
    }
    ok = v3_run_command_logged_argv(arg_count, args, log_path, "ordinary fixture compile");
    if (ok && !v3_path_exists_nonempty(out_path)) {
        fprintf(stderr, "[cheng_v3_seed] ordinary fixture compile missing output: %s\n", out_path);
        ok = false;
    }
    return ok;
}

static bool v3_compile_fixture_with_backend_driver_target(const char *compiler_bin,
                                                          const char *root_v3,
                                                          const char *src_path,
                                                          const char *target,
                                                          const char *out_bin,
                                                          const char *log_path) {
    return v3_compile_fixture_with_backend_driver_emit_target(compiler_bin,
                                                              root_v3,
                                                              src_path,
                                                              "exe",
                                                              target,
                                                              out_bin,
                                                              log_path,
                                                              NULL);
}

static int v3_cmd_verify_tcp_bio_linux_regression_impl(void) {
    char saved_chain_node_linux_artifact[64];
    char saved_chain_node_run_self_test[64];
    char *bio_same_person_argv[3];
    const char *saved_env;
    bool had_chain_node_linux_artifact = false;
    bool had_chain_node_run_self_test = false;
    puts("[v3 gate] tcp/bio/linux early regression: bio_same_person_smoke");
    bio_same_person_argv[0] = "cheng_v3_seed";
    bio_same_person_argv[1] = "run-host-smokes";
    bio_same_person_argv[2] = "bio_same_person_smoke";
    if (v3_cmd_run_host_smokes_impl(3, bio_same_person_argv) != 0) {
        return 1;
    }
    saved_env = getenv("CHAIN_NODE_LINUX_ARTIFACT");
    if (saved_env != NULL && saved_env[0] != '\0') {
        had_chain_node_linux_artifact = true;
        v3_copy_text(saved_chain_node_linux_artifact, sizeof(saved_chain_node_linux_artifact), saved_env);
    }
    saved_env = getenv("CHAIN_NODE_RUN_SELF_TEST");
    if (saved_env != NULL && saved_env[0] != '\0') {
        had_chain_node_run_self_test = true;
        v3_copy_text(saved_chain_node_run_self_test, sizeof(saved_chain_node_run_self_test), saved_env);
    }
    setenv("CHAIN_NODE_LINUX_ARTIFACT", "exe", 1);
    setenv("CHAIN_NODE_RUN_SELF_TEST", "0", 1);
    puts("[v3 gate] tcp/bio/linux early regression: chain_node linux exe");
    if (v3_cmd_build_chain_node_linux_impl(0, NULL) != 0) {
        if (had_chain_node_linux_artifact) {
            setenv("CHAIN_NODE_LINUX_ARTIFACT", saved_chain_node_linux_artifact, 1);
        } else {
            unsetenv("CHAIN_NODE_LINUX_ARTIFACT");
        }
        if (had_chain_node_run_self_test) {
            setenv("CHAIN_NODE_RUN_SELF_TEST", saved_chain_node_run_self_test, 1);
        } else {
            unsetenv("CHAIN_NODE_RUN_SELF_TEST");
        }
        return 1;
    }
    if (had_chain_node_linux_artifact) {
        setenv("CHAIN_NODE_LINUX_ARTIFACT", saved_chain_node_linux_artifact, 1);
    } else {
        unsetenv("CHAIN_NODE_LINUX_ARTIFACT");
    }
    if (had_chain_node_run_self_test) {
        setenv("CHAIN_NODE_RUN_SELF_TEST", saved_chain_node_run_self_test, 1);
    } else {
        unsetenv("CHAIN_NODE_RUN_SELF_TEST");
    }
    return 0;
}

static int v3_cmd_slice_gate(int argc, char **argv) {
    V3BootstrapPaths paths;
    char remote_x86_log[PATH_MAX];
    char bft_verify_log[PATH_MAX];
    char saved_chain_node_linux_artifact[64];
    char saved_chain_node_run_self_test[64];
    char saved_rwad_bft_linux_artifact[64];
    char saved_rwad_bft_run_self_test[64];
    char *host_fixed_argv[3];
    char *host_default_argv[3];
    char *host_full_argv[2];
    char *remote_x86_argv[3];
    char *bft_verify_argv[3];
    char *stage23_full_argv[2];
    int status = 0;
    const char *saved_env;
    bool had_chain_node_linux_artifact = false;
    bool had_chain_node_run_self_test = false;
    bool had_rwad_bft_linux_artifact = false;
    bool had_rwad_bft_run_self_test = false;
    (void)argc;
    (void)argv;
    v3_bootstrap_paths_init(&paths);
    if (paths.root[0] == '\0') {
        fprintf(stderr, "[cheng_v3_seed] slice-gate root detect failed\n");
        return 1;
    }
    puts("[v3 gate] hot-path forbidden scan");
    if (v3_cmd_scan_hotpath(0, NULL) != 0) {
        return 1;
    }
    puts("[v3 gate] orphan guard + bootstrap path");
    if (v3_cmd_verify_orphan_guard_impl(0, NULL) != 0) {
        return 1;
    }
    puts("[v3 gate] zero-exit ordinary compile");
    if (v3_cmd_build_zero_exit_impl(0, NULL) != 0) {
        return 1;
    }
    puts("[v3 gate] panic-trace ordinary compile");
    if (v3_cmd_build_panic_trace_impl(0, NULL) != 0) {
        return 1;
    }
    puts("[v3 gate] bounds-trace ordinary compile");
    if (v3_cmd_build_bounds_trace_impl(0, NULL) != 0) {
        return 1;
    }
    puts("[v3 gate] signal-trace ordinary compile");
    if (v3_cmd_build_signal_trace_impl(0, NULL) != 0) {
        return 1;
    }
    puts("[v3 gate] debug tools");
    if (v3_cmd_verify_debug_tools_impl(0, NULL) != 0) {
        return 1;
    }
    puts("[v3 gate] debug runtime");
    if (v3_cmd_verify_debug_runtime_impl(0, NULL) != 0) {
        return 1;
    }
    puts("[v3 gate] debug profile");
    if (v3_cmd_verify_debug_profile_impl(0, NULL) != 0) {
        return 1;
    }
    puts("[v3 gate] call-chain ordinary compile");
    if (v3_cmd_build_call_chain_impl(0, NULL) != 0) {
        return 1;
    }
    puts("[v3 gate] ffi-handle ordinary compile");
    if (v3_cmd_build_ffi_handle_impl(0, NULL) != 0) {
        return 1;
    }
    puts("[v3 gate] program-selfhost");
    if (v3_cmd_build_program_selfhost_impl(0, NULL) != 0) {
        return 1;
    }
    puts("[v3 gate] chain_node");
    if (v3_cmd_build_chain_node_impl(0, NULL) != 0) {
        return 1;
    }
    if (v3_cmd_verify_tcp_bio_linux_regression_impl() != 0) {
        return 1;
    }
    puts("[v3 gate] linux aarch64 object smokes");
    if (v3_cmd_run_linux_object_smokes_impl(0, NULL) != 0) {
        return 1;
    }
    saved_env = getenv("CHAIN_NODE_LINUX_ARTIFACT");
    if (saved_env != NULL && saved_env[0] != '\0') {
        had_chain_node_linux_artifact = true;
        v3_copy_text(saved_chain_node_linux_artifact, sizeof(saved_chain_node_linux_artifact), saved_env);
    }
    saved_env = getenv("CHAIN_NODE_RUN_SELF_TEST");
    if (saved_env != NULL && saved_env[0] != '\0') {
        had_chain_node_run_self_test = true;
        v3_copy_text(saved_chain_node_run_self_test, sizeof(saved_chain_node_run_self_test), saved_env);
    }
    saved_env = getenv("RWAD_BFT_LINUX_ARTIFACT");
    if (saved_env != NULL && saved_env[0] != '\0') {
        had_rwad_bft_linux_artifact = true;
        v3_copy_text(saved_rwad_bft_linux_artifact, sizeof(saved_rwad_bft_linux_artifact), saved_env);
    }
    saved_env = getenv("RWAD_BFT_RUN_SELF_TEST");
    if (saved_env != NULL && saved_env[0] != '\0') {
        had_rwad_bft_run_self_test = true;
        v3_copy_text(saved_rwad_bft_run_self_test, sizeof(saved_rwad_bft_run_self_test), saved_env);
    }
    setenv("CHAIN_NODE_LINUX_ARTIFACT", "exe", 1);
    setenv("CHAIN_NODE_RUN_SELF_TEST", "0", 1);
    puts("[v3 gate] chain_node linux exe");
    if (v3_cmd_build_chain_node_linux_impl(0, NULL) != 0) {
        return 1;
    }
    setenv("RWAD_BFT_LINUX_ARTIFACT", "exe", 1);
    setenv("RWAD_BFT_RUN_SELF_TEST", "0", 1);
    puts("[v3 gate] rwad_bft linux exe");
    if (v3_cmd_build_rwad_bft_linux_impl(0, NULL) != 0) {
        return 1;
    }
    if (had_chain_node_linux_artifact) {
        setenv("CHAIN_NODE_LINUX_ARTIFACT", saved_chain_node_linux_artifact, 1);
    } else {
        unsetenv("CHAIN_NODE_LINUX_ARTIFACT");
    }
    if (had_chain_node_run_self_test) {
        setenv("CHAIN_NODE_RUN_SELF_TEST", saved_chain_node_run_self_test, 1);
    } else {
        unsetenv("CHAIN_NODE_RUN_SELF_TEST");
    }
    if (had_rwad_bft_linux_artifact) {
        setenv("RWAD_BFT_LINUX_ARTIFACT", saved_rwad_bft_linux_artifact, 1);
    } else {
        unsetenv("RWAD_BFT_LINUX_ARTIFACT");
    }
    if (had_rwad_bft_run_self_test) {
        setenv("RWAD_BFT_RUN_SELF_TEST", saved_rwad_bft_run_self_test, 1);
    } else {
        unsetenv("RWAD_BFT_RUN_SELF_TEST");
    }
    puts("[v3 gate] bootstrap subset self-checks");
    if (v3_cmd_run_smokes(0, NULL) != 0) {
        return 1;
    }
    puts("[v3 gate] host fixed256 sha256 smoke");
    host_fixed_argv[0] = "cheng_v3_seed";
    host_fixed_argv[1] = "run-host-smokes";
    host_fixed_argv[2] = "fixed256_sha256_smoke";
    if (v3_cmd_run_host_smokes_impl(3, host_fixed_argv) != 0) {
        return 1;
    }
    puts("[v3 gate] host default-init literals smoke");
    host_default_argv[0] = "cheng_v3_seed";
    host_default_argv[1] = "run-host-smokes";
    host_default_argv[2] = "default_init_literals_smoke";
    if (v3_cmd_run_host_smokes_impl(3, host_default_argv) != 0) {
        return 1;
    }
    puts("[v3 gate] backend_driver command surface");
    if (v3_cmd_verify_backend_driver_command_surface_impl(0, NULL) != 0) {
        return 1;
    }
    puts("[v3 gate] cross-target builtin smokes");
    if (v3_cmd_run_cross_target_smokes_impl(0, NULL) != 0) {
        return 1;
    }
    puts("[v3 gate] v3 host smokes");
    host_full_argv[0] = "cheng_v3_seed";
    host_full_argv[1] = "run-host-smokes";
    if (v3_cmd_run_host_smokes_impl(2, host_full_argv) != 0) {
        return 1;
    }
    puts("[v3 gate] stage2/stage3 libp2p smokes");
    stage23_full_argv[0] = "cheng_v3_seed";
    stage23_full_argv[1] = "run-stage23-libp2p-smokes";
    if (v3_cmd_run_stage23_libp2p_smokes_impl(2, stage23_full_argv) != 0) {
        return 1;
    }
    puts("[v3 gate] remote x86 linux release gate");
    v3_join_path(remote_x86_log, sizeof(remote_x86_log), paths.root, "artifacts/v3_gate/verify_x86_linux_remote.log");
    remote_x86_argv[0] = paths.backend_driver_out;
    remote_x86_argv[1] = (char *)"verify-x86-linux-remote";
    remote_x86_argv[2] = NULL;
    if (!v3_run_binary_capture_output(remote_x86_argv, remote_x86_log, &status) ||
        !v3_status_is_exit_code(status, 0)) {
        fprintf(stderr, "[cheng_v3_seed] slice-gate remote x86 release gate failed log=%s\n", remote_x86_log);
        return 1;
    }
    puts("[v3 gate] chain_node three-node smoke");
    if (v3_cmd_run_chain_node_three_node_smoke_impl(0, NULL) != 0) {
        return 1;
    }
    puts("[v3 gate] bft three-node verify");
    v3_join_path(bft_verify_log,
                 sizeof(bft_verify_log),
                 paths.root,
                 "artifacts/v3_gate/verify_bft_validator_three_node.log");
    bft_verify_argv[0] = paths.backend_driver_out;
    bft_verify_argv[1] = (char *)"verify-bft-validator-three-node";
    bft_verify_argv[2] = NULL;
    if (!v3_run_binary_capture_output(bft_verify_argv, bft_verify_log, &status) ||
        !v3_status_is_exit_code(status, 0)) {
        fprintf(stderr, "[cheng_v3_seed] slice-gate bft three-node verify failed log=%s\n", bft_verify_log);
        return 1;
    }
    puts("[v3 gate] ok");
    return 0;
}

static bool v3_compile_host_fixture_with_backend_driver(const char *compiler_bin,
                                                        const char *root_v3,
                                                        const char *src_path,
                                                        const char *out_bin,
                                                        const char *log_path) {
    V3BootstrapPaths paths;
    const char *resolved_compiler = compiler_bin;
    const char *host_target = NULL;
    v3_bootstrap_paths_init(&paths);
    if (resolved_compiler != NULL &&
        resolved_compiler[0] != '\0') {
        if (strcmp(resolved_compiler, paths.backend_driver_out) == 0 &&
            access(resolved_compiler, X_OK) != 0) {
            if (!v3_backend_driver_ready(&paths) &&
                v3_cmd_build_backend_driver(0, NULL) != 0) {
                return false;
            }
            resolved_compiler = paths.backend_driver_out;
        } else if ((strcmp(resolved_compiler, paths.stage0) == 0 ||
                    strcmp(resolved_compiler, paths.stage1) == 0 ||
                    strcmp(resolved_compiler, paths.stage2) == 0 ||
                    strcmp(resolved_compiler, paths.stage3) == 0) &&
                   access(resolved_compiler, X_OK) != 0) {
            if (!v3_bootstrap_artifacts_fresh(&paths) &&
                v3_cmd_bootstrap_bridge(0, NULL) != 0) {
                return false;
            }
        }
    }
    if (resolved_compiler == NULL || resolved_compiler[0] == '\0' || access(resolved_compiler, X_OK) != 0) {
        fprintf(stderr, "[cheng_v3_seed] missing compiler during fixture compile: %s\n",
                resolved_compiler != NULL ? resolved_compiler : "");
        return false;
    }
    host_target = v3_host_target_triple();
    if (host_target == NULL || host_target[0] == '\0') {
        fprintf(stderr, "[cheng_v3_seed] host fixture compile unsupported host target\n");
        return false;
    }
    return v3_compile_fixture_with_backend_driver_target(resolved_compiler,
                                                         root_v3,
                                                         src_path,
                                                         host_target,
                                                         out_bin,
                                                         log_path);
}

static bool v3_leaf_prepare_backend_driver(V3BootstrapPaths *paths,
                                           char *root_v3,
                                           size_t root_v3_cap) {
    v3_bootstrap_paths_init(paths);
    if (!v3_ensure_backend_driver_ready(paths, "leaf-prepare-backend-driver")) {
        return false;
    }
    v3_join_path(root_v3, root_v3_cap, paths->root, "v3");
    return true;
}

static bool v3_dump_file_stdout(const char *path) {
    char *text = v3_read_file(path);
    if (text == NULL) {
        fprintf(stderr, "[cheng_v3_seed] missing output log: %s\n", path);
        return false;
    }
    fputs(text, stdout);
    free(text);
    return true;
}

static bool v3_expect_line_map_text(const char *label,
                                    const char *line_map_path,
                                    const char *fixture_substring,
                                    bool require_header) {
    char *text = v3_read_file(line_map_path);
    bool ok = true;
    if (text == NULL) {
        fprintf(stderr, "[cheng_v3_seed] %s missing line-map: %s\n", label, line_map_path);
        return false;
    }
    if (require_header) {
        ok = ok && v3_expect_text_has_line(label, text, "v3_line_map_v1");
    }
    if (fixture_substring != NULL && fixture_substring[0] != '\0') {
        ok = ok && v3_expect_text_contains(label, text, fixture_substring);
    }
    free(text);
    return ok;
}

static const char *v3_host_target_triple(void) {
#if defined(__APPLE__) && (defined(__aarch64__) || defined(__arm64__))
    return "arm64-apple-darwin";
#elif defined(__linux__) && defined(__x86_64__)
    return "x86_64-unknown-linux-gnu";
#elif defined(__linux__) && (defined(__aarch64__) || defined(__arm64__))
    return "aarch64-unknown-linux-gnu";
#else
    return "";
#endif
}

static int v3_cmd_build_zero_exit_impl(int argc, char **argv) {
    V3BootstrapPaths paths;
    char root_v3[PATH_MAX];
    char out_dir[PATH_MAX];
    char src[PATH_MAX];
    char out_bin[PATH_MAX];
    char line_map[PATH_MAX];
    char compile_log[PATH_MAX];
    char run_log[PATH_MAX];
    char *run_argv[2];
    int status = 0;
    (void)argc;
    (void)argv;
    if (!v3_leaf_prepare_backend_driver(&paths, root_v3, sizeof(root_v3))) {
        return 1;
    }
    v3_join_path(out_dir, sizeof(out_dir), paths.root, "artifacts/v3_zero_exit");
    v3_join_path(src, sizeof(src), paths.root, "v3/src/tests/ordinary_zero_exit_fixture.cheng");
    v3_join_path(out_bin, sizeof(out_bin), out_dir, "ordinary_zero_exit_fixture");
    snprintf(line_map, sizeof(line_map), "%s.v3.map", out_bin);
    v3_join_path(compile_log, sizeof(compile_log), out_dir, "ordinary_zero_exit_fixture.compile.log");
    v3_join_path(run_log, sizeof(run_log), out_dir, "ordinary_zero_exit_fixture.run.log");
    if (!v3_mkdir_p(out_dir) ||
        !v3_compile_host_fixture_with_backend_driver(paths.backend_driver_out, root_v3, src, out_bin, compile_log) ||
        !v3_expect_file_exists_nonempty("build-zero-exit line-map", line_map)) {
        return 1;
    }
    run_argv[0] = out_bin;
    run_argv[1] = NULL;
    if (!v3_run_binary_capture_output(run_argv, run_log, &status) ||
        !v3_status_is_exit_code(status, 0)) {
        fprintf(stderr, "[cheng_v3_seed] build-zero-exit expected success log=%s\n", run_log);
        return 1;
    }
    return v3_dump_file_stdout(run_log) ? 0 : 1;
}

static int v3_cmd_build_call_chain_impl(int argc, char **argv) {
    V3BootstrapPaths paths;
    char root_v3[PATH_MAX];
    char out_dir[PATH_MAX];
    char src[PATH_MAX];
    char out_bin[PATH_MAX];
    char compile_log[PATH_MAX];
    char run_log[PATH_MAX];
    char *run_argv[2];
    int status = 0;
    (void)argc;
    (void)argv;
    if (!v3_leaf_prepare_backend_driver(&paths, root_v3, sizeof(root_v3))) {
        return 1;
    }
    v3_join_path(out_dir, sizeof(out_dir), paths.root, "artifacts/v3_call_chain");
    v3_join_path(src, sizeof(src), paths.root, "v3/src/tests/ordinary_call_chain_fixture.cheng");
    v3_join_path(out_bin, sizeof(out_bin), out_dir, "ordinary_call_chain_fixture");
    v3_join_path(compile_log, sizeof(compile_log), out_dir, "ordinary_call_chain_fixture.compile.log");
    v3_join_path(run_log, sizeof(run_log), out_dir, "ordinary_call_chain_fixture.run.log");
    if (!v3_mkdir_p(out_dir) ||
        !v3_compile_host_fixture_with_backend_driver(paths.backend_driver_out, root_v3, src, out_bin, compile_log)) {
        return 1;
    }
    run_argv[0] = out_bin;
    run_argv[1] = NULL;
    if (!v3_run_binary_capture_output(run_argv, run_log, &status) ||
        !v3_status_is_exit_code(status, 0)) {
        fprintf(stderr, "[cheng_v3_seed] build-call-chain expected success log=%s\n", run_log);
        return 1;
    }
    return v3_dump_file_stdout(run_log) ? 0 : 1;
}

static int v3_cmd_build_program_selfhost_impl(int argc, char **argv) {
    V3BootstrapPaths paths;
    char root_v3[PATH_MAX];
    char out_dir[PATH_MAX];
    char src[PATH_MAX];
    char out_bin[PATH_MAX];
    char compile_log[PATH_MAX];
    char run_log[PATH_MAX];
    char *run_argv[2];
    int status = 0;
    (void)argc;
    (void)argv;
    if (!v3_leaf_prepare_backend_driver(&paths, root_v3, sizeof(root_v3))) {
        return 1;
    }
    v3_join_path(out_dir, sizeof(out_dir), paths.root, "artifacts/v3_program_selfhost");
    v3_join_path(src, sizeof(src), paths.root, "v3/src/tests/program_selfhost_smoke.cheng");
    v3_join_path(out_bin, sizeof(out_bin), out_dir, "program_selfhost_smoke");
    v3_join_path(compile_log, sizeof(compile_log), out_dir, "program_selfhost_smoke.compile.log");
    v3_join_path(run_log, sizeof(run_log), out_dir, "program_selfhost_smoke.run.log");
    if (!v3_mkdir_p(out_dir) ||
        !v3_compile_host_fixture_with_backend_driver(paths.backend_driver_out, root_v3, src, out_bin, compile_log)) {
        return 1;
    }
    run_argv[0] = out_bin;
    run_argv[1] = NULL;
    if (!v3_run_binary_capture_output(run_argv, run_log, &status) ||
        !v3_status_is_exit_code(status, 0)) {
        fprintf(stderr, "[cheng_v3_seed] build-program-selfhost expected success log=%s\n", run_log);
        return 1;
    }
    return v3_dump_file_stdout(run_log) ? 0 : 1;
}

static int v3_cmd_build_bft_state_machine_impl(int argc, char **argv) {
    V3BootstrapPaths paths;
    char root_v3[PATH_MAX];
    char src[PATH_MAX];
    char default_out_dir[PATH_MAX];
    char out_dir[PATH_MAX];
    char out_bin[PATH_MAX];
    char compile_log[PATH_MAX];
    char run_log[PATH_MAX];
    char target_buf[256];
    const char *compiler_bin;
    const char *target = getenv("BFT_STATE_TARGET");
    const char *out_dir_env = getenv("BFT_STATE_OUT_DIR");
    const char *out_bin_env = getenv("BFT_STATE_OUT_BIN");
    const char *run_self_test_env = getenv("BFT_STATE_RUN_SELF_TEST");
    const char *host_target = v3_host_target_triple();
    char *run_argv[3];
    char *run_text = NULL;
    int status = 0;
    bool run_self_test = false;
    const char *build_inputs[2];
    const char *run_inputs[3];
    if (!v3_leaf_prepare_backend_driver(&paths, root_v3, sizeof(root_v3))) {
        return 1;
    }
    compiler_bin = v3_leaf_resolve_compiler_bin(argc,
                                                argv,
                                                "CHENG_V3_BFT_COMPILER",
                                                NULL,
                                                paths.backend_driver_out);
    if (!v3_leaf_expect_compiler("build-bft-state-machine", compiler_bin)) {
        return 1;
    }
    if (target == NULL || target[0] == '\0') {
        target = "arm64-apple-darwin";
    }
    v3_copy_text(target_buf, sizeof(target_buf), target);
    v3_join_path(src, sizeof(src), paths.root, "v3/src/project/bft_state_machine_main.cheng");
    v3_join_path(default_out_dir, sizeof(default_out_dir), paths.root, "artifacts/v3_bft_state_machine");
    if (!v3_streq(target_buf, "arm64-apple-darwin")) {
        v3_join_path(default_out_dir, sizeof(default_out_dir), default_out_dir, target_buf);
    }
    if (out_dir_env != NULL && out_dir_env[0] != '\0') {
        v3_copy_text(out_dir, sizeof(out_dir), out_dir_env);
    } else {
        v3_copy_text(out_dir, sizeof(out_dir), default_out_dir);
    }
    if (out_bin_env != NULL && out_bin_env[0] != '\0') {
        v3_copy_text(out_bin, sizeof(out_bin), out_bin_env);
    } else {
        v3_join_path(out_bin, sizeof(out_bin), out_dir, "bft_state_machine");
    }
    v3_join_path(compile_log, sizeof(compile_log), out_dir, "bft_state_machine.compile.log");
    v3_join_path(run_log, sizeof(run_log), out_dir, "bft_state_machine.self-test.log");
    if (run_self_test_env != NULL && run_self_test_env[0] != '\0' && !v3_streq(run_self_test_env, "auto")) {
        run_self_test = !v3_streq(run_self_test_env, "0");
    } else {
        run_self_test = host_target[0] != '\0' && v3_streq(target_buf, host_target);
    }
    build_inputs[0] = compiler_bin;
    build_inputs[1] = src;
    if (access(out_bin, X_OK) == 0 &&
        v3_output_is_fresh_against_inputs(out_bin, build_inputs, 2U)) {
        if (!run_self_test) {
            printf("v3 bft_state_machine: build ok target=%s out=%s\n", target_buf, out_bin);
            return 0;
        }
        run_inputs[0] = compiler_bin;
        run_inputs[1] = src;
        run_inputs[2] = out_bin;
        if (access(run_log, R_OK) == 0 &&
            v3_output_is_fresh_against_inputs(run_log, run_inputs, 3U)) {
            run_text = v3_read_file(run_log);
            if (run_text != NULL && strstr(run_text, "v3 bft_state_machine self-test ok") != NULL) {
                fputs(run_text, stdout);
                free(run_text);
                return 0;
            }
            free(run_text);
        }
    }
    if (!v3_mkdir_p(out_dir) ||
        !v3_ensure_parent_dir(out_bin) ||
        !v3_compile_fixture_with_backend_driver_target(compiler_bin,
                                                       root_v3,
                                                       src,
                                                       target_buf,
                                                       out_bin,
                                                       compile_log) ||
        access(out_bin, X_OK) != 0) {
        fprintf(stderr, "[cheng_v3_seed] build-bft-state-machine missing output: %s\n", out_bin);
        return 1;
    }
    if (!run_self_test) {
        printf("v3 bft_state_machine: build ok target=%s out=%s\n", target_buf, out_bin);
        return 0;
    }
    run_argv[0] = out_bin;
    run_argv[1] = "self-test";
    run_argv[2] = NULL;
    if (!v3_run_binary_capture_output(run_argv, run_log, &status) ||
        !v3_status_is_exit_code(status, 0)) {
        fprintf(stderr, "[cheng_v3_seed] build-bft-state-machine self-test failed log=%s\n", run_log);
        return 1;
    }
    return v3_dump_file_stdout(run_log) ? 0 : 1;
}

typedef struct {
    char local_deploy_dir[PATH_MAX];
    char server_root[PATH_MAX];
    char relay_root[PATH_MAX];
    char server_ssh[256];
    char relay_ssh[256];
    char server_addr[256];
    char app_id[256];
    int proposal_port;
    int commit_port;
    int vote_port;
    int state_port;
    int proposer_interval_ms;
    int follower_interval_ms;
    int proposal_window_ms;
    int vote_window_ms;
    int commit_window_ms;
    int default_max_txs;
    int asset_id;
    int alice_id;
    int bob_id;
    long long mint_amount;
    long long transfer_amount;
} V3BftDeployConfig;

static void v3_bft_expected_baseline(const V3BftDeployConfig *cfg,
                                     char *height_out,
                                     size_t height_cap,
                                     char *alice_out,
                                     size_t alice_cap,
                                     char *bob_out,
                                     size_t bob_cap);

static void v3_bft_config_init(const V3BootstrapPaths *paths,
                               V3BftDeployConfig *cfg) {
    const char *value;
    memset(cfg, 0, sizeof(*cfg));
    v3_join_path(cfg->local_deploy_dir,
                 sizeof(cfg->local_deploy_dir),
                 paths->root,
                 "artifacts/v3_bft_validator_deploy");
    value = getenv("CHENG_V3_BFT_SERVER_ROOT");
    snprintf(cfg->server_root,
             sizeof(cfg->server_root),
             "%s",
             value != NULL && value[0] != '\0' ? value : "/root/cheng-lang-deploy");
    value = getenv("CHENG_V3_BFT_RELAY_ROOT");
    snprintf(cfg->relay_root,
             sizeof(cfg->relay_root),
             "%s",
             value != NULL && value[0] != '\0' ? value : "/root/cheng-lang-deploy");
    value = getenv("CHENG_V3_BFT_SERVER_SSH");
    snprintf(cfg->server_ssh,
             sizeof(cfg->server_ssh),
             "%s",
             value != NULL && value[0] != '\0' ? value : "root@64.176.84.12");
    value = getenv("CHENG_V3_BFT_RELAY_SSH");
    snprintf(cfg->relay_ssh,
             sizeof(cfg->relay_ssh),
             "%s",
             value != NULL && value[0] != '\0' ? value : "dmit");
    value = getenv("CHENG_V3_BFT_SERVER_ADDR");
    snprintf(cfg->server_addr,
             sizeof(cfg->server_addr),
             "%s",
             value != NULL && value[0] != '\0' ? value : "64.176.84.12");
    value = getenv("CHENG_V3_BFT_APP_ID");
    snprintf(cfg->app_id,
             sizeof(cfg->app_id),
             "%s",
             value != NULL && value[0] != '\0' ? value : "cheng-bft-live-20260415");
    value = getenv("CHENG_V3_BFT_PROPOSAL_PORT");
    cfg->proposal_port = value != NULL && value[0] != '\0' ? atoi(value) : 42061;
    value = getenv("CHENG_V3_BFT_COMMIT_PORT");
    cfg->commit_port = value != NULL && value[0] != '\0' ? atoi(value) : 42062;
    value = getenv("CHENG_V3_BFT_VOTE_PORT");
    cfg->vote_port = value != NULL && value[0] != '\0' ? atoi(value) : 42063;
    value = getenv("CHENG_V3_BFT_STATE_PORT");
    cfg->state_port = value != NULL && value[0] != '\0' ? atoi(value) : 42064;
    value = getenv("CHENG_V3_BFT_PROPOSER_INTERVAL_MS");
    cfg->proposer_interval_ms = value != NULL && value[0] != '\0' ? atoi(value) : 20;
    value = getenv("CHENG_V3_BFT_FOLLOWER_INTERVAL_MS");
    cfg->follower_interval_ms = value != NULL && value[0] != '\0' ? atoi(value) : 20;
    value = getenv("CHENG_V3_BFT_PROPOSAL_WINDOW_MS");
    cfg->proposal_window_ms = value != NULL && value[0] != '\0' ? atoi(value) : 1000;
    value = getenv("CHENG_V3_BFT_VOTE_WINDOW_MS");
    cfg->vote_window_ms = value != NULL && value[0] != '\0' ? atoi(value) : 1000;
    value = getenv("CHENG_V3_BFT_COMMIT_WINDOW_MS");
    cfg->commit_window_ms = value != NULL && value[0] != '\0' ? atoi(value) : 1000;
    value = getenv("CHENG_V3_BFT_DEFAULT_MAX_TXS");
    cfg->default_max_txs = value != NULL && value[0] != '\0' ? atoi(value) : 128;
    value = getenv("CHENG_V3_BFT_ASSET_ID");
    cfg->asset_id = value != NULL && value[0] != '\0' ? atoi(value) : 7;
    value = getenv("CHENG_V3_BFT_ALICE_ID");
    cfg->alice_id = value != NULL && value[0] != '\0' ? atoi(value) : 1001;
    value = getenv("CHENG_V3_BFT_BOB_ID");
    cfg->bob_id = value != NULL && value[0] != '\0' ? atoi(value) : 2002;
    value = getenv("CHENG_V3_BFT_MINT_AMOUNT");
    cfg->mint_amount = value != NULL && value[0] != '\0' ? atoll(value) : 100LL;
    value = getenv("CHENG_V3_BFT_TRANSFER_AMOUNT");
    cfg->transfer_amount = value != NULL && value[0] != '\0' ? atoll(value) : 33LL;
}

static void v3_bft_remote_deploy_dir(const char *repo_root,
                                     char *out,
                                     size_t cap) {
    v3_join_path(out, cap, repo_root, "artifacts/v3_bft_validator_deploy");
}

static void v3_bft_remote_bin(const char *repo_root,
                              char *out,
                              size_t cap) {
    char temp[PATH_MAX];
    v3_join_path(temp, sizeof(temp), repo_root, "artifacts/v3_bft_state_machine");
    v3_join_path(temp, sizeof(temp), temp, "x86_64-unknown-linux-gnu");
    v3_join_path(out, cap, temp, "bft_state_machine");
}

static void v3_bft_local_bin(const V3BootstrapPaths *paths,
                             char *out,
                             size_t cap) {
    char temp[PATH_MAX];
    v3_join_path(temp, sizeof(temp), paths->root, "artifacts/v3_bft_state_machine");
    v3_join_path(out, cap, temp, "bft_state_machine");
}

static void v3_bft_remote_run_log(const char *repo_root,
                                  char *out,
                                  size_t cap) {
    char out_dir[PATH_MAX];
    v3_join_path(out_dir,
                 sizeof(out_dir),
                 repo_root,
                 "artifacts/v3_bft_state_machine/x86_64-unknown-linux-gnu");
    v3_join_path(out, cap, out_dir, "bft_state_machine.self-test.log");
}

static void v3_bft_remote_backend_driver(const char *repo_root,
                                         char *out,
                                         size_t cap) {
    char temp[PATH_MAX];
    v3_join_path(temp, sizeof(temp), repo_root, "artifacts/v3_backend_driver");
    v3_join_path(out, cap, temp, "cheng");
}

static void v3_bft_remote_stage3(const char *repo_root,
                                 char *out,
                                 size_t cap) {
    char temp[PATH_MAX];
    v3_join_path(temp, sizeof(temp), repo_root, "artifacts/v3_bootstrap");
    v3_join_path(out, cap, temp, "cheng.stage3");
}

static void v3_bft_remote_snapshot(const char *repo_root,
                                   char *out,
                                   size_t cap) {
    char temp[PATH_MAX];
    v3_join_path(temp, sizeof(temp), repo_root, "artifacts/v3_bootstrap");
    v3_join_path(out, cap, temp, "bootstrap_snapshot.txt");
}

static char *v3_bft_remote_build_ready_shell(const char *repo_root) {
    char compiler[PATH_MAX];
    char src[PATH_MAX];
    char out_bin[PATH_MAX];
    char run_log[PATH_MAX];
    char *quoted_compiler = NULL;
    char *quoted_src = NULL;
    char *quoted_out_bin = NULL;
    char *quoted_run_log = NULL;
    char *command = NULL;
    size_t cap;
    v3_bft_remote_backend_driver(repo_root, compiler, sizeof(compiler));
    v3_join_path(src, sizeof(src), repo_root, "v3/src/project/bft_state_machine_main.cheng");
    v3_bft_remote_bin(repo_root, out_bin, sizeof(out_bin));
    v3_bft_remote_run_log(repo_root, run_log, sizeof(run_log));
    quoted_compiler = v3_shell_quote(compiler);
    quoted_src = v3_shell_quote(src);
    quoted_out_bin = v3_shell_quote(out_bin);
    quoted_run_log = v3_shell_quote(run_log);
    cap = strlen(quoted_compiler) * 3U +
          strlen(quoted_src) * 2U +
          strlen(quoted_out_bin) * 3U +
          strlen(quoted_run_log) * 4U +
          256U;
    command = (char *)v3_xmalloc(cap);
    snprintf(command,
             cap,
             "test -x %s && test -x %s && test %s -ot %s && test %s -ot %s && test %s -ot %s && test %s -ot %s && test %s -ot %s && grep -qF %s %s",
             quoted_out_bin,
             quoted_compiler,
             quoted_compiler,
             quoted_out_bin,
             quoted_src,
             quoted_out_bin,
             quoted_out_bin,
             quoted_run_log,
             quoted_compiler,
             quoted_run_log,
             quoted_src,
             quoted_run_log,
             "\"v3 bft_state_machine self-test ok\"",
             quoted_run_log);
    free(quoted_compiler);
    free(quoted_src);
    free(quoted_out_bin);
    free(quoted_run_log);
    return command;
}

static char *v3_bft_remote_stage3_fresh_shell(const char *repo_root) {
    char stage3[PATH_MAX];
    char snapshot[PATH_MAX];
    char seed[PATH_MAX];
    char stage1[PATH_MAX];
    char compiler_main[PATH_MAX];
    char compiler_runtime[PATH_MAX];
    char compiler_request[PATH_MAX];
    char gate_main[PATH_MAX];
    char *quoted_stage3 = NULL;
    char *quoted_snapshot = NULL;
    char *quoted_seed = NULL;
    char *quoted_stage1 = NULL;
    char *quoted_compiler_main = NULL;
    char *quoted_compiler_runtime = NULL;
    char *quoted_compiler_request = NULL;
    char *quoted_gate_main = NULL;
    char *command = NULL;
    size_t cap;
    v3_bft_remote_stage3(repo_root, stage3, sizeof(stage3));
    v3_bft_remote_snapshot(repo_root, snapshot, sizeof(snapshot));
    v3_join_path(seed, sizeof(seed), repo_root, "v3/bootstrap/cheng_v3_seed.c");
    v3_join_path(stage1, sizeof(stage1), repo_root, "v3/bootstrap/stage1_bootstrap.cheng");
    v3_join_path(compiler_main, sizeof(compiler_main), repo_root, "v3/src/tooling/compiler_main.cheng");
    v3_join_path(compiler_runtime, sizeof(compiler_runtime), repo_root, "v3/src/tooling/compiler_runtime.cheng");
    v3_join_path(compiler_request, sizeof(compiler_request), repo_root, "v3/src/tooling/compiler_request.cheng");
    v3_join_path(gate_main, sizeof(gate_main), repo_root, "v3/src/tooling/gate_main.cheng");
    quoted_stage3 = v3_shell_quote(stage3);
    quoted_snapshot = v3_shell_quote(snapshot);
    quoted_seed = v3_shell_quote(seed);
    quoted_stage1 = v3_shell_quote(stage1);
    quoted_compiler_main = v3_shell_quote(compiler_main);
    quoted_compiler_runtime = v3_shell_quote(compiler_runtime);
    quoted_compiler_request = v3_shell_quote(compiler_request);
    quoted_gate_main = v3_shell_quote(gate_main);
    cap = strlen(quoted_stage3) * 7U +
          strlen(quoted_snapshot) +
          strlen(quoted_seed) +
          strlen(quoted_stage1) +
          strlen(quoted_compiler_main) +
          strlen(quoted_compiler_runtime) +
          strlen(quoted_compiler_request) +
          strlen(quoted_gate_main) +
          640U;
    command = (char *)v3_xmalloc(cap);
    snprintf(command,
             cap,
             "test -x %s && snapshot=%s && stage3=%s && if [ -f \"$snapshot\" ]; then fresh=$(grep \"^fresh_input_paths=\" \"$snapshot\" | tail -n 1 | cut -d= -f2-); if [ -n \"$fresh\" ]; then old_ifs=\"$IFS\"; IFS=,; set -- $fresh; IFS=\"$old_ifs\"; for input in \"$@\"; do if [ -n \"$input\" ] && ! test \"$input\" -ot \"$stage3\"; then exit 1; fi; done; exit 0; fi; fi && test %s -ot \"$stage3\" && test %s -ot \"$stage3\" && test %s -ot \"$stage3\" && test %s -ot \"$stage3\" && test %s -ot \"$stage3\" && test %s -ot \"$stage3\"",
             quoted_stage3,
             quoted_snapshot,
             quoted_stage3,
             quoted_seed,
             quoted_stage1,
             quoted_compiler_main,
             quoted_compiler_runtime,
             quoted_compiler_request,
             quoted_gate_main);
    free(quoted_stage3);
    free(quoted_snapshot);
    free(quoted_seed);
    free(quoted_stage1);
    free(quoted_compiler_main);
    free(quoted_compiler_runtime);
    free(quoted_compiler_request);
    free(quoted_gate_main);
    return command;
}

static char *v3_bft_remote_locked_shell(const char *repo_root,
                                        const char *inner_command) {
    char *quoted_root = NULL;
    char *command = NULL;
    size_t cap;
    quoted_root = v3_shell_quote(repo_root);
    cap = strlen(quoted_root) + strlen(inner_command) + 768U;
    command = (char *)v3_xmalloc(cap);
    snprintf(command,
             cap,
             "set -eu && cd %s && mkdir -p artifacts/v3_gate && lock_dir=artifacts/v3_gate/bft_remote_build.lock && lock_pid=\"$lock_dir/pid\" && while ! mkdir \"$lock_dir\" 2>/dev/null; do holder=\"\"; if [ -f \"$lock_pid\" ]; then holder=$(cat \"$lock_pid\" 2>/dev/null || true); fi; if [ -z \"$holder\" ] || ! kill -0 \"$holder\" 2>/dev/null; then rm -f \"$lock_pid\" 2>/dev/null || true; rmdir \"$lock_dir\" 2>/dev/null || true; continue; fi; sleep 0.1; done && printf \"%%s\\n\" \"$$\" > \"$lock_pid\" && trap \"rm -f \\\"$lock_pid\\\" 2>/dev/null || true; rmdir \\\"$lock_dir\\\" 2>/dev/null || true\" EXIT INT TERM && %s",
             quoted_root,
             inner_command);
    free(quoted_root);
    return command;
}

static bool v3_bft_write_manifest(const V3BftDeployConfig *cfg) {
    char manifest[PATH_MAX];
    char text[2048];
    v3_join_path(manifest, sizeof(manifest), cfg->local_deploy_dir, "deploy.env");
    if (!v3_mkdir_p(cfg->local_deploy_dir)) {
        return false;
    }
    snprintf(text,
             sizeof(text),
             "app_id=%s\n"
             "server_ssh=%s\n"
             "relay_ssh=%s\n"
             "server_addr=%s\n"
             "server_root=%s\n"
             "relay_root=%s\n"
             "local_deploy_dir=%s\n"
             "proposal_port=%d\n"
             "commit_port=%d\n"
             "vote_port=%d\n"
             "state_port=%d\n"
             "proposer_interval_ms=%d\n"
             "follower_interval_ms=%d\n"
             "proposal_window_ms=%d\n"
             "vote_window_ms=%d\n"
             "commit_window_ms=%d\n"
             "default_max_txs=%d\n"
             "asset_id=%d\n"
             "alice_id=%d\n"
             "bob_id=%d\n"
             "mint_amount=%lld\n"
             "transfer_amount=%lld\n",
             cfg->app_id,
             cfg->server_ssh,
             cfg->relay_ssh,
             cfg->server_addr,
             cfg->server_root,
             cfg->relay_root,
             cfg->local_deploy_dir,
             cfg->proposal_port,
             cfg->commit_port,
             cfg->vote_port,
             cfg->state_port,
             cfg->proposer_interval_ms,
             cfg->follower_interval_ms,
             cfg->proposal_window_ms,
             cfg->vote_window_ms,
             cfg->commit_window_ms,
             cfg->default_max_txs,
             cfg->asset_id,
             cfg->alice_id,
             cfg->bob_id,
             cfg->mint_amount,
             cfg->transfer_amount);
    return v3_write_text_file(manifest, text);
}

static bool v3_bft_run_local_shell(const char *command,
                                   const char *log_path,
                                   const char *label) {
    return v3_run_shell_command_logged(command, log_path, label);
}

static bool v3_bft_remote_retryable_log(const char *log_path) {
    char *text = v3_read_file(log_path);
    bool retryable;
    retryable = text != NULL &&
                (strstr(text, "Connection timed out during banner exchange") != NULL ||
                 (strstr(text, "Connection to ") != NULL &&
                  strstr(text, " timed out") != NULL) ||
                 strstr(text, "No route to host") != NULL ||
                 strstr(text, "Connection reset by peer") != NULL ||
                 strstr(text, "kex_exchange_identification") != NULL ||
                 strstr(text, "Connection closed by remote host") != NULL);
    free(text);
    return retryable;
}

static bool v3_bft_run_remote_shell(const char *ssh_host,
                                    const char *command,
                                    const char *log_path,
                                    const char *label) {
    char safe_label[128];
    char local_script[PATH_MAX];
    char *script_text;
    const char *ssh_argv[13];
    bool ok = false;
    size_t attempt;
    size_t i;
    if (ssh_host == NULL || ssh_host[0] == '\0' ||
        command == NULL ||
        log_path == NULL || log_path[0] == '\0' ||
        label == NULL || label[0] == '\0') {
        return false;
    }
    for (i = 0U; label[i] != '\0' && i + 1U < sizeof(safe_label); ++i) {
        unsigned char ch = (unsigned char)label[i];
        if (isalnum(ch)) {
            safe_label[i] = (char)ch;
        } else {
            safe_label[i] = '_';
        }
    }
    if (i == 0U) {
        safe_label[i++] = 'x';
    }
    safe_label[i] = '\0';
    snprintf(local_script, sizeof(local_script), "%s.remote.sh", log_path);
    script_text = (char *)v3_xmalloc(strlen(command) + 12U);
    snprintf(script_text, strlen(command) + 12U, "#!/bin/sh\n%s\n", command);
    if (!v3_ensure_parent_dir(local_script) || !v3_write_text_file(local_script, script_text)) {
        free(script_text);
        return false;
    }
    free(script_text);
    ssh_argv[0] = "ssh";
    ssh_argv[1] = "-T";
    ssh_argv[2] = "-o";
    ssh_argv[3] = "BatchMode=yes";
    ssh_argv[4] = "-o";
    ssh_argv[5] = "ConnectTimeout=20";
    ssh_argv[6] = "-o";
    ssh_argv[7] = "ServerAliveInterval=5";
    ssh_argv[8] = "-o";
    ssh_argv[9] = "ServerAliveCountMax=3";
    ssh_argv[10] = ssh_host;
    ssh_argv[11] = "sh";
    ssh_argv[12] = NULL;
    for (attempt = 0U; attempt < 3U; ++attempt) {
        ok = v3_run_command_logged_argv_with_input_file(12U,
                                                        ssh_argv,
                                                        local_script,
                                                        log_path,
                                                        label,
                                                        false);
        if (ok || attempt + 1U >= 3U || !v3_bft_remote_retryable_log(log_path)) {
            break;
        }
        fprintf(stdout, "[v3 gate] remote ssh retry %zu %s\n", attempt + 1U, label);
        usleep((useconds_t)((attempt + 1U) * 1000000U));
    }
    return ok;
}

static bool v3_bft_run_remote_shell_quiet(const char *ssh_host,
                                          const char *command,
                                          const char *log_path,
                                          const char *label) {
    char safe_label[128];
    char local_script[PATH_MAX];
    char *script_text;
    const char *ssh_argv[13];
    bool ok = false;
    size_t attempt;
    size_t i;
    if (ssh_host == NULL || ssh_host[0] == '\0' ||
        command == NULL ||
        log_path == NULL || log_path[0] == '\0' ||
        label == NULL || label[0] == '\0') {
        return false;
    }
    for (i = 0U; label[i] != '\0' && i + 1U < sizeof(safe_label); ++i) {
        unsigned char ch = (unsigned char)label[i];
        if (isalnum(ch)) {
            safe_label[i] = (char)ch;
        } else {
            safe_label[i] = '_';
        }
    }
    if (i == 0U) {
        safe_label[i++] = 'x';
    }
    safe_label[i] = '\0';
    snprintf(local_script, sizeof(local_script), "%s.remote.sh", log_path);
    script_text = (char *)v3_xmalloc(strlen(command) + 12U);
    snprintf(script_text, strlen(command) + 12U, "#!/bin/sh\n%s\n", command);
    if (!v3_ensure_parent_dir(local_script) || !v3_write_text_file(local_script, script_text)) {
        free(script_text);
        return false;
    }
    free(script_text);
    ssh_argv[0] = "ssh";
    ssh_argv[1] = "-T";
    ssh_argv[2] = "-o";
    ssh_argv[3] = "BatchMode=yes";
    ssh_argv[4] = "-o";
    ssh_argv[5] = "ConnectTimeout=20";
    ssh_argv[6] = "-o";
    ssh_argv[7] = "ServerAliveInterval=5";
    ssh_argv[8] = "-o";
    ssh_argv[9] = "ServerAliveCountMax=3";
    ssh_argv[10] = ssh_host;
    ssh_argv[11] = "sh";
    ssh_argv[12] = NULL;
    for (attempt = 0U; attempt < 3U; ++attempt) {
        ok = v3_run_command_logged_argv_with_input_file(12U,
                                                        ssh_argv,
                                                        local_script,
                                                        log_path,
                                                        label,
                                                        true);
        if (ok || attempt + 1U >= 3U || !v3_bft_remote_retryable_log(log_path)) {
            break;
        }
        fprintf(stdout, "[v3 gate] remote ssh retry %zu %s\n", attempt + 1U, label);
        usleep((useconds_t)((attempt + 1U) * 1000000U));
    }
    return ok;
}

static bool v3_bft_run_local_argv(const char **args,
                                  size_t arg_count,
                                  const char *log_path,
                                  const char *label) {
    return v3_run_command_logged_argv(arg_count, args, log_path, label);
}

static bool v3_bft_run_local_argv_quiet(const char **args,
                                        size_t arg_count,
                                        const char *log_path,
                                        const char *label) {
    return v3_run_command_logged_argv_quiet(arg_count, args, log_path, label);
}

static bool v3_bft_run_remote_argv(const char *ssh_host,
                                   const char **args,
                                   size_t arg_count,
                                   const char *log_path,
                                   const char *label) {
    const char *argv_remote[28];
    size_t i;
    bool ok = false;
    size_t attempt;
    if (arg_count + 12U >= sizeof(argv_remote) / sizeof(argv_remote[0])) {
        fprintf(stderr, "[cheng_v3_seed] %s argv overflow\n", label);
        return false;
    }
    argv_remote[0] = "ssh";
    argv_remote[1] = "-n";
    argv_remote[2] = "-o";
    argv_remote[3] = "BatchMode=yes";
    argv_remote[4] = "-o";
    argv_remote[5] = "ConnectTimeout=20";
    argv_remote[6] = "-o";
    argv_remote[7] = "ServerAliveInterval=5";
    argv_remote[8] = "-o";
    argv_remote[9] = "ServerAliveCountMax=3";
    argv_remote[10] = ssh_host;
    for (i = 0U; i < arg_count; ++i) {
        argv_remote[i + 11U] = args[i];
    }
    argv_remote[arg_count + 11U] = NULL;
    for (attempt = 0U; attempt < 3U; ++attempt) {
        ok = v3_run_command_logged_argv(arg_count + 11U, argv_remote, log_path, label);
        if (ok || attempt + 1U >= 3U || !v3_bft_remote_retryable_log(log_path)) {
            break;
        }
        fprintf(stdout, "[v3 gate] remote ssh retry %zu %s\n", attempt + 1U, label);
        usleep((useconds_t)((attempt + 1U) * 1000000U));
    }
    return ok;
}

static bool v3_bft_run_remote_argv_quiet(const char *ssh_host,
                                         const char **args,
                                         size_t arg_count,
                                         const char *log_path,
                                         const char *label) {
    const char *argv_remote[28];
    size_t i;
    bool ok = false;
    size_t attempt;
    if (arg_count + 12U >= sizeof(argv_remote) / sizeof(argv_remote[0])) {
        return false;
    }
    argv_remote[0] = "ssh";
    argv_remote[1] = "-n";
    argv_remote[2] = "-o";
    argv_remote[3] = "BatchMode=yes";
    argv_remote[4] = "-o";
    argv_remote[5] = "ConnectTimeout=20";
    argv_remote[6] = "-o";
    argv_remote[7] = "ServerAliveInterval=5";
    argv_remote[8] = "-o";
    argv_remote[9] = "ServerAliveCountMax=3";
    argv_remote[10] = ssh_host;
    for (i = 0U; i < arg_count; ++i) {
        argv_remote[i + 11U] = args[i];
    }
    argv_remote[arg_count + 11U] = NULL;
    for (attempt = 0U; attempt < 3U; ++attempt) {
        ok = v3_run_command_logged_argv_quiet(arg_count + 11U, argv_remote, log_path, label);
        if (ok || attempt + 1U >= 3U || !v3_bft_remote_retryable_log(log_path)) {
            break;
        }
        fprintf(stdout, "[v3 gate] remote ssh retry %zu %s\n", attempt + 1U, label);
        usleep((useconds_t)((attempt + 1U) * 1000000U));
    }
    return ok;
}

static bool v3_bft_rsync_repo(const char *root,
                              const char *ssh_host,
                              const char *remote_root,
                              const char *log_path) {
    const char *argv_rsync[20];
    char src[PATH_MAX];
    char dst[PATH_MAX + 256];
    char remote_shell[256];
    bool ok = false;
    size_t attempt;
    snprintf(src, sizeof(src), "%s/", root);
    snprintf(dst, sizeof(dst), "%s:%s/", ssh_host, remote_root);
    snprintf(remote_shell,
             sizeof(remote_shell),
             "ssh -o BatchMode=yes -o ConnectTimeout=20 -o ServerAliveInterval=5 -o ServerAliveCountMax=3");
    argv_rsync[0] = "rsync";
    argv_rsync[1] = "-az";
    argv_rsync[2] = "-e";
    argv_rsync[3] = remote_shell;
    argv_rsync[4] = "--delete";
    argv_rsync[5] = "--exclude";
    argv_rsync[6] = ".git";
    argv_rsync[7] = "--exclude";
    argv_rsync[8] = "artifacts";
    argv_rsync[9] = "--exclude";
    argv_rsync[10] = "chengcache";
    argv_rsync[11] = "--exclude";
    argv_rsync[12] = "node_modules";
    argv_rsync[13] = src;
    argv_rsync[14] = dst;
    argv_rsync[15] = NULL;
    for (attempt = 0U; attempt < 3U; ++attempt) {
        ok = v3_run_command_logged_argv(15U, argv_rsync, log_path, "bft_rsync");
        if (ok || attempt + 1U >= 3U || !v3_bft_remote_retryable_log(log_path)) {
            break;
        }
        fprintf(stdout, "[v3 gate] remote rsync retry %zu %s\n", attempt + 1U, ssh_host);
        usleep((useconds_t)((attempt + 1U) * 1000000U));
    }
    return ok;
}

static bool v3_bft_build_remote_x86_state_machine(const char *ssh_host,
                                                  const char *remote_root,
                                                  const char *log_path,
                                                  const char *label) {
    char remote_seed_source[PATH_MAX];
    char remote_seed_runner[PATH_MAX];
    char remote_stage3[PATH_MAX];
    char ready_log[PATH_MAX];
    char stage3_log[PATH_MAX];
    char *quoted_seed_source = NULL;
    char *quoted_seed_runner = NULL;
    char *quoted_remote_root = NULL;
    char *quoted_remote_stage3 = NULL;
    char *ready_command = NULL;
    char *stage3_fresh_command = NULL;
    char *incremental_inner = NULL;
    char *incremental_command = NULL;
    char *full_inner = NULL;
    char *command = NULL;
    size_t cap;
    bool ok;
    if (ssh_host == NULL || ssh_host[0] == '\0' ||
        remote_root == NULL || remote_root[0] == '\0' ||
        log_path == NULL || log_path[0] == '\0' ||
        label == NULL || label[0] == '\0') {
        return false;
    }
    v3_join_path(remote_seed_source, sizeof(remote_seed_source), remote_root, "v3/bootstrap/cheng_v3_seed.c");
    v3_join_path(remote_seed_runner, sizeof(remote_seed_runner), remote_root, "artifacts/v3_gate/cheng.remote_seed_runner");
    v3_join_path(remote_stage3, sizeof(remote_stage3), remote_root, "artifacts/v3_bootstrap/cheng.stage3");
    snprintf(ready_log, sizeof(ready_log), "%s.ready.log", log_path);
    snprintf(stage3_log, sizeof(stage3_log), "%s.stage3.log", log_path);
    quoted_seed_source = v3_shell_quote(remote_seed_source);
    quoted_seed_runner = v3_shell_quote(remote_seed_runner);
    quoted_remote_root = v3_shell_quote(remote_root);
    quoted_remote_stage3 = v3_shell_quote(remote_stage3);
    ready_command = v3_bft_remote_build_ready_shell(remote_root);
    ok = v3_bft_run_remote_shell_quiet(ssh_host, ready_command, ready_log, "bft_remote_ready");
    free(ready_command);
    if (ok) {
        fprintf(stdout, "[v3 gate] remote bft reuse %s\n", ssh_host);
        free(quoted_seed_source);
        free(quoted_seed_runner);
        free(quoted_remote_root);
        free(quoted_remote_stage3);
        return true;
    }
    stage3_fresh_command = v3_bft_remote_stage3_fresh_shell(remote_root);
    ok = v3_bft_run_remote_shell_quiet(ssh_host,
                                       stage3_fresh_command,
                                       stage3_log,
                                       "bft_remote_stage3_fresh");
    free(stage3_fresh_command);
    if (ok) {
        cap = strlen(quoted_remote_stage3) + 256U;
        incremental_inner = (char *)v3_xmalloc(cap);
        snprintf(incremental_inner,
                 cap,
                 "BFT_STATE_TARGET=x86_64-unknown-linux-gnu BFT_STATE_RUN_SELF_TEST=1 %s build-bft-state-machine",
                 quoted_remote_stage3);
        incremental_command = v3_bft_remote_locked_shell(remote_root, incremental_inner);
        free(incremental_inner);
        ok = v3_bft_run_remote_shell(ssh_host, incremental_command, log_path, label);
        free(incremental_command);
        if (ok) {
            fprintf(stdout, "[v3 gate] remote bft incremental %s\n", ssh_host);
        }
        free(quoted_seed_source);
        free(quoted_seed_runner);
        free(quoted_remote_root);
        free(quoted_remote_stage3);
        return ok;
    }
    cap = strlen(quoted_seed_source) +
          strlen(quoted_seed_runner) * 2U +
          strlen(quoted_remote_root) +
          strlen(quoted_remote_stage3) +
          512U;
    full_inner = (char *)v3_xmalloc(cap);
    snprintf(full_inner,
             cap,
             "mkdir -p artifacts/v3_bootstrap && cc -std=c11 -O2 -Wall -Wextra -pedantic %s -o %s && CHENG_V3_ROOT=%s CHENG_V3_TARGET=x86_64-unknown-linux-gnu %s bootstrap-bridge >/dev/null && BFT_STATE_TARGET=x86_64-unknown-linux-gnu BFT_STATE_RUN_SELF_TEST=1 %s build-bft-state-machine",
             quoted_seed_source,
             quoted_seed_runner,
             quoted_remote_root,
             quoted_seed_runner,
             quoted_remote_stage3);
    command = v3_bft_remote_locked_shell(remote_root, full_inner);
    free(full_inner);
    ok = v3_bft_run_remote_shell(ssh_host, command, log_path, label);
    free(command);
    free(quoted_seed_source);
    free(quoted_seed_runner);
    free(quoted_remote_root);
    free(quoted_remote_stage3);
    if (ok) {
        fprintf(stdout, "[v3 gate] remote bft rebuild %s\n", ssh_host);
    }
    return ok;
}

static bool v3_bft_copy_remote_file_via_local_scp(const char *src_ssh,
                                                  const char *src_path,
                                                  const char *dst_ssh,
                                                  const char *dst_path,
                                                  const char *log_path,
                                                  const char *label) {
    const char *argv_scp[7];
    char src[PATH_MAX + 256];
    char dst[PATH_MAX + 256];
    if (src_ssh == NULL || src_ssh[0] == '\0' ||
        src_path == NULL || src_path[0] == '\0' ||
        dst_ssh == NULL || dst_ssh[0] == '\0' ||
        dst_path == NULL || dst_path[0] == '\0' ||
        log_path == NULL || log_path[0] == '\0' ||
        label == NULL || label[0] == '\0') {
        return false;
    }
    snprintf(src, sizeof(src), "%s:%s", src_ssh, src_path);
    snprintf(dst, sizeof(dst), "%s:%s", dst_ssh, dst_path);
    argv_scp[0] = "scp";
    argv_scp[1] = "-3";
    argv_scp[2] = "-q";
    argv_scp[3] = "-B";
    argv_scp[4] = src;
    argv_scp[5] = dst;
    argv_scp[6] = NULL;
    return v3_run_command_logged_argv(6U, argv_scp, log_path, label);
}

static bool v3_bft_remote_allow_udp_port(const char *ssh_host,
                                         int port,
                                         const char *log_path,
                                         const char *label) {
    char command[4096];
    if (ssh_host == NULL || ssh_host[0] == '\0' ||
        port <= 0 ||
        log_path == NULL || log_path[0] == '\0' ||
        label == NULL || label[0] == '\0') {
        return false;
    }
    snprintf(command,
             sizeof(command),
             "set -eu && port=%d && if command -v ufw >/dev/null 2>&1 && ufw status 2>/dev/null | grep -q '^Status: active'; then ufw allow \"$port/udp\" >/dev/null 2>&1 || ufw allow \"$port/udp\"; exit 0; fi && if command -v iptables >/dev/null 2>&1; then if ! iptables -C INPUT -p udp --dport \"$port\" -j ACCEPT >/dev/null 2>&1; then iptables -I INPUT -p udp --dport \"$port\" -j ACCEPT; fi; exit 0; fi && if command -v nft >/dev/null 2>&1 && nft list table inet filter >/dev/null 2>&1; then if ! nft list ruleset 2>/dev/null | grep -q \"udp dport $port accept\"; then nft add rule inet filter input udp dport \"$port\" accept; fi; exit 0; fi && exit 0",
             port);
    return v3_bft_run_remote_shell(ssh_host, command, log_path, label);
}

static bool v3_bft_collect_local_status(const char *bin_path,
                                        const char *state_path,
                                        const V3BftDeployConfig *cfg,
                                        bool quiet_failures,
                                        const char *summary_log,
                                        char *height_out,
                                        size_t height_cap,
                                        char *hash_out,
                                        size_t hash_cap,
                                        char *alice_out,
                                        size_t alice_cap,
                                        char *bob_out,
                                        size_t bob_cap) {
    const char *summary_argv[7];
    char asset_arg[64];
    char account_a_arg[64];
    char account_b_arg[64];
    char state_arg[PATH_MAX + 32];
    char *summary_text;
    snprintf(state_arg, sizeof(state_arg), "--state:%s", state_path);
    snprintf(asset_arg, sizeof(asset_arg), "--asset:%d", cfg->asset_id);
    snprintf(account_a_arg, sizeof(account_a_arg), "--account-a:%d", cfg->alice_id);
    snprintf(account_b_arg, sizeof(account_b_arg), "--account-b:%d", cfg->bob_id);
    summary_argv[0] = bin_path;
    summary_argv[1] = "query-summary";
    summary_argv[2] = state_arg;
    summary_argv[3] = asset_arg;
    summary_argv[4] = account_a_arg;
    summary_argv[5] = account_b_arg;
    summary_argv[6] = NULL;
    if ((!quiet_failures
             ? !v3_bft_run_local_argv(summary_argv, 6U, summary_log, "bft_local_summary")
             : !v3_bft_run_local_argv_quiet(summary_argv, 6U, summary_log, "bft_local_summary"))) {
        return false;
    }
    summary_text = v3_read_file(summary_log);
    if (summary_text == NULL) {
        free(summary_text);
        return false;
    }
    v3_text_line_value(summary_text, "height", height_out, height_cap);
    v3_text_line_value(summary_text, "app_hash", hash_out, hash_cap);
    v3_text_line_value(summary_text, "balance_a", alice_out, alice_cap);
    v3_text_line_value(summary_text, "balance_b", bob_out, bob_cap);
    free(summary_text);
    return height_out[0] != '\0' &&
           hash_out[0] != '\0' &&
           alice_out[0] != '\0' &&
           bob_out[0] != '\0';
}

static bool v3_bft_collect_remote_status(const char *ssh_host,
                                         const char *bin_path,
                                         const char *state_path,
                                         const V3BftDeployConfig *cfg,
                                         bool quiet_failures,
                                         const char *summary_log,
                                         char *height_out,
                                         size_t height_cap,
                                         char *hash_out,
                                         size_t hash_cap,
                                         char *alice_out,
                                         size_t alice_cap,
                                         char *bob_out,
                                         size_t bob_cap) {
    const char *summary_args[7];
    char asset_arg[64];
    char account_a_arg[64];
    char account_b_arg[64];
    char state_arg[PATH_MAX + 32];
    char *summary_text;
    bool have_cached = height_out != NULL && height_cap > 0U &&
                       hash_out != NULL && hash_cap > 0U &&
                       alice_out != NULL && alice_cap > 0U &&
                       bob_out != NULL && bob_cap > 0U &&
                       height_out[0] != '\0' &&
                       hash_out[0] != '\0' &&
                       alice_out[0] != '\0' &&
                       bob_out[0] != '\0';
    snprintf(state_arg, sizeof(state_arg), "--state:%s", state_path);
    snprintf(asset_arg, sizeof(asset_arg), "--asset:%d", cfg->asset_id);
    snprintf(account_a_arg, sizeof(account_a_arg), "--account-a:%d", cfg->alice_id);
    snprintf(account_b_arg, sizeof(account_b_arg), "--account-b:%d", cfg->bob_id);
    summary_args[0] = bin_path;
    summary_args[1] = "query-summary";
    summary_args[2] = state_arg;
    summary_args[3] = asset_arg;
    summary_args[4] = account_a_arg;
    summary_args[5] = account_b_arg;
    summary_args[6] = NULL;
    if ((!quiet_failures
             ? !v3_bft_run_remote_argv(ssh_host, summary_args, 6U, summary_log, "bft_remote_summary")
             : !v3_bft_run_remote_argv_quiet(ssh_host, summary_args, 6U, summary_log, "bft_remote_summary"))) {
        if (quiet_failures && have_cached && v3_bft_remote_retryable_log(summary_log)) {
            return true;
        }
        return false;
    }
    summary_text = v3_read_file(summary_log);
    if (summary_text == NULL) {
        free(summary_text);
        return false;
    }
    v3_text_line_value(summary_text, "height", height_out, height_cap);
    v3_text_line_value(summary_text, "app_hash", hash_out, hash_cap);
    v3_text_line_value(summary_text, "balance_a", alice_out, alice_cap);
    v3_text_line_value(summary_text, "balance_b", bob_out, bob_cap);
    free(summary_text);
    return height_out[0] != '\0' &&
           hash_out[0] != '\0' &&
           alice_out[0] != '\0' &&
           bob_out[0] != '\0';
}

static void v3_bft_print_status_line(const char *name,
                                     const char *height,
                                     const char *hash,
                                     const char *alice,
                                     const char *bob) {
    printf("node=%s\n", name);
    printf("height=%s\n", height);
    printf("app_hash=%s\n", hash);
    printf("alice_balance=%s\n", alice);
    printf("bob_balance=%s\n", bob);
}

static bool v3_bft_cluster_matches_values(const char *want_height,
                                          const char *want_alice,
                                          const char *want_bob,
                                          const char *server_height,
                                          const char *server_hash,
                                          const char *server_alice,
                                          const char *server_bob,
                                          const char *relay_height,
                                          const char *relay_hash,
                                          const char *relay_alice,
                                          const char *relay_bob,
                                          const char *local_height,
                                          const char *local_hash,
                                          const char *local_alice,
                                          const char *local_bob) {
    return want_height != NULL &&
           want_alice != NULL &&
           want_bob != NULL &&
           server_height != NULL &&
           server_hash != NULL &&
           server_alice != NULL &&
           server_bob != NULL &&
           relay_height != NULL &&
           relay_hash != NULL &&
           relay_alice != NULL &&
           relay_bob != NULL &&
           local_height != NULL &&
           local_hash != NULL &&
           local_alice != NULL &&
           local_bob != NULL &&
           strcmp(server_height, want_height) == 0 &&
           strcmp(relay_height, want_height) == 0 &&
           strcmp(local_height, want_height) == 0 &&
           server_hash[0] != '\0' &&
           strcmp(server_hash, relay_hash) == 0 &&
           strcmp(server_hash, local_hash) == 0 &&
           strcmp(server_alice, want_alice) == 0 &&
           strcmp(relay_alice, want_alice) == 0 &&
           strcmp(local_alice, want_alice) == 0 &&
           strcmp(server_bob, want_bob) == 0 &&
           strcmp(relay_bob, want_bob) == 0 &&
           strcmp(local_bob, want_bob) == 0;
}

static void v3_bft_expected_baseline(const V3BftDeployConfig *cfg,
                                     char *height_out,
                                     size_t height_cap,
                                     char *alice_out,
                                     size_t alice_cap,
                                     char *bob_out,
                                     size_t bob_cap);

static bool v3_bft_write_verify_report(const char *report_path,
                                       const V3BftDeployConfig *cfg,
                                       bool verified,
                                       const char *server_height,
                                       const char *server_hash,
                                       const char *server_alice,
                                       const char *server_bob,
                                       const char *relay_height,
                                       const char *relay_hash,
                                       const char *relay_alice,
                                       const char *relay_bob,
                                       const char *local_height,
                                       const char *local_hash,
                                       const char *local_alice,
                                       const char *local_bob) {
    char want_height[64];
    char want_alice[64];
    char want_bob[64];
    char text[4096];
    v3_bft_expected_baseline(cfg,
                             want_height,
                             sizeof(want_height),
                             want_alice,
                             sizeof(want_alice),
                             want_bob,
                             sizeof(want_bob));
    text[0] = '\0';
    v3_text_appendf(text, sizeof(text), "version=v3.bft_three_node_verify.v1\n");
    v3_text_appendf(text, sizeof(text), "app_id=%s\n", cfg != NULL ? cfg->app_id : "");
    v3_text_appendf(text, sizeof(text), "server_ssh=%s\n", cfg != NULL ? cfg->server_ssh : "");
    v3_text_appendf(text, sizeof(text), "relay_ssh=%s\n", cfg != NULL ? cfg->relay_ssh : "");
    v3_text_appendf(text, sizeof(text), "want_height=%s\n", want_height);
    v3_text_appendf(text, sizeof(text), "want_alice_balance=%s\n", want_alice);
    v3_text_appendf(text, sizeof(text), "want_bob_balance=%s\n", want_bob);
    v3_text_appendf(text, sizeof(text), "server_name=server\nserver_height=%s\nserver_app_hash=%s\nserver_alice_balance=%s\nserver_bob_balance=%s\n",
                    server_height != NULL ? server_height : "",
                    server_hash != NULL ? server_hash : "",
                    server_alice != NULL ? server_alice : "",
                    server_bob != NULL ? server_bob : "");
    v3_text_appendf(text, sizeof(text), "relay_name=relay\nrelay_height=%s\nrelay_app_hash=%s\nrelay_alice_balance=%s\nrelay_bob_balance=%s\n",
                    relay_height != NULL ? relay_height : "",
                    relay_hash != NULL ? relay_hash : "",
                    relay_alice != NULL ? relay_alice : "",
                    relay_bob != NULL ? relay_bob : "");
    v3_text_appendf(text, sizeof(text), "local_name=local\nlocal_height=%s\nlocal_app_hash=%s\nlocal_alice_balance=%s\nlocal_bob_balance=%s\n",
                    local_height != NULL ? local_height : "",
                    local_hash != NULL ? local_hash : "",
                    local_alice != NULL ? local_alice : "",
                    local_bob != NULL ? local_bob : "");
    v3_text_appendf(text, sizeof(text), "verified=%s\n", verified ? "true" : "false");
    return v3_write_text_file(report_path, text);
}

static long long v3_bft_monotime_ms(void) {
    struct timespec ts;
    if (clock_gettime(CLOCK_MONOTONIC, &ts) != 0) {
        return 0LL;
    }
    return (long long)ts.tv_sec * 1000LL + (long long)(ts.tv_nsec / 1000000L);
}

static long long CHENG_V3_UNUSED v3_bft_walltime_ms(void) {
    struct timespec ts;
    if (clock_gettime(CLOCK_REALTIME, &ts) != 0) {
        return 0LL;
    }
    return (long long)ts.tv_sec * 1000LL + (long long)(ts.tv_nsec / 1000000L);
}

static bool CHENG_V3_UNUSED v3_bft_remote_state_mtime_ms(const char *ssh_host,
                                                         const char *state_path,
                                                         const char *log_path,
                                                         const char *label,
                                                         long long *out_ms) {
    char command[PATH_MAX + 256];
    char value_buf[64];
    char *text;
    if (ssh_host == NULL || ssh_host[0] == '\0' ||
        state_path == NULL || state_path[0] == '\0' ||
        log_path == NULL || log_path[0] == '\0' ||
        out_ms == NULL) {
        return false;
    }
    snprintf(command,
             sizeof(command),
             "set -eu && test -f %s && printf 'mtime_ms=%%s000\\n' \"$(stat -c %%Y %s)\"",
             state_path,
             state_path);
    if (!v3_bft_run_remote_shell(ssh_host, command, log_path, label)) {
        return false;
    }
    text = v3_read_file(log_path);
    if (text == NULL) {
        return false;
    }
    if (!v3_text_line_value(text, "mtime_ms", value_buf, sizeof(value_buf))) {
        free(text);
        return false;
    }
    *out_ms = atoll(value_buf);
    free(text);
    return *out_ms > 0LL;
}

static void v3_bft_format_milli(long long value_milli,
                                char *out,
                                size_t cap) {
    long long whole = value_milli / 1000LL;
    long long frac = value_milli % 1000LL;
    if (frac < 0) {
        frac = -frac;
    }
    snprintf(out, cap, "%lld.%03lld", whole, frac);
}

static bool v3_bft_wait_cluster_state(const V3BftDeployConfig *cfg,
                                      const char *server_bin,
                                      const char *relay_bin,
                                      const char *local_bin,
                                      const char *server_state,
                                      const char *relay_state,
                                      const char *local_state,
                                      const char *log_prefix,
                                      const char *want_height,
                                      const char *want_alice,
                                      const char *want_bob,
                                      char *server_height,
                                      size_t server_height_cap,
                                      char *server_hash,
                                      size_t server_hash_cap,
                                      char *server_alice,
                                      size_t server_alice_cap,
                                      char *server_bob,
                                      size_t server_bob_cap,
                                      char *relay_height,
                                      size_t relay_height_cap,
                                      char *relay_hash,
                                      size_t relay_hash_cap,
                                      char *relay_alice,
                                      size_t relay_alice_cap,
                                      char *relay_bob,
                                      size_t relay_bob_cap,
                                      char *local_height,
                                      size_t local_height_cap,
                                      char *local_hash,
                                      size_t local_hash_cap,
                                      char *local_alice,
                                      size_t local_alice_cap,
                                      char *local_bob,
                                      size_t local_bob_cap) {
    size_t attempt;
    char server_summary_log[PATH_MAX];
    char relay_summary_log[PATH_MAX];
    char local_summary_log[PATH_MAX];
    if (server_height != NULL && server_height_cap > 0U) {
        server_height[0] = '\0';
    }
    if (server_hash != NULL && server_hash_cap > 0U) {
        server_hash[0] = '\0';
    }
    if (server_alice != NULL && server_alice_cap > 0U) {
        server_alice[0] = '\0';
    }
    if (server_bob != NULL && server_bob_cap > 0U) {
        server_bob[0] = '\0';
    }
    if (relay_height != NULL && relay_height_cap > 0U) {
        relay_height[0] = '\0';
    }
    if (relay_hash != NULL && relay_hash_cap > 0U) {
        relay_hash[0] = '\0';
    }
    if (relay_alice != NULL && relay_alice_cap > 0U) {
        relay_alice[0] = '\0';
    }
    if (relay_bob != NULL && relay_bob_cap > 0U) {
        relay_bob[0] = '\0';
    }
    if (local_height != NULL && local_height_cap > 0U) {
        local_height[0] = '\0';
    }
    if (local_hash != NULL && local_hash_cap > 0U) {
        local_hash[0] = '\0';
    }
    if (local_alice != NULL && local_alice_cap > 0U) {
        local_alice[0] = '\0';
    }
    if (local_bob != NULL && local_bob_cap > 0U) {
        local_bob[0] = '\0';
    }
    snprintf(server_summary_log, sizeof(server_summary_log), "%s.server.summary.log", log_prefix);
    snprintf(relay_summary_log, sizeof(relay_summary_log), "%s.relay.summary.log", log_prefix);
    snprintf(local_summary_log, sizeof(local_summary_log), "%s.local.summary.log", log_prefix);
    for (attempt = 0U; attempt < 300U; ++attempt) {
        if (v3_bft_collect_remote_status(cfg->server_ssh,
                                         server_bin,
                                         server_state,
                                         cfg,
                                         true,
                                         server_summary_log,
                                         server_height,
                                         server_height_cap,
                                         server_hash,
                                         server_hash_cap,
                                         server_alice,
                                         server_alice_cap,
                                         server_bob,
                                         server_bob_cap) &&
            v3_bft_collect_remote_status(cfg->relay_ssh,
                                         relay_bin,
                                         relay_state,
                                         cfg,
                                         true,
                                         relay_summary_log,
                                         relay_height,
                                         relay_height_cap,
                                         relay_hash,
                                         relay_hash_cap,
                                         relay_alice,
                                         relay_alice_cap,
                                         relay_bob,
                                         relay_bob_cap) &&
            v3_bft_collect_local_status(local_bin,
                                        local_state,
                                        cfg,
                                        true,
                                        local_summary_log,
                                        local_height,
                                        local_height_cap,
                                        local_hash,
                                        local_hash_cap,
                                        local_alice,
                                        local_alice_cap,
                                        local_bob,
                                        local_bob_cap) &&
            strcmp(server_height, want_height) == 0 &&
            strcmp(relay_height, want_height) == 0 &&
            strcmp(local_height, want_height) == 0 &&
            server_hash[0] != '\0' &&
            strcmp(server_hash, relay_hash) == 0 &&
            strcmp(server_hash, local_hash) == 0 &&
            strcmp(server_alice, want_alice) == 0 &&
            strcmp(relay_alice, want_alice) == 0 &&
            strcmp(local_alice, want_alice) == 0 &&
            strcmp(server_bob, want_bob) == 0 &&
            strcmp(relay_bob, want_bob) == 0 &&
            strcmp(local_bob, want_bob) == 0) {
            return true;
        }
        usleep(200000);
    }
    return false;
}

static void v3_bft_expected_baseline(const V3BftDeployConfig *cfg,
                                     char *height_out,
                                     size_t height_cap,
                                     char *alice_out,
                                     size_t alice_cap,
                                     char *bob_out,
                                     size_t bob_cap) {
    long long want_alice = 0LL;
    long long want_bob = 0LL;
    if (height_out != NULL && height_cap > 0U) {
        snprintf(height_out, height_cap, "1");
    }
    if (cfg != NULL) {
        want_alice = cfg->mint_amount - cfg->transfer_amount;
        want_bob = cfg->transfer_amount;
    }
    if (alice_out != NULL && alice_cap > 0U) {
        snprintf(alice_out, alice_cap, "%lld", want_alice);
    }
    if (bob_out != NULL && bob_cap > 0U) {
        snprintf(bob_out, bob_cap, "%lld", want_bob);
    }
}

static bool v3_bft_wait_cluster_baseline(const V3BftDeployConfig *cfg,
                                         const char *server_bin,
                                         const char *relay_bin,
                                         const char *local_bin,
                                         const char *server_state,
                                         const char *relay_state,
                                         const char *local_state,
                                         const char *log_prefix,
                                         char *server_height,
                                         size_t server_height_cap,
                                         char *server_hash,
                                         size_t server_hash_cap,
                                         char *server_alice,
                                         size_t server_alice_cap,
                                         char *server_bob,
                                         size_t server_bob_cap,
                                         char *relay_height,
                                         size_t relay_height_cap,
                                         char *relay_hash,
                                         size_t relay_hash_cap,
                                         char *relay_alice,
                                         size_t relay_alice_cap,
                                         char *relay_bob,
                                         size_t relay_bob_cap,
                                         char *local_height,
                                         size_t local_height_cap,
                                         char *local_hash,
                                         size_t local_hash_cap,
                                         char *local_alice,
                                         size_t local_alice_cap,
                                         char *local_bob,
                                         size_t local_bob_cap) {
    char want_height[64];
    char want_alice[64];
    char want_bob[64];
    v3_bft_expected_baseline(cfg,
                             want_height,
                             sizeof(want_height),
                             want_alice,
                             sizeof(want_alice),
                             want_bob,
                             sizeof(want_bob));
    return v3_bft_wait_cluster_state(cfg,
                                     server_bin,
                                     relay_bin,
                                     local_bin,
                                     server_state,
                                     relay_state,
                                     local_state,
                                     log_prefix,
                                     want_height,
                                     want_alice,
                                     want_bob,
                                     server_height,
                                     server_height_cap,
                                     server_hash,
                                     server_hash_cap,
                                     server_alice,
                                     server_alice_cap,
                                     server_bob,
                                     server_bob_cap,
                                     relay_height,
                                     relay_height_cap,
                                     relay_hash,
                                     relay_hash_cap,
                                     relay_alice,
                                     relay_alice_cap,
                                     relay_bob,
                                     relay_bob_cap,
                                     local_height,
                                     local_height_cap,
                                     local_hash,
                                     local_hash_cap,
                                     local_alice,
                                     local_alice_cap,
                                     local_bob,
                                     local_bob_cap);
}

static int v3_cmd_stop_bft_validator_three_node_impl(int argc, char **argv) {
    V3BootstrapPaths paths;
    V3BftDeployConfig cfg;
    char server_dir[PATH_MAX];
    char relay_dir[PATH_MAX];
    char local_follower_pid[PATH_MAX];
    char server_log[PATH_MAX];
    char relay_log[PATH_MAX];
    char local_log[PATH_MAX];
    char command[4096];
    (void)argc;
    (void)argv;
    v3_bootstrap_paths_init(&paths);
    v3_bft_config_init(&paths, &cfg);
    v3_bft_remote_deploy_dir(cfg.server_root, server_dir, sizeof(server_dir));
    v3_bft_remote_deploy_dir(cfg.relay_root, relay_dir, sizeof(relay_dir));
    v3_join_path(local_follower_pid, sizeof(local_follower_pid), cfg.local_deploy_dir, "local_follower.pid");
    v3_join_path(server_log, sizeof(server_log), paths.root, "artifacts/v3_gate/bft_stop_server.log");
    v3_join_path(relay_log, sizeof(relay_log), paths.root, "artifacts/v3_gate/bft_stop_relay.log");
    v3_join_path(local_log, sizeof(local_log), paths.root, "artifacts/v3_gate/bft_stop_local.log");
    snprintf(command,
             sizeof(command),
             "set -u ; for pid in %s/server_proposal.pid %s/server_commit.pid %s/server_vote.pid %s/server_state.pid %s/server_proposer.pid; do if [ -f \"$pid\" ]; then p=$(cat \"$pid\"); if [ -n \"$p\" ]; then kill \"$p\" >/dev/null 2>&1 || true; sleep 1; kill -9 \"$p\" >/dev/null 2>&1 || true; fi; rm -f \"$pid\"; fi; done ; pkill -f -- \"[p]roposal-serve-daemon --file:%s/server.proposal.hex\" >/dev/null 2>&1 || true ; pkill -f -- \"[c]ommit-serve-daemon --file:%s/server.commit.hex\" >/dev/null 2>&1 || true ; pkill -f -- \"[s]tate-serve-daemon --file:%s/server.machine.hex\" >/dev/null 2>&1 || true ; pkill -f -- \"[v]ote-capture-daemon --file:%s/server.votes\" >/dev/null 2>&1 || true ; pkill -f -- \"[v]alidator-proposer-daemon --state:%s/server.state\" >/dev/null 2>&1 || true",
             server_dir,
             server_dir,
             server_dir,
             server_dir,
             server_dir,
             server_dir,
             server_dir,
             server_dir,
             server_dir,
             server_dir);
    if (!v3_bft_run_remote_shell(cfg.server_ssh, command, server_log, "bft_stop_server")) {
        return 1;
    }
    snprintf(command,
             sizeof(command),
             "set -u ; pid=%s/relay_follower.pid; if [ -f \"$pid\" ]; then p=$(cat \"$pid\"); if [ -n \"$p\" ]; then kill \"$p\" >/dev/null 2>&1 || true; sleep 1; kill -9 \"$p\" >/dev/null 2>&1 || true; fi; rm -f \"$pid\"; fi ; pkill -f -- \"[v]alidator-follower-daemon --state:%s/relay.state\" >/dev/null 2>&1 || true",
             relay_dir,
             relay_dir);
    if (!v3_bft_run_remote_shell(cfg.relay_ssh, command, relay_log, "bft_stop_relay")) {
        return 1;
    }
    snprintf(command,
             sizeof(command),
             "set -u ; pid=%s; if [ -f \"$pid\" ]; then p=$(cat \"$pid\"); if [ -n \"$p\" ]; then kill \"$p\" >/dev/null 2>&1 || true; sleep 1; kill -9 \"$p\" >/dev/null 2>&1 || true; fi; rm -f \"$pid\"; fi ; pkill -f -- \"[v]alidator-follower-daemon --state:%s/local.state\" >/dev/null 2>&1 || true",
             local_follower_pid,
             cfg.local_deploy_dir);
    if (!v3_bft_run_local_shell(command, local_log, "bft_stop_local")) {
        return 1;
    }
    puts("v3 bft deploy: stopped");
    return 0;
}

static void v3_bft_bench_report_path(const char *root,
                                     char *out,
                                     size_t cap) {
    if (root == NULL || root[0] == '\0' || out == NULL || cap == 0U) {
        return;
    }
    v3_join_path(out, cap, root, "artifacts/v3_gate/bench_bft_validator_three_node.report.txt");
}

static bool v3_bft_bench_restart_target_valid(const char *target) {
    return target != NULL &&
           (strcmp(target, "none") == 0 ||
            strcmp(target, "server") == 0 ||
            strcmp(target, "relay") == 0 ||
            strcmp(target, "local") == 0);
}

static bool v3_bft_bench_start_server(const char *root,
                                      const V3BftDeployConfig *cfg,
                                      const char *server_dir,
                                      const char *server_bin,
                                      const char *local_bin,
                                      int max_txs_per_block,
                                      const char *log_rel_prefix) {
    char rel_log[PATH_MAX];
    char log_path[PATH_MAX];
    char command[16384];
    char probe_cmd[PATH_MAX + 256];
    if (root == NULL || root[0] == '\0' ||
        cfg == NULL ||
        server_dir == NULL || server_dir[0] == '\0' ||
        server_bin == NULL || server_bin[0] == '\0' ||
        local_bin == NULL || local_bin[0] == '\0' ||
        log_rel_prefix == NULL || log_rel_prefix[0] == '\0' ||
        max_txs_per_block <= 0) {
        return false;
    }
    snprintf(rel_log, sizeof(rel_log), "%s.server_start.log", log_rel_prefix);
    v3_join_path(log_path, sizeof(log_path), root, rel_log);
    command[0] = '\0';
    v3_text_appendf(command,
                    sizeof(command),
                    "set -eu && rm -f %s/server.proposal.ready %s/server.commit.ready %s/server.vote.ready %s/server.state.ready %s/server_proposer.pid %s/server_state.pid",
                    server_dir,
                    server_dir,
                    server_dir,
                    server_dir,
                    server_dir,
                    server_dir);
    v3_text_appendf(command,
                    sizeof(command),
                    " && setsid -f sh -lc 'echo $$ > %s/server_proposer.pid && exec %s validator-proposer-daemon --state:%s/server.state --queue:%s/server.queue --proposal-host:0.0.0.0 --proposal-port:%d --vote-host:0.0.0.0 --vote-port:%d --commit-host:0.0.0.0 --commit-port:%d --proposal-file:%s/server.proposal.hex --commit-file:%s/server.commit.hex --state-sync-file:%s/server.machine.hex --proposal-ready-path:%s/server.proposal.ready --vote-ready-path:%s/server.vote.ready --commit-ready-path:%s/server.commit.ready --interval-ms:%d --proposal-window-ms:%d --vote-window-ms:%d --commit-window-ms:%d --max-txs:%d > %s/server.proposer.log 2>&1 < /dev/null'",
                    server_dir,
                    server_bin,
                    server_dir,
                    server_dir,
                    cfg->proposal_port,
                    cfg->vote_port,
                    cfg->commit_port,
                    server_dir,
                    server_dir,
                    server_dir,
                    server_dir,
                    server_dir,
                    server_dir,
                    cfg->proposer_interval_ms,
                    cfg->proposal_window_ms,
                    cfg->vote_window_ms,
                    cfg->commit_window_ms,
                    max_txs_per_block,
                    server_dir);
    v3_text_appendf(command,
                    sizeof(command),
                    " && setsid -f sh -lc 'echo $$ > %s/server_state.pid && exec %s state-serve-daemon --file:%s/server.machine.hex --host:0.0.0.0 --port:%d --interval-ms:%d > %s/server.state.log 2>&1 < /dev/null'",
                    server_dir,
                    server_bin,
                    server_dir,
                    cfg->state_port,
                    cfg->follower_interval_ms,
                    server_dir);
    v3_text_appendf(command,
                    sizeof(command),
                    " && i=0 && while { [ ! -f %s/server_proposer.pid ] || [ ! -f %s/server_state.pid ]; } && [ \"$i\" -lt 50 ]; do i=$((i + 1)); sleep 0.1; done && test -f %s/server_proposer.pid && test -f %s/server_state.pid",
                    server_dir,
                    server_dir,
                    server_dir,
                    server_dir);
    if (!v3_bft_run_remote_shell(cfg->server_ssh, command, log_path, "bft_bench_start_server")) {
        return false;
    }
    snprintf(rel_log, sizeof(rel_log), "%s.server_probe.log", log_rel_prefix);
    v3_join_path(log_path, sizeof(log_path), root, rel_log);
    snprintf(probe_cmd,
             sizeof(probe_cmd),
             "set -eu && %s probe-proposal --host:%s --port:%d --attempts:3 --interval-ms:100 --handshake-only:1",
             local_bin,
             cfg->server_addr,
             cfg->proposal_port);
    return v3_bft_run_local_shell(probe_cmd, log_path, "bft_bench_start_server_probe");
}

static bool v3_bft_bench_start_relay(const char *root,
                                     const V3BftDeployConfig *cfg,
                                     const char *relay_dir,
                                     const char *relay_bin,
                                     const char *log_rel_prefix) {
    char rel_log[PATH_MAX];
    char log_path[PATH_MAX];
    char command[8192];
    if (root == NULL || root[0] == '\0' ||
        cfg == NULL ||
        relay_dir == NULL || relay_dir[0] == '\0' ||
        relay_bin == NULL || relay_bin[0] == '\0' ||
        log_rel_prefix == NULL || log_rel_prefix[0] == '\0') {
        return false;
    }
    snprintf(rel_log, sizeof(rel_log), "%s.relay_start.log", log_rel_prefix);
    v3_join_path(log_path, sizeof(log_path), root, rel_log);
    snprintf(command,
             sizeof(command),
             "set -eu && rm -f %s/relay_follower.pid && setsid -f sh -lc 'echo $$ > %s/relay_follower.pid && exec %s validator-follower-daemon --state:%s/relay.state --proposal-host:%s --proposal-port:%d --vote-host:%s --vote-port:%d --commit-host:%s --commit-port:%d --sync-host:%s --sync-port:%d --interval-ms:%d > %s/relay.follower.log 2>&1 < /dev/null' && i=0 && while [ ! -f %s/relay_follower.pid ] && [ \"$i\" -lt 50 ]; do i=$((i + 1)); sleep 0.1; done && test -f %s/relay_follower.pid",
             relay_dir,
             relay_dir,
             relay_bin,
             relay_dir,
             cfg->server_addr,
             cfg->proposal_port,
             cfg->server_addr,
             cfg->vote_port,
             cfg->server_addr,
             cfg->commit_port,
             cfg->server_addr,
             cfg->state_port,
             cfg->follower_interval_ms,
             relay_dir,
             relay_dir,
             relay_dir);
    return v3_bft_run_remote_shell(cfg->relay_ssh, command, log_path, "bft_bench_start_relay");
}

static bool v3_bft_bench_start_local(const char *root,
                                     const V3BftDeployConfig *cfg,
                                     const char *local_dir,
                                     const char *local_bin,
                                     const char *log_rel_prefix) {
    char rel_log[PATH_MAX];
    char log_path[PATH_MAX];
    char command[8192];
    if (root == NULL || root[0] == '\0' ||
        cfg == NULL ||
        local_dir == NULL || local_dir[0] == '\0' ||
        local_bin == NULL || local_bin[0] == '\0' ||
        log_rel_prefix == NULL || log_rel_prefix[0] == '\0') {
        return false;
    }
    snprintf(rel_log, sizeof(rel_log), "%s.local_start.log", log_rel_prefix);
    v3_join_path(log_path, sizeof(log_path), root, rel_log);
    snprintf(command,
             sizeof(command),
             "set -eu && rm -f %s/local_follower.pid && setsid -f sh -lc 'echo $$ > %s/local_follower.pid && exec %s validator-follower-daemon --state:%s/local.state --proposal-host:%s --proposal-port:%d --vote-host:%s --vote-port:%d --commit-host:%s --commit-port:%d --sync-host:%s --sync-port:%d --interval-ms:%d > %s/local.follower.log 2>&1 < /dev/null' && i=0 && while [ ! -f %s/local_follower.pid ] && [ \"$i\" -lt 50 ]; do i=$((i + 1)); sleep 0.1; done && test -f %s/local_follower.pid",
             local_dir,
             local_dir,
             local_bin,
             local_dir,
             cfg->server_addr,
             cfg->proposal_port,
             cfg->server_addr,
             cfg->vote_port,
             cfg->server_addr,
             cfg->commit_port,
             cfg->server_addr,
             cfg->state_port,
             cfg->follower_interval_ms,
             local_dir,
             local_dir,
             local_dir);
    return v3_bft_run_local_shell(command, log_path, "bft_bench_start_local");
}

static bool v3_bft_bench_restart_node(const char *root,
                                      const V3BftDeployConfig *cfg,
                                      const char *server_dir,
                                      const char *relay_dir,
                                      const char *local_dir,
                                      const char *server_bin,
                                      const char *relay_bin,
                                      const char *local_bin,
                                      const char *restart_node,
                                      int restart_pause_ms,
                                      int max_txs_per_block,
                                      const char *log_rel_prefix) {
    char rel_log[PATH_MAX];
    char log_path[PATH_MAX];
    char command[8192];
    if (!v3_bft_bench_restart_target_valid(restart_node)) {
        return false;
    }
    if (strcmp(restart_node, "none") == 0) {
        return true;
    }
    if (root == NULL || root[0] == '\0' ||
        cfg == NULL ||
        log_rel_prefix == NULL || log_rel_prefix[0] == '\0') {
        return false;
    }
    if (strcmp(restart_node, "server") == 0) {
        snprintf(rel_log, sizeof(rel_log), "%s.server_stop.log", log_rel_prefix);
        v3_join_path(log_path, sizeof(log_path), root, rel_log);
        snprintf(command,
                 sizeof(command),
                 "set -u ; for pid in %s/server_proposer.pid %s/server_state.pid; do if [ -f \"$pid\" ]; then p=$(cat \"$pid\"); if [ -n \"$p\" ]; then kill \"$p\" >/dev/null 2>&1 || true; sleep 1; kill -9 \"$p\" >/dev/null 2>&1 || true; fi; rm -f \"$pid\"; fi; done ; pkill -f -- \"[v]alidator-proposer-daemon --state:%s/server.state\" >/dev/null 2>&1 || true ; pkill -f -- \"[s]tate-serve-daemon --file:%s/server.machine.hex\" >/dev/null 2>&1 || true",
                 server_dir,
                 server_dir,
                 server_dir,
                 server_dir);
        if (!v3_bft_run_remote_shell(cfg->server_ssh, command, log_path, "bft_bench_restart_server_stop")) {
            return false;
        }
        if (restart_pause_ms > 0) {
            usleep((useconds_t)restart_pause_ms * 1000U);
        }
        return v3_bft_bench_start_server(root,
                                         cfg,
                                         server_dir,
                                         server_bin,
                                         local_bin,
                                         max_txs_per_block,
                                         log_rel_prefix);
    }
    if (strcmp(restart_node, "relay") == 0) {
        snprintf(rel_log, sizeof(rel_log), "%s.relay_stop.log", log_rel_prefix);
        v3_join_path(log_path, sizeof(log_path), root, rel_log);
        snprintf(command,
                 sizeof(command),
                 "set -u ; pid=%s/relay_follower.pid; if [ -f \"$pid\" ]; then p=$(cat \"$pid\"); if [ -n \"$p\" ]; then kill \"$p\" >/dev/null 2>&1 || true; sleep 1; kill -9 \"$p\" >/dev/null 2>&1 || true; fi; rm -f \"$pid\"; fi ; pkill -f -- \"[v]alidator-follower-daemon --state:%s/relay.state\" >/dev/null 2>&1 || true",
                 relay_dir,
                 relay_dir);
        if (!v3_bft_run_remote_shell(cfg->relay_ssh, command, log_path, "bft_bench_restart_relay_stop")) {
            return false;
        }
        if (restart_pause_ms > 0) {
            usleep((useconds_t)restart_pause_ms * 1000U);
        }
        return v3_bft_bench_start_relay(root, cfg, relay_dir, relay_bin, log_rel_prefix);
    }
    snprintf(rel_log, sizeof(rel_log), "%s.local_stop.log", log_rel_prefix);
    v3_join_path(log_path, sizeof(log_path), root, rel_log);
    snprintf(command,
             sizeof(command),
             "set -u ; pid=%s/local_follower.pid; if [ -f \"$pid\" ]; then p=$(cat \"$pid\"); if [ -n \"$p\" ]; then kill \"$p\" >/dev/null 2>&1 || true; sleep 1; kill -9 \"$p\" >/dev/null 2>&1 || true; fi; rm -f \"$pid\"; fi ; pkill -f -- \"[v]alidator-follower-daemon --state:%s/local.state\" >/dev/null 2>&1 || true",
             local_dir,
             local_dir);
    if (!v3_bft_run_local_shell(command, log_path, "bft_bench_restart_local_stop")) {
        return false;
    }
    if (restart_pause_ms > 0) {
        usleep((useconds_t)restart_pause_ms * 1000U);
    }
    return v3_bft_bench_start_local(root, cfg, local_dir, local_bin, log_rel_prefix);
}

static bool v3_bft_write_bench_report(const char *report_path,
                                      const V3BftDeployConfig *cfg,
                                      long long tx_count_per_wave,
                                      int wave_count,
                                      long long transfer_amount,
                                      int max_txs_per_block,
                                      const char *restart_node,
                                      int restart_wave,
                                      int restart_pause_ms,
                                      long long total_tx_count,
                                      long long tx_wire_bytes,
                                      long long committed_bytes,
                                      long long block_count,
                                      long long elapsed_ms,
                                      long long tps_milli,
                                      long long bytes_per_sec,
                                      long long kib_per_sec_milli,
                                      const long long *wave_elapsed_ms,
                                      const long long *wave_tps_milli,
                                      const long long *wave_block_counts,
                                      const bool *wave_restarted,
                                      const char *final_height,
                                      const char *final_hash,
                                      const char *final_alice,
                                      const char *final_bob) {
    char tps_text[64];
    char kib_text[64];
    char *text = NULL;
    size_t cap;
    int i;
    if (report_path == NULL || report_path[0] == '\0' ||
        cfg == NULL ||
        restart_node == NULL ||
        final_height == NULL ||
        final_hash == NULL ||
        final_alice == NULL ||
        final_bob == NULL ||
        wave_count < 0) {
        return false;
    }
    cap = 4096U + (size_t)(wave_count > 0 ? wave_count : 0) * 256U;
    text = (char *)calloc(cap, 1U);
    if (text == NULL) {
        return false;
    }
    v3_bft_format_milli(tps_milli, tps_text, sizeof(tps_text));
    v3_bft_format_milli(kib_per_sec_milli, kib_text, sizeof(kib_text));
    v3_text_appendf(text, cap, "version=v3.bft_three_node_bench.v2\n");
    v3_text_appendf(text, cap, "app_id=%s\n", cfg->app_id);
    v3_text_appendf(text, cap, "tx_count_per_wave=%lld\n", tx_count_per_wave);
    v3_text_appendf(text, cap, "wave_count=%d\n", wave_count);
    v3_text_appendf(text, cap, "transfer_amount=%lld\n", transfer_amount);
    v3_text_appendf(text, cap, "max_txs_per_block=%d\n", max_txs_per_block);
    v3_text_appendf(text, cap, "restart_node=%s\n", restart_node);
    v3_text_appendf(text, cap, "restart_wave=%d\n", restart_wave);
    v3_text_appendf(text, cap, "restart_pause_ms=%d\n", restart_pause_ms);
    v3_text_appendf(text, cap, "tx_count=%lld\n", total_tx_count);
    v3_text_appendf(text, cap, "tx_wire_bytes=%lld\n", tx_wire_bytes);
    v3_text_appendf(text, cap, "committed_bytes=%lld\n", committed_bytes);
    v3_text_appendf(text, cap, "block_count=%lld\n", block_count);
    v3_text_appendf(text, cap, "elapsed_ms=%lld\n", elapsed_ms);
    v3_text_appendf(text, cap, "tps=%s\n", tps_text);
    v3_text_appendf(text, cap, "throughput_bytes_per_sec=%lld\n", bytes_per_sec);
    v3_text_appendf(text, cap, "throughput_kib_per_sec=%s\n", kib_text);
    v3_text_appendf(text, cap, "final_height=%s\n", final_height);
    v3_text_appendf(text, cap, "app_hash=%s\n", final_hash);
    v3_text_appendf(text, cap, "alice_balance=%s\n", final_alice);
    v3_text_appendf(text, cap, "bob_balance=%s\n", final_bob);
    for (i = 0; i < wave_count; ++i) {
        char wave_tps_text[64];
        v3_bft_format_milli(wave_tps_milli[i], wave_tps_text, sizeof(wave_tps_text));
        v3_text_appendf(text, cap, "wave_%d_elapsed_ms=%lld\n", i + 1, wave_elapsed_ms[i]);
        v3_text_appendf(text, cap, "wave_%d_tps=%s\n", i + 1, wave_tps_text);
        v3_text_appendf(text, cap, "wave_%d_block_count=%lld\n", i + 1, wave_block_counts[i]);
        v3_text_appendf(text, cap, "wave_%d_restarted=%s\n", i + 1, wave_restarted[i] ? "true" : "false");
    }
    if (!v3_ensure_parent_dir(report_path) || !v3_write_text_file(report_path, text)) {
        free(text);
        return false;
    }
    free(text);
    return true;
}

static int v3_cmd_status_bft_validator_three_node_impl(int argc, char **argv) {
    V3BootstrapPaths paths;
    V3BftDeployConfig cfg;
    char server_dir[PATH_MAX];
    char relay_dir[PATH_MAX];
    char server_state[PATH_MAX];
    char relay_state[PATH_MAX];
    char local_state[PATH_MAX];
    char server_bin[PATH_MAX];
    char relay_bin[PATH_MAX];
    char local_bin[PATH_MAX];
    char server_height[128];
    char server_hash[128];
    char server_alice[128];
    char server_bob[128];
    char relay_height[128];
    char relay_hash[128];
    char relay_alice[128];
    char relay_bob[128];
    char local_height[128];
    char local_hash[128];
    char local_alice[128];
    char local_bob[128];
    (void)argc;
    (void)argv;
    v3_bootstrap_paths_init(&paths);
    v3_bft_config_init(&paths, &cfg);
    v3_bft_remote_deploy_dir(cfg.server_root, server_dir, sizeof(server_dir));
    v3_bft_remote_deploy_dir(cfg.relay_root, relay_dir, sizeof(relay_dir));
    v3_join_path(server_state, sizeof(server_state), server_dir, "server.state");
    v3_join_path(relay_state, sizeof(relay_state), relay_dir, "relay.state");
    v3_join_path(local_state, sizeof(local_state), cfg.local_deploy_dir, "local.state");
    v3_bft_remote_bin(cfg.server_root, server_bin, sizeof(server_bin));
    v3_bft_remote_bin(cfg.relay_root, relay_bin, sizeof(relay_bin));
    v3_bft_local_bin(&paths, local_bin, sizeof(local_bin));
    if (!v3_bft_collect_remote_status(cfg.server_ssh,
                                      server_bin,
                                      server_state,
                                      &cfg,
                                      false,
                                      "artifacts/v3_gate/bft_server_status.summary.log",
                                      server_height,
                                      sizeof(server_height),
                                      server_hash,
                                      sizeof(server_hash),
                                      server_alice,
                                      sizeof(server_alice),
                                      server_bob,
                                      sizeof(server_bob))) {
        fprintf(stderr, "[cheng_v3_seed] status-bft-validator-three-node server failed\n");
        return 1;
    }
    if (!v3_bft_collect_remote_status(cfg.relay_ssh,
                                      relay_bin,
                                      relay_state,
                                      &cfg,
                                      false,
                                      "artifacts/v3_gate/bft_relay_status.summary.log",
                                      relay_height,
                                      sizeof(relay_height),
                                      relay_hash,
                                      sizeof(relay_hash),
                                      relay_alice,
                                      sizeof(relay_alice),
                                      relay_bob,
                                      sizeof(relay_bob))) {
        fprintf(stderr, "[cheng_v3_seed] status-bft-validator-three-node relay failed\n");
        return 1;
    }
    if (!v3_bft_collect_local_status(local_bin,
                                     local_state,
                                     &cfg,
                                     false,
                                     "artifacts/v3_gate/bft_local_status.summary.log",
                                     local_height,
                                     sizeof(local_height),
                                     local_hash,
                                     sizeof(local_hash),
                                     local_alice,
                                     sizeof(local_alice),
                                     local_bob,
                                     sizeof(local_bob))) {
        fprintf(stderr, "[cheng_v3_seed] status-bft-validator-three-node local failed\n");
        return 1;
    }
    v3_bft_print_status_line("server", server_height, server_hash, server_alice, server_bob);
    puts("---");
    v3_bft_print_status_line("relay", relay_height, relay_hash, relay_alice, relay_bob);
    puts("---");
    v3_bft_print_status_line("local", local_height, local_hash, local_alice, local_bob);
    return 0;
}

static int v3_cmd_deploy_bft_validator_three_node_impl(int argc, char **argv) {
    V3BootstrapPaths paths;
    V3BftDeployConfig cfg;
    char server_dir[PATH_MAX];
    char relay_dir[PATH_MAX];
    char server_state[PATH_MAX];
    char relay_state[PATH_MAX];
    char local_state[PATH_MAX];
    char local_bin[PATH_MAX];
    char server_bin[PATH_MAX];
    char relay_bin[PATH_MAX];
    char log_path[PATH_MAX];
    char command[16384];
    char server_height[128];
    char server_hash[128];
    char server_alice[128];
    char server_bob[128];
    char relay_height[128];
    char relay_hash[128];
    char relay_alice[128];
    char relay_bob[128];
    char local_height[128];
    char local_hash[128];
    char local_alice[128];
    char local_bob[128];
    char local_follower_pid[PATH_MAX];
    char local_follower_log[PATH_MAX];
    char local_state_arg[PATH_MAX + 32];
    char local_proposal_host_arg[128];
    char local_proposal_port_arg[64];
    char local_vote_host_arg[128];
    char local_vote_port_arg[64];
    char local_commit_host_arg[128];
    char local_commit_port_arg[64];
    char local_sync_host_arg[128];
    char local_sync_port_arg[64];
    char local_interval_arg[64];
    char local_start_note[256];
    char saved_bft_self_test[32];
    pid_t local_pid = -1;
    bool had_saved_bft_self_test = false;
    char *local_follower_argv[13];
    (void)argc;
    (void)argv;
    v3_bootstrap_paths_init(&paths);
    v3_bft_config_init(&paths, &cfg);
    v3_bft_remote_deploy_dir(cfg.server_root, server_dir, sizeof(server_dir));
    v3_bft_remote_deploy_dir(cfg.relay_root, relay_dir, sizeof(relay_dir));
    v3_join_path(server_state, sizeof(server_state), server_dir, "server.state");
    v3_join_path(relay_state, sizeof(relay_state), relay_dir, "relay.state");
    v3_join_path(local_state, sizeof(local_state), cfg.local_deploy_dir, "local.state");
    v3_join_path(local_follower_pid, sizeof(local_follower_pid), cfg.local_deploy_dir, "local_follower.pid");
    v3_join_path(local_follower_log, sizeof(local_follower_log), cfg.local_deploy_dir, "local.follower.log");
    v3_bft_remote_bin(cfg.server_root, server_bin, sizeof(server_bin));
    v3_bft_remote_bin(cfg.relay_root, relay_bin, sizeof(relay_bin));
    v3_bft_local_bin(&paths, local_bin, sizeof(local_bin));
    if (!v3_bft_write_manifest(&cfg)) {
        fprintf(stderr, "[cheng_v3_seed] deploy-bft-validator-three-node failed to write manifest\n");
        return 1;
    }
    if (v3_cmd_stop_bft_validator_three_node_impl(argc, argv) != 0) {
        return 1;
    }
    v3_join_path(log_path, sizeof(log_path), paths.root, "artifacts/v3_gate/bft_sync_server.mkdir.log");
    snprintf(command, sizeof(command), "set -eu && mkdir -p %s", cfg.server_root);
    if (!v3_bft_run_remote_shell(cfg.server_ssh, command, log_path, "bft_sync_server_mkdir")) {
        return 1;
    }
    v3_join_path(log_path, sizeof(log_path), paths.root, "artifacts/v3_gate/bft_sync_server.log");
    if (!v3_bft_rsync_repo(paths.root, cfg.server_ssh, cfg.server_root, log_path)) {
        return 1;
    }
    v3_join_path(log_path, sizeof(log_path), paths.root, "artifacts/v3_gate/bft_sync_relay.mkdir.log");
    snprintf(command, sizeof(command), "set -eu && mkdir -p %s", cfg.relay_root);
    if (!v3_bft_run_remote_shell(cfg.relay_ssh, command, log_path, "bft_sync_relay_mkdir")) {
        return 1;
    }
    v3_join_path(log_path, sizeof(log_path), paths.root, "artifacts/v3_gate/bft_sync_relay.log");
    if (!v3_bft_rsync_repo(paths.root, cfg.relay_ssh, cfg.relay_root, log_path)) {
        return 1;
    }
    v3_save_env_value("BFT_STATE_RUN_SELF_TEST",
                      saved_bft_self_test,
                      sizeof(saved_bft_self_test),
                      &had_saved_bft_self_test);
    setenv("BFT_STATE_RUN_SELF_TEST", "0", 1);
    if (v3_cmd_build_bft_state_machine_impl(argc, argv) != 0) {
        v3_restore_env_value("BFT_STATE_RUN_SELF_TEST", saved_bft_self_test, had_saved_bft_self_test);
        return 1;
    }
    v3_restore_env_value("BFT_STATE_RUN_SELF_TEST", saved_bft_self_test, had_saved_bft_self_test);
    v3_join_path(log_path, sizeof(log_path), paths.root, "artifacts/v3_gate/bft_build_relay.log");
    if (!v3_bft_build_remote_x86_state_machine(cfg.relay_ssh,
                                               cfg.relay_root,
                                               log_path,
                                               "bft_build_relay")) {
        return 1;
    }
    v3_join_path(log_path, sizeof(log_path), paths.root, "artifacts/v3_gate/bft_prepare_server_bin_dir.log");
    snprintf(command,
             sizeof(command),
             "set -eu && mkdir -p %s/artifacts/v3_bft_state_machine/x86_64-unknown-linux-gnu",
             cfg.server_root);
    if (!v3_bft_run_remote_shell(cfg.server_ssh, command, log_path, "bft_prepare_server_bin_dir")) {
        return 1;
    }
    v3_join_path(log_path, sizeof(log_path), paths.root, "artifacts/v3_gate/bft_copy_server_bin.log");
    if (!v3_bft_copy_remote_file_via_local_scp(cfg.relay_ssh,
                                               relay_bin,
                                               cfg.server_ssh,
                                               server_bin,
                                               log_path,
                                               "bft_copy_server_bin")) {
        return 1;
    }
    v3_join_path(log_path, sizeof(log_path), paths.root, "artifacts/v3_gate/bft_open_server_proposal_udp.log");
    if (!v3_bft_remote_allow_udp_port(cfg.server_ssh,
                                      cfg.proposal_port,
                                      log_path,
                                      "bft_open_server_proposal_udp")) {
        return 1;
    }
    v3_join_path(log_path, sizeof(log_path), paths.root, "artifacts/v3_gate/bft_open_server_vote_udp.log");
    if (!v3_bft_remote_allow_udp_port(cfg.server_ssh,
                                      cfg.vote_port,
                                      log_path,
                                      "bft_open_server_vote_udp")) {
        return 1;
    }
    v3_join_path(log_path, sizeof(log_path), paths.root, "artifacts/v3_gate/bft_open_server_commit_udp.log");
    if (!v3_bft_remote_allow_udp_port(cfg.server_ssh,
                                      cfg.commit_port,
                                      log_path,
                                      "bft_open_server_commit_udp")) {
        return 1;
    }
    v3_join_path(log_path, sizeof(log_path), paths.root, "artifacts/v3_gate/bft_open_server_state_udp.log");
    if (!v3_bft_remote_allow_udp_port(cfg.server_ssh,
                                      cfg.state_port,
                                      log_path,
                                      "bft_open_server_state_udp")) {
        return 1;
    }
    v3_join_path(log_path, sizeof(log_path), paths.root, "artifacts/v3_gate/bft_init_server.log");
    snprintf(command,
             sizeof(command),
             "set -eu && mkdir -p %s && rm -f %s/server.proposal.ready %s/server.commit.ready %s/server.vote.ready %s/server.state.ready %s/server.state.vote_lock %s/server.queue.cursor %s/server.machine.hex %s/server_state.pid && rm -rf %s/server.machine.hex.snapshots && : > %s/server.queue && : > %s/server.proposal.hex && : > %s/server.commit.hex && : > %s/server.votes && %s init --state:%s/server.state --app-id:%s --validator-slot:1 --force",
             server_dir,
             server_dir,
             server_dir,
             server_dir,
             server_dir,
             server_dir,
             server_dir,
             server_dir,
             server_dir,
             server_dir,
             server_dir,
             server_dir,
             server_dir,
             server_dir,
             server_bin,
             server_dir,
             cfg.app_id);
    if (!v3_bft_run_remote_shell(cfg.server_ssh, command, log_path, "bft_init_server")) {
        return 1;
    }
    v3_join_path(log_path, sizeof(log_path), paths.root, "artifacts/v3_gate/bft_init_relay.log");
    snprintf(command,
             sizeof(command),
             "set -eu && mkdir -p %s && rm -f %s/relay.state.vote_lock && %s init --state:%s/relay.state --app-id:%s --validator-slot:2 --force",
             relay_dir,
             relay_dir,
             relay_bin,
             relay_dir,
             cfg.app_id);
    if (!v3_bft_run_remote_shell(cfg.relay_ssh, command, log_path, "bft_init_relay")) {
        return 1;
    }
    v3_join_path(log_path, sizeof(log_path), paths.root, "artifacts/v3_gate/bft_init_local.log");
    snprintf(command,
             sizeof(command),
             "set -eu && mkdir -p %s && rm -f %s.vote_lock && %s init --state:%s --app-id:%s --validator-slot:3 --force",
             cfg.local_deploy_dir,
             local_state,
             local_bin,
             local_state,
             cfg.app_id);
    if (!v3_bft_run_local_shell(command, log_path, "bft_init_local")) {
        return 1;
    }
    v3_join_path(log_path, sizeof(log_path), paths.root, "artifacts/v3_gate/bft_seed_queue.log");
    snprintf(command,
             sizeof(command),
             "set -eu && %s submit-mint --queue:%s/server.queue --asset:%d --account:%d --amount:%lld && %s submit-transfer --queue:%s/server.queue --asset:%d --from:%d --to:%d --amount:%lld",
             server_bin,
             server_dir,
             cfg.asset_id,
             cfg.alice_id,
             cfg.mint_amount,
             server_bin,
             server_dir,
             cfg.asset_id,
             cfg.alice_id,
             cfg.bob_id,
             cfg.transfer_amount);
    if (!v3_bft_run_remote_shell(cfg.server_ssh, command, log_path, "bft_seed_queue")) {
        return 1;
    }
    v3_join_path(log_path, sizeof(log_path), paths.root, "artifacts/v3_gate/bft_start_server_proposer.log");
    command[0] = '\0';
    v3_text_appendf(command,
                    sizeof(command),
                    "set -eu && rm -f %s/server.proposal.ready %s/server.commit.ready %s/server.vote.ready %s/server.state.ready %s/server_proposal.pid %s/server_commit.pid %s/server_vote.pid %s/server_proposer.pid %s/server_state.pid",
                    server_dir,
                    server_dir,
                    server_dir,
                    server_dir,
                    server_dir,
                    server_dir,
                    server_dir,
                    server_dir,
                    server_dir);
    v3_text_appendf(command,
                    sizeof(command),
                    " && setsid -f sh -lc 'echo $$ > %s/server_proposer.pid && exec %s validator-proposer-daemon --state:%s/server.state --queue:%s/server.queue --proposal-host:0.0.0.0 --proposal-port:%d --vote-host:0.0.0.0 --vote-port:%d --commit-host:0.0.0.0 --commit-port:%d --proposal-file:%s/server.proposal.hex --commit-file:%s/server.commit.hex --state-sync-file:%s/server.machine.hex --proposal-ready-path:%s/server.proposal.ready --vote-ready-path:%s/server.vote.ready --commit-ready-path:%s/server.commit.ready --interval-ms:%d --proposal-window-ms:%d --vote-window-ms:%d --commit-window-ms:%d --max-txs:%d > %s/server.proposer.log 2>&1 < /dev/null'",
                    server_dir,
                    server_bin,
                    server_dir,
                    server_dir,
                    cfg.proposal_port,
                    cfg.vote_port,
                    cfg.commit_port,
                    server_dir,
                    server_dir,
                    server_dir,
                    server_dir,
                    server_dir,
                    server_dir,
                    cfg.proposer_interval_ms,
                    cfg.proposal_window_ms,
                    cfg.vote_window_ms,
                    cfg.commit_window_ms,
                    (getenv("CHENG_V3_BFT_MAX_TXS") != NULL && getenv("CHENG_V3_BFT_MAX_TXS")[0] != '\0') ? atoi(getenv("CHENG_V3_BFT_MAX_TXS")) : cfg.default_max_txs,
                    server_dir);
    v3_text_appendf(command,
                    sizeof(command),
                    " && setsid -f sh -lc 'echo $$ > %s/server_state.pid && exec %s state-serve-daemon --file:%s/server.machine.hex --host:0.0.0.0 --port:%d --interval-ms:%d > %s/server.state.log 2>&1 < /dev/null'",
                    server_dir,
                    server_bin,
                    server_dir,
                    cfg.state_port,
                    cfg.follower_interval_ms,
                    server_dir);
    v3_text_appendf(command,
                    sizeof(command),
                    " && i=0 && while { [ ! -f %s/server_proposer.pid ] || [ ! -f %s/server_state.pid ]; } && [ \"$i\" -lt 50 ]; do i=$((i + 1)); sleep 0.1; done && test -f %s/server_proposer.pid && test -f %s/server_state.pid",
                    server_dir,
                    server_dir,
                    server_dir,
                    server_dir);
    if (!v3_bft_run_remote_shell(cfg.server_ssh, command, log_path, "bft_start_server_proposer")) {
        return 1;
    }
    v3_join_path(log_path, sizeof(log_path), paths.root, "artifacts/v3_gate/bft_wait_server_ready.log");
    snprintf(command,
             sizeof(command),
             "set -eu && i=0 && while { [ ! -f %s/server.proposal.ready ] || [ ! -f %s/server.vote.ready ]; } && [ \"$i\" -lt 200 ]; do i=$((i + 1)); sleep 0.05; done && test -f %s/server.proposal.ready && test -f %s/server.vote.ready",
             server_dir,
             server_dir,
             server_dir,
             server_dir);
    if (!v3_bft_run_remote_shell(cfg.server_ssh, command, log_path, "bft_wait_server_ready")) {
        (void)v3_cmd_stop_bft_validator_three_node_impl(argc, argv);
        return 1;
    }
    v3_join_path(log_path, sizeof(log_path), paths.root, "artifacts/v3_gate/bft_start_relay_follower.log");
    snprintf(command,
             sizeof(command),
             "set -eu && rm -f %s/relay_follower.pid && setsid -f sh -lc 'echo $$ > %s/relay_follower.pid && exec %s validator-follower-daemon --state:%s/relay.state --proposal-host:%s --proposal-port:%d --vote-host:%s --vote-port:%d --commit-host:%s --commit-port:%d --sync-host:%s --sync-port:%d --interval-ms:%d > %s/relay.follower.log 2>&1 < /dev/null' && i=0 && while [ ! -f %s/relay_follower.pid ] && [ \"$i\" -lt 50 ]; do i=$((i + 1)); sleep 0.1; done && test -f %s/relay_follower.pid",
             relay_dir,
             relay_dir,
             relay_bin,
             relay_dir,
             cfg.server_addr,
             cfg.proposal_port,
             cfg.server_addr,
             cfg.vote_port,
             cfg.server_addr,
             cfg.commit_port,
             cfg.server_addr,
             cfg.state_port,
             cfg.follower_interval_ms,
             relay_dir,
             relay_dir,
             relay_dir);
    if (!v3_bft_run_remote_shell(cfg.relay_ssh, command, log_path, "bft_start_relay_follower")) {
        return 1;
    }
    v3_join_path(log_path, sizeof(log_path), paths.root, "artifacts/v3_gate/bft_start_local_follower.log");
    unlink(local_follower_pid);
    snprintf(local_state_arg, sizeof(local_state_arg), "--state:%s", local_state);
    snprintf(local_proposal_host_arg, sizeof(local_proposal_host_arg), "--proposal-host:%s", cfg.server_addr);
    snprintf(local_proposal_port_arg, sizeof(local_proposal_port_arg), "--proposal-port:%d", cfg.proposal_port);
    snprintf(local_vote_host_arg, sizeof(local_vote_host_arg), "--vote-host:%s", cfg.server_addr);
    snprintf(local_vote_port_arg, sizeof(local_vote_port_arg), "--vote-port:%d", cfg.vote_port);
    snprintf(local_commit_host_arg, sizeof(local_commit_host_arg), "--commit-host:%s", cfg.server_addr);
    snprintf(local_commit_port_arg, sizeof(local_commit_port_arg), "--commit-port:%d", cfg.commit_port);
    snprintf(local_sync_host_arg, sizeof(local_sync_host_arg), "--sync-host:%s", cfg.server_addr);
    snprintf(local_sync_port_arg, sizeof(local_sync_port_arg), "--sync-port:%d", cfg.state_port);
    snprintf(local_interval_arg, sizeof(local_interval_arg), "--interval-ms:%d", cfg.follower_interval_ms);
    local_follower_argv[0] = local_bin;
    local_follower_argv[1] = "validator-follower-daemon";
    local_follower_argv[2] = local_state_arg;
    local_follower_argv[3] = local_proposal_host_arg;
    local_follower_argv[4] = local_proposal_port_arg;
    local_follower_argv[5] = local_vote_host_arg;
    local_follower_argv[6] = local_vote_port_arg;
    local_follower_argv[7] = local_commit_host_arg;
    local_follower_argv[8] = local_commit_port_arg;
    local_follower_argv[9] = local_sync_host_arg;
    local_follower_argv[10] = local_sync_port_arg;
    local_follower_argv[11] = local_interval_arg;
    local_follower_argv[12] = NULL;
    if (!v3_spawn_binary_detached_logged(local_follower_argv,
                                         local_follower_log,
                                         local_follower_pid,
                                         &local_pid)) {
        fprintf(stderr, "[cheng_v3_seed] bft_start_local_follower spawn failed log=%s pid=%s\n",
                local_follower_log,
                local_follower_pid);
        return 1;
    }
    {
        int spin;
        bool alive = false;
        for (spin = 0; spin < 50; ++spin) {
            if (v3_process_alive(local_pid)) {
                alive = true;
                break;
            }
            usleep(100000);
        }
        if (!alive) {
            fprintf(stderr, "[cheng_v3_seed] bft_start_local_follower died pid=%ld log=%s\n",
                    (long)local_pid,
                    local_follower_log);
            return 1;
        }
    }
    snprintf(local_start_note,
             sizeof(local_start_note),
             "pid=%ld\nlog=%s\n",
             (long)local_pid,
             local_follower_log);
    if (!v3_write_text_file(log_path, local_start_note)) {
        return 1;
    }
    if (!v3_bft_wait_cluster_baseline(&cfg,
                                      server_bin,
                                      relay_bin,
                                      local_bin,
                                      server_state,
                                      relay_state,
                                      local_state,
                                      "artifacts/v3_gate/bft_wait_baseline",
                                      server_height,
                                      sizeof(server_height),
                                      server_hash,
                                      sizeof(server_hash),
                                      server_alice,
                                      sizeof(server_alice),
                                      server_bob,
                                      sizeof(server_bob),
                                      relay_height,
                                      sizeof(relay_height),
                                      relay_hash,
                                      sizeof(relay_hash),
                                      relay_alice,
                                      sizeof(relay_alice),
                                      relay_bob,
                                      sizeof(relay_bob),
                                      local_height,
                                      sizeof(local_height),
                                      local_hash,
                                      sizeof(local_hash),
                                      local_alice,
                                      sizeof(local_alice),
                                      local_bob,
                                      sizeof(local_bob))) {
        fprintf(stderr, "[cheng_v3_seed] deploy-bft-validator-three-node did not converge\n");
        return 1;
    }
    v3_join_path(log_path, sizeof(log_path), paths.root, "artifacts/v3_gate/bft_ue_check.log");
    snprintf(command,
             sizeof(command),
             "ps -Ao pid=,ppid=,state=,etime=,command= | awk '$2==1 && $3 ~ /^U/ && $5 ~ /bft_state_machine/'");
    if (!v3_bft_run_local_shell(command, log_path, "bft_ue_check")) {
        return 1;
    }
    {
        char *ue_text = v3_read_file(log_path);
        if (ue_text != NULL && ue_text[0] != '\0') {
            fprintf(stderr, "[cheng_v3_seed] deploy-bft-validator-three-node unexpected UE process\n%s", ue_text);
            free(ue_text);
            return 1;
        }
        free(ue_text);
    }
    v3_bft_print_status_line("server", server_height, server_hash, server_alice, server_bob);
    puts("---");
    v3_bft_print_status_line("relay", relay_height, relay_hash, relay_alice, relay_bob);
    puts("---");
    v3_bft_print_status_line("local", local_height, local_hash, local_alice, local_bob);
    return 0;
}

static int v3_cmd_verify_bft_validator_three_node_impl(int argc, char **argv) {
    V3BootstrapPaths paths;
    V3BftDeployConfig cfg;
    char server_dir[PATH_MAX];
    char relay_dir[PATH_MAX];
    char server_state[PATH_MAX];
    char relay_state[PATH_MAX];
    char local_state[PATH_MAX];
    char report_path[PATH_MAX];
    char server_bin[PATH_MAX];
    char relay_bin[PATH_MAX];
    char local_bin[PATH_MAX];
    char want_height[64];
    char want_alice[64];
    char want_bob[64];
    char server_height[128] = {0};
    char server_hash[128] = {0};
    char server_alice[128] = {0};
    char server_bob[128] = {0};
    char relay_height[128] = {0};
    char relay_hash[128] = {0};
    char relay_alice[128] = {0};
    char relay_bob[128] = {0};
    char local_height[128] = {0};
    char local_hash[128] = {0};
    char local_alice[128] = {0};
    char local_bob[128] = {0};
    bool server_ok = false;
    bool relay_ok = false;
    bool local_ok = false;
    bool verified = false;
    int stop_rc = 0;
    v3_bootstrap_paths_init(&paths);
    v3_bft_config_init(&paths, &cfg);
    v3_bft_remote_deploy_dir(cfg.server_root, server_dir, sizeof(server_dir));
    v3_bft_remote_deploy_dir(cfg.relay_root, relay_dir, sizeof(relay_dir));
    v3_join_path(server_state, sizeof(server_state), server_dir, "server.state");
    v3_join_path(relay_state, sizeof(relay_state), relay_dir, "relay.state");
    v3_join_path(local_state, sizeof(local_state), cfg.local_deploy_dir, "local.state");
    v3_join_path(report_path,
                 sizeof(report_path),
                 paths.root,
                 "artifacts/v3_gate/verify_bft_validator_three_node.report.txt");
    if (v3_cmd_deploy_bft_validator_three_node_impl(argc, argv) != 0) {
        (void)v3_cmd_stop_bft_validator_three_node_impl(argc, argv);
        return 1;
    }
    v3_bft_remote_bin(cfg.server_root, server_bin, sizeof(server_bin));
    v3_bft_remote_bin(cfg.relay_root, relay_bin, sizeof(relay_bin));
    v3_bft_local_bin(&paths, local_bin, sizeof(local_bin));
    server_ok = v3_bft_collect_remote_status(cfg.server_ssh,
                                             server_bin,
                                             server_state,
                                             &cfg,
                                             false,
                                             "artifacts/v3_gate/bft_verify_server.summary.log",
                                             server_height,
                                             sizeof(server_height),
                                             server_hash,
                                             sizeof(server_hash),
                                             server_alice,
                                             sizeof(server_alice),
                                             server_bob,
                                             sizeof(server_bob));
    relay_ok = v3_bft_collect_remote_status(cfg.relay_ssh,
                                            relay_bin,
                                            relay_state,
                                            &cfg,
                                            false,
                                            "artifacts/v3_gate/bft_verify_relay.summary.log",
                                            relay_height,
                                            sizeof(relay_height),
                                            relay_hash,
                                            sizeof(relay_hash),
                                            relay_alice,
                                            sizeof(relay_alice),
                                            relay_bob,
                                            sizeof(relay_bob));
    local_ok = v3_bft_collect_local_status(local_bin,
                                           local_state,
                                           &cfg,
                                           false,
                                           "artifacts/v3_gate/bft_verify_local.summary.log",
                                           local_height,
                                           sizeof(local_height),
                                           local_hash,
                                           sizeof(local_hash),
                                           local_alice,
                                           sizeof(local_alice),
                                           local_bob,
                                           sizeof(local_bob));
    v3_bft_expected_baseline(&cfg,
                             want_height,
                             sizeof(want_height),
                             want_alice,
                             sizeof(want_alice),
                             want_bob,
                             sizeof(want_bob));
    verified = server_ok &&
               relay_ok &&
               local_ok &&
               v3_bft_cluster_matches_values(want_height,
                                             want_alice,
                                             want_bob,
                                             server_height,
                                             server_hash,
                                             server_alice,
                                             server_bob,
                                             relay_height,
                                             relay_hash,
                                             relay_alice,
                                             relay_bob,
                                             local_height,
                                             local_hash,
                                             local_alice,
                                             local_bob);
    if (!v3_bft_write_verify_report(report_path,
                                    &cfg,
                                    verified,
                                    server_height,
                                    server_hash,
                                    server_alice,
                                    server_bob,
                                    relay_height,
                                    relay_hash,
                                    relay_alice,
                                    relay_bob,
                                    local_height,
                                    local_hash,
                                    local_alice,
                                    local_bob)) {
        fprintf(stderr, "[cheng_v3_seed] verify-bft-validator-three-node failed to write report\n");
        (void)v3_cmd_stop_bft_validator_three_node_impl(argc, argv);
        return 1;
    }
    stop_rc = v3_cmd_stop_bft_validator_three_node_impl(argc, argv);
    if (!server_ok) {
        fprintf(stderr, "[cheng_v3_seed] verify-bft-validator-three-node server status failed\n");
        return 1;
    }
    if (!relay_ok) {
        fprintf(stderr, "[cheng_v3_seed] verify-bft-validator-three-node relay status failed\n");
        return 1;
    }
    if (!local_ok) {
        fprintf(stderr, "[cheng_v3_seed] verify-bft-validator-three-node local status failed\n");
        return 1;
    }
    if (!verified) {
        fprintf(stderr, "[cheng_v3_seed] verify-bft-validator-three-node cluster drifted\n");
        v3_bft_print_status_line("server", server_height, server_hash, server_alice, server_bob);
        puts("---");
        v3_bft_print_status_line("relay", relay_height, relay_hash, relay_alice, relay_bob);
        puts("---");
        v3_bft_print_status_line("local", local_height, local_hash, local_alice, local_bob);
        return 1;
    }
    if (stop_rc != 0) {
        fprintf(stderr, "[cheng_v3_seed] verify-bft-validator-three-node stop failed\n");
        return stop_rc;
    }
    puts("v3 bft verify ok");
    printf("report=%s\n", report_path);
    return 0;
}

static int v3_cmd_bench_bft_validator_three_node_impl(int argc, char **argv) {
    V3BootstrapPaths paths;
    V3BftDeployConfig cfg;
    const char *tx_count_text = v3_flag_value(argc, argv, "--tx-count");
    const char *wave_count_text = v3_flag_value(argc, argv, "--wave-count");
    const char *transfer_amount_text = v3_flag_value(argc, argv, "--transfer-amount");
    const char *max_txs_text = v3_flag_value(argc, argv, "--max-txs-per-block");
    const char *restart_node_text = v3_flag_value(argc, argv, "--restart-node");
    const char *restart_wave_text = v3_flag_value(argc, argv, "--restart-wave");
    const char *restart_pause_ms_text = v3_flag_value(argc, argv, "--restart-pause-ms");
    int tx_count = tx_count_text != NULL && tx_count_text[0] != '\0' ? atoi(tx_count_text) : 256;
    int wave_count = wave_count_text != NULL && wave_count_text[0] != '\0' ? atoi(wave_count_text) : 1;
    long long transfer_amount = transfer_amount_text != NULL && transfer_amount_text[0] != '\0' ? atoll(transfer_amount_text) : 1LL;
    int max_txs_per_block = max_txs_text != NULL && max_txs_text[0] != '\0' ? atoi(max_txs_text) : tx_count;
    char restart_node[32];
    int restart_pause_ms = restart_pause_ms_text != NULL && restart_pause_ms_text[0] != '\0' ? atoi(restart_pause_ms_text) : 1000;
    int restart_wave;
    char saved_max_txs_env[32];
    bool had_saved_max_txs_env = false;
    char server_dir[PATH_MAX];
    char relay_dir[PATH_MAX];
    char report_path[PATH_MAX];
    char server_state[PATH_MAX];
    char relay_state[PATH_MAX];
    char local_state[PATH_MAX];
    char server_bin[PATH_MAX];
    char relay_bin[PATH_MAX];
    char local_bin[PATH_MAX];
    char log_path[PATH_MAX];
    char log_rel[PATH_MAX];
    char command[16384];
    char server_height[128];
    char server_hash[128];
    char server_alice[128];
    char server_bob[128];
    char relay_height[128];
    char relay_hash[128];
    char relay_alice[128];
    char relay_bob[128];
    char local_height[128];
    char local_hash[128];
    char local_alice[128];
    char local_bob[128];
    char final_tps_text[64];
    char final_kib_text[64];
    char queued_count_buf[64];
    char queued_bytes_buf[64];
    char tx_wire_bytes_buf[64];
    long long *wave_elapsed_ms = NULL;
    long long *wave_tps_milli = NULL;
    long long *wave_block_counts = NULL;
    bool *wave_restarted = NULL;
    char tps_text[64];
    char kib_text[64];
    char *bench_text = NULL;
    char *ue_text = NULL;
    long long base_height;
    long long base_alice;
    long long base_bob;
    long long required_amount_per_wave;
    long long total_required_amount;
    long long total_queued_count = 0LL;
    long long total_queued_bytes = 0LL;
    long long tx_wire_bytes = 0LL;
    long long total_blocks = 0LL;
    long long expected_height;
    long long expected_alice;
    long long expected_bob;
    long long bench_start_ms;
    long long elapsed_ms;
    long long tps_milli;
    long long bytes_per_sec;
    long long kib_per_sec_milli;
    long long topup_amount;
    int wave_index;
    int rc = 1;
    v3_trim_copy_text(restart_node_text != NULL ? restart_node_text : "none",
                      restart_node,
                      sizeof(restart_node));
    if (restart_node[0] == '\0') {
        snprintf(restart_node, sizeof(restart_node), "none");
    }
    restart_wave = restart_wave_text != NULL && restart_wave_text[0] != '\0' ?
        atoi(restart_wave_text) :
        (wave_count >= 2 ? 2 : 1);
    if (tx_count <= 0 || wave_count <= 0 || transfer_amount <= 0LL || max_txs_per_block <= 0 || restart_pause_ms < 0) {
        fprintf(stderr, "[cheng_v3_seed] bench-bft-validator-three-node requires positive --tx-count --wave-count --transfer-amount --max-txs-per-block and non-negative --restart-pause-ms\n");
        return 1;
    }
    if (!v3_bft_bench_restart_target_valid(restart_node)) {
        fprintf(stderr, "[cheng_v3_seed] bench-bft-validator-three-node requires --restart-node none|server|relay|local\n");
        return 1;
    }
    if (strcmp(restart_node, "none") == 0) {
        restart_wave = 0;
    } else if (restart_wave <= 0 || restart_wave > wave_count) {
        fprintf(stderr, "[cheng_v3_seed] bench-bft-validator-three-node requires --restart-wave within 1..wave-count when restart is enabled\n");
        return 1;
    }
    wave_elapsed_ms = (long long *)calloc((size_t)wave_count, sizeof(long long));
    wave_tps_milli = (long long *)calloc((size_t)wave_count, sizeof(long long));
    wave_block_counts = (long long *)calloc((size_t)wave_count, sizeof(long long));
    wave_restarted = (bool *)calloc((size_t)wave_count, sizeof(bool));
    if (wave_elapsed_ms == NULL ||
        wave_tps_milli == NULL ||
        wave_block_counts == NULL ||
        wave_restarted == NULL) {
        fprintf(stderr, "[cheng_v3_seed] bench-bft-validator-three-node failed to allocate wave stats\n");
        goto cleanup;
    }
    {
        const char *saved_max_txs_env_ptr = getenv("CHENG_V3_BFT_MAX_TXS");
        if (saved_max_txs_env_ptr != NULL && saved_max_txs_env_ptr[0] != '\0') {
            snprintf(saved_max_txs_env, sizeof(saved_max_txs_env), "%s", saved_max_txs_env_ptr);
            had_saved_max_txs_env = true;
        }
        char max_txs_env[32];
        snprintf(max_txs_env, sizeof(max_txs_env), "%d", max_txs_per_block);
        setenv("CHENG_V3_BFT_MAX_TXS", max_txs_env, 1);
    }
    if (v3_cmd_deploy_bft_validator_three_node_impl(argc, argv) != 0) {
        if (had_saved_max_txs_env) {
            setenv("CHENG_V3_BFT_MAX_TXS", saved_max_txs_env, 1);
        } else {
            unsetenv("CHENG_V3_BFT_MAX_TXS");
        }
        goto cleanup;
    }
    if (had_saved_max_txs_env) {
        setenv("CHENG_V3_BFT_MAX_TXS", saved_max_txs_env, 1);
    } else {
        unsetenv("CHENG_V3_BFT_MAX_TXS");
    }
    v3_bootstrap_paths_init(&paths);
    v3_bft_config_init(&paths, &cfg);
    v3_bft_remote_deploy_dir(cfg.server_root, server_dir, sizeof(server_dir));
    v3_bft_remote_deploy_dir(cfg.relay_root, relay_dir, sizeof(relay_dir));
    v3_bft_bench_report_path(paths.root, report_path, sizeof(report_path));
    v3_join_path(server_state, sizeof(server_state), server_dir, "server.state");
    v3_join_path(relay_state, sizeof(relay_state), relay_dir, "relay.state");
    v3_join_path(local_state, sizeof(local_state), cfg.local_deploy_dir, "local.state");
    v3_bft_remote_bin(cfg.server_root, server_bin, sizeof(server_bin));
    v3_bft_remote_bin(cfg.relay_root, relay_bin, sizeof(relay_bin));
    v3_bft_local_bin(&paths, local_bin, sizeof(local_bin));
    if (!v3_bft_wait_cluster_baseline(&cfg,
                                      server_bin,
                                      relay_bin,
                                      local_bin,
                                      server_state,
                                      relay_state,
                                      local_state,
                                      "artifacts/v3_gate/bft_bench_bootstrap_wait",
                                      server_height,
                                      sizeof(server_height),
                                      server_hash,
                                      sizeof(server_hash),
                                      server_alice,
                                      sizeof(server_alice),
                                      server_bob,
                                      sizeof(server_bob),
                                      relay_height,
                                      sizeof(relay_height),
                                      relay_hash,
                                      sizeof(relay_hash),
                                      relay_alice,
                                      sizeof(relay_alice),
                                      relay_bob,
                                      sizeof(relay_bob),
                                      local_height,
                                      sizeof(local_height),
                                      local_hash,
                                      sizeof(local_hash),
                                      local_alice,
                                      sizeof(local_alice),
                                      local_bob,
                                      sizeof(local_bob))) {
        fprintf(stderr, "[cheng_v3_seed] bench-bft-validator-three-node baseline wait failed\n");
        goto cleanup;
    }
    base_height = atoll(server_height);
    base_alice = atoll(server_alice);
    base_bob = atoll(server_bob);
    required_amount_per_wave = (long long)tx_count * transfer_amount;
    total_required_amount = (long long)tx_count * (long long)wave_count * transfer_amount;
    if (base_alice < total_required_amount) {
        topup_amount = total_required_amount - base_alice + transfer_amount;
        v3_join_path(log_path, sizeof(log_path), paths.root, "artifacts/v3_gate/bft_bench_topup.log");
        snprintf(command,
                 sizeof(command),
                 "set -eu && %s submit-mint --queue:%s/server.queue --asset:%d --account:%d --amount:%lld",
                 server_bin,
                 server_dir,
                 cfg.asset_id,
                 cfg.alice_id,
                 topup_amount);
        if (!v3_bft_run_remote_shell(cfg.server_ssh, command, log_path, "bft_bench_topup")) {
            goto cleanup;
        }
        base_height += 1LL;
        base_alice += topup_amount;
        snprintf(tps_text, sizeof(tps_text), "%lld", base_height);
        snprintf(kib_text, sizeof(kib_text), "%lld", base_alice);
        snprintf(queued_count_buf, sizeof(queued_count_buf), "%lld", base_bob);
        if (!v3_bft_wait_cluster_state(&cfg,
                                       server_bin,
                                       relay_bin,
                                       local_bin,
                                       server_state,
                                       relay_state,
                                       local_state,
                                       "artifacts/v3_gate/bft_bench_topup_wait",
                                       tps_text,
                                       kib_text,
                                       queued_count_buf,
                                       server_height,
                                       sizeof(server_height),
                                       server_hash,
                                       sizeof(server_hash),
                                       server_alice,
                                       sizeof(server_alice),
                                       server_bob,
                                       sizeof(server_bob),
                                       relay_height,
                                       sizeof(relay_height),
                                       relay_hash,
                                       sizeof(relay_hash),
                                       relay_alice,
                                       sizeof(relay_alice),
                                       relay_bob,
                                       sizeof(relay_bob),
                                       local_height,
                                       sizeof(local_height),
                                       local_hash,
                                       sizeof(local_hash),
                                       local_alice,
                                       sizeof(local_alice),
                                       local_bob,
                                       sizeof(local_bob))) {
            fprintf(stderr, "[cheng_v3_seed] bench-bft-validator-three-node topup did not converge\n");
            goto cleanup;
        }
    }
    expected_height = base_height;
    expected_alice = base_alice;
    expected_bob = base_bob;
    bench_start_ms = v3_bft_monotime_ms();
    for (wave_index = 0; wave_index < wave_count; ++wave_index) {
        long long wave_start_ms;
        long long wave_queued_count;
        long long wave_queued_bytes;
        long long wave_tx_wire_bytes;
        long long blocks_needed;
        snprintf(log_rel, sizeof(log_rel), "artifacts/v3_gate/bft_bench_wave_%d.submit.log", wave_index + 1);
        v3_join_path(log_path, sizeof(log_path), paths.root, log_rel);
        wave_start_ms = v3_bft_monotime_ms();
        snprintf(command,
                 sizeof(command),
                 "set -eu && %s submit-transfer-series --queue:%s/server.queue --asset:%d --from:%d --to:%d --amount:%lld --count:%d",
                 server_bin,
                 server_dir,
                 cfg.asset_id,
                 cfg.alice_id,
                 cfg.bob_id,
                 transfer_amount,
                 tx_count);
        if (!v3_bft_run_remote_shell(cfg.server_ssh, command, log_path, "bft_bench_submit_series")) {
            goto cleanup;
        }
        free(bench_text);
        bench_text = v3_read_file(log_path);
        if (bench_text == NULL ||
            !v3_text_line_value(bench_text, "queued_count", queued_count_buf, sizeof(queued_count_buf)) ||
            !v3_text_line_value(bench_text, "queued_bytes", queued_bytes_buf, sizeof(queued_bytes_buf)) ||
            !v3_text_line_value(bench_text, "tx_wire_bytes", tx_wire_bytes_buf, sizeof(tx_wire_bytes_buf))) {
            fprintf(stderr, "[cheng_v3_seed] bench-bft-validator-three-node failed to parse submit series report\n");
            goto cleanup;
        }
        wave_queued_count = atoll(queued_count_buf);
        wave_queued_bytes = atoll(queued_bytes_buf);
        wave_tx_wire_bytes = atoll(tx_wire_bytes_buf);
        free(bench_text);
        bench_text = NULL;
        if (wave_queued_count != (long long)tx_count || wave_queued_bytes <= 0LL || wave_tx_wire_bytes <= 0LL) {
            fprintf(stderr, "[cheng_v3_seed] bench-bft-validator-three-node invalid submit series report\n");
            goto cleanup;
        }
        if (tx_wire_bytes <= 0LL) {
            tx_wire_bytes = wave_tx_wire_bytes;
        }
        blocks_needed = ((long long)tx_count + (long long)max_txs_per_block - 1LL) / (long long)max_txs_per_block;
        total_queued_count += wave_queued_count;
        total_queued_bytes += wave_queued_bytes;
        total_blocks += blocks_needed;
        expected_height += blocks_needed;
        expected_alice -= required_amount_per_wave;
        expected_bob += required_amount_per_wave;
        if (strcmp(restart_node, "none") != 0 && wave_index + 1 == restart_wave) {
            snprintf(log_rel, sizeof(log_rel), "artifacts/v3_gate/bft_bench_wave_%d.restart", wave_index + 1);
            if (!v3_bft_bench_restart_node(paths.root,
                                           &cfg,
                                           server_dir,
                                           relay_dir,
                                           cfg.local_deploy_dir,
                                           server_bin,
                                           relay_bin,
                                           local_bin,
                                           restart_node,
                                           restart_pause_ms,
                                           max_txs_per_block,
                                           log_rel)) {
                fprintf(stderr, "[cheng_v3_seed] bench-bft-validator-three-node restart failed\n");
                goto cleanup;
            }
            wave_restarted[wave_index] = true;
        }
        snprintf(tps_text, sizeof(tps_text), "%lld", expected_height);
        snprintf(kib_text, sizeof(kib_text), "%lld", expected_alice);
        snprintf(queued_count_buf, sizeof(queued_count_buf), "%lld", expected_bob);
        snprintf(log_rel, sizeof(log_rel), "artifacts/v3_gate/bft_bench_wave_%d.wait", wave_index + 1);
        if (!v3_bft_wait_cluster_state(&cfg,
                                       server_bin,
                                       relay_bin,
                                       local_bin,
                                       server_state,
                                       relay_state,
                                       local_state,
                                       log_rel,
                                       tps_text,
                                       kib_text,
                                       queued_count_buf,
                                       server_height,
                                       sizeof(server_height),
                                       server_hash,
                                       sizeof(server_hash),
                                       server_alice,
                                       sizeof(server_alice),
                                       server_bob,
                                       sizeof(server_bob),
                                       relay_height,
                                       sizeof(relay_height),
                                       relay_hash,
                                       sizeof(relay_hash),
                                       relay_alice,
                                       sizeof(relay_alice),
                                       relay_bob,
                                       sizeof(relay_bob),
                                       local_height,
                                       sizeof(local_height),
                                       local_hash,
                                       sizeof(local_hash),
                                       local_alice,
                                       sizeof(local_alice),
                                       local_bob,
                                       sizeof(local_bob))) {
            fprintf(stderr, "[cheng_v3_seed] bench-bft-validator-three-node did not converge\n");
            goto cleanup;
        }
        wave_elapsed_ms[wave_index] = v3_bft_monotime_ms() - wave_start_ms;
        if (wave_elapsed_ms[wave_index] <= 0LL) {
            wave_elapsed_ms[wave_index] = 1LL;
        }
        wave_tps_milli[wave_index] = wave_queued_count * 1000000LL / wave_elapsed_ms[wave_index];
        wave_block_counts[wave_index] = blocks_needed;
    }
    elapsed_ms = v3_bft_monotime_ms() - bench_start_ms;
    if (elapsed_ms <= 0LL) {
        elapsed_ms = 1LL;
    }
    tps_milli = total_queued_count * 1000000LL / elapsed_ms;
    bytes_per_sec = total_queued_bytes * 1000LL / elapsed_ms;
    kib_per_sec_milli = total_queued_bytes * 1000000LL / (elapsed_ms * 1024LL);
    v3_bft_format_milli(tps_milli, final_tps_text, sizeof(final_tps_text));
    v3_bft_format_milli(kib_per_sec_milli, final_kib_text, sizeof(final_kib_text));
    v3_join_path(log_path, sizeof(log_path), paths.root, "artifacts/v3_gate/bft_bench_ue_check.log");
    snprintf(command,
             sizeof(command),
             "ps -Ao pid=,ppid=,state=,etime=,command= | awk '$2==1 && $3 ~ /^U/ && $5 ~ /bft_state_machine/'");
    if (!v3_bft_run_local_shell(command, log_path, "bft_bench_ue_check")) {
        goto cleanup;
    }
    ue_text = v3_read_file(log_path);
    if (ue_text != NULL && ue_text[0] != '\0') {
        fprintf(stderr, "[cheng_v3_seed] bench-bft-validator-three-node unexpected UE process\n%s", ue_text);
        goto cleanup;
    }
    if (!v3_bft_write_bench_report(report_path,
                                   &cfg,
                                   (long long)tx_count,
                                   wave_count,
                                   transfer_amount,
                                   max_txs_per_block,
                                   restart_node,
                                   restart_wave,
                                   restart_pause_ms,
                                   total_queued_count,
                                   tx_wire_bytes,
                                   total_queued_bytes,
                                   total_blocks,
                                   elapsed_ms,
                                   tps_milli,
                                   bytes_per_sec,
                                   kib_per_sec_milli,
                                   wave_elapsed_ms,
                                   wave_tps_milli,
                                   wave_block_counts,
                                   wave_restarted,
                                   server_height,
                                   server_hash,
                                   server_alice,
                                   server_bob)) {
        fprintf(stderr, "[cheng_v3_seed] bench-bft-validator-three-node failed to write report\n");
        goto cleanup;
    }
    printf("tx_count_per_wave=%d\n", tx_count);
    printf("wave_count=%d\n", wave_count);
    printf("restart_node=%s\n", restart_node);
    printf("restart_wave=%d\n", restart_wave);
    printf("restart_pause_ms=%d\n", restart_pause_ms);
    printf("tx_count=%lld\n", total_queued_count);
    printf("tx_wire_bytes=%lld\n", tx_wire_bytes);
    printf("committed_bytes=%lld\n", total_queued_bytes);
    printf("block_count=%lld\n", total_blocks);
    printf("elapsed_ms=%lld\n", elapsed_ms);
    printf("tps=%s\n", final_tps_text);
    printf("throughput_bytes_per_sec=%lld\n", bytes_per_sec);
    printf("throughput_kib_per_sec=%s\n", final_kib_text);
    printf("final_height=%s\n", server_height);
    printf("app_hash=%s\n", server_hash);
    printf("alice_balance=%s\n", server_alice);
    printf("bob_balance=%s\n", server_bob);
    for (wave_index = 0; wave_index < wave_count; ++wave_index) {
        v3_bft_format_milli(wave_tps_milli[wave_index], tps_text, sizeof(tps_text));
        printf("wave_%d_elapsed_ms=%lld\n", wave_index + 1, wave_elapsed_ms[wave_index]);
        printf("wave_%d_tps=%s\n", wave_index + 1, tps_text);
        printf("wave_%d_block_count=%lld\n", wave_index + 1, wave_block_counts[wave_index]);
        printf("wave_%d_restarted=%s\n", wave_index + 1, wave_restarted[wave_index] ? "true" : "false");
    }
    printf("report=%s\n", report_path);
    rc = 0;
cleanup:
    free(bench_text);
    free(ue_text);
    free(wave_elapsed_ms);
    free(wave_tps_milli);
    free(wave_block_counts);
    free(wave_restarted);
    return rc;
}

static int v3_cmd_build_panic_trace_impl(int argc, char **argv) {
    V3BootstrapPaths paths;
    char root_v3[PATH_MAX];
    char out_dir[PATH_MAX];
    char src[PATH_MAX];
    char out_bin[PATH_MAX];
    char line_map[PATH_MAX];
    char compile_log[PATH_MAX];
    char run_log[PATH_MAX];
    char report_path[PATH_MAX];
    char report_log[PATH_MAX];
    char *report_text = NULL;
    char *run_argv[2];
    int status = 0;
    int rc = 1;
    (void)argc;
    (void)argv;
    if (!v3_leaf_prepare_backend_driver(&paths, root_v3, sizeof(root_v3))) {
        return 1;
    }
    v3_join_path(out_dir, sizeof(out_dir), paths.root, "artifacts/v3_panic_trace");
    v3_join_path(src, sizeof(src), paths.root, "v3/src/tests/ordinary_panic_fixture.cheng");
    v3_join_path(out_bin, sizeof(out_bin), out_dir, "ordinary_panic_fixture");
    snprintf(line_map, sizeof(line_map), "%s.v3.map", out_bin);
    v3_join_path(compile_log, sizeof(compile_log), out_dir, "ordinary_panic_fixture.compile.log");
    v3_join_path(run_log, sizeof(run_log), out_dir, "ordinary_panic_fixture.run.log");
    v3_join_path(report_path, sizeof(report_path), out_dir, "ordinary_panic_fixture.report.txt");
    snprintf(report_log, sizeof(report_log), "%s.crash-report.log", report_path);
    if (!v3_mkdir_p(out_dir) ||
        !v3_compile_host_fixture_with_backend_driver(paths.backend_driver_out, root_v3, src, out_bin, compile_log) ||
        !v3_expect_file_exists_nonempty("build-panic-trace line-map", line_map) ||
        !v3_expect_line_map_text("build-panic-trace line-map", line_map, "ordinary_panic_fixture.cheng", true)) {
        return 1;
    }
    run_argv[0] = out_bin;
    run_argv[1] = NULL;
    if (!v3_run_binary_capture_output(run_argv, run_log, &status) ||
        v3_status_is_exit_code(status, 0)) {
        return 1;
    }
    if (!v3_crash_report_transform_raw(run_log, report_path, report_log)) {
        return 1;
    }
    if (!v3_verify_runtime_fixture_output("build-panic-trace",
                                          src,
                                          run_log,
                                          report_path,
                                          "v3 panic fixture",
                                          "reason=panic mode=backtrace",
                                          false)) {
        return 1;
    }
    report_text = v3_read_file(report_path);
    if (report_text == NULL) {
        return 1;
    }
    fputs(report_text, stdout);
    rc = 0;
    free(report_text);
    return rc;
}

static int v3_cmd_build_bounds_trace_impl(int argc, char **argv) {
    V3BootstrapPaths paths;
    char root_v3[PATH_MAX];
    char out_dir[PATH_MAX];
    char src[PATH_MAX];
    char out_bin[PATH_MAX];
    char line_map[PATH_MAX];
    char compile_log[PATH_MAX];
    char run_log[PATH_MAX];
    char report_path[PATH_MAX];
    char report_log[PATH_MAX];
    char *run_argv[2];
    char *report_text = NULL;
    int status = 0;
    int rc = 1;
    (void)argc;
    (void)argv;
    if (!v3_leaf_prepare_backend_driver(&paths, root_v3, sizeof(root_v3))) {
        return 1;
    }
    v3_join_path(out_dir, sizeof(out_dir), paths.root, "artifacts/v3_bounds_trace");
    v3_join_path(src, sizeof(src), paths.root, "v3/src/tests/ordinary_bounds_trace_fixture.cheng");
    v3_join_path(out_bin, sizeof(out_bin), out_dir, "ordinary_bounds_trace_fixture");
    snprintf(line_map, sizeof(line_map), "%s.v3.map", out_bin);
    v3_join_path(compile_log, sizeof(compile_log), out_dir, "ordinary_bounds_trace_fixture.compile.log");
    v3_join_path(run_log, sizeof(run_log), out_dir, "ordinary_bounds_trace_fixture.run.log");
    v3_join_path(report_path, sizeof(report_path), out_dir, "ordinary_bounds_trace_fixture.report.txt");
    snprintf(report_log, sizeof(report_log), "%s.crash-report.log", report_path);
    if (!v3_mkdir_p(out_dir) ||
        !v3_compile_host_fixture_with_backend_driver(paths.backend_driver_out, root_v3, src, out_bin, compile_log) ||
        !v3_expect_file_exists_nonempty("build-bounds-trace line-map", line_map)) {
        return 1;
    }
    run_argv[0] = out_bin;
    run_argv[1] = NULL;
    if (!v3_run_binary_capture_output(run_argv, run_log, &status) ||
        v3_status_is_exit_code(status, 0)) {
        return 1;
    }
    if (!v3_crash_report_transform_raw(run_log, report_path, report_log)) {
        return 1;
    }
    if (!v3_verify_runtime_fixture_output("build-bounds-trace",
                                          src,
                                          run_log,
                                          report_path,
                                          "[cheng] bounds check failed: idx=1 len=1",
                                          "reason=bounds mode=backtrace",
                                          false)) {
        return 1;
    }
    report_text = v3_read_file(report_path);
    if (report_text == NULL) {
        return 1;
    }
    fputs(report_text, stdout);
    rc = 0;
    free(report_text);
    return rc;
}

static int v3_cmd_build_signal_trace_impl(int argc, char **argv) {
    V3BootstrapPaths paths;
    char root_v3[PATH_MAX];
    char out_dir[PATH_MAX];
    char src[PATH_MAX];
    char out_bin[PATH_MAX];
    char line_map[PATH_MAX];
    char compile_log[PATH_MAX];
    char run_log[PATH_MAX];
    char report_path[PATH_MAX];
    char report_log[PATH_MAX];
    char *report_text = NULL;
    char *run_argv[2];
    int status = 0;
    int rc = 1;
    (void)argc;
    (void)argv;
    if (!v3_leaf_prepare_backend_driver(&paths, root_v3, sizeof(root_v3))) {
        return 1;
    }
    v3_join_path(out_dir, sizeof(out_dir), paths.root, "artifacts/v3_signal_trace");
    v3_join_path(src, sizeof(src), paths.root, "v3/src/tests/ordinary_signal_trace_fixture.cheng");
    v3_join_path(out_bin, sizeof(out_bin), out_dir, "ordinary_signal_trace_fixture");
    snprintf(line_map, sizeof(line_map), "%s.v3.map", out_bin);
    v3_join_path(compile_log, sizeof(compile_log), out_dir, "ordinary_signal_trace_fixture.compile.log");
    v3_join_path(run_log, sizeof(run_log), out_dir, "ordinary_signal_trace_fixture.run.log");
    v3_join_path(report_path, sizeof(report_path), out_dir, "ordinary_signal_trace_fixture.report.txt");
    snprintf(report_log, sizeof(report_log), "%s.crash-report.log", report_path);
    if (!v3_mkdir_p(out_dir) ||
        !v3_compile_host_fixture_with_backend_driver(paths.backend_driver_out, root_v3, src, out_bin, compile_log) ||
        !v3_expect_file_exists_nonempty("build-signal-trace line-map", line_map)) {
        return 1;
    }
    run_argv[0] = out_bin;
    run_argv[1] = NULL;
    if (!v3_run_binary_capture_output(run_argv, run_log, &status) ||
        v3_status_is_exit_code(status, 0)) {
        return 1;
    }
    if (!v3_crash_report_transform_raw(run_log, report_path, report_log)) {
        return 1;
    }
    if (!v3_verify_runtime_fixture_output("build-signal-trace",
                                          src,
                                          run_log,
                                          report_path,
                                          "v3_crash_raw_v1",
                                          "reason=signal mode=signal",
                                          true)) {
        return 1;
    }
    report_text = v3_read_file(report_path);
    if (report_text == NULL) {
        return 1;
    }
    fputs(report_text, stdout);
    rc = 0;
    free(report_text);
    return rc;
}

static int v3_cmd_build_ffi_handle_impl(int argc, char **argv) {
    V3BootstrapPaths paths;
    char root_v3[PATH_MAX];
    char out_dir[PATH_MAX];
    char ok_src[PATH_MAX];
    char trap_src[PATH_MAX];
    char ok_bin[PATH_MAX];
    char trap_bin[PATH_MAX];
    char ok_line_map[PATH_MAX];
    char trap_line_map[PATH_MAX];
    char ok_compile_log[PATH_MAX];
    char trap_compile_log[PATH_MAX];
    char ok_run_log[PATH_MAX];
    char trap_run_log[PATH_MAX];
    char *ok_argv[2];
    char *trap_argv[2];
    char *trap_text = NULL;
    int status = 0;
    int rc = 1;
    (void)argc;
    (void)argv;
    if (!v3_leaf_prepare_backend_driver(&paths, root_v3, sizeof(root_v3))) {
        return 1;
    }
    v3_join_path(out_dir, sizeof(out_dir), paths.root, "artifacts/v3_ffi_handle");
    v3_join_path(ok_src, sizeof(ok_src), paths.root, "v3/src/tests/ffi_handle_smoke.cheng");
    v3_join_path(trap_src, sizeof(trap_src), paths.root, "v3/src/tests/ffi_handle_stale_trap_smoke.cheng");
    v3_join_path(ok_bin, sizeof(ok_bin), out_dir, "ffi_handle_smoke");
    v3_join_path(trap_bin, sizeof(trap_bin), out_dir, "ffi_handle_stale_trap_smoke");
    snprintf(ok_line_map, sizeof(ok_line_map), "%s.v3.map", ok_bin);
    snprintf(trap_line_map, sizeof(trap_line_map), "%s.v3.map", trap_bin);
    v3_join_path(ok_compile_log, sizeof(ok_compile_log), out_dir, "ffi_handle_smoke.compile.log");
    v3_join_path(trap_compile_log, sizeof(trap_compile_log), out_dir, "ffi_handle_stale_trap_smoke.compile.log");
    v3_join_path(ok_run_log, sizeof(ok_run_log), out_dir, "ffi_handle_smoke.run.log");
    v3_join_path(trap_run_log, sizeof(trap_run_log), out_dir, "ffi_handle_stale_trap_smoke.run.log");
    if (!v3_mkdir_p(out_dir) ||
        !v3_compile_host_fixture_with_backend_driver(paths.backend_driver_out, root_v3, ok_src, ok_bin, ok_compile_log) ||
        !v3_compile_host_fixture_with_backend_driver(paths.backend_driver_out, root_v3, trap_src, trap_bin, trap_compile_log) ||
        !v3_expect_file_exists_nonempty("build-ffi-handle ok line-map", ok_line_map) ||
        !v3_expect_file_exists_nonempty("build-ffi-handle trap line-map", trap_line_map)) {
        return 1;
    }
    ok_argv[0] = ok_bin;
    ok_argv[1] = NULL;
    trap_argv[0] = trap_bin;
    trap_argv[1] = NULL;
    if (!v3_run_binary_capture_output(ok_argv, ok_run_log, &status) ||
        !v3_status_is_exit_code(status, 0)) {
        fprintf(stderr, "[cheng_v3_seed] build-ffi-handle expected success log=%s\n", ok_run_log);
        return 1;
    }
    if (!v3_run_binary_capture_output(trap_argv, trap_run_log, &status) ||
        v3_status_is_exit_code(status, 0)) {
        fprintf(stderr, "[cheng_v3_seed] build-ffi-handle expected trap log=%s\n", trap_run_log);
        return 1;
    }
    trap_text = v3_read_file(trap_run_log);
    if (trap_text == NULL ||
        !v3_expect_text_contains("build-ffi-handle", trap_text, "[cheng] ffi handle invalid: op=resolve_ptr detail=released handle=") ||
        !(v3_text_contains_substring(trap_text, "[cheng-v3] reason=ffi_handle mode=backtrace") ||
          v3_text_contains_substring(trap_text, "[cheng-crash-trace] reason=ffi_handle")) ||
        !v3_expect_text_contains("build-ffi-handle", trap_text, "ffi_handle_stale_trap_smoke.cheng:")) {
        free(trap_text);
        return 1;
    }
    free(trap_text);
    if (!v3_dump_file_stdout(ok_run_log) || !v3_dump_file_stdout(trap_run_log)) {
        return 1;
    }
    rc = 0;
    return rc;
}

static int v3_cmd_build_browser_host_wasm_impl(int argc, char **argv) {
    V3BootstrapPaths paths;
    char root_v3[PATH_MAX];
    char src[PATH_MAX];
    char out_dir[PATH_MAX];
    char out_path[PATH_MAX];
    char compile_log[PATH_MAX];
    const char *compiler_bin;
    const char *positionals[2];
    size_t positional_count = 0U;
    const char *out_env = getenv("CHENG_V3_BROWSER_HOST_WASM_OUT");
    const char *compiler_env = getenv("CHENG_V3_WASM_COMPILER");
    v3_bootstrap_paths_init(&paths);
    v3_join_path(root_v3, sizeof(root_v3), paths.root, "v3");
    if (compiler_env == NULL || compiler_env[0] == '\0') {
        if (!v3_leaf_prepare_backend_driver(&paths, root_v3, sizeof(root_v3))) {
            return 1;
        }
    }
    compiler_bin = v3_leaf_resolve_compiler_bin(argc,
                                                argv,
                                                "CHENG_V3_WASM_COMPILER",
                                                NULL,
                                                paths.backend_driver_out);
    if (!v3_leaf_expect_compiler("build-browser-host-wasm", compiler_bin)) {
        return 1;
    }
    v3_join_path(src, sizeof(src), paths.root, "v3/src/libp2p/browser/browser_host_wasm_probe.cheng");
    v3_join_path(out_dir, sizeof(out_dir), paths.root, "artifacts/v3_browser_host_wasm");
    positional_count = v3_collect_smoke_positionals(argc, argv, positionals, sizeof(positionals) / sizeof(positionals[0]));
    if (out_env != NULL && out_env[0] != '\0') {
        if (v3_const_os_is_absolute(out_env)) {
            v3_copy_text(out_path, sizeof(out_path), out_env);
        } else {
            v3_join_path(out_path, sizeof(out_path), paths.root, out_env);
        }
    } else if (positional_count > 0U && positionals[0][0] != '\0') {
        if (v3_const_os_is_absolute(positionals[0])) {
            v3_copy_text(out_path, sizeof(out_path), positionals[0]);
        } else {
            v3_join_path(out_path, sizeof(out_path), paths.root, positionals[0]);
        }
    } else {
        v3_join_path(out_path, sizeof(out_path), out_dir, "cheng_browser_host_abi.wasm");
    }
    v3_join_path(compile_log, sizeof(compile_log), out_dir, "cheng_browser_host_abi.compile.log");
    if (!v3_mkdir_p(out_dir) ||
        !v3_ensure_parent_dir(out_path) ||
        !v3_compile_fixture_with_backend_driver_emit_target(compiler_bin,
                                                            root_v3,
                                                            src,
                                                            "shared",
                                                            "wasm32-unknown-unknown",
                                                            out_path,
                                                            compile_log,
                                                            NULL) ||
        !v3_expect_file_exists_nonempty("build-browser-host-wasm output", out_path)) {
        return 1;
    }
    printf("v3 browser host wasm: build ok out=%s\n", out_path);
    return 0;
}

static int v3_cmd_build_chain_node_impl(int argc, char **argv) {
    V3BootstrapPaths paths;
    char root_v3[PATH_MAX];
    char src[PATH_MAX];
    char default_out_dir[PATH_MAX];
    char out_dir[PATH_MAX];
    char out_bin[PATH_MAX];
    char compile_log[PATH_MAX];
    char run_log[PATH_MAX];
    char target_buf[256];
    char *run_argv[3];
    const char *target = getenv("CHAIN_NODE_TARGET");
    const char *out_dir_env = getenv("CHAIN_NODE_OUT_DIR");
    const char *out_bin_env = getenv("CHAIN_NODE_OUT_BIN");
    const char *run_self_test_env = getenv("CHAIN_NODE_RUN_SELF_TEST");
    const char *host_target = v3_host_target_triple();
    int status = 0;
    bool run_self_test = false;
    (void)argc;
    (void)argv;
    if (!v3_leaf_prepare_backend_driver(&paths, root_v3, sizeof(root_v3))) {
        return 1;
    }
    if (target == NULL || target[0] == '\0') {
        target = "arm64-apple-darwin";
    }
    v3_copy_text(target_buf, sizeof(target_buf), target);
    v3_join_path(src, sizeof(src), paths.root, "v3/src/project/chain_node_main.cheng");
    v3_join_path(default_out_dir, sizeof(default_out_dir), paths.root, "artifacts/v3_chain_node");
    if (!v3_streq(target_buf, "arm64-apple-darwin")) {
        v3_join_path(default_out_dir, sizeof(default_out_dir), default_out_dir, target_buf);
    }
    if (out_dir_env != NULL && out_dir_env[0] != '\0') {
        v3_copy_text(out_dir, sizeof(out_dir), out_dir_env);
    } else {
        v3_copy_text(out_dir, sizeof(out_dir), default_out_dir);
    }
    if (out_bin_env != NULL && out_bin_env[0] != '\0') {
        v3_copy_text(out_bin, sizeof(out_bin), out_bin_env);
    } else {
        v3_join_path(out_bin, sizeof(out_bin), out_dir, "chain_node");
    }
    v3_join_path(compile_log, sizeof(compile_log), out_dir, "chain_node.compile.log");
    v3_join_path(run_log, sizeof(run_log), out_dir, "chain_node.self-test.log");
    if (!v3_mkdir_p(out_dir) ||
        !v3_compile_fixture_with_backend_driver_target(paths.backend_driver_out, root_v3, src, target_buf, out_bin, compile_log) ||
        access(out_bin, X_OK) != 0) {
        fprintf(stderr, "[cheng_v3_seed] build-chain-node missing output: %s\n", out_bin);
        return 1;
    }
    if (run_self_test_env != NULL && run_self_test_env[0] != '\0' && !v3_streq(run_self_test_env, "auto")) {
        run_self_test = !v3_streq(run_self_test_env, "0");
    } else {
        run_self_test = host_target[0] != '\0' && v3_streq(target_buf, host_target);
    }
    if (!run_self_test) {
        printf("v3 chain_node: build ok target=%s out=%s\n", target_buf, out_bin);
        return 0;
    }
    run_argv[0] = out_bin;
    run_argv[1] = "self-test";
    run_argv[2] = NULL;
    if (!v3_run_binary_capture_output(run_argv, run_log, &status) ||
        !v3_status_is_exit_code(status, 0)) {
        fprintf(stderr, "[cheng_v3_seed] build-chain-node self-test failed log=%s\n", run_log);
        return 1;
    }
    return v3_dump_file_stdout(run_log) ? 0 : 1;
}

static int v3_cmd_build_rwad_bft_state_machine_impl(int argc, char **argv) {
    V3BootstrapPaths paths;
    char root_v3[PATH_MAX];
    char src[PATH_MAX];
    char default_out_dir[PATH_MAX];
    char out_dir[PATH_MAX];
    char out_bin[PATH_MAX];
    char compile_log[PATH_MAX];
    char run_log[PATH_MAX];
    char target_buf[256];
    const char *compiler_bin;
    const char *target = getenv("RWAD_BFT_TARGET");
    const char *out_dir_env = getenv("RWAD_BFT_OUT_DIR");
    const char *out_bin_env = getenv("RWAD_BFT_OUT_BIN");
    const char *run_self_test_env = getenv("RWAD_BFT_RUN_SELF_TEST");
    const char *host_target = v3_host_target_triple();
    bool run_self_test = false;
    char *run_argv[3];
    int status = 0;
    if (!v3_leaf_prepare_backend_driver(&paths, root_v3, sizeof(root_v3))) {
        return 1;
    }
    compiler_bin = v3_leaf_resolve_compiler_bin(argc,
                                                argv,
                                                "CHENG_V3_RWAD_COMPILER",
                                                NULL,
                                                paths.backend_driver_out);
    if (!v3_leaf_expect_compiler("build-rwad-bft-state-machine", compiler_bin)) {
        return 1;
    }
    if (target == NULL || target[0] == '\0') {
        target = "arm64-apple-darwin";
    }
    v3_copy_text(target_buf, sizeof(target_buf), target);
    v3_join_path(src, sizeof(src), paths.root, "v3/src/project/rwad_bft_state_machine_main.cheng");
    v3_join_path(default_out_dir, sizeof(default_out_dir), paths.root, "artifacts/v3_rwad_bft_state_machine");
    if (!v3_streq(target_buf, "arm64-apple-darwin")) {
        v3_join_path(default_out_dir, sizeof(default_out_dir), default_out_dir, target_buf);
    }
    if (out_dir_env != NULL && out_dir_env[0] != '\0') {
        v3_copy_text(out_dir, sizeof(out_dir), out_dir_env);
    } else {
        v3_copy_text(out_dir, sizeof(out_dir), default_out_dir);
    }
    if (out_bin_env != NULL && out_bin_env[0] != '\0') {
        v3_copy_text(out_bin, sizeof(out_bin), out_bin_env);
    } else {
        v3_join_path(out_bin, sizeof(out_bin), out_dir, "rwad_bft_state_machine");
    }
    v3_join_path(compile_log, sizeof(compile_log), out_dir, "rwad_bft_state_machine.compile.log");
    v3_join_path(run_log, sizeof(run_log), out_dir, "rwad_bft_state_machine.self-test.log");
    if (!v3_mkdir_p(out_dir) ||
        !v3_ensure_parent_dir(out_bin) ||
        !v3_compile_fixture_with_backend_driver_target(compiler_bin,
                                                       root_v3,
                                                       src,
                                                       target_buf,
                                                       out_bin,
                                                       compile_log) ||
        access(out_bin, X_OK) != 0) {
        fprintf(stderr, "[cheng_v3_seed] build-rwad-bft-state-machine missing output: %s\n", out_bin);
        return 1;
    }
    if (run_self_test_env != NULL && run_self_test_env[0] != '\0' && !v3_streq(run_self_test_env, "auto")) {
        run_self_test = !v3_streq(run_self_test_env, "0");
    } else {
        run_self_test = host_target[0] != '\0' && v3_streq(target_buf, host_target);
    }
    if (!run_self_test) {
        printf("v3 rwad_bft_state_machine: build ok target=%s out=%s\n", target_buf, out_bin);
        return 0;
    }
    run_argv[0] = out_bin;
    run_argv[1] = "self-test";
    run_argv[2] = NULL;
    if (!v3_run_binary_capture_output(run_argv, run_log, &status) ||
        !v3_status_is_exit_code(status, 0)) {
        fprintf(stderr, "[cheng_v3_seed] build-rwad-bft-state-machine self-test failed log=%s\n", run_log);
        return 1;
    }
    return v3_dump_file_stdout(run_log) ? 0 : 1;
}

static bool v3_text_is_decimal_digits(const char *text) {
    size_t i;
    if (text == NULL || text[0] == '\0') {
        return false;
    }
    for (i = 0; text[i] != '\0'; ++i) {
        if (text[i] < '0' || text[i] > '9') {
            return false;
        }
    }
    return true;
}

static bool v3_expect_linux_aarch64_object_report(const char *label,
                                                  const char *path) {
    char *text = v3_debug_object_report(path);
    bool ok = true;
    if (text == NULL) {
        fprintf(stderr, "[cheng_v3_seed] %s object report missing: %s\n", label, path);
        return false;
    }
    ok = ok && v3_expect_text_has_line(label, text, "v3_object_report_v1");
    ok = ok && v3_expect_text_has_line(label, text, "format=elf64");
    ok = ok && v3_expect_text_has_line(label, text, "machine=183");
    ok = ok && v3_expect_text_has_prefix(label, text, "section_count=");
    ok = ok && v3_expect_text_has_prefix(label, text, "symbol_count=");
    free(text);
    return ok;
}

static bool v3_expect_linux_x86_64_object_report(const char *label,
                                                 const char *path) {
    char *text = v3_debug_object_report(path);
    bool ok = true;
    if (text == NULL) {
        fprintf(stderr, "[cheng_v3_seed] %s object report missing: %s\n", label, path);
        return false;
    }
    ok = ok && v3_expect_text_has_line(label, text, "v3_object_report_v1");
    ok = ok && v3_expect_text_has_line(label, text, "format=elf64");
    ok = ok && v3_expect_text_has_line(label, text, "machine=62");
    ok = ok && v3_expect_text_has_prefix(label, text, "section_count=");
    ok = ok && v3_expect_text_has_prefix(label, text, "symbol_count=");
    free(text);
    return ok;
}

static int v3_cmd_build_chain_node_linux_impl(int argc, char **argv) {
    V3BootstrapPaths paths;
    char root_v3[PATH_MAX];
    char src[PATH_MAX];
    char default_out_dir[PATH_MAX];
    char out_dir[PATH_MAX];
    char out_path[PATH_MAX];
    char compile_log[PATH_MAX];
    char run_log[PATH_MAX];
    char target_buf[256];
    const char *compiler_bin;
    const char *compiler_env = getenv("CHENG_V3_CHAIN_NODE_COMPILER");
    const char *target = getenv("CHAIN_NODE_TARGET");
    const char *artifact_kind = getenv("CHAIN_NODE_LINUX_ARTIFACT");
    const char *out_dir_env = getenv("CHAIN_NODE_OUT_DIR");
    const char *out_bin_env = getenv("CHAIN_NODE_OUT_BIN");
    const char *run_self_test_env = getenv("CHAIN_NODE_RUN_SELF_TEST");
    const char *host_target = v3_host_target_triple();
    bool want_exe = false;
    bool run_self_test = false;
    char *run_argv[3];
    int status = 0;
    (void)argc;
    (void)argv;
    if (!v3_leaf_prepare_backend_driver(&paths, root_v3, sizeof(root_v3))) {
        return 1;
    }
    compiler_bin = (compiler_env != NULL && compiler_env[0] != '\0') ? compiler_env : paths.backend_driver_out;
    if (access(compiler_bin, X_OK) != 0) {
        fprintf(stderr, "[cheng_v3_seed] build-chain-node-linux missing compiler: %s\n", compiler_bin);
        return 1;
    }
    if (target == NULL || target[0] == '\0') {
        target = "aarch64-unknown-linux-gnu";
    }
    if (v3_streq(target, "arm64-unknown-linux-gnu")) {
        target = "aarch64-unknown-linux-gnu";
    }
    if (!v3_streq(target, "aarch64-unknown-linux-gnu") &&
        !v3_streq(target, "x86_64-unknown-linux-gnu")) {
        fprintf(stderr, "v3 chain_node linux: unsupported target=%s\n", target);
        fprintf(stderr,
                "v3 chain_node linux: supported linux targets are aarch64-unknown-linux-gnu and x86_64-unknown-linux-gnu\n");
        return 1;
    }
    v3_copy_text(target_buf, sizeof(target_buf), target);
    if (artifact_kind != NULL && artifact_kind[0] != '\0') {
        if (v3_streq(artifact_kind, "exe")) {
            want_exe = true;
        } else if (!v3_streq(artifact_kind, "obj")) {
            fprintf(stderr, "v3 chain_node linux: unsupported artifact kind=%s\n", artifact_kind);
            return 1;
        }
    }
    v3_join_path(src, sizeof(src), paths.root, "v3/src/project/chain_node_main.cheng");
    if (want_exe) {
        v3_join_path(default_out_dir, sizeof(default_out_dir), paths.root, "artifacts/v3_chain_node");
        v3_join_path(default_out_dir, sizeof(default_out_dir), default_out_dir, target_buf);
    } else {
        v3_join_path(default_out_dir, sizeof(default_out_dir), paths.root, "artifacts/v3_chain_node_obj");
        v3_join_path(default_out_dir, sizeof(default_out_dir), default_out_dir, target_buf);
    }
    if (out_dir_env != NULL && out_dir_env[0] != '\0') {
        v3_copy_text(out_dir, sizeof(out_dir), out_dir_env);
    } else {
        v3_copy_text(out_dir, sizeof(out_dir), default_out_dir);
    }
    if (out_bin_env != NULL && out_bin_env[0] != '\0') {
        v3_copy_text(out_path, sizeof(out_path), out_bin_env);
    } else {
        v3_join_path(out_path, sizeof(out_path), out_dir, want_exe ? "chain_node" : "chain_node.o");
    }
    v3_join_path(compile_log, sizeof(compile_log), out_dir, want_exe ? "chain_node.compile.log" : "chain_node.obj.compile.log");
    v3_join_path(run_log, sizeof(run_log), out_dir, "chain_node.self-test.log");
    if (!v3_mkdir_p(out_dir) ||
        !v3_compile_fixture_with_backend_driver_emit_target(compiler_bin,
                                                            root_v3,
                                                            src,
                                                            want_exe ? "exe" : "obj",
                                                            target_buf,
                                                            out_path,
                                                            compile_log,
                                                            NULL) ||
        !v3_expect_file_exists_nonempty("build-chain-node-linux output", out_path)) {
        return 1;
    }
    if (!want_exe) {
        if ((v3_streq(target_buf, "aarch64-unknown-linux-gnu") &&
             !v3_expect_linux_aarch64_object_report("build-chain-node-linux", out_path)) ||
            (v3_streq(target_buf, "x86_64-unknown-linux-gnu") &&
             !v3_expect_linux_x86_64_object_report("build-chain-node-linux", out_path))) {
            return 1;
        }
        printf("v3 chain_node linux obj: build ok target=%s out=%s\n", target_buf, out_path);
        return 0;
    }
    if (run_self_test_env != NULL && run_self_test_env[0] != '\0' && !v3_streq(run_self_test_env, "auto")) {
        run_self_test = !v3_streq(run_self_test_env, "0");
    } else {
        run_self_test = host_target[0] != '\0' && v3_streq(target_buf, host_target);
    }
    if (!run_self_test) {
        printf("v3 chain_node linux: build ok target=%s out=%s\n", target_buf, out_path);
        return 0;
    }
    run_argv[0] = out_path;
    run_argv[1] = "self-test";
    run_argv[2] = NULL;
    if (!v3_run_binary_capture_output(run_argv, run_log, &status) ||
        !v3_status_is_exit_code(status, 0)) {
        fprintf(stderr, "[cheng_v3_seed] build-chain-node-linux self-test failed log=%s\n", run_log);
        return 1;
    }
    return v3_dump_file_stdout(run_log) ? 0 : 1;
}

static int v3_cmd_build_linux_nolibc_exe_impl(int argc, char **argv) {
    V3BootstrapPaths paths;
    char root_v3[PATH_MAX];
    char src_path[PATH_MAX];
    char out_path[PATH_MAX];
    char report_path[PATH_MAX];
    char compile_log[PATH_MAX];
    char target_buf[256];
    const char *compiler_bin;
    const char *target = getenv("CHENG_V3_LINUX_EXE_TARGET");
    const char *source_env = getenv("CHENG_V3_LINUX_EXE_SOURCE");
    const char *out_env = getenv("CHENG_V3_LINUX_EXE_OUT");
    const char *report_env = getenv("CHENG_V3_LINUX_EXE_REPORT");
    if (!v3_leaf_prepare_backend_driver(&paths, root_v3, sizeof(root_v3))) {
        return 1;
    }
    compiler_bin = v3_leaf_resolve_compiler_bin(argc,
                                                argv,
                                                "CHENG_V3_LINUX_EXE_COMPILER",
                                                NULL,
                                                paths.backend_driver_out);
    if (!v3_leaf_expect_compiler("build-linux-nolibc-exe", compiler_bin)) {
        return 1;
    }
    if (source_env == NULL || source_env[0] == '\0') {
        fprintf(stderr, "v3 linux exe: missing CHENG_V3_LINUX_EXE_SOURCE\n");
        return 1;
    }
    if (out_env == NULL || out_env[0] == '\0') {
        fprintf(stderr, "v3 linux exe: missing CHENG_V3_LINUX_EXE_OUT\n");
        return 1;
    }
    if (target == NULL || target[0] == '\0') {
        target = "aarch64-unknown-linux-gnu";
    }
    if (v3_streq(target, "arm64-unknown-linux-gnu")) {
        target = "aarch64-unknown-linux-gnu";
    }
    if (!v3_streq(target, "aarch64-unknown-linux-gnu")) {
        fprintf(stderr, "v3 linux exe: unsupported target=%s\n", target);
        return 1;
    }
    v3_copy_text(target_buf, sizeof(target_buf), target);
    if (v3_const_os_is_absolute(source_env)) {
        v3_copy_text(src_path, sizeof(src_path), source_env);
    } else {
        v3_join_path(src_path, sizeof(src_path), paths.root, source_env);
    }
    if (v3_const_os_is_absolute(out_env)) {
        v3_copy_text(out_path, sizeof(out_path), out_env);
    } else {
        v3_join_path(out_path, sizeof(out_path), paths.root, out_env);
    }
    if (report_env != NULL && report_env[0] != '\0') {
        if (v3_const_os_is_absolute(report_env)) {
            v3_copy_text(report_path, sizeof(report_path), report_env);
        } else {
            v3_join_path(report_path, sizeof(report_path), paths.root, report_env);
        }
    } else {
        report_path[0] = '\0';
    }
    snprintf(compile_log, sizeof(compile_log), "%s.compile.log", out_path);
    if (!v3_ensure_parent_dir(out_path) ||
        (report_path[0] != '\0' && !v3_ensure_parent_dir(report_path)) ||
        !v3_compile_fixture_with_backend_driver_emit_target(compiler_bin,
                                                            root_v3,
                                                            src_path,
                                                            "exe",
                                                            target_buf,
                                                            out_path,
                                                            compile_log,
                                                            report_path[0] != '\0' ? report_path : NULL) ||
        !v3_expect_file_exists_nonempty("build-linux-nolibc-exe output", out_path)) {
        return 1;
    }
    printf("v3 linux exe: build ok target=%s out=%s\n", target_buf, out_path);
    return 0;
}

static int v3_cmd_build_rwad_bft_linux_impl(int argc, char **argv) {
    V3BootstrapPaths paths;
    char root_v3[PATH_MAX];
    char src[PATH_MAX];
    char default_out_dir[PATH_MAX];
    char out_dir[PATH_MAX];
    char out_path[PATH_MAX];
    char compile_log[PATH_MAX];
    char run_log[PATH_MAX];
    char target_buf[256];
    const char *compiler_bin;
    const char *compiler_env = getenv("CHENG_V3_RWAD_BFT_COMPILER");
    const char *target = getenv("RWAD_BFT_TARGET");
    const char *artifact_kind = getenv("RWAD_BFT_LINUX_ARTIFACT");
    const char *out_dir_env = getenv("RWAD_BFT_OUT_DIR");
    const char *out_bin_env = getenv("RWAD_BFT_OUT_BIN");
    const char *run_self_test_env = getenv("RWAD_BFT_RUN_SELF_TEST");
    const char *host_target = v3_host_target_triple();
    bool want_exe = false;
    bool run_self_test = false;
    char *run_argv[3];
    int status = 0;
    (void)argc;
    (void)argv;
    if (!v3_leaf_prepare_backend_driver(&paths, root_v3, sizeof(root_v3))) {
        return 1;
    }
    compiler_bin = (compiler_env != NULL && compiler_env[0] != '\0') ? compiler_env : paths.backend_driver_out;
    if (access(compiler_bin, X_OK) != 0) {
        fprintf(stderr, "[cheng_v3_seed] build-rwad-bft-linux missing compiler: %s\n", compiler_bin);
        return 1;
    }
    if (target == NULL || target[0] == '\0') {
        target = "aarch64-unknown-linux-gnu";
    }
    if (v3_streq(target, "arm64-unknown-linux-gnu")) {
        target = "aarch64-unknown-linux-gnu";
    }
    if (!v3_streq(target, "aarch64-unknown-linux-gnu") &&
        !v3_streq(target, "x86_64-unknown-linux-gnu")) {
        fprintf(stderr, "v3 rwad_bft_state_machine linux: unsupported target=%s\n", target);
        fprintf(stderr,
                "v3 rwad_bft_state_machine linux: supported linux targets are aarch64-unknown-linux-gnu and x86_64-unknown-linux-gnu\n");
        return 1;
    }
    v3_copy_text(target_buf, sizeof(target_buf), target);
    if (artifact_kind != NULL && artifact_kind[0] != '\0') {
        if (v3_streq(artifact_kind, "exe")) {
            want_exe = true;
        } else if (!v3_streq(artifact_kind, "obj")) {
            fprintf(stderr, "v3 rwad_bft_state_machine linux: unsupported artifact kind=%s\n", artifact_kind);
            return 1;
        }
    }
    v3_join_path(src, sizeof(src), paths.root, "v3/src/project/rwad_bft_state_machine_main.cheng");
    if (want_exe) {
        v3_join_path(default_out_dir, sizeof(default_out_dir), paths.root, "artifacts/v3_rwad_bft_state_machine");
        v3_join_path(default_out_dir, sizeof(default_out_dir), default_out_dir, target_buf);
    } else {
        v3_join_path(default_out_dir, sizeof(default_out_dir), paths.root, "artifacts/v3_rwad_bft_state_machine_obj");
        v3_join_path(default_out_dir, sizeof(default_out_dir), default_out_dir, target_buf);
    }
    if (out_dir_env != NULL && out_dir_env[0] != '\0') {
        v3_copy_text(out_dir, sizeof(out_dir), out_dir_env);
    } else {
        v3_copy_text(out_dir, sizeof(out_dir), default_out_dir);
    }
    if (out_bin_env != NULL && out_bin_env[0] != '\0') {
        v3_copy_text(out_path, sizeof(out_path), out_bin_env);
    } else {
        v3_join_path(out_path, sizeof(out_path), out_dir, want_exe ? "rwad_bft_state_machine" : "rwad_bft_state_machine.o");
    }
    v3_join_path(compile_log, sizeof(compile_log), out_dir, want_exe ? "rwad_bft_state_machine.compile.log" : "rwad_bft_state_machine.obj.compile.log");
    v3_join_path(run_log, sizeof(run_log), out_dir, "rwad_bft_state_machine.self-test.log");
    if (!v3_mkdir_p(out_dir) ||
        !v3_compile_fixture_with_backend_driver_emit_target(compiler_bin,
                                                            root_v3,
                                                            src,
                                                            want_exe ? "exe" : "obj",
                                                            target_buf,
                                                            out_path,
                                                            compile_log,
                                                            NULL) ||
        !v3_expect_file_exists_nonempty("build-rwad-bft-linux output", out_path)) {
        return 1;
    }
    if (!want_exe) {
        if ((v3_streq(target_buf, "aarch64-unknown-linux-gnu") &&
             !v3_expect_linux_aarch64_object_report("build-rwad-bft-linux", out_path)) ||
            (v3_streq(target_buf, "x86_64-unknown-linux-gnu") &&
             !v3_expect_linux_x86_64_object_report("build-rwad-bft-linux", out_path))) {
            return 1;
        }
        printf("v3 rwad_bft_state_machine linux obj: build ok target=%s out=%s\n", target_buf, out_path);
        return 0;
    }
    if (run_self_test_env != NULL && run_self_test_env[0] != '\0' && !v3_streq(run_self_test_env, "auto")) {
        run_self_test = !v3_streq(run_self_test_env, "0");
    } else {
        run_self_test = host_target[0] != '\0' && v3_streq(target_buf, host_target);
    }
    if (!run_self_test) {
        printf("v3 rwad_bft_state_machine linux: build ok target=%s out=%s\n", target_buf, out_path);
        return 0;
    }
    run_argv[0] = out_path;
    run_argv[1] = "self-test";
    run_argv[2] = NULL;
    if (!v3_run_binary_capture_output(run_argv, run_log, &status) ||
        !v3_status_is_exit_code(status, 0)) {
        fprintf(stderr, "[cheng_v3_seed] build-rwad-bft-linux self-test failed log=%s\n", run_log);
        return 1;
    }
    return v3_dump_file_stdout(run_log) ? 0 : 1;
}

static int v3_cmd_run_linux_object_smokes_impl(int argc, char **argv) {
    V3BootstrapPaths paths;
    char root_v3[PATH_MAX];
    char chain_src[PATH_MAX];
    char rwad_src[PATH_MAX];
    char chain_out_dir[PATH_MAX];
    char rwad_out_dir[PATH_MAX];
    char chain_obj[PATH_MAX];
    char rwad_obj[PATH_MAX];
    char chain_log[PATH_MAX];
    char rwad_log[PATH_MAX];
    const char *target = "aarch64-unknown-linux-gnu";
    (void)argc;
    (void)argv;
    if (!v3_leaf_prepare_backend_driver(&paths, root_v3, sizeof(root_v3))) {
        return 1;
    }
    v3_join_path(chain_src, sizeof(chain_src), paths.root, "v3/src/project/chain_node_main.cheng");
    v3_join_path(rwad_src, sizeof(rwad_src), paths.root, "v3/src/project/rwad_bft_state_machine_main.cheng");
    v3_join_path(chain_out_dir, sizeof(chain_out_dir), paths.root, "artifacts/v3_chain_node_obj/aarch64-unknown-linux-gnu");
    v3_join_path(rwad_out_dir, sizeof(rwad_out_dir), paths.root, "artifacts/v3_rwad_bft_state_machine_obj/aarch64-unknown-linux-gnu");
    v3_join_path(chain_obj, sizeof(chain_obj), chain_out_dir, "chain_node.o");
    v3_join_path(rwad_obj, sizeof(rwad_obj), rwad_out_dir, "rwad_bft_state_machine.o");
    v3_join_path(chain_log, sizeof(chain_log), chain_out_dir, "chain_node.compile.log");
    v3_join_path(rwad_log, sizeof(rwad_log), rwad_out_dir, "rwad_bft_state_machine.compile.log");
    if (!v3_mkdir_p(chain_out_dir) ||
        !v3_mkdir_p(rwad_out_dir) ||
        !v3_compile_fixture_with_backend_driver_emit_target(paths.backend_driver_out,
                                                            root_v3,
                                                            chain_src,
                                                            "obj",
                                                            target,
                                                            chain_obj,
                                                            chain_log,
                                                            NULL) ||
        !v3_compile_fixture_with_backend_driver_emit_target(paths.backend_driver_out,
                                                            root_v3,
                                                            rwad_src,
                                                            "obj",
                                                            target,
                                                            rwad_obj,
                                                            rwad_log,
                                                            NULL) ||
        !v3_expect_file_exists_nonempty("run-linux-object-smokes chain_node", chain_obj) ||
        !v3_expect_file_exists_nonempty("run-linux-object-smokes rwad_bft", rwad_obj) ||
        !v3_expect_linux_aarch64_object_report("run-linux-object-smokes chain_node", chain_obj) ||
        !v3_expect_linux_aarch64_object_report("run-linux-object-smokes rwad_bft", rwad_obj)) {
        return 1;
    }
    puts("v3 linux object smokes: ok");
    return 0;
}

static const char *v3_leaf_resolve_compiler_bin(int argc,
                                                char **argv,
                                                const char *env_primary,
                                                const char *env_fallback,
                                                const char *default_bin) {
    const char *flag = v3_flag_value(argc, argv, "--compiler");
    const char *value;
    if (flag != NULL && flag[0] != '\0') {
        return flag;
    }
    value = env_primary != NULL ? getenv(env_primary) : NULL;
    if (value != NULL && value[0] != '\0') {
        return value;
    }
    value = env_fallback != NULL ? getenv(env_fallback) : NULL;
    if (value != NULL && value[0] != '\0') {
        return value;
    }
    return default_bin;
}

static const char *v3_leaf_resolve_label(int argc,
                                         char **argv,
                                         const char *env_key,
                                         const char *default_label) {
    const char *flag = v3_flag_value(argc, argv, "--label");
    const char *value;
    if (flag != NULL && flag[0] != '\0') {
        return flag;
    }
    value = env_key != NULL ? getenv(env_key) : NULL;
    if (value != NULL && value[0] != '\0') {
        return value;
    }
    return default_label;
}

static const char *v3_verify_export_visibility_label_for_argv0(const char *argv0) {
    if (argv0 == NULL || argv0[0] == '\0') {
        return "verify_export_seed";
    }
    if (strstr(argv0, "cheng.stage3") != NULL || strstr(argv0, "/v3_bootstrap/") != NULL) {
        return "verify_export_stage3";
    }
    if (strstr(argv0, "cheng.stage2") != NULL) {
        return "verify_export_stage2";
    }
    if (strstr(argv0, "cheng.stage1") != NULL) {
        return "verify_export_stage1";
    }
    return "verify_export_seed";
}

static const char *v3_verify_export_visibility_parallel_label_for_argv0(const char *argv0) {
    if (argv0 == NULL || argv0[0] == '\0') {
        return "verify_export_parallel_seed";
    }
    if (strstr(argv0, "cheng.stage3") != NULL || strstr(argv0, "/v3_bootstrap/") != NULL) {
        return "verify_export_parallel_stage3";
    }
    if (strstr(argv0, "cheng.stage2") != NULL) {
        return "verify_export_parallel_stage2";
    }
    if (strstr(argv0, "cheng.stage1") != NULL) {
        return "verify_export_parallel_stage1";
    }
    return "verify_export_parallel_seed";
}

static bool v3_leaf_expect_compiler(const char *label,
                                    const char *compiler_bin) {
    V3BootstrapPaths paths;
    v3_bootstrap_paths_init(&paths);
    if (compiler_bin != NULL && compiler_bin[0] != '\0') {
        if (strcmp(compiler_bin, paths.backend_driver_out) == 0 &&
            access(compiler_bin, X_OK) != 0) {
            if (!v3_backend_driver_ready(&paths) &&
                v3_cmd_build_backend_driver(0, NULL) != 0) {
                fprintf(stderr, "[cheng_v3_seed] %s failed to rebuild backend driver: %s\n",
                        label,
                        compiler_bin);
                return false;
            }
        } else if ((strcmp(compiler_bin, paths.stage0) == 0 ||
                    strcmp(compiler_bin, paths.stage1) == 0 ||
                    strcmp(compiler_bin, paths.stage2) == 0 ||
                    strcmp(compiler_bin, paths.stage3) == 0) &&
                   access(compiler_bin, X_OK) != 0) {
            if (!v3_bootstrap_artifacts_fresh(&paths) &&
                v3_cmd_bootstrap_bridge(0, NULL) != 0) {
                fprintf(stderr, "[cheng_v3_seed] %s failed to rebuild bootstrap compiler: %s\n",
                        label,
                        compiler_bin);
                return false;
            }
        }
    }
    if (compiler_bin != NULL && compiler_bin[0] != '\0' && access(compiler_bin, X_OK) == 0) {
        return true;
    }
    fprintf(stderr, "[cheng_v3_seed] %s missing compiler: %s\n",
            label,
            compiler_bin != NULL ? compiler_bin : "");
    return false;
}

static bool v3_run_compiler_command_logged(const char *compiler_bin,
                                           size_t extra_arg_count,
                                           const char **extra_args,
                                           const char *log_path,
                                           const char *label) {
    const char **args;
    size_t i;
    bool ok;
    args = (const char **)v3_xmalloc(sizeof(char *) * (extra_arg_count + 1U));
    args[0] = compiler_bin;
    for (i = 0U; i < extra_arg_count; ++i) {
        args[i + 1U] = extra_args[i];
    }
    ok = v3_run_command_logged_argv(extra_arg_count + 1U, args, log_path, label);
    free(args);
    return ok;
}

static int v3_cmd_run_tcp_twoproc_smoke_impl(int argc, char **argv) {
    V3BootstrapPaths paths;
    char root_v3[PATH_MAX];
    char out_dir[PATH_MAX];
    char server_src[PATH_MAX];
    char client_src[PATH_MAX];
    char server_bin[PATH_MAX];
    char client_bin[PATH_MAX];
    char server_log[PATH_MAX];
    char client_log[PATH_MAX];
    char ready_path[PATH_MAX];
    char port_text[64];
    const char *compiler_bin;
    const char *label;
    char *server_argv[4];
    char *client_argv[3];
    char ready_arg[PATH_MAX + 32];
    char port_arg[128];
    pid_t server_pid = -1;
    int status = 0;
    if (!v3_leaf_prepare_backend_driver(&paths, root_v3, sizeof(root_v3))) {
        return 1;
    }
    compiler_bin = v3_leaf_resolve_compiler_bin(argc,
                                                argv,
                                                "CHENG_V3_SMOKE_COMPILER",
                                                NULL,
                                                paths.backend_driver_out);
    label = v3_leaf_resolve_label(argc, argv, "CHENG_V3_SMOKE_LABEL", "host");
    if (!v3_leaf_expect_compiler("run-tcp-twoproc-smoke", compiler_bin)) {
        return 1;
    }
    v3_join_path(server_src, sizeof(server_src), paths.root, "v3/src/tests/libp2p_tcp_twoproc_server_smoke.cheng");
    v3_join_path(client_src, sizeof(client_src), paths.root, "v3/src/tests/libp2p_tcp_twoproc_client_smoke.cheng");
    v3_join_path(out_dir, sizeof(out_dir), paths.root, "artifacts/v3_tcp_twoproc");
    if (!v3_mkdir_p(out_dir)) {
        return 1;
    }
    snprintf(server_bin, sizeof(server_bin), "%s/libp2p_tcp_twoproc_server.%s", out_dir, label);
    snprintf(client_bin, sizeof(client_bin), "%s/libp2p_tcp_twoproc_client.%s", out_dir, label);
    snprintf(server_log, sizeof(server_log), "%s/libp2p_tcp_twoproc_server.%s.run.log", out_dir, label);
    snprintf(client_log, sizeof(client_log), "%s/libp2p_tcp_twoproc_client.%s.run.log", out_dir, label);
    snprintf(ready_path, sizeof(ready_path), "%s/libp2p_tcp_twoproc_server.%s.ready", out_dir, label);
    unlink(ready_path);
    if (!v3_compile_host_fixture_with_backend_driver(compiler_bin, root_v3, server_src, server_bin, server_log) ||
        !v3_compile_host_fixture_with_backend_driver(compiler_bin, root_v3, client_src, client_bin, client_log)) {
        return 1;
    }
    snprintf(ready_arg, sizeof(ready_arg), "--ready-path:%s", ready_path);
    server_argv[0] = server_bin;
    server_argv[1] = "--port:0";
    server_argv[2] = ready_arg;
    server_argv[3] = NULL;
    if (!v3_spawn_binary_capture_output(server_argv, server_log, &server_pid) ||
        !v3_wait_ready_port_file("run-tcp-twoproc-smoke server", ready_path, server_pid, 20000, port_text, sizeof(port_text)) ||
        !v3_text_is_decimal_digits(port_text)) {
        v3_kill_and_wait_pid(&server_pid);
        return 1;
    }
    snprintf(port_arg, sizeof(port_arg), "--port:%s", port_text);
    client_argv[0] = client_bin;
    client_argv[1] = port_arg;
    client_argv[2] = NULL;
    if (!v3_run_binary_capture_output(client_argv, client_log, &status) ||
        !v3_status_is_exit_code(status, 0) ||
        !v3_wait_pid_success(&server_pid, "run-tcp-twoproc-smoke server", server_log)) {
        v3_kill_and_wait_pid(&server_pid);
        return 1;
    }
    if (!v3_dump_file_stdout(server_log) || !v3_dump_file_stdout(client_log)) {
        return 1;
    }
    printf("v3 libp2p_tcp_twoproc_smoke ok (%s)\n", label);
    return 0;
}

static int v3_cmd_run_quic_twoproc_smoke_impl(int argc, char **argv) {
    V3BootstrapPaths paths;
    char root_v3[PATH_MAX];
    char out_dir[PATH_MAX];
    char server_src[PATH_MAX];
    char client_src[PATH_MAX];
    char server_bin[PATH_MAX];
    char client_bin[PATH_MAX];
    char server_log[PATH_MAX];
    char client_log[PATH_MAX];
    char ready_path[PATH_MAX];
    char port_text[64];
    const char *compiler_bin;
    const char *label;
    char *server_argv[4];
    char *client_argv[3];
    char ready_arg[PATH_MAX + 32];
    char port_arg[128];
    pid_t server_pid = -1;
    int status = 0;
    if (!v3_leaf_prepare_backend_driver(&paths, root_v3, sizeof(root_v3))) {
        return 1;
    }
    compiler_bin = v3_leaf_resolve_compiler_bin(argc,
                                                argv,
                                                "CHENG_V3_SMOKE_COMPILER",
                                                NULL,
                                                paths.backend_driver_out);
    label = v3_leaf_resolve_label(argc, argv, "CHENG_V3_SMOKE_LABEL", "host");
    if (!v3_leaf_expect_compiler("run-quic-twoproc-smoke", compiler_bin)) {
        return 1;
    }
    v3_join_path(server_src, sizeof(server_src), paths.root, "v3/src/tests/libp2p_quic_twoproc_server_smoke.cheng");
    v3_join_path(client_src, sizeof(client_src), paths.root, "v3/src/tests/libp2p_quic_twoproc_client_smoke.cheng");
    v3_join_path(out_dir, sizeof(out_dir), paths.root, "artifacts/v3_quic_twoproc");
    if (!v3_mkdir_p(out_dir)) {
        return 1;
    }
    snprintf(server_bin, sizeof(server_bin), "%s/libp2p_quic_twoproc_server.%s", out_dir, label);
    snprintf(client_bin, sizeof(client_bin), "%s/libp2p_quic_twoproc_client.%s", out_dir, label);
    snprintf(server_log, sizeof(server_log), "%s/libp2p_quic_twoproc_server.%s.run.log", out_dir, label);
    snprintf(client_log, sizeof(client_log), "%s/libp2p_quic_twoproc_client.%s.run.log", out_dir, label);
    snprintf(ready_path, sizeof(ready_path), "%s/libp2p_quic_twoproc_server.%s.ready", out_dir, label);
    unlink(ready_path);
    if (!v3_compile_host_fixture_with_backend_driver(compiler_bin, root_v3, server_src, server_bin, server_log) ||
        !v3_compile_host_fixture_with_backend_driver(compiler_bin, root_v3, client_src, client_bin, client_log)) {
        return 1;
    }
    snprintf(ready_arg, sizeof(ready_arg), "--ready-path:%s", ready_path);
    server_argv[0] = server_bin;
    server_argv[1] = "--port:0";
    server_argv[2] = ready_arg;
    server_argv[3] = NULL;
    if (!v3_spawn_binary_capture_output(server_argv, server_log, &server_pid) ||
        !v3_wait_ready_port_file("run-quic-twoproc-smoke server", ready_path, server_pid, 20000, port_text, sizeof(port_text)) ||
        !v3_text_is_decimal_digits(port_text)) {
        v3_kill_and_wait_pid(&server_pid);
        return 1;
    }
    snprintf(port_arg, sizeof(port_arg), "--port:%s", port_text);
    client_argv[0] = client_bin;
    client_argv[1] = port_arg;
    client_argv[2] = NULL;
    if (!v3_run_binary_capture_output(client_argv, client_log, &status) ||
        !v3_status_is_exit_code(status, 0) ||
        !v3_wait_pid_success(&server_pid, "run-quic-twoproc-smoke server", server_log)) {
        v3_kill_and_wait_pid(&server_pid);
        return 1;
    }
    if (!v3_dump_file_stdout(server_log) || !v3_dump_file_stdout(client_log)) {
        return 1;
    }
    printf("v3 libp2p_quic_twoproc_smoke ok (%s)\n", label);
    return 0;
}

static bool v3_run_diloco_fixture_direct(const char *suite_label,
                                         const char *compile_label,
                                         const char *compiler_bin,
                                         const char *root_diloco,
                                         const char *src,
                                         const char *out_bin) {
    char compile_log[PATH_MAX];
    char run_log[PATH_MAX];
    char line_map[PATH_MAX];
    char *run_argv[2];
    int status = 0;
    if (access(src, R_OK) != 0) {
        fprintf(stderr, "%s: missing source: %s\n", suite_label, src);
        return false;
    }
    snprintf(compile_log, sizeof(compile_log), "%s.compile.log", out_bin);
    snprintf(run_log, sizeof(run_log), "%s.run.log", out_bin);
    snprintf(line_map, sizeof(line_map), "%s.v3.map", out_bin);
    unlink(out_bin);
    unlink(compile_log);
    unlink(run_log);
    unlink(line_map);
    printf("[%s] compile %s\n", suite_label, compile_label);
    if (!v3_compile_host_fixture_with_backend_driver(compiler_bin, root_diloco, src, out_bin, compile_log)) {
        fprintf(stderr, "%s: compile failed: %s\n", suite_label, compile_label);
        return false;
    }
    printf("[%s] run %s\n", suite_label, compile_label);
    run_argv[0] = (char *)out_bin;
    run_argv[1] = NULL;
    if (!v3_run_binary_capture_output(run_argv, run_log, &status) ||
        !v3_status_is_exit_code(status, 0)) {
        fprintf(stderr, "%s: run failed: %s\n", suite_label, compile_label);
        return false;
    }
    return v3_dump_file_stdout(run_log);
}

static int v3_run_diloco_quic_twoproc_direct(const char *compiler_bin,
                                             const char *label,
                                             const char *root,
                                             const char *root_diloco) {
    char out_dir[PATH_MAX];
    char server_src[PATH_MAX];
    char client_src[PATH_MAX];
    char server_bin[PATH_MAX];
    char client_bin[PATH_MAX];
    char server_log[PATH_MAX];
    char client_log[PATH_MAX];
    char ready_path[PATH_MAX];
    char port_text[64];
    char *server_argv[4];
    char *client_argv[3];
    char ready_arg[PATH_MAX + 32];
    char port_arg[128];
    pid_t server_pid = -1;
    int status = 0;
    v3_join_path(server_src, sizeof(server_src), root_diloco, "src/tests/diloco_quic_twoproc_server_smoke.cheng");
    v3_join_path(client_src, sizeof(client_src), root_diloco, "src/tests/diloco_quic_twoproc_client_smoke.cheng");
    v3_join_path(out_dir, sizeof(out_dir), root, "artifacts/v3_diloco");
    if (!v3_mkdir_p(out_dir)) {
        return 1;
    }
    snprintf(server_bin, sizeof(server_bin), "%s/diloco_quic_twoproc_server.%s", out_dir, label);
    snprintf(client_bin, sizeof(client_bin), "%s/diloco_quic_twoproc_client.%s", out_dir, label);
    snprintf(server_log, sizeof(server_log), "%s/diloco_quic_twoproc_server.%s.run.log", out_dir, label);
    snprintf(client_log, sizeof(client_log), "%s/diloco_quic_twoproc_client.%s.run.log", out_dir, label);
    snprintf(ready_path, sizeof(ready_path), "%s/diloco_quic_twoproc_server.%s.ready", out_dir, label);
    unlink(ready_path);
    if (!v3_compile_host_fixture_with_backend_driver(compiler_bin, root_diloco, server_src, server_bin, server_log) ||
        !v3_compile_host_fixture_with_backend_driver(compiler_bin, root_diloco, client_src, client_bin, client_log)) {
        return 1;
    }
    snprintf(ready_arg, sizeof(ready_arg), "--ready-path:%s", ready_path);
    server_argv[0] = server_bin;
    server_argv[1] = "--port:0";
    server_argv[2] = ready_arg;
    server_argv[3] = NULL;
    if (!v3_spawn_binary_capture_output(server_argv, server_log, &server_pid) ||
        !v3_wait_ready_port_file("verify-diloco server", ready_path, server_pid, 20000, port_text, sizeof(port_text)) ||
        !v3_text_is_decimal_digits(port_text)) {
        v3_kill_and_wait_pid(&server_pid);
        return 1;
    }
    snprintf(port_arg, sizeof(port_arg), "--port:%s", port_text);
    client_argv[0] = client_bin;
    client_argv[1] = port_arg;
    client_argv[2] = NULL;
    if (!v3_run_binary_capture_output(client_argv, client_log, &status) ||
        !v3_status_is_exit_code(status, 0) ||
        !v3_wait_pid_success(&server_pid, "verify-diloco server", server_log)) {
        v3_kill_and_wait_pid(&server_pid);
        return 1;
    }
    if (!v3_dump_file_stdout(server_log) || !v3_dump_file_stdout(client_log)) {
        return 1;
    }
    printf("v3 diloco_quic_twoproc_smoke ok (%s)\n", label);
    return 0;
}

static int v3_verify_diloco_stage(const char *compiler_bin,
                                  const char *label,
                                  const char *root,
                                  const char *root_diloco) {
    char out_dir[PATH_MAX];
    char state_src[PATH_MAX];
    char wire_src[PATH_MAX];
    char state_bin[PATH_MAX];
    char wire_bin[PATH_MAX];
    char state_label[256];
    char wire_label[256];
    v3_join_path(out_dir, sizeof(out_dir), root, "artifacts/v3_diloco");
    if (!v3_mkdir_p(out_dir)) {
        return 1;
    }
    v3_join_path(state_src, sizeof(state_src), root_diloco, "src/tests/diloco_state_smoke.cheng");
    v3_join_path(wire_src, sizeof(wire_src), root_diloco, "src/tests/diloco_wire_smoke.cheng");
    snprintf(state_bin, sizeof(state_bin), "%s/diloco_state_smoke.%s", out_dir, label);
    snprintf(wire_bin, sizeof(wire_bin), "%s/diloco_wire_smoke.%s", out_dir, label);
    snprintf(state_label, sizeof(state_label), "diloco_state_smoke.%s", label);
    snprintf(wire_label, sizeof(wire_label), "diloco_wire_smoke.%s", label);
    if (!v3_run_diloco_fixture_direct("v3 verify-diloco",
                                      state_label,
                                      compiler_bin,
                                      root_diloco,
                                      state_src,
                                      state_bin)) {
        return 1;
    }
    if (!v3_run_diloco_fixture_direct("v3 verify-diloco",
                                      wire_label,
                                      compiler_bin,
                                      root_diloco,
                                      wire_src,
                                      wire_bin)) {
        return 1;
    }
    return v3_run_diloco_quic_twoproc_direct(compiler_bin, label, root, root_diloco);
}

static int v3_cmd_verify_diloco_impl(int argc, char **argv) {
    V3BootstrapPaths paths;
    char root_diloco[PATH_MAX];
    char package_manifest[PATH_MAX];
    char saved_incremental[64];
    char saved_multi_cache[64];
    bool had_incremental = false;
    bool had_multi_cache = false;
    int rc;
    (void)argc;
    (void)argv;
    v3_bootstrap_paths_init(&paths);
    if (paths.root[0] == '\0') {
        fprintf(stderr, "[cheng_v3_seed] verify-diloco root detect failed\n");
        return 1;
    }
    v3_join_path(root_diloco, sizeof(root_diloco), paths.root, "v3/DiLoCo");
    v3_join_path(package_manifest, sizeof(package_manifest), root_diloco, "cheng-package.toml");
    if (access(package_manifest, R_OK) != 0) {
        fprintf(stderr, "[cheng_v3_seed] verify-diloco missing package root: %s\n", root_diloco);
        return 1;
    }
    if (!v3_leaf_expect_compiler("verify-diloco host", paths.stage3)) {
        return 1;
    }
    if ((rc = v3_verify_diloco_stage(paths.stage3, "host", paths.root, root_diloco)) != 0) {
        return rc;
    }
    if (!v3_leaf_expect_compiler("verify-diloco stage2", paths.stage2) ||
        !v3_leaf_expect_compiler("verify-diloco stage3", paths.stage3)) {
        return 1;
    }
    v3_save_env_value("BACKEND_INCREMENTAL", saved_incremental, sizeof(saved_incremental), &had_incremental);
    v3_save_env_value("BACKEND_MULTI_MODULE_CACHE", saved_multi_cache, sizeof(saved_multi_cache), &had_multi_cache);
    setenv("BACKEND_INCREMENTAL", "0", 1);
    setenv("BACKEND_MULTI_MODULE_CACHE", "0", 1);
    rc = v3_verify_diloco_stage(paths.stage2, "stage2", paths.root, root_diloco);
    if (rc == 0) {
        rc = v3_verify_diloco_stage(paths.stage3, "stage3", paths.root, root_diloco);
    }
    v3_restore_env_value("BACKEND_INCREMENTAL", saved_incremental, had_incremental);
    v3_restore_env_value("BACKEND_MULTI_MODULE_CACHE", saved_multi_cache, had_multi_cache);
    if (rc != 0) {
        return rc;
    }
    puts("v3 verify-diloco ok");
    return 0;
}

static int v3_cmd_run_udp_importc_smoke_impl(int argc, char **argv) {
    V3BootstrapPaths paths;
    char root_v3[PATH_MAX];
    char out_dir[PATH_MAX];
    char src[PATH_MAX];
    char out_bin[PATH_MAX];
    char compile_log[PATH_MAX];
    char run_log[PATH_MAX];
    char *run_argv[2];
    const char *compiler_bin;
    const char *label;
    int status = 0;
    if (!v3_leaf_prepare_backend_driver(&paths, root_v3, sizeof(root_v3))) {
        return 1;
    }
    compiler_bin = v3_leaf_resolve_compiler_bin(argc,
                                                argv,
                                                "CHENG_V3_SMOKE_COMPILER",
                                                NULL,
                                                paths.backend_driver_out);
    label = v3_leaf_resolve_label(argc, argv, "CHENG_V3_SMOKE_LABEL", "host");
    if (!v3_leaf_expect_compiler("run-udp-importc-smoke", compiler_bin)) {
        return 1;
    }
    v3_join_path(src, sizeof(src), paths.root, "v3/src/tests/udp_importc_smoke.cheng");
    v3_join_path(out_dir, sizeof(out_dir), paths.root, "artifacts/v3_udp_importc");
    if (!v3_mkdir_p(out_dir)) {
        return 1;
    }
    snprintf(out_bin, sizeof(out_bin), "%s/udp_importc_smoke.%s", out_dir, label);
    snprintf(compile_log, sizeof(compile_log), "%s/udp_importc_smoke.%s.compile.log", out_dir, label);
    snprintf(run_log, sizeof(run_log), "%s/udp_importc_smoke.%s.run.log", out_dir, label);
    if (!v3_compile_host_fixture_with_backend_driver(compiler_bin, root_v3, src, out_bin, compile_log)) {
        return 1;
    }
    run_argv[0] = out_bin;
    run_argv[1] = NULL;
    if (!v3_run_binary_capture_output(run_argv, run_log, &status) ||
        !v3_status_is_exit_code(status, 0)) {
        fprintf(stderr, "[cheng_v3_seed] run-udp-importc-smoke failed log=%s\n", run_log);
        return 1;
    }
    return v3_dump_file_stdout(run_log) ? 0 : 1;
}

static int v3_cmd_run_wasm_smokes_impl(int argc, char **argv) {
    V3BootstrapPaths paths;
    char root_v3[PATH_MAX];
    char out_dir[PATH_MAX];
    char js_path[PATH_MAX];
    char js_log[PATH_MAX];
    char out_zero[PATH_MAX];
    char out_call[PATH_MAX];
    char out_importc[PATH_MAX];
    char out_control[PATH_MAX];
    char out_composite[PATH_MAX];
    char out_func_block[PATH_MAX];
    char log_zero[PATH_MAX];
    char log_call[PATH_MAX];
    char log_importc[PATH_MAX];
    char log_control[PATH_MAX];
    char log_composite[PATH_MAX];
    char log_func_block[PATH_MAX];
    char src_zero[PATH_MAX];
    char src_call[PATH_MAX];
    char src_importc[PATH_MAX];
    char src_control[PATH_MAX];
    char src_composite[PATH_MAX];
    char src_func_block[PATH_MAX];
    const char *compiler_bin;
    const char *label;
    char *js_text;
    char *run_argv[10];
    int status = 0;
    if (!v3_leaf_prepare_backend_driver(&paths, root_v3, sizeof(root_v3))) {
        return 1;
    }
    compiler_bin = v3_leaf_resolve_compiler_bin(argc,
                                                argv,
                                                "CHENG_V3_WASM_COMPILER",
                                                "CHENG_V3_SMOKE_COMPILER",
                                                paths.backend_driver_out);
    label = v3_leaf_resolve_label(argc, argv, "CHENG_V3_WASM_LABEL", "host");
    if (!v3_leaf_expect_compiler("run-wasm-smokes", compiler_bin)) {
        return 1;
    }
    v3_join_path(out_dir, sizeof(out_dir), paths.root, "artifacts/v3_wasm_smokes");
    if (!v3_mkdir_p(out_dir)) {
        return 1;
    }
    v3_join_path(src_zero, sizeof(src_zero), paths.root, "v3/src/tests/wasm_zero_smoke.cheng");
    v3_join_path(src_call, sizeof(src_call), paths.root, "v3/src/tests/wasm_internal_call_smoke.cheng");
    v3_join_path(src_importc, sizeof(src_importc), paths.root, "v3/src/tests/wasm_importc_noarg_i32_smoke.cheng");
    v3_join_path(src_control, sizeof(src_control), paths.root, "v3/src/tests/wasm_scalar_control_flow_smoke.cheng");
    v3_join_path(src_composite, sizeof(src_composite), paths.root, "v3/src/tests/wasm_composite_field_smoke.cheng");
    v3_join_path(src_func_block, sizeof(src_func_block), paths.root, "v3/src/tests/wasm_func_block_shared_smoke.cheng");
    snprintf(out_zero, sizeof(out_zero), "%s/wasm_zero_smoke.%s.wasm", out_dir, label);
    snprintf(out_call, sizeof(out_call), "%s/wasm_internal_call_smoke.%s.wasm", out_dir, label);
    snprintf(out_importc, sizeof(out_importc), "%s/wasm_importc_noarg_i32_smoke.%s.wasm", out_dir, label);
    snprintf(out_control, sizeof(out_control), "%s/wasm_scalar_control_flow_smoke.%s.wasm", out_dir, label);
    snprintf(out_composite, sizeof(out_composite), "%s/wasm_composite_field_smoke.%s.wasm", out_dir, label);
    snprintf(out_func_block, sizeof(out_func_block), "%s/wasm_func_block_shared_smoke.%s.wasm", out_dir, label);
    snprintf(log_zero, sizeof(log_zero), "%s/wasm_zero_smoke.%s.compile.log", out_dir, label);
    snprintf(log_call, sizeof(log_call), "%s/wasm_internal_call_smoke.%s.compile.log", out_dir, label);
    snprintf(log_importc, sizeof(log_importc), "%s/wasm_importc_noarg_i32_smoke.%s.compile.log", out_dir, label);
    snprintf(log_control, sizeof(log_control), "%s/wasm_scalar_control_flow_smoke.%s.compile.log", out_dir, label);
    snprintf(log_composite, sizeof(log_composite), "%s/wasm_composite_field_smoke.%s.compile.log", out_dir, label);
    snprintf(log_func_block, sizeof(log_func_block), "%s/wasm_func_block_shared_smoke.%s.compile.log", out_dir, label);
    if (!v3_compile_fixture_with_backend_driver_emit_target(compiler_bin, root_v3, src_zero, "exe", "wasm32-unknown-unknown", out_zero, log_zero, NULL) ||
        !v3_compile_fixture_with_backend_driver_emit_target(compiler_bin, root_v3, src_call, "exe", "wasm32-unknown-unknown", out_call, log_call, NULL) ||
        !v3_compile_fixture_with_backend_driver_emit_target(compiler_bin, root_v3, src_importc, "exe", "wasm32-unknown-unknown", out_importc, log_importc, NULL) ||
        !v3_compile_fixture_with_backend_driver_emit_target(compiler_bin, root_v3, src_control, "exe", "wasm32-unknown-unknown", out_control, log_control, NULL) ||
        !v3_compile_fixture_with_backend_driver_emit_target(compiler_bin, root_v3, src_composite, "shared", "wasm32-unknown-unknown", out_composite, log_composite, NULL) ||
        !v3_compile_fixture_with_backend_driver_emit_target(compiler_bin, root_v3, src_func_block, "shared", "wasm32-unknown-unknown", out_func_block, log_func_block, NULL)) {
        return 1;
    }
    snprintf(js_path, sizeof(js_path), "%s/run_wasm_smokes.%s.js", out_dir, label);
    snprintf(js_log, sizeof(js_log), "%s/run_wasm_smokes.%s.run.log", out_dir, label);
    {
        static const char *const JS_PARTS[] = {
            "const fs = require(\"fs\");\n",
            "const outZero = process.argv[2];\n",
            "const outCall = process.argv[3];\n",
            "const outImportc = process.argv[4];\n",
            "const outControl = process.argv[5];\n",
            "const outComposite = process.argv[6];\n",
            "const outFuncBlock = process.argv[7];\n",
            "async function run(name, file, imports) {\n",
            "  const wasm = fs.readFileSync(file);\n",
            "  const env = Object.assign({}, (imports && imports.env) || {});\n",
            "  if (!(env.memory instanceof WebAssembly.Memory)) env.memory = new WebAssembly.Memory({ initial: 32, maximum: 512 });\n",
            "  const resolvedImports = Object.assign({}, imports || {}, { env });\n",
            "  const { instance } = await WebAssembly.instantiate(wasm, resolvedImports);\n",
            "  const rc = instance.exports.main();\n",
            "  if ((rc | 0) !== 0) throw new Error(name + ' returned ' + rc);\n",
            "  console.log(name + ': ok');\n",
            "}\n",
            "(async () => {\n",
            "  await run('wasm_zero_smoke', outZero, { env: {} });\n",
            "  await run('wasm_internal_call_smoke', outCall, { env: {} });\n",
            "  await run('wasm_importc_noarg_i32_smoke', outImportc, { env: { cheng_test_zero: () => 0 } });\n",
            "  await run('wasm_scalar_control_flow_smoke', outControl, { env: {} });\n",
            "  {\n",
            "    const wasm = fs.readFileSync(outComposite);\n",
            "    const memory = new WebAssembly.Memory({ initial: 32, maximum: 512 });\n",
            "    const { instance } = await WebAssembly.instantiate(wasm, { env: { memory } });\n",
            "    const view = new DataView(memory.buffer);\n",
            "    const bytesPtr = 64;\n",
            "    const textPtr = 96;\n",
            "    const seqPtr = 128;\n",
            "    const resultPtr = 160;\n",
            "    view.setUint32(bytesPtr + 0, 900, true);\n",
            "    view.setInt32(bytesPtr + 4, 7, true);\n",
            "    view.setUint32(textPtr + 0, 920, true);\n",
            "    view.setInt32(textPtr + 4, 11, true);\n",
            "    view.setInt32(textPtr + 8, 0, true);\n",
            "    view.setInt32(textPtr + 12, 0, true);\n",
            "    view.setInt32(seqPtr + 0, 5, true);\n",
            "    view.setInt32(seqPtr + 4, 8, true);\n",
            "    view.setUint32(seqPtr + 8, 960, true);\n",
            "    view.setUint8(resultPtr + 0, 1);\n",
            "    view.setInt32(resultPtr + 4, 1234, true);\n",
            "    if (typeof instance.exports.bytesLen !== 'function') throw new Error('wasm_composite_field_smoke missing bytesLen');\n",
            "    if (typeof instance.exports.textLen !== 'function') throw new Error('wasm_composite_field_smoke missing textLen');\n",
            "    if (typeof instance.exports.seqLen !== 'function') throw new Error('wasm_composite_field_smoke missing seqLen');\n",
            "    if (typeof instance.exports.resultOk !== 'function') throw new Error('wasm_composite_field_smoke missing resultOk');\n",
            "    if (typeof instance.exports.resultValue !== 'function') throw new Error('wasm_composite_field_smoke missing resultValue');\n",
            "    if (typeof instance.exports.echoBytes !== 'function') throw new Error('wasm_composite_field_smoke missing echoBytes');\n",
            "    if (typeof instance.exports.echoText !== 'function') throw new Error('wasm_composite_field_smoke missing echoText');\n",
            "    if (typeof instance.exports.echoSeq !== 'function') throw new Error('wasm_composite_field_smoke missing echoSeq');\n",
            "    if (typeof instance.exports.echoResult !== 'function') throw new Error('wasm_composite_field_smoke missing echoResult');\n",
            "    const bytesRet = instance.exports.echoBytes(bytesPtr) | 0;\n",
            "    const textRet = instance.exports.echoText(textPtr) | 0;\n",
            "    const seqRet = instance.exports.echoSeq(seqPtr) | 0;\n",
            "    const resultRet = instance.exports.echoResult(resultPtr) | 0;\n",
            "    if ((instance.exports.bytesLen(bytesPtr) | 0) !== 7) throw new Error('wasm_composite_field_smoke bytesLen mismatch');\n",
            "    if ((instance.exports.textLen(textPtr) | 0) !== 11) throw new Error('wasm_composite_field_smoke textLen mismatch');\n",
            "    if ((instance.exports.seqLen(seqPtr) | 0) !== 5) throw new Error('wasm_composite_field_smoke seqLen mismatch');\n",
            "    if ((instance.exports.resultOk(resultPtr) | 0) !== 1) throw new Error('wasm_composite_field_smoke resultOk mismatch');\n",
            "    if ((instance.exports.resultValue(resultPtr) | 0) !== 1234) throw new Error('wasm_composite_field_smoke resultValue mismatch');\n",
            "    if ((instance.exports.bytesLen(bytesRet) | 0) !== 7) throw new Error('wasm_composite_field_smoke echoBytes mismatch');\n",
            "    if ((instance.exports.textLen(textRet) | 0) !== 11) throw new Error('wasm_composite_field_smoke echoText mismatch');\n",
            "    if ((instance.exports.seqLen(seqRet) | 0) !== 5) throw new Error('wasm_composite_field_smoke echoSeq mismatch');\n",
            "    if ((instance.exports.resultOk(resultRet) | 0) !== 1) throw new Error('wasm_composite_field_smoke echoResult ok mismatch');\n",
            "    if ((instance.exports.resultValue(resultRet) | 0) !== 1234) throw new Error('wasm_composite_field_smoke echoResult value mismatch');\n",
            "  }\n",
            "  {\n",
            "    const wasm = fs.readFileSync(outFuncBlock);\n",
            "    const memory = new WebAssembly.Memory({ initial: 32, maximum: 512 });\n",
            "    const { instance } = await WebAssembly.instantiate(wasm, { env: { memory } });\n",
            "    if (typeof instance.exports.constantSeven !== 'function') throw new Error('wasm_func_block_shared_smoke missing constantSeven');\n",
            "    if ((instance.exports.constantSeven() | 0) !== 7) throw new Error('wasm_func_block_shared_smoke value mismatch');\n",
            "  }\n",
            "  console.log('v3 wasm smokes: ok');\n",
            "})().catch((err) => { console.error(err); process.exit(1); });\n"
        };
        js_text = v3_strdup_parts(JS_PARTS, sizeof(JS_PARTS) / sizeof(JS_PARTS[0]));
    }
    if (!v3_write_text_file(js_path, js_text)) {
        free(js_text);
        return 1;
    }
    free(js_text);
    run_argv[0] = "/usr/bin/env";
    run_argv[1] = "node";
    run_argv[2] = js_path;
    run_argv[3] = out_zero;
    run_argv[4] = out_call;
    run_argv[5] = out_importc;
    run_argv[6] = out_control;
    run_argv[7] = out_composite;
    run_argv[8] = out_func_block;
    run_argv[9] = NULL;
    if (!v3_run_binary_capture_output(run_argv, js_log, &status) ||
        !v3_status_is_exit_code(status, 0)) {
        fprintf(stderr, "[cheng_v3_seed] run-wasm-smokes node failed log=%s\n", js_log);
        return 1;
    }
    return v3_dump_file_stdout(js_log) ? 0 : 1;
}

static int v3_cmd_run_browser_host_wasm_smoke_impl(int argc, char **argv) {
    return v3_require_backend_driver_cli_passthrough("run-browser-host-wasm-smoke", argc, argv);
}

static int v3_cmd_run_fresh_node_selfhost_gate_impl(int argc, char **argv) {
    V3BootstrapPaths paths;
    char root_v3[PATH_MAX];
    char out_dir[PATH_MAX];
    char src[PATH_MAX];
    char sync_prefix[PATH_MAX];
    char fresh_prefix[PATH_MAX];
    char sync_log[PATH_MAX];
    char fresh_log[PATH_MAX];
    const char *compiler_bin;
    const char *label;
    const char *sync_args[7];
    const char *fresh_args[10];
    if (!v3_leaf_prepare_backend_driver(&paths, root_v3, sizeof(root_v3))) {
        return 1;
    }
    compiler_bin = v3_leaf_resolve_compiler_bin(argc,
                                                argv,
                                                "CHENG_V3_FRESH_NODE_COMPILER",
                                                "CHENG_V3_SMOKE_COMPILER",
                                                paths.backend_driver_out);
    label = v3_leaf_resolve_label(argc, argv, "CHENG_V3_FRESH_NODE_LABEL", "host");
    if (!v3_leaf_expect_compiler("run-fresh-node-selfhost-gate", compiler_bin)) {
        return 1;
    }
    v3_join_path(src, sizeof(src), paths.root, "v3/src/tests/program_selfhost_smoke.cheng");
    v3_join_path(out_dir, sizeof(out_dir), paths.root, "artifacts/v3_fresh_node_gate");
    v3_join_path(out_dir, sizeof(out_dir), out_dir, label);
    if (!v3_mkdir_p(out_dir)) {
        return 1;
    }
    v3_join_path(sync_prefix, sizeof(sync_prefix), out_dir, "program_selfhost_smoke.sync");
    v3_join_path(fresh_prefix, sizeof(fresh_prefix), out_dir, "program_selfhost_smoke.fresh");
    v3_join_path(sync_log, sizeof(sync_log), out_dir, "world_sync.log");
    v3_join_path(fresh_log, sizeof(fresh_log), out_dir, "fresh_node.log");
    {
        const char *sync_args_full[6];
        char root_flag[PATH_MAX + 16];
        char in_flag[PATH_MAX + 16];
        char out_flag[PATH_MAX + 16];
        size_t i;
        snprintf(root_flag, sizeof(root_flag), "--root:%s", root_v3);
        snprintf(in_flag, sizeof(in_flag), "--in:%s", src);
        sync_args[0] = "world-sync";
        sync_args[1] = root_flag;
        sync_args[2] = in_flag;
        sync_args[3] = "--target:arm64-apple-darwin";
        sync_args[4] = "--channel:stable";
        for (i = 0U; i < 5U; ++i) {
            sync_args_full[i] = sync_args[i];
        }
        snprintf(out_flag, sizeof(out_flag), "--out:%s", sync_prefix);
        sync_args_full[5] = out_flag;
        if (!v3_run_compiler_command_logged(compiler_bin, 6U, sync_args_full, sync_log, "run-fresh-node-selfhost-gate world-sync")) {
            return 1;
        }
    }
    {
        char world_report[PATH_MAX];
        char compiler_snapshot[PATH_MAX];
        char std_snapshot[PATH_MAX];
        char runtime_snapshot[PATH_MAX];
        char csg_report[PATH_MAX];
        char surface_report[PATH_MAX];
        char receipt_report[PATH_MAX];
        char lock_report[PATH_MAX];
        snprintf(world_report, sizeof(world_report), "%s.world.txt", sync_prefix);
        snprintf(compiler_snapshot, sizeof(compiler_snapshot), "%s.compiler.snapshot.txt", sync_prefix);
        snprintf(std_snapshot, sizeof(std_snapshot), "%s.std.snapshot.txt", sync_prefix);
        snprintf(runtime_snapshot, sizeof(runtime_snapshot), "%s.runtime.snapshot.txt", sync_prefix);
        snprintf(csg_report, sizeof(csg_report), "%s.csg.txt", sync_prefix);
        snprintf(surface_report, sizeof(surface_report), "%s.surface.txt", sync_prefix);
        snprintf(receipt_report, sizeof(receipt_report), "%s.receipt.txt", sync_prefix);
        snprintf(lock_report, sizeof(lock_report), "%s.lock.toml", sync_prefix);
        if (!v3_expect_file_exists_nonempty("run-fresh-node-selfhost-gate world report", world_report) ||
            !v3_expect_file_exists_nonempty("run-fresh-node-selfhost-gate compiler snapshot", compiler_snapshot) ||
            !v3_expect_file_exists_nonempty("run-fresh-node-selfhost-gate std snapshot", std_snapshot) ||
            !v3_expect_file_exists_nonempty("run-fresh-node-selfhost-gate runtime snapshot", runtime_snapshot) ||
            !v3_expect_file_exists_nonempty("run-fresh-node-selfhost-gate csg", csg_report) ||
            !v3_expect_file_exists_nonempty("run-fresh-node-selfhost-gate surface", surface_report) ||
            !v3_expect_file_exists_nonempty("run-fresh-node-selfhost-gate receipt", receipt_report) ||
            !v3_expect_file_exists_nonempty("run-fresh-node-selfhost-gate lock", lock_report)) {
            return 1;
        }
    }
    {
        char baseline_csg[PATH_MAX + 24];
        char baseline_surface[PATH_MAX + 28];
        char baseline_receipt[PATH_MAX + 28];
        char root_flag[PATH_MAX + 16];
        char in_flag[PATH_MAX + 16];
        const char *fresh_args_full[9];
        char out_flag[PATH_MAX + 16];
        size_t i;
        snprintf(baseline_csg, sizeof(baseline_csg), "--baseline-csg:%s.csg.txt", sync_prefix);
        snprintf(baseline_surface, sizeof(baseline_surface), "--baseline-surface:%s.surface.txt", sync_prefix);
        snprintf(baseline_receipt, sizeof(baseline_receipt), "--baseline-receipt:%s.receipt.txt", sync_prefix);
        snprintf(root_flag, sizeof(root_flag), "--root:%s", root_v3);
        snprintf(in_flag, sizeof(in_flag), "--in:%s", src);
        fresh_args[0] = "fresh-node-selfhost";
        fresh_args[1] = root_flag;
        fresh_args[2] = in_flag;
        fresh_args[3] = "--target:arm64-apple-darwin";
        fresh_args[4] = "--channel:stable";
        fresh_args[5] = baseline_csg;
        fresh_args[6] = baseline_surface;
        fresh_args[7] = baseline_receipt;
        for (i = 0U; i < 8U; ++i) {
            fresh_args_full[i] = fresh_args[i];
        }
        snprintf(out_flag, sizeof(out_flag), "--out:%s", fresh_prefix);
        fresh_args_full[8] = out_flag;
        if (!v3_run_compiler_command_logged(compiler_bin, 9U, fresh_args_full, fresh_log, "run-fresh-node-selfhost-gate fresh-node-selfhost")) {
            return 1;
        }
    }
    {
        char *fresh_text = v3_read_file(fresh_log);
        char fresh_report[PATH_MAX];
        if (fresh_text == NULL ||
            !v3_expect_text_has_line("run-fresh-node-selfhost-gate", fresh_text, "equivalence_passed=1") ||
            !v3_expect_text_has_line("run-fresh-node-selfhost-gate", fresh_text, "publish_allowed=1") ||
            !v3_expect_text_has_line("run-fresh-node-selfhost-gate", fresh_text, "receipt_equivalent=1") ||
            !v3_expect_text_has_line("run-fresh-node-selfhost-gate", fresh_text, "receipt_cid_match=1")) {
            free(fresh_text);
            return 1;
        }
        free(fresh_text);
        snprintf(fresh_report, sizeof(fresh_report), "%s.fresh.txt", fresh_prefix);
        if (!v3_expect_file_exists_nonempty("run-fresh-node-selfhost-gate fresh report", fresh_report)) {
            return 1;
        }
    }
    if (!v3_dump_file_stdout(sync_log) || !v3_dump_file_stdout(fresh_log)) {
        return 1;
    }
    printf("v3 fresh-node selfhost gate ok (%s)\n", label);
    return 0;
}

static int v3_cmd_run_migration_gate_impl(int argc, char **argv) {
    V3BootstrapPaths paths;
    char root_v3[PATH_MAX];
    char out_dir[PATH_MAX];
    char current_src[PATH_MAX];
    char legacy_src[PATH_MAX];
    char migrate_prefix[PATH_MAX];
    char proof_prefix[PATH_MAX];
    char blocked_prefix[PATH_MAX];
    char publish_prefix[PATH_MAX];
    char migrate_log[PATH_MAX];
    char prove_log[PATH_MAX];
    char blocked_log[PATH_MAX];
    char publish_log[PATH_MAX];
    const char *compiler_bin;
    const char *label;
    if (!v3_leaf_prepare_backend_driver(&paths, root_v3, sizeof(root_v3))) {
        return 1;
    }
    compiler_bin = v3_leaf_resolve_compiler_bin(argc,
                                                argv,
                                                "CHENG_V3_MIGRATION_COMPILER",
                                                "CHENG_V3_SMOKE_COMPILER",
                                                paths.backend_driver_out);
    label = v3_leaf_resolve_label(argc, argv, "CHENG_V3_MIGRATION_LABEL", "host");
    if (!v3_leaf_expect_compiler("run-migration-gate", compiler_bin)) {
        return 1;
    }
    v3_join_path(current_src, sizeof(current_src), paths.root, "v3/src/tests/compiler_migration_fixture.cheng");
    v3_join_path(legacy_src, sizeof(legacy_src), paths.root, "v3/testdata/compiler_migration/compiler_migration_fixture_legacy.cheng");
    v3_join_path(out_dir, sizeof(out_dir), paths.root, "artifacts/v3_migration_gate");
    v3_join_path(out_dir, sizeof(out_dir), out_dir, label);
    if (!v3_mkdir_p(out_dir)) {
        return 1;
    }
    v3_join_path(migrate_prefix, sizeof(migrate_prefix), out_dir, "compiler_migration_fixture.migrate");
    v3_join_path(proof_prefix, sizeof(proof_prefix), out_dir, "compiler_migration_fixture.prove");
    v3_join_path(blocked_prefix, sizeof(blocked_prefix), out_dir, "compiler_migration_fixture.blocked");
    v3_join_path(publish_prefix, sizeof(publish_prefix), out_dir, "compiler_migration_fixture.publish");
    v3_join_path(migrate_log, sizeof(migrate_log), out_dir, "migrate_csg.log");
    v3_join_path(prove_log, sizeof(prove_log), out_dir, "prove_migration.log");
    v3_join_path(blocked_log, sizeof(blocked_log), out_dir, "publish_blocked.log");
    v3_join_path(publish_log, sizeof(publish_log), out_dir, "publish_migration.log");
    {
        char out_flag[PATH_MAX + 16];
        snprintf(out_flag, sizeof(out_flag), "--out:%s", migrate_prefix);
        {
            char root_flag[PATH_MAX + 16];
            char in_flag[PATH_MAX + 16];
            char legacy_flag[PATH_MAX + 16];
            snprintf(root_flag, sizeof(root_flag), "--root:%s", root_v3);
            snprintf(in_flag, sizeof(in_flag), "--in:%s", current_src);
            snprintf(legacy_flag, sizeof(legacy_flag), "--legacy-in:%s", legacy_src);
            if (!v3_run_compiler_command_logged(compiler_bin,
                                                5U,
                                                (const char *[5]){ "migrate-csg", root_flag, in_flag, legacy_flag, out_flag },
                                                migrate_log,
                                                "run-migration-gate migrate-csg")) {
                return 1;
            }
        }
    }
    {
        char *text = v3_read_file(migrate_log);
        char path_buf[PATH_MAX];
        if (text == NULL || !v3_expect_text_contains("run-migration-gate migrate", text, "canonical_graph_cid=")) {
            free(text);
            return 1;
        }
        free(text);
        snprintf(path_buf, sizeof(path_buf), "%s.migration.txt", migrate_prefix);
        if (!v3_expect_file_exists_nonempty("run-migration-gate migration report", path_buf)) {
            return 1;
        }
        snprintf(path_buf, sizeof(path_buf), "%s.migration.csg.txt", migrate_prefix);
        if (!v3_expect_file_exists_nonempty("run-migration-gate migration csg", path_buf)) {
            return 1;
        }
        snprintf(path_buf, sizeof(path_buf), "%s.migration.surface.txt", migrate_prefix);
        if (!v3_expect_file_exists_nonempty("run-migration-gate migration surface", path_buf)) {
            return 1;
        }
        snprintf(path_buf, sizeof(path_buf), "%s.migration.receipt.txt", migrate_prefix);
        if (!v3_expect_file_exists_nonempty("run-migration-gate migration receipt", path_buf)) {
            return 1;
        }
    }
    {
        char out_flag[PATH_MAX + 16];
        snprintf(out_flag, sizeof(out_flag), "--out:%s", proof_prefix);
        {
            char root_flag[PATH_MAX + 16];
            char in_flag[PATH_MAX + 16];
            char legacy_flag[PATH_MAX + 16];
            snprintf(root_flag, sizeof(root_flag), "--root:%s", root_v3);
            snprintf(in_flag, sizeof(in_flag), "--in:%s", current_src);
            snprintf(legacy_flag, sizeof(legacy_flag), "--legacy-in:%s", legacy_src);
            if (!v3_run_compiler_command_logged(compiler_bin,
                                                7U,
                                                (const char *[7]){ "prove-migration", root_flag, in_flag, legacy_flag, "--target:arm64-apple-darwin", "--channel:stable", out_flag },
                                                prove_log,
                                                "run-migration-gate prove-migration")) {
                return 1;
            }
        }
    }
    {
        char *text = v3_read_file(prove_log);
        char path_buf[PATH_MAX];
        if (text == NULL ||
            !v3_expect_text_has_line("run-migration-gate prove", text, "equivalence_kind=canonical_semantic_identity") ||
            !v3_expect_text_has_line("run-migration-gate prove", text, "graph_equivalent=1") ||
            !v3_expect_text_has_line("run-migration-gate prove", text, "export_surface_compatible=1") ||
            !v3_expect_text_has_line("run-migration-gate prove", text, "compile_receipt_equivalent=1")) {
            free(text);
            return 1;
        }
        free(text);
        snprintf(path_buf, sizeof(path_buf), "%s.proof.txt", proof_prefix);
        if (!v3_expect_file_exists_nonempty("run-migration-gate proof", path_buf)) {
            return 1;
        }
    }
    {
        char out_flag[PATH_MAX + 16];
        snprintf(out_flag, sizeof(out_flag), "--out:%s", blocked_prefix);
        {
            char root_flag[PATH_MAX + 16];
            char in_flag[PATH_MAX + 16];
            snprintf(root_flag, sizeof(root_flag), "--root:%s", root_v3);
            snprintf(in_flag, sizeof(in_flag), "--in:%s", current_src);
            if (!v3_run_command_logged_argv_quiet(7U,
                                                  (const char *[7]){ compiler_bin, "publish-world", root_flag, in_flag, "--target:arm64-apple-darwin", out_flag, "--channel:stable" },
                                                  blocked_log,
                                                  "run-migration-gate publish-blocked")) {
                char *blocked_text = v3_read_file(blocked_log);
                bool ok = blocked_text != NULL &&
                          (v3_text_contains_substring(blocked_text, "stable requires") ||
                           v3_text_contains_substring(blocked_text, "publish blocked") ||
                           v3_text_contains_substring(blocked_text, "missing --baseline"));
                free(blocked_text);
                if (!ok) {
                    return 1;
                }
            } else {
                fprintf(stderr, "[cheng_v3_seed] run-migration-gate stable publish unexpectedly passed\n");
                return 1;
            }
        }
    }
    {
        char out_flag[PATH_MAX + 16];
        snprintf(out_flag, sizeof(out_flag), "--out:%s", publish_prefix);
        {
            char root_flag[PATH_MAX + 16];
            char in_flag[PATH_MAX + 16];
            char legacy_flag[PATH_MAX + 16];
            snprintf(root_flag, sizeof(root_flag), "--root:%s", root_v3);
            snprintf(in_flag, sizeof(in_flag), "--in:%s", current_src);
            snprintf(legacy_flag, sizeof(legacy_flag), "--legacy-in:%s", legacy_src);
            if (!v3_run_compiler_command_logged(compiler_bin,
                                                8U,
                                                (const char *[8]){ "publish-world", root_flag, in_flag, legacy_flag, "--target:arm64-apple-darwin", out_flag, "--channel:stable" },
                                                publish_log,
                                                "run-migration-gate publish-world")) {
                return 1;
            }
        }
    }
    {
        char *text = v3_read_file(publish_log);
        char path_buf[PATH_MAX];
        if (text == NULL ||
            !v3_expect_text_has_line("run-migration-gate publish", text, "proof_mode=migration") ||
            !v3_expect_text_has_line("run-migration-gate publish", text, "equivalence_passed=1") ||
            !v3_expect_text_has_line("run-migration-gate publish", text, "publish_allowed=1")) {
            free(text);
            return 1;
        }
        free(text);
        snprintf(path_buf, sizeof(path_buf), "%s.publish.txt", publish_prefix);
        if (!v3_expect_file_exists_nonempty("run-migration-gate publish proof", path_buf)) {
            return 1;
        }
    }
    if (!v3_dump_file_stdout(migrate_log) ||
        !v3_dump_file_stdout(prove_log) ||
        !v3_dump_file_stdout(blocked_log) ||
        !v3_dump_file_stdout(publish_log)) {
        return 1;
    }
    printf("v3 migration gate ok (%s)\n", label);
    return 0;
}

static int v3_cmd_run_browser_webrtc_smokes_impl(int argc, char **argv) {
    V3BootstrapPaths paths;
    char root_v3[PATH_MAX];
    char out_dir[PATH_MAX];
    char browser_dir[PATH_MAX];
    char npm_log[PATH_MAX];
    char playwright_log[PATH_MAX];
    char out_bin[PATH_MAX];
    char compile_log[PATH_MAX];
    char run_log[PATH_MAX];
    char src[PATH_MAX];
    char compile_label[PATH_MAX];
    const char *host_compiler;
    const char *tests[32];
    size_t test_count;
    size_t i;
    bool had_incremental = false;
    bool had_multi_cache = false;
    char saved_incremental[64];
    char saved_multi_cache[64];
    int status = 0;
    static const char *DEFAULT_TESTS[] = {
        "webrtc_signal_stream_smoke",
        "webrtc_datachannel_browser_smoke",
        "webrtc_browser_content_smoke",
        "libp2p_webrtc_sync_smoke",
        "webrtc_browser_pubsub_smoke",
        "webrtc_browser_light_node_smoke",
        "browser_sdk_smoke"
    };
    if (!v3_leaf_prepare_backend_driver(&paths, root_v3, sizeof(root_v3))) {
        return 1;
    }
    if (!v3_bootstrap_artifacts_fresh(&paths) && v3_cmd_bootstrap_bridge(0, NULL) != 0) {
        return 1;
    }
    host_compiler = v3_leaf_resolve_compiler_bin(argc,
                                                 argv,
                                                 "CHENG_V3_SMOKE_COMPILER",
                                                 NULL,
                                                 paths.backend_driver_out);
    if (!v3_leaf_expect_compiler("run-browser-webrtc-smokes host", host_compiler) ||
        !v3_leaf_expect_compiler("run-browser-webrtc-smokes stage2", paths.stage2) ||
        !v3_leaf_expect_compiler("run-browser-webrtc-smokes stage3", paths.stage3)) {
        return 1;
    }
    v3_join_path(browser_dir, sizeof(browser_dir), paths.root, "v3/tooling/browser_webrtc");
    v3_join_path(out_dir, sizeof(out_dir), paths.root, "artifacts/v3_browser_webrtc");
    if (!v3_mkdir_p(out_dir)) {
        return 1;
    }
    v3_join_path(npm_log, sizeof(npm_log), out_dir, "npm_install.log");
    v3_join_path(playwright_log, sizeof(playwright_log), out_dir, "playwright_install.log");
    {
        char playwright_dir[PATH_MAX];
        char *quoted_browser_dir;
        char *command;
        v3_join_path(playwright_dir, sizeof(playwright_dir), browser_dir, "node_modules/playwright");
        if (access(playwright_dir, R_OK) != 0) {
            quoted_browser_dir = v3_shell_quote(browser_dir);
            command = (char *)v3_xmalloc(strlen(quoted_browser_dir) + 96U);
            snprintf(command, strlen(quoted_browser_dir) + 96U, "cd %s && npm install --no-fund --no-audit", quoted_browser_dir);
            free(quoted_browser_dir);
            if (!v3_run_shell_command_logged(command, npm_log, "run-browser-webrtc-smokes npm-install")) {
                free(command);
                return 1;
            }
            free(command);
        }
        quoted_browser_dir = v3_shell_quote(browser_dir);
        command = (char *)v3_xmalloc(strlen(quoted_browser_dir) + 96U);
        snprintf(command, strlen(quoted_browser_dir) + 96U, "cd %s && npx playwright install chromium", quoted_browser_dir);
        free(quoted_browser_dir);
        if (!v3_run_shell_command_logged(command, playwright_log, "run-browser-webrtc-smokes playwright-install")) {
            free(command);
            return 1;
        }
        free(command);
    }
    test_count = v3_collect_smoke_positionals(argc, argv, tests, sizeof(tests) / sizeof(tests[0]));
    if (test_count == 0U) {
        for (i = 0U; i < sizeof(DEFAULT_TESTS) / sizeof(DEFAULT_TESTS[0]); ++i) {
            tests[i] = DEFAULT_TESTS[i];
        }
        test_count = sizeof(DEFAULT_TESTS) / sizeof(DEFAULT_TESTS[0]);
    }
    v3_save_env_value("BACKEND_INCREMENTAL", saved_incremental, sizeof(saved_incremental), &had_incremental);
    v3_save_env_value("BACKEND_MULTI_MODULE_CACHE", saved_multi_cache, sizeof(saved_multi_cache), &had_multi_cache);
    setenv("BACKEND_INCREMENTAL", "0", 1);
    setenv("BACKEND_MULTI_MODULE_CACHE", "0", 1);
    for (i = 0U; i < test_count; ++i) {
        snprintf(src, sizeof(src), "%s/v3/src/tests/%s.cheng", paths.root, tests[i]);
        snprintf(out_bin, sizeof(out_bin), "%s/%s.host", out_dir, tests[i]);
        snprintf(compile_log, sizeof(compile_log), "%s/%s.host.compile.log", out_dir, tests[i]);
        snprintf(run_log, sizeof(run_log), "%s/%s.host.run.log", out_dir, tests[i]);
        if (!v3_compile_host_fixture_with_backend_driver(host_compiler, root_v3, src, out_bin, compile_log)) {
            v3_restore_env_value("BACKEND_INCREMENTAL", saved_incremental, had_incremental);
            v3_restore_env_value("BACKEND_MULTI_MODULE_CACHE", saved_multi_cache, had_multi_cache);
            return 1;
        }
        {
            char *run_argv[2];
            run_argv[0] = out_bin;
            run_argv[1] = NULL;
            if (!v3_run_binary_capture_output(run_argv, run_log, &status) || !v3_status_is_exit_code(status, 0)) {
                v3_restore_env_value("BACKEND_INCREMENTAL", saved_incremental, had_incremental);
                v3_restore_env_value("BACKEND_MULTI_MODULE_CACHE", saved_multi_cache, had_multi_cache);
                return 1;
            }
        }
        if (!v3_dump_file_stdout(run_log)) {
            v3_restore_env_value("BACKEND_INCREMENTAL", saved_incremental, had_incremental);
            v3_restore_env_value("BACKEND_MULTI_MODULE_CACHE", saved_multi_cache, had_multi_cache);
            return 1;
        }
        snprintf(out_bin, sizeof(out_bin), "%s/%s.stage2", out_dir, tests[i]);
        snprintf(compile_label, sizeof(compile_label), "stage2 %s", tests[i]);
        if (!v3_run_fixture_smoke_direct("run-browser-webrtc-smokes", compile_label, paths.stage2, paths.root, root_v3, tests[i], out_bin)) {
            v3_restore_env_value("BACKEND_INCREMENTAL", saved_incremental, had_incremental);
            v3_restore_env_value("BACKEND_MULTI_MODULE_CACHE", saved_multi_cache, had_multi_cache);
            return 1;
        }
        snprintf(out_bin, sizeof(out_bin), "%s/%s.stage3", out_dir, tests[i]);
        snprintf(compile_label, sizeof(compile_label), "stage3 %s", tests[i]);
        if (!v3_run_fixture_smoke_direct("run-browser-webrtc-smokes", compile_label, paths.stage3, paths.root, root_v3, tests[i], out_bin)) {
            v3_restore_env_value("BACKEND_INCREMENTAL", saved_incremental, had_incremental);
            v3_restore_env_value("BACKEND_MULTI_MODULE_CACHE", saved_multi_cache, had_multi_cache);
            return 1;
        }
    }
    v3_restore_env_value("BACKEND_INCREMENTAL", saved_incremental, had_incremental);
    v3_restore_env_value("BACKEND_MULTI_MODULE_CACHE", saved_multi_cache, had_multi_cache);
    puts("v3 browser webrtc smokes: ok");
    return 0;
}

static int v3_cmd_run_chain_node_cli_smoke_impl(int argc, char **argv) {
    V3BootstrapPaths paths;
    char root_v3[PATH_MAX];
    char out_root[PATH_MAX];
    char tmp_template[PATH_MAX];
    char *tmpdir;
    char bin[PATH_MAX];
    char build_log[PATH_MAX];
    char server_state[PATH_MAX];
    char client_state[PATH_MAX];
    char ready_path[PATH_MAX];
    char server_log[PATH_MAX];
    char sync_log[PATH_MAX];
    char init_log[PATH_MAX];
    char mint_log[PATH_MAX];
    char transfer_log[PATH_MAX];
    char balance_a_log[PATH_MAX];
    char balance_b_log[PATH_MAX];
    char show_log[PATH_MAX];
    char snapshot_log[PATH_MAX];
    char client_balance_a_log[PATH_MAX];
    char client_balance_b_log[PATH_MAX];
    char client_show_log[PATH_MAX];
    char server_port[64];
    const char *compiler_bin;
    const char *label;
    char saved_compiler[PATH_MAX];
    char saved_out_dir[PATH_MAX];
    char saved_self_test[32];
    bool had_compiler = false;
    bool had_out_dir = false;
    bool had_self_test = false;
    pid_t server_pid = -1;
    int status = 0;
    char *daemon_argv[7];
    char *sync_argv[10];
    if (!v3_leaf_prepare_backend_driver(&paths, root_v3, sizeof(root_v3))) {
        return 1;
    }
    compiler_bin = v3_leaf_resolve_compiler_bin(argc,
                                                argv,
                                                "CHENG_V3_CHAIN_NODE_COMPILER",
                                                "CHENG_V3_SMOKE_COMPILER",
                                                paths.backend_driver_out);
    label = v3_leaf_resolve_label(argc, argv, "CHENG_V3_CHAIN_NODE_LABEL", "host");
    if (!v3_leaf_expect_compiler("run-chain-node-cli-smoke", compiler_bin)) {
        return 1;
    }
    v3_join_path(out_root, sizeof(out_root), paths.root, "artifacts/v3_chain_node_cli");
    v3_join_path(out_root, sizeof(out_root), out_root, label);
    if (!v3_mkdir_p(out_root)) {
        return 1;
    }
    v3_join_path(bin, sizeof(bin), out_root, "chain_node");
    v3_join_path(build_log, sizeof(build_log), out_root, "chain_node.compile.log");
    v3_save_env_value("CHENG_V3_CHAIN_NODE_COMPILER", saved_compiler, sizeof(saved_compiler), &had_compiler);
    v3_save_env_value("CHAIN_NODE_OUT_DIR", saved_out_dir, sizeof(saved_out_dir), &had_out_dir);
    v3_save_env_value("CHAIN_NODE_RUN_SELF_TEST", saved_self_test, sizeof(saved_self_test), &had_self_test);
    setenv("CHENG_V3_CHAIN_NODE_COMPILER", compiler_bin, 1);
    setenv("CHAIN_NODE_OUT_DIR", out_root, 1);
    setenv("CHAIN_NODE_RUN_SELF_TEST", "0", 1);
    if (v3_cmd_build_chain_node_impl(0, NULL) != 0) {
        v3_restore_env_value("CHENG_V3_CHAIN_NODE_COMPILER", saved_compiler, had_compiler);
        v3_restore_env_value("CHAIN_NODE_OUT_DIR", saved_out_dir, had_out_dir);
        v3_restore_env_value("CHAIN_NODE_RUN_SELF_TEST", saved_self_test, had_self_test);
        return 1;
    }
    v3_restore_env_value("CHENG_V3_CHAIN_NODE_COMPILER", saved_compiler, had_compiler);
    v3_restore_env_value("CHAIN_NODE_OUT_DIR", saved_out_dir, had_out_dir);
    v3_restore_env_value("CHAIN_NODE_RUN_SELF_TEST", saved_self_test, had_self_test);
    if (!v3_leaf_expect_compiler("run-chain-node-cli-smoke binary", bin)) {
        return 1;
    }
    snprintf(tmp_template, sizeof(tmp_template), "%s/tmp.XXXXXX", out_root);
    tmpdir = mkdtemp(tmp_template);
    if (tmpdir == NULL) {
        fprintf(stderr, "[cheng_v3_seed] run-chain-node-cli-smoke mkdtemp failed\n");
        return 1;
    }
    snprintf(server_state, sizeof(server_state), "%s/server.state", tmpdir);
    snprintf(client_state, sizeof(client_state), "%s/client.state", tmpdir);
    snprintf(ready_path, sizeof(ready_path), "%s/server.ready", tmpdir);
    snprintf(server_log, sizeof(server_log), "%s/server.log", tmpdir);
    snprintf(sync_log, sizeof(sync_log), "%s/sync.log", tmpdir);
    snprintf(init_log, sizeof(init_log), "%s/init.log", tmpdir);
    snprintf(mint_log, sizeof(mint_log), "%s/mint.log", tmpdir);
    snprintf(transfer_log, sizeof(transfer_log), "%s/transfer.log", tmpdir);
    snprintf(balance_a_log, sizeof(balance_a_log), "%s/balance_a.log", tmpdir);
    snprintf(balance_b_log, sizeof(balance_b_log), "%s/balance_b.log", tmpdir);
    snprintf(show_log, sizeof(show_log), "%s/show.log", tmpdir);
    snprintf(snapshot_log, sizeof(snapshot_log), "%s/snapshot.log", tmpdir);
    snprintf(client_balance_a_log, sizeof(client_balance_a_log), "%s/client_balance_a.log", tmpdir);
    snprintf(client_balance_b_log, sizeof(client_balance_b_log), "%s/client_balance_b.log", tmpdir);
    snprintf(client_show_log, sizeof(client_show_log), "%s/client_show.log", tmpdir);
    {
        char state_arg[PATH_MAX + 16];
        snprintf(state_arg, sizeof(state_arg), "--state:%s", server_state);
        if (!v3_run_command_logged_argv(5U,
                                        (const char *[5]){ bin, "init", state_arg, "--node-id:node-a", "--address-seed:chain-node-a" },
                                        init_log,
                                        "run-chain-node-cli-smoke init") ||
            !v3_run_command_logged_argv(6U,
                                        (const char *[6]){ bin, "mint", state_arg, "--asset:7", "--account:1001", "--amount:100" },
                                        mint_log,
                                        "run-chain-node-cli-smoke mint") ||
            !v3_run_command_logged_argv(7U,
                                        (const char *[7]){ bin, "transfer", state_arg, "--asset:7", "--from:1001", "--to:2002", "--amount:40" },
                                        transfer_log,
                                        "run-chain-node-cli-smoke transfer")) {
            return 1;
        }
        if (!v3_run_command_logged_argv(5U,
                                        (const char *[5]){ bin, "balance", state_arg, "--asset:7", "--account:1001" },
                                        balance_a_log,
                                        "run-chain-node-cli-smoke balance-a") ||
            !v3_run_command_logged_argv(5U,
                                        (const char *[5]){ bin, "balance", state_arg, "--asset:7", "--account:2002" },
                                        balance_b_log,
                                        "run-chain-node-cli-smoke balance-b") ||
            !v3_run_command_logged_argv(3U,
                                        (const char *[3]){ bin, "show-state", state_arg },
                                        show_log,
                                        "run-chain-node-cli-smoke show") ||
            !v3_run_command_logged_argv(3U,
                                        (const char *[3]){ bin, "dump-snapshot", state_arg },
                                        snapshot_log,
                                        "run-chain-node-cli-smoke snapshot")) {
            return 1;
        }
    }
    {
        char state_arg[PATH_MAX + 16];
        char ready_arg[PATH_MAX + 24];
        snprintf(state_arg, sizeof(state_arg), "--state:%s", server_state);
        snprintf(ready_arg, sizeof(ready_arg), "--ready-path:%s", ready_path);
        daemon_argv[0] = bin;
        daemon_argv[1] = "daemon-serve";
        daemon_argv[2] = state_arg;
        daemon_argv[3] = "--port:0";
        daemon_argv[4] = ready_arg;
        daemon_argv[5] = "--max-serve-count:1";
        daemon_argv[6] = NULL;
        if (!v3_spawn_binary_capture_output(daemon_argv, server_log, &server_pid) ||
            !v3_wait_ready_port_file("run-chain-node-cli-smoke daemon", ready_path, server_pid, 20000, server_port, sizeof(server_port)) ||
            !v3_text_is_decimal_digits(server_port)) {
            v3_kill_and_wait_pid(&server_pid);
            return 1;
        }
    }
    {
        char state_arg[PATH_MAX + 16];
        char port_arg[128];
        snprintf(state_arg, sizeof(state_arg), "--state:%s", client_state);
        snprintf(port_arg, sizeof(port_arg), "--port:%s", server_port);
        sync_argv[0] = bin;
        sync_argv[1] = "daemon-sync";
        sync_argv[2] = state_arg;
        sync_argv[3] = "--node-id:node-b";
        sync_argv[4] = "--address-seed:chain-node-b";
        sync_argv[5] = "--host:127.0.0.1";
        sync_argv[6] = port_arg;
        sync_argv[7] = "--max-sync-count:1";
        sync_argv[8] = "--interval-ms:10";
        sync_argv[9] = NULL;
        if (!v3_run_binary_capture_output(sync_argv, sync_log, &status) ||
            !v3_status_is_exit_code(status, 0) ||
            !v3_wait_pid_success(&server_pid, "run-chain-node-cli-smoke daemon", server_log)) {
            v3_kill_and_wait_pid(&server_pid);
            return 1;
        }
    }
    {
        char state_arg[PATH_MAX + 16];
        snprintf(state_arg, sizeof(state_arg), "--state:%s", client_state);
        if (!v3_run_command_logged_argv(5U,
                                        (const char *[5]){ bin, "balance", state_arg, "--asset:7", "--account:1001" },
                                        client_balance_a_log,
                                        "run-chain-node-cli-smoke client-balance-a") ||
            !v3_run_command_logged_argv(5U,
                                        (const char *[5]){ bin, "balance", state_arg, "--asset:7", "--account:2002" },
                                        client_balance_b_log,
                                        "run-chain-node-cli-smoke client-balance-b") ||
            !v3_run_command_logged_argv(3U,
                                        (const char *[3]){ bin, "show-state", state_arg },
                                        client_show_log,
                                        "run-chain-node-cli-smoke client-show")) {
            return 1;
        }
    }
    {
        char *text = v3_read_file(init_log);
        if (text == NULL || !v3_expect_text_contains("run-chain-node-cli-smoke init", text, "node_id=node-a")) {
            free(text);
            return 1;
        }
        free(text);
        text = v3_read_file(balance_a_log);
        if (text == NULL || !v3_expect_text_contains("run-chain-node-cli-smoke balance-a", text, "balance=60")) {
            free(text);
            return 1;
        }
        free(text);
        text = v3_read_file(balance_b_log);
        if (text == NULL || !v3_expect_text_contains("run-chain-node-cli-smoke balance-b", text, "balance=40")) {
            free(text);
            return 1;
        }
        free(text);
        text = v3_read_file(show_log);
        if (text == NULL || !v3_expect_text_contains("run-chain-node-cli-smoke show", text, "node_id=node-a")) {
            free(text);
            return 1;
        }
        free(text);
        text = v3_read_file(snapshot_log);
        if (text == NULL || !v3_expect_text_contains("run-chain-node-cli-smoke snapshot", text, "payload_hex=")) {
            free(text);
            return 1;
        }
        free(text);
        text = v3_read_file(sync_log);
        if (text == NULL || !v3_expect_text_contains("run-chain-node-cli-smoke sync", text, "changed=1")) {
            free(text);
            return 1;
        }
        free(text);
        text = v3_read_file(client_balance_a_log);
        if (text == NULL || !v3_expect_text_contains("run-chain-node-cli-smoke client-balance-a", text, "balance=60")) {
            free(text);
            return 1;
        }
        free(text);
        text = v3_read_file(client_balance_b_log);
        if (text == NULL || !v3_expect_text_contains("run-chain-node-cli-smoke client-balance-b", text, "balance=40")) {
            free(text);
            return 1;
        }
        free(text);
        text = v3_read_file(client_show_log);
        if (text == NULL || !v3_expect_text_contains("run-chain-node-cli-smoke client-show", text, "node_id=node-b")) {
            free(text);
            return 1;
        }
        free(text);
    }
    if (!v3_dump_file_stdout(init_log) ||
        !v3_dump_file_stdout(mint_log) ||
        !v3_dump_file_stdout(transfer_log) ||
        !v3_dump_file_stdout(balance_a_log) ||
        !v3_dump_file_stdout(balance_b_log) ||
        !v3_dump_file_stdout(show_log) ||
        !v3_dump_file_stdout(snapshot_log) ||
        !v3_dump_file_stdout(server_log) ||
        !v3_dump_file_stdout(sync_log) ||
        !v3_dump_file_stdout(client_balance_a_log) ||
        !v3_dump_file_stdout(client_balance_b_log) ||
        !v3_dump_file_stdout(client_show_log)) {
        return 1;
    }
    printf("v3 chain_node cli smoke ok (%s)\n", label);
    return 0;
}

static int v3_cmd_run_chain_node_process_smoke_impl(int argc, char **argv) {
    V3BootstrapPaths paths;
    char root_v3[PATH_MAX];
    char out_dir[PATH_MAX];
    char server_src[PATH_MAX];
    char client_src[PATH_MAX];
    char server_bin[PATH_MAX];
    char client_bin[PATH_MAX];
    char server_log[PATH_MAX];
    char client_log[PATH_MAX];
    char ready_path[PATH_MAX];
    char port_text[64];
    const char *compiler_bin;
    const char *label;
    char *server_argv[4];
    char *client_argv[3];
    char ready_arg[PATH_MAX + 32];
    char client_port_arg[128];
    pid_t server_pid = -1;
    int status = 0;
    if (!v3_leaf_prepare_backend_driver(&paths, root_v3, sizeof(root_v3))) {
        return 1;
    }
    compiler_bin = v3_leaf_resolve_compiler_bin(argc,
                                                argv,
                                                "CHENG_V3_CHAIN_NODE_COMPILER",
                                                "CHENG_V3_SMOKE_COMPILER",
                                                paths.backend_driver_out);
    label = v3_leaf_resolve_label(argc, argv, "CHENG_V3_CHAIN_NODE_LABEL", "host");
    if (!v3_leaf_expect_compiler("run-chain-node-process-smoke", compiler_bin)) {
        return 1;
    }
    v3_join_path(server_src, sizeof(server_src), paths.root, "v3/src/tests/chain_node_process_server_smoke.cheng");
    v3_join_path(client_src, sizeof(client_src), paths.root, "v3/src/tests/chain_node_process_client_smoke.cheng");
    v3_join_path(out_dir, sizeof(out_dir), paths.root, "artifacts/v3_chain_node_process");
    v3_join_path(out_dir, sizeof(out_dir), out_dir, label);
    v3_join_path(server_bin, sizeof(server_bin), out_dir, "chain_node_process_server");
    v3_join_path(client_bin, sizeof(client_bin), out_dir, "chain_node_process_client");
    v3_join_path(server_log, sizeof(server_log), out_dir, "server.log");
    v3_join_path(client_log, sizeof(client_log), out_dir, "client.log");
    v3_join_path(ready_path, sizeof(ready_path), out_dir, "server.ready");
    unlink(ready_path);
    if (!v3_mkdir_p(out_dir) ||
        !v3_compile_host_fixture_with_backend_driver(compiler_bin, root_v3, server_src, server_bin, server_log) ||
        !v3_compile_host_fixture_with_backend_driver(compiler_bin, root_v3, client_src, client_bin, client_log)) {
        return 1;
    }
    snprintf(ready_arg, sizeof(ready_arg), "--ready-path:%s", ready_path);
    server_argv[0] = server_bin;
    server_argv[1] = "--port:0";
    server_argv[2] = ready_arg;
    server_argv[3] = NULL;
    if (!v3_spawn_binary_capture_output(server_argv, server_log, &server_pid) ||
        !v3_wait_ready_port_file("run-chain-node-process-smoke", ready_path, server_pid, 20000, port_text, sizeof(port_text)) ||
        !v3_text_is_decimal_digits(port_text)) {
        v3_kill_and_wait_pid(&server_pid);
        return 1;
    }
    snprintf(client_port_arg, sizeof(client_port_arg), "--port:%s", port_text);
    client_argv[0] = client_bin;
    client_argv[1] = client_port_arg;
    client_argv[2] = NULL;
    if (!v3_run_binary_capture_output(client_argv, client_log, &status) ||
        !v3_status_is_exit_code(status, 0) ||
        !v3_wait_pid_success(&server_pid, "run-chain-node-process-smoke server", server_log)) {
        v3_kill_and_wait_pid(&server_pid);
        return 1;
    }
    if (!v3_dump_file_stdout(server_log) || !v3_dump_file_stdout(client_log)) {
        return 1;
    }
    printf("v3 chain_node process smoke ok (%s)\n", label);
    return 0;
}

static int v3_cmd_run_chain_node_three_node_smoke_impl(int argc, char **argv) {
    V3BootstrapPaths paths;
    char root_v3[PATH_MAX];
    char out_dir[PATH_MAX];
    char server_src[PATH_MAX];
    char relay_src[PATH_MAX];
    char client_src[PATH_MAX];
    char server_bin[PATH_MAX];
    char relay_bin[PATH_MAX];
    char client_bin[PATH_MAX];
    char server_log[PATH_MAX];
    char relay_log[PATH_MAX];
    char client_log[PATH_MAX];
    char server_ready[PATH_MAX];
    char relay_ready[PATH_MAX];
    char server_port[64];
    char relay_port[64];
    const char *compiler_bin;
    const char *label;
    char *server_argv[4];
    char *relay_argv[5];
    char *client_argv[3];
    char server_ready_arg[PATH_MAX + 32];
    char relay_ready_arg[PATH_MAX + 32];
    char upstream_arg[128];
    char relay_port_arg[128];
    pid_t server_pid = -1;
    pid_t relay_pid = -1;
    int status = 0;
    if (!v3_leaf_prepare_backend_driver(&paths, root_v3, sizeof(root_v3))) {
        return 1;
    }
    compiler_bin = v3_leaf_resolve_compiler_bin(argc,
                                                argv,
                                                "CHENG_V3_CHAIN_NODE_COMPILER",
                                                "CHENG_V3_SMOKE_COMPILER",
                                                paths.backend_driver_out);
    label = v3_leaf_resolve_label(argc, argv, "CHENG_V3_CHAIN_NODE_LABEL", "host");
    if (!v3_leaf_expect_compiler("run-chain-node-three-node-smoke", compiler_bin)) {
        return 1;
    }
    v3_join_path(server_src, sizeof(server_src), paths.root, "v3/src/tests/chain_node_process_server_smoke.cheng");
    v3_join_path(relay_src, sizeof(relay_src), paths.root, "v3/src/tests/chain_node_process_relay_smoke.cheng");
    v3_join_path(client_src, sizeof(client_src), paths.root, "v3/src/tests/chain_node_process_client_smoke.cheng");
    v3_join_path(out_dir, sizeof(out_dir), paths.root, "artifacts/v3_chain_node_three_node");
    v3_join_path(out_dir, sizeof(out_dir), out_dir, label);
    v3_join_path(server_bin, sizeof(server_bin), out_dir, "chain_node_process_server");
    v3_join_path(relay_bin, sizeof(relay_bin), out_dir, "chain_node_process_relay");
    v3_join_path(client_bin, sizeof(client_bin), out_dir, "chain_node_process_client");
    v3_join_path(server_log, sizeof(server_log), out_dir, "server.log");
    v3_join_path(relay_log, sizeof(relay_log), out_dir, "relay.log");
    v3_join_path(client_log, sizeof(client_log), out_dir, "client.log");
    v3_join_path(server_ready, sizeof(server_ready), out_dir, "server.ready");
    v3_join_path(relay_ready, sizeof(relay_ready), out_dir, "relay.ready");
    unlink(server_ready);
    unlink(relay_ready);
    if (!v3_mkdir_p(out_dir) ||
        !v3_compile_host_fixture_with_backend_driver(compiler_bin, root_v3, server_src, server_bin, server_log) ||
        !v3_compile_host_fixture_with_backend_driver(compiler_bin, root_v3, relay_src, relay_bin, relay_log) ||
        !v3_compile_host_fixture_with_backend_driver(compiler_bin, root_v3, client_src, client_bin, client_log)) {
        return 1;
    }
    snprintf(server_ready_arg, sizeof(server_ready_arg), "--ready-path:%s", server_ready);
    server_argv[0] = server_bin;
    server_argv[1] = "--port:0";
    server_argv[2] = server_ready_arg;
    server_argv[3] = NULL;
    if (!v3_spawn_binary_capture_output(server_argv, server_log, &server_pid) ||
        !v3_wait_ready_port_file("run-chain-node-three-node-smoke server", server_ready, server_pid, 20000, server_port, sizeof(server_port)) ||
        !v3_text_is_decimal_digits(server_port)) {
        v3_kill_and_wait_pid(&relay_pid);
        v3_kill_and_wait_pid(&server_pid);
        return 1;
    }
    snprintf(upstream_arg, sizeof(upstream_arg), "--upstream-port:%s", server_port);
    snprintf(relay_ready_arg, sizeof(relay_ready_arg), "--ready-path:%s", relay_ready);
    relay_argv[0] = relay_bin;
    relay_argv[1] = upstream_arg;
    relay_argv[2] = "--port:0";
    relay_argv[3] = relay_ready_arg;
    relay_argv[4] = NULL;
    if (!v3_spawn_binary_capture_output(relay_argv, relay_log, &relay_pid) ||
        !v3_wait_ready_port_file("run-chain-node-three-node-smoke relay", relay_ready, relay_pid, 20000, relay_port, sizeof(relay_port)) ||
        !v3_text_is_decimal_digits(relay_port)) {
        v3_kill_and_wait_pid(&relay_pid);
        v3_kill_and_wait_pid(&server_pid);
        return 1;
    }
    snprintf(relay_port_arg, sizeof(relay_port_arg), "--port:%s", relay_port);
    client_argv[0] = client_bin;
    client_argv[1] = relay_port_arg;
    client_argv[2] = NULL;
    if (!v3_run_binary_capture_output(client_argv, client_log, &status) ||
        !v3_status_is_exit_code(status, 0) ||
        !v3_wait_pid_success(&relay_pid, "run-chain-node-three-node-smoke relay", relay_log) ||
        !v3_wait_pid_success(&server_pid, "run-chain-node-three-node-smoke server", server_log)) {
        v3_kill_and_wait_pid(&relay_pid);
        v3_kill_and_wait_pid(&server_pid);
        return 1;
    }
    if (!v3_dump_file_stdout(server_log) ||
        !v3_dump_file_stdout(relay_log) ||
        !v3_dump_file_stdout(client_log)) {
        return 1;
    }
    printf("v3 chain_node three-node smoke ok (%s)\n", label);
    return 0;
}

static int v3_cmd_run_tailnet_control_smoke_impl(int argc, char **argv) {
    V3BootstrapPaths paths;
    char root_v3[PATH_MAX];
    char out_dir[PATH_MAX];
    char probe_dir[PATH_MAX];
    char probe_port_log[PATH_MAX];
    char probe_ready_log[PATH_MAX];
    char probe_log[PATH_MAX];
    char baseline_src[PATH_MAX];
    char core_smoke_src[PATH_MAX];
    char core_src[PATH_MAX];
    char baseline_bin[PATH_MAX];
    char core_smoke_bin[PATH_MAX];
    char core_bin[PATH_MAX];
    char baseline_compile_log[PATH_MAX];
    char core_smoke_compile_log[PATH_MAX];
    char core_compile_log[PATH_MAX];
    char baseline_run_log[PATH_MAX];
    char core_smoke_run_log[PATH_MAX];
    char configure_log[PATH_MAX];
    char listen_log[PATH_MAX];
    char dial_log[PATH_MAX];
    char probe_port[64];
    char control_probe_endpoint[PATH_MAX];
    char relay_probe_endpoint[PATH_MAX];
    const char *compiler_bin;
    const char *label;
    char *probe_argv[10];
    pid_t probe_pid = -1;
    int rc = 1;
    if (!v3_leaf_prepare_backend_driver(&paths, root_v3, sizeof(root_v3))) {
        return 1;
    }
    compiler_bin = v3_leaf_resolve_compiler_bin(argc,
                                                argv,
                                                "CHENG_V3_TAILNET_COMPILER",
                                                "CHENG_V3_SMOKE_COMPILER",
                                                paths.backend_driver_out);
    label = v3_leaf_resolve_label(argc, argv, "CHENG_V3_TAILNET_LABEL", "host");
    if (!v3_leaf_expect_compiler("run-tailnet-control-smoke", compiler_bin)) {
        return 1;
    }
    v3_join_path(out_dir, sizeof(out_dir), paths.root, "artifacts/v3_tailnet_control");
    v3_join_path(probe_dir, sizeof(probe_dir), paths.root, "v3/testdata/tailnet");
    if (!v3_mkdir_p(out_dir)) {
        return 1;
    }
    snprintf(probe_port_log, sizeof(probe_port_log), "%s/tailnet_probe_port.%s.log", out_dir, label);
    snprintf(probe_ready_log, sizeof(probe_ready_log), "%s/tailnet_probe_ready.%s.log", out_dir, label);
    snprintf(probe_log, sizeof(probe_log), "%s/tailnet_probe_server.%s.log", out_dir, label);
    if (!v3_run_command_logged_argv(4U,
                                    (const char *[4]){
                                        "/usr/bin/env",
                                        "python3",
                                        "-c",
                                        "import socket; s = socket.socket(); s.bind(('127.0.0.1', 0)); print(s.getsockname()[1]); s.close()"
                                    },
                                    probe_port_log,
                                    "run-tailnet-control-smoke pick-port") ||
        !v3_read_trimmed_file_text(probe_port_log, probe_port, sizeof(probe_port)) ||
        !v3_text_is_decimal_digits(probe_port)) {
        return 1;
    }
    probe_argv[0] = "/usr/bin/env";
    probe_argv[1] = "python3";
    probe_argv[2] = "-m";
    probe_argv[3] = "http.server";
    probe_argv[4] = probe_port;
    probe_argv[5] = "--bind";
    probe_argv[6] = "127.0.0.1";
    probe_argv[7] = "--directory";
    probe_argv[8] = probe_dir;
    probe_argv[9] = NULL;
    if (!v3_spawn_binary_capture_output(probe_argv, probe_log, &probe_pid)) {
        return 1;
    }
    snprintf(control_probe_endpoint, sizeof(control_probe_endpoint), "http://127.0.0.1:%s/headscale_control_ready.json", probe_port);
    snprintf(relay_probe_endpoint, sizeof(relay_probe_endpoint), "http://127.0.0.1:%s/derp_ready.json", probe_port);
    if (!v3_run_command_logged_argv(5U,
                                    (const char *[5]){
                                        "/usr/bin/env",
                                        "python3",
                                        "-c",
                                        "import sys, time, urllib.request\nurl = sys.argv[1]\nfor _ in range(50):\n    try:\n        with urllib.request.urlopen(url, timeout=0.2) as resp:\n            if resp.status == 200:\n                raise SystemExit(0)\n    except Exception:\n        time.sleep(0.1)\nraise SystemExit(1)\n",
                                        control_probe_endpoint
                                    },
                                    probe_ready_log,
                                    "run-tailnet-control-smoke wait-probe")) {
        v3_kill_and_wait_pid(&probe_pid);
        return 1;
    }
    v3_join_path(baseline_src, sizeof(baseline_src), paths.root, "v3/src/tests/libp2p_tailnet_transport_smoke.cheng");
    v3_join_path(core_smoke_src, sizeof(core_smoke_src), paths.root, "v3/src/tests/tailnet_control_core_smoke.cheng");
    v3_join_path(core_src, sizeof(core_src), paths.root, "v3/src/project/tailnet_control_core.cheng");
    snprintf(baseline_bin, sizeof(baseline_bin), "%s/libp2p_tailnet_transport_smoke.%s", out_dir, label);
    snprintf(core_smoke_bin, sizeof(core_smoke_bin), "%s/tailnet_control_core_smoke.%s", out_dir, label);
    snprintf(core_bin, sizeof(core_bin), "%s/tailnet_control_core.%s", out_dir, label);
    snprintf(baseline_compile_log, sizeof(baseline_compile_log), "%s/libp2p_tailnet_transport_smoke.%s.compile.log", out_dir, label);
    snprintf(core_smoke_compile_log, sizeof(core_smoke_compile_log), "%s/tailnet_control_core_smoke.%s.compile.log", out_dir, label);
    snprintf(core_compile_log, sizeof(core_compile_log), "%s/tailnet_control_core.%s.compile.log", out_dir, label);
    snprintf(baseline_run_log, sizeof(baseline_run_log), "%s/libp2p_tailnet_transport_smoke.%s.run.log", out_dir, label);
    snprintf(core_smoke_run_log, sizeof(core_smoke_run_log), "%s/tailnet_control_core_smoke.%s.run.log", out_dir, label);
    snprintf(configure_log, sizeof(configure_log), "%s/tailnet_control_configure.%s.run.log", out_dir, label);
    snprintf(listen_log, sizeof(listen_log), "%s/tailnet_control_listen.%s.run.log", out_dir, label);
    snprintf(dial_log, sizeof(dial_log), "%s/tailnet_control_dial.%s.run.log", out_dir, label);
    if (!v3_compile_host_fixture_with_backend_driver(compiler_bin, root_v3, baseline_src, baseline_bin, baseline_compile_log) ||
        !v3_compile_host_fixture_with_backend_driver(compiler_bin, root_v3, core_smoke_src, core_smoke_bin, core_smoke_compile_log) ||
        !v3_compile_host_fixture_with_backend_driver(compiler_bin, root_v3, core_src, core_bin, core_compile_log)) {
        v3_kill_and_wait_pid(&probe_pid);
        return 1;
    }
    if (!v3_run_command_logged_argv(1U, (const char *[1]){ baseline_bin }, baseline_run_log, "run-tailnet-control-smoke baseline") ||
        !v3_run_command_logged_argv(1U, (const char *[1]){ core_smoke_bin }, core_smoke_run_log, "run-tailnet-control-smoke decode")) {
        v3_kill_and_wait_pid(&probe_pid);
        return 1;
    }
    {
        const char *configure_args[] = {
            core_bin, "--command", "configure", "--provider-headscale", "1",
            "--control-url", "https://headscale.example",
            "--control-endpoint", "https://headscale.example/control",
            "--relay-endpoint", "https://derp.sfo.example:443",
            "--control-probe-endpoint", control_probe_endpoint,
            "--relay-probe-endpoint", relay_probe_endpoint,
            "--namespace", "infra",
            "--noise-public-key", "noise-pub-cli",
            "--derp-region-code", "sfo",
            "--derp-region-name", "SanFrancisco",
            "--derp-hostname", "derp.sfo.example",
            "--synthetic-ipv4", "100.64.0.44",
            "--synthetic-ipv6", "fd7a:115c:a1e0::44",
            "--local-peer-id", "tailnet-control-test",
            "--exact-routing", "1",
            "--provider-ready", "1",
            "--proxy-ready", "1"
        };
        const char *listen_args[] = {
            core_bin, "--command", "listen", "--provider-headscale", "1",
            "--control-url", "https://headscale.example",
            "--control-endpoint", "https://headscale.example/control",
            "--relay-endpoint", "https://derp.sfo.example:443",
            "--control-probe-endpoint", control_probe_endpoint,
            "--relay-probe-endpoint", relay_probe_endpoint,
            "--namespace", "infra",
            "--noise-public-key", "noise-pub-cli",
            "--derp-region-code", "sfo",
            "--derp-region-name", "SanFrancisco",
            "--derp-hostname", "derp.sfo.example",
            "--synthetic-ipv4", "100.64.0.44",
            "--synthetic-ipv6", "fd7a:115c:a1e0::44",
            "--local-peer-id", "tailnet-control-test",
            "--exact-routing", "1",
            "--provider-ready", "1",
            "--proxy-ready", "1",
            "--host", "127.0.0.1",
            "--port", "7101",
            "--transport", "tcp"
        };
        const char *dial_args[] = {
            core_bin, "--command", "dial-plan", "--provider-headscale", "1",
            "--control-url", "https://headscale.example",
            "--control-endpoint", "https://headscale.example/control",
            "--relay-endpoint", "https://derp.sfo.example:443",
            "--control-probe-endpoint", control_probe_endpoint,
            "--relay-probe-endpoint", relay_probe_endpoint,
            "--namespace", "infra",
            "--noise-public-key", "noise-pub-cli",
            "--derp-region-code", "sfo",
            "--derp-region-name", "SanFrancisco",
            "--derp-hostname", "derp.sfo.example",
            "--synthetic-ipv4", "100.64.0.44",
            "--synthetic-ipv6", "fd7a:115c:a1e0::44",
            "--local-peer-id", "tailnet-control-test",
            "--exact-routing", "1",
            "--provider-ready", "1",
            "--proxy-ready", "1",
            "--derp-healthy", "1",
            "--bootstrap-listen-count", "1",
            "--bootstrap-listen-raw-0", "/ip4/127.0.0.1/tcp/7101",
            "--bootstrap-peer-count", "1",
            "--bootstrap-peer-id-0", "peer-direct",
            "--bootstrap-peer-remote-tailnet-ip-0", "100.64.0.88",
            "--bootstrap-peer-direct-reachable-0", "1",
            "--bootstrap-peer-relay-allowed-0", "1",
            "--bootstrap-peer-relay-only-0", "0",
            "--bootstrap-peer-derp-region-code-0", "sfo",
            "--bootstrap-peer-signed-preface-0", "test-preface",
            "--bootstrap-peer-bound-addr-text-0", "/ip4/127.0.0.1/tcp/7101",
            "--bootstrap-peer-last-path-0", "direct",
            "--bootstrap-peer-last-error-0", "",
            "--peer-id", "peer-direct",
            "--addr", "/ip4/100.64.0.44/tcp/7101"
        };
        if (!v3_run_command_logged_argv(sizeof(configure_args) / sizeof(configure_args[0]),
                                        configure_args,
                                        configure_log,
                                        "run-tailnet-control-smoke configure") ||
            !v3_run_command_logged_argv(sizeof(listen_args) / sizeof(listen_args[0]),
                                        listen_args,
                                        listen_log,
                                        "run-tailnet-control-smoke listen") ||
            !v3_run_command_logged_argv(sizeof(dial_args) / sizeof(dial_args[0]),
                                        dial_args,
                                        dial_log,
                                        "run-tailnet-control-smoke dial")) {
            v3_kill_and_wait_pid(&probe_pid);
            return 1;
        }
    }
    {
        char *text = v3_read_file(configure_log);
        if (text == NULL ||
            !v3_expect_text_has_line("run-tailnet-control-smoke configure", text, "configured=1") ||
            !v3_expect_text_has_line("run-tailnet-control-smoke configure", text, "providerKind=headscale") ||
            !v3_expect_text_contains("run-tailnet-control-smoke configure", text, control_probe_endpoint) ||
            !v3_expect_text_has_line("run-tailnet-control-smoke configure", text, "controlPlane=headscale") ||
            !v3_expect_text_has_line("run-tailnet-control-smoke configure", text, "providerReady=1") ||
            !v3_expect_text_has_line("run-tailnet-control-smoke configure", text, "proxyListenersReady=1") ||
            !v3_expect_text_has_line("run-tailnet-control-smoke configure", text, "providerRuntimeActive=1")) {
            free(text);
            v3_kill_and_wait_pid(&probe_pid);
            return 1;
        }
        free(text);
        text = v3_read_file(listen_log);
        if (text == NULL ||
            !v3_expect_text_has_line("run-tailnet-control-smoke listen", text, "publishedAddr=/ip4/100.64.0.44/tcp/7101") ||
            !v3_expect_text_has_line("run-tailnet-control-smoke listen", text, "listener.0.raw=/ip4/127.0.0.1/tcp/7101") ||
            !v3_expect_text_has_line("run-tailnet-control-smoke listen", text, "providerKind=headscale") ||
            !v3_expect_text_has_line("run-tailnet-control-smoke listen", text, "tailnetDerpMapSummary=headscale/infra@https://headscale.example#sfo/derp.sfo.example") ||
            !v3_expect_text_has_line("run-tailnet-control-smoke listen", text, "providerRuntimeActive=1")) {
            free(text);
            v3_kill_and_wait_pid(&probe_pid);
            return 1;
        }
        free(text);
        text = v3_read_file(dial_log);
        if (text == NULL ||
            !v3_expect_text_has_line("run-tailnet-control-smoke dial", text, "dial.pathKind=direct") ||
            !v3_expect_text_has_line("run-tailnet-control-smoke dial", text, "dial.routeText=/ip4/127.0.0.1/tcp/7101") ||
            !v3_expect_text_has_line("run-tailnet-control-smoke dial", text, "sessionCount=1") ||
            !v3_expect_text_has_line("run-tailnet-control-smoke dial", text, "relayHealthy=1") ||
            !v3_expect_text_has_line("run-tailnet-control-smoke dial", text, "startupStage=derp-ready") ||
            !v3_expect_text_has_line("run-tailnet-control-smoke dial", text, "tailnetDerpMapSummary=headscale/infra@https://headscale.example#sfo/derp.sfo.example") ||
            !v3_expect_text_has_line("run-tailnet-control-smoke dial", text, "peer.0.lastPath=direct") ||
            !v3_expect_text_has_line("run-tailnet-control-smoke dial", text, "providerRuntimeActive=1") ||
            !v3_expect_text_has_line("run-tailnet-control-smoke dial", text, "providerRuntimeCommands=3")) {
            free(text);
            v3_kill_and_wait_pid(&probe_pid);
            return 1;
        }
        free(text);
    }
    if (!v3_dump_file_stdout(baseline_run_log) ||
        !v3_dump_file_stdout(core_smoke_run_log) ||
        !v3_dump_file_stdout(configure_log) ||
        !v3_dump_file_stdout(listen_log) ||
        !v3_dump_file_stdout(dial_log)) {
        v3_kill_and_wait_pid(&probe_pid);
        return 1;
    }
    rc = 0;
    printf("v3 tailnet_control_smoke ok (%s)\n", label);
    v3_kill_and_wait_pid(&probe_pid);
    return rc;
}

static int v3_cmd_verify_debug_tools(int argc, char **argv) {
    return v3_cmd_verify_debug_tools_impl(argc, argv);
    V3BootstrapPaths paths;
    char out_dir[PATH_MAX];
    char src_return[PATH_MAX];
    char src_program_selfhost[PATH_MAX];
    char src_call_chain[PATH_MAX];
    char src_windows[PATH_MAX];
    char linux_obj[PATH_MAX];
    char host_obj[PATH_MAX];
    char asm_obj[PATH_MAX];
    char windows_exe[PATH_MAX];
    char debug_report[PATH_MAX];
    char symbols_report[PATH_MAX];
    char line_map_report[PATH_MAX];
    char elf_report[PATH_MAX];
    char elf_alias_report[PATH_MAX];
    char macho_report[PATH_MAX];
    char pe_report[PATH_MAX];
    char asm_report[PATH_MAX];
    char asm_source_path[PATH_MAX];
    char *debug_text = NULL;
    char *symbols_text = NULL;
    char *line_map_text = NULL;
    char *elf_text = NULL;
    char *elf_alias_text = NULL;
    char *macho_text = NULL;
    char *pe_text = NULL;
    char *asm_text = NULL;
    char *asm_source_text = NULL;
    int rc = 1;
    (void)argc;
    (void)argv;
    v3_bootstrap_paths_init(&paths);
    if (!v3_bootstrap_artifacts_ready(&paths) && v3_cmd_bootstrap_bridge(0, NULL) != 0) {
        return 1;
    }
    v3_join_path(out_dir, sizeof(out_dir), paths.root, "artifacts/v3_debug_tools_verify");
    v3_join_path(src_return, sizeof(src_return), paths.root, "tests/cheng/backend/fixtures/return_add.cheng");
    v3_join_path(src_program_selfhost, sizeof(src_program_selfhost), paths.root, "v3/src/tests/program_selfhost_smoke.cheng");
    v3_join_path(src_call_chain, sizeof(src_call_chain), paths.root, "v3/src/tests/ordinary_call_chain_fixture.cheng");
    v3_join_path(src_windows, sizeof(src_windows), paths.root, "tests/cheng/backend/fixtures/hello_importc_puts.cheng");
    v3_join_path(linux_obj, sizeof(linux_obj), out_dir, "return_add_linux_aarch64.o");
    v3_join_path(host_obj, sizeof(host_obj), out_dir, "return_add_host.o");
    v3_join_path(asm_obj, sizeof(asm_obj), out_dir, "ordinary_call_chain_host.o");
    v3_join_path(windows_exe, sizeof(windows_exe), out_dir, "hello_importc_puts.exe");
    v3_join_path(debug_report, sizeof(debug_report), out_dir, "debug-report.txt");
    v3_join_path(symbols_report, sizeof(symbols_report), out_dir, "symbols.txt");
    v3_join_path(line_map_report, sizeof(line_map_report), out_dir, "line-map.txt");
    v3_join_path(elf_report, sizeof(elf_report), out_dir, "object-elf.txt");
    v3_join_path(elf_alias_report, sizeof(elf_alias_report), out_dir, "object-elf-alias.txt");
    v3_join_path(macho_report, sizeof(macho_report), out_dir, "object-macho.txt");
    v3_join_path(pe_report, sizeof(pe_report), out_dir, "object-pe.txt");
    v3_join_path(asm_report, sizeof(asm_report), out_dir, "asm.txt");
    snprintf(asm_source_path, sizeof(asm_source_path), "%s.s", asm_obj);
    if (!v3_mkdir_p(out_dir)) {
        return 1;
    }
    if (v3_invoke_command_with_common_flags(v3_cmd_debug_report,
                                            "debug-report",
                                            paths.stage1_source,
                                            paths.root,
                                            src_return,
                                            "obj",
                                            "aarch64-unknown-linux-gnu",
                                            linux_obj,
                                            debug_report) != 0) {
        goto cleanup;
    }
    debug_text = v3_read_file(debug_report);
    if (debug_text == NULL ||
        !v3_expect_text_has_line("verify-debug-tools debug-report", debug_text, "v3_debug_report_v1") ||
        !v3_expect_text_has_prefix("verify-debug-tools debug-report", debug_text, "entry=") ||
        !v3_expect_text_has_prefix("verify-debug-tools debug-report", debug_text, "lowering_function_count=") ||
        !v3_expect_text_has_prefix("verify-debug-tools debug-report", debug_text, "primary_object_item_count=") ||
        !v3_expect_text_has_prefix("verify-debug-tools debug-report", debug_text, "native_linker_program=") ||
        !v3_expect_text_contains("verify-debug-tools debug-report", debug_text, src_return)) {
        goto cleanup;
    }
    if (v3_invoke_command_with_common_flags(v3_cmd_print_symbols,
                                            "print-symbols",
                                            paths.stage1_source,
                                            paths.root,
                                            src_program_selfhost,
                                            "obj",
                                            "arm64-apple-darwin",
                                            host_obj,
                                            symbols_report) != 0) {
        goto cleanup;
    }
    symbols_text = v3_read_file(symbols_report);
    if (symbols_text == NULL ||
        !v3_expect_text_has_line("verify-debug-tools symbols", symbols_text, "v3_symbols_v1") ||
        !v3_expect_text_has_prefix("verify-debug-tools symbols", symbols_text, "lowering_symbol_count=") ||
        !v3_expect_text_has_prefix("verify-debug-tools symbols", symbols_text, "lowering_symbol[0]=") ||
        !v3_expect_text_has_prefix("verify-debug-tools symbols", symbols_text, "primary_symbol_count=") ||
        !v3_expect_text_has_line("verify-debug-tools symbols", symbols_text, "primary_unsupported_count=0")) {
        goto cleanup;
    }
    if (v3_invoke_command_with_common_flags(v3_cmd_print_line_map,
                                            "print-line-map",
                                            paths.stage1_source,
                                            paths.root,
                                            src_return,
                                            "obj",
                                            "aarch64-unknown-linux-gnu",
                                            linux_obj,
                                            line_map_report) != 0) {
        goto cleanup;
    }
    line_map_text = v3_read_file(line_map_report);
    if (line_map_text == NULL ||
        !v3_expect_text_has_line("verify-debug-tools line-map", line_map_text, "v3_line_map_v1") ||
        !v3_expect_text_has_prefix("verify-debug-tools line-map", line_map_text, "entry_count=") ||
        !v3_expect_text_contains("verify-debug-tools line-map", line_map_text, "entry\t")) {
        goto cleanup;
    }
    if (v3_invoke_command_with_common_flags(v3_cmd_system_link_exec,
                                            "system-link-exec",
                                            paths.stage1_source,
                                            paths.root,
                                            src_return,
                                            "obj",
                                            "aarch64-unknown-linux-gnu",
                                            linux_obj,
                                            NULL) != 0) {
        goto cleanup;
    }
    if (v3_invoke_command_with_common_flags(v3_cmd_system_link_exec,
                                            "system-link-exec",
                                            paths.stage1_source,
                                            paths.root,
                                            src_return,
                                            "obj",
                                            "arm64-apple-darwin",
                                            host_obj,
                                            NULL) != 0) {
        goto cleanup;
    }
    if (v3_invoke_command_with_common_flags(v3_cmd_print_object,
                                            "print-object",
                                            NULL,
                                            NULL,
                                            NULL,
                                            NULL,
                                            NULL,
                                            NULL,
                                            elf_report) != 0) {
        goto cleanup;
    }
    /* print-object needs --object; re-run directly */
cleanup:
    free(debug_text);
    free(symbols_text);
    free(line_map_text);
    free(elf_text);
    free(elf_alias_text);
    free(macho_text);
    free(pe_text);
    free(asm_text);
    free(asm_source_text);
    return rc;
}

static bool v3_orphan_ascii_space(char ch) {
    return ch == ' ' || ch == '\t' || ch == '\n' || ch == '\r';
}

static bool v3_append_text_piece(char **buffer,
                                 size_t *len,
                                 size_t *cap,
                                 const char *text,
                                 size_t text_len) {
    char *grown;
    size_t need;
    size_t new_cap;
    if (buffer == NULL || len == NULL || cap == NULL) {
        return false;
    }
    need = *len + text_len + 1U;
    if (need > *cap) {
        new_cap = (*cap > 0U) ? *cap : 64U;
        while (new_cap < need) {
            new_cap *= 2U;
        }
        grown = (char *)realloc(*buffer, new_cap);
        if (grown == NULL) {
            free(*buffer);
            *buffer = NULL;
            *len = 0U;
            *cap = 0U;
            return false;
        }
        *buffer = grown;
        *cap = new_cap;
    }
    if (text_len > 0U) {
        memcpy(*buffer + *len, text, text_len);
    }
    *len += text_len;
    (*buffer)[*len] = '\0';
    return true;
}

static char *v3_normalize_orphan_snapshot_text(const char *text) {
    char *out;
    size_t out_len = 0U;
    size_t out_cap = 1U;
    const char *cursor;
    if (text == NULL) {
        return NULL;
    }
    out = (char *)malloc(out_cap);
    if (out == NULL) {
        return NULL;
    }
    out[0] = '\0';
    cursor = text;
    while (*cursor != '\0') {
        const char *line_start = cursor;
        const char *line_end;
        const char *scan;
        const char *pid_start;
        const char *pid_end;
        int skip;
        bool ok = true;
        while (*cursor != '\0' && *cursor != '\n' && *cursor != '\r') {
            cursor++;
        }
        line_end = cursor;
        while (line_start < line_end && v3_orphan_ascii_space(*line_start)) {
            line_start++;
        }
        while (line_end > line_start && v3_orphan_ascii_space(line_end[-1])) {
            line_end--;
        }
        if (line_end > line_start) {
            scan = line_start;
            pid_start = scan;
            while (scan < line_end && !v3_orphan_ascii_space(*scan)) {
                scan++;
            }
            pid_end = scan;
            if (pid_end > pid_start) {
                for (skip = 0; skip < 3; ++skip) {
                    const char *field_start;
                    while (scan < line_end && v3_orphan_ascii_space(*scan)) {
                        scan++;
                    }
                    field_start = scan;
                    while (scan < line_end && !v3_orphan_ascii_space(*scan)) {
                        scan++;
                    }
                    if (scan <= field_start) {
                        ok = false;
                        break;
                    }
                }
                while (scan < line_end && v3_orphan_ascii_space(*scan)) {
                    scan++;
                }
                if (ok &&
                    scan < line_end &&
                    v3_append_text_piece(&out, &out_len, &out_cap, pid_start, (size_t)(pid_end - pid_start)) &&
                    v3_append_text_piece(&out, &out_len, &out_cap, "|", 1U) &&
                    v3_append_text_piece(&out, &out_len, &out_cap, scan, (size_t)(line_end - scan)) &&
                    v3_append_text_piece(&out, &out_len, &out_cap, "\n", 1U)) {
                    /* normalized line appended */
                } else if (ok) {
                    return NULL;
                }
            }
        }
        while (*cursor == '\n' || *cursor == '\r') {
            cursor++;
        }
    }
    return out;
}

static bool v3_text_has_exact_line(const char *text,
                                   const char *line,
                                   size_t line_len) {
    const char *cursor;
    if (text == NULL || line == NULL) {
        return false;
    }
    cursor = text;
    while (*cursor != '\0') {
        const char *start = cursor;
        const char *end;
        while (*cursor != '\0' && *cursor != '\n' && *cursor != '\r') {
            cursor++;
        }
        end = cursor;
        if ((size_t)(end - start) == line_len && memcmp(start, line, line_len) == 0) {
            return true;
        }
        while (*cursor == '\n' || *cursor == '\r') {
            cursor++;
        }
    }
    return false;
}

static bool v3_print_new_orphan_lines(const char *before_text,
                                      const char *after_text) {
    const char *cursor;
    bool found = false;
    if (after_text == NULL) {
        return false;
    }
    cursor = after_text;
    while (*cursor != '\0') {
        const char *start = cursor;
        const char *end;
        while (*cursor != '\0' && *cursor != '\n' && *cursor != '\r') {
            cursor++;
        }
        end = cursor;
        if (end > start && !v3_text_has_exact_line(before_text, start, (size_t)(end - start))) {
            fprintf(stderr, "%.*s\n", (int)(end - start), start);
            found = true;
        }
        while (*cursor == '\n' || *cursor == '\r') {
            cursor++;
        }
    }
    return found;
}

static bool v3_has_new_orphan_lines(const char *before_text,
                                    const char *after_text) {
    const char *cursor;
    if (after_text == NULL) {
        return false;
    }
    cursor = after_text;
    while (*cursor != '\0') {
        const char *start = cursor;
        const char *end;
        while (*cursor != '\0' && *cursor != '\n' && *cursor != '\r') {
            cursor++;
        }
        end = cursor;
        if (end > start && !v3_text_has_exact_line(before_text, start, (size_t)(end - start))) {
            return true;
        }
        while (*cursor == '\n' || *cursor == '\r') {
            cursor++;
        }
    }
    return false;
}

static char *v3_read_cheng_orphan_snapshot_text(const char *root,
                                                const char *log_path) {
    char command[PATH_MAX * 4];
    char *quoted_root;
    char *argv_local[4];
    int status = 0;
    if (root == NULL || root[0] == '\0') {
        return NULL;
    }
    quoted_root = v3_shell_quote(root);
    if (quoted_root == NULL) {
        return NULL;
    }
    snprintf(command,
             sizeof(command),
             "ps -Ao pid=,ppid=,state=,etime=,command= | "
             "awk -v root=%s '$2==1 && $3 ~ /^U/ && index($0, root) > 0'",
             quoted_root);
    free(quoted_root);
    argv_local[0] = (char *)"/bin/sh";
    argv_local[1] = (char *)"-lc";
    argv_local[2] = command;
    argv_local[3] = NULL;
    if (!v3_run_binary_capture_output(argv_local, log_path, &status) ||
        !v3_status_is_exit_code(status, 0)) {
        fprintf(stderr, "[cheng_v3_seed] verify-orphan-guard snapshot failed log=%s\n", log_path);
        return NULL;
    }
    return v3_read_file(log_path);
}

static int v3_cmd_verify_orphan_guard_impl(int argc, char **argv) {
    V3BootstrapPaths paths;
    char tooling_dir[PATH_MAX];
    char before_log[PATH_MAX];
    char after_log[PATH_MAX];
    DIR *dir;
    struct dirent *entry;
    char *before_snapshot = NULL;
    char *after_snapshot = NULL;
    char *before_normalized = NULL;
    char *after_normalized = NULL;
    int rc = 1;
    static const char *banned_entries[] = {
        "guarded_exec_v3.py",
        "orphan_guard_run.sh",
        "failfast_parallel.sh",
        "bootstrap_bridge_v3.sh",
        "build_bft_state_machine_v3.sh",
        "build_backend_driver_v3.sh",
        "build_bounds_trace_v3.sh",
        "build_browser_host_wasm_v3.sh",
        "browser_host_wasm_runner.js",
        "build_call_chain_v3.sh",
        "build_chain_node_linux_obj_v3.sh",
        "build_chain_node_linux_v3.sh",
        "build_chain_node_v3.sh",
        "build_ffi_handle_v3.sh",
        "build_linux_nolibc_exe_v3.sh",
        "build_panic_trace_v3.sh",
        "build_program_selfhost_v3.sh",
        "build_rwad_bft_state_machine_linux_obj_v3.sh",
        "build_rwad_bft_state_machine_linux_v3.sh",
        "build_rwad_bft_state_machine_v3.sh",
        "build_signal_trace_v3.sh",
        "build_twoway_search_smoke_v3.sh",
        "build_zero_exit_v3.sh",
        "cheng_tooling_repo_dispatch.sh",
        "cheng_v3.sh",
        "compare_bench.sh",
        "profile_run_v3.sh",
        "r2c_react_v3_fresh_clean_gate.sh",
        "run_slice_gate.sh",
        "run_v3_browser_host_wasm_smoke.sh",
        "run_v3_browser_webrtc_smokes.sh",
        "run_v3_chain_node_cli_smoke.sh",
        "run_v3_chain_node_process_smoke.sh",
        "run_v3_chain_node_three_node_smoke.sh",
        "run_v3_compile_exe.sh",
        "run_v3_fresh_node_selfhost_gate.sh",
        "run_v3_host_smokes.sh",
        "run_v3_linux_object_smokes.sh",
        "run_v3_migration_gate.sh",
        "run_v3_single_smoke.sh",
        "run_v3_stage23_libp2p_smokes.sh",
        "run_v3_tailnet_control_smoke.sh",
        "run_v3_tcp_twoproc_smoke.sh",
        "run_v3_quic_twoproc_smoke.sh",
        "run_v3_udp_importc_smoke.sh",
        "run_v3_wasm_smokes.sh",
        "run_v3_windows_riscv_builtin_smokes.sh",
        "scan_forbidden_hotpath.sh",
        "verify_debug_profile_v3.sh",
        "verify_debug_runtime_v3.sh",
        "verify_debug_tools_v3.sh",
        "verify_orphan_guard_v3.sh",
        "verify_riscv64_builtin_linker_v3.sh",
        "verify_windows_builtin_linker_v3.sh"
    };
    size_t i;
    v3_bootstrap_paths_init(&paths);
    (void)argc;
    (void)argv;
    if (paths.root[0] == '\0') {
        fprintf(stderr, "[cheng_v3_seed] verify-orphan-guard root detect failed\n");
        return 1;
    }
    v3_join_path(tooling_dir, sizeof(tooling_dir), paths.root, "v3/tooling");
    dir = opendir(tooling_dir);
    if (dir == NULL) {
        fprintf(stderr, "[cheng_v3_seed] verify-orphan-guard open failed: %s\n", tooling_dir);
        return 1;
    }
    while ((entry = readdir(dir)) != NULL) {
        char child_path[PATH_MAX];
        struct stat st;
        const char *name = entry->d_name;
        const char *ext = strrchr(name, '.');
        char *text;
        if (strcmp(name, ".") == 0 || strcmp(name, "..") == 0) {
            continue;
        }
        for (i = 0U; i < sizeof(banned_entries) / sizeof(banned_entries[0]); ++i) {
            if (strcmp(name, banned_entries[i]) == 0) {
                fprintf(stderr, "verify_orphan_guard_v3: banned tooling entry name: %s/%s\n", tooling_dir, name);
                closedir(dir);
                return 1;
            }
        }
        if (ext != NULL && strcmp(ext, ".sh") == 0) {
            v3_join_path(child_path, sizeof(child_path), tooling_dir, name);
            fprintf(stderr, "verify_orphan_guard_v3: shell tooling entry not allowed: %s\n", child_path);
            closedir(dir);
            return 1;
        }
        if (ext == NULL || strcmp(ext, ".py") != 0) {
            continue;
        }
        v3_join_path(child_path, sizeof(child_path), tooling_dir, name);
        if (stat(child_path, &st) != 0) {
            fprintf(stderr, "[cheng_v3_seed] verify-orphan-guard stat failed: %s\n", child_path);
            closedir(dir);
            return 1;
        }
        if (!S_ISREG(st.st_mode)) {
            continue;
        }
        text = v3_read_file(child_path);
        if (text == NULL) {
            fprintf(stderr, "[cheng_v3_seed] verify-orphan-guard read failed: %s\n", child_path);
            closedir(dir);
            return 1;
        }
        for (i = 0U; i < sizeof(banned_entries) / sizeof(banned_entries[0]); ++i) {
            if (strstr(text, banned_entries[i]) != NULL) {
                fprintf(stderr, "verify_orphan_guard_v3: banned direct process guard ref in %s\n", child_path);
                free(text);
                closedir(dir);
                return 1;
            }
        }
        free(text);
    }
    closedir(dir);
    dir = NULL;
    v3_join_path(before_log, sizeof(before_log), paths.root, "artifacts/v3_gate/orphan_guard_before.log");
    v3_join_path(after_log, sizeof(after_log), paths.root, "artifacts/v3_gate/orphan_guard_after.log");
    before_snapshot = v3_read_cheng_orphan_snapshot_text(paths.root, before_log);
    if (before_snapshot == NULL) {
        goto cleanup;
    }
    if (!v3_ensure_backend_driver_ready(&paths, "verify-orphan-guard")) {
        goto cleanup;
    }
    after_snapshot = v3_read_cheng_orphan_snapshot_text(paths.root, after_log);
    if (after_snapshot == NULL) {
        goto cleanup;
    }
    before_normalized = v3_normalize_orphan_snapshot_text(before_snapshot);
    after_normalized = v3_normalize_orphan_snapshot_text(after_snapshot);
    if (before_normalized == NULL || after_normalized == NULL) {
        goto cleanup;
    }
    if (v3_has_new_orphan_lines(before_normalized, after_normalized)) {
        fprintf(stderr,
                "[cheng_v3_seed] verify-orphan-guard new Cheng U/UE process leaked after bootstrap/build\n");
        v3_print_new_orphan_lines(before_normalized, after_normalized);
        goto cleanup;
    }
    rc = 0;
    puts("verify_orphan_guard_v3 ok");
cleanup:
    if (dir != NULL) {
        closedir(dir);
    }
    free(before_snapshot);
    free(after_snapshot);
    free(before_normalized);
    free(after_normalized);
    return rc;
}

static int v3_cmd_verify_debug_tools_impl(int argc, char **argv) {
    V3BootstrapPaths paths;
    char out_dir[PATH_MAX];
    char src_return[PATH_MAX];
    char src_program_selfhost[PATH_MAX];
    char src_call_chain[PATH_MAX];
    char src_windows[PATH_MAX];
    char linux_obj[PATH_MAX];
    char host_obj[PATH_MAX];
    char asm_obj[PATH_MAX];
    char windows_exe[PATH_MAX];
    char debug_report[PATH_MAX];
    char symbols_report[PATH_MAX];
    char line_map_report[PATH_MAX];
    char elf_report[PATH_MAX];
    char elf_alias_report[PATH_MAX];
    char macho_report[PATH_MAX];
    char pe_report[PATH_MAX];
    char asm_report[PATH_MAX];
    char asm_source_path[PATH_MAX];
    char object_flag[PATH_MAX + 16];
    char report_flag[PATH_MAX + 24];
    char *argv_local[4];
    char *debug_text = NULL;
    char *symbols_text = NULL;
    char *line_map_text = NULL;
    char *elf_text = NULL;
    char *elf_alias_text = NULL;
    char *macho_text = NULL;
    char *pe_text = NULL;
    char *asm_text = NULL;
    char *asm_source_text = NULL;
    const char *asm_payload = NULL;
    int rc = 1;
    (void)argc;
    (void)argv;
    v3_bootstrap_paths_init(&paths);
    if (!v3_bootstrap_artifacts_ready(&paths) && v3_cmd_bootstrap_bridge(0, NULL) != 0) {
        return 1;
    }
    v3_join_path(out_dir, sizeof(out_dir), paths.root, "artifacts/v3_debug_tools_verify");
    v3_join_path(src_return, sizeof(src_return), paths.root, "tests/cheng/backend/fixtures/return_add.cheng");
    v3_join_path(src_program_selfhost, sizeof(src_program_selfhost), paths.root, "v3/src/tests/program_selfhost_smoke.cheng");
    v3_join_path(src_call_chain, sizeof(src_call_chain), paths.root, "v3/src/tests/ordinary_call_chain_fixture.cheng");
    v3_join_path(src_windows, sizeof(src_windows), paths.root, "tests/cheng/backend/fixtures/hello_importc_puts.cheng");
    v3_join_path(linux_obj, sizeof(linux_obj), out_dir, "return_add_linux_aarch64.o");
    v3_join_path(host_obj, sizeof(host_obj), out_dir, "return_add_host.o");
    v3_join_path(asm_obj, sizeof(asm_obj), out_dir, "ordinary_call_chain_host.o");
    v3_join_path(windows_exe, sizeof(windows_exe), out_dir, "hello_importc_puts.exe");
    v3_join_path(debug_report, sizeof(debug_report), out_dir, "debug-report.txt");
    v3_join_path(symbols_report, sizeof(symbols_report), out_dir, "symbols.txt");
    v3_join_path(line_map_report, sizeof(line_map_report), out_dir, "line-map.txt");
    v3_join_path(elf_report, sizeof(elf_report), out_dir, "object-elf.txt");
    v3_join_path(elf_alias_report, sizeof(elf_alias_report), out_dir, "object-elf-alias.txt");
    v3_join_path(macho_report, sizeof(macho_report), out_dir, "object-macho.txt");
    v3_join_path(pe_report, sizeof(pe_report), out_dir, "object-pe.txt");
    v3_join_path(asm_report, sizeof(asm_report), out_dir, "asm.txt");
    snprintf(asm_source_path, sizeof(asm_source_path), "%s.s", asm_obj);
    if (!v3_mkdir_p(out_dir)) {
        return 1;
    }
    if (v3_invoke_command_with_common_flags(v3_cmd_debug_report,
                                            "debug-report",
                                            paths.stage1_source,
                                            paths.root,
                                            src_return,
                                            "obj",
                                            "aarch64-unknown-linux-gnu",
                                            linux_obj,
                                            debug_report) != 0) {
        goto cleanup;
    }
    debug_text = v3_read_file(debug_report);
    if (debug_text == NULL ||
        !v3_expect_text_has_line("verify-debug-tools debug-report", debug_text, "v3_debug_report_v1") ||
        !v3_expect_text_has_prefix("verify-debug-tools debug-report", debug_text, "entry=") ||
        !v3_expect_text_has_prefix("verify-debug-tools debug-report", debug_text, "lowering_function_count=") ||
        !v3_expect_text_has_prefix("verify-debug-tools debug-report", debug_text, "primary_object_item_count=") ||
        !v3_expect_text_has_prefix("verify-debug-tools debug-report", debug_text, "native_linker_program=") ||
        !v3_expect_text_contains("verify-debug-tools debug-report", debug_text, src_return)) {
        goto cleanup;
    }
    if (v3_invoke_command_with_common_flags(v3_cmd_print_symbols,
                                            "print-symbols",
                                            paths.stage1_source,
                                            paths.root,
                                            src_program_selfhost,
                                            "obj",
                                            "arm64-apple-darwin",
                                            host_obj,
                                            symbols_report) != 0) {
        goto cleanup;
    }
    symbols_text = v3_read_file(symbols_report);
    if (symbols_text == NULL ||
        !v3_expect_text_has_line("verify-debug-tools symbols", symbols_text, "v3_symbols_v1") ||
        !v3_expect_text_has_prefix("verify-debug-tools symbols", symbols_text, "lowering_symbol_count=") ||
        !v3_expect_text_has_prefix("verify-debug-tools symbols", symbols_text, "lowering_symbol[0]=") ||
        !v3_expect_text_has_prefix("verify-debug-tools symbols", symbols_text, "primary_symbol_count=")) {
        goto cleanup;
    }
    if (v3_invoke_command_with_common_flags(v3_cmd_print_line_map,
                                            "print-line-map",
                                            paths.stage1_source,
                                            paths.root,
                                            src_return,
                                            "obj",
                                            "aarch64-unknown-linux-gnu",
                                            linux_obj,
                                            line_map_report) != 0) {
        goto cleanup;
    }
    line_map_text = v3_read_file(line_map_report);
    if (line_map_text == NULL ||
        !v3_expect_text_has_line("verify-debug-tools line-map", line_map_text, "v3_line_map_v1") ||
        !v3_expect_text_has_prefix("verify-debug-tools line-map", line_map_text, "entry_count=") ||
        !v3_expect_text_contains("verify-debug-tools line-map", line_map_text, "entry\t")) {
        goto cleanup;
    }
    if (v3_invoke_command_with_common_flags(v3_cmd_system_link_exec,
                                            "system-link-exec",
                                            paths.stage1_source,
                                            paths.root,
                                            src_return,
                                            "obj",
                                            "aarch64-unknown-linux-gnu",
                                            linux_obj,
                                            NULL) != 0 ||
        v3_invoke_command_with_common_flags(v3_cmd_system_link_exec,
                                            "system-link-exec",
                                            paths.stage1_source,
                                            paths.root,
                                            src_return,
                                            "obj",
                                            "arm64-apple-darwin",
                                            host_obj,
                                            NULL) != 0 ||
        v3_invoke_command_with_common_flags(v3_cmd_system_link_exec,
                                            "system-link-exec",
                                            paths.stage1_source,
                                            paths.root,
                                            src_windows,
                                            "exe",
                                            "aarch64-pc-windows-msvc",
                                            windows_exe,
                                            NULL) != 0) {
        goto cleanup;
    }
    argv_local[0] = "cheng_v3_seed";
    argv_local[1] = "print-object";
    snprintf(object_flag, sizeof(object_flag), "--object:%s", linux_obj);
    snprintf(report_flag, sizeof(report_flag), "--report-out:%s", elf_report);
    argv_local[2] = object_flag;
    argv_local[3] = report_flag;
    if (v3_cmd_print_object(4, argv_local) != 0) {
        goto cleanup;
    }
    elf_text = v3_read_file(elf_report);
    if (elf_text == NULL ||
        !v3_expect_text_has_line("verify-debug-tools elf", elf_text, "v3_object_report_v1") ||
        !v3_expect_text_has_line("verify-debug-tools elf", elf_text, "format=elf64") ||
        !v3_expect_text_has_prefix("verify-debug-tools elf", elf_text, "section_count=") ||
        !v3_expect_text_has_prefix("verify-debug-tools elf", elf_text, "symbol_count=") ||
        !v3_expect_text_has_prefix("verify-debug-tools elf", elf_text, "reloc_count=")) {
        goto cleanup;
    }
    argv_local[1] = "print-elf";
    snprintf(report_flag, sizeof(report_flag), "--report-out:%s", elf_alias_report);
    argv_local[3] = report_flag;
    if (v3_cmd_print_elf(4, argv_local) != 0) {
        goto cleanup;
    }
    elf_alias_text = v3_read_file(elf_alias_report);
    if (elf_alias_text == NULL || strcmp(elf_text, elf_alias_text) != 0) {
        fprintf(stderr, "[cheng_v3_seed] verify-debug-tools print-elf alias drift\n");
        goto cleanup;
    }
    argv_local[1] = "print-object";
    snprintf(object_flag, sizeof(object_flag), "--object:%s", host_obj);
    snprintf(report_flag, sizeof(report_flag), "--report-out:%s", macho_report);
    argv_local[2] = object_flag;
    argv_local[3] = report_flag;
    if (v3_cmd_print_object(4, argv_local) != 0) {
        goto cleanup;
    }
    macho_text = v3_read_file(macho_report);
    if (macho_text == NULL ||
        !v3_expect_text_has_line("verify-debug-tools macho", macho_text, "v3_object_report_v1") ||
        !v3_expect_text_has_line("verify-debug-tools macho", macho_text, "format=macho64") ||
        !v3_expect_text_has_prefix("verify-debug-tools macho", macho_text, "load_command_count=") ||
        !v3_expect_text_has_prefix("verify-debug-tools macho", macho_text, "section_count=")) {
        goto cleanup;
    }
    snprintf(object_flag, sizeof(object_flag), "--object:%s", windows_exe);
    snprintf(report_flag, sizeof(report_flag), "--report-out:%s", pe_report);
    argv_local[2] = object_flag;
    argv_local[3] = report_flag;
    if (v3_cmd_print_object(4, argv_local) != 0) {
        goto cleanup;
    }
    pe_text = v3_read_file(pe_report);
    if (pe_text == NULL ||
        !v3_expect_text_has_line("verify-debug-tools pe", pe_text, "v3_object_report_v1") ||
        !v3_expect_text_has_line("verify-debug-tools pe", pe_text, "format=pe/coff") ||
        !v3_expect_text_has_prefix("verify-debug-tools pe", pe_text, "import_count=") ||
        !v3_expect_text_contains("verify-debug-tools pe", pe_text, "UCRTBASE.dll!puts") ||
        !v3_expect_text_contains("verify-debug-tools pe", pe_text, "KERNEL32.dll!ExitProcess")) {
        goto cleanup;
    }
    if (v3_invoke_command_with_common_flags(v3_cmd_print_asm,
                                            "print-asm",
                                            paths.stage1_source,
                                            paths.root,
                                            src_call_chain,
                                            "obj",
                                            "arm64-apple-darwin",
                                            asm_obj,
                                            asm_report) != 0) {
        goto cleanup;
    }
    asm_text = v3_read_file(asm_report);
    asm_source_text = v3_read_file(asm_source_path);
    asm_payload = v3_text_after_marker(asm_text, "asm_text_begin\n");
    if (asm_text == NULL ||
        asm_source_text == NULL ||
        asm_payload == NULL ||
        !v3_expect_text_has_line("verify-debug-tools asm", asm_text, "v3_asm_report_v1") ||
        !v3_expect_text_has_line("verify-debug-tools asm", asm_text, "asm_text_begin") ||
        !v3_expect_text_contains("verify-debug-tools asm", asm_payload, ".globl") ||
        strcmp(asm_payload, asm_source_text) != 0) {
        fprintf(stderr, "[cheng_v3_seed] verify-debug-tools asm payload mismatch\n");
        goto cleanup;
    }
    if (v3_cmd_verify_orphan_guard_impl(0, NULL) != 0) {
        goto cleanup;
    }
    puts("v3 debug tools: ok");
    rc = 0;
cleanup:
    free(debug_text);
    free(symbols_text);
    free(line_map_text);
    free(elf_text);
    free(elf_alias_text);
    free(macho_text);
    free(pe_text);
    free(asm_text);
    free(asm_source_text);
    return rc;
}

static bool v3_verify_runtime_fixture_output(const char *label,
                                             const char *fixture_path,
                                             const char *run_log_path,
                                             const char *report_path,
                                             const char *message_substring,
                                             const char *reason_line,
                                             bool require_signal_fields) {
    char *run_text = v3_read_file(run_log_path);
    char *report_text = v3_read_file(report_path);
    char prefixed_reason[512];
    bool ok = true;
    if (run_text == NULL) {
        fprintf(stderr, "[cheng_v3_seed] %s missing run log: %s\n", label, run_log_path);
        free(report_text);
        return false;
    }
    if (report_text == NULL) {
        fprintf(stderr, "[cheng_v3_seed] %s missing report: %s\n", label, report_path);
        free(run_text);
        return false;
    }
    snprintf(prefixed_reason, sizeof(prefixed_reason), "[cheng-v3] %s", reason_line);
    ok = ok && v3_expect_text_contains(label, run_text, message_substring);
    ok = ok && v3_expect_text_has_line(label, run_text, "v3_crash_raw_v1");
    ok = ok && v3_expect_text_has_line(label, run_text, reason_line);
    ok = ok && v3_expect_text_contains(label, run_text, "regs pc=0x");
    ok = ok && v3_expect_text_contains(label, run_text, " fp=0x");
    ok = ok && v3_expect_text_contains(label, run_text, " sp=0x");
    ok = ok && v3_expect_text_contains(label, run_text, " lr=0x");
    if (require_signal_fields) {
        ok = ok && v3_expect_text_contains(label, run_text, " sig=");
        ok = ok && v3_expect_text_contains(label, run_text, " fault=0x");
    }
    ok = ok && v3_expect_text_contains(label, run_text, "m#");
    ok = ok && v3_expect_text_contains(label, run_text, "#");
    ok = ok && v3_expect_text_contains(label, run_text, fixture_path);
    ok = ok && v3_expect_text_has_line(label, report_text, "[cheng-v3] v3_crash_report_v1");
    ok = ok && v3_expect_text_has_line(label, report_text, prefixed_reason);
    ok = ok && v3_expect_text_contains(label, report_text, "[cheng-v3] regs pc=0x");
    ok = ok && v3_expect_text_contains(label, report_text, " fp=0x");
    ok = ok && v3_expect_text_contains(label, report_text, " sp=0x");
    ok = ok && v3_expect_text_contains(label, report_text, " lr=0x");
    if (require_signal_fields) {
        ok = ok && v3_expect_text_contains(label, report_text, " sig=");
        ok = ok && v3_expect_text_contains(label, report_text, " fault=0x");
    }
    ok = ok && v3_expect_text_contains(label, report_text, "[cheng-v3] m#");
    ok = ok && v3_expect_text_contains(label, report_text, "[cheng-v3] #");
    ok = ok && v3_expect_text_contains(label, report_text, fixture_path);
    free(run_text);
    free(report_text);
    return ok;
}

static int v3_cmd_verify_debug_runtime_impl(int argc, char **argv) {
    V3BootstrapPaths paths;
    char root_v3[PATH_MAX];
    char out_dir[PATH_MAX];
    char src_panic[PATH_MAX];
    char src_bounds[PATH_MAX];
    char src_signal[PATH_MAX];
    char panic_bin[PATH_MAX];
    char bounds_bin[PATH_MAX];
    char signal_bin[PATH_MAX];
    char linux_bin[PATH_MAX];
    char panic_compile_log[PATH_MAX];
    char bounds_compile_log[PATH_MAX];
    char signal_compile_log[PATH_MAX];
    char panic_run_log[PATH_MAX];
    char bounds_run_log[PATH_MAX];
    char signal_run_log[PATH_MAX];
    char panic_report[PATH_MAX];
    char panic_crash_report[PATH_MAX];
    char bounds_crash_report[PATH_MAX];
    char signal_crash_report[PATH_MAX];
    char panic_crash_report_log[PATH_MAX];
    char bounds_crash_report_log[PATH_MAX];
    char signal_crash_report_log[PATH_MAX];
    char linux_compile_log[PATH_MAX];
    char linux_report[PATH_MAX];
    char panic_line_map[PATH_MAX];
    char bounds_line_map[PATH_MAX];
    char signal_line_map[PATH_MAX];
    char direct_debug_source[PATH_MAX];
    char linux_runtime_source[PATH_MAX];
    char legacy_runtime_native_dir[PATH_MAX];
    char panic_debug_provider_object[PATH_MAX];
    char linux_runtime_object[PATH_MAX];
    char *panic_argv[2];
    char *bounds_argv[2];
    char *signal_argv[2];
    char *bounds_log_text = NULL;
    char *direct_debug_source_text = NULL;
    char *linux_report_text = NULL;
    char *panic_report_text = NULL;
    const char *host_target = NULL;
    const char *const *allowed_debug_runtime_undefined = NULL;
    size_t allowed_debug_runtime_undefined_count = 0U;
    const char *linux_target = "aarch64-unknown-linux-gnu";
    const char *debug_runtime_internal_export_prefix = "cheng_v3_runtime_debug_runtime_stub_v3__";
    static const char *const allowed_debug_runtime_exports[] = {
        "cheng_crash_trace_enabled",
        "cheng_crash_trace_mark",
        "cheng_crash_trace_set_phase",
        "cheng_dump_backtrace_if_enabled",
        "cheng_force_segv",
        "cheng_v3_debug_profile_flush_from_argv0",
        "cheng_v3_native_dump_backtrace_and_exit",
        "cheng_v3_native_register_line_map_from_argv0"
    };
    static const char *const allowed_debug_runtime_undefined_darwin[] = {
        "_cheng_bounds_check",
        "_cheng_host_exit_runtime",
        "_cheng_host_fclose",
        "_cheng_host_fflush",
        "_cheng_host_fgets",
        "_cheng_host_fopen",
        "_cheng_host_fwrite",
        "_cheng_host_getenv",
        "_cheng_host_puts_runtime"
    };
    static const char *const allowed_linux_runtime_undefined[] = {
        "__cheng_linux_syscall1",
        "__cheng_linux_syscall2",
        "__cheng_linux_syscall3",
        "__cheng_linux_syscall4",
        "__cheng_linux_syscall5",
        "__cheng_linux_syscall6"
    };
    int status = 0;
    (void)argc;
    (void)argv;
    v3_bootstrap_paths_init(&paths);
    if (!v3_ensure_backend_driver_ready(&paths, "verify-debug-runtime")) {
        return 1;
    }
    v3_join_path(root_v3, sizeof(root_v3), paths.root, "v3");
    v3_join_path(out_dir, sizeof(out_dir), paths.root, "artifacts/v3_debug_runtime_verify");
    v3_join_path(src_panic, sizeof(src_panic), paths.root, "v3/src/tests/ordinary_panic_fixture.cheng");
    v3_join_path(src_bounds, sizeof(src_bounds), paths.root, "v3/src/tests/ordinary_bounds_trace_fixture.cheng");
    v3_join_path(src_signal, sizeof(src_signal), paths.root, "v3/src/tests/ordinary_signal_trace_fixture.cheng");
    v3_join_path(direct_debug_source, sizeof(direct_debug_source), paths.root, "v3/src/runtime/debug_runtime_stub_v3.cheng");
    v3_join_path(linux_runtime_source,
                 sizeof(linux_runtime_source),
                 paths.root,
                 "src/std/system_helpers_backend_nolibc_linux_aarch64.cheng");
    v3_join_path(legacy_runtime_native_dir,
                 sizeof(legacy_runtime_native_dir),
                 paths.root,
                 "v3/runtime/native");
    v3_join_path(panic_bin, sizeof(panic_bin), out_dir, "ordinary_panic_fixture");
    v3_join_path(bounds_bin, sizeof(bounds_bin), out_dir, "ordinary_bounds_trace_fixture");
    v3_join_path(signal_bin, sizeof(signal_bin), out_dir, "ordinary_signal_trace_fixture");
    v3_join_path(linux_bin, sizeof(linux_bin), out_dir, "ordinary_panic_fixture_linux");
    v3_join_path(panic_compile_log, sizeof(panic_compile_log), out_dir, "ordinary_panic_fixture.compile.log");
    v3_join_path(linux_compile_log, sizeof(linux_compile_log), out_dir, "ordinary_panic_fixture_linux.compile.log");
    v3_join_path(bounds_compile_log, sizeof(bounds_compile_log), out_dir, "ordinary_bounds_trace_fixture.compile.log");
    v3_join_path(signal_compile_log, sizeof(signal_compile_log), out_dir, "ordinary_signal_trace_fixture.compile.log");
    v3_join_path(panic_run_log, sizeof(panic_run_log), out_dir, "ordinary_panic_fixture.run.log");
    v3_join_path(bounds_run_log, sizeof(bounds_run_log), out_dir, "ordinary_bounds_trace_fixture.run.log");
    v3_join_path(signal_run_log, sizeof(signal_run_log), out_dir, "ordinary_signal_trace_fixture.run.log");
    v3_join_path(panic_report, sizeof(panic_report), out_dir, "ordinary_panic_fixture.report.txt");
    v3_join_path(panic_crash_report, sizeof(panic_crash_report), out_dir, "ordinary_panic_fixture.crash.txt");
    v3_join_path(bounds_crash_report, sizeof(bounds_crash_report), out_dir, "ordinary_bounds_trace_fixture.crash.txt");
    v3_join_path(signal_crash_report, sizeof(signal_crash_report), out_dir, "ordinary_signal_trace_fixture.crash.txt");
    snprintf(panic_crash_report_log, sizeof(panic_crash_report_log), "%s.crash-report.log", panic_crash_report);
    snprintf(bounds_crash_report_log, sizeof(bounds_crash_report_log), "%s.crash-report.log", bounds_crash_report);
    snprintf(signal_crash_report_log, sizeof(signal_crash_report_log), "%s.crash-report.log", signal_crash_report);
    v3_join_path(linux_report, sizeof(linux_report), out_dir, "ordinary_panic_fixture_linux.report.txt");
    snprintf(panic_line_map, sizeof(panic_line_map), "%s.v3.map", panic_bin);
    snprintf(bounds_line_map, sizeof(bounds_line_map), "%s.v3.map", bounds_bin);
    snprintf(signal_line_map, sizeof(signal_line_map), "%s.v3.map", signal_bin);
    snprintf(panic_debug_provider_object,
             sizeof(panic_debug_provider_object),
             "%s.provider.runtime_debug_runtime_v3.o",
             panic_bin);
    snprintf(linux_runtime_object,
             sizeof(linux_runtime_object),
             "%s.runtime.system_helpers_backend_nolibc_linux_aarch64.o",
             linux_bin);
    host_target = v3_host_target_triple();
    if (!v3_mkdir_p(out_dir)) {
        return 1;
    }
    if (!v3_expect_file_exists_nonempty("verify-debug-runtime direct source", direct_debug_source)) {
        return 1;
    }
    direct_debug_source_text = v3_read_file(direct_debug_source);
    if (direct_debug_source_text == NULL ||
        !v3_expect_text_contains("verify-debug-runtime direct source",
                                 direct_debug_source_text,
                                 "@exportc(\"cheng_v3_native_dump_backtrace_and_exit\")") ||
        !v3_expect_text_contains("verify-debug-runtime direct source",
                                 direct_debug_source_text,
                                 "@exportc(\"cheng_force_segv\")") ||
        !v3_expect_text_contains("verify-debug-runtime direct source",
                                 direct_debug_source_text,
                                 "@exportc(\"cheng_v3_debug_profile_flush_from_argv0\")")) {
        free(direct_debug_source_text);
        return 1;
    }
    free(direct_debug_source_text);
    if (host_target == NULL || host_target[0] == '\0') {
        fprintf(stderr, "[cheng_v3_seed] verify-debug-runtime unsupported host target\n");
        return 1;
    }
    if (v3_text_contains_substring(host_target, "-apple-darwin")) {
        allowed_debug_runtime_undefined = allowed_debug_runtime_undefined_darwin;
        allowed_debug_runtime_undefined_count =
            sizeof(allowed_debug_runtime_undefined_darwin) / sizeof(allowed_debug_runtime_undefined_darwin[0]);
    } else {
        fprintf(stderr,
                "[cheng_v3_seed] verify-debug-runtime undefined whitelist unsupported host target: %s\n",
                host_target);
        return 1;
    }
    if (!v3_compile_fixture_with_backend_driver_emit_target(paths.backend_driver_out,
                                                            root_v3,
                                                            src_panic,
                                                            "exe",
                                                            host_target,
                                                            panic_bin,
                                                            panic_compile_log,
                                                            panic_report) ||
        !v3_compile_host_fixture_with_backend_driver(paths.backend_driver_out, root_v3, src_bounds, bounds_bin, bounds_compile_log) ||
        !v3_compile_host_fixture_with_backend_driver(paths.backend_driver_out, root_v3, src_signal, signal_bin, signal_compile_log) ||
        !v3_compile_fixture_with_backend_driver_emit_target(paths.backend_driver_out,
                                                            root_v3,
                                                            src_panic,
                                                            "exe",
                                                            linux_target,
                                                            linux_bin,
                                                            linux_compile_log,
                                                            linux_report) ||
        !v3_expect_file_exists_nonempty("verify-debug-runtime panic report", panic_report) ||
        !v3_expect_file_exists_nonempty("verify-debug-runtime panic provider object", panic_debug_provider_object) ||
        !v3_expect_file_exists_nonempty("verify-debug-runtime linux report", linux_report) ||
        !v3_expect_file_exists_nonempty("verify-debug-runtime linux runtime object", linux_runtime_object) ||
        !v3_expect_file_exists_nonempty("verify-debug-runtime panic line-map", panic_line_map) ||
        !v3_expect_file_exists_nonempty("verify-debug-runtime bounds line-map", bounds_line_map) ||
        !v3_expect_file_exists_nonempty("verify-debug-runtime signal line-map", signal_line_map)) {
        return 1;
    }
    panic_report_text = v3_read_file(panic_report);
    if (panic_report_text == NULL ||
        !v3_expect_text_contains("verify-debug-runtime panic report", panic_report_text, "provider_source_paths=") ||
        !v3_expect_text_contains("verify-debug-runtime panic report", panic_report_text, direct_debug_source) ||
        !v3_expect_text_not_contains("verify-debug-runtime panic report", panic_report_text, legacy_runtime_native_dir) ||
        !v3_expect_text_contains("verify-debug-runtime panic report", panic_report_text, panic_debug_provider_object) ||
        !v3_expect_object_export_contract("verify-debug-runtime provider exports",
                                          panic_debug_provider_object,
                                          allowed_debug_runtime_exports,
                                          sizeof(allowed_debug_runtime_exports) / sizeof(allowed_debug_runtime_exports[0]),
                                          debug_runtime_internal_export_prefix) ||
        !v3_expect_object_undefined_whitelist("verify-debug-runtime provider undefined",
                                              panic_debug_provider_object,
                                              allowed_debug_runtime_undefined,
                                              allowed_debug_runtime_undefined_count)) {
        free(panic_report_text);
        return 1;
    }
    free(panic_report_text);
    linux_report_text = v3_read_file(linux_report);
    if (linux_report_text == NULL ||
        !v3_expect_text_has_line("verify-debug-runtime linux report", linux_report_text, "linux_nolibc_support_enabled=1") ||
        !v3_expect_text_contains("verify-debug-runtime linux report", linux_report_text, linux_runtime_source) ||
        !v3_expect_text_not_contains("verify-debug-runtime linux report", linux_report_text, legacy_runtime_native_dir) ||
        !v3_expect_text_contains("verify-debug-runtime linux report", linux_report_text, linux_runtime_object) ||
        !v3_expect_object_undefined_whitelist("verify-debug-runtime linux runtime undefined",
                                              linux_runtime_object,
                                              allowed_linux_runtime_undefined,
                                              sizeof(allowed_linux_runtime_undefined) /
                                                  sizeof(allowed_linux_runtime_undefined[0]))) {
        free(linux_report_text);
        return 1;
    }
    free(linux_report_text);
    panic_argv[0] = panic_bin;
    panic_argv[1] = NULL;
    bounds_argv[0] = bounds_bin;
    bounds_argv[1] = NULL;
    signal_argv[0] = signal_bin;
    signal_argv[1] = NULL;
    if (!v3_run_binary_capture_output(panic_argv, panic_run_log, &status) || v3_status_is_exit_code(status, 0) ||
        !v3_crash_report_transform_raw(panic_run_log, panic_crash_report, panic_crash_report_log) ||
        !v3_verify_runtime_fixture_output("verify-debug-runtime panic",
                                          src_panic,
                                          panic_run_log,
                                          panic_crash_report,
                                          "v3 panic fixture",
                                          "reason=panic mode=backtrace",
                                          false)) {
        return 1;
    }
    if (!v3_run_binary_capture_output(bounds_argv, bounds_run_log, &status) || v3_status_is_exit_code(status, 0) ||
        !v3_crash_report_transform_raw(bounds_run_log, bounds_crash_report, bounds_crash_report_log) ||
        !v3_verify_runtime_fixture_output("verify-debug-runtime bounds",
                                          src_bounds,
                                          bounds_run_log,
                                          bounds_crash_report,
                                          "[cheng] bounds check failed: idx=1 len=1",
                                          "reason=bounds mode=backtrace",
                                          false)) {
        return 1;
    }
    bounds_log_text = v3_read_file(bounds_run_log);
    if (bounds_log_text == NULL ||
        !v3_expect_text_contains("verify-debug-runtime bounds", bounds_log_text, "ordinary_bounds_trace_fixture")) {
        free(bounds_log_text);
        return 1;
    }
    free(bounds_log_text);
    if (!v3_run_binary_capture_output(signal_argv, signal_run_log, &status) || v3_status_is_exit_code(status, 0) ||
        !v3_crash_report_transform_raw(signal_run_log, signal_crash_report, signal_crash_report_log) ||
        !v3_verify_runtime_fixture_output("verify-debug-runtime signal",
                                          src_signal,
                                          signal_run_log,
                                          signal_crash_report,
                                          "v3_crash_raw_v1",
                                          "reason=signal mode=signal",
                                          true)) {
        return 1;
    }
    puts("v3 debug runtime: ok");
    return 0;
}

static int v3_cmd_verify_debug_profile_impl(int argc, char **argv) {
    (void)argc;
    (void)argv;
    V3BootstrapPaths paths;
    char out_dir[PATH_MAX];
    char src[PATH_MAX];
    char out_bin[PATH_MAX];
    char report[PATH_MAX];
    char raw_report[PATH_MAX];
    char direct_report[PATH_MAX];
    char direct_log[PATH_MAX];
    char compiler_bin[PATH_MAX];
    char in_flag[PATH_MAX + 16];
    char out_flag[PATH_MAX + 16];
    char *report_argv[5];
    char *raw_report_text = NULL;
    char *report_text = NULL;
    char *direct_report_text = NULL;
    int status = 0;
    int rc = 1;
    v3_bootstrap_paths_init(&paths);
    if (!v3_bootstrap_artifacts_ready(&paths) && v3_cmd_bootstrap_bridge(0, NULL) != 0) {
        return 1;
    }
    v3_join_path(out_dir, sizeof(out_dir), paths.root, "artifacts/v3_debug_profile_verify");
    v3_join_path(src, sizeof(src), paths.root, "v3/src/tests/ordinary_profile_hotspots_fixture.cheng");
    v3_join_path(out_bin, sizeof(out_bin), out_dir, "ordinary_profile_hotspots_fixture");
    v3_join_path(report, sizeof(report), out_dir, "profile.txt");
    v3_join_path(direct_report, sizeof(direct_report), out_dir, "profile.direct.txt");
    v3_profile_raw_report_path_from_report(report, raw_report, sizeof(raw_report));
    snprintf(direct_log, sizeof(direct_log), "%s.profile-report.log", direct_report);
    if (!v3_mkdir_p(out_dir)) {
        return 1;
    }
    if (v3_invoke_profile_run(paths.stage1_source,
                              paths.root,
                              src,
                              "arm64-apple-darwin",
                              out_bin,
                              "200",
                              report) != 0) {
        return 1;
    }
    report_text = v3_read_file(report);
    if (report_text == NULL ||
        !v3_expect_text_has_line("verify-debug-profile", report_text, "v3_profile_v1") ||
        !v3_text_has_positive_integer_line(report_text, "total_samples=") ||
        !v3_expect_text_contains("verify-debug-profile", report_text, "hot_function[0]=") ||
        !v3_expect_text_contains("verify-debug-profile", report_text, "hot_function[1]=") ||
        !v3_expect_text_contains("verify-debug-profile", report_text, "hot_") ||
        !v3_expect_text_contains("verify-debug-profile", report_text, "ordinary_profile_hotspots_fixture.cheng") ||
        !v3_expect_text_contains("verify-debug-profile", report_text, "hot_pc[0]=") ||
        !v3_expect_text_contains("verify-debug-profile", report_text, "|0x")) {
        goto cleanup;
    }
    raw_report_text = v3_read_file(raw_report);
    if (raw_report_text == NULL ||
        !v3_expect_text_has_line("verify-debug-profile raw", raw_report_text, "v3_profile_raw_v1") ||
        !v3_text_has_positive_integer_line(raw_report_text, "total_samples=") ||
        !v3_expect_text_contains("verify-debug-profile raw", raw_report_text, "sample\t0x")) {
        goto cleanup;
    }
    if (!v3_profile_report_live_compiler_bin(&paths, compiler_bin, sizeof(compiler_bin))) {
        fprintf(stderr, "[cheng_v3_seed] verify-debug-profile missing live compiler\n");
        goto cleanup;
    }
    snprintf(in_flag, sizeof(in_flag), "--in:%s", raw_report);
    snprintf(out_flag, sizeof(out_flag), "--out:%s", direct_report);
    report_argv[0] = compiler_bin;
    report_argv[1] = "profile-report";
    report_argv[2] = in_flag;
    report_argv[3] = out_flag;
    report_argv[4] = NULL;
    if (!v3_run_binary_capture_output(report_argv, direct_log, &status) ||
        !v3_status_is_exit_code(status, 0) ||
        !v3_expect_file_exists_nonempty("verify-debug-profile direct report", direct_report)) {
        goto cleanup;
    }
    direct_report_text = v3_read_file(direct_report);
    if (direct_report_text == NULL ||
        !v3_expect_text_has_line("verify-debug-profile direct", direct_report_text, "v3_profile_v1") ||
        strcmp(report_text, direct_report_text) != 0) {
        fprintf(stderr, "[cheng_v3_seed] verify-debug-profile direct report mismatch: %s\n", direct_report);
        goto cleanup;
    }
    puts("v3 debug profile: ok");
    rc = 0;
cleanup:
    free(direct_report_text);
    free(raw_report_text);
    free(report_text);
    return rc;
}

static bool v3_verify_builtin_output_common(const char *label,
                                            const char *out_path,
                                            const char *report_path,
                                            const char *expected_linker_program,
                                            const char *expected_flavor) {
    char *report_text = v3_read_file(report_path);
    bool ok = true;
    if (!v3_expect_file_exists_nonempty(label, out_path) || report_text == NULL) {
        free(report_text);
        return false;
    }
    ok = ok && v3_expect_text_contains(label, report_text, expected_linker_program);
    ok = ok && v3_expect_text_contains(label, report_text, expected_flavor);
    ok = ok && v3_expect_text_contains(label, report_text, "native_link_input_count=0");
    ok = ok && v3_expect_text_contains(label, report_text, "native_link_missing_reasons=-");
    free(report_text);
    return ok;
}

static bool v3_build_builtin_fixture_current(const char *contract_path,
                                             const char *root_path,
                                             const char *src_path,
                                             const char *target,
                                             const char *out_path,
                                             const char *report_path) {
    return v3_invoke_command_with_common_flags(v3_cmd_system_link_exec,
                                               "system-link-exec",
                                               contract_path,
                                               root_path,
                                               src_path,
                                               "exe",
                                               target,
                                               out_path,
                                               report_path) == 0;
}

static int v3_cmd_verify_windows_builtin_impl(int argc, char **argv) {
    static const char *RETURN_FIXTURES[] = {
        "return_add", "return_call", "return_if_stmt", "return_while_sum",
        "return_and_expr", "return_bitwise", "return_shift", "return_for_sum",
        "return_while_break", "return_while_continue", "return_inline_if_and_ternary",
        "return_call_mixed", "return_float32_arith_chain", "return_float32_roundtrip",
        "return_float64_ops", "return_float_compare_cast", "return_float_mixed_int_cast",
        "return_cast_i32",
        "return_cast_i64"
    };
    V3BootstrapPaths paths;
    char out_dir[PATH_MAX];
    char hello_src[PATH_MAX];
    char hello_out[PATH_MAX];
    char hello_report[PATH_MAX];
    char pe_report[PATH_MAX];
    char object_flag[PATH_MAX + 16];
    char report_flag[PATH_MAX + 24];
    char *argv_local[4];
    char *pe_text = NULL;
    size_t i;
    (void)argc;
    (void)argv;
    v3_bootstrap_paths_init(&paths);
    if (!v3_bootstrap_artifacts_ready(&paths) && v3_cmd_bootstrap_bridge(0, NULL) != 0) {
        return 1;
    }
    v3_join_path(out_dir, sizeof(out_dir), paths.root, "artifacts/v3_windows_builtin_verify");
    v3_join_path(hello_src, sizeof(hello_src), paths.root, "tests/cheng/backend/fixtures/hello_importc_puts.cheng");
    v3_join_path(hello_out, sizeof(hello_out), out_dir, "hello_importc_puts.exe");
    v3_join_path(hello_report, sizeof(hello_report), out_dir, "hello_importc_puts.report");
    v3_join_path(pe_report, sizeof(pe_report), out_dir, "object-pe.txt");
    if (!v3_mkdir_p(out_dir) ||
        !v3_build_builtin_fixture_current(paths.stage1_source,
                                          paths.root,
                                          hello_src,
                                          "aarch64-pc-windows-msvc",
                                          hello_out,
                                          hello_report) ||
        !v3_verify_builtin_output_common("verify-windows-builtin hello",
                                         hello_out,
                                         hello_report,
                                         "native_linker_program=internal_pe_consteval_linker",
                                         "native_linker_flavor=pe_aarch64_consteval_internal")) {
        return 1;
    }
    argv_local[0] = "cheng_v3_seed";
    argv_local[1] = "print-object";
    snprintf(object_flag, sizeof(object_flag), "--object:%s", hello_out);
    snprintf(report_flag, sizeof(report_flag), "--report-out:%s", pe_report);
    argv_local[2] = object_flag;
    argv_local[3] = report_flag;
    if (v3_cmd_print_object(4, argv_local) != 0) {
        return 1;
    }
    pe_text = v3_read_file(pe_report);
    if (pe_text == NULL ||
        !v3_expect_text_has_line("verify-windows-builtin", pe_text, "format=pe/coff") ||
        !v3_expect_text_contains("verify-windows-builtin", pe_text, "UCRTBASE.dll!puts") ||
        !v3_expect_text_contains("verify-windows-builtin", pe_text, "KERNEL32.dll!ExitProcess")) {
        free(pe_text);
        return 1;
    }
    free(pe_text);
    for (i = 0U; i < sizeof(RETURN_FIXTURES) / sizeof(RETURN_FIXTURES[0]); ++i) {
        char src[PATH_MAX];
        char out_path[PATH_MAX];
        char report_path[PATH_MAX];
        v3_join_path(src, sizeof(src), paths.root, "tests/cheng/backend/fixtures");
        v3_join_path(src, sizeof(src), src, RETURN_FIXTURES[i]);
        strncat(src, ".cheng", sizeof(src) - strlen(src) - 1U);
        v3_join_path(out_path, sizeof(out_path), out_dir, RETURN_FIXTURES[i]);
        strncat(out_path, ".exe", sizeof(out_path) - strlen(out_path) - 1U);
        v3_join_path(report_path, sizeof(report_path), out_dir, RETURN_FIXTURES[i]);
        strncat(report_path, ".report", sizeof(report_path) - strlen(report_path) - 1U);
        if (!v3_build_builtin_fixture_current(paths.stage1_source,
                                              paths.root,
                                              src,
                                              "aarch64-pc-windows-msvc",
                                              out_path,
                                              report_path) ||
            !v3_verify_builtin_output_common("verify-windows-builtin fixture",
                                             out_path,
                                             report_path,
                                             "native_linker_program=internal_pe_consteval_linker",
                                             "native_linker_flavor=pe_aarch64_consteval_internal")) {
            return 1;
        }
    }
    puts("verify_windows_builtin_linker_v3 ok");
    return 0;
}

static int v3_cmd_verify_riscv64_builtin_impl(int argc, char **argv) {
    static const char *RETURN_FIXTURES[] = {
        "return_add", "return_call", "return_if_stmt", "return_while_sum",
        "return_and_expr", "return_bitwise", "return_shift", "return_for_sum",
        "return_while_break", "return_while_continue", "return_inline_if_and_ternary",
        "return_call_mixed", "return_float32_arith_chain", "return_float32_roundtrip",
        "return_float64_ops", "return_float_compare_cast", "return_float_mixed_int_cast",
        "return_cast_i32",
        "return_cast_i64"
    };
    V3BootstrapPaths paths;
    char out_dir[PATH_MAX];
    char hello_src[PATH_MAX];
    char hello_out[PATH_MAX];
    char hello_report[PATH_MAX];
    char elf_report[PATH_MAX];
    char object_flag[PATH_MAX + 16];
    char report_flag[PATH_MAX + 24];
    char *argv_local[4];
    char *elf_text = NULL;
    size_t i;
    (void)argc;
    (void)argv;
    v3_bootstrap_paths_init(&paths);
    if (!v3_bootstrap_artifacts_ready(&paths) && v3_cmd_bootstrap_bridge(0, NULL) != 0) {
        return 1;
    }
    v3_join_path(out_dir, sizeof(out_dir), paths.root, "artifacts/v3_riscv64_builtin_verify");
    v3_join_path(hello_src, sizeof(hello_src), paths.root, "tests/cheng/backend/fixtures/hello_puts.cheng");
    v3_join_path(hello_out, sizeof(hello_out), out_dir, "hello_puts");
    v3_join_path(hello_report, sizeof(hello_report), out_dir, "hello_puts.report");
    v3_join_path(elf_report, sizeof(elf_report), out_dir, "object-elf.txt");
    if (!v3_mkdir_p(out_dir) ||
        !v3_build_builtin_fixture_current(paths.stage1_source,
                                          paths.root,
                                          hello_src,
                                          "riscv64-unknown-linux-gnu",
                                          hello_out,
                                          hello_report) ||
        !v3_verify_builtin_output_common("verify-riscv64-builtin hello",
                                         hello_out,
                                         hello_report,
                                         "native_linker_program=internal_elf_consteval_linker",
                                         "native_linker_flavor=elf_riscv64_consteval_internal")) {
        return 1;
    }
    argv_local[0] = "cheng_v3_seed";
    argv_local[1] = "print-object";
    snprintf(object_flag, sizeof(object_flag), "--object:%s", hello_out);
    snprintf(report_flag, sizeof(report_flag), "--report-out:%s", elf_report);
    argv_local[2] = object_flag;
    argv_local[3] = report_flag;
    if (v3_cmd_print_object(4, argv_local) != 0) {
        return 1;
    }
    elf_text = v3_read_file(elf_report);
    if (elf_text == NULL ||
        !v3_expect_text_has_line("verify-riscv64-builtin", elf_text, "format=elf64") ||
        !v3_expect_text_has_line("verify-riscv64-builtin", elf_text, "elf_type=exec") ||
        !v3_expect_text_contains("verify-riscv64-builtin", elf_text, "machine=243")) {
        free(elf_text);
        return 1;
    }
    free(elf_text);
    for (i = 0U; i < sizeof(RETURN_FIXTURES) / sizeof(RETURN_FIXTURES[0]); ++i) {
        char src[PATH_MAX];
        char out_path[PATH_MAX];
        char report_path[PATH_MAX];
        v3_join_path(src, sizeof(src), paths.root, "tests/cheng/backend/fixtures");
        v3_join_path(src, sizeof(src), src, RETURN_FIXTURES[i]);
        strncat(src, ".cheng", sizeof(src) - strlen(src) - 1U);
        v3_join_path(out_path, sizeof(out_path), out_dir, RETURN_FIXTURES[i]);
        v3_join_path(report_path, sizeof(report_path), out_dir, RETURN_FIXTURES[i]);
        strncat(report_path, ".report", sizeof(report_path) - strlen(report_path) - 1U);
        if (!v3_build_builtin_fixture_current(paths.stage1_source,
                                              paths.root,
                                              src,
                                              "riscv64-unknown-linux-gnu",
                                              out_path,
                                              report_path) ||
            !v3_verify_builtin_output_common("verify-riscv64-builtin fixture",
                                             out_path,
                                             report_path,
                                             "native_linker_program=internal_elf_consteval_linker",
                                             "native_linker_flavor=elf_riscv64_consteval_internal")) {
            return 1;
        }
    }
    puts("verify_riscv64_builtin_linker_v3 ok");
    return 0;
}

static int v3_cmd_run_cross_target_smokes_impl(int argc, char **argv) {
    (void)argc;
    (void)argv;
    if (v3_cmd_verify_windows_builtin_impl(0, NULL) != 0) {
        return 1;
    }
    if (v3_cmd_verify_riscv64_builtin_impl(0, NULL) != 0) {
        return 1;
    }
    puts("run_v3_windows_riscv_builtin_smokes ok");
    return 0;
}

static size_t v3_collect_smoke_positionals(int argc,
                                           char **argv,
                                           const char **out,
                                           size_t cap) {
    size_t count = 0U;
    int i;
    for (i = 2; i < argc && count < cap; ++i) {
        const char *arg = argv[i];
        if (arg == NULL || arg[0] == '\0') {
            continue;
        }
        if (strncmp(arg, "--", 2U) == 0) {
            if ((strcmp(arg, "--compiler") == 0 ||
                 strcmp(arg, "--tail-only") == 0 ||
                 strcmp(arg, "--label") == 0) &&
                (i + 1) < argc) {
                i += 1;
            }
            continue;
        }
        out[count++] = arg;
    }
    return count;
}

static bool v3_run_fixture_smoke_direct(const char *suite_label,
                                        const char *compile_label,
                                        const char *compiler_bin,
                                        const char *root,
                                        const char *root_v3,
                                        const char *smoke_name,
                                        const char *out_bin) {
    char src[PATH_MAX];
    char compile_log[PATH_MAX];
    char run_log[PATH_MAX];
    char line_map[PATH_MAX];
    char *run_argv[2];
    int status = 0;
    v3_join_path(src, sizeof(src), root, "v3/src/tests");
    v3_join_path(src, sizeof(src), src, smoke_name);
    strncat(src, ".cheng", sizeof(src) - strlen(src) - 1U);
    snprintf(compile_log, sizeof(compile_log), "%s.compile.log", out_bin);
    snprintf(run_log, sizeof(run_log), "%s.run.log", out_bin);
    snprintf(line_map, sizeof(line_map), "%s.v3.map", out_bin);
    unlink(out_bin);
    unlink(compile_log);
    unlink(run_log);
    unlink(line_map);
    printf("[%s] compile %s\n", suite_label, compile_label);
    if (!v3_compile_host_fixture_with_backend_driver(compiler_bin, root_v3, src, out_bin, compile_log)) {
        fprintf(stderr, "%s: compile failed: %s\n", suite_label, compile_label);
        return false;
    }
    printf("[%s] run %s\n", suite_label, compile_label);
    run_argv[0] = (char *)out_bin;
    run_argv[1] = NULL;
    if (!v3_run_binary_capture_output(run_argv, run_log, &status) ||
        !v3_status_is_exit_code(status, 0)) {
        fprintf(stderr, "%s: run failed: %s\n", suite_label, compile_label);
        return false;
    }
    return v3_dump_file_stdout(run_log);
}

static int v3_run_chain_node_process_smoke_with_env(const char *compiler_bin,
                                                    const char *label,
                                                    bool stage23_env) {
    char saved_compiler[PATH_MAX];
    char saved_label[256];
    char saved_incremental[64];
    char saved_multi_cache[64];
    bool had_compiler = false;
    bool had_label = false;
    bool had_incremental = false;
    bool had_multi_cache = false;
    int rc;
    v3_save_env_value("CHENG_V3_CHAIN_NODE_COMPILER", saved_compiler, sizeof(saved_compiler), &had_compiler);
    v3_save_env_value("CHENG_V3_CHAIN_NODE_LABEL", saved_label, sizeof(saved_label), &had_label);
    setenv("CHENG_V3_CHAIN_NODE_COMPILER", compiler_bin, 1);
    setenv("CHENG_V3_CHAIN_NODE_LABEL", label, 1);
    if (stage23_env) {
        v3_save_env_value("BACKEND_INCREMENTAL", saved_incremental, sizeof(saved_incremental), &had_incremental);
        v3_save_env_value("BACKEND_MULTI_MODULE_CACHE", saved_multi_cache, sizeof(saved_multi_cache), &had_multi_cache);
        setenv("BACKEND_INCREMENTAL", "0", 1);
        setenv("BACKEND_MULTI_MODULE_CACHE", "0", 1);
    }
    rc = v3_cmd_run_chain_node_process_smoke_impl(0, NULL);
    if (stage23_env) {
        v3_restore_env_value("BACKEND_INCREMENTAL", saved_incremental, had_incremental);
        v3_restore_env_value("BACKEND_MULTI_MODULE_CACHE", saved_multi_cache, had_multi_cache);
    }
    v3_restore_env_value("CHENG_V3_CHAIN_NODE_COMPILER", saved_compiler, had_compiler);
    v3_restore_env_value("CHENG_V3_CHAIN_NODE_LABEL", saved_label, had_label);
    return rc;
}

static int v3_run_chain_node_three_node_smoke_with_env(const char *compiler_bin,
                                                       const char *label,
                                                       bool stage23_env) {
    char saved_compiler[PATH_MAX];
    char saved_label[256];
    char saved_incremental[64];
    char saved_multi_cache[64];
    bool had_compiler = false;
    bool had_label = false;
    bool had_incremental = false;
    bool had_multi_cache = false;
    int rc;
    v3_save_env_value("CHENG_V3_CHAIN_NODE_COMPILER", saved_compiler, sizeof(saved_compiler), &had_compiler);
    v3_save_env_value("CHENG_V3_CHAIN_NODE_LABEL", saved_label, sizeof(saved_label), &had_label);
    setenv("CHENG_V3_CHAIN_NODE_COMPILER", compiler_bin, 1);
    setenv("CHENG_V3_CHAIN_NODE_LABEL", label, 1);
    if (stage23_env) {
        v3_save_env_value("BACKEND_INCREMENTAL", saved_incremental, sizeof(saved_incremental), &had_incremental);
        v3_save_env_value("BACKEND_MULTI_MODULE_CACHE", saved_multi_cache, sizeof(saved_multi_cache), &had_multi_cache);
        setenv("BACKEND_INCREMENTAL", "0", 1);
        setenv("BACKEND_MULTI_MODULE_CACHE", "0", 1);
    }
    rc = v3_cmd_run_chain_node_three_node_smoke_impl(0, NULL);
    if (stage23_env) {
        v3_restore_env_value("BACKEND_INCREMENTAL", saved_incremental, had_incremental);
        v3_restore_env_value("BACKEND_MULTI_MODULE_CACHE", saved_multi_cache, had_multi_cache);
    }
    v3_restore_env_value("CHENG_V3_CHAIN_NODE_COMPILER", saved_compiler, had_compiler);
    v3_restore_env_value("CHENG_V3_CHAIN_NODE_LABEL", saved_label, had_label);
    return rc;
}

static int v3_run_host_leaf_command(const char *leaf_id,
                                    const char *compiler_bin,
                                    const char *label) {
    char compiler_flag[PATH_MAX + 16];
    char label_flag[256];
    char *argv_local[4];
    snprintf(compiler_flag, sizeof(compiler_flag), "--compiler:%s", compiler_bin);
    snprintf(label_flag, sizeof(label_flag), "--label:%s", label);
    argv_local[0] = (char *)"cheng_v3_seed";
    argv_local[2] = compiler_flag;
    argv_local[3] = label_flag;
    if (strcmp(leaf_id, "wasm") == 0) {
        argv_local[1] = (char *)"run-wasm-smokes";
        return v3_cmd_run_wasm_smokes_impl(4, argv_local);
    }
    if (strcmp(leaf_id, "browser_host_wasm") == 0) {
        argv_local[1] = (char *)"run-browser-host-wasm-smoke";
        return v3_cmd_run_browser_host_wasm_smoke_impl(4, argv_local);
    }
    if (strcmp(leaf_id, "tcp_twoproc") == 0) {
        argv_local[1] = (char *)"run-tcp-twoproc-smoke";
        return v3_cmd_run_tcp_twoproc_smoke_impl(4, argv_local);
    }
    if (strcmp(leaf_id, "quic_twoproc") == 0) {
        argv_local[1] = (char *)"run-quic-twoproc-smoke";
        return v3_cmd_run_quic_twoproc_smoke_impl(4, argv_local);
    }
    if (strcmp(leaf_id, "udp_importc") == 0) {
        argv_local[1] = (char *)"run-udp-importc-smoke";
        return v3_cmd_run_udp_importc_smoke_impl(4, argv_local);
    }
    if (strcmp(leaf_id, "chain_node_cli") == 0) {
        argv_local[1] = (char *)"run-chain-node-cli-smoke";
        return v3_cmd_run_chain_node_cli_smoke_impl(4, argv_local);
    }
    if (strcmp(leaf_id, "tailnet_control") == 0) {
        argv_local[1] = (char *)"run-tailnet-control-smoke";
        return v3_cmd_run_tailnet_control_smoke_impl(4, argv_local);
    }
    if (strcmp(leaf_id, "fresh_node_selfhost") == 0) {
        argv_local[1] = (char *)"run-fresh-node-selfhost-gate";
        return v3_cmd_run_fresh_node_selfhost_gate_impl(4, argv_local);
    }
    if (strcmp(leaf_id, "migration") == 0) {
        argv_local[1] = (char *)"run-migration-gate";
        return v3_cmd_run_migration_gate_impl(4, argv_local);
    }
    return 1;
}

static int v3_run_stage23_leaf_command(const char *leaf_id,
                                       const char *compiler_bin,
                                       const char *label) {
    char saved_incremental[64];
    char saved_multi_cache[64];
    bool had_incremental = false;
    bool had_multi_cache = false;
    int rc;
    v3_save_env_value("BACKEND_INCREMENTAL", saved_incremental, sizeof(saved_incremental), &had_incremental);
    v3_save_env_value("BACKEND_MULTI_MODULE_CACHE", saved_multi_cache, sizeof(saved_multi_cache), &had_multi_cache);
    setenv("BACKEND_INCREMENTAL", "0", 1);
    setenv("BACKEND_MULTI_MODULE_CACHE", "0", 1);
    rc = v3_run_host_leaf_command(leaf_id, compiler_bin, label);
    v3_restore_env_value("BACKEND_INCREMENTAL", saved_incremental, had_incremental);
    v3_restore_env_value("BACKEND_MULTI_MODULE_CACHE", saved_multi_cache, had_multi_cache);
    return rc;
}

static int v3_run_stage23_browser_webrtc_script(const char *root,
                                                const char *compiler_bin,
                                                size_t positional_count,
                                                const char **positionals) {
    char saved_compiler[PATH_MAX];
    char saved_incremental[64];
    char saved_multi_cache[64];
    bool had_compiler = false;
    bool had_incremental = false;
    bool had_multi_cache = false;
    char **argv_local;
    size_t i;
    int rc;
    (void)root;
    v3_save_env_value("CHENG_V3_SMOKE_COMPILER", saved_compiler, sizeof(saved_compiler), &had_compiler);
    v3_save_env_value("BACKEND_INCREMENTAL", saved_incremental, sizeof(saved_incremental), &had_incremental);
    v3_save_env_value("BACKEND_MULTI_MODULE_CACHE", saved_multi_cache, sizeof(saved_multi_cache), &had_multi_cache);
    setenv("CHENG_V3_SMOKE_COMPILER", compiler_bin, 1);
    setenv("BACKEND_INCREMENTAL", "0", 1);
    setenv("BACKEND_MULTI_MODULE_CACHE", "0", 1);
    argv_local = (char **)v3_xmalloc(sizeof(char *) * (positional_count + 3U));
    argv_local[0] = (char *)"cheng_v3_seed";
    argv_local[1] = (char *)"run-browser-webrtc-smokes";
    for (i = 0U; i < positional_count; ++i) {
        argv_local[i + 2U] = (char *)(positionals[i] != NULL ? positionals[i] : "");
    }
    argv_local[positional_count + 2U] = NULL;
    rc = v3_cmd_run_browser_webrtc_smokes_impl((int)(positional_count + 2U), argv_local);
    free(argv_local);
    v3_restore_env_value("CHENG_V3_SMOKE_COMPILER", saved_compiler, had_compiler);
    v3_restore_env_value("BACKEND_INCREMENTAL", saved_incremental, had_incremental);
    v3_restore_env_value("BACKEND_MULTI_MODULE_CACHE", saved_multi_cache, had_multi_cache);
    return rc;
}

static int v3_cmd_run_host_smokes_impl(int argc, char **argv) {
    V3BootstrapPaths paths;
    char root_v3[PATH_MAX];
    char out_dir[PATH_MAX];
    char hostrun_root[PATH_MAX];
    char out_bin[PATH_MAX];
    const char *tail_only;
    const char *explicit_compiler;
    const char *env_compiler;
    const char *compiler_bin;
    const char *label;
    const char *positionals[256];
    size_t positional_count;
    size_t i;
    bool run_tail = true;
    static const char *DEFAULT_SMOKES[] = {
        "ref10_ashr_smoke",
        "fixed256_curve25519_smoke",
        "fixedbytes32_seq_index_smoke",
        "compiler_runtime_smoke",
        "artifact_isolation_audit_smoke",
        "backend_driver_command_surface_smoke",
        "backend_driver_candidate_output_smoke",
        "runtime_link_input_surface_smoke",
        "runtime_zero_c_surface_smoke",
        "composite_default_init_smoke",
        "composite_default_init_negative_smoke",
        "chain_node_snapshot_return_boundary_smoke",
        "chain_node_snapshot_write_return_boundary_smoke",
        "chain_node_snapshot_print_return_boundary_smoke",
        "chain_node_cli_return_boundary_smoke",
        "export_visibility_smoke",
        "export_visibility_negative_smoke",
        "strformat_export_negative_smoke",
        "func_range_loop_smoke",
        "json_bracket_assign_smoke",
        "os_dir_exists_smoke",
        "os_file_exists_smoke",
        "os_file_size_smoke",
        "os_get_current_dir_smoke",
        "os_rename_file_smoke",
        "os_remove_dir_smoke",
        "os_walk_dir_smoke",
        "strformat_fmt_smoke",
        "strformat_fmt_negative_smoke",
        "global_fixed_array_composite_smoke",
        "multiline_header_indent_smoke",
        "rwad_serial_state_machine_smoke",
        "rwad_bft_state_machine_smoke",
        "parser_path_smoke",
        "compiler_pipeline_stub_smoke",
        "lowering_plan_smoke",
        "compiler_equivalence_smoke",
        "compiler_publish_smoke",
        "bft_three_replica_smoke",
        "primary_object_plan_smoke",
        "object_native_link_plan_smoke",
        "ffi_handle_smoke",
        "program_selfhost_smoke",
        "csg_smoke",
        "consensus_smoke",
        "chain_node_smoke",
        "chain_node_zero_snapshot_replay_smoke",
        "chain_node_tailnet_smoke",
        "chain_node_libp2p_smoke",
        "bft_finalize_summary_smoke",
        "bft_state_machine_smoke",
        "oracle_plane_smoke",
        "oracle_bft_state_machine_smoke",
        "oracle_bft_three_replica_smoke",
        "oracle_runtime_host_smoke",
        "overlay_contracts_smoke",
        "pubsub_smoke",
        "dag_mempool_smoke",
        "plumtree_smoke",
        "erasure_swarm_smoke",
        "pin_plane_smoke",
        "pin_scheduler_smoke",
        "pin_runtime_host_smoke",
        "pin_runtime_quic_smoke",
        "content_codec_smoke",
        "content_runtime_smoke",
        "bio_reed_solomon_smoke",
        "bio_same_person_smoke",
        "bio_did_chain_node_smoke",
        "bio_did_mobile_biometric_smoke",
        "native_client_hello_wire_smoke",
        "msquic_bindtext_expr_smoke",
        "protocol_validation_smoke",
        "platform_release_validation_smoke",
        "congestion_validation_smoke",
        "msquic_platform_smoke",
        "quic_tls_transport_ecdsa_smoke",
        "quic_transport_loopback_smoke",
        "quic_transport_sequential_accept_smoke",
        "libp2p_quic_tls_smoke",
        "quic_feature_matrix_smoke",
        "quic_close_order_smoke",
        "bft_daemon_quic_close_order_smoke",
        "libp2p_tailnet_policy_smoke",
        "libp2p_tailnet_visibility_smoke",
        "libp2p_tailnet_transport_smoke",
        "libp2p_tailnet_derp_smoke",
        "libp2p_resource_smoke",
        "libp2p_scheduler_smoke",
        "tailnet_control_core_smoke",
        "tailnet_train_island_smoke",
        "webrtc_signal_codec_smoke",
        "webrtc_signal_session_smoke",
        "webrtc_turn_fallback_smoke",
        "libp2p_webrtc_sync_smoke",
        "content_quic_smoke",
        "webrtc_datachannel_content_smoke",
        "content_stub_smoke",
        "libp2p_protocols_smoke",
        "location_proof_smoke",
        "chain_codec_binary_smoke",
        "anti_entropy_smoke",
        "anti_entropy_signature_fields_smoke",
        "lsmr_types_smoke",
        "lsmr_locality_storage_smoke",
        "lsmr_bagua_prefix_tree_smoke",
        "udp_importc_smoke",
        "mobile_bridge_smoke",
        "mobile_sdk_smoke",
        "did_identity_smoke"
    };
    v3_bootstrap_paths_init(&paths);
    v3_join_path(root_v3, sizeof(root_v3), paths.root, "v3");
    explicit_compiler = v3_flag_value(argc, argv, "--compiler");
    env_compiler = getenv("CHENG_V3_SMOKE_COMPILER");
    compiler_bin = (explicit_compiler != NULL && explicit_compiler[0] != '\0') ?
                       explicit_compiler :
                       ((env_compiler != NULL && env_compiler[0] != '\0') ? env_compiler : paths.backend_driver_out);
    if ((explicit_compiler == NULL || explicit_compiler[0] == '\0') &&
        (env_compiler == NULL || env_compiler[0] == '\0')) {
        if (!v3_leaf_prepare_backend_driver(&paths, root_v3, sizeof(root_v3))) {
            return 1;
        }
        compiler_bin = paths.backend_driver_out;
    }
    if (access(compiler_bin, X_OK) != 0) {
        fprintf(stderr, "[cheng_v3_seed] run-host-smokes missing compiler: %s\n", compiler_bin);
        return 1;
    }
    label = v3_leaf_resolve_label(argc, argv, "CHENG_V3_SMOKE_LABEL", "host");
    tail_only = v3_flag_value(argc, argv, "--tail-only");
    if (tail_only != NULL && tail_only[0] != '\0') {
        if (strcmp(tail_only, "wasm") == 0) {
            return v3_run_host_leaf_command("wasm", compiler_bin, label);
        }
        if (strcmp(tail_only, "browser_host_wasm") == 0) {
            return v3_run_host_leaf_command("browser_host_wasm", compiler_bin, label);
        }
        if (strcmp(tail_only, "chain_node_cli") == 0) {
            return v3_run_host_leaf_command("chain_node_cli", compiler_bin, label);
        }
        if (strcmp(tail_only, "chain_node_process") == 0) {
            return v3_run_chain_node_process_smoke_with_env(compiler_bin, label, false);
        }
        if (strcmp(tail_only, "chain_node_three_node") == 0) {
            return v3_run_chain_node_three_node_smoke_with_env(compiler_bin, label, false);
        }
        if (strcmp(tail_only, "tailnet_control") == 0) {
            return v3_run_host_leaf_command("tailnet_control", compiler_bin, label);
        }
        if (strcmp(tail_only, "fresh_node_selfhost") == 0) {
            return v3_run_host_leaf_command("fresh_node_selfhost", compiler_bin, label);
        }
        if (strcmp(tail_only, "migration") == 0) {
            return v3_run_host_leaf_command("migration", compiler_bin, label);
        }
        fprintf(stderr, "[cheng_v3_seed] run-host-smokes unknown --tail-only: %s\n", tail_only);
        return 1;
    }
    positional_count = v3_collect_smoke_positionals(argc, argv, positionals, sizeof(positionals) / sizeof(positionals[0]));
    if (positional_count > 0U) {
        run_tail = false;
    }
    v3_join_path(hostrun_root, sizeof(hostrun_root), paths.root, "artifacts/v3_hostrun");
    if (label != NULL && label[0] != '\0' && strcmp(label, "host") != 0) {
        v3_join_path(out_dir, sizeof(out_dir), hostrun_root, label);
    } else {
        v3_copy_text(out_dir, sizeof(out_dir), hostrun_root);
    }
    if (!v3_mkdir_p(out_dir)) {
        return 1;
    }
    if (positional_count > 0U) {
        for (i = 0U; i < positional_count; ++i) {
            v3_join_path(out_bin, sizeof(out_bin), out_dir, positionals[i]);
            if (!v3_run_fixture_smoke_direct("v3 host smokes",
                                             positionals[i],
                                             compiler_bin,
                                             paths.root,
                                             root_v3,
                                             positionals[i],
                                             out_bin)) {
                return 1;
            }
            if (strcmp(positionals[i], "chain_node_libp2p_smoke") == 0 &&
                v3_run_host_leaf_command("tcp_twoproc", compiler_bin, label) != 0) {
                return 1;
            }
            if (strcmp(positionals[i], "libp2p_quic_tls_smoke") == 0 &&
                v3_run_host_leaf_command("quic_twoproc", compiler_bin, label) != 0) {
                return 1;
            }
        }
    } else {
        for (i = 0U; i < sizeof(DEFAULT_SMOKES) / sizeof(DEFAULT_SMOKES[0]); ++i) {
            v3_join_path(out_bin, sizeof(out_bin), out_dir, DEFAULT_SMOKES[i]);
            if (!v3_run_fixture_smoke_direct("v3 host smokes",
                                             DEFAULT_SMOKES[i],
                                             compiler_bin,
                                             paths.root,
                                             root_v3,
                                             DEFAULT_SMOKES[i],
                                             out_bin)) {
                return 1;
            }
            if (strcmp(DEFAULT_SMOKES[i], "chain_node_libp2p_smoke") == 0 &&
                v3_run_host_leaf_command("tcp_twoproc", compiler_bin, label) != 0) {
                return 1;
            }
            if (strcmp(DEFAULT_SMOKES[i], "libp2p_quic_tls_smoke") == 0 &&
                v3_run_host_leaf_command("quic_twoproc", compiler_bin, label) != 0) {
                return 1;
            }
        }
    }
    if (run_tail) {
        if (v3_run_host_leaf_command("chain_node_cli", compiler_bin, label) != 0 ||
            v3_run_chain_node_process_smoke_with_env(compiler_bin, label, false) != 0 ||
            v3_run_chain_node_three_node_smoke_with_env(compiler_bin, label, false) != 0 ||
            v3_run_host_leaf_command("tailnet_control", compiler_bin, label) != 0 ||
            v3_run_host_leaf_command("fresh_node_selfhost", compiler_bin, label) != 0 ||
            v3_run_host_leaf_command("migration", compiler_bin, label) != 0) {
            return 1;
        }
    }
    puts("v3 host smokes: ok");
    return 0;
}

static int v3_cmd_verify_backend_driver_command_surface_impl(int argc, char **argv) {
    char *surface_argv[3];
    const char *argv0 = "cheng_v3_seed";
    (void)argc;
    if (argv != NULL && argv[0] != NULL && argv[0][0] != '\0') {
        argv0 = argv[0];
    }
    surface_argv[0] = (char *)argv0;
    surface_argv[1] = (char *)"run-host-smokes";
    surface_argv[2] = (char *)"backend_driver_command_surface_smoke";
    return v3_cmd_run_host_smokes_impl(3, surface_argv);
}

static int v3_cmd_verify_mobile_shell_build_probe_impl(int argc, char **argv) {
    return v3_require_backend_driver_cli_passthrough("verify-mobile-shell-build-probe", argc, argv);
}

static int v3_cmd_verify_export_visibility_impl(int argc, char **argv) {
    char compiler_flag[PATH_MAX + 16];
    char label_flag[128];
    char *surface_argv[7];
    const char *argv0 = "cheng_v3_seed";
    (void)argc;
    if (argv != NULL && argv[0] != NULL && argv[0][0] != '\0') {
        argv0 = argv[0];
    }
    snprintf(compiler_flag, sizeof(compiler_flag), "--compiler:%s", argv0);
    snprintf(label_flag, sizeof(label_flag), "--label:%s", v3_verify_export_visibility_label_for_argv0(argv0));
    surface_argv[0] = (char *)argv0;
    surface_argv[1] = (char *)"run-host-smokes";
    surface_argv[2] = compiler_flag;
    surface_argv[3] = label_flag;
    surface_argv[4] = (char *)"export_visibility_smoke";
    surface_argv[5] = (char *)"export_visibility_negative_smoke";
    surface_argv[6] = (char *)"strformat_export_negative_smoke";
    return v3_cmd_run_host_smokes_impl(7, surface_argv);
}

static int v3_cmd_verify_export_visibility_parallel_impl(int argc, char **argv) {
    char compiler_flag[PATH_MAX + 16];
    char label_flag[128];
    char *surface_argv[5];
    const char *argv0 = "cheng_v3_seed";
    (void)argc;
    if (argv != NULL && argv[0] != NULL && argv[0][0] != '\0') {
        argv0 = argv[0];
    }
    snprintf(compiler_flag, sizeof(compiler_flag), "--compiler:%s", argv0);
    snprintf(label_flag,
             sizeof(label_flag),
             "--label:%s",
             v3_verify_export_visibility_parallel_label_for_argv0(argv0));
    surface_argv[0] = (char *)argv0;
    surface_argv[1] = (char *)"run-host-smokes";
    surface_argv[2] = compiler_flag;
    surface_argv[3] = label_flag;
    surface_argv[4] = (char *)"export_visibility_parallel_smoke";
    return v3_cmd_run_host_smokes_impl(5, surface_argv);
}

static int v3_cmd_verify_r2c_react_v3_surface_impl(int argc, char **argv) {
    char *surface_argv[3];
    const char *argv0 = "cheng_v3_seed";
    (void)argc;
    if (argv != NULL && argv[0] != NULL && argv[0][0] != '\0') {
        argv0 = argv[0];
    }
    surface_argv[0] = (char *)argv0;
    surface_argv[1] = (char *)"run-host-smokes";
    surface_argv[2] = (char *)"r2c_react_v3_surface_smoke";
    return v3_cmd_run_host_smokes_impl(3, surface_argv);
}

static int v3_cmd_run_stage23_libp2p_smokes_impl(int argc, char **argv) {
    V3BootstrapPaths paths;
    char root_v3[PATH_MAX];
    char out_dir[PATH_MAX];
    char out_bin[PATH_MAX];
    char compile_label[PATH_MAX];
    const char *tail_only;
    const char *positionals[256];
    size_t positional_count;
    size_t i;
    char saved_incremental[64];
    char saved_multi_cache[64];
    bool had_incremental = false;
    bool had_multi_cache = false;
    bool run_tail = true;
    static const char *DEFAULT_SMOKES[] = {
        "fixed256_curve25519_smoke",
        "compiler_runtime_smoke",
        "compiler_pipeline_stub_smoke",
        "lowering_plan_smoke",
        "compiler_equivalence_smoke",
        "compiler_publish_smoke",
        "bft_three_replica_smoke",
        "primary_object_plan_smoke",
        "object_native_link_plan_smoke",
        "program_selfhost_smoke",
        "libp2p_core_smoke",
        "libp2p_multiaddr_parse_boundary_smoke",
        "libp2p_host_smoke",
        "libp2p_tailnet_policy_smoke",
        "libp2p_tailnet_visibility_smoke",
        "libp2p_tailnet_transport_smoke",
        "libp2p_tailnet_derp_smoke",
        "libp2p_resource_smoke",
        "libp2p_scheduler_smoke",
        "libp2p_quic_tls_smoke",
        "quic_feature_matrix_smoke",
        "quic_close_order_smoke",
        "bft_daemon_quic_close_order_smoke",
        "tailnet_control_core_smoke",
        "tailnet_train_island_smoke",
        "webrtc_signal_codec_smoke",
        "webrtc_signal_session_smoke",
        "webrtc_turn_fallback_smoke",
        "libp2p_webrtc_sync_smoke",
        "libp2p_tcp_smoke",
        "libp2p_overlay_smoke",
        "libp2p_protocols_smoke",
        "chain_codec_binary_smoke",
        "location_proof_smoke",
        "anti_entropy_smoke",
        "anti_entropy_signature_fields_smoke",
        "lsmr_types_smoke",
        "lsmr_locality_storage_smoke",
        "lsmr_bagua_prefix_tree_smoke",
        "overlay_contracts_smoke",
        "pubsub_smoke",
        "dag_mempool_smoke",
        "plumtree_smoke",
        "erasure_swarm_smoke",
        "pin_plane_smoke",
        "pin_scheduler_smoke",
        "content_codec_smoke",
        "content_runtime_smoke",
        "content_stub_smoke",
        "chain_node_tailnet_smoke",
        "chain_node_libp2p_smoke",
        "chain_node_zero_snapshot_replay_smoke",
        "mobile_bridge_smoke",
        "mobile_sdk_smoke",
        "did_identity_smoke"
    };
    v3_bootstrap_paths_init(&paths);
    if (paths.root[0] == '\0') {
        fprintf(stderr, "[cheng_v3_seed] run-stage23-libp2p-smokes root detect failed\n");
        return 1;
    }
    if (!v3_bootstrap_artifacts_fresh(&paths) && v3_cmd_bootstrap_bridge(0, NULL) != 0) {
        return 1;
    }
    v3_join_path(root_v3, sizeof(root_v3), paths.root, "v3");
    if (access(paths.stage2, X_OK) != 0 || access(paths.stage3, X_OK) != 0) {
        fprintf(stderr, "[cheng_v3_seed] run-stage23-libp2p-smokes missing bootstrap compiler\n");
        return 1;
    }
    tail_only = v3_flag_value(argc, argv, "--tail-only");
    positional_count = v3_collect_smoke_positionals(argc, argv, positionals, sizeof(positionals) / sizeof(positionals[0]));
    if (positional_count > 0U) {
        run_tail = false;
    }
    v3_save_env_value("BACKEND_INCREMENTAL", saved_incremental, sizeof(saved_incremental), &had_incremental);
    v3_save_env_value("BACKEND_MULTI_MODULE_CACHE", saved_multi_cache, sizeof(saved_multi_cache), &had_multi_cache);
    setenv("BACKEND_INCREMENTAL", "0", 1);
    setenv("BACKEND_MULTI_MODULE_CACHE", "0", 1);
    if (tail_only != NULL && tail_only[0] != '\0') {
        if (strcmp(tail_only, "browser_webrtc") == 0) {
            int rc = v3_run_stage23_browser_webrtc_script(paths.root, paths.stage3, positional_count, positionals);
            v3_restore_env_value("BACKEND_INCREMENTAL", saved_incremental, had_incremental);
            v3_restore_env_value("BACKEND_MULTI_MODULE_CACHE", saved_multi_cache, had_multi_cache);
            return rc;
        }
        fprintf(stderr, "[cheng_v3_seed] run-stage23-libp2p-smokes unknown --tail-only: %s\n", tail_only);
        v3_restore_env_value("BACKEND_INCREMENTAL", saved_incremental, had_incremental);
        v3_restore_env_value("BACKEND_MULTI_MODULE_CACHE", saved_multi_cache, had_multi_cache);
        return 1;
    }
    v3_join_path(out_dir, sizeof(out_dir), paths.root, "artifacts/v3_stage23_libp2p");
    if (!v3_mkdir_p(out_dir)) {
        v3_restore_env_value("BACKEND_INCREMENTAL", saved_incremental, had_incremental);
        v3_restore_env_value("BACKEND_MULTI_MODULE_CACHE", saved_multi_cache, had_multi_cache);
        return 1;
    }
    if (positional_count > 0U) {
        for (i = 0U; i < positional_count; ++i) {
            v3_join_path(out_bin, sizeof(out_bin), out_dir, positionals[i]);
            strncat(out_bin, ".stage2", sizeof(out_bin) - strlen(out_bin) - 1U);
            snprintf(compile_label, sizeof(compile_label), "stage2 %s", positionals[i]);
            if (!v3_run_fixture_smoke_direct("v3 stage2/stage3 libp2p",
                                             compile_label,
                                             paths.stage2,
                                             paths.root,
                                             root_v3,
                                             positionals[i],
                                             out_bin)) {
                v3_restore_env_value("BACKEND_INCREMENTAL", saved_incremental, had_incremental);
                v3_restore_env_value("BACKEND_MULTI_MODULE_CACHE", saved_multi_cache, had_multi_cache);
                return 1;
            }
            if (strcmp(positionals[i], "libp2p_tcp_smoke") == 0 &&
                v3_run_stage23_leaf_command("tcp_twoproc", paths.stage2, "stage2") != 0) {
                v3_restore_env_value("BACKEND_INCREMENTAL", saved_incremental, had_incremental);
                v3_restore_env_value("BACKEND_MULTI_MODULE_CACHE", saved_multi_cache, had_multi_cache);
                return 1;
            }
            if (strcmp(positionals[i], "libp2p_quic_tls_smoke") == 0 &&
                v3_run_stage23_leaf_command("quic_twoproc", paths.stage2, "stage2") != 0) {
                v3_restore_env_value("BACKEND_INCREMENTAL", saved_incremental, had_incremental);
                v3_restore_env_value("BACKEND_MULTI_MODULE_CACHE", saved_multi_cache, had_multi_cache);
                return 1;
            }
            v3_join_path(out_bin, sizeof(out_bin), out_dir, positionals[i]);
            strncat(out_bin, ".stage3", sizeof(out_bin) - strlen(out_bin) - 1U);
            snprintf(compile_label, sizeof(compile_label), "stage3 %s", positionals[i]);
            if (!v3_run_fixture_smoke_direct("v3 stage2/stage3 libp2p",
                                             compile_label,
                                             paths.stage3,
                                             paths.root,
                                             root_v3,
                                             positionals[i],
                                             out_bin)) {
                v3_restore_env_value("BACKEND_INCREMENTAL", saved_incremental, had_incremental);
                v3_restore_env_value("BACKEND_MULTI_MODULE_CACHE", saved_multi_cache, had_multi_cache);
                return 1;
            }
            if (strcmp(positionals[i], "libp2p_tcp_smoke") == 0 &&
                v3_run_stage23_leaf_command("tcp_twoproc", paths.stage3, "stage3") != 0) {
                v3_restore_env_value("BACKEND_INCREMENTAL", saved_incremental, had_incremental);
                v3_restore_env_value("BACKEND_MULTI_MODULE_CACHE", saved_multi_cache, had_multi_cache);
                return 1;
            }
            if (strcmp(positionals[i], "libp2p_quic_tls_smoke") == 0 &&
                v3_run_stage23_leaf_command("quic_twoproc", paths.stage3, "stage3") != 0) {
                v3_restore_env_value("BACKEND_INCREMENTAL", saved_incremental, had_incremental);
                v3_restore_env_value("BACKEND_MULTI_MODULE_CACHE", saved_multi_cache, had_multi_cache);
                return 1;
            }
        }
    } else {
        for (i = 0U; i < sizeof(DEFAULT_SMOKES) / sizeof(DEFAULT_SMOKES[0]); ++i) {
            v3_join_path(out_bin, sizeof(out_bin), out_dir, DEFAULT_SMOKES[i]);
            strncat(out_bin, ".stage2", sizeof(out_bin) - strlen(out_bin) - 1U);
            snprintf(compile_label, sizeof(compile_label), "stage2 %s", DEFAULT_SMOKES[i]);
            if (!v3_run_fixture_smoke_direct("v3 stage2/stage3 libp2p",
                                             compile_label,
                                             paths.stage2,
                                             paths.root,
                                             root_v3,
                                             DEFAULT_SMOKES[i],
                                             out_bin)) {
                v3_restore_env_value("BACKEND_INCREMENTAL", saved_incremental, had_incremental);
                v3_restore_env_value("BACKEND_MULTI_MODULE_CACHE", saved_multi_cache, had_multi_cache);
                return 1;
            }
            if (strcmp(DEFAULT_SMOKES[i], "libp2p_tcp_smoke") == 0 &&
                v3_run_stage23_leaf_command("tcp_twoproc", paths.stage2, "stage2") != 0) {
                v3_restore_env_value("BACKEND_INCREMENTAL", saved_incremental, had_incremental);
                v3_restore_env_value("BACKEND_MULTI_MODULE_CACHE", saved_multi_cache, had_multi_cache);
                return 1;
            }
            if (strcmp(DEFAULT_SMOKES[i], "libp2p_quic_tls_smoke") == 0 &&
                v3_run_stage23_leaf_command("quic_twoproc", paths.stage2, "stage2") != 0) {
                v3_restore_env_value("BACKEND_INCREMENTAL", saved_incremental, had_incremental);
                v3_restore_env_value("BACKEND_MULTI_MODULE_CACHE", saved_multi_cache, had_multi_cache);
                return 1;
            }
            v3_join_path(out_bin, sizeof(out_bin), out_dir, DEFAULT_SMOKES[i]);
            strncat(out_bin, ".stage3", sizeof(out_bin) - strlen(out_bin) - 1U);
            snprintf(compile_label, sizeof(compile_label), "stage3 %s", DEFAULT_SMOKES[i]);
            if (!v3_run_fixture_smoke_direct("v3 stage2/stage3 libp2p",
                                             compile_label,
                                             paths.stage3,
                                             paths.root,
                                             root_v3,
                                             DEFAULT_SMOKES[i],
                                             out_bin)) {
                v3_restore_env_value("BACKEND_INCREMENTAL", saved_incremental, had_incremental);
                v3_restore_env_value("BACKEND_MULTI_MODULE_CACHE", saved_multi_cache, had_multi_cache);
                return 1;
            }
            if (strcmp(DEFAULT_SMOKES[i], "libp2p_tcp_smoke") == 0 &&
                v3_run_stage23_leaf_command("tcp_twoproc", paths.stage3, "stage3") != 0) {
                v3_restore_env_value("BACKEND_INCREMENTAL", saved_incremental, had_incremental);
                v3_restore_env_value("BACKEND_MULTI_MODULE_CACHE", saved_multi_cache, had_multi_cache);
                return 1;
            }
            if (strcmp(DEFAULT_SMOKES[i], "libp2p_quic_tls_smoke") == 0 &&
                v3_run_stage23_leaf_command("quic_twoproc", paths.stage3, "stage3") != 0) {
                v3_restore_env_value("BACKEND_INCREMENTAL", saved_incremental, had_incremental);
                v3_restore_env_value("BACKEND_MULTI_MODULE_CACHE", saved_multi_cache, had_multi_cache);
                return 1;
            }
        }
    }
    if (run_tail) {
        if (v3_run_stage23_leaf_command("chain_node_cli", paths.stage2, "stage2") != 0 ||
            v3_run_stage23_leaf_command("chain_node_cli", paths.stage3, "stage3") != 0 ||
            v3_run_chain_node_process_smoke_with_env(paths.stage2, "stage2", true) != 0 ||
            v3_run_chain_node_process_smoke_with_env(paths.stage3, "stage3", true) != 0 ||
            v3_run_chain_node_three_node_smoke_with_env(paths.stage2, "stage2", true) != 0 ||
            v3_run_chain_node_three_node_smoke_with_env(paths.stage3, "stage3", true) != 0 ||
            v3_run_stage23_leaf_command("tailnet_control", paths.stage2, "stage2") != 0 ||
            v3_run_stage23_leaf_command("tailnet_control", paths.stage3, "stage3") != 0 ||
            v3_run_stage23_leaf_command("fresh_node_selfhost", paths.stage2, "stage2") != 0 ||
            v3_run_stage23_leaf_command("fresh_node_selfhost", paths.stage3, "stage3") != 0 ||
            v3_run_stage23_leaf_command("migration", paths.stage2, "stage2") != 0 ||
            v3_run_stage23_leaf_command("migration", paths.stage3, "stage3") != 0 ||
            v3_run_stage23_browser_webrtc_script(paths.root, paths.stage3, 0U, NULL) != 0) {
            v3_restore_env_value("BACKEND_INCREMENTAL", saved_incremental, had_incremental);
            v3_restore_env_value("BACKEND_MULTI_MODULE_CACHE", saved_multi_cache, had_multi_cache);
            return 1;
        }
    }
    v3_restore_env_value("BACKEND_INCREMENTAL", saved_incremental, had_incremental);
    v3_restore_env_value("BACKEND_MULTI_MODULE_CACHE", saved_multi_cache, had_multi_cache);
    puts("v3 stage2/stage3 libp2p: ok");
    return 0;
}

static bool v3_write_optional_report_text(const char *report_path,
                                          const char *text) {
    if (!report_path || *report_path == '\0') {
        return true;
    }
    return v3_ensure_parent_dir(report_path) && v3_write_text_file(report_path, text);
}

static char *v3_report_with_header(const char *header,
                                   const char *body) {
    size_t header_len = header != NULL ? strlen(header) : 0U;
    size_t body_len = body != NULL ? strlen(body) : 0U;
    char *out = (char *)v3_xmalloc(header_len + body_len + 3U);
    if (body_len > 0U) {
        snprintf(out, header_len + body_len + 3U, "%s\n%s", header, body);
    } else {
        snprintf(out, header_len + body_len + 3U, "%s\n", header);
    }
    return out;
}

static void v3_default_debug_output_path(const V3SystemLinkPlanStub *plan,
                                         char *out,
                                         size_t cap) {
    char safe_module[PATH_MAX];
    char safe_target[PATH_MAX];
    char safe_emit[64];
    v3_sanitize_path_part(plan->module_stem[0] != '\0' ? plan->module_stem : "debug",
                          safe_module,
                          sizeof(safe_module));
    v3_sanitize_path_part(plan->target_triple[0] != '\0' ? plan->target_triple : "host",
                          safe_target,
                          sizeof(safe_target));
    v3_sanitize_path_part(plan->emit_kind[0] != '\0' ? plan->emit_kind : "exe",
                          safe_emit,
                          sizeof(safe_emit));
    snprintf(out,
             cap,
             "%s/artifacts/v3_debug/%s.%s.%s",
             plan->workspace_root,
             safe_module,
             safe_target,
             safe_emit);
}

static void CHENG_V3_UNUSED v3_default_profile_output_path(const V3SystemLinkPlanStub *plan,
                                                           char *out,
                                                           size_t cap) {
    char prefix[PATH_MAX];
    v3_default_debug_output_path(plan, prefix, sizeof(prefix));
    snprintf(out, cap, "%s.profile.txt", prefix);
}

static bool v3_build_exec_context(const V3BootstrapContract *contract,
                                  int argc,
                                  char **argv,
                                  bool require_output,
                                  bool enable_baseline_surface_check,
                                  V3SystemLinkPlanStub *plan,
                                  V3CompilerWorldArtifacts *world,
                                  V3LoweringPlanStub **lowering_out,
                                  V3PrimaryObjectPlanStub *primary,
                                  V3ObjectPlanStub *object_plan,
                                  V3NativeLinkPlanStub *native_link) {
    const char *channel = v3_flag_value(argc, argv, "--channel");
    const char *requested_world_head = v3_flag_value(argc, argv, "--world-head");
    const char *lock_path = v3_flag_value(argc, argv, "--lock");
    const char *baseline_surface_path = enable_baseline_surface_check ?
                                            v3_flag_value(argc, argv, "--baseline-surface") :
                                            NULL;
    V3LoweringPlanStub *lowering = NULL;
    if (!v3_build_system_link_plan_stub(contract, argc, argv, require_output, plan)) {
        return false;
    }
    if (!require_output && plan->output_path[0] == '\0') {
        v3_default_debug_output_path(plan, plan->output_path, sizeof(plan->output_path));
    }
    if (!v3_build_compiler_world_artifacts(plan,
                                           channel ? channel : "",
                                           requested_world_head ? requested_world_head : "",
                                           lock_path ? lock_path : "",
                                           baseline_surface_path ? baseline_surface_path : "",
                                           world)) {
        return false;
    }
    lowering = (V3LoweringPlanStub *)v3_xmalloc(sizeof(V3LoweringPlanStub));
    if (!v3_build_lowering_plan_stub(plan, lowering)) {
        free(lowering);
        v3_compiler_world_artifacts_free(world);
        return false;
    }
    v3_sync_lowering_exports_from_world(world, lowering);
    if (!v3_build_primary_object_plan_stub(plan, lowering, primary)) {
        free(lowering);
        v3_compiler_world_artifacts_free(world);
        return false;
    }
    if (!v3_build_object_plan_stub(plan, lowering, primary, object_plan)) {
        free(lowering);
        v3_compiler_world_artifacts_free(world);
        return false;
    }
    if (!v3_build_native_link_plan_stub(plan, primary, object_plan, native_link)) {
        free(lowering);
        v3_compiler_world_artifacts_free(world);
        return false;
    }
    *lowering_out = lowering;
    return true;
}

static char *v3_debug_symbols_report(const V3SystemLinkPlanStub *plan,
                                     const V3CompilerWorldArtifacts *world,
                                     const V3LoweringPlanStub *lowering,
                                     const V3PrimaryObjectPlanStub *primary,
                                     const V3ObjectPlanStub *object_plan,
                                     const V3NativeLinkPlanStub *native_link) {
    size_t cap = 32768U + lowering->function_count * 512U + primary->item_count * PATH_MAX;
    size_t used = 0U;
    char *out = (char *)v3_xmalloc(cap);
    char line[PATH_MAX * 2];
    size_t i;
    out[0] = '\0';
    v3_report_append(&out, &cap, &used, "v3_symbols_v1");
    snprintf(line, sizeof(line), "entry=%s", plan->entry_path);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "target=%s", plan->target_triple);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "emit=%s", plan->emit_kind);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "world_export_count=%zu", world != NULL ? world->exported_count : 0U);
    v3_report_append(&out, &cap, &used, line);
    if (world != NULL) {
        for (i = 0U; i < world->exported_count; ++i) {
            snprintf(line,
                     sizeof(line),
                     "world_export[%zu]=%s|%s|%d|%d|%d",
                     i,
                     world->exports[i].symbol_text,
                     world->exports[i].export_symbol_name,
                     world->exports[i].layout_kind,
                     world->exports[i].effect_kind,
                     world->exports[i].capability_kind);
            v3_report_append(&out, &cap, &used, line);
        }
    }
    snprintf(line, sizeof(line), "lowering_symbol_count=%zu", lowering->function_count);
    v3_report_append(&out, &cap, &used, line);
    for (i = 0U; i < lowering->function_count; ++i) {
        char abi_summary[256];
        const V3LoweredFunctionStub *function = &lowering->functions[i];
        v3_lowered_function_abi_summary(function, abi_summary, sizeof(abi_summary));
        snprintf(line, sizeof(line), "lowering_symbol[%zu]=%s", i, function->symbol_text);
        v3_report_append(&out, &cap, &used, line);
        snprintf(line, sizeof(line), "lowering_meta[%zu]=%s|%s|%s|%zu|%zu|%zu|entry=%d|reachable=%d|export=%d|export_name=%s",
                 i,
                 function->function_name,
                 function->body_kind,
                 abi_summary,
                 function->signature_line_number,
                 function->body_first_line_number,
                 function->body_last_line_number,
                 function->is_entry ? 1 : 0,
                 function->reachable ? 1 : 0,
                 function->export_symbol_name[0] != '\0' ? 1 : 0,
                 function->export_symbol_name[0] != '\0' ? function->export_symbol_name : "-");
        v3_report_append(&out, &cap, &used, line);
        snprintf(line, sizeof(line), "lowering_source[%zu]=%s", i, function->source_path);
        v3_report_append(&out, &cap, &used, line);
    }
    snprintf(line, sizeof(line), "primary_symbol_count=%zu", primary->item_count);
    v3_report_append(&out, &cap, &used, line);
    for (i = 0U; i < primary->item_count; ++i) {
        snprintf(line, sizeof(line), "primary_symbol[%zu]=%s", i, primary->symbol_names[i].text);
        v3_report_append(&out, &cap, &used, line);
    }
    snprintf(line, sizeof(line), "primary_unsupported_count=%zu", primary->unsupported_function_count);
    v3_report_append(&out, &cap, &used, line);
    for (i = 0U; i < primary->unsupported_function_count; ++i) {
        snprintf(line, sizeof(line), "primary_unsupported[%zu]=%s|%s|%s|%s",
                 i,
                 primary->unsupported_function_symbols[i].text,
                 primary->unsupported_body_kinds[i],
                 primary->unsupported_abi_summaries[i],
                 primary->unsupported_first_lines[i]);
        v3_report_append(&out, &cap, &used, line);
    }
    snprintf(line, sizeof(line), "provider_object_count=%zu", object_plan->provider_object_count);
    v3_report_append(&out, &cap, &used, line);
    for (i = 0U; i < object_plan->provider_object_count; ++i) {
        snprintf(line, sizeof(line), "provider_object[%zu]=%s", i, object_plan->provider_object_paths[i].text);
        v3_report_append(&out, &cap, &used, line);
    }
    snprintf(line, sizeof(line), "native_link_input_count=%zu", native_link->link_input_count);
    v3_report_append(&out, &cap, &used, line);
    for (i = 0U; i < native_link->link_input_count; ++i) {
        snprintf(line, sizeof(line), "native_link_input[%zu]=%s", i, native_link->link_input_paths[i].text);
        v3_report_append(&out, &cap, &used, line);
    }
    return out;
}

static const char *v3_elf_layout_kind_name(int kind) {
    if (kind == V3_ELF_LAYOUT_TEXT) {
        return "text";
    }
    if (kind == V3_ELF_LAYOUT_RODATA) {
        return "rodata";
    }
    if (kind == V3_ELF_LAYOUT_DATA) {
        return "data";
    }
    if (kind == V3_ELF_LAYOUT_BSS) {
        return "bss";
    }
    return "none";
}

static const char *v3_elf_file_type_name(uint16_t file_type) {
    if (file_type == V3_ELF_ET_REL) {
        return "rel";
    }
    if (file_type == V3_ELF_ET_EXEC) {
        return "exec";
    }
    return "unknown";
}

static char *v3_debug_elf_object_report(const char *path) {
    V3ElfObjectStub obj;
    size_t cap = 65536U;
    size_t used = 0U;
    char *out = (char *)v3_xmalloc(cap);
    char line[PATH_MAX * 2];
    size_t i;
    memset(&obj, 0, sizeof(obj));
    if (!v3_parse_elf_relocatable_object(path, &obj)) {
        free(out);
        return NULL;
    }
    out[0] = '\0';
    v3_report_append(&out, &cap, &used, "v3_object_report_v1");
    v3_report_append(&out, &cap, &used, "format=elf64");
    snprintf(line, sizeof(line), "path=%s", obj.path);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "elf_type=%s", v3_elf_file_type_name(obj.file_type));
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "machine=%u", (unsigned)obj.machine);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "section_count=%zu", obj.section_count);
    v3_report_append(&out, &cap, &used, line);
    for (i = 0U; i < obj.section_count; ++i) {
        snprintf(line, sizeof(line), "section[%zu]=%s|type=%u|layout=%s|offset=%llu|size=%llu|align=%llu",
                 i,
                 obj.sections[i].name,
                 (unsigned)obj.sections[i].type,
                 v3_elf_layout_kind_name(obj.sections[i].layout_kind),
                 (unsigned long long)obj.sections[i].offset,
                 (unsigned long long)obj.sections[i].size,
                 (unsigned long long)obj.sections[i].addralign);
        v3_report_append(&out, &cap, &used, line);
    }
    snprintf(line, sizeof(line), "load_command_count=0");
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "symbol_count=%zu", obj.symbol_count);
    v3_report_append(&out, &cap, &used, line);
    for (i = 0U; i < obj.symbol_count; ++i) {
        snprintf(line, sizeof(line), "symbol[%zu]=%s|bind=%u|type=%u|shndx=%u|value=%llu|size=%llu",
                 i,
                 obj.symbols[i].name[0] != '\0' ? obj.symbols[i].name : "<anonymous>",
                 (unsigned)obj.symbols[i].bind,
                 (unsigned)obj.symbols[i].type,
                 (unsigned)obj.symbols[i].shndx,
                 (unsigned long long)obj.symbols[i].value,
                 (unsigned long long)obj.symbols[i].size);
        v3_report_append(&out, &cap, &used, line);
    }
    snprintf(line, sizeof(line), "reloc_count=%zu", obj.reloc_count);
    v3_report_append(&out, &cap, &used, line);
    for (i = 0U; i < obj.reloc_count; ++i) {
        const V3ElfRelocStub *rel = &obj.relocs[i];
        const char *target_name = rel->target_shndx < obj.section_count ?
                                      obj.sections[rel->target_shndx].name :
                                      "<bad-section>";
        const char *symbol_name = rel->sym_index < obj.symbol_count &&
                                          obj.symbols[rel->sym_index].name[0] != '\0' ?
                                      obj.symbols[rel->sym_index].name :
                                      "<anonymous>";
        snprintf(line, sizeof(line), "reloc[%zu]=target=%s|offset=%llu|type=%u|symbol=%s|addend=%lld",
                 i,
                 target_name,
                 (unsigned long long)rel->offset,
                 (unsigned)rel->type,
                 symbol_name,
                 (long long)rel->addend);
        v3_report_append(&out, &cap, &used, line);
    }
    v3_report_append(&out, &cap, &used, "import_count=0");
    v3_elf_object_free(&obj);
    return out;
}

static char *v3_debug_macho_object_report(const char *path) {
    unsigned char *bytes = NULL;
    size_t len = 0U;
    size_t cap = 65536U;
    size_t used = 0U;
    char *out = (char *)v3_xmalloc(cap);
    char line[PATH_MAX * 2];
    size_t section_count = 0U;
    size_t symbol_count = 0U;
    size_t reloc_count = 0U;
    uint32_t ncmds;
    uint32_t filetype;
    uint32_t flags;
    uint32_t cputype;
    uint32_t cpusubtype;
    uint32_t symoff = 0U;
    uint32_t nsyms = 0U;
    uint32_t stroff = 0U;
    uint32_t strsize = 0U;
    size_t load_off = 32U;
    size_t i;
    if (!v3_read_binary_file_alloc(path, &bytes, &len)) {
        free(out);
        return NULL;
    }
    if (!v3_range_fits(len, 0U, 32U) || v3_read_u32_le_at(bytes, 0U) != V3_MACHO_MAGIC_64) {
        fprintf(stderr, "[cheng_v3_seed] unsupported Mach-O object: %s\n", path);
        free(bytes);
        free(out);
        return NULL;
    }
    cputype = v3_read_u32_le_at(bytes, 4U);
    cpusubtype = v3_read_u32_le_at(bytes, 8U);
    filetype = v3_read_u32_le_at(bytes, 12U);
    ncmds = v3_read_u32_le_at(bytes, 16U);
    flags = v3_read_u32_le_at(bytes, 24U);
    out[0] = '\0';
    v3_report_append(&out, &cap, &used, "v3_object_report_v1");
    v3_report_append(&out, &cap, &used, "format=macho64");
    snprintf(line, sizeof(line), "path=%s", path);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "cputype=%u", cputype);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "cpusubtype=%u", cpusubtype);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "filetype=%u", filetype);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "flags=%u", flags);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "load_command_count=%u", ncmds);
    v3_report_append(&out, &cap, &used, line);
    for (i = 0U; i < (size_t)ncmds; ++i) {
        uint32_t cmd;
        uint32_t cmdsize;
        if (!v3_range_fits(len, load_off, 8U)) {
            free(bytes);
            free(out);
            return NULL;
        }
        cmd = v3_read_u32_le_at(bytes, load_off + 0U);
        cmdsize = v3_read_u32_le_at(bytes, load_off + 4U);
        if (cmdsize < 8U || !v3_range_fits(len, load_off, cmdsize)) {
            free(bytes);
            free(out);
            return NULL;
        }
        snprintf(line, sizeof(line), "load_command[%zu]=cmd=0x%x|size=%u", i, cmd, cmdsize);
        v3_report_append(&out, &cap, &used, line);
        if (cmd == V3_MACHO_LC_SEGMENT_64 && cmdsize >= 72U) {
            char segname[32];
            uint32_t nsects = v3_read_u32_le_at(bytes, load_off + 64U);
            size_t sect_off = load_off + 72U;
            size_t j;
            v3_copy_fixed_name_bytes(bytes + load_off + 8U, 16U, segname, sizeof(segname));
            for (j = 0U; j < (size_t)nsects; ++j) {
                char sectname[32];
                char seglabel[32];
                uint64_t addr;
                uint64_t size_value;
                uint32_t offset;
                uint32_t align;
                uint32_t reloff;
                uint32_t nreloc;
                uint32_t sect_flags;
                size_t k;
                if (!v3_range_fits(len, sect_off, 80U)) {
                    free(bytes);
                    free(out);
                    return NULL;
                }
                v3_copy_fixed_name_bytes(bytes + sect_off + 0U, 16U, sectname, sizeof(sectname));
                v3_copy_fixed_name_bytes(bytes + sect_off + 16U, 16U, seglabel, sizeof(seglabel));
                addr = v3_read_u64_le_at(bytes, sect_off + 32U);
                size_value = v3_read_u64_le_at(bytes, sect_off + 40U);
                offset = v3_read_u32_le_at(bytes, sect_off + 48U);
                align = v3_read_u32_le_at(bytes, sect_off + 52U);
                reloff = v3_read_u32_le_at(bytes, sect_off + 56U);
                nreloc = v3_read_u32_le_at(bytes, sect_off + 60U);
                sect_flags = v3_read_u32_le_at(bytes, sect_off + 64U);
                snprintf(line,
                         sizeof(line),
                         "section[%zu]=%s,%s|addr=%llu|size=%llu|offset=%u|align=%u|flags=0x%x",
                         section_count,
                         seglabel[0] != '\0' ? seglabel : segname,
                         sectname,
                         (unsigned long long)addr,
                         (unsigned long long)size_value,
                         offset,
                         align,
                         sect_flags);
                v3_report_append(&out, &cap, &used, line);
                for (k = 0U; k < (size_t)nreloc; ++k) {
                    uint32_t r_address;
                    uint32_t raw;
                    uint32_t symbolnum;
                    uint32_t pcrel;
                    uint32_t length;
                    uint32_t ext;
                    uint32_t type;
                    size_t reloc_off = (size_t)reloff + k * 8U;
                    if (!v3_range_fits(len, reloc_off, 8U)) {
                        free(bytes);
                        free(out);
                        return NULL;
                    }
                    r_address = v3_read_u32_le_at(bytes, reloc_off + 0U);
                    raw = v3_read_u32_le_at(bytes, reloc_off + 4U);
                    symbolnum = raw & 0x00ffffffU;
                    pcrel = (raw >> 24U) & 0x1U;
                    length = (raw >> 25U) & 0x3U;
                    ext = (raw >> 27U) & 0x1U;
                    type = (raw >> 28U) & 0xfU;
                    snprintf(line,
                             sizeof(line),
                             "reloc[%zu]=target=%s,%s|address=%u|symbol=%u|pcrel=%u|length=%u|extern=%u|type=%u",
                             reloc_count,
                             seglabel[0] != '\0' ? seglabel : segname,
                             sectname,
                             r_address,
                             symbolnum,
                             pcrel,
                             length,
                             ext,
                             type);
                    v3_report_append(&out, &cap, &used, line);
                    reloc_count += 1U;
                }
                section_count += 1U;
                sect_off += 80U;
            }
        } else if (cmd == V3_MACHO_LC_SYMTAB && cmdsize >= 24U) {
            symoff = v3_read_u32_le_at(bytes, load_off + 8U);
            nsyms = v3_read_u32_le_at(bytes, load_off + 12U);
            stroff = v3_read_u32_le_at(bytes, load_off + 16U);
            strsize = v3_read_u32_le_at(bytes, load_off + 20U);
        }
        load_off += cmdsize;
    }
    snprintf(line, sizeof(line), "section_count=%zu", section_count);
    v3_report_append(&out, &cap, &used, line);
    if (nsyms > 0U) {
        const unsigned char *strtab = NULL;
        for (i = 0U; i < (size_t)nsyms; ++i) {
            size_t sym_base = (size_t)symoff + i * 16U;
            uint32_t strx;
            uint8_t type;
            uint8_t sect;
            uint16_t desc;
            uint64_t value;
            char name[PATH_MAX];
            if (!v3_range_fits(len, (size_t)symoff, (size_t)nsyms * 16U) ||
                !v3_range_fits(len, (size_t)stroff, (size_t)strsize)) {
                free(bytes);
                free(out);
                return NULL;
            }
            strtab = bytes + stroff;
            strx = v3_read_u32_le_at(bytes, sym_base + 0U);
            type = bytes[sym_base + 4U];
            sect = bytes[sym_base + 5U];
            desc = v3_read_u16_le_at(bytes, sym_base + 6U);
            value = v3_read_u64_le_at(bytes, sym_base + 8U);
            if (!v3_elf_copy_name_from_table(strtab, (size_t)strsize, strx, name, sizeof(name))) {
                snprintf(name, sizeof(name), "%s", "<anonymous>");
            }
            snprintf(line,
                     sizeof(line),
                     "symbol[%zu]=%s|type=0x%x|sect=%u|desc=%u|value=%llu",
                     symbol_count,
                     name,
                     (unsigned)type,
                     (unsigned)sect,
                     (unsigned)desc,
                     (unsigned long long)value);
            v3_report_append(&out, &cap, &used, line);
            symbol_count += 1U;
        }
    }
    snprintf(line, sizeof(line), "symbol_count=%zu", symbol_count);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "reloc_count=%zu", reloc_count);
    v3_report_append(&out, &cap, &used, line);
    v3_report_append(&out, &cap, &used, "import_count=0");
    free(bytes);
    return out;
}

static uint32_t v3_coff_rva_to_file_offset(const unsigned char *bytes,
                                           size_t len,
                                           size_t section_table_off,
                                           uint16_t section_count,
                                           uint32_t rva) {
    size_t i;
    for (i = 0U; i < (size_t)section_count; ++i) {
        size_t base = section_table_off + i * 40U;
        uint32_t virtual_size;
        uint32_t virtual_address;
        uint32_t size_of_raw_data;
        uint32_t pointer_to_raw_data;
        uint32_t span;
        if (!v3_range_fits(len, base, 40U)) {
            return 0U;
        }
        virtual_size = v3_read_u32_le_at(bytes, base + 8U);
        virtual_address = v3_read_u32_le_at(bytes, base + 12U);
        size_of_raw_data = v3_read_u32_le_at(bytes, base + 16U);
        pointer_to_raw_data = v3_read_u32_le_at(bytes, base + 20U);
        span = virtual_size > size_of_raw_data ? virtual_size : size_of_raw_data;
        if (rva >= virtual_address && rva < virtual_address + span) {
            return pointer_to_raw_data + (rva - virtual_address);
        }
    }
    return 0U;
}

static char *v3_debug_coff_pe_report(const char *path) {
    unsigned char *bytes = NULL;
    size_t len = 0U;
    size_t cap = 65536U;
    size_t used = 0U;
    char *out = (char *)v3_xmalloc(cap);
    char line[PATH_MAX * 2];
    bool is_pe = false;
    size_t header_off = 0U;
    size_t section_table_off;
    uint16_t machine;
    uint16_t section_count_u16;
    uint32_t ptr_symtab;
    uint32_t num_syms;
    uint16_t size_optional;
    uint16_t characteristics;
    uint16_t optional_magic = 0U;
    uint16_t subsystem = 0U;
    size_t section_count = 0U;
    size_t symbol_count = 0U;
    size_t reloc_count = 0U;
    size_t import_count = 0U;
    size_t i;
    if (!v3_read_binary_file_alloc(path, &bytes, &len)) {
        free(out);
        return NULL;
    }
    if (v3_range_fits(len, 0U, 2U) &&
        bytes[0] == 'M' &&
        bytes[1] == 'Z') {
        uint32_t pe_off;
        if (!v3_range_fits(len, 0x3cU, 4U)) {
            free(bytes);
            free(out);
            return NULL;
        }
        pe_off = v3_read_u32_le_at(bytes, 0x3cU);
        if (!v3_range_fits(len, pe_off, 24U) ||
            bytes[pe_off] != 'P' ||
            bytes[pe_off + 1U] != 'E' ||
            bytes[pe_off + 2U] != 0U ||
            bytes[pe_off + 3U] != 0U) {
            free(bytes);
            free(out);
            return NULL;
        }
        header_off = (size_t)pe_off + 4U;
        is_pe = true;
    } else {
        header_off = 0U;
    }
    if (!v3_range_fits(len, header_off, 20U)) {
        free(bytes);
        free(out);
        return NULL;
    }
    machine = v3_read_u16_le_at(bytes, header_off + 0U);
    section_count_u16 = v3_read_u16_le_at(bytes, header_off + 2U);
    ptr_symtab = v3_read_u32_le_at(bytes, header_off + 8U);
    num_syms = v3_read_u32_le_at(bytes, header_off + 12U);
    size_optional = v3_read_u16_le_at(bytes, header_off + 16U);
    characteristics = v3_read_u16_le_at(bytes, header_off + 18U);
    section_table_off = header_off + 20U + (size_t)size_optional;
    if (!v3_range_fits(len, section_table_off, (size_t)section_count_u16 * 40U)) {
        free(bytes);
        free(out);
        return NULL;
    }
    if (is_pe && size_optional >= 70U && v3_range_fits(len, header_off + 20U, size_optional)) {
        optional_magic = v3_read_u16_le_at(bytes, header_off + 20U);
        subsystem = v3_read_u16_le_at(bytes, header_off + 20U + 68U);
    }
    out[0] = '\0';
    v3_report_append(&out, &cap, &used, "v3_object_report_v1");
    v3_report_append(&out, &cap, &used, is_pe ? "format=pe/coff" : "format=coff");
    snprintf(line, sizeof(line), "path=%s", path);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "machine=0x%x", machine);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "characteristics=0x%x", characteristics);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "optional_magic=0x%x", optional_magic);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "subsystem=%u", subsystem);
    v3_report_append(&out, &cap, &used, line);
    v3_report_append(&out, &cap, &used, "load_command_count=0");
    for (i = 0U; i < (size_t)section_count_u16; ++i) {
        size_t base = section_table_off + i * 40U;
        char name[32];
        uint32_t virtual_size = v3_read_u32_le_at(bytes, base + 8U);
        uint32_t virtual_address = v3_read_u32_le_at(bytes, base + 12U);
        uint32_t size_of_raw_data = v3_read_u32_le_at(bytes, base + 16U);
        uint32_t pointer_to_raw_data = v3_read_u32_le_at(bytes, base + 20U);
        uint32_t pointer_to_relocations = v3_read_u32_le_at(bytes, base + 24U);
        uint16_t number_of_relocations = v3_read_u16_le_at(bytes, base + 32U);
        uint32_t section_chars = v3_read_u32_le_at(bytes, base + 36U);
        size_t j;
        v3_copy_fixed_name_bytes(bytes + base, 8U, name, sizeof(name));
        snprintf(line,
                 sizeof(line),
                 "section[%zu]=%s|vaddr=%u|vsize=%u|raw_off=%u|raw_size=%u|chars=0x%x",
                 section_count,
                 name[0] != '\0' ? name : "<anonymous>",
                 virtual_address,
                 virtual_size,
                 pointer_to_raw_data,
                 size_of_raw_data,
                 section_chars);
        v3_report_append(&out, &cap, &used, line);
        if (!is_pe && number_of_relocations > 0U) {
            for (j = 0U; j < (size_t)number_of_relocations; ++j) {
                size_t reloc_off = (size_t)pointer_to_relocations + j * 10U;
                uint32_t virtual_addr;
                uint32_t symbol_index;
                uint16_t type;
                if (!v3_range_fits(len, reloc_off, 10U)) {
                    free(bytes);
                    free(out);
                    return NULL;
                }
                virtual_addr = v3_read_u32_le_at(bytes, reloc_off + 0U);
                symbol_index = v3_read_u32_le_at(bytes, reloc_off + 4U);
                type = v3_read_u16_le_at(bytes, reloc_off + 8U);
                snprintf(line,
                         sizeof(line),
                         "reloc[%zu]=target=%s|offset=%u|symbol_index=%u|type=0x%x",
                         reloc_count,
                         name[0] != '\0' ? name : "<anonymous>",
                         virtual_addr,
                         symbol_index,
                         type);
                v3_report_append(&out, &cap, &used, line);
                reloc_count += 1U;
            }
        }
        section_count += 1U;
    }
    snprintf(line, sizeof(line), "section_count=%zu", section_count);
    v3_report_append(&out, &cap, &used, line);
    if (ptr_symtab != 0U && num_syms > 0U) {
        size_t sym_table_size = (size_t)num_syms * 18U;
        const unsigned char *string_table = NULL;
        uint32_t string_table_size = 0U;
        if (!v3_range_fits(len, (size_t)ptr_symtab, sym_table_size + 4U)) {
            free(bytes);
            free(out);
            return NULL;
        }
        string_table = bytes + ptr_symtab + sym_table_size;
        string_table_size = v3_read_u32_le_at(string_table, 0U);
        for (i = 0U; i < (size_t)num_syms; ++i) {
            size_t sym_off = (size_t)ptr_symtab + i * 18U;
            char name[PATH_MAX];
            uint32_t zeroes = v3_read_u32_le_at(bytes, sym_off + 0U);
            uint32_t name_off = v3_read_u32_le_at(bytes, sym_off + 4U);
            uint32_t value = v3_read_u32_le_at(bytes, sym_off + 8U);
            uint16_t section_number = v3_read_u16_le_at(bytes, sym_off + 12U);
            uint16_t type = v3_read_u16_le_at(bytes, sym_off + 14U);
            uint8_t storage_class = bytes[sym_off + 16U];
            uint8_t aux_count = bytes[sym_off + 17U];
            if (zeroes == 0U && name_off > 0U && name_off < string_table_size) {
                v3_elf_copy_name_from_table(string_table, (size_t)string_table_size, name_off, name, sizeof(name));
            } else {
                v3_copy_fixed_name_bytes(bytes + sym_off, 8U, name, sizeof(name));
            }
            snprintf(line,
                     sizeof(line),
                     "symbol[%zu]=%s|value=%u|section=%d|type=0x%x|storage=%u|aux=%u",
                     symbol_count,
                     name[0] != '\0' ? name : "<anonymous>",
                     value,
                     (int)section_number,
                     (unsigned)type,
                     (unsigned)storage_class,
                     (unsigned)aux_count);
            v3_report_append(&out, &cap, &used, line);
            symbol_count += 1U;
            i += (size_t)aux_count;
        }
    }
    snprintf(line, sizeof(line), "symbol_count=%zu", symbol_count);
    v3_report_append(&out, &cap, &used, line);
    snprintf(line, sizeof(line), "reloc_count=%zu", reloc_count);
    v3_report_append(&out, &cap, &used, line);
    if (is_pe && optional_magic == V3_PE_OPTIONAL_MAGIC_PE32_PLUS && size_optional >= 0x78U) {
        uint32_t import_rva = v3_read_u32_le_at(bytes, header_off + 20U + 0x78U);
        uint32_t import_size = v3_read_u32_le_at(bytes, header_off + 20U + 0x7cU);
        uint32_t import_off = v3_coff_rva_to_file_offset(bytes, len, section_table_off, section_count_u16, import_rva);
        if (import_rva != 0U && import_size != 0U && import_off != 0U) {
            size_t desc_off = (size_t)import_off;
            while (v3_range_fits(len, desc_off, 20U)) {
                uint32_t original_first_thunk = v3_read_u32_le_at(bytes, desc_off + 0U);
                uint32_t name_rva = v3_read_u32_le_at(bytes, desc_off + 12U);
                uint32_t first_thunk = v3_read_u32_le_at(bytes, desc_off + 16U);
                uint32_t thunk_rva;
                uint32_t name_off;
                char dll_name[256];
                size_t thunk_off;
                if (original_first_thunk == 0U && name_rva == 0U && first_thunk == 0U) {
                    break;
                }
                name_off = v3_coff_rva_to_file_offset(bytes, len, section_table_off, section_count_u16, name_rva);
                if (name_off == 0U || !v3_elf_copy_name_from_table(bytes + name_off, len - name_off, 0U, dll_name, sizeof(dll_name))) {
                    snprintf(dll_name, sizeof(dll_name), "%s", "<unknown-dll>");
                }
                thunk_rva = original_first_thunk != 0U ? original_first_thunk : first_thunk;
                thunk_off = (size_t)v3_coff_rva_to_file_offset(bytes, len, section_table_off, section_count_u16, thunk_rva);
                while (thunk_off != 0U && v3_range_fits(len, thunk_off, 8U)) {
                    uint64_t thunk_data = v3_read_u64_le_at(bytes, thunk_off);
                    if (thunk_data == 0U) {
                        break;
                    }
                    if ((thunk_data & (1ULL << 63U)) != 0ULL) {
                        snprintf(line,
                                 sizeof(line),
                                 "import[%zu]=%s!ordinal_%llu",
                                 import_count,
                                 dll_name,
                                 (unsigned long long)(thunk_data & 0xffffULL));
                    } else {
                        uint32_t hint_name_rva = (uint32_t)(thunk_data & 0xffffffffU);
                        uint32_t hint_name_off = v3_coff_rva_to_file_offset(bytes, len, section_table_off, section_count_u16, hint_name_rva);
                        char func_name[256];
                        if (hint_name_off == 0U || !v3_range_fits(len, (size_t)hint_name_off + 2U, 1U) ||
                            !v3_elf_copy_name_from_table(bytes + hint_name_off + 2U, len - (size_t)hint_name_off - 2U, 0U, func_name, sizeof(func_name))) {
                            snprintf(func_name, sizeof(func_name), "%s", "<unknown>");
                        }
                        snprintf(line,
                                 sizeof(line),
                                 "import[%zu]=%s!%s",
                                 import_count,
                                 dll_name,
                                 func_name);
                    }
                    v3_report_append(&out, &cap, &used, line);
                    import_count += 1U;
                    thunk_off += 8U;
                }
                desc_off += 20U;
            }
        }
    }
    snprintf(line, sizeof(line), "import_count=%zu", import_count);
    v3_report_append(&out, &cap, &used, line);
    free(bytes);
    return out;
}

static char *v3_debug_object_report(const char *path) {
    FILE *fp = NULL;
    unsigned char magic[8];
    size_t got = 0U;
    fp = fopen(path, "rb");
    if (fp == NULL) {
        fprintf(stderr, "[cheng_v3_seed] failed to open object: %s\n", path);
        return NULL;
    }
    got = fread(magic, 1U, sizeof(magic), fp);
    fclose(fp);
    if (got >= 4U &&
        magic[0] == 0x7fU &&
        magic[1] == 'E' &&
        magic[2] == 'L' &&
        magic[3] == 'F') {
        return v3_debug_elf_object_report(path);
    }
    if (got >= 4U &&
        magic[0] == 0xcfU &&
        magic[1] == 0xfaU &&
        magic[2] == 0xedU &&
        magic[3] == 0xfeU) {
        return v3_debug_macho_object_report(path);
    }
    if (got >= 2U &&
        magic[0] == 'M' &&
        magic[1] == 'Z') {
        return v3_debug_coff_pe_report(path);
    }
    if (got >= 2U) {
        uint16_t machine = v3_read_u16_le_at(magic, 0U);
        if (machine == 0x14cU || machine == 0x8664U || machine == 0xaa64U) {
            return v3_debug_coff_pe_report(path);
        }
    }
    fprintf(stderr, "[cheng_v3_seed] unsupported object format: %s\n", path);
    return NULL;
}

static char *v3_debug_asm_report(const V3SystemLinkPlanStub *plan,
                                 const V3PrimaryObjectPlanStub *primary) {
    char asm_path[PATH_MAX];
    char *asm_text;
    size_t cap;
    size_t len;
    char *out;
    snprintf(asm_path, sizeof(asm_path), "%s.s", primary->output_path);
    asm_text = v3_read_file(asm_path);
    if (!asm_text) {
        return NULL;
    }
    len = strlen(asm_text);
    cap = len + PATH_MAX * 2U + 256U;
    out = (char *)v3_xmalloc(cap);
    snprintf(out,
             cap,
             "v3_asm_report_v1\npath=%s\ntarget=%s\nemit=%s\nasm_text_begin\n%s",
             asm_path,
             plan->target_triple,
             plan->emit_kind,
             asm_text);
    free(asm_text);
    return out;
}

static bool v3_run_profiled_binary(const char *path) {
    const char *argv_local[2];
    argv_local[0] = path;
    argv_local[1] = NULL;
    return v3_run_command_status_argv(1U, argv_local, false, "profiled-program");
}

static int v3_cmd_debug_report(int argc, char **argv) {
    V3BootstrapContract contract;
    V3SystemLinkPlanStub plan;
    V3CompilerWorldArtifacts world;
    V3LoweringPlanStub *lowering = NULL;
    V3PrimaryObjectPlanStub primary;
    V3ObjectPlanStub object_plan;
    V3NativeLinkPlanStub native_link;
    const char *report_path = v3_flag_value(argc, argv, "--report-out");
    char *report;
    if (!v3_load_runtime_contract(argc, argv, &contract)) {
        return 1;
    }
    if (!v3_validate_contract(&contract)) {
        v3_contract_free(&contract);
        return 1;
    }
    if (!v3_build_exec_context(&contract,
                               argc,
                               argv,
                               false,
                               true,
                               &plan,
                               &world,
                               &lowering,
                               &primary,
                               &object_plan,
                               &native_link)) {
        v3_contract_free(&contract);
        return 1;
    }
    report = v3_system_link_exec_report(&contract, &plan, &world, lowering, &primary, &object_plan, &native_link);
    {
        char *wrapped = v3_report_with_header("v3_debug_report_v1", report);
        free(report);
        report = wrapped;
    }
    if (!v3_write_optional_report_text(report_path, report)) {
        fprintf(stderr, "v3 compiler: failed to write report: %s\n", report_path);
        free(report);
        free(lowering);
        v3_compiler_world_artifacts_free(&world);
        v3_contract_free(&contract);
        return 1;
    }
    fputs(report, stdout);
    free(report);
    free(lowering);
    v3_compiler_world_artifacts_free(&world);
    v3_contract_free(&contract);
    return 0;
}

static int v3_cmd_print_symbols(int argc, char **argv) {
    V3BootstrapContract contract;
    V3SystemLinkPlanStub plan;
    V3CompilerWorldArtifacts world;
    V3LoweringPlanStub *lowering = NULL;
    V3PrimaryObjectPlanStub primary;
    V3ObjectPlanStub object_plan;
    V3NativeLinkPlanStub native_link;
    const char *report_path = v3_flag_value(argc, argv, "--report-out");
    char *report;
    if (!v3_load_runtime_contract(argc, argv, &contract)) {
        return 1;
    }
    if (!v3_validate_contract(&contract)) {
        v3_contract_free(&contract);
        return 1;
    }
    if (!v3_build_exec_context(&contract,
                               argc,
                               argv,
                               false,
                               false,
                               &plan,
                               &world,
                               &lowering,
                               &primary,
                               &object_plan,
                               &native_link)) {
        v3_contract_free(&contract);
        return 1;
    }
    report = v3_debug_symbols_report(&plan, &world, lowering, &primary, &object_plan, &native_link);
    if (!v3_write_optional_report_text(report_path, report)) {
        fprintf(stderr, "v3 compiler: failed to write report: %s\n", report_path);
        free(report);
        free(lowering);
        v3_compiler_world_artifacts_free(&world);
        v3_contract_free(&contract);
        return 1;
    }
    fputs(report, stdout);
    free(report);
    free(lowering);
    v3_compiler_world_artifacts_free(&world);
    v3_contract_free(&contract);
    return 0;
}

static int v3_cmd_print_line_map(int argc, char **argv) {
    V3BootstrapContract contract;
    V3SystemLinkPlanStub plan;
    V3CompilerWorldArtifacts world;
    V3LoweringPlanStub *lowering = NULL;
    V3PrimaryObjectPlanStub primary;
    V3ObjectPlanStub object_plan;
    V3NativeLinkPlanStub native_link;
    const char *report_path = v3_flag_value(argc, argv, "--report-out");
    char *report;
    if (!v3_load_runtime_contract(argc, argv, &contract)) {
        return 1;
    }
    if (!v3_validate_contract(&contract)) {
        v3_contract_free(&contract);
        return 1;
    }
    if (!v3_build_exec_context(&contract,
                               argc,
                               argv,
                               false,
                               false,
                               &plan,
                               &world,
                               &lowering,
                               &primary,
                               &object_plan,
                               &native_link)) {
        v3_contract_free(&contract);
        return 1;
    }
    report = v3_build_line_map_text(&plan, lowering);
    if (!v3_write_optional_report_text(report_path, report)) {
        fprintf(stderr, "v3 compiler: failed to write report: %s\n", report_path);
        free(report);
        free(lowering);
        v3_compiler_world_artifacts_free(&world);
        v3_contract_free(&contract);
        return 1;
    }
    fputs(report, stdout);
    free(report);
    free(lowering);
    v3_compiler_world_artifacts_free(&world);
    v3_contract_free(&contract);
    return 0;
}

static int v3_cmd_print_object(int argc, char **argv);

static int v3_cmd_print_elf(int argc, char **argv) {
    return v3_cmd_print_object(argc, argv);
}

static int v3_cmd_print_object(int argc, char **argv) {
    const char *object_path = v3_flag_value(argc, argv, "--object");
    const char *report_path = v3_flag_value(argc, argv, "--report-out");
    char *report;
    if (!object_path || *object_path == '\0') {
        fprintf(stderr, "[cheng_v3_seed] missing --object:<path>\n");
        return 1;
    }
    report = v3_debug_object_report(object_path);
    if (!report) {
        return 1;
    }
    if (!v3_write_optional_report_text(report_path, report)) {
        fprintf(stderr, "v3 compiler: failed to write report: %s\n", report_path);
        free(report);
        return 1;
    }
    fputs(report, stdout);
    free(report);
    return 0;
}

static int v3_cmd_print_asm(int argc, char **argv) {
    V3BootstrapContract contract;
    V3SystemLinkPlanStub plan;
    V3CompilerWorldArtifacts world;
    V3LoweringPlanStub *lowering = NULL;
    V3PrimaryObjectPlanStub primary;
    V3ObjectPlanStub object_plan;
    V3NativeLinkPlanStub native_link;
    const char *report_path = v3_flag_value(argc, argv, "--report-out");
    char *report;
    if (!v3_load_runtime_contract(argc, argv, &contract)) {
        return 1;
    }
    if (!v3_validate_contract(&contract)) {
        v3_contract_free(&contract);
        return 1;
    }
    if (!v3_build_exec_context(&contract,
                               argc,
                               argv,
                               false,
                               false,
                               &plan,
                               &world,
                               &lowering,
                               &primary,
                               &object_plan,
                               &native_link)) {
        v3_contract_free(&contract);
        return 1;
    }
    if (primary.missing_reason_count > 0U ||
        !v3_materialize_primary_asm_only(&plan, lowering, &primary)) {
        free(lowering);
        v3_compiler_world_artifacts_free(&world);
        v3_contract_free(&contract);
        return 1;
    }
    report = v3_debug_asm_report(&plan, &primary);
    if (!report) {
        free(lowering);
        v3_compiler_world_artifacts_free(&world);
        v3_contract_free(&contract);
        return 1;
    }
    if (!v3_write_optional_report_text(report_path, report)) {
        fprintf(stderr, "v3 compiler: failed to write report: %s\n", report_path);
        free(report);
        free(lowering);
        v3_compiler_world_artifacts_free(&world);
        v3_contract_free(&contract);
        return 1;
    }
    fputs(report, stdout);
    free(report);
    free(lowering);
    v3_compiler_world_artifacts_free(&world);
    v3_contract_free(&contract);
    return 0;
}

static int v3_cmd_profile_run(int argc, char **argv) {
    V3BootstrapPaths paths;
    const char *source_path = v3_flag_value(argc, argv, "--in");
    const char *root_path = v3_flag_value(argc, argv, "--root");
    const char *out_flag = v3_flag_value(argc, argv, "--out");
    const char *report_flag = v3_flag_value(argc, argv, "--report-out");
    const char *profile_hz = v3_flag_value(argc, argv, "--profile-hz");
    const char *target = v3_flag_value(argc, argv, "--target");
    const char *emit = v3_flag_value(argc, argv, "--emit");
    char workspace_root[PATH_MAX];
    char package_root[PATH_MAX];
    char package_probe[PATH_MAX];
    char output_path[PATH_MAX];
    char report_path[PATH_MAX];
    char raw_report_path[PATH_MAX];
    char compile_log[PATH_MAX];
    char profile_report_log[PATH_MAX];
    const char *slash;
    const char *base;
    size_t stem_len;
    char *report = NULL;
    int rc = 1;
    v3_bootstrap_paths_init(&paths);
    if (!source_path || *source_path == '\0') {
        fprintf(stderr, "[cheng_v3_seed] profile-run requires --in:<path>\n");
        return 1;
    }
    if (!target || *target == '\0') {
        target = "arm64-apple-darwin";
    }
    if (!v3_streq(target, "arm64-apple-darwin")) {
        fprintf(stderr, "[cheng_v3_seed] profile-run currently requires local runnable target arm64-apple-darwin\n");
        return 1;
    }
    if (emit && *emit != '\0' && !v3_streq(emit, "exe")) {
        fprintf(stderr, "[cheng_v3_seed] profile-run requires --emit:exe\n");
        return 1;
    }
    if (root_path && *root_path != '\0') {
        v3_copy_text(workspace_root, sizeof(workspace_root), root_path);
    } else if (paths.root[0] != '\0') {
        v3_copy_text(workspace_root, sizeof(workspace_root), paths.root);
    } else if (!getcwd(workspace_root, sizeof(workspace_root))) {
        perror("getcwd");
        return 1;
    }
    v3_copy_text(package_root, sizeof(package_root), workspace_root);
    v3_join_path(package_probe, sizeof(package_probe), workspace_root, "v3/src");
    if (v3_path_is_directory(package_probe)) {
        v3_join_path(package_root, sizeof(package_root), workspace_root, "v3");
    }
    if (out_flag && *out_flag != '\0') {
        v3_copy_text(output_path, sizeof(output_path), out_flag);
    } else {
        slash = strrchr(source_path, '/');
        base = slash ? slash + 1 : source_path;
        stem_len = strlen(base);
        if (stem_len >= 6U && strcmp(base + stem_len - 6U, ".cheng") == 0) {
            stem_len -= 6U;
        }
        if (stem_len == 0U) {
            base = "profile_run";
            stem_len = strlen(base);
        }
        v3_join_path(output_path, sizeof(output_path), workspace_root, "artifacts/v3_debug_profile");
        strncat(output_path, "/", sizeof(output_path) - strlen(output_path) - 1U);
        strncat(output_path, base, stem_len);
        output_path[strlen(output_path)] = '\0';
    }
    if (report_flag && *report_flag != '\0') {
        v3_copy_text(report_path, sizeof(report_path), report_flag);
    } else {
        snprintf(report_path, sizeof(report_path), "%s.v3.profile.txt", output_path);
    }
    v3_profile_raw_report_path_from_report(report_path, raw_report_path, sizeof(raw_report_path));
    snprintf(compile_log, sizeof(compile_log), "%s.compile.log", output_path);
    snprintf(profile_report_log, sizeof(profile_report_log), "%s.profile-report.log", output_path);
    if (!v3_ensure_parent_dir(output_path) ||
        !v3_ensure_parent_dir(report_path) ||
        !v3_ensure_parent_dir(raw_report_path)) {
        return 1;
    }
    if (!v3_ensure_backend_driver_ready(&paths, "profile-run")) {
        return 1;
    }
    if (!v3_compile_host_fixture_with_backend_driver(paths.backend_driver_out,
                                                     package_root,
                                                     source_path,
                                                     output_path,
                                                     compile_log)) {
        return 1;
    }
    setenv("CHENG_V3_PROFILE_ENABLE", "1", 1);
    unsetenv("CHENG_V3_PROFILE_OUT");
    setenv("CHENG_V3_PROFILE_RAW_OUT", raw_report_path, 1);
    setenv("CHENG_V3_PROFILE_HZ", (profile_hz && *profile_hz != '\0') ? profile_hz : "200", 1);
    if (!v3_run_profiled_binary(output_path)) {
        goto cleanup;
    }
    unsetenv("CHENG_V3_PROFILE_ENABLE");
    unsetenv("CHENG_V3_PROFILE_RAW_OUT");
    unsetenv("CHENG_V3_PROFILE_OUT");
    unsetenv("CHENG_V3_PROFILE_HZ");
    if (!v3_path_exists_nonempty(raw_report_path)) {
        fprintf(stderr, "[cheng_v3_seed] missing raw profile report: %s\n", raw_report_path);
        goto cleanup;
    }
    if (!v3_profile_report_transform_raw(raw_report_path,
                                         report_path,
                                         profile_report_log)) {
        fprintf(stderr, "[cheng_v3_seed] profile report transform failed: %s\n", profile_report_log);
        goto cleanup;
    }
    report = v3_read_file(report_path);
    if (!report) {
        fprintf(stderr, "[cheng_v3_seed] missing profile report: %s\n", report_path);
        goto cleanup;
    }
    fputs(report, stdout);
    rc = 0;
cleanup:
    free(report);
    return rc;
}

static int v3_cmd_profile_report(int argc, char **argv) {
    V3BootstrapPaths paths;
    const char *raw_report_path = v3_flag_value(argc, argv, "--in");
    const char *out_flag = v3_flag_value(argc, argv, "--out");
    char report_path[PATH_MAX];
    char profile_report_log[PATH_MAX];
    char *report = NULL;
    int rc = 1;
    v3_bootstrap_paths_init(&paths);
    if (raw_report_path == NULL || raw_report_path[0] == '\0') {
        fprintf(stderr, "[cheng_v3_seed] profile-report requires --in:<raw-report>\n");
        return 1;
    }
    if (out_flag != NULL && out_flag[0] != '\0') {
        v3_copy_text(report_path, sizeof(report_path), out_flag);
    } else {
        v3_profile_report_path_from_raw(raw_report_path, report_path, sizeof(report_path));
    }
    snprintf(profile_report_log, sizeof(profile_report_log), "%s.profile-report.log", report_path);
    if (!v3_profile_report_transform_raw(raw_report_path,
                                         report_path,
                                         profile_report_log)) {
        fprintf(stderr, "[cheng_v3_seed] profile report transform failed: %s\n", profile_report_log);
        return 1;
    }
    report = v3_read_file(report_path);
    if (report == NULL) {
        fprintf(stderr, "[cheng_v3_seed] missing profile report: %s\n", report_path);
        return 1;
    }
    fputs(report, stdout);
    rc = 0;
    free(report);
    return rc;
}

static int v3_cmd_crash_report(int argc, char **argv) {
    const char *raw_log_path = v3_flag_value(argc, argv, "--in");
    const char *out_flag = v3_flag_value(argc, argv, "--out");
    char report_path[PATH_MAX];
    char crash_report_log[PATH_MAX];
    char *report = NULL;
    int rc = 1;
    if (raw_log_path == NULL || raw_log_path[0] == '\0') {
        fprintf(stderr, "[cheng_v3_seed] crash-report requires --in:<raw-log>\n");
        return 1;
    }
    if (out_flag != NULL && out_flag[0] != '\0') {
        v3_copy_text(report_path, sizeof(report_path), out_flag);
    } else {
        v3_crash_report_path_from_raw(raw_log_path, report_path, sizeof(report_path));
    }
    snprintf(crash_report_log, sizeof(crash_report_log), "%s.crash-report.log", report_path);
    if (!v3_crash_report_transform_raw(raw_log_path,
                                       report_path,
                                       crash_report_log)) {
        fprintf(stderr, "[cheng_v3_seed] crash report transform failed: %s\n", crash_report_log);
        return 1;
    }
    report = v3_read_file(report_path);
    if (report == NULL) {
        fprintf(stderr, "[cheng_v3_seed] missing crash report: %s\n", report_path);
        return 1;
    }
    fputs(report, stdout);
    rc = 0;
    free(report);
    return rc;
}

static int v3_cmd_emit_csg(int argc, char **argv) {
    V3BootstrapContract contract;
    V3SystemLinkPlanStub plan;
    V3CompilerWorldArtifacts world;
    const char *report_path = v3_flag_value(argc, argv, "--report-out");
    if (!v3_load_runtime_contract(argc, argv, &contract)) {
        return 1;
    }
    if (!v3_validate_contract(&contract)) {
        v3_contract_free(&contract);
        return 1;
    }
    if (!v3_build_system_link_plan_stub(&contract, argc, argv, false, &plan)) {
        v3_contract_free(&contract);
        return 1;
    }
    if (!v3_build_compiler_world_artifacts(&plan,
                                           v3_flag_value(argc, argv, "--channel") ? v3_flag_value(argc, argv, "--channel") : "",
                                           "",
                                           "",
                                           "",
                                           &world)) {
        v3_contract_free(&contract);
        return 1;
    }
    if (!v3_write_optional_report_text(report_path, world.csg_report)) {
        fprintf(stderr, "v3 compiler: failed to write report: %s\n", report_path);
        v3_compiler_world_artifacts_free(&world);
        v3_contract_free(&contract);
        return 1;
    }
    fputs(world.csg_report, stdout);
    v3_compiler_world_artifacts_free(&world);
    v3_contract_free(&contract);
    return 0;
}

static int v3_cmd_migrate_csg(int argc, char **argv) {
    V3BootstrapContract contract;
    V3CompilerLegacyMigration migration;
    V3CompilerWorldArtifacts migration_world;
    V3SystemLinkPlanStub migration_plan;
    const char *report_path = v3_flag_value(argc, argv, "--report-out");
    const char *prefix = v3_flag_value(argc, argv, "--out");
    const char *current_source_path = v3_flag_value(argc, argv, "--in");
    const char *package_root_flag = v3_flag_value(argc, argv, "--root");
    char package_root[PATH_MAX];
    char workspace_root[PATH_MAX];
    char package_id[PATH_MAX];
    char entry_module_override[PATH_MAX];
    V3CompilerWorldArtifacts baseline_world;
    char *migration_report = NULL;
    char *migration_receipt_report = NULL;
    int rc = 1;
    memset(&migration, 0, sizeof(migration));
    memset(&migration_world, 0, sizeof(migration_world));
    memset(&migration_plan, 0, sizeof(migration_plan));
    memset(&baseline_world, 0, sizeof(baseline_world));
    entry_module_override[0] = '\0';
    if (!v3_load_runtime_contract(argc, argv, &contract)) {
        return 1;
    }
    if (!v3_validate_contract(&contract)) {
        v3_contract_free(&contract);
        return 1;
    }
    v3_resolve_effective_package_root(&contract,
                                      package_root_flag,
                                      current_source_path,
                                      package_root,
                                      sizeof(package_root));
    v3_workspace_root_from_package_root(package_root, workspace_root, sizeof(workspace_root));
    v3_package_id_from_root(package_root, package_id, sizeof(package_id));
    if (current_source_path && *current_source_path != '\0') {
        v3_source_path_to_module_path(workspace_root,
                                      package_root,
                                      package_id,
                                      current_source_path,
                                      entry_module_override,
                                      sizeof(entry_module_override));
    }
    if (!v3_compiler_legacy_build(argc,
                                  argv,
                                  &contract,
                                  package_id,
                                  entry_module_override,
                                  v3_flag_value(argc, argv, "--channel"),
                                  &migration_plan,
                                  &migration,
                                  &migration_world)) {
        v3_contract_free(&contract);
        return 1;
    }
    baseline_world.world_head_cid = v3_cid_zero();
    if (!v3_compiler_migration_receipt_report(&baseline_world,
                                              &migration_plan,
                                              &migration_world,
                                              "legacy_migration",
                                              &baseline_world.receipt_cid,
                                              &migration_receipt_report)) {
        goto cleanup;
    }
    migration_report = v3_compiler_legacy_migration_report(&migration, &migration_world);
    if (!v3_compiler_write_legacy_artifacts(prefix ? prefix : "",
                                            migration_report,
                                            migration_world.csg_report,
                                            migration_world.surface_report,
                                            migration_receipt_report)) {
        fprintf(stderr, "v3 compiler migration: failed to write migration artifacts: %s\n",
                prefix ? prefix : "");
        goto cleanup;
    }
    if (!v3_write_optional_report_text(report_path, migration_report)) {
        fprintf(stderr, "v3 compiler: failed to write report: %s\n", report_path);
        goto cleanup;
    }
    fputs(migration_report, stdout);
    rc = 0;
cleanup:
    free(migration_report);
    free(migration_receipt_report);
    v3_compiler_world_artifacts_free(&migration_world);
    v3_contract_free(&contract);
    return rc;
}

static int v3_cmd_verify_world(int argc, char **argv) {
    V3BootstrapContract contract;
    V3SystemLinkPlanStub plan;
    V3CompilerWorldArtifacts world;
    V3LoweringPlanStub *lowering = NULL;
    V3PrimaryObjectPlanStub primary;
    V3ObjectPlanStub object_plan;
    V3NativeLinkPlanStub native_link;
    const char *report_path = v3_flag_value(argc, argv, "--report-out");
    char *report;
    if (!v3_load_runtime_contract(argc, argv, &contract)) {
        return 1;
    }
    if (!v3_validate_contract(&contract)) {
        v3_contract_free(&contract);
        return 1;
    }
    if (!v3_build_exec_context(&contract,
                               argc,
                               argv,
                               false,
                               true,
                               &plan,
                               &world,
                               &lowering,
                               &primary,
                               &object_plan,
                               &native_link)) {
        v3_contract_free(&contract);
        return 1;
    }
    report = v3_system_link_exec_report(&contract,
                                        &plan,
                                        &world,
                                        lowering,
                                        &primary,
                                        &object_plan,
                                        &native_link);
    if (!v3_write_optional_report_text(report_path, report)) {
        fprintf(stderr, "v3 compiler: failed to write report: %s\n", report_path);
        free(report);
        free(lowering);
        v3_compiler_world_artifacts_free(&world);
        v3_contract_free(&contract);
        return 1;
    }
    fputs(report, stdout);
    free(report);
    free(lowering);
    v3_compiler_world_artifacts_free(&world);
    v3_contract_free(&contract);
    return 0;
}

static bool v3_write_world_bundle_text(const char *prefix,
                                       const char *suffix,
                                       const char *text) {
    char path[PATH_MAX];
    snprintf(path, sizeof(path), "%s%s", prefix, suffix);
    return v3_ensure_parent_dir(path) && v3_write_text_file(path, text);
}

static int v3_cmd_world_sync(int argc, char **argv) {
    V3BootstrapContract contract;
    V3SystemLinkPlanStub plan;
    V3CompilerWorldArtifacts world;
    const char *report_path = v3_flag_value(argc, argv, "--report-out");
    char summary[2048];
    if (!v3_load_runtime_contract(argc, argv, &contract)) {
        return 1;
    }
    if (!v3_validate_contract(&contract)) {
        v3_contract_free(&contract);
        return 1;
    }
    if (!v3_build_system_link_plan_stub(&contract, argc, argv, true, &plan)) {
        v3_contract_free(&contract);
        return 1;
    }
    if (!v3_build_compiler_world_artifacts(&plan,
                                           v3_flag_value(argc, argv, "--channel") ? v3_flag_value(argc, argv, "--channel") : "",
                                           v3_flag_value(argc, argv, "--world-head") ? v3_flag_value(argc, argv, "--world-head") : "",
                                           v3_flag_value(argc, argv, "--lock") ? v3_flag_value(argc, argv, "--lock") : "",
                                           v3_flag_value(argc, argv, "--baseline-surface") ? v3_flag_value(argc, argv, "--baseline-surface") : "",
                                           &world)) {
        v3_contract_free(&contract);
        return 1;
    }
    if (!v3_write_world_bundle_text(plan.output_path, ".world.txt", world.world_report) ||
        !v3_write_world_bundle_text(plan.output_path, ".compiler.snapshot.txt", world.compiler_snapshot_report) ||
        !v3_write_world_bundle_text(plan.output_path, ".std.snapshot.txt", world.std_snapshot_report) ||
        !v3_write_world_bundle_text(plan.output_path, ".runtime.snapshot.txt", world.runtime_snapshot_report) ||
        !v3_write_world_bundle_text(plan.output_path, ".csg.txt", world.csg_report) ||
        !v3_write_world_bundle_text(plan.output_path, ".surface.txt", world.surface_report) ||
        !v3_write_world_bundle_text(plan.output_path, ".receipt.txt", world.receipt_report) ||
        !v3_write_world_bundle_text(plan.output_path, ".lock.toml", world.lock_text)) {
        fprintf(stderr, "v3 compiler: failed to materialize world bundle: %s\n", plan.output_path);
        v3_compiler_world_artifacts_free(&world);
        v3_contract_free(&contract);
        return 1;
    }
    snprintf(summary,
             sizeof(summary),
             "bundle_prefix=%s\nworld_head_cid=%s\ncsg_root_cid=%s\nsurface_cid=%s\nreceipt_cid=%s\n",
             plan.output_path,
             world.world_head_cid.hex,
             world.graph_cid.hex,
             world.surface_cid.hex,
             world.receipt_cid.hex);
    if (!v3_write_optional_report_text(report_path, summary)) {
        fprintf(stderr, "v3 compiler: failed to write report: %s\n", report_path);
        v3_compiler_world_artifacts_free(&world);
        v3_contract_free(&contract);
        return 1;
    }
    fputs(summary, stdout);
    v3_compiler_world_artifacts_free(&world);
    v3_contract_free(&contract);
    return 0;
}

static int v3_cmd_prove_equivalence(int argc, char **argv) {
    V3BootstrapContract contract;
    V3SystemLinkPlanStub plan;
    V3CompilerWorldArtifacts world;
    V3CompilerEquivalenceProof proof;
    const char *report_path = v3_flag_value(argc, argv, "--report-out");
    bool enabled = false;
    char *report;
    if (!v3_load_runtime_contract(argc, argv, &contract)) {
        return 1;
    }
    if (!v3_validate_contract(&contract)) {
        v3_contract_free(&contract);
        return 1;
    }
    if (!v3_compiler_equivalence_inputs_ready(argc, argv, true, &enabled) || !enabled) {
        v3_contract_free(&contract);
        return 1;
    }
    if (!v3_build_system_link_plan_stub(&contract, argc, argv, false, &plan)) {
        v3_contract_free(&contract);
        return 1;
    }
    if (!v3_build_compiler_world_artifacts(&plan,
                                           v3_flag_value(argc, argv, "--channel") ? v3_flag_value(argc, argv, "--channel") : "",
                                           v3_flag_value(argc, argv, "--world-head") ? v3_flag_value(argc, argv, "--world-head") : "",
                                           v3_flag_value(argc, argv, "--lock") ? v3_flag_value(argc, argv, "--lock") : "",
                                           "",
                                           &world)) {
        v3_contract_free(&contract);
        return 1;
    }
    if (!v3_compiler_equivalence_proof_from_args(&plan, &world, argc, argv, &proof)) {
        v3_compiler_world_artifacts_free(&world);
        v3_contract_free(&contract);
        return 1;
    }
    report = v3_compiler_equivalence_proof_report(&proof);
    if (!v3_write_optional_report_text(report_path, report)) {
        fprintf(stderr, "v3 compiler: failed to write report: %s\n", report_path);
        free(report);
        v3_compiler_world_artifacts_free(&world);
        v3_contract_free(&contract);
        return 1;
    }
    fputs(report, stdout);
    free(report);
    v3_compiler_world_artifacts_free(&world);
    v3_contract_free(&contract);
    return 0;
}

static int v3_cmd_prove_migration(int argc, char **argv) {
    V3BootstrapContract contract;
    V3SystemLinkPlanStub plan;
    V3CompilerWorldArtifacts world;
    V3CompilerLegacyMigration migration;
    V3CompilerWorldArtifacts migration_world;
    V3CompilerEquivalenceProof proof;
    const char *report_path = v3_flag_value(argc, argv, "--report-out");
    const char *prefix = v3_flag_value(argc, argv, "--out");
    char entry_module_override[PATH_MAX];
    char *migration_report = NULL;
    char *migration_receipt_report = NULL;
    char *proof_report = NULL;
    char *combined_report = NULL;
    int rc = 1;
    memset(&world, 0, sizeof(world));
    memset(&migration, 0, sizeof(migration));
    memset(&migration_world, 0, sizeof(migration_world));
    entry_module_override[0] = '\0';
    if (!v3_load_runtime_contract(argc, argv, &contract)) {
        return 1;
    }
    if (!v3_validate_contract(&contract)) {
        v3_contract_free(&contract);
        return 1;
    }
    if (!v3_flag_value(argc, argv, "--legacy-in")) {
        fprintf(stderr, "v3 compiler migration: missing --legacy-in\n");
        v3_contract_free(&contract);
        return 1;
    }
    if (!v3_build_system_link_plan_stub(&contract, argc, argv, false, &plan)) {
        v3_contract_free(&contract);
        return 1;
    }
    if (!v3_build_compiler_world_artifacts(&plan,
                                           v3_flag_value(argc, argv, "--channel") ? v3_flag_value(argc, argv, "--channel") : "",
                                           v3_flag_value(argc, argv, "--world-head") ? v3_flag_value(argc, argv, "--world-head") : "",
                                           v3_flag_value(argc, argv, "--lock") ? v3_flag_value(argc, argv, "--lock") : "",
                                           "",
                                           &world)) {
        v3_contract_free(&contract);
        return 1;
    }
    v3_source_path_to_module_path(plan.workspace_root,
                                  plan.package_root,
                                  plan.package_id,
                                  plan.entry_path,
                                  entry_module_override,
                                  sizeof(entry_module_override));
    if (!v3_compiler_legacy_build(argc,
                                  argv,
                                  &contract,
                                  plan.package_id,
                                  entry_module_override,
                                  world.channel,
                                  NULL,
                                  &migration,
                                  &migration_world)) {
        v3_compiler_world_artifacts_free(&world);
        v3_contract_free(&contract);
        return 1;
    }
    if (!v3_compiler_migration_proof_from_reports(&plan,
                                                  &world,
                                                  &migration_world,
                                                  &proof,
                                                  &migration_receipt_report)) {
        goto cleanup;
    }
    migration_report = v3_compiler_legacy_migration_report(&migration, &migration_world);
    if (!v3_compiler_write_legacy_artifacts(prefix ? prefix : "",
                                            migration_report,
                                            migration_world.csg_report,
                                            migration_world.surface_report,
                                            migration_receipt_report)) {
        fprintf(stderr, "v3 compiler migration: failed to write migration artifacts: %s\n",
                prefix ? prefix : "");
        goto cleanup;
    }
    proof_report = v3_compiler_equivalence_proof_report(&proof);
    if (prefix && *prefix != '\0' && !v3_write_world_bundle_text(prefix, ".proof.txt", proof_report)) {
        fprintf(stderr, "v3 compiler: failed to write proof: %s.proof.txt\n", prefix);
        goto cleanup;
    }
    {
        size_t combined_len = strlen(migration_report) + 1U + strlen(proof_report) + 1U;
        combined_report = (char *)v3_xmalloc(combined_len);
        snprintf(combined_report, combined_len, "%s\n%s", migration_report, proof_report);
    }
    if (!v3_write_optional_report_text(report_path, combined_report)) {
        fprintf(stderr, "v3 compiler: failed to write report: %s\n", report_path);
        goto cleanup;
    }
    fputs(combined_report, stdout);
    rc = 0;
cleanup:
    free(combined_report);
    free(proof_report);
    free(migration_report);
    free(migration_receipt_report);
    v3_compiler_world_artifacts_free(&migration_world);
    v3_compiler_world_artifacts_free(&world);
    v3_contract_free(&contract);
    return rc;
}

static int v3_cmd_publish_world(int argc, char **argv) {
    V3BootstrapContract contract;
    V3SystemLinkPlanStub plan;
    V3CompilerWorldArtifacts world;
    V3CompilerLegacyMigration migration;
    V3CompilerWorldArtifacts migration_world;
    V3CompilerEquivalenceProof proof;
    V3CompilerPublishDecision decision;
    const char *report_path = v3_flag_value(argc, argv, "--report-out");
    char proof_mode[32];
    bool proof_enabled = false;
    char entry_module_override[PATH_MAX];
    char *migration_report = NULL;
    char *migration_receipt_report = NULL;
    char *proof_report = NULL;
    char *decision_report = NULL;
    char *summary = NULL;
    int rc = 1;
    memset(&world, 0, sizeof(world));
    memset(&migration, 0, sizeof(migration));
    memset(&migration_world, 0, sizeof(migration_world));
    proof_mode[0] = '\0';
    entry_module_override[0] = '\0';
    if (!v3_load_runtime_contract(argc, argv, &contract)) {
        return 1;
    }
    if (!v3_validate_contract(&contract)) {
        v3_contract_free(&contract);
        return 1;
    }
    if (!v3_build_system_link_plan_stub(&contract, argc, argv, true, &plan)) {
        v3_contract_free(&contract);
        return 1;
    }
    if (!v3_build_compiler_world_artifacts(&plan,
                                           v3_flag_value(argc, argv, "--channel") ? v3_flag_value(argc, argv, "--channel") : "",
                                           v3_flag_value(argc, argv, "--world-head") ? v3_flag_value(argc, argv, "--world-head") : "",
                                           v3_flag_value(argc, argv, "--lock") ? v3_flag_value(argc, argv, "--lock") : "",
                                           "",
                                           &world)) {
        v3_contract_free(&contract);
        return 1;
    }
    if (!v3_compiler_proof_mode_from_args(argc,
                                          argv,
                                          v3_streq(world.channel, "stable"),
                                          &proof_enabled,
                                          proof_mode,
                                          sizeof(proof_mode))) {
        goto cleanup;
    }
    memset(&proof, 0, sizeof(proof));
    proof.proof_cid = v3_cid_zero();
    if (proof_enabled) {
        if (v3_streq(proof_mode, "migration")) {
            v3_source_path_to_module_path(plan.workspace_root,
                                          plan.package_root,
                                          plan.package_id,
                                          plan.entry_path,
                                          entry_module_override,
                                          sizeof(entry_module_override));
            if (!v3_compiler_legacy_build(argc,
                                          argv,
                                          &contract,
                                          plan.package_id,
                                          entry_module_override,
                                          world.channel,
                                          NULL,
                                          &migration,
                                          &migration_world) ||
                !v3_compiler_migration_proof_from_reports(&plan,
                                                          &world,
                                                          &migration_world,
                                                          &proof,
                                                          &migration_receipt_report)) {
                goto cleanup;
            }
            migration_report = v3_compiler_legacy_migration_report(&migration, &migration_world);
            if (!v3_compiler_write_legacy_artifacts(plan.output_path,
                                                    migration_report,
                                                    migration_world.csg_report,
                                                    migration_world.surface_report,
                                                    migration_receipt_report)) {
                fprintf(stderr, "v3 compiler migration: failed to write migration artifacts: %s\n", plan.output_path);
                goto cleanup;
            }
        } else if (!v3_compiler_equivalence_proof_from_args(&plan, &world, argc, argv, &proof)) {
            goto cleanup;
        }
        proof_report = v3_compiler_equivalence_proof_report(&proof);
    }
    if (!v3_compiler_publish_decision_make(world.channel,
                                           world.world_head_cid,
                                           proof_enabled,
                                           &proof,
                                           &decision)) {
        goto cleanup;
    }
    decision_report = v3_compiler_publish_decision_report(&decision);
    if (decision.publish_allowed == 0) {
        if (!v3_write_optional_report_text(report_path, decision_report)) {
            fprintf(stderr, "v3 compiler: failed to write report: %s\n", report_path);
            goto cleanup;
        }
        fprintf(stderr, "v3 compiler: publish blocked by canonical equivalence gate\n");
        fputs(decision_report, stderr);
        fputc('\n', stderr);
        if (proof_report) {
            if (migration_report) {
                fputs(migration_report, stderr);
                fputc('\n', stderr);
            }
            fputs(proof_report, stderr);
            fputc('\n', stderr);
        }
        rc = 2;
        goto cleanup;
    }
    if (!v3_write_world_bundle_text(plan.output_path, ".world.txt", world.world_report) ||
        !v3_write_world_bundle_text(plan.output_path, ".compiler.snapshot.txt", world.compiler_snapshot_report) ||
        !v3_write_world_bundle_text(plan.output_path, ".std.snapshot.txt", world.std_snapshot_report) ||
        !v3_write_world_bundle_text(plan.output_path, ".runtime.snapshot.txt", world.runtime_snapshot_report) ||
        !v3_write_world_bundle_text(plan.output_path, ".csg.txt", world.csg_report) ||
        !v3_write_world_bundle_text(plan.output_path, ".surface.txt", world.surface_report) ||
        !v3_write_world_bundle_text(plan.output_path, ".receipt.txt", world.receipt_report) ||
        !v3_write_world_bundle_text(plan.output_path, ".lock.toml", world.lock_text)) {
        fprintf(stderr, "v3 compiler: failed to materialize world bundle: %s\n", plan.output_path);
        goto cleanup;
    }
    if (proof_report && !v3_write_world_bundle_text(plan.output_path, ".proof.txt", proof_report)) {
        fprintf(stderr, "v3 compiler: failed to write proof: %s.proof.txt\n", plan.output_path);
        goto cleanup;
    }
    if (!v3_write_world_bundle_text(plan.output_path, ".publish.txt", decision_report)) {
        fprintf(stderr, "v3 compiler: failed to write publish gate report: %s.publish.txt\n", plan.output_path);
        goto cleanup;
    }
    summary = v3_compiler_publish_summary(plan.output_path, proof_mode, &world, &decision);
    if (!v3_write_optional_report_text(report_path, summary)) {
        fprintf(stderr, "v3 compiler: failed to write report: %s\n", report_path);
        goto cleanup;
    }
    fputs(summary, stdout);
    rc = 0;
cleanup:
    free(summary);
    free(decision_report);
    free(proof_report);
    free(migration_report);
    free(migration_receipt_report);
    v3_compiler_world_artifacts_free(&migration_world);
    v3_compiler_world_artifacts_free(&world);
    v3_contract_free(&contract);
    return rc;
}

static int v3_cmd_fresh_node_selfhost(int argc, char **argv) {
    V3BootstrapContract contract;
    V3SystemLinkPlanStub plan;
    V3CompilerWorldArtifacts world;
    V3CompilerWorldArtifacts fresh_world;
    V3CompilerLegacyMigration migration;
    V3CompilerWorldArtifacts migration_world;
    V3CompilerEquivalenceProof proof;
    V3CompilerPublishDecision decision;
    V3Cid32 baseline_receipt_cid;
    V3Cid32 fresh_receipt_cid;
    int32_t compile_receipt_equivalent = 0;
    char target_triple[128];
    const char *report_path = v3_flag_value(argc, argv, "--report-out");
    char proof_mode[32];
    bool proof_enabled = false;
    char entry_module_override[PATH_MAX];
    char *migration_report = NULL;
    char *migration_receipt_report = NULL;
    char *proof_report = NULL;
    char *decision_report = NULL;
    char *publish_summary = NULL;
    char *fresh_report = NULL;
    char *summary = NULL;
    int rc = 1;
    memset(&world, 0, sizeof(world));
    memset(&fresh_world, 0, sizeof(fresh_world));
    memset(&migration, 0, sizeof(migration));
    memset(&migration_world, 0, sizeof(migration_world));
    proof_mode[0] = '\0';
    entry_module_override[0] = '\0';
    if (!v3_load_runtime_contract(argc, argv, &contract)) {
        return 1;
    }
    if (!v3_validate_contract(&contract)) {
        v3_contract_free(&contract);
        return 1;
    }
    if (!v3_build_system_link_plan_stub(&contract, argc, argv, true, &plan)) {
        v3_contract_free(&contract);
        return 1;
    }
    if (!v3_build_compiler_world_artifacts(&plan,
                                           v3_flag_value(argc, argv, "--channel") ? v3_flag_value(argc, argv, "--channel") : "",
                                           v3_flag_value(argc, argv, "--world-head") ? v3_flag_value(argc, argv, "--world-head") : "",
                                           v3_flag_value(argc, argv, "--lock") ? v3_flag_value(argc, argv, "--lock") : "",
                                           "",
                                           &world)) {
        v3_contract_free(&contract);
        return 1;
    }
    if (!v3_compiler_proof_mode_from_args(argc,
                                          argv,
                                          v3_streq(world.channel, "stable"),
                                          &proof_enabled,
                                          proof_mode,
                                          sizeof(proof_mode))) {
        goto cleanup;
    }
    memset(&proof, 0, sizeof(proof));
    proof.proof_cid = v3_cid_zero();
    if (proof_enabled) {
        if (v3_streq(proof_mode, "migration")) {
            v3_source_path_to_module_path(plan.workspace_root,
                                          plan.package_root,
                                          plan.package_id,
                                          plan.entry_path,
                                          entry_module_override,
                                          sizeof(entry_module_override));
            if (!v3_compiler_legacy_build(argc,
                                          argv,
                                          &contract,
                                          plan.package_id,
                                          entry_module_override,
                                          world.channel,
                                          NULL,
                                          &migration,
                                          &migration_world) ||
                !v3_compiler_migration_proof_from_reports(&plan,
                                                          &world,
                                                          &migration_world,
                                                          &proof,
                                                          &migration_receipt_report)) {
                goto cleanup;
            }
            migration_report = v3_compiler_legacy_migration_report(&migration, &migration_world);
            if (!v3_compiler_write_legacy_artifacts(plan.output_path,
                                                    migration_report,
                                                    migration_world.csg_report,
                                                    migration_world.surface_report,
                                                    migration_receipt_report)) {
                fprintf(stderr, "v3 compiler migration: failed to write migration artifacts: %s\n", plan.output_path);
                goto cleanup;
            }
        } else if (!v3_compiler_equivalence_proof_from_args(&plan, &world, argc, argv, &proof)) {
            goto cleanup;
        }
        proof_report = v3_compiler_equivalence_proof_report(&proof);
    }
    if (!v3_compiler_publish_decision_make(world.channel,
                                           world.world_head_cid,
                                           proof_enabled,
                                           &proof,
                                           &decision)) {
        goto cleanup;
    }
    decision_report = v3_compiler_publish_decision_report(&decision);
    if (decision.publish_allowed == 0) {
        if (!v3_write_optional_report_text(report_path, decision_report)) {
            fprintf(stderr, "v3 compiler: failed to write report: %s\n", report_path);
            goto cleanup;
        }
        fprintf(stderr, "v3 compiler: publish blocked by canonical equivalence gate\n");
        fputs(decision_report, stderr);
        fputc('\n', stderr);
        if (proof_report) {
            if (migration_report) {
                fputs(migration_report, stderr);
                fputc('\n', stderr);
            }
            fputs(proof_report, stderr);
            fputc('\n', stderr);
        }
        rc = 2;
        goto cleanup;
    }
    if (!v3_write_world_bundle_text(plan.output_path, ".world.txt", world.world_report) ||
        !v3_write_world_bundle_text(plan.output_path, ".compiler.snapshot.txt", world.compiler_snapshot_report) ||
        !v3_write_world_bundle_text(plan.output_path, ".std.snapshot.txt", world.std_snapshot_report) ||
        !v3_write_world_bundle_text(plan.output_path, ".runtime.snapshot.txt", world.runtime_snapshot_report) ||
        !v3_write_world_bundle_text(plan.output_path, ".csg.txt", world.csg_report) ||
        !v3_write_world_bundle_text(plan.output_path, ".surface.txt", world.surface_report) ||
        !v3_write_world_bundle_text(plan.output_path, ".receipt.txt", world.receipt_report) ||
        !v3_write_world_bundle_text(plan.output_path, ".lock.toml", world.lock_text)) {
        fprintf(stderr, "v3 compiler: failed to materialize world bundle: %s\n", plan.output_path);
        goto cleanup;
    }
    if (proof_report && !v3_write_world_bundle_text(plan.output_path, ".proof.txt", proof_report)) {
        fprintf(stderr, "v3 compiler: failed to write proof: %s.proof.txt\n", plan.output_path);
        goto cleanup;
    }
    if (!v3_write_world_bundle_text(plan.output_path, ".publish.txt", decision_report)) {
        fprintf(stderr, "v3 compiler: failed to write publish gate report: %s.publish.txt\n", plan.output_path);
        goto cleanup;
    }
    if (!v3_build_compiler_world_artifacts(&plan,
                                           world.channel,
                                           world.world_head_cid.hex,
                                           "",
                                           "",
                                           &fresh_world)) {
        goto cleanup;
    }
    if (!v3_compiler_equivalence_receipt_compare_reports(world.receipt_report,
                                                         fresh_world.receipt_report,
                                                         &baseline_receipt_cid,
                                                         &fresh_receipt_cid,
                                                         target_triple,
                                                         sizeof(target_triple),
                                                         &compile_receipt_equivalent)) {
        goto cleanup;
    }
    if (compile_receipt_equivalent == 0) {
        fprintf(stderr, "v3 compiler world libp2p: fresh rebuild receipt drift\n");
        goto cleanup;
    }
    if (!v3_cid_equal(baseline_receipt_cid, fresh_receipt_cid)) {
        fprintf(stderr, "v3 compiler world libp2p: fresh rebuild receipt cid mismatch\n");
        goto cleanup;
    }
    {
        size_t cap = 2048U;
        size_t used = 0U;
        char *out = (char *)v3_xmalloc(cap);
        char line[256];
        out[0] = '\0';
        v3_report_append(&out, &cap, &used, "seed_entries=7");
        v3_report_append(&out, &cap, &used, "fresh_entries=7");
        v3_report_append(&out, &cap, &used, "published_heads=1");
        v3_report_append(&out, &cap, &used, "synced_entries=7");
        snprintf(line, sizeof(line), "world_head_cid=%s", world.world_head_cid.hex);
        v3_report_append(&out, &cap, &used, line);
        snprintf(line, sizeof(line), "seed_receipt_cid=%s", world.receipt_cid.hex);
        v3_report_append(&out, &cap, &used, line);
        snprintf(line, sizeof(line), "synced_receipt_cid=%s", world.receipt_cid.hex);
        v3_report_append(&out, &cap, &used, line);
        snprintf(line, sizeof(line), "fresh_receipt_cid=%s", fresh_world.receipt_cid.hex);
        v3_report_append(&out, &cap, &used, line);
        v3_report_append(&out, &cap, &used, "receipt_equivalent=1");
        v3_report_append(&out, &cap, &used, "receipt_cid_match=1");
        fresh_report = out;
    }
    if (!v3_write_world_bundle_text(plan.output_path, ".fresh.txt", fresh_report)) {
        fprintf(stderr, "v3 compiler: failed to write fresh-node report: %s.fresh.txt\n", plan.output_path);
        goto cleanup;
    }
    publish_summary = v3_compiler_publish_summary(plan.output_path, proof_mode, &world, &decision);
    {
        size_t summary_len = strlen(publish_summary);
        size_t fresh_len = strlen(fresh_report);
        summary = (char *)v3_xmalloc(summary_len + fresh_len + 2U);
        snprintf(summary, summary_len + fresh_len + 2U, "%s\n%s", publish_summary, fresh_report);
    }
    if (!v3_write_optional_report_text(report_path, summary)) {
        fprintf(stderr, "v3 compiler: failed to write report: %s\n", report_path);
        goto cleanup;
    }
    fputs(summary, stdout);
    rc = 0;
cleanup:
    free(summary);
    free(publish_summary);
    free(fresh_report);
    free(decision_report);
    free(proof_report);
    free(migration_report);
    free(migration_receipt_report);
    v3_compiler_world_artifacts_free(&migration_world);
    v3_compiler_world_artifacts_free(&fresh_world);
    v3_compiler_world_artifacts_free(&world);
    v3_contract_free(&contract);
    return rc;
}

static int v3_cmd_selfhost_build(int argc, char **argv) {
    V3BootstrapContract contract;
    V3SystemLinkPlanStub plan;
    V3CompilerWorldArtifacts world;
    V3LoweringPlanStub *lowering = NULL;
    V3PrimaryObjectPlanStub primary;
    V3ObjectPlanStub object_plan;
    V3NativeLinkPlanStub native_link;
    const char *report_path = v3_flag_value(argc, argv, "--report-out");
    char *report;
    if (!v3_load_runtime_contract(argc, argv, &contract)) {
        return 1;
    }
    if (!v3_validate_contract(&contract)) {
        v3_contract_free(&contract);
        return 1;
    }
    if (!v3_build_exec_context(&contract,
                               argc,
                               argv,
                               true,
                               true,
                               &plan,
                               &world,
                               &lowering,
                               &primary,
                               &object_plan,
                               &native_link)) {
        v3_contract_free(&contract);
        return 1;
    }
    report = v3_system_link_exec_report(&contract,
                                        &plan,
                                        &world,
                                        lowering,
                                        &primary,
                                        &object_plan,
                                        &native_link);
    if (!v3_write_optional_report_text(report_path, report)) {
        fprintf(stderr, "v3 compiler: failed to write report: %s\n", report_path);
        free(report);
        free(lowering);
        v3_compiler_world_artifacts_free(&world);
        v3_contract_free(&contract);
        return 1;
    }
    if (primary.missing_reason_count > 0U) {
        fprintf(stderr, "v3 compiler: selfhost primary object machine code not emitted\n");
    } else {
        fprintf(stderr, "v3 compiler: selfhost native link not implemented\n");
    }
    fputs(report, stderr);
    free(report);
    free(lowering);
    v3_compiler_world_artifacts_free(&world);
    v3_contract_free(&contract);
    return 2;
}

static int v3_cmd_system_link_exec(int argc, char **argv) {
    V3BootstrapContract contract;
    V3SystemLinkPlanStub *plan = NULL;
    V3CompilerWorldArtifacts *world = NULL;
    V3LoweringPlanStub *lowering = NULL;
    V3PrimaryObjectPlanStub *primary = NULL;
    V3ObjectPlanStub *object_plan = NULL;
    V3NativeLinkPlanStub *native_link = NULL;
    const char *report_path = v3_flag_value(argc, argv, "--report-out");
    char *report = NULL;
    int rc = 2;
    bool pipeline_ready;
    plan = (V3SystemLinkPlanStub *)calloc(1U, sizeof(V3SystemLinkPlanStub));
    world = (V3CompilerWorldArtifacts *)calloc(1U, sizeof(V3CompilerWorldArtifacts));
    primary = (V3PrimaryObjectPlanStub *)calloc(1U, sizeof(V3PrimaryObjectPlanStub));
    object_plan = (V3ObjectPlanStub *)calloc(1U, sizeof(V3ObjectPlanStub));
    native_link = (V3NativeLinkPlanStub *)calloc(1U, sizeof(V3NativeLinkPlanStub));
    if (plan == NULL || world == NULL || primary == NULL || object_plan == NULL || native_link == NULL) {
        free(plan);
        free(world);
        free(primary);
        free(object_plan);
        free(native_link);
        fprintf(stderr, "v3 compiler: system-link-exec out of memory allocating execution context\n");
        return 1;
    }
    if (!v3_load_runtime_contract(argc, argv, &contract)) {
        free(plan);
        free(world);
        free(primary);
        free(object_plan);
        free(native_link);
        return 1;
    }
    if (!v3_validate_contract(&contract)) {
        v3_contract_free(&contract);
        free(plan);
        free(world);
        free(primary);
        free(object_plan);
        free(native_link);
        return 1;
    }
    if (!v3_build_exec_context(&contract,
                               argc,
                               argv,
                               true,
                               true,
                               plan,
                               world,
                               &lowering,
                               primary,
                               object_plan,
                               native_link)) {
        v3_contract_free(&contract);
        free(plan);
        free(world);
        free(primary);
        free(object_plan);
        free(native_link);
        return 1;
    }
    if (!v3_streq(plan->emit_kind, "obj") &&
        object_plan->missing_reason_count <= 0U &&
        object_plan->linux_nolibc_support_enabled &&
        !v3_materialize_linux_nolibc_support_objects(&contract, plan, object_plan)) {
        free(lowering);
        v3_compiler_world_artifacts_free(world);
        v3_contract_free(&contract);
        free(plan);
        free(world);
        free(primary);
        free(object_plan);
        free(native_link);
        return 1;
    }
    if (!v3_streq(plan->emit_kind, "obj") &&
        native_link->missing_reason_count <= 0U &&
        object_plan->provider_source_count > 0U &&
        !v3_materialize_provider_objects(&contract, plan, object_plan)) {
        free(lowering);
        v3_compiler_world_artifacts_free(world);
        v3_contract_free(&contract);
        free(plan);
        free(world);
        free(primary);
        free(object_plan);
        free(native_link);
        return 1;
    }
    if (primary->missing_reason_count <= 0U &&
        !v3_materialize_primary_object(plan, lowering, primary)) {
        free(lowering);
        v3_compiler_world_artifacts_free(world);
        v3_contract_free(&contract);
        free(plan);
        free(world);
        free(primary);
        free(object_plan);
        free(native_link);
        return 1;
    }
    report = v3_system_link_exec_report(&contract, plan, world, lowering, primary, object_plan, native_link);
    if (report_path && *report_path != '\0') {
        if (!v3_ensure_parent_dir(report_path) || !v3_write_text_file(report_path, report)) {
            free(report);
            free(lowering);
            v3_compiler_world_artifacts_free(world);
            v3_contract_free(&contract);
            free(plan);
            free(world);
            free(primary);
            free(object_plan);
            free(native_link);
            return 1;
        }
    }
    if (v3_streq(plan->emit_kind, "obj")) {
        if (primary->missing_reason_count > 0U) {
            fprintf(stderr, "v3 compiler: primary object machine code not emitted\n");
            fputs(report, stderr);
            free(report);
            free(lowering);
            v3_compiler_world_artifacts_free(world);
            v3_contract_free(&contract);
            free(plan);
            free(world);
            free(primary);
            free(object_plan);
            free(native_link);
            return 2;
        }
        rc = 0;
        fputs(report, stderr);
        free(report);
        free(lowering);
        v3_compiler_world_artifacts_free(world);
        v3_contract_free(&contract);
        free(plan);
        free(world);
        free(primary);
        free(object_plan);
        free(native_link);
        return rc;
    }
    pipeline_ready = plan->missing_reason_count <= 0U &&
                     lowering->missing_reason_count <= 0U &&
                     primary->missing_reason_count <= 0U &&
                     object_plan->missing_reason_count <= 0U &&
                     native_link->missing_reason_count <= 0U;
    if (primary->missing_reason_count > 0U) {
        if (v3_plan_reasons_contains(primary->missing_reasons,
                                     primary->missing_reason_count,
                                     "primary_object_target_codegen_not_supported") ||
            v3_plan_reasons_contains(plan->missing_reasons,
                                     plan->missing_reason_count,
                                     "target_codegen_not_supported")) {
            fprintf(stderr, "v3 compiler: target codegen not supported for %s\n", plan->target_triple);
            fprintf(stderr, "v3 compiler: supported targets=%s\n", v3_supported_seed_target_csv());
            fprintf(stderr, "v3 compiler: target detail=%s\n",
                    v3_requested_emit_support_detail(plan->target_triple, plan->emit_kind));
        } else if (v3_plan_reasons_contains(primary->missing_reasons,
                                     primary->missing_reason_count,
                                     "primary_object_body_semantics_missing")) {
            fprintf(stderr, "v3 compiler: primary object body semantics missing\n");
            if (primary->unsupported_function_count > 0U) {
                fprintf(stderr,
                        "v3 compiler: first unsupported function=%s body_kind=%s abi=%s first_line=%s\n",
                        primary->unsupported_function_symbols[0].text,
                        primary->unsupported_body_kinds[0],
                        primary->unsupported_abi_summaries[0],
                        primary->unsupported_first_lines[0]);
            }
        } else if (v3_plan_reasons_contains(primary->missing_reasons,
                                            primary->missing_reason_count,
                                            "primary_object_call_target_missing")) {
            fprintf(stderr, "v3 compiler: primary object call target missing\n");
        } else {
            fprintf(stderr, "v3 compiler: primary object machine code not emitted\n");
        }
    } else if (!pipeline_ready) {
        fprintf(stderr, "v3 compiler: ordinary pipeline blocked\n");
    } else if (v3_streq(plan->emit_kind, "shared") ?
                   !v3_link_native_shared_library(native_link, plan->output_path) :
                   !v3_link_native_executable(native_link, plan->output_path)) {
        free(report);
        free(lowering);
        v3_compiler_world_artifacts_free(world);
        v3_contract_free(&contract);
        free(plan);
        free(world);
        free(primary);
        free(object_plan);
        free(native_link);
        return 1;
    } else if (v3_streq(plan->emit_kind, "exe") &&
               !v3_write_executable_line_map(plan, lowering)) {
        free(report);
        free(lowering);
        v3_compiler_world_artifacts_free(world);
        v3_contract_free(&contract);
        free(plan);
        free(world);
        free(primary);
        free(object_plan);
        free(native_link);
        return 1;
    } else {
        rc = 0;
    }
    fputs(report, stderr);
    free(report);
    free(lowering);
    v3_compiler_world_artifacts_free(world);
    v3_contract_free(&contract);
    free(plan);
    free(world);
    free(primary);
    free(object_plan);
    free(native_link);
    return rc;
}

int main(int argc, char **argv) {
    V3BootstrapPaths paths;
    int passthrough_rc;
    if (argc < 2) {
        v3_usage();
        return 1;
    }
    if (v3_streq(argv[1], "print-contract")) {
        return v3_cmd_print_contract(argc, argv);
    }
    if (v3_streq(argv[1], "self-check")) {
        return v3_cmd_self_check(argc, argv);
    }
    if (v3_streq(argv[1], "status") ||
        v3_streq(argv[1], "print-build-plan")) {
        v3_bootstrap_paths_init(&paths);
        passthrough_rc = v3_try_exec_backend_driver_cli_passthrough(&paths,
                                                                    argc,
                                                                    argv,
                                                                    "cheng.backend_driver",
                                                                    true);
        if (passthrough_rc >= 0) {
            return passthrough_rc;
        }
    }
    if (v3_streq(argv[1], "status")) {
        return v3_cmd_status(argc, argv);
    }
    if (v3_streq(argv[1], "print-build-plan")) {
        return v3_cmd_print_build_plan(argc, argv);
    }
    if (v3_streq(argv[1], "scan-hotpath")) {
        return v3_cmd_scan_hotpath(argc, argv);
    }
    if (v3_streq(argv[1], "print-bootstrap")) {
        return v3_cmd_print_bootstrap(argc, argv);
    }
    if (v3_streq(argv[1], "bootstrap-bridge")) {
        return v3_cmd_bootstrap_bridge(argc, argv);
    }
    if (v3_streq(argv[1], "build-backend-driver")) {
        return v3_cmd_build_backend_driver(argc, argv);
    }
    if (v3_command_requires_backend_driver_passthrough(argv[1])) {
        return v3_require_backend_driver_cli_passthrough(argv[1], argc, argv);
    }
    if (v3_command_prefers_backend_driver_passthrough(argv[1])) {
        v3_bootstrap_paths_init(&paths);
        passthrough_rc = v3_try_exec_backend_driver_cli_passthrough(&paths,
                                                                    argc,
                                                                    argv,
                                                                    "cheng.backend_driver",
                                                                    true);
        if (passthrough_rc >= 0) {
            return passthrough_rc;
        }
    }
    if (v3_streq(argv[1], "run-smokes")) {
        return v3_cmd_run_smokes(argc, argv);
    }
    if (v3_streq(argv[1], "slice-gate")) {
        return v3_cmd_slice_gate(argc, argv);
    }
    if (v3_streq(argv[1], "debug-report")) {
        return v3_cmd_debug_report(argc, argv);
    }
    if (v3_streq(argv[1], "print-symbols")) {
        return v3_cmd_print_symbols(argc, argv);
    }
    if (v3_streq(argv[1], "print-line-map")) {
        return v3_cmd_print_line_map(argc, argv);
    }
    if (v3_streq(argv[1], "print-object")) {
        return v3_cmd_print_object(argc, argv);
    }
    if (v3_streq(argv[1], "print-elf")) {
        return v3_cmd_print_elf(argc, argv);
    }
    if (v3_streq(argv[1], "print-asm")) {
        return v3_cmd_print_asm(argc, argv);
    }
    if (v3_streq(argv[1], "profile-run")) {
        return v3_cmd_profile_run(argc, argv);
    }
    if (v3_streq(argv[1], "profile-report")) {
        return v3_cmd_profile_report(argc, argv);
    }
    if (v3_streq(argv[1], "crash-report")) {
        return v3_cmd_crash_report(argc, argv);
    }
    if (v3_streq(argv[1], "verify-orphan-guard")) {
        return v3_cmd_verify_orphan_guard_impl(argc, argv);
    }
    if (v3_streq(argv[1], "verify-export-visibility")) {
        return v3_cmd_verify_export_visibility_impl(argc, argv);
    }
    if (v3_streq(argv[1], "verify-export-visibility-parallel")) {
        return v3_cmd_verify_export_visibility_parallel_impl(argc, argv);
    }
    if (v3_streq(argv[1], "verify-backend-driver-command-surface")) {
        return v3_cmd_verify_backend_driver_command_surface_impl(argc, argv);
    }
    if (v3_streq(argv[1], "verify-mobile-shell-build-probe")) {
        return v3_cmd_verify_mobile_shell_build_probe_impl(argc, argv);
    }
    if (v3_streq(argv[1], "verify-r2c-react-v3-surface")) {
        return v3_cmd_verify_r2c_react_v3_surface_impl(argc, argv);
    }
    if (v3_streq(argv[1], "verify-debug-tools")) {
        return v3_cmd_verify_debug_tools(argc, argv);
    }
    if (v3_streq(argv[1], "verify-debug-runtime")) {
        return v3_cmd_verify_debug_runtime_impl(argc, argv);
    }
    if (v3_streq(argv[1], "verify-debug-profile")) {
        return v3_cmd_verify_debug_profile_impl(argc, argv);
    }
    if (v3_streq(argv[1], "verify-diloco")) {
        return v3_cmd_verify_diloco_impl(argc, argv);
    }
    if (v3_streq(argv[1], "verify-windows-builtin")) {
        return v3_cmd_verify_windows_builtin_impl(argc, argv);
    }
    if (v3_streq(argv[1], "verify-riscv64-builtin")) {
        return v3_cmd_verify_riscv64_builtin_impl(argc, argv);
    }
    if (v3_streq(argv[1], "run-cross-target-smokes")) {
        return v3_cmd_run_cross_target_smokes_impl(argc, argv);
    }
    if (v3_streq(argv[1], "run-host-smokes")) {
        return v3_cmd_run_host_smokes_impl(argc, argv);
    }
    if (v3_streq(argv[1], "run-stage23-libp2p-smokes")) {
        return v3_cmd_run_stage23_libp2p_smokes_impl(argc, argv);
    }
    if (v3_streq(argv[1], "r2c-react-v3")) {
        return v3_cmd_r2c_react_v3_impl(argc, argv);
    }
    if (v3_streq(argv[1], "build-zero-exit")) {
        return v3_cmd_build_zero_exit_impl(argc, argv);
    }
    if (v3_streq(argv[1], "build-panic-trace")) {
        return v3_cmd_build_panic_trace_impl(argc, argv);
    }
    if (v3_streq(argv[1], "build-bounds-trace")) {
        return v3_cmd_build_bounds_trace_impl(argc, argv);
    }
    if (v3_streq(argv[1], "build-signal-trace")) {
        return v3_cmd_build_signal_trace_impl(argc, argv);
    }
    if (v3_streq(argv[1], "build-call-chain")) {
        return v3_cmd_build_call_chain_impl(argc, argv);
    }
    if (v3_streq(argv[1], "build-ffi-handle")) {
        return v3_cmd_build_ffi_handle_impl(argc, argv);
    }
    if (v3_streq(argv[1], "build-program-selfhost")) {
        return v3_cmd_build_program_selfhost_impl(argc, argv);
    }
    if (v3_streq(argv[1], "build-bft-state-machine")) {
        return v3_cmd_build_bft_state_machine_impl(argc, argv);
    }
    if (v3_streq(argv[1], "deploy-bft-validator-three-node")) {
        return v3_cmd_deploy_bft_validator_three_node_impl(argc, argv);
    }
    if (v3_streq(argv[1], "verify-bft-validator-three-node")) {
        return v3_cmd_verify_bft_validator_three_node_impl(argc, argv);
    }
    if (v3_streq(argv[1], "status-bft-validator-three-node")) {
        return v3_cmd_status_bft_validator_three_node_impl(argc, argv);
    }
    if (v3_streq(argv[1], "stop-bft-validator-three-node")) {
        return v3_cmd_stop_bft_validator_three_node_impl(argc, argv);
    }
    if (v3_streq(argv[1], "bench-bft-validator-three-node")) {
        return v3_cmd_bench_bft_validator_three_node_impl(argc, argv);
    }
    if (v3_streq(argv[1], "build-browser-host-wasm")) {
        return v3_cmd_build_browser_host_wasm_impl(argc, argv);
    }
    if (v3_streq(argv[1], "build-chain-node")) {
        return v3_cmd_build_chain_node_impl(argc, argv);
    }
    if (v3_streq(argv[1], "build-rwad-bft-state-machine")) {
        return v3_cmd_build_rwad_bft_state_machine_impl(argc, argv);
    }
    if (v3_streq(argv[1], "build-chain-node-linux")) {
        return v3_cmd_build_chain_node_linux_impl(argc, argv);
    }
    if (v3_streq(argv[1], "build-linux-nolibc-exe")) {
        return v3_cmd_build_linux_nolibc_exe_impl(argc, argv);
    }
    if (v3_streq(argv[1], "build-rwad-bft-linux")) {
        return v3_cmd_build_rwad_bft_linux_impl(argc, argv);
    }
    if (v3_streq(argv[1], "run-linux-object-smokes")) {
        return v3_cmd_run_linux_object_smokes_impl(argc, argv);
    }
    if (v3_streq(argv[1], "run-tcp-twoproc-smoke")) {
        return v3_cmd_run_tcp_twoproc_smoke_impl(argc, argv);
    }
    if (v3_streq(argv[1], "run-quic-twoproc-smoke")) {
        return v3_cmd_run_quic_twoproc_smoke_impl(argc, argv);
    }
    if (v3_streq(argv[1], "run-udp-importc-smoke")) {
        return v3_cmd_run_udp_importc_smoke_impl(argc, argv);
    }
    if (v3_streq(argv[1], "run-wasm-smokes")) {
        return v3_cmd_run_wasm_smokes_impl(argc, argv);
    }
    if (v3_streq(argv[1], "run-browser-host-wasm-smoke")) {
        return v3_cmd_run_browser_host_wasm_smoke_impl(argc, argv);
    }
    if (v3_streq(argv[1], "run-chain-node-cli-smoke")) {
        return v3_cmd_run_chain_node_cli_smoke_impl(argc, argv);
    }
    if (v3_streq(argv[1], "run-chain-node-process-smoke")) {
        return v3_cmd_run_chain_node_process_smoke_impl(argc, argv);
    }
    if (v3_streq(argv[1], "run-chain-node-three-node-smoke")) {
        return v3_cmd_run_chain_node_three_node_smoke_impl(argc, argv);
    }
    if (v3_streq(argv[1], "run-tailnet-control-smoke")) {
        return v3_cmd_run_tailnet_control_smoke_impl(argc, argv);
    }
    if (v3_streq(argv[1], "run-fresh-node-selfhost-gate")) {
        return v3_cmd_run_fresh_node_selfhost_gate_impl(argc, argv);
    }
    if (v3_streq(argv[1], "run-migration-gate")) {
        return v3_cmd_run_migration_gate_impl(argc, argv);
    }
    if (v3_streq(argv[1], "run-browser-webrtc-smokes")) {
        return v3_cmd_run_browser_webrtc_smokes_impl(argc, argv);
    }
    if (v3_streq(argv[1], "emit-csg")) {
        return v3_cmd_emit_csg(argc, argv);
    }
    if (v3_streq(argv[1], "migrate-csg")) {
        return v3_cmd_migrate_csg(argc, argv);
    }
    if (v3_streq(argv[1], "verify-world")) {
        return v3_cmd_verify_world(argc, argv);
    }
    if (v3_streq(argv[1], "world-sync")) {
        return v3_cmd_world_sync(argc, argv);
    }
    if (v3_streq(argv[1], "prove-equivalence")) {
        return v3_cmd_prove_equivalence(argc, argv);
    }
    if (v3_streq(argv[1], "prove-migration")) {
        return v3_cmd_prove_migration(argc, argv);
    }
    if (v3_streq(argv[1], "publish-world")) {
        return v3_cmd_publish_world(argc, argv);
    }
    if (v3_streq(argv[1], "fresh-node-selfhost")) {
        return v3_cmd_fresh_node_selfhost(argc, argv);
    }
    if (v3_streq(argv[1], "selfhost-build")) {
        return v3_cmd_selfhost_build(argc, argv);
    }
    if (v3_streq(argv[1], "system-link-exec")) {
        return v3_cmd_system_link_exec(argc, argv);
    }
    if (v3_streq(argv[1], "compile-bootstrap")) {
        return v3_cmd_compile_bootstrap(argc, argv);
    }
    if (v3_streq(argv[1], "--help") || v3_streq(argv[1], "-h") || v3_streq(argv[1], "help")) {
        v3_usage();
        return 0;
    }
    fprintf(stderr, "[cheng_v3_seed] unknown command: %s\n", argv[1]);
    v3_usage();
    return 2;
}
