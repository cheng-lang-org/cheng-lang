## 进度

| 记录口径 | 内容 |
|---|---|
| 规则 | 从 `2026-04-09` 起，`progress.md` 只写开始时间；完成时间不在这里记录，由用户统计真实时间。历史条目不追改。 |

| 步骤 | 开始时间 | 状态 | 备注 |
|---|---|---|---|
| `v3` 纯 Cheng `QUIC/TLS` ordinary 假根继续剥离 | `2026-04-11 20:11 +0800` | 进行中 | `MsQuicTlsPolicy` 数组壳已经拆平，localhost test PKI 也已切成标量 decode 入口；`msquicNativeEnsureInit()` 里的 eager session reset 已去掉，`initMsQuicSettings()` 也已从 record literal 改成逐字段赋值。fresh `quic_native_listener_smoke` 现在不再把 `initMsQuicSettings/msquicTlsPolicyHasServerInputs` 挂在第一现场，真 blocker 已前移到 `native_runtime::msquicNativeTaggedAddr/msquicNativeStartListenerServerInputs` 和 `platform/datapath_runtime/datapath_udp`。 |
| `v3` 纯 Cheng `QUIC/TLS` 继续迁移 + split smoke 真 blocker 收口 | `2026-04-11 19:01 +0800` | 进行中 | `strToCStringTemp` 已收成 runtime bridge，`std/os + udp_syscall` 也已切到 `libc_*` 宿主桥；新的 `udp_importc_smoke` 已在 host、stage2、stage3 真编真跑通过，并已正式接进 `run_v3_host_smokes.sh` 且全量 host smokes 继续全绿。与此同时，full `libp2p_quic_tls_smoke` 的 `rc=139 + 空日志` 已被拆成两条可解释的 ordinary 真 blocker：`quic_tls_policy_smoke` 现在稳定卡在 `test_pki/policy` 的 `stmt_call/stmt_var/composite return`，`quic_native_listener_smoke` 稳定卡在 `msquictransport_native/native_runtime` 的大复合 ABI/布局缺口，不再被 UDP 假根或 seed 空日志崩溃误导。 |
| fresh `backend_driver/host/slice_gate` 真相源收正 | `2026-04-11 18:52 +0800` | 已完成 | fresh `run_slice_gate.sh` 先诚实暴露出 `host -> quic_transport -> msquicconnection` 的无条件强耦合，会把 `chain_node_libp2p_smoke` 这条 TCP 主线拖进 `msquic` 闭包；现在已把 base [host.cheng](/Users/lbcheng/cheng-lang/v3/src/libp2p/host/host.cheng) 收回 TCP 语义，并新增 [host_quic.cheng](/Users/lbcheng/cheng-lang/v3/src/libp2p/host/host_quic.cheng) 承接 QUIC 扩展。随后把两份 README 和两份状态文档里的旧 `chain_node` 入口/CLI 说法收正，并把 `run_v3_stage23_libp2p_smokes.sh` 的 mandatory 列表收回到当前真闭合的 TCP/libp2p 主线。fresh `run_v3_host_smokes.sh`、`run_v3_stage23_libp2p_smokes.sh`、`run_slice_gate.sh` 已全部重新前台通过。 |
| `v3` stale handle 默认带栈 crash + TCP/process 主 gate 复核 | `2026-04-11 18:14 +0800` | 进行中 | 已先收掉 `cheng_v3_seed.c` 里 `v3_call_expr_string_literal_count(...)` 的自递归爆栈，再把 `libp2p_tcp_twoproc` 握手和 `chain_node_process_client` 的 `--port` 读取修正。`system_helpers.c` 的 generational handle 也已从 stale 返回码收严成默认 `reason=ffi_handle` 栈回溯，并新增 `ffi_handle_stale_trap_smoke`；`build_ffi_handle_v3.sh` 已 fresh 真过。随后把 `run_v3_stage23_libp2p_smokes.sh` 收成禁用 incremental/module-cache 的 correctness 口径，`libp2p_host_smoke` 的假性 QUIC 闭包污染已经消失；当前新的第一处真 blocker 已前移到 `libp2p_quic_tls_smoke`，卡在 `std/system::strToCStringTemp` 和 QUIC/TLS ordinary 子集。 |
| `v3` libp2p sync request-response 真 TCP 化 + 两进程 gate 收口 | `2026-04-11 18:10 +0800` | 已完成 | 已在 native runtime 补 `cheng_v3_tcp_loopback_request_response_bridge(...)`，`store_sync` 也已改成真 `encode/decode`，`host.v3Libp2pSyncRequest(...)` 现在真走 `encode -> tcp rr -> decode`。新增的 `libp2p_tcp_twoproc_server/client` 与 `run_v3_tcp_twoproc_smoke.sh` 已接进 host 与 `stage2/stage3` gate；`chain_node_process_smoke` 也已统一成 `ready-path` 文件写端口握手，并在 host、`stage2`、`stage3` 全部真过。 |
| `v3` libp2p tcp 真实二进制 underlay 收口 | `2026-04-11 17:48 +0800` | 已完成 | 已在 `tcp_transport` 接上新的 native `payload bridge`，不再只测 multistream 空握手；`libp2p_tcp_smoke` 现会真跑 `chain_node snapshot` 的编码、真 TCP 往返、解码和 `syncOnce` 校验。targeted host、整组 `run_v3_stage23_libp2p_smokes.sh`、整组 `run_v3_host_smokes.sh` 都已 fresh 前台通过。 |
| `chain_node_libp2p` snapshot codec 收口并升进正式 gate | `2026-04-11 17:06 +0800` | 已完成 | 已把 `v3ChainNodeLibp2pDecodeSignature/DecodeEvent` 收成 `Result[int32]` 新偏移返回，并把 `v3ChainNodeLibp2pReadI64BE(...)` 对齐到 `rwad_bft` 的固定口径。随后补了 `payloadLen/payloadCid/raw payload` 校验，确认问题不在 `store`/`Bytes` 拷贝链。现在 `chain_node_libp2p_smoke` 已在 targeted host、`stage2`、`stage3`、整组 `run_v3_stage23_libp2p_smokes.sh` 与整组 `run_v3_host_smokes.sh` 全部真过。 |
| `v3` slice gate 全绿 | `2026-04-11 16:53 +0800` | 已完成 | 已恢复 `chain_node_smoke` 的 `signature一致` 正式断言，并把 `system_link_plan` 的 `parserSourceKind` 从热点 `str` 收回整型 kind；随后 fresh 重跑 `bootstrap_bridge_v3.sh` 刷新 `cheng.stage2/cheng.stage3`，消掉旧自举产物把 `libp2p_protocols_smoke` 误报成 `stmt_var` 的假失败。当前 `build_chain_node_v3.sh`、`run_v3_host_smokes.sh`、`run_v3_stage23_libp2p_smokes.sh`、`run_slice_gate.sh` 已全部前台通过。 |
| `v3` libp2p payload bytes / protocols `stage2/stage3` 全绿 | `2026-04-11 16:41 +0800` | 已完成 | `overlay.V3IngressEnvelope` 已补上真实 `payload: Bytes`，`libp2p_protocols_smoke` 现会真验 `topicId / publishLog / storeEntries / payloadLen / payloadCid / raw payload head`。`run_v3_stage23_libp2p_smokes.sh` 也已扩到 `libp2p_core/libp2p_host/libp2p_tcp/libp2p_overlay/libp2p_protocols` 五个 smoke，并在 `stage2/stage3` 双编双跑全绿后输出 `v3 stage2/stage3 libp2p: ok`。 |
| `v3` libp2p overlay `stage2/stage3` 全绿 | `2026-04-11 16:12 +0800` | 已完成 | 已把 `rawbytes/sha256/libp2p/overlay smoke` 这条 ordinary 形状收平：去掉复合 zero helper 和多层 wrapper，`libp2p_overlay_smoke` 改成直接检查 host 真实 store state，`sha256` 的 `u32/u32Mask` 嵌套表达式也已拆平。fresh `run_v3_stage23_libp2p_smokes.sh` 现已完整输出 `libp2p_core/libp2p_host/libp2p_tcp/libp2p_overlay` 的 `stage2/stage3` 双编双跑全绿，并以 `v3 stage2/stage3 libp2p: ok` 收尾。 |
| `v3` zero-arg call 脏栈清零 | `2026-04-11 15:55 +0800` | 已完成 | 已在 `v3/bootstrap/cheng_v3_seed.c` 把 `arg_text/result_arg_text` 和 `V3ExprPrepScratch` 全部收成确定初始化，修掉 `emptyBytes()` 这类零参调用把旧栈字节误读成前缀实参的真 bug。fresh `build_backend_driver_v3.sh`、`sha256_inline_smoke`、`sha256_runtime_smoke`、`sha256_core_smoke`、`get_u32be_smoke`、`sha256_schedule_smoke`、`sha256_round_smoke`、`fixed256_sha256_smoke`、`compiler_pipeline_stub_smoke` 已全部真编真跑通过。 |
| `v3` ordinary prepare/call-depth/stack 帧真修复 | `2026-04-11 15:55 +0800` | 已完成 | 已在 `v3/bootstrap/cheng_v3_seed.c` 修掉 `V3ExprPrepScratch` 数组退化成指针后 `sizeof(...) == 8` 的真 bug，并把构造器从 prepare 的“普通运行时调用”路径里摘掉。fresh `bootstrap_bridge_v3.sh`、`build_backend_driver_v3.sh`、`compiler_runtime_smoke`、`compiler_pipeline_stub_smoke`、`build_twoway_search_smoke_v3.sh` 全部真过；`v3BackendBuildPlanDefault` 汇编帧已从旧的危险布局抬到 `sub sp, sp, #1616`，字符串 scratch 在 `sp+1384`，不会再踩到 `sp+1600` 的 `FP/LR` 保存区。 |
| `v3` libp2p stage2/stage3 `core/host/tcp` ordinary 收口 | `2026-04-11 15:50 +0800` | 进行中 | `peer_id` 已从复合 `FixedBytes32` 返回改成 `sha256 word bridge + out.value.data[0..31]` 直接写回，`libp2p_peer_id_smoke` 在 `stage2/stage3` 已真过。`multiaddr parser` 仍会把 ordinary 拉进 `Result[Multiaddr]` / `__cstring` 崩点，所以主 gate 先改走 `v3Libp2pMultiaddrFillTcp/FillQuic` 真实构造地址对象。当前 fresh 结果：`libp2p_core_smoke`、`libp2p_host_smoke`、`libp2p_tcp_smoke` 已在 `stage2/stage3` 全绿；新的第一处真 blocker 已前移到 `libp2p_overlay_smoke`，卡在 `byteBufView / v3HashInts / gossipsub / store_sync / host store&sync` 这组 ordinary 复合参数 lowering。 |
| `v3` Two-way 字符串搜索核 native 落地 | `2026-04-11 15:30 +0800` | 已完成 | 已在 `v3/runtime/native` 新增 bytes-aware `Two-way` 搜索核和 native 穷举 smoke；`system_helpers.c`、`system_helpers_selflink_min_runtime.c`、`system_helpers_selflink_shim.c` 现已统一接入，bridge 路径优先吃 `str.len` 而不是回退 `strlen`。`build_twoway_search_smoke_v3.sh` 已 fresh 输出 `v3 twoway search smoke ok`，`compiler_pipeline_stub_smoke` 也继续真跑通过。 |
| `v3` 机器级真相源 | `2026-04-11 14:59 +0800` | 已完成 | `src/runtime/native/system_helpers.c` 已把 `signal` 和 `panic/bounds` 两条崩溃链统一升级成 `machine-trace + source-trace`：signal 直接打印 `pc/fp/sp/lr` 和 FP 链机器帧，普通 panic/bounds 走真实 `backtrace pc` 并优先用内嵌 `PC range -> source span` 表映射。`build_signal_trace_v3.sh`、`build_bounds_trace_v3.sh`、`build_panic_trace_v3.sh` 也已收严并 fresh 真跑通过；`compiler_runtime_smoke` 这类以前只报 `mapped_frames=0` 的崩点现在也会先打印机器帧。 |
| `v3` 默认异常堆栈全开 + parser bridge 堆所有权收口 | `2026-04-11 14:52 +0800` | 已完成 | 已把 `src/runtime/native/system_helpers.c` 里 parser bridge 的空串/错误返回统一成 `cheng_v3_bridge_owned_str(...)`，消掉 `compiler_pipeline_stub_smoke` generated exe 在 `cheng_v3_bridge_release_owned(...)` 处的非法释放。随后用 fresh `artifacts/v3_backend_driver/cheng` 真跑 `build_panic_trace_v3.sh`、`build_bounds_trace_v3.sh`、`build_signal_trace_v3.sh`，三条都已默认打印源码栈；`compiler_pipeline_stub_smoke` 重新真跑输出 `v3 compiler_pipeline_stub_smoke ok`。`lowering_plan_smoke` 仍然崩，但现在会默认把第一现场打到 `v3/src/backend/lowering_plan.cheng:75`。 |
| `compiler_pipeline` generated exe parser 路径链收口 | `2026-04-11 13:22 +0800` | 进行中 | 已先把 `v3/src/backend/system_link_plan.cheng` 的 `v3SystemLinkPlanStubReport(...)` 拆成 getter + 局部标量，`compiler_pipeline_stub_smoke` 编译面重新回到 `COMPILE_RC:0`。随后继续把 `v3/src/lang/parser.cheng` 里 `packageId/sourceExists/fileStem/dirPart/modulePathToSourcePath/sourcePathToModulePath` 改成更线性的 ordinary 形状，并把 parser 主链里的 `v3PathSplitFile`、内部 `v3PathJoin` 和直接 `return a + b + c` 逐步拔掉。当前 generated exe 已能稳定跑过顶层 `packageId/contractsPath/sourceExists` 与 `v3ParseOrdinarySourceStub(...)->parse5`，新的第一处真 blocker 已收窄到 `v3ParserReadImportEdges(...)` 中处理 `std/...` 导入时再次调用 `v3ParserModulePathToSourcePath(...)` 的返回链。 |
| `lowering_plan` ordinary 收口 + seed call lowering 真阻塞前移 | `2026-04-11 12:56 +0800` | 进行中 | 已把 `v3/src/backend/lowering_plan.cheng` 和 `v3/src/tests/lowering_plan_smoke.cheng` 再压成更硬的 ordinary shape，并在 `v3/bootstrap/cheng_v3_seed.c` 修掉 `v3_prepare_expr_call_state(...)` 的 Darwin 爆栈，`___chkstk_darwin` 已不再是 blocker。当前 fresh `lowering_plan_smoke` 的第一处真失败已经前移到 seed 的 composite call-arg lowering：`v3PathTrim(raw)`、`v3path.v3ReadTextFile("", sourcePath)` 这类复合参数表达式还没有被稳定物化成临时局部，emit 阶段会报 `composite-arg local missing`。 |
| `RWAD-BFT` 冷轨适配层落地 | `2026-04-11 12:43 +0800` | 进行中 | 已在 `v3/src/project/rwad_serial_state_machine.cheng` 把 `lastFinalizedHeight` 和 `lastApplyOrderKey` 分开，并新增 `v3/src/project/rwad_bft_state_machine.cheng`、`rwad_bft_state_machine_main.cheng`、`v3/src/tests/rwad_bft_state_machine_smoke.cheng`、`v3/tooling/build_rwad_bft_state_machine_v3.sh`。这轮又把 `RWAD-BFT` 继续收成固定宽度二进制接口：已补 `v3RwadBftEncodeTx/DecodeTx/checkTxBytes/finalizeBlockBytesSummary`，并用 fresh `artifacts/v3_backend_driver/cheng` 真编真跑通过。整条 `v3/tooling/run_v3_host_smokes.sh` 现在已经正式跑过 `rwad_serial_state_machine_smoke`、`rwad_bft_state_machine_smoke` 和 `compiler_pipeline_stub_smoke`，新的第一处真 blocker 已前移到 `lowering_plan_smoke`。 |
| `RWAD` 原子级序列冷轨落地 | `2026-04-11 12:23 +0800` | 进行中 | 已新增 `v3/src/project/rwad_serial_state_machine.cheng`，把 `interval note + split transfer + nullifier + ownerRoot/serialRoot/nullifierRoot/appHash` 收成固定布局冷轨状态机；`v3/src/tests/rwad_serial_state_machine_smoke.cheng` 已用 fresh `artifacts/v3_backend_driver/cheng` 真编真跑输出 `v3 rwad_serial_state_machine_smoke ok`。同时已把新 smoke 接进 `v3/tooling/run_v3_host_smokes.sh` 前半段，并在 `v3/docs/cheng语言特性矩阵和开发计划.md` 写明当前只真落区间账本和确定性摘要，不把未实现的 accumulator/zk 伪装成已完成。 |
| `compiler_pipeline_stub_smoke` source shape 拆平 + seed 真 blocker 钉死 | `2026-04-11 16:09 +0800` | 进行中 | 已把 `v3/src/tests/compiler_pipeline_stub_smoke.cheng` 里的嵌套 `if/echo` 收成断言；`v3/src/lang/parser.cheng` 的 `v3ParserFirstToken(...)` 改成 ordinary 可吃的显式扫描，`v3ParseOrdinarySourceStub(...)` 改成显式字段写回；`v3/src/backend/system_link_plan.cheng` 也把 nested composite arg、整块 `build plan` 复合读取和多层 source shape 拆平。现在 `compiler_pipeline_stub_smoke` 已不再被 `parser/path/runtime` 卡住，fresh compile 只剩 `cheng/v3/backend/system_link_plan::v3BuildSystemLinkPlanStub` 这一处真 lowering 缺口；日志路径固定是 `/tmp/compiler_pipeline_stub_smoke.debug.log`。 |
| `v3` parser/path/runtime bridge 收口 + host 第一阻塞前移 | `2026-04-11 15:23 +0800` | 进行中 | 已把 `parser_path_smoke` 的真根收掉：`v3/bootstrap/cheng_v3_seed.c` 里 `str == str` 改走 `_driver_c_str_eq_bridge`，composite-return call 的字符串字面量 scratch 让开一个 `str` 槽位并给每层 `string_temp_stride` 额外预留一个槽；`src/runtime/native/system_helpers.c` 的 `driver_c_str_slice_bridge(...)` 也改成手工组 `ChengStrBridge`，不再把短字符串返回桥坏。配套上，`v3/src/lang/parser.cheng` 已把 `ModulePathToSourcePath` 收成 ordinary 可吃的内联 shape，`parser_path_smoke` fresh 真编真跑已输出 `v3 parser_path_smoke ok`。继续往前跑 `run_v3_host_smokes.sh` 后，新的第一处真 blocker 已前移到 `compiler_pipeline_stub_smoke`。 |
| `v3` 冷热双轨主线收口 + `overlay/BFT` 新 smoke 接通 | `2026-04-11 13:20 +0800` | 进行中 | 已把 `v3` 默认主线正式写成 `libp2p underlay + LSMR overlay + Cheng <-> BFT state machine interface`，并新增 `v3/src/overlay/contracts.cheng`、`v3/src/tests/overlay_contracts_smoke.cheng`，同时在 `v3/src/project/bft_state_machine.cheng` 补上 `TxHeader/TxBody/BlockContext/TxCheckResult/queryAppHash`。文档和 host smokes 也已切到冷热双轨口径。`src/runtime/native/system_helpers.c` 的 `cheng_dir_exists` 原型缺口已补齐后，`overlay_contracts_smoke` 已 fresh 真编真跑输出 `v3 overlay_contracts_smoke ok`。`bft_state_machine_smoke` 也已经从“编不过”前移到真运行断言：当前卡在 `block1 appliedCount`，根因已经收窄成 `BFT` 状态机把权威账本塞进嵌套复合字段，ordinary 对这类嵌套 `var` 状态更新不稳。 |
| `Cheng <-> BFT state machine interface` 纯预演与复合输出子集收紧 | `2026-04-11 12:32 +0800` | 进行中 | 已确认 `v3BftStateMachineCheckTx(...)` 里的 `var probe = machine` 会浅拷贝污染原状态，现已改成 clone-index 纯预演路径，并在自测里补了“`CheckTx` 不得改写 event log / state”守卫。继续往前推后，又确认 fresh ordinary compile/runtime 仍对 `BFT` 这层的复合输出 shape 不稳：`FinalizeBatch` 返回/写回的逐 tx 结果面还没完全压进当前 backend 能稳定吃下的子集。当前下一步是继续把 `finalize` 结果口径压向更硬的 fixed-layout/out-param 形状，或者直接补 ordinary lowering 对这类复合输出的支持。 |
| `Cheng <-> BFT state machine interface` 最小边界落地 | `2026-04-11 11:35 +0800` | 进行中 | 已新增 `v3/src/project/bft_state_machine.cheng`，先把 `tx` 固定宽度二进制、`checkTx/finalizeBlock/commit/queryBalance`、块高单调和 `appHash` 提交摘要落成独立边界，并补了 `bft_state_machine_main.cheng`、`bft_state_machine_smoke.cheng`、`build_bft_state_machine_v3.sh`。这层已从最初的 `chain_node` 适配收正成“只复用 `consensus` 规则的独立结算账本”，避免把 `LSMR` 未闭合的 ordinary 形状带进 `BFT` 接口。当前下一步是用 fresh backend driver 真编真跑这条新链，并把失败收窄到它自身的 ordinary 子集缺口。 |
| `v3` 固定热核 smoke 拆分 + `LSMR` 安全边界文档收口 | `2026-04-11 10:48 +0800` | 进行中 | `fixed_surface_smoke` 已拆成 `fixed256_sha256_smoke`、`ref10_ashr_smoke`、`fixed256_curve25519_smoke` 三个更小的入口。先前用这个拆分钉死了错误公钥 `de2f913413e4be337fd0dd513b48c97184d5e5dedb0074478ed360dbd2181d06`；随后继续深挖发现真根不是 `ref10` 算法文本，而是 `v3/bootstrap/cheng_v3_seed.c` 对 `>>` 一律发 `lsrv`，把 signed right shift 做错了。现在已经按 `signed/unsigned + i32/i64` 改成 `asrv/lsrv`，并给 signed `i32` 路补了 `sxtw`；`ref10_ashr_smoke`、`fixed256_curve25519_smoke`、`fixed_surface_smoke` 都已 fresh 真编真跑转绿。与此同时，`v3/docs/LSMR.md` 和 `v3/docs/cheng语言特性矩阵和开发计划.md` 已补上默认安全边界、`local/regional/global` finality 与 `RWAD` 结算边界，以及 `LSMR` 安全 gate 与默认不承诺项。当前 `run_v3_host_smokes.sh` 的第一处新 blocker 已前移到 `compiler_runtime_smoke`：ordinary body 还缺 `stmt_let / stmt_if / return_expr`。 |
| `v3` seed `add(seq, complex rhs)` spill + `chain_node` 主链打通 | `2026-04-11 09:32 +0800` | 已完成 | `v3/bootstrap/cheng_v3_seed.c` 已把 `cheng_seq_set_grow` 返回的新元素槽位先 spill，再算右值，再写回；新增 `seq_add_member_index_rhs_smoke` 后，这条 lowering bug 已被最小用例钉死。随后 `bagua_prefix_tree_fill_smoke`、`chain_node_transfer_wrapper_forms_smoke`、`chain_node_smoke`、正式 `build_chain_node_v3.sh` 已全部 fresh 前台通过，`v3BuildBaguaPrefixTreeFill(...)` 和 `v3ChainNodeTransfer(...)` 这两条本轮真 blocker 已收口。 |
| `v3` lsmr types ordinary `str` compare / concat 收口 | `2026-04-11 10:12 +0800` | 进行中 | `v3/bootstrap/cheng_v3_seed.c` 已补上 `str/composite` 比较 lowering：左右表达式先物化成 `str`，转 C string 后走 `cheng_strcmp`。同时收掉了 `str` compare/concat 两处跨 `bl` 继续直接用 `x15/x14` 的寄存器踩坏问题，改成每次调用后重取 scratch/dest 地址。现在 `lsmr_types_smoke` 已 fresh 真编真跑输出 `v3 lsmr_types_smoke ok`。继续往前跑 `run_v3_host_smokes.sh` 后，新的第一处真 blocker 已切到 `fixed_surface_smoke`，当前暴露的是大 crypto surface 的 ordinary 子集缺口，不再是 `lsmr_types` 那条。 |
| `v3` 彻底切断 `v2` 依赖并 clean rebuild | `2026-04-11 09:26 +0800` | 已完成 | `v3/tooling/run_v3_host_smokes.sh` 已改成只认 `artifacts/v3_backend_driver/cheng`，`v3/docs` 里的 `v2/` 活路径也已清零；随后已物理删除 `v2`、旧 `artifacts`、`chengcache`、`.cheng-cache`，并 fresh 跑通 `bootstrap_bridge_v3.sh`、`build_backend_driver_v3.sh`。当前再次确认：`v3` 下已没有 `v2` 文本依赖，`artifacts` 里也已搜不到 `cheng_v2` 或 `v2/`。clean rebuild 暴露出来的 `fixed_surface_smoke` 与 `v3BuildBaguaPrefixTreeFill(...)` 已转入下一阶段作为纯 `v3` blocker 继续处理。 |
| `v3` 复合 `seq[index]` 与 `lsmr` host smoke 收口 | `2026-04-11 08:44 +0800` | 进行中 | `v3/bootstrap/cheng_v3_seed.c` 已把 `seq[index]` 内部用到的 `dest/base/index` spill 分成独立槽位，`fixedbytes32_seq_index_smoke` 现在 fresh 真编真跑输出 `v3 fixedbytes32_seq_index_smoke ok`。同时 `v3/src/chain/lsmr.cheng` 已补回 `v3LsmrAddressBagua(...)`，并把 `v3LsmrLocalityHashText(...)` 收成大小写/空白规范化，`lsmr_locality_storage_smoke` fresh 真编真跑已转绿。继续往前跑 `run_v3_host_smokes.sh` 后，新第一处真 blocker 已收窄到 `lsmr_types_smoke`：ordinary lowering 还不会吃 `Result[V3PrefixRef]` 的复合返回、非空列表字面量和 `lt.v3PrefixRefText(...) == \"6/1/5\"` 这种 `str` 复合返回比较。 |
| `v3` bounds 默认异常栈收口 | `2026-04-11 08:29 +0800` | 已完成 | `src/runtime/native/system_helpers.c` 里的 `cheng_bounds_check` 已收成和 `panic` 一样的统一异常栈入口，默认直接打印 `reason=bounds + Cheng 源码栈 + native 栈`；同时 `v3/bootstrap/cheng_v3_seed.c` 也已给 ordinary `[]` 地址计算补上真实 `cheng_bounds_check` 调用，并在索引求值和 check 调用前后保住 `base/index`。新增 `v3/src/tests/ordinary_bounds_trace_fixture.cheng` 和 `v3/tooling/build_bounds_trace_v3.sh` 后，前台已真打出 `ordinary_bounds_trace_fixture.cheng:4-10`。 |
| `v3` wrapper smoke 假红收正 | `2026-04-11 07:43 +0800` | 已完成 | 新补 `member_index_call_smoke`、`bytes_param_helper_smoke` 后，确认 `member[index(call)]` 和 `Bytes, Bytes` helper 都已经打通。随后发现一串 `consensus_*wrapper*_smoke` 的失败并不是 seed 回归，而是测试把 `accountIds.cap == 1` 误当语义；根据 `src/runtime/native/system_helpers.c` 里的 `cheng_seq_set_grow()`，首次扩容最小就是 `4`。现已把这些假 gate 统一改成 `cap >= 1`，并真编真跑通过。 |
| `v3` 二元表达式 spill / 栈帧 guard 收口 | `2026-04-11 07:03 +0800` | 已完成 | `v3/bootstrap/cheng_v3_seed.c` 已把二元表达式改成“左值 spill + 嵌套子树下沉一层 `call_depth`”，并新增 16 字节栈顶 guard，修掉 `getU32BE` 这类嵌套表达式把 spill 写到保存的 `x29/x30` 上的真栈破坏。fresh `stage2 + backend driver` 下，`sha256_core_smoke`、`get_u32be_smoke`、`sha256_distinct_smoke`、`consensus_init_smoke`、`consensus_transfer_apply_smoke` 已全部真编真跑通过。 |
| `FFI generation handle` 验收面收正 | `2026-04-11 05:46 +0800` | 已完成 | `src/tooling/cheng_tooling_embedded_scripts/verify_backend_ffi_handle_sandbox.sh` 已收正成“C probe 真跑 + `@ffi_handle` fixture compile gate”：C probe 会验证释放后旧 handle 立即失效、同槽复用后 `h1 != h0`；`@ffi_handle` fixture 会真编过，并强制检查 primary object 里已经带上 `signature_line.1=@ffi_handle`。同时补了 `tests/cheng/backend/cheng-package.toml`，让 backend fixture 有正式包根；`tests/cheng/backend/fixtures/ffi_importc_handle_sandbox_i32.cheng` 也已补上 `h1 != h0` 断言。 |
| linkerless signal getter 兼容收口 | `2026-04-11 05:46 +0800` | 已完成 | `src/runtime/native/system_helpers.c` 现在会在安全时机用 `dlsym` 缓存 `_cheng_v3_embedded_line_map_*` getter，避免 host compiler 产物因为缺符号直接链接失败；`cheng_v3_register_line_map_from_argv0` 也已改成不再把装 signal handler 绑死在 `.v3.map` 读成功上，所以 `build_signal_trace_v3.sh` 继续真打印 `ordinary_signal_trace_fixture.cheng:2-6`。 |
| `v3` linkerless signal source-trace 落地 | `2026-04-11 05:40 +0800` | 已完成 | `v3/bootstrap/cheng_v3_seed.c` 现在会把 ordinary 函数和 entry bridge 的 `start/end` 边界、源码路径、函数名、行号表直接编进产物，并导出 `_cheng_v3_embedded_line_map_{entries,count}_get`；`src/runtime/native/system_helpers.c` 也已改成 `SA_SIGINFO + PC/frame pointer walk + write(2,...)` 的 async-safe 源码栈反查。前台已通过 `cc -std=c11 -O0 -Wall -Wextra -pedantic`、`build_backend_driver_v3.sh`、`build_zero_exit_v3.sh`、`build_call_chain_v3.sh`、`build_panic_trace_v3.sh`、`build_signal_trace_v3.sh`，其中 signal fixture 已真打印 `ordinary_signal_trace_fixture.cheng:2-6`。 |
| `v3` linkerless panic source-trace 落地 | `2026-04-11 05:23 +0800` | 已完成 | `v3/bootstrap/cheng_v3_seed.c` 现在会为 ordinary 可执行产物写出 `<out>.v3.map`；`v3/runtime/native/{v3_program_argv_native.c,v3_tooling_argv_native.c}` 启动时会自动注册 line-map；`src/runtime/native/system_helpers.c` 里的 `panic/assert/bounds` 已能打印函数级源码 span。前台已通过 `cc -std=c11 -O0 -Wall -Wextra -pedantic`、`build_backend_driver_v3.sh`、`build_zero_exit_v3.sh`、`build_call_chain_v3.sh`、`build_panic_trace_v3.sh`，其中 panic fixture 已真打印 `ordinary_panic_fixture.cheng:2-3`。 |
| `v3` 文档硬边界收正 | `2026-04-11 05:06 +0800` | 已完成 | `v3/docs/LSMR.md` 已收正成“稳定洛书前缀 + 动态延迟/负载坐标 + 桶上限”口径，不再把地址稳定性绑死绝对地理，也不再把 `O(log_9 N)` 写成无条件真理；`v3/docs/cheng语言特性矩阵和开发计划.md` 也已补上 `FFI generation:index handle`、`debug 双轨`、`RWAD finalized reserve 才能进 NAV`、`UniMaker 标价与结算解耦` 四条硬规则。 |
| `v3` seed 无注解绑定类型反推落地 | `2026-04-10 17:20 +0800` | 进行中 | `v3/bootstrap/cheng_v3_seed.c` 现已把字符串字面量、标量字面量、top-level const、本地槽位、单参 cast、普通函数调用返回类型和首字母大写 record 构造接进 `expr -> type/abi` 反推；`V3AsmLocalSlot` 也开始记录 `type_text`。`cc -std=c11 -O0 -Wall -Wextra -pedantic`、`build_backend_driver_v3.sh`、`build_program_selfhost_v3.sh` 都继续通过；`build_chain_node_v3.sh` 仍稳定卡在 `v3ChainNodeMainSelfTest` 的 `stmt_var`，说明下一刀该直接做固定布局和复合 ABI。 |
| `v3` ordinary compile 假 blocker 收口到复合 ABI | `2026-04-10 17:09 +0800` | 进行中 | `v3/bootstrap/cheng_v3_seed.c` 已接上 top-level `const` 收集和标量常量解析，`chain_node.compile.log` 里的 `cheng/v3/chain/binary_types::v3LsmrDigitOk`、`cheng/v3/chain/lsmr_types::v3BaguaValid` 已从 `primary_object_unsupported_functions` 消失；name-only 重载解析也已收紧成“单候选才放行”，不再让 `rawmemAsVoid` 静默错绑。`build_backend_driver_v3.sh`、`scan_forbidden_hotpath.sh` 继续通过，`chain_node` 首个 blocker 仍稳定是 `v3ChainNodeMainSelfTest` 的 `stmt_var`：`var server = node.v3ChainNodeInit(\"node-a\", \"chain-node-a\")`。 |
| `v3` seed primary object / native link 最小闭环 | `2026-04-10 14:50 +0800` | 进行中 | `v3/bootstrap/cheng_v3_seed.c` 现已能为严格 `return_zero_i32` 子集真发 `primary .o`、真编 provider `.o`、真链接并真跑；`build_zero_exit_v3.sh` 已前台通过。`program-selfhost` 与 `chain_node` 现在统一稳定报 `v3 compiler: primary object body semantics missing`，说明 object/link 已接通，剩下的是函数体语义子集。 |
| `v3` seed ordinary command 面与 chain_node 段错误收口 | `2026-04-10 13:04 CST` | 进行中 | `v3/bootstrap/cheng_v3_seed.c` 现已把 ordinary runtime contract 和 bootstrap contract 分开，`status/print-build-plan/system-link-exec` 不再误读普通源码；CLI 也已接受空格/冒号/等号三种 flag 写法。与此同时，`system_link_plan` report 的固定缓冲区 `strcat` 和 `v3_collect_source_closure` 的栈上大 scratch 都已切掉，`chain_node` 的 `rc=139` 已消失。当前 `build_backend_driver_v3.sh` 稳定通过，`run_v3_host_smokes.sh` 全绿；`program-selfhost` 与 `chain_node` 现在统一死在 `runtime_targets_not_lowered + runtime_provider_modules_not_selected`。 |
| `v3` ordinary compile typed plan 与 honest backend-driver gate 收口 | `2026-04-10 12:40 CST` | 进行中 | `v3/src/lang/parser.cheng`、`v3/src/backend/system_link_plan.cheng`、`v3/src/backend/system_link_exec.cheng`、`v3/src/tooling/{compiler_request,compiler_runtime,compiler_main}.cheng` 这条普通编译 control/data plane 现在已经带上 package/workspace root、closure path、owner module、entry symbol、typed missing reasons，并且 `run_v3_host_smokes.sh` 已经全绿。与此同时，`v3/tooling/build_backend_driver_v3.sh` 已被收严成必须验证 built artifact 真的支持 `status/print-build-plan`；`run_slice_gate.sh` 现在会更早、更诚实地死在真正主根：`artifacts/v3_backend_driver/cheng` 还是 bootstrap 壳，`unknown command: status`。 |
| `v3` ordinary program/chain_node 真构建 gate 收严 | `2026-04-10 11:06 CST` | 进行中 | 已新增 `v3/tooling/build_program_selfhost_v3.sh`、`v3/tooling/build_chain_node_v3.sh` 和 `v3/src/project/chain_node_main.cheng`，并把 `v3/tooling/run_slice_gate.sh` 改成直接跑这两条真实构建脚本。`run_v3_host_smokes.sh` 继续全绿；`build_program_selfhost_v3.sh` 和 `run_slice_gate.sh` 现在都会稳定死在同一个真缺口：`[cheng_v3_seed] unknown command: system-link-exec`。另外，host compiler 直接编 `v3/src/project/chain_node_main.cheng` 还会暴露 `program_entry_exec_plan_missing local_payload_label0=type`，说明 `project` 入口这层还有额外执行面缺口。 |
| `v3` compiler control-plane 分离与 env/report 合同分离 | `2026-04-10 11:54 CST` | 进行中 | 已新增 `v3/src/tooling/{compiler_main,compiler_runtime,compiler_request}.cheng`、`v3/src/lang/parser.cheng`、`v3/src/backend/system_link_exec.cheng` 和 `v3/src/tests/compiler_runtime_smoke.cheng`，`v3/src/backend/build_plan.cheng` 也已把 `entryPath` 切到 `compiler_main.cheng`。host compiler 现已能真编 `compiler_main`，`help/status` 正常，`system-link-exec` 会稳定经过 `parser stub -> backend system_link_exec stub` 再报 `v3 compiler: ordinary pipeline not implemented`。`bootstrap_bridge_v3.sh` 现已把 `V3_COMPILER_ENTRY_SOURCE/V3_COMPILER_RUNTIME_SOURCE/V3_COMPILER_REQUEST_SOURCE` 写进 `bootstrap.env`，`build_backend_driver_v3.report.txt` 也会同时暴露 `planned_entry_source` 和 `materialized_source`。当前还没闭合的是 backend driver 构建脚本继续吃 `stage1_bootstrap.cheng`。 |
| `v3` 编译工具链 bootstrap bridge 收口与 sidecar 死代码清理 | `2026-04-10 08:24 CST` | 进行中 | 已删掉 `src/tooling` 下 32 个已跟踪 `cheng_sidecar_rewrite_*.cheng`、`cheng_tooling_embedded_stable.cheng` 和 `src/backend/tooling/backend_driver_uir_sidecar_direct_build.cheng`，并清掉源码树里未跟踪 sidecar 残片。`cheng_tooling.cheng`、`verify_backend_sidecar_cheng_fresh.sh`、`resolve_backend_sidecar_defaults.sh`、`backend_driver_currentsrc_sidecar_wrapper.sh`、`cheng_tooling.sh` 已改成接受 `probe_currentsrc_proof/cheng_stage0_currentsrc.proof` 作为 strict bootstrap bridge，`v3/tooling/run_slice_gate.sh` 默认已切到源码 launcher。当前 `verify_backend_sidecar_cheng_fresh.sh` 和 `v3/tooling/run_slice_gate.sh` 都已经越过旧的 published-surface / direct-export / mode-contract 断点，并稳定收敛到同一个真阻塞：`strict bootstrap sidecar driver failed wrapper-source build`。 |
| `build-backend-driver` source-list 与 launcher 真阻塞定位 | `2026-04-10 07:05 CST` | 进行中 | `src/std/system.cheng` 已删掉无效 `import std/seqs`；`src/tooling/cheng_tooling.cheng` 已把 `tooling_stage0CapabilitySourceList()` 改成“固定主链 + 运行时扫描 `uir_core_builder*.cheng` + 静态名单回落”。同时确认 `cheng_tooling.cheng` 默认产物是 launcher，只有 `TOOLING_EMIT_SELFHOST_LAUNCHER=0` 才会尝试直出 native。当前 canonical tooling 执行面仍旧，强制 native compile 还在 `artifacts/backend_driver/cheng` 上 `rc=223`，还没闭合。 |
| `v3` interner、IR 固定 ID 骨架和脚本化 gate 收口 | `2026-04-10 06:50 CST` | 已完成 | `v3/src/lang/intern.cheng`、`v3/src/ir/core_types.cheng`、`v3/src/tests/ir_core_smoke.cheng`、`v3/tooling/{scan_forbidden_hotpath,compare_bench,run_slice_gate}.sh` 和冻结基线样本都已落库。`scan_forbidden_hotpath.sh`、`compare_bench.sh` 已真跑通过；`run_slice_gate.sh` 已稳定先过 `scan + c_ref + frozen-vs-latest`，随后继续卡在仓库现有 `build-backend-driver` 全局故障，不是 `v3` 新代码单点错误。 |
| `v3` 固定布局骨架、二进制链帧和同机 C 基线 bring-up | `2026-04-10 06:45 CST` | 进行中 | `v3` 目录契约、`ByteSpan/ByteBuf/FixedBytes`、`fixed256` 接口、`chain` 二进制 codec、`bench/c_ref` 已落库；`make -C v3/bench/c_ref run` 已真跑出本机基线。当前 Cheng smoke 还不能验收，不是 `v3` 代码单点错误，而是现有 canonical backend driver 全局 `rc=223`，`build-backend-driver` 继续暴露 `src/std/system.cheng -> src/std/seqs.cheng -> src/std/system.cheng` 导入环。 |
| `program-selfhost` 真实多进程 gate 与 runtime type-text intern 收口 | `2026-04-10 06:21 CST` | 已完成 | `system_helpers_stdio_bridge.c` 已把 `array.elem_type_text / record.type_text` 收成全局 intern 稳定指针，不再在热路径里重复 `dup/free`；`cheng_v2c_tooling.c` 的 `program-selfhost-check` 已正式加入真实多进程 `chain_node_process_smoke`，固定要求 stage2 编译器真编真跑 `mint -> serve-once -> sync-once -> balance`。完整前台 gate 已在 `manifest_fnv1a64=b55b66018e18ab44` 下重新收口。 |
| `program runtime` fresh slot 与 nil-slot aggregate 白 clone 收口 | `2026-04-09 23:42 CST` | 已完成 | `MAKE_ARRAY/MAKE_RECORD/array_push/add/setLen/reserve/field-index create_if_nil` 已收成新槽位直写，`nil slot` 上新建 `array/record` 现在走 temp 所有权，不再先造空 aggregate 再深拷一次。`compiler_core_system_link_exec/program-selfhost/full-selfhost` 已在 `manifest_fnv1a64=b8a740987bf39861` 下重新收口。前台 3 次中位数更新为 `pubkey=1.1300s`、`sign=1.6300s`、`mul_xonly=1.3200s`、`kinv=0.7000s`；当前单次 `maximum resident set size` 中位数约 `125MB/262MB`，`peak memory footprint` 中位数约 `116MB/253MB`。 |
| `program runtime` typed record shared-shape 真收口 | `2026-04-09 23:06 CST` | 已完成 | shared-shape `record` 构造和 clone 现在统一只扩 `values`，直接复用 `TypeDecl/ZeroPlan` 的共享 `field_names/field_lookup`，不再先分配 names 再立刻切回 shared names。`compiler_core_system_link_exec/program-selfhost/full-selfhost` 已在当前 tree manifest `manifest_fnv1a64=e946db8ec549aaf2` 下重新收口。前台 3 次中位数更新为 `pubkey=1.1300s`、`sign=1.6500s`、`mul_xonly=1.3200s`、`kinv=0.7100s`；当前单次 `maximum resident set size` 中位数约 `125MB/262MB`，`peak memory footprint` 中位数约 `116MB/253MB`。 |
| `program runtime` typed record decl-shape 初始化收口 | `2026-04-09 22:35 CST` | 已完成 | `driver_c_prog_record_init_from_decl()` 已接进 `MAKE_RECORD`，typed record 现在会直接复用 `TypeDecl` 的共享 `field_lookup` 和 decl 顺序，不再每次 rebuild shape/lookup，也不会在顺序一致时继续做字段名查找。`compiler_core_system_link_exec/program-selfhost/full-selfhost` 已在当前 tree manifest `manifest_fnv1a64=fee6dad00582e08f` 下重新收口。前台 3 次中位数更新为 `pubkey=1.1800s`、`sign=1.7100s`、`mul_xonly=1.3500s`、`kinv=0.7200s`；当前单次 `maximum resident set size` 约 `125MB/262MB`，`peak memory footprint` 约 `117MB/253MB`。 |
| `field ordinal + record slot` 三层收口 | `2026-04-09 22:35 CST` | 已完成 | `stmtAssignTargetFieldOrdinals` 已接进 stage0 facts/high_uir/low_uir mirror，`addr_of_field/load_field/store_*field` 不再只靠字段名；runtime 侧 `driver_c_prog_record_slot_at(...)` 已成正式 slot 入口，`infer_type_arg_from_record_fields/make_record` 两处已知 decl 顺序也改成按 ordinal 直达。完整前台 gate 已在这轮 fixed point 上收口到 `manifest_fnv1a64=29cea01991c4689b`，`program-selfhost/full-selfhost` 继续通过。 |
| `program runtime` aggregate 自拷贝与 shape clone 收口 | `2026-04-09 20:40 CST` | 已完成 | `driver_c_prog_assign_slot` 现在会跳过“同一 array/record 写回原 slot”的纯自拷贝；`driver_c_prog_clone_value_deep` 也已收成保留 `array cap / record cap / record lookup shape` 后再递归 clone 值。前台 3 次中位数更新为 `pubkey=1.1700s`、`sign=1.6700s`、`mul_xonly=1.3400s`、`kinv=0.7200s`；完整前台 gate 已重新收口到 `manifest_fnv1a64=0f4abbf4fb95ca60`。 |
| `program runtime` root-only ephemeral clear 收口 | `2026-04-09 20:12 CST` | 已完成 | `driver_c_prog_value_clear_ephemeral_flag_root` 已通过 `p256_fixed_core_probe`、完整前台 gate、`program-selfhost`、`full-selfhost` 验证并保留。前台 3 次中位数更新为 `pubkey=1.1700s`、`sign=1.6800s`、`mul_xonly=1.3500s`、`kinv=0.7300s`；当前单次 `maximum resident set size` 约 `125MB/262MB`，`peak memory footprint` 约 `116MB/254MB`。`compiler-core-system-link-exec/program-selfhost/full-selfhost` 已重新收口到 `manifest_fnv1a64=4cb2f42ef1d06835`。 |
| `program runtime` nested `ZERO_PLAN` eager materialize 清理 | `2026-04-09 19:45 CST` | 已完成 | `driver_c_prog_clone_value_deep` 和 `driver_c_prog_value_clear_ephemeral_flags_deep` 现在只解 `REF`，不再把 nested `ZERO_PLAN` 提前整棵物化成真实 aggregate。前台 3 次中位数更新为 `pubkey=1.1900s`、`sign=1.7100s`、`mul_xonly=1.3600s`、`kinv=0.7300s`；当前单次 `maximum resident set size` 约 `125MB/262MB`，`peak memory footprint` 约 `116MB/254MB`。`compiler-core-system-link-exec/program-selfhost/full-selfhost` 已重新收口到 `manifest_fnv1a64=7da955d0c0bacad4`。 |
| `program runtime` lazy zero-plan 与 slot 写回修复 | `2026-04-09 17:35 CST` | 已完成 | typed `param/local_decl` 默认零值已改成 lazy `ZERO_PLAN`，ref load 改成 slot 级原地 materialize，`assign_slot` 会按 zero plan refine。顺手修掉 nested field store 在 `ZERO_PLAN` 下先物化临时值但不写回 slot 的丢写入 bug，`program-selfhost/full-selfhost` 继续通过。前台 3 次中位数更新为 `pubkey=0.9900s`、`sign=1.4100s`、`mul_xonly=1.1500s`、`kinv=0.6200s`；单次前台 RSS 约 `148MB/332MB`。 |
| `program runtime` 小 aggregate 内联与 zero-plan 预解码 | `2026-04-09 17:03 CST` | 已完成 | `DriverCProgArray/DriverCProgRecord` 现在都有小容量内联存储，`clone/zero plan` 不再走 `record_slot(...)` 搭壳，`DriverCProgFrame` 还缓存了 `param/local zero plans`。前台 3 次中位数更新为 `pubkey=1.0700s`、`sign=1.5300s`、`mul_xonly=1.2300s`、`kinv=0.6800s`；单次前台 RSS 约 `148MB/332MB`。 |
| `program runtime` 帧清理与小帧内联 | `2026-04-09 16:40 CST` | 已完成 | `eval_item` 现在会清理 `params/locals/stack/labels/loops`，小帧改成内联存储；`MAKE_ARRAY/MAKE_RECORD/STORE_* / NEW_REF` 也收成临时 aggregate move。前台 3 次中位数更新为 `pubkey=1.2500s`、`sign=1.7700s`，峰值 RSS 约 `147.9MB/331.6MB`。 |
| `double/triple/quad/eight -> TrustedInto` 试刀撤回 | `2026-04-09 15:25 CST` | 已完成 | `double/add/comb/repr` 对拍都继续全绿，但 `pubkey/sign` 单次前台样本回到 `1.50s/2.22s`，比稳定基线更差，所以整刀撤回，不留在 active 热链。 |
| 主线改成“现有闭合主链上重建关键热层” | `2026-04-09 15:15 CST` | 已完成 | 已把“不整仓重建，只重建 `program` 执行热层和纯 Cheng 热核接口”写进 `v2/docs/自举和性能.md`、`task_plan.md`、`findings.md`。 |
| 进度记录口径改为只写开始时间 | `2026-04-09 15:15 CST` | 已完成 | 已同步 `lessons.md`、`task_plan.md`、`progress.md`、`findings.md`；后续新条目按这套口径写。 |

| 步骤 | 状态 | 备注 |
|---|---|---|
| 自举与性能原则文档化 | 已完成 | `v2/docs/自举和性能.md` 已新增“从第一天就保证自举后仍有 C 级性能”和“当前仓库离这个目标还差什么”，正式写死：静态系统语义、AOT-only 程序轨、固定宽度专核、同机 C `1:1` benchmark 不能后补 |

| 步骤 | 状态 | 备注 |
|---|---|---|
| `p256_fixed` 低层真原地输出 | 已完成 | `p256FixedModAddTrustedInto`、`p256FixedModSubTrustedInto`、raw add/sub 和条件减模都已改成真原地输出，不再先返回整块 `P256Fixed` 再赋值 |
| `double cmp` correctness | 已完成 | `_tmp_p256_mul_double_cmp_probe_bin=ok` |
| `add cmp` correctness | 已完成 | `_tmp_p256_mul_add_cmp_probe_bin=ok` |
| `comb6` 生产路径 correctness | 已完成 | `_tmp_p256_comb_cmp_probe_bin=ok`，`_tmp_p256_repr_sign_r_cmp_probe_bin=repr_r_eq=1` |
| 新性能中位数 | 已完成 | 前台 3 次中位数：`pubkey=1.2830s`、`sign=1.8210s`、`mul=1.4628s`、`kinv=0.7521s` |
| 本轮主 gate | 已完成 | `make -j1 -C /Users/lbcheng/cheng-lang/v2/bootstrap compiler-core-release compiler-core-system-link compiler-core-system-link-exec tooling-selfhost lsmr-contracts selfhost full-selfhost` 前台通过，`manifest_fnv1a64=5ff3e0484c3bfc43` |

| 步骤 | 状态 | 备注 |
|---|---|---|
| `wnaf/python` 外部真值对拍 | 已完成 | 重新前台编译 `_tmp_p256_comb_external_cmp_probe` 后，`wnaf` 与 Python `cryptography` 的 P-256 公钥字节一致；之前“comb 也一致”的结论来自旧二进制，已作废 |
| `comb6` 最小双比特定位 | 已完成 | `_tmp_p256_comb_two_bit_probe_bin` 现在已经把边界钉死：`single_col0` 和 `cross_row_same_col` 都塌成 `04 + 0...0`，`single_col1` 和 `top_row_cols01` 正常，`same_row_cols01` 退化成 `digit=2` 的结果 |
| `comb6` 表常量/索引排除 | 已完成 | `_tmp_p256_comb_table_entry_probe_bin` 和 `_tmp_p256_comb_digit_lookup_probe_bin` 证明 `p256GComb6[1/2/3]` 常量本身正确，直接和可变索引读取也正确 |
| `comb6` 小函数形状验证 | 已完成 | `_tmp_p256_comb_first_digit_probe_bin` 证明同样的 `find first nonzero row -> setFromAffine -> toAffine` 在小函数里是对的，说明根已经收缩到完整函数形状/代码生成，不是数学和表 |
| 生产路径保护 | 已完成 | 尝试把 `publicKeyFromPrivateKey` 切到新的 `comb6` 入口后，双比特 probe 仍失败，所以这条接线已经撤回，生产树保持稳定 |

| 步骤 | 状态 | 备注 |
|---|---|---|
| `EcPointJacobianFixed` 值流老根复验 | 已完成 | `_tmp_p256_jacobian_valueflow_probe_bin` 现在 `local_copy/local_return/imported_return/imported_copy_then_var_assign` 全部 `=1`，这条老根已经不活了 |
| `comb6` 固定基点 `x-only` 对拍 | 已完成 | `_tmp_p256_sign_r_comb_cmp_probe_bin` 继续前台 `ok`，说明这条局部性质存在，但还不能当整链 active 依据 |
| `comb6` 完整 affine 对拍 | 已完成 | `_tmp_p256_comb_cmp_probe_bin` 当前稳定报 `neq`，而 `_tmp_p256_repr_sign_r_cmp_probe_bin` 也继续不等价，说明完整 affine `comb6` 仍未闭合 |
| unsafe `comb6` active 接线回滚 | 已完成 | 上一轮临时把签名 `R` 路切到 `comb6` 的改动已经撤回，生产路径恢复安全 `wnaf` |
| 回滚后安全 `pubkey/sign` 前台基线 | 已完成 | `_tmp_p256_pubkey_scalar_probe_bin` 最新前台样本 `4.47s`；`_tmp_ecdsa_sign_probe_bin` 最新前台样本 `6.98s` |
| 回滚后拆段基线 | 已完成 | `_tmp_ecdsa_mul_probe_bin` 最新前台真跑 `3.67s`；`_tmp_ecdsa_kinv_probe_bin` 最新前台真跑 `0.95s`；当前约占整签名 `52.58%` 和 `13.61%` |
| 本轮主 gate 复验 | 已完成 | `make -j1 -C /Users/lbcheng/cheng-lang/v2/bootstrap compiler-core-release compiler-core-system-link compiler-core-system-link-exec tooling-selfhost lsmr-contracts selfhost full-selfhost` 前台通过，`manifest_fnv1a64=1660bb3be59d7e85` |

| 步骤 | 状态 | 备注 |
|---|---|---|
| `msquicNativeDial` outline 真根 | 已完成 | `native_runtime.cheng` 已把会掉出 exec subset 的 `if-expr` body 改回显式 `var + if`，`msquicNativeDial` 已从 `compiler_fn_outline/local_payload/ast_ready=0` 收回 `compiler_fn_exec/exec_plan_payload/ast_ready=1` |
| `chain frame: short read` 真根 | 已完成 | `connection.cheng` 已把 pipe 读收成带缓存流读取，`native_runtime.cheng` 已把 `msquicNativePipeRead` 收成按 side 的阻塞收包，不再把“暂时没到齐”误判成短读 |
| 真实多进程 `chain_node` 联网 | 已完成 | 前台真跑 `serve-once/sync-once` 已稳定得到 `synced=1` 和 `balance=11`；当前生产证明已经从单进程 `msquic_chain_smoke` 切到 `chain_node_process_smoke` |

| 步骤 | 状态 | 备注 |
|---|---|---|
| app stream `offset` 重组 | 已完成 | `native_runtime.cheng` 已把应用层 `FrameData` 收包从“必须连续 append”改成复用 `MsQuicCryptoStream` 的 `offset` 重组，`stream reassembly gap` 这条运行时假根已消失 |
| `msquic_chain_smoke` 真联网 smoke | 已完成 | 前台运行 `v2/artifacts/bootstrap/msquic_chain_smoke_test` 已稳定输出 `hello_ok/sync_ok/transfer_ok/replay_ok/msquic_chain_smoke=ok`，说明 LSMR 链路已真正穿过 QUIC/TLS 双节点运行面 |
| `lsmr-contracts` 运行面扩充 | 进行中 | 这轮把 `chain_state_tree_sync_smoke` 和 `msquic_chain_smoke` 的 stdout 固定成新的 contract，并接进 `v2/bootstrap/Makefile` 的 `lsmr-contracts` |
| `LSH 双重寻址` | 已完成 | `v2/cheng-quic/src/chain/types.cheng` 和 `v2/cheng-quic/src/chain/lsmr.cheng` 已新增 `LsmrLocalityCid/LsmrStateCell`，正文现在可以同时拿到 `contentCid + localityHash + bagua/prefix/resolution/layer` |
| `edge/regional/global` 三层状态快照 | 已完成 | `LsmrStateLayerForest` 已落地，能把同一批 state cell 投影成三棵独立前缀树，并导出 `localOpCount/regionalBatchCount/globalAnchorCount` |
| `ChainIndex -> 三层快照` | 已完成 | `chainLsmrIndexStateCells/chainLsmrBuildIndexStateLayerForest` 已落地，账户状态不再只能派生一棵全量状态树 |
| `lsmr_locality_storage_smoke` | 已完成 | 前台真编过并真运行通过，输出 `forest_cid=da324ef9bc25f637347795ba8cb7edbb0e4441406b5c0792c8006ce12fbbc600`、`index_forest_cid=89f0a4df7842c1b165886cbeb0d59fdabc2190c3734b715846d982939f18fc92` |
| `EcPointJacobianFixed` 最小值流复现 | 已完成 | 新增 `_tmp_p256_jacobian_valueflow_probe_bin`。前台真跑结果已经钉死：`inline/local_nocopy/local_return_nocopy/imported/imported_return` 都通过；只有 `local_copy/local_return` 稳定变成 `inf=1 z=[0..]`。这说明 `comb` 数学和普通 return 都不是根，真根是“非 owner 模块里，把 imported 函数返回的 `EcPointJacobianFixed` 直接赋给本地变量”这条值流。 |
| `comb6` 手工内联复核 | 已完成 | `_tmp_p256_generator_comb6_step_probe_bin` 前台继续 `ok`，`k1/k3` 都保持 `inf=0` 和稳定 `z`；所以这轮没有把半坏 `comb` 接回 active 热链。 |
| `double/add in-place` correctness 对拍 | 已完成 | 新增 `_tmp_p256_mul_double_cmp_probe_bin` 和 `_tmp_p256_mul_add_cmp_probe_bin`，前台真跑都已 `ok`；确认这轮之前的 `double/add in-place` 与纯函数实现不一致，当前已收回到同构实现 |
| `double` 子路径基线 | 已完成 | `_tmp_p256_mul_double_probe_bin` 前台真跑 `1.14s / 16 doubles`，平均每次 `double` 约 `0.0713s` |
| `addAffine` 子路径基线 | 已完成 | `_tmp_p256_mul_add_probe_bin` 前台真跑 `16.54s / 64 adds`，平均每次 `addAffine` 约 `0.2584s`，约为 `double` 的 `3.63x` |
| correctness 回收后的真基线 | 已完成 | `_tmp_p256_pubkey_scalar_probe_bin` 前台真跑 `7.84s`，`_tmp_ecdsa_sign_probe_bin` 前台真跑 `10.29s`，`_tmp_ecdsa_mul_probe_bin` 前台真跑 `7.65s`，`_tmp_ecdsa_kinv_probe_bin` 前台真跑 `2.65s`；旧的 `7.18s/9.41s` 口径已作废 |
| 本轮主 gate 复验 | 已完成 | `make -j1 -C /Users/lbcheng/cheng-lang/v2/bootstrap compiler-core-release compiler-core-system-link compiler-core-system-link-exec tooling-selfhost lsmr-contracts selfhost full-selfhost` 前台通过，`manifest_fnv1a64=c3b367ba02d2c029` 未漂 |
| 本轮主 gate 复验 | 已完成 | `make -j1 -C /Users/lbcheng/cheng-lang/v2/bootstrap compiler-core-release compiler-core-system-link compiler-core-system-link-exec tooling-selfhost lsmr-contracts selfhost full-selfhost` 前台通过，`manifest_fnv1a64=c3b367ba02d2c029` 未漂 |
| `double/add` Montgomery scratch 调用层复用 | 已完成 | `p256FixedMontgomery{Mul,Square}CoreTrustedScratchInto` 已接进 `pointJacobianDoubleFixedInPlace/pointJacobianAddAffineFixedInPlace`，内层不再每次新建一份 scratch record |
| 固定宽度标量签名链试刀 | 已撤回 | 把 `r/rd/sum/kinv/s` 强行留在 fixed record 链里会回退，而且 `Result[pfix.P256Fixed]` 会撞普通 program runtime 边界；已整块撤回，不留 active 路径 |
| 本轮常规 `pubkey` 热核复测 | 已完成 | `_tmp_p256_pubkey_scalar_probe_bin` 前台真跑 `7.21s / 7.17s / 7.18s`，中位数压到 `7.18s` |
| 本轮 `ecdsaSignBytes` 热核复测 | 已完成 | `_tmp_ecdsa_sign_probe_bin` 前台真跑 `9.82s / 9.41s / 9.37s`，中位数压到 `9.41s` |
| 本轮外部拆段计时复测 | 已完成 | `_tmp_ecdsa_mul_probe_bin` 前台真跑 `7.77s / 7.29s / 7.32s`，`_tmp_ecdsa_kinv_probe_bin` 前台真跑 `2.07s / 2.04s / 2.04s`，当前中位数分别是 `7.32s` 和 `2.04s` |
| 本轮主 gate 复验 | 已完成 | `make -j1 -C /Users/lbcheng/cheng-lang/v2/bootstrap compiler-core-release compiler-core-system-link compiler-core-system-link-exec tooling-selfhost lsmr-contracts selfhost full-selfhost` 前台通过，`manifest_fnv1a64=c3b367ba02d2c029` 未漂 |
| `Into` 热路径真原地输出 | 已完成 | `p256FixedMontgomery{Mul,Square}CoreTrustedInto`、`p256FixedMod{Add,Sub}TrustedInto` 不再先返回整块 `P256Fixed` 再赋值；`double/add` 主链的 record 回拷贝又少一层 |
| 本轮常规 `pubkey` 热核复测 | 已完成 | `_tmp_p256_pubkey_scalar_probe_bin` 前台真跑 `7.39s / 7.37s / 7.38s`，中位数压到 `7.38s` |
| 本轮 `ecdsaSignBytes` 热核复测 | 已完成 | `_tmp_ecdsa_sign_probe_bin` 前台真跑 `9.60s / 9.52s / 9.54s`，中位数压到 `9.54s` |
| 本轮外部拆段计时复测 | 已完成 | `_tmp_ecdsa_mul_probe_bin` 前台真跑 `8.74s / 7.41s / 7.46s`，`_tmp_ecdsa_kinv_probe_bin` 前台真跑 `3.44s / 2.08s / 2.10s`，当前中位数分别是 `7.46s` 和 `2.10s` |
| `Jacobian -> x-only -> 单次减 n` 签名主链 | 已完成 | `ecdsaSign()` 不再走“完整仿射点 + 通用 `nMod(BigInt)`”；现在直接从 Jacobian 只拉 `x`，并在固定宽度上做一次比较/减 `n` |
| 外部拆段计时 | 已完成 | 不再信 `epochTimeNs()`；改成前台独立探针 `_tmp_ecdsa_mul_probe_bin/_tmp_ecdsa_kinv_probe_bin`，量得中位数 `mul_xonly=16.2622s`、`kinv=2.3078s` |
| 本轮常规 `pubkey` 热核复测 | 已完成 | `_tmp_p256_pubkey_scalar_probe_bin` 前台真跑 `15.6564s / 15.8525s / 15.9512s`，中位数压到 `15.8525s` |
| 本轮 `ecdsaSignBytes` 热核复测 | 已完成 | `_tmp_ecdsa_sign_probe_bin` 前台真跑 `18.6038s / 18.6960s / 18.2833s`，中位数压到 `18.6038s` |
| 本轮核心探针 | 已完成 | `p256_fixed_core_probe_bin` 前台真跑 `probe=ok p_mul=1 n_mul=1 p_square=1 n_square=1` |
| trusted 固定宽度 `modmul/modexp` | 已完成 | `p256_fixed` 已新增 `p256FixedModMulWithContextTrusted/p256FixedModExpWithContextTrusted`，`ecnist` 的 `p256ModMul/p256ModInv/nModMul/nModInv/p256FieldFixedInv` 已切到 trusted 路 |
| 本轮常规 `pubkey` 热核复测 | 已完成 | `_tmp_p256_pubkey_scalar_probe_bin` 前台真跑 `21.7294s / 15.5675s / 15.9608s`，中位数已压到 `15.9608s` |
| 本轮 `ecdsaSignBytes` 热核复测 | 已完成 | `_tmp_ecdsa_sign_probe_bin` 前台真跑 `19.0723s / 18.0080s / 24.0553s`，中位数已压到 `19.0723s` |
| 本轮主 gate 复验 | 已完成 | `make -j1 -C /Users/lbcheng/cheng-lang/v2/bootstrap compiler-core-release compiler-core-system-link compiler-core-system-link-exec tooling-selfhost lsmr-contracts selfhost full-selfhost` 前台通过，`manifest_fnv1a64=c3b367ba02d2c029` 未漂 |
| trusted 无 `Result` 固定域内核 | 已完成 | `pointJacobianDoubleFixedInPlace/pointJacobianAddAffineFixedInPlace` 已直接切到 `p256FixedMontgomery{Mul,Square}CoreTrusted` 和 `p256FixedMod{Add,Sub}Trusted`，热点里不再层层建 `Result` |
| 本轮常规 `pubkey` 复测 | 已完成 | `_tmp_p256_pubkey_scalar_probe_bin` 前台真跑 `16.8481s / 19.2838s`，较上轮 active 基线 `21.0261s` 继续下降 |
| 本轮 `ecdsaSignBytes` 复测 | 已完成 | `_tmp_ecdsa_sign_probe_bin` 前台真跑 `19.8186s / 21.5582s`，较上轮 active 基线 `25.1703s` 继续下降 |
| 本轮主 gate | 已完成 | `make -j1 -C /Users/lbcheng/cheng-lang/v2/bootstrap compiler-core-release compiler-core-system-link compiler-core-system-link-exec tooling-selfhost lsmr-contracts selfhost full-selfhost` 前台通过，`manifest_fnv1a64=c3b367ba02d2c029` 未漂 |
| `Ref helper` 试刀 | 已撤回 | 给固定域热算子再包一层 `...Ref` 调用后，常规 `pubkey` 退到 `23.4243s`、整签名退到 `27.6701s`，已整块撤回 |
| 原地写回 `result` | 已完成 | 只保留 `pointJacobianDoubleFixedInPlace/pointJacobianAddAffineFixedInPlace` 后，常规 `pubkey` 压到 `21.0261s`，整签名压到 `25.1703s` |
| 当前 active 性能基线 | 已完成 | `_tmp_p256_pubkey_scalar_probe_bin` 前台真跑 `21.0261s`；`_tmp_ecdsa_sign_probe_bin` 前台真跑 `25.1703s` |
| 本轮主 gate | 已完成 | `make -j1 -C /Users/lbcheng/cheng-lang/v2/bootstrap compiler-core-release compiler-core-system-link compiler-core-system-link-exec tooling-selfhost lsmr-contracts selfhost full-selfhost` 前台通过，`manifest_fnv1a64=c3b367ba02d2c029` 未漂 |
| 固定逆元加法链试刀 | 已撤回 | 跑通但更慢；常规 `pubkey` 退到 `23.1760s`，整签名退到 `27.4899s`，已整块撤回，不留 active 路径 |
| `x-only` 签名收尾试刀 | 已撤回 | 直接返回 `Result[pfix.P256Fixed]` 会碰 `program runtime` 的 imported fixed record 返回面；包回本地记录后也没有净收益，已撤回 |
| 读 `lessons.md` | 已完成 | 继续只用前台 `cheng_v2c` 和前台 `make` |
| `EcPointFixed` 常驻 Montgomery 域 | 已完成 | `pointFixedFromBig/pointBigFromFixed` 现在只在边界做一次 `toMont/fromMont`，`pointJacobian*Fixed` 内部不再每次字段乘法都重复进出域 |
| 固定点热点去 `Result` | 已完成 | `pointJacobianToAffineFixed/DoubleFixed/AddFixed/AddAffineFixed` 已改成内部 crash 版本字段运算，热点里不再层层建 `Result` 盒子 |
| 生成元 `4-bit fixed window` 试刀 | 已撤回 | `pubkey` 从 `12.76s` 变更差，已撤回 |
| 生成元 `comb` 试刀 | 已撤回 | 初始化直接拖爆到 `stage=priv`，已撤回 |
| 当前 `pubkey` probe | 已完成 | `_tmp_p256_pubkey_probe_bin` 前台真跑 `7.55s`，优于这轮开始前的 `8.61s` |
| 当前 `sign` stage probe | 已完成 | `_tmp_ecdsa_sign_stage_probe_bin` 前台 `20s` 限时仍卡 `stage=mul`，说明热根继续收敛在固定点点乘，不在 `deterministicK/nModInv` |
| 主 gate | 已完成 | `make -j1 -C /Users/lbcheng/cheng-lang/v2/bootstrap compiler-core-release compiler-core-system-link compiler-core-system-link-exec tooling-selfhost lsmr-contracts selfhost full-selfhost` 前台通过，`manifest_fnv1a64=c3b367ba02d2c029` 未漂 |
| `bigint` 热核主根重定位 | 已完成 | 真正最坏点不是旧 `bigMul`，而是 `bigModMul/bigModExp` 还在走 `bigMul -> bigMod(bit-by-bit)` |
| `BigMontgomeryContext` 落地 | 已完成 | `src/std/crypto/bigint.cheng` 已新增 `Montgomery n0Inv/r2/oneMont` 和 limb 级 `bigMontgomeryMulCore` |
| `ECDSA` 上下文复用 | 已完成 | `src/std/crypto/ecnist.cheng` 现在会在 `p256EnsureInit()` 里一次性构建 `P/N` 的 Montgomery 上下文，`p256ModMul/p256ModInv/nModMul/nModInv` 不再走旧路径 |
| `program runtime` 固定数组根因定位 | 已完成 | 当前 `T[N]` 本地 scratch 的真坑不是数组本身，而是 runtime 只会把长度解析成字面量/顶层 `const int32`；复杂长度表达式会误掉进 `type item` 路径 |
| 前台探针验收 | 已完成 | `_tmp_ecdsa_sign_probe` 已真编译并进入新路径；不再秒崩，但中断时仍有重内存和长耗时，说明真瓶颈已经收缩到普通程序执行面的“大对象值传递/热路径拷贝” |
| 主根重定位 | 已完成 | 现在的根不是单个 overload case，而是 `compiler_core` 名字解析靠全局扫，不尊重模块可见域 |
| `cheng-quic` 裸指针面扫描 | 已完成 | 当前活口集中在 `v2/cheng-quic/tests/msquic_transport_smoke.cheng` |
| 任务文件收口 | 已完成 | 已改成语言主线，不再停在旧的 `&` 小口子 |
| 模块可见域解析改造 | 进行中 | 先改 source/stage0 的 `type/global/routine` 可见域查找和 call 解析 |
| 显式模块函数迁移 | 进行中 | 已把 `v2_source_parser`、`release_artifact`、`manifest_resolver`、`obj_file`、`cheng_tooling`、`compiler_core_lowering` 里的裸 `panic/add/len` 收成显式模块调用，`semantic_facts` 正在继续清理 |
| `compiler_core` 链接底座补齐 | 已完成 | `system-link-exec` 和 runtime provider 现在走最小 selflink 闭包：`system_helpers_selflink_cmdline_bridge.c + system_helpers_selflink_str_eq_bridge.c`，不再拖 `min_runtime` |
| `compiler_core-system-link-exec` | 已完成 | 前台 `cheng_v2c system-link-exec` 已真实跑通；`status/release-compile/lsmr-address` smoke、`trace-phase-space`、`emit-proof-obligations` 都已前台跑通 |
| `stage0` 语义镜像收口 | 已完成 | `cheng_v2c_tooling.c` 剩余的 type alias/type lookup/indexed element/routine return/type match helper 已切到 `header-arena + visibility` 口径，不再留旧的全局扫描尾巴 |
| fixed-point 刷新 | 已完成 | `release/system-link-plan/system-link-exec/tooling-selfhost/selfhost/full-selfhost` 已前台跑绿并全部对齐 |
| 裸指针语法清理 | 进行中 | `compiler_core_pipeline_surface.cheng` 顶层 `ptr`/裸 extern/地址样本已整块删掉，`taiji-seed-set` 的 stage0/stage3 对拍已重新闭合 |
| ZRPC surface gate | 已完成 | 新增 `verify-compiler-core-zrpc-surface`，24 个 probe 固化成正式 contract，直接挂进 `compiler-core-release` |
| cycle/import surface gate | 已完成 | 新增 `verify-compiler-core-cycle-surface`，真实 fixture 验证 `循环 import + 零前置声明 + import_field_routine` 已挂进 `compiler-core-release` |
| `cheng-quic` 裸指针现状复核 | 已完成 | `v2/cheng-quic/**/*.cheng` 当前真实裸指针表面为 0，不再误追 `msquic_transport_smoke` |
| enum 成员头表化 | 已完成 | `compiler_core` 的 enum member 解析已改成 `EnumMemberHeaderArena`，不再每次扫整份 `program.topLevelKinds/typeBlocks` |
| `field` 的 type body 索引 | 已完成 | 剩余那处 `for i in 0..<typeItemId` 数 `type` 的顺序假设已切回 `typeBodyIdx` |
| source/stage0 enum 语义镜像 | 已完成 | `compiler_core_facts_v2.cheng` 和 `cheng_v2c_tooling.c` 现在同构携带 `enumArena` 参数线 |
| 主 gate fixed-point 刷新 | 已完成 | `release/system-link-plan/system-link-exec/full-selfhost` 已刷新到新 facts 版本，整条前台 gate 重新跑绿 |
| `import/type/value/routine` 可见桶 | 已完成 | `compiler_core_facts_v2.cheng` 已加入 `ImportHeaderIndex / TypeHeaderIndex / ValueHeaderIndex / RoutineHeaderIndex`，名字解析不再扫整份 header arena |
| cycle/noise 负例 | 已完成 | `compiler_core_cycle_surface` fixture 新增未导入同名噪音模块，`manifest.has.noise=0` 已进正式 contract |
| 本轮 fixed-point 刷新 | 已完成 | `compiler_core_cycle_surface.expected`、`compiler_core_release_artifact.expected`、`compiler_core_system_link_plan.expected`、`compiler_core_system_link_exec.expected`、`full_selfhost.expected` 已对齐，完整主 gate 前台跑绿 |
| `module path -> id` 索引 | 已完成 | `VisibilityIndex` 已补排序查表，source 侧不再线性扫 `modulePaths[]` |
| `type itemId -> bodyIdx` 索引 | 已完成 | `TypeHeaderArena` 已补 `itemIdToTypeBodyIdx[]` 稠密表，透明 alias 和 call/type 推导不再回扫 `typeArena.itemIds[]` |
| `enum member` 索引 | 已完成 | `EnumMemberHeaderArena` 已补排序查表，source 侧不再线性扫 `ownerModulePaths/memberNames` |
| `stage0` residual lookup mirror | 已完成 | `cheng_v2c_tooling.c` 现在同构带 `lookupPaths/lookupIds`、`itemIdToTypeBodyIdx`、`enum lookup`，三处旧线性扫描已删掉 |
| 本轮 fixed-point 刷新 | 已完成 | `compiler_core_system_link_exec.expected`、`full_selfhost.expected` 已对齐，完整主 gate 前台跑绿 |
| `routine itemId -> declIdx` 索引 | 已完成 | `source + stage0` 都已补 `itemIdToRoutineDeclIdxs` 稠密表，routine 返回类型和后续 call/type 推导不再扫 `program.topLevelKinds/routineArena.itemIds` |
| 新一轮 fixed-point 刷新 | 已完成 | `compiler_core_release_artifact.expected`、`compiler_core_system_link_plan.expected`、`compiler_core_system_link_exec.expected`、`full_selfhost.expected` 已对齐，完整主 gate 前台跑绿 |
| `compiler_core` 合法源码根集合 | 已完成 | `manifest_resolver_v2.cheng` 和 `cheng_v2c_tooling.c` 现在统一接受 `package/src`、`package/tests/contracts`、`package/examples`、`workspace/src` |
| closure 图驱动收集 | 已完成 | `collectCompilerCoreClosurePaths` 和 stage0 mirror 继续保持 `ModuleGraph + SCC` 出序，不再把 `tests/contracts` 样本当越界路径 |
| `taiji-seed-set` 尾部闭合 | 已完成 | `cheng_v2c taiji-seed-set`、native `taiji-seed-set`、stage0/stage3 seed 对拍都已前台跑通 |
| 本轮 fixed-point 刷新 | 已完成 | `compiler_core_release_artifact.expected`、`compiler_core_system_link_plan.expected`、`compiler_core_system_link_exec.expected`、`tooling_release_artifact.expected`、`network_selfhost.expected`、`tooling_selfhost.expected`、`full_selfhost.expected` 已对齐，完整主 gate 前台跑绿 |
| `compiler_core` no-pointer closure gate | 已完成 | 新增 `verify-compiler-core-no-pointer-closure`，正式检查 `v2/src` 生产 closure，固定输出 `closure_manifest_cid + violation_count + first_violation_*` |
| no-pointer 生产范围 | 已完成 | `compilerCoreSourcePathNeedsNoPointerPolicy` 已补到 `v2/examples`，但正式 `compiler_core` closure gate 只扫真正的 `compiler_core` 生产根 `v2/src` |
| 本轮 fixed-point 刷新 | 已完成 | `compiler_core_release_artifact.expected`、`compiler_core_system_link_plan.expected`、`compiler_core_system_link_exec.expected`、`full_selfhost.expected` 已按新 no-pointer closure gate 重新对齐，完整主 gate 前台跑绿 |
| closure graph `module_path -> module_id` 索引 | 已完成 | `manifest_resolver_v2` 和 `cheng_v2c_tooling.c` 的 closure graph 已补 `lookupModulePaths/lookupModuleIds` 排序索引，闭包边构造不再线性扫 `modulePaths[]` |
| 本轮 fixed-point 刷新 | 已完成 | `compiler_core_release_artifact.expected`、`compiler_core_system_link_plan.expected`、`compiler_core_system_link_exec.expected`、`full_selfhost.expected` 已按新 closure graph 索引重新对齐，完整主 gate 前台跑绿 |
| `topLevelOwnerModules` 语义钉死 | 已完成 | `compiler_core` 的 top-level owner module 不再允许回退到 `sourcePath -> modulePath` 猜测；缺 owner 直接 crash |
| module graph 归属来源收口 | 已完成 | `buildCompilerCoreModuleGraph` 现在只吃 `topLevelOwnerModules + import module paths`，不再把 `topLevelSourcePaths` 当模块归属来源 |
| 本轮 fixed-point 刷新 | 已完成 | `compiler_core_release_artifact.expected`、`compiler_core_system_link_plan.expected`、`compiler_core_system_link_exec.expected`、`full_selfhost.expected` 已按新 owner-module 语义重新对齐，完整主 gate 前台跑绿 |
| routine 语义链 `ownerModulePath` 前移 | 已完成 | source 侧 `expr/type/call/assign` 主链现在从 `buildCompilerCoreSemanticFacts` 一次拿到 `ownerModulePath`，不再在主解析链里局部现推 `sourcePath -> modulePath` |
| 本轮 fixed-point 刷新 | 已完成 | `compiler_core_release_artifact.expected`、`compiler_core_system_link_plan.expected`、`compiler_core_system_link_exec.expected`、`full_selfhost.expected` 已按新 routine 语义链重新对齐，完整主 gate 前台跑绿 |
| `owner_source_path` 尾巴清理 | 已完成 | source 侧死掉的 `compilerCoreFindTypeItemIdInSourcePath/FindVisibleTypeItemId` 已删除；stage0 侧死掉的 `visible type cache + source_path wrapper` 已整块删掉 |
| 本轮 fixed-point 刷新 | 已完成 | `compiler_core_release_artifact.expected`、`compiler_core_system_link_plan.expected`、`compiler_core_system_link_exec.expected`、`full_selfhost.expected` 已按新 stage0/source 收缩重新对齐，完整主 gate 前台跑绿 |
| closure/import 图重建 | 已完成 | `manifest_resolver_v2` 和 `cheng_v2c_tooling.c` 现在用 `DeclaredModuleIndex + ModuleGraph + SCC` 按模块路径惰性装配闭包，不再预扫整个 `workspace/src` |
| closure alias 崩溃点修复 | 已完成 | stage0 单 owner alias 分支的 canonical module path 已修成先查表再释放，已消除释放后读取脏指针 |
| closure 顺序去路径化 | 已完成 | owner modules/imports 保留解析顺序；ready SCC 改成首次发现顺序；SCC 内 source 发射按图插入顺序，不再按 `modulePath` 排序 |
| 本轮 fixed-point 刷新 | 已完成 | `compiler_core_release_artifact.expected`、`compiler_core_system_link_plan.expected`、`compiler_core_system_link_exec.expected`、`full_selfhost.expected` 已按新 closure/import 图重新对齐，完整主 gate 前台跑绿 |
| import 目标 owner 模块映射 | 已完成 | `semantic_facts -> high_uir -> low_uir -> machine` 已新增 `importTargetModulePaths`，统一保存 `declared import path -> owner module path` 结果 |
| stage0 import owner mirror | 已完成 | `cheng_v2c_tooling.c` 已拆出独立 `DeclaredOwnerModuleIndex`，不再把 manifest 的 `module->source` 索引和 semantic-facts 的 `declared->owner` 索引混成一套 |
| `compiler_core-system-link-exec` smoke 修通 | 已完成 | `status` 路径里的 `cmdline.cliParam1Eq(...)` 已恢复成可识别的 qualified import call，完整主 gate 前台跑绿 |
| 本轮 fixed-point 刷新 | 已完成 | `compiler_core_release_artifact.expected`、`compiler_core_system_link_plan.expected`、`compiler_core_system_link_exec.expected`、`full_selfhost.expected` 已按新 import-owner 语义重新对齐，完整主 gate 前台跑绿 |
| stage0 same-module owner 匹配 | 已完成 | `compiler_core_plan_item_matches_owner_scope` 已改成直接比较 `low_plan.owner_modules`，不再把 `source_path -> modulePath` 当 same-module 依据 |
| source/stage0 死 wrapper 清理 | 已完成 | 已删除 source 侧 4 个和 stage0 侧 4 个只做 `owner_source_path -> owner_module_path` 转发的无调用薄 wrapper |
| 本轮 fixed-point 刷新 | 已完成 | `compiler_core_release_artifact.expected`、`compiler_core_system_link_plan.expected`、`compiler_core_system_link_exec.expected`、`full_selfhost.expected` 已按新 owner-scope 与 wrapper 收缩重新对齐，完整主 gate 前台跑绿 |
| `Call/AssignTargetFacts` 外壳清理 | 已完成 | source 侧 `compilerCoreCallTargetFacts/AssignTargetFacts` 已删除，主链只保留 `...InModule` 真接口，不再保留 `ownerSourcePath -> modulePath` 转发层 |
| stage0 `visible_top_level` 外壳清理 | 已完成 | stage0 侧 `compiler_core_program_find_visible_top_level_{item_id,kind}` 已删除，名字解析只走 `_in_module` 真接口 |
| 本轮 fixed-point 刷新 | 已完成 | `compiler_core_release_artifact.expected`、`compiler_core_system_link_plan.expected`、`compiler_core_system_link_exec.expected`、`full_selfhost.expected` 已按新 wrapper 收缩重新对齐，完整主 gate 前台跑绿 |
| `DeclaredModuleIndex` import 索引化 | 已完成 | source 侧 `CompilerCoreDeclaredModuleIndex` 已补 `importTargetModulePaths/start/count`，owner/import 现在一次 parse 一次入索引 |
| closure 图去重复 parse | 已完成 | `manifest_resolver_v2.cheng` 的 closure 构造不再每轮拿 `ownerSourcePath` 重 parse imports，改成 `declaredModuleImportTargetPaths(...)` 查表 |
| stage0 declared/import mirror | 已完成 | `cheng_v2c_tooling.c` 已同构补齐 declared-module import target 索引，closure graph 后半段只查索引 |
| parser 默认 owner 结论 | 已完成 | 已确认仓内真实源码仍依赖 parser 末尾默认 owner 回填维持 `topLevelOwnerModules` 非空；这刀不误砍 |
| 本轮 fixed-point 刷新 | 已完成 | `compiler_core_release_artifact.expected`、`compiler_core_system_link_plan.expected`、`compiler_core_system_link_exec.expected`、`full_selfhost.expected` 已按新 declared/import 索引重新对齐，完整主 gate 前台跑绿 |
| `declared module path` 语义钉死 | 已完成 | source `semantic_facts` 和 stage0 owner-index 都把旧 `sourcePath -> modulePath` helper 改名成显式 `declared module path for source path`，不再伪装成 owner 语义 |
| dead `enum/sourcePath` 外壳清理 | 已完成 | source `semantic_facts`、source `low_uir`、stage0 facts 里的 `ResolveEnumMemberOrdinalInSourcePath` 都已删除 |
| stage0 未限定 enum owner 解析 | 已完成 | `cheng_v2c_tooling.c` 里活着的 enum literal 解析已从 `low_plan.source_paths[current]` 改成 `low_plan.owner_modules[current]` |
| 全局 no-pointer gate 试接 | 已完成 | 已验证直接把 `v2/examples/cheng-quic` 并进当前 gate 不闭合；根因是统一 parser 还没覆盖非 `compiler_core` surface，不是 no-pointer 策略本身 |
| 本轮 fixed-point 刷新 | 已完成 | `compiler_core_release_artifact.expected`、`compiler_core_system_link_plan.expected`、`compiler_core_system_link_exec.expected`、`full_selfhost.expected` 已按新 owner/enum 收缩重新对齐，完整主 gate 前台跑绿 |
| source multiline `if-expr` parser | 已完成 | `v2_source_parser` 现在能稳定解析 `return/let/var/assign` 的 block `if-expr`，joined header 和缩进分支都闭合了 |
| stage0 multiline `if-expr` mirror | 已完成 | `cheng_v2c_tooling.c` 已补 `collect_compiler_if_expr_tail_text(...)` 和 inline `elif`，source/stage0 不再在 `if-expr` 上分叉 |
| `break/continue` parser 支持 | 已完成 | source/stage0 都已正式识别，不再把它们误落成普通表达式行 |
| line-surface no-pointer helper | 已完成 | 已新增 `no_pointer_surface_v2.cheng`，统一承载非 `compiler_core` surface 的 token 级 no-pointer 检查 |
| no-pointer closure gate 扩面 | 已完成 | `verify-compiler-core-no-pointer-closure` 已从旧 `v1/root=v2/src` 升到 `v2/root_count=4`，正式覆盖 `v2/src`、`v2/examples`、`v2/cheng-quic/src`、`v2/cheng-quic/tests` |
| package root 模块解析 | 已完成 | `compilerPackageSourceRoots` 和 stage0 mirror 已补 package root 自身，`quic/tests/*`、`quic/project/*` 不再误报 unresolved import |
| `cheng-quic` 旧 `$()` 语法迁移 | 已完成 | `tls_x509_test.cheng` 已改成显式 `intToStr(...)`，不再要求 parser 回头兼容旧插值表面 |
| no-pointer `->` 误报修正 | 已完成 | line-surface 检查已去掉把函数返回箭头误判成 pointer 的错误规则，`Result[...]` 返回签名不再误炸 |
| 新 no-pointer contract 收口 | 已完成 | `compiler_core_no_pointer_closure.expected` 已切到真实 `v2` 口径，完整主 gate 前台跑绿 |
| no-pointer 产线硬 gate | 已完成 | `release_artifact_v2.cheng` 和 `cheng_v2c_tooling.c` 现在都会在真实 `release-compile/system-link-plan/system-link-exec` 入口先 parse，再直接执行 `requireV2NoPointerSurfaceFree(...)`，no-pointer 不再只是 verifier 才知道 |
| 全局 v2 no-pointer probe contract | 已完成 | 新增 `verify-v2-no-pointer-surface`，正式覆盖 `compiler_core/unimaker/topology/network_distribution` 四类 surface 的 8 个正反样本，`pass_count=4`、`fail_count=4`、`mismatch_count=0` |
| no-pointer 诊断文案同构 | 已完成 | stage0 `v2_surface_first_no_pointer_violation(...)` 已改成和 source helper 同一口径：`v2 semantic facts surface: ZRPC no-pointer policy forbids ...` |
| 本轮 fixed-point 刷新 | 已完成 | `v2_no_pointer_surface.expected`、`compiler_core_release_artifact.expected`、`compiler_core_system_link_plan.expected`、`compiler_core_system_link_exec.expected`、`full_selfhost.expected` 已重新对齐，完整主 gate 前台跑绿 |
| no-pointer 真实编译负例 contract | 已完成 | `compiler-core-release` 现在会前台跑 3 个坏样本的 `cheng_v2c release-compile`，并把稳定报错文案和 `rc=1` 固定到 `v2_no_pointer_compile_failures.expected` |
| 本轮 gate 验收 | 已完成 | 新负例 contract 已挂进 `compiler-core-release`，完整 `v2/bootstrap` 主 gate 前台跑绿，且没有新增孤儿进程 |
| no-pointer 真实 package 入口 contract | 已完成 | `compiler-core-release` 现在会前台跑 `unimaker_robot_node`、`network_distribution_module`、`topology_code_sync` 三个真实 package 入口的 `cheng_v2c release-compile`，并把关键成功字段固定到 `v2_no_pointer_package_entries.expected` |
| package 入口选择 | 已完成 | 这轮只纳入 `v2/examples` 的 3 个真入口，不把 `v2/cheng-quic/src/project/*` 混进来，避免把 no-pointer gate 和 `program` runtime 缺口搅在一起 |
| no-pointer 真实 package 闭包 contract | 已完成 | `compiler-core-release` 现在会前台跑 `verify-v2-no-pointer-package-closures`，固定验证 `v2/examples` 下 3 个真实 package 入口的 manifest 闭包都零裸指针，并固化到 `v2_no_pointer_package_closures.expected` |
| package closure gate 产线化 | 已完成 | 旧的 `v2_no_pointer_package_entries.expected` 已退场；source/stage0 新命令、Makefile gate、fixed-point contract 已全部收口，完整主 gate 前台跑绿 |
| no-pointer compile-closure 硬 gate | 已完成 | `release_artifact_v2.cheng` 和 `cheng_v2c_tooling.c` 现在都会对真实 manifest 闭包执行 `requireV2NoPointerManifestFree(...)`，`release-compile/system-link-plan/system-link-exec` 入口不再只检查单文件 surface |
| no-pointer 真实编译负例 fixture 隔离 | 已完成 | 3 个 compile-failure probe 已搬进各自独立的 fixture 根，避免 `release-compile` 把整个 `v2/tests/contracts` 目录装进闭包后被别的历史 fixture 抢先打死 |
| no-pointer 真实 package compile-closure contract | 已完成 | 新增 `v2_no_pointer_package_compile_closures.expected`，`compiler-core-release` 现在会前台跑 3 个 `v2/examples` 真实入口的 `release-compile`，并固定 `manifest_file_count/no_pointer_checked_*` 成功字段 |
| compile-path fixed-point 刷新 | 已完成 | `compiler_core_release_artifact.expected`、`compiler_core_system_link_plan.expected`、`compiler_core_system_link_exec.expected`、`compiler_core_system_link_exec_smoke.expected`、`tooling_release_artifact.expected`、`full_selfhost.expected` 已按 compile-closure no-pointer 字段重新对齐，完整主 gate 前台跑绿 |
| no-pointer 真实 package shared compile-closure contract | 已完成 | 复用既有 `tooling/topology/selfhost` 的 `system-link-exec --emit shared` contract，固定 `manifest_file_count=3`、`no_pointer_checked_file_count=3`、`no_pointer_checked_line_surface_file_count=3`、`no_pointer_closure_ok=1` |
| shared-path fixed-point 刷新 | 已完成 | `tooling_system_link_exec_shared.expected`、`topology_system_link_exec_shared.expected`、`selfhost_system_link_exec_shared.expected` 以及下游 fixed-point 已对齐，完整主 gate 前台跑绿 |
| shared plan no-pointer compile-closure contract | 已完成 | `system-link-plan --emit shared` 现在也正式携带 `manifest_file_count/no_pointer_checked_*/no_pointer_closure_ok`，并收进 `tooling/topology/selfhost` 三条真实 shared plan contract |
| stage0 `manifest_file_count` 活 bug | 已完成 | `cheng_v2c_tooling.c` 里 `build_system_link_plan(...)` 之前误传了未初始化的 `out.manifest_file_count`；现已改成直接传 `manifest.file_paths.len` |
| 本轮 fixed-point 刷新 | 已完成 | 新增 `tooling_system_link_plan_shared.expected`、`topology_system_link_plan_shared.expected`、`selfhost_system_link_plan_shared.expected`，并刷新 `compiler_core_release_artifact.expected`、`compiler_core_system_link_plan.expected`、`compiler_core_system_link_exec.expected`、`compiler_core_system_link_exec_smoke.expected`、`tooling_release_artifact.expected`、`full_selfhost.expected`，完整主 gate 前台跑绿 |
| program low_uir `break/continue` lowering | 已完成 | source `compiler_core_lowering_v2.cheng` 已把 `break/continue` 正规 lower 成现有 `jump`，`for_range` 额外补了 `step` label，`while/for_range` 都显式维护 loop label 栈 |
| stage0 `routine_op_count=0` 吞错回退 | 已完成 | `cheng_v2c_tooling.c` 已删掉低层 lowering 失败就 truncate exec ops 然后写 `routine_op_count=0` 的旧回退，改成和 source 一样直接失败 |
| `chain_node` exec-plan 缺口 | 已完成 | `./v2/artifacts/bootstrap/cheng_v2c system-link-plan --in ./v2/cheng-quic/src/project/chain_node.cheng --emit exe --target arm64-apple-darwin` 现在稳定输出 `compiler_core_program_local_payload_exec_plan_function_count=1418`、`compiler_core_program_local_payload_missing_exec_plan_function_count=0`、`system_link_ready=1` |
| `chain_node` 可执行产物 | 已完成 | `./v2/artifacts/bootstrap/cheng_v2c system-link-exec --in ./v2/cheng-quic/src/project/chain_node.cheng --emit exe --target arm64-apple-darwin --out ./v2/artifacts/bootstrap/chain_node_test` 已真生成可执行文件 |
| `pointMulGenerator` 固定基点路径 | 已完成 | `ecnist` 已从普通窗口乘法收成 `16` 项仿射表、一次 batch inverse、`pointJacobianAddAffineFixed(...)` 和 `pointMulGeneratorWnaf(...)`，不再在热路径里做通用 Jacobian-Jacobian 加法 |
| 证伪路径 | 已完成 | `256` 项运行时生成元大表已验证不是最短路：冷启动 `pubkey` 直接退化到 `20s timeout`；`bigModInvOddBinary` 替换现有逆元也已验证更慢，已全部撤回 |
| 当前性能口径 | 已完成 | 前台单测 `./v2/artifacts/bootstrap/_tmp_p256_pubkey_probe_bin` 冷启动降到 `11.63s`；`./v2/artifacts/bootstrap/_tmp_ecdsa_sign_probe_bin` 在 `40.05s` 仍未完成，说明 `sign` 剩余主热根已经不在固定基点乘法本身 |
| 本轮主 gate | 已完成 | `make -j1 -C /Users/lbcheng/cheng-lang/v2/bootstrap compiler-core-release compiler-core-system-link compiler-core-system-link-exec tooling-selfhost lsmr-contracts selfhost full-selfhost` 前台通过，当前 `manifest_fnv1a64=c3b367ba02d2c029` |

| 本轮 fixed-point 刷新 | 已完成 | 已刷新 `compiler_core_release_artifact.expected`、`compiler_core_system_link_plan.expected`、`compiler_core_system_link_exec.expected`、`full_selfhost.expected`，完整主 gate 前台跑绿 |
| Cheng-Chain 非 VM 数据面 | 已完成 | `v2/cheng-quic/src/chain/*` 已落下洛书多播树、信息分散、被动反熵、batch 共识 |
| `msquic_chain_smoke` 算法验收 | 已完成 | smoke 已固定验证 multicast tree、fragment recover、anti-entropy causal order、consensus batch ordering |
| `tooling-selfhost-check` source/runtime/native 收口 | 已完成 | source 命令、native bridge、runtime usage 已对齐，`full-selfhost` 不再卡 stage2 tooling check 退出码 |
| stage2 `tooling-selfhost-check` 直验 | 已完成 | `/Users/lbcheng/cheng-lang/v2/artifacts/full_selfhost/stages/cheng_v2.stage2/cheng_v2 tooling-selfhost-check ...` 现在稳定 `rc=0` |
| `chain_node_test` 运行阻塞定位 | 已完成 | 当前不是链算法错，而是普通程序运行时仍报 `driver_c compiler_core program local payload bridge: exec-plan interpreter missing label=main ... exec_op_count=298` |
| 本轮 fixed-point 刷新 | 已完成 | 已刷新 `compiler_core_release_artifact.expected`、`compiler_core_system_link_plan.expected`、`compiler_core_system_link_exec.expected`、`full_selfhost.expected`，完整主 gate 前台跑绿 |
| 洛书-八卦多维前缀树 | 已完成 | `v2/cheng-quic/src/chain/types.cheng` 和 `v2/cheng-quic/src/chain/lsmr.cheng` 已新增 `LsmrBaguaPrefixEntry/LsmrBaguaPrefixTree`，按 `bagua + luoshu prefix + value cid` 构建稳定 SoA 前缀树，节点 cid 和树 cid 都走 canonical text |
| 前缀树专用 smoke | 已完成 | 新增 `v2/cheng-quic/src/tests/lsmr_bagua_prefix_tree_smoke.cheng`，前台编译运行稳定输出 `nodes=9`、`entries=4`、固定 `treeCid`，并验证输入重排不漂 |
| `msquic_chain_smoke` 前缀树覆盖 | 已完成 | `src/tests` 和包外 `tests` 两份 chain smoke 都已把多维前缀树纳入固定算法验收，不再只测多播树/分片/反熵/共识 |
| 本轮 fixed-point 刷新 | 已完成 | 已刷新 `compiler_core_release_artifact.expected`、`compiler_core_no_pointer_closure.expected`、`compiler_core_system_link_plan.expected`、`compiler_core_system_link_exec.expected`、`full_selfhost.expected`，完整主 gate 前台跑绿 |
| `ChainIndex` 状态树化 | 已完成 | `types/log/codec/lsmr/consensus` 现在把账户状态 canonical 化成 `state cid`，并把 `ChainIndex` 正式派生为洛书-八卦多维前缀树 |
| 反熵状态树摘要 | 已完成 | `anti_entropy` 的 `signature/hello/advertise` 现在都携带 `state_tree_cid/state_tree_node_count/state_tree_entry_count`，advertise 还会带各 bagua root 摘要 |
| 状态树 dirty 机制 | 已完成 | 共识层只标记 `stateTreeDirty`，真正读签名时才按需精确重建，不再把树更新和共识写路径搅在一起 |
| 链 smoke 状态树验收 | 已完成 | `msquic_chain_smoke` 现在固定验证 mint 后 state tree 存在、sync 后两端 `stateTreeCid` 相等、transfer 后 entry 数为 2、replay 后树根不漂 |
| 前台验收 | 已完成 | `lsmr_bagua_prefix_tree_smoke_test` 前台运行通过；`msquic_verify_runner_test` 继续暴露旧的 `quic/msquictransport.initMsQuicTransport` 运行时老根，但这次前缀树改动没有放大它；完整 `v2/bootstrap` 主 gate 继续前台跑绿 |
| 反熵子树差分协议 | 已完成 | `anti_entropy` 已新增 `wantStateSubtrees`、`state subtree payload`、`state subtree diff`，同步不再主走全量 `event_cid` 差集，而是先按 `state_root -> bagua/prefix` 下钻 |
| `chain_node` 树差分同步 | 已完成 | `node.cheng` 已新增 `WANT_STATE_SUBTREE/PUSH_STATE_SUBTREE` 和按账户 `headCid` 追事件祖先链的同步路径，先定位状态差异，再补事件 DAG |
| 本地节点命令运行 | 已完成 | `chain_node_test` 前台已真实跑通 `balance=0` 与 `mint -> balance=11`，说明不含 QUIC 的节点日志、索引、状态树路径已经闭合 |
| 子树同步 smoke | 已完成 | 新增 `chain_state_tree_sync_smoke.cheng`，源码面固定验证 `state_root -> subtree -> headCid ancestor chain`；当前 `system-link-exec` 真链接，但运行仍暴露普通程序 runtime 对 `anti_entropy` label 的老根 |
| no-pointer closure contract 刷新 | 已完成 | 因新增链 smoke，`compiler_core_no_pointer_closure.expected` 已同步从 `291/288` 刷到 `292/289`；完整 `v2/bootstrap` 主 gate 前台跑绿 |
| 子树同步后序遍历修正 | 已完成 | `chainNodePullStateDiff(...)` 和 `chain_state_tree_sync_smoke` 都改成先下钻 child subtree、再同步 leaf head、最后校验 parent subtree 的后序遍历，不再先验父节点 |
| `chain_state_tree_sync_smoke` 真运行 | 已完成 | 前台运行 `v2/cheng-quic/artifacts/chain_state_tree_sync_smoke_test` 已稳定输出 `chain_state_tree_sync_smoke_synced=2` 和固定 `tree_cid=48b8d3153a70958c7c7b61d4c427b9b7c068f98264fe815a43c47ce8ed1b1d5a` |
| 本轮前台验收 | 已完成 | `make -j1 -C /Users/lbcheng/cheng-lang/v2/bootstrap compiler-core-release compiler-core-system-link compiler-core-system-link-exec tooling-selfhost lsmr-contracts selfhost full-selfhost` 已完整通过，`manifest_fnv1a64=ad181b63dba7a714` |
| `P-256 8x32` 专用内核骨架 | 已完成 | 新增 `src/std/crypto/p256_fixed.cheng`，把 `modulus/r2/oneMont/n0Inv` 固化成专用 `8x32` 数据面，去掉初始化阶段对 `bigBuildMontgomeryContext(...)` 的依赖 |
| 固定宽度 probe | 已完成 | 新增 `v2/tests/contracts/p256_fixed_core_probe.cheng`，前台 `cheng_v2c system-link-exec` 真编过，运行时已把问题压缩到 `p_a_mont` 第一次 Montgomery 乘法 |
| 新内核当前断点 | 已完成 | `p256_fixed_core_probe` 当前稳定报 `probe=fail p_a_mont p256_fixed: montgomery carry overflow idx=32`，说明问题已从“通用 BigInt 太慢”收缩到“固定宽度 Montgomery carry 传播实现与 Python 参考不一致” |
| `p256_fixed` 最小 stage probe | 已完成 | 临时最小 probe 已把断点继续压缩：`a * R^2` 第一拍的 `word0/mWord` 都对，真正炸点是同一拍的 `+ mWord * modulus`，稳定报 `mul reduce i=0 / carry overflow offset=0 idx=17` |
| `p256_fixed` active 接线 | 已撤回 | 试接 `ecnist` 后发现专用核还没闭合；已把 `p256ModMul/p256ModInv/nModMul/nModInv` 恢复成稳定的 Jacobian + generic Montgomery 路径，不留半坏状态 |
| 本轮结论 | 已完成 | 真根已经从“ECDSA 数学公式”收缩到“普通 program 轨的 64 位热算子/固定宽度 Montgomery 语义”；下一刀该直接查 program 轨 `uint64` 乘法与移位，不再盲接 `p256_fixed` |
| program runtime `u32/u64` 值语义 | 已完成 | `src/runtime/native/system_helpers_stdio_bridge.c` 已新增真实 `U32/U64` value kind，`cast/zero-init/binary_op/bitwise/shift/k512/uint64ToStr*` 都不再把 unsigned 值偷降成 signed |
| `uint64 >>` 最小前台 probe | 已完成 | `_tmp_p256_runtime_probe` 现已恢复 `mul_hi_ok=1`、`mul_hi_bit32=0`、`mul_hi_bit63=0`，说明普通 program 轨的 `uint64` 右移符号扩展 bug 已被收掉 |
| `p256_fixed` Montgomery 末尾减模 | 已完成 | `src/std/crypto/p256_fixed.cheng` 已从“高字非零直接报溢出”改成“带高字条件减模”，`n` 模数路径不再把合法结果误判成 `montgomery high carry overflow` |
| `p256_fixed_core_probe` | 已完成 | 前台真跑现在稳定输出 `probe=ok p_mul=1 n_mul=1`，说明 `P/N` 两条固定宽度 Montgomery 路已经闭合 |
| 本轮 fixed-point 刷新 | 已完成 | 已刷新 `compiler_core_system_link_exec.expected` 和 `full_selfhost.expected`，完整主 gate 前台重新跑绿 |
| 泛型零初始化双作用域解析 | 已完成 | `system_helpers_stdio_bridge.c` 已把 `zero_value_from_type` 收成“声明模块作用域 + 实例化模块作用域”，`Result[pfix.P256Fixed]` 不再拿 `std/result` 去解 `pfix.P256Fixed` |
| `ecdsaSignBytes` 类型解析活根 | 已完成 | `_tmp_ecdsa_sign_probe` 不再报 `missing type item for zero init type=pfix.P256Fixed`，现在会真实跑进签名热核 |
| `pointMul` 热点复核 | 已完成 | 打点 probe 证明当前超时卡在 `stage=mul`，不是 `nonce/hash/modinv`，真正热点已经收敛到 `pointMul` 路径 |
| `G` 基点窗口表预计算 | 已完成 | `ecnist.cheng` 已把 `p256GWindow4` 收进 `p256EnsureInit()`，`ecdsaSign/publicKeyFromPrivateKey` 改走生成元专用窗口表，不再每次临时重建 `G` 的 16 项窗口 |
| 本轮 fixed-point 刷新 | 已完成 | 已刷新 `compiler_core_system_link_exec.expected` 和 `full_selfhost.expected`，完整主 gate 前台通过，当前 `manifest_fnv1a64=c3b367ba02d2c029` |
| `P/N/B/G` 与 `1..15*G` 静态常量化 | 已完成 | `ecnist.cheng` 已把主常量和生成器 `Montgomery` 窗口表直接固化到源码，`p256EnsureInit()` 不再跑 `hexDecode + bigFromBytes + Jacobian 建表` |
| `z==1` affine 快路 | 已完成 | `pointJacobianToAffineFixed()` 现在遇到 `z == oneMont` 直接返回，不再白做一次模逆；`k=1` 的生成元点乘不再被尾部 affine 逆元拖死 |
| 签名只取 `x` 坐标 | 已完成 | `ecdsaSign()` 已改走 `pointMulGeneratorFixed()`，并只把 `x` 从 `Montgomery` 域转回 `BigInt` 做 `r`，不再把没用的 `y` 坐标也转回去 |
| `pubkey` 单探针 | 已完成 | `_tmp_p256_pubkey_probe_bin` 前台真跑从 `7.55s` 压到 `1.32s` |
| `ecdsaSignBytes` 单探针 | 已完成 | `_tmp_ecdsa_sign_probe_bin` 前台真跑已从“20s 内只到 `stage=mul`”推进到真实完成，当前 `rc=0 real=29.73 stdout=ecdsa_sign_probe=ok` |
| 主 gate 复验 | 已完成 | `make -j1 -C /Users/lbcheng/cheng-lang/v2/bootstrap compiler-core-release compiler-core-system-link compiler-core-system-link-exec tooling-selfhost lsmr-contracts selfhost full-selfhost` 继续前台通过，`manifest_fnv1a64` 仍为 `c3b367ba02d2c029` |
| 生成器固定标量重编码 | 已完成 | `pointMulGeneratorWnaf()` 已改成固定 `8x32` 标量原地 `odd/low5/+small/-small/>>1`，不再走 `BigInt` 的 `bigAdd/bigSub/bigShiftRight1` |
| 负点表预计算 | 已完成 | `p256GWindow4Neg` 已在 init 时一次性建好，主循环不再现算 `-digit * G` |
| `pubkey` 单探针第二刀 | 已完成 | `_tmp_p256_pubkey_probe_bin` 前台真跑从 `1.32s` 继续压到 `0.44s` |
| `ecdsaSignBytes` 单探针第二刀 | 已完成 | `_tmp_ecdsa_sign_probe_bin` 前台真跑从 `29.73s` 继续压到 `26.81s` |
| 主 gate 第二次复验 | 已完成 | `make -j1 -C /Users/lbcheng/cheng-lang/v2/bootstrap compiler-core-release compiler-core-system-link compiler-core-system-link-exec tooling-selfhost lsmr-contracts selfhost full-selfhost` 继续前台通过，`manifest_fnv1a64` 仍为 `c3b367ba02d2c029` |
| `p256_fixed` 专用 square 核 | 已完成 | `src/std/crypto/p256_fixed.cheng` 已新增固定 `8x32` Comba square + Montgomery reduction，并补了 `p_square/n_square` 对拍到 `p256_fixed_core_probe` |
| 退化 square 实现 | 已撤回 | 第一版“逐项积累 + 可变 carry 链”的 square 虽然正确，但会把 `stage=mul` 拉慢到 `31.84s`，已整块撤回，不留在 active 路径 |
| field 自乘接线 | 已完成 | `ecnist.cheng` 里 `pointJacobianDoubleFixed/pointJacobianAddFixed/pointJacobianAddAffineFixed` 和 `modexp` 的真自乘位点已改走 `p256FieldFixedSquare` |
| 本轮性能收益 | 已完成 | `p256_fixed_core_probe` 前台真跑 `p_mul=1 n_mul=1 p_square=1 n_square=1`；`_tmp_p256_pubkey_probe_bin` 前台真跑恢复到 `0.30s/0.13s`；`_tmp_ecdsa_sign_stage_probe_bin` 前台真跑 `stage=mul -> stage=r` 压到 `21.84s`，总时长 `24.59s`；`_tmp_ecdsa_sign_probe_bin` 前台真跑稳定到 `25.94s/24.32s` |
| 本轮 gate 复验 | 已完成 | `make -j1 -C /Users/lbcheng/cheng-lang/v2/bootstrap compiler-core-release compiler-core-system-link compiler-core-system-link-exec tooling-selfhost lsmr-contracts selfhost full-selfhost` 前台通过，`manifest_fnv1a64` 仍为 `c3b367ba02d2c029` |
| 第一梯队理论值 | 已完成 | 同机 `openssl speed -seconds 3 ecdsap256 ecdhp256` 已测得 `sign=11.9109us/op`、`ecdh/pubkey=24.4003us/op`；后续全部按 `1:1` 口径记，不再用 `2x` |
| benchmark 口径修正 | 已完成 | 已新增 `_tmp_p256_pubkey_scalar_probe_bin`，固定非平凡私钥前台真跑 `23.4166s/20.9950s/21.7849s`；`_tmp_p256_pubkey_probe_bin` 的 `priv=1` 现在只保留作回归 probe |
| 总体工程进度 | 已完成 | 已按 `普通程序执行面 35% + 纯 Cheng 性能内核 35% + 自举扩链路 15% + 非 VM 链主线 15%` 量化，总体约 `32.05%`，口径已写进 `v2/docs/自举和性能.md` |
| trusted `add/sub Into` 根修复 | 已完成 | `src/std/crypto/p256_fixed.cheng` 里的 `p256FixedModAddTrustedInto/p256FixedModSubTrustedInto` 已收成和 value-return 同构；逐拍 `addAffine` probe 已不再卡 `h/i/yDiff/r` 这些基础量 |
| `addAffine` 逐拍 probe | 已完成 | 新增 `_tmp_p256_mul_add_stage_cmp_probe.cheng`，前台真跑已把 trusted 路第一处错位稳定推进到 `x3` |
| 核心回归 | 已完成 | `p256_fixed_core_probe_bin` 继续 `probe=ok p_mul=1 n_mul=1 p_square=1 n_square=1`，`_tmp_p256_mul_add_cmp_probe_bin` 继续 `p256_mul_add_cmp_probe=ok` |
| 主 gate 复验 | 已完成 | `make -j1 -C /Users/lbcheng/cheng-lang/v2/bootstrap compiler-core-release compiler-core-system-link compiler-core-system-link-exec tooling-selfhost lsmr-contracts selfhost full-selfhost` 前台通过，`manifest_fnv1a64` 仍为 `c3b367ba02d2c029` |
| `comb6 digits[]` 预计算 | 已撤回 | 这轮 correctness probe 全绿，但 3 次中位数是 `pubkey=1.3587s`、`sign=2.0194s`、`mul=1.5392s`、`kinv=0.7741s`、`comb=1.1910s`。虽然 `comb` 局部比稳定版更快，但和稳定基线 `pubkey=1.2830s`、`sign=1.8210s`、`mul=1.4628s`、`kinv=0.7521s` 相比，总链路明确回归，所以已整块撤回，生产路径回到稳定版。 |
| 撤回后 correctness 复核 | 已完成 | 前台重新编译并运行 `_tmp_p256_mul_double_cmp_probe/_tmp_p256_mul_add_cmp_probe/_tmp_p256_comb_cmp_probe/_tmp_p256_repr_sign_r_cmp_probe`，结果继续 `ok/ok/ok/repr_r_eq=1`，没有留下坏状态。 |

| `addAffine` stage probe 全绿 | 已完成 | 这轮把 `z3a/z3` 也拆成 `value/into` 后，`_tmp_p256_mul_add_stage_cmp_probe_bin` 已稳定 `ok`，说明 `pfix` 底层已经闭合 |
| probe 假包装清理 | 已完成 | `fieldAdd/Sub/Double/QuadTrustedInto` 这一组 dead wrapper 已从 `_tmp_p256_mul_add_stage_cmp_probe.cheng` 删除，probe 只保留直接 `pfix` 调用 |
| `addAffine` 全 trusted active 接线 | 已撤回 | 把 `pointJacobianAddAffineFixedInPlace` 整段切到 trusted `Into` 路后，`pubkey` 会回归到 `19.88/19.49/19.47s`，`sign` 回归到 `23.20s`；已整块撤回 |
| `Crash -> trusted` 直连 | 已撤回 | 把 `p256FieldFixed{Add,Sub,Mul,Square}Crash` 直接连到 trusted 核会更差，`pubkey` 单样本直接变成 `30.73s`；已整块撤回 |
| 本轮保留内容 | 已完成 | 当前只保留 stage probe 和诊断收口，active `ecnist` 热路径没有留下回归接线；完整主 gate 继续前台通过，`manifest_fnv1a64=c3b367ba02d2c029` |
| `addAffine` 7 次 `mul -> helper shared scratch` | 已撤回 | 这轮只换 `pointJacobianAddAffineFixedInPlace` 里的 7 次乘法，`_tmp_p256_mul_add_cmp_probe` 和 `_tmp_p256_mul_add_stage_cmp_probe` 都能过，但三次前台基准把 `pubkey/sign` 拉回到约 `19.50s/22.38s`，已整块撤回 |
| `double` 3 次 `mul -> inline trusted scratch` | 已撤回 | 把 `pointJacobianDoubleFixedInPlace` 的 3 次乘法直接内联到 `pfix.p256FixedMontgomeryMulCoreTrustedScratchInto` 后，`_tmp_p256_mul_double_cmp_probe` 直接崩；已整块撤回 |
| `addAffine` 7 次 `mul -> inline trusted scratch` | 已撤回 | 继续把 helper 边界也砍掉后，`_tmp_p256_mul_add_cmp_probe` 和 stage probe 仍然能过，但 `_tmp_p256_mul_add_probe_bin` 单样本升到 `26.71s`，说明 helper 不是主根，这条路本身就错；已整块撤回 |
| 当前回归确认 | 已完成 | 撤回后 `_tmp_p256_mul_add_cmp_probe_bin`、`_tmp_p256_mul_double_cmp_probe_bin` 继续 `ok`，完整主 gate 继续前台通过，`manifest_fnv1a64` 仍为 `c3b367ba02d2c029` |
| pure `out-parameter` 热路 | 已撤回 | 这轮试了 `p256_fixed/ecnist` 的 pure `Into` 路，想直接砍掉 `Result[P256Fixed]` 返回值搬运。结果字段级 direct `add/sub/mul/square` 虽能对拍，但组合层 `double/quad/eight` 一接进 `pointJacobianDoubleFixedInPlace/pointJacobianAddAffineFixedInPlace` 就出现 `step=0` 偏差，补完自别名拆解后还会直接崩；生产代码已整块撤回 |
| 本轮 gate 复验 | 已完成 | 撤回 pure `out-parameter` 后，`_tmp_p256_mul_double_cmp_probe_bin`、`_tmp_p256_mul_add_cmp_probe_bin` 重新回到 `ok`，完整主 gate 前台通过，`manifest_fnv1a64` 仍为 `c3b367ba02d2c029` |
| `copyBytesRange` 重载歧义 | 已完成 | `v2/cheng-quic/src/connection.cheng` 里只读 `copyBytesRange` 已删掉 `var Bytes` 假重载；在正确 `var` 语义下不再因为 `Bytes/var Bytes` 双候选导致 `bytesTakePrefix` unresolved overload |
| 链入口重新编通 | 已完成 | `chain_node.cheng` 和 `chain_state_tree_sync_smoke.cheng` 现在都能前台 `system-link-exec --emit exe` 真链过；`chain_state_tree_sync_smoke_bin` 输出 `output_file_cid=413c5208cb226273112c15b7f1ad4b709d6deb821d59e5bbfd4551a4054769a9`，`chain_node_test` 输出 `output_file_cid=07cbd4070a856dbf821497d3c625b29c946eb0bce5e981a681e35446e7cbdb07` |
| 真实节点命令 | 已完成 | `chain_node_test balance --root ... --account alice --asset cheng` 已真跑出 `balance=0`；`mint --amount 11` 后再 `balance` 已真跑出 `balance=11` |
| 状态树 smoke 运行面 | 已定位 | `chain_state_tree_sync_smoke_bin` 运行不再是编译错误，而是长时间挂在 `runtime_compiler_core_program_local_payload_entry -> driver_c_prog_eval_item`；这说明当前真根已经从链算法表层收缩到普通 `program` 运行面 |
| 本轮主 gate | 已完成 | 修掉 `copyBytesRange` 后，`make -j1 -C /Users/lbcheng/cheng-lang/v2/bootstrap compiler-core-release compiler-core-system-link compiler-core-system-link-exec tooling-selfhost lsmr-contracts selfhost full-selfhost` 继续前台通过，没有新增孤儿进程 |
| `program` runtime 可见项查找 | 已完成 | `system_helpers_stdio_bridge.c` 这轮把 `top_level_tag`、authoritative lookup cache 和 `op.kind_tag` 都接进 hot path，`chain_state_tree_sync_smoke.cheng` 和 `chain_node_test balance` 继续前台通过 |
| `builtin` 分发预解码 | 已完成 | 新增 `builtin_tag` 数据面，`driver_c_prog_execute_call()` 现在只让 `builtin/importc` 进入 `driver_c_prog_try_builtin`；普通 Cheng 函数不再白扫整串 builtin label |
| `system_link_exec` fixed-point | 已完成 | 这轮只有 `compiler_core_system_link_exec.expected` 漂移，已按新 support object/runtime 对齐；完整主 gate 重新前台通过 |
| `msquic` 握手重传去重 | 已完成 | `v2/cheng-quic/src/native_runtime.cheng` 已把 `Initial/Handshake` 的 `FrameCrypto` 输入改成 `MsQuicCryptoStream` 按 `offset` 重组后再喂 TLS，不再把重传包里的重复 `frame.data` 直接塞进 `msquicTls13HandshakeFeed` |
| 311GB 内存根定位 | 已完成 | 已静态确认真根是“重传包重复喂 TLS -> transcript 重复追加 -> `appendBytes/toBytes` 整块复制放大”，不是递归；这轮按用户要求没有再编译或跑 smoke |
| TLS 握手 fail-fast 上限 | 已完成 | `v2/cheng-quic/src/tls/handshake13.cheng` 已新增 transcript/buffer 硬上限；后续再有重复输入只会直接报 `transcript overflow` / `handshake buffer overflow`，不再把机器内存拖爆 |
| `var arg not ref` stage0 真根 | 已完成 | 已静态确认 `chain_state_tree_sync_smoke` 当前不是链算法错，而是 `v2/bootstrap/cheng_v2c_tooling.c` 在 lowering `call` 时按“半成品 `low_plan`”反查 callee 参数签名；callee 若还没生成，`var_param` 会丢失，最终把本地数组按值降。现已改成按完整 `program/itemId` 查真实 routine 参数签名。 |
| 本轮执行方式 | 已完成 | 只做静态修根和代码 review，没有编译，没有跑 smoke，避免再触发大内存路径。 |
| `chain_state_tree_sync_smoke` 真运行 | 已完成 | 这轮把 `anti_entropy` 和 smoke 里的字符串场景裸 `len(...)` 全改成显式 `strings.len(...)` 后，`chain_state_tree_sync_smoke_test` 已前台真跑 `rc=0`，输出 `chain_state_tree_sync_smoke_synced=2` 和 `chain state tree sync smoke ok`；20 秒内已完成，峰值 RSS 约 `11.8GB`，不再卡在旧的 `len` 歧义根。 |
| `msquic_chain_smoke` 新最小根因 | 已完成 | `msquic_chain_smoke_test` 已前台真链接，并在 20 秒受控执行里稳定输出 `lsmr_ok/dispersal_ok/broadcast_ok/anti_entropy_ok/consensus_ok`，随后死在 `driver_c program runtime: index access on non-array label=add base_kind=record`。这说明链算法层已经越过，新的真根是 `v2/cheng-quic/src/chain/lsmr.cheng` 里整批裸 `add(...)` 仍停在 `load_name|add`，不是 QUIC/TLS/共识算法本身。 |
| `LSMR.md` 状态同步 | 已完成 | [LSMR.md](/Users/lbcheng/cheng-lang/v2/docs/LSMR.md) 顶部已补成正式状态表，明确写出“已完成/部分完成/未完成/代码落点/验证”，不再让愿景和现状混在一起。 |
| `LSMR` 剩余三块技术面 | 已完成 | `大衍流转 PubSub`、`CSG 子图载荷与网络层过滤`、`去中心化空间证明` 已分别落在 `v2/cheng-quic/src/chain/pubsub.cheng`、`v2/cheng-quic/src/chain/csg.cheng`、`v2/cheng-quic/src/chain/location_proof.cheng`，共用 `types.cheng` 新增正式数据结构。 |
| 新 smoke | 已完成 | `v2/cheng-quic/src/tests/lsmr_advanced_features_smoke.cheng` 已前台真编真跑通过，固定输出 `topic/plan/safe_graph/safe_delta/location_proof` 五个 cid，最后 `lsmr_advanced_features_smoke=ok`。 |
| 新 gate | 已完成 | `v2/bootstrap/Makefile` 的 `lsmr-contracts` 已接入 `lsmr_advanced_features_smoke`；前台执行 `make -j1 -C /Users/lbcheng/cheng-lang/v2/bootstrap lsmr-contracts` 已通过，当前 `manifest_fnv1a64=868e9aef9383a872`。 |
| 文档同步 | 已完成 | [LSMR.md](/Users/lbcheng/cheng-lang/v2/docs/LSMR.md) 和 [cheng-chain-mvp.md](/Users/lbcheng/cheng-lang/v2/docs/cheng-chain-mvp.md) 已同步成“技术面闭合、真实联网面仍部分完成”的正式口径。 |
| `FrameData` 真实编码长度 | 已完成 | `v2/cheng-quic/src/core/frame_model.cheng` 现在按 `kind/streamId/offset/value/dataLen/data` 的真实 QUIC varint 编码长度计算 `msquicFrameSize(...)`，不再把 `FrameData/Crypto/Datagram` 只按裸 payload 估长。 |
| `connection_impl` 精确切片 | 已完成 | `v2/cheng-quic/src/core/connection_impl.cheng` 已新增 `msquicConnImplFrameBudget(...)`、`msquicConnImplMaxDataChunkLen(...)`、`msquicConnImplMaxQueuedDataLen(...)`；`msquicConnImplQueueData(...)` 现在会先精确预估需要多少 frame，再按包预算切成多个 `FrameData`，不再让首帧大于 MTU 时整包一个都塞不进去。 |
| `pipeWrite` 分批 flush | 已完成 | `v2/cheng-quic/src/native_runtime.cheng` 的 `msquicNativePipeWrite(...)` 已改成按“当前队列还能装下的理论最大字节数”分批 `queueData`，每批立刻 `flushShortPackets + pump`，避免一次大写入先撞单帧上限，再撞 `128` 帧队列上限。 |
| 本轮执行方式 | 已完成 | 这轮按用户要求没有编译、没有跑 smoke，只做静态修根和记录同步，避免再次触发大内存路径。 |
| `system.panic` 老根收口 | 已完成 | `src/std/system.cheng` 已新增非重载 `panicStr`，`v2/src/compiler` 下全部 `system.panic(...)` 已切到 `system.panicStr(...)`。`compiler-core-release` 已不再死在 stage0 的 imported-field overload 解析上。 |
| `compiler-core` 主链恢复 | 已完成 | `compiler-core-release`、`compiler-core-system-link`、`compiler-core-system-link-exec`、native smoke 都已经重新前台跑绿；`compiler_core_system_link_exec_smoke` 也已按新 fixed-point 收口。 |
| `tooling/selfhost/full-selfhost` 收口 | 已完成 | `tooling_release_artifact`、`tooling_system_link_plan_shared`、`topology_system_link_plan_shared`、`network_selfhost`、`tooling_selfhost`、`selfhost_system_link_plan_shared`、`full_selfhost` 都已刷新并重新前台验过。 |
| LSMR runtime smoke 正式进 gate | 已完成 | `chain_state_tree_sync_smoke.expected` 和 `msquic_chain_smoke.expected` 已 checked-in，`lsmr-contracts` 现在不只验算法层，也真跑两条 runtime smoke。 |
| 本轮总验收 | 已完成 | `make -j1 -C /Users/lbcheng/cheng-lang/v2/bootstrap compiler-core-release compiler-core-system-link compiler-core-system-link-exec tooling-selfhost lsmr-contracts selfhost full-selfhost` 已前台通过，当前 `manifest_fnv1a64=7d152e8db0302b97`。 |
| 开始时间 | `2026-04-09 16:07 +0800` | `program-selfhost` 正式 gate 收口开始。 |
| `program-selfhost` 正式 gate | 已完成 | source/runtime/native/stage0 四层已经同构补齐 `program-selfhost-check`，`Makefile` 已新增正式 `program-selfhost` 目标并纳入 `full-selfhost`；当前固定要求 stage2 编译器真编真跑 `lsmr_advanced_features_smoke`、`chain_state_tree_sync_smoke` 和 `chain_node balance/mint/balance`，同时 stage2/stage3 的 release-plan-exec-binary 持续相等。 |
| 外部 C provider 收口 | 已完成 | `program-selfhost` 现在会正式检查 `compiler_core` 运行面不再依赖外部 C provider；stage1/stage2/stage3 当前都固定为 `external_cc_provider_count=0`，并已写入 `program_selfhost.expected`。 |
| 新主 gate | 已完成 | `make -j1 -C /Users/lbcheng/cheng-lang/v2/bootstrap program-selfhost` 已前台通过；完整 `compiler-core-release -> full-selfhost` 也已继续前台通过，当前 `manifest_fnv1a64=01628c9c884af1a8`。 |
| 开始时间 | `2026-04-09 22:14 +0800` | 继续把 `program` 热路径里的 typed `record` 固定布局 lookup 接到 runtime，并重新量 `pubkey/sign/mul/kinv`。 |
| typed `record` 共享 lookup | 已完成 | `system_helpers_stdio_bridge.c` 已给 `TypeDecl/ZeroPlan` 加共享 `field_lookup`，`zero_value_from_plan()` 和 `zero_record_shell_from_plan()` 直接复用，不再每次 rebuild typed record lookup。 |
| 新 fixed point | 已完成 | `compiler_core_system_link_exec/program_selfhost/full_selfhost` 已在 `manifest_fnv1a64=e4aa17192dd2cde3` 下重新前台收口，完整主 gate 已通过。 |
| 新性能口径 | 已完成 | 前台重编 probe 后，3 次中位数更新为 `pubkey=1.1427s`、`sign=1.6491s`、`mul_xonly=1.3204s`、`kinv=0.7149s`；单次 RSS 口径是 `pubkey≈125MB/116MB`、`sign≈262MB/254MB`。 |
2026-04-10 21:38
- `v3` 新增 `src/tooling` 纯 Cheng 控制面：`path/host_ops/bootstrap_contracts/hotpath_scan/perf_gate/gate_main`，以及 `src/backend/build_plan.cheng`、`bootstrap_contracts_smoke/perf_gate_smoke`。
- `v3/tooling` 新增 `cheng_v3.sh`、`bootstrap_bridge_v3.sh`、`build_backend_driver_v3.sh`，并把 `run_slice_gate.sh` 改成只认这两条 `v3` shell 入口，不再直接叫旧总控 `build-backend-driver` 子命令。
- 实测 `sh v3/tooling/scan_forbidden_hotpath.sh` 通过；`sh v3/tooling/bootstrap_bridge_v3.sh` 和 `sh v3/tooling/run_slice_gate.sh` 都稳定失败在同一个真阻塞：`strict bootstrap sidecar driver failed wrapper-source build`；`sh v3/tooling/cheng_v3.sh print-bootstrap` 目前还会在 seed 编译阶段被仓库现有 `artifacts/backend_driver/cheng rc=223` 挡住。
2026-04-10 22:02
- `v3/tooling/cheng_v3.sh` 已把外层日志改到 `artifacts/v3_tooling/cheng_v3_gate.seed.stderr.log`，避开 seed 内层自动生成的 `cheng_v3_gate.compile.log`；现在前台执行 `sh /Users/lbcheng/cheng-lang/v3/tooling/cheng_v3.sh print-bootstrap` 会直接打印 `rc=223 kind=deterministic_exit_223`、`hint=child_exited_223_directly_non_posix_signal` 和 seed 原始报错，不再只剩一句 `compile gate_main failed`。
- `v3/tooling/build_backend_driver_v3.sh` 也已改成显式收 `rc`，失败时会打印 `rc/kind/log`，不再依赖 `if ! VAR=... cmd >log` 这种会吞日志的写法。
- `verify_backend_sidecar_cheng_fresh.sh` 现在会把 `wrapper-source build` 失败明确打成 `rc/kind/hint/log`。proof launcher 默认 timeout 也已和外层 compile budget 对齐，不再把 20 秒内层 timeout 混成外层黑盒。
- `backend_driver_currentsrc_sidecar_wrapper.sh` 不再对 bootstrap proof surface 偷开 `wrapper_preserve_sidecar=1`；proof launcher 也开始尊重 `BACKEND_UIR_SIDECAR_DISABLE=1`。结果是 `sh /Users/lbcheng/cheng-lang/v3/tooling/bootstrap_bridge_v3.sh` 和 `sh /Users/lbcheng/cheng-lang/v3/tooling/run_slice_gate.sh` 的失败口径已经从“超时无输出”前进成确定性的 `rc=223 kind=deterministic_exit_223`，并稳定暴露 `backend_driver sidecar: missing strict sidecar mode contract`。
2026-04-10 22:18
- 已按用户要求新建 `v3/docs`，不是 `v3/doc`。当前文档入口是 [v3/docs/README.md](/Users/lbcheng/cheng-lang/v3/docs/README.md)。
- 已新增 [v3/docs/v2已踩性能和自举坑.md](/Users/lbcheng/cheng-lang/v3/docs/v2已踩性能和自举坑.md)，把 `v2` 已证明过的坑收成 `v3` 硬禁令：包括 `fixed point` 冒充完全自举、旧 tooling 入口回流、bootstrap 黑盒、sidecar 合同递归、热路径字符串壳、通用 `BigInt/Bytes/Seq` 回流、普通程序掉回解释执行、文本链帧、QUIC 长度估错、TLS 重传重复喂入、bagua/BPI 越界等。
- 已新增 [v3/docs/自举和性能开发计划.md](/Users/lbcheng/cheng-lang/v3/docs/自举和性能开发计划.md)，把这些坑直接写成 `v3` 里程碑和验收标准：`stage2/stage3`、同机 C `1:1`、AOT-only、bagua/BPI sidecar-only、`program-selfhost + chain_node` 正式 gate 都已明确写死。
- [v3/README.md](/Users/lbcheng/cheng-lang/v3/README.md) 已接上 `v3/docs` 文档入口，后续 `v3` 计划和避坑说明不再散落在 README 或 tooling README 里。
| 开始时间 | `2026-04-10 09:27 +0800` | 把 `v3` 之前的编译产物和旧编译工具链全部清空，再从零重编 `v3` 自举入口和 C 性能基线。 |
| 根旧产物清理 | 已完成 | 根目录 `artifacts/chengcache/build/dist`、根旧二进制 `backend_closedloop_fullspec` 和 `backend_closedloop_fullspec.stage1_fullspec.stamp` 已删除；旧 bootstrap 进程也已先停掉，避免边删边写。 |
| `v2/artifacts` 清理重编 | 已完成 | `v2/artifacts` 已整目录删除后重建；`make -C /Users/lbcheng/cheng-lang/v2/bootstrap all` 已重新产出 `v2/artifacts/bootstrap/cheng_v2_bootstrap` 和 `v2/artifacts/bootstrap/cheng_v2c`。 |
| `v3` 热路径扫描 | 已完成 | `v3/tooling/scan_forbidden_hotpath.sh` 在纯净环境下重新通过。 |
| `v3` C 基线重编 | 已完成 | `make -C /Users/lbcheng/cheng-lang/v3/bench/c_ref clean run` 已从零重跑，当前结果是 `sha256=221.86ns`、`x25519_shared=22282.10ns`、`p256_pubkey=2906.60ns`、`p256_sign=18223.00ns`、`p256_verify=36826.50ns`、`chain_encode=254.25ns`、`chain_decode=265.19ns`。 |
| frozen 对拍 | 已完成 | `v3/tooling/compare_bench.sh` 已跑，当前 clean run 相比 frozen baseline 在 `1.0712x` 到 `1.2762x` 之间波动。 |
| `v2` clean seed 复验 | 已完成 | 纯净 `v2` seed 再跑 `tooling-selfhost-host` 仍然稳定失败在 `release stdout mismatch`；手工把 `tooling_stage1_bootstrap` 往可执行推进会继续撞 `program_entry_exec_plan_missing local_payload_*`。 |
| `v3` clean bootstrap 现状 | 已完成 | 在没有旧 `probe_currentsrc_proof` 产物的纯净环境下，`v3/tooling/bootstrap_bridge_v3.sh` 现在直接失败为 `missing executable bridge driver: /Users/lbcheng/cheng-lang/artifacts/backend_selfhost_self_obj/probe_currentsrc_proof/cheng_stage0_currentsrc.proof`。 |
| 开始时间 | `2026-04-10 23:10 +0800` | 把 `v3/docs/cheng-plan-full.md`、`LSMR.md`、`baguaCSG.md`、`自举和性能.md` 收成一份统一的 `cheng v3` 特性矩阵和开发计划。 |
| `v3` 文档总览统一 | 已完成 | 已新增 [cheng语言特性矩阵和开发计划.md](/Users/lbcheng/cheng-lang/v3/docs/cheng语言特性矩阵和开发计划.md)，把编译器语义、并行模型、bootstrap、性能、LSMR、bagua/BPI、链主线、正式 gate 收成一个总表。 |
| `v3/docs` 索引同步 | 已完成 | [v3/docs/README.md](/Users/lbcheng/cheng-lang/v3/docs/README.md) 已接入新总览，并明确它是 `v3` 当前总口径入口。 |
| 开始时间 | `2026-04-10 23:32 +0800` | 把 `v3` bootstrap/tooling 主线切到 `v3/bootstrap` 极小 seed，并让 `run_slice_gate.sh` 在不依赖旧 proof/sidecar/tooling_cmd 的前提下真跑通过。 |
| `v3` bootstrap 主线重建 | 已完成 | `bootstrap_bridge_v3.sh` 现在会直接用 `cc` 编 [cheng_v3_seed.c](/Users/lbcheng/cheng-lang/v3/bootstrap/cheng_v3_seed.c) 产出 `cheng.stage0`，再连续产出 `cheng.stage1/stage2/stage3`，并写出 [bootstrap.env](/Users/lbcheng/cheng-lang/artifacts/v3_bootstrap/bootstrap.env) 和 [bootstrap_snapshot.txt](/Users/lbcheng/cheng-lang/artifacts/v3_bootstrap/bootstrap_snapshot.txt)。`sh /Users/lbcheng/cheng-lang/v3/tooling/bootstrap_bridge_v3.sh` 已前台通过。 |
| `v3` canonical bootstrap compiler | 已完成 | `build_backend_driver_v3.sh` 现在直接用 `cheng.stage2` 再物化一次 `v3/bootstrap/stage1_bootstrap.cheng`，产出 [artifacts/v3_backend_driver/cheng](/Users/lbcheng/cheng-lang/artifacts/v3_backend_driver/cheng)。`sh /Users/lbcheng/cheng-lang/v3/tooling/build_backend_driver_v3.sh` 已前台通过。 |
| `v3` launcher 收口 | 已完成 | `cheng_v3.sh` 不再依赖 `artifacts/tooling_cmd/cheng_tooling` 编 `gate_main`，而是变成 `v3/tooling` 目录下的 shell dispatcher。当前 `print-bootstrap` 和 `print-build-plan` 都已直接输出新 `v3/bootstrap` 主线信息。 |
| `v3` slice gate | 已完成 | `sh /Users/lbcheng/cheng-lang/v3/tooling/run_slice_gate.sh` 已前台通过。当前 gate 顺序是 `scan_forbidden_hotpath -> same-machine C baseline -> bootstrap bridge -> canonical bootstrap compiler -> bootstrap subset self-check -> stage2/stage3 contract equivalence`。 |
| 开始时间 | `2026-04-10 23:58 +0800` | 清掉 `v3` 最后一批旧 bootstrap/tooling 引用，并让文档、入口、seed 合同、实际 gate 完全一致。 |
| `v3` 旧引用清零 | 已完成 | `rg -n "probe_currentsrc_proof|stage2_proof|stage3_witness|currentsrc_proof|tooling_cmd/cheng_tooling|cheng_stage0_currentsrc\\.proof|sidecar contract" /Users/lbcheng/cheng-lang/v3 -S` 现已零命中。 |
| `gate_main` 收正 | 已完成 | [gate_main.cheng](/Users/lbcheng/cheng-lang/v3/src/tooling/gate_main.cheng) 的 `run-smokes` 已改成只跑 `artifacts/v3_bootstrap/cheng.stage0~3` 的 `self-check`，不再假定存在旧 `tooling_cmd` 编译面。 |
| seed 合同收正 | 已完成 | [cheng_v3_seed.c](/Users/lbcheng/cheng-lang/v3/bootstrap/cheng_v3_seed.c) 和 [stage1_bootstrap.cheng](/Users/lbcheng/cheng-lang/v3/bootstrap/stage1_bootstrap.cheng) 已把旧 proof 禁词统一改成泛化的 `legacy_proof_surface/legacy_sidecar_mode`。 |
| README 收正 | 已完成 | [v3/README.md](/Users/lbcheng/cheng-lang/v3/README.md)、[v3/tooling/README.md](/Users/lbcheng/cheng-lang/v3/tooling/README.md)、[v3/bootstrap/README.md](/Users/lbcheng/cheng-lang/v3/bootstrap/README.md) 现在都只描述 `seed -> stage0 -> stage1 -> stage2 -> stage3` 主线。 |
| 全链回归 | 已完成 | `sh /Users/lbcheng/cheng-lang/v3/tooling/bootstrap_bridge_v3.sh`、`sh /Users/lbcheng/cheng-lang/v3/tooling/build_backend_driver_v3.sh`、`sh /Users/lbcheng/cheng-lang/v3/tooling/cheng_v3.sh run-smokes`、`sh /Users/lbcheng/cheng-lang/v3/tooling/run_slice_gate.sh` 已全部前台通过。当前 `run_slice_gate.sh` 的最新同机 C 基线是 `sha256=178.92ns`、`x25519=19291.50ns`、`p256_pubkey=2589.80ns`、`p256_sign=17380.00ns`、`p256_verify=34383.00ns`、`chain_encode=235.46ns`、`chain_decode=225.57ns`。 |
| 开始时间 | `2026-04-10 10:57 +0800` | 按 `v3` 统一计划并行推进当前执行顺序 `1-5`，把链语义固定布局主线、host smoke 和正式 gate 接起来。 |
| `v3` 链语义核扩面 | 已完成 | 已新增 [lsmr_types.cheng](/Users/lbcheng/cheng-lang/v3/src/chain/lsmr_types.cheng)、[lsmr.cheng](/Users/lbcheng/cheng-lang/v3/src/chain/lsmr.cheng)、[anti_entropy.cheng](/Users/lbcheng/cheng-lang/v3/src/chain/anti_entropy.cheng)、[csg.cheng](/Users/lbcheng/cheng-lang/v3/src/chain/csg.cheng)、[pubsub.cheng](/Users/lbcheng/cheng-lang/v3/src/chain/pubsub.cheng)、[location_proof.cheng](/Users/lbcheng/cheng-lang/v3/src/chain/location_proof.cheng)、[consensus.cheng](/Users/lbcheng/cheng-lang/v3/src/chain/consensus.cheng)。 |
| `v3` 链 smoke 扩面 | 已完成 | 已新增 [lsmr_types_smoke.cheng](/Users/lbcheng/cheng-lang/v3/src/tests/lsmr_types_smoke.cheng)、[lsmr_locality_storage_smoke.cheng](/Users/lbcheng/cheng-lang/v3/src/tests/lsmr_locality_storage_smoke.cheng)、[lsmr_bagua_prefix_tree_smoke.cheng](/Users/lbcheng/cheng-lang/v3/src/tests/lsmr_bagua_prefix_tree_smoke.cheng)、[anti_entropy_smoke.cheng](/Users/lbcheng/cheng-lang/v3/src/tests/anti_entropy_smoke.cheng)、[csg_smoke.cheng](/Users/lbcheng/cheng-lang/v3/src/tests/csg_smoke.cheng)、[pubsub_smoke.cheng](/Users/lbcheng/cheng-lang/v3/src/tests/pubsub_smoke.cheng)、[location_proof_smoke.cheng](/Users/lbcheng/cheng-lang/v3/src/tests/location_proof_smoke.cheng)、[consensus_smoke.cheng](/Users/lbcheng/cheng-lang/v3/src/tests/consensus_smoke.cheng)。 |
| host smoke 主线 | 已完成 | 已新增 [run_v3_host_smokes.sh](/Users/lbcheng/cheng-lang/v3/tooling/run_v3_host_smokes.sh)，并把 [run_slice_gate.sh](/Users/lbcheng/cheng-lang/v3/tooling/run_slice_gate.sh) 接上这条入口。当前 host smoke 顺序是 `fixed_surface -> csg -> consensus -> pubsub -> location_proof -> chain_codec -> anti_entropy -> lsmr*`。 |
| host smoke 已通过项 | 已完成 | `fixed_surface_smoke`、`csg_smoke`、`consensus_smoke`、`pubsub_smoke`、`location_proof_smoke` 已在当前 workspace 下用 `cheng_v2c system-link-exec --root /Users/lbcheng/cheng-lang/v3 ...` 真编真跑通过。 |
| `chain_codec` host-run 真阻塞 | 已完成 | `chain_codec_binary_smoke` 已从 `imported record unresolved module field` 推进成稳定的 `driver_c program runtime: missing type decl for zero init type=int32 inst_owner=v3/tests/chain_codec_binary_smoke`。这也是 `sh /Users/lbcheng/cheng-lang/v3/tooling/run_slice_gate.sh` 当前前台失败位。 |
| 文档和 README 收正 | 已完成 | [v3/README.md](/Users/lbcheng/cheng-lang/v3/README.md)、[v3/tooling/README.md](/Users/lbcheng/cheng-lang/v3/tooling/README.md)、[cheng语言特性矩阵和开发计划.md](/Users/lbcheng/cheng-lang/v3/docs/cheng语言特性矩阵和开发计划.md) 已同步到当前事实：`LSMR digit` 已修正，当前真冲突是 host runtime 的 `int32 zero-init/type-decl` 缺口。 |
| 开始时间 | `2026-04-10 14:11 +0800` | 把 `v3` ordinary compile 从 `lowering` 再推进到 `object/native-link`，并让 seed/bootstrap/backend-driver 合同同步到这一层。 |
| `object/native-link` 数据面 | 已完成 | 已新增 `v3/src/backend/object_plan.cheng`、`v3/src/backend/native_link_plan.cheng`，并由 `v3/src/backend/system_link_exec.cheng` 统一导出 `primary_object_path/provider_object_paths/object_link_inputs/native_link_inputs`。 |
| 合同和 seed 同步 | 已完成 | `v3/src/tooling/bootstrap_contracts.cheng`、`v3/src/backend/build_plan.cheng`、`v3/bootstrap/stage1_bootstrap.cheng`、`v3/bootstrap/cheng_v3_seed.c` 已同步新增 `backend_object_plan_source/backend_native_link_plan_source`，`ordinary_pipeline_state` 已切到 `native_link_plan_stub_missing_codegen`。 |
| 新 smoke 和禁词回归 | 已完成 | `scan_forbidden_hotpath.sh` 已重新通过；`run_v3_host_smokes.sh` 已重新全绿；`object_native_link_plan_smoke` 也已固定成宿主可稳定执行的最小类型面 smoke。 |
| 新统一真阻塞 | 已完成 | `build_program_selfhost_v3.sh`、`build_chain_node_v3.sh`、`run_slice_gate.sh` 现在统一稳定失败在 `v3 compiler: object and native link plans ready, machine code emission and final link not implemented`，不再停在 lowering、runtime target/provider 或旧 proof/tooling 黑盒。 |
| 开始时间 | `2026-04-10 15:09 +0800` | 继续推进 `v3` ordinary compile，先修 ordinary 入口桥的真实运行时根，再把最小 ordinary 子集扩到 no-arg 尾调用。 |
| ordinary 入口桥修根 | 已完成 | `v3/bootstrap/cheng_v3_seed.c` 已把 ordinary entry bridge 和 `return_call_noarg_i32` 从 `bl callee; ret` 改成 `b callee`。这轮用全新 seed 二进制和全新输出目录前台验证过：旧写法确实会因为覆盖 `LR` 在入口桥自旋。 |
| 最小 ordinary 子集 | 已完成 | 已新增 `v3/src/tests/ordinary_call_chain_fixture.cheng` 和 `v3/tooling/build_call_chain_v3.sh`；`build_zero_exit_v3.sh` 与 `build_call_chain_v3.sh` 现在都已真编、真链、真跑 `rc=0`。 |
| host smoke | 已完成 | `sh /Users/lbcheng/cheng-lang/v3/tooling/run_v3_host_smokes.sh` 已重新前台全绿，`program_selfhost_smoke` 和 `chain_node_smoke` 继续留在 host smoke 主线。 |
| 新真阻塞 | 已完成 | `build_program_selfhost_v3.sh` 仍稳定报 `v3 compiler: primary object body semantics missing`；`build_chain_node_v3.sh` 也稳定报同一类错误，并额外带多处 `primary_object_call_target_missing`。说明 ordinary 主链当前只剩函数体语义子集没接通。 |
| 开始时间 | `2026-04-10 15:28 +0800` | 继续并行推进 `v3` ordinary compile，先把 lowering 改成按 entry 可达函数裁剪，再以前台验收重新确认真实语义缺口。 |
| reachability 裁剪 | 已完成 | `v3/bootstrap/cheng_v3_seed.c` 已补上 `import alias` 解析和调用边采集，ordinary lowering 现在按 entry 可达函数裁剪，不再把整包导入函数全拖进 primary object。 |
| 最新前台验收 | 已完成 | `build_backend_driver_v3.sh`、`build_zero_exit_v3.sh`、`build_call_chain_v3.sh`、`run_v3_host_smokes.sh`、`scan_forbidden_hotpath.sh` 都已重新前台通过。`build_program_selfhost_v3.sh` 现在把 `lowering_function_count` 从 `598` 压到 `16`；`build_chain_node_v3.sh` 则从 `1120` 压到 `112`。两条 ordinary 构建仍统一停在 `primary object body semantics missing`。 |
| 开始时间 | `2026-04-10 15:58 +0800` | 继续把 `v3` ordinary compile 往前推，先收 `program_selfhost` 的 consteval 真根，再把 `chain_node` 的假 tail-call 缺口清掉。 |
| consteval 栈爆与源码缓冲 | 已完成 | `v3/bootstrap/cheng_v3_seed.c` 已把 `v3_const_eval_expr` 的 `args[32][4096]`、`v3_const_eval_function` 的 `aliases/param_names` 改成堆对象，并把 `v3_load_function_source_lines` 的源码缓冲 `owned_lines` 正式释放；此前的 `___chkstk_darwin` 和 `rc=139` 已消失。 |
| multi-line signature 收正 | 已完成 | `v3_load_function_source_lines` 现在会先完整读文件，再第二遍匹配函数签名；`collect_lowering_functions_from_source` 也不再原地截断源码行，并且会跳过 `importc fn` 这种无函数体声明。`v3BootstrapArtifactNew`、`v3BootstrapSnapshotDefault` 这类多行签名函数已经能被正确识别。 |
| `program_selfhost` 真编真跑 | 已完成 | consteval 生成汇编已新增 `x30/lr` 保存恢复，收掉了 `bl _puts` 之后 `ret` 跳回自身的死循环。当前 `sh /Users/lbcheng/cheng-lang/v3/tooling/build_program_selfhost_v3.sh` 已前台通过并输出 `v3 program_selfhost_smoke ok`。 |
| `chain_node` 假 tail-call 根清理 | 已完成 | no-arg tail-call 现在直接按 lowering 已采集的完整 `callee_symbols[0]` 解析，不再靠“同模块裸函数名”回找。`build_chain_node_v3.sh` 已不再出现 `primary_object_call_target_missing`，失败位统一收敛到 `primary_object_body_semantics_missing`。 |
| `chain_node` unsupported 明细 | 已完成 | `v3/bootstrap/cheng_v3_seed.c` 现在会把前 8 个 unsupported 函数、`body_kind` 和第一条语句写进 compile report，并在 stderr 直接打印第一个 blocker。当前 `build_chain_node_v3.sh` 的首个真实 blocker 已固定成 `cheng/v3/project/chain_node_main::v3ChainNodeMainPrint -> echo(text)`；前 8 项还包括 `v3ChainNodeMainSelfTest`、`std/system::panic/panicStr`、`bytes_layout::byteSpanFromBytes/byteBufInit/byteBufEnsure/byteBufAppendByte`。 |
| 最新总 gate | 已完成 | `scan_forbidden_hotpath.sh` 通过，`run_v3_host_smokes.sh` 前台全绿，`run_slice_gate.sh` 已重新前台跑到 `chain_node` 阶段并稳定只死在 `primary_object_body_semantics_missing`。本轮同机 C baseline 是 `sha256=181.29ns`、`x25519=19154.60ns`、`p256_pubkey=2576.60ns`、`p256_sign=16879.00ns`、`p256_verify=33809.00ns`、`chain_encode=228.69ns`、`chain_decode=223.20ns`。 |
| 开始时间 | `2026-04-10 16:21 +0800` | 把 `v3` seed ordinary lowering 的 reachable 图修成真实依赖图，并扩大 `chain_node` unsupported 明细，停止被 bare import 漏边和 8 条截断误导。 |
| bare import 调用解析 | 已完成 | `v3/bootstrap/cheng_v3_seed.c` 已改成两阶段收集调用边：先收 lowering 函数，再按完整 lowering 库重刷每个函数体的 callee。新的 bare call 解析会优先命中同模块，否则只在当前 import 集里按“唯一已收集符号”解析到真实模块，不再把 `bytesAlloc/bytesLen/bytesSet/intToStr/rawmemCopy` 这类调用错判成当前模块。 |
| `chain_node` reachable 图收正 | 已完成 | `sh /Users/lbcheng/cheng-lang/v3/tooling/build_chain_node_v3.sh` 现在把 `lowering_function_count` 从 `110` 提升到 `126`。新增暴露的真实 reachable 依赖包括 `std/system::strEmpty/strDataPtr/strViewWithFlags/strViewUnsafe/strFromCStringBorrow`、`std/rawbytes::bytesView/emptyBytes/bytesAlloc/bytesFromString/bytesLen/bytesGet/bytesSet/bytesSliceView`、`std/strings::intToStr`、`std/rawmem_support::rawmemAsVoid/rawmemCopy`。 |
| unsupported 前沿放大 | 已完成 | `CHENG_V3_MAX_UNSUPPORTED_DETAILS` 已从 `8` 放大到 `64`。当前 `build_chain_node_v3.sh` 的 `primary_object_unsupported_function_count=64`，已经不再只是 `print/panic/bytes_layout` 这一层，而是把 `std/system`、`rawbytes`、`bytes_layout`、`anti_entropy`、`lsmr`、`sha256` 这些真实主链上的普通函数体缺口一起暴露出来。 |
| 回归状态 | 已完成 | `cc -std=c11 -O0 -Wall -Wextra -pedantic /Users/lbcheng/cheng-lang/v3/bootstrap/cheng_v3_seed.c -o /tmp/cheng_v3_seed_check` 无 warning 通过；`sh /Users/lbcheng/cheng-lang/v3/tooling/build_backend_driver_v3.sh` 通过；`sh /Users/lbcheng/cheng-lang/v3/tooling/run_v3_host_smokes.sh` 继续前台全绿；`sh /Users/lbcheng/cheng-lang/v3/tooling/scan_forbidden_hotpath.sh` 继续通过。 |
| 开始时间 | `2026-04-10 16:28 +0800` | 刷新整条 `stage0~stage3` 自举链，让新 seed 里的句型分类和 reachable 修正真正进入 `artifacts/v3_backend_driver/cheng`，并把 `chain_node` 的 unsupported 从“纯 unsupported”推进成可执行句型族。 |
| bootstrap 链刷新 | 已完成 | `sh /Users/lbcheng/cheng-lang/v3/tooling/bootstrap_bridge_v3.sh` 已重新前台通过，新的 `cheng.stage2` 已吃到这轮 seed 改动；随后 `sh /Users/lbcheng/cheng-lang/v3/tooling/build_backend_driver_v3.sh` 也重新前台通过。 |
| body kind 真分类 | 已完成 | 现在 `build_chain_node_v3.sh` 的首个 blocker 仍是 `v3ChainNodeMainPrint`，但 `body_kind` 已从统一的 `unsupported` 收敛成 `stmt_call`。同一份 compile log 里前 64 个 blocker 也已经分裂成 `stmt_call / stmt_var / stmt_let / stmt_if / stmt_for / return_expr` 这些真实句型族，不再是一坨 `unsupported`。 |
| 真实下一刀 | 已完成 | 当前最该补的已经不是“猜函数名单”，而是按句型族补 ordinary body semantics：先 `stmt_call + return_expr`，再 `stmt_var/stmt_let + if`，最后 `for/while`。`build_program_selfhost_v3.sh` 仍继续通过，说明这轮刷新没有把已通路径打坏。 |
| 开始时间 | `2026-04-10 17:05 +0800` | 给 `v3` lowering 补 ABI 元信息，并继续把 `chain_node` 的首个 ordinary blocker 从 `str` 包装函数往主流程推进。 |
| ABI 元信息进 compile log | 已完成 | `v3/bootstrap/cheng_v3_seed.c` 已新增参数名、参数类型、返回类型和 ABI 分类采集，并把 `primary_object_unsupported_abi` 写进 [chain_node.compile.log](/Users/lbcheng/cheng-lang/artifacts/v3_chain_node/chain_node.compile.log)。现在 stderr 会直接打印 `first unsupported function=... abi=...`，不再只报函数名和句型。 |
| `chain_node` blocker 前移 | 已完成 | [chain_node_main.cheng](/Users/lbcheng/cheng-lang/v3/src/project/chain_node_main.cheng) 已删掉 `v3ChainNodeMainPrint` 空包装；重新前台跑 [build_chain_node_v3.sh](/Users/lbcheng/cheng-lang/v3/tooling/build_chain_node_v3.sh) 后，首个 blocker 已前移到 `cheng/v3/project/chain_node_main::v3ChainNodeMainSelfTest`，并固定显示 `body_kind=stmt_var abi=ret=int32/i32 params=-`。`lowering_function_count` 也从 `126` 降到 `124`。 |
| 清理薄包装调用面 | 已完成 | [fixed256.cheng](/Users/lbcheng/cheng-lang/v3/src/std/crypto/fixed256.cheng) 已把唯一的 `panicStr` 调用收成 `panic`，进一步缩小了普通编译里无意义的薄包装层。 |
| 回归状态 | 已完成 | `cc -std=c11 -O0 -Wall -Wextra -pedantic /Users/lbcheng/cheng-lang/v3/bootstrap/cheng_v3_seed.c -o /tmp/cheng_v3_seed_check` 无 warning 通过；`bootstrap_bridge_v3.sh`、`build_backend_driver_v3.sh`、`build_program_selfhost_v3.sh`、`run_v3_host_smokes.sh`、`scan_forbidden_hotpath.sh` 均已前台通过。 |
| 开始时间 | `2026-04-10 19:05 +0800` | 继续把 `v3` ordinary compile 从复合值错归属推进到统一复合 ABI，目标是让 `chain_node` 不再卡在 `V3ChainNode` 落槽和 composite param 总拒绝。 |
| 跨模块复合返回值归一化 | 已完成 | `v3/bootstrap/cheng_v3_seed.c` 已让 internal callee 的参数/返回类型按 callee 自己的 `owner_module + import aliases` 归一化。现在 `node.v3ChainNodeInit(...)` 返回的 `V3ChainNode` 会稳定保留为 `cheng/v3/project/chain_node::V3ChainNode`，不再被 caller 误归成 `chain_node_main::V3ChainNode`。 |
| 复合参数入口与单层字段读取 | 已完成 | ordinary emitter 已允许 `composite` 参数按地址进入局部槽位，并新增 `base.field` 的单层标量字段读取，`str/Bytes` 的 `.len` 也一并接通到推断和 codegen。`build_chain_node_v3.sh` 已不再报 `emit reject composite param` 整批失败。 |
| 当前普通编译真阻塞 | 已完成 | `build_chain_node_v3.sh` 现在稳定收敛到 `std/crypto/sha256::getU32BE` 的 `let b0: int64 = int64(bytesGet(data, offset))`。这说明下一刀该补的是“标量调用里复合实参的最后漏口”，再往后才是数组 `.len`、索引、链式投影和复合返回值。 |
| 开始时间 | `2026-04-10 19:32 +0800` | 继续把 ordinary call 数据面收严，目标是一次收掉 bare `assert/echo` 和“复合临时实参无地址”这两个真根，并把 `chain_node` 失败位推进到下一层真实语义。 |
| `assert/echo` 语句级内建 | 已完成 | `v3/bootstrap/cheng_v3_seed.c` 现在直接在 ordinary emitter 里特判 bare `assert/echo`。`assert(cond, "msg")` 会先生成条件判断，失败时发 `_puts` 打印确定性字符串再 `_exit(1)`；`echo("msg")` 直接发 `_puts`。`build_chain_node_v3.sh` 的 compile log 已不再出现 `scalar call resolve failed ... callee=assert`。 |
| ordinary 调用暂存区 | 已完成 | `v3/bootstrap/cheng_v3_seed.c` 里的 `v3_codegen_call_scalar` 和 `v3_codegen_call_into_slot` 已改成按 `call_depth` 先把参数落到调用暂存区，再统一装入 `x0..x7` 调用。nested call 不会再冲掉前一个参数。 |
| 复合临时实参 materialize | 已完成 | 现在 ordinary emitter 会先为复合实参加临时槽规划，再把 `v3AntiEntropySignatureCid(local)`、`layout.fixedBytes32ToBytes(value)` 这类复合临时表达式先落成可取址 slot 再传地址。`build_chain_node_v3.sh` 的 compile log 已不再出现 `scalar call composite-arg local missing`。 |
| 当前新主根 | 已完成 | `build_program_selfhost_v3.sh` 继续通过，`run_v3_host_smokes.sh` 继续全绿；`build_chain_node_v3.sh` 仍失败，但当前 compile log 已经只剩更深一层的普通语义缺口：成片 `ret=.../composite` 函数还没进入 ordinary emitter，复合字段投影也还没完全接通。当前最该补的是 `sret/composite return function` 和 `composite field projection`，不是 `assert/echo` 或复合临时实参。 |
| 开始时间 | `2026-04-10 21:11 +0800` | 把 `sret/x8`、复合零初始化、赋值语句和复合 return 直接接进 ordinary emitter，让 `chain_node` 的首个 blocker 从复合 ABI 前移到控制流。 |
| `sret/x8` 主链 | 已完成 | `v3/bootstrap/cheng_v3_seed.c` 已把复合返回统一改成 `x8` hidden sret。callee 进入函数先把 `x8` spill 到隐藏本地槽位，caller 侧复合调用直接把目标地址装到 `x8`，用户参数继续占 `x0..x7`，不再整体右移。 |
| 无初始化声明与赋值语句 | 已完成 | ordinary emitter 已新增 `var/let` 无初始化声明解析、零初始化发码和普通赋值语句解析。现在已经支持 `local = expr`、`local.field = expr`，复合值会走 address materialize，标量字段会按偏移直接 `str` 回写。 |
| 复合 return 与字段路径 | 已完成 | `Result[T]` 字段布局、`store_id` 别名、多层字段路径读取和复合表达式 materialize 都已接进 `v3/bootstrap/cheng_v3_seed.c`。当前支持的复合 materialize 已覆盖 `local`、`local.field`、字符串字面量、复合调用、构造器和 `[]` 空序列。 |
| 回归状态 | 已完成 | `cc -std=c11 -O0 -Wall -Wextra -pedantic /Users/lbcheng/cheng-lang/v3/bootstrap/cheng_v3_seed.c -o /tmp/cheng_v3_seed_check` 通过；`bootstrap_bridge_v3.sh`、`build_backend_driver_v3.sh`、`build_program_selfhost_v3.sh`、`run_v3_host_smokes.sh`、`scan_forbidden_hotpath.sh` 全部前台通过。 |
| `chain_node` 新前沿 | 已完成 | `build_chain_node_v3.sh` 仍失败，但 `first unsupported function` 已经稳定收敛到 `std/system::strViewWithFlags body_kind=stmt_if`。这说明复合 ABI 这一层已经基本让开，下一刀该正面补 `if`，而不是继续围着 `sret/field assign` 打转。 |
| 开始时间 | `2026-04-10 22:32 +0800` | 继续并行推进 `v3` ordinary compile，先把 `base[index]` 读值、复合取址和 indexed lvalue 写回接进 seed 主链，再用 fresh `stage2/stage3` 重跑 `chain_node` 看失败位是否继续前移。 |
| 索引表达式主链 | 已完成 | `v3/bootstrap/cheng_v3_seed.c` 已新增 `v3_parse_index_access_expr(...)` 和 `v3_emit_index_access_address(...)`，并接进 `v3_prepare_expr_call_state(...)`、`v3_infer_expr_type(...)`、`v3_codegen_expr_scalar(...)`、`v3_materialize_composite_expr_into_address(...)`，还补了 indexed lvalue 赋值写回。现在 `Bytes/str/T[]/T[N]` 的 `base[index]` 已经能进入 ordinary 主链。 |
| fresh 自举回归 | 已完成 | `cc -std=c11 -O0 -Wall -Wextra -pedantic /Users/lbcheng/cheng-lang/v3/bootstrap/cheng_v3_seed.c -o /tmp/cheng_v3_seed_check` 无 warning 通过；随后 `sh /Users/lbcheng/cheng-lang/v3/tooling/bootstrap_bridge_v3.sh` 和 `sh /Users/lbcheng/cheng-lang/v3/tooling/build_backend_driver_v3.sh` 继续前台通过。 |
| `chain_node` 前沿继续前移 | 已完成 | 重新前台跑 `sh /Users/lbcheng/cheng-lang/v3/tooling/build_chain_node_v3.sh` 后，[chain_node.compile.log](/Users/lbcheng/cheng-lang/artifacts/v3_chain_node/chain_node.compile.log) 已不再报 `index.accountHeads[pos]`、`index.balances[pos]`、`lt.v3AppendFixed32(buf, index.accountHeads[pos])` 这类索引错误。新的第一批真阻塞已经变成 `bytesFromString(...)` 复合临时实参、`lt.v3HashInts("...", [assetId, accountId])` 的非空序列字面量，以及 bare `add(seq,val)`。 |
| host smoke 回归 | 已完成 | `sh /Users/lbcheng/cheng-lang/v3/tooling/run_v3_host_smokes.sh` 已前台重新全绿，`fixed_surface_smoke`、`compiler_runtime_smoke`、`parser_path_smoke`、`compiler_pipeline_stub_smoke`、`lowering_plan_smoke`、`primary_object_plan_smoke`、`object_native_link_plan_smoke`、`program_selfhost_smoke`、`csg_smoke`、`consensus_smoke`、`chain_node_smoke`、`pubsub_smoke`、`location_proof_smoke`、`chain_codec_binary_smoke`、`anti_entropy_smoke`、`lsmr_types_smoke`、`lsmr_locality_storage_smoke`、`lsmr_bagua_prefix_tree_smoke` 全部继续通过。 |
| 开始时间 | `2026-04-11 06:14 +0800` | 继续把异常链路收成默认出栈，并给 `driver_c program runtime` 补最小 Cheng 层调用栈。 |
| 默认 crash trace / backtrace | 已完成 | `src/runtime/native/system_helpers.c`、`system_helpers_selflink_min_runtime.c`、`system_helpers_selflink_shim.c` 现在默认开启 `CHENG_CRASH_TRACE` 和 `CHENG_PANIC_BACKTRACE`，只有显式设成 `0` 才关闭。`sh /Users/lbcheng/cheng-lang/v3/tooling/build_panic_trace_v3.sh` 已前台通过，而且不再依赖手工环境变量。 |
| `driver_c` 解释器栈 | 已完成 | `src/runtime/native/system_helpers_stdio_bridge.c` 已给 `DriverCProgFrame` 加 caller 链；`driver_c_die(...)` 和 `driver_c_abort_with_label_output(...)` 现在会先打印 `[driver_c stack]`，再继续打 crash trace 和原生 backtrace。前台真跑 `/Users/lbcheng/cheng-lang/artifacts/backend_runtime_trace/ffi_importc_handle_annotated_i32` 已验证 `unsupported callee` 会打印 `module=fixtures/ffi_importc_handle_annotated_i32 label=main source=... pc=4 op=call`。 |
| 当前遗留点 | 已完成 | `sh /Users/lbcheng/cheng-lang/v3/tooling/build_signal_trace_v3.sh` 这轮在当前 Darwin 环境卡在 `ordinary_signal_trace_fixture` 子进程 `UE` 状态，`kill -SEGV/-KILL` 都没收掉；这条不是默认出栈逻辑回退，而是 signal fixture 本身还要单独拆。 |
| ordinary `importc` 尾调用解析 | 已完成 | `v3/bootstrap/cheng_v3_seed.c` 现在会从源码文件解析同文件 `importc fn` 的 ABI 元信息，并把 `return_call_noarg_i32` 的尾调用快路径接回统一 call-target resolver。顺手删掉了这轮不再使用的旧同模块 tail-call helper，`cc -std=c11 -O0 -Wall -Wextra -pedantic /Users/lbcheng/cheng-lang/v3/bootstrap/cheng_v3_seed.c -o /tmp/cheng_v3_seed_check` 已无 warning 通过。 |
| `signal` fixture 自触发 | 已完成 | `v3/src/tests/ordinary_signal_trace_fixture.cheng` 现在通过 `cheng_force_segv()` 前台自触发；`v3/tooling/build_signal_trace_v3.sh` 也改成直接前台运行，不再后台起子进程再外部 `kill -SEGV`。这条已经把 Darwin 上的 `UE` 卡死面彻底移掉。 |
| `signal` 默认源码栈 + 原生栈 | 已完成 | `src/runtime/native/system_helpers.c`、`system_helpers_selflink_min_runtime.c`、`system_helpers_selflink_shim.c` 的 signal handler 现在都会在 crash trace 后继续打印 native backtrace。前台重跑 `bootstrap_bridge_v3.sh`、`build_backend_driver_v3.sh`、`build_signal_trace_v3.sh`、`build_panic_trace_v3.sh` 全部通过；`build_signal_trace_v3.sh` 现在稳定输出 `ordinary_signal_trace_fixture.cheng:4-5`，并带 `cheng_force_segv`、`main` 的原生栈帧。 |
| 开始时间 | `2026-04-11 07:58 +0800` | 把 generational handle 收进 `v3` 标准面，并把 ordinary `ffi_handle` 真编真跑接进 gate。 |
| `program_support_v3` 标准面 | 已完成 | 已新增 [program_support_v3.cheng](/Users/lbcheng/cheng-lang/v3/src/runtime/program_support_v3.cheng)，把 `cheng_ffi_handle_new/get/add/release_i32` 暴露成 `v3FfiHandle*` 包装；[system_link_exec.cheng](/Users/lbcheng/cheng-lang/v3/src/backend/system_link_exec.cheng) 也已接入 `runtime/program_support_v3` provider source。 |
| `var` 地址装参截断修正 | 已完成 | [cheng_v3_seed.c](/Users/lbcheng/cheng-lang/v3/bootstrap/cheng_v3_seed.c) 新增 `v3_call_arg_load_abi(...)`，`target.param_is_var[i]` 现在会统一按 `ptr` reload，不再把 64 位地址按 `i32` 读回。这个真根之前会把 `v3FfiHandleGetI32` 的 out 指针截成坏地址直接 `SIGSEGV`。 |
| `i32 -> i64` 返回归一化修正 | 已完成 | [cheng_v3_seed.c](/Users/lbcheng/cheng-lang/v3/bootstrap/cheng_v3_seed.c) 现在在允许 `i32` 返回喂给 `i64` 期望时显式发 `sxtw`。这条修掉以后，`-1` 不再被 ordinary caller 看成 `4294967295`，stale handle 校验终于和 runtime 的 invalid code 对齐。 |
| `ffi_handle` 真验收 | 已完成 | `cc -std=c11 -O0 -Wall -Wextra -pedantic /Users/lbcheng/cheng-lang/v3/bootstrap/cheng_v3_seed.c -o /tmp/cheng_v3_seed_check`、`bootstrap_bridge_v3.sh`、`build_backend_driver_v3.sh`、`build_ffi_handle_v3.sh`、`build_signal_trace_v3.sh`、`build_panic_trace_v3.sh`、`build_call_chain_v3.sh` 全部前台通过；单独用 host compiler 真编真跑 `/Users/lbcheng/cheng-lang/artifacts/v3_hostrun/ffi_handle_smoke.single` 也已经输出 `v3 ffi_handle_smoke ok`。 |

| 开始时间 | `2026-04-11 11:10 +0800` | 继续把 `v3` 冷轨 `BFT-SMI` 打到可验收，先把 `CheckTx -> Finalize` 这条预演/落账链的真 blocker 压清楚。 |
| `BFT` apply 主链复核 | 已完成 | [bft_apply_single_smoke.cheng](/Users/lbcheng/cheng-lang/v3/src/tests/bft_apply_single_smoke.cheng) 继续前台输出 `v3 bft_apply_single_smoke ok`，说明 `V3BftStateMachine -> applyTx` 主路径没坏。 |
| `BFT` index 主链复核 | 已完成 | 已新增 [bft_apply_to_index_smoke.cheng](/Users/lbcheng/cheng-lang/v3/src/tests/bft_apply_to_index_smoke.cheng)，并且前台输出 `v3 bft_apply_to_index_smoke ok`。这说明本地 `V3ChainIndex` 上的权威 apply 也没坏，`consensus.v3ConsensusApplyChecked(...)` 和 `v3BftApplyTxToIndex(...)` 这条核链是通的。 |
| `CheckTx` 真 blocker 收敛 | 已完成 | [bft_finalize_summary_smoke.cheng](/Users/lbcheng/cheng-lang/v3/src/tests/bft_finalize_summary_smoke.cheng) 现在把 `mint/badTransfer` 的 `CheckTx` 前导补成和 self-test 同口径后，失败稳定前移到 `v3 bft finalize summary: applied count`；[bft_state_machine_smoke.cheng](/Users/lbcheng/cheng-lang/v3/src/tests/bft_state_machine_smoke.cheng) 也继续稳定失败在 `v3 bft self-test: block1 applied count`。这说明真正污染后续 finalize 的是 `CheckTx` 预演链，不是 `consensus` 算法，也不是单次 apply。 |
| `str -> Bytes` 复合实参爆栈 | 已完成 | [consensus.cheng](/Users/lbcheng/cheng-lang/v3/src/chain/consensus.cheng) 和 [bft_state_machine.cheng](/Users/lbcheng/cheng-lang/v3/src/project/bft_state_machine.cheng) 里 `bytesFromString(\"...\")` 直接喂给 `byteBufAppendBytes(...)` 的形状，已经统一改成先绑定本地 `Bytes`。这条收掉后，`bft_finalize_summary_smoke` 再次回到可 fresh 编译状态，不再炸 seed 的 `direct str-arg scratch overflow`。 |
| host smoke 前沿 | 已完成 | `bash /Users/lbcheng/cheng-lang/v3/tooling/run_v3_host_smokes.sh` 这轮最新首个 host blocker 已经前移到 `parser_path_smoke`；在它前面的 `fixed256_sha256/ref10_ashr/fixed256_curve25519/fixedbytes32_seq_index/compiler_runtime` 都继续通过。 |
| 开始时间 | `2026-04-11 15:26 +0800` | 把 `lowering_plan_smoke` 的字符串符号拼接真崩点挖穿，并回归默认异常堆栈链路。 |
| `lowering plan` 字符串拼接修正 | 已完成 | [cheng_v3_seed.c](/Users/lbcheng/cheng-lang/v3/bootstrap/cheng_v3_seed.c) 现在把 `str` 的 `+` lowering 改成“左右 `str` 先 materialize 到 scratch slot，再调用 `driver_c_str_concat_bridge` 返回完整 `str`”，不再手拆 `str.data` 去直调 `___cheng_str_concat`。这条收掉后，[lowering_plan_smoke.cheng](/Users/lbcheng/cheng-lang/v3/src/tests/lowering_plan_smoke.cheng) 已前台输出 `v3 lowering_plan_smoke ok`，之前稳定炸在 `v3LoweringFunctionSymbolText` 的崩点已经消失。 |
| `lowering` 局部回归 | 已完成 | [lowering_line_local_smoke.cheng](/Users/lbcheng/cheng-lang/v3/src/tests/lowering_line_local_smoke.cheng) 和 `compiler_pipeline_stub_smoke` 已继续前台输出 `ok`；说明这次改动没有把 slice 出来的 `functionName`、`ownerModulePath` 和 compiler pipeline 主链带坏。 |
| 默认异常堆栈回归 | 已完成 | `build_panic_trace_v3.sh`、`build_bounds_trace_v3.sh`、`build_signal_trace_v3.sh` 已重新前台通过，分别继续打印 `.cheng` 源码位置和 native backtrace；默认 `panic/bounds/signal` 堆栈链路保持成立。 |
2026-04-11 18:42
- `v3` 这一轮把 `bft -> chain_node -> anti_entropy -> lsmr` 的 host 主线一次性收绿了。现在前台重跑 `sh /Users/lbcheng/cheng-lang/v3/tooling/run_v3_host_smokes.sh` 已稳定输出 `v3 host smokes: ok`。
- `BFT` 的真根已经不是算法核，而是 finalize 路径的 batch 形态。[bft_state_machine.cheng](/Users/lbcheng/cheng-lang/v3/src/project/bft_state_machine.cheng) 现在在 finalize 阶段先冻结 `batchCount`，再从 SoA 标量字段显式重建 `V3BftTx`，`bft_finalize_summary_smoke` 和 `bft_state_machine_smoke` 都已前台通过。
- `pubsub/location_proof` 两处 `while j >= 0 && ...` 的短路写法在当前 lowering 下会把 `j == -1` 继续带进索引，已经分别在 [pubsub.cheng](/Users/lbcheng/cheng-lang/v3/src/chain/pubsub.cheng) 和 [location_proof.cheng](/Users/lbcheng/cheng-lang/v3/src/chain/location_proof.cheng) 改成显式 `while + break`。对应的 [pubsub_smoke.cheng](/Users/lbcheng/cheng-lang/v3/src/tests/pubsub_smoke.cheng) 和 [location_proof_smoke.cheng](/Users/lbcheng/cheng-lang/v3/src/tests/location_proof_smoke.cheng) 也都收成了 ordinary 稳定可吃的显式 `var + Result` 形态。
- `chain_node` 二次同步真正炸的不是共识算法，而是 `LSMR` 树构建对嵌套 `seq` 字段做原地 grow。LLDB 已经把坏点钉到 [lsmr.cheng](/Users/lbcheng/cheng-lang/v3/src/chain/lsmr.cheng) 的 `entryKeyIds` grow，所以现在树和森林都改成“局部值构建 -> 一次性赋回”，[chain_node.cheng](/Users/lbcheng/cheng-lang/v3/src/project/chain_node.cheng) 的 `rebuildDerived` 也改成 fresh forest 再整体写回；[chain_node_smoke.cheng](/Users/lbcheng/cheng-lang/v3/src/tests/chain_node_smoke.cheng) 已重新前台通过，并清掉了临时调试 echo。
- `anti_entropy/lsmr` 这几条 smoke 当前已经完全回到 `v3` 正式 fixed-layout 口径，不再让 `AddressFromSeed/LocalityHashText/StateCellFromText` 这类文本壳反入 host 主线。[anti_entropy_smoke.cheng](/Users/lbcheng/cheng-lang/v3/src/tests/anti_entropy_smoke.cheng)、[lsmr_locality_storage_smoke.cheng](/Users/lbcheng/cheng-lang/v3/src/tests/lsmr_locality_storage_smoke.cheng)、[lsmr_bagua_prefix_tree_smoke.cheng](/Users/lbcheng/cheng-lang/v3/src/tests/lsmr_bagua_prefix_tree_smoke.cheng) 都已改成固定哈希、直接地址 digits、直接 `StateCellFill`，并全部前台输出 `ok`。
- 继续外推之后，`chain_node_libp2p_smoke` 的 ordinary 编译面也已经通过；现在它剩下的是纯运行期语义前沿：快照已经能通过 `LatestSnapshot` 取到，但 `decodeSnapshot` 仍然失败。这说明 host 主线之外的下一刀不该再打 ordinary 句型，而该直接查 `chain_node_libp2p` 的快照编解码一致性。
2026-04-11 17:11
- `v3` 这轮把数组默认值和字面量初始化收严了。真根不是 `list literal -> T[N]` 物化本身，而是 [cheng_v3_seed.c](/Users/lbcheng/cheng-lang/v3/bootstrap/cheng_v3_seed.c) 的 `v3_parse_fixed_array_type(...)` 会把 `int32[]` 误当成 `int32[0]`，于是 `var zs = [1,2,3]` 会先走 fixed-array 分支再被长度检查拦死。现在已经改成只有 `[...]` 里有非空纯数字时才算 `T[N]`，`[]` 继续只表示动态序列。
- 新增并扩了 [default_init_literals_smoke.cheng](/Users/lbcheng/cheng-lang/v3/src/tests/default_init_literals_smoke.cheng)，现在同时真验 6 条：`var xs: int32[]` 默认空序列、`var ys: int32[3]` 默认零值、`var zs = [1,2,3]`、`var ws: int32[3] = [4,5,6]`、`sumFixed([1,2,3])` 参数字面量、`return [7,8,9]` 返回 fixed array 字面量。
- 验证已经前台跑过三路：`sh /Users/lbcheng/cheng-lang/v3/tooling/bootstrap_bridge_v3.sh` 重建 `cheng.stage2/cheng.stage3`；随后 `default_init_literals_smoke` 用 `stage2` 和 `stage3` 都真编真跑通过；最后 fresh `build_backend_driver_v3.sh` 后，用 `/Users/lbcheng/cheng-lang/artifacts/v3_backend_driver/cheng` 也真编真跑通过。并且这条 smoke 已经接进 [run_v3_host_smokes.sh](/Users/lbcheng/cheng-lang/v3/tooling/run_v3_host_smokes.sh)。
2026-04-11 17:19
- `v3` 这轮把 fixed-array 的 `if/ternary` 上下文也收掉了。真根不是 list literal 物化本体，而是 [cheng_v3_seed.c](/Users/lbcheng/cheng-lang/v3/bootstrap/cheng_v3_seed.c) 里 `v3_materialize_composite_expr_into_address(...)` 先拿无上下文 `v3_infer_expr_type(...)` 做分支全等预判，把 `if flag: [1,2,3] else: [4,5,6]` 里的两个分支都看成 `int32[]`，从而拦掉 `int32[3]` 目标。现在这层已经直接改成按目标类型 materialize。
- 参数面也一起收了：`v3_prepare_composite_call_arg_temp(...)` 现在在“目标参数已知是 composite，实参是 `if/ternary`”时，临时槽直接按目标类型开，不再先走一遍会丢 `T[N]` 上下文的无类型推导。最小探针 `sumFixed(if true: [1,2,3] else: [4,5,6])` 和 `sumFixed(true ? [7,8,9] : [10,11,12])` 已经在 `stage2/stage3` 真编真跑通过。
- [default_init_literals_smoke.cheng](/Users/lbcheng/cheng-lang/v3/src/tests/default_init_literals_smoke.cheng) 已继续扩成 12 条硬断言，新增覆盖 `var x: T[N] = if ...`、`var x: T[N] = cond ? ... : ...`、`sumFixed(if ...)`、`sumFixed(cond ? ... : ...)`、`return if ...`、`return cond ? ... : ...`。随后 `stage2`、`stage3`、fresh host compiler 三路都已经前台通过。
- 并行核对结果也收口了：`chain_node_libp2p_smoke` 当前在 host、`stage2`、`stage3` 三条线上都直接输出 `v3 chain_node_libp2p_smoke ok`，所以它现在不是活动 blocker；旧的 `decodeSnapshot` 失败已经是历史问题，不该继续当作当前前沿。 |
2026-04-11 17:32
- `v3` 这轮把模块级全局初始化主线补上了。真根在 [cheng_v3_seed.c](/Users/lbcheng/cheng-lang/v3/bootstrap/cheng_v3_seed.c)：之前 `v3_append_module_global_storage(...)` 只给顶层 `var` 发 `.space`，入口桥也只 `b entry`，所以 `var g: int32[3] = [...] / if ... / cond ? ... : ...` 其实从来没有执行初始化表达式。现在已经新增模块级 `__v3_module_init` 发射器，先规划 expr 的 call-depth/string temp/composite 临时槽，再复用现成 `v3_codegen_expr_scalar(...)` 和 `v3_materialize_composite_expr_into_address(...)` 真写全局地址；exe 入口桥也改成了“保存 `x30` -> `bl module_init` -> 恢复 `x30` -> `b entry`”。
- [default_init_literals_smoke.cheng](/Users/lbcheng/cheng-lang/v3/src/tests/default_init_literals_smoke.cheng) 现在新增了 `gFixed/gIf/gTernary` 三条模块级 fixed-array 初始化断言。`sh /Users/lbcheng/cheng-lang/v3/tooling/bootstrap_bridge_v3.sh` 已重新前台通过；随后 `default_init_literals_smoke` 在 `stage2`、`stage3`、fresh host compiler 三路都再次真编真跑通过，分别确认了模块级 `list literal / if / ternary` 初始化。
- 入口桥接加上模块 init 以后，我又并行前台回归了 [chain_node_libp2p_smoke.cheng](/Users/lbcheng/cheng-lang/v3/src/tests/chain_node_libp2p_smoke.cheng)。结果 host、`stage2`、`stage3` 三路都继续直接输出 `v3 chain_node_libp2p_smoke ok`，说明这次改动没有把现有 libp2p 主线带坏。
- 这轮并行顺手核到的独立剩余边角也明确了：`Wrap(xs: if ... )` 这条现在真卡的是同模块 record 类型布局/复合返回链，不是模块 init。本轮已经把它从 [default_init_literals_smoke.cheng](/Users/lbcheng/cheng-lang/v3/src/tests/default_init_literals_smoke.cheng) 里拆出去，避免把两个根因混在一起。 |
- 模块 init 真接进总 gate 后，又暴露了第二个硬点：`std/crypto/ed25519/ref10` 里的 `CurveOrder: Bytes = initCurveOrder()` 这种顶层复合初始化，会调用只服务全局初始化的本地函数；这些函数之前不会从 `main` 可达，所以不会进 reachable/lowering。现在已经把“顶层 `var` 初始化表达式”正式接进 reachable seed，`initCurveOrder/initBasePointBytes/initDConstValue/initD2ConstValue/initSqrtM1Value` 这类路径都会随着模块 init 一起进 lowering。
- 最新前台回归已经完整收口：fresh `build_backend_driver_v3.sh` 后，`sh /Users/lbcheng/cheng-lang/v3/tooling/run_v3_host_smokes.sh` 重新跑到 `v3 host smokes: ok`；并行的 `sh /Users/lbcheng/cheng-lang/v3/tooling/run_v3_stage23_libp2p_smokes.sh` 也重新跑到 `v3 stage2/stage3 libp2p: ok`。这说明入口桥接和 reachable 补丁都已经闭环。 |
2026-04-11 17:39
- `Wrap(xs: if ... )` 这条并行复核已经收口：不是编译器还缺“constructor 字段 + implicit composite return”能力，真根是我之前探针把记录类型写成了 `type Wrap = { ... }`。当前 seed 的 type-def 收集只吃正式的 `type / Wrap = / xs: int32[3]` 缩进记录定义；换成这个写法后，本地最小探针 `return Wrap(xs: if flag: [1,2,3] else: [4,5,6])` 已经在 `stage2` 直接真编真跑通过。
- [default_init_literals_smoke.cheng](/Users/lbcheng/cheng-lang/v3/src/tests/default_init_literals_smoke.cheng) 现在已经把这条正式接回去了：新增 `Wrap` 缩进记录定义、`makeWrapIf/makeWrapTernary` 两个函数，以及字段读取断言。随后 `default_init_literals_smoke` 在 `stage2`、`stage3`、fresh host compiler 三路都重新真编真跑通过。
- 并行子代理结论和本地结果一致：只要 record 类型被正确收集，`return -> constructor -> field meta -> if/ternary -> fixed-array literal` 这条链在当前源码里已经是闭合的。所以这条不再是下一刀前沿。 |
| 开始时间 | `2026-04-11 19:01 +0800` | 并行推进 `libp2p_quic_tls_smoke`：一条线补 `core/option` 的 import resolver 漏口，一条线补 `ptr == nil` compare ABI 错配，并隔离 fresh `stage1/2/3` 在 QUIC/TLS 闭包上的新崩点。 |
| `core/` 导入漏口 | 已完成 | [cheng_v3_seed.c](/Users/lbcheng/cheng-lang/v3/bootstrap/cheng_v3_seed.c) 的 `v3_module_path_to_source_path(...)` 已补上 `core/` 映射。此前 `std/option -> core/option` 会导致 `libp2p_quic_tls_smoke` 的 `unresolved_import_count=1`；补完后，用 fresh `cheng.stage2` 真编这条 smoke，`unresolved_import_count` 已经压到 `0`。 |
| `ptr == nil` 比较 ABI | 已完成 | 同文件 `v3_codegen_compare_expr_scalar(...)` 已把 `ptr == nil` 收成按 `ptr` 发射，不再把 `strDataPtr(s)` 错当 `i64` 去比较。首次 fresh 定向编译时，`std/system::strToCStringTemp` 的首个真缺口已从 `if strDataPtr(s) == nil:` 前移到后面的 `if bitandCompat(s.flags, chengStrFlagOwned) != 0:`。 |
| 试探改动回退 | 已完成 | 为了继续前推 `bitandCompat(...) != 0`，试过一版“窄标量统一走 `w` 比较”的 compare lowering；这版会让 fresh `stage1/2/3` 在 `libp2p_quic_tls_smoke` 上直接崩溃，所以已回退，没有留在工作树。 |
| 当前新阻塞 | 进行中 | 当前最真实的新问题不是导入或 `ptr` 比较，而是 fresh `stage1/2/3` 在 `libp2p_quic_tls_smoke` 上会直接崩进 `libsystem_c::__vfprintf`；同一份 fresh `cheng.stage2` 对 [libp2p_host_smoke.cheng](/Users/lbcheng/cheng-lang/v3/src/tests/libp2p_host_smoke.cheng) 仍能稳定编过，说明这是 QUIC/TLS 深闭包上的 self-host compiler 诊断/unsupported 打印稳定性问题。 |
| 开始时间 | `2026-04-11 20:06 +0800` | 继续并行推进 `libp2p_quic_tls_smoke`，把前沿从 `std/os/std/times` 压进 QUIC/TLS 主体。 |
| `std/os` blocker 清空 | 已完成 | [src/std/os.cheng](/Users/lbcheng/cheng-lang/src/std/os.cheng) 已去掉 `openImpl*` 的 `str mode` 局部绑定，并把 `udpRecvFromFdAddrEx(...)` 改成显式 `payloadLen/addrCap/payloadPtr/addrPtr` 路径。最新定向编译里，首个 unsupported 已不再落在 `std/os`。 |
| `std/times` blocker 清空 | 已完成 | [src/std/times.cheng](/Users/lbcheng/cheng-lang/src/std/times.cheng) 现在通过新增的 `cheng_epoch_time_seconds()` 走整数秒主线；对应 runtime/native、stdio bridge、selflink backend helper 都已同步补齐。最新定向编译里，`std/times::epochTimeSeconds` 已从第一处 unsupported 消失。 |
| seed prepare 补丁 | 已完成 | [cheng_v3_seed.c](/Users/lbcheng/cheng-lang/v3/bootstrap/cheng_v3_seed.c) 已补 builtin cast prepare 特判，并补 `param_is_var[i]` 不建复合 temp 的规则。`build_backend_driver_v3.sh` 持续前台通过。 |
| QUIC 前沿前移 | 已完成 | `DIAG_CONTEXT=1 /Users/lbcheng/cheng-lang/artifacts/v3_backend_driver/cheng system-link-exec --root /Users/lbcheng/cheng-lang/v3 --in /Users/lbcheng/cheng-lang/v3/src/tests/libp2p_quic_tls_smoke.cheng --emit exe --target arm64-apple-darwin --out /tmp/libp2p_quic_tls_smoke.host` 最新已把首个 unsupported 前移到 [native_runtime.cheng](/Users/lbcheng/cheng-lang/v3/src/quic/native_runtime.cheng) 的 `msquicNativeSendClientHello`。 |
| 当前真 blocker | 进行中 | 当前卡点是 `msquicTls13BuildClientHello(...)` 与 `msquicTls13BuildEncryptedExtensions(...)` 的 prepare expr state，已经正式进入 `native_runtime + tls/crypto` 主体，不再是宿主 `os/time` 入口层。 |
