# 当前进度

- `bootstrap/cheng_seed.c` 已完成命名清理，并能用 `cc -std=c11 -O2 -Wall -Wextra -pedantic` 构建 `artifacts/bootstrap/cheng.stage0`。
- `artifacts/bootstrap/cheng.stage0 bootstrap-bridge` 已通过。
- `artifacts/bootstrap/cheng.stage3 status` 已确认主入口指向 `src/core/tooling/compiler_main.cheng`。
- `src/bootstrap/tests` 下源码与文件名已扫清旧版本标识。
- `src/core/tooling/gate_main.cheng` 不再把 `chain_node` 编译和三节点 smoke 当核心门禁。
- `src/tests/backend_driver_command_surface_smoke.cheng` 不再强制构建 `chain_node`。
- `src/core/tooling/path.cheng` 已删掉机械迁移留下的同名递归 wrapper。
- `tools/r2c/experimental` 已改用根 `tools/r2c/experimental`、`artifacts/bootstrap`、`artifacts/backend_driver` 和 `R2C_REACT_*` 环境变量。
- 最小验证已通过：`os_join_path_probe_min`、`os_get_current_dir_smoke`、`os_dir_exists_smoke`、`os_file_exists_smoke`、`compiler_runtime_path_probe`。
- `build-backend-driver` / `follow-world` 自托管安装路径已补实时进度、默认 8GB RSS 硬门禁、no-handoff 子进程环境；`system-link-exec` 和 compiler progress 也补了自身 RSS 超限 `exit(125)`。
- `compiler_csg`、`compiler_world_libp2p` 的本地 line-value helper 已改成私有唯一名，避免和 `compiler_world.CompilerWorldLineValue` 生成重复全局符号。
- `bootstrap/cheng_seed.c` 已补当前 self compiler 选择逻辑：provider object 物化优先使用正在运行的 self executable，不再因源码 mtime 变化把新 exe smoke 卡死。
- `bootstrap/cheng_seed.c` 已补 C seed 级 RSS 守卫；`artifacts/bootstrap/cheng.stage3` 直接 `system-link-exec` 也会按 `CHENG_PROCESS_MAX_RSS_BYTES` 超限退出 125。
- `bootstrap/cheng_seed.c` 的 `build-backend-driver` 已同步默认进度、默认 8GiB RSS 上限、禁 incremental/module/primary/quick cache，并改用 live logged runner 实时透出 `compile_progress`。
- `artifacts/bootstrap/cheng.stage0 -> stage3` 已重新刷新到 `bootstrap/cheng_seed.c` 最新实现。
- `artifacts/bootstrap/cheng.stage3 build-backend-driver` 已完成受控刷新，新 `artifacts/backend_driver/cheng` 生成时间为 2026-04-23 15:56:56，入口为 `src/core/tooling/backend_driver_main.cheng`。
- 新 backend driver 已通过最小核心 smoke：`backend_driver_resource_guard_contract_smoke`、`host_ops_logged_run_smoke`、`compiler_world_line_value_symbol_smoke`。
- 最小验证已通过：`backend_driver_resource_guard_contract_smoke`、`host_ops_logged_run_smoke`、`compiler_world_smoke`、`compiler_world_line_value_symbol_smoke`、`CHENG_PROCESS_MAX_RSS_BYTES=1` 自杀守卫探针。
- 旧根 shared builtin、空语义目录、根级 tooling 目录已移除；ABI 合同和 tooling 说明已迁到 `src/core/tooling`。
- 旧驼峰应用目录已并入 `src/apps/wow_export`，导入路径改为 `cheng/apps/wow_export/...`。
- 已通过验证：`shared_builtin_import_external_package_smoke`、`parser_path_smoke`、`cheng_skill_consistency_smoke`、`dev_hotpatch_100ms_scope_contract_smoke`、`wow_export_md5_smoke`、`wow_export_salsa20_smoke`、`wow_export_blte_smoke`、`wow_export_northshire_mvp_smoke`。
- 残留扫描已清零：旧包导入、旧包 ID、旧 artifact 前缀、旧 C 符号前缀、旧环境变量前缀、旧 module prefix、根级旧 tooling 目录、旧驼峰应用目录、旧 decentralized 目录。
- seed 最小化第一步已落地：新增 `src/core/tooling/host_smoke_gate.cheng`，`artifacts/backend_driver/cheng run-host-smokes <smoke...>` 由 Cheng host smoke gate 执行，不再进入 C seed 的 `run-host-smokes` 控制面。
- `src/core/tooling/gate_main.cheng` 已去掉旧 `stage1/frontend_lib` 导入，export visibility 校验改为直接编译运行三个 smoke。
- 新 backend driver 已刷新，并通过 `parser_path_smoke`、`backend_driver_command_surface_smoke`、`shared_builtin_import_external_package_smoke` 的 Cheng host smoke gate 验证。
- libp2p 跨模块重复导出已清理：`kad` 底层 provide 改为 `Libp2pKadStateProvide`，`store_sync` 底层 query/put/serve 改为 `Libp2pStoreEntries*` / `Libp2pStoreSyncServePayload`，`host_quic` host 层 helper 改为 `Libp2pHostQuicRequestResponse` / `Libp2pQuicSyncRequest`。
- 已通过主线 gate：`libp2p_register_handler_symbol_smoke`、`libp2p_host_smoke`、`compiler_managed_dependency_mirror_smoke`、`compiler_world_libp2p_smoke`、`compiler_world_fresh_node_selfhost_smoke`、`compiler_migration_multihop_smoke`。
- seed 最小化第二步已落地：新增 `src/core/tooling/world_receipt_gate.cheng`，`artifacts/backend_driver/cheng world-receipt` 由纯 Cheng 控制面生成 world/receipt 报告，不再转发到 C seed 的 `system-link-exec`。该命令属于 world/proof 侧链，不属于核心编译门禁。
- `world_receipt_gate` 的 staging 已改成最小源码快照，只复制 `cheng-package.toml`、可选 `cheng.lock.toml` 和 `src/`，不再复制整个仓库根。
- `bootstrap/cheng_seed.c` 的 backend driver freshness 输入已加入 `world_receipt_gate.cheng`，并重新刷新 `artifacts/bootstrap/cheng.stage0 -> stage3`。
- 已通过验证：`cc -std=c11 -Wall -Wextra -pedantic -fsyntax-only bootstrap/cheng_seed.c`、`artifacts/bootstrap/cheng.stage0 bootstrap-bridge`、临时 backend driver 编译、临时/正式 `world-receipt` 探针、正式 driver `status/help`、backend driver 与 stage3 二进制旧版本字符串扫描。
- 这次未宣称 `system-link-exec` 全量迁移；object materialize、provider object materialize、native link 仍在 C seed 路径内，后续要按 materializer 小模块继续拆。
- `backend_driver` 命令面已按新口径刷新：`core commands` 只列 `status, print-build-plan, system-link-exec`，`world-receipt` 单独列为 `world/proof commands`。
- 最新正式 driver 已安装到 `artifacts/backend_driver/cheng`，并通过 `parser_path_smoke`、`backend_driver_command_surface_smoke`、正式 `world-receipt` 显式探针、源码残留扫描和二进制残留扫描。
- `bootstrap/cheng_seed.c build-backend-driver` 已去掉候选 driver 的额外 `world-receipt` 收据门禁；backend driver 编译报告和 zero-exit smoke 报告都改由真实 `system-link-exec --report-out` 生成。
- `bootstrap/cheng_seed.c build-backend-driver` 已同步安装 backend driver line map：`cheng.build_candidate.map -> cheng.next.map -> cheng.map`，避免新二进制配旧 map。
- 已通过最终验证：`bootstrap-bridge`、`build-backend-driver` 真重建、`build-backend-driver` ready 快路径、`parser_path_smoke`、`backend_driver_command_surface_smoke`、源码残留扫描、路径名残留扫描、backend driver/stage3 二进制残留扫描、相关 diff whitespace 检查。
