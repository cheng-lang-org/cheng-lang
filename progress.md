# 当前进度

- `bootstrap/cheng_seed.c` 已完成命名清理，并能用 `cc -std=c11 -O2 -Wall -Wextra -pedantic` 构建 `artifacts/bootstrap/cheng.stage0`。
- `artifacts/bootstrap/cheng.stage0 bootstrap-bridge` 已通过。
- `artifacts/bootstrap/cheng.stage3 status` 已确认主入口指向 `src/core/tooling/compiler_main.cheng`。
- `src/bootstrap/tests` 下源码与文件名已扫清旧版本标识。
- `src/core/tooling/gate_main.cheng` 不再把 `chain_node` 编译和三节点 smoke 当核心门禁。
- `src/tests/backend_driver_command_surface_smoke.cheng` 不再强制构建 `chain_node`。
- `src/core/tooling/path.cheng` 已删掉机械迁移留下的同名递归 wrapper。
- `tools/r2c/experimental` 已改用根 `tools/r2c/experimental`、`artifacts/bootstrap`、`artifacts/backend_driver` 和 `R2C_REACT_*` 环境变量。
- 最小验证已通过：`os_join_path_probe_min`、`os_get_current_dir_smoke`、`os_dir_exists_smoke`、`os_file_exists_smoke`、`compiler_runtime_path_probe`。
- `build-backend-driver` / `follow-world` 自托管安装路径已补实时进度、默认 8GB RSS 硬门禁、no-handoff 子进程环境；`system-link-exec` 和 compiler progress 也补了自身 RSS 超限 `exit(125)`。
- `compiler_csg`、`compiler_world_libp2p` 的本地 line-value helper 已改成私有唯一名，避免和 `compiler_world.CompilerWorldLineValue` 生成重复全局符号。
- `bootstrap/cheng_seed.c` 已补当前 self compiler 选择逻辑：provider object 物化优先使用正在运行的 self executable，不再因源码 mtime 变化把新 exe smoke 卡死。
- `bootstrap/cheng_seed.c` 已补 C seed 级 RSS 守卫；`artifacts/bootstrap/cheng.stage3` 直接 `system-link-exec` 也会按 `CHENG_PROCESS_MAX_RSS_BYTES` 超限退出 125。
- `bootstrap/cheng_seed.c` 的 `build-backend-driver` 已同步默认进度、默认 8GiB RSS 上限、禁 incremental/module/primary/quick cache，并改用 live logged runner 实时透出 `compile_progress`。
- `artifacts/bootstrap/cheng.stage0 -> stage3` 已重新刷新到 `bootstrap/cheng_seed.c` 最新实现。
- `artifacts/bootstrap/cheng.stage3 build-backend-driver` 已完成受控刷新，新 `artifacts/backend_driver/cheng` 生成时间为 2026-04-23 15:56:56，入口为 `src/core/tooling/backend_driver_main.cheng`。
- 新 backend driver 已通过最小核心 smoke：`backend_driver_resource_guard_contract_smoke`、`host_ops_logged_run_smoke`、`compiler_world_line_value_symbol_smoke`。
- 最小验证已通过：`backend_driver_resource_guard_contract_smoke`、`host_ops_logged_run_smoke`、`compiler_world_smoke`、`compiler_world_line_value_symbol_smoke`、`CHENG_PROCESS_MAX_RSS_BYTES=1` 自杀守卫探针。
- 旧根 shared builtin、空语义目录、根级 tooling 目录已移除；ABI 合同和 tooling 说明已迁到 `src/core/tooling`。
- 旧驼峰应用目录已并入 `src/apps/wow_export`，导入路径改为 `cheng/apps/wow_export/...`。
- 已通过验证：`shared_builtin_import_external_package_smoke`、`parser_path_smoke`、`cheng_skill_consistency_smoke`、`dev_hotpatch_100ms_scope_contract_smoke`、`wow_export_md5_smoke`、`wow_export_salsa20_smoke`、`wow_export_blte_smoke`、`wow_export_northshire_mvp_smoke`。
- 残留扫描已清零：旧包导入、旧包 ID、旧 artifact 前缀、旧 C 符号前缀、旧环境变量前缀、旧 module prefix、根级旧 tooling 目录、旧驼峰应用目录、旧 decentralized 目录。
- seed 最小化第一步已落地：新增 `src/core/tooling/host_smoke_gate.cheng`，`artifacts/backend_driver/cheng run-host-smokes <smoke...>` 由 Cheng host smoke gate 执行，不再进入 C seed 的 `run-host-smokes` 控制面。
- `src/core/tooling/gate_main.cheng` 已去掉旧 `stage1/frontend_lib` 导入，export visibility 校验改为直接编译运行三个 smoke。
- 新 backend driver 已刷新，并通过 `parser_path_smoke`、`backend_driver_command_surface_smoke`、`shared_builtin_import_external_package_smoke` 的 Cheng host smoke gate 验证。
- libp2p 跨模块重复导出已清理：`kad` 底层 provide 改为 `Libp2pKadStateProvide`，`store_sync` 底层 query/put/serve 改为 `Libp2pStoreEntries*` / `Libp2pStoreSyncServePayload`，`host_quic` host 层 helper 改为 `Libp2pHostQuicRequestResponse` / `Libp2pQuicSyncRequest`。
- 已通过主线 gate：`libp2p_register_handler_symbol_smoke`、`libp2p_host_smoke`、`compiler_managed_dependency_mirror_smoke`、`compiler_world_libp2p_smoke`、`compiler_world_fresh_node_selfhost_smoke`、`compiler_migration_multihop_smoke`。
- seed 最小化第二步已落地：新增 `src/core/tooling/world_receipt_gate.cheng`，`artifacts/backend_driver/cheng world-receipt` 由纯 Cheng 控制面生成 world/receipt 报告，不再转发到 C seed 的 `system-link-exec`。该命令属于 world/proof 侧链，不属于核心编译门禁。
- `world_receipt_gate` 的 staging 已改成最小源码快照，只复制 `cheng-package.toml`、可选 `cheng.lock.toml` 和 `src/`，不再复制整个仓库根。
- `bootstrap/cheng_seed.c` 的 backend driver freshness 输入已加入 `world_receipt_gate.cheng`，并重新刷新 `artifacts/bootstrap/cheng.stage0 -> stage3`。
- 已通过验证：`cc -std=c11 -Wall -Wextra -pedantic -fsyntax-only bootstrap/cheng_seed.c`、`artifacts/bootstrap/cheng.stage0 bootstrap-bridge`、临时 backend driver 编译、临时/正式 `world-receipt` 探针、正式 driver `status/help`、backend driver 与 stage3 二进制旧版本字符串扫描。
- 这次未宣称 `system-link-exec` 全量迁移；object materialize、provider object materialize、native link 仍在 C seed 路径内，后续要按 materializer 小模块继续拆。
- `backend_driver` 命令面已按新口径刷新：`core commands` 只列 `status, print-build-plan, system-link-exec`，`world-receipt` 单独列为 `world/proof commands`。
- 最新正式 driver 已安装到 `artifacts/backend_driver/cheng`，并通过 `parser_path_smoke`、`backend_driver_command_surface_smoke`、正式 `world-receipt` 显式探针、源码残留扫描和二进制残留扫描。
- `bootstrap/cheng_seed.c build-backend-driver` 已去掉候选 driver 的额外 `world-receipt` 收据门禁；backend driver 编译报告和 zero-exit smoke 报告都改由真实 `system-link-exec --report-out` 生成。
- `bootstrap/cheng_seed.c build-backend-driver` 已同步安装 backend driver line map：`cheng.build_candidate.map -> cheng.next.map -> cheng.map`，避免新二进制配旧 map。
- 已通过最终验证：`bootstrap-bridge`、`build-backend-driver` 真重建、`build-backend-driver` ready 快路径、`parser_path_smoke`、`backend_driver_command_surface_smoke`、源码残留扫描、路径名残留扫描、backend driver/stage3 二进制残留扫描、相关 diff whitespace 检查。
- 已补最小边界审计文件 `src/tests/seed_minimization_boundary_smoke.cheng`，专门盯 `help` 不把 `world-receipt` 算进 core commands，并确认 `print-line-map` 走 backend driver 直接路径时会吐出 `cheng_line_map_v1`、`entry_count=` 和源文件路径。
- seed 最小化第三步已落地：新增 `src/core/backend/line_map.cheng`，backend driver 的 `print-line-map` 由纯 Cheng 构建 line-map 文本并支持 `--report-out`，不再转发到 C seed。
- `src/core/backend/build_plan.cheng` 和 `bootstrap/cheng_seed.c` 的 backend driver freshness 输入已纳入 `src/core/backend/line_map.cheng`，`build_source_unit_count` 刷新为 23。
- 并行子代理已补两块后续切片雏形：`src/core/tooling/debug_tools_gate.cheng` 生成纯 Cheng debug/symbol 文本，`src/core/backend/native_link_exec.cheng` 生成 Darwin arm64 native linker argv 并拒绝非目标 fallback。
- 已通过验证：临时 backend driver 编译、正式 `build-backend-driver`、正式 `print-line-map --report-out`、`backend_driver_command_surface_smoke`、`seed_minimization_boundary_smoke`、`debug_tools_gate_text_smoke`、`native_link_exec_command_smoke`。
- seed 最小化第四步已落地：`src/core/tooling/debug_tools_gate.cheng` 已轻量化为 `system_link_plan + lowering_plan + primary_object_plan` 文本生成，不再导入完整 `system_link_exec`。
- `artifacts/backend_driver/cheng debug-report` 和 `print-symbols` 已由 backend driver 直接执行，并支持 `--report-out`；命令面更新为 `debug commands: debug-report, print-symbols, print-line-map`。
- `bootstrap/compiler_bootstrap_manifest.cheng`、`bootstrap/cheng_seed.c`、`src/core/backend/build_plan.cheng` 已同步纳入 `line_map.cheng` 和 `debug_tools_gate.cheng`，build plan source 范围刷新到 `source[0]..source[23]` / `build_source_unit_count=24`。
- 已通过验证：`cc -std=c11 -Wall -Wextra -pedantic -fsyntax-only bootstrap/cheng_seed.c`、`cc -std=c11 -O2 -Wall -Wextra -pedantic bootstrap/cheng_seed.c -o artifacts/bootstrap/cheng.stage0`、`artifacts/bootstrap/cheng.stage0 bootstrap-bridge`、`artifacts/bootstrap/cheng.stage3 build-backend-driver` 真重建和 ready 快路径、正式 `debug-report` / `print-symbols` / `print-line-map` 探针、`backend_driver_command_surface_smoke`、`seed_minimization_boundary_smoke`、`debug_tools_gate_text_smoke`、`native_link_exec_command_smoke`、`debug_tools_surface_smoke`、源码/路径名/二进制旧版本标识扫描。
- world/canonical CSG 口径已修正：`system_link_exec`、manual universe smoke、bundle verify 均使用 `canonicalGraphCid` 作为 world `csg_root_cid`，raw `graphCid` 只保留为报告/receipt raw 字段。
- CSG source closure 已改为按 module path 稳定排序，修复同内容换 root 后 manifest/world head 漂移，同时避免巨型 expr/typed row 排序导致的 `compiler_csg` 性能退化。
- 最新验证已通过：`compiler_world_manifest_closure_smoke`、`compiler_migration_publish_smoke`、`compiler_world_libp2p_smoke`、`compiler_world_fresh_node_selfhost_smoke`、`compiler_managed_dependency_mirror_smoke`、`compiler_migration_multihop_smoke`、`libp2p_register_handler_symbol_smoke`。
- 刷新后的正式 `artifacts/backend_driver/cheng` 已重新跑过同一批 world/libp2p/selfhost gate：`compiler_migration_publish_smoke`、`compiler_world_libp2p_smoke`、`compiler_world_fresh_node_selfhost_smoke`、`compiler_managed_dependency_mirror_smoke`、`compiler_migration_multihop_smoke`、`libp2p_register_handler_symbol_smoke` 均通过。
- line-map sidecar write 已从 backend driver `system-link-exec` 路径迁到 Cheng 侧：driver 转发 stage3 时设置内部 `CHENG_SKIP_SEED_LINE_MAP=1`，链接成功后调用 `src/core/backend/line_map.cheng::WriteExecutableLineMap` 写 `<out>.map`，并保证 `line_map done` 先于 `build done`。
- 已刷新 `artifacts/bootstrap/cheng.stage0 -> stage3` 和正式 `artifacts/backend_driver/cheng`；验证已通过：`cc -std=c11 -Wall -Wextra -pedantic -fsyntax-only bootstrap/cheng_seed.c`、`bootstrap-bridge`、`build-backend-driver`、`backend_driver_command_surface_smoke`、`seed_minimization_boundary_smoke`。
- backend driver 已新增 `verify-debug-tools`，直接调用 Cheng host smoke gate 验证 `debug_tools_surface_smoke`；新安装的 `artifacts/backend_driver/cheng` 已通过 `verify-debug-tools`、`backend_driver_command_surface_smoke`、`seed_minimization_boundary_smoke` 和 `build-backend-driver` ready 快路径。
- `src/apps/wow_export` 已打通本机 CASC 的 BLTE 多块解码、encoding root/install key 查找、root 首块解析和 install manifest 解析；相关 smoke 与 `wow_export_tool_main probe` 已通过，北郡仍等待已审计 fileDataID manifest。
- 已定位并修复 seed backend 的嵌套调用结果保值踩外层参数槽问题：字符串比较和 arm64 调用清理路径不再把返回值暂存到裸 `call_arg_base`，改为按 `call_depth` 隔离的 result spill；`artifacts/bootstrap/cheng.stage0 -> stage3` 与正式 `artifacts/backend_driver/cheng` 已重新刷新。
- 已通过回归：`str_return_bridge_probe`、`str_concat_lowering_smoke`、`std_file_io_smoke`、`build_backend_driver_report_smoke`、`str_concat_call_arg_probe`、`str_concat_call_arg_ret_probe`、`str_concat_cross_module_probe`、`str_concat_lowering_shape_probe`。
- pure `system-link-exec` 已接入纯 Cheng Mach-O direct object writer：standalone zero-exit exe 和 return-zero `emit:obj` 均不再生成 `.s` 或调用 `cc -c` 物化 primary object，报告写出 `primary_object_direct=1`、`direct_object_bytes`、`direct_object_symbol_count`、`direct_object_reloc_count`。
- 最新正式 `artifacts/backend_driver/cheng` 已刷新；验证通过：`build-backend-driver`、`ordinary_zero_exit_fixture` exe 直写运行、`ordinary_zero_exit_fixture --emit:obj` 直写、无 `.s/.compile.log` 回潮检查。当时 `float64_mul_backend_smoke` 仍 hard-fail 于 `primary_object_body_semantics_missing`；后续已由最小真实退出码语义打通。
- pure Mach-O writer 已打开 ARM64 BR26 relocation 写入，`ordinary_call_chain_fixture --emit:obj` 现在直写 `.o`，报告为 `primary_object_direct=1` / `direct_object_reloc_count=1`，`otool -r -v` 显示 relocation 指向 Leaf 符号，且无 `.s/.compile.log` sidecar。
- `build-backend-driver` ready freshness 已同步 direct object writer 5 个源、support matrix 和 build plan 中的 tooling 源；修改 `direct_object_emit.cheng` 这类文件后不再误走 ready 快路径。
- 已复核 `build-backend-driver` 锁边界：candidate 编译、candidate 自检、plan 对比、zero-exit 自测都在 `artifacts/backend_driver/builds/pid-*` 私有目录完成；只在最终 `cheng.next -> artifacts/backend_driver/cheng` 与 `.map` rename 安装阶段拿 `.build_backend_driver.lock`。
- seed 最小化边界已落到 backend driver 内置 smoke：`direct_object_writer_smoke` 断言 standalone zero-exit 走 `primary_object_direct=1`，`seed_minimization_boundary_smoke` 断言隐藏 C seed command/env 不外露，并覆盖 system-link-exec cache prime/restore。
- provider object cache 与 system-link-exec cache 的 codegen key 已加入纯 Cheng backend driver、runtime、direct object writer 相关模块；不能再只用 `bootstrap/cheng_seed.c` 代表 codegen 身份。
- 最新正式 `artifacts/backend_driver/cheng` 已重新刷新；验证通过：`artifacts/bootstrap/cheng.stage3 build-backend-driver`、`artifacts/backend_driver/cheng run-host-smokes direct_object_writer_smoke seed_minimization_boundary_smoke`、`artifacts/backend_driver/cheng verify-export-visibility`、`cc -std=c11 -Wall -Wextra -pedantic -fsyntax-only bootstrap/cheng_seed.c`、`bootstrap/cheng_seed.c` 旧版本标识扫描。
- C seed 活命令面已收窄：`CHENG_NO_BACKEND_DRIVER_HANDOFF=1 artifacts/bootstrap/cheng.stage3 print-asm` 现在硬失败为 `non-bootstrap command requires backend driver handoff`，`__seed-*` 隐藏命令仍保持 `unknown command`；非自举命令不再回落到 C 旧实现。
- 已重新刷新 `artifacts/bootstrap/cheng.stage0 -> stage3` 和正式 `artifacts/backend_driver/cheng`；验证通过：`bootstrap-bridge`、`build-backend-driver`、`direct_object_writer_smoke`、`seed_minimization_boundary_smoke`、`verify-export-visibility`、C seed 语法检查、旧版本标识扫描、临时 cache fixture 残留扫描。
- pure direct object writer 已新增最小 `float64 * float64` 函数对象形状：`fn MulF64(a: float64, b: float64): float64 = return a * b` 会进入 `return_f64_mul_args`，直接写入 `fmul d0, d0, d1; ret`，不生成 `.s/.compile.log`。
- `direct_object_writer_smoke` 已内置 f64 对象编译和 `otool -tV` 断言；`bootstrap/stage1_bootstrap.cheng` 的 `supported_commands` 已和 C seed v2 合同同步为 `build-backend-driver`，stage0/stage3 self-check 均通过。
- 最新验证已通过：`cc -std=c11 -Wall -Wextra -pedantic -fsyntax-only bootstrap/cheng_seed.c`、`cc -std=c11 -O2 -Wall -Wextra -pedantic bootstrap/cheng_seed.c -o artifacts/bootstrap/cheng.stage0`、`artifacts/bootstrap/cheng.stage0 bootstrap-bridge`、`artifacts/bootstrap/cheng.stage3 build-backend-driver`、`artifacts/backend_driver/cheng run-host-smokes direct_object_writer_smoke`、`artifacts/backend_driver/cheng run-host-smokes seed_minimization_boundary_smoke`、`artifacts/bootstrap/cheng.stage0 self-check`、`artifacts/bootstrap/cheng.stage3 self-check`、`git diff --check`。
- C seed 物理最小化继续推进：已删除约 200 个不可达 `static` 旧命令/辅助函数，`bootstrap/cheng_seed.c` 缩到 64824 行，`cc -std=c11 -Wall -Wextra -pedantic -fsyntax-only bootstrap/cheng_seed.c` 无警告。
- 已用 `compile-bootstrap --in:bootstrap/stage1_bootstrap.cheng` 生成带嵌入合同的 `artifacts/bootstrap/cheng.stage0`，随后 `bootstrap-bridge`、`stage3 self-check`、`stage3 build-backend-driver`、`direct_object_writer_smoke`、`seed_minimization_boundary_smoke`、`verify-export-visibility` 全部通过。
- standalone no-runtime direct path 已修成全函数发射：入口函数必须是 `return_zero_i32`，其它真实函数只要已有严格 codegen 且内部 return-call relocation 都能在同一对象内解析，就会和 `_main` 一起写入 Mach-O；`main return 0` 后面挂 `float64 * float64` 函数现在会保留 `fmul d0, d0, d1`，不再被丢弃成假成功。
- pure direct object writer 已把 `entry_bridge` 的真实 arm64 指令数记为 17，并补了 entry bridge 机器码/BR26 relocation 生成；runtime 暂不启用非 standalone exe 直写，因为 provider object materializer 还没有完全迁出 C seed，贸然打开会在 runtime provider 编译处硬失败。
- `direct_object_writer_smoke` 已覆盖三条边界：standalone zero-exit 直写、`float64 * float64 -> float64` 对象函数直写并由 `otool -tV` 看到 `fmul d0, d0, d1`、dead-f64 exe 成功直写运行且 primary object 里保留 f64 函数机器码。
- 最新验证已通过：`artifacts/bootstrap/cheng.stage3 build-backend-driver`、`artifacts/backend_driver/cheng run-host-smokes direct_object_writer_smoke`、`artifacts/backend_driver/cheng run-host-smokes float64_mul_direct_object_smoke`、`artifacts/backend_driver/cheng run-host-smokes seed_minimization_boundary_smoke`、`artifacts/backend_driver/cheng verify-export-visibility`、`git diff --check`、dead-f64 primary object `otool -tV` 可见 `_main`、原始 main 和 `fmul d0, d0, d1`。
- `float64_mul_backend_smoke` 已收窄成最小真实退出码语义：`1.5 * 2.0 == 3.0` 返回 0，否则返回 1；pure direct executable 现在生成 `_main` 的 `fmul/fcmp/b.ne` 机器码并运行通过，不再走 provider object 或 `.s` 汇编文本。
- 最新验证已通过：`artifacts/bootstrap/cheng.stage3 build-backend-driver`、`artifacts/backend_driver/cheng run-host-smokes direct_object_writer_smoke seed_minimization_boundary_smoke float64_mul_direct_object_smoke float64_mul_backend_smoke`、`artifacts/backend_driver/cheng verify-export-visibility`、`cc -std=c11 -Wall -Wextra -pedantic -fsyntax-only bootstrap/cheng_seed.c`、`otool -tV /tmp/float64_mul_backend_smoke.primary.o` 中可见 `fmul`/`fcmp`。
- standalone no-runtime direct executable 已支持最小真实 call-chain：`int32_const_return_direct_fixture` 现在直写 Mach-O primary object，`_main` 通过 BR26 跳到 Cheng entry，entry 再 BR26 跳到 `ReturnSeven`；报告为 `system_link_exec_runtime_standalone_no_runtime=1`、`primary_object_body_kinds=return_i32_const_7,return_call_noarg_i32,entry_bridge`、`primary_object_direct=1`、`direct_object_symbol_count=3`、`direct_object_reloc_count=2`、`provider_object_count=0`，且不生成 `.s/.compile.log`。
- 最新验证已通过：`artifacts/bootstrap/cheng.stage3 build-backend-driver`、`artifacts/backend_driver/cheng run-host-smokes direct_object_writer_smoke`、`artifacts/backend_driver/cheng run-host-smokes float64_mul_direct_object_smoke`、`artifacts/backend_driver/cheng run-host-smokes seed_minimization_boundary_smoke`、`artifacts/backend_driver/cheng verify-export-visibility`、`git diff --check`；`otool -rv artifacts/hostrun/int32_const_return_direct_fixture.primary.o` 可见两条 `BR26` relocation。
- pure direct object writer 已新增最小二参 `int32 + int32 -> int32` 对象函数形状：`src/tests/int32_add_direct_object_smoke.cheng` 进入 `return_i32_add_args`，直接写入 `add w0, w0, w1; ret`，不生成 `.s/.compile.log`。
- 最新验证已通过：`artifacts/bootstrap/cheng.stage3 build-backend-driver`、`artifacts/backend_driver/cheng run-host-smokes direct_object_writer_smoke`、`git diff --check`；`artifacts/hostrun/int32_add_direct_object_smoke.o.report.txt` 显示 `primary_object_body_kinds=return_i32_add_args`、`primary_object_direct=1`、`direct_object_reloc_count=0`，`otool -tV artifacts/hostrun/int32_add_direct_object_smoke.o` 可见 `add w0, w0, w1`。
- `return_i32_const_N` 已作为 standalone no-runtime direct entry 的正式最小语义接入：`int32_const_return_direct_fixture` 直写 Mach-O 后退出码为 7，`_main` 通过 BR26 跳到 Cheng `main`，再跳到 `ReturnSeven`，函数体为 `mov w0, #0x7; ret`。
- runtime report 已把旧 zero-exit 字段改名为 `system_link_exec_runtime_standalone_direct_entry`，避免常量 7 这类非零退出码路径还被命名成 zero-exit。
- 最新验证已通过：`artifacts/backend_driver/cheng run-host-smokes direct_object_writer_smoke`、`artifacts/backend_driver/cheng run-host-smokes seed_minimization_boundary_smoke float64_mul_direct_object_smoke float64_mul_backend_smoke`、`artifacts/backend_driver/cheng verify-export-visibility`、`cc -std=c11 -Wall -Wextra -pedantic -fsyntax-only bootstrap/cheng_seed.c`、相关 `git diff --check` 和旧 zero-exit 字段源码扫描。
- primary object plan 已把 `return expr` 与表达式体 `= expr` 归一到同一套严格 body kind：`= 7` 进入 `return_i32_const_7`，`= ReturnSeven()` 进入 `return_call_noarg_i32`，`= a + b` / `= a * b` 继续复用已存在的 i32/f64 direct 形状。
- 最新正式 `artifacts/backend_driver/cheng` 已刷新；验证通过：`build-backend-driver`、`direct_object_writer_smoke`、`seed_minimization_boundary_smoke`、`float64_mul_direct_object_smoke`、`float64_mul_backend_smoke`、`verify-export-visibility`、C seed 语法检查、手工 inline const/call-chain fixture 退出码 7 和 `system_link_exec_runtime_standalone_direct_entry=1` 报告。
- pure direct object writer 已新增最小二参 `int32 - int32 -> int32` 与 `int32 * int32 -> int32` 对象函数形状：`int32_sub_direct_object_smoke` 进入 `return_i32_sub_args` 并写入 `sub w0, w0, w1; ret`，`int32_mul_direct_object_smoke` 进入 `return_i32_mul_args` 并写入 `mul w0, w0, w1; ret`，两者均不生成 `.s/.compile.log`。
- 最新验证已通过：`artifacts/bootstrap/cheng.stage3 build-backend-driver`、`artifacts/backend_driver/cheng run-host-smokes direct_object_writer_smoke`、`artifacts/backend_driver/cheng run-host-smokes float64_mul_direct_object_smoke`、`artifacts/backend_driver/cheng run-host-smokes seed_minimization_boundary_smoke`、`artifacts/backend_driver/cheng verify-export-visibility`；`artifacts/hostrun/int32_sub_direct_object_smoke.o.report.txt` 和 `artifacts/hostrun/int32_mul_direct_object_smoke.o.report.txt` 均显示 `primary_object_direct=1`、`direct_object_reloc_count=0`，`otool -tV` 分别可见 `sub w0, w0, w1` 与 `mul w0, w0, w1`。
- pure direct object writer 已新增最小三参 `int32 * int32 + int32 -> int32` 乘加对象函数形状，并覆盖 `acc + lhs * rhs` 交换写法：`int32_madd_direct_object_smoke` 两个函数均进入 `return_i32_madd_args`，直接写入 `madd w0, w0, w1, w2; ret`，不生成 `.s/.compile.log`。
- 最新验证已通过：`artifacts/bootstrap/cheng.stage3 build-backend-driver`、`artifacts/backend_driver/cheng run-host-smokes direct_object_writer_smoke`、`artifacts/backend_driver/cheng run-host-smokes seed_minimization_boundary_smoke`、`artifacts/backend_driver/cheng run-host-smokes float64_mul_direct_object_smoke`、`artifacts/backend_driver/cheng verify-export-visibility`、`git diff --check`；`artifacts/hostrun/int32_madd_direct_object_smoke.o.report.txt` 显示 `primary_object_item_count=2`、`primary_object_body_kinds=return_i32_madd_args,return_i32_madd_args`、`primary_object_direct=1`、`direct_object_reloc_count=0`，`otool -tV` 可见 `MaddI32` 与 `MaddI32Commuted` 均为 `madd w0, w0, w1, w2; ret`。
- pure direct object writer 已新增最小二参 `int32 >> int32 -> int32` 算术右移对象函数形状：`int32_asr_direct_object_smoke` 进入 `return_i32_asr_args`，直接写入 `asr w0, w0, w1; ret`，不生成 `.s/.compile.log`。
- typed return-op 已从 `primary_object_plan` 下沉到 `typed_expr`/`lowering_plan`：`TypedExprReturnOpFact` 负责记录二参 add/sub/mul/asr、f64 mul 和三参 madd，lowering report 输出 `typed_return_op_*` 字段，primary object plan 只消费 typed facts 映射 body kind，不再本地解析算术返回表达式。
- 最新验证已通过：`artifacts/bootstrap/cheng.stage3 build-backend-driver`、`artifacts/backend_driver/cheng run-host-smokes direct_object_writer_smoke`、`artifacts/backend_driver/cheng run-host-smokes seed_minimization_boundary_smoke`、`artifacts/backend_driver/cheng run-host-smokes float64_mul_direct_object_smoke`、`artifacts/backend_driver/cheng run-host-smokes float64_mul_backend_smoke`、`artifacts/backend_driver/cheng verify-export-visibility`、`git diff --check`；`artifacts/hostrun/int32_asr_direct_object_smoke.o.report.txt` 显示 `typed_return_op_count=1`、`typed_return_op_kinds=i32_asr_args`、`primary_object_body_kinds=return_i32_asr_args`、`primary_object_direct=1`、`direct_object_reloc_count=0`，`artifacts/hostrun/int32_madd_direct_object_smoke.o.report.txt` 显示 `typed_return_op_count=2`、`typed_return_op_kinds=i32_madd_args,i32_madd_args`，`otool -tV` 可见 `asr w0, w0, w1`。
- `typed_expr` 已新增结构化 `TypedExprIr` 节点/函数表，`lowering_plan` 现在输出 `PrimaryObjectIR`，`primary_object_plan` 和 `line_map` 改为消费结构化 facts；primary object plan 不再读源码、拆行、扫描签名或猜 `return a + b`。
- `system_link_exec_runtime` 的 Darwin arm64 主路径已优先调用 `DirectObjectEmitPlanObject` 直写 Mach-O `.o`，`.s` 只保留在 fallback/debug 输出；`float64_mul_direct_object_smoke.o` 报告显示 `typed_ir_return_kinds=f64_mul`、`primary_object_ir_body_kinds=return_f64_mul_args`、`primary_object_direct=1`、`direct_object_reloc_count=0`，`otool -tV` 可见 `fmul d0, d0, d1; ret`。
- 已修复本轮规范回潮：`system_link_exec.cheng` 的新增字符串组合改用 `Fmt`，`typed_expr.cheng` 的两个自增空白扫描 `while` 改为 `for ... in range`。
- 最新正式 `artifacts/backend_driver/cheng` 已刷新到 2026-04-25 09:14:53；验证通过：`run-host-smokes direct_object_writer_smoke`、`seed_minimization_boundary_smoke`、`float64_mul_direct_object_smoke`、`float64_mul_backend_smoke`、`verify-export-visibility`、`git diff --check`。正常纯 self build 仍 hard-fail：`backend_driver_main::main => unsupported`，报告为 `primary_object_missing_reasons=primary_object_body_semantics_missing`、`object_missing_reasons=primary_object_not_ready_for_object_plan`、`native_link_missing_reasons=object_plan_not_ready_for_native_link`。
- `build-backend-driver` 性能已先行压回 2.5 分钟以内：C seed 的 `primary_object_emit` 直接复用 `primary_object_plan` 阶段缓存的 scalar 函数汇编文本，不再二次调用 `cheng_seed_try_emit_scalar_function`。最新正式重建 `artifacts/backend_driver/builds/pid-86922/build_backend_driver.compile.report.txt` 显示 `exec_phase_backend_total_ms=138992`，`exec_phase_primary_object_function_emit_ms=11`，对比优化前约 `206394ms/70843ms` 已去掉约 70 秒重复 codegen。
- 最新验证已通过：`cc -std=c11 -Wall -Wextra -pedantic -fsyntax-only bootstrap/cheng_seed.c`、`git diff --check`、`artifacts/bootstrap/cheng.stage3 bootstrap-bridge`、`artifacts/bootstrap/cheng.stage3 self-check`、`artifacts/bootstrap/cheng.stage3 build-backend-driver`、`artifacts/backend_driver/cheng run-host-smokes direct_object_writer_smoke`、`artifacts/backend_driver/cheng run-host-smokes seed_minimization_boundary_smoke float64_mul_direct_object_smoke float64_mul_backend_smoke`、`artifacts/backend_driver/cheng verify-export-visibility`。
- `build-backend-driver` 闭包已裁掉非最小 seed 主线 gate：backend driver 不再直接 import `seed_cross_target_gate/seed_debug_runtime_gate/seed_network_gate/seed_orphan_guard_gate/seed_r2c_gate/seed_stage23_libp2p_gate/wasm_smoke_gate/wasm_binary_audit`，这些显式领域命令保留为 stage3 转发，不进入核心 driver 自举闭包。
- 最新正式重建 `artifacts/backend_driver/builds/pid-30559/build_backend_driver.compile.report.txt`：`build_source_unit_count=37`、`source_closure_count=64`、`lowering_function_count=1476`、`exec_phase_lowering_plan_ms=47934`、`exec_phase_primary_object_plan_ms=54940`、`exec_phase_primary_object_emit_ms=6309`、`exec_phase_backend_total_ms=109314`，约 1分49秒。
- 最新验证已通过：`cc -std=c11 -Wall -Wextra -pedantic -fsyntax-only bootstrap/cheng_seed.c`、`git diff --check`、`artifacts/bootstrap/cheng.stage3 self-check`、`artifacts/bootstrap/cheng.stage3 build-backend-driver`、`artifacts/backend_driver/cheng verify-export-visibility`、`artifacts/backend_driver/cheng run-host-smokes direct_object_writer_smoke seed_minimization_boundary_smoke float64_mul_direct_object_smoke float64_mul_backend_smoke backend_driver_command_surface_smoke build_plan_report_smoke`。
- `primary_object_plan` root/reachability 已改为：`--emit:exe` 只从入口函数走内部 call 闭包，`--emit:obj` 以源内全部函数为 root；动态 pending `while` 已改为 bounded frontier `for ... in range`，不可达 unsupported 不再污染 object plan。
- primary object report 已补 `primary_object_reachable_function_count`、`primary_object_first_missing_function`、`primary_object_first_missing_body_kind`；backend driver 的 pure `system-link-exec` plan-not-ready 输出会同步打印首个不可发射函数。
- 本轮没有实现或修改 `docs/ai-native-gui-execution-semantics.md`；当前工作线仍是 Typed IR / PrimaryObjectIR / direct Mach-O object writer。
- 当前验证：`git diff --check` 通过；`artifacts/bootstrap/cheng.stage3 build-backend-driver --require-rebuild` 仍 hard-fail 于 pure self build，报告为 `primary_object_items_missing,primary_object_machine_words_missing,primary_object_body_semantics_missing`，根因仍是 `backend_driver_main::main`/命令分发未进入真实 codegen。尝试直接编跑 `primary_object_codegen_smoke` 也被同一 pure codegen 闭包限制挡住，不能用 C seed fallback 冒充通过。
- `strLenFast` 已进入结构化 `TypedExprIr -> PrimaryObjectIR`：typed/lowering report 分别输出 `typed_ir_str_len_fast_count=1`、`primary_object_ir_str_len_fast_count=1`，Darwin arm64 direct writer 写入 `ldr/cmp/csel/ret` 机器码，文本汇编 fallback 同步保留同形指令。
- 已修正 return-call lowering 语义：只有 `return Foo(...)` 或表达式体 `fn f(...) = Foo(...)` 的根表达式 fact 带 `returnRoot` 才能生成 `return_call_*` body kind；普通 `let value = Foo()` 不再被误判成返回调用。纯探针 `/tmp/cheng_non_return_call_probe/report.txt` 显示该 fixture 的 `primary_object_first_missing_function=main`，且 body kinds 只包含 `return_i32_const_7`。
- 最新 backend driver self probe `/tmp/cheng_backend_driver_probe_returnroot/report.txt`：`strLenFast` 仍在 IR，误报的 `BackendDriverRoot` 已消失，真实 blocker 前移到 `BackendDriverMain`，报告为 `primary_object_reachable_function_count=2`、`primary_object_first_missing_function=BackendDriverMain`、`primary_object_first_missing_body_kind=unsupported`。
- primary object plan 现在额外报告首个不可发射函数的结构化行号，不把源码文本塞进 `PrimaryObjectIR`。新增 `bodyLineText: str` 曾让 backend driver 候选编译在 `LoweringBuildPrimaryObjectIr` 复合返回处失败，已收窄为 `firstMissingLineNumber` 旁路字段。
- 最新验证：`CHENG_BUILD_BACKEND_DRIVER_FORCE_C_SEED=1 artifacts/bootstrap/cheng.stage3 build-backend-driver --require-rebuild` 成功刷新 `artifacts/backend_driver/cheng`；`/tmp/cheng_backend_driver_probe_line/report.txt` 显示 `typed_ir_str_len_fast_count=1`、`primary_object_ir_str_len_fast_count=1`、`primary_object_first_missing_function=BackendDriverMain`、`primary_object_first_missing_body_kind=unsupported`、`primary_object_first_missing_line_number=2878`。`primary_object_plan_smoke` 在纯 driver 下仍按预期 hard-fail 于自身 `main` 第 11 行，不能记作通过。
- typed IR 已新增最小 `let/var binding = Call(...)` 语句节点 `TypedExprIrOpLetCall`，lowering 映射为 `stmt_let_call`。backend driver self probe `/tmp/cheng_backend_driver_probe_stmt/report.txt` 显示 `typed_ir_let_call_count=284`、`primary_object_ir_stmt_let_call_count=289`，首个 blocker 已从笼统 `unsupported` 前进到 `BackendDriverMain` 第 2878 行 `stmt_let_call`。
- 最新验证：`CHENG_BUILD_BACKEND_DRIVER_FORCE_C_SEED=1 artifacts/bootstrap/cheng.stage3 build-backend-driver --require-rebuild` 成功；`artifacts/backend_driver/cheng run-host-smokes --label:stmt_let_call_regression direct_object_writer_smoke` 通过，确认新增 statement IR 没破坏已支持的 direct object writer 路径。
- 本轮恢复：撤销把 `stmt_*`/`return_expr` 诊断标签写入 `PrimaryObjectIR.bodyKind` 的改动，后台 `build-backend-driver --require-rebuild` 成功刷新 `artifacts/backend_driver/cheng`，报告为 `primary_object_unsupported_function_count=0`、`exec_phase_backend_total_ms=104139`。
- 本轮纯 driver 探针：`return_call_three_args_fixture --emit:obj` 通过，报告 `primary_object_body_kinds=return_i32_const_7,return_call_arg0_arg1_arg2_i32`、`direct_object_reloc_count=1`，`otool -rv` 可见 `BR26` 指向 `Leaf3`；`ordinary_zero_exit_fixture --emit:exe` 通过并运行退出 0，报告 `system_link_exec_runtime_standalone_direct_entry=1`、`direct_object_reloc_count=0`。
- 最新纯 self-build 边界：`artifacts/backend_driver/cheng system-link-exec --in:src/core/tooling/backend_driver_main.cheng --emit:exe` 仍硬失败在 `BackendDriverMain` 第 2878 行，`primary_object_reachable_function_count=2`、`primary_object_first_missing_body_kind=unsupported`；下一步只能补真实 statement/control-flow/scalar call codegen，不能用特殊 main/stub/C seed fallback。
- 已修复 `--emit:exe` 可达裁剪后的 direct writer 回归：入口语义仍只走 call 闭包，但额外保留已严格 codegen 的无调用 leaf 函数，避免 dead but supported 函数从对象里消失。最新 `direct_object_writer_smoke seed_minimization_boundary_smoke float64_mul_direct_object_smoke float64_mul_backend_smoke` 全部通过；dead-f64 报告恢复为 `primary_object_body_kinds=return_zero_i32,return_f64_mul_args,entry_bridge`、`direct_object_symbol_count=3`，`otool -tV` 可见 `MulF64` 的 `fmul d0, d0, d1`。
- 最新纯 self-build 复核仍按预期 hard-fail：`/tmp/cheng_backend_driver_pure.report.txt` 显示 `primary_object_reachable_function_count=9`，其中只额外保留了 `return_i32_const_*`、`return_zero_i32`、`str_len_fast_i32` 这类严格 leaf；首个真实 blocker 仍是 `BackendDriverMain` 第 2878 行 `unsupported`。
- pure direct object writer 已新增最小真实 `if x <= 0: return 0; return 7` 控制流对象函数形状：typed IR 产出 `i32_if_arg0_le_zero_const_else_const`，PrimaryObjectIR/body kind 为 `if_arg0_le_zero_return_i32_const_0_else_i32_const_7`，Darwin arm64 direct writer 写入 `cmp w0,#0; b.gt; mov; ret`，不生成 `.s/.compile.log`。
- 为避免新 helper 污染 backend-driver 自举闭包，本轮没有引入多分支 body-kind 解析函数；当前只支持已验证的 `0/7` 最小 slice，其它常量形态继续 hard-fail，后续等 statement/scalar parser helper 自身可被 primary object 编译后再泛化。
- 最新验证已通过：`CHENG_BUILD_BACKEND_DRIVER_FORCE_C_SEED=1 artifacts/bootstrap/cheng.stage3 build-backend-driver --require-rebuild` 刷新 `artifacts/backend_driver/cheng`，候选报告 `primary_object_unsupported_function_count=0`；`int32_if_le_zero_direct_object_smoke --emit:obj` 报告 `primary_object_direct=1`、`direct_object_reloc_count=0`，`otool -tV` 可见 `cmp/b.gt/mov/ret`；`run-host-smokes direct_object_writer_smoke seed_minimization_boundary_smoke float64_mul_direct_object_smoke float64_mul_backend_smoke` 全部通过。
- 最新纯 self-build 边界前进到 `stmt_let_call`：`CHENG_NO_BACKEND_DRIVER_HANDOFF=1 artifacts/backend_driver/cheng system-link-exec --in:src/core/tooling/backend_driver_main.cheng --emit:exe` 约 61.7s 后 hard-fail，报告 `primary_object_reachable_function_count=9`、`primary_object_first_missing_function=BackendDriverMain`、`primary_object_first_missing_typed_ir_kind=stmt_let_call`、`primary_object_first_missing_line_number=2956`。下一步必须补普通 statement/control-flow/call codegen，不能用特殊 main 或 C seed fallback。
- 已修复 `stmt_let_call` 的诊断/合同分层：`PrimaryObjectIR.bodyKind` 只保留可发射 codegen 合同，首个 typed statement blocker 通过 `primary_object_first_missing_typed_ir_kind=stmt_let_call` 报告，不再把 `stmt_*` 写进 body kind。
- pure direct object writer 已新增最小真实 `let value: int32 = Helper(); return 0` 和 `let value: int32 = Helper(); return value` 语句切片：typed IR 产出 `TypedExprIrOpLetCall + return_i32_const/let_call_result_i32`，PrimaryObjectIR/body kind 为 `let_call_noarg_return_i32_const_0` / `let_call_noarg_return_result_i32`，Darwin arm64 直写 `stp x29,x30; mov x29,sp; bl target; ...; ldp x29,x30; ret`，文本汇编 fallback 同步同形输出。
- 已修复 let-call 非叶子函数 LR 保存问题：最初只写 `bl; mov; ret` 会让 `x30` 指向函数内部并循环，现已保存恢复 `x29/x30`，PrimaryObjectPlan word count 从 3 改为 6，BR26 relocation word offset 改到真实 `bl` 所在位置。
- 最新正式 driver 已刷新：`artifacts/backend_driver/builds/pid-89804/build_backend_driver.compile.report.txt` 显示 `primary_object_unsupported_function_count=0`、`exec_phase_backend_total_ms=107996`。手工 fixture `/tmp/cheng_non_return_call_codegen/app` 在 5 秒 timeout 下正常退出 0，报告 `primary_object_body_kinds=return_i32_const_7,let_call_noarg_return_i32_const_0,entry_bridge`、`primary_object_word_counts=2,6,17`、`direct_object_reloc_count=2`，`otool -rv` 可见 wrapper 和 let-call 两条 `BR26`，最终 `otool -tV` 可见保存恢复 LR 的非叶子函数形态。
- 最新验证已通过：`git diff --check`、`CHENG_BUILD_BACKEND_DRIVER_FORCE_C_SEED=1 artifacts/bootstrap/cheng.stage3 build-backend-driver --require-rebuild`、手工 `non_return_call_not_primary_fixture` direct exe 编译/运行/`otool`、`artifacts/backend_driver/cheng run-host-smokes direct_object_writer_smoke seed_minimization_boundary_smoke float64_mul_direct_object_smoke float64_mul_backend_smoke`、单独复跑 `direct_object_writer_smoke`。`let_call_return_result_direct_object_smoke` 作为普通 exe 会返回 7，不作为单独 host-smoke 通过项；它已由 `direct_object_writer_smoke` 按 object 形状验证。最新 pure self-build 边界仍 hard-fail 在 `BackendDriverMain`，`/tmp/cheng_backend_driver_pure.report.txt` 显示 `primary_object_first_missing_typed_ir_kind=stmt_let_call`、`primary_object_first_missing_line_number=3049`。
- pure direct object writer 已新增最小真实 `let value: int32 = Helper(); return value` 语句切片：typed IR 产出 `let_call_result_i32`，PrimaryObjectIR/body kind 为 `let_call_noarg_return_result_i32`，Darwin arm64 直写 `stp x29,x30; mov x29,sp; bl target; ldp x29,x30; ret`，文本汇编 fallback 同步同形输出。
- 最新正式 driver 已刷新：`artifacts/backend_driver/builds/pid-89804/build_backend_driver.compile.report.txt` 显示 `primary_object_missing_reasons=-`、`primary_object_unsupported_function_count=0`、`exec_phase_total_ms=101839`。专门 fixture `/tmp/cheng_let_result.o` 报告 `typed_ir_return_kinds=i32_const,let_call_result_i32`、`primary_object_body_kinds=return_i32_const_7,let_call_noarg_return_result_i32`、`primary_object_direct=1`、`direct_object_reloc_count=1`，`otool -rv` 可见 `BR26` 指向 `LeafResult`。
- 最新验证已通过：`git diff --check`、`CHENG_BUILD_BACKEND_DRIVER_FORCE_C_SEED=1 artifacts/bootstrap/cheng.stage3 build-backend-driver --require-rebuild`、手工 `let_call_return_result_direct_object_smoke --emit:obj` direct object/reloc 探针、同 fixture `--emit:exe` 运行退出码 7、`artifacts/backend_driver/cheng run-host-smokes direct_object_writer_smoke seed_minimization_boundary_smoke float64_mul_direct_object_smoke float64_mul_backend_smoke`。
- 最新纯 self-build 边界仍是复合返回 statement：`artifacts/backend_driver/cheng system-link-exec --in:src/core/tooling/backend_driver_main.cheng --emit:exe` 约 61.0s 后 hard-fail，报告 `primary_object_reachable_function_count=9`、`primary_object_first_missing_function=BackendDriverMain`、`primary_object_first_missing_typed_ir_kind=stmt_let_call`、`primary_object_first_missing_line_number=3049`。当前 blocker 是 `let rootDir = BackendDriverRoot()` 的 `str` 复合 call result/local，不是 int32 标量返回。
- pure direct object writer 已新增最小 `str` 复合 sret ABI 切片：`fn EmptyText(): str = var out: str; return out` 进入 `return_str_empty`，Darwin arm64 callee 通过隐藏 sret 地址 `x8` 写 24 字节零值；`let value: str = EmptyText(); return strLenFast(value)` 进入 `let_call_noarg_str_return_len_i32`，caller 在栈上保留 `str` local，`bl` 后按 `str` 布局读取 `data/len` 并用 `csel` 实现 `strLenFast`。
- 新增 fixture `src/tests/str_call_return_len_direct_object_smoke.cheng`，`direct_object_writer_smoke` 已纳入 typed IR、PrimaryObjectIR、direct Mach-O、BR26 reloc 和无 `.s/.compile.log` 回潮断言。
- 最新验证已通过：`CHENG_BUILD_BACKEND_DRIVER_FORCE_C_SEED=1 CHENG_PROGRESS=1 CHENG_PROCESS_MAX_RSS_BYTES=8589934592 artifacts/bootstrap/cheng.stage3 build-backend-driver --require-rebuild`、手工 `str_call_return_len_direct_object_smoke --emit:obj` direct object/reloc 探针、同 fixture `--emit:exe` 运行退出码 0、`artifacts/backend_driver/cheng run-host-smokes direct_object_writer_smoke seed_minimization_boundary_smoke float64_mul_direct_object_smoke float64_mul_backend_smoke`、`git diff --check`。
- 最新纯 self-build 边界仍是 `BackendDriverMain` 的真实 statement scheduler：`/tmp/cheng_backend_driver_pure.report.txt` 显示 `exec_phase_total_ms=61528`、`primary_object_reachable_function_count=9`、`primary_object_first_missing_function=BackendDriverMain`、`primary_object_first_missing_typed_ir_kind=stmt_let_call`、`primary_object_first_missing_line_number=3245`，对应 `let rootDir = BackendDriverRoot()`。下一步必须补一个函数内多 statement、多 call、多 reloc、复合 local 跨语句存活，不能用特殊 main 或 C seed fallback。
- pure direct object writer 已新增最小真实 `let value: int32 = Helper(); if value <= 0: return 0; return value` 控制流切片：typed IR 产出 `let_call_i32_if_result_le_zero_return_zero_else_result_i32`，PrimaryObjectIR/body kind 为 `let_call_noarg_i32_if_result_le_zero_return_zero_else_result_i32`，Darwin arm64 直写 `stp/mov/bl/cmp/b.gt/mov/ldp/ret`，文本汇编 fallback 同步同形输出。
- 新增 fixture `src/tests/let_call_i32_guard_direct_object_smoke.cheng`，`direct_object_writer_smoke` 已纳入 typed IR、PrimaryObjectIR、word count、direct Mach-O、BR26 reloc、`cmp/b.gt` 和无 `.s/.compile.log` 回潮断言。
- 最新正式 driver 已刷新：`artifacts/backend_driver/builds/pid-35684/build_backend_driver.compile.report.txt` 显示 `primary_object_unsupported_function_count=0`、`primary_object_instruction_word_count=23091`、`exec_phase_backend_total_ms=114885`。最新验证已通过：`git diff --check`、手工 `let_call_i32_guard_direct_object_smoke --emit:obj` direct object/reloc 探针、`CHENG_BUILD_BACKEND_DRIVER_FORCE_C_SEED=1 artifacts/bootstrap/cheng.stage3 build-backend-driver --require-rebuild`、`artifacts/backend_driver/cheng run-host-smokes direct_object_writer_smoke`。
- 最新纯 self-build 探针 `/tmp/cheng_backend_driver_pure_current.report.txt` 仍按预期 hard-fail：`exec_phase_total_ms=61718`、`primary_object_reachable_function_count=9`、`primary_object_first_missing_function=BackendDriverMain`、`primary_object_first_missing_typed_ir_kind=stmt_let_call`、`primary_object_first_missing_line_number=3245`。当前没有用特殊 main/stub/C seed fallback 掩盖剩余多 statement 调度问题。
- pure direct object writer 已新增最小真实 `str` 复合 local 跨语句传参切片：`let value: str = EmptyText(); ConsumeText(value); return 0` 进入结构化 `TypedExprIrOpLetCallStrThenCallArg0ReturnI32Const0`，lowering 产出 `let_call_noarg_str_then_call_arg0_return_i32_const_0`，Darwin arm64 直写一个函数内两次 `bl`，第一次用 `x8` 指向 caller 栈上 24 字节 `str` local，第二次用 `add x0, sp, #16` 把同一 local 地址作为 arg0。
- 新增 fixture `src/tests/str_then_call_arg0_direct_object_smoke.cheng`，`direct_object_writer_smoke` 已断言 typed IR、PrimaryObjectIR/body kind、`primary_object_word_counts=4,2,9`、`direct_object_reloc_count=2`、无 `.s/.compile.log` 回潮，以及 `otool -rv` 可见两条 `BR26` 指向 `EmptyText` 和 `ConsumeText`。
- 最新正式 driver 已刷新：`artifacts/backend_driver/builds/pid-10089/build_backend_driver.compile.report.txt` 显示 `primary_object_unsupported_function_count=0`、`primary_object_instruction_word_count=23251`、`exec_phase_backend_total_ms=119003`。最新验证已通过：手工 `str_then_call_arg0_direct_object_smoke --emit:obj` direct object/reloc 探针、`otool -tV` 可见 `stp/mov/add/bl/add/bl/mov/ldp/ret`，`artifacts/backend_driver/cheng run-host-smokes direct_object_writer_smoke` 通过。最新纯 self-build 仍真实 hard-fail 在 `BackendDriverMain` 第 3344 行，报告 `primary_object_first_missing_typed_ir_kind=stmt_let_call`，下一步继续补通用 statement scheduler，不能用特殊 main 或 C seed fallback。
- 本轮尝试清理四调用 `str_call_arg0_then_i32_guard` active path，但检测到源码被外部写回恢复旧内容；为保持当前树可构建，暂以已通过 smoke 的现有 source 口径推进。若下一步要删除该切片，必须先确认没有编辑器/旧任务 watcher 写回，再一次性清 typed IR、lowering、primary plan、direct writer、asm fallback 与 smoke。
- 已推进无显式类型 let-call：`let value = EmptyText(); ConsumeText(value); return 0` 现在由 typed call fact 的返回类型证明为 `str`，lowering 才产出 `let_call_noarg_str_then_call_arg0_return_i32_const_0`；非 `str` 首 call 不会被该 body kind 接收。
- 新增 fixture `src/tests/str_then_call_arg0_inferred_direct_object_smoke.cheng`，`direct_object_writer_smoke` 已纳入无 `.s/.compile.log`、typed IR、PrimaryObjectIR、direct writer 和双 BR26 断言。
- 最新正式 driver 已刷新：`artifacts/backend_driver/builds/pid-42293/build_backend_driver.compile.report.txt` 显示 `primary_object_unsupported_function_count=0`、`primary_object_instruction_word_count=23251`、`exec_phase_backend_total_ms=120627`。最新验证通过：`git diff --check`、`CHENG_BUILD_BACKEND_DRIVER_FORCE_C_SEED=1 CHENG_PROGRESS=1 CHENG_PROCESS_MAX_RSS_BYTES=8589934592 artifacts/bootstrap/cheng.stage3 build-backend-driver --require-rebuild`、`artifacts/backend_driver/cheng run-host-smokes direct_object_writer_smoke`、手工 `str_then_call_arg0_inferred_direct_object_smoke --emit:obj`，其 report 显示 `typed_ir_return_kinds=str_empty_return,i32_const,let_call_str_then_call_arg0_return_i32_const_0`、`primary_object_body_kinds=return_str_empty,return_zero_i32,let_call_noarg_str_then_call_arg0_return_i32_const_0`、`primary_object_direct=1`、`direct_object_reloc_count=2`，`otool -rv` 可见 `EmptyText` 和 `ConsumeText` 两条 `BR26`。最新纯 self-build 仍真实 hard-fail 在 `BackendDriverMain` 第 3397 行 `stmt_let_call`，对应 `let rootDir = BackendDriverRoot()`；下一步仍是通用 statement scheduler/local/call/control-flow。
- 最新正式 driver 已刷新：`artifacts/backend_driver/builds/pid-65295/build_backend_driver.compile.report.txt` 显示 `primary_object_unsupported_function_count=0`、`primary_object_instruction_word_count=23331`、`exec_phase_backend_total_ms=119396`。验证通过：`git diff --check`、`CHENG_BUILD_BACKEND_DRIVER_FORCE_C_SEED=1 CHENG_PROGRESS=1 CHENG_PROCESS_MAX_RSS_BYTES=8589934592 artifacts/bootstrap/cheng.stage3 build-backend-driver --require-rebuild`、`artifacts/backend_driver/cheng run-host-smokes direct_object_writer_smoke`、`artifacts/backend_driver/cheng run-host-smokes seed_minimization_boundary_smoke`、手工 `str_then_call_arg0_inferred_direct_object_smoke --emit:obj`；其 report 显示 `primary_object_direct=1`、`direct_object_reloc_count=2`，`otool -rv` 可见 `EmptyText`/`ConsumeText` 两条 `BR26`。最新纯 self-build 仍硬失败在 `BackendDriverMain` 第 3397 行 `stmt_let_call`，下一步是通用 statement scheduler/local/call/control-flow。
- 后台完成的新 driver `artifacts/backend_driver/builds/pid-77281/build_backend_driver.compile.report.txt` 已安装到 `artifacts/backend_driver/cheng`：`primary_object_unsupported_function_count=0`、`primary_object_instruction_word_count=23331`、`exec_phase_backend_total_ms=120153`、`primary_object_missing_reasons=-`、`object_missing_reasons=-`、`native_link_missing_reasons=-`。安装后二次验证通过：`artifacts/backend_driver/cheng run-host-smokes direct_object_writer_smoke` 与 `artifacts/backend_driver/cheng run-host-smokes seed_minimization_boundary_smoke`。
- 外部排队构建又完成并安装了最新 driver：`artifacts/backend_driver/builds/pid-84929/build_backend_driver.compile.report.txt` 显示 `primary_object_unsupported_function_count=0`、`primary_object_instruction_word_count=23331`、`exec_phase_backend_total_ms=121973`、`primary_object_missing_reasons=-`、`object_missing_reasons=-`、`native_link_missing_reasons=-`。最新安装产物已再次验证通过：`artifacts/backend_driver/cheng run-host-smokes direct_object_writer_smoke` 与 `artifacts/backend_driver/cheng run-host-smokes seed_minimization_boundary_smoke`。
- primary object plan 已把结构化 body kind / statement mode / BodyIR 写成真实 `instructionWords`，direct writer 主路径不再按 body kind 重新生成 text bytes。最新正式 driver 已刷新：`artifacts/backend_driver/builds/pid-79502/build_backend_driver.compile.report.txt` 显示 `primary_object_unsupported_function_count=0`、`primary_object_instruction_word_count=19716`、`primary_object_missing_reasons=-`、`object_missing_reasons=-`、`native_link_missing_reasons=-`。
- 本轮验证通过：`int32_const_return_direct_fixture` standalone direct exe 运行退出 7，报告 `system_link_exec_runtime_standalone_direct_entry=1`、`provider_object_count=0`、`primary_object_direct=1`、`direct_object_reloc_count=2`，`otool -rv` 可见 `_main -> Cheng main -> ReturnSeven` 两条 BR26；`float64_mul_direct_object_smoke --emit:obj` 报告 `primary_object_direct=1`，`otool -tV` 可见 `fmul d0, d0, d1`。
- 修复纯 Cheng 最小 backend driver 的 lowering 入口收集：`BuildLoweringPlanStub` 传参顺序改为 `sourcePath, plan`，候选 driver 运行 `system-link-exec` 时不再丢失 entry 函数。最新 `CHENG_BUILD_BACKEND_DRIVER_FORCE_C_SEED=1 artifacts/bootstrap/cheng.stage3 build-backend-driver --target:arm64-apple-darwin` 成功安装 `artifacts/backend_driver/cheng`，候选 `ordinary_zero_exit_fixture` 自检报告 `system_link_exec_runtime_execute=1`、`provider_object_count=0`、`primary_object_direct=1`；无 force 的 `build-backend-driver` 也已直接 ready。
- C seed 的 live 子进程 runner 已新增进程组 RSS 守卫：外部 `posix_spawn` 和内部 `fork` 子命令都进入独立 process group，超过 `CHENG_PROCESS_MAX_RSS_BYTES` 时写 `rss_limit_exceeded`、终止进程组并返回 125。低上限验证 `CHENG_PROCESS_MAX_RSS_BYTES=67108864 artifacts/bootstrap/cheng.stage3 build-backend-driver --require-rebuild` 在 1.1 秒内硬失败，日志显示 `rss_bytes=118816768 limit_bytes=67108864`；随后用 C seed 强制刷新当前最小 driver，普通 `build-backend-driver --target:arm64-apple-darwin` 已恢复秒级 ready，`ordinary_zero_exit_fixture` 编译和运行退出码 0。
- 修正 build-backend-driver RSS 上限优先级：`CHENG_BUILD_BACKEND_DRIVER_MAX_RSS_BYTES` > `CHENG_PROCESS_MAX_RSS_BYTES` > `CHENG_MAX_RSS_BYTES` > 默认 8GiB。带 `CHENG_RSS_GUARD_TRACE=1` 的 64MB probe 现在显示 limit 正确保持 `67108864`，在 `system_link_plan` 阶段 RSS 到 `117850112` 时立刻 `rss_limit_exceeded` 并退出 125，无残留构建进程。最新 `stage3`、`backend_driver/cheng` 均已刷新，普通 ready 和 `ordinary_zero_exit_fixture` 编译/运行通过。
- 本轮修复并验证最小 driver 主线：`lowering_plan` 用规范化绝对路径标记 entry，`primary_object_plan` 继续写真实 `instructionWords`，`backend_driver_dispatch_min` 内置纯 Cheng `verify-export-visibility`/parallel gate。最新 `artifacts/backend_driver/builds/pid-70260/build_backend_driver.compile.report.txt` 显示 `primary_object_missing_reasons=-`、`primary_object_unsupported_function_count=0`、`primary_object_instruction_word_count=14614`；候选 zero-exit selftest 报告 `system_link_exec_runtime_execute=1`、`provider_object_count=0`、`primary_object_direct=1`。
- 最新验证通过：`git diff --check`、`CHENG_BUILD_BACKEND_DRIVER_FORCE_C_SEED=1 CHENG_PROGRESS=1 CHENG_PROCESS_MAX_RSS_BYTES=8589934592 artifacts/bootstrap/cheng.stage3 build-backend-driver --require-rebuild`、`CHENG_VERIFY_EXPORT_VISIBILITY_DIR=artifacts/forward/verify_export_visibility_current artifacts/backend_driver/cheng verify-export-visibility`、隔离目录下 `verify-export-visibility-parallel`、`int32_const_return_direct_fixture` direct exe 运行退出 7 且 `otool -rv` 可见两条 `BR26`、`float64_mul_direct_object_smoke.o` 的 `otool -tV` 可见 `fmul d0, d0, d1`。
- 2026-04-26 本轮继续收敛：`mobile_capi.cheng` 删除本地 raw pointer 文本/C string helper 后，所有调用改为显式走 `mobile_capi_bridge_internal` 的 `InternalMobileBridge*` 边界；`mobile_shell_build_probe_smoke` 增加静态防回潮，检查 `@exportc` 签名不暴露 `ptr` 且旧 `mobileCapi*RawText/CString` helper 名清零。
- 2026-04-26 本轮同步清理一个明确字符串拼接回潮：`compiler_main.cheng` 的 pure `system-link-exec` report 写失败诊断改为 Nim 风格 `Fmt"..."`。本轮期间观察到外部 `build-backend-driver --require-rebuild` 候选编译 RSS 约 7.4GiB，因此只执行静态校验，没有启动新的编译/host smoke。
- 2026-04-26 本轮继续推进 typed statement scheduler：`TypedExprIrAppendStatementsForScope` 与首个 binding call 查找改为从 sourceFacts 行号游标 `startIndex` 直接推进，删除函数内重复 `ParserNormalizePath(facts[i].sourcePath)` 过滤，避免每个函数重新扫同一 source 的前缀 facts。静态验证 `git diff --check src/core/lang/typed_expr.cheng` 通过；因外部 `build-backend-driver --require-rebuild` 正在运行，本轮未启动新的重编译。
- 2026-04-26 复跑 pure self-build 边界探针时发现 `CHENG_PROCESS_MAX_RSS_BYTES=8589934592` 未拦住 `artifacts/backend_driver/cheng system-link-exec` 自身 RSS，进程约 2 分钟到 13GiB 后已手动中断，未产出通过/失败报告。已同步 `lessons.md`：补进程级守卫前不再长跑该探针，下一步优先补 runtime/driver 自身 RSS hard-fail 或用外部 watchdog 包住。
- 2026-04-26 本轮修复最小 backend driver 自身 RSS 守卫：`backend_driver_dispatch_min.cheng` 新增默认 8GiB、纯 Cheng 解析 `CHENG_PROCESS_MAX_RSS_BYTES/CHENG_MAX_RSS_BYTES`、读取当前进程 RSS，并在启动和每次 compile progress 前检查，超限打印 `rss_limit_exceeded` 后以 125 硬退出；`backend_driver_resource_guard_contract_smoke.cheng` 同步加入静态防回潮断言。中途验证过 `setrlimit(RLIMIT_AS)` 在当前 Darwin 面会让候选 `status` 失败，已撤回，下一步 phase 内防爆要进 parser/lowering/primary 长循环。
- 最新验证通过：`git diff --check`、`CHENG_BUILD_BACKEND_DRIVER_FORCE_C_SEED=1 CHENG_PROGRESS=1 CHENG_PROCESS_MAX_RSS_BYTES=8589934592 artifacts/bootstrap/cheng.stage3 build-backend-driver --require-rebuild`、`artifacts/backend_driver/cheng help` 确认 verify 命令已在 min closure 内、`verify-export-visibility`、隔离目录下 `verify-export-visibility-parallel`；最新报告 `artifacts/backend_driver/builds/pid-18976/build_backend_driver.compile.report.txt` 显示 `primary_object_missing_reasons=-`、`primary_object_unsupported_function_count=0`、`primary_object_instruction_word_count=14758`、`provider_object_count=5`、`native_link_missing_reasons=-`。
- 低 RSS 自守卫探针通过：`CHENG_PROCESS_MAX_RSS_BYTES=1 CHENG_PROGRESS=1 artifacts/backend_driver/cheng system-link-exec ... ordinary_zero_exit_fixture.cheng` 以 125 退出并打印 `compile_progress phase=resource_guard status=rss_limit_exceeded rss_bytes=1605632 limit_bytes=1`。当前最小 driver 不暴露 `run-host-smokes`，旧完整候选也会转到已最小化 stage3，因此本轮没有把 host-smoke 合同冒充为通过。
- 2026-04-26 本轮继续推进通用 statement/callSequence lowering：`lowering_plan.cheng` 在函数仍为 `unsupported` 时，直接消费已结构化的 `PrimaryObjectIrStatement[]`，为每个 `stmt_let_call/stmt_if_call/stmt_call` 生成 `callSequence` 与 `BodyIR Call`，但不把函数标成 supported，避免伪完成。新增 `multi_stmt_call_sequence_unsupported_fixture.cheng` 和 `lowering_plan_smoke` 断言，锁定两个 statement call 会出现在 lowering report 的 `primary_object_ir_call_sequence_counts/body_ir_call_counts/targets`。
- 本轮验证：`git diff --check src/core/backend/lowering_plan.cheng src/tests/lowering_plan_smoke.cheng src/tests/multi_stmt_call_sequence_unsupported_fixture.cheng` 通过；`multi_stmt_call_sequence_unsupported_fixture` 通过最小 driver 编译探针按预期 hard-fail 在 primary plan，报告 `primary_first_missing_function=main`、`primary_first_missing_body_kind=unsupported`、`primary_first_missing_typed_ir_kind=stmt_let_call`、`primary_first_missing_line_number=8`；`ordinary_zero_exit_fixture` 编译运行退出 0；`CHENG_PROCESS_MAX_RSS_BYTES=1` 低 RSS 探针退出 125。`run-host-smokes lowering_plan_smoke` 当前最小 stage3 不暴露，未绕行旧入口。
- 2026-04-26 本轮继续把 statement sequence 消费端迁到 BodyIR：`primary_object_plan.cheng` 新增 `PrimaryBodyIrStatementReturnMode/PrimaryBodyIrStatementI32Value`，`PrimaryBodyIrWordCount/RequiresCallSequence/RelocWordOffset` 先识别 `BodyIR Call... + Return(statementMode)`；instruction word 填充也优先按 BodyIR mode 走。`direct_object_emit.cheng` 新增 `DirectObjectEmitTryWriteStatementSequenceBodyIR`，direct writer 先消费 BodyIR statement sequence，再落回旧 body kind 分支。
- 本轮验证：`git diff --check src/core/backend/primary_object_plan.cheng src/core/backend/direct_object_emit.cheng src/tests/primary_object_codegen_smoke.cheng` 通过；`primary_object_codegen_smoke.cheng` 已补断言，确认 let-call BodyIR mode、word count 和 call sequence requirement 与 primary plan 一致；`ordinary_zero_exit_fixture` 编译运行退出 0；`CHENG_PROCESS_MAX_RSS_BYTES=1` 低 RSS 探针继续 125。未跑完整重编译。
- 2026-04-26 本轮按审查报告先修 mobile buffer handle 生命周期表面：`InternalMobileBridgeBufferHandle` 改名为 `InternalMobileBridgeBufferHandleCopy`，`MobileCapiBytesBufferHandle` 删除 snapshot 分配、逐字节 while 拷贝和 `bytesFree(snapshot)`，直接把 payload data 交给已有复制型 runtime bridge；`mobile_shell_build_probe_smoke` 增加静态防回潮检查。静态验证和 `ordinary_zero_exit_fixture` direct 编译运行通过；`bytes_buffer_handle_copy_smoke` 在当前最小 driver 下仍按预期 hard-fail 于已知 `stmt_let_call` primary lowering 缺口。
- 2026-04-26 本轮清理 backend 低风险重复：`primary_object_emit.cheng`、`direct_object_emit.cheng`、`primary_object_plan.cheng` 删除各自手写 StartsWith helper，统一用已有 `std/strutils.StartsWith`；`body_kind_parse.cheng` 删除未使用且与 emit 侧重复的 `BodyKindExpectedWords`，避免两个 body-kind word-count 合同并存。`git diff --check` 通过。
- 2026-04-26 本轮修复最小 driver 和 `std/os` 的自身 RSS 守卫活路径：删除 `/bin/ps -p ... -o rss=` 读取，改为纯 Cheng `@importc("getrusage")` 直读 `ru_maxrss`；`processResourceGuardCheck` 与 `BackendDriverDispatchMinProgressResourceGuardCheck` 不再是 no-op，RSS 不可得会 `rss_unavailable` 硬失败。同步更新 `backend_driver_resource_guard_contract_smoke.cheng` 防回潮断言。
- 最新验证通过：`git diff --check`、`CHENG_BUILD_BACKEND_DRIVER_FORCE_C_SEED=1 CHENG_PROGRESS=1 CHENG_PROCESS_MAX_RSS_BYTES=8589934592 artifacts/bootstrap/cheng.stage3 build-backend-driver --require-rebuild`；最新报告 `artifacts/backend_driver/builds/pid-23503/build_backend_driver.compile.report.txt` 显示 `primary_object_missing_reasons=-`、`primary_object_unsupported_function_count=0`、`primary_object_instruction_word_count=14582`、`provider_object_count=5`、`native_link_missing_reasons=-`。`CHENG_PROCESS_MAX_RSS_BYTES=1 artifacts/backend_driver/cheng status` 退出 125，`CHENG_PROCESS_MAX_RSS_BYTES=67108864 ... backend_driver_main.cheng` 在 `rss_bytes=72384512` 退出 125，`CHENG_PARENT_RSS_GUARD=1 CHENG_PROCESS_MAX_RSS_BYTES=1 artifacts/backend_driver/cheng status` 正常通过，`ordinary_zero_exit_fixture` 在 1GiB 上限下编译运行退出 0，`verify-export-visibility` 与隔离目录下 `verify-export-visibility-parallel` 均通过。
- 2026-04-26 本轮继续推进 BodyIR 消费链：`direct_object_emit.cheng` 主路径直接写 `PrimaryObjectPlan.instructionWords`，不再按 body kind 二次拼 text；`lowering_plan.cheng` 新增结构化两次 `int32` noarg call + `if second <= 0: return 0; return first` BodyIR 切片，`primary_object_plan.cheng` 补齐该切片的 word count、call sequence requirement、BR26 relocation offset 和 Darwin arm64 instruction fill。
- 新增 `src/tests/multi_stmt_i32_guard_direct_object_smoke.cheng`。最新验证通过：`git diff --check`、`CHENG_BUILD_BACKEND_DRIVER_FORCE_C_SEED=1 CHENG_PROGRESS=1 CHENG_PROCESS_MAX_RSS_BYTES=8589934592 artifacts/bootstrap/cheng.stage3 build-backend-driver --require-rebuild`；最新报告 `artifacts/backend_driver/builds/pid-20492/build_backend_driver.compile.report.txt` 显示 `primary_object_missing_reasons=-`、`primary_object_unsupported_function_count=0`、`primary_object_instruction_word_count=14358`。该 fixture direct exe 编译成功并运行退出 1，报告 `primary_object_direct=1`、`direct_object_reloc_count=3`，`otool -rv` 可见 wrapper、`LeftValue`、`RightValue` 三条 BR26；`ordinary_zero_exit_fixture` 运行退出 0，`int32_const_return_direct_fixture` 运行退出 7，`CHENG_PROCESS_MAX_RSS_BYTES=1` 低 RSS 探针退出 125。
- 最新 pure driver 自举边界仍真实 hard-fail，没有转发：`artifacts/backend_driver/cheng system-link-exec --in:src/core/tooling/backend_driver_dispatch_min.cheng --emit:exe` 报告 `primary_first_missing_function=BackendDriverDispatchMinRun`、`primary_first_missing_body_kind=unsupported`、`primary_first_missing_typed_ir_kind=stmt_let_call`、`primary_first_missing_line_number=599`。该缺口已不是单个 let-call body kind，而是入口分发里的 `str[]` local、字符串比较、多路 return-call 和命令参数解析，需要下一步落通用 locals/branch/string literal/call arg lowering。
- 2026-04-26 本轮补齐已有结构化 guard-call BodyIR 的汇编 fallback：`primary_object_emit.cheng` 新增 `I32GuardThenCommandReturn` 与 `I32GuardHelpThenCommandReturn` 两条 asm 发射路径，并在 `PrimaryObjectEmitLeafBodyIRFunctionAsm` 中优先识别，避免 direct writer 与 `.s` fallback 能力不一致。新增 `i32_guard_then_command_direct_object_smoke.cheng`、`i32_guard_help_then_command_direct_object_smoke.cheng`，`primary_object_codegen_smoke.cheng` 增加 word count、reloc offset 和 asm 文本断言。
- 本轮验证：`git diff --check src/core/backend/primary_object_emit.cheng src/tests/primary_object_codegen_smoke.cheng src/tests/i32_guard_then_command_direct_object_smoke.cheng src/tests/i32_guard_help_then_command_direct_object_smoke.cheng` 通过；两个 fixture 经当前最小 driver 编译为 direct exe 后分别运行退出 9 和 7，`otool -rv` 可见 guard-command 的 `_CountOne/_Command` BR26、guard-help-command 的 `_CountZero/_Help/_Command` BR26。当前最小 driver 仍不能编译完整 `primary_object_codegen_smoke`，未把源码 smoke 冒充为已运行。
- 2026-04-26 本轮继续把 `@importc("puts")` + cstring 字面量调用序列纳入纯 Cheng direct object 主线：typed/lowering 新增 `StatementReturnModeCstringLiteralCallsReturnI32Const0`，primary plan 输出 cstring data label/payload，direct Mach-O writer 写 text+data 对象、BR26 call reloc 与 PAGE21/PAGEOFF12 data reloc，`.s` fallback 同步补齐。`BackendDriverDispatchMinHelp()` 改为三次 `puts` bridge 调用，移除 `os.WriteLine(os.Get_stdout(), "...")` 对纯自举的阻塞。
- 本轮验证通过：`git diff --check` 覆盖 backend 改动与 `puts_literal_sequence_direct_object_smoke.cheng`；`CHENG_BUILD_BACKEND_DRIVER_FORCE_C_SEED=1 CHENG_PROGRESS=1 CHENG_PROCESS_MAX_RSS_BYTES=8589934592 artifacts/bootstrap/cheng.stage3 build-backend-driver --require-rebuild` 成功刷新正式 min driver；`artifacts/backend_driver/cheng help` 输出真实 help；`puts_literal_sequence_direct_object_smoke` direct exe 编译运行输出两行 puts 文本且退出 0，报告 `provider_object_count=0`、`primary_object_direct=1`、`direct_object_reloc_count=7`，`otool -rv` 可见两条 `_puts` BR26 和两组 cstring data reloc。
- 当前最新 pure self-build 探针已推进到下一真实缺口：`BackendDriverDispatchMinRunCommandWithCmdAndCount` 第一句 `let commandArgs = BackendDriverDispatchMinCollectArgsFrom(2, count)`，报告为 `primary_first_missing_typed_ir_kind=stmt_let_call`、`primary_first_missing_line_number=603`。并发的 `primary_object_codegen_smoke` 编译探针被自身 8GiB RSS 守卫硬失败，说明下一步除语义切片外还要继续压 typed/lowering/primary 的重复扫描。
- 2026-04-26 本轮补齐 `let value: str = TextAt(1); return UseText(value)` 的真实 sret 调用链：`TypedExprFunctionBodyIsStrEmptyReturn` 允许带未使用参数的空 `str` 返回函数，`primary_object_emit` 增加 `str_call_const_then_call_arg0_return_i32` asm fallback，`primary_object_plan` 将该具体 BodyIR 判定放到通用 i32 call-chain 前，并把通用判定收紧到首个 call op 仅一个 operand，避免 `str` ABI 被误当 i32 返回。新增 `str_call_const_then_call_arg0_return_i32_direct_object_smoke.cheng`，验证 direct exe 运行退出 7，`otool -tV` 可见 `add x8, sp,#0x10`、`mov w0,#0x1` 和两次 `bl`；`ordinary_zero_exit_fixture` 仍 direct 运行退出 0。`run-host-smokes` 当前 min driver 不暴露，未冒充整套 host smoke 通过。
- 2026-04-26 本轮继续推进结构化 call sequence：新增 `int32` call result 作为后续 arg0、`str` noarg sret local 作为后续 arg0、`str arg0 + int32 local` 作为后续双参调用三条 BodyIR 切片；`primary_object_plan` 写真实 Darwin arm64 机器字和 reloc offset，`primary_object_emit` 同步 asm fallback。修正 `mov w1, w0` 机器字为 `0x2a0003e1`，避免误写成 `orr w1, w1, w0`；`direct_object_emit` 的 data section 改为以 `dataLabels` 为主合同，不再在无 data label 的 plan 中读取 payload。
- 最新正式 driver 已刷新：`artifacts/backend_driver/builds/pid-68953/build_backend_driver.compile.report.txt` 显示 `primary_object_missing_reasons=-`、`primary_object_unsupported_function_count=0`、`primary_object_instruction_word_count=15094`、`native_link_missing_reasons=-`、`exec_phase_backend_total_ms=46721`。新增 direct fixture `i32_call_arg0_return_direct_object_smoke` 和 `arg0_i32_call_arg0_arg1_return_direct_object_smoke` 均编译通过并运行退出 1；后者 `otool -rv` 可见 wrapper、`CountOne`、`UseTextCount`、`EmptyText`、`RunWithText` 五条 BR26 relocation。
- 最新 pure self-build 边界继续前进到命令分发字符串分支：`/tmp/cheng-pure-driver-self.report.txt` 显示 `primary_first_missing_function=BackendDriverDispatchMinRunCommandWithCmd`、`primary_first_missing_typed_ir_kind=stmt_call`、`primary_first_missing_line_number=657`，对应 `if cmd == "help": return BackendDriverDispatchMinHelp()`。下一步必须补 `str` 字面量/字符串比较/条件分支到 BodyIR/primary/direct writer，不能继续用命令名特殊壳或 C seed fallback。
- 2026-04-28 本轮纯自举继续推进：`LoweringAppendCfgFinalReturnTerm` 改为 append final `return Call(...)` 到结构化 callSequence 和 BodyIR callSequence，不再复用上一条 call result；`LoweringSyncLegacyCallSequence` 遇到已有 BodyIR ops/blocks/terms 且仅 legacy callTargets 时直接标 `call_sequence_body_ir_drift`，不再合成半截 IR；lowering report 增加 structured/bodyIR call count，primary report 增加 first call-sequence drift 字段。新增 `cfg_final_return_call_sequence_fixture.cheng` 并接入 `lowering_plan_smoke`。
- 2026-04-28 纯 driver 自编译仍未完成，未使用 C seed。当前未热补丁 `artifacts/backend_driver/cheng system-link-exec --in:src/core/tooling/backend_driver_dispatch_min.cheng` 仍在 primary 阶段崩为 `[cheng] bounds check failed: idx=1 len=1`。lldb 已反解到旧 artifact 的 `BuildPrimaryObjectPlan + 37008`：target 函数是 `src/core/tooling/backend_driver_dispatch_min.cheng` 第 1176 行 `main(): int32 = return BackendDriverDispatchMinRun()`，该单 call 序列触发旧机器码把 `x9` 跨 `bl cheng_bounds_check` 当活值用。这个是旧 artifact codegen 活值污染，不是当前 target IR 数据越界。
- 2026-04-28 诊断性 `/tmp` 热补丁只用于过桥定位：最小补丁应在 `BuildPrimaryObjectPlan` 的循环退出判断前 reload 栈上 loop index，而不是 NOP `cheng_bounds_check`。热补丁未写回仓库、未作为验收；最终只认未热补丁候选 A 自编译出 B。
- 2026-04-28 本轮继续用子代理推进且未使用 C seed：修正 `lowering_plan.cheng` 中 `main` 误进 `BackendDriverDispatchMinRunCommandWithCmd` 专用命令分发 lowering 的问题；同步把 `PrimaryObjectPlan` 新增 drift 字段后的 aggregate slot 大小从 768 调到 832；`backend_driver_dispatch_min.cheng` 入口改回语义正确的 no-arg `main -> BackendDriverDispatchMinRun()`。`git diff --check` 通过。
- 2026-04-28 旧 installed artifact 的纯编译探针有两个已确认陈旧阻塞：`main -> BackendDriverDispatchMinRun()` 或 `main -> BackendDriverDispatchMinRunCommand()` 会在 primary 阶段触发同一个 caller-saved/bounds 污染；把入口改成 `ParamStr -> dispatch` 或首句 guard 变形会越过 bounds 但旧 lowering 报 `primary_first_missing_function=main`、`primary_first_missing_body_kind=cfg_no_top_if`、`primary_first_missing_line_number=1177`。这些是旧 artifact 自身能力缺口，不能作为当前源码最终形态。收尾验证通过：`git diff --check`、`ordinary_zero_exit_fixture` 编译运行退出 0、`perf_memory_gate_contract_smoke` 编译运行退出 0；最终纯 self probe 仍在 primary 阶段复现 `[cheng] bounds check failed: idx=1 len=1`，未完成自举闭环。
- 2026-04-28 本轮用子代理继续推进，未使用 C seed、未写回热补丁 artifact。`/tmp/cheng.bounds-reload-x9` 对旧 artifact 增加两个精确 reload 补丁后，纯 self probe 越过 `BuildPrimaryObjectPlan` bounds 崩溃；随后 first missing 依次暴露为 `BackendDriverDispatchMinRun`、`BackendDriverDispatchMinRunCommand`、`BackendDriverDispatchMinRunSystemLinkExecCommandFromCmdline` wrapper，最终停在真实大主体 `BackendDriverDispatchMinRunSystemLinkExecFromCmdline` 的 `cfg_no_top_if`。源码侧已补 `BackendDriverDispatchMinRunCommand` wrapper 的 BodyIR lowering，并把 `RunSystemLinkExecFromCmdline` 收口为从 `--root` 读取 driverRoot/packageRoot；A/B 纯自举仍未产出。
- 2026-04-28 本轮继续用子代理推进，未使用 C seed、未创建分支/worktree。已修复 `primary_object_plan` 同长度 callSequence/BodyIR callSequence 漂移漏检：现在逐项比较 target/result/arg count/arg slot，漂移会 hard-fail 并写 `firstCallSequenceDrift*`。`backend_driver_dispatch_min` 增加 report 内 phase ms、CPU/RSS、first-missing、drift 字段，且 direct 路径对非 `arm64-apple-darwin` 立即 hard-fail，避免晚到 `cc -arch arm64`。
- 2026-04-28 验证结果：`git diff --check` 通过，`./artifacts/backend_driver/cheng status` 通过，子代理纯 artifact `primary_object_plan_smoke` 编译运行退出 0，本地主 `ordinary_zero_exit_fixture` 编译运行退出 0。`run-host-smokes lowering_plan_smoke` 当前最小 driver 不支持；改用 `system-link-exec` 后在 `lowering_plan` 阶段超过 150 秒且未产 report，已中止。`cfg_final_return_call_sequence_fixture` 在旧 installed artifact 下仍 `plan_not_ready`，这是因为新 lowering/report 还未由纯 candidate 自举出来；不能当作已闭环。
- 2026-04-28 本轮继续用子代理推进，未使用 C seed、未创建分支/worktree。`backend_driver_dispatch_min` 的 `RunSystemLinkExecFromCmdline` 已从 `Result/Value` 和 `var emitKind` out-param 改成 bool guard + 分支 return-call；provider export roots 去掉 `str[]` roots 聚合，改为 CSV 字符串去重；plan-not-ready report 不再展开复合 missingReasons，只输出计数和 first-missing/drift 标量。
- 2026-04-28 采样确认 5 分钟长跑主因在 `compiler_csg` 前端，而不是 linker：pure self 探针约 90 秒仍停在 `compiler_csg`，`sample` 显示 `CompilerCsgExprLayerForText -> ParserAppendAssignStmtExprsLine -> ParserLineStartsWithStatementKeyword -> PathTrim/Strip/NewStringCopy` 占主要栈。`parser.cheng` 已把 statement keyword/assignment candidate 判断改为无分配 token/range 比较，减少每行多次 trim/copy。
- 2026-04-28 子代理并行补齐 `Result[str]` 最小投影闭环：`core_types` 新增 `BodyOpResultProject` 和 `Result[str]` 字段偏移/64 字节布局；`lowering_plan` 将 `IsErr/Error/Value` 物化为本地 slot，并支持 `if IsErr(res)` 与 `Call(Value(res))`；`primary_object_plan` 为 bool 投影和 24 字节 `str` 投影生成真实 arm64 机器字。
- 2026-04-28 CSG 热路径追加低风险索引优化：模块节点创建时保存 `moduleNodeIds`，import edge 和 source 两轮处理通过已排序 `modules` 二分取 nodeId，并复用 `orderedSourceModules`，不再每次线性扫描 `nodes` 或重复计算 source module path。
- 2026-04-28 本轮短验证通过：`git diff --check`、`./artifacts/backend_driver/cheng status`、`ordinary_zero_exit_fixture` direct exe 编译并运行退出 0。完整 pure self-build 未完成，因当前安装产物未包含本轮 source 优化，长探针仍只作为热点采样使用，不能冒充闭环。
- 2026-04-26 本轮修复 `str` 无参 sret call-chain 的 ABI 错配：`LoweringMaybeFillStrLetCallThenCallArg0ReturnI32FromStatements` 不再给首个无参 call op 追加 const operand，`PrimaryBodyIrIsI32LetCallThenReturnCallArg0` 与 `PrimaryBodyIrIsArg0I32LetCallThenReturnCallArg0Arg1` 现在要求首个 call result slot 的 local type 必须是 `LocalI32Tag`，避免把 `str` local 误判成 i32 call result。新增 `str_let_call_then_call_arg0_return_i32_direct_object_smoke.cheng`，`primary_object_codegen_smoke.cheng` 补 word count、reloc offset、asm fallback 断言。
- 最新验证通过：`git diff --check`、两次 `CHENG_BUILD_BACKEND_DRIVER_FORCE_C_SEED=1 CHENG_PROGRESS=1 CHENG_PROCESS_MAX_RSS_BYTES=8589934592 artifacts/bootstrap/cheng.stage3 build-backend-driver --require-rebuild`；最新报告 `artifacts/backend_driver/builds/pid-47705/build_backend_driver.compile.report.txt` 显示 `primary_object_missing_reasons=-`、`primary_object_unsupported_function_count=0`、`primary_object_instruction_word_count=15126`、`provider_object_count=5`、`native_link_missing_reasons=-`。新 fixture direct exe 运行退出 7，`otool -rv` 可见 wrapper、`_EmptyText`、`_UseText` 三条 BR26，`otool -tV` 可见 `add x8, sp,#0x10`、`add x0, sp,#0x10` 且无错误的 `mov w0,#1`；旧 const-arg fixture 仍保留 `mov w0,#1` 并运行退出 7，`i32_call_arg0_return_direct_object_smoke` 仍运行退出 1，`ordinary_zero_exit_fixture` 运行退出 0。低 RSS self `system-link-exec` 探针在 64MiB 下以 125 和 `rss_limit_exceeded` 硬失败。
- 2026-04-26 本轮继续推进最小 driver pure self-build 主线：`LoweringMaybeFillBackendDriverRootFromCmdline` 已接入 `LoweringBuildPrimaryObjectIr`，`str` let-call 后接 `return Call(local)` 的判定改为消费 typed statement `resultType`/local slot 类型，避免无显式类型 `let rootDir = ...` 因 call fact `typeText` 为空而退回 `stmt_let_call`。`backend_driver_dispatch_min` 的 `status/print-build-plan/help` 收口到 `puts` 字面量输出，help 不再暴露 verify 命令。
- 最新验证：`CHENG_BUILD_BACKEND_DRIVER_FORCE_C_SEED=1 CHENG_PROGRESS=1 CHENG_PROCESS_MAX_RSS_BYTES=8589934592 artifacts/bootstrap/cheng.stage3 build-backend-driver --require-rebuild` 成功安装 `artifacts/backend_driver/cheng`；`artifacts/backend_driver/cheng help/status/print-build-plan` 正常输出。pure self-build 探针 `/tmp/cheng-pure-driver-self-codex-main.report.txt` 已越过 root 和入口包装，新的真实 hard-fail 为 `primary_first_missing_function=BackendDriverDispatchMinRunSystemLinkExecWithRequest`、`primary_first_missing_typed_ir_kind=stmt_call`、`primary_first_missing_line_number=453`，对应 `if IsErr(reqRes): return BackendDriverDispatchMinUnsupported(Error(reqRes))`。
- 2026-04-26 本轮继续清理纯自举阻塞：新增 `return_u64_arg0_and_const_4_not_zero` 和 `return_u64_arg0_or_const_4` 的 typed/lowering/body-kind/primary/direct/asm fallback 闭环，`std/os::execCommandHasEvalOption` 与 `execCommandWithEvalOption` 改成固定 `uint64(4)` 位语义；`std/os` 的 `cheng_fopen/cheng_fflush` 入口改绑现有 host runtime 符号，避免旧 stage3 provider roots 缺 `cheng_fopen/cheng_fflush` 时卡在链接。
- 本轮验证：`git diff --check` 通过；强制 C seed 刷新从 `artifacts/backend_driver/builds/pid-45065` 的 `primary_object_unsupported_function_count=64` 降到 `artifacts/backend_driver/builds/pid-54889` 的 `primary_object_unsupported_function_count=9`。当前刷新仍未成功安装，新 first blockers 是 `BackendDriverDispatchMinRequestWithAbsoluteSourcePath` 的复合 `CompilerRequest` 重写、`BackendDriverDispatchMinRunSystemLinkExecConcreteRequest` 的多 statement/control-flow 主体、`BackendDriverDispatchMinRunSystemLinkExecFromCmdline` 的 `Result` call local，以及 `lowering_plan/primary_object_plan` 内部若干 `stmt_if/stmt_var/stmt_call`；下一步必须落通用复合 local、Result 传播和 CFG statement codegen，不能再靠单函数壳。
- 2026-04-26 本轮继续推进 Low-UIR 主线：`low_uir.cheng` 去掉对 `core/backend/lowering_plan` 的反向导入，改成独立 `LowUirStatement` 输入和 `lowUirTryBuildFromStatements`，backend 适配逻辑留在 `lowering_plan`，避免 IR 层一接入就形成循环依赖。当前 `low_uir` 仍未进入 backend driver 自举闭包。
- 本轮验证：`low_uir_linear_call_chain_smoke.cheng` direct exe 编译运行退出 7，报告 `primary_object_direct=1`、`direct_object_reloc_count=2`，`otool -rv` 可见 `_FirstValue` 与 `_EchoValue` 两条 BR26；pure self-build 探针仍硬失败在 `BackendDriverDispatchMinRunSystemLinkExecConcreteFields`，报告 `body_ir_call_ops_98_calls_97_returns_1_locals_38`，确认下一步是真 CFG/Result/复合 local lowering，不是继续扩 body-kind。
- 2026-04-26 本轮补齐 BodyIR call-sequence 的 Darwin arm64 栈实参：第 9 个及以后实参通过临时 outgoing stack arg area 传递，caller frame 读取改用 `x29` 稳定基址；同时修复动态寄存器字段 `baseWord + argIndex` 自举误编译，改为小步进 register encoder。
- 最新验证：`CHENG_BUILD_BACKEND_DRIVER_FORCE_C_SEED=1 CHENG_PROGRESS=1 CHENG_PROCESS_MAX_RSS_BYTES=8589934592 artifacts/bootstrap/cheng.stage3 build-backend-driver --require-rebuild` 成功安装最新 `artifacts/backend_driver/cheng`；新增 `low_uir_stack_args_smoke.cheng` direct exe 运行退出 9，报告 `direct_object_reloc_count=11`，反汇编可见 `ldr w0..w7`、`sub sp,#0x10`、`str x16,[sp]`、`str x16,[sp,#0x8]`、`add sp,#0x10`；旧 `low_uir_linear_call_chain_smoke.cheng` 仍运行退出 7。pure self-build 探针仍停在 `BackendDriverDispatchMinRunSystemLinkExecConcreteFields` 的 `body_ir_call_ops_98_calls_97_returns_1_locals_38`，下一步必须实现 guard/early-return CFG，不能把错误分支当直线 call sequence 放行。
