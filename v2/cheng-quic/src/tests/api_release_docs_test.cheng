# MsQuic API release docs test(manual).

import std/result
import cheng/quic/api/release_docs
fn runApiReleaseDocsTest(): Result[bool] =
    let notes: str = msquicApiReleaseNotes()
    if len(notes) <= 0:
        return Err[bool]("api: release notes empty")
    return Ok[bool](true)
