# Sample tool(minimal).

import std/strings
import cheng/quic/tooling/common
fn sampleToolRun(): ToolResult =
    return toolOk("sample tool ok")

fn sampleToolHelp(): str =
    return "sample_tool: no options"
