# MsQuic API release notes(minimal).

const
    msQuicApiReleaseNotes = "msquic api: minimal Cheng surface(settings/diagnostics/events/runtime)"

fn msquicApiReleaseNotes(): str =
    return msQuicApiReleaseNotes
