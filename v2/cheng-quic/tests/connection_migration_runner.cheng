# Connection migration runner(main wrapper).

import std/result
import cheng/quic/tests/test_connection_migration

fn main(): int32 =
    let res: Result[bool] = runConnectionMigrationTest()
    if ! res.ok:
        echo("connection migration failed")
        echo(Error(res))
        return 1
    echo("connection migration ok")
    return 0
