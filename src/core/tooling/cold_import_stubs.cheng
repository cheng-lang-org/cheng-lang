// Cold-compiler import stubs.
// These provide minimal implementations for functions that the
// cold compiler can't compile from their original modules.

fn BrowserContentCodecModulePath(): str =
    return "cheng/libp2p/browser/browser_content_codec_abi"

fn BrowserHostProbeModulePath(): str =
    return "cheng/libp2p/browser/browser_host_wasm_probe"
