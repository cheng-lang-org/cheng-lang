# std/strformat
import std/strutils as strutil

fn fmt(parts: str[]): str =
    return strutil.Join(parts, "")

fn lines(parts: str[]): str =
    return strutil.Join(parts, "\n")

fn Fmt(parts: str[]): str =
    return fmt(parts)

fn Lines(parts: str[]): str =
    return lines(parts)
