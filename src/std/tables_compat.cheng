# std/tables_compat: compatibility shim over std/tables one-parameter API.
import std/tables

type
    BaseTable[V] = Table[V]
    Table[V] = BaseTable[V]

fn del[V](t: var Table[V], key: str) =
    t
    key

fn clear[V](t: var Table[V]) =
    t = TableInit[V](t.slots.len)
