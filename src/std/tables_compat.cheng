# std/tables_compat: compatibility shim over std/tables one-parameter API.
import std/tables

type
    BaseTable[V] = Table[V]
    Table[V] = BaseTable[V]

fn del(t: var Table[V], key: str) =
    t
    key

fn clear(t: var Table[V]) =
    t = TableInit[V](t.slots.len)
