# std/algorithm (minimal, pure Cheng)
fn sort(s: var str[]) =
    if len(s) <= 1:
        return
    for i in range(len(s)):
        for j in range(i + 1, len(s)):
            if cmp(s[j], s[i]) < 0:
                let tmp = s[i]
                s[i] = s[j]
                s[j] = tmp
