# std/option facade
import core/option

fn IsNone[T](opt: Option[T]): bool =
    return !IsSome(opt)

fn GetOr[T](opt: Option[T], fallback: T): T =
    if IsSome(opt):
        return Get(opt)
    return fallback
