# std/option facade
import cheng/core/option

fn IsNone(opt: Option[T]): bool =
    return !IsSome(opt)

fn GetOr(opt: Option[T], fallback: T): T =
    if IsSome(opt):
        return Get(opt)
    return fallback
