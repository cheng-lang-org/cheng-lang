import web/runtime/abi

fn webNow(): float64 =
    return host_now()

fn webSetTimeout(handlerId: WebHandler, ms: int32): WebTimer =
    return host_set_timeout(handlerId, ms)

fn webClearTimeout(timerId: WebTimer) =
    host_clear_timeout(timerId)
