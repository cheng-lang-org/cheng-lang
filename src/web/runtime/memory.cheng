# Web runtime memory helpers

import std/rawbytes

fn readString(data: Bytes): str =
    if bytesLen(data) <= 0:
        return ""
    return bytesToString(data)

fn safeLen(s: str): int32 =
    return len(s)
