# Generated by cheng route-manifest. DO NOT EDIT.
import web/router/router

fn routeEntries(): RouteEntry[] =
    var out: RouteEntry[] 
    var entry: RouteEntry
    entry.id = 1
    entry.path = "/users/[id]"
    entry.file = "users/[id].cheng"
    out.add(entry)
    entry.id = 2
    entry.path = "/docs/[...slug]"
    entry.file = "docs/[...slug].cheng"
    out.add(entry)
    entry.id = 3
    entry.path = "/about"
    entry.file = "about.cheng"
    out.add(entry)
    entry.id = 4
    entry.path = "/"
    entry.file = "index.cheng"
    out.add(entry)
    return out

fn buildRoutes(): Route[] =
    let entries = routeEntries()
    var out: Route[] 
    for i in 0..<len(entries):
        let entry = entries[i]
        out.add(newRoute(entry.id, entry.path))
    return out

fn routeFile(entries: RouteEntry[], id: int32): str =
    for i in 0..<len(entries):
        let entry = entries[i]
        if entry.id == id:
            return entry.file
    return ""

