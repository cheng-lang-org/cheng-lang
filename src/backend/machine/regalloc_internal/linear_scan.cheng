# Linear scan register allocation (MVP: no-op for fixed regs).
import cheng/backend/machine/machine_types

fn linearScanAssign(module: MachineModule): MachineModule =
    if module != nil:
        memRetain(void*(module))
    return module
