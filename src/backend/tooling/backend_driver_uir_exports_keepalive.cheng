fn backendDriverUirExportsKeepalive(): int32 =
    # Export wrappers now live directly in backend_driver.cheng. Keep this
    # helper as a stable no-op so the thin-shell call surface stays unchanged
    # without importing the legacy duplicate wrapper module.
    return 0
