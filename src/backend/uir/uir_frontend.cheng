# UIR frontend facade.
import std/result
import backend/uir/uir_types
import backend/uir/uir_internal/uir_core_builder
import backend/uir/uir_internal/uir_core_types

fn uirFrontendBuildModuleFromFile(path: str): Result[UirModule] =
    # UirModule is a direct alias of UirCoreModule.
    # Returning the core result directly avoids stage0-specific Result repack instability.
    return uirCoreBuildModuleFromFile(path)

fn uirFrontendBuildModuleFromFileStage1(path: str): Result[UirModule] =
    # UirModule is a direct alias of UirCoreModule.
    # Returning the core result directly avoids stage0-specific Result repack instability.
    return uirCoreBuildModuleFromFileStage1(path)
