# Unified IR facade (phase 1): backed by MIR until native UIR lowering lands.
import cheng/backend/mir/mir_types

type
    UirModule = MirModule
    UirFunc = MirFunc
    UirGlobal = MirGlobal
    UirCString = MirCString
    UirTypeAlias = MirTypeAlias
    UirObjType = MirObjType

fn uirDemoModule(): UirModule =
    return mirDemoModule()
