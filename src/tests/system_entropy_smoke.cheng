import std/result
import std/rawbytes
import std/crypto/rand

fn main(): int32 =
    let entropyRes: Result[Bytes] = rand.systemRandomBytes(16)
    assert(IsOk(entropyRes), " system entropy smoke: result")
    let entropy: Bytes = Value(entropyRes)
    assert(bytesLen(entropy) == 16, " system entropy smoke: len")
    echo(" system_entropy_smoke ok")
    return 0
