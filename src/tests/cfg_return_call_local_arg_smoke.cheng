fn ReadCommandText(index: int32): str =
    var out: str
    return out

fn RunCommandText(cmd: str): int32 =
    return 0

fn main(): int32 =
    let cmd = ReadCommandText(1)
    return RunCommandText(cmd)
