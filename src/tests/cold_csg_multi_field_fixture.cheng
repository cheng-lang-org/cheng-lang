type Point = Cartesian(x: int32, y: int32) | Polar(radius: int32, angle: int32)

fn MakePoint(): Point =
    return Cartesian(10, 20)

fn main(): int32 =
    let p = MakePoint()
    match p:
        Cartesian(x, y) =>
            return x + y
        Polar(r, a) =>
            return 0
