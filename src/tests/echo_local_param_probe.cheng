import std/strutils as strutil
fn echoLocal(path: str): bool =
    let msg: str = strutil.Join(["concat_param=", path], "")
    echo(msg)
    return true

fn main(): int32 =
    if !echoLocal("/tmp/no_model"):
        return 1
    return 0
