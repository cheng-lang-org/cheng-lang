# legacy bootstrap contract fixture that must stay rejected by stage0

syntax = bootstrap-v1
bootstrap_name = cheng_bootstrap
bootstrap_entry = bootstrap_main
compiler_class = bootstrap_subset
target = arm64-apple-darwin
ordinary_command = system-link-exec
ordinary_pipeline_state = canonical_csg_verified_primary_object_codegen_ready
supported_commands = print-contract,self-check,compile-bootstrap,bootstrap-bridge,status
