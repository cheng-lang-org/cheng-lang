type IntResult = OkInt(value: int32) | ErrStr(error: str)

fn MakeErr(): IntResult =
    return ErrStr("failed")

fn main(): int32 =
    let r = MakeErr()
    match r:
        OkInt(value) =>
            return value
        ErrStr(e) =>
            return len(e)
