import std/system
import std/rawbytes

type
    PacketBox =
        payload: Bytes

fn main(): int32 =
    var box: PacketBox
    box.payload = bytesAlloc(24)
    if box.payload.data == nil:
        return 101
    let rc = memRefCount(box.payload.data)
    BytesFree(box.payload)
    return rc
