type TextResult = Ok(message: str, code: int32) | Err(error: str, code: int32)

fn main(): int32 =
    let result = Err("cold", 38)
    match result {
        Ok(message, code) =>
            return len(message) + code
        Err(error, code) =>
            return len(error) + code
    }
