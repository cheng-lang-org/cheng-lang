type Choice = Left(value: int32) | Right(value: int32)

fn Pick(flag: int32): Choice =
    if flag == 1:
        return Left(41)
    return Right(7)

fn main(): int32 =
    match Pick(1):
        Left(value) =>
            return value + 1
        Right(value) =>
            return value
