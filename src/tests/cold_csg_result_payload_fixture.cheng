type Result[T, E] = Ok(value: T, extra: T) | Err(error: E, code: E)

fn main(): int32 =
    let result = Ok(20, 22)
    match result {
        Ok(value, extra) =>
            return value + extra
        Err(error, code) =>
            return error + code
    }
