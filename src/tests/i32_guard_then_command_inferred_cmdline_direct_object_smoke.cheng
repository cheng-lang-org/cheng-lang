import std/cmdline as cmdline

fn Command(): int32 =
    return 9

fn main(argc: int32, argv: str*): int32 =
    let count = cmdline.ParamCount()
    if count <= 0:
        return 0
    return Command()
