import std/os

fn main(): int32 =
    let missing = os.GetEnv("CHENG_PROCESS_MAX_RSS_BYTES")
    if len(missing) != 0:
        echo(missing)
        return 2
    let missing2 = os.GetEnv("CHENG_MAX_RSS_BYTES")
    if len(missing2) != 0:
        echo(missing2)
        return 3
    return 0
