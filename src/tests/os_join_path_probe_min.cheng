import std/os as os

fn main(): int32 =
    let cwd: str = os.GetCurrentDir()
    if cwd.len <= 0:
        return 1
    let repoRootDir: str = cwd
    if repoRootDir.len <= 0:
        return 2
    return 0
