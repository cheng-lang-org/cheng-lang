import std/rawbytes

fn makeBytes(): Bytes =
    return bytesFromString("ab")

fn main(): int32 =
    let text: str = bytesToHex(makeBytes())
    assert(text == "6162", " zeroarg composite call arg: hex")
    echo(" zeroarg_composite_call_arg_probe ok")
    return 0
