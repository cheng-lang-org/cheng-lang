import std/system
import std/strutils as strutil

fn main(): int32 =
    let path: str = "/tmp/no_model"
    let msg: str = strutil.Join(["concat_param=", path], "")
    puts(panicCString(msg))
    return 0
