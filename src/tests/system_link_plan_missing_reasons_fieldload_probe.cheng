import std/strutils as strutil
import cheng/core/backend/system_link_plan as slplan

fn main(): int32 =
    var plan: slplan.SystemLinkPlanStub
    let text = strutil.Join(plan.missingReasons, ",")
    return text.len
