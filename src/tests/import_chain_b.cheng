// import_chain_b.cheng — B in the A->B->C->D chain
// Imports C, provides a function using C

import cheng/tests/import_chain_c as c_mod

fn chainBCompute(x: int32): int32 =
    let step1 = c_mod.chainCTriple(x)
    let step2 = c_mod.chainCMul(step1, 2)
    return c_mod.chainCMul(x, step2)
