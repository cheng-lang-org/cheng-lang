import std/system
import std/strutils as strutil

fn main() =
    let parts: str[] = strutil.Split("a\nb\n", '\n')
    assert(parts.len == 3, "std/split len")
    assert(parts[0] == "a", "std/split first")
    assert(parts[1] == "b", "std/split second")
