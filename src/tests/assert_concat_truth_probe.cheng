import std/strutils as strutil
fn main(): int32 =
    let name: str = "alpha"
    let text: str = strutil.Join(["box=", name], "")
    assert(text == "box=alpha", "concat truth")
    echo("assert-concat-ok")
    return 0
