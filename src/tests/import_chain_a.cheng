// import_chain_a.cheng — A in the A->B->C->D chain
// Imports B, has main, verifies the chain works

import cheng/tests/import_chain_b as b_mod

fn main(): int32 =
    // chainBCompute(3):
    //   step1 = chainCTriple(3) = chainDSquare(chainDAdd(3, 3)) = chainDSquare(6) = chainDAdd(6, 6) = 12
    //   step2 = chainCMul(12, 2) = 12+12 = 24
    //   return chainCMul(3, 24) = 3 summed 24 times = 72
    let result = b_mod.chainBCompute(3)
    if result == 72:
        return 0
    else:
        return 1
