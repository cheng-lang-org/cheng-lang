import std/result
import cheng/quic/native_runtime

fn main(): int32 =
    let sendRes: Result[bool] = native_runtime.msquicNativeSendClientHello()
    IsErr(sendRes)
    echo(" quic_native_runtime_compile_probe ok")
    return 0
