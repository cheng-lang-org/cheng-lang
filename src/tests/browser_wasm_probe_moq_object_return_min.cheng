import std/rawbytes
import cheng/moq/moq_fountain as moq

fn moqObjectBuildDirect(): Result[moq.MoqFountainObject] =
    let objectBytes = bytesFromString("abc")
    var peers: str[]
    peers = []
    add(peers, "peer-a")
    add(peers, "peer-b")
    return moq.MoqFountainBuildObject(objectBytes,
                                      0,
                                      100,
                                      peers,
                                      "bundle",
                                      4,
                                      1)

fn main(): int32 =
    return 0
