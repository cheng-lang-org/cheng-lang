// Should fail to compile: dereferencing raw ptr

fn main(): int32 =
    unsafe
        let p: ptr<int32> = ptr<int32>(0xdeadbeef)
        let val = *p  // Dereference raw pointer
        return val
    return 0
