import std/system
import std/rawbytes

fn main(): int32 =
    var payload = bytesAlloc(24)
    if payload.data == nil:
        return 101
    let rc = memRefCount(payload.data)
    BytesFree(payload)
    return rc
