import std/os
import cheng/core/tooling/path as chengpath

fn main(): int32 =
    let root = os.GetCurrentDir()
    let reportPath = os.JoinPath(root, "artifacts/str_concat_lowering_smoke/str_concat_lowering_probe.compile.report.txt")
    assert(chengpath.PathFileExists(reportPath), " read report: report exists")
    let reportText = os.ReadFile(reportPath)
    assert(len(reportText) > 0, " read report: text len")
    echo(" os_read_report_probe ok")
    return 0
