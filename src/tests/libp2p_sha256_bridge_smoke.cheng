import std/system

importc fn cheng_sha256_hex_bridge(text: str): str

fn main(): int32 =
    let digest = cheng_sha256_hex_bridge("abc")
    echo(digest)
    echo(intToStr(len(digest)))
    return 0
