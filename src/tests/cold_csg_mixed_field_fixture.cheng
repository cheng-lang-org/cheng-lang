type Entry = Named(name: str, count: int32) | Empty

fn MakeEntry(): Entry =
    return Named("test", 7)

fn main(): int32 =
    let e = MakeEntry()
    match e:
        Named(n, c) =>
            return len(n) + c
        Empty =>
            return 1
