import std/strings

type
    Box = object
        title: str

fn `$`(box: Box): str =
    return box.title

fn main(): int32 =
    let box = Box(title: "ok")
    let rendered = `$`(box)
    if rendered != "ok":
        return 1
    return 0
