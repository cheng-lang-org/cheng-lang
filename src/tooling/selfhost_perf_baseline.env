# Selfhost bootstrap performance baseline (seconds).
# Environment variables with the same names can still override these defaults.
# Last refreshed: 2026-02-18 (session: prod.noreuse, observed stage1=8s stage2=0s total=8s)

SELFHOST_PERF_MAX_LOCK_WAIT="${SELFHOST_PERF_MAX_LOCK_WAIT:-5}"
SELFHOST_PERF_MAX_STAGE1="${SELFHOST_PERF_MAX_STAGE1:-20}"
SELFHOST_PERF_MAX_STAGE2="${SELFHOST_PERF_MAX_STAGE2:-15}"
SELFHOST_PERF_MAX_SMOKE_STAGE1="${SELFHOST_PERF_MAX_SMOKE_STAGE1:-10}"
SELFHOST_PERF_MAX_SMOKE_STAGE2="${SELFHOST_PERF_MAX_SMOKE_STAGE2:-10}"
SELFHOST_PERF_MAX_TOTAL="${SELFHOST_PERF_MAX_TOTAL:-30}"
