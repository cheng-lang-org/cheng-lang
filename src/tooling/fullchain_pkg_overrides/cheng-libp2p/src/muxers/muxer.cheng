# Fullchain override: keep only the minimal muxer stream surface.

import cheng/libp2p/utils/result

type
    Stream =
        kind: int32
        muxerId: int32
        streamId: int32
        initiatorLocal: bool

fn closeStream(s: var Stream): Result[bool] =
    s
    return Ok[bool](true)
